import { createRequire as VPV_createRequire } from "node:module";
import { fileURLToPath as VPV_fileURLToPath } from "node:url";
import { dirname as VPV_dirname } from "node:path";
const require = VPV_createRequire(import.meta.url);
const __filename = VPV_fileURLToPath(import.meta.url);
const __dirname = VPV_dirname(__filename);

var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __require = /* @__PURE__ */ ((x3) => typeof require !== "undefined" ? require : typeof Proxy !== "undefined" ? new Proxy(x3, {
  get: (a18, b4) => (typeof require !== "undefined" ? require : a18)[b4]
}) : x3)(function(x3) {
  if (typeof require !== "undefined") return require.apply(this, arguments);
  throw Error('Dynamic require of "' + x3 + '" is not supported');
});
var __esm = (fn, res) => function __init() {
  return fn && (res = (0, fn[__getOwnPropNames(fn)[0]])(fn = 0)), res;
};
var __commonJS = (cb, mod) => function __require2() {
  return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
};
var __export = (target, all) => {
  for (var name2 in all)
    __defProp(target, name2, { get: all[name2], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// node_modules/vike/dist/esm/utils/isBrowser.js
function isBrowser() {
  return typeof window !== "undefined" && typeof window.scrollY === "number";
}
var init_isBrowser = __esm({
  "node_modules/vike/dist/esm/utils/isBrowser.js"() {
  }
});

// node_modules/vike/dist/esm/utils/unique.js
function unique(arr) {
  return Array.from(new Set(arr));
}
var init_unique = __esm({
  "node_modules/vike/dist/esm/utils/unique.js"() {
  }
});

// node_modules/vike/dist/esm/utils/PROJECT_VERSION.js
var PROJECT_VERSION;
var init_PROJECT_VERSION = __esm({
  "node_modules/vike/dist/esm/utils/PROJECT_VERSION.js"() {
    PROJECT_VERSION = "0.4.206";
  }
});

// node_modules/vike/dist/esm/utils/projectInfo.js
var projectInfo;
var init_projectInfo = __esm({
  "node_modules/vike/dist/esm/utils/projectInfo.js"() {
    init_PROJECT_VERSION();
    projectInfo = {
      projectName: "Vike",
      projectVersion: PROJECT_VERSION
    };
  }
});

// node_modules/vike/dist/esm/utils/getGlobalObject.js
function getGlobalObject(key, defaultValue2) {
  const globalObjects = getGlobalObjects();
  const globalObject12 = globalObjects[key] = globalObjects[key] || defaultValue2;
  return globalObject12;
}
function getGlobalObjects() {
  const globalObjects = globalThis[projectKey] = globalThis[projectKey] || {};
  return globalObjects;
}
var projectKey;
var init_getGlobalObject = __esm({
  "node_modules/vike/dist/esm/utils/getGlobalObject.js"() {
    init_assert();
    init_projectInfo();
    projectKey = `_${projectInfo.projectName.toLowerCase()}`;
  }
});

// node_modules/@brillout/picocolors/picocolors.js
var require_picocolors = __commonJS({
  "node_modules/@brillout/picocolors/picocolors.js"(exports, module) {
    if (isBrowser3()) throw new Error("This file should never be included in the browser.");
    var isColorSupported = (() => {
      if (typeof process === "undefined") {
        return false;
      }
      const argv = process.argv || [];
      if ("NO_COLOR" in process.env || argv.includes("--no-color")) {
        return false;
      }
      if ("FORCE_COLOR" in process.env || argv.includes("--color") || "CI" in process.env) {
        return true;
      }
      if (process.platform === "win32") {
        return true;
      } else {
        let tty;
        try {
          const req = __require;
          tty = req("tty");
        } catch {
          return false;
        }
        return tty.isatty(1) && process.env.TERM !== "dumb";
      }
    })();
    var formatter = (open, close, replace = open) => (input2) => {
      let string = "" + input2;
      let index3 = string.indexOf(close, open.length);
      return ~index3 ? open + replaceClose(string, close, replace, index3) + close : open + string + close;
    };
    var replaceClose = (string, close, replace, index3) => {
      let start = string.substring(0, index3) + replace;
      let end = string.substring(index3 + close.length);
      let nextIndex = end.indexOf(close);
      return ~nextIndex ? start + replaceClose(end, close, replace, nextIndex) : start + end;
    };
    var createColors = (enabled = isColorSupported) => {
      const cyan = formatter("\x1B[36m", "\x1B[39m");
      return {
        isColorSupported: enabled,
        code: enabled ? cyan : (s36) => `\`${s36}\``,
        string: enabled ? cyan : (s36) => `'${s36}'`,
        reset: enabled ? (s36) => `\x1B[0m${s36}\x1B[0m` : String,
        bold: enabled ? formatter("\x1B[1m", "\x1B[22m", "\x1B[22m\x1B[1m") : String,
        dim: enabled ? formatter("\x1B[2m", "\x1B[22m", "\x1B[22m\x1B[2m") : String,
        italic: enabled ? formatter("\x1B[3m", "\x1B[23m") : String,
        underline: enabled ? formatter("\x1B[4m", "\x1B[24m") : String,
        inverse: enabled ? formatter("\x1B[7m", "\x1B[27m") : String,
        hidden: enabled ? formatter("\x1B[8m", "\x1B[28m") : String,
        strikethrough: enabled ? formatter("\x1B[9m", "\x1B[29m") : String,
        black: enabled ? formatter("\x1B[30m", "\x1B[39m") : String,
        red: enabled ? formatter("\x1B[31m", "\x1B[39m") : String,
        green: enabled ? formatter("\x1B[32m", "\x1B[39m") : String,
        yellow: enabled ? formatter("\x1B[33m", "\x1B[39m") : String,
        blue: enabled ? formatter("\x1B[34m", "\x1B[39m") : String,
        magenta: enabled ? formatter("\x1B[35m", "\x1B[39m") : String,
        cyan: enabled ? cyan : String,
        white: enabled ? formatter("\x1B[37m", "\x1B[39m") : String,
        gray: enabled ? formatter("\x1B[90m", "\x1B[39m") : String,
        bgBlack: enabled ? formatter("\x1B[40m", "\x1B[49m") : String,
        bgRed: enabled ? formatter("\x1B[41m", "\x1B[49m") : String,
        bgGreen: enabled ? formatter("\x1B[42m", "\x1B[49m") : String,
        bgYellow: enabled ? formatter("\x1B[43m", "\x1B[49m") : String,
        bgBlue: enabled ? formatter("\x1B[44m", "\x1B[49m") : String,
        bgMagenta: enabled ? formatter("\x1B[45m", "\x1B[49m") : String,
        bgCyan: enabled ? formatter("\x1B[46m", "\x1B[49m") : String,
        bgWhite: enabled ? formatter("\x1B[47m", "\x1B[49m") : String
      };
    };
    module.exports = createColors();
    module.exports.createColors = createColors;
    module.exports.rm = stripAnsi2;
    function isBrowser3() {
      return Object.getOwnPropertyDescriptor(globalThis, "window")?.get?.toString().includes("[native code]") ?? false;
    }
    function stripAnsi2(string) {
      return string.replace(ansiRegex2, "");
    }
    var ansiRegex2 = getAnsiRegex2();
    function getAnsiRegex2() {
      const pattern = [
        "[\\u001B\\u009B][[\\]()#;?]*(?:(?:(?:(?:;[-a-zA-Z\\d\\/#&.:=?%@~_]+)*|[a-zA-Z\\d]+(?:;[-a-zA-Z\\d\\/#&.:=?%@~_]*)*)?\\u0007)",
        "(?:(?:\\d{1,4}(?:;\\d{0,4})*)?[\\dA-PR-TZcf-nq-uy=><~]))"
      ].join("|");
      return new RegExp(pattern, "g");
    }
  }
});

// node_modules/vike/dist/esm/utils/assertSingleInstance.js
function assertSingleInstance() {
  {
    const versions = unique(globalObject.instances);
    assertUsage(
      versions.length <= 1,
      // DO *NOT* patch vike to remove this error: because of multiple conflicting versions, you *will* eventually encounter insidious issues that hard to debug and potentially a security hazard, see for example https://github.com/vikejs/vike/issues/1108
      `vike@${import_picocolors.default.bold(versions[0])} and vike@${import_picocolors.default.bold(versions[1])} loaded but only one version should be loaded`
    );
  }
  if (globalObject.checkSingleInstance && globalObject.instances.length > 1) {
    assertWarning(false, clientNotSingleInstance, { onlyOnce: true, showStackTrace: true });
  }
}
function assertSingleInstance_onAssertModuleLoad() {
  globalObject.instances.push(projectInfo.projectVersion);
  assertSingleInstance();
}
function assertUsage(condition2, errorMessage) {
  if (condition2) {
    return;
  }
  const errMsg = `[vike][Wrong Usage] ${errorMessage}`;
  throw new Error(errMsg);
}
function assertWarning(condition2, errorMessage, { onlyOnce, showStackTrace }) {
  if (condition2) {
    return;
  }
  const msg = `[vike][Warning] ${errorMessage}`;
  if (onlyOnce) {
    const { alreadyLogged } = globalObject;
    const key = onlyOnce === true ? msg : onlyOnce;
    if (alreadyLogged.has(key)) {
      return;
    } else {
      alreadyLogged.add(key);
    }
  }
  if (showStackTrace) {
    console.warn(new Error(msg));
  } else {
    console.warn(msg);
  }
}
var import_picocolors, globalObject, clientNotSingleInstance;
var init_assertSingleInstance = __esm({
  "node_modules/vike/dist/esm/utils/assertSingleInstance.js"() {
    init_unique();
    init_getGlobalObject();
    init_projectInfo();
    import_picocolors = __toESM(require_picocolors(), 1);
    globalObject = getGlobalObject("assertSingleInstance.ts", {
      instances: [],
      alreadyLogged: /* @__PURE__ */ new Set()
    });
    clientNotSingleInstance = "Client runtime loaded twice https://vike.dev/client-runtime-duplicated";
  }
});

// node_modules/vike/dist/esm/utils/isNodeJS.js
function isNodeJS() {
  if (typeof process === "undefined")
    return false;
  if (!process.cwd)
    return false;
  if (!process.versions || typeof process.versions.node === "undefined")
    return false;
  if (!process.release || process.release.name !== "node")
    return false;
  return true;
}
var init_isNodeJS = __esm({
  "node_modules/vike/dist/esm/utils/isNodeJS.js"() {
  }
});

// node_modules/vike/dist/esm/utils/createErrorWithCleanStackTrace.js
function createErrorWithCleanStackTrace(errorMessage, numberOfStackTraceLinesToRemove2) {
  const err = new Error(errorMessage);
  if (isNodeJS()) {
    err.stack = clean(err.stack, numberOfStackTraceLinesToRemove2);
  }
  return err;
}
function clean(errStack, numberOfStackTraceLinesToRemove2) {
  if (!errStack) {
    return errStack;
  }
  const stackLines = splitByLine(errStack);
  let linesRemoved = 0;
  const stackLine__cleaned = stackLines.filter((line) => {
    if (line.includes(" (internal/") || line.includes(" (node:internal")) {
      return false;
    }
    if (linesRemoved < numberOfStackTraceLinesToRemove2 && isStackTraceLine(line)) {
      linesRemoved++;
      return false;
    }
    return true;
  }).join("\n");
  return stackLine__cleaned;
}
function isStackTraceLine(line) {
  return line.startsWith("    at ");
}
function splitByLine(str) {
  return str.split(/\r?\n/);
}
var init_createErrorWithCleanStackTrace = __esm({
  "node_modules/vike/dist/esm/utils/createErrorWithCleanStackTrace.js"() {
    init_isNodeJS();
  }
});

// node_modules/vike/dist/esm/utils/isObject.js
function isObject(value) {
  return typeof value === "object" && value !== null;
}
var init_isObject = __esm({
  "node_modules/vike/dist/esm/utils/isObject.js"() {
  }
});

// node_modules/vike/dist/esm/utils/assert.js
function assert(condition2, debugInfo) {
  if (condition2)
    return;
  const debugStr = (() => {
    if (!debugInfo) {
      return null;
    }
    const debugInfoSerialized = typeof debugInfo === "string" ? debugInfo : JSON.stringify(debugInfo);
    return import_picocolors2.default.dim(`Debug info (for Vike maintainers; you can ignore this): ${debugInfoSerialized}`);
  })();
  const link2 = import_picocolors2.default.blue("https://github.com/vikejs/vike/issues/new");
  let errMsg = [
    `You stumbled upon a Vike bug. Go to ${link2} and copy-paste this error. A maintainer will fix the bug (usually under 24 hours).`,
    debugStr
  ].filter(Boolean).join(" ");
  errMsg = addWhitespace(errMsg);
  errMsg = addPrefixAssertType(errMsg, "Bug");
  errMsg = addPrefixProjctName(errMsg, true);
  const internalError = createErrorWithCleanStackTrace(errMsg, numberOfStackTraceLinesToRemove);
  globalObject2.onBeforeLog?.();
  throw internalError;
}
function assertUsage2(condition2, errMsg, { showStackTrace } = {}) {
  if (condition2)
    return;
  showStackTrace = showStackTrace || globalObject2.alwaysShowStackTrace;
  errMsg = addWhitespace(errMsg);
  errMsg = addPrefixAssertType(errMsg, "Wrong Usage");
  errMsg = addPrefixProjctName(errMsg);
  const usageError = createErrorWithCleanStackTrace(errMsg, numberOfStackTraceLinesToRemove);
  if (showStackTrace) {
    globalObject2.showStackTraceList.add(usageError);
  }
  globalObject2.onBeforeLog?.();
  throw usageError;
}
function getProjectError(errMsg) {
  errMsg = addWhitespace(errMsg);
  errMsg = addPrefixAssertType(errMsg, "Error");
  errMsg = addPrefixProjctName(errMsg);
  const projectError = createErrorWithCleanStackTrace(errMsg, numberOfStackTraceLinesToRemove);
  return projectError;
}
function assertWarning2(condition2, msg, { onlyOnce, showStackTrace }) {
  if (condition2)
    return;
  showStackTrace = showStackTrace || globalObject2.alwaysShowStackTrace;
  msg = addWhitespace(msg);
  msg = addPrefixAssertType(msg, "Warning");
  msg = addPrefixProjctName(msg);
  if (onlyOnce) {
    const { alreadyLogged } = globalObject2;
    const key = onlyOnce === true ? msg : onlyOnce;
    if (alreadyLogged.has(key)) {
      return;
    } else {
      alreadyLogged.add(key);
    }
  }
  globalObject2.onBeforeLog?.();
  if (showStackTrace) {
    const err = createErrorWithCleanStackTrace(msg, numberOfStackTraceLinesToRemove);
    globalObject2.showStackTraceList.add(err);
    globalObject2.logger(err, "warn");
  } else {
    globalObject2.logger(msg, "warn");
  }
}
function assertInfo(condition2, msg, { onlyOnce }) {
  if (condition2) {
    return;
  }
  msg = addWhitespace(msg);
  msg = addPrefixProjctName(msg);
  if (onlyOnce) {
    const { alreadyLogged } = globalObject2;
    const key = msg;
    if (alreadyLogged.has(key)) {
      return;
    } else {
      alreadyLogged.add(key);
    }
  }
  globalObject2.onBeforeLog?.();
  globalObject2.logger(msg, "info");
}
function addPrefixAssertType(msg, tag2) {
  let prefix = `[${tag2}]`;
  const color2 = tag2 === "Warning" ? "yellow" : "red";
  prefix = import_picocolors2.default.bold(import_picocolors2.default[color2](prefix));
  return `${prefix}${msg}`;
}
function addWhitespace(msg) {
  if (msg.startsWith("[")) {
    return msg;
  } else {
    return ` ${msg}`;
  }
}
function addPrefixProjctName(msg, showProjectVersion = false) {
  const prefix = showProjectVersion ? projectTagWithVersion : projectTag;
  return `${prefix}${msg}`;
}
function isBug(err) {
  return !String(err).includes("[Bug]");
}
function setAlwaysShowStackTrace() {
  globalObject2.alwaysShowStackTrace = true;
}
var import_picocolors2, globalObject2, projectTag, projectTagWithVersion, numberOfStackTraceLinesToRemove;
var init_assert = __esm({
  "node_modules/vike/dist/esm/utils/assert.js"() {
    init_assertSingleInstance();
    init_createErrorWithCleanStackTrace();
    init_getGlobalObject();
    init_isObject();
    init_projectInfo();
    import_picocolors2 = __toESM(require_picocolors(), 1);
    globalObject2 = getGlobalObject("utils/assert.ts", {
      alreadyLogged: /* @__PURE__ */ new Set(),
      // Production logger. Overwritten by loggerNotProd.ts in non-production environments.
      logger(msg, logType) {
        if (logType === "info") {
          console.log(msg);
        } else {
          console.warn(msg);
        }
      },
      showStackTraceList: /* @__PURE__ */ new WeakSet()
    });
    assertSingleInstance_onAssertModuleLoad();
    projectTag = `[vike]`;
    projectTagWithVersion = `[vike@${projectInfo.projectVersion}]`;
    numberOfStackTraceLinesToRemove = 2;
  }
});

// node_modules/vike/dist/esm/utils/slice.js
function slice(thing, from, to) {
  if (typeof thing === "string") {
    return sliceArray(thing.split(""), from, to).join("");
  } else {
    return sliceArray(thing, from, to);
  }
}
function sliceArray(list, from, to) {
  const listSlice = [];
  let start = from >= 0 ? from : list.length + from;
  assert(start >= 0 && start <= list.length);
  let end = to >= 0 ? to : list.length + to;
  assert(end >= 0 && end <= list.length);
  while (true) {
    if (start === end) {
      break;
    }
    if (start === list.length) {
      start = 0;
    }
    if (start === end) {
      break;
    }
    const el2 = list[start];
    assert(el2 !== void 0);
    listSlice.push(el2);
    start++;
  }
  return listSlice;
}
var init_slice = __esm({
  "node_modules/vike/dist/esm/utils/slice.js"() {
    init_assert();
  }
});

// node_modules/vike/dist/esm/utils/parseUrl.js
function parseUrl(url, baseServer2) {
  assert(isUrl(url), url);
  assert(baseServer2.startsWith("/"));
  const { hashString: hashOriginal, withoutHash: urlWithoutHash } = extractHash(url);
  assert(hashOriginal === null || hashOriginal.startsWith("#"));
  const hash = hashOriginal === null ? "" : decodeSafe(hashOriginal.slice(1));
  const { searchString: searchOriginal, withoutSearch: urlWithoutHashNorSearch } = extractSearch(urlWithoutHash);
  assert(searchOriginal === null || searchOriginal.startsWith("?"));
  let searchString = "";
  if (searchOriginal !== null) {
    searchString = searchOriginal;
  } else if (url.startsWith("#")) {
    const baseURI = getBaseURI();
    searchString = baseURI && extractSearch(baseURI).searchString || "";
  }
  const search = {};
  const searchAll = {};
  Array.from(new URLSearchParams(searchString)).forEach(([key, val]) => {
    search[key] = val;
    searchAll[key] = [...searchAll.hasOwnProperty(key) ? searchAll[key] : [], val];
  });
  let { protocol, origin, pathnameAbsoluteWithBase } = getPathnameAbsoluteWithBase(urlWithoutHashNorSearch, baseServer2);
  const pathnameOriginal = urlWithoutHashNorSearch.slice((origin || "").length);
  assertUrlComponents(url, origin, pathnameOriginal, searchOriginal, hashOriginal);
  let { pathname, hasBaseServer } = removeBaseServer(pathnameAbsoluteWithBase, baseServer2);
  const href = createUrlFromComponents(origin, pathname, searchOriginal, hashOriginal);
  const host = !origin ? null : origin.slice(protocol.length);
  const { hostname, port } = parseHost(host, url);
  pathname = decodePathname(pathname);
  assert(pathname.startsWith("/"));
  return {
    href,
    protocol,
    hostname,
    port,
    origin,
    pathname,
    pathnameOriginal,
    hasBaseServer,
    search,
    searchAll,
    searchOriginal,
    hash,
    hashOriginal
  };
}
function extractHash(url) {
  const [withoutHash, ...parts] = url.split("#");
  const hashString = ["", ...parts].join("#") || null;
  return { hashString, withoutHash };
}
function extractSearch(url) {
  const [withoutSearch, ...parts] = url.split("?");
  const searchString = ["", ...parts].join("?") || null;
  return { searchString, withoutSearch };
}
function decodeSafe(urlComponent) {
  try {
    return decodeURIComponent(urlComponent);
  } catch {
  }
  try {
    return decodeURI(urlComponent);
  } catch {
  }
  return urlComponent;
}
function decodePathname(urlPathname) {
  urlPathname = urlPathname.replace(/\s+$/, "");
  urlPathname = urlPathname.split("/").map((dir) => decodeSafe(dir).split("/").join("%2F")).join("/");
  return urlPathname;
}
function getPathnameAbsoluteWithBase(url, baseServer2) {
  assert(!url.includes("?") && !url.includes("#"));
  {
    const { protocol, origin, pathname } = parseOrigin(url);
    if (origin) {
      return { protocol, origin, pathnameAbsoluteWithBase: pathname };
    }
    assert(pathname === url);
  }
  if (url.startsWith("/")) {
    return { protocol: null, origin: null, pathnameAbsoluteWithBase: url };
  } else {
    const baseURI = getBaseURI();
    let base;
    if (baseURI) {
      base = parseOrigin(baseURI.split("?")[0].split("#")[0]).pathname;
    } else {
      base = baseServer2;
    }
    const pathnameAbsoluteWithBase = resolveUrlPathnameRelative(url, base);
    return { protocol: null, origin: null, pathnameAbsoluteWithBase };
  }
}
function getBaseURI() {
  const baseURI = typeof window !== "undefined" ? window?.document?.baseURI : void 0;
  return baseURI;
}
function parseOrigin(url) {
  if (!isUrlWithProtocol(url)) {
    return { pathname: url, origin: null, protocol: null };
  } else {
    const { protocol, uriWithoutProtocol } = parseProtocol(url);
    assert(protocol);
    const [host, ...rest] = uriWithoutProtocol.split("/");
    const origin = protocol + host;
    const pathname = "/" + rest.join("/");
    return { pathname, origin, protocol };
  }
}
function parseHost(host, url) {
  const ret = { hostname: null, port: null };
  if (!host)
    return ret;
  const parts = host.split(":");
  if (parts.length > 1) {
    const port = parseInt(parts.pop(), 10);
    assert(port || port === 0, url);
    ret.port = port;
  }
  ret.hostname = parts.join(":");
  return ret;
}
function parseProtocol(uri) {
  const SEP = ":";
  const [before, ...after] = uri.split(SEP);
  if (after.length === 0 || // https://github.com/vikejs/vike/commit/886a99ff21e86a8ca699a25cee7edc184aa058e4#r143308934
  // https://en.wikipedia.org/wiki/List_of_URI_schemes
  // https://www.rfc-editor.org/rfc/rfc7595
  !/^[a-z][a-z0-9\+\-]*$/i.test(before)) {
    return { protocol: null, uriWithoutProtocol: uri };
  }
  let protocol = before + SEP;
  let uriWithoutProtocol = after.join(SEP);
  const SEP2 = "//";
  if (uriWithoutProtocol.startsWith(SEP2)) {
    protocol = protocol + SEP2;
    uriWithoutProtocol = uriWithoutProtocol.slice(SEP2.length);
  }
  return { protocol, uriWithoutProtocol };
}
function isUrlProtocol(protocol) {
  const blacklist = [
    // https://docs.ipfs.tech/how-to/address-ipfs-on-web
    "ipfs://",
    "ipns://"
  ];
  if (blacklist.includes(protocol))
    return false;
  return protocol.endsWith("://");
}
function resolveUrlPathnameRelative(pathnameRelative, base) {
  const stack3 = base.split("/");
  const parts = pathnameRelative.split("/");
  let baseRestoreTrailingSlash = base.endsWith("/");
  if (pathnameRelative.startsWith(".")) {
    stack3.pop();
  }
  for (const i13 in parts) {
    const p11 = parts[i13];
    if (p11 == "" && i13 === "0")
      continue;
    if (p11 == ".")
      continue;
    if (p11 == "..")
      stack3.pop();
    else {
      baseRestoreTrailingSlash = false;
      stack3.push(p11);
    }
  }
  let pathnameAbsolute = stack3.join("/");
  if (baseRestoreTrailingSlash && !pathnameAbsolute.endsWith("/"))
    pathnameAbsolute += "/";
  if (!pathnameAbsolute.startsWith("/"))
    pathnameAbsolute = "/" + pathnameAbsolute;
  return pathnameAbsolute;
}
function removeBaseServer(pathnameAbsoluteWithBase, baseServer2) {
  assert(pathnameAbsoluteWithBase.startsWith("/"));
  assert(isBaseServer(baseServer2));
  let urlPathname = pathnameAbsoluteWithBase;
  assert(urlPathname.startsWith("/"));
  assert(baseServer2.startsWith("/"));
  if (baseServer2 === "/") {
    const pathname = pathnameAbsoluteWithBase;
    return { pathname, hasBaseServer: true };
  }
  let baseServerNormalized = baseServer2;
  if (baseServer2.endsWith("/") && urlPathname === slice(baseServer2, 0, -1)) {
    baseServerNormalized = slice(baseServer2, 0, -1);
    assert(urlPathname === baseServerNormalized);
  }
  if (!urlPathname.startsWith(baseServerNormalized)) {
    const pathname = pathnameAbsoluteWithBase;
    return { pathname, hasBaseServer: false };
  }
  assert(urlPathname.startsWith("/") || urlPathname.startsWith("http"));
  assert(urlPathname.startsWith(baseServerNormalized));
  urlPathname = urlPathname.slice(baseServerNormalized.length);
  if (!urlPathname.startsWith("/"))
    urlPathname = "/" + urlPathname;
  assert(urlPathname.startsWith("/"));
  return { pathname: urlPathname, hasBaseServer: true };
}
function isBaseServer(baseServer2) {
  return baseServer2.startsWith("/");
}
function assertUrlComponents(url, origin, pathnameOriginal, searchOriginal, hashOriginal) {
  const urlRecreated = createUrlFromComponents(origin, pathnameOriginal, searchOriginal, hashOriginal);
  assert(url === urlRecreated);
}
function createUrlFromComponents(origin, pathname, search, hash) {
  const urlRecreated = `${origin || ""}${pathname}${search || ""}${hash || ""}`;
  return urlRecreated;
}
function isUrl(url) {
  return isUrlWithProtocol(url) || url.startsWith("/") || isUrlRelative(url);
}
function isUrlRedirectTarget(url) {
  return url.startsWith("/") || isUri(url) || isUrlWithProtocol(url);
}
function isUrlRelative(url) {
  return [".", "?", "#"].some((c15) => url.startsWith(c15)) || url === "";
}
function isUrlWithProtocol(url) {
  const { protocol } = parseProtocol(url);
  return !!protocol && isUrlProtocol(protocol);
}
function isUri(uri) {
  const { protocol } = parseProtocol(uri);
  return !!protocol && !isUrlProtocol(uri);
}
function assertUsageUrlPathnameAbsolute(url, errPrefix) {
  assertUsageUrl(url, errPrefix);
}
function assertUsageUrlRedirectTarget(url, errPrefix, isUnresolved) {
  assertUsageUrl(url, errPrefix, { isRedirectTarget: isUnresolved ? "unresolved" : true });
}
function assertUsageUrl(url, errPrefix, { isRedirectTarget } = {}) {
  if (url.startsWith("/"))
    return;
  let errMsg = `${errPrefix} is ${import_picocolors3.default.string(url)} but it should start with ${import_picocolors3.default.string("/")}`;
  if (isRedirectTarget) {
    if (isUrlRedirectTarget(url))
      return;
    errMsg += ` or a protocol (${import_picocolors3.default.string("http://")}, ${import_picocolors3.default.string("mailto:")}, ...)`;
    if (isRedirectTarget === "unresolved") {
      if (url === "*")
        return;
      errMsg += `, or be ${import_picocolors3.default.string("*")}`;
    }
  }
  assertUsage2(false, errMsg);
}
var import_picocolors3;
var init_parseUrl = __esm({
  "node_modules/vike/dist/esm/utils/parseUrl.js"() {
    init_slice();
    init_assert();
    import_picocolors3 = __toESM(require_picocolors(), 1);
  }
});

// node_modules/vike/dist/esm/utils/objectAssign.js
function objectAssign(obj, objAddendum) {
  if (objAddendum) {
    assert(!("_isPageContextObject" in objAddendum));
    Object.defineProperties(obj, Object.getOwnPropertyDescriptors(objAddendum));
  }
}
var init_objectAssign = __esm({
  "node_modules/vike/dist/esm/utils/objectAssign.js"() {
    init_assert();
  }
});

// node_modules/vike/dist/esm/utils/isCallable.js
function isCallable(thing) {
  return thing instanceof Function || typeof thing === "function";
}
var init_isCallable = __esm({
  "node_modules/vike/dist/esm/utils/isCallable.js"() {
  }
});

// node_modules/vike/dist/esm/utils/sorter.js
function higherFirst(getValue) {
  return (element1, element2) => {
    const val1 = getValue(element1);
    const val2 = getValue(element2);
    if (val1 === val2) {
      return 0;
    }
    return val1 > val2 ? -1 : 1;
  };
}
function lowerFirst(getValue) {
  return (element1, element2) => {
    const val1 = getValue(element1);
    const val2 = getValue(element2);
    if (val1 === val2) {
      return 0;
    }
    return val1 < val2 ? -1 : 1;
  };
}
function makeFirst(getValue) {
  return (element1, element2) => {
    const val1 = getValue(element1);
    const val2 = getValue(element2);
    assert([true, false, null].includes(val1));
    assert([true, false, null].includes(val2));
    if (val1 === val2) {
      return 0;
    }
    if (val1 === true || val2 === false) {
      return -1;
    }
    if (val2 === true || val1 === false) {
      return 1;
    }
    assert(false);
  };
}
function makeLast(getValue) {
  return makeFirst((element) => {
    const val = getValue(element);
    if (val === null) {
      return null;
    } else {
      return !val;
    }
  });
}
var init_sorter = __esm({
  "node_modules/vike/dist/esm/utils/sorter.js"() {
    init_assert();
  }
});

// node_modules/vike/dist/esm/utils/isArray.js
function isArray(value) {
  return Array.isArray(value);
}
var init_isArray = __esm({
  "node_modules/vike/dist/esm/utils/isArray.js"() {
  }
});

// node_modules/vike/dist/esm/utils/isArrayOfStrings.js
function isArrayOfStrings(val) {
  return isArray(val) && val.every((v5) => typeof v5 === "string");
}
var init_isArrayOfStrings = __esm({
  "node_modules/vike/dist/esm/utils/isArrayOfStrings.js"() {
    init_isArray();
  }
});

// node_modules/vike/dist/esm/utils/isObjectOfStrings.js
function isObjectOfStrings(val) {
  return isObject(val) && Object.values(val).every((v5) => typeof v5 === "string");
}
var init_isObjectOfStrings = __esm({
  "node_modules/vike/dist/esm/utils/isObjectOfStrings.js"() {
    init_isObject();
  }
});

// node_modules/vike/dist/esm/utils/hasProp.js
function hasProp(obj, prop, type2) {
  if (!isObject(obj))
    return false;
  if (!(prop in obj)) {
    return type2 === "undefined";
  }
  if (type2 === void 0) {
    return true;
  }
  const propValue = obj[prop];
  if (type2 === "undefined") {
    return propValue === void 0;
  }
  if (type2 === "array") {
    return isArray(propValue);
  }
  if (type2 === "object") {
    return isObject(propValue);
  }
  if (type2 === "string[]") {
    return isArrayOfStrings(propValue);
  }
  if (type2 === "string{}") {
    return isObjectOfStrings(propValue);
  }
  if (type2 === "function") {
    return isCallable(propValue);
  }
  if (isArray(type2)) {
    return typeof propValue === "string" && type2.includes(propValue);
  }
  if (type2 === "null") {
    return propValue === null;
  }
  if (type2 === "true") {
    return propValue === true;
  }
  if (type2 === "false") {
    return propValue === false;
  }
  return typeof propValue === type2;
}
var init_hasProp = __esm({
  "node_modules/vike/dist/esm/utils/hasProp.js"() {
    init_isCallable();
    init_isObject();
    init_isArrayOfStrings();
    init_isObjectOfStrings();
    init_isArray();
  }
});

// node_modules/vike/dist/esm/utils/isPlainObject.js
function isPlainObject(value) {
  if (typeof value !== "object" || value === null) {
    return false;
  }
  if (Object.getPrototypeOf(value) === null) {
    return true;
  }
  return (
    /* Doesn't work in Cloudflare Pages workers
    value.constructor === Object
    */
    value.constructor.name === "Object"
  );
}
var init_isPlainObject = __esm({
  "node_modules/vike/dist/esm/utils/isPlainObject.js"() {
  }
});

// node_modules/vike/dist/esm/utils/compareString.js
function compareString(str1, str2) {
  if (str1.toLowerCase() < str2.toLowerCase())
    return -1;
  if (str1.toLowerCase() > str2.toLowerCase())
    return 1;
  return 0;
}
var init_compareString = __esm({
  "node_modules/vike/dist/esm/utils/compareString.js"() {
  }
});

// node_modules/vike/dist/esm/utils/isNotNullish.js
var isNotNullish;
var init_isNotNullish = __esm({
  "node_modules/vike/dist/esm/utils/isNotNullish.js"() {
    isNotNullish = (p11) => p11 !== null && p11 !== void 0;
  }
});

// node_modules/vike/dist/esm/utils/stringifyStringArray.js
function stringifyStringArray(stringList) {
  return "[" + stringList.map((str) => "'" + str + "'").join(", ") + "]";
}
var init_stringifyStringArray = __esm({
  "node_modules/vike/dist/esm/utils/stringifyStringArray.js"() {
  }
});

// node_modules/vike/dist/esm/utils/filesystemPathHandling.js
function toPosixPath(path) {
  const pathPosix = path.split("\\").join("/");
  assertPosixPath(pathPosix);
  return pathPosix;
}
function assertPosixPath(path) {
  const errMsg = (msg) => `Not a posix path: ${msg}`;
  assert(path !== null, errMsg("null"));
  assert(typeof path === "string", errMsg(`typeof path === ${JSON.stringify(typeof path)}`));
  assert(path !== "", errMsg("(empty string)"));
  assert(path);
  assert(!path.includes("\\"), errMsg(path));
}
var init_filesystemPathHandling = __esm({
  "node_modules/vike/dist/esm/utils/filesystemPathHandling.js"() {
    init_assert();
  }
});

// node_modules/vike/dist/esm/utils/cast.js
function cast(_thing) {
}
var init_cast = __esm({
  "node_modules/vike/dist/esm/utils/cast.js"() {
  }
});

// node_modules/vike/dist/esm/utils/isPropertyGetter.js
function isPropertyGetter(obj, prop) {
  const descriptor = Object.getOwnPropertyDescriptor(obj, prop);
  return !!descriptor && !("value" in descriptor) && !!descriptor.get;
}
var init_isPropertyGetter = __esm({
  "node_modules/vike/dist/esm/utils/isPropertyGetter.js"() {
  }
});

// node_modules/vike/dist/esm/utils/isPromise.js
function isPromise(val) {
  return typeof val === "object" && val !== null && "then" in val && isCallable(val.then);
}
var init_isPromise = __esm({
  "node_modules/vike/dist/esm/utils/isPromise.js"() {
    init_isCallable();
  }
});

// node_modules/vike/dist/esm/utils/checkType.js
function checkType(_2) {
}
function castType(_2) {
}
var init_checkType = __esm({
  "node_modules/vike/dist/esm/utils/checkType.js"() {
  }
});

// node_modules/vike/dist/esm/utils/getValuePrintable.js
function getValuePrintable(value) {
  if ([null, void 0].includes(value))
    return String(value);
  if (["boolean", "number", "string"].includes(typeof value))
    return JSON.stringify(value);
  return null;
}
var init_getValuePrintable = __esm({
  "node_modules/vike/dist/esm/utils/getValuePrintable.js"() {
  }
});

// node_modules/vike/dist/esm/utils/escapeRegex.js
function escapeRegex(str) {
  return str.replace(/[/\-\\^$*+?.()|[\]{}]/g, "\\$&");
}
var init_escapeRegex = __esm({
  "node_modules/vike/dist/esm/utils/escapeRegex.js"() {
  }
});

// node_modules/vike/dist/esm/utils/changeEnumerable.js
function changeEnumerable(obj, prop, enumerable) {
  const descriptor = Object.getOwnPropertyDescriptor(obj, prop);
  Object.defineProperty(obj, prop, { ...descriptor, enumerable });
}
var init_changeEnumerable = __esm({
  "node_modules/vike/dist/esm/utils/changeEnumerable.js"() {
  }
});

// node_modules/vike/dist/esm/utils/objectDefineProperty.js
function objectDefineProperty(obj, prop, { get, ...args }) {
  Object.defineProperty(obj, prop, { ...args, get });
}
var init_objectDefineProperty = __esm({
  "node_modules/vike/dist/esm/utils/objectDefineProperty.js"() {
  }
});

// node_modules/vike/dist/esm/shared/utils.js
var init_utils = __esm({
  "node_modules/vike/dist/esm/shared/utils.js"() {
    init_assert();
    init_parseUrl();
    init_objectAssign();
    init_isCallable();
    init_isObject();
    init_unique();
    init_slice();
    init_sorter();
    init_isBrowser();
    init_hasProp();
    init_isPlainObject();
    init_compareString();
    init_isNotNullish();
    init_stringifyStringArray();
    init_filesystemPathHandling();
    init_cast();
    init_isPropertyGetter();
    init_isPromise();
    init_checkType();
    init_getValuePrintable();
    init_escapeRegex();
    init_isArray();
    init_changeEnumerable();
    init_objectDefineProperty();
  }
});

// node_modules/vike/dist/esm/shared/error-page.js
function getErrorPageId(pageFilesAll, pageConfigs) {
  if (pageConfigs.length > 0) {
    const errorPageConfigs = pageConfigs.filter((p11) => p11.isErrorPage);
    if (errorPageConfigs.length === 0)
      return null;
    assertUsage2(errorPageConfigs.length === 1, "Only one error page can be defined");
    return errorPageConfigs[0].pageId;
  }
  const errorPageIds = unique(pageFilesAll.map(({ pageId }) => pageId).filter((pageId) => isErrorPageId(pageId, false)));
  assertUsage2(errorPageIds.length <= 1, `Only one _error.page.js is allowed, but found several: ${errorPageIds.join(" ")}`);
  if (errorPageIds.length > 0) {
    const errorPageId = errorPageIds[0];
    assert(errorPageId);
    return errorPageId;
  }
  return null;
}
function isErrorPageId(pageId, _isV1Design) {
  assert(!pageId.includes("\\"));
  return pageId.includes("/_error");
}
function isErrorPage(pageId, pageConfigs) {
  if (pageConfigs.length > 0) {
    const pageConfig = pageConfigs.find((p11) => p11.pageId === pageId);
    assert(pageConfig);
    return !!pageConfig.isErrorPage;
  } else {
    return isErrorPageId(pageId, false);
  }
}
var init_error_page = __esm({
  "node_modules/vike/dist/esm/shared/error-page.js"() {
    init_utils();
  }
});

// node_modules/vike/dist/esm/utils/getTerminWidth.js
function getTerminalWidth() {
  return typeof process !== "undefined" && typeof process.stdout !== "undefined" && process.stdout.columns || void 0;
}
var init_getTerminWidth = __esm({
  "node_modules/vike/dist/esm/utils/getTerminWidth.js"() {
  }
});

// node_modules/vike/dist/esm/utils/debug.js
function createDebugger(flag, optionsGlobal) {
  checkType(flag);
  assert(flags.includes(flag));
  const debugWithOptions = (optionsLocal) => {
    return (...msgs) => {
      const options = { ...optionsGlobal, ...optionsLocal };
      debug_(flag, options, ...msgs);
    };
  };
  const debug6 = (...msgs) => debugWithOptions({})(...msgs);
  objectAssign(debug6, { options: debugWithOptions, isActivated: isDebugActivated(flag) });
  return debug6;
}
function debug_(flag, options, ...msgs) {
  if (!isDebugActivated(flag))
    return;
  let [msgFirst, ...msgsRest] = msgs;
  const padding = " ".repeat(flag.length + 1);
  msgFirst = formatMsg(msgFirst, options, padding, "FIRST");
  msgsRest = msgsRest.map((msg, i13) => {
    const position = i13 === msgsRest.length - 1 ? "LAST" : "MIDDLE";
    return formatMsg(msg, options, padding, position);
  });
  let logFirst;
  let logsRest;
  const noNewLine = msgsRest.length <= 1 && [msgFirst, ...msgsRest].every((m12) => typeof m12 === "string" && !m12.includes("\n"));
  if (noNewLine) {
    logFirst = [msgFirst, ...msgsRest].map((m12) => String(m12).trim());
    logsRest = [];
  } else {
    logFirst = [msgFirst];
    logsRest = msgsRest;
  }
  console.log("\x1B[1m%s\x1B[0m", flag, ...logFirst);
  logsRest.forEach((msg) => {
    console.log(msg);
  });
}
function isDebugActivated(flag) {
  checkType(flag);
  assert(flags.includes(flag));
  const DEBUG = getDEBUG();
  const isActivated = DEBUG?.includes(flag) ?? false;
  return isActivated;
}
function formatMsg(info, options, padding, position) {
  if (info === void 0) {
    return void 0;
  }
  let str = position === "FIRST" ? "" : padding;
  if (typeof info === "string") {
    str += info;
  } else if (isArray(info)) {
    if (info.length === 0) {
      str += options.serialization?.emptyArray ?? "[]";
    } else {
      str += info.map(strUnknown).join("\n");
    }
  } else {
    str += strUnknown(info);
  }
  str = pad(str, padding);
  if (position !== "LAST" && position !== "FIRST") {
    str += "\n";
  }
  return str;
}
function pad(str, padding) {
  const terminalWidth = getTerminalWidth();
  const lines = [];
  str.split("\n").forEach((line) => {
    if (!terminalWidth) {
      lines.push(line);
    } else {
      chunk(line, terminalWidth - padding.length).forEach((chunk2) => {
        lines.push(chunk2);
      });
    }
  });
  return lines.join("\n" + padding);
}
function chunk(str, size4) {
  if (str.length <= size4) {
    return [str];
  }
  const chunks = str.match(new RegExp(".{1," + size4 + "}", "g"));
  assert(chunks);
  return chunks;
}
function strUnknown(thing) {
  return typeof thing === "string" ? thing : strObj(thing);
}
function strObj(obj, newLines = true) {
  return JSON.stringify(obj, replaceFunctionSerializer, newLines ? 2 : void 0);
}
function replaceFunctionSerializer(_key, value) {
  if (isCallable(value)) {
    return value.toString().split(/\s+/).join(" ");
  }
  return value;
}
function assertDEBUG() {
  const DEBUG = getDEBUG() ?? "";
  const flagsActivated = DEBUG.match(flagRegex) ?? [];
  flagsActivated.forEach((flag) => {
    assertUsage2(flags.includes(flag), `Unknown DEBUG flag ${import_picocolors4.default.cyan(flag)}. Valid flags:
${flags.map((f15) => `  ${import_picocolors4.default.cyan(f15)}`).join("\n")}`);
  });
}
function getDEBUG() {
  let DEBUG;
  try {
    DEBUG = process.env.DEBUG;
  } catch {
  }
  return DEBUG;
}
var import_picocolors4, flags, flagRegex;
var init_debug = __esm({
  "node_modules/vike/dist/esm/utils/debug.js"() {
    init_isBrowser();
    init_isCallable();
    init_objectAssign();
    init_assert();
    init_checkType();
    init_getTerminWidth();
    import_picocolors4 = __toESM(require_picocolors(), 1);
    init_isArray();
    assert(!isBrowser());
    globalThis.__brillout_debug_createDebugger = createDebugger;
    flags = [
      "vike:error",
      "vike:extractAssets",
      "vike:extractExportNames",
      "vike:glob",
      "vike:log",
      "vike:optimizeDeps",
      "vike:outDir",
      "vike:pageFiles",
      "vike:pointer-imports",
      "vike:routing",
      "vike:setup",
      "vike:stream",
      "vike:virtual-files",
      "vike:esbuild-resolve"
    ];
    flagRegex = /\bvike:[a-zA-Z-]+/g;
    assertDEBUG();
  }
});

// node_modules/vike/dist/esm/utils/assertIsNotBrowser.js
function assertIsNotBrowser() {
  assert(!isBrowser());
}
var init_assertIsNotBrowser = __esm({
  "node_modules/vike/dist/esm/utils/assertIsNotBrowser.js"() {
    init_isBrowser();
    init_assert();
  }
});

// node_modules/vike/dist/esm/utils/trackLogs.js
function trackLogs() {
  const logOriginal = process.stdout.write;
  const log = (msg) => logOriginal.call(process.stdout, msg + "\n");
  ["stdout", "stderr"].forEach((stdName) => {
    var methodOriginal = process[stdName].write;
    process[stdName].write = function(...args) {
      log(import_picocolors5.default.bold(import_picocolors5.default.blue("*** LOG ***")));
      methodOriginal.apply(process[stdName], args);
      log(new Error().stack.replace(/^Error(\:|)/, import_picocolors5.default.magenta("*** LOG ORIGIN ***")));
    };
  });
  Error.stackTraceLimit = Infinity;
}
var import_picocolors5;
var init_trackLogs = __esm({
  "node_modules/vike/dist/esm/utils/trackLogs.js"() {
    init_debug();
    import_picocolors5 = __toESM(require_picocolors(), 1);
    init_assertIsNotBrowser();
    assertIsNotBrowser();
    if (isDebugActivated("vike:log")) {
      trackLogs();
    }
  }
});

// node_modules/vike/dist/esm/utils/assertVersion.js
function assertVersion(dependencyName, versionActual, versionExpected) {
  assertUsage2(isVersionOrAbove(versionActual, versionExpected), `${dependencyName} ${versionActual} isn't supported, use ${dependencyName} >= ${versionExpected} instead.`);
}
function isVersionOrAbove(versionActual, versionExpected) {
  const p1 = parseVersion(versionActual);
  const p22 = parseVersion(versionExpected);
  if (p1[0] !== p22[0])
    return p1[0] > p22[0];
  if (p1[1] !== p22[1])
    return p1[1] > p22[1];
  if (p1[2] !== p22[2])
    return p1[2] > p22[2];
  return true;
}
function parseVersion(version) {
  version = version.split("-")[0];
  let partsStr = version.split(".");
  partsStr = partsStr.slice(0, 3);
  assert(partsStr.length === 3);
  assert(partsStr.every((s36) => s36.length > 0));
  const parts = partsStr.map((s36) => parseInt(s36, 10));
  return parts;
}
var init_assertVersion = __esm({
  "node_modules/vike/dist/esm/utils/assertVersion.js"() {
    init_assert();
  }
});

// node_modules/vike/dist/esm/utils/assertNodeVersion.js
function assertNodeVersion() {
  if (!isNodeJS())
    return;
  const version = process.versions.node;
  assertVersion("Node.js", version, "18.0.0");
}
var init_assertNodeVersion = __esm({
  "node_modules/vike/dist/esm/utils/assertNodeVersion.js"() {
    init_isNodeJS();
    init_assertVersion();
  }
});

// node_modules/@brillout/require-shim/dist/utils.cjs
var require_utils = __commonJS({
  "node_modules/@brillout/require-shim/dist/utils.cjs"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.getGlobalObject = exports.isVitest = exports.pathJoin = exports.assertIsNotBrowser = exports.assert = exports.assertPosixPath = exports.toPosixPath = void 0;
    function toPosixPath2(path) {
      const pathPosix = path.split("\\").join("/");
      assertPosixPath2(pathPosix);
      return pathPosix;
    }
    exports.toPosixPath = toPosixPath2;
    function assertPosixPath2(path) {
      const errMsg = (msg) => `Not a posix path: ${msg}`;
      assert2(path !== null, errMsg("null"));
      assert2(typeof path === "string", errMsg(`typeof path === '${typeof path}'`));
      assert2(path !== "", errMsg("(empty string)"));
      assert2(path);
      assert2(!path.includes("\\"), errMsg(path));
    }
    exports.assertPosixPath = assertPosixPath2;
    function assert2(condition2, debugInfo) {
      if (condition2)
        return;
      const githubRepository = "https://github.com/brillout/require-shim";
      let errMsg = [
        "[@brillout/require-shim]",
        "You stumbled upon a bug.",
        `Go to ${githubRepository}/issues/new and copy-paste this error.`,
        "A maintainer will fix the bug.",
        debugInfo
      ].filter(Boolean).join(" ");
      throw new Error(errMsg);
    }
    exports.assert = assert2;
    function assertIsNotBrowser2() {
      assert2(!isBrowser3());
    }
    exports.assertIsNotBrowser = assertIsNotBrowser2;
    function isBrowser3() {
      return typeof window !== "undefined" && typeof window.scrollY === "number";
    }
    function pathJoin2(path1, path2) {
      assert2(!path1.includes("\\"));
      assert2(!path2.includes("\\"));
      let joined = [...path1.split("/"), ...path2.split("/")].filter(Boolean).join("/");
      if (path1.startsWith("/"))
        joined = "/" + joined;
      return joined;
    }
    exports.pathJoin = pathJoin2;
    function isVitest2() {
      return typeof process !== "undefined" && typeof process.env !== "undefined" && "VITEST" in process.env;
    }
    exports.isVitest = isVitest2;
    function getGlobalObject3(key, defaultValue2) {
      const allGlobalObjects = globalThis.__brillout_require_shim = globalThis.__brillout_require_shim || {};
      const globalObject12 = allGlobalObjects[key] = allGlobalObjects[key] || defaultValue2;
      return globalObject12;
    }
    exports.getGlobalObject = getGlobalObject3;
  }
});

// node_modules/@brillout/require-shim/dist/runtime-test.cjs
var require_runtime_test = __commonJS({
  "node_modules/@brillout/require-shim/dist/runtime-test.cjs"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var utils_cjs_1 = require_utils();
    testRequireShim();
    function testRequireShim() {
      let req;
      try {
        req = __require;
      } catch (_a) {
      }
      if (!req)
        return;
      (0, utils_cjs_1.assert)(!("_is_brillout_require_shim" in __require));
    }
  }
});

// node_modules/@brillout/require-shim/dist/index.cjs
var require_dist = __commonJS({
  "node_modules/@brillout/require-shim/dist/index.cjs"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.installRequireShim_setUserRootDir = exports.installRequireShim = void 0;
    var utils_cjs_1 = require_utils();
    var globalObject12 = (0, utils_cjs_1.getGlobalObject)("utils/require-shim.ts", {});
    (0, utils_cjs_1.assertIsNotBrowser)();
    function installRequireShim2() {
      if (globalObject12.alreadyCalled)
        return;
      globalObject12.alreadyCalled = true;
      let requireLocal;
      try {
        requireLocal = __require;
      } catch (_a) {
      }
      if (!requireLocal)
        return;
      let module2;
      try {
        module2 = requireLocal("module");
      } catch (_b) {
        return;
      }
      if (globalThis.require === void 0) {
        install();
      }
      testShim();
      return;
      function install() {
        Object.defineProperty(globalThis, "require", {
          get() {
            let callsites;
            {
              const prepareStackTraceOrg = Error.prepareStackTrace;
              Error.prepareStackTrace = (_2, stack3) => stack3;
              const err = new Error();
              callsites = err.stack;
              Error.prepareStackTrace = prepareStackTraceOrg;
            }
            const callerFile = getCallerFile(callsites);
            const callerFileFallback = __filename;
            const requireContextFile = callerFile || callerFileFallback;
            (0, utils_cjs_1.assert)(requireContextFile);
            const requireUserLand = module2.createRequire(requireContextFile);
            requireUserLand._is_brillout_require_shim = true;
            return requireUserLand;
          }
        });
      }
      function getCallerFile(callsites) {
        const caller = callsites[1];
        (0, utils_cjs_1.assert)(caller);
        if (!caller.getFileName)
          return null;
        {
          const filePath = caller.getFileName();
          (0, utils_cjs_1.assert)(typeof filePath === "string" && filePath || filePath === void 0);
          if (filePath)
            return filePath;
        }
        {
          const filePath = deriveFilePath(caller);
          if (filePath) {
            return filePath;
          }
        }
        return null;
      }
      function deriveFilePath(caller) {
        let filePath = caller.getEvalOrigin();
        if (!filePath)
          return null;
        if (doesPathExist(filePath)) {
          return filePath;
        }
        const { userRootDir } = globalObject12;
        if (!userRootDir)
          return null;
        let filePathAbsolute = (0, utils_cjs_1.toPosixPath)(filePath);
        (0, utils_cjs_1.assertPosixPath)(userRootDir);
        filePathAbsolute = (0, utils_cjs_1.pathJoin)(userRootDir, filePathAbsolute);
        if (doesPathExist(filePathAbsolute)) {
          return filePathAbsolute;
        }
        return null;
      }
      function doesPathExist(filePath) {
        (0, utils_cjs_1.assert)(requireLocal);
        try {
          requireLocal.resolve(filePath);
          return true;
        } catch (_a) {
          return false;
        }
      }
    }
    exports.installRequireShim = installRequireShim2;
    function testShim() {
      if ((0, utils_cjs_1.isVitest)())
        return;
      (0, utils_cjs_1.assert)(__require !== globalThis.require);
      (0, utils_cjs_1.assert)(!("_is_brillout_require_shim" in __require));
      Promise.resolve().then(() => __toESM(require_runtime_test()));
    }
    function installRequireShim_setUserRootDir(userRootDir) {
      globalObject12.userRootDir = userRootDir;
    }
    exports.installRequireShim_setUserRootDir = installRequireShim_setUserRootDir;
  }
});

// node_modules/vike/dist/esm/utils/getFileExtension.js
function getFileExtension(id3) {
  id3 = id3.split("?")[0];
  const fileName = slice(id3.split("/"), -1, 0)[0];
  if (!fileName) {
    return null;
  }
  const fileExtension = slice(fileName.split("."), -1, 0)[0];
  if (!fileExtension) {
    return null;
  }
  return fileExtension;
}
var init_getFileExtension = __esm({
  "node_modules/vike/dist/esm/utils/getFileExtension.js"() {
    init_slice();
  }
});

// node_modules/vike/dist/esm/utils/parseUrl-extras.js
function prependBase(url, baseServer2) {
  if (baseServer2.startsWith("http")) {
    const baseAssets = baseServer2;
    const baseAssetsNormalized = normalizeBaseAssets(baseAssets);
    assert(!baseAssetsNormalized.endsWith("/"));
    assert(url.startsWith("/"));
    return `${baseAssetsNormalized}${url}`;
  }
  assert(isBaseServer(baseServer2));
  const baseServerNormalized = normalizeBaseServer(baseServer2);
  if (baseServerNormalized === "/")
    return url;
  assert(!baseServerNormalized.endsWith("/"));
  assert(url.startsWith("/"));
  return `${baseServerNormalized}${url}`;
}
function removeBaseServer2(url, baseServer2) {
  const { hasBaseServer, origin, pathname, pathnameOriginal, searchOriginal, hashOriginal } = parseUrl(url, baseServer2);
  assert(hasBaseServer);
  assertUrlComponents(url, origin, pathnameOriginal, searchOriginal, hashOriginal);
  const urlWithoutBase = createUrlFromComponents(origin, pathname, searchOriginal, hashOriginal);
  return urlWithoutBase;
}
function normalizeBaseAssets(baseAssets) {
  let baseAssetsNormalized = baseAssets;
  if (baseAssetsNormalized.endsWith("/")) {
    baseAssetsNormalized = slice(baseAssetsNormalized, 0, -1);
  }
  assert(!baseAssetsNormalized.endsWith("/"));
  return baseAssetsNormalized;
}
function normalizeBaseServer(baseServer2) {
  let baseServerNormalized = baseServer2;
  if (baseServerNormalized.endsWith("/") && baseServerNormalized !== "/") {
    baseServerNormalized = slice(baseServerNormalized, 0, -1);
  }
  assert(!baseServerNormalized.endsWith("/") || baseServerNormalized === "/");
  return baseServerNormalized;
}
function isBaseAssets(base) {
  return base.startsWith("/") || base.startsWith("http://") || base.startsWith("https://");
}
function normalizeUrlPathname(urlOriginal, trailingSlash, baseServer2) {
  const urlNormalized = modifyUrlPathname(urlOriginal, (urlPathname) => {
    assert(urlPathname.startsWith("/"));
    let urlPathnameNormalized = normalize(urlPathname);
    if (urlPathnameNormalized === "/") {
      return urlPathnameNormalized;
    }
    if (baseServer2.endsWith("/") && baseServer2 !== "/" && normalize(baseServer2) === urlPathnameNormalized) {
      trailingSlash = true;
    }
    assert(!urlPathnameNormalized.endsWith("/"));
    if (trailingSlash) {
      urlPathnameNormalized = urlPathnameNormalized + "/";
    }
    return urlPathnameNormalized;
  });
  if (urlNormalized === urlOriginal)
    return null;
  return urlNormalized;
}
function normalize(urlPathname) {
  assert(urlPathname.startsWith("/"));
  return "/" + urlPathname.split("/").filter(Boolean).join("/");
}
function modifyUrlPathname(url, modifier) {
  const { origin, pathnameOriginal, searchOriginal, hashOriginal } = parseUrl(url, "/");
  const pathnameModified = modifier(pathnameOriginal);
  if (pathnameModified === null)
    return url;
  assertUrlComponents(url, origin, pathnameOriginal, searchOriginal, hashOriginal);
  const urlModified = createUrlFromComponents(origin, pathnameModified, searchOriginal, hashOriginal);
  assert(pathnameOriginal === pathnameModified === (url === urlModified));
  return urlModified;
}
function removeUrlOrigin(url) {
  const { origin, pathnameOriginal, searchOriginal, hashOriginal } = parseUrl(url, "/");
  const urlModified = createUrlFromComponents(null, pathnameOriginal, searchOriginal, hashOriginal);
  return { urlModified, origin };
}
function setUrlOrigin(url, origin) {
  const { origin: originCurrent, pathnameOriginal, searchOriginal, hashOriginal } = parseUrl(url, "/");
  if (origin === originCurrent)
    return false;
  assert(origin === null || origin.startsWith("http"));
  const urlModified = createUrlFromComponents(origin, pathnameOriginal, searchOriginal, hashOriginal);
  return urlModified;
}
function getUrlPretty(url) {
  const { urlModified } = removeUrlOrigin(url);
  return urlModified;
}
var init_parseUrl_extras = __esm({
  "node_modules/vike/dist/esm/utils/parseUrl-extras.js"() {
    init_parseUrl();
    init_assert();
    init_slice();
    init_assertIsNotBrowser();
    assertIsNotBrowser();
  }
});

// node_modules/vike/dist/esm/node/shared/utils.js
var init_utils2 = __esm({
  "node_modules/vike/dist/esm/node/shared/utils.js"() {
    init_assertIsNotBrowser();
    init_assert();
    init_getFileExtension();
    init_isPlainObject();
    init_projectInfo();
    init_checkType();
    init_hasProp();
    init_objectAssign();
    init_checkType();
    init_hasProp();
    init_parseUrl();
    init_parseUrl_extras();
    init_isObject();
    init_assertIsNotBrowser();
    init_isNotNullish();
    init_unique();
    init_debug();
    assertIsNotBrowser();
  }
});

// node_modules/vike/dist/esm/node/shared/isErrorDebug.js
function isErrorDebug() {
  return isDebugActivated("vike:error");
}
var init_isErrorDebug = __esm({
  "node_modules/vike/dist/esm/node/shared/isErrorDebug.js"() {
    init_utils2();
  }
});

// node_modules/vike/dist/esm/node/runtime/onLoad.js
function onLoad() {
  assertIsNotBrowser();
  assertNodeVersion();
  (0, import_require_shim.installRequireShim)();
  if (isErrorDebug())
    setAlwaysShowStackTrace();
}
var import_require_shim;
var init_onLoad = __esm({
  "node_modules/vike/dist/esm/node/runtime/onLoad.js"() {
    init_assertIsNotBrowser();
    init_assertNodeVersion();
    import_require_shim = __toESM(require_dist(), 1);
    init_utils3();
    init_isErrorDebug();
  }
});

// node_modules/vike/dist/esm/utils/PromiseType.js
var init_PromiseType = __esm({
  "node_modules/vike/dist/esm/utils/PromiseType.js"() {
  }
});

// node_modules/vike/dist/esm/utils/isObjectWithKeys.js
function isObjectWithKeys(obj, keys2) {
  if (!isPlainObject(obj)) {
    return false;
  }
  for (const key of Object.keys(obj)) {
    if (!keys2.includes(key)) {
      return false;
    }
  }
  return true;
}
var init_isObjectWithKeys = __esm({
  "node_modules/vike/dist/esm/utils/isObjectWithKeys.js"() {
    init_isPlainObject();
  }
});

// node_modules/vike/dist/esm/utils/viteIsSSR.js
var init_viteIsSSR = __esm({
  "node_modules/vike/dist/esm/utils/viteIsSSR.js"() {
    init_assert();
    init_isObject();
  }
});

// node_modules/vike/dist/esm/utils/path-shim.js
var init_path_shim = __esm({
  "node_modules/vike/dist/esm/utils/path-shim.js"() {
    init_assert();
  }
});

// node_modules/vike/dist/esm/utils/getOutDirs.js
var import_picocolors6, debug;
var init_getOutDirs = __esm({
  "node_modules/vike/dist/esm/utils/getOutDirs.js"() {
    init_viteIsSSR();
    init_assert();
    init_path_shim();
    init_filesystemPathHandling();
    import_picocolors6 = __toESM(require_picocolors(), 1);
    init_debug();
    debug = createDebugger("vike:outDir");
  }
});

// node_modules/vike/dist/esm/utils/capitalizeFirstLetter.js
function capitalizeFirstLetter(word) {
  if (!word[0]) {
    return word;
  }
  return word[0].toUpperCase() + word.slice(1);
}
var init_capitalizeFirstLetter = __esm({
  "node_modules/vike/dist/esm/utils/capitalizeFirstLetter.js"() {
  }
});

// node_modules/vike/dist/esm/utils/debugGlob.js
var debugGlob;
var init_debugGlob = __esm({
  "node_modules/vike/dist/esm/utils/debugGlob.js"() {
    init_debug();
    debugGlob = createDebugger("vike:glob");
  }
});

// node_modules/vike/dist/esm/utils/isSameErrorMessage.js
function isSameErrorMessage(err1, err2) {
  if (!isObject(err1) || !isObject(err2))
    return false;
  return err1.message === err2.message;
}
var init_isSameErrorMessage = __esm({
  "node_modules/vike/dist/esm/utils/isSameErrorMessage.js"() {
    init_isObject();
  }
});

// node_modules/vike/dist/esm/utils/styleFileRE.js
var styleFileRE;
var init_styleFileRE = __esm({
  "node_modules/vike/dist/esm/utils/styleFileRE.js"() {
    styleFileRE = /\.(css|less|sass|scss|styl|stylus|pcss|postcss)($|\?)/;
  }
});

// node_modules/vike/dist/esm/utils/urlToFile.js
var baseServer;
var init_urlToFile = __esm({
  "node_modules/vike/dist/esm/utils/urlToFile.js"() {
    init_assert();
    init_parseUrl();
    init_slice();
    baseServer = "/";
  }
});

// node_modules/vike/dist/esm/utils/freezePartial.js
function freezePartial(obj, allowList) {
  Object.entries(obj).forEach(([key, val]) => {
    Object.defineProperty(obj, key, {
      get() {
        return val;
      },
      set(newVal) {
        if (key in allowList) {
          const isAllowed = allowList[key](newVal);
          if (isAllowed) {
            val = newVal;
            return;
          } else {
            throw new Error(`Setting wrong value ${import_picocolors7.default.cyan(JSON.stringify(newVal))} for property ${import_picocolors7.default.cyan(key)}`);
          }
        }
        throw new Error(`You aren't allowed to mutate property ${import_picocolors7.default.cyan(key)}`);
      },
      configurable: false,
      enumerable: true
    });
  });
  Object.preventExtensions(obj);
}
var import_picocolors7;
var init_freezePartial = __esm({
  "node_modules/vike/dist/esm/utils/freezePartial.js"() {
    import_picocolors7 = __toESM(require_picocolors(), 1);
    init_assertIsNotBrowser();
    assertIsNotBrowser();
  }
});

// node_modules/vike/dist/esm/utils/isNpmPackage.js
function isNpmPackageImport(str, { cannotBePathAlias }) {
  assert(cannotBePathAlias);
  return isNpmPackageImport_unreliable(str);
}
function isNpmPackageImport_unreliable(str) {
  const res = parse(str);
  return res !== null;
}
function assertIsNpmPackageImport(str) {
  assert(isNpmPackageImport(str, {
    // If `str` is a path alias that looks like an npm package => assertIsNpmPackageImport() is erroneous but that's okay because the assertion will eventually fail for some other user using a disambiguated path alias.
    cannotBePathAlias: true
  }), str);
}
function parse(str) {
  if (!str)
    return null;
  let scope = null;
  if (str.startsWith("@")) {
    if (!str.includes("/"))
      return null;
    const [scope_, ...rest] = str.split("/");
    scope = scope_;
    str = rest.join("/");
    if (!str)
      return null;
    if (scope === "@" || invalid(scope.slice(1)))
      return null;
  }
  const [name2, ...importPathParts] = str.split("/");
  if (!name2 || invalid(name2))
    return null;
  const importPath = importPathParts.length === 0 ? null : importPathParts.join("/");
  return {
    pkgName: scope ? `${scope}/${name2}` : name2,
    importPath
  };
}
function invalid(s36) {
  const firstLetter = s36[0];
  if (!firstLetter || !/[a-z0-9]/.test(firstLetter))
    return true;
  if (/[^a-z0-9_\-\.]/.test(s36))
    return true;
  return false;
}
var init_isNpmPackage = __esm({
  "node_modules/vike/dist/esm/utils/isNpmPackage.js"() {
    init_assert();
    init_assertIsNotBrowser();
    assertIsNotBrowser();
  }
});

// node_modules/vike/dist/esm/utils/isScriptFile.js
function isScriptFile(filePath) {
  const yes = scriptFileExtensionList.some((ext) => filePath.endsWith("." + ext));
  if (isPlainJavaScriptFile(filePath))
    assert(yes);
  return yes;
}
function isPlainJavaScriptFile(filePath) {
  const yes1 = /\.(c|m)?(j|t)s$/.test(filePath);
  const yes2 = extJavaScript.some((ext) => filePath.endsWith("." + ext));
  assert(yes1 === yes2);
  return yes1;
}
function isTemplateFile(filePath) {
  return extTemplates.some((ext) => filePath.endsWith("." + ext));
}
var extJavaScript, extJsx, extTemplates, scriptFileExtensionList, scriptFileExtensions;
var init_isScriptFile = __esm({
  "node_modules/vike/dist/esm/utils/isScriptFile.js"() {
    init_assert();
    extJavaScript = [
      "js",
      "ts",
      "cjs",
      "cts",
      "mjs",
      "mts"
    ];
    extJsx = [
      "jsx",
      "tsx",
      "cjsx",
      "ctsx",
      "mjsx",
      "mtsx"
    ];
    extTemplates = [
      "vue",
      "svelte",
      "marko",
      "md",
      "mdx"
    ];
    scriptFileExtensionList = [...extJavaScript, ...extJsx, ...extTemplates];
    scriptFileExtensions = "(" + scriptFileExtensionList.join("|") + ")";
  }
});

// node_modules/vike/dist/esm/utils/removeFileExtention.js
var init_removeFileExtention = __esm({
  "node_modules/vike/dist/esm/utils/removeFileExtention.js"() {
  }
});

// node_modules/vike/dist/esm/utils/objectKeys.js
var init_objectKeys = __esm({
  "node_modules/vike/dist/esm/utils/objectKeys.js"() {
  }
});

// node_modules/vike/dist/esm/utils/objectEntries.js
var init_objectEntries = __esm({
  "node_modules/vike/dist/esm/utils/objectEntries.js"() {
  }
});

// node_modules/vike/dist/esm/utils/objectFromEntries.js
var init_objectFromEntries = __esm({
  "node_modules/vike/dist/esm/utils/objectFromEntries.js"() {
  }
});

// node_modules/vike/dist/esm/utils/isVitest.js
function isVitest() {
  return typeof process !== "undefined" && typeof process.env !== "undefined" && "VITEST" in process.env;
}
var init_isVitest = __esm({
  "node_modules/vike/dist/esm/utils/isVitest.js"() {
  }
});

// node_modules/vike/dist/esm/utils/assertIsNotProductionRuntime.js
function assertEnv() {
  if (debug2.isActivated)
    debug2("assertEnv()", new Error().stack);
  if (isVitest())
    return;
  const isProduction = !env.isViteDev && !env.isVitePreview;
  if (isProduction) {
    assertUsage2(!env.isVikePluginLoaded, vikeVitePluginLoadedInProductionError);
    assert(!env.shouldNotBeProduction);
  } else {
    assert(env.shouldNotBeProduction);
    assert(env.isVikePluginLoaded);
  }
}
var debug2, vikeVitePluginLoadedInProductionError, env;
var init_assertIsNotProductionRuntime = __esm({
  "node_modules/vike/dist/esm/utils/assertIsNotProductionRuntime.js"() {
    init_assert();
    init_assertIsNotBrowser();
    init_debug();
    init_getGlobalObject();
    init_isVitest();
    assertIsNotBrowser();
    debug2 = createDebugger("vike:setup");
    vikeVitePluginLoadedInProductionError = `Loading Vike's Vite plugin (the vike/plugin module) is prohibited in production.`;
    env = getGlobalObject("utils/assertIsNotProductionRuntime.ts", {});
  }
});

// node_modules/vike/dist/esm/utils/assertNodeEnv.js
function assertNodeEnv_runtime(isViteDev) {
  const nodeEnv = getNodeEnv();
  if (nodeEnv === null || nodeEnv === "test")
    return;
  const isNodeDev = isNodeEnvDev();
  if (isViteDev === isNodeDev)
    return;
  const nodeEnvDesc = getNodeEnvDesc();
  const errMsg = `Running ${isViteDev ? import_picocolors8.default.cyan("$ vite dev") : "app in production"} while the ${nodeEnvDesc} which is contradictory, see https://vike.dev/NODE_ENV`;
  assertWarning2(false, errMsg, { onlyOnce: true });
}
function getNodeEnv() {
  if (typeof process === "undefined")
    return null;
  return "production";
}
function isNodeEnvDev() {
  const nodeEnv = getNodeEnv();
  if (!nodeEnv)
    return true;
  if (["development", "dev"].includes(nodeEnv))
    return true;
  return false;
}
function getNodeEnvDesc() {
  const nodeEnv = getNodeEnv();
  const isDev = isNodeEnvDev();
  const nodeEnvDesc = `environment is set to be a ${isDev ? "development" : "production"} environment by ${import_picocolors8.default.cyan(`process.env.NODE_ENV === ${JSON.stringify(nodeEnv)}`)}`;
  return nodeEnvDesc;
}
var import_picocolors8;
var init_assertNodeEnv = __esm({
  "node_modules/vike/dist/esm/utils/assertNodeEnv.js"() {
    import_picocolors8 = __toESM(require_picocolors(), 1);
    init_assertIsNotBrowser();
    init_assert();
    init_assertIsNotProductionRuntime();
    assertIsNotBrowser();
  }
});

// node_modules/vike/dist/esm/utils/isHtml.js
function isHtml(str) {
  const re3 = /(<\/[^<]+>)|(<[^<]+\/>)/;
  return re3.test(str);
}
var init_isHtml = __esm({
  "node_modules/vike/dist/esm/utils/isHtml.js"() {
  }
});

// node_modules/vike/dist/esm/utils/warnIfErrorIsNotObject.js
function warnIfErrorIsNotObject(err) {
  if (!isObject(err)) {
    console.warn("[vike] The thrown value is:");
    console.warn(err);
    assertWarning2(false, `One of your hooks threw an error ${import_picocolors9.default.cyan("throw someValue")} but ${import_picocolors9.default.cyan("someValue")} isn't an object (it's ${import_picocolors9.default.cyan(`typeof someValue === ${typeof err}`)} instead). Make sure thrown values are always wrapped with ${import_picocolors9.default.cyan("new Error()")}, in other words: ${import_picocolors9.default.cyan("throw someValue")} should be replaced with ${import_picocolors9.default.cyan("throw new Error(someValue)")}. The thrown value is printed above.`, { onlyOnce: false });
  }
}
var import_picocolors9;
var init_warnIfErrorIsNotObject = __esm({
  "node_modules/vike/dist/esm/utils/warnIfErrorIsNotObject.js"() {
    init_assert();
    init_assertIsNotBrowser();
    init_isObject();
    import_picocolors9 = __toESM(require_picocolors(), 1);
    assertIsNotBrowser();
  }
});

// node_modules/vike/dist/esm/utils/stripAnsi.js
function stripAnsi(string) {
  return string.replace(ansiRegex, "");
}
function getAnsiRegex() {
  const pattern = [
    "[\\u001B\\u009B][[\\]()#;?]*(?:(?:(?:(?:;[-a-zA-Z\\d\\/#&.:=?%@~_]+)*|[a-zA-Z\\d]+(?:;[-a-zA-Z\\d\\/#&.:=?%@~_]*)*)?\\u0007)",
    "(?:(?:\\d{1,4}(?:;\\d{0,4})*)?[\\dA-PR-TZcf-nq-uy=><~]))"
  ].join("|");
  return new RegExp(pattern, "g");
}
var ansiRegex;
var init_stripAnsi = __esm({
  "node_modules/vike/dist/esm/utils/stripAnsi.js"() {
    init_assertIsNotBrowser();
    assertIsNotBrowser();
    ansiRegex = getAnsiRegex();
  }
});

// node_modules/vike/dist/esm/utils/truncateString.js
function truncateString(str, lenMax) {
  const lenMaxReal = lenMax - 3;
  assert(lenMaxReal >= 1);
  if (str.length < lenMax) {
    return str;
  } else {
    str = str.substring(0, lenMaxReal);
    const ellipsis = import_picocolors10.default.dim("...");
    str = str + ellipsis;
    return str;
  }
}
var import_picocolors10;
var init_truncateString = __esm({
  "node_modules/vike/dist/esm/utils/truncateString.js"() {
    import_picocolors10 = __toESM(require_picocolors(), 1);
    init_assert();
  }
});

// node_modules/vike/dist/esm/utils/formatHintLog.js
function formatHintLog(msg) {
  assert(msg.length > 0);
  const msgLength = stripAnsi(msg).length;
  const sep = "\u2500".repeat(msgLength);
  return [
    // prettier-ignore
    // biome-ignore format:
    `\u250C\u2500${sep}\u2500\u2510`,
    `\u2502 ${msg} \u2502`,
    `\u2514\u2500${sep}\u2500\u2518`
  ].join("\n");
}
var init_formatHintLog = __esm({
  "node_modules/vike/dist/esm/utils/formatHintLog.js"() {
    init_assert();
    init_stripAnsi();
  }
});

// node_modules/vike/dist/esm/utils/joinEnglish.js
function joinEnglish(arr, conjunction, colorizer = (s36) => s36) {
  assert(arr.length > 0);
  if (arr.length === 1)
    return colorizer(arr[0]);
  const firsts = arr.slice(0, arr.length - 1);
  const last = arr[arr.length - 1];
  return firsts.map(colorizer).join(", ") + `, ${conjunction} ` + colorizer(last);
}
var init_joinEnglish = __esm({
  "node_modules/vike/dist/esm/utils/joinEnglish.js"() {
    init_assert();
  }
});

// node_modules/vike/dist/esm/utils/escapeHtml.js
function escapeHtml(unsafeString) {
  const safe = unsafeString.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;").replace(/'/g, "&#039;");
  return safe;
}
var init_escapeHtml = __esm({
  "node_modules/vike/dist/esm/utils/escapeHtml.js"() {
  }
});

// node_modules/vike/dist/esm/utils/normalizeHeaders.js
function normalizeHeaders(headersOriginal) {
  let headersCleaned = headersOriginal;
  if (isObject(headersCleaned) && headersCleaned[":method"])
    headersCleaned = Object.fromEntries(Object.entries(headersCleaned).filter(([key]) => !key.startsWith(":")));
  const headersStandard = new Headers(headersCleaned);
  const headers = Object.fromEntries(headersStandard.entries());
  return headers;
}
var init_normalizeHeaders = __esm({
  "node_modules/vike/dist/esm/utils/normalizeHeaders.js"() {
    init_isObject();
  }
});

// node_modules/vike/dist/esm/utils/isVikeReactApp.js
function isVikeReactApp() {
  const g7 = globalThis;
  return !!g7._isVikeReactApp;
}
var init_isVikeReactApp = __esm({
  "node_modules/vike/dist/esm/utils/isVikeReactApp.js"() {
  }
});

// node_modules/vike/dist/esm/utils/getPropAccessNotation.js
function getPropAccessNotation(key) {
  return typeof key === "string" && isKeyDotNotationCompatible(key) ? `.${key}` : `[${JSON.stringify(key)}]`;
}
function isKeyDotNotationCompatible(key) {
  return /^[a-z0-9\$_]+$/i.test(key);
}
var init_getPropAccessNotation = __esm({
  "node_modules/vike/dist/esm/utils/getPropAccessNotation.js"() {
  }
});

// node_modules/vike/dist/esm/utils/genPromise.js
function genPromise() {
  let resolve;
  const promise = new Promise((r7) => resolve = r7);
  return { promise, resolve };
}
var init_genPromise = __esm({
  "node_modules/vike/dist/esm/utils/genPromise.js"() {
  }
});

// node_modules/vike/dist/esm/node/runtime/utils.js
var init_utils3 = __esm({
  "node_modules/vike/dist/esm/node/runtime/utils.js"() {
    init_trackLogs();
    init_onLoad();
    init_assert();
    init_cast();
    init_checkType();
    init_isCallable();
    init_isBrowser();
    init_isPlainObject();
    init_isPromise();
    init_hasProp();
    init_parseUrl();
    init_parseUrl_extras();
    init_slice();
    init_sorter();
    init_projectInfo();
    init_isArray();
    init_isObject();
    init_objectAssign();
    init_PromiseType();
    init_compareString();
    init_isObjectWithKeys();
    init_stringifyStringArray();
    init_unique();
    init_filesystemPathHandling();
    init_getOutDirs();
    init_capitalizeFirstLetter();
    init_debugGlob();
    init_isSameErrorMessage();
    init_styleFileRE();
    init_isPropertyGetter();
    init_debug();
    init_urlToFile();
    init_getGlobalObject();
    init_freezePartial();
    init_isNpmPackage();
    init_isNotNullish();
    init_isScriptFile();
    init_removeFileExtention();
    init_objectKeys();
    init_objectEntries();
    init_objectFromEntries();
    init_getFileExtension();
    init_assertIsNotProductionRuntime();
    init_path_shim();
    init_assertNodeEnv();
    init_isHtml();
    init_warnIfErrorIsNotObject();
    init_stripAnsi();
    init_getTerminWidth();
    init_truncateString();
    init_formatHintLog();
    init_joinEnglish();
    init_isArrayOfStrings();
    init_escapeHtml();
    init_normalizeHeaders();
    init_isVikeReactApp();
    init_getPropAccessNotation();
    init_PROJECT_VERSION();
    init_genPromise();
    onLoad();
  }
});

// node_modules/vike/dist/esm/shared/assertPageContextProvidedByUser.js
function assertPageContextProvidedByUser(pageContextProvidedByUser, { hookName, hookFilePath }) {
  if (pageContextProvidedByUser === void 0 || pageContextProvidedByUser === null)
    return;
  assert(!hookName.endsWith(")"));
  const errPrefix = `The ${import_picocolors11.default.cyan("pageContext")} object provided by the ${hookName}() hook defined by ${hookFilePath}`;
  assertUsage2(isObject(pageContextProvidedByUser), `${errPrefix} should be an object (but it's ${import_picocolors11.default.cyan(`typeof pageContext === ${JSON.stringify(typeof pageContextProvidedByUser)}`)} instead)`);
  assertUsage2(!("_objectCreatedByVike" in pageContextProvidedByUser), `${errPrefix} shouldn't be the whole ${import_picocolors11.default.cyan("pageContext")} object, see https://vike.dev/pageContext-manipulation#do-not-return-entire-pagecontext`);
  assertWarning2(!("pageId" in pageContextProvidedByUser), `${errPrefix} sets ${import_picocolors11.default.cyan("pageContext.pageId")} which means that Vike's routing is overriden. This is an experimental feature: make sure to contact a vike maintainer before using this.`, { onlyOnce: true });
  assertUsage2(!("is404" in pageContextProvidedByUser), `${errPrefix} sets ${import_picocolors11.default.cyan("pageContext.is404")} which is forbidden, use ${import_picocolors11.default.cyan("throw render()")} instead, see https://vike.dev/render`);
}
var import_picocolors11;
var init_assertPageContextProvidedByUser = __esm({
  "node_modules/vike/dist/esm/shared/assertPageContextProvidedByUser.js"() {
    init_utils();
    import_picocolors11 = __toESM(require_picocolors(), 1);
  }
});

// node_modules/vike/dist/esm/node/runtime/html/injectAssets/injectHtmlTags.js
function injectHtmlTags(htmlString, htmlTags, position) {
  const htmlFragment = joinHtmlTags(htmlTags.filter((h6) => h6.position === position));
  if (htmlFragment) {
    htmlString = injectHtmlFragment(position, htmlFragment, htmlString);
  }
  return htmlString;
}
function injectHtmlTagsUsingStream(htmlTags, streamFromReactStreamingPackage) {
  const htmlFragment = joinHtmlTags(htmlTags.filter((h6) => h6.position === "HTML_STREAM"));
  if (htmlFragment) {
    assert(!streamFromReactStreamingPackage.hasStreamEnded());
    streamFromReactStreamingPackage.injectToStream(htmlFragment, { flush: true });
  }
}
function joinHtmlTags(htmlTags) {
  const htmlFragment = htmlTags.map((h6) => resolveHtmlTag(h6.htmlTag)).join("");
  return htmlFragment;
}
function injectHtmlFragment(position, htmlFragment, htmlString) {
  if (position === "HTML_BEGIN") {
    {
      const res = injectAtPaceholder(htmlFragment, htmlString, true);
      if (res)
        return res;
    }
    assert(tagOpeningExists("head", htmlString));
    htmlString = injectAtOpeningTag("head", htmlString, htmlFragment);
    return htmlString;
  }
  if (position === "HTML_END") {
    {
      const res = injectAtPaceholder(htmlFragment, htmlString, false);
      if (res)
        return res;
    }
    if (tagClosingExists("body", htmlString)) {
      return injectAtClosingTag("body", htmlString, htmlFragment);
    }
    if (tagClosingExists("html", htmlString)) {
      return injectAtClosingTag("html", htmlString, htmlFragment);
    }
    return htmlString + "\n" + htmlFragment;
  }
  assert(false);
}
function resolveHtmlTag(htmlTag) {
  return typeof htmlTag !== "string" ? htmlTag() : htmlTag;
}
function injectAtOpeningTag(tag2, htmlString, htmlFragment) {
  const openingTag = getTagOpening(tag2);
  const matches = htmlString.match(openingTag);
  assert(matches && matches.length >= 1);
  const tagInstance = matches[0];
  assert(tagInstance);
  const htmlParts = htmlString.split(tagInstance);
  assert(htmlParts.length >= 2);
  const before = slice(htmlParts, 0, 1)[0] + tagInstance;
  const after = slice(htmlParts, 1, 0).join(tagInstance);
  htmlFragment = injectBreakLines(htmlFragment, before, after);
  return before + htmlFragment + after;
}
function injectAtClosingTag(tag2, htmlString, htmlFragment) {
  const tagClosing = getTagClosing(tag2);
  const matches = htmlString.match(tagClosing);
  assert(matches && matches.length >= 1);
  const tagInstance = matches[0];
  assert(tagInstance);
  const htmlParts = htmlString.split(tagInstance);
  assert(htmlParts.length >= 2);
  const before = slice(htmlParts, 0, -1).join(tagInstance);
  const after = tagInstance + slice(htmlParts, -1, 0);
  htmlFragment = injectBreakLines(htmlFragment, before, after);
  return before + htmlFragment + after;
}
function injectBreakLines(htmlFragment, before, after) {
  assert(htmlFragment.trim() === htmlFragment);
  const currentLineBefore = before.split("\n").slice(-1)[0];
  let paddingParent = currentLineBefore.match(/\s*$/)[0];
  let isBlankLine = !!paddingParent;
  if (!paddingParent) {
    paddingParent = currentLineBefore.match(/^\s*/)[0];
  }
  if (!paddingParent)
    return htmlFragment;
  const whitespaceExtra = paddingParent ? "  " : "";
  const whitespace = `${paddingParent}${whitespaceExtra}`;
  const padding = `
${whitespace}`;
  htmlFragment = htmlFragment.replace(/<[^\/]/g, (match) => `${padding}${match}`);
  if (isBlankLine) {
    assert(htmlFragment.startsWith(padding), { htmlFragment });
    htmlFragment = whitespaceExtra + htmlFragment.slice(padding.length);
  }
  const currentLineAfter = after.split("\n")[0];
  if (currentLineAfter.trim().length > 0) {
    htmlFragment += "\n" + paddingParent;
  }
  return htmlFragment;
}
function createHtmlHeadIfMissing(htmlString) {
  const assertion = () => assert(tagOpeningExists("head", htmlString) && tagClosingExists("head", htmlString));
  if (tagOpeningExists("head", htmlString) && tagClosingExists("head", htmlString)) {
    assertion();
    return htmlString;
  }
  const htmlFragment = "<head></head>";
  if (tagOpeningExists("html", htmlString)) {
    htmlString = injectAtOpeningTag("html", htmlString, htmlFragment);
    assertion();
    return htmlString;
  }
  if (tagOpeningExists("!doctype", htmlString)) {
    htmlString = injectAtOpeningTag("!doctype", htmlString, htmlFragment);
    assertion();
    return htmlString;
  }
  htmlString = htmlFragment + "\n" + htmlString;
  assertion();
  return htmlString;
}
function tagOpeningExists(tag2, htmlString) {
  const tagOpeningRE = getTagOpening(tag2);
  return tagOpeningRE.test(htmlString);
}
function tagClosingExists(tag2, htmlString) {
  const tagClosingRE = getTagClosing(tag2);
  return tagClosingRE.test(htmlString);
}
function getTagOpening(tag2) {
  const tagOpening = new RegExp(`<${tag2}(>| [^>]*>)`, "i");
  return tagOpening;
}
function getTagClosing(tag2) {
  const tagClosing = new RegExp(`</${tag2}>`, "i");
  return tagClosing;
}
function injectAtPaceholder(htmlFragment, htmlString, isFirst) {
  const placeholder = isFirst ? "__VITE_PLUGIN_SSR__ASSETS_FIRST__" : "__VITE_PLUGIN__SSR_ASSETS_LAST__";
  const parts = htmlString.split(placeholder);
  if (parts.length === 1)
    return null;
  assertUsage2(parts.length === 2, "You're inserting assets twice into your HTML", { showStackTrace: true });
  return [parts[0], htmlFragment, parts[1]].join("");
}
var init_injectHtmlTags = __esm({
  "node_modules/vike/dist/esm/node/runtime/html/injectAssets/injectHtmlTags.js"() {
    init_utils3();
  }
});

// node_modules/@brillout/json-serializer/dist/cjs/types.js
var require_types = __commonJS({
  "node_modules/@brillout/json-serializer/dist/cjs/types.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.types = void 0;
    var types = [
      ts({
        is: (val) => val === void 0,
        match: (str) => str === "!undefined",
        serialize: () => "!undefined",
        deserialize: () => void 0
      }),
      ts({
        is: (val) => val === Infinity,
        match: (str) => str === "!Infinity",
        serialize: () => "!Infinity",
        deserialize: () => Infinity
      }),
      ts({
        is: (val) => val === -Infinity,
        match: (str) => str === "!-Infinity",
        serialize: () => "!-Infinity",
        deserialize: () => -Infinity
      }),
      ts({
        is: (val) => typeof val === "number" && isNaN(val),
        match: (str) => str === "!NaN",
        serialize: () => "!NaN",
        deserialize: () => NaN
      }),
      ts({
        is: (val) => val instanceof Date,
        match: (str) => str.startsWith("!Date:"),
        serialize: (val) => "!Date:" + val.toISOString(),
        deserialize: (str) => new Date(str.slice("!Date:".length))
      }),
      ts({
        is: (val) => typeof val === "bigint",
        match: (str) => str.startsWith("!BigInt:"),
        serialize: (val) => "!BigInt:" + val.toString(),
        deserialize: (str) => {
          if (typeof BigInt === "undefined") {
            throw new Error("Your JavaScript environement does not support BigInt. Consider adding a polyfill.");
          }
          return BigInt(str.slice("!BigInt:".length));
        }
      }),
      ts({
        is: (val) => val instanceof RegExp,
        match: (str) => str.startsWith("!RegExp:"),
        serialize: (val) => "!RegExp:" + val.toString(),
        deserialize: (str) => {
          str = str.slice("!RegExp:".length);
          const args = str.match(/\/(.*)\/(.*)?/);
          const pattern = args[1];
          const flags2 = args[2];
          return new RegExp(pattern, flags2);
        }
      }),
      ts({
        is: (val) => val instanceof Map,
        match: (str) => str.startsWith("!Map:"),
        serialize: (val, serializer) => "!Map:" + serializer(Array.from(val.entries())),
        deserialize: (str, deserializer) => new Map(deserializer(str.slice("!Map:".length)))
      }),
      ts({
        is: (val) => val instanceof Set,
        match: (str) => str.startsWith("!Set:"),
        serialize: (val, serializer) => "!Set:" + serializer(Array.from(val.values())),
        deserialize: (str, deserializer) => new Set(deserializer(str.slice("!Set:".length)))
      }),
      // Avoid collisions with the special strings defined above
      ts({
        is: (val) => typeof val === "string" && val.startsWith("!"),
        match: (str) => str.startsWith("!"),
        serialize: (val) => "!" + val,
        deserialize: (str) => str.slice(1)
      })
    ];
    exports.types = types;
    function ts(t20) {
      return t20;
    }
  }
});

// node_modules/@brillout/json-serializer/dist/cjs/utils/isReactElement.js
var require_isReactElement = __commonJS({
  "node_modules/@brillout/json-serializer/dist/cjs/utils/isReactElement.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.isReactElement = void 0;
    function isReactElement2(value) {
      return typeof value === "object" && value !== null && String(value["$$typeof"]) === "Symbol(react.element)";
    }
    exports.isReactElement = isReactElement2;
  }
});

// node_modules/@brillout/json-serializer/dist/cjs/utils/isCallable.js
var require_isCallable = __commonJS({
  "node_modules/@brillout/json-serializer/dist/cjs/utils/isCallable.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.isCallable = void 0;
    function isCallable3(thing) {
      return thing instanceof Function || typeof thing === "function";
    }
    exports.isCallable = isCallable3;
  }
});

// node_modules/@brillout/json-serializer/dist/cjs/utils/isObject.js
var require_isObject = __commonJS({
  "node_modules/@brillout/json-serializer/dist/cjs/utils/isObject.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.isObject = void 0;
    function isObject4(value) {
      if (typeof value !== "object" || value === null) {
        return false;
      }
      if (Array.isArray(value)) {
        return false;
      }
      return true;
    }
    exports.isObject = isObject4;
  }
});

// node_modules/@brillout/json-serializer/dist/cjs/utils/replacerWithPath.js
var require_replacerWithPath = __commonJS({
  "node_modules/@brillout/json-serializer/dist/cjs/utils/replacerWithPath.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.replacerWithPath = void 0;
    function replacerWithPath(replacer) {
      const pathMap = /* @__PURE__ */ new WeakMap();
      return function(key, value) {
        var _a;
        const pathPrevious = (_a = pathMap.get(this)) !== null && _a !== void 0 ? _a : [];
        const path = [...pathPrevious];
        if (key !== "") {
          const pathEntry = !Array.isArray(this) ? key : parseInt(key, 10);
          path.push(pathEntry);
        }
        if (isIterable(value))
          pathMap.set(value, path);
        return replacer.call(this, key, value, path);
      };
    }
    exports.replacerWithPath = replacerWithPath;
    function isIterable(value) {
      return value === Object(value);
    }
  }
});

// node_modules/@brillout/json-serializer/dist/cjs/stringify.js
var require_stringify = __commonJS({
  "node_modules/@brillout/json-serializer/dist/cjs/stringify.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.isJsonSerializerError = exports.stringify = void 0;
    var types_1 = require_types();
    var isReactElement_1 = require_isReactElement();
    var isCallable_1 = require_isCallable();
    var isObject_1 = require_isObject();
    var replacerWithPath_1 = require_replacerWithPath();
    function stringify3(value, {
      forbidReactElements,
      space,
      valueName,
      sortObjectKeys,
      // Used by Vike: https://github.com/vikejs/vike/blob/b4ba6b70e6bdc2e1f460c0d2e4c3faae5d0a733c/vike/node/plugin/plugins/importUserCode/v1-design/getConfigValuesSerialized.ts#L78
      replacer: replacerUserProvided
    } = {}) {
      const serializer = (val) => JSON.stringify(val, (0, replacerWithPath_1.replacerWithPath)(replacer), space);
      return serializer(value);
      function replacer(key, value2, path) {
        {
          const ret = replacerUserProvided === null || replacerUserProvided === void 0 ? void 0 : replacerUserProvided.call(this, key, value2);
          if (ret)
            return ret.replacement;
        }
        if (forbidReactElements && (0, isReactElement_1.isReactElement)(value2)) {
          throw genErr({
            value: value2,
            valueType: "React element",
            path,
            rootValueName: valueName
          });
        }
        if ((0, isCallable_1.isCallable)(value2)) {
          const functionName = value2.name;
          throw genErr({
            value: value2,
            valueType: "function",
            path,
            rootValueName: valueName,
            problematicValueName: path.length === 0 ? functionName : void 0
          });
        }
        const valueOriginal = this[key];
        for (const { is, serialize: serialize2 } of types_1.types.slice().reverse()) {
          if (is(valueOriginal)) {
            return serialize2(valueOriginal, serializer);
          }
        }
        if (sortObjectKeys && (0, isObject_1.isObject)(value2)) {
          const copy2 = {};
          Object.keys(value2).sort().forEach((key2) => {
            copy2[key2] = value2[key2];
          });
          value2 = copy2;
        }
        return value2;
      }
    }
    exports.stringify = stringify3;
    function genErr({ value, valueType, path, rootValueName, problematicValueName }) {
      const subjectName = getSubjectName({ path, rootValueName, problematicValueName });
      const messageCore = `cannot serialize ${subjectName} because it's a ${valueType}`;
      const err = new Error(`[@brillout/json-serializer](https://github.com/brillout/json-serializer) ${messageCore}.`);
      const pathString = getPathString(path, true);
      const errAddendum = {
        [stamp3]: true,
        messageCore,
        value,
        path,
        pathString,
        subjectName
      };
      Object.assign(err, errAddendum);
      return err;
    }
    var stamp3 = "_isJsonSerializerError";
    function isJsonSerializerError2(thing) {
      return (0, isObject_1.isObject)(thing) && thing[stamp3] === true;
    }
    exports.isJsonSerializerError = isJsonSerializerError2;
    function getSubjectName({ path, rootValueName, problematicValueName }) {
      const pathString = getPathString(path, !rootValueName);
      let subjectName;
      if (!pathString) {
        subjectName = rootValueName || problematicValueName || "value";
      } else {
        if (problematicValueName) {
          subjectName = problematicValueName + " at ";
        } else {
          subjectName = "";
        }
        subjectName = subjectName + (rootValueName || "") + pathString;
      }
      return subjectName;
    }
    function getPathString(path, canBeFirstKey) {
      const pathString = path.map((key, i13) => {
        if (typeof key === "number") {
          return `[${key}]`;
        }
        if (i13 === 0 && canBeFirstKey && isKeyDotNotationCompatible2(key)) {
          return key;
        }
        return getPropAccessNotation2(key);
      }).join("");
      return pathString;
    }
    function getPropAccessNotation2(key) {
      return typeof key === "string" && isKeyDotNotationCompatible2(key) ? `.${key}` : `[${JSON.stringify(key)}]`;
    }
    function isKeyDotNotationCompatible2(key) {
      return /^[a-z0-9\$_]+$/i.test(key);
    }
  }
});

// node_modules/vike/dist/esm/shared/addIs404ToPageProps.js
function addIs404ToPageProps(pageContext) {
  assertIs404(pageContext);
  addIs404(pageContext);
}
function assertIs404(pageContext) {
  if (isErrorPage(pageContext.pageId, pageContext._pageConfigs)) {
    assert(hasProp(pageContext, "is404", "boolean"));
  }
}
function addIs404(pageContext) {
  if (pageContext.is404 === void 0 || pageContext.is404 === null)
    return;
  const pageProps = pageContext.pageProps || {};
  if (!isObject(pageProps)) {
    assertWarning2(false, "pageContext.pageProps should be an object", { showStackTrace: true, onlyOnce: true });
    return;
  }
  pageProps.is404 = pageProps.is404 || pageContext.is404;
  pageContext.pageProps = pageProps;
}
var init_addIs404ToPageProps = __esm({
  "node_modules/vike/dist/esm/shared/addIs404ToPageProps.js"() {
    init_utils();
    init_error_page();
  }
});

// node_modules/vike/dist/esm/shared/notSerializable.js
var notSerializable;
var init_notSerializable = __esm({
  "node_modules/vike/dist/esm/shared/notSerializable.js"() {
    notSerializable = "not-serializable";
  }
});

// node_modules/vike/dist/esm/shared/misc/pageContextInitIsPassedToClient.js
var pageContextInitIsPassedToClient;
var init_pageContextInitIsPassedToClient = __esm({
  "node_modules/vike/dist/esm/shared/misc/pageContextInitIsPassedToClient.js"() {
    pageContextInitIsPassedToClient = "_pageContextInitIsPassedToClient";
  }
});

// node_modules/vike/dist/esm/shared/misc/isServerSideError.js
var isServerSideError;
var init_isServerSideError = __esm({
  "node_modules/vike/dist/esm/shared/misc/isServerSideError.js"() {
    isServerSideError = "_isServerSideError";
  }
});

// node_modules/vike/dist/esm/node/runtime/html/serializePageContextClientSide.js
function serializePageContextClientSide(pageContext) {
  const passToClient = getPassToClient(pageContext);
  const pageContextClient = {};
  passToClient.forEach((prop) => {
    pageContextClient[prop] = pageContext[prop];
  });
  if (Object.keys(pageContext._pageContextInit).some((p11) => passToClient.includes(p11))) {
    pageContextClient[pageContextInitIsPassedToClient] = true;
  }
  let pageContextSerialized;
  try {
    pageContextSerialized = serialize(pageContextClient);
  } catch (err) {
    const h6 = (s36) => import_picocolors12.default.cyan(s36);
    let hasWarned = false;
    const propsNonSerializable = [];
    passToClient.forEach((prop) => {
      const propName1 = getPropAccessNotation(prop);
      const propName2 = JSON.stringify(prop);
      const varName = `pageContext${propName1}`;
      try {
        serialize(pageContext[prop], varName);
      } catch (err2) {
        hasWarned = true;
        propsNonSerializable.push(prop);
        if (prop === "_configFromHook") {
          let pathString = "";
          if ((0, import_stringify.isJsonSerializerError)(err2)) {
            pathString = err2.pathString;
          }
          assertUsage2(false, `Cannot serialize config ${h6(pathString)} set by useConfig(), see https://vike.dev/useConfig#serialization-error`);
        }
        let msg = [
          `${h6(varName)} can't be serialized and, therefore, can't be passed to the client side.`,
          `Make sure ${h6(varName)} is serializable, or remove ${h6(propName2)} from ${h6("passToClient")}.`
        ].join(" ");
        if ((0, import_stringify.isJsonSerializerError)(err2)) {
          msg = `${msg} Serialization error: ${err2.messageCore}.`;
        } else {
          console.warn("Serialization error:");
          console.warn(err2);
          msg = `${msg} The serialization failed because of the error printed above.`;
        }
        assertWarning2(false, msg, { onlyOnce: false });
      }
    });
    assert(hasWarned);
    propsNonSerializable.forEach((prop) => {
      pageContextClient[prop] = notSerializable;
    });
    try {
      pageContextSerialized = serialize(pageContextClient);
    } catch (err2) {
      assert(false);
    }
  }
  return pageContextSerialized;
}
function serialize(value, varName) {
  return (0, import_stringify.stringify)(value, { forbidReactElements: true, valueName: varName });
}
function getPassToClient(pageContext) {
  let passToClient = [...pageContext._passToClient, ...PASS_TO_CLIENT];
  if (isErrorPage(pageContext.pageId, pageContext._pageConfigs)) {
    assert(hasProp(pageContext, "is404", "boolean"));
    addIs404ToPageProps(pageContext);
    passToClient.push(...PASS_TO_CLIENT_ERROR_PAGE);
  }
  passToClient = unique(passToClient);
  return passToClient;
}
function serializePageContextAbort(pageContext) {
  assert(pageContext._urlRedirect || pageContext._urlRewrite || pageContext.abortStatusCode);
  assert(pageContext._abortCall);
  assert(pageContext._abortCaller);
  delete pageContext._abortCaller;
  const unknownProps = Object.keys(pageContext).filter((prop) => ![
    // prettier-ignore
    // biome-ignore format:
    "_abortCall",
    /* Not needed on the client-side
    '_abortCaller',
    */
    "_urlRedirect",
    "_urlRewrite",
    "abortStatusCode",
    "abortReason",
    "is404",
    "pageProps"
  ].includes(prop));
  if (!pageContext._isLegacyRenderErrorPage) {
    assert(unknownProps.length === 0);
  } else {
    assertWarning2(unknownProps.length === 0, [
      "The following pageContext values won't be available on the client-side:",
      unknownProps.map((p11) => `  pageContext[${JSON.stringify(p11)}]`),
      "Use `throw render()` instead of `throw RenderErrorPage()`"
    ].join("\n"), {
      onlyOnce: false
    });
  }
  return serialize(pageContext);
}
var import_stringify, import_picocolors12, PASS_TO_CLIENT, PASS_TO_CLIENT_ERROR_PAGE;
var init_serializePageContextClientSide = __esm({
  "node_modules/vike/dist/esm/node/runtime/html/serializePageContextClientSide.js"() {
    import_stringify = __toESM(require_stringify(), 1);
    init_utils3();
    init_error_page();
    init_addIs404ToPageProps();
    import_picocolors12 = __toESM(require_picocolors(), 1);
    init_notSerializable();
    init_pageContextInitIsPassedToClient();
    init_isServerSideError();
    PASS_TO_CLIENT = [
      "abortReason",
      "_urlRewrite",
      "_urlRedirect",
      "abortStatusCode",
      "_abortCall",
      /* Not needed on the client-side
      '_abortCaller',
      */
      pageContextInitIsPassedToClient,
      "pageId",
      "routeParams",
      "data"
      // for data() hook
    ];
    PASS_TO_CLIENT_ERROR_PAGE = ["pageProps", "is404", isServerSideError];
  }
});

// node_modules/vike/dist/esm/node/runtime/html/injectAssets/sanitizeJson.js
function sanitizeJson(unsafe) {
  const safe = unsafe.replace(/</g, "\\u003c");
  return safe;
}
var init_sanitizeJson = __esm({
  "node_modules/vike/dist/esm/node/runtime/html/injectAssets/sanitizeJson.js"() {
  }
});

// node_modules/vike/dist/esm/node/runtime/html/injectAssets/inferHtmlTags.js
function inferPreloadTag(pageAsset) {
  const { src, assetType, mediaType } = pageAsset;
  const rel = getRel(pageAsset);
  const attributes = [
    `rel="${rel}"`,
    `href="${src}"`,
    !assetType ? null : `as="${assetType}"`,
    !mediaType ? null : `type="${mediaType}"`,
    // `crossorigin` is needed for fonts, see https://developer.mozilla.org/en-US/docs/Web/HTML/Link_types/preload#cors-enabled_fetches
    !isCrossOrigin(pageAsset) ? null : "crossorigin"
  ].filter(Boolean).join(" ");
  return `<link ${attributes}>`;
}
function inferAssetTag(pageAsset) {
  const { src, assetType, mediaType } = pageAsset;
  if (assetType === "script") {
    assert(mediaType === "text/javascript");
    return `<script src="${src}" ${scriptAttrs}></script>`;
  }
  if (assetType === "style") {
    return `<link rel="stylesheet" type="text/css" href="${src}">`;
  }
  assert(false, { pageAsset });
}
function inferEarlyHintLink(pageAsset) {
  const { src, assetType } = pageAsset;
  const rel = getRel(pageAsset);
  return [`<${src}>`, `rel=${rel}`, !assetType ? null : `as=${assetType}`].filter(Boolean).join("; ");
}
function getRel({ assetType }) {
  if (assetType === "script") {
    return "modulepreload";
  }
  return "preload";
}
function isCrossOrigin({ src, assetType }) {
  return assetType === "font" || src.startsWith("http://") || src.startsWith("https://");
}
var scriptAttrs;
var init_inferHtmlTags = __esm({
  "node_modules/vike/dist/esm/node/runtime/html/injectAssets/inferHtmlTags.js"() {
    init_utils3();
    scriptAttrs = 'type="module" async';
  }
});

// node_modules/vike/dist/esm/node/runtime/html/injectAssets/mergeScriptTags.js
function mergeScriptTags(scriptTagsHtml) {
  let scriptTag = "";
  const scripts = parseScripts(scriptTagsHtml);
  {
    const scriptsModule = scripts.filter(({ isModule }) => isModule);
    if (scriptsModule.length === 1) {
      scriptTag += scriptsModule[0].outerHtml;
    } else {
      const contents = [];
      scriptsModule.forEach(({ src, innerHtml }) => {
        const hasInnerHtml = !!innerHtml.trim();
        if (src) {
          assert(!hasInnerHtml);
          contents.push(`import ${JSON.stringify(src)};`);
        } else if (hasInnerHtml) {
          innerHtml = innerHtml.split("\n").filter(Boolean).join("\n");
          contents.push(innerHtml);
        }
      });
      if (contents.length > 0) {
        scriptTag += `<script ${scriptAttrs}>
${contents.join("\n")}
</script>`;
      }
    }
  }
  {
    const scriptsES5 = scripts.filter(({ isModule }) => !isModule);
    scriptsES5.forEach(({ outerHtml }) => {
      scriptTag += outerHtml;
    });
  }
  return scriptTag;
}
function parseScripts(htmlString) {
  const scripts = [];
  let match;
  while (match = scriptRE.exec(htmlString)) {
    const [outerHtml, openTag, innerHtml] = match;
    assert(outerHtml && openTag && innerHtml !== void 0);
    let isModule = false;
    {
      const typeMatch = openTag.match(typeRE);
      const type2 = typeMatch && (typeMatch[1] || typeMatch[2] || typeMatch[3]);
      isModule = type2 === "module";
    }
    let src = null;
    {
      const srcMatch = openTag.match(srcRE);
      src = srcMatch && (srcMatch[1] || srcMatch[2] || srcMatch[3]) || "";
    }
    scripts.push({ isModule, src, innerHtml, outerHtml });
  }
  return scripts;
}
var scriptRE, srcRE, typeRE;
var init_mergeScriptTags = __esm({
  "node_modules/vike/dist/esm/node/runtime/html/injectAssets/mergeScriptTags.js"() {
    init_utils3();
    init_inferHtmlTags();
    scriptRE = /(<script\b(?:\s[^>]*>|>))(.*?)<\/script>/gims;
    srcRE = /\bsrc\s*=\s*(?:"([^"]+)"|'([^']+)'|([^\s'">]+))/im;
    typeRE = /\btype\s*=\s*(?:"([^"]+)"|'([^']+)'|([^\s'">]+))/im;
  }
});

// node_modules/vike/dist/esm/shared/page-configs/helpers.js
function getPageConfig(pageId, pageConfigs) {
  const pageConfig = pageConfigs.find((p11) => p11.pageId === pageId);
  assert(pageConfigs.length > 0);
  assert(pageConfig);
  return pageConfig;
}
function getConfigValueFilePathToShowToUser(definedAtData) {
  if (!definedAtData || isArray(definedAtData))
    return null;
  const { filePathToShowToUser } = definedAtData;
  assert(filePathToShowToUser);
  return filePathToShowToUser;
}
function getHookFilePathToShowToUser(definedAtData) {
  const filePathToShowToUser = getConfigValueFilePathToShowToUser(definedAtData);
  assert(filePathToShowToUser);
  return filePathToShowToUser;
}
var init_helpers = __esm({
  "node_modules/vike/dist/esm/shared/page-configs/helpers.js"() {
    init_utils();
  }
});

// node_modules/vike/dist/esm/shared/page-configs/getExportPath.js
function getExportPath(fileExportPathToShowToUser, configName) {
  if (!fileExportPathToShowToUser)
    return null;
  let [exportName, ...exportObjectPath] = fileExportPathToShowToUser;
  if (!exportName)
    return null;
  if (exportObjectPath.length === 0 && ["*", "default", configName].includes(exportName))
    return null;
  assert(exportName !== "*");
  let prefix = "";
  let suffix = "";
  if (exportName === "default") {
    prefix = "export default";
  } else {
    prefix = "export";
    exportObjectPath = [exportName, ...exportObjectPath];
  }
  exportObjectPath.forEach((prop) => {
    prefix = `${prefix} { ${prop}`;
    suffix = ` }${suffix}`;
  });
  const exportPath = prefix + suffix;
  return exportPath;
}
var init_getExportPath = __esm({
  "node_modules/vike/dist/esm/shared/page-configs/getExportPath.js"() {
    init_utils();
  }
});

// node_modules/vike/dist/esm/shared/page-configs/getConfigDefinedAt.js
function getConfigDefinedAt(sentenceBegin, configName, definedAtData) {
  return `${begin(sentenceBegin, configName)} at ${getDefinedAtString(definedAtData, configName)}`;
}
function getConfigDefinedAtOptional(sentenceBegin, configName, definedAtData) {
  if (!definedAtData) {
    return `${begin(sentenceBegin, configName)} internally`;
  } else {
    return `${begin(sentenceBegin, configName)} at ${getDefinedAtString(definedAtData, configName)}`;
  }
}
function begin(sentenceBegin, configName) {
  return `${sentenceBegin} ${import_picocolors13.default.cyan(configName)} defined`;
}
function getDefinedAtString(definedAtData, configName) {
  let files;
  if (isArray(definedAtData)) {
    files = definedAtData;
  } else {
    files = [definedAtData];
  }
  assert(files.length >= 1);
  const definedAtString = files.map((source) => {
    const { filePathToShowToUser, fileExportPathToShowToUser } = source;
    let s36 = filePathToShowToUser;
    const exportPath = getExportPath(fileExportPathToShowToUser, configName);
    if (exportPath) {
      s36 = `${s36} > ${import_picocolors13.default.cyan(exportPath)}`;
    }
    return s36;
  }).join(" / ");
  return definedAtString;
}
var import_picocolors13;
var init_getConfigDefinedAt = __esm({
  "node_modules/vike/dist/esm/shared/page-configs/getConfigDefinedAt.js"() {
    init_utils();
    import_picocolors13 = __toESM(require_picocolors(), 1);
    init_getExportPath();
  }
});

// node_modules/vike/dist/esm/shared/page-configs/getConfigValue.js
function getConfigValueTyped(configValue, configName, type2) {
  const { value, definedAtData } = configValue;
  if (type2)
    assertConfigValueType(value, type2, configName, definedAtData);
  return configValue;
}
function getConfigValueRuntime(pageConfig, configName, type2) {
  const configValue = pageConfig.configValues[configName];
  if (!configValue)
    return null;
  return getConfigValueTyped(configValue, configName, type2);
}
function assertConfigValueType(value, type2, configName, definedAtData) {
  assert(value !== null);
  const typeActual = typeof value;
  if (typeActual === type2)
    return;
  const valuePrintable = getValuePrintable(value);
  const problem = valuePrintable !== null ? `value ${import_picocolors14.default.cyan(valuePrintable)}` : `type ${import_picocolors14.default.cyan(typeActual)}`;
  const configDefinedAt = getConfigDefinedAtOptional("Config", configName, definedAtData);
  const errMsg = `${configDefinedAt} has an invalid ${problem}: it should be a ${import_picocolors14.default.cyan(type2)} instead`;
  assertUsage2(false, errMsg);
}
var import_picocolors14;
var init_getConfigValue = __esm({
  "node_modules/vike/dist/esm/shared/page-configs/getConfigValue.js"() {
    init_utils();
    import_picocolors14 = __toESM(require_picocolors(), 1);
    init_getConfigDefinedAt();
  }
});

// node_modules/@brillout/vite-plugin-server-entry/dist/utils/createErrorWithCleanStackTrace.js
var require_createErrorWithCleanStackTrace = __commonJS({
  "node_modules/@brillout/vite-plugin-server-entry/dist/utils/createErrorWithCleanStackTrace.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createErrorWithCleanStackTrace = createErrorWithCleanStackTrace2;
    function createErrorWithCleanStackTrace2(errorMessage, numberOfStackTraceLinesToRemove2) {
      const err = new Error(errorMessage);
      err.stack = clean2(err.stack, numberOfStackTraceLinesToRemove2);
      return err;
    }
    function clean2(errStack, numberOfStackTraceLinesToRemove2) {
      if (!errStack) {
        return errStack;
      }
      const stackLines = splitByLine2(errStack);
      let linesRemoved = 0;
      const stackLine__cleaned = stackLines.filter((line) => {
        if (line.includes(" (internal/") || line.includes(" (node:internal")) {
          return false;
        }
        if (linesRemoved < numberOfStackTraceLinesToRemove2 && isStackTraceLine2(line)) {
          linesRemoved++;
          return false;
        }
        return true;
      }).join("\n");
      return stackLine__cleaned;
    }
    function isStackTraceLine2(line) {
      return line.startsWith("    at ");
    }
    function splitByLine2(str) {
      return str.split(/\r?\n/);
    }
  }
});

// node_modules/@brillout/vite-plugin-server-entry/dist/utils/PROJECT_VERSION.js
var require_PROJECT_VERSION = __commonJS({
  "node_modules/@brillout/vite-plugin-server-entry/dist/utils/PROJECT_VERSION.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.PROJECT_VERSION = void 0;
    exports.PROJECT_VERSION = "0.5.4";
  }
});

// node_modules/@brillout/vite-plugin-server-entry/dist/utils/projectInfo.js
var require_projectInfo = __commonJS({
  "node_modules/@brillout/vite-plugin-server-entry/dist/utils/projectInfo.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.projectInfo = void 0;
    var PROJECT_VERSION_1 = require_PROJECT_VERSION();
    exports.projectInfo = {
      projectName: "@brillout/vite-plugin-server-entry",
      npmPackageName: "@brillout/vite-plugin-server-entry",
      projectVersion: PROJECT_VERSION_1.PROJECT_VERSION,
      githubRepository: "https://github.com/brillout/vite-plugin-server-entry"
    };
  }
});

// node_modules/@brillout/vite-plugin-server-entry/dist/utils/assert.js
var require_assert = __commonJS({
  "node_modules/@brillout/vite-plugin-server-entry/dist/utils/assert.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.logLabel = void 0;
    exports.assert = assert2;
    exports.assertUsage = assertUsage3;
    var createErrorWithCleanStackTrace_1 = require_createErrorWithCleanStackTrace();
    var projectInfo_1 = require_projectInfo();
    var logLabel = `[${projectInfo_1.projectInfo.npmPackageName}@${projectInfo_1.projectInfo.projectVersion}]`;
    exports.logLabel = logLabel;
    var internalErrorPrefix = `${logLabel}[Bug]`;
    var usageErrorPrefix = `${logLabel}[Wrong Usage]`;
    var numberOfStackTraceLinesToRemove2 = 2;
    function assert2(condition2, debugInfo) {
      if (condition2) {
        return;
      }
      const debugStr = (() => {
        if (!debugInfo) {
          return null;
        }
        const debugInfoSerialized = typeof debugInfo === "string" ? debugInfo : "`" + JSON.stringify(debugInfo) + "`";
        return `Debug info (this is for the ${projectInfo_1.projectInfo.projectName} maintainers; you can ignore this): ${debugInfoSerialized}`;
      })();
      const internalError = (0, createErrorWithCleanStackTrace_1.createErrorWithCleanStackTrace)([
        `${internalErrorPrefix} You stumbled upon a bug in the source code of ${projectInfo_1.projectInfo.projectName}.`,
        `Reach out at ${projectInfo_1.projectInfo.githubRepository}/issues/new and include this error stack (the error stack is usually enough to fix the problem).`,
        "A maintainer will fix the bug (usually under 24 hours).",
        `Don't hesitate to reach out as it makes ${projectInfo_1.projectInfo.projectName} more robust.`,
        debugStr
      ].filter(Boolean).join(" "), numberOfStackTraceLinesToRemove2);
      throw internalError;
    }
    function assertUsage3(condition2, errorMessage) {
      if (condition2) {
        return;
      }
      const errMsg = `${usageErrorPrefix} ${errorMessage}`;
      const usageError = (0, createErrorWithCleanStackTrace_1.createErrorWithCleanStackTrace)(errMsg, numberOfStackTraceLinesToRemove2);
      throw usageError;
    }
  }
});

// node_modules/@brillout/vite-plugin-server-entry/dist/utils/filesystemPathHandling.js
var require_filesystemPathHandling = __commonJS({
  "node_modules/@brillout/vite-plugin-server-entry/dist/utils/filesystemPathHandling.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.toPosixPath = toPosixPath2;
    exports.assertPosixPath = assertPosixPath2;
    exports.toSystemPath = toSystemPath;
    var assert_1 = require_assert();
    var sepPosix = "/";
    var sepWin32 = "\\";
    function toPosixPath2(path) {
      if (isPosix()) {
        assertPosixPath2(path);
        return path;
      }
      if (isWin32()) {
        const pathPosix = path.split(sepWin32).join(sepPosix);
        assertPosixPath2(pathPosix);
        return pathPosix;
      }
      (0, assert_1.assert)(false);
    }
    function assertPosixPath2(path) {
      (0, assert_1.assert)(path && !path.includes(sepWin32), `Wrongly formatted path: ${path}`);
    }
    function toSystemPath(path) {
      if (isPosix()) {
        return toPosixPath2(path);
      }
      if (isWin32()) {
        return path.split(sepPosix).join(sepWin32);
      }
      (0, assert_1.assert)(false);
    }
    function isWin32() {
      return process.platform === "win32";
    }
    function isPosix() {
      return !isWin32();
    }
  }
});

// node_modules/@brillout/vite-plugin-server-entry/dist/utils/getCwdSafe.js
var require_getCwdSafe = __commonJS({
  "node_modules/@brillout/vite-plugin-server-entry/dist/utils/getCwdSafe.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.getCwdSafe = getCwdSafe;
    var filesystemPathHandling_1 = require_filesystemPathHandling();
    function getCwdSafe() {
      if (typeof process == "undefined" || !("cwd" in process))
        return null;
      return (0, filesystemPathHandling_1.toPosixPath)(process.cwd());
    }
  }
});

// node_modules/@brillout/import/dist/index.js
var require_dist2 = __commonJS({
  "node_modules/@brillout/import/dist/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.import_ = void 0;
    exports.default = import_2;
    function import_2(id3) {
      id3 = fixWindowsBug(id3);
      return import(
        /*webpackIgnore: true*/
        id3
      );
    }
    exports.import_ = import_2;
    var prefix = "file://";
    function fixWindowsBug(id3) {
      if (process.platform === "win32" && isAbsolute(id3) && !id3.startsWith(prefix)) {
        return prefix + id3;
      } else {
        return id3;
      }
    }
    function isAbsolute(path) {
      return /^[/\\](?![/\\])|^[/\\]{2}(?!\.)|^[A-Za-z]:[/\\]/.test(path);
    }
  }
});

// node_modules/@brillout/vite-plugin-server-entry/dist/utils/require.js
var require_require = __commonJS({
  "node_modules/@brillout/vite-plugin-server-entry/dist/utils/require.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.requireResolve = requireResolve;
    var import_1 = require_dist2();
    async function requireResolve(id3, currentFilePath) {
      const req = await getRequire(currentFilePath);
      return req.resolve(id3);
    }
    async function getRequire(currentFilePath) {
      const { createRequire } = await (0, import_1.import_)("module");
      const req = createRequire(currentFilePath);
      return req;
    }
  }
});

// node_modules/@brillout/vite-plugin-server-entry/dist/utils/isWebpackResolve.js
var require_isWebpackResolve = __commonJS({
  "node_modules/@brillout/vite-plugin-server-entry/dist/utils/isWebpackResolve.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.isWebpackResolve = isWebpackResolve;
    function isWebpackResolve(moduleResolve) {
      return typeof moduleResolve === "number";
    }
  }
});

// node_modules/@brillout/vite-plugin-server-entry/dist/runtime/utils.js
var require_utils2 = __commonJS({
  "node_modules/@brillout/vite-plugin-server-entry/dist/runtime/utils.js"(exports) {
    "use strict";
    var __createBinding2 = exports && exports.__createBinding || (Object.create ? function(o8, m12, k5, k22) {
      if (k22 === void 0) k22 = k5;
      var desc = Object.getOwnPropertyDescriptor(m12, k5);
      if (!desc || ("get" in desc ? !m12.__esModule : desc.writable || desc.configurable)) {
        desc = { enumerable: true, get: function() {
          return m12[k5];
        } };
      }
      Object.defineProperty(o8, k22, desc);
    } : function(o8, m12, k5, k22) {
      if (k22 === void 0) k22 = k5;
      o8[k22] = m12[k5];
    });
    var __exportStar2 = exports && exports.__exportStar || function(m12, exports2) {
      for (var p11 in m12) if (p11 !== "default" && !Object.prototype.hasOwnProperty.call(exports2, p11)) __createBinding2(exports2, m12, p11);
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    __exportStar2(require_assert(), exports);
    __exportStar2(require_getCwdSafe(), exports);
    __exportStar2(require_filesystemPathHandling(), exports);
    __exportStar2(require_require(), exports);
    __exportStar2(require_isWebpackResolve(), exports);
  }
});

// node_modules/@brillout/vite-plugin-server-entry/dist/shared/utils.js
var require_utils3 = __commonJS({
  "node_modules/@brillout/vite-plugin-server-entry/dist/shared/utils.js"(exports) {
    "use strict";
    var __createBinding2 = exports && exports.__createBinding || (Object.create ? function(o8, m12, k5, k22) {
      if (k22 === void 0) k22 = k5;
      var desc = Object.getOwnPropertyDescriptor(m12, k5);
      if (!desc || ("get" in desc ? !m12.__esModule : desc.writable || desc.configurable)) {
        desc = { enumerable: true, get: function() {
          return m12[k5];
        } };
      }
      Object.defineProperty(o8, k22, desc);
    } : function(o8, m12, k5, k22) {
      if (k22 === void 0) k22 = k5;
      o8[k22] = m12[k5];
    });
    var __exportStar2 = exports && exports.__exportStar || function(m12, exports2) {
      for (var p11 in m12) if (p11 !== "default" && !Object.prototype.hasOwnProperty.call(exports2, p11)) __createBinding2(exports2, m12, p11);
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    __exportStar2(require_assert(), exports);
    __exportStar2(require_getCwdSafe(), exports);
  }
});

// node_modules/@brillout/vite-plugin-server-entry/dist/shared/debug.js
var require_debug = __commonJS({
  "node_modules/@brillout/vite-plugin-server-entry/dist/shared/debug.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.DEBUG = void 0;
    exports.logDebug = logDebug;
    var utils_1 = require_utils3();
    var DEBUG = false;
    exports.DEBUG = DEBUG;
    function logDebug(...msgs) {
      console.log(`${utils_1.logLabel}[DEBUG]`, ...msgs);
    }
  }
});

// node_modules/@brillout/vite-plugin-server-entry/dist/runtime/debugLogsRuntime.js
var require_debugLogsRuntime = __commonJS({
  "node_modules/@brillout/vite-plugin-server-entry/dist/runtime/debugLogsRuntime.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.debugLogsRuntimePre = debugLogsRuntimePre;
    exports.debugLogsRuntimePost = debugLogsRuntimePost;
    var utils_1 = require_utils3();
    var debug_1 = require_debug();
    function debugLogsRuntimePre(autoImporter) {
      if (!debug_1.DEBUG)
        return;
      (0, debug_1.logDebug)("DEBUG_LOGS_RUNTIME [begin]");
      try {
        (0, debug_1.logDebug)("process.platform", JSON.stringify(process.platform));
      } catch {
        (0, debug_1.logDebug)("process.platform", "undefined");
      }
      try {
        (0, debug_1.logDebug)("process.release", JSON.stringify(process.release));
      } catch {
        (0, debug_1.logDebug)("process.release", "undefined");
      }
      try {
        (0, debug_1.logDebug)("navigator", JSON.stringify(navigator));
      } catch {
        (0, debug_1.logDebug)("navigator", "undefined");
      }
      (0, debug_1.logDebug)("cwd", (0, utils_1.getCwdSafe)());
      (0, debug_1.logDebug)("importer.status", autoImporter.status);
      if (autoImporter.status === "SET") {
        (0, debug_1.logDebug)("importer.paths.autoImporterFilePathOriginal", autoImporter.paths.autoImporterFilePathOriginal);
        (0, debug_1.logDebug)("importer.paths.autoImporterFileDirActual", autoImporter.paths.autoImporterFileDirActual);
        (0, debug_1.logDebug)("importer.paths.serverEntryFilePathRelative", autoImporter.paths.serverEntryFilePathRelative);
        (0, debug_1.logDebug)("importer.paths.serverEntryFilePathOriginal", autoImporter.paths.serverEntryFilePathOriginal);
        try {
          (0, debug_1.logDebug)("importer.paths.serverEntryFilePathResolved()", autoImporter.paths.serverEntryFilePathResolved());
        } catch (err) {
          (0, debug_1.logDebug)("importer.paths.serverEntryFilePathResolved() error:", err);
          (0, debug_1.logDebug)("importer.paths.serverEntryFilePathResolved()", "ERRORED");
        }
      }
    }
    function debugLogsRuntimePost({ success, requireError, outDir, isOutsideOfCwd }) {
      if (!debug_1.DEBUG)
        return;
      (0, debug_1.logDebug)("requireError", requireError);
      (0, debug_1.logDebug)("outDir", outDir);
      (0, debug_1.logDebug)("isOutsideOfCwd", isOutsideOfCwd);
      (0, debug_1.logDebug)("success", success);
      (0, debug_1.logDebug)("DEBUG_LOGS_RUNTIME [end]");
    }
  }
});

// node_modules/@brillout/vite-plugin-server-entry/dist/shared/serverEntryFileNameBase.js
var require_serverEntryFileNameBase = __commonJS({
  "node_modules/@brillout/vite-plugin-server-entry/dist/shared/serverEntryFileNameBase.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.serverEntryFileNameBaseAlternative = exports.serverEntryFileNameBase = void 0;
    exports.serverEntryFileNameBase = "entry";
    exports.serverEntryFileNameBaseAlternative = "entryOthers";
  }
});

// node_modules/@brillout/vite-plugin-server-entry/dist/runtime/crawlServerEntry.js
var require_crawlServerEntry = __commonJS({
  "node_modules/@brillout/vite-plugin-server-entry/dist/runtime/crawlServerEntry.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.crawlServerEntry = crawlServerEntry;
    var utils_1 = require_utils2();
    var import_1 = require_dist2();
    var serverEntryFileNameBase_1 = require_serverEntryFileNameBase();
    async function crawlServerEntry(outDir) {
      let path;
      let fs;
      try {
        path = await (0, import_1.import_)("path");
        fs = await (0, import_1.import_)("fs");
      } catch {
        return false;
      }
      const cwd = process.cwd();
      const isPathAbsolute = (p11) => {
        if (process.platform === "win32") {
          return path.win32.isAbsolute(p11);
        } else {
          return p11.startsWith("/");
        }
      };
      if (outDir) {
        (0, utils_1.assertPosixPath)(outDir);
        (0, utils_1.assert)(isPathAbsolute(outDir), outDir);
      } else {
        if (!cwd)
          return false;
        outDir = path.posix.join(cwd, "dist");
      }
      const serverEntryFileDir = path.posix.join(outDir, "server");
      if (!fs.existsSync(serverEntryFileDir))
        return false;
      let serverEntryFilePath = null;
      const entryFileCandidates = [
        `${serverEntryFileNameBase_1.serverEntryFileNameBase}.mjs`,
        `${serverEntryFileNameBase_1.serverEntryFileNameBase}.js`,
        `${serverEntryFileNameBase_1.serverEntryFileNameBase}.cjs`,
        `${serverEntryFileNameBase_1.serverEntryFileNameBaseAlternative}.mjs`,
        `${serverEntryFileNameBase_1.serverEntryFileNameBaseAlternative}.js`,
        `${serverEntryFileNameBase_1.serverEntryFileNameBaseAlternative}.cjs`
      ];
      for (const entryFileName of entryFileCandidates) {
        const serverEntryFilePathSpeculative = path.posix.join(serverEntryFileDir, entryFileName);
        (0, utils_1.assert)(isPathAbsolute(serverEntryFilePathSpeculative));
        try {
          serverEntryFilePath = await (0, utils_1.requireResolve)(
            serverEntryFilePathSpeculative,
            // Since `serverEntryFilePathSpeculative` is absolute, we can pass a wrong `currentFilePath` argument value.
            // - We avoid using `__filename` because it isn't defined when this file is included in an ESM bundle.
            // - We cannot use `import.meta.filename` (nor `import.meta.url`) because there doesn't seem to be a way to safely/conditionally access `import.meta`.
            cwd
          );
        } catch {
        }
      }
      (0, utils_1.assertUsage)(serverEntryFilePath, `Cannot find server entry. If you use rollupOptions.output.entryFileNames then make sure to not rename the server entry file. Make sure that one of the following exists: 
${entryFileCandidates.map((e14) => `  ${e14}`).join("\n")}`);
      if ((0, utils_1.isWebpackResolve)(serverEntryFilePath)) {
        return false;
      }
      await (0, import_1.import_)(serverEntryFilePath);
      return true;
    }
  }
});

// node_modules/react/cjs/react.production.min.js
var require_react_production_min = __commonJS({
  "node_modules/react/cjs/react.production.min.js"(exports) {
    "use strict";
    var l12 = Symbol.for("react.element");
    var n13 = Symbol.for("react.portal");
    var p11 = Symbol.for("react.fragment");
    var q2 = Symbol.for("react.strict_mode");
    var r7 = Symbol.for("react.profiler");
    var t20 = Symbol.for("react.provider");
    var u4 = Symbol.for("react.context");
    var v5 = Symbol.for("react.forward_ref");
    var w11 = Symbol.for("react.suspense");
    var x3 = Symbol.for("react.memo");
    var y4 = Symbol.for("react.lazy");
    var z2 = Symbol.iterator;
    function A4(a18) {
      if (null === a18 || "object" !== typeof a18) return null;
      a18 = z2 && a18[z2] || a18["@@iterator"];
      return "function" === typeof a18 ? a18 : null;
    }
    var B4 = { isMounted: function() {
      return false;
    }, enqueueForceUpdate: function() {
    }, enqueueReplaceState: function() {
    }, enqueueSetState: function() {
    } };
    var C5 = Object.assign;
    var D5 = {};
    function E6(a18, b4, e14) {
      this.props = a18;
      this.context = b4;
      this.refs = D5;
      this.updater = e14 || B4;
    }
    E6.prototype.isReactComponent = {};
    E6.prototype.setState = function(a18, b4) {
      if ("object" !== typeof a18 && "function" !== typeof a18 && null != a18) throw Error("setState(...): takes an object of state variables to update or a function which returns an object of state variables.");
      this.updater.enqueueSetState(this, a18, b4, "setState");
    };
    E6.prototype.forceUpdate = function(a18) {
      this.updater.enqueueForceUpdate(this, a18, "forceUpdate");
    };
    function F4() {
    }
    F4.prototype = E6.prototype;
    function G3(a18, b4, e14) {
      this.props = a18;
      this.context = b4;
      this.refs = D5;
      this.updater = e14 || B4;
    }
    var H4 = G3.prototype = new F4();
    H4.constructor = G3;
    C5(H4, E6.prototype);
    H4.isPureReactComponent = true;
    var I6 = Array.isArray;
    var J2 = Object.prototype.hasOwnProperty;
    var K3 = { current: null };
    var L3 = { key: true, ref: true, __self: true, __source: true };
    function M6(a18, b4, e14) {
      var d12, c15 = {}, k5 = null, h6 = null;
      if (null != b4) for (d12 in void 0 !== b4.ref && (h6 = b4.ref), void 0 !== b4.key && (k5 = "" + b4.key), b4) J2.call(b4, d12) && !L3.hasOwnProperty(d12) && (c15[d12] = b4[d12]);
      var g7 = arguments.length - 2;
      if (1 === g7) c15.children = e14;
      else if (1 < g7) {
        for (var f15 = Array(g7), m12 = 0; m12 < g7; m12++) f15[m12] = arguments[m12 + 2];
        c15.children = f15;
      }
      if (a18 && a18.defaultProps) for (d12 in g7 = a18.defaultProps, g7) void 0 === c15[d12] && (c15[d12] = g7[d12]);
      return { $$typeof: l12, type: a18, key: k5, ref: h6, props: c15, _owner: K3.current };
    }
    function N4(a18, b4) {
      return { $$typeof: l12, type: a18.type, key: b4, ref: a18.ref, props: a18.props, _owner: a18._owner };
    }
    function O5(a18) {
      return "object" === typeof a18 && null !== a18 && a18.$$typeof === l12;
    }
    function escape2(a18) {
      var b4 = { "=": "=0", ":": "=2" };
      return "$" + a18.replace(/[=:]/g, function(a19) {
        return b4[a19];
      });
    }
    var P4 = /\/+/g;
    function Q4(a18, b4) {
      return "object" === typeof a18 && null !== a18 && null != a18.key ? escape2("" + a18.key) : b4.toString(36);
    }
    function R10(a18, b4, e14, d12, c15) {
      var k5 = typeof a18;
      if ("undefined" === k5 || "boolean" === k5) a18 = null;
      var h6 = false;
      if (null === a18) h6 = true;
      else switch (k5) {
        case "string":
        case "number":
          h6 = true;
          break;
        case "object":
          switch (a18.$$typeof) {
            case l12:
            case n13:
              h6 = true;
          }
      }
      if (h6) return h6 = a18, c15 = c15(h6), a18 = "" === d12 ? "." + Q4(h6, 0) : d12, I6(c15) ? (e14 = "", null != a18 && (e14 = a18.replace(P4, "$&/") + "/"), R10(c15, b4, e14, "", function(a19) {
        return a19;
      })) : null != c15 && (O5(c15) && (c15 = N4(c15, e14 + (!c15.key || h6 && h6.key === c15.key ? "" : ("" + c15.key).replace(P4, "$&/") + "/") + a18)), b4.push(c15)), 1;
      h6 = 0;
      d12 = "" === d12 ? "." : d12 + ":";
      if (I6(a18)) for (var g7 = 0; g7 < a18.length; g7++) {
        k5 = a18[g7];
        var f15 = d12 + Q4(k5, g7);
        h6 += R10(k5, b4, e14, f15, c15);
      }
      else if (f15 = A4(a18), "function" === typeof f15) for (a18 = f15.call(a18), g7 = 0; !(k5 = a18.next()).done; ) k5 = k5.value, f15 = d12 + Q4(k5, g7++), h6 += R10(k5, b4, e14, f15, c15);
      else if ("object" === k5) throw b4 = String(a18), Error("Objects are not valid as a React child (found: " + ("[object Object]" === b4 ? "object with keys {" + Object.keys(a18).join(", ") + "}" : b4) + "). If you meant to render a collection of children, use an array instead.");
      return h6;
    }
    function S2(a18, b4, e14) {
      if (null == a18) return a18;
      var d12 = [], c15 = 0;
      R10(a18, d12, "", "", function(a19) {
        return b4.call(e14, a19, c15++);
      });
      return d12;
    }
    function T3(a18) {
      if (-1 === a18._status) {
        var b4 = a18._result;
        b4 = b4();
        b4.then(function(b5) {
          if (0 === a18._status || -1 === a18._status) a18._status = 1, a18._result = b5;
        }, function(b5) {
          if (0 === a18._status || -1 === a18._status) a18._status = 2, a18._result = b5;
        });
        -1 === a18._status && (a18._status = 0, a18._result = b4);
      }
      if (1 === a18._status) return a18._result.default;
      throw a18._result;
    }
    var U3 = { current: null };
    var V3 = { transition: null };
    var W4 = { ReactCurrentDispatcher: U3, ReactCurrentBatchConfig: V3, ReactCurrentOwner: K3 };
    function X4() {
      throw Error("act(...) is not supported in production builds of React.");
    }
    exports.Children = { map: S2, forEach: function(a18, b4, e14) {
      S2(a18, function() {
        b4.apply(this, arguments);
      }, e14);
    }, count: function(a18) {
      var b4 = 0;
      S2(a18, function() {
        b4++;
      });
      return b4;
    }, toArray: function(a18) {
      return S2(a18, function(a19) {
        return a19;
      }) || [];
    }, only: function(a18) {
      if (!O5(a18)) throw Error("React.Children.only expected to receive a single React element child.");
      return a18;
    } };
    exports.Component = E6;
    exports.Fragment = p11;
    exports.Profiler = r7;
    exports.PureComponent = G3;
    exports.StrictMode = q2;
    exports.Suspense = w11;
    exports.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED = W4;
    exports.act = X4;
    exports.cloneElement = function(a18, b4, e14) {
      if (null === a18 || void 0 === a18) throw Error("React.cloneElement(...): The argument must be a React element, but you passed " + a18 + ".");
      var d12 = C5({}, a18.props), c15 = a18.key, k5 = a18.ref, h6 = a18._owner;
      if (null != b4) {
        void 0 !== b4.ref && (k5 = b4.ref, h6 = K3.current);
        void 0 !== b4.key && (c15 = "" + b4.key);
        if (a18.type && a18.type.defaultProps) var g7 = a18.type.defaultProps;
        for (f15 in b4) J2.call(b4, f15) && !L3.hasOwnProperty(f15) && (d12[f15] = void 0 === b4[f15] && void 0 !== g7 ? g7[f15] : b4[f15]);
      }
      var f15 = arguments.length - 2;
      if (1 === f15) d12.children = e14;
      else if (1 < f15) {
        g7 = Array(f15);
        for (var m12 = 0; m12 < f15; m12++) g7[m12] = arguments[m12 + 2];
        d12.children = g7;
      }
      return { $$typeof: l12, type: a18.type, key: c15, ref: k5, props: d12, _owner: h6 };
    };
    exports.createContext = function(a18) {
      a18 = { $$typeof: u4, _currentValue: a18, _currentValue2: a18, _threadCount: 0, Provider: null, Consumer: null, _defaultValue: null, _globalName: null };
      a18.Provider = { $$typeof: t20, _context: a18 };
      return a18.Consumer = a18;
    };
    exports.createElement = M6;
    exports.createFactory = function(a18) {
      var b4 = M6.bind(null, a18);
      b4.type = a18;
      return b4;
    };
    exports.createRef = function() {
      return { current: null };
    };
    exports.forwardRef = function(a18) {
      return { $$typeof: v5, render: a18 };
    };
    exports.isValidElement = O5;
    exports.lazy = function(a18) {
      return { $$typeof: y4, _payload: { _status: -1, _result: a18 }, _init: T3 };
    };
    exports.memo = function(a18, b4) {
      return { $$typeof: x3, type: a18, compare: void 0 === b4 ? null : b4 };
    };
    exports.startTransition = function(a18) {
      var b4 = V3.transition;
      V3.transition = {};
      try {
        a18();
      } finally {
        V3.transition = b4;
      }
    };
    exports.unstable_act = X4;
    exports.useCallback = function(a18, b4) {
      return U3.current.useCallback(a18, b4);
    };
    exports.useContext = function(a18) {
      return U3.current.useContext(a18);
    };
    exports.useDebugValue = function() {
    };
    exports.useDeferredValue = function(a18) {
      return U3.current.useDeferredValue(a18);
    };
    exports.useEffect = function(a18, b4) {
      return U3.current.useEffect(a18, b4);
    };
    exports.useId = function() {
      return U3.current.useId();
    };
    exports.useImperativeHandle = function(a18, b4, e14) {
      return U3.current.useImperativeHandle(a18, b4, e14);
    };
    exports.useInsertionEffect = function(a18, b4) {
      return U3.current.useInsertionEffect(a18, b4);
    };
    exports.useLayoutEffect = function(a18, b4) {
      return U3.current.useLayoutEffect(a18, b4);
    };
    exports.useMemo = function(a18, b4) {
      return U3.current.useMemo(a18, b4);
    };
    exports.useReducer = function(a18, b4, e14) {
      return U3.current.useReducer(a18, b4, e14);
    };
    exports.useRef = function(a18) {
      return U3.current.useRef(a18);
    };
    exports.useState = function(a18) {
      return U3.current.useState(a18);
    };
    exports.useSyncExternalStore = function(a18, b4, e14) {
      return U3.current.useSyncExternalStore(a18, b4, e14);
    };
    exports.useTransition = function() {
      return U3.current.useTransition();
    };
    exports.version = "18.3.1";
  }
});

// node_modules/react/index.js
var require_react = __commonJS({
  "node_modules/react/index.js"(exports, module) {
    "use strict";
    if (true) {
      module.exports = require_react_production_min();
    } else {
      module.exports = null;
    }
  }
});

// node_modules/vike-react/dist/integration/Loading.js
function LoadingComponent() {
  return import_react.default.createElement("div", { style: {
    width: "100%",
    height: "100%",
    maxHeight: "100%",
    background: "linear-gradient(110deg, #ececec 8%, #f5f5f5 18%, #ececec 33%)",
    borderRadius: "5px",
    backgroundSize: "200% 100%",
    animation: "1.3s vike-react-shine linear infinite",
    aspectRatio: "2.5/1"
  } });
}
var import_react, Loading_default;
var init_Loading = __esm({
  "node_modules/vike-react/dist/integration/Loading.js"() {
    import_react = __toESM(require_react(), 1);
    Loading_default = {
      component: LoadingComponent
    };
  }
});

// node_modules/react-dom/cjs/react-dom-server-legacy.node.production.min.js
var require_react_dom_server_legacy_node_production_min = __commonJS({
  "node_modules/react-dom/cjs/react-dom-server-legacy.node.production.min.js"(exports) {
    "use strict";
    var ea = require_react();
    var fa = __require("stream");
    var n13 = Object.prototype.hasOwnProperty;
    var ha = /^[:A-Z_a-z\u00C0-\u00D6\u00D8-\u00F6\u00F8-\u02FF\u0370-\u037D\u037F-\u1FFF\u200C-\u200D\u2070-\u218F\u2C00-\u2FEF\u3001-\uD7FF\uF900-\uFDCF\uFDF0-\uFFFD][:A-Z_a-z\u00C0-\u00D6\u00D8-\u00F6\u00F8-\u02FF\u0370-\u037D\u037F-\u1FFF\u200C-\u200D\u2070-\u218F\u2C00-\u2FEF\u3001-\uD7FF\uF900-\uFDCF\uFDF0-\uFFFD\-.0-9\u00B7\u0300-\u036F\u203F-\u2040]*$/;
    var ia = {};
    var ja = {};
    function ka(a18) {
      if (n13.call(ja, a18)) return true;
      if (n13.call(ia, a18)) return false;
      if (ha.test(a18)) return ja[a18] = true;
      ia[a18] = true;
      return false;
    }
    function q2(a18, b4, c15, d12, f15, e14, g7) {
      this.acceptsBooleans = 2 === b4 || 3 === b4 || 4 === b4;
      this.attributeName = d12;
      this.attributeNamespace = f15;
      this.mustUseProperty = c15;
      this.propertyName = a18;
      this.type = b4;
      this.sanitizeURL = e14;
      this.removeEmptyString = g7;
    }
    var r7 = {};
    "children dangerouslySetInnerHTML defaultValue defaultChecked innerHTML suppressContentEditableWarning suppressHydrationWarning style".split(" ").forEach(function(a18) {
      r7[a18] = new q2(a18, 0, false, a18, null, false, false);
    });
    [["acceptCharset", "accept-charset"], ["className", "class"], ["htmlFor", "for"], ["httpEquiv", "http-equiv"]].forEach(function(a18) {
      var b4 = a18[0];
      r7[b4] = new q2(b4, 1, false, a18[1], null, false, false);
    });
    ["contentEditable", "draggable", "spellCheck", "value"].forEach(function(a18) {
      r7[a18] = new q2(a18, 2, false, a18.toLowerCase(), null, false, false);
    });
    ["autoReverse", "externalResourcesRequired", "focusable", "preserveAlpha"].forEach(function(a18) {
      r7[a18] = new q2(a18, 2, false, a18, null, false, false);
    });
    "allowFullScreen async autoFocus autoPlay controls default defer disabled disablePictureInPicture disableRemotePlayback formNoValidate hidden loop noModule noValidate open playsInline readOnly required reversed scoped seamless itemScope".split(" ").forEach(function(a18) {
      r7[a18] = new q2(a18, 3, false, a18.toLowerCase(), null, false, false);
    });
    ["checked", "multiple", "muted", "selected"].forEach(function(a18) {
      r7[a18] = new q2(a18, 3, true, a18, null, false, false);
    });
    ["capture", "download"].forEach(function(a18) {
      r7[a18] = new q2(a18, 4, false, a18, null, false, false);
    });
    ["cols", "rows", "size", "span"].forEach(function(a18) {
      r7[a18] = new q2(a18, 6, false, a18, null, false, false);
    });
    ["rowSpan", "start"].forEach(function(a18) {
      r7[a18] = new q2(a18, 5, false, a18.toLowerCase(), null, false, false);
    });
    var la = /[\-:]([a-z])/g;
    function ma(a18) {
      return a18[1].toUpperCase();
    }
    "accent-height alignment-baseline arabic-form baseline-shift cap-height clip-path clip-rule color-interpolation color-interpolation-filters color-profile color-rendering dominant-baseline enable-background fill-opacity fill-rule flood-color flood-opacity font-family font-size font-size-adjust font-stretch font-style font-variant font-weight glyph-name glyph-orientation-horizontal glyph-orientation-vertical horiz-adv-x horiz-origin-x image-rendering letter-spacing lighting-color marker-end marker-mid marker-start overline-position overline-thickness paint-order panose-1 pointer-events rendering-intent shape-rendering stop-color stop-opacity strikethrough-position strikethrough-thickness stroke-dasharray stroke-dashoffset stroke-linecap stroke-linejoin stroke-miterlimit stroke-opacity stroke-width text-anchor text-decoration text-rendering underline-position underline-thickness unicode-bidi unicode-range units-per-em v-alphabetic v-hanging v-ideographic v-mathematical vector-effect vert-adv-y vert-origin-x vert-origin-y word-spacing writing-mode xmlns:xlink x-height".split(" ").forEach(function(a18) {
      var b4 = a18.replace(
        la,
        ma
      );
      r7[b4] = new q2(b4, 1, false, a18, null, false, false);
    });
    "xlink:actuate xlink:arcrole xlink:role xlink:show xlink:title xlink:type".split(" ").forEach(function(a18) {
      var b4 = a18.replace(la, ma);
      r7[b4] = new q2(b4, 1, false, a18, "http://www.w3.org/1999/xlink", false, false);
    });
    ["xml:base", "xml:lang", "xml:space"].forEach(function(a18) {
      var b4 = a18.replace(la, ma);
      r7[b4] = new q2(b4, 1, false, a18, "http://www.w3.org/XML/1998/namespace", false, false);
    });
    ["tabIndex", "crossOrigin"].forEach(function(a18) {
      r7[a18] = new q2(a18, 1, false, a18.toLowerCase(), null, false, false);
    });
    r7.xlinkHref = new q2("xlinkHref", 1, false, "xlink:href", "http://www.w3.org/1999/xlink", true, false);
    ["src", "href", "action", "formAction"].forEach(function(a18) {
      r7[a18] = new q2(a18, 1, false, a18.toLowerCase(), null, true, true);
    });
    var t20 = {
      animationIterationCount: true,
      aspectRatio: true,
      borderImageOutset: true,
      borderImageSlice: true,
      borderImageWidth: true,
      boxFlex: true,
      boxFlexGroup: true,
      boxOrdinalGroup: true,
      columnCount: true,
      columns: true,
      flex: true,
      flexGrow: true,
      flexPositive: true,
      flexShrink: true,
      flexNegative: true,
      flexOrder: true,
      gridArea: true,
      gridRow: true,
      gridRowEnd: true,
      gridRowSpan: true,
      gridRowStart: true,
      gridColumn: true,
      gridColumnEnd: true,
      gridColumnSpan: true,
      gridColumnStart: true,
      fontWeight: true,
      lineClamp: true,
      lineHeight: true,
      opacity: true,
      order: true,
      orphans: true,
      tabSize: true,
      widows: true,
      zIndex: true,
      zoom: true,
      fillOpacity: true,
      floodOpacity: true,
      stopOpacity: true,
      strokeDasharray: true,
      strokeDashoffset: true,
      strokeMiterlimit: true,
      strokeOpacity: true,
      strokeWidth: true
    };
    var na = ["Webkit", "ms", "Moz", "O"];
    Object.keys(t20).forEach(function(a18) {
      na.forEach(function(b4) {
        b4 = b4 + a18.charAt(0).toUpperCase() + a18.substring(1);
        t20[b4] = t20[a18];
      });
    });
    var oa = /["'&<>]/;
    function u4(a18) {
      if ("boolean" === typeof a18 || "number" === typeof a18) return "" + a18;
      a18 = "" + a18;
      var b4 = oa.exec(a18);
      if (b4) {
        var c15 = "", d12, f15 = 0;
        for (d12 = b4.index; d12 < a18.length; d12++) {
          switch (a18.charCodeAt(d12)) {
            case 34:
              b4 = "&quot;";
              break;
            case 38:
              b4 = "&amp;";
              break;
            case 39:
              b4 = "&#x27;";
              break;
            case 60:
              b4 = "&lt;";
              break;
            case 62:
              b4 = "&gt;";
              break;
            default:
              continue;
          }
          f15 !== d12 && (c15 += a18.substring(f15, d12));
          f15 = d12 + 1;
          c15 += b4;
        }
        a18 = f15 !== d12 ? c15 + a18.substring(f15, d12) : c15;
      }
      return a18;
    }
    var pa = /([A-Z])/g;
    var qa = /^ms-/;
    var ra = Array.isArray;
    function v5(a18, b4) {
      return { insertionMode: a18, selectedValue: b4 };
    }
    function sa(a18, b4, c15) {
      switch (b4) {
        case "select":
          return v5(1, null != c15.value ? c15.value : c15.defaultValue);
        case "svg":
          return v5(2, null);
        case "math":
          return v5(3, null);
        case "foreignObject":
          return v5(1, null);
        case "table":
          return v5(4, null);
        case "thead":
        case "tbody":
        case "tfoot":
          return v5(5, null);
        case "colgroup":
          return v5(7, null);
        case "tr":
          return v5(6, null);
      }
      return 4 <= a18.insertionMode || 0 === a18.insertionMode ? v5(1, null) : a18;
    }
    var ta = /* @__PURE__ */ new Map();
    function ua(a18, b4, c15) {
      if ("object" !== typeof c15) throw Error("The `style` prop expects a mapping from style properties to values, not a string. For example, style={{marginRight: spacing + 'em'}} when using JSX.");
      b4 = true;
      for (var d12 in c15) if (n13.call(c15, d12)) {
        var f15 = c15[d12];
        if (null != f15 && "boolean" !== typeof f15 && "" !== f15) {
          if (0 === d12.indexOf("--")) {
            var e14 = u4(d12);
            f15 = u4(("" + f15).trim());
          } else {
            e14 = d12;
            var g7 = ta.get(e14);
            void 0 !== g7 ? e14 = g7 : (g7 = u4(e14.replace(pa, "-$1").toLowerCase().replace(qa, "-ms-")), ta.set(e14, g7), e14 = g7);
            f15 = "number" === typeof f15 ? 0 === f15 || n13.call(
              t20,
              d12
            ) ? "" + f15 : f15 + "px" : u4(("" + f15).trim());
          }
          b4 ? (b4 = false, a18.push(' style="', e14, ":", f15)) : a18.push(";", e14, ":", f15);
        }
      }
      b4 || a18.push('"');
    }
    function w11(a18, b4, c15, d12) {
      switch (c15) {
        case "style":
          ua(a18, b4, d12);
          return;
        case "defaultValue":
        case "defaultChecked":
        case "innerHTML":
        case "suppressContentEditableWarning":
        case "suppressHydrationWarning":
          return;
      }
      if (!(2 < c15.length) || "o" !== c15[0] && "O" !== c15[0] || "n" !== c15[1] && "N" !== c15[1]) {
        if (b4 = r7.hasOwnProperty(c15) ? r7[c15] : null, null !== b4) {
          switch (typeof d12) {
            case "function":
            case "symbol":
              return;
            case "boolean":
              if (!b4.acceptsBooleans) return;
          }
          c15 = b4.attributeName;
          switch (b4.type) {
            case 3:
              d12 && a18.push(" ", c15, '=""');
              break;
            case 4:
              true === d12 ? a18.push(" ", c15, '=""') : false !== d12 && a18.push(" ", c15, '="', u4(d12), '"');
              break;
            case 5:
              isNaN(d12) || a18.push(" ", c15, '="', u4(d12), '"');
              break;
            case 6:
              !isNaN(d12) && 1 <= d12 && a18.push(" ", c15, '="', u4(d12), '"');
              break;
            default:
              b4.sanitizeURL && (d12 = "" + d12), a18.push(" ", c15, '="', u4(d12), '"');
          }
        } else if (ka(c15)) {
          switch (typeof d12) {
            case "function":
            case "symbol":
              return;
            case "boolean":
              if (b4 = c15.toLowerCase().slice(0, 5), "data-" !== b4 && "aria-" !== b4) return;
          }
          a18.push(" ", c15, '="', u4(d12), '"');
        }
      }
    }
    function x3(a18, b4, c15) {
      if (null != b4) {
        if (null != c15) throw Error("Can only set one of `children` or `props.dangerouslySetInnerHTML`.");
        if ("object" !== typeof b4 || !("__html" in b4)) throw Error("`props.dangerouslySetInnerHTML` must be in the form `{__html: ...}`. Please visit https://reactjs.org/link/dangerously-set-inner-html for more information.");
        b4 = b4.__html;
        null !== b4 && void 0 !== b4 && a18.push("" + b4);
      }
    }
    function va(a18) {
      var b4 = "";
      ea.Children.forEach(a18, function(a19) {
        null != a19 && (b4 += a19);
      });
      return b4;
    }
    function wa(a18, b4, c15, d12) {
      a18.push(z2(c15));
      var f15 = c15 = null, e14;
      for (e14 in b4) if (n13.call(b4, e14)) {
        var g7 = b4[e14];
        if (null != g7) switch (e14) {
          case "children":
            c15 = g7;
            break;
          case "dangerouslySetInnerHTML":
            f15 = g7;
            break;
          default:
            w11(a18, d12, e14, g7);
        }
      }
      a18.push(">");
      x3(a18, f15, c15);
      return "string" === typeof c15 ? (a18.push(u4(c15)), null) : c15;
    }
    var xa = /^[a-zA-Z][a-zA-Z:_\.\-\d]*$/;
    var ya = /* @__PURE__ */ new Map();
    function z2(a18) {
      var b4 = ya.get(a18);
      if (void 0 === b4) {
        if (!xa.test(a18)) throw Error("Invalid tag: " + a18);
        b4 = "<" + a18;
        ya.set(a18, b4);
      }
      return b4;
    }
    function za(a18, b4, c15, d12, f15) {
      switch (b4) {
        case "select":
          a18.push(z2("select"));
          var e14 = null, g7 = null;
          for (l12 in c15) if (n13.call(c15, l12)) {
            var h6 = c15[l12];
            if (null != h6) switch (l12) {
              case "children":
                e14 = h6;
                break;
              case "dangerouslySetInnerHTML":
                g7 = h6;
                break;
              case "defaultValue":
              case "value":
                break;
              default:
                w11(a18, d12, l12, h6);
            }
          }
          a18.push(">");
          x3(a18, g7, e14);
          return e14;
        case "option":
          g7 = f15.selectedValue;
          a18.push(z2("option"));
          var k5 = h6 = null, m12 = null;
          var l12 = null;
          for (e14 in c15) if (n13.call(c15, e14)) {
            var p11 = c15[e14];
            if (null != p11) switch (e14) {
              case "children":
                h6 = p11;
                break;
              case "selected":
                m12 = p11;
                break;
              case "dangerouslySetInnerHTML":
                l12 = p11;
                break;
              case "value":
                k5 = p11;
              default:
                w11(a18, d12, e14, p11);
            }
          }
          if (null != g7) if (c15 = null !== k5 ? "" + k5 : va(h6), ra(g7)) for (d12 = 0; d12 < g7.length; d12++) {
            if ("" + g7[d12] === c15) {
              a18.push(' selected=""');
              break;
            }
          }
          else "" + g7 === c15 && a18.push(' selected=""');
          else m12 && a18.push(' selected=""');
          a18.push(">");
          x3(a18, l12, h6);
          return h6;
        case "textarea":
          a18.push(z2("textarea"));
          l12 = g7 = e14 = null;
          for (h6 in c15) if (n13.call(c15, h6) && (k5 = c15[h6], null != k5)) switch (h6) {
            case "children":
              l12 = k5;
              break;
            case "value":
              e14 = k5;
              break;
            case "defaultValue":
              g7 = k5;
              break;
            case "dangerouslySetInnerHTML":
              throw Error("`dangerouslySetInnerHTML` does not make sense on <textarea>.");
            default:
              w11(a18, d12, h6, k5);
          }
          null === e14 && null !== g7 && (e14 = g7);
          a18.push(">");
          if (null != l12) {
            if (null != e14) throw Error("If you supply `defaultValue` on a <textarea>, do not pass children.");
            if (ra(l12) && 1 < l12.length) throw Error("<textarea> can only have at most one child.");
            e14 = "" + l12;
          }
          "string" === typeof e14 && "\n" === e14[0] && a18.push("\n");
          null !== e14 && a18.push(u4("" + e14));
          return null;
        case "input":
          a18.push(z2("input"));
          k5 = l12 = h6 = e14 = null;
          for (g7 in c15) if (n13.call(c15, g7) && (m12 = c15[g7], null != m12)) switch (g7) {
            case "children":
            case "dangerouslySetInnerHTML":
              throw Error("input is a self-closing tag and must neither have `children` nor use `dangerouslySetInnerHTML`.");
            case "defaultChecked":
              k5 = m12;
              break;
            case "defaultValue":
              h6 = m12;
              break;
            case "checked":
              l12 = m12;
              break;
            case "value":
              e14 = m12;
              break;
            default:
              w11(a18, d12, g7, m12);
          }
          null !== l12 ? w11(a18, d12, "checked", l12) : null !== k5 && w11(a18, d12, "checked", k5);
          null !== e14 ? w11(a18, d12, "value", e14) : null !== h6 && w11(a18, d12, "value", h6);
          a18.push("/>");
          return null;
        case "menuitem":
          a18.push(z2("menuitem"));
          for (var B4 in c15) if (n13.call(c15, B4) && (e14 = c15[B4], null != e14)) switch (B4) {
            case "children":
            case "dangerouslySetInnerHTML":
              throw Error("menuitems cannot have `children` nor `dangerouslySetInnerHTML`.");
            default:
              w11(
                a18,
                d12,
                B4,
                e14
              );
          }
          a18.push(">");
          return null;
        case "title":
          a18.push(z2("title"));
          e14 = null;
          for (p11 in c15) if (n13.call(c15, p11) && (g7 = c15[p11], null != g7)) switch (p11) {
            case "children":
              e14 = g7;
              break;
            case "dangerouslySetInnerHTML":
              throw Error("`dangerouslySetInnerHTML` does not make sense on <title>.");
            default:
              w11(a18, d12, p11, g7);
          }
          a18.push(">");
          return e14;
        case "listing":
        case "pre":
          a18.push(z2(b4));
          g7 = e14 = null;
          for (k5 in c15) if (n13.call(c15, k5) && (h6 = c15[k5], null != h6)) switch (k5) {
            case "children":
              e14 = h6;
              break;
            case "dangerouslySetInnerHTML":
              g7 = h6;
              break;
            default:
              w11(a18, d12, k5, h6);
          }
          a18.push(">");
          if (null != g7) {
            if (null != e14) throw Error("Can only set one of `children` or `props.dangerouslySetInnerHTML`.");
            if ("object" !== typeof g7 || !("__html" in g7)) throw Error("`props.dangerouslySetInnerHTML` must be in the form `{__html: ...}`. Please visit https://reactjs.org/link/dangerously-set-inner-html for more information.");
            c15 = g7.__html;
            null !== c15 && void 0 !== c15 && ("string" === typeof c15 && 0 < c15.length && "\n" === c15[0] ? a18.push("\n", c15) : a18.push("" + c15));
          }
          "string" === typeof e14 && "\n" === e14[0] && a18.push("\n");
          return e14;
        case "area":
        case "base":
        case "br":
        case "col":
        case "embed":
        case "hr":
        case "img":
        case "keygen":
        case "link":
        case "meta":
        case "param":
        case "source":
        case "track":
        case "wbr":
          a18.push(z2(b4));
          for (var C5 in c15) if (n13.call(c15, C5) && (e14 = c15[C5], null != e14)) switch (C5) {
            case "children":
            case "dangerouslySetInnerHTML":
              throw Error(b4 + " is a self-closing tag and must neither have `children` nor use `dangerouslySetInnerHTML`.");
            default:
              w11(a18, d12, C5, e14);
          }
          a18.push("/>");
          return null;
        case "annotation-xml":
        case "color-profile":
        case "font-face":
        case "font-face-src":
        case "font-face-uri":
        case "font-face-format":
        case "font-face-name":
        case "missing-glyph":
          return wa(a18, c15, b4, d12);
        case "html":
          return 0 === f15.insertionMode && a18.push("<!DOCTYPE html>"), wa(a18, c15, b4, d12);
        default:
          if (-1 === b4.indexOf("-") && "string" !== typeof c15.is) return wa(a18, c15, b4, d12);
          a18.push(z2(b4));
          g7 = e14 = null;
          for (m12 in c15) if (n13.call(c15, m12) && (h6 = c15[m12], null != h6)) switch (m12) {
            case "children":
              e14 = h6;
              break;
            case "dangerouslySetInnerHTML":
              g7 = h6;
              break;
            case "style":
              ua(a18, d12, h6);
              break;
            case "suppressContentEditableWarning":
            case "suppressHydrationWarning":
              break;
            default:
              ka(m12) && "function" !== typeof h6 && "symbol" !== typeof h6 && a18.push(" ", m12, '="', u4(h6), '"');
          }
          a18.push(">");
          x3(a18, g7, e14);
          return e14;
      }
    }
    function Aa(a18, b4, c15) {
      a18.push('<!--$?--><template id="');
      if (null === c15) throw Error("An ID must have been assigned before we can complete the boundary.");
      a18.push(c15);
      return a18.push('"></template>');
    }
    function Ba(a18, b4, c15, d12) {
      switch (c15.insertionMode) {
        case 0:
        case 1:
          return a18.push('<div hidden id="'), a18.push(b4.segmentPrefix), b4 = d12.toString(16), a18.push(b4), a18.push('">');
        case 2:
          return a18.push('<svg aria-hidden="true" style="display:none" id="'), a18.push(b4.segmentPrefix), b4 = d12.toString(16), a18.push(b4), a18.push('">');
        case 3:
          return a18.push('<math aria-hidden="true" style="display:none" id="'), a18.push(b4.segmentPrefix), b4 = d12.toString(16), a18.push(b4), a18.push('">');
        case 4:
          return a18.push('<table hidden id="'), a18.push(b4.segmentPrefix), b4 = d12.toString(16), a18.push(b4), a18.push('">');
        case 5:
          return a18.push('<table hidden><tbody id="'), a18.push(b4.segmentPrefix), b4 = d12.toString(16), a18.push(b4), a18.push('">');
        case 6:
          return a18.push('<table hidden><tr id="'), a18.push(b4.segmentPrefix), b4 = d12.toString(16), a18.push(b4), a18.push('">');
        case 7:
          return a18.push('<table hidden><colgroup id="'), a18.push(b4.segmentPrefix), b4 = d12.toString(16), a18.push(b4), a18.push('">');
        default:
          throw Error("Unknown insertion mode. This is a bug in React.");
      }
    }
    function Ca(a18, b4) {
      switch (b4.insertionMode) {
        case 0:
        case 1:
          return a18.push("</div>");
        case 2:
          return a18.push("</svg>");
        case 3:
          return a18.push("</math>");
        case 4:
          return a18.push("</table>");
        case 5:
          return a18.push("</tbody></table>");
        case 6:
          return a18.push("</tr></table>");
        case 7:
          return a18.push("</colgroup></table>");
        default:
          throw Error("Unknown insertion mode. This is a bug in React.");
      }
    }
    var Da = /[<\u2028\u2029]/g;
    function Ea(a18) {
      return JSON.stringify(a18).replace(Da, function(a19) {
        switch (a19) {
          case "<":
            return "\\u003c";
          case "\u2028":
            return "\\u2028";
          case "\u2029":
            return "\\u2029";
          default:
            throw Error("escapeJSStringsForInstructionScripts encountered a match it does not know how to replace. this means the match regex and the replacement characters are no longer in sync. This is a bug in React");
        }
      });
    }
    function Fa(a18, b4) {
      b4 = void 0 === b4 ? "" : b4;
      return { bootstrapChunks: [], startInlineScript: "<script>", placeholderPrefix: b4 + "P:", segmentPrefix: b4 + "S:", boundaryPrefix: b4 + "B:", idPrefix: b4, nextSuspenseID: 0, sentCompleteSegmentFunction: false, sentCompleteBoundaryFunction: false, sentClientRenderFunction: false, generateStaticMarkup: a18 };
    }
    function Ga() {
      return { insertionMode: 1, selectedValue: null };
    }
    function Ha(a18, b4, c15, d12) {
      if (c15.generateStaticMarkup) return a18.push(u4(b4)), false;
      "" === b4 ? a18 = d12 : (d12 && a18.push("<!-- -->"), a18.push(u4(b4)), a18 = true);
      return a18;
    }
    var A4 = Object.assign;
    var Ia = Symbol.for("react.element");
    var Ja = Symbol.for("react.portal");
    var Ka = Symbol.for("react.fragment");
    var La = Symbol.for("react.strict_mode");
    var Ma = Symbol.for("react.profiler");
    var Na = Symbol.for("react.provider");
    var Oa = Symbol.for("react.context");
    var Pa = Symbol.for("react.forward_ref");
    var Qa = Symbol.for("react.suspense");
    var Ra = Symbol.for("react.suspense_list");
    var Sa = Symbol.for("react.memo");
    var Ta = Symbol.for("react.lazy");
    var Ua = Symbol.for("react.scope");
    var Va = Symbol.for("react.debug_trace_mode");
    var Wa = Symbol.for("react.legacy_hidden");
    var Xa = Symbol.for("react.default_value");
    var Ya = Symbol.iterator;
    function Za(a18) {
      if (null == a18) return null;
      if ("function" === typeof a18) return a18.displayName || a18.name || null;
      if ("string" === typeof a18) return a18;
      switch (a18) {
        case Ka:
          return "Fragment";
        case Ja:
          return "Portal";
        case Ma:
          return "Profiler";
        case La:
          return "StrictMode";
        case Qa:
          return "Suspense";
        case Ra:
          return "SuspenseList";
      }
      if ("object" === typeof a18) switch (a18.$$typeof) {
        case Oa:
          return (a18.displayName || "Context") + ".Consumer";
        case Na:
          return (a18._context.displayName || "Context") + ".Provider";
        case Pa:
          var b4 = a18.render;
          a18 = a18.displayName;
          a18 || (a18 = b4.displayName || b4.name || "", a18 = "" !== a18 ? "ForwardRef(" + a18 + ")" : "ForwardRef");
          return a18;
        case Sa:
          return b4 = a18.displayName || null, null !== b4 ? b4 : Za(a18.type) || "Memo";
        case Ta:
          b4 = a18._payload;
          a18 = a18._init;
          try {
            return Za(a18(b4));
          } catch (c15) {
          }
      }
      return null;
    }
    var $a = {};
    function ab(a18, b4) {
      a18 = a18.contextTypes;
      if (!a18) return $a;
      var c15 = {}, d12;
      for (d12 in a18) c15[d12] = b4[d12];
      return c15;
    }
    var D5 = null;
    function E6(a18, b4) {
      if (a18 !== b4) {
        a18.context._currentValue2 = a18.parentValue;
        a18 = a18.parent;
        var c15 = b4.parent;
        if (null === a18) {
          if (null !== c15) throw Error("The stacks must reach the root at the same time. This is a bug in React.");
        } else {
          if (null === c15) throw Error("The stacks must reach the root at the same time. This is a bug in React.");
          E6(a18, c15);
        }
        b4.context._currentValue2 = b4.value;
      }
    }
    function bb(a18) {
      a18.context._currentValue2 = a18.parentValue;
      a18 = a18.parent;
      null !== a18 && bb(a18);
    }
    function cb(a18) {
      var b4 = a18.parent;
      null !== b4 && cb(b4);
      a18.context._currentValue2 = a18.value;
    }
    function db(a18, b4) {
      a18.context._currentValue2 = a18.parentValue;
      a18 = a18.parent;
      if (null === a18) throw Error("The depth must equal at least at zero before reaching the root. This is a bug in React.");
      a18.depth === b4.depth ? E6(a18, b4) : db(a18, b4);
    }
    function eb(a18, b4) {
      var c15 = b4.parent;
      if (null === c15) throw Error("The depth must equal at least at zero before reaching the root. This is a bug in React.");
      a18.depth === c15.depth ? E6(a18, c15) : eb(a18, c15);
      b4.context._currentValue2 = b4.value;
    }
    function F4(a18) {
      var b4 = D5;
      b4 !== a18 && (null === b4 ? cb(a18) : null === a18 ? bb(b4) : b4.depth === a18.depth ? E6(b4, a18) : b4.depth > a18.depth ? db(b4, a18) : eb(b4, a18), D5 = a18);
    }
    var fb = { isMounted: function() {
      return false;
    }, enqueueSetState: function(a18, b4) {
      a18 = a18._reactInternals;
      null !== a18.queue && a18.queue.push(b4);
    }, enqueueReplaceState: function(a18, b4) {
      a18 = a18._reactInternals;
      a18.replace = true;
      a18.queue = [b4];
    }, enqueueForceUpdate: function() {
    } };
    function gb(a18, b4, c15, d12) {
      var f15 = void 0 !== a18.state ? a18.state : null;
      a18.updater = fb;
      a18.props = c15;
      a18.state = f15;
      var e14 = { queue: [], replace: false };
      a18._reactInternals = e14;
      var g7 = b4.contextType;
      a18.context = "object" === typeof g7 && null !== g7 ? g7._currentValue2 : d12;
      g7 = b4.getDerivedStateFromProps;
      "function" === typeof g7 && (g7 = g7(c15, f15), f15 = null === g7 || void 0 === g7 ? f15 : A4({}, f15, g7), a18.state = f15);
      if ("function" !== typeof b4.getDerivedStateFromProps && "function" !== typeof a18.getSnapshotBeforeUpdate && ("function" === typeof a18.UNSAFE_componentWillMount || "function" === typeof a18.componentWillMount)) if (b4 = a18.state, "function" === typeof a18.componentWillMount && a18.componentWillMount(), "function" === typeof a18.UNSAFE_componentWillMount && a18.UNSAFE_componentWillMount(), b4 !== a18.state && fb.enqueueReplaceState(a18, a18.state, null), null !== e14.queue && 0 < e14.queue.length) if (b4 = e14.queue, g7 = e14.replace, e14.queue = null, e14.replace = false, g7 && 1 === b4.length) a18.state = b4[0];
      else {
        e14 = g7 ? b4[0] : a18.state;
        f15 = true;
        for (g7 = g7 ? 1 : 0; g7 < b4.length; g7++) {
          var h6 = b4[g7];
          h6 = "function" === typeof h6 ? h6.call(a18, e14, c15, d12) : h6;
          null != h6 && (f15 ? (f15 = false, e14 = A4({}, e14, h6)) : A4(e14, h6));
        }
        a18.state = e14;
      }
      else e14.queue = null;
    }
    var hb = { id: 1, overflow: "" };
    function ib(a18, b4, c15) {
      var d12 = a18.id;
      a18 = a18.overflow;
      var f15 = 32 - G3(d12) - 1;
      d12 &= ~(1 << f15);
      c15 += 1;
      var e14 = 32 - G3(b4) + f15;
      if (30 < e14) {
        var g7 = f15 - f15 % 5;
        e14 = (d12 & (1 << g7) - 1).toString(32);
        d12 >>= g7;
        f15 -= g7;
        return { id: 1 << 32 - G3(b4) + f15 | c15 << f15 | d12, overflow: e14 + a18 };
      }
      return { id: 1 << e14 | c15 << f15 | d12, overflow: a18 };
    }
    var G3 = Math.clz32 ? Math.clz32 : jb;
    var kb = Math.log;
    var lb = Math.LN2;
    function jb(a18) {
      a18 >>>= 0;
      return 0 === a18 ? 32 : 31 - (kb(a18) / lb | 0) | 0;
    }
    function mb(a18, b4) {
      return a18 === b4 && (0 !== a18 || 1 / a18 === 1 / b4) || a18 !== a18 && b4 !== b4;
    }
    var nb = "function" === typeof Object.is ? Object.is : mb;
    var H4 = null;
    var ob = null;
    var I6 = null;
    var J2 = null;
    var K3 = false;
    var L3 = false;
    var M6 = 0;
    var N4 = null;
    var O5 = 0;
    function P4() {
      if (null === H4) throw Error("Invalid hook call. Hooks can only be called inside of the body of a function component. This could happen for one of the following reasons:\n1. You might have mismatching versions of React and the renderer (such as React DOM)\n2. You might be breaking the Rules of Hooks\n3. You might have more than one copy of React in the same app\nSee https://reactjs.org/link/invalid-hook-call for tips about how to debug and fix this problem.");
      return H4;
    }
    function rb() {
      if (0 < O5) throw Error("Rendered more hooks than during the previous render");
      return { memoizedState: null, queue: null, next: null };
    }
    function sb() {
      null === J2 ? null === I6 ? (K3 = false, I6 = J2 = rb()) : (K3 = true, J2 = I6) : null === J2.next ? (K3 = false, J2 = J2.next = rb()) : (K3 = true, J2 = J2.next);
      return J2;
    }
    function tb() {
      ob = H4 = null;
      L3 = false;
      I6 = null;
      O5 = 0;
      J2 = N4 = null;
    }
    function ub(a18, b4) {
      return "function" === typeof b4 ? b4(a18) : b4;
    }
    function vb(a18, b4, c15) {
      H4 = P4();
      J2 = sb();
      if (K3) {
        var d12 = J2.queue;
        b4 = d12.dispatch;
        if (null !== N4 && (c15 = N4.get(d12), void 0 !== c15)) {
          N4.delete(d12);
          d12 = J2.memoizedState;
          do
            d12 = a18(d12, c15.action), c15 = c15.next;
          while (null !== c15);
          J2.memoizedState = d12;
          return [d12, b4];
        }
        return [J2.memoizedState, b4];
      }
      a18 = a18 === ub ? "function" === typeof b4 ? b4() : b4 : void 0 !== c15 ? c15(b4) : b4;
      J2.memoizedState = a18;
      a18 = J2.queue = { last: null, dispatch: null };
      a18 = a18.dispatch = wb.bind(null, H4, a18);
      return [J2.memoizedState, a18];
    }
    function xb(a18, b4) {
      H4 = P4();
      J2 = sb();
      b4 = void 0 === b4 ? null : b4;
      if (null !== J2) {
        var c15 = J2.memoizedState;
        if (null !== c15 && null !== b4) {
          var d12 = c15[1];
          a: if (null === d12) d12 = false;
          else {
            for (var f15 = 0; f15 < d12.length && f15 < b4.length; f15++) if (!nb(b4[f15], d12[f15])) {
              d12 = false;
              break a;
            }
            d12 = true;
          }
          if (d12) return c15[0];
        }
      }
      a18 = a18();
      J2.memoizedState = [a18, b4];
      return a18;
    }
    function wb(a18, b4, c15) {
      if (25 <= O5) throw Error("Too many re-renders. React limits the number of renders to prevent an infinite loop.");
      if (a18 === H4) if (L3 = true, a18 = { action: c15, next: null }, null === N4 && (N4 = /* @__PURE__ */ new Map()), c15 = N4.get(b4), void 0 === c15) N4.set(b4, a18);
      else {
        for (b4 = c15; null !== b4.next; ) b4 = b4.next;
        b4.next = a18;
      }
    }
    function yb() {
      throw Error("startTransition cannot be called during server rendering.");
    }
    function Q4() {
    }
    var zb = { readContext: function(a18) {
      return a18._currentValue2;
    }, useContext: function(a18) {
      P4();
      return a18._currentValue2;
    }, useMemo: xb, useReducer: vb, useRef: function(a18) {
      H4 = P4();
      J2 = sb();
      var b4 = J2.memoizedState;
      return null === b4 ? (a18 = { current: a18 }, J2.memoizedState = a18) : b4;
    }, useState: function(a18) {
      return vb(ub, a18);
    }, useInsertionEffect: Q4, useLayoutEffect: function() {
    }, useCallback: function(a18, b4) {
      return xb(function() {
        return a18;
      }, b4);
    }, useImperativeHandle: Q4, useEffect: Q4, useDebugValue: Q4, useDeferredValue: function(a18) {
      P4();
      return a18;
    }, useTransition: function() {
      P4();
      return [false, yb];
    }, useId: function() {
      var a18 = ob.treeContext;
      var b4 = a18.overflow;
      a18 = a18.id;
      a18 = (a18 & ~(1 << 32 - G3(a18) - 1)).toString(32) + b4;
      var c15 = R10;
      if (null === c15) throw Error("Invalid hook call. Hooks can only be called inside of the body of a function component.");
      b4 = M6++;
      a18 = ":" + c15.idPrefix + "R" + a18;
      0 < b4 && (a18 += "H" + b4.toString(32));
      return a18 + ":";
    }, useMutableSource: function(a18, b4) {
      P4();
      return b4(a18._source);
    }, useSyncExternalStore: function(a18, b4, c15) {
      if (void 0 === c15) throw Error("Missing getServerSnapshot, which is required for server-rendered content. Will revert to client rendering.");
      return c15();
    } };
    var R10 = null;
    var Ab = ea.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED.ReactCurrentDispatcher;
    function Bb(a18) {
      console.error(a18);
      return null;
    }
    function S2() {
    }
    function Cb(a18, b4, c15, d12, f15, e14, g7, h6, k5) {
      var m12 = [], l12 = /* @__PURE__ */ new Set();
      b4 = { destination: null, responseState: b4, progressiveChunkSize: void 0 === d12 ? 12800 : d12, status: 0, fatalError: null, nextSegmentId: 0, allPendingTasks: 0, pendingRootTasks: 0, completedRootSegment: null, abortableTasks: l12, pingedTasks: m12, clientRenderedBoundaries: [], completedBoundaries: [], partialBoundaries: [], onError: void 0 === f15 ? Bb : f15, onAllReady: void 0 === e14 ? S2 : e14, onShellReady: void 0 === g7 ? S2 : g7, onShellError: void 0 === h6 ? S2 : h6, onFatalError: void 0 === k5 ? S2 : k5 };
      c15 = T3(b4, 0, null, c15, false, false);
      c15.parentFlushed = true;
      a18 = Db(b4, a18, null, c15, l12, $a, null, hb);
      m12.push(a18);
      return b4;
    }
    function Db(a18, b4, c15, d12, f15, e14, g7, h6) {
      a18.allPendingTasks++;
      null === c15 ? a18.pendingRootTasks++ : c15.pendingTasks++;
      var k5 = { node: b4, ping: function() {
        var b5 = a18.pingedTasks;
        b5.push(k5);
        1 === b5.length && Eb(a18);
      }, blockedBoundary: c15, blockedSegment: d12, abortSet: f15, legacyContext: e14, context: g7, treeContext: h6 };
      f15.add(k5);
      return k5;
    }
    function T3(a18, b4, c15, d12, f15, e14) {
      return { status: 0, id: -1, index: b4, parentFlushed: false, chunks: [], children: [], formatContext: d12, boundary: c15, lastPushedText: f15, textEmbedded: e14 };
    }
    function U3(a18, b4) {
      a18 = a18.onError(b4);
      if (null != a18 && "string" !== typeof a18) throw Error('onError returned something with a type other than "string". onError should return a string and may return null or undefined but must not return anything else. It received something of type "' + typeof a18 + '" instead');
      return a18;
    }
    function V3(a18, b4) {
      var c15 = a18.onShellError;
      c15(b4);
      c15 = a18.onFatalError;
      c15(b4);
      null !== a18.destination ? (a18.status = 2, a18.destination.destroy(b4)) : (a18.status = 1, a18.fatalError = b4);
    }
    function Fb(a18, b4, c15, d12, f15) {
      H4 = {};
      ob = b4;
      M6 = 0;
      for (a18 = c15(d12, f15); L3; ) L3 = false, M6 = 0, O5 += 1, J2 = null, a18 = c15(d12, f15);
      tb();
      return a18;
    }
    function Gb(a18, b4, c15, d12) {
      var f15 = c15.render(), e14 = d12.childContextTypes;
      if (null !== e14 && void 0 !== e14) {
        var g7 = b4.legacyContext;
        if ("function" !== typeof c15.getChildContext) d12 = g7;
        else {
          c15 = c15.getChildContext();
          for (var h6 in c15) if (!(h6 in e14)) throw Error((Za(d12) || "Unknown") + '.getChildContext(): key "' + h6 + '" is not defined in childContextTypes.');
          d12 = A4({}, g7, c15);
        }
        b4.legacyContext = d12;
        W4(a18, b4, f15);
        b4.legacyContext = g7;
      } else W4(a18, b4, f15);
    }
    function Hb(a18, b4) {
      if (a18 && a18.defaultProps) {
        b4 = A4({}, b4);
        a18 = a18.defaultProps;
        for (var c15 in a18) void 0 === b4[c15] && (b4[c15] = a18[c15]);
        return b4;
      }
      return b4;
    }
    function Ib(a18, b4, c15, d12, f15) {
      if ("function" === typeof c15) if (c15.prototype && c15.prototype.isReactComponent) {
        f15 = ab(c15, b4.legacyContext);
        var e14 = c15.contextType;
        e14 = new c15(d12, "object" === typeof e14 && null !== e14 ? e14._currentValue2 : f15);
        gb(e14, c15, d12, f15);
        Gb(a18, b4, e14, c15);
      } else {
        e14 = ab(c15, b4.legacyContext);
        f15 = Fb(a18, b4, c15, d12, e14);
        var g7 = 0 !== M6;
        if ("object" === typeof f15 && null !== f15 && "function" === typeof f15.render && void 0 === f15.$$typeof) gb(f15, c15, d12, e14), Gb(a18, b4, f15, c15);
        else if (g7) {
          d12 = b4.treeContext;
          b4.treeContext = ib(d12, 1, 0);
          try {
            W4(a18, b4, f15);
          } finally {
            b4.treeContext = d12;
          }
        } else W4(a18, b4, f15);
      }
      else if ("string" === typeof c15) {
        f15 = b4.blockedSegment;
        e14 = za(f15.chunks, c15, d12, a18.responseState, f15.formatContext);
        f15.lastPushedText = false;
        g7 = f15.formatContext;
        f15.formatContext = sa(g7, c15, d12);
        Jb(a18, b4, e14);
        f15.formatContext = g7;
        switch (c15) {
          case "area":
          case "base":
          case "br":
          case "col":
          case "embed":
          case "hr":
          case "img":
          case "input":
          case "keygen":
          case "link":
          case "meta":
          case "param":
          case "source":
          case "track":
          case "wbr":
            break;
          default:
            f15.chunks.push("</", c15, ">");
        }
        f15.lastPushedText = false;
      } else {
        switch (c15) {
          case Wa:
          case Va:
          case La:
          case Ma:
          case Ka:
            W4(a18, b4, d12.children);
            return;
          case Ra:
            W4(a18, b4, d12.children);
            return;
          case Ua:
            throw Error("ReactDOMServer does not yet support scope components.");
          case Qa:
            a: {
              c15 = b4.blockedBoundary;
              f15 = b4.blockedSegment;
              e14 = d12.fallback;
              d12 = d12.children;
              g7 = /* @__PURE__ */ new Set();
              var h6 = { id: null, rootSegmentID: -1, parentFlushed: false, pendingTasks: 0, forceClientRender: false, completedSegments: [], byteSize: 0, fallbackAbortableTasks: g7, errorDigest: null }, k5 = T3(a18, f15.chunks.length, h6, f15.formatContext, false, false);
              f15.children.push(k5);
              f15.lastPushedText = false;
              var m12 = T3(a18, 0, null, f15.formatContext, false, false);
              m12.parentFlushed = true;
              b4.blockedBoundary = h6;
              b4.blockedSegment = m12;
              try {
                if (Jb(a18, b4, d12), a18.responseState.generateStaticMarkup || m12.lastPushedText && m12.textEmbedded && m12.chunks.push("<!-- -->"), m12.status = 1, X4(h6, m12), 0 === h6.pendingTasks) break a;
              } catch (l12) {
                m12.status = 4, h6.forceClientRender = true, h6.errorDigest = U3(a18, l12);
              } finally {
                b4.blockedBoundary = c15, b4.blockedSegment = f15;
              }
              b4 = Db(a18, e14, c15, k5, g7, b4.legacyContext, b4.context, b4.treeContext);
              a18.pingedTasks.push(b4);
            }
            return;
        }
        if ("object" === typeof c15 && null !== c15) switch (c15.$$typeof) {
          case Pa:
            d12 = Fb(a18, b4, c15.render, d12, f15);
            if (0 !== M6) {
              c15 = b4.treeContext;
              b4.treeContext = ib(c15, 1, 0);
              try {
                W4(a18, b4, d12);
              } finally {
                b4.treeContext = c15;
              }
            } else W4(a18, b4, d12);
            return;
          case Sa:
            c15 = c15.type;
            d12 = Hb(c15, d12);
            Ib(a18, b4, c15, d12, f15);
            return;
          case Na:
            f15 = d12.children;
            c15 = c15._context;
            d12 = d12.value;
            e14 = c15._currentValue2;
            c15._currentValue2 = d12;
            g7 = D5;
            D5 = d12 = { parent: g7, depth: null === g7 ? 0 : g7.depth + 1, context: c15, parentValue: e14, value: d12 };
            b4.context = d12;
            W4(a18, b4, f15);
            a18 = D5;
            if (null === a18) throw Error("Tried to pop a Context at the root of the app. This is a bug in React.");
            d12 = a18.parentValue;
            a18.context._currentValue2 = d12 === Xa ? a18.context._defaultValue : d12;
            a18 = D5 = a18.parent;
            b4.context = a18;
            return;
          case Oa:
            d12 = d12.children;
            d12 = d12(c15._currentValue2);
            W4(a18, b4, d12);
            return;
          case Ta:
            f15 = c15._init;
            c15 = f15(c15._payload);
            d12 = Hb(c15, d12);
            Ib(a18, b4, c15, d12, void 0);
            return;
        }
        throw Error("Element type is invalid: expected a string (for built-in components) or a class/function (for composite components) but got: " + ((null == c15 ? c15 : typeof c15) + "."));
      }
    }
    function W4(a18, b4, c15) {
      b4.node = c15;
      if ("object" === typeof c15 && null !== c15) {
        switch (c15.$$typeof) {
          case Ia:
            Ib(a18, b4, c15.type, c15.props, c15.ref);
            return;
          case Ja:
            throw Error("Portals are not currently supported by the server renderer. Render them conditionally so that they only appear on the client render.");
          case Ta:
            var d12 = c15._init;
            c15 = d12(c15._payload);
            W4(a18, b4, c15);
            return;
        }
        if (ra(c15)) {
          Kb(a18, b4, c15);
          return;
        }
        null === c15 || "object" !== typeof c15 ? d12 = null : (d12 = Ya && c15[Ya] || c15["@@iterator"], d12 = "function" === typeof d12 ? d12 : null);
        if (d12 && (d12 = d12.call(c15))) {
          c15 = d12.next();
          if (!c15.done) {
            var f15 = [];
            do
              f15.push(c15.value), c15 = d12.next();
            while (!c15.done);
            Kb(a18, b4, f15);
          }
          return;
        }
        a18 = Object.prototype.toString.call(c15);
        throw Error("Objects are not valid as a React child (found: " + ("[object Object]" === a18 ? "object with keys {" + Object.keys(c15).join(", ") + "}" : a18) + "). If you meant to render a collection of children, use an array instead.");
      }
      "string" === typeof c15 ? (d12 = b4.blockedSegment, d12.lastPushedText = Ha(b4.blockedSegment.chunks, c15, a18.responseState, d12.lastPushedText)) : "number" === typeof c15 && (d12 = b4.blockedSegment, d12.lastPushedText = Ha(
        b4.blockedSegment.chunks,
        "" + c15,
        a18.responseState,
        d12.lastPushedText
      ));
    }
    function Kb(a18, b4, c15) {
      for (var d12 = c15.length, f15 = 0; f15 < d12; f15++) {
        var e14 = b4.treeContext;
        b4.treeContext = ib(e14, d12, f15);
        try {
          Jb(a18, b4, c15[f15]);
        } finally {
          b4.treeContext = e14;
        }
      }
    }
    function Jb(a18, b4, c15) {
      var d12 = b4.blockedSegment.formatContext, f15 = b4.legacyContext, e14 = b4.context;
      try {
        return W4(a18, b4, c15);
      } catch (k5) {
        if (tb(), "object" === typeof k5 && null !== k5 && "function" === typeof k5.then) {
          c15 = k5;
          var g7 = b4.blockedSegment, h6 = T3(a18, g7.chunks.length, null, g7.formatContext, g7.lastPushedText, true);
          g7.children.push(h6);
          g7.lastPushedText = false;
          a18 = Db(a18, b4.node, b4.blockedBoundary, h6, b4.abortSet, b4.legacyContext, b4.context, b4.treeContext).ping;
          c15.then(a18, a18);
          b4.blockedSegment.formatContext = d12;
          b4.legacyContext = f15;
          b4.context = e14;
          F4(e14);
        } else throw b4.blockedSegment.formatContext = d12, b4.legacyContext = f15, b4.context = e14, F4(e14), k5;
      }
    }
    function Lb(a18) {
      var b4 = a18.blockedBoundary;
      a18 = a18.blockedSegment;
      a18.status = 3;
      Mb(this, b4, a18);
    }
    function Nb(a18, b4, c15) {
      var d12 = a18.blockedBoundary;
      a18.blockedSegment.status = 3;
      null === d12 ? (b4.allPendingTasks--, 2 !== b4.status && (b4.status = 2, null !== b4.destination && b4.destination.push(null))) : (d12.pendingTasks--, d12.forceClientRender || (d12.forceClientRender = true, d12.errorDigest = b4.onError(void 0 === c15 ? Error("The render was aborted by the server without a reason.") : c15), d12.parentFlushed && b4.clientRenderedBoundaries.push(d12)), d12.fallbackAbortableTasks.forEach(function(a19) {
        return Nb(a19, b4, c15);
      }), d12.fallbackAbortableTasks.clear(), b4.allPendingTasks--, 0 === b4.allPendingTasks && (a18 = b4.onAllReady, a18()));
    }
    function X4(a18, b4) {
      if (0 === b4.chunks.length && 1 === b4.children.length && null === b4.children[0].boundary) {
        var c15 = b4.children[0];
        c15.id = b4.id;
        c15.parentFlushed = true;
        1 === c15.status && X4(a18, c15);
      } else a18.completedSegments.push(b4);
    }
    function Mb(a18, b4, c15) {
      if (null === b4) {
        if (c15.parentFlushed) {
          if (null !== a18.completedRootSegment) throw Error("There can only be one root segment. This is a bug in React.");
          a18.completedRootSegment = c15;
        }
        a18.pendingRootTasks--;
        0 === a18.pendingRootTasks && (a18.onShellError = S2, b4 = a18.onShellReady, b4());
      } else b4.pendingTasks--, b4.forceClientRender || (0 === b4.pendingTasks ? (c15.parentFlushed && 1 === c15.status && X4(b4, c15), b4.parentFlushed && a18.completedBoundaries.push(b4), b4.fallbackAbortableTasks.forEach(Lb, a18), b4.fallbackAbortableTasks.clear()) : c15.parentFlushed && 1 === c15.status && (X4(b4, c15), 1 === b4.completedSegments.length && b4.parentFlushed && a18.partialBoundaries.push(b4)));
      a18.allPendingTasks--;
      0 === a18.allPendingTasks && (a18 = a18.onAllReady, a18());
    }
    function Eb(a18) {
      if (2 !== a18.status) {
        var b4 = D5, c15 = Ab.current;
        Ab.current = zb;
        var d12 = R10;
        R10 = a18.responseState;
        try {
          var f15 = a18.pingedTasks, e14;
          for (e14 = 0; e14 < f15.length; e14++) {
            var g7 = f15[e14];
            var h6 = a18, k5 = g7.blockedSegment;
            if (0 === k5.status) {
              F4(g7.context);
              try {
                W4(h6, g7, g7.node), h6.responseState.generateStaticMarkup || k5.lastPushedText && k5.textEmbedded && k5.chunks.push("<!-- -->"), g7.abortSet.delete(g7), k5.status = 1, Mb(h6, g7.blockedBoundary, k5);
              } catch (y4) {
                if (tb(), "object" === typeof y4 && null !== y4 && "function" === typeof y4.then) {
                  var m12 = g7.ping;
                  y4.then(m12, m12);
                } else {
                  g7.abortSet.delete(g7);
                  k5.status = 4;
                  var l12 = g7.blockedBoundary, p11 = y4, B4 = U3(h6, p11);
                  null === l12 ? V3(h6, p11) : (l12.pendingTasks--, l12.forceClientRender || (l12.forceClientRender = true, l12.errorDigest = B4, l12.parentFlushed && h6.clientRenderedBoundaries.push(l12)));
                  h6.allPendingTasks--;
                  if (0 === h6.allPendingTasks) {
                    var C5 = h6.onAllReady;
                    C5();
                  }
                }
              } finally {
              }
            }
          }
          f15.splice(0, e14);
          null !== a18.destination && Ob(a18, a18.destination);
        } catch (y4) {
          U3(a18, y4), V3(a18, y4);
        } finally {
          R10 = d12, Ab.current = c15, c15 === zb && F4(b4);
        }
      }
    }
    function Y3(a18, b4, c15) {
      c15.parentFlushed = true;
      switch (c15.status) {
        case 0:
          var d12 = c15.id = a18.nextSegmentId++;
          c15.lastPushedText = false;
          c15.textEmbedded = false;
          a18 = a18.responseState;
          b4.push('<template id="');
          b4.push(a18.placeholderPrefix);
          a18 = d12.toString(16);
          b4.push(a18);
          return b4.push('"></template>');
        case 1:
          c15.status = 2;
          var f15 = true;
          d12 = c15.chunks;
          var e14 = 0;
          c15 = c15.children;
          for (var g7 = 0; g7 < c15.length; g7++) {
            for (f15 = c15[g7]; e14 < f15.index; e14++) b4.push(d12[e14]);
            f15 = Z5(a18, b4, f15);
          }
          for (; e14 < d12.length - 1; e14++) b4.push(d12[e14]);
          e14 < d12.length && (f15 = b4.push(d12[e14]));
          return f15;
        default:
          throw Error("Aborted, errored or already flushed boundaries should not be flushed again. This is a bug in React.");
      }
    }
    function Z5(a18, b4, c15) {
      var d12 = c15.boundary;
      if (null === d12) return Y3(a18, b4, c15);
      d12.parentFlushed = true;
      if (d12.forceClientRender) return a18.responseState.generateStaticMarkup || (d12 = d12.errorDigest, b4.push("<!--$!-->"), b4.push("<template"), d12 && (b4.push(' data-dgst="'), d12 = u4(d12), b4.push(d12), b4.push('"')), b4.push("></template>")), Y3(a18, b4, c15), a18 = a18.responseState.generateStaticMarkup ? true : b4.push("<!--/$-->"), a18;
      if (0 < d12.pendingTasks) {
        d12.rootSegmentID = a18.nextSegmentId++;
        0 < d12.completedSegments.length && a18.partialBoundaries.push(d12);
        var f15 = a18.responseState;
        var e14 = f15.nextSuspenseID++;
        f15 = f15.boundaryPrefix + e14.toString(16);
        d12 = d12.id = f15;
        Aa(b4, a18.responseState, d12);
        Y3(a18, b4, c15);
        return b4.push("<!--/$-->");
      }
      if (d12.byteSize > a18.progressiveChunkSize) return d12.rootSegmentID = a18.nextSegmentId++, a18.completedBoundaries.push(d12), Aa(b4, a18.responseState, d12.id), Y3(a18, b4, c15), b4.push("<!--/$-->");
      a18.responseState.generateStaticMarkup || b4.push("<!--$-->");
      c15 = d12.completedSegments;
      if (1 !== c15.length) throw Error("A previously unvisited boundary must have exactly one root segment. This is a bug in React.");
      Z5(a18, b4, c15[0]);
      a18 = a18.responseState.generateStaticMarkup ? true : b4.push("<!--/$-->");
      return a18;
    }
    function Pb(a18, b4, c15) {
      Ba(b4, a18.responseState, c15.formatContext, c15.id);
      Z5(a18, b4, c15);
      return Ca(b4, c15.formatContext);
    }
    function Qb(a18, b4, c15) {
      for (var d12 = c15.completedSegments, f15 = 0; f15 < d12.length; f15++) Rb(a18, b4, c15, d12[f15]);
      d12.length = 0;
      a18 = a18.responseState;
      d12 = c15.id;
      c15 = c15.rootSegmentID;
      b4.push(a18.startInlineScript);
      a18.sentCompleteBoundaryFunction ? b4.push('$RC("') : (a18.sentCompleteBoundaryFunction = true, b4.push('function $RC(a,b){a=document.getElementById(a);b=document.getElementById(b);b.parentNode.removeChild(b);if(a){a=a.previousSibling;var f=a.parentNode,c=a.nextSibling,e=0;do{if(c&&8===c.nodeType){var d=c.data;if("/$"===d)if(0===e)break;else e--;else"$"!==d&&"$?"!==d&&"$!"!==d||e++}d=c.nextSibling;f.removeChild(c);c=d}while(c);for(;b.firstChild;)f.insertBefore(b.firstChild,c);a.data="$";a._reactRetry&&a._reactRetry()}};$RC("'));
      if (null === d12) throw Error("An ID must have been assigned before we can complete the boundary.");
      c15 = c15.toString(16);
      b4.push(d12);
      b4.push('","');
      b4.push(a18.segmentPrefix);
      b4.push(c15);
      return b4.push('")</script>');
    }
    function Rb(a18, b4, c15, d12) {
      if (2 === d12.status) return true;
      var f15 = d12.id;
      if (-1 === f15) {
        if (-1 === (d12.id = c15.rootSegmentID)) throw Error("A root segment ID must have been assigned by now. This is a bug in React.");
        return Pb(a18, b4, d12);
      }
      Pb(a18, b4, d12);
      a18 = a18.responseState;
      b4.push(a18.startInlineScript);
      a18.sentCompleteSegmentFunction ? b4.push('$RS("') : (a18.sentCompleteSegmentFunction = true, b4.push('function $RS(a,b){a=document.getElementById(a);b=document.getElementById(b);for(a.parentNode.removeChild(a);a.firstChild;)b.parentNode.insertBefore(a.firstChild,b);b.parentNode.removeChild(b)};$RS("'));
      b4.push(a18.segmentPrefix);
      f15 = f15.toString(16);
      b4.push(f15);
      b4.push('","');
      b4.push(a18.placeholderPrefix);
      b4.push(f15);
      return b4.push('")</script>');
    }
    function Ob(a18, b4) {
      try {
        var c15 = a18.completedRootSegment;
        if (null !== c15 && 0 === a18.pendingRootTasks) {
          Z5(a18, b4, c15);
          a18.completedRootSegment = null;
          var d12 = a18.responseState.bootstrapChunks;
          for (c15 = 0; c15 < d12.length - 1; c15++) b4.push(d12[c15]);
          c15 < d12.length && b4.push(d12[c15]);
        }
        var f15 = a18.clientRenderedBoundaries, e14;
        for (e14 = 0; e14 < f15.length; e14++) {
          var g7 = f15[e14];
          d12 = b4;
          var h6 = a18.responseState, k5 = g7.id, m12 = g7.errorDigest, l12 = g7.errorMessage, p11 = g7.errorComponentStack;
          d12.push(h6.startInlineScript);
          h6.sentClientRenderFunction ? d12.push('$RX("') : (h6.sentClientRenderFunction = true, d12.push('function $RX(b,c,d,e){var a=document.getElementById(b);a&&(b=a.previousSibling,b.data="$!",a=a.dataset,c&&(a.dgst=c),d&&(a.msg=d),e&&(a.stck=e),b._reactRetry&&b._reactRetry())};$RX("'));
          if (null === k5) throw Error("An ID must have been assigned before we can complete the boundary.");
          d12.push(k5);
          d12.push('"');
          if (m12 || l12 || p11) {
            d12.push(",");
            var B4 = Ea(m12 || "");
            d12.push(B4);
          }
          if (l12 || p11) {
            d12.push(",");
            var C5 = Ea(l12 || "");
            d12.push(C5);
          }
          if (p11) {
            d12.push(",");
            var y4 = Ea(p11);
            d12.push(y4);
          }
          if (!d12.push(")</script>")) {
            a18.destination = null;
            e14++;
            f15.splice(0, e14);
            return;
          }
        }
        f15.splice(0, e14);
        var aa = a18.completedBoundaries;
        for (e14 = 0; e14 < aa.length; e14++) if (!Qb(a18, b4, aa[e14])) {
          a18.destination = null;
          e14++;
          aa.splice(0, e14);
          return;
        }
        aa.splice(0, e14);
        var ba = a18.partialBoundaries;
        for (e14 = 0; e14 < ba.length; e14++) {
          var pb = ba[e14];
          a: {
            f15 = a18;
            g7 = b4;
            var ca = pb.completedSegments;
            for (h6 = 0; h6 < ca.length; h6++) if (!Rb(f15, g7, pb, ca[h6])) {
              h6++;
              ca.splice(0, h6);
              var qb = false;
              break a;
            }
            ca.splice(0, h6);
            qb = true;
          }
          if (!qb) {
            a18.destination = null;
            e14++;
            ba.splice(0, e14);
            return;
          }
        }
        ba.splice(0, e14);
        var da = a18.completedBoundaries;
        for (e14 = 0; e14 < da.length; e14++) if (!Qb(a18, b4, da[e14])) {
          a18.destination = null;
          e14++;
          da.splice(0, e14);
          return;
        }
        da.splice(0, e14);
      } finally {
        0 === a18.allPendingTasks && 0 === a18.pingedTasks.length && 0 === a18.clientRenderedBoundaries.length && 0 === a18.completedBoundaries.length && b4.push(null);
      }
    }
    function Sb(a18, b4) {
      if (1 === a18.status) a18.status = 2, b4.destroy(a18.fatalError);
      else if (2 !== a18.status && null === a18.destination) {
        a18.destination = b4;
        try {
          Ob(a18, b4);
        } catch (c15) {
          U3(a18, c15), V3(a18, c15);
        }
      }
    }
    function Tb(a18, b4) {
      try {
        var c15 = a18.abortableTasks;
        c15.forEach(function(c16) {
          return Nb(c16, a18, b4);
        });
        c15.clear();
        null !== a18.destination && Ob(a18, a18.destination);
      } catch (d12) {
        U3(a18, d12), V3(a18, d12);
      }
    }
    function Ub() {
    }
    function Vb(a18, b4, c15, d12) {
      var f15 = false, e14 = null, g7 = "", h6 = false;
      a18 = Cb(a18, Fa(c15, b4 ? b4.identifierPrefix : void 0), Ga(), Infinity, Ub, void 0, function() {
        h6 = true;
      }, void 0, void 0);
      Eb(a18);
      Tb(a18, d12);
      Sb(a18, { push: function(a19) {
        null !== a19 && (g7 += a19);
        return true;
      }, destroy: function(a19) {
        f15 = true;
        e14 = a19;
      } });
      if (f15) throw e14;
      if (!h6) throw Error("A component suspended while responding to synchronous input. This will cause the UI to be replaced with a loading indicator. To fix, updates that suspend should be wrapped with startTransition.");
      return g7;
    }
    function Wb(a18, b4) {
      a18.prototype = Object.create(b4.prototype);
      a18.prototype.constructor = a18;
      a18.__proto__ = b4;
    }
    var Xb = function(a18) {
      function b4() {
        var b5 = a18.call(this, {}) || this;
        b5.request = null;
        b5.startedFlowing = false;
        return b5;
      }
      Wb(b4, a18);
      var c15 = b4.prototype;
      c15._destroy = function(a19, b5) {
        Tb(this.request);
        b5(a19);
      };
      c15._read = function() {
        this.startedFlowing && Sb(this.request, this);
      };
      return b4;
    }(fa.Readable);
    function Yb() {
    }
    function Zb(a18, b4) {
      var c15 = new Xb(), d12 = Cb(a18, Fa(false, b4 ? b4.identifierPrefix : void 0), Ga(), Infinity, Yb, function() {
        c15.startedFlowing = true;
        Sb(d12, c15);
      }, void 0, void 0);
      c15.request = d12;
      Eb(d12);
      return c15;
    }
    exports.renderToNodeStream = function(a18, b4) {
      return Zb(a18, b4);
    };
    exports.renderToStaticMarkup = function(a18, b4) {
      return Vb(a18, b4, true, 'The server used "renderToStaticMarkup" which does not support Suspense. If you intended to have the server wait for the suspended component please switch to "renderToPipeableStream" which supports Suspense on the server');
    };
    exports.renderToStaticNodeStream = function(a18, b4) {
      return Zb(a18, b4);
    };
    exports.renderToString = function(a18, b4) {
      return Vb(a18, b4, false, 'The server used "renderToString" which does not support Suspense. If you intended for this Suspense boundary to render the fallback content on the server consider throwing an Error somewhere within the Suspense boundary. If you intended to have the server wait for the suspended component please switch to "renderToPipeableStream" which supports Suspense on the server');
    };
    exports.version = "18.3.1";
  }
});

// node_modules/react-dom/cjs/react-dom-server.node.production.min.js
var require_react_dom_server_node_production_min = __commonJS({
  "node_modules/react-dom/cjs/react-dom-server.node.production.min.js"(exports) {
    "use strict";
    var aa = __require("util");
    var ba = require_react();
    var k5 = null;
    var l12 = 0;
    var q2 = true;
    function r7(a18, b4) {
      if ("string" === typeof b4) {
        if (0 !== b4.length) if (2048 < 3 * b4.length) 0 < l12 && (t20(a18, k5.subarray(0, l12)), k5 = new Uint8Array(2048), l12 = 0), t20(a18, u4.encode(b4));
        else {
          var c15 = k5;
          0 < l12 && (c15 = k5.subarray(l12));
          c15 = u4.encodeInto(b4, c15);
          var d12 = c15.read;
          l12 += c15.written;
          d12 < b4.length && (t20(a18, k5), k5 = new Uint8Array(2048), l12 = u4.encodeInto(b4.slice(d12), k5).written);
          2048 === l12 && (t20(a18, k5), k5 = new Uint8Array(2048), l12 = 0);
        }
      } else 0 !== b4.byteLength && (2048 < b4.byteLength ? (0 < l12 && (t20(a18, k5.subarray(0, l12)), k5 = new Uint8Array(2048), l12 = 0), t20(a18, b4)) : (c15 = k5.length - l12, c15 < b4.byteLength && (0 === c15 ? t20(
        a18,
        k5
      ) : (k5.set(b4.subarray(0, c15), l12), l12 += c15, t20(a18, k5), b4 = b4.subarray(c15)), k5 = new Uint8Array(2048), l12 = 0), k5.set(b4, l12), l12 += b4.byteLength, 2048 === l12 && (t20(a18, k5), k5 = new Uint8Array(2048), l12 = 0)));
    }
    function t20(a18, b4) {
      a18 = a18.write(b4);
      q2 = q2 && a18;
    }
    function w11(a18, b4) {
      r7(a18, b4);
      return q2;
    }
    function ca(a18) {
      k5 && 0 < l12 && a18.write(k5.subarray(0, l12));
      k5 = null;
      l12 = 0;
      q2 = true;
    }
    var u4 = new aa.TextEncoder();
    function x3(a18) {
      return u4.encode(a18);
    }
    var y4 = Object.prototype.hasOwnProperty;
    var da = /^[:A-Z_a-z\u00C0-\u00D6\u00D8-\u00F6\u00F8-\u02FF\u0370-\u037D\u037F-\u1FFF\u200C-\u200D\u2070-\u218F\u2C00-\u2FEF\u3001-\uD7FF\uF900-\uFDCF\uFDF0-\uFFFD][:A-Z_a-z\u00C0-\u00D6\u00D8-\u00F6\u00F8-\u02FF\u0370-\u037D\u037F-\u1FFF\u200C-\u200D\u2070-\u218F\u2C00-\u2FEF\u3001-\uD7FF\uF900-\uFDCF\uFDF0-\uFFFD\-.0-9\u00B7\u0300-\u036F\u203F-\u2040]*$/;
    var ea = {};
    var fa = {};
    function ha(a18) {
      if (y4.call(fa, a18)) return true;
      if (y4.call(ea, a18)) return false;
      if (da.test(a18)) return fa[a18] = true;
      ea[a18] = true;
      return false;
    }
    function z2(a18, b4, c15, d12, f15, e14, g7) {
      this.acceptsBooleans = 2 === b4 || 3 === b4 || 4 === b4;
      this.attributeName = d12;
      this.attributeNamespace = f15;
      this.mustUseProperty = c15;
      this.propertyName = a18;
      this.type = b4;
      this.sanitizeURL = e14;
      this.removeEmptyString = g7;
    }
    var A4 = {};
    "children dangerouslySetInnerHTML defaultValue defaultChecked innerHTML suppressContentEditableWarning suppressHydrationWarning style".split(" ").forEach(function(a18) {
      A4[a18] = new z2(a18, 0, false, a18, null, false, false);
    });
    [["acceptCharset", "accept-charset"], ["className", "class"], ["htmlFor", "for"], ["httpEquiv", "http-equiv"]].forEach(function(a18) {
      var b4 = a18[0];
      A4[b4] = new z2(b4, 1, false, a18[1], null, false, false);
    });
    ["contentEditable", "draggable", "spellCheck", "value"].forEach(function(a18) {
      A4[a18] = new z2(a18, 2, false, a18.toLowerCase(), null, false, false);
    });
    ["autoReverse", "externalResourcesRequired", "focusable", "preserveAlpha"].forEach(function(a18) {
      A4[a18] = new z2(a18, 2, false, a18, null, false, false);
    });
    "allowFullScreen async autoFocus autoPlay controls default defer disabled disablePictureInPicture disableRemotePlayback formNoValidate hidden loop noModule noValidate open playsInline readOnly required reversed scoped seamless itemScope".split(" ").forEach(function(a18) {
      A4[a18] = new z2(a18, 3, false, a18.toLowerCase(), null, false, false);
    });
    ["checked", "multiple", "muted", "selected"].forEach(function(a18) {
      A4[a18] = new z2(a18, 3, true, a18, null, false, false);
    });
    ["capture", "download"].forEach(function(a18) {
      A4[a18] = new z2(a18, 4, false, a18, null, false, false);
    });
    ["cols", "rows", "size", "span"].forEach(function(a18) {
      A4[a18] = new z2(a18, 6, false, a18, null, false, false);
    });
    ["rowSpan", "start"].forEach(function(a18) {
      A4[a18] = new z2(a18, 5, false, a18.toLowerCase(), null, false, false);
    });
    var ia = /[\-:]([a-z])/g;
    function ja(a18) {
      return a18[1].toUpperCase();
    }
    "accent-height alignment-baseline arabic-form baseline-shift cap-height clip-path clip-rule color-interpolation color-interpolation-filters color-profile color-rendering dominant-baseline enable-background fill-opacity fill-rule flood-color flood-opacity font-family font-size font-size-adjust font-stretch font-style font-variant font-weight glyph-name glyph-orientation-horizontal glyph-orientation-vertical horiz-adv-x horiz-origin-x image-rendering letter-spacing lighting-color marker-end marker-mid marker-start overline-position overline-thickness paint-order panose-1 pointer-events rendering-intent shape-rendering stop-color stop-opacity strikethrough-position strikethrough-thickness stroke-dasharray stroke-dashoffset stroke-linecap stroke-linejoin stroke-miterlimit stroke-opacity stroke-width text-anchor text-decoration text-rendering underline-position underline-thickness unicode-bidi unicode-range units-per-em v-alphabetic v-hanging v-ideographic v-mathematical vector-effect vert-adv-y vert-origin-x vert-origin-y word-spacing writing-mode xmlns:xlink x-height".split(" ").forEach(function(a18) {
      var b4 = a18.replace(
        ia,
        ja
      );
      A4[b4] = new z2(b4, 1, false, a18, null, false, false);
    });
    "xlink:actuate xlink:arcrole xlink:role xlink:show xlink:title xlink:type".split(" ").forEach(function(a18) {
      var b4 = a18.replace(ia, ja);
      A4[b4] = new z2(b4, 1, false, a18, "http://www.w3.org/1999/xlink", false, false);
    });
    ["xml:base", "xml:lang", "xml:space"].forEach(function(a18) {
      var b4 = a18.replace(ia, ja);
      A4[b4] = new z2(b4, 1, false, a18, "http://www.w3.org/XML/1998/namespace", false, false);
    });
    ["tabIndex", "crossOrigin"].forEach(function(a18) {
      A4[a18] = new z2(a18, 1, false, a18.toLowerCase(), null, false, false);
    });
    A4.xlinkHref = new z2("xlinkHref", 1, false, "xlink:href", "http://www.w3.org/1999/xlink", true, false);
    ["src", "href", "action", "formAction"].forEach(function(a18) {
      A4[a18] = new z2(a18, 1, false, a18.toLowerCase(), null, true, true);
    });
    var B4 = {
      animationIterationCount: true,
      aspectRatio: true,
      borderImageOutset: true,
      borderImageSlice: true,
      borderImageWidth: true,
      boxFlex: true,
      boxFlexGroup: true,
      boxOrdinalGroup: true,
      columnCount: true,
      columns: true,
      flex: true,
      flexGrow: true,
      flexPositive: true,
      flexShrink: true,
      flexNegative: true,
      flexOrder: true,
      gridArea: true,
      gridRow: true,
      gridRowEnd: true,
      gridRowSpan: true,
      gridRowStart: true,
      gridColumn: true,
      gridColumnEnd: true,
      gridColumnSpan: true,
      gridColumnStart: true,
      fontWeight: true,
      lineClamp: true,
      lineHeight: true,
      opacity: true,
      order: true,
      orphans: true,
      tabSize: true,
      widows: true,
      zIndex: true,
      zoom: true,
      fillOpacity: true,
      floodOpacity: true,
      stopOpacity: true,
      strokeDasharray: true,
      strokeDashoffset: true,
      strokeMiterlimit: true,
      strokeOpacity: true,
      strokeWidth: true
    };
    var ka = ["Webkit", "ms", "Moz", "O"];
    Object.keys(B4).forEach(function(a18) {
      ka.forEach(function(b4) {
        b4 = b4 + a18.charAt(0).toUpperCase() + a18.substring(1);
        B4[b4] = B4[a18];
      });
    });
    var la = /["'&<>]/;
    function F4(a18) {
      if ("boolean" === typeof a18 || "number" === typeof a18) return "" + a18;
      a18 = "" + a18;
      var b4 = la.exec(a18);
      if (b4) {
        var c15 = "", d12, f15 = 0;
        for (d12 = b4.index; d12 < a18.length; d12++) {
          switch (a18.charCodeAt(d12)) {
            case 34:
              b4 = "&quot;";
              break;
            case 38:
              b4 = "&amp;";
              break;
            case 39:
              b4 = "&#x27;";
              break;
            case 60:
              b4 = "&lt;";
              break;
            case 62:
              b4 = "&gt;";
              break;
            default:
              continue;
          }
          f15 !== d12 && (c15 += a18.substring(f15, d12));
          f15 = d12 + 1;
          c15 += b4;
        }
        a18 = f15 !== d12 ? c15 + a18.substring(f15, d12) : c15;
      }
      return a18;
    }
    var ma = /([A-Z])/g;
    var pa = /^ms-/;
    var qa = Array.isArray;
    var ra = x3("<script>");
    var sa = x3("</script>");
    var ta = x3('<script src="');
    var ua = x3('<script type="module" src="');
    var va = x3('" async=""></script>');
    var wa = /(<\/|<)(s)(cript)/gi;
    function xa(a18, b4, c15, d12) {
      return "" + b4 + ("s" === c15 ? "\\u0073" : "\\u0053") + d12;
    }
    function G3(a18, b4) {
      return { insertionMode: a18, selectedValue: b4 };
    }
    function ya(a18, b4, c15) {
      switch (b4) {
        case "select":
          return G3(1, null != c15.value ? c15.value : c15.defaultValue);
        case "svg":
          return G3(2, null);
        case "math":
          return G3(3, null);
        case "foreignObject":
          return G3(1, null);
        case "table":
          return G3(4, null);
        case "thead":
        case "tbody":
        case "tfoot":
          return G3(5, null);
        case "colgroup":
          return G3(7, null);
        case "tr":
          return G3(6, null);
      }
      return 4 <= a18.insertionMode || 0 === a18.insertionMode ? G3(1, null) : a18;
    }
    var za = x3("<!-- -->");
    function Aa(a18, b4, c15, d12) {
      if ("" === b4) return d12;
      d12 && a18.push(za);
      a18.push(F4(b4));
      return true;
    }
    var Ba = /* @__PURE__ */ new Map();
    var Ca = x3(' style="');
    var Da = x3(":");
    var Ea = x3(";");
    function Fa(a18, b4, c15) {
      if ("object" !== typeof c15) throw Error("The `style` prop expects a mapping from style properties to values, not a string. For example, style={{marginRight: spacing + 'em'}} when using JSX.");
      b4 = true;
      for (var d12 in c15) if (y4.call(c15, d12)) {
        var f15 = c15[d12];
        if (null != f15 && "boolean" !== typeof f15 && "" !== f15) {
          if (0 === d12.indexOf("--")) {
            var e14 = F4(d12);
            f15 = F4(("" + f15).trim());
          } else {
            e14 = d12;
            var g7 = Ba.get(e14);
            void 0 !== g7 ? e14 = g7 : (g7 = x3(F4(e14.replace(ma, "-$1").toLowerCase().replace(pa, "-ms-"))), Ba.set(e14, g7), e14 = g7);
            f15 = "number" === typeof f15 ? 0 === f15 || y4.call(
              B4,
              d12
            ) ? "" + f15 : f15 + "px" : F4(("" + f15).trim());
          }
          b4 ? (b4 = false, a18.push(Ca, e14, Da, f15)) : a18.push(Ea, e14, Da, f15);
        }
      }
      b4 || a18.push(H4);
    }
    var I6 = x3(" ");
    var J2 = x3('="');
    var H4 = x3('"');
    var Ga = x3('=""');
    function K3(a18, b4, c15, d12) {
      switch (c15) {
        case "style":
          Fa(a18, b4, d12);
          return;
        case "defaultValue":
        case "defaultChecked":
        case "innerHTML":
        case "suppressContentEditableWarning":
        case "suppressHydrationWarning":
          return;
      }
      if (!(2 < c15.length) || "o" !== c15[0] && "O" !== c15[0] || "n" !== c15[1] && "N" !== c15[1]) {
        if (b4 = A4.hasOwnProperty(c15) ? A4[c15] : null, null !== b4) {
          switch (typeof d12) {
            case "function":
            case "symbol":
              return;
            case "boolean":
              if (!b4.acceptsBooleans) return;
          }
          c15 = b4.attributeName;
          switch (b4.type) {
            case 3:
              d12 && a18.push(I6, c15, Ga);
              break;
            case 4:
              true === d12 ? a18.push(I6, c15, Ga) : false !== d12 && a18.push(I6, c15, J2, F4(d12), H4);
              break;
            case 5:
              isNaN(d12) || a18.push(I6, c15, J2, F4(d12), H4);
              break;
            case 6:
              !isNaN(d12) && 1 <= d12 && a18.push(I6, c15, J2, F4(d12), H4);
              break;
            default:
              b4.sanitizeURL && (d12 = "" + d12), a18.push(I6, c15, J2, F4(d12), H4);
          }
        } else if (ha(c15)) {
          switch (typeof d12) {
            case "function":
            case "symbol":
              return;
            case "boolean":
              if (b4 = c15.toLowerCase().slice(0, 5), "data-" !== b4 && "aria-" !== b4) return;
          }
          a18.push(I6, c15, J2, F4(d12), H4);
        }
      }
    }
    var L3 = x3(">");
    var Ha = x3("/>");
    function M6(a18, b4, c15) {
      if (null != b4) {
        if (null != c15) throw Error("Can only set one of `children` or `props.dangerouslySetInnerHTML`.");
        if ("object" !== typeof b4 || !("__html" in b4)) throw Error("`props.dangerouslySetInnerHTML` must be in the form `{__html: ...}`. Please visit https://reactjs.org/link/dangerously-set-inner-html for more information.");
        b4 = b4.__html;
        null !== b4 && void 0 !== b4 && a18.push("" + b4);
      }
    }
    function Ia(a18) {
      var b4 = "";
      ba.Children.forEach(a18, function(a19) {
        null != a19 && (b4 += a19);
      });
      return b4;
    }
    var Ja = x3(' selected=""');
    function Ka(a18, b4, c15, d12) {
      a18.push(N4(c15));
      var f15 = c15 = null, e14;
      for (e14 in b4) if (y4.call(b4, e14)) {
        var g7 = b4[e14];
        if (null != g7) switch (e14) {
          case "children":
            c15 = g7;
            break;
          case "dangerouslySetInnerHTML":
            f15 = g7;
            break;
          default:
            K3(a18, d12, e14, g7);
        }
      }
      a18.push(L3);
      M6(a18, f15, c15);
      return "string" === typeof c15 ? (a18.push(F4(c15)), null) : c15;
    }
    var La = x3("\n");
    var Ma = /^[a-zA-Z][a-zA-Z:_\.\-\d]*$/;
    var Na = /* @__PURE__ */ new Map();
    function N4(a18) {
      var b4 = Na.get(a18);
      if (void 0 === b4) {
        if (!Ma.test(a18)) throw Error("Invalid tag: " + a18);
        b4 = x3("<" + a18);
        Na.set(a18, b4);
      }
      return b4;
    }
    var Oa = x3("<!DOCTYPE html>");
    function Pa(a18, b4, c15, d12, f15) {
      switch (b4) {
        case "select":
          a18.push(N4("select"));
          var e14 = null, g7 = null;
          for (p11 in c15) if (y4.call(c15, p11)) {
            var h6 = c15[p11];
            if (null != h6) switch (p11) {
              case "children":
                e14 = h6;
                break;
              case "dangerouslySetInnerHTML":
                g7 = h6;
                break;
              case "defaultValue":
              case "value":
                break;
              default:
                K3(a18, d12, p11, h6);
            }
          }
          a18.push(L3);
          M6(a18, g7, e14);
          return e14;
        case "option":
          g7 = f15.selectedValue;
          a18.push(N4("option"));
          var m12 = h6 = null, n13 = null;
          var p11 = null;
          for (e14 in c15) if (y4.call(c15, e14)) {
            var v5 = c15[e14];
            if (null != v5) switch (e14) {
              case "children":
                h6 = v5;
                break;
              case "selected":
                n13 = v5;
                break;
              case "dangerouslySetInnerHTML":
                p11 = v5;
                break;
              case "value":
                m12 = v5;
              default:
                K3(a18, d12, e14, v5);
            }
          }
          if (null != g7) if (c15 = null !== m12 ? "" + m12 : Ia(h6), qa(g7)) for (d12 = 0; d12 < g7.length; d12++) {
            if ("" + g7[d12] === c15) {
              a18.push(Ja);
              break;
            }
          }
          else "" + g7 === c15 && a18.push(Ja);
          else n13 && a18.push(Ja);
          a18.push(L3);
          M6(a18, p11, h6);
          return h6;
        case "textarea":
          a18.push(N4("textarea"));
          p11 = g7 = e14 = null;
          for (h6 in c15) if (y4.call(c15, h6) && (m12 = c15[h6], null != m12)) switch (h6) {
            case "children":
              p11 = m12;
              break;
            case "value":
              e14 = m12;
              break;
            case "defaultValue":
              g7 = m12;
              break;
            case "dangerouslySetInnerHTML":
              throw Error("`dangerouslySetInnerHTML` does not make sense on <textarea>.");
            default:
              K3(a18, d12, h6, m12);
          }
          null === e14 && null !== g7 && (e14 = g7);
          a18.push(L3);
          if (null != p11) {
            if (null != e14) throw Error("If you supply `defaultValue` on a <textarea>, do not pass children.");
            if (qa(p11) && 1 < p11.length) throw Error("<textarea> can only have at most one child.");
            e14 = "" + p11;
          }
          "string" === typeof e14 && "\n" === e14[0] && a18.push(La);
          null !== e14 && a18.push(F4("" + e14));
          return null;
        case "input":
          a18.push(N4("input"));
          m12 = p11 = h6 = e14 = null;
          for (g7 in c15) if (y4.call(c15, g7) && (n13 = c15[g7], null != n13)) switch (g7) {
            case "children":
            case "dangerouslySetInnerHTML":
              throw Error("input is a self-closing tag and must neither have `children` nor use `dangerouslySetInnerHTML`.");
            case "defaultChecked":
              m12 = n13;
              break;
            case "defaultValue":
              h6 = n13;
              break;
            case "checked":
              p11 = n13;
              break;
            case "value":
              e14 = n13;
              break;
            default:
              K3(a18, d12, g7, n13);
          }
          null !== p11 ? K3(a18, d12, "checked", p11) : null !== m12 && K3(a18, d12, "checked", m12);
          null !== e14 ? K3(a18, d12, "value", e14) : null !== h6 && K3(a18, d12, "value", h6);
          a18.push(Ha);
          return null;
        case "menuitem":
          a18.push(N4("menuitem"));
          for (var C5 in c15) if (y4.call(c15, C5) && (e14 = c15[C5], null != e14)) switch (C5) {
            case "children":
            case "dangerouslySetInnerHTML":
              throw Error("menuitems cannot have `children` nor `dangerouslySetInnerHTML`.");
            default:
              K3(a18, d12, C5, e14);
          }
          a18.push(L3);
          return null;
        case "title":
          a18.push(N4("title"));
          e14 = null;
          for (v5 in c15) if (y4.call(c15, v5) && (g7 = c15[v5], null != g7)) switch (v5) {
            case "children":
              e14 = g7;
              break;
            case "dangerouslySetInnerHTML":
              throw Error("`dangerouslySetInnerHTML` does not make sense on <title>.");
            default:
              K3(a18, d12, v5, g7);
          }
          a18.push(L3);
          return e14;
        case "listing":
        case "pre":
          a18.push(N4(b4));
          g7 = e14 = null;
          for (m12 in c15) if (y4.call(c15, m12) && (h6 = c15[m12], null != h6)) switch (m12) {
            case "children":
              e14 = h6;
              break;
            case "dangerouslySetInnerHTML":
              g7 = h6;
              break;
            default:
              K3(a18, d12, m12, h6);
          }
          a18.push(L3);
          if (null != g7) {
            if (null != e14) throw Error("Can only set one of `children` or `props.dangerouslySetInnerHTML`.");
            if ("object" !== typeof g7 || !("__html" in g7)) throw Error("`props.dangerouslySetInnerHTML` must be in the form `{__html: ...}`. Please visit https://reactjs.org/link/dangerously-set-inner-html for more information.");
            c15 = g7.__html;
            null !== c15 && void 0 !== c15 && ("string" === typeof c15 && 0 < c15.length && "\n" === c15[0] ? a18.push(La, c15) : a18.push("" + c15));
          }
          "string" === typeof e14 && "\n" === e14[0] && a18.push(La);
          return e14;
        case "area":
        case "base":
        case "br":
        case "col":
        case "embed":
        case "hr":
        case "img":
        case "keygen":
        case "link":
        case "meta":
        case "param":
        case "source":
        case "track":
        case "wbr":
          a18.push(N4(b4));
          for (var D5 in c15) if (y4.call(c15, D5) && (e14 = c15[D5], null != e14)) switch (D5) {
            case "children":
            case "dangerouslySetInnerHTML":
              throw Error(b4 + " is a self-closing tag and must neither have `children` nor use `dangerouslySetInnerHTML`.");
            default:
              K3(a18, d12, D5, e14);
          }
          a18.push(Ha);
          return null;
        case "annotation-xml":
        case "color-profile":
        case "font-face":
        case "font-face-src":
        case "font-face-uri":
        case "font-face-format":
        case "font-face-name":
        case "missing-glyph":
          return Ka(a18, c15, b4, d12);
        case "html":
          return 0 === f15.insertionMode && a18.push(Oa), Ka(
            a18,
            c15,
            b4,
            d12
          );
        default:
          if (-1 === b4.indexOf("-") && "string" !== typeof c15.is) return Ka(a18, c15, b4, d12);
          a18.push(N4(b4));
          g7 = e14 = null;
          for (n13 in c15) if (y4.call(c15, n13) && (h6 = c15[n13], null != h6)) switch (n13) {
            case "children":
              e14 = h6;
              break;
            case "dangerouslySetInnerHTML":
              g7 = h6;
              break;
            case "style":
              Fa(a18, d12, h6);
              break;
            case "suppressContentEditableWarning":
            case "suppressHydrationWarning":
              break;
            default:
              ha(n13) && "function" !== typeof h6 && "symbol" !== typeof h6 && a18.push(I6, n13, J2, F4(h6), H4);
          }
          a18.push(L3);
          M6(a18, g7, e14);
          return e14;
      }
    }
    var Qa = x3("</");
    var Ra = x3(">");
    var Sa = x3('<template id="');
    var Ta = x3('"></template>');
    var Ua = x3("<!--$-->");
    var Va = x3('<!--$?--><template id="');
    var Wa = x3('"></template>');
    var Xa = x3("<!--$!-->");
    var Ya = x3("<!--/$-->");
    var Za = x3("<template");
    var $a = x3('"');
    var ab = x3(' data-dgst="');
    x3(' data-msg="');
    x3(' data-stck="');
    var bb = x3("></template>");
    function cb(a18, b4, c15) {
      r7(a18, Va);
      if (null === c15) throw Error("An ID must have been assigned before we can complete the boundary.");
      r7(a18, c15);
      return w11(a18, Wa);
    }
    var db = x3('<div hidden id="');
    var eb = x3('">');
    var fb = x3("</div>");
    var gb = x3('<svg aria-hidden="true" style="display:none" id="');
    var hb = x3('">');
    var ib = x3("</svg>");
    var jb = x3('<math aria-hidden="true" style="display:none" id="');
    var kb = x3('">');
    var lb = x3("</math>");
    var mb = x3('<table hidden id="');
    var nb = x3('">');
    var ob = x3("</table>");
    var pb = x3('<table hidden><tbody id="');
    var qb = x3('">');
    var rb = x3("</tbody></table>");
    var sb = x3('<table hidden><tr id="');
    var tb = x3('">');
    var ub = x3("</tr></table>");
    var vb = x3('<table hidden><colgroup id="');
    var wb = x3('">');
    var xb = x3("</colgroup></table>");
    function yb(a18, b4, c15, d12) {
      switch (c15.insertionMode) {
        case 0:
        case 1:
          return r7(a18, db), r7(a18, b4.segmentPrefix), r7(a18, d12.toString(16)), w11(a18, eb);
        case 2:
          return r7(a18, gb), r7(a18, b4.segmentPrefix), r7(a18, d12.toString(16)), w11(a18, hb);
        case 3:
          return r7(a18, jb), r7(a18, b4.segmentPrefix), r7(a18, d12.toString(16)), w11(a18, kb);
        case 4:
          return r7(a18, mb), r7(a18, b4.segmentPrefix), r7(a18, d12.toString(16)), w11(a18, nb);
        case 5:
          return r7(a18, pb), r7(a18, b4.segmentPrefix), r7(a18, d12.toString(16)), w11(a18, qb);
        case 6:
          return r7(a18, sb), r7(a18, b4.segmentPrefix), r7(a18, d12.toString(16)), w11(a18, tb);
        case 7:
          return r7(a18, vb), r7(
            a18,
            b4.segmentPrefix
          ), r7(a18, d12.toString(16)), w11(a18, wb);
        default:
          throw Error("Unknown insertion mode. This is a bug in React.");
      }
    }
    function zb(a18, b4) {
      switch (b4.insertionMode) {
        case 0:
        case 1:
          return w11(a18, fb);
        case 2:
          return w11(a18, ib);
        case 3:
          return w11(a18, lb);
        case 4:
          return w11(a18, ob);
        case 5:
          return w11(a18, rb);
        case 6:
          return w11(a18, ub);
        case 7:
          return w11(a18, xb);
        default:
          throw Error("Unknown insertion mode. This is a bug in React.");
      }
    }
    var Ab = x3('function $RS(a,b){a=document.getElementById(a);b=document.getElementById(b);for(a.parentNode.removeChild(a);a.firstChild;)b.parentNode.insertBefore(a.firstChild,b);b.parentNode.removeChild(b)};$RS("');
    var Bb = x3('$RS("');
    var Cb = x3('","');
    var Db = x3('")</script>');
    var Fb = x3('function $RC(a,b){a=document.getElementById(a);b=document.getElementById(b);b.parentNode.removeChild(b);if(a){a=a.previousSibling;var f=a.parentNode,c=a.nextSibling,e=0;do{if(c&&8===c.nodeType){var d=c.data;if("/$"===d)if(0===e)break;else e--;else"$"!==d&&"$?"!==d&&"$!"!==d||e++}d=c.nextSibling;f.removeChild(c);c=d}while(c);for(;b.firstChild;)f.insertBefore(b.firstChild,c);a.data="$";a._reactRetry&&a._reactRetry()}};$RC("');
    var Gb = x3('$RC("');
    var Hb = x3('","');
    var Ib = x3('")</script>');
    var Jb = x3('function $RX(b,c,d,e){var a=document.getElementById(b);a&&(b=a.previousSibling,b.data="$!",a=a.dataset,c&&(a.dgst=c),d&&(a.msg=d),e&&(a.stck=e),b._reactRetry&&b._reactRetry())};$RX("');
    var Kb = x3('$RX("');
    var Lb = x3('"');
    var Mb = x3(")</script>");
    var Nb = x3(",");
    var Ob = /[<\u2028\u2029]/g;
    function Pb(a18) {
      return JSON.stringify(a18).replace(Ob, function(a19) {
        switch (a19) {
          case "<":
            return "\\u003c";
          case "\u2028":
            return "\\u2028";
          case "\u2029":
            return "\\u2029";
          default:
            throw Error("escapeJSStringsForInstructionScripts encountered a match it does not know how to replace. this means the match regex and the replacement characters are no longer in sync. This is a bug in React");
        }
      });
    }
    var O5 = Object.assign;
    var Qb = Symbol.for("react.element");
    var Rb = Symbol.for("react.portal");
    var Sb = Symbol.for("react.fragment");
    var Tb = Symbol.for("react.strict_mode");
    var Ub = Symbol.for("react.profiler");
    var Vb = Symbol.for("react.provider");
    var Wb = Symbol.for("react.context");
    var Xb = Symbol.for("react.forward_ref");
    var Yb = Symbol.for("react.suspense");
    var Zb = Symbol.for("react.suspense_list");
    var $b = Symbol.for("react.memo");
    var ac = Symbol.for("react.lazy");
    var bc = Symbol.for("react.scope");
    var cc = Symbol.for("react.debug_trace_mode");
    var dc = Symbol.for("react.legacy_hidden");
    var ec = Symbol.for("react.default_value");
    var fc = Symbol.iterator;
    function gc(a18) {
      if (null == a18) return null;
      if ("function" === typeof a18) return a18.displayName || a18.name || null;
      if ("string" === typeof a18) return a18;
      switch (a18) {
        case Sb:
          return "Fragment";
        case Rb:
          return "Portal";
        case Ub:
          return "Profiler";
        case Tb:
          return "StrictMode";
        case Yb:
          return "Suspense";
        case Zb:
          return "SuspenseList";
      }
      if ("object" === typeof a18) switch (a18.$$typeof) {
        case Wb:
          return (a18.displayName || "Context") + ".Consumer";
        case Vb:
          return (a18._context.displayName || "Context") + ".Provider";
        case Xb:
          var b4 = a18.render;
          a18 = a18.displayName;
          a18 || (a18 = b4.displayName || b4.name || "", a18 = "" !== a18 ? "ForwardRef(" + a18 + ")" : "ForwardRef");
          return a18;
        case $b:
          return b4 = a18.displayName || null, null !== b4 ? b4 : gc(a18.type) || "Memo";
        case ac:
          b4 = a18._payload;
          a18 = a18._init;
          try {
            return gc(a18(b4));
          } catch (c15) {
          }
      }
      return null;
    }
    var hc = {};
    function ic(a18, b4) {
      a18 = a18.contextTypes;
      if (!a18) return hc;
      var c15 = {}, d12;
      for (d12 in a18) c15[d12] = b4[d12];
      return c15;
    }
    var P4 = null;
    function Q4(a18, b4) {
      if (a18 !== b4) {
        a18.context._currentValue = a18.parentValue;
        a18 = a18.parent;
        var c15 = b4.parent;
        if (null === a18) {
          if (null !== c15) throw Error("The stacks must reach the root at the same time. This is a bug in React.");
        } else {
          if (null === c15) throw Error("The stacks must reach the root at the same time. This is a bug in React.");
          Q4(a18, c15);
        }
        b4.context._currentValue = b4.value;
      }
    }
    function jc(a18) {
      a18.context._currentValue = a18.parentValue;
      a18 = a18.parent;
      null !== a18 && jc(a18);
    }
    function kc(a18) {
      var b4 = a18.parent;
      null !== b4 && kc(b4);
      a18.context._currentValue = a18.value;
    }
    function lc(a18, b4) {
      a18.context._currentValue = a18.parentValue;
      a18 = a18.parent;
      if (null === a18) throw Error("The depth must equal at least at zero before reaching the root. This is a bug in React.");
      a18.depth === b4.depth ? Q4(a18, b4) : lc(a18, b4);
    }
    function mc(a18, b4) {
      var c15 = b4.parent;
      if (null === c15) throw Error("The depth must equal at least at zero before reaching the root. This is a bug in React.");
      a18.depth === c15.depth ? Q4(a18, c15) : mc(a18, c15);
      b4.context._currentValue = b4.value;
    }
    function nc(a18) {
      var b4 = P4;
      b4 !== a18 && (null === b4 ? kc(a18) : null === a18 ? jc(b4) : b4.depth === a18.depth ? Q4(b4, a18) : b4.depth > a18.depth ? lc(b4, a18) : mc(b4, a18), P4 = a18);
    }
    var oc = { isMounted: function() {
      return false;
    }, enqueueSetState: function(a18, b4) {
      a18 = a18._reactInternals;
      null !== a18.queue && a18.queue.push(b4);
    }, enqueueReplaceState: function(a18, b4) {
      a18 = a18._reactInternals;
      a18.replace = true;
      a18.queue = [b4];
    }, enqueueForceUpdate: function() {
    } };
    function pc43(a18, b4, c15, d12) {
      var f15 = void 0 !== a18.state ? a18.state : null;
      a18.updater = oc;
      a18.props = c15;
      a18.state = f15;
      var e14 = { queue: [], replace: false };
      a18._reactInternals = e14;
      var g7 = b4.contextType;
      a18.context = "object" === typeof g7 && null !== g7 ? g7._currentValue : d12;
      g7 = b4.getDerivedStateFromProps;
      "function" === typeof g7 && (g7 = g7(c15, f15), f15 = null === g7 || void 0 === g7 ? f15 : O5({}, f15, g7), a18.state = f15);
      if ("function" !== typeof b4.getDerivedStateFromProps && "function" !== typeof a18.getSnapshotBeforeUpdate && ("function" === typeof a18.UNSAFE_componentWillMount || "function" === typeof a18.componentWillMount)) if (b4 = a18.state, "function" === typeof a18.componentWillMount && a18.componentWillMount(), "function" === typeof a18.UNSAFE_componentWillMount && a18.UNSAFE_componentWillMount(), b4 !== a18.state && oc.enqueueReplaceState(a18, a18.state, null), null !== e14.queue && 0 < e14.queue.length) if (b4 = e14.queue, g7 = e14.replace, e14.queue = null, e14.replace = false, g7 && 1 === b4.length) a18.state = b4[0];
      else {
        e14 = g7 ? b4[0] : a18.state;
        f15 = true;
        for (g7 = g7 ? 1 : 0; g7 < b4.length; g7++) {
          var h6 = b4[g7];
          h6 = "function" === typeof h6 ? h6.call(a18, e14, c15, d12) : h6;
          null != h6 && (f15 ? (f15 = false, e14 = O5({}, e14, h6)) : O5(e14, h6));
        }
        a18.state = e14;
      }
      else e14.queue = null;
    }
    var qc = { id: 1, overflow: "" };
    function rc(a18, b4, c15) {
      var d12 = a18.id;
      a18 = a18.overflow;
      var f15 = 32 - sc(d12) - 1;
      d12 &= ~(1 << f15);
      c15 += 1;
      var e14 = 32 - sc(b4) + f15;
      if (30 < e14) {
        var g7 = f15 - f15 % 5;
        e14 = (d12 & (1 << g7) - 1).toString(32);
        d12 >>= g7;
        f15 -= g7;
        return { id: 1 << 32 - sc(b4) + f15 | c15 << f15 | d12, overflow: e14 + a18 };
      }
      return { id: 1 << e14 | c15 << f15 | d12, overflow: a18 };
    }
    var sc = Math.clz32 ? Math.clz32 : tc;
    var uc = Math.log;
    var vc = Math.LN2;
    function tc(a18) {
      a18 >>>= 0;
      return 0 === a18 ? 32 : 31 - (uc(a18) / vc | 0) | 0;
    }
    function wc(a18, b4) {
      return a18 === b4 && (0 !== a18 || 1 / a18 === 1 / b4) || a18 !== a18 && b4 !== b4;
    }
    var xc = "function" === typeof Object.is ? Object.is : wc;
    var R10 = null;
    var yc = null;
    var zc = null;
    var S2 = null;
    var T3 = false;
    var Ac = false;
    var U3 = 0;
    var V3 = null;
    var Bc = 0;
    function W4() {
      if (null === R10) throw Error("Invalid hook call. Hooks can only be called inside of the body of a function component. This could happen for one of the following reasons:\n1. You might have mismatching versions of React and the renderer (such as React DOM)\n2. You might be breaking the Rules of Hooks\n3. You might have more than one copy of React in the same app\nSee https://reactjs.org/link/invalid-hook-call for tips about how to debug and fix this problem.");
      return R10;
    }
    function Cc() {
      if (0 < Bc) throw Error("Rendered more hooks than during the previous render");
      return { memoizedState: null, queue: null, next: null };
    }
    function Dc() {
      null === S2 ? null === zc ? (T3 = false, zc = S2 = Cc()) : (T3 = true, S2 = zc) : null === S2.next ? (T3 = false, S2 = S2.next = Cc()) : (T3 = true, S2 = S2.next);
      return S2;
    }
    function Ec() {
      yc = R10 = null;
      Ac = false;
      zc = null;
      Bc = 0;
      S2 = V3 = null;
    }
    function Fc(a18, b4) {
      return "function" === typeof b4 ? b4(a18) : b4;
    }
    function Gc(a18, b4, c15) {
      R10 = W4();
      S2 = Dc();
      if (T3) {
        var d12 = S2.queue;
        b4 = d12.dispatch;
        if (null !== V3 && (c15 = V3.get(d12), void 0 !== c15)) {
          V3.delete(d12);
          d12 = S2.memoizedState;
          do
            d12 = a18(d12, c15.action), c15 = c15.next;
          while (null !== c15);
          S2.memoizedState = d12;
          return [d12, b4];
        }
        return [S2.memoizedState, b4];
      }
      a18 = a18 === Fc ? "function" === typeof b4 ? b4() : b4 : void 0 !== c15 ? c15(b4) : b4;
      S2.memoizedState = a18;
      a18 = S2.queue = { last: null, dispatch: null };
      a18 = a18.dispatch = Hc.bind(null, R10, a18);
      return [S2.memoizedState, a18];
    }
    function Ic(a18, b4) {
      R10 = W4();
      S2 = Dc();
      b4 = void 0 === b4 ? null : b4;
      if (null !== S2) {
        var c15 = S2.memoizedState;
        if (null !== c15 && null !== b4) {
          var d12 = c15[1];
          a: if (null === d12) d12 = false;
          else {
            for (var f15 = 0; f15 < d12.length && f15 < b4.length; f15++) if (!xc(b4[f15], d12[f15])) {
              d12 = false;
              break a;
            }
            d12 = true;
          }
          if (d12) return c15[0];
        }
      }
      a18 = a18();
      S2.memoizedState = [a18, b4];
      return a18;
    }
    function Hc(a18, b4, c15) {
      if (25 <= Bc) throw Error("Too many re-renders. React limits the number of renders to prevent an infinite loop.");
      if (a18 === R10) if (Ac = true, a18 = { action: c15, next: null }, null === V3 && (V3 = /* @__PURE__ */ new Map()), c15 = V3.get(b4), void 0 === c15) V3.set(b4, a18);
      else {
        for (b4 = c15; null !== b4.next; ) b4 = b4.next;
        b4.next = a18;
      }
    }
    function Jc() {
      throw Error("startTransition cannot be called during server rendering.");
    }
    function Kc() {
    }
    var Mc = { readContext: function(a18) {
      return a18._currentValue;
    }, useContext: function(a18) {
      W4();
      return a18._currentValue;
    }, useMemo: Ic, useReducer: Gc, useRef: function(a18) {
      R10 = W4();
      S2 = Dc();
      var b4 = S2.memoizedState;
      return null === b4 ? (a18 = { current: a18 }, S2.memoizedState = a18) : b4;
    }, useState: function(a18) {
      return Gc(Fc, a18);
    }, useInsertionEffect: Kc, useLayoutEffect: function() {
    }, useCallback: function(a18, b4) {
      return Ic(function() {
        return a18;
      }, b4);
    }, useImperativeHandle: Kc, useEffect: Kc, useDebugValue: Kc, useDeferredValue: function(a18) {
      W4();
      return a18;
    }, useTransition: function() {
      W4();
      return [false, Jc];
    }, useId: function() {
      var a18 = yc.treeContext;
      var b4 = a18.overflow;
      a18 = a18.id;
      a18 = (a18 & ~(1 << 32 - sc(a18) - 1)).toString(32) + b4;
      var c15 = Lc;
      if (null === c15) throw Error("Invalid hook call. Hooks can only be called inside of the body of a function component.");
      b4 = U3++;
      a18 = ":" + c15.idPrefix + "R" + a18;
      0 < b4 && (a18 += "H" + b4.toString(32));
      return a18 + ":";
    }, useMutableSource: function(a18, b4) {
      W4();
      return b4(a18._source);
    }, useSyncExternalStore: function(a18, b4, c15) {
      if (void 0 === c15) throw Error("Missing getServerSnapshot, which is required for server-rendered content. Will revert to client rendering.");
      return c15();
    } };
    var Lc = null;
    var Nc = ba.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED.ReactCurrentDispatcher;
    function Oc(a18) {
      console.error(a18);
      return null;
    }
    function X4() {
    }
    function Pc(a18, b4) {
      var c15 = a18.pingedTasks;
      c15.push(b4);
      1 === c15.length && setImmediate(function() {
        return Qc(a18);
      });
    }
    function Rc(a18, b4, c15, d12, f15, e14, g7, h6) {
      a18.allPendingTasks++;
      null === c15 ? a18.pendingRootTasks++ : c15.pendingTasks++;
      var m12 = { node: b4, ping: function() {
        return Pc(a18, m12);
      }, blockedBoundary: c15, blockedSegment: d12, abortSet: f15, legacyContext: e14, context: g7, treeContext: h6 };
      f15.add(m12);
      return m12;
    }
    function Sc(a18, b4, c15, d12, f15, e14) {
      return { status: 0, id: -1, index: b4, parentFlushed: false, chunks: [], children: [], formatContext: d12, boundary: c15, lastPushedText: f15, textEmbedded: e14 };
    }
    function Y3(a18, b4) {
      a18 = a18.onError(b4);
      if (null != a18 && "string" !== typeof a18) throw Error('onError returned something with a type other than "string". onError should return a string and may return null or undefined but must not return anything else. It received something of type "' + typeof a18 + '" instead');
      return a18;
    }
    function Tc(a18, b4) {
      var c15 = a18.onShellError;
      c15(b4);
      c15 = a18.onFatalError;
      c15(b4);
      null !== a18.destination ? (a18.status = 2, a18.destination.destroy(b4)) : (a18.status = 1, a18.fatalError = b4);
    }
    function Uc(a18, b4, c15, d12, f15) {
      R10 = {};
      yc = b4;
      U3 = 0;
      for (a18 = c15(d12, f15); Ac; ) Ac = false, U3 = 0, Bc += 1, S2 = null, a18 = c15(d12, f15);
      Ec();
      return a18;
    }
    function Vc(a18, b4, c15, d12) {
      var f15 = c15.render(), e14 = d12.childContextTypes;
      if (null !== e14 && void 0 !== e14) {
        var g7 = b4.legacyContext;
        if ("function" !== typeof c15.getChildContext) d12 = g7;
        else {
          c15 = c15.getChildContext();
          for (var h6 in c15) if (!(h6 in e14)) throw Error((gc(d12) || "Unknown") + '.getChildContext(): key "' + h6 + '" is not defined in childContextTypes.');
          d12 = O5({}, g7, c15);
        }
        b4.legacyContext = d12;
        Z5(a18, b4, f15);
        b4.legacyContext = g7;
      } else Z5(a18, b4, f15);
    }
    function Wc(a18, b4) {
      if (a18 && a18.defaultProps) {
        b4 = O5({}, b4);
        a18 = a18.defaultProps;
        for (var c15 in a18) void 0 === b4[c15] && (b4[c15] = a18[c15]);
        return b4;
      }
      return b4;
    }
    function Xc(a18, b4, c15, d12, f15) {
      if ("function" === typeof c15) if (c15.prototype && c15.prototype.isReactComponent) {
        f15 = ic(c15, b4.legacyContext);
        var e14 = c15.contextType;
        e14 = new c15(d12, "object" === typeof e14 && null !== e14 ? e14._currentValue : f15);
        pc43(e14, c15, d12, f15);
        Vc(a18, b4, e14, c15);
      } else {
        e14 = ic(c15, b4.legacyContext);
        f15 = Uc(a18, b4, c15, d12, e14);
        var g7 = 0 !== U3;
        if ("object" === typeof f15 && null !== f15 && "function" === typeof f15.render && void 0 === f15.$$typeof) pc43(f15, c15, d12, e14), Vc(a18, b4, f15, c15);
        else if (g7) {
          d12 = b4.treeContext;
          b4.treeContext = rc(d12, 1, 0);
          try {
            Z5(a18, b4, f15);
          } finally {
            b4.treeContext = d12;
          }
        } else Z5(a18, b4, f15);
      }
      else if ("string" === typeof c15) {
        f15 = b4.blockedSegment;
        e14 = Pa(f15.chunks, c15, d12, a18.responseState, f15.formatContext);
        f15.lastPushedText = false;
        g7 = f15.formatContext;
        f15.formatContext = ya(g7, c15, d12);
        Yc(a18, b4, e14);
        f15.formatContext = g7;
        switch (c15) {
          case "area":
          case "base":
          case "br":
          case "col":
          case "embed":
          case "hr":
          case "img":
          case "input":
          case "keygen":
          case "link":
          case "meta":
          case "param":
          case "source":
          case "track":
          case "wbr":
            break;
          default:
            f15.chunks.push(Qa, c15, Ra);
        }
        f15.lastPushedText = false;
      } else {
        switch (c15) {
          case dc:
          case cc:
          case Tb:
          case Ub:
          case Sb:
            Z5(a18, b4, d12.children);
            return;
          case Zb:
            Z5(
              a18,
              b4,
              d12.children
            );
            return;
          case bc:
            throw Error("ReactDOMServer does not yet support scope components.");
          case Yb:
            a: {
              c15 = b4.blockedBoundary;
              f15 = b4.blockedSegment;
              e14 = d12.fallback;
              d12 = d12.children;
              g7 = /* @__PURE__ */ new Set();
              var h6 = { id: null, rootSegmentID: -1, parentFlushed: false, pendingTasks: 0, forceClientRender: false, completedSegments: [], byteSize: 0, fallbackAbortableTasks: g7, errorDigest: null }, m12 = Sc(a18, f15.chunks.length, h6, f15.formatContext, false, false);
              f15.children.push(m12);
              f15.lastPushedText = false;
              var n13 = Sc(a18, 0, null, f15.formatContext, false, false);
              n13.parentFlushed = true;
              b4.blockedBoundary = h6;
              b4.blockedSegment = n13;
              try {
                if (Yc(a18, b4, d12), n13.lastPushedText && n13.textEmbedded && n13.chunks.push(za), n13.status = 1, Zc(h6, n13), 0 === h6.pendingTasks) break a;
              } catch (p11) {
                n13.status = 4, h6.forceClientRender = true, h6.errorDigest = Y3(a18, p11);
              } finally {
                b4.blockedBoundary = c15, b4.blockedSegment = f15;
              }
              b4 = Rc(a18, e14, c15, m12, g7, b4.legacyContext, b4.context, b4.treeContext);
              a18.pingedTasks.push(b4);
            }
            return;
        }
        if ("object" === typeof c15 && null !== c15) switch (c15.$$typeof) {
          case Xb:
            d12 = Uc(a18, b4, c15.render, d12, f15);
            if (0 !== U3) {
              c15 = b4.treeContext;
              b4.treeContext = rc(c15, 1, 0);
              try {
                Z5(a18, b4, d12);
              } finally {
                b4.treeContext = c15;
              }
            } else Z5(
              a18,
              b4,
              d12
            );
            return;
          case $b:
            c15 = c15.type;
            d12 = Wc(c15, d12);
            Xc(a18, b4, c15, d12, f15);
            return;
          case Vb:
            f15 = d12.children;
            c15 = c15._context;
            d12 = d12.value;
            e14 = c15._currentValue;
            c15._currentValue = d12;
            g7 = P4;
            P4 = d12 = { parent: g7, depth: null === g7 ? 0 : g7.depth + 1, context: c15, parentValue: e14, value: d12 };
            b4.context = d12;
            Z5(a18, b4, f15);
            a18 = P4;
            if (null === a18) throw Error("Tried to pop a Context at the root of the app. This is a bug in React.");
            d12 = a18.parentValue;
            a18.context._currentValue = d12 === ec ? a18.context._defaultValue : d12;
            a18 = P4 = a18.parent;
            b4.context = a18;
            return;
          case Wb:
            d12 = d12.children;
            d12 = d12(c15._currentValue);
            Z5(a18, b4, d12);
            return;
          case ac:
            f15 = c15._init;
            c15 = f15(c15._payload);
            d12 = Wc(c15, d12);
            Xc(a18, b4, c15, d12, void 0);
            return;
        }
        throw Error("Element type is invalid: expected a string (for built-in components) or a class/function (for composite components) but got: " + ((null == c15 ? c15 : typeof c15) + "."));
      }
    }
    function Z5(a18, b4, c15) {
      b4.node = c15;
      if ("object" === typeof c15 && null !== c15) {
        switch (c15.$$typeof) {
          case Qb:
            Xc(a18, b4, c15.type, c15.props, c15.ref);
            return;
          case Rb:
            throw Error("Portals are not currently supported by the server renderer. Render them conditionally so that they only appear on the client render.");
          case ac:
            var d12 = c15._init;
            c15 = d12(c15._payload);
            Z5(a18, b4, c15);
            return;
        }
        if (qa(c15)) {
          $c(a18, b4, c15);
          return;
        }
        null === c15 || "object" !== typeof c15 ? d12 = null : (d12 = fc && c15[fc] || c15["@@iterator"], d12 = "function" === typeof d12 ? d12 : null);
        if (d12 && (d12 = d12.call(c15))) {
          c15 = d12.next();
          if (!c15.done) {
            var f15 = [];
            do
              f15.push(c15.value), c15 = d12.next();
            while (!c15.done);
            $c(a18, b4, f15);
          }
          return;
        }
        a18 = Object.prototype.toString.call(c15);
        throw Error("Objects are not valid as a React child (found: " + ("[object Object]" === a18 ? "object with keys {" + Object.keys(c15).join(", ") + "}" : a18) + "). If you meant to render a collection of children, use an array instead.");
      }
      "string" === typeof c15 ? (d12 = b4.blockedSegment, d12.lastPushedText = Aa(b4.blockedSegment.chunks, c15, a18.responseState, d12.lastPushedText)) : "number" === typeof c15 && (d12 = b4.blockedSegment, d12.lastPushedText = Aa(
        b4.blockedSegment.chunks,
        "" + c15,
        a18.responseState,
        d12.lastPushedText
      ));
    }
    function $c(a18, b4, c15) {
      for (var d12 = c15.length, f15 = 0; f15 < d12; f15++) {
        var e14 = b4.treeContext;
        b4.treeContext = rc(e14, d12, f15);
        try {
          Yc(a18, b4, c15[f15]);
        } finally {
          b4.treeContext = e14;
        }
      }
    }
    function Yc(a18, b4, c15) {
      var d12 = b4.blockedSegment.formatContext, f15 = b4.legacyContext, e14 = b4.context;
      try {
        return Z5(a18, b4, c15);
      } catch (m12) {
        if (Ec(), "object" === typeof m12 && null !== m12 && "function" === typeof m12.then) {
          c15 = m12;
          var g7 = b4.blockedSegment, h6 = Sc(a18, g7.chunks.length, null, g7.formatContext, g7.lastPushedText, true);
          g7.children.push(h6);
          g7.lastPushedText = false;
          a18 = Rc(a18, b4.node, b4.blockedBoundary, h6, b4.abortSet, b4.legacyContext, b4.context, b4.treeContext).ping;
          c15.then(a18, a18);
          b4.blockedSegment.formatContext = d12;
          b4.legacyContext = f15;
          b4.context = e14;
          nc(e14);
        } else throw b4.blockedSegment.formatContext = d12, b4.legacyContext = f15, b4.context = e14, nc(e14), m12;
      }
    }
    function ad(a18) {
      var b4 = a18.blockedBoundary;
      a18 = a18.blockedSegment;
      a18.status = 3;
      bd(this, b4, a18);
    }
    function cd(a18, b4, c15) {
      var d12 = a18.blockedBoundary;
      a18.blockedSegment.status = 3;
      null === d12 ? (b4.allPendingTasks--, 2 !== b4.status && (b4.status = 2, null !== b4.destination && b4.destination.end())) : (d12.pendingTasks--, d12.forceClientRender || (d12.forceClientRender = true, d12.errorDigest = b4.onError(void 0 === c15 ? Error("The render was aborted by the server without a reason.") : c15), d12.parentFlushed && b4.clientRenderedBoundaries.push(d12)), d12.fallbackAbortableTasks.forEach(function(a19) {
        return cd(a19, b4, c15);
      }), d12.fallbackAbortableTasks.clear(), b4.allPendingTasks--, 0 === b4.allPendingTasks && (a18 = b4.onAllReady, a18()));
    }
    function Zc(a18, b4) {
      if (0 === b4.chunks.length && 1 === b4.children.length && null === b4.children[0].boundary) {
        var c15 = b4.children[0];
        c15.id = b4.id;
        c15.parentFlushed = true;
        1 === c15.status && Zc(a18, c15);
      } else a18.completedSegments.push(b4);
    }
    function bd(a18, b4, c15) {
      if (null === b4) {
        if (c15.parentFlushed) {
          if (null !== a18.completedRootSegment) throw Error("There can only be one root segment. This is a bug in React.");
          a18.completedRootSegment = c15;
        }
        a18.pendingRootTasks--;
        0 === a18.pendingRootTasks && (a18.onShellError = X4, b4 = a18.onShellReady, b4());
      } else b4.pendingTasks--, b4.forceClientRender || (0 === b4.pendingTasks ? (c15.parentFlushed && 1 === c15.status && Zc(b4, c15), b4.parentFlushed && a18.completedBoundaries.push(b4), b4.fallbackAbortableTasks.forEach(ad, a18), b4.fallbackAbortableTasks.clear()) : c15.parentFlushed && 1 === c15.status && (Zc(b4, c15), 1 === b4.completedSegments.length && b4.parentFlushed && a18.partialBoundaries.push(b4)));
      a18.allPendingTasks--;
      0 === a18.allPendingTasks && (a18 = a18.onAllReady, a18());
    }
    function Qc(a18) {
      if (2 !== a18.status) {
        var b4 = P4, c15 = Nc.current;
        Nc.current = Mc;
        var d12 = Lc;
        Lc = a18.responseState;
        try {
          var f15 = a18.pingedTasks, e14;
          for (e14 = 0; e14 < f15.length; e14++) {
            var g7 = f15[e14];
            var h6 = a18, m12 = g7.blockedSegment;
            if (0 === m12.status) {
              nc(g7.context);
              try {
                Z5(h6, g7, g7.node), m12.lastPushedText && m12.textEmbedded && m12.chunks.push(za), g7.abortSet.delete(g7), m12.status = 1, bd(h6, g7.blockedBoundary, m12);
              } catch (E6) {
                if (Ec(), "object" === typeof E6 && null !== E6 && "function" === typeof E6.then) {
                  var n13 = g7.ping;
                  E6.then(n13, n13);
                } else {
                  g7.abortSet.delete(g7);
                  m12.status = 4;
                  var p11 = g7.blockedBoundary, v5 = E6, C5 = Y3(h6, v5);
                  null === p11 ? Tc(h6, v5) : (p11.pendingTasks--, p11.forceClientRender || (p11.forceClientRender = true, p11.errorDigest = C5, p11.parentFlushed && h6.clientRenderedBoundaries.push(p11)));
                  h6.allPendingTasks--;
                  if (0 === h6.allPendingTasks) {
                    var D5 = h6.onAllReady;
                    D5();
                  }
                }
              } finally {
              }
            }
          }
          f15.splice(0, e14);
          null !== a18.destination && dd(a18, a18.destination);
        } catch (E6) {
          Y3(a18, E6), Tc(a18, E6);
        } finally {
          Lc = d12, Nc.current = c15, c15 === Mc && nc(b4);
        }
      }
    }
    function ed(a18, b4, c15) {
      c15.parentFlushed = true;
      switch (c15.status) {
        case 0:
          var d12 = c15.id = a18.nextSegmentId++;
          c15.lastPushedText = false;
          c15.textEmbedded = false;
          a18 = a18.responseState;
          r7(b4, Sa);
          r7(b4, a18.placeholderPrefix);
          a18 = d12.toString(16);
          r7(b4, a18);
          return w11(b4, Ta);
        case 1:
          c15.status = 2;
          var f15 = true;
          d12 = c15.chunks;
          var e14 = 0;
          c15 = c15.children;
          for (var g7 = 0; g7 < c15.length; g7++) {
            for (f15 = c15[g7]; e14 < f15.index; e14++) r7(b4, d12[e14]);
            f15 = fd(a18, b4, f15);
          }
          for (; e14 < d12.length - 1; e14++) r7(b4, d12[e14]);
          e14 < d12.length && (f15 = w11(b4, d12[e14]));
          return f15;
        default:
          throw Error("Aborted, errored or already flushed boundaries should not be flushed again. This is a bug in React.");
      }
    }
    function fd(a18, b4, c15) {
      var d12 = c15.boundary;
      if (null === d12) return ed(a18, b4, c15);
      d12.parentFlushed = true;
      if (d12.forceClientRender) d12 = d12.errorDigest, w11(b4, Xa), r7(b4, Za), d12 && (r7(b4, ab), r7(b4, F4(d12)), r7(b4, $a)), w11(b4, bb), ed(a18, b4, c15);
      else if (0 < d12.pendingTasks) {
        d12.rootSegmentID = a18.nextSegmentId++;
        0 < d12.completedSegments.length && a18.partialBoundaries.push(d12);
        var f15 = a18.responseState;
        var e14 = f15.nextSuspenseID++;
        f15 = x3(f15.boundaryPrefix + e14.toString(16));
        d12 = d12.id = f15;
        cb(b4, a18.responseState, d12);
        ed(a18, b4, c15);
      } else if (d12.byteSize > a18.progressiveChunkSize) d12.rootSegmentID = a18.nextSegmentId++, a18.completedBoundaries.push(d12), cb(b4, a18.responseState, d12.id), ed(a18, b4, c15);
      else {
        w11(b4, Ua);
        c15 = d12.completedSegments;
        if (1 !== c15.length) throw Error("A previously unvisited boundary must have exactly one root segment. This is a bug in React.");
        fd(a18, b4, c15[0]);
      }
      return w11(b4, Ya);
    }
    function gd(a18, b4, c15) {
      yb(b4, a18.responseState, c15.formatContext, c15.id);
      fd(a18, b4, c15);
      return zb(b4, c15.formatContext);
    }
    function hd(a18, b4, c15) {
      for (var d12 = c15.completedSegments, f15 = 0; f15 < d12.length; f15++) id3(a18, b4, c15, d12[f15]);
      d12.length = 0;
      a18 = a18.responseState;
      d12 = c15.id;
      c15 = c15.rootSegmentID;
      r7(b4, a18.startInlineScript);
      a18.sentCompleteBoundaryFunction ? r7(b4, Gb) : (a18.sentCompleteBoundaryFunction = true, r7(b4, Fb));
      if (null === d12) throw Error("An ID must have been assigned before we can complete the boundary.");
      c15 = c15.toString(16);
      r7(b4, d12);
      r7(b4, Hb);
      r7(b4, a18.segmentPrefix);
      r7(b4, c15);
      return w11(b4, Ib);
    }
    function id3(a18, b4, c15, d12) {
      if (2 === d12.status) return true;
      var f15 = d12.id;
      if (-1 === f15) {
        if (-1 === (d12.id = c15.rootSegmentID)) throw Error("A root segment ID must have been assigned by now. This is a bug in React.");
        return gd(a18, b4, d12);
      }
      gd(a18, b4, d12);
      a18 = a18.responseState;
      r7(b4, a18.startInlineScript);
      a18.sentCompleteSegmentFunction ? r7(b4, Bb) : (a18.sentCompleteSegmentFunction = true, r7(b4, Ab));
      r7(b4, a18.segmentPrefix);
      f15 = f15.toString(16);
      r7(b4, f15);
      r7(b4, Cb);
      r7(b4, a18.placeholderPrefix);
      r7(b4, f15);
      return w11(b4, Db);
    }
    function dd(a18, b4) {
      k5 = new Uint8Array(2048);
      l12 = 0;
      q2 = true;
      try {
        var c15 = a18.completedRootSegment;
        if (null !== c15 && 0 === a18.pendingRootTasks) {
          fd(a18, b4, c15);
          a18.completedRootSegment = null;
          var d12 = a18.responseState.bootstrapChunks;
          for (c15 = 0; c15 < d12.length - 1; c15++) r7(b4, d12[c15]);
          c15 < d12.length && w11(b4, d12[c15]);
        }
        var f15 = a18.clientRenderedBoundaries, e14;
        for (e14 = 0; e14 < f15.length; e14++) {
          var g7 = f15[e14];
          d12 = b4;
          var h6 = a18.responseState, m12 = g7.id, n13 = g7.errorDigest, p11 = g7.errorMessage, v5 = g7.errorComponentStack;
          r7(d12, h6.startInlineScript);
          h6.sentClientRenderFunction ? r7(d12, Kb) : (h6.sentClientRenderFunction = true, r7(d12, Jb));
          if (null === m12) throw Error("An ID must have been assigned before we can complete the boundary.");
          r7(d12, m12);
          r7(d12, Lb);
          if (n13 || p11 || v5) r7(d12, Nb), r7(d12, Pb(n13 || ""));
          if (p11 || v5) r7(d12, Nb), r7(d12, Pb(p11 || ""));
          v5 && (r7(d12, Nb), r7(d12, Pb(v5)));
          if (!w11(d12, Mb)) {
            a18.destination = null;
            e14++;
            f15.splice(0, e14);
            return;
          }
        }
        f15.splice(0, e14);
        var C5 = a18.completedBoundaries;
        for (e14 = 0; e14 < C5.length; e14++) if (!hd(a18, b4, C5[e14])) {
          a18.destination = null;
          e14++;
          C5.splice(0, e14);
          return;
        }
        C5.splice(0, e14);
        ca(b4);
        k5 = new Uint8Array(2048);
        l12 = 0;
        q2 = true;
        var D5 = a18.partialBoundaries;
        for (e14 = 0; e14 < D5.length; e14++) {
          var E6 = D5[e14];
          a: {
            f15 = a18;
            g7 = b4;
            var na = E6.completedSegments;
            for (h6 = 0; h6 < na.length; h6++) if (!id3(f15, g7, E6, na[h6])) {
              h6++;
              na.splice(0, h6);
              var Eb = false;
              break a;
            }
            na.splice(0, h6);
            Eb = true;
          }
          if (!Eb) {
            a18.destination = null;
            e14++;
            D5.splice(0, e14);
            return;
          }
        }
        D5.splice(0, e14);
        var oa = a18.completedBoundaries;
        for (e14 = 0; e14 < oa.length; e14++) if (!hd(a18, b4, oa[e14])) {
          a18.destination = null;
          e14++;
          oa.splice(0, e14);
          return;
        }
        oa.splice(0, e14);
      } finally {
        ca(b4), "function" === typeof b4.flush && b4.flush(), 0 === a18.allPendingTasks && 0 === a18.pingedTasks.length && 0 === a18.clientRenderedBoundaries.length && 0 === a18.completedBoundaries.length && b4.end();
      }
    }
    function jd(a18) {
      setImmediate(function() {
        return Qc(a18);
      });
    }
    function kd(a18, b4) {
      if (1 === a18.status) a18.status = 2, b4.destroy(a18.fatalError);
      else if (2 !== a18.status && null === a18.destination) {
        a18.destination = b4;
        try {
          dd(a18, b4);
        } catch (c15) {
          Y3(a18, c15), Tc(a18, c15);
        }
      }
    }
    function ld(a18, b4) {
      try {
        var c15 = a18.abortableTasks;
        c15.forEach(function(c16) {
          return cd(c16, a18, b4);
        });
        c15.clear();
        null !== a18.destination && dd(a18, a18.destination);
      } catch (d12) {
        Y3(a18, d12), Tc(a18, d12);
      }
    }
    function md(a18, b4) {
      return function() {
        return kd(b4, a18);
      };
    }
    function nd(a18, b4) {
      return function() {
        return ld(a18, b4);
      };
    }
    function od(a18, b4) {
      var c15 = b4 ? b4.identifierPrefix : void 0, d12 = b4 ? b4.nonce : void 0, f15 = b4 ? b4.bootstrapScriptContent : void 0, e14 = b4 ? b4.bootstrapScripts : void 0;
      var g7 = b4 ? b4.bootstrapModules : void 0;
      c15 = void 0 === c15 ? "" : c15;
      d12 = void 0 === d12 ? ra : x3('<script nonce="' + F4(d12) + '">');
      var h6 = [];
      void 0 !== f15 && h6.push(d12, ("" + f15).replace(wa, xa), sa);
      if (void 0 !== e14) for (f15 = 0; f15 < e14.length; f15++) h6.push(ta, F4(e14[f15]), va);
      if (void 0 !== g7) for (e14 = 0; e14 < g7.length; e14++) h6.push(ua, F4(g7[e14]), va);
      g7 = {
        bootstrapChunks: h6,
        startInlineScript: d12,
        placeholderPrefix: x3(c15 + "P:"),
        segmentPrefix: x3(c15 + "S:"),
        boundaryPrefix: c15 + "B:",
        idPrefix: c15,
        nextSuspenseID: 0,
        sentCompleteSegmentFunction: false,
        sentCompleteBoundaryFunction: false,
        sentClientRenderFunction: false
      };
      e14 = b4 ? b4.namespaceURI : void 0;
      e14 = G3("http://www.w3.org/2000/svg" === e14 ? 2 : "http://www.w3.org/1998/Math/MathML" === e14 ? 3 : 0, null);
      f15 = b4 ? b4.progressiveChunkSize : void 0;
      d12 = b4 ? b4.onError : void 0;
      h6 = b4 ? b4.onAllReady : void 0;
      var m12 = b4 ? b4.onShellReady : void 0, n13 = b4 ? b4.onShellError : void 0;
      b4 = [];
      c15 = /* @__PURE__ */ new Set();
      g7 = {
        destination: null,
        responseState: g7,
        progressiveChunkSize: void 0 === f15 ? 12800 : f15,
        status: 0,
        fatalError: null,
        nextSegmentId: 0,
        allPendingTasks: 0,
        pendingRootTasks: 0,
        completedRootSegment: null,
        abortableTasks: c15,
        pingedTasks: b4,
        clientRenderedBoundaries: [],
        completedBoundaries: [],
        partialBoundaries: [],
        onError: void 0 === d12 ? Oc : d12,
        onAllReady: void 0 === h6 ? X4 : h6,
        onShellReady: void 0 === m12 ? X4 : m12,
        onShellError: void 0 === n13 ? X4 : n13,
        onFatalError: X4
      };
      e14 = Sc(g7, 0, null, e14, false, false);
      e14.parentFlushed = true;
      a18 = Rc(g7, a18, null, e14, c15, hc, null, qc);
      b4.push(a18);
      return g7;
    }
    exports.renderToPipeableStream = function(a18, b4) {
      var c15 = od(a18, b4), d12 = false;
      jd(c15);
      return { pipe: function(a19) {
        if (d12) throw Error("React currently only supports piping to one writable stream.");
        d12 = true;
        kd(c15, a19);
        a19.on("drain", md(a19, c15));
        a19.on("error", nd(c15, Error("The destination stream errored while writing data.")));
        a19.on("close", nd(c15, Error("The destination stream closed early.")));
        return a19;
      }, abort: function(a19) {
        ld(c15, a19);
      } };
    };
    exports.version = "18.3.1";
  }
});

// node_modules/react-dom/server.node.js
var require_server_node = __commonJS({
  "node_modules/react-dom/server.node.js"(exports) {
    "use strict";
    var l12;
    var s36;
    if (true) {
      l12 = require_react_dom_server_legacy_node_production_min();
      s36 = require_react_dom_server_node_production_min();
    } else {
      l12 = null;
      s36 = null;
    }
    exports.version = l12.version;
    exports.renderToString = l12.renderToString;
    exports.renderToStaticMarkup = l12.renderToStaticMarkup;
    exports.renderToNodeStream = l12.renderToNodeStream;
    exports.renderToStaticNodeStream = l12.renderToStaticNodeStream;
    exports.renderToPipeableStream = s36.renderToPipeableStream;
  }
});

// node_modules/react-streaming/dist/cjs/utils/createErrorWithCleanStackTrace.js
var require_createErrorWithCleanStackTrace2 = __commonJS({
  "node_modules/react-streaming/dist/cjs/utils/createErrorWithCleanStackTrace.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createErrorWithCleanStackTrace = void 0;
    function createErrorWithCleanStackTrace2(errorMessage, numberOfStackTraceLinesToRemove2) {
      const err = new Error(errorMessage);
      err.stack = clean2(err.stack, numberOfStackTraceLinesToRemove2);
      return err;
    }
    exports.createErrorWithCleanStackTrace = createErrorWithCleanStackTrace2;
    function clean2(errStack, numberOfStackTraceLinesToRemove2) {
      if (!errStack) {
        return errStack;
      }
      const stackLines = splitByLine2(errStack);
      let linesRemoved = 0;
      const stackLine__cleaned = stackLines.filter((line) => {
        if (line.includes(" (internal/") || line.includes(" (node:internal")) {
          return false;
        }
        if (linesRemoved < numberOfStackTraceLinesToRemove2 && isStackTraceLine2(line)) {
          linesRemoved++;
          return false;
        }
        return true;
      }).join("\n");
      return stackLine__cleaned;
    }
    function isStackTraceLine2(line) {
      return line.startsWith("    at ");
    }
    function splitByLine2(str) {
      return str.split(/\r?\n/);
    }
  }
});

// node_modules/react-streaming/dist/cjs/utils/getGlobalObject.js
var require_getGlobalObject = __commonJS({
  "node_modules/react-streaming/dist/cjs/utils/getGlobalObject.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.getGlobalObject = void 0;
    function getGlobalObject3(key, defaultValue2) {
      const globalObjectsAll = globalThis[projectKey3] = globalThis[projectKey3] || {};
      const globalObject12 = globalObjectsAll[key] = globalObjectsAll[key] || defaultValue2;
      return globalObject12;
    }
    exports.getGlobalObject = getGlobalObject3;
    var projectKey3 = "_react_streaming";
  }
});

// node_modules/react-streaming/dist/cjs/utils/PROJECT_VERSION.js
var require_PROJECT_VERSION2 = __commonJS({
  "node_modules/react-streaming/dist/cjs/utils/PROJECT_VERSION.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.PROJECT_VERSION = void 0;
    exports.PROJECT_VERSION = "0.3.44";
  }
});

// node_modules/react-streaming/dist/cjs/utils/projectInfo.js
var require_projectInfo2 = __commonJS({
  "node_modules/react-streaming/dist/cjs/utils/projectInfo.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.projectInfo = void 0;
    var getGlobalObject_1 = require_getGlobalObject();
    var PROJECT_VERSION_1 = require_PROJECT_VERSION2();
    var projectInfo2 = {
      projectName: "react-streaming",
      projectVersion: PROJECT_VERSION_1.PROJECT_VERSION,
      npmPackageName: "react-streaming",
      githubRepository: "https://github.com/brillout/react-streaming"
    };
    exports.projectInfo = projectInfo2;
    var { versions } = (0, getGlobalObject_1.getGlobalObject)("projectInfo.ts", {
      versions: /* @__PURE__ */ new Set()
    });
    versions.add(projectInfo2.projectVersion);
    if (versions.size >= 2) {
      const versionsStr = Array.from(versions).map((v5) => `${projectInfo2.projectName}@${v5}`).join(" and ");
      throw new Error(`${versionsStr} loaded but loading different versions is forbidden`);
    }
  }
});

// node_modules/react-streaming/dist/cjs/utils/assert.js
var require_assert2 = __commonJS({
  "node_modules/react-streaming/dist/cjs/utils/assert.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.getProjectError = exports.assertInfo = exports.assertWarning = exports.assertUsage = exports.assert = void 0;
    var createErrorWithCleanStackTrace_1 = require_createErrorWithCleanStackTrace2();
    var projectInfo_1 = require_projectInfo2();
    var errorPrefix = `[${projectInfo_1.projectInfo.npmPackageName}@${projectInfo_1.projectInfo.projectVersion}]`;
    var internalErrorPrefix = `${errorPrefix}[Bug]`;
    var usageErrorPrefix = `${errorPrefix}[Wrong Usage]`;
    var warningPrefix = `${errorPrefix}[Warning]`;
    var infoPrefix = `${errorPrefix}[Info]`;
    var numberOfStackTraceLinesToRemove2 = 2;
    function assert2(condition2, debugInfo) {
      if (condition2) {
        return;
      }
      const debugStr = (() => {
        if (!debugInfo) {
          return "";
        }
        const debugInfoSerialized = typeof debugInfo === "string" ? debugInfo : "`" + JSON.stringify(debugInfo) + "`";
        return `Debug info (this is for the ${projectInfo_1.projectInfo.projectName} maintainers; you can ignore this): ${debugInfoSerialized}.`;
      })();
      const internalError = (0, createErrorWithCleanStackTrace_1.createErrorWithCleanStackTrace)([
        `${internalErrorPrefix} You stumbled upon a bug in ${projectInfo_1.projectInfo.projectName}'s source code.`,
        `Reach out at ${projectInfo_1.projectInfo.githubRepository}/issues/new and include this error stack (the error stack is usually enough to fix the problem).`,
        "A maintainer will fix the bug (usually under 24 hours).",
        `Do not hesitate to reach out as it makes ${projectInfo_1.projectInfo.projectName} more robust.`,
        debugStr
      ].join(" "), numberOfStackTraceLinesToRemove2);
      throw internalError;
    }
    exports.assert = assert2;
    function assertUsage3(condition2, errorMessage) {
      if (condition2) {
        return;
      }
      const whiteSpace = errorMessage.startsWith("[") ? "" : " ";
      const usageError = (0, createErrorWithCleanStackTrace_1.createErrorWithCleanStackTrace)(`${usageErrorPrefix}${whiteSpace}${errorMessage}`, numberOfStackTraceLinesToRemove2);
      throw usageError;
    }
    exports.assertUsage = assertUsage3;
    function getProjectError2(errorMessage) {
      const pluginError = (0, createErrorWithCleanStackTrace_1.createErrorWithCleanStackTrace)(`${errorPrefix} ${errorMessage}`, numberOfStackTraceLinesToRemove2);
      return pluginError;
    }
    exports.getProjectError = getProjectError2;
    var alreadyLogged = /* @__PURE__ */ new Set();
    function assertWarning3(condition2, errorMessage, { onlyOnce, showStackTrace }) {
      if (condition2) {
        return;
      }
      const msg = `${warningPrefix} ${errorMessage}`;
      if (onlyOnce) {
        const key = onlyOnce === true ? msg : onlyOnce;
        if (alreadyLogged.has(key)) {
          return;
        } else {
          alreadyLogged.add(key);
        }
      }
      if (showStackTrace) {
        console.warn(new Error(msg));
      } else {
        console.warn(msg);
      }
    }
    exports.assertWarning = assertWarning3;
    function assertInfo2(condition2, errorMessage, { onlyOnce }) {
      if (condition2) {
        return;
      }
      const msg = `${infoPrefix} ${errorMessage}`;
      if (onlyOnce) {
        const key = msg;
        if (alreadyLogged.has(key)) {
          return;
        } else {
          alreadyLogged.add(key);
        }
      }
      console.log(msg);
    }
    exports.assertInfo = assertInfo2;
  }
});

// node_modules/react-streaming/dist/cjs/utils/isBrowser.js
var require_isBrowser = __commonJS({
  "node_modules/react-streaming/dist/cjs/utils/isBrowser.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.isBrowser = void 0;
    function isBrowser3() {
      return typeof window !== "undefined" && typeof window.scrollY === "number";
    }
    exports.isBrowser = isBrowser3;
  }
});

// node_modules/react-streaming/dist/cjs/server/useAsync/useSuspenseData.js
var require_useSuspenseData = __commonJS({
  "node_modules/react-streaming/dist/cjs/server/useAsync/useSuspenseData.js"(exports) {
    "use strict";
    var __createBinding2 = exports && exports.__createBinding || (Object.create ? function(o8, m12, k5, k22) {
      if (k22 === void 0) k22 = k5;
      var desc = Object.getOwnPropertyDescriptor(m12, k5);
      if (!desc || ("get" in desc ? !m12.__esModule : desc.writable || desc.configurable)) {
        desc = { enumerable: true, get: function() {
          return m12[k5];
        } };
      }
      Object.defineProperty(o8, k22, desc);
    } : function(o8, m12, k5, k22) {
      if (k22 === void 0) k22 = k5;
      o8[k22] = m12[k5];
    });
    var __setModuleDefault2 = exports && exports.__setModuleDefault || (Object.create ? function(o8, v5) {
      Object.defineProperty(o8, "default", { enumerable: true, value: v5 });
    } : function(o8, v5) {
      o8["default"] = v5;
    });
    var __importStar2 = exports && exports.__importStar || function(mod) {
      if (mod && mod.__esModule) return mod;
      var result = {};
      if (mod != null) {
        for (var k5 in mod) if (k5 !== "default" && Object.prototype.hasOwnProperty.call(mod, k5)) __createBinding2(result, mod, k5);
      }
      __setModuleDefault2(result, mod);
      return result;
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.SuspenseData = exports.useSuspenseData = void 0;
    var react_1 = __importStar2(require_react());
    var ctxSuspenses = (0, react_1.createContext)(void 0);
    function SuspenseData({ children }) {
      const suspenses = {};
      return react_1.default.createElement(ctxSuspenses.Provider, { value: suspenses }, children);
    }
    exports.SuspenseData = SuspenseData;
    function useSuspenseData() {
      const suspenses = (0, react_1.useContext)(ctxSuspenses);
      return suspenses;
    }
    exports.useSuspenseData = useSuspenseData;
  }
});

// node_modules/react-streaming/dist/cjs/utils/isCallable.js
var require_isCallable2 = __commonJS({
  "node_modules/react-streaming/dist/cjs/utils/isCallable.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.isCallable = void 0;
    function isCallable3(thing) {
      return thing instanceof Function || typeof thing === "function";
    }
    exports.isCallable = isCallable3;
  }
});

// node_modules/react-streaming/dist/cjs/utils/objectAssign.js
var require_objectAssign = __commonJS({
  "node_modules/react-streaming/dist/cjs/utils/objectAssign.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.objectAssign = void 0;
    function objectAssign2(obj, objAddendum) {
      Object.assign(obj, objAddendum);
    }
    exports.objectAssign = objectAssign2;
  }
});

// node_modules/react-streaming/dist/cjs/utils/debug.js
var require_debug2 = __commonJS({
  "node_modules/react-streaming/dist/cjs/utils/debug.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.isDebugEnabled = exports.createDebugger = void 0;
    var isBrowser_1 = require_isBrowser();
    var isCallable_1 = require_isCallable2();
    var objectAssign_1 = require_objectAssign();
    var assert_1 = require_assert2();
    (0, assert_1.assert)(!(0, isBrowser_1.isBrowser)(), "utils/debug.js loaded in browser");
    globalThis.__brillout_debug_createDebugger = createDebugger2;
    function createDebugger2(namespace, optionsGlobal) {
      const debugWithOptions = (options) => {
        return (msg, info) => {
          if (!isDebugEnabled(namespace))
            return;
          if (info !== void 0) {
            msg += strInfo(info, Object.assign(Object.assign({}, optionsGlobal), options));
          }
          console.log("\x1B[1m%s\x1B[0m", namespace, msg);
        };
      };
      const debug6 = (msg, info) => debugWithOptions({})(msg, info);
      (0, objectAssign_1.objectAssign)(debug6, { options: debugWithOptions, isEnabled: isDebugEnabled(namespace) });
      return debug6;
    }
    exports.createDebugger = createDebugger2;
    function isDebugEnabled(namespace) {
      var _a;
      let DEBUG;
      try {
        DEBUG = process.env.DEBUG;
      } catch (_b) {
      }
      return (_a = DEBUG === null || DEBUG === void 0 ? void 0 : DEBUG.includes(namespace)) !== null && _a !== void 0 ? _a : false;
    }
    exports.isDebugEnabled = isDebugEnabled;
    function strInfo(info, options) {
      var _a, _b;
      if (info === void 0) {
        return void 0;
      }
      let str = "\n";
      if (typeof info === "string") {
        str += info;
      } else if (Array.isArray(info)) {
        if (info.length === 0) {
          str += (_b = (_a = options.serialization) === null || _a === void 0 ? void 0 : _a.emptyArray) !== null && _b !== void 0 ? _b : "[]";
        } else {
          str += info.map(strUnknown2).join("\n");
        }
      } else {
        str += strUnknown2(info);
      }
      str = pad2(str);
      return str;
    }
    function pad2(str) {
      const PADDING = "     ";
      const terminalWidth = getTerminalWidth2();
      const lines = [];
      str.split("\n").forEach((line) => {
        if (!terminalWidth) {
          lines.push(line);
        } else {
          chunk2(line, terminalWidth - PADDING.length).forEach((chunk3) => {
            lines.push(chunk3);
          });
        }
      });
      return lines.join("\n" + PADDING);
    }
    function chunk2(str, size4) {
      if (str.length <= size4) {
        return [str];
      }
      const chunks = str.match(new RegExp(".{1," + size4 + "}", "g"));
      (0, assert_1.assert)(chunks);
      return chunks;
    }
    function strUnknown2(thing) {
      return typeof thing === "string" ? thing : strObj2(thing);
    }
    function strObj2(obj, newLines = false) {
      return JSON.stringify(obj, replaceFunctionSerializer2, newLines ? 2 : void 0);
    }
    function replaceFunctionSerializer2(_key, value) {
      if ((0, isCallable_1.isCallable)(value)) {
        return value.toString().split(/\s+/).join(" ");
      }
      return value;
    }
    function getTerminalWidth2() {
      return typeof process !== "undefined" && typeof process.stdout !== "undefined" && process.stdout.columns || void 0;
    }
  }
});

// node_modules/react-streaming/dist/cjs/utils/isPromise.js
var require_isPromise = __commonJS({
  "node_modules/react-streaming/dist/cjs/utils/isPromise.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.isPromise = void 0;
    var isCallable_1 = require_isCallable2();
    function isPromise2(val) {
      return typeof val === "object" && val !== null && "then" in val && (0, isCallable_1.isCallable)(val.then);
    }
    exports.isPromise = isPromise2;
  }
});

// node_modules/react-streaming/dist/cjs/utils/isVikeReactApp.js
var require_isVikeReactApp = __commonJS({
  "node_modules/react-streaming/dist/cjs/utils/isVikeReactApp.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.isVikeReactApp = void 0;
    function isVikeReactApp2() {
      const g7 = globalThis;
      return !!g7._isVikeReactApp;
    }
    exports.isVikeReactApp = isVikeReactApp2;
  }
});

// node_modules/react-streaming/dist/cjs/server/utils.js
var require_utils4 = __commonJS({
  "node_modules/react-streaming/dist/cjs/server/utils.js"(exports) {
    "use strict";
    var __createBinding2 = exports && exports.__createBinding || (Object.create ? function(o8, m12, k5, k22) {
      if (k22 === void 0) k22 = k5;
      var desc = Object.getOwnPropertyDescriptor(m12, k5);
      if (!desc || ("get" in desc ? !m12.__esModule : desc.writable || desc.configurable)) {
        desc = { enumerable: true, get: function() {
          return m12[k5];
        } };
      }
      Object.defineProperty(o8, k22, desc);
    } : function(o8, m12, k5, k22) {
      if (k22 === void 0) k22 = k5;
      o8[k22] = m12[k5];
    });
    var __exportStar2 = exports && exports.__exportStar || function(m12, exports2) {
      for (var p11 in m12) if (p11 !== "default" && !Object.prototype.hasOwnProperty.call(exports2, p11)) __createBinding2(exports2, m12, p11);
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    __exportStar2(require_assert2(), exports);
    __exportStar2(require_debug2(), exports);
    __exportStar2(require_isPromise(), exports);
    __exportStar2(require_isBrowser(), exports);
    __exportStar2(require_getGlobalObject(), exports);
    __exportStar2(require_isVikeReactApp(), exports);
  }
});

// node_modules/react-streaming/dist/cjs/server/useStream.js
var require_useStream = __commonJS({
  "node_modules/react-streaming/dist/cjs/server/useStream.js"(exports) {
    "use strict";
    var __createBinding2 = exports && exports.__createBinding || (Object.create ? function(o8, m12, k5, k22) {
      if (k22 === void 0) k22 = k5;
      var desc = Object.getOwnPropertyDescriptor(m12, k5);
      if (!desc || ("get" in desc ? !m12.__esModule : desc.writable || desc.configurable)) {
        desc = { enumerable: true, get: function() {
          return m12[k5];
        } };
      }
      Object.defineProperty(o8, k22, desc);
    } : function(o8, m12, k5, k22) {
      if (k22 === void 0) k22 = k5;
      o8[k22] = m12[k5];
    });
    var __setModuleDefault2 = exports && exports.__setModuleDefault || (Object.create ? function(o8, v5) {
      Object.defineProperty(o8, "default", { enumerable: true, value: v5 });
    } : function(o8, v5) {
      o8["default"] = v5;
    });
    var __importStar2 = exports && exports.__importStar || function(mod) {
      if (mod && mod.__esModule) return mod;
      var result = {};
      if (mod != null) {
        for (var k5 in mod) if (k5 !== "default" && Object.prototype.hasOwnProperty.call(mod, k5)) __createBinding2(result, mod, k5);
      }
      __setModuleDefault2(result, mod);
      return result;
    };
    var __importDefault2 = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { "default": mod };
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.StreamProvider = exports.useStream = void 0;
    var picocolors_1 = __importDefault2(require_picocolors());
    var react_1 = __importStar2(require_react());
    var utils_1 = require_utils4();
    var globalObject12 = (0, utils_1.getGlobalObject)("useStream.ts", {
      StreamContext: react_1.default.createContext(null)
    });
    var StreamProvider = globalObject12.StreamContext.Provider;
    exports.StreamProvider = StreamProvider;
    function useStream() {
      const streamUtils = (0, react_1.useContext)(globalObject12.StreamContext);
      (0, utils_1.assertUsage)(streamUtils, getErrMsg2());
      return streamUtils;
    }
    exports.useStream = useStream;
    function getErrMsg2() {
      if ((0, utils_1.isVikeReactApp)()) {
        return `HTML streaming (https://vike.dev/streaming) disabled: set the setting ${picocolors_1.default.code("stream")} (https://vike.dev/stream) to ${picocolors_1.default.code("true")}.'`;
      } else {
        return `react-streaming (https://github.com/brillout/react-streaming) isn't installed: make sure to use ${picocolors_1.default.code("renderToStream()")} to render your root React component, see https://github.com/brillout/react-streaming#get-started`;
      }
    }
  }
});

// node_modules/isbot-fast/index.js
var require_isbot_fast = __commonJS({
  "node_modules/isbot-fast/index.js"(exports, module) {
    var bots = [
      // generic
      "bot",
      // googlebot, bingbot, telegrambot, twitterbot, yandexbot, etc.
      "check",
      "cloud",
      // cloudflare, cloudinary, etc.
      "crawler",
      "download",
      "monitor",
      // monitor & monitoring
      "preview",
      // skypeuripreview, bingpreview, yahoo link preview, etc.
      "scan",
      "spider",
      // baiduspider, 360spider, screaming frog seo spider, etc.
      // search engines
      "google",
      "qwantify",
      "yahoo",
      // aggregators, messengers and social networks
      "facebookexternalhit",
      "flipboard",
      "tumblr",
      "vkshare",
      "whatsapp",
      // downloaders
      "curl",
      "perl",
      "python",
      "wget",
      // high activity scanners
      "heritrix",
      "ia_archiver"
    ];
    var createRegex = () => new RegExp(`(${bots.join("|")})`, "i");
    var isBotRegex = createRegex();
    module.exports = (userAgent) => isBotRegex.test(userAgent);
    module.exports.extend = (additionalBots) => {
      bots = [...new Set(bots.concat(additionalBots))];
      isBotRegex = createRegex();
    };
  }
});

// node_modules/react-streaming/dist/cjs/server/renderToStream/resolveSeoStrategy.js
var require_resolveSeoStrategy = __commonJS({
  "node_modules/react-streaming/dist/cjs/server/renderToStream/resolveSeoStrategy.js"(exports) {
    "use strict";
    var __importDefault2 = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { "default": mod };
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.resolveSeoStrategy = void 0;
    var isbot_fast_1 = __importDefault2(require_isbot_fast());
    var picocolors_1 = __importDefault2(require_picocolors());
    var utils_1 = require_utils4();
    function resolveSeoStrategy(options = {}) {
      const seoStrategy = options.seoStrategy || "conservative";
      if (!options.userAgent) {
        showWarning();
        return { disableStream: true };
      }
      if (!(0, isbot_fast_1.default)(options.userAgent)) {
        return { disableStream: false };
      }
      const isGoogleBot = options.userAgent.toLowerCase().includes("googlebot");
      if (seoStrategy === "google-speed" && isGoogleBot) {
        return { disableStream: false };
      }
      return { disableStream: true };
    }
    exports.resolveSeoStrategy = resolveSeoStrategy;
    function showWarning() {
      const isVikeReact = (0, utils_1.isVikeReactApp)();
      const link2 = isVikeReact ? "https://vike.dev/streaming" : "https://github.com/brillout/react-streaming";
      const help = isVikeReact ? `the HTTP request headers with ${picocolors_1.default.code("renderPage({ headersOriginal })")}` : picocolors_1.default.code("options.userAgent");
      const errMsg = [
        `HTML streaming (${link2}) disabled because the User-Agent request header is unknown: make sure to provide`,
        `${help}.`,
        "The HTTP User-Agent request header is needed so that HTML streaming can automatically be disabled for bots and crawlers, see https://github.com/brillout/react-streaming#bots for more information."
      ];
      if (!isVikeReact) {
        errMsg.push(`Or set ${picocolors_1.default.code("options.disable")} to ${picocolors_1.default.code("true")} to suppress this warning.`);
      }
      (0, utils_1.assertWarning)(false, errMsg.join(" "), { onlyOnce: true });
    }
  }
});

// node_modules/react-streaming/dist/cjs/server/renderToStream/common.js
var require_common = __commonJS({
  "node_modules/react-streaming/dist/cjs/server/renderToStream/common.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.wrapStreamEnd = exports.assertReactImport = exports.afterReactBugCatch = exports.debugFlow = void 0;
    var utils_1 = require_utils4();
    exports.debugFlow = (0, utils_1.createDebugger)("react-streaming:flow");
    function afterReactBugCatch(fn) {
      setTimeout(() => {
        fn();
      }, 0);
    }
    exports.afterReactBugCatch = afterReactBugCatch;
    function assertReactImport(fn, fnName) {
      (0, utils_1.assert)(typeof fn === "function");
      (0, utils_1.assertUsage)(fn, `Couldn't import ${fnName}() from 'react-dom'`);
    }
    exports.assertReactImport = assertReactImport;
    function wrapStreamEnd(streamEnd, didError) {
      return streamEnd.then(() => new Promise((r7) => setTimeout(r7, 0))).then(() => !didError);
    }
    exports.wrapStreamEnd = wrapStreamEnd;
  }
});

// node_modules/react-streaming/dist/cjs/server/renderToStream.js
var require_renderToStream = __commonJS({
  "node_modules/react-streaming/dist/cjs/server/renderToStream.js"(exports) {
    "use strict";
    var __createBinding2 = exports && exports.__createBinding || (Object.create ? function(o8, m12, k5, k22) {
      if (k22 === void 0) k22 = k5;
      var desc = Object.getOwnPropertyDescriptor(m12, k5);
      if (!desc || ("get" in desc ? !m12.__esModule : desc.writable || desc.configurable)) {
        desc = { enumerable: true, get: function() {
          return m12[k5];
        } };
      }
      Object.defineProperty(o8, k22, desc);
    } : function(o8, m12, k5, k22) {
      if (k22 === void 0) k22 = k5;
      o8[k22] = m12[k5];
    });
    var __setModuleDefault2 = exports && exports.__setModuleDefault || (Object.create ? function(o8, v5) {
      Object.defineProperty(o8, "default", { enumerable: true, value: v5 });
    } : function(o8, v5) {
      o8["default"] = v5;
    });
    var __importStar2 = exports && exports.__importStar || function(mod) {
      if (mod && mod.__esModule) return mod;
      var result = {};
      if (mod != null) {
        for (var k5 in mod) if (k5 !== "default" && Object.prototype.hasOwnProperty.call(mod, k5)) __createBinding2(result, mod, k5);
      }
      __setModuleDefault2(result, mod);
      return result;
    };
    var __importDefault2 = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { "default": mod };
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.renderToWebStream_set = exports.renderToNodeStream_set = exports.disable = exports.renderToStream = void 0;
    var react_1 = __importDefault2(require_react());
    var server_1 = __importStar2(require_server_node());
    var useSuspenseData_1 = require_useSuspenseData();
    var useStream_1 = require_useStream();
    var resolveSeoStrategy_1 = require_resolveSeoStrategy();
    var utils_1 = require_utils4();
    var common_1 = require_common();
    var globalObject12 = (0, utils_1.getGlobalObject)("renderToStream.ts", {
      renderToNodeStream: null,
      renderToWebStream: null
    });
    assertReact();
    var globalConfig = globalThis.__react_streaming = globalThis.__react_streaming || {
      disable: false
    };
    function disable() {
      globalConfig.disable = true;
    }
    exports.disable = disable;
    async function renderToStream2(element, options = {}) {
      var _a, _b;
      (0, utils_1.assertUsage)(!options.renderToPipeableStream && !options.renderToReadableStream, "using deprecated options");
      element = react_1.default.createElement(useSuspenseData_1.SuspenseData, null, element);
      const buffer = [];
      let injectToStream = async (chunk2) => {
        buffer.push(chunk2);
      };
      const doNotClosePromise = { promise: null };
      let doNotCloseTimeout = null;
      const doNotClose = () => {
        let resolve;
        doNotClosePromise.promise = new Promise((r7) => resolve = r7);
        if (doNotCloseTimeout)
          clearTimeout(doNotCloseTimeout);
        doNotCloseTimeout = setTimeout(() => {
          (0, utils_1.assertUsage)(false, "makeClosableAgain() not called after 10 seconds (`const makeClosableAgain = stream.doNotClose()`)");
        }, 10 * 1e3);
        const makeClosableAgain = () => {
          resolve();
          clearTimeout(doNotCloseTimeout);
        };
        return makeClosableAgain;
      };
      let abortFn;
      const setAbortFn = (fn) => abortFn = fn;
      const streamTimeout = (() => {
        var _a2;
        if (options.timeout === null)
          return null;
        return setTimeout(() => {
          var _a3;
          (0, utils_1.assert)(abortFn);
          abortFn();
          (_a3 = options.onTimeout) === null || _a3 === void 0 ? void 0 : _a3.call(options);
        }, ((_a2 = options.timeout) !== null && _a2 !== void 0 ? _a2 : 20) * 1e3);
      })();
      const clearTimeouts = () => {
        if (streamTimeout !== null)
          clearTimeout(streamTimeout);
        if (doNotCloseTimeout !== null)
          clearTimeout(doNotCloseTimeout);
      };
      let hasStreamEnded = () => false;
      element = react_1.default.createElement(useStream_1.StreamProvider, {
        value: {
          injectToStream: (chunk2, options2) => injectToStream(chunk2, options2),
          hasStreamEnded: () => hasStreamEnded(),
          doNotClose
        }
      }, element);
      const disable2 = globalConfig.disable || ((_a = options.disable) !== null && _a !== void 0 ? _a : (0, resolveSeoStrategy_1.resolveSeoStrategy)(options).disableStream);
      const webStream = (_b = options.webStream) !== null && _b !== void 0 ? _b : !globalObject12.renderToNodeStream;
      (0, common_1.debugFlow)(`disable === ${disable2} && webStream === ${webStream}`);
      let ret;
      const retCommon = { disabled: disable2, doNotClose };
      if (!webStream) {
        ret = Object.assign(Object.assign({}, retCommon), await globalObject12.renderToNodeStream(element, disable2, options, doNotClosePromise, setAbortFn, clearTimeouts));
      } else {
        (0, utils_1.assert)(globalObject12.renderToWebStream);
        ret = Object.assign(Object.assign({}, retCommon), await globalObject12.renderToWebStream(element, disable2, options, doNotClosePromise, setAbortFn, clearTimeouts));
      }
      injectToStream = ret.injectToStream;
      buffer.forEach((chunk2) => injectToStream(chunk2));
      buffer.length = 0;
      hasStreamEnded = ret.hasStreamEnded;
      (0, common_1.debugFlow)("promise `await renderToStream()` resolved");
      return ret;
    }
    exports.renderToStream = renderToStream2;
    function renderToNodeStream_set(renderToNodeStream) {
      globalObject12.renderToNodeStream = renderToNodeStream;
    }
    exports.renderToNodeStream_set = renderToNodeStream_set;
    function renderToWebStream_set(renderToWebStream) {
      globalObject12.renderToWebStream = renderToWebStream;
    }
    exports.renderToWebStream_set = renderToWebStream_set;
    function assertReact() {
      const versionMajor = parseInt(server_1.version.split(".")[0], 10);
      (0, utils_1.assertUsage)(versionMajor >= 18, `\`react-dom@${server_1.version}\` was loaded, but react-streaming only works with React version 18 or greater.`);
      (0, utils_1.assert)(typeof server_1.default.renderToPipeableStream === "function" || typeof server_1.default.renderToReadableStream === "function");
    }
  }
});

// node_modules/react-streaming/dist/cjs/server/index.js
var require_server = __commonJS({
  "node_modules/react-streaming/dist/cjs/server/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.disable = exports.renderToStream = void 0;
    var assert_1 = require_assert2();
    var isBrowser_1 = require_isBrowser();
    (0, assert_1.assertUsage)(!(0, isBrowser_1.isBrowser)(), "The file node_modules/react-streaming/dist/{esm,cjs}/server/index.js is loaded in the browser but it shouldn't. Make sure to never `import { something } from 'react-streaming/server'` in code that runs on the client-side. Also make sure your bundler picks the right node_modules/react-streaming/package.json#exports entries.");
    var renderToStream_1 = require_renderToStream();
    Object.defineProperty(exports, "renderToStream", { enumerable: true, get: function() {
      return renderToStream_1.renderToStream;
    } });
    Object.defineProperty(exports, "disable", { enumerable: true, get: function() {
      return renderToStream_1.disable;
    } });
  }
});

// node_modules/react-streaming/dist/cjs/server/renderToStream/orchestrateChunks.js
var require_orchestrateChunks = __commonJS({
  "node_modules/react-streaming/dist/cjs/server/renderToStream/orchestrateChunks.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.orchestrateChunks = void 0;
    var utils_1 = require_utils4();
    var debug6 = (0, utils_1.createDebugger)("react-streaming:chunks");
    function orchestrateChunks(streamOperations, doNotClosePromise) {
      let hasEnded = false;
      let lastWritePromise = null;
      let firstReactWritePromise_resolve;
      let firstReactWritePromise = new Promise((resolve) => {
        firstReactWritePromise_resolve = () => {
          if (firstReactWritePromise === null)
            return;
          firstReactWritePromise = null;
          resolve();
        };
      });
      let isFirstReactWrite = true;
      return { injectToStream, onReactWrite, onBeforeEnd, hasStreamEnded: () => hasEnded };
      function injectToStream(chunk2, options) {
        if (debug6.isEnabled)
          debug6("injectToStream()", getChunkAsString(chunk2));
        if (hasEnded) {
          (0, utils_1.assertUsage)(false, `Cannot inject the following chunk because the stream has already ended. Consider using the doNotClose() and hasStreamEnded() utilities. The chunk:
${getChunkAsString(chunk2)}`);
        }
        writeChunkInSequence(chunk2, options === null || options === void 0 ? void 0 : options.flush);
      }
      function writeChunkInSequence(chunk2, flush) {
        const lastWritePromisePrevious = lastWritePromise;
        lastWritePromise = (async () => {
          if (firstReactWritePromise)
            await firstReactWritePromise;
          if (lastWritePromisePrevious)
            await lastWritePromisePrevious;
          if ((0, utils_1.isPromise)(chunk2))
            chunk2 = await chunk2;
          writeChunkNow(chunk2, flush);
        })();
      }
      function writeChunkNow(chunk2, flush) {
        (0, utils_1.assert)(!hasEnded);
        (0, utils_1.assert)(streamOperations.operations);
        streamOperations.operations.writeChunk(chunk2);
        if (debug6.isEnabled)
          debug6(">>> WRITE", getChunkAsString(chunk2));
        if (flush && streamOperations.operations.flush !== null) {
          streamOperations.operations.flush();
          debug6(">>> FLUSH");
        }
      }
      function onReactWrite(chunk2) {
        if (debug6.isEnabled)
          debug6("onReactWrite()", getChunkAsString(chunk2));
        (0, utils_1.assert)(!hasEnded);
        const flush = true;
        if (isFirstReactWrite) {
          debug6(">>> START");
          writeChunkNow(chunk2, flush);
          setTimeout(() => {
            isFirstReactWrite = false;
            firstReactWritePromise_resolve();
          }, 0);
        } else {
          writeChunkInSequence(chunk2, flush);
        }
      }
      async function onBeforeEnd() {
        firstReactWritePromise_resolve();
        await new Promise((r7) => setTimeout(r7, 0));
        await doNotClosePromise.promise;
        await lastWritePromise;
        hasEnded = true;
        debug6(">>> END");
      }
    }
    exports.orchestrateChunks = orchestrateChunks;
    function getChunkAsString(chunk2) {
      try {
        return new TextDecoder().decode(chunk2);
      } catch (err) {
        return String(chunk2);
      }
    }
  }
});

// node_modules/react-streaming/dist/cjs/server/renderToStream/createPipeWrapper.js
var require_createPipeWrapper = __commonJS({
  "node_modules/react-streaming/dist/cjs/server/renderToStream/createPipeWrapper.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createPipeWrapper = void 0;
    var utils_1 = require_utils4();
    var orchestrateChunks_1 = require_orchestrateChunks();
    var debug6 = (0, utils_1.createDebugger)("react-streaming:createPipeWrapper");
    var stream_1 = __require("stream");
    async function createPipeWrapper(pipeFromReact, onReactBug, clearTimeouts, doNotClosePromise) {
      const { pipeForUser, streamEnd } = createPipeForUser();
      const streamOperations = {
        operations: null
      };
      const { injectToStream, onReactWrite, onBeforeEnd, hasStreamEnded } = (0, orchestrateChunks_1.orchestrateChunks)(streamOperations, doNotClosePromise);
      return { pipeForUser, streamEnd, injectToStream, hasStreamEnded };
      function createPipeForUser() {
        debug6("createPipeForUser()");
        let onEnded;
        const streamEnd2 = new Promise((r7) => {
          onEnded = () => r7();
        });
        const pipeForUser2 = (writableFromUser) => {
          const writableForReact = new stream_1.Writable({
            write(chunk2, _encoding, callback) {
              debug6("write");
              if (!writableFromUser.destroyed) {
                onReactWrite(chunk2);
              } else {
                writableForReact.destroy();
              }
              callback();
            },
            async final(callback) {
              debug6("final");
              clearTimeouts();
              await onBeforeEnd();
              writableFromUser.end();
              onEnded();
              callback();
            },
            destroy(err) {
              debug6(`destroy (\`!!err === ${!!err}\`)`);
              clearTimeouts();
              if (err)
                onReactBug(err);
              writableFromUser.destroy(err !== null && err !== void 0 ? err : void 0);
              onEnded();
            }
          });
          const flush = () => {
            if (typeof writableFromUser.flush === "function") {
              ;
              writableFromUser.flush();
              debug6("stream flushed (Node.js Writable)");
            }
          };
          streamOperations.operations = {
            flush,
            writeChunk(chunk2) {
              writableFromUser.write(chunk2);
            }
          };
          writableForReact.flush = flush;
          pipeFromReact(writableForReact);
        };
        return { pipeForUser: pipeForUser2, streamEnd: streamEnd2 };
      }
    }
    exports.createPipeWrapper = createPipeWrapper;
  }
});

// node_modules/react-streaming/dist/cjs/server/renderToStream/renderToNodeStream.js
var require_renderToNodeStream = __commonJS({
  "node_modules/react-streaming/dist/cjs/server/renderToStream/renderToNodeStream.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.renderToNodeStream = void 0;
    var server_node_1 = require_server_node();
    var createPipeWrapper_1 = require_createPipeWrapper();
    var common_1 = require_common();
    async function renderToNodeStream(element, disable, options, doNotClosePromise, setAbortFn, clearTimeouts) {
      var _a;
      (0, common_1.debugFlow)("creating Node.js Stream Pipe");
      let onAllReady;
      const allReady = new Promise((r7) => {
        onAllReady = () => r7();
      });
      let onShellReady;
      const shellReady = new Promise((r7) => {
        onShellReady = () => r7();
      });
      let didError = false;
      let firstErr = null;
      let reactBug = null;
      const onError = (err) => {
        (0, common_1.debugFlow)("[react] onError() / onShellError()");
        didError = true;
        firstErr !== null && firstErr !== void 0 ? firstErr : firstErr = err;
        onShellReady();
        (0, common_1.afterReactBugCatch)(() => {
          var _a2;
          if (err !== reactBug) {
            (_a2 = options.onBoundaryError) === null || _a2 === void 0 ? void 0 : _a2.call(options, err);
          }
        });
      };
      const renderToPipeableStream = (_a = options.renderToPipeableStream) !== null && _a !== void 0 ? _a : server_node_1.renderToPipeableStream;
      if (!options.renderToPipeableStream) {
        (0, common_1.assertReactImport)(renderToPipeableStream, "renderToPipeableStream");
      }
      const { pipe: pipeOriginal, abort } = renderToPipeableStream(element, Object.assign(Object.assign({}, options.streamOptions), {
        onShellReady() {
          (0, common_1.debugFlow)("[react] onShellReady()");
          onShellReady();
        },
        onAllReady() {
          (0, common_1.debugFlow)("[react] onAllReady()");
          onShellReady();
          onAllReady();
        },
        onShellError: onError,
        onError
      }));
      setAbortFn(() => {
        abort();
      });
      let promiseResolved = false;
      const onReactBug = (err) => {
        (0, common_1.debugFlow)("react bug");
        didError = true;
        firstErr !== null && firstErr !== void 0 ? firstErr : firstErr = err;
        reactBug = err;
        if (reactBug !== firstErr || promiseResolved) {
          console.error(reactBug);
        }
      };
      const { pipeForUser, injectToStream, streamEnd, hasStreamEnded } = await (0, createPipeWrapper_1.createPipeWrapper)(pipeOriginal, onReactBug, clearTimeouts, doNotClosePromise);
      await shellReady;
      if (didError)
        throw firstErr;
      if (disable)
        await allReady;
      if (didError)
        throw firstErr;
      promiseResolved = true;
      return {
        pipe: pipeForUser,
        abort,
        readable: null,
        streamEnd: (0, common_1.wrapStreamEnd)(streamEnd, didError),
        injectToStream,
        hasStreamEnded
      };
    }
    exports.renderToNodeStream = renderToNodeStream;
  }
});

// node_modules/react-dom/cjs/react-dom-server-legacy.browser.production.min.js
var require_react_dom_server_legacy_browser_production_min = __commonJS({
  "node_modules/react-dom/cjs/react-dom-server-legacy.browser.production.min.js"(exports) {
    "use strict";
    var aa = require_react();
    function l12(a18) {
      for (var b4 = "https://reactjs.org/docs/error-decoder.html?invariant=" + a18, c15 = 1; c15 < arguments.length; c15++) b4 += "&args[]=" + encodeURIComponent(arguments[c15]);
      return "Minified React error #" + a18 + "; visit " + b4 + " for the full message or use the non-minified dev environment for full errors and additional helpful warnings.";
    }
    var p11 = Object.prototype.hasOwnProperty;
    var fa = /^[:A-Z_a-z\u00C0-\u00D6\u00D8-\u00F6\u00F8-\u02FF\u0370-\u037D\u037F-\u1FFF\u200C-\u200D\u2070-\u218F\u2C00-\u2FEF\u3001-\uD7FF\uF900-\uFDCF\uFDF0-\uFFFD][:A-Z_a-z\u00C0-\u00D6\u00D8-\u00F6\u00F8-\u02FF\u0370-\u037D\u037F-\u1FFF\u200C-\u200D\u2070-\u218F\u2C00-\u2FEF\u3001-\uD7FF\uF900-\uFDCF\uFDF0-\uFFFD\-.0-9\u00B7\u0300-\u036F\u203F-\u2040]*$/;
    var ha = {};
    var ia = {};
    function ja(a18) {
      if (p11.call(ia, a18)) return true;
      if (p11.call(ha, a18)) return false;
      if (fa.test(a18)) return ia[a18] = true;
      ha[a18] = true;
      return false;
    }
    function r7(a18, b4, c15, d12, f15, e14, g7) {
      this.acceptsBooleans = 2 === b4 || 3 === b4 || 4 === b4;
      this.attributeName = d12;
      this.attributeNamespace = f15;
      this.mustUseProperty = c15;
      this.propertyName = a18;
      this.type = b4;
      this.sanitizeURL = e14;
      this.removeEmptyString = g7;
    }
    var t20 = {};
    "children dangerouslySetInnerHTML defaultValue defaultChecked innerHTML suppressContentEditableWarning suppressHydrationWarning style".split(" ").forEach(function(a18) {
      t20[a18] = new r7(a18, 0, false, a18, null, false, false);
    });
    [["acceptCharset", "accept-charset"], ["className", "class"], ["htmlFor", "for"], ["httpEquiv", "http-equiv"]].forEach(function(a18) {
      var b4 = a18[0];
      t20[b4] = new r7(b4, 1, false, a18[1], null, false, false);
    });
    ["contentEditable", "draggable", "spellCheck", "value"].forEach(function(a18) {
      t20[a18] = new r7(a18, 2, false, a18.toLowerCase(), null, false, false);
    });
    ["autoReverse", "externalResourcesRequired", "focusable", "preserveAlpha"].forEach(function(a18) {
      t20[a18] = new r7(a18, 2, false, a18, null, false, false);
    });
    "allowFullScreen async autoFocus autoPlay controls default defer disabled disablePictureInPicture disableRemotePlayback formNoValidate hidden loop noModule noValidate open playsInline readOnly required reversed scoped seamless itemScope".split(" ").forEach(function(a18) {
      t20[a18] = new r7(a18, 3, false, a18.toLowerCase(), null, false, false);
    });
    ["checked", "multiple", "muted", "selected"].forEach(function(a18) {
      t20[a18] = new r7(a18, 3, true, a18, null, false, false);
    });
    ["capture", "download"].forEach(function(a18) {
      t20[a18] = new r7(a18, 4, false, a18, null, false, false);
    });
    ["cols", "rows", "size", "span"].forEach(function(a18) {
      t20[a18] = new r7(a18, 6, false, a18, null, false, false);
    });
    ["rowSpan", "start"].forEach(function(a18) {
      t20[a18] = new r7(a18, 5, false, a18.toLowerCase(), null, false, false);
    });
    var ka = /[\-:]([a-z])/g;
    function la(a18) {
      return a18[1].toUpperCase();
    }
    "accent-height alignment-baseline arabic-form baseline-shift cap-height clip-path clip-rule color-interpolation color-interpolation-filters color-profile color-rendering dominant-baseline enable-background fill-opacity fill-rule flood-color flood-opacity font-family font-size font-size-adjust font-stretch font-style font-variant font-weight glyph-name glyph-orientation-horizontal glyph-orientation-vertical horiz-adv-x horiz-origin-x image-rendering letter-spacing lighting-color marker-end marker-mid marker-start overline-position overline-thickness paint-order panose-1 pointer-events rendering-intent shape-rendering stop-color stop-opacity strikethrough-position strikethrough-thickness stroke-dasharray stroke-dashoffset stroke-linecap stroke-linejoin stroke-miterlimit stroke-opacity stroke-width text-anchor text-decoration text-rendering underline-position underline-thickness unicode-bidi unicode-range units-per-em v-alphabetic v-hanging v-ideographic v-mathematical vector-effect vert-adv-y vert-origin-x vert-origin-y word-spacing writing-mode xmlns:xlink x-height".split(" ").forEach(function(a18) {
      var b4 = a18.replace(
        ka,
        la
      );
      t20[b4] = new r7(b4, 1, false, a18, null, false, false);
    });
    "xlink:actuate xlink:arcrole xlink:role xlink:show xlink:title xlink:type".split(" ").forEach(function(a18) {
      var b4 = a18.replace(ka, la);
      t20[b4] = new r7(b4, 1, false, a18, "http://www.w3.org/1999/xlink", false, false);
    });
    ["xml:base", "xml:lang", "xml:space"].forEach(function(a18) {
      var b4 = a18.replace(ka, la);
      t20[b4] = new r7(b4, 1, false, a18, "http://www.w3.org/XML/1998/namespace", false, false);
    });
    ["tabIndex", "crossOrigin"].forEach(function(a18) {
      t20[a18] = new r7(a18, 1, false, a18.toLowerCase(), null, false, false);
    });
    t20.xlinkHref = new r7("xlinkHref", 1, false, "xlink:href", "http://www.w3.org/1999/xlink", true, false);
    ["src", "href", "action", "formAction"].forEach(function(a18) {
      t20[a18] = new r7(a18, 1, false, a18.toLowerCase(), null, true, true);
    });
    var u4 = {
      animationIterationCount: true,
      aspectRatio: true,
      borderImageOutset: true,
      borderImageSlice: true,
      borderImageWidth: true,
      boxFlex: true,
      boxFlexGroup: true,
      boxOrdinalGroup: true,
      columnCount: true,
      columns: true,
      flex: true,
      flexGrow: true,
      flexPositive: true,
      flexShrink: true,
      flexNegative: true,
      flexOrder: true,
      gridArea: true,
      gridRow: true,
      gridRowEnd: true,
      gridRowSpan: true,
      gridRowStart: true,
      gridColumn: true,
      gridColumnEnd: true,
      gridColumnSpan: true,
      gridColumnStart: true,
      fontWeight: true,
      lineClamp: true,
      lineHeight: true,
      opacity: true,
      order: true,
      orphans: true,
      tabSize: true,
      widows: true,
      zIndex: true,
      zoom: true,
      fillOpacity: true,
      floodOpacity: true,
      stopOpacity: true,
      strokeDasharray: true,
      strokeDashoffset: true,
      strokeMiterlimit: true,
      strokeOpacity: true,
      strokeWidth: true
    };
    var ma = ["Webkit", "ms", "Moz", "O"];
    Object.keys(u4).forEach(function(a18) {
      ma.forEach(function(b4) {
        b4 = b4 + a18.charAt(0).toUpperCase() + a18.substring(1);
        u4[b4] = u4[a18];
      });
    });
    var na = /["'&<>]/;
    function v5(a18) {
      if ("boolean" === typeof a18 || "number" === typeof a18) return "" + a18;
      a18 = "" + a18;
      var b4 = na.exec(a18);
      if (b4) {
        var c15 = "", d12, f15 = 0;
        for (d12 = b4.index; d12 < a18.length; d12++) {
          switch (a18.charCodeAt(d12)) {
            case 34:
              b4 = "&quot;";
              break;
            case 38:
              b4 = "&amp;";
              break;
            case 39:
              b4 = "&#x27;";
              break;
            case 60:
              b4 = "&lt;";
              break;
            case 62:
              b4 = "&gt;";
              break;
            default:
              continue;
          }
          f15 !== d12 && (c15 += a18.substring(f15, d12));
          f15 = d12 + 1;
          c15 += b4;
        }
        a18 = f15 !== d12 ? c15 + a18.substring(f15, d12) : c15;
      }
      return a18;
    }
    var oa = /([A-Z])/g;
    var pa = /^ms-/;
    var qa = Array.isArray;
    function w11(a18, b4) {
      return { insertionMode: a18, selectedValue: b4 };
    }
    function ra(a18, b4, c15) {
      switch (b4) {
        case "select":
          return w11(1, null != c15.value ? c15.value : c15.defaultValue);
        case "svg":
          return w11(2, null);
        case "math":
          return w11(3, null);
        case "foreignObject":
          return w11(1, null);
        case "table":
          return w11(4, null);
        case "thead":
        case "tbody":
        case "tfoot":
          return w11(5, null);
        case "colgroup":
          return w11(7, null);
        case "tr":
          return w11(6, null);
      }
      return 4 <= a18.insertionMode || 0 === a18.insertionMode ? w11(1, null) : a18;
    }
    var sa = /* @__PURE__ */ new Map();
    function ta(a18, b4, c15) {
      if ("object" !== typeof c15) throw Error(l12(62));
      b4 = true;
      for (var d12 in c15) if (p11.call(c15, d12)) {
        var f15 = c15[d12];
        if (null != f15 && "boolean" !== typeof f15 && "" !== f15) {
          if (0 === d12.indexOf("--")) {
            var e14 = v5(d12);
            f15 = v5(("" + f15).trim());
          } else {
            e14 = d12;
            var g7 = sa.get(e14);
            void 0 !== g7 ? e14 = g7 : (g7 = v5(e14.replace(oa, "-$1").toLowerCase().replace(pa, "-ms-")), sa.set(e14, g7), e14 = g7);
            f15 = "number" === typeof f15 ? 0 === f15 || p11.call(u4, d12) ? "" + f15 : f15 + "px" : v5(("" + f15).trim());
          }
          b4 ? (b4 = false, a18.push(' style="', e14, ":", f15)) : a18.push(";", e14, ":", f15);
        }
      }
      b4 || a18.push('"');
    }
    function x3(a18, b4, c15, d12) {
      switch (c15) {
        case "style":
          ta(a18, b4, d12);
          return;
        case "defaultValue":
        case "defaultChecked":
        case "innerHTML":
        case "suppressContentEditableWarning":
        case "suppressHydrationWarning":
          return;
      }
      if (!(2 < c15.length) || "o" !== c15[0] && "O" !== c15[0] || "n" !== c15[1] && "N" !== c15[1]) {
        if (b4 = t20.hasOwnProperty(c15) ? t20[c15] : null, null !== b4) {
          switch (typeof d12) {
            case "function":
            case "symbol":
              return;
            case "boolean":
              if (!b4.acceptsBooleans) return;
          }
          c15 = b4.attributeName;
          switch (b4.type) {
            case 3:
              d12 && a18.push(" ", c15, '=""');
              break;
            case 4:
              true === d12 ? a18.push(" ", c15, '=""') : false !== d12 && a18.push(" ", c15, '="', v5(d12), '"');
              break;
            case 5:
              isNaN(d12) || a18.push(" ", c15, '="', v5(d12), '"');
              break;
            case 6:
              !isNaN(d12) && 1 <= d12 && a18.push(" ", c15, '="', v5(d12), '"');
              break;
            default:
              b4.sanitizeURL && (d12 = "" + d12), a18.push(" ", c15, '="', v5(d12), '"');
          }
        } else if (ja(c15)) {
          switch (typeof d12) {
            case "function":
            case "symbol":
              return;
            case "boolean":
              if (b4 = c15.toLowerCase().slice(0, 5), "data-" !== b4 && "aria-" !== b4) return;
          }
          a18.push(" ", c15, '="', v5(d12), '"');
        }
      }
    }
    function y4(a18, b4, c15) {
      if (null != b4) {
        if (null != c15) throw Error(l12(60));
        if ("object" !== typeof b4 || !("__html" in b4)) throw Error(l12(61));
        b4 = b4.__html;
        null !== b4 && void 0 !== b4 && a18.push("" + b4);
      }
    }
    function ua(a18) {
      var b4 = "";
      aa.Children.forEach(a18, function(a19) {
        null != a19 && (b4 += a19);
      });
      return b4;
    }
    function va(a18, b4, c15, d12) {
      a18.push(A4(c15));
      var f15 = c15 = null, e14;
      for (e14 in b4) if (p11.call(b4, e14)) {
        var g7 = b4[e14];
        if (null != g7) switch (e14) {
          case "children":
            c15 = g7;
            break;
          case "dangerouslySetInnerHTML":
            f15 = g7;
            break;
          default:
            x3(a18, d12, e14, g7);
        }
      }
      a18.push(">");
      y4(a18, f15, c15);
      return "string" === typeof c15 ? (a18.push(v5(c15)), null) : c15;
    }
    var wa = /^[a-zA-Z][a-zA-Z:_\.\-\d]*$/;
    var xa = /* @__PURE__ */ new Map();
    function A4(a18) {
      var b4 = xa.get(a18);
      if (void 0 === b4) {
        if (!wa.test(a18)) throw Error(l12(65, a18));
        b4 = "<" + a18;
        xa.set(a18, b4);
      }
      return b4;
    }
    function ya(a18, b4, c15, d12, f15) {
      switch (b4) {
        case "select":
          a18.push(A4("select"));
          var e14 = null, g7 = null;
          for (n13 in c15) if (p11.call(c15, n13)) {
            var h6 = c15[n13];
            if (null != h6) switch (n13) {
              case "children":
                e14 = h6;
                break;
              case "dangerouslySetInnerHTML":
                g7 = h6;
                break;
              case "defaultValue":
              case "value":
                break;
              default:
                x3(a18, d12, n13, h6);
            }
          }
          a18.push(">");
          y4(a18, g7, e14);
          return e14;
        case "option":
          g7 = f15.selectedValue;
          a18.push(A4("option"));
          var k5 = h6 = null, m12 = null;
          var n13 = null;
          for (e14 in c15) if (p11.call(c15, e14)) {
            var q2 = c15[e14];
            if (null != q2) switch (e14) {
              case "children":
                h6 = q2;
                break;
              case "selected":
                m12 = q2;
                break;
              case "dangerouslySetInnerHTML":
                n13 = q2;
                break;
              case "value":
                k5 = q2;
              default:
                x3(a18, d12, e14, q2);
            }
          }
          if (null != g7) if (c15 = null !== k5 ? "" + k5 : ua(h6), qa(g7)) for (d12 = 0; d12 < g7.length; d12++) {
            if ("" + g7[d12] === c15) {
              a18.push(' selected=""');
              break;
            }
          }
          else "" + g7 === c15 && a18.push(' selected=""');
          else m12 && a18.push(' selected=""');
          a18.push(">");
          y4(a18, n13, h6);
          return h6;
        case "textarea":
          a18.push(A4("textarea"));
          n13 = g7 = e14 = null;
          for (h6 in c15) if (p11.call(c15, h6) && (k5 = c15[h6], null != k5)) switch (h6) {
            case "children":
              n13 = k5;
              break;
            case "value":
              e14 = k5;
              break;
            case "defaultValue":
              g7 = k5;
              break;
            case "dangerouslySetInnerHTML":
              throw Error(l12(91));
            default:
              x3(
                a18,
                d12,
                h6,
                k5
              );
          }
          null === e14 && null !== g7 && (e14 = g7);
          a18.push(">");
          if (null != n13) {
            if (null != e14) throw Error(l12(92));
            if (qa(n13) && 1 < n13.length) throw Error(l12(93));
            e14 = "" + n13;
          }
          "string" === typeof e14 && "\n" === e14[0] && a18.push("\n");
          null !== e14 && a18.push(v5("" + e14));
          return null;
        case "input":
          a18.push(A4("input"));
          k5 = n13 = h6 = e14 = null;
          for (g7 in c15) if (p11.call(c15, g7) && (m12 = c15[g7], null != m12)) switch (g7) {
            case "children":
            case "dangerouslySetInnerHTML":
              throw Error(l12(399, "input"));
            case "defaultChecked":
              k5 = m12;
              break;
            case "defaultValue":
              h6 = m12;
              break;
            case "checked":
              n13 = m12;
              break;
            case "value":
              e14 = m12;
              break;
            default:
              x3(a18, d12, g7, m12);
          }
          null !== n13 ? x3(a18, d12, "checked", n13) : null !== k5 && x3(a18, d12, "checked", k5);
          null !== e14 ? x3(a18, d12, "value", e14) : null !== h6 && x3(a18, d12, "value", h6);
          a18.push("/>");
          return null;
        case "menuitem":
          a18.push(A4("menuitem"));
          for (var C5 in c15) if (p11.call(c15, C5) && (e14 = c15[C5], null != e14)) switch (C5) {
            case "children":
            case "dangerouslySetInnerHTML":
              throw Error(l12(400));
            default:
              x3(a18, d12, C5, e14);
          }
          a18.push(">");
          return null;
        case "title":
          a18.push(A4("title"));
          e14 = null;
          for (q2 in c15) if (p11.call(c15, q2) && (g7 = c15[q2], null != g7)) switch (q2) {
            case "children":
              e14 = g7;
              break;
            case "dangerouslySetInnerHTML":
              throw Error(l12(434));
            default:
              x3(a18, d12, q2, g7);
          }
          a18.push(">");
          return e14;
        case "listing":
        case "pre":
          a18.push(A4(b4));
          g7 = e14 = null;
          for (k5 in c15) if (p11.call(c15, k5) && (h6 = c15[k5], null != h6)) switch (k5) {
            case "children":
              e14 = h6;
              break;
            case "dangerouslySetInnerHTML":
              g7 = h6;
              break;
            default:
              x3(a18, d12, k5, h6);
          }
          a18.push(">");
          if (null != g7) {
            if (null != e14) throw Error(l12(60));
            if ("object" !== typeof g7 || !("__html" in g7)) throw Error(l12(61));
            c15 = g7.__html;
            null !== c15 && void 0 !== c15 && ("string" === typeof c15 && 0 < c15.length && "\n" === c15[0] ? a18.push("\n", c15) : a18.push("" + c15));
          }
          "string" === typeof e14 && "\n" === e14[0] && a18.push("\n");
          return e14;
        case "area":
        case "base":
        case "br":
        case "col":
        case "embed":
        case "hr":
        case "img":
        case "keygen":
        case "link":
        case "meta":
        case "param":
        case "source":
        case "track":
        case "wbr":
          a18.push(A4(b4));
          for (var D5 in c15) if (p11.call(c15, D5) && (e14 = c15[D5], null != e14)) switch (D5) {
            case "children":
            case "dangerouslySetInnerHTML":
              throw Error(l12(399, b4));
            default:
              x3(a18, d12, D5, e14);
          }
          a18.push("/>");
          return null;
        case "annotation-xml":
        case "color-profile":
        case "font-face":
        case "font-face-src":
        case "font-face-uri":
        case "font-face-format":
        case "font-face-name":
        case "missing-glyph":
          return va(
            a18,
            c15,
            b4,
            d12
          );
        case "html":
          return 0 === f15.insertionMode && a18.push("<!DOCTYPE html>"), va(a18, c15, b4, d12);
        default:
          if (-1 === b4.indexOf("-") && "string" !== typeof c15.is) return va(a18, c15, b4, d12);
          a18.push(A4(b4));
          g7 = e14 = null;
          for (m12 in c15) if (p11.call(c15, m12) && (h6 = c15[m12], null != h6)) switch (m12) {
            case "children":
              e14 = h6;
              break;
            case "dangerouslySetInnerHTML":
              g7 = h6;
              break;
            case "style":
              ta(a18, d12, h6);
              break;
            case "suppressContentEditableWarning":
            case "suppressHydrationWarning":
              break;
            default:
              ja(m12) && "function" !== typeof h6 && "symbol" !== typeof h6 && a18.push(" ", m12, '="', v5(h6), '"');
          }
          a18.push(">");
          y4(a18, g7, e14);
          return e14;
      }
    }
    function za(a18, b4, c15) {
      a18.push('<!--$?--><template id="');
      if (null === c15) throw Error(l12(395));
      a18.push(c15);
      return a18.push('"></template>');
    }
    function Aa(a18, b4, c15, d12) {
      switch (c15.insertionMode) {
        case 0:
        case 1:
          return a18.push('<div hidden id="'), a18.push(b4.segmentPrefix), b4 = d12.toString(16), a18.push(b4), a18.push('">');
        case 2:
          return a18.push('<svg aria-hidden="true" style="display:none" id="'), a18.push(b4.segmentPrefix), b4 = d12.toString(16), a18.push(b4), a18.push('">');
        case 3:
          return a18.push('<math aria-hidden="true" style="display:none" id="'), a18.push(b4.segmentPrefix), b4 = d12.toString(16), a18.push(b4), a18.push('">');
        case 4:
          return a18.push('<table hidden id="'), a18.push(b4.segmentPrefix), b4 = d12.toString(16), a18.push(b4), a18.push('">');
        case 5:
          return a18.push('<table hidden><tbody id="'), a18.push(b4.segmentPrefix), b4 = d12.toString(16), a18.push(b4), a18.push('">');
        case 6:
          return a18.push('<table hidden><tr id="'), a18.push(b4.segmentPrefix), b4 = d12.toString(16), a18.push(b4), a18.push('">');
        case 7:
          return a18.push('<table hidden><colgroup id="'), a18.push(b4.segmentPrefix), b4 = d12.toString(16), a18.push(b4), a18.push('">');
        default:
          throw Error(l12(397));
      }
    }
    function Ba(a18, b4) {
      switch (b4.insertionMode) {
        case 0:
        case 1:
          return a18.push("</div>");
        case 2:
          return a18.push("</svg>");
        case 3:
          return a18.push("</math>");
        case 4:
          return a18.push("</table>");
        case 5:
          return a18.push("</tbody></table>");
        case 6:
          return a18.push("</tr></table>");
        case 7:
          return a18.push("</colgroup></table>");
        default:
          throw Error(l12(397));
      }
    }
    var Ca = /[<\u2028\u2029]/g;
    function Da(a18) {
      return JSON.stringify(a18).replace(Ca, function(a19) {
        switch (a19) {
          case "<":
            return "\\u003c";
          case "\u2028":
            return "\\u2028";
          case "\u2029":
            return "\\u2029";
          default:
            throw Error("escapeJSStringsForInstructionScripts encountered a match it does not know how to replace. this means the match regex and the replacement characters are no longer in sync. This is a bug in React");
        }
      });
    }
    function Ea(a18, b4) {
      b4 = void 0 === b4 ? "" : b4;
      return { bootstrapChunks: [], startInlineScript: "<script>", placeholderPrefix: b4 + "P:", segmentPrefix: b4 + "S:", boundaryPrefix: b4 + "B:", idPrefix: b4, nextSuspenseID: 0, sentCompleteSegmentFunction: false, sentCompleteBoundaryFunction: false, sentClientRenderFunction: false, generateStaticMarkup: a18 };
    }
    function Fa(a18, b4, c15, d12) {
      if (c15.generateStaticMarkup) return a18.push(v5(b4)), false;
      "" === b4 ? a18 = d12 : (d12 && a18.push("<!-- -->"), a18.push(v5(b4)), a18 = true);
      return a18;
    }
    var B4 = Object.assign;
    var Ga = Symbol.for("react.element");
    var Ha = Symbol.for("react.portal");
    var Ia = Symbol.for("react.fragment");
    var Ja = Symbol.for("react.strict_mode");
    var Ka = Symbol.for("react.profiler");
    var La = Symbol.for("react.provider");
    var Ma = Symbol.for("react.context");
    var Na = Symbol.for("react.forward_ref");
    var Oa = Symbol.for("react.suspense");
    var Pa = Symbol.for("react.suspense_list");
    var Qa = Symbol.for("react.memo");
    var Ra = Symbol.for("react.lazy");
    var Sa = Symbol.for("react.scope");
    var Ta = Symbol.for("react.debug_trace_mode");
    var Ua = Symbol.for("react.legacy_hidden");
    var Va = Symbol.for("react.default_value");
    var Wa = Symbol.iterator;
    function Xa(a18) {
      if (null == a18) return null;
      if ("function" === typeof a18) return a18.displayName || a18.name || null;
      if ("string" === typeof a18) return a18;
      switch (a18) {
        case Ia:
          return "Fragment";
        case Ha:
          return "Portal";
        case Ka:
          return "Profiler";
        case Ja:
          return "StrictMode";
        case Oa:
          return "Suspense";
        case Pa:
          return "SuspenseList";
      }
      if ("object" === typeof a18) switch (a18.$$typeof) {
        case Ma:
          return (a18.displayName || "Context") + ".Consumer";
        case La:
          return (a18._context.displayName || "Context") + ".Provider";
        case Na:
          var b4 = a18.render;
          a18 = a18.displayName;
          a18 || (a18 = b4.displayName || b4.name || "", a18 = "" !== a18 ? "ForwardRef(" + a18 + ")" : "ForwardRef");
          return a18;
        case Qa:
          return b4 = a18.displayName || null, null !== b4 ? b4 : Xa(a18.type) || "Memo";
        case Ra:
          b4 = a18._payload;
          a18 = a18._init;
          try {
            return Xa(a18(b4));
          } catch (c15) {
          }
      }
      return null;
    }
    var Ya = {};
    function Za(a18, b4) {
      a18 = a18.contextTypes;
      if (!a18) return Ya;
      var c15 = {}, d12;
      for (d12 in a18) c15[d12] = b4[d12];
      return c15;
    }
    var E6 = null;
    function F4(a18, b4) {
      if (a18 !== b4) {
        a18.context._currentValue2 = a18.parentValue;
        a18 = a18.parent;
        var c15 = b4.parent;
        if (null === a18) {
          if (null !== c15) throw Error(l12(401));
        } else {
          if (null === c15) throw Error(l12(401));
          F4(a18, c15);
        }
        b4.context._currentValue2 = b4.value;
      }
    }
    function $a(a18) {
      a18.context._currentValue2 = a18.parentValue;
      a18 = a18.parent;
      null !== a18 && $a(a18);
    }
    function ab(a18) {
      var b4 = a18.parent;
      null !== b4 && ab(b4);
      a18.context._currentValue2 = a18.value;
    }
    function bb(a18, b4) {
      a18.context._currentValue2 = a18.parentValue;
      a18 = a18.parent;
      if (null === a18) throw Error(l12(402));
      a18.depth === b4.depth ? F4(a18, b4) : bb(a18, b4);
    }
    function cb(a18, b4) {
      var c15 = b4.parent;
      if (null === c15) throw Error(l12(402));
      a18.depth === c15.depth ? F4(a18, c15) : cb(a18, c15);
      b4.context._currentValue2 = b4.value;
    }
    function G3(a18) {
      var b4 = E6;
      b4 !== a18 && (null === b4 ? ab(a18) : null === a18 ? $a(b4) : b4.depth === a18.depth ? F4(b4, a18) : b4.depth > a18.depth ? bb(b4, a18) : cb(b4, a18), E6 = a18);
    }
    var db = { isMounted: function() {
      return false;
    }, enqueueSetState: function(a18, b4) {
      a18 = a18._reactInternals;
      null !== a18.queue && a18.queue.push(b4);
    }, enqueueReplaceState: function(a18, b4) {
      a18 = a18._reactInternals;
      a18.replace = true;
      a18.queue = [b4];
    }, enqueueForceUpdate: function() {
    } };
    function eb(a18, b4, c15, d12) {
      var f15 = void 0 !== a18.state ? a18.state : null;
      a18.updater = db;
      a18.props = c15;
      a18.state = f15;
      var e14 = { queue: [], replace: false };
      a18._reactInternals = e14;
      var g7 = b4.contextType;
      a18.context = "object" === typeof g7 && null !== g7 ? g7._currentValue2 : d12;
      g7 = b4.getDerivedStateFromProps;
      "function" === typeof g7 && (g7 = g7(c15, f15), f15 = null === g7 || void 0 === g7 ? f15 : B4({}, f15, g7), a18.state = f15);
      if ("function" !== typeof b4.getDerivedStateFromProps && "function" !== typeof a18.getSnapshotBeforeUpdate && ("function" === typeof a18.UNSAFE_componentWillMount || "function" === typeof a18.componentWillMount)) if (b4 = a18.state, "function" === typeof a18.componentWillMount && a18.componentWillMount(), "function" === typeof a18.UNSAFE_componentWillMount && a18.UNSAFE_componentWillMount(), b4 !== a18.state && db.enqueueReplaceState(a18, a18.state, null), null !== e14.queue && 0 < e14.queue.length) if (b4 = e14.queue, g7 = e14.replace, e14.queue = null, e14.replace = false, g7 && 1 === b4.length) a18.state = b4[0];
      else {
        e14 = g7 ? b4[0] : a18.state;
        f15 = true;
        for (g7 = g7 ? 1 : 0; g7 < b4.length; g7++) {
          var h6 = b4[g7];
          h6 = "function" === typeof h6 ? h6.call(a18, e14, c15, d12) : h6;
          null != h6 && (f15 ? (f15 = false, e14 = B4({}, e14, h6)) : B4(e14, h6));
        }
        a18.state = e14;
      }
      else e14.queue = null;
    }
    var fb = { id: 1, overflow: "" };
    function gb(a18, b4, c15) {
      var d12 = a18.id;
      a18 = a18.overflow;
      var f15 = 32 - H4(d12) - 1;
      d12 &= ~(1 << f15);
      c15 += 1;
      var e14 = 32 - H4(b4) + f15;
      if (30 < e14) {
        var g7 = f15 - f15 % 5;
        e14 = (d12 & (1 << g7) - 1).toString(32);
        d12 >>= g7;
        f15 -= g7;
        return { id: 1 << 32 - H4(b4) + f15 | c15 << f15 | d12, overflow: e14 + a18 };
      }
      return { id: 1 << e14 | c15 << f15 | d12, overflow: a18 };
    }
    var H4 = Math.clz32 ? Math.clz32 : hb;
    var ib = Math.log;
    var jb = Math.LN2;
    function hb(a18) {
      a18 >>>= 0;
      return 0 === a18 ? 32 : 31 - (ib(a18) / jb | 0) | 0;
    }
    function kb(a18, b4) {
      return a18 === b4 && (0 !== a18 || 1 / a18 === 1 / b4) || a18 !== a18 && b4 !== b4;
    }
    var lb = "function" === typeof Object.is ? Object.is : kb;
    var I6 = null;
    var ob = null;
    var J2 = null;
    var K3 = null;
    var L3 = false;
    var M6 = false;
    var N4 = 0;
    var O5 = null;
    var P4 = 0;
    function Q4() {
      if (null === I6) throw Error(l12(321));
      return I6;
    }
    function pb() {
      if (0 < P4) throw Error(l12(312));
      return { memoizedState: null, queue: null, next: null };
    }
    function qb() {
      null === K3 ? null === J2 ? (L3 = false, J2 = K3 = pb()) : (L3 = true, K3 = J2) : null === K3.next ? (L3 = false, K3 = K3.next = pb()) : (L3 = true, K3 = K3.next);
      return K3;
    }
    function rb() {
      ob = I6 = null;
      M6 = false;
      J2 = null;
      P4 = 0;
      K3 = O5 = null;
    }
    function sb(a18, b4) {
      return "function" === typeof b4 ? b4(a18) : b4;
    }
    function tb(a18, b4, c15) {
      I6 = Q4();
      K3 = qb();
      if (L3) {
        var d12 = K3.queue;
        b4 = d12.dispatch;
        if (null !== O5 && (c15 = O5.get(d12), void 0 !== c15)) {
          O5.delete(d12);
          d12 = K3.memoizedState;
          do
            d12 = a18(d12, c15.action), c15 = c15.next;
          while (null !== c15);
          K3.memoizedState = d12;
          return [d12, b4];
        }
        return [K3.memoizedState, b4];
      }
      a18 = a18 === sb ? "function" === typeof b4 ? b4() : b4 : void 0 !== c15 ? c15(b4) : b4;
      K3.memoizedState = a18;
      a18 = K3.queue = { last: null, dispatch: null };
      a18 = a18.dispatch = ub.bind(null, I6, a18);
      return [K3.memoizedState, a18];
    }
    function vb(a18, b4) {
      I6 = Q4();
      K3 = qb();
      b4 = void 0 === b4 ? null : b4;
      if (null !== K3) {
        var c15 = K3.memoizedState;
        if (null !== c15 && null !== b4) {
          var d12 = c15[1];
          a: if (null === d12) d12 = false;
          else {
            for (var f15 = 0; f15 < d12.length && f15 < b4.length; f15++) if (!lb(b4[f15], d12[f15])) {
              d12 = false;
              break a;
            }
            d12 = true;
          }
          if (d12) return c15[0];
        }
      }
      a18 = a18();
      K3.memoizedState = [a18, b4];
      return a18;
    }
    function ub(a18, b4, c15) {
      if (25 <= P4) throw Error(l12(301));
      if (a18 === I6) if (M6 = true, a18 = { action: c15, next: null }, null === O5 && (O5 = /* @__PURE__ */ new Map()), c15 = O5.get(b4), void 0 === c15) O5.set(b4, a18);
      else {
        for (b4 = c15; null !== b4.next; ) b4 = b4.next;
        b4.next = a18;
      }
    }
    function wb() {
      throw Error(l12(394));
    }
    function R10() {
    }
    var xb = { readContext: function(a18) {
      return a18._currentValue2;
    }, useContext: function(a18) {
      Q4();
      return a18._currentValue2;
    }, useMemo: vb, useReducer: tb, useRef: function(a18) {
      I6 = Q4();
      K3 = qb();
      var b4 = K3.memoizedState;
      return null === b4 ? (a18 = { current: a18 }, K3.memoizedState = a18) : b4;
    }, useState: function(a18) {
      return tb(sb, a18);
    }, useInsertionEffect: R10, useLayoutEffect: function() {
    }, useCallback: function(a18, b4) {
      return vb(function() {
        return a18;
      }, b4);
    }, useImperativeHandle: R10, useEffect: R10, useDebugValue: R10, useDeferredValue: function(a18) {
      Q4();
      return a18;
    }, useTransition: function() {
      Q4();
      return [
        false,
        wb
      ];
    }, useId: function() {
      var a18 = ob.treeContext;
      var b4 = a18.overflow;
      a18 = a18.id;
      a18 = (a18 & ~(1 << 32 - H4(a18) - 1)).toString(32) + b4;
      var c15 = S2;
      if (null === c15) throw Error(l12(404));
      b4 = N4++;
      a18 = ":" + c15.idPrefix + "R" + a18;
      0 < b4 && (a18 += "H" + b4.toString(32));
      return a18 + ":";
    }, useMutableSource: function(a18, b4) {
      Q4();
      return b4(a18._source);
    }, useSyncExternalStore: function(a18, b4, c15) {
      if (void 0 === c15) throw Error(l12(407));
      return c15();
    } };
    var S2 = null;
    var yb = aa.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED.ReactCurrentDispatcher;
    function zb(a18) {
      console.error(a18);
      return null;
    }
    function T3() {
    }
    function Ab(a18, b4, c15, d12, f15, e14, g7, h6, k5) {
      var m12 = [], n13 = /* @__PURE__ */ new Set();
      b4 = { destination: null, responseState: b4, progressiveChunkSize: void 0 === d12 ? 12800 : d12, status: 0, fatalError: null, nextSegmentId: 0, allPendingTasks: 0, pendingRootTasks: 0, completedRootSegment: null, abortableTasks: n13, pingedTasks: m12, clientRenderedBoundaries: [], completedBoundaries: [], partialBoundaries: [], onError: void 0 === f15 ? zb : f15, onAllReady: void 0 === e14 ? T3 : e14, onShellReady: void 0 === g7 ? T3 : g7, onShellError: void 0 === h6 ? T3 : h6, onFatalError: void 0 === k5 ? T3 : k5 };
      c15 = U3(b4, 0, null, c15, false, false);
      c15.parentFlushed = true;
      a18 = Bb(b4, a18, null, c15, n13, Ya, null, fb);
      m12.push(a18);
      return b4;
    }
    function Bb(a18, b4, c15, d12, f15, e14, g7, h6) {
      a18.allPendingTasks++;
      null === c15 ? a18.pendingRootTasks++ : c15.pendingTasks++;
      var k5 = { node: b4, ping: function() {
        var b5 = a18.pingedTasks;
        b5.push(k5);
        1 === b5.length && Cb(a18);
      }, blockedBoundary: c15, blockedSegment: d12, abortSet: f15, legacyContext: e14, context: g7, treeContext: h6 };
      f15.add(k5);
      return k5;
    }
    function U3(a18, b4, c15, d12, f15, e14) {
      return { status: 0, id: -1, index: b4, parentFlushed: false, chunks: [], children: [], formatContext: d12, boundary: c15, lastPushedText: f15, textEmbedded: e14 };
    }
    function V3(a18, b4) {
      a18 = a18.onError(b4);
      if (null != a18 && "string" !== typeof a18) throw Error('onError returned something with a type other than "string". onError should return a string and may return null or undefined but must not return anything else. It received something of type "' + typeof a18 + '" instead');
      return a18;
    }
    function W4(a18, b4) {
      var c15 = a18.onShellError;
      c15(b4);
      c15 = a18.onFatalError;
      c15(b4);
      null !== a18.destination ? (a18.status = 2, a18.destination.destroy(b4)) : (a18.status = 1, a18.fatalError = b4);
    }
    function Db(a18, b4, c15, d12, f15) {
      I6 = {};
      ob = b4;
      N4 = 0;
      for (a18 = c15(d12, f15); M6; ) M6 = false, N4 = 0, P4 += 1, K3 = null, a18 = c15(d12, f15);
      rb();
      return a18;
    }
    function Eb(a18, b4, c15, d12) {
      var f15 = c15.render(), e14 = d12.childContextTypes;
      if (null !== e14 && void 0 !== e14) {
        var g7 = b4.legacyContext;
        if ("function" !== typeof c15.getChildContext) d12 = g7;
        else {
          c15 = c15.getChildContext();
          for (var h6 in c15) if (!(h6 in e14)) throw Error(l12(108, Xa(d12) || "Unknown", h6));
          d12 = B4({}, g7, c15);
        }
        b4.legacyContext = d12;
        X4(a18, b4, f15);
        b4.legacyContext = g7;
      } else X4(a18, b4, f15);
    }
    function Fb(a18, b4) {
      if (a18 && a18.defaultProps) {
        b4 = B4({}, b4);
        a18 = a18.defaultProps;
        for (var c15 in a18) void 0 === b4[c15] && (b4[c15] = a18[c15]);
        return b4;
      }
      return b4;
    }
    function Gb(a18, b4, c15, d12, f15) {
      if ("function" === typeof c15) if (c15.prototype && c15.prototype.isReactComponent) {
        f15 = Za(c15, b4.legacyContext);
        var e14 = c15.contextType;
        e14 = new c15(d12, "object" === typeof e14 && null !== e14 ? e14._currentValue2 : f15);
        eb(e14, c15, d12, f15);
        Eb(a18, b4, e14, c15);
      } else {
        e14 = Za(c15, b4.legacyContext);
        f15 = Db(a18, b4, c15, d12, e14);
        var g7 = 0 !== N4;
        if ("object" === typeof f15 && null !== f15 && "function" === typeof f15.render && void 0 === f15.$$typeof) eb(f15, c15, d12, e14), Eb(a18, b4, f15, c15);
        else if (g7) {
          d12 = b4.treeContext;
          b4.treeContext = gb(d12, 1, 0);
          try {
            X4(a18, b4, f15);
          } finally {
            b4.treeContext = d12;
          }
        } else X4(a18, b4, f15);
      }
      else if ("string" === typeof c15) {
        f15 = b4.blockedSegment;
        e14 = ya(f15.chunks, c15, d12, a18.responseState, f15.formatContext);
        f15.lastPushedText = false;
        g7 = f15.formatContext;
        f15.formatContext = ra(g7, c15, d12);
        Hb(a18, b4, e14);
        f15.formatContext = g7;
        switch (c15) {
          case "area":
          case "base":
          case "br":
          case "col":
          case "embed":
          case "hr":
          case "img":
          case "input":
          case "keygen":
          case "link":
          case "meta":
          case "param":
          case "source":
          case "track":
          case "wbr":
            break;
          default:
            f15.chunks.push("</", c15, ">");
        }
        f15.lastPushedText = false;
      } else {
        switch (c15) {
          case Ua:
          case Ta:
          case Ja:
          case Ka:
          case Ia:
            X4(a18, b4, d12.children);
            return;
          case Pa:
            X4(a18, b4, d12.children);
            return;
          case Sa:
            throw Error(l12(343));
          case Oa:
            a: {
              c15 = b4.blockedBoundary;
              f15 = b4.blockedSegment;
              e14 = d12.fallback;
              d12 = d12.children;
              g7 = /* @__PURE__ */ new Set();
              var h6 = { id: null, rootSegmentID: -1, parentFlushed: false, pendingTasks: 0, forceClientRender: false, completedSegments: [], byteSize: 0, fallbackAbortableTasks: g7, errorDigest: null }, k5 = U3(a18, f15.chunks.length, h6, f15.formatContext, false, false);
              f15.children.push(k5);
              f15.lastPushedText = false;
              var m12 = U3(a18, 0, null, f15.formatContext, false, false);
              m12.parentFlushed = true;
              b4.blockedBoundary = h6;
              b4.blockedSegment = m12;
              try {
                if (Hb(
                  a18,
                  b4,
                  d12
                ), a18.responseState.generateStaticMarkup || m12.lastPushedText && m12.textEmbedded && m12.chunks.push("<!-- -->"), m12.status = 1, Y3(h6, m12), 0 === h6.pendingTasks) break a;
              } catch (n13) {
                m12.status = 4, h6.forceClientRender = true, h6.errorDigest = V3(a18, n13);
              } finally {
                b4.blockedBoundary = c15, b4.blockedSegment = f15;
              }
              b4 = Bb(a18, e14, c15, k5, g7, b4.legacyContext, b4.context, b4.treeContext);
              a18.pingedTasks.push(b4);
            }
            return;
        }
        if ("object" === typeof c15 && null !== c15) switch (c15.$$typeof) {
          case Na:
            d12 = Db(a18, b4, c15.render, d12, f15);
            if (0 !== N4) {
              c15 = b4.treeContext;
              b4.treeContext = gb(c15, 1, 0);
              try {
                X4(a18, b4, d12);
              } finally {
                b4.treeContext = c15;
              }
            } else X4(a18, b4, d12);
            return;
          case Qa:
            c15 = c15.type;
            d12 = Fb(c15, d12);
            Gb(a18, b4, c15, d12, f15);
            return;
          case La:
            f15 = d12.children;
            c15 = c15._context;
            d12 = d12.value;
            e14 = c15._currentValue2;
            c15._currentValue2 = d12;
            g7 = E6;
            E6 = d12 = { parent: g7, depth: null === g7 ? 0 : g7.depth + 1, context: c15, parentValue: e14, value: d12 };
            b4.context = d12;
            X4(a18, b4, f15);
            a18 = E6;
            if (null === a18) throw Error(l12(403));
            d12 = a18.parentValue;
            a18.context._currentValue2 = d12 === Va ? a18.context._defaultValue : d12;
            a18 = E6 = a18.parent;
            b4.context = a18;
            return;
          case Ma:
            d12 = d12.children;
            d12 = d12(c15._currentValue2);
            X4(a18, b4, d12);
            return;
          case Ra:
            f15 = c15._init;
            c15 = f15(c15._payload);
            d12 = Fb(c15, d12);
            Gb(
              a18,
              b4,
              c15,
              d12,
              void 0
            );
            return;
        }
        throw Error(l12(130, null == c15 ? c15 : typeof c15, ""));
      }
    }
    function X4(a18, b4, c15) {
      b4.node = c15;
      if ("object" === typeof c15 && null !== c15) {
        switch (c15.$$typeof) {
          case Ga:
            Gb(a18, b4, c15.type, c15.props, c15.ref);
            return;
          case Ha:
            throw Error(l12(257));
          case Ra:
            var d12 = c15._init;
            c15 = d12(c15._payload);
            X4(a18, b4, c15);
            return;
        }
        if (qa(c15)) {
          Ib(a18, b4, c15);
          return;
        }
        null === c15 || "object" !== typeof c15 ? d12 = null : (d12 = Wa && c15[Wa] || c15["@@iterator"], d12 = "function" === typeof d12 ? d12 : null);
        if (d12 && (d12 = d12.call(c15))) {
          c15 = d12.next();
          if (!c15.done) {
            var f15 = [];
            do
              f15.push(c15.value), c15 = d12.next();
            while (!c15.done);
            Ib(a18, b4, f15);
          }
          return;
        }
        a18 = Object.prototype.toString.call(c15);
        throw Error(l12(31, "[object Object]" === a18 ? "object with keys {" + Object.keys(c15).join(", ") + "}" : a18));
      }
      "string" === typeof c15 ? (d12 = b4.blockedSegment, d12.lastPushedText = Fa(b4.blockedSegment.chunks, c15, a18.responseState, d12.lastPushedText)) : "number" === typeof c15 && (d12 = b4.blockedSegment, d12.lastPushedText = Fa(b4.blockedSegment.chunks, "" + c15, a18.responseState, d12.lastPushedText));
    }
    function Ib(a18, b4, c15) {
      for (var d12 = c15.length, f15 = 0; f15 < d12; f15++) {
        var e14 = b4.treeContext;
        b4.treeContext = gb(e14, d12, f15);
        try {
          Hb(a18, b4, c15[f15]);
        } finally {
          b4.treeContext = e14;
        }
      }
    }
    function Hb(a18, b4, c15) {
      var d12 = b4.blockedSegment.formatContext, f15 = b4.legacyContext, e14 = b4.context;
      try {
        return X4(a18, b4, c15);
      } catch (k5) {
        if (rb(), "object" === typeof k5 && null !== k5 && "function" === typeof k5.then) {
          c15 = k5;
          var g7 = b4.blockedSegment, h6 = U3(a18, g7.chunks.length, null, g7.formatContext, g7.lastPushedText, true);
          g7.children.push(h6);
          g7.lastPushedText = false;
          a18 = Bb(a18, b4.node, b4.blockedBoundary, h6, b4.abortSet, b4.legacyContext, b4.context, b4.treeContext).ping;
          c15.then(a18, a18);
          b4.blockedSegment.formatContext = d12;
          b4.legacyContext = f15;
          b4.context = e14;
          G3(e14);
        } else throw b4.blockedSegment.formatContext = d12, b4.legacyContext = f15, b4.context = e14, G3(e14), k5;
      }
    }
    function Jb(a18) {
      var b4 = a18.blockedBoundary;
      a18 = a18.blockedSegment;
      a18.status = 3;
      Kb(this, b4, a18);
    }
    function Lb(a18, b4, c15) {
      var d12 = a18.blockedBoundary;
      a18.blockedSegment.status = 3;
      null === d12 ? (b4.allPendingTasks--, 2 !== b4.status && (b4.status = 2, null !== b4.destination && b4.destination.push(null))) : (d12.pendingTasks--, d12.forceClientRender || (d12.forceClientRender = true, a18 = void 0 === c15 ? Error(l12(432)) : c15, d12.errorDigest = b4.onError(a18), d12.parentFlushed && b4.clientRenderedBoundaries.push(d12)), d12.fallbackAbortableTasks.forEach(function(a19) {
        return Lb(a19, b4, c15);
      }), d12.fallbackAbortableTasks.clear(), b4.allPendingTasks--, 0 === b4.allPendingTasks && (d12 = b4.onAllReady, d12()));
    }
    function Y3(a18, b4) {
      if (0 === b4.chunks.length && 1 === b4.children.length && null === b4.children[0].boundary) {
        var c15 = b4.children[0];
        c15.id = b4.id;
        c15.parentFlushed = true;
        1 === c15.status && Y3(a18, c15);
      } else a18.completedSegments.push(b4);
    }
    function Kb(a18, b4, c15) {
      if (null === b4) {
        if (c15.parentFlushed) {
          if (null !== a18.completedRootSegment) throw Error(l12(389));
          a18.completedRootSegment = c15;
        }
        a18.pendingRootTasks--;
        0 === a18.pendingRootTasks && (a18.onShellError = T3, b4 = a18.onShellReady, b4());
      } else b4.pendingTasks--, b4.forceClientRender || (0 === b4.pendingTasks ? (c15.parentFlushed && 1 === c15.status && Y3(b4, c15), b4.parentFlushed && a18.completedBoundaries.push(b4), b4.fallbackAbortableTasks.forEach(Jb, a18), b4.fallbackAbortableTasks.clear()) : c15.parentFlushed && 1 === c15.status && (Y3(b4, c15), 1 === b4.completedSegments.length && b4.parentFlushed && a18.partialBoundaries.push(b4)));
      a18.allPendingTasks--;
      0 === a18.allPendingTasks && (a18 = a18.onAllReady, a18());
    }
    function Cb(a18) {
      if (2 !== a18.status) {
        var b4 = E6, c15 = yb.current;
        yb.current = xb;
        var d12 = S2;
        S2 = a18.responseState;
        try {
          var f15 = a18.pingedTasks, e14;
          for (e14 = 0; e14 < f15.length; e14++) {
            var g7 = f15[e14];
            var h6 = a18, k5 = g7.blockedSegment;
            if (0 === k5.status) {
              G3(g7.context);
              try {
                X4(h6, g7, g7.node), h6.responseState.generateStaticMarkup || k5.lastPushedText && k5.textEmbedded && k5.chunks.push("<!-- -->"), g7.abortSet.delete(g7), k5.status = 1, Kb(h6, g7.blockedBoundary, k5);
              } catch (z2) {
                if (rb(), "object" === typeof z2 && null !== z2 && "function" === typeof z2.then) {
                  var m12 = g7.ping;
                  z2.then(m12, m12);
                } else {
                  g7.abortSet.delete(g7);
                  k5.status = 4;
                  var n13 = g7.blockedBoundary, q2 = z2, C5 = V3(h6, q2);
                  null === n13 ? W4(h6, q2) : (n13.pendingTasks--, n13.forceClientRender || (n13.forceClientRender = true, n13.errorDigest = C5, n13.parentFlushed && h6.clientRenderedBoundaries.push(n13)));
                  h6.allPendingTasks--;
                  if (0 === h6.allPendingTasks) {
                    var D5 = h6.onAllReady;
                    D5();
                  }
                }
              } finally {
              }
            }
          }
          f15.splice(0, e14);
          null !== a18.destination && Mb(a18, a18.destination);
        } catch (z2) {
          V3(a18, z2), W4(a18, z2);
        } finally {
          S2 = d12, yb.current = c15, c15 === xb && G3(b4);
        }
      }
    }
    function Z5(a18, b4, c15) {
      c15.parentFlushed = true;
      switch (c15.status) {
        case 0:
          var d12 = c15.id = a18.nextSegmentId++;
          c15.lastPushedText = false;
          c15.textEmbedded = false;
          a18 = a18.responseState;
          b4.push('<template id="');
          b4.push(a18.placeholderPrefix);
          a18 = d12.toString(16);
          b4.push(a18);
          return b4.push('"></template>');
        case 1:
          c15.status = 2;
          var f15 = true;
          d12 = c15.chunks;
          var e14 = 0;
          c15 = c15.children;
          for (var g7 = 0; g7 < c15.length; g7++) {
            for (f15 = c15[g7]; e14 < f15.index; e14++) b4.push(d12[e14]);
            f15 = Nb(a18, b4, f15);
          }
          for (; e14 < d12.length - 1; e14++) b4.push(d12[e14]);
          e14 < d12.length && (f15 = b4.push(d12[e14]));
          return f15;
        default:
          throw Error(l12(390));
      }
    }
    function Nb(a18, b4, c15) {
      var d12 = c15.boundary;
      if (null === d12) return Z5(a18, b4, c15);
      d12.parentFlushed = true;
      if (d12.forceClientRender) return a18.responseState.generateStaticMarkup || (d12 = d12.errorDigest, b4.push("<!--$!-->"), b4.push("<template"), d12 && (b4.push(' data-dgst="'), d12 = v5(d12), b4.push(d12), b4.push('"')), b4.push("></template>")), Z5(a18, b4, c15), a18 = a18.responseState.generateStaticMarkup ? true : b4.push("<!--/$-->"), a18;
      if (0 < d12.pendingTasks) {
        d12.rootSegmentID = a18.nextSegmentId++;
        0 < d12.completedSegments.length && a18.partialBoundaries.push(d12);
        var f15 = a18.responseState;
        var e14 = f15.nextSuspenseID++;
        f15 = f15.boundaryPrefix + e14.toString(16);
        d12 = d12.id = f15;
        za(b4, a18.responseState, d12);
        Z5(a18, b4, c15);
        return b4.push("<!--/$-->");
      }
      if (d12.byteSize > a18.progressiveChunkSize) return d12.rootSegmentID = a18.nextSegmentId++, a18.completedBoundaries.push(d12), za(b4, a18.responseState, d12.id), Z5(a18, b4, c15), b4.push("<!--/$-->");
      a18.responseState.generateStaticMarkup || b4.push("<!--$-->");
      c15 = d12.completedSegments;
      if (1 !== c15.length) throw Error(l12(391));
      Nb(a18, b4, c15[0]);
      a18 = a18.responseState.generateStaticMarkup ? true : b4.push("<!--/$-->");
      return a18;
    }
    function Ob(a18, b4, c15) {
      Aa(b4, a18.responseState, c15.formatContext, c15.id);
      Nb(a18, b4, c15);
      return Ba(b4, c15.formatContext);
    }
    function Pb(a18, b4, c15) {
      for (var d12 = c15.completedSegments, f15 = 0; f15 < d12.length; f15++) Qb(a18, b4, c15, d12[f15]);
      d12.length = 0;
      a18 = a18.responseState;
      d12 = c15.id;
      c15 = c15.rootSegmentID;
      b4.push(a18.startInlineScript);
      a18.sentCompleteBoundaryFunction ? b4.push('$RC("') : (a18.sentCompleteBoundaryFunction = true, b4.push('function $RC(a,b){a=document.getElementById(a);b=document.getElementById(b);b.parentNode.removeChild(b);if(a){a=a.previousSibling;var f=a.parentNode,c=a.nextSibling,e=0;do{if(c&&8===c.nodeType){var d=c.data;if("/$"===d)if(0===e)break;else e--;else"$"!==d&&"$?"!==d&&"$!"!==d||e++}d=c.nextSibling;f.removeChild(c);c=d}while(c);for(;b.firstChild;)f.insertBefore(b.firstChild,c);a.data="$";a._reactRetry&&a._reactRetry()}};$RC("'));
      if (null === d12) throw Error(l12(395));
      c15 = c15.toString(16);
      b4.push(d12);
      b4.push('","');
      b4.push(a18.segmentPrefix);
      b4.push(c15);
      return b4.push('")</script>');
    }
    function Qb(a18, b4, c15, d12) {
      if (2 === d12.status) return true;
      var f15 = d12.id;
      if (-1 === f15) {
        if (-1 === (d12.id = c15.rootSegmentID)) throw Error(l12(392));
        return Ob(a18, b4, d12);
      }
      Ob(a18, b4, d12);
      a18 = a18.responseState;
      b4.push(a18.startInlineScript);
      a18.sentCompleteSegmentFunction ? b4.push('$RS("') : (a18.sentCompleteSegmentFunction = true, b4.push('function $RS(a,b){a=document.getElementById(a);b=document.getElementById(b);for(a.parentNode.removeChild(a);a.firstChild;)b.parentNode.insertBefore(a.firstChild,b);b.parentNode.removeChild(b)};$RS("'));
      b4.push(a18.segmentPrefix);
      f15 = f15.toString(16);
      b4.push(f15);
      b4.push('","');
      b4.push(a18.placeholderPrefix);
      b4.push(f15);
      return b4.push('")</script>');
    }
    function Mb(a18, b4) {
      try {
        var c15 = a18.completedRootSegment;
        if (null !== c15 && 0 === a18.pendingRootTasks) {
          Nb(a18, b4, c15);
          a18.completedRootSegment = null;
          var d12 = a18.responseState.bootstrapChunks;
          for (c15 = 0; c15 < d12.length - 1; c15++) b4.push(d12[c15]);
          c15 < d12.length && b4.push(d12[c15]);
        }
        var f15 = a18.clientRenderedBoundaries, e14;
        for (e14 = 0; e14 < f15.length; e14++) {
          var g7 = f15[e14];
          d12 = b4;
          var h6 = a18.responseState, k5 = g7.id, m12 = g7.errorDigest, n13 = g7.errorMessage, q2 = g7.errorComponentStack;
          d12.push(h6.startInlineScript);
          h6.sentClientRenderFunction ? d12.push('$RX("') : (h6.sentClientRenderFunction = true, d12.push('function $RX(b,c,d,e){var a=document.getElementById(b);a&&(b=a.previousSibling,b.data="$!",a=a.dataset,c&&(a.dgst=c),d&&(a.msg=d),e&&(a.stck=e),b._reactRetry&&b._reactRetry())};$RX("'));
          if (null === k5) throw Error(l12(395));
          d12.push(k5);
          d12.push('"');
          if (m12 || n13 || q2) {
            d12.push(",");
            var C5 = Da(m12 || "");
            d12.push(C5);
          }
          if (n13 || q2) {
            d12.push(",");
            var D5 = Da(n13 || "");
            d12.push(D5);
          }
          if (q2) {
            d12.push(",");
            var z2 = Da(q2);
            d12.push(z2);
          }
          if (!d12.push(")</script>")) {
            a18.destination = null;
            e14++;
            f15.splice(0, e14);
            return;
          }
        }
        f15.splice(0, e14);
        var ba = a18.completedBoundaries;
        for (e14 = 0; e14 < ba.length; e14++) if (!Pb(a18, b4, ba[e14])) {
          a18.destination = null;
          e14++;
          ba.splice(0, e14);
          return;
        }
        ba.splice(0, e14);
        var ca = a18.partialBoundaries;
        for (e14 = 0; e14 < ca.length; e14++) {
          var mb = ca[e14];
          a: {
            f15 = a18;
            g7 = b4;
            var da = mb.completedSegments;
            for (h6 = 0; h6 < da.length; h6++) if (!Qb(f15, g7, mb, da[h6])) {
              h6++;
              da.splice(0, h6);
              var nb = false;
              break a;
            }
            da.splice(0, h6);
            nb = true;
          }
          if (!nb) {
            a18.destination = null;
            e14++;
            ca.splice(0, e14);
            return;
          }
        }
        ca.splice(0, e14);
        var ea = a18.completedBoundaries;
        for (e14 = 0; e14 < ea.length; e14++) if (!Pb(a18, b4, ea[e14])) {
          a18.destination = null;
          e14++;
          ea.splice(0, e14);
          return;
        }
        ea.splice(0, e14);
      } finally {
        0 === a18.allPendingTasks && 0 === a18.pingedTasks.length && 0 === a18.clientRenderedBoundaries.length && 0 === a18.completedBoundaries.length && b4.push(null);
      }
    }
    function Rb(a18, b4) {
      try {
        var c15 = a18.abortableTasks;
        c15.forEach(function(c16) {
          return Lb(c16, a18, b4);
        });
        c15.clear();
        null !== a18.destination && Mb(a18, a18.destination);
      } catch (d12) {
        V3(a18, d12), W4(a18, d12);
      }
    }
    function Sb() {
    }
    function Tb(a18, b4, c15, d12) {
      var f15 = false, e14 = null, g7 = "", h6 = { push: function(a19) {
        null !== a19 && (g7 += a19);
        return true;
      }, destroy: function(a19) {
        f15 = true;
        e14 = a19;
      } }, k5 = false;
      a18 = Ab(a18, Ea(c15, b4 ? b4.identifierPrefix : void 0), { insertionMode: 1, selectedValue: null }, Infinity, Sb, void 0, function() {
        k5 = true;
      }, void 0, void 0);
      Cb(a18);
      Rb(a18, d12);
      if (1 === a18.status) a18.status = 2, h6.destroy(a18.fatalError);
      else if (2 !== a18.status && null === a18.destination) {
        a18.destination = h6;
        try {
          Mb(a18, h6);
        } catch (m12) {
          V3(a18, m12), W4(a18, m12);
        }
      }
      if (f15) throw e14;
      if (!k5) throw Error(l12(426));
      return g7;
    }
    exports.renderToNodeStream = function() {
      throw Error(l12(207));
    };
    exports.renderToStaticMarkup = function(a18, b4) {
      return Tb(a18, b4, true, 'The server used "renderToStaticMarkup" which does not support Suspense. If you intended to have the server wait for the suspended component please switch to "renderToReadableStream" which supports Suspense on the server');
    };
    exports.renderToStaticNodeStream = function() {
      throw Error(l12(208));
    };
    exports.renderToString = function(a18, b4) {
      return Tb(a18, b4, false, 'The server used "renderToString" which does not support Suspense. If you intended for this Suspense boundary to render the fallback content on the server consider throwing an Error somewhere within the Suspense boundary. If you intended to have the server wait for the suspended component please switch to "renderToReadableStream" which supports Suspense on the server');
    };
    exports.version = "18.3.1";
  }
});

// node_modules/react-dom/cjs/react-dom-server.browser.production.min.js
var require_react_dom_server_browser_production_min = __commonJS({
  "node_modules/react-dom/cjs/react-dom-server.browser.production.min.js"(exports) {
    "use strict";
    var aa = require_react();
    function k5(a18) {
      for (var b4 = "https://reactjs.org/docs/error-decoder.html?invariant=" + a18, c15 = 1; c15 < arguments.length; c15++) b4 += "&args[]=" + encodeURIComponent(arguments[c15]);
      return "Minified React error #" + a18 + "; visit " + b4 + " for the full message or use the non-minified dev environment for full errors and additional helpful warnings.";
    }
    var l12 = null;
    var n13 = 0;
    function p11(a18, b4) {
      if (0 !== b4.length) if (512 < b4.length) 0 < n13 && (a18.enqueue(new Uint8Array(l12.buffer, 0, n13)), l12 = new Uint8Array(512), n13 = 0), a18.enqueue(b4);
      else {
        var c15 = l12.length - n13;
        c15 < b4.length && (0 === c15 ? a18.enqueue(l12) : (l12.set(b4.subarray(0, c15), n13), a18.enqueue(l12), b4 = b4.subarray(c15)), l12 = new Uint8Array(512), n13 = 0);
        l12.set(b4, n13);
        n13 += b4.length;
      }
    }
    function t20(a18, b4) {
      p11(a18, b4);
      return true;
    }
    function ba(a18) {
      l12 && 0 < n13 && (a18.enqueue(new Uint8Array(l12.buffer, 0, n13)), l12 = null, n13 = 0);
    }
    var ca = new TextEncoder();
    function u4(a18) {
      return ca.encode(a18);
    }
    function w11(a18) {
      return ca.encode(a18);
    }
    function da(a18, b4) {
      "function" === typeof a18.error ? a18.error(b4) : a18.close();
    }
    var x3 = Object.prototype.hasOwnProperty;
    var ea = /^[:A-Z_a-z\u00C0-\u00D6\u00D8-\u00F6\u00F8-\u02FF\u0370-\u037D\u037F-\u1FFF\u200C-\u200D\u2070-\u218F\u2C00-\u2FEF\u3001-\uD7FF\uF900-\uFDCF\uFDF0-\uFFFD][:A-Z_a-z\u00C0-\u00D6\u00D8-\u00F6\u00F8-\u02FF\u0370-\u037D\u037F-\u1FFF\u200C-\u200D\u2070-\u218F\u2C00-\u2FEF\u3001-\uD7FF\uF900-\uFDCF\uFDF0-\uFFFD\-.0-9\u00B7\u0300-\u036F\u203F-\u2040]*$/;
    var fa = {};
    var ha = {};
    function ia(a18) {
      if (x3.call(ha, a18)) return true;
      if (x3.call(fa, a18)) return false;
      if (ea.test(a18)) return ha[a18] = true;
      fa[a18] = true;
      return false;
    }
    function y4(a18, b4, c15, d12, f15, e14, g7) {
      this.acceptsBooleans = 2 === b4 || 3 === b4 || 4 === b4;
      this.attributeName = d12;
      this.attributeNamespace = f15;
      this.mustUseProperty = c15;
      this.propertyName = a18;
      this.type = b4;
      this.sanitizeURL = e14;
      this.removeEmptyString = g7;
    }
    var z2 = {};
    "children dangerouslySetInnerHTML defaultValue defaultChecked innerHTML suppressContentEditableWarning suppressHydrationWarning style".split(" ").forEach(function(a18) {
      z2[a18] = new y4(a18, 0, false, a18, null, false, false);
    });
    [["acceptCharset", "accept-charset"], ["className", "class"], ["htmlFor", "for"], ["httpEquiv", "http-equiv"]].forEach(function(a18) {
      var b4 = a18[0];
      z2[b4] = new y4(b4, 1, false, a18[1], null, false, false);
    });
    ["contentEditable", "draggable", "spellCheck", "value"].forEach(function(a18) {
      z2[a18] = new y4(a18, 2, false, a18.toLowerCase(), null, false, false);
    });
    ["autoReverse", "externalResourcesRequired", "focusable", "preserveAlpha"].forEach(function(a18) {
      z2[a18] = new y4(a18, 2, false, a18, null, false, false);
    });
    "allowFullScreen async autoFocus autoPlay controls default defer disabled disablePictureInPicture disableRemotePlayback formNoValidate hidden loop noModule noValidate open playsInline readOnly required reversed scoped seamless itemScope".split(" ").forEach(function(a18) {
      z2[a18] = new y4(a18, 3, false, a18.toLowerCase(), null, false, false);
    });
    ["checked", "multiple", "muted", "selected"].forEach(function(a18) {
      z2[a18] = new y4(a18, 3, true, a18, null, false, false);
    });
    ["capture", "download"].forEach(function(a18) {
      z2[a18] = new y4(a18, 4, false, a18, null, false, false);
    });
    ["cols", "rows", "size", "span"].forEach(function(a18) {
      z2[a18] = new y4(a18, 6, false, a18, null, false, false);
    });
    ["rowSpan", "start"].forEach(function(a18) {
      z2[a18] = new y4(a18, 5, false, a18.toLowerCase(), null, false, false);
    });
    var ja = /[\-:]([a-z])/g;
    function ka(a18) {
      return a18[1].toUpperCase();
    }
    "accent-height alignment-baseline arabic-form baseline-shift cap-height clip-path clip-rule color-interpolation color-interpolation-filters color-profile color-rendering dominant-baseline enable-background fill-opacity fill-rule flood-color flood-opacity font-family font-size font-size-adjust font-stretch font-style font-variant font-weight glyph-name glyph-orientation-horizontal glyph-orientation-vertical horiz-adv-x horiz-origin-x image-rendering letter-spacing lighting-color marker-end marker-mid marker-start overline-position overline-thickness paint-order panose-1 pointer-events rendering-intent shape-rendering stop-color stop-opacity strikethrough-position strikethrough-thickness stroke-dasharray stroke-dashoffset stroke-linecap stroke-linejoin stroke-miterlimit stroke-opacity stroke-width text-anchor text-decoration text-rendering underline-position underline-thickness unicode-bidi unicode-range units-per-em v-alphabetic v-hanging v-ideographic v-mathematical vector-effect vert-adv-y vert-origin-x vert-origin-y word-spacing writing-mode xmlns:xlink x-height".split(" ").forEach(function(a18) {
      var b4 = a18.replace(
        ja,
        ka
      );
      z2[b4] = new y4(b4, 1, false, a18, null, false, false);
    });
    "xlink:actuate xlink:arcrole xlink:role xlink:show xlink:title xlink:type".split(" ").forEach(function(a18) {
      var b4 = a18.replace(ja, ka);
      z2[b4] = new y4(b4, 1, false, a18, "http://www.w3.org/1999/xlink", false, false);
    });
    ["xml:base", "xml:lang", "xml:space"].forEach(function(a18) {
      var b4 = a18.replace(ja, ka);
      z2[b4] = new y4(b4, 1, false, a18, "http://www.w3.org/XML/1998/namespace", false, false);
    });
    ["tabIndex", "crossOrigin"].forEach(function(a18) {
      z2[a18] = new y4(a18, 1, false, a18.toLowerCase(), null, false, false);
    });
    z2.xlinkHref = new y4("xlinkHref", 1, false, "xlink:href", "http://www.w3.org/1999/xlink", true, false);
    ["src", "href", "action", "formAction"].forEach(function(a18) {
      z2[a18] = new y4(a18, 1, false, a18.toLowerCase(), null, true, true);
    });
    var B4 = {
      animationIterationCount: true,
      aspectRatio: true,
      borderImageOutset: true,
      borderImageSlice: true,
      borderImageWidth: true,
      boxFlex: true,
      boxFlexGroup: true,
      boxOrdinalGroup: true,
      columnCount: true,
      columns: true,
      flex: true,
      flexGrow: true,
      flexPositive: true,
      flexShrink: true,
      flexNegative: true,
      flexOrder: true,
      gridArea: true,
      gridRow: true,
      gridRowEnd: true,
      gridRowSpan: true,
      gridRowStart: true,
      gridColumn: true,
      gridColumnEnd: true,
      gridColumnSpan: true,
      gridColumnStart: true,
      fontWeight: true,
      lineClamp: true,
      lineHeight: true,
      opacity: true,
      order: true,
      orphans: true,
      tabSize: true,
      widows: true,
      zIndex: true,
      zoom: true,
      fillOpacity: true,
      floodOpacity: true,
      stopOpacity: true,
      strokeDasharray: true,
      strokeDashoffset: true,
      strokeMiterlimit: true,
      strokeOpacity: true,
      strokeWidth: true
    };
    var la = ["Webkit", "ms", "Moz", "O"];
    Object.keys(B4).forEach(function(a18) {
      la.forEach(function(b4) {
        b4 = b4 + a18.charAt(0).toUpperCase() + a18.substring(1);
        B4[b4] = B4[a18];
      });
    });
    var oa = /["'&<>]/;
    function C5(a18) {
      if ("boolean" === typeof a18 || "number" === typeof a18) return "" + a18;
      a18 = "" + a18;
      var b4 = oa.exec(a18);
      if (b4) {
        var c15 = "", d12, f15 = 0;
        for (d12 = b4.index; d12 < a18.length; d12++) {
          switch (a18.charCodeAt(d12)) {
            case 34:
              b4 = "&quot;";
              break;
            case 38:
              b4 = "&amp;";
              break;
            case 39:
              b4 = "&#x27;";
              break;
            case 60:
              b4 = "&lt;";
              break;
            case 62:
              b4 = "&gt;";
              break;
            default:
              continue;
          }
          f15 !== d12 && (c15 += a18.substring(f15, d12));
          f15 = d12 + 1;
          c15 += b4;
        }
        a18 = f15 !== d12 ? c15 + a18.substring(f15, d12) : c15;
      }
      return a18;
    }
    var pa = /([A-Z])/g;
    var qa = /^ms-/;
    var ra = Array.isArray;
    var sa = w11("<script>");
    var ta = w11("</script>");
    var ua = w11('<script src="');
    var va = w11('<script type="module" src="');
    var wa = w11('" async=""></script>');
    var xa = /(<\/|<)(s)(cript)/gi;
    function ya(a18, b4, c15, d12) {
      return "" + b4 + ("s" === c15 ? "\\u0073" : "\\u0053") + d12;
    }
    function za(a18, b4, c15, d12, f15) {
      a18 = void 0 === a18 ? "" : a18;
      b4 = void 0 === b4 ? sa : w11('<script nonce="' + C5(b4) + '">');
      var e14 = [];
      void 0 !== c15 && e14.push(b4, u4(("" + c15).replace(xa, ya)), ta);
      if (void 0 !== d12) for (c15 = 0; c15 < d12.length; c15++) e14.push(ua, u4(C5(d12[c15])), wa);
      if (void 0 !== f15) for (d12 = 0; d12 < f15.length; d12++) e14.push(va, u4(C5(f15[d12])), wa);
      return { bootstrapChunks: e14, startInlineScript: b4, placeholderPrefix: w11(a18 + "P:"), segmentPrefix: w11(a18 + "S:"), boundaryPrefix: a18 + "B:", idPrefix: a18, nextSuspenseID: 0, sentCompleteSegmentFunction: false, sentCompleteBoundaryFunction: false, sentClientRenderFunction: false };
    }
    function D5(a18, b4) {
      return { insertionMode: a18, selectedValue: b4 };
    }
    function Aa(a18) {
      return D5("http://www.w3.org/2000/svg" === a18 ? 2 : "http://www.w3.org/1998/Math/MathML" === a18 ? 3 : 0, null);
    }
    function Ba(a18, b4, c15) {
      switch (b4) {
        case "select":
          return D5(1, null != c15.value ? c15.value : c15.defaultValue);
        case "svg":
          return D5(2, null);
        case "math":
          return D5(3, null);
        case "foreignObject":
          return D5(1, null);
        case "table":
          return D5(4, null);
        case "thead":
        case "tbody":
        case "tfoot":
          return D5(5, null);
        case "colgroup":
          return D5(7, null);
        case "tr":
          return D5(6, null);
      }
      return 4 <= a18.insertionMode || 0 === a18.insertionMode ? D5(1, null) : a18;
    }
    var Ca = w11("<!-- -->");
    function Da(a18, b4, c15, d12) {
      if ("" === b4) return d12;
      d12 && a18.push(Ca);
      a18.push(u4(C5(b4)));
      return true;
    }
    var Ea = /* @__PURE__ */ new Map();
    var Fa = w11(' style="');
    var Ga = w11(":");
    var Ha = w11(";");
    function Ia(a18, b4, c15) {
      if ("object" !== typeof c15) throw Error(k5(62));
      b4 = true;
      for (var d12 in c15) if (x3.call(c15, d12)) {
        var f15 = c15[d12];
        if (null != f15 && "boolean" !== typeof f15 && "" !== f15) {
          if (0 === d12.indexOf("--")) {
            var e14 = u4(C5(d12));
            f15 = u4(C5(("" + f15).trim()));
          } else {
            e14 = d12;
            var g7 = Ea.get(e14);
            void 0 !== g7 ? e14 = g7 : (g7 = w11(C5(e14.replace(pa, "-$1").toLowerCase().replace(qa, "-ms-"))), Ea.set(e14, g7), e14 = g7);
            f15 = "number" === typeof f15 ? 0 === f15 || x3.call(B4, d12) ? u4("" + f15) : u4(f15 + "px") : u4(C5(("" + f15).trim()));
          }
          b4 ? (b4 = false, a18.push(Fa, e14, Ga, f15)) : a18.push(Ha, e14, Ga, f15);
        }
      }
      b4 || a18.push(E6);
    }
    var H4 = w11(" ");
    var I6 = w11('="');
    var E6 = w11('"');
    var Ja = w11('=""');
    function J2(a18, b4, c15, d12) {
      switch (c15) {
        case "style":
          Ia(a18, b4, d12);
          return;
        case "defaultValue":
        case "defaultChecked":
        case "innerHTML":
        case "suppressContentEditableWarning":
        case "suppressHydrationWarning":
          return;
      }
      if (!(2 < c15.length) || "o" !== c15[0] && "O" !== c15[0] || "n" !== c15[1] && "N" !== c15[1]) {
        if (b4 = z2.hasOwnProperty(c15) ? z2[c15] : null, null !== b4) {
          switch (typeof d12) {
            case "function":
            case "symbol":
              return;
            case "boolean":
              if (!b4.acceptsBooleans) return;
          }
          c15 = u4(b4.attributeName);
          switch (b4.type) {
            case 3:
              d12 && a18.push(H4, c15, Ja);
              break;
            case 4:
              true === d12 ? a18.push(H4, c15, Ja) : false !== d12 && a18.push(H4, c15, I6, u4(C5(d12)), E6);
              break;
            case 5:
              isNaN(d12) || a18.push(H4, c15, I6, u4(C5(d12)), E6);
              break;
            case 6:
              !isNaN(d12) && 1 <= d12 && a18.push(H4, c15, I6, u4(C5(d12)), E6);
              break;
            default:
              b4.sanitizeURL && (d12 = "" + d12), a18.push(H4, c15, I6, u4(C5(d12)), E6);
          }
        } else if (ia(c15)) {
          switch (typeof d12) {
            case "function":
            case "symbol":
              return;
            case "boolean":
              if (b4 = c15.toLowerCase().slice(0, 5), "data-" !== b4 && "aria-" !== b4) return;
          }
          a18.push(H4, u4(c15), I6, u4(C5(d12)), E6);
        }
      }
    }
    var K3 = w11(">");
    var Ka = w11("/>");
    function L3(a18, b4, c15) {
      if (null != b4) {
        if (null != c15) throw Error(k5(60));
        if ("object" !== typeof b4 || !("__html" in b4)) throw Error(k5(61));
        b4 = b4.__html;
        null !== b4 && void 0 !== b4 && a18.push(u4("" + b4));
      }
    }
    function La(a18) {
      var b4 = "";
      aa.Children.forEach(a18, function(a19) {
        null != a19 && (b4 += a19);
      });
      return b4;
    }
    var Ma = w11(' selected=""');
    function Na(a18, b4, c15, d12) {
      a18.push(M6(c15));
      var f15 = c15 = null, e14;
      for (e14 in b4) if (x3.call(b4, e14)) {
        var g7 = b4[e14];
        if (null != g7) switch (e14) {
          case "children":
            c15 = g7;
            break;
          case "dangerouslySetInnerHTML":
            f15 = g7;
            break;
          default:
            J2(a18, d12, e14, g7);
        }
      }
      a18.push(K3);
      L3(a18, f15, c15);
      return "string" === typeof c15 ? (a18.push(u4(C5(c15))), null) : c15;
    }
    var Oa = w11("\n");
    var Pa = /^[a-zA-Z][a-zA-Z:_\.\-\d]*$/;
    var Qa = /* @__PURE__ */ new Map();
    function M6(a18) {
      var b4 = Qa.get(a18);
      if (void 0 === b4) {
        if (!Pa.test(a18)) throw Error(k5(65, a18));
        b4 = w11("<" + a18);
        Qa.set(a18, b4);
      }
      return b4;
    }
    var Ra = w11("<!DOCTYPE html>");
    function Sa(a18, b4, c15, d12, f15) {
      switch (b4) {
        case "select":
          a18.push(M6("select"));
          var e14 = null, g7 = null;
          for (r7 in c15) if (x3.call(c15, r7)) {
            var h6 = c15[r7];
            if (null != h6) switch (r7) {
              case "children":
                e14 = h6;
                break;
              case "dangerouslySetInnerHTML":
                g7 = h6;
                break;
              case "defaultValue":
              case "value":
                break;
              default:
                J2(a18, d12, r7, h6);
            }
          }
          a18.push(K3);
          L3(a18, g7, e14);
          return e14;
        case "option":
          g7 = f15.selectedValue;
          a18.push(M6("option"));
          var m12 = h6 = null, q2 = null;
          var r7 = null;
          for (e14 in c15) if (x3.call(c15, e14)) {
            var v5 = c15[e14];
            if (null != v5) switch (e14) {
              case "children":
                h6 = v5;
                break;
              case "selected":
                q2 = v5;
                break;
              case "dangerouslySetInnerHTML":
                r7 = v5;
                break;
              case "value":
                m12 = v5;
              default:
                J2(a18, d12, e14, v5);
            }
          }
          if (null != g7) if (c15 = null !== m12 ? "" + m12 : La(h6), ra(g7)) for (d12 = 0; d12 < g7.length; d12++) {
            if ("" + g7[d12] === c15) {
              a18.push(Ma);
              break;
            }
          }
          else "" + g7 === c15 && a18.push(Ma);
          else q2 && a18.push(Ma);
          a18.push(K3);
          L3(a18, r7, h6);
          return h6;
        case "textarea":
          a18.push(M6("textarea"));
          r7 = g7 = e14 = null;
          for (h6 in c15) if (x3.call(c15, h6) && (m12 = c15[h6], null != m12)) switch (h6) {
            case "children":
              r7 = m12;
              break;
            case "value":
              e14 = m12;
              break;
            case "defaultValue":
              g7 = m12;
              break;
            case "dangerouslySetInnerHTML":
              throw Error(k5(91));
            default:
              J2(a18, d12, h6, m12);
          }
          null === e14 && null !== g7 && (e14 = g7);
          a18.push(K3);
          if (null != r7) {
            if (null != e14) throw Error(k5(92));
            if (ra(r7) && 1 < r7.length) throw Error(k5(93));
            e14 = "" + r7;
          }
          "string" === typeof e14 && "\n" === e14[0] && a18.push(Oa);
          null !== e14 && a18.push(u4(C5("" + e14)));
          return null;
        case "input":
          a18.push(M6("input"));
          m12 = r7 = h6 = e14 = null;
          for (g7 in c15) if (x3.call(c15, g7) && (q2 = c15[g7], null != q2)) switch (g7) {
            case "children":
            case "dangerouslySetInnerHTML":
              throw Error(k5(399, "input"));
            case "defaultChecked":
              m12 = q2;
              break;
            case "defaultValue":
              h6 = q2;
              break;
            case "checked":
              r7 = q2;
              break;
            case "value":
              e14 = q2;
              break;
            default:
              J2(a18, d12, g7, q2);
          }
          null !== r7 ? J2(
            a18,
            d12,
            "checked",
            r7
          ) : null !== m12 && J2(a18, d12, "checked", m12);
          null !== e14 ? J2(a18, d12, "value", e14) : null !== h6 && J2(a18, d12, "value", h6);
          a18.push(Ka);
          return null;
        case "menuitem":
          a18.push(M6("menuitem"));
          for (var A4 in c15) if (x3.call(c15, A4) && (e14 = c15[A4], null != e14)) switch (A4) {
            case "children":
            case "dangerouslySetInnerHTML":
              throw Error(k5(400));
            default:
              J2(a18, d12, A4, e14);
          }
          a18.push(K3);
          return null;
        case "title":
          a18.push(M6("title"));
          e14 = null;
          for (v5 in c15) if (x3.call(c15, v5) && (g7 = c15[v5], null != g7)) switch (v5) {
            case "children":
              e14 = g7;
              break;
            case "dangerouslySetInnerHTML":
              throw Error(k5(434));
            default:
              J2(a18, d12, v5, g7);
          }
          a18.push(K3);
          return e14;
        case "listing":
        case "pre":
          a18.push(M6(b4));
          g7 = e14 = null;
          for (m12 in c15) if (x3.call(c15, m12) && (h6 = c15[m12], null != h6)) switch (m12) {
            case "children":
              e14 = h6;
              break;
            case "dangerouslySetInnerHTML":
              g7 = h6;
              break;
            default:
              J2(a18, d12, m12, h6);
          }
          a18.push(K3);
          if (null != g7) {
            if (null != e14) throw Error(k5(60));
            if ("object" !== typeof g7 || !("__html" in g7)) throw Error(k5(61));
            c15 = g7.__html;
            null !== c15 && void 0 !== c15 && ("string" === typeof c15 && 0 < c15.length && "\n" === c15[0] ? a18.push(Oa, u4(c15)) : a18.push(u4("" + c15)));
          }
          "string" === typeof e14 && "\n" === e14[0] && a18.push(Oa);
          return e14;
        case "area":
        case "base":
        case "br":
        case "col":
        case "embed":
        case "hr":
        case "img":
        case "keygen":
        case "link":
        case "meta":
        case "param":
        case "source":
        case "track":
        case "wbr":
          a18.push(M6(b4));
          for (var F4 in c15) if (x3.call(c15, F4) && (e14 = c15[F4], null != e14)) switch (F4) {
            case "children":
            case "dangerouslySetInnerHTML":
              throw Error(k5(399, b4));
            default:
              J2(a18, d12, F4, e14);
          }
          a18.push(Ka);
          return null;
        case "annotation-xml":
        case "color-profile":
        case "font-face":
        case "font-face-src":
        case "font-face-uri":
        case "font-face-format":
        case "font-face-name":
        case "missing-glyph":
          return Na(a18, c15, b4, d12);
        case "html":
          return 0 === f15.insertionMode && a18.push(Ra), Na(a18, c15, b4, d12);
        default:
          if (-1 === b4.indexOf("-") && "string" !== typeof c15.is) return Na(a18, c15, b4, d12);
          a18.push(M6(b4));
          g7 = e14 = null;
          for (q2 in c15) if (x3.call(c15, q2) && (h6 = c15[q2], null != h6)) switch (q2) {
            case "children":
              e14 = h6;
              break;
            case "dangerouslySetInnerHTML":
              g7 = h6;
              break;
            case "style":
              Ia(a18, d12, h6);
              break;
            case "suppressContentEditableWarning":
            case "suppressHydrationWarning":
              break;
            default:
              ia(q2) && "function" !== typeof h6 && "symbol" !== typeof h6 && a18.push(H4, u4(q2), I6, u4(C5(h6)), E6);
          }
          a18.push(K3);
          L3(a18, g7, e14);
          return e14;
      }
    }
    var Ta = w11("</");
    var Ua = w11(">");
    var Va = w11('<template id="');
    var Wa = w11('"></template>');
    var Xa = w11("<!--$-->");
    var Ya = w11('<!--$?--><template id="');
    var Za = w11('"></template>');
    var $a = w11("<!--$!-->");
    var ab = w11("<!--/$-->");
    var bb = w11("<template");
    var cb = w11('"');
    var db = w11(' data-dgst="');
    w11(' data-msg="');
    w11(' data-stck="');
    var eb = w11("></template>");
    function fb(a18, b4, c15) {
      p11(a18, Ya);
      if (null === c15) throw Error(k5(395));
      p11(a18, c15);
      return t20(a18, Za);
    }
    var gb = w11('<div hidden id="');
    var hb = w11('">');
    var ib = w11("</div>");
    var jb = w11('<svg aria-hidden="true" style="display:none" id="');
    var kb = w11('">');
    var lb = w11("</svg>");
    var mb = w11('<math aria-hidden="true" style="display:none" id="');
    var nb = w11('">');
    var ob = w11("</math>");
    var pb = w11('<table hidden id="');
    var qb = w11('">');
    var rb = w11("</table>");
    var sb = w11('<table hidden><tbody id="');
    var tb = w11('">');
    var ub = w11("</tbody></table>");
    var vb = w11('<table hidden><tr id="');
    var wb = w11('">');
    var xb = w11("</tr></table>");
    var yb = w11('<table hidden><colgroup id="');
    var zb = w11('">');
    var Ab = w11("</colgroup></table>");
    function Bb(a18, b4, c15, d12) {
      switch (c15.insertionMode) {
        case 0:
        case 1:
          return p11(a18, gb), p11(a18, b4.segmentPrefix), p11(a18, u4(d12.toString(16))), t20(a18, hb);
        case 2:
          return p11(a18, jb), p11(a18, b4.segmentPrefix), p11(a18, u4(d12.toString(16))), t20(a18, kb);
        case 3:
          return p11(a18, mb), p11(a18, b4.segmentPrefix), p11(a18, u4(d12.toString(16))), t20(a18, nb);
        case 4:
          return p11(a18, pb), p11(a18, b4.segmentPrefix), p11(a18, u4(d12.toString(16))), t20(a18, qb);
        case 5:
          return p11(a18, sb), p11(a18, b4.segmentPrefix), p11(a18, u4(d12.toString(16))), t20(a18, tb);
        case 6:
          return p11(a18, vb), p11(a18, b4.segmentPrefix), p11(a18, u4(d12.toString(16))), t20(a18, wb);
        case 7:
          return p11(
            a18,
            yb
          ), p11(a18, b4.segmentPrefix), p11(a18, u4(d12.toString(16))), t20(a18, zb);
        default:
          throw Error(k5(397));
      }
    }
    function Cb(a18, b4) {
      switch (b4.insertionMode) {
        case 0:
        case 1:
          return t20(a18, ib);
        case 2:
          return t20(a18, lb);
        case 3:
          return t20(a18, ob);
        case 4:
          return t20(a18, rb);
        case 5:
          return t20(a18, ub);
        case 6:
          return t20(a18, xb);
        case 7:
          return t20(a18, Ab);
        default:
          throw Error(k5(397));
      }
    }
    var Db = w11('function $RS(a,b){a=document.getElementById(a);b=document.getElementById(b);for(a.parentNode.removeChild(a);a.firstChild;)b.parentNode.insertBefore(a.firstChild,b);b.parentNode.removeChild(b)};$RS("');
    var Eb = w11('$RS("');
    var Gb = w11('","');
    var Hb = w11('")</script>');
    var Ib = w11('function $RC(a,b){a=document.getElementById(a);b=document.getElementById(b);b.parentNode.removeChild(b);if(a){a=a.previousSibling;var f=a.parentNode,c=a.nextSibling,e=0;do{if(c&&8===c.nodeType){var d=c.data;if("/$"===d)if(0===e)break;else e--;else"$"!==d&&"$?"!==d&&"$!"!==d||e++}d=c.nextSibling;f.removeChild(c);c=d}while(c);for(;b.firstChild;)f.insertBefore(b.firstChild,c);a.data="$";a._reactRetry&&a._reactRetry()}};$RC("');
    var Jb = w11('$RC("');
    var Kb = w11('","');
    var Lb = w11('")</script>');
    var Mb = w11('function $RX(b,c,d,e){var a=document.getElementById(b);a&&(b=a.previousSibling,b.data="$!",a=a.dataset,c&&(a.dgst=c),d&&(a.msg=d),e&&(a.stck=e),b._reactRetry&&b._reactRetry())};$RX("');
    var Nb = w11('$RX("');
    var Ob = w11('"');
    var Pb = w11(")</script>");
    var Qb = w11(",");
    var Rb = /[<\u2028\u2029]/g;
    function Sb(a18) {
      return JSON.stringify(a18).replace(Rb, function(a19) {
        switch (a19) {
          case "<":
            return "\\u003c";
          case "\u2028":
            return "\\u2028";
          case "\u2029":
            return "\\u2029";
          default:
            throw Error("escapeJSStringsForInstructionScripts encountered a match it does not know how to replace. this means the match regex and the replacement characters are no longer in sync. This is a bug in React");
        }
      });
    }
    var N4 = Object.assign;
    var Tb = Symbol.for("react.element");
    var Ub = Symbol.for("react.portal");
    var Vb = Symbol.for("react.fragment");
    var Wb = Symbol.for("react.strict_mode");
    var Xb = Symbol.for("react.profiler");
    var Yb = Symbol.for("react.provider");
    var Zb = Symbol.for("react.context");
    var $b = Symbol.for("react.forward_ref");
    var ac = Symbol.for("react.suspense");
    var bc = Symbol.for("react.suspense_list");
    var cc = Symbol.for("react.memo");
    var dc = Symbol.for("react.lazy");
    var ec = Symbol.for("react.scope");
    var fc = Symbol.for("react.debug_trace_mode");
    var gc = Symbol.for("react.legacy_hidden");
    var hc = Symbol.for("react.default_value");
    var ic = Symbol.iterator;
    function jc(a18) {
      if (null == a18) return null;
      if ("function" === typeof a18) return a18.displayName || a18.name || null;
      if ("string" === typeof a18) return a18;
      switch (a18) {
        case Vb:
          return "Fragment";
        case Ub:
          return "Portal";
        case Xb:
          return "Profiler";
        case Wb:
          return "StrictMode";
        case ac:
          return "Suspense";
        case bc:
          return "SuspenseList";
      }
      if ("object" === typeof a18) switch (a18.$$typeof) {
        case Zb:
          return (a18.displayName || "Context") + ".Consumer";
        case Yb:
          return (a18._context.displayName || "Context") + ".Provider";
        case $b:
          var b4 = a18.render;
          a18 = a18.displayName;
          a18 || (a18 = b4.displayName || b4.name || "", a18 = "" !== a18 ? "ForwardRef(" + a18 + ")" : "ForwardRef");
          return a18;
        case cc:
          return b4 = a18.displayName || null, null !== b4 ? b4 : jc(a18.type) || "Memo";
        case dc:
          b4 = a18._payload;
          a18 = a18._init;
          try {
            return jc(a18(b4));
          } catch (c15) {
          }
      }
      return null;
    }
    var kc = {};
    function lc(a18, b4) {
      a18 = a18.contextTypes;
      if (!a18) return kc;
      var c15 = {}, d12;
      for (d12 in a18) c15[d12] = b4[d12];
      return c15;
    }
    var O5 = null;
    function P4(a18, b4) {
      if (a18 !== b4) {
        a18.context._currentValue = a18.parentValue;
        a18 = a18.parent;
        var c15 = b4.parent;
        if (null === a18) {
          if (null !== c15) throw Error(k5(401));
        } else {
          if (null === c15) throw Error(k5(401));
          P4(a18, c15);
        }
        b4.context._currentValue = b4.value;
      }
    }
    function mc(a18) {
      a18.context._currentValue = a18.parentValue;
      a18 = a18.parent;
      null !== a18 && mc(a18);
    }
    function nc(a18) {
      var b4 = a18.parent;
      null !== b4 && nc(b4);
      a18.context._currentValue = a18.value;
    }
    function oc(a18, b4) {
      a18.context._currentValue = a18.parentValue;
      a18 = a18.parent;
      if (null === a18) throw Error(k5(402));
      a18.depth === b4.depth ? P4(a18, b4) : oc(a18, b4);
    }
    function pc43(a18, b4) {
      var c15 = b4.parent;
      if (null === c15) throw Error(k5(402));
      a18.depth === c15.depth ? P4(a18, c15) : pc43(a18, c15);
      b4.context._currentValue = b4.value;
    }
    function Q4(a18) {
      var b4 = O5;
      b4 !== a18 && (null === b4 ? nc(a18) : null === a18 ? mc(b4) : b4.depth === a18.depth ? P4(b4, a18) : b4.depth > a18.depth ? oc(b4, a18) : pc43(b4, a18), O5 = a18);
    }
    var qc = { isMounted: function() {
      return false;
    }, enqueueSetState: function(a18, b4) {
      a18 = a18._reactInternals;
      null !== a18.queue && a18.queue.push(b4);
    }, enqueueReplaceState: function(a18, b4) {
      a18 = a18._reactInternals;
      a18.replace = true;
      a18.queue = [b4];
    }, enqueueForceUpdate: function() {
    } };
    function rc(a18, b4, c15, d12) {
      var f15 = void 0 !== a18.state ? a18.state : null;
      a18.updater = qc;
      a18.props = c15;
      a18.state = f15;
      var e14 = { queue: [], replace: false };
      a18._reactInternals = e14;
      var g7 = b4.contextType;
      a18.context = "object" === typeof g7 && null !== g7 ? g7._currentValue : d12;
      g7 = b4.getDerivedStateFromProps;
      "function" === typeof g7 && (g7 = g7(c15, f15), f15 = null === g7 || void 0 === g7 ? f15 : N4({}, f15, g7), a18.state = f15);
      if ("function" !== typeof b4.getDerivedStateFromProps && "function" !== typeof a18.getSnapshotBeforeUpdate && ("function" === typeof a18.UNSAFE_componentWillMount || "function" === typeof a18.componentWillMount)) if (b4 = a18.state, "function" === typeof a18.componentWillMount && a18.componentWillMount(), "function" === typeof a18.UNSAFE_componentWillMount && a18.UNSAFE_componentWillMount(), b4 !== a18.state && qc.enqueueReplaceState(a18, a18.state, null), null !== e14.queue && 0 < e14.queue.length) if (b4 = e14.queue, g7 = e14.replace, e14.queue = null, e14.replace = false, g7 && 1 === b4.length) a18.state = b4[0];
      else {
        e14 = g7 ? b4[0] : a18.state;
        f15 = true;
        for (g7 = g7 ? 1 : 0; g7 < b4.length; g7++) {
          var h6 = b4[g7];
          h6 = "function" === typeof h6 ? h6.call(a18, e14, c15, d12) : h6;
          null != h6 && (f15 ? (f15 = false, e14 = N4({}, e14, h6)) : N4(e14, h6));
        }
        a18.state = e14;
      }
      else e14.queue = null;
    }
    var sc = { id: 1, overflow: "" };
    function tc(a18, b4, c15) {
      var d12 = a18.id;
      a18 = a18.overflow;
      var f15 = 32 - uc(d12) - 1;
      d12 &= ~(1 << f15);
      c15 += 1;
      var e14 = 32 - uc(b4) + f15;
      if (30 < e14) {
        var g7 = f15 - f15 % 5;
        e14 = (d12 & (1 << g7) - 1).toString(32);
        d12 >>= g7;
        f15 -= g7;
        return { id: 1 << 32 - uc(b4) + f15 | c15 << f15 | d12, overflow: e14 + a18 };
      }
      return { id: 1 << e14 | c15 << f15 | d12, overflow: a18 };
    }
    var uc = Math.clz32 ? Math.clz32 : vc;
    var wc = Math.log;
    var xc = Math.LN2;
    function vc(a18) {
      a18 >>>= 0;
      return 0 === a18 ? 32 : 31 - (wc(a18) / xc | 0) | 0;
    }
    function yc(a18, b4) {
      return a18 === b4 && (0 !== a18 || 1 / a18 === 1 / b4) || a18 !== a18 && b4 !== b4;
    }
    var zc = "function" === typeof Object.is ? Object.is : yc;
    var R10 = null;
    var Ac = null;
    var Bc = null;
    var S2 = null;
    var T3 = false;
    var Cc = false;
    var U3 = 0;
    var V3 = null;
    var Dc = 0;
    function W4() {
      if (null === R10) throw Error(k5(321));
      return R10;
    }
    function Ec() {
      if (0 < Dc) throw Error(k5(312));
      return { memoizedState: null, queue: null, next: null };
    }
    function Fc() {
      null === S2 ? null === Bc ? (T3 = false, Bc = S2 = Ec()) : (T3 = true, S2 = Bc) : null === S2.next ? (T3 = false, S2 = S2.next = Ec()) : (T3 = true, S2 = S2.next);
      return S2;
    }
    function Gc() {
      Ac = R10 = null;
      Cc = false;
      Bc = null;
      Dc = 0;
      S2 = V3 = null;
    }
    function Hc(a18, b4) {
      return "function" === typeof b4 ? b4(a18) : b4;
    }
    function Ic(a18, b4, c15) {
      R10 = W4();
      S2 = Fc();
      if (T3) {
        var d12 = S2.queue;
        b4 = d12.dispatch;
        if (null !== V3 && (c15 = V3.get(d12), void 0 !== c15)) {
          V3.delete(d12);
          d12 = S2.memoizedState;
          do
            d12 = a18(d12, c15.action), c15 = c15.next;
          while (null !== c15);
          S2.memoizedState = d12;
          return [d12, b4];
        }
        return [S2.memoizedState, b4];
      }
      a18 = a18 === Hc ? "function" === typeof b4 ? b4() : b4 : void 0 !== c15 ? c15(b4) : b4;
      S2.memoizedState = a18;
      a18 = S2.queue = { last: null, dispatch: null };
      a18 = a18.dispatch = Jc.bind(null, R10, a18);
      return [S2.memoizedState, a18];
    }
    function Kc(a18, b4) {
      R10 = W4();
      S2 = Fc();
      b4 = void 0 === b4 ? null : b4;
      if (null !== S2) {
        var c15 = S2.memoizedState;
        if (null !== c15 && null !== b4) {
          var d12 = c15[1];
          a: if (null === d12) d12 = false;
          else {
            for (var f15 = 0; f15 < d12.length && f15 < b4.length; f15++) if (!zc(b4[f15], d12[f15])) {
              d12 = false;
              break a;
            }
            d12 = true;
          }
          if (d12) return c15[0];
        }
      }
      a18 = a18();
      S2.memoizedState = [a18, b4];
      return a18;
    }
    function Jc(a18, b4, c15) {
      if (25 <= Dc) throw Error(k5(301));
      if (a18 === R10) if (Cc = true, a18 = { action: c15, next: null }, null === V3 && (V3 = /* @__PURE__ */ new Map()), c15 = V3.get(b4), void 0 === c15) V3.set(b4, a18);
      else {
        for (b4 = c15; null !== b4.next; ) b4 = b4.next;
        b4.next = a18;
      }
    }
    function Lc() {
      throw Error(k5(394));
    }
    function Mc() {
    }
    var Oc = { readContext: function(a18) {
      return a18._currentValue;
    }, useContext: function(a18) {
      W4();
      return a18._currentValue;
    }, useMemo: Kc, useReducer: Ic, useRef: function(a18) {
      R10 = W4();
      S2 = Fc();
      var b4 = S2.memoizedState;
      return null === b4 ? (a18 = { current: a18 }, S2.memoizedState = a18) : b4;
    }, useState: function(a18) {
      return Ic(Hc, a18);
    }, useInsertionEffect: Mc, useLayoutEffect: function() {
    }, useCallback: function(a18, b4) {
      return Kc(function() {
        return a18;
      }, b4);
    }, useImperativeHandle: Mc, useEffect: Mc, useDebugValue: Mc, useDeferredValue: function(a18) {
      W4();
      return a18;
    }, useTransition: function() {
      W4();
      return [false, Lc];
    }, useId: function() {
      var a18 = Ac.treeContext;
      var b4 = a18.overflow;
      a18 = a18.id;
      a18 = (a18 & ~(1 << 32 - uc(a18) - 1)).toString(32) + b4;
      var c15 = Nc;
      if (null === c15) throw Error(k5(404));
      b4 = U3++;
      a18 = ":" + c15.idPrefix + "R" + a18;
      0 < b4 && (a18 += "H" + b4.toString(32));
      return a18 + ":";
    }, useMutableSource: function(a18, b4) {
      W4();
      return b4(a18._source);
    }, useSyncExternalStore: function(a18, b4, c15) {
      if (void 0 === c15) throw Error(k5(407));
      return c15();
    } };
    var Nc = null;
    var Pc = aa.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED.ReactCurrentDispatcher;
    function Qc(a18) {
      console.error(a18);
      return null;
    }
    function X4() {
    }
    function Rc(a18, b4, c15, d12, f15, e14, g7, h6, m12) {
      var q2 = [], r7 = /* @__PURE__ */ new Set();
      b4 = { destination: null, responseState: b4, progressiveChunkSize: void 0 === d12 ? 12800 : d12, status: 0, fatalError: null, nextSegmentId: 0, allPendingTasks: 0, pendingRootTasks: 0, completedRootSegment: null, abortableTasks: r7, pingedTasks: q2, clientRenderedBoundaries: [], completedBoundaries: [], partialBoundaries: [], onError: void 0 === f15 ? Qc : f15, onAllReady: void 0 === e14 ? X4 : e14, onShellReady: void 0 === g7 ? X4 : g7, onShellError: void 0 === h6 ? X4 : h6, onFatalError: void 0 === m12 ? X4 : m12 };
      c15 = Sc(b4, 0, null, c15, false, false);
      c15.parentFlushed = true;
      a18 = Tc(b4, a18, null, c15, r7, kc, null, sc);
      q2.push(a18);
      return b4;
    }
    function Tc(a18, b4, c15, d12, f15, e14, g7, h6) {
      a18.allPendingTasks++;
      null === c15 ? a18.pendingRootTasks++ : c15.pendingTasks++;
      var m12 = { node: b4, ping: function() {
        var b5 = a18.pingedTasks;
        b5.push(m12);
        1 === b5.length && Uc(a18);
      }, blockedBoundary: c15, blockedSegment: d12, abortSet: f15, legacyContext: e14, context: g7, treeContext: h6 };
      f15.add(m12);
      return m12;
    }
    function Sc(a18, b4, c15, d12, f15, e14) {
      return { status: 0, id: -1, index: b4, parentFlushed: false, chunks: [], children: [], formatContext: d12, boundary: c15, lastPushedText: f15, textEmbedded: e14 };
    }
    function Y3(a18, b4) {
      a18 = a18.onError(b4);
      if (null != a18 && "string" !== typeof a18) throw Error('onError returned something with a type other than "string". onError should return a string and may return null or undefined but must not return anything else. It received something of type "' + typeof a18 + '" instead');
      return a18;
    }
    function Vc(a18, b4) {
      var c15 = a18.onShellError;
      c15(b4);
      c15 = a18.onFatalError;
      c15(b4);
      null !== a18.destination ? (a18.status = 2, da(a18.destination, b4)) : (a18.status = 1, a18.fatalError = b4);
    }
    function Wc(a18, b4, c15, d12, f15) {
      R10 = {};
      Ac = b4;
      U3 = 0;
      for (a18 = c15(d12, f15); Cc; ) Cc = false, U3 = 0, Dc += 1, S2 = null, a18 = c15(d12, f15);
      Gc();
      return a18;
    }
    function Xc(a18, b4, c15, d12) {
      var f15 = c15.render(), e14 = d12.childContextTypes;
      if (null !== e14 && void 0 !== e14) {
        var g7 = b4.legacyContext;
        if ("function" !== typeof c15.getChildContext) d12 = g7;
        else {
          c15 = c15.getChildContext();
          for (var h6 in c15) if (!(h6 in e14)) throw Error(k5(108, jc(d12) || "Unknown", h6));
          d12 = N4({}, g7, c15);
        }
        b4.legacyContext = d12;
        Z5(a18, b4, f15);
        b4.legacyContext = g7;
      } else Z5(a18, b4, f15);
    }
    function Yc(a18, b4) {
      if (a18 && a18.defaultProps) {
        b4 = N4({}, b4);
        a18 = a18.defaultProps;
        for (var c15 in a18) void 0 === b4[c15] && (b4[c15] = a18[c15]);
        return b4;
      }
      return b4;
    }
    function Zc(a18, b4, c15, d12, f15) {
      if ("function" === typeof c15) if (c15.prototype && c15.prototype.isReactComponent) {
        f15 = lc(c15, b4.legacyContext);
        var e14 = c15.contextType;
        e14 = new c15(d12, "object" === typeof e14 && null !== e14 ? e14._currentValue : f15);
        rc(e14, c15, d12, f15);
        Xc(a18, b4, e14, c15);
      } else {
        e14 = lc(c15, b4.legacyContext);
        f15 = Wc(a18, b4, c15, d12, e14);
        var g7 = 0 !== U3;
        if ("object" === typeof f15 && null !== f15 && "function" === typeof f15.render && void 0 === f15.$$typeof) rc(f15, c15, d12, e14), Xc(a18, b4, f15, c15);
        else if (g7) {
          d12 = b4.treeContext;
          b4.treeContext = tc(d12, 1, 0);
          try {
            Z5(a18, b4, f15);
          } finally {
            b4.treeContext = d12;
          }
        } else Z5(a18, b4, f15);
      }
      else if ("string" === typeof c15) {
        f15 = b4.blockedSegment;
        e14 = Sa(f15.chunks, c15, d12, a18.responseState, f15.formatContext);
        f15.lastPushedText = false;
        g7 = f15.formatContext;
        f15.formatContext = Ba(g7, c15, d12);
        $c(a18, b4, e14);
        f15.formatContext = g7;
        switch (c15) {
          case "area":
          case "base":
          case "br":
          case "col":
          case "embed":
          case "hr":
          case "img":
          case "input":
          case "keygen":
          case "link":
          case "meta":
          case "param":
          case "source":
          case "track":
          case "wbr":
            break;
          default:
            f15.chunks.push(Ta, u4(c15), Ua);
        }
        f15.lastPushedText = false;
      } else {
        switch (c15) {
          case gc:
          case fc:
          case Wb:
          case Xb:
          case Vb:
            Z5(a18, b4, d12.children);
            return;
          case bc:
            Z5(a18, b4, d12.children);
            return;
          case ec:
            throw Error(k5(343));
          case ac:
            a: {
              c15 = b4.blockedBoundary;
              f15 = b4.blockedSegment;
              e14 = d12.fallback;
              d12 = d12.children;
              g7 = /* @__PURE__ */ new Set();
              var h6 = { id: null, rootSegmentID: -1, parentFlushed: false, pendingTasks: 0, forceClientRender: false, completedSegments: [], byteSize: 0, fallbackAbortableTasks: g7, errorDigest: null }, m12 = Sc(a18, f15.chunks.length, h6, f15.formatContext, false, false);
              f15.children.push(m12);
              f15.lastPushedText = false;
              var q2 = Sc(a18, 0, null, f15.formatContext, false, false);
              q2.parentFlushed = true;
              b4.blockedBoundary = h6;
              b4.blockedSegment = q2;
              try {
                if ($c(
                  a18,
                  b4,
                  d12
                ), q2.lastPushedText && q2.textEmbedded && q2.chunks.push(Ca), q2.status = 1, ad(h6, q2), 0 === h6.pendingTasks) break a;
              } catch (r7) {
                q2.status = 4, h6.forceClientRender = true, h6.errorDigest = Y3(a18, r7);
              } finally {
                b4.blockedBoundary = c15, b4.blockedSegment = f15;
              }
              b4 = Tc(a18, e14, c15, m12, g7, b4.legacyContext, b4.context, b4.treeContext);
              a18.pingedTasks.push(b4);
            }
            return;
        }
        if ("object" === typeof c15 && null !== c15) switch (c15.$$typeof) {
          case $b:
            d12 = Wc(a18, b4, c15.render, d12, f15);
            if (0 !== U3) {
              c15 = b4.treeContext;
              b4.treeContext = tc(c15, 1, 0);
              try {
                Z5(a18, b4, d12);
              } finally {
                b4.treeContext = c15;
              }
            } else Z5(a18, b4, d12);
            return;
          case cc:
            c15 = c15.type;
            d12 = Yc(c15, d12);
            Zc(a18, b4, c15, d12, f15);
            return;
          case Yb:
            f15 = d12.children;
            c15 = c15._context;
            d12 = d12.value;
            e14 = c15._currentValue;
            c15._currentValue = d12;
            g7 = O5;
            O5 = d12 = { parent: g7, depth: null === g7 ? 0 : g7.depth + 1, context: c15, parentValue: e14, value: d12 };
            b4.context = d12;
            Z5(a18, b4, f15);
            a18 = O5;
            if (null === a18) throw Error(k5(403));
            d12 = a18.parentValue;
            a18.context._currentValue = d12 === hc ? a18.context._defaultValue : d12;
            a18 = O5 = a18.parent;
            b4.context = a18;
            return;
          case Zb:
            d12 = d12.children;
            d12 = d12(c15._currentValue);
            Z5(a18, b4, d12);
            return;
          case dc:
            f15 = c15._init;
            c15 = f15(c15._payload);
            d12 = Yc(c15, d12);
            Zc(a18, b4, c15, d12, void 0);
            return;
        }
        throw Error(k5(
          130,
          null == c15 ? c15 : typeof c15,
          ""
        ));
      }
    }
    function Z5(a18, b4, c15) {
      b4.node = c15;
      if ("object" === typeof c15 && null !== c15) {
        switch (c15.$$typeof) {
          case Tb:
            Zc(a18, b4, c15.type, c15.props, c15.ref);
            return;
          case Ub:
            throw Error(k5(257));
          case dc:
            var d12 = c15._init;
            c15 = d12(c15._payload);
            Z5(a18, b4, c15);
            return;
        }
        if (ra(c15)) {
          bd(a18, b4, c15);
          return;
        }
        null === c15 || "object" !== typeof c15 ? d12 = null : (d12 = ic && c15[ic] || c15["@@iterator"], d12 = "function" === typeof d12 ? d12 : null);
        if (d12 && (d12 = d12.call(c15))) {
          c15 = d12.next();
          if (!c15.done) {
            var f15 = [];
            do
              f15.push(c15.value), c15 = d12.next();
            while (!c15.done);
            bd(a18, b4, f15);
          }
          return;
        }
        a18 = Object.prototype.toString.call(c15);
        throw Error(k5(31, "[object Object]" === a18 ? "object with keys {" + Object.keys(c15).join(", ") + "}" : a18));
      }
      "string" === typeof c15 ? (d12 = b4.blockedSegment, d12.lastPushedText = Da(b4.blockedSegment.chunks, c15, a18.responseState, d12.lastPushedText)) : "number" === typeof c15 && (d12 = b4.blockedSegment, d12.lastPushedText = Da(b4.blockedSegment.chunks, "" + c15, a18.responseState, d12.lastPushedText));
    }
    function bd(a18, b4, c15) {
      for (var d12 = c15.length, f15 = 0; f15 < d12; f15++) {
        var e14 = b4.treeContext;
        b4.treeContext = tc(e14, d12, f15);
        try {
          $c(a18, b4, c15[f15]);
        } finally {
          b4.treeContext = e14;
        }
      }
    }
    function $c(a18, b4, c15) {
      var d12 = b4.blockedSegment.formatContext, f15 = b4.legacyContext, e14 = b4.context;
      try {
        return Z5(a18, b4, c15);
      } catch (m12) {
        if (Gc(), "object" === typeof m12 && null !== m12 && "function" === typeof m12.then) {
          c15 = m12;
          var g7 = b4.blockedSegment, h6 = Sc(a18, g7.chunks.length, null, g7.formatContext, g7.lastPushedText, true);
          g7.children.push(h6);
          g7.lastPushedText = false;
          a18 = Tc(a18, b4.node, b4.blockedBoundary, h6, b4.abortSet, b4.legacyContext, b4.context, b4.treeContext).ping;
          c15.then(a18, a18);
          b4.blockedSegment.formatContext = d12;
          b4.legacyContext = f15;
          b4.context = e14;
          Q4(e14);
        } else throw b4.blockedSegment.formatContext = d12, b4.legacyContext = f15, b4.context = e14, Q4(e14), m12;
      }
    }
    function cd(a18) {
      var b4 = a18.blockedBoundary;
      a18 = a18.blockedSegment;
      a18.status = 3;
      dd(this, b4, a18);
    }
    function ed(a18, b4, c15) {
      var d12 = a18.blockedBoundary;
      a18.blockedSegment.status = 3;
      null === d12 ? (b4.allPendingTasks--, 2 !== b4.status && (b4.status = 2, null !== b4.destination && b4.destination.close())) : (d12.pendingTasks--, d12.forceClientRender || (d12.forceClientRender = true, a18 = void 0 === c15 ? Error(k5(432)) : c15, d12.errorDigest = b4.onError(a18), d12.parentFlushed && b4.clientRenderedBoundaries.push(d12)), d12.fallbackAbortableTasks.forEach(function(a19) {
        return ed(a19, b4, c15);
      }), d12.fallbackAbortableTasks.clear(), b4.allPendingTasks--, 0 === b4.allPendingTasks && (d12 = b4.onAllReady, d12()));
    }
    function ad(a18, b4) {
      if (0 === b4.chunks.length && 1 === b4.children.length && null === b4.children[0].boundary) {
        var c15 = b4.children[0];
        c15.id = b4.id;
        c15.parentFlushed = true;
        1 === c15.status && ad(a18, c15);
      } else a18.completedSegments.push(b4);
    }
    function dd(a18, b4, c15) {
      if (null === b4) {
        if (c15.parentFlushed) {
          if (null !== a18.completedRootSegment) throw Error(k5(389));
          a18.completedRootSegment = c15;
        }
        a18.pendingRootTasks--;
        0 === a18.pendingRootTasks && (a18.onShellError = X4, b4 = a18.onShellReady, b4());
      } else b4.pendingTasks--, b4.forceClientRender || (0 === b4.pendingTasks ? (c15.parentFlushed && 1 === c15.status && ad(b4, c15), b4.parentFlushed && a18.completedBoundaries.push(b4), b4.fallbackAbortableTasks.forEach(cd, a18), b4.fallbackAbortableTasks.clear()) : c15.parentFlushed && 1 === c15.status && (ad(b4, c15), 1 === b4.completedSegments.length && b4.parentFlushed && a18.partialBoundaries.push(b4)));
      a18.allPendingTasks--;
      0 === a18.allPendingTasks && (a18 = a18.onAllReady, a18());
    }
    function Uc(a18) {
      if (2 !== a18.status) {
        var b4 = O5, c15 = Pc.current;
        Pc.current = Oc;
        var d12 = Nc;
        Nc = a18.responseState;
        try {
          var f15 = a18.pingedTasks, e14;
          for (e14 = 0; e14 < f15.length; e14++) {
            var g7 = f15[e14];
            var h6 = a18, m12 = g7.blockedSegment;
            if (0 === m12.status) {
              Q4(g7.context);
              try {
                Z5(h6, g7, g7.node), m12.lastPushedText && m12.textEmbedded && m12.chunks.push(Ca), g7.abortSet.delete(g7), m12.status = 1, dd(h6, g7.blockedBoundary, m12);
              } catch (G3) {
                if (Gc(), "object" === typeof G3 && null !== G3 && "function" === typeof G3.then) {
                  var q2 = g7.ping;
                  G3.then(q2, q2);
                } else {
                  g7.abortSet.delete(g7);
                  m12.status = 4;
                  var r7 = g7.blockedBoundary, v5 = G3, A4 = Y3(h6, v5);
                  null === r7 ? Vc(h6, v5) : (r7.pendingTasks--, r7.forceClientRender || (r7.forceClientRender = true, r7.errorDigest = A4, r7.parentFlushed && h6.clientRenderedBoundaries.push(r7)));
                  h6.allPendingTasks--;
                  if (0 === h6.allPendingTasks) {
                    var F4 = h6.onAllReady;
                    F4();
                  }
                }
              } finally {
              }
            }
          }
          f15.splice(0, e14);
          null !== a18.destination && fd(a18, a18.destination);
        } catch (G3) {
          Y3(a18, G3), Vc(a18, G3);
        } finally {
          Nc = d12, Pc.current = c15, c15 === Oc && Q4(b4);
        }
      }
    }
    function gd(a18, b4, c15) {
      c15.parentFlushed = true;
      switch (c15.status) {
        case 0:
          var d12 = c15.id = a18.nextSegmentId++;
          c15.lastPushedText = false;
          c15.textEmbedded = false;
          a18 = a18.responseState;
          p11(b4, Va);
          p11(b4, a18.placeholderPrefix);
          a18 = u4(d12.toString(16));
          p11(b4, a18);
          return t20(b4, Wa);
        case 1:
          c15.status = 2;
          var f15 = true;
          d12 = c15.chunks;
          var e14 = 0;
          c15 = c15.children;
          for (var g7 = 0; g7 < c15.length; g7++) {
            for (f15 = c15[g7]; e14 < f15.index; e14++) p11(b4, d12[e14]);
            f15 = hd(a18, b4, f15);
          }
          for (; e14 < d12.length - 1; e14++) p11(b4, d12[e14]);
          e14 < d12.length && (f15 = t20(b4, d12[e14]));
          return f15;
        default:
          throw Error(k5(390));
      }
    }
    function hd(a18, b4, c15) {
      var d12 = c15.boundary;
      if (null === d12) return gd(a18, b4, c15);
      d12.parentFlushed = true;
      if (d12.forceClientRender) d12 = d12.errorDigest, t20(b4, $a), p11(b4, bb), d12 && (p11(b4, db), p11(b4, u4(C5(d12))), p11(b4, cb)), t20(b4, eb), gd(a18, b4, c15);
      else if (0 < d12.pendingTasks) {
        d12.rootSegmentID = a18.nextSegmentId++;
        0 < d12.completedSegments.length && a18.partialBoundaries.push(d12);
        var f15 = a18.responseState;
        var e14 = f15.nextSuspenseID++;
        f15 = w11(f15.boundaryPrefix + e14.toString(16));
        d12 = d12.id = f15;
        fb(b4, a18.responseState, d12);
        gd(a18, b4, c15);
      } else if (d12.byteSize > a18.progressiveChunkSize) d12.rootSegmentID = a18.nextSegmentId++, a18.completedBoundaries.push(d12), fb(b4, a18.responseState, d12.id), gd(a18, b4, c15);
      else {
        t20(b4, Xa);
        c15 = d12.completedSegments;
        if (1 !== c15.length) throw Error(k5(391));
        hd(a18, b4, c15[0]);
      }
      return t20(b4, ab);
    }
    function id3(a18, b4, c15) {
      Bb(b4, a18.responseState, c15.formatContext, c15.id);
      hd(a18, b4, c15);
      return Cb(b4, c15.formatContext);
    }
    function jd(a18, b4, c15) {
      for (var d12 = c15.completedSegments, f15 = 0; f15 < d12.length; f15++) kd(a18, b4, c15, d12[f15]);
      d12.length = 0;
      a18 = a18.responseState;
      d12 = c15.id;
      c15 = c15.rootSegmentID;
      p11(b4, a18.startInlineScript);
      a18.sentCompleteBoundaryFunction ? p11(b4, Jb) : (a18.sentCompleteBoundaryFunction = true, p11(b4, Ib));
      if (null === d12) throw Error(k5(395));
      c15 = u4(c15.toString(16));
      p11(b4, d12);
      p11(b4, Kb);
      p11(b4, a18.segmentPrefix);
      p11(b4, c15);
      return t20(b4, Lb);
    }
    function kd(a18, b4, c15, d12) {
      if (2 === d12.status) return true;
      var f15 = d12.id;
      if (-1 === f15) {
        if (-1 === (d12.id = c15.rootSegmentID)) throw Error(k5(392));
        return id3(a18, b4, d12);
      }
      id3(a18, b4, d12);
      a18 = a18.responseState;
      p11(b4, a18.startInlineScript);
      a18.sentCompleteSegmentFunction ? p11(b4, Eb) : (a18.sentCompleteSegmentFunction = true, p11(b4, Db));
      p11(b4, a18.segmentPrefix);
      f15 = u4(f15.toString(16));
      p11(b4, f15);
      p11(b4, Gb);
      p11(b4, a18.placeholderPrefix);
      p11(b4, f15);
      return t20(b4, Hb);
    }
    function fd(a18, b4) {
      l12 = new Uint8Array(512);
      n13 = 0;
      try {
        var c15 = a18.completedRootSegment;
        if (null !== c15 && 0 === a18.pendingRootTasks) {
          hd(a18, b4, c15);
          a18.completedRootSegment = null;
          var d12 = a18.responseState.bootstrapChunks;
          for (c15 = 0; c15 < d12.length - 1; c15++) p11(b4, d12[c15]);
          c15 < d12.length && t20(b4, d12[c15]);
        }
        var f15 = a18.clientRenderedBoundaries, e14;
        for (e14 = 0; e14 < f15.length; e14++) {
          var g7 = f15[e14];
          d12 = b4;
          var h6 = a18.responseState, m12 = g7.id, q2 = g7.errorDigest, r7 = g7.errorMessage, v5 = g7.errorComponentStack;
          p11(d12, h6.startInlineScript);
          h6.sentClientRenderFunction ? p11(d12, Nb) : (h6.sentClientRenderFunction = true, p11(
            d12,
            Mb
          ));
          if (null === m12) throw Error(k5(395));
          p11(d12, m12);
          p11(d12, Ob);
          if (q2 || r7 || v5) p11(d12, Qb), p11(d12, u4(Sb(q2 || "")));
          if (r7 || v5) p11(d12, Qb), p11(d12, u4(Sb(r7 || "")));
          v5 && (p11(d12, Qb), p11(d12, u4(Sb(v5))));
          if (!t20(d12, Pb)) {
            a18.destination = null;
            e14++;
            f15.splice(0, e14);
            return;
          }
        }
        f15.splice(0, e14);
        var A4 = a18.completedBoundaries;
        for (e14 = 0; e14 < A4.length; e14++) if (!jd(a18, b4, A4[e14])) {
          a18.destination = null;
          e14++;
          A4.splice(0, e14);
          return;
        }
        A4.splice(0, e14);
        ba(b4);
        l12 = new Uint8Array(512);
        n13 = 0;
        var F4 = a18.partialBoundaries;
        for (e14 = 0; e14 < F4.length; e14++) {
          var G3 = F4[e14];
          a: {
            f15 = a18;
            g7 = b4;
            var ma = G3.completedSegments;
            for (h6 = 0; h6 < ma.length; h6++) if (!kd(
              f15,
              g7,
              G3,
              ma[h6]
            )) {
              h6++;
              ma.splice(0, h6);
              var Fb = false;
              break a;
            }
            ma.splice(0, h6);
            Fb = true;
          }
          if (!Fb) {
            a18.destination = null;
            e14++;
            F4.splice(0, e14);
            return;
          }
        }
        F4.splice(0, e14);
        var na = a18.completedBoundaries;
        for (e14 = 0; e14 < na.length; e14++) if (!jd(a18, b4, na[e14])) {
          a18.destination = null;
          e14++;
          na.splice(0, e14);
          return;
        }
        na.splice(0, e14);
      } finally {
        ba(b4), 0 === a18.allPendingTasks && 0 === a18.pingedTasks.length && 0 === a18.clientRenderedBoundaries.length && 0 === a18.completedBoundaries.length && b4.close();
      }
    }
    function ld(a18, b4) {
      try {
        var c15 = a18.abortableTasks;
        c15.forEach(function(c16) {
          return ed(c16, a18, b4);
        });
        c15.clear();
        null !== a18.destination && fd(a18, a18.destination);
      } catch (d12) {
        Y3(a18, d12), Vc(a18, d12);
      }
    }
    exports.renderToReadableStream = function(a18, b4) {
      return new Promise(function(c15, d12) {
        var f15, e14, g7 = new Promise(function(a19, b5) {
          e14 = a19;
          f15 = b5;
        }), h6 = Rc(a18, za(b4 ? b4.identifierPrefix : void 0, b4 ? b4.nonce : void 0, b4 ? b4.bootstrapScriptContent : void 0, b4 ? b4.bootstrapScripts : void 0, b4 ? b4.bootstrapModules : void 0), Aa(b4 ? b4.namespaceURI : void 0), b4 ? b4.progressiveChunkSize : void 0, b4 ? b4.onError : void 0, e14, function() {
          var a19 = new ReadableStream({ type: "bytes", pull: function(a20) {
            if (1 === h6.status) h6.status = 2, da(a20, h6.fatalError);
            else if (2 !== h6.status && null === h6.destination) {
              h6.destination = a20;
              try {
                fd(h6, a20);
              } catch (A4) {
                Y3(h6, A4), Vc(h6, A4);
              }
            }
          }, cancel: function() {
            ld(h6);
          } }, { highWaterMark: 0 });
          a19.allReady = g7;
          c15(a19);
        }, function(a19) {
          g7.catch(function() {
          });
          d12(a19);
        }, f15);
        if (b4 && b4.signal) {
          var m12 = b4.signal, q2 = function() {
            ld(h6, m12.reason);
            m12.removeEventListener("abort", q2);
          };
          m12.addEventListener("abort", q2);
        }
        Uc(h6);
      });
    };
    exports.version = "18.3.1";
  }
});

// node_modules/react-dom/server.browser.js
var require_server_browser = __commonJS({
  "node_modules/react-dom/server.browser.js"(exports) {
    "use strict";
    var l12;
    var s36;
    if (true) {
      l12 = require_react_dom_server_legacy_browser_production_min();
      s36 = require_react_dom_server_browser_production_min();
    } else {
      l12 = null;
      s36 = null;
    }
    exports.version = l12.version;
    exports.renderToString = l12.renderToString;
    exports.renderToStaticMarkup = l12.renderToStaticMarkup;
    exports.renderToNodeStream = l12.renderToNodeStream;
    exports.renderToStaticNodeStream = l12.renderToStaticNodeStream;
    exports.renderToReadableStream = s36.renderToReadableStream;
  }
});

// node_modules/react-streaming/dist/cjs/server/renderToStream/createReadableWrapper.js
var require_createReadableWrapper = __commonJS({
  "node_modules/react-streaming/dist/cjs/server/renderToStream/createReadableWrapper.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createReadableWrapper = void 0;
    var orchestrateChunks_1 = require_orchestrateChunks();
    function createReadableWrapper(readableFromReact, clearTimeouts, doNotClosePromise) {
      const streamOperations = {
        operations: null
      };
      let controllerOfUserStream;
      let onEnded;
      const streamEnd = new Promise((r7) => {
        onEnded = () => r7();
      });
      const readableForUser = new ReadableStream({
        start(controller) {
          controllerOfUserStream = controller;
          onReady(onEnded);
        }
      });
      const { injectToStream, onReactWrite, onBeforeEnd, hasStreamEnded } = (0, orchestrateChunks_1.orchestrateChunks)(streamOperations, doNotClosePromise);
      return { readableForUser, streamEnd, injectToStream, hasStreamEnded };
      async function onReady(onEnded2) {
        streamOperations.operations = {
          writeChunk(chunk2) {
            controllerOfUserStream.enqueue(encodeForWebStream2(chunk2));
          },
          flush: null
        };
        const reader = readableFromReact.getReader();
        while (true) {
          let result;
          try {
            result = await reader.read();
          } catch (err) {
            controllerOfUserStream.close();
            throw err;
          }
          const { value, done } = result;
          if (done) {
            break;
          }
          onReactWrite(value);
        }
        clearTimeouts();
        await onBeforeEnd();
        controllerOfUserStream.close();
        onEnded2();
      }
    }
    exports.createReadableWrapper = createReadableWrapper;
    var encoder2;
    function encodeForWebStream2(thing) {
      if (!encoder2) {
        encoder2 = new TextEncoder();
      }
      if (typeof thing === "string") {
        return encoder2.encode(thing);
      }
      return thing;
    }
  }
});

// node_modules/react-streaming/dist/cjs/server/renderToStream/renderToWebStream.js
var require_renderToWebStream = __commonJS({
  "node_modules/react-streaming/dist/cjs/server/renderToStream/renderToWebStream.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.renderToWebStream = void 0;
    var server_browser_1 = require_server_browser();
    var createReadableWrapper_1 = require_createReadableWrapper();
    var common_1 = require_common();
    async function renderToWebStream(element, disable, options, doNotClosePromise, setAbortFn, clearTimeouts) {
      var _a;
      (0, common_1.debugFlow)("creating Web Stream Pipe");
      const controller = new AbortController();
      setAbortFn(() => {
        controller.abort();
      });
      let didError = false;
      let firstErr = null;
      let reactBug = null;
      const onError = (err) => {
        didError = true;
        firstErr = firstErr || err;
        (0, common_1.afterReactBugCatch)(() => {
          var _a2;
          if (err !== reactBug) {
            (_a2 = options.onBoundaryError) === null || _a2 === void 0 ? void 0 : _a2.call(options, err);
          }
        });
      };
      const renderToReadableStream = (_a = options.renderToReadableStream) !== null && _a !== void 0 ? _a : server_browser_1.renderToReadableStream;
      if (!options.renderToReadableStream) {
        (0, common_1.assertReactImport)(renderToReadableStream, "renderToReadableStream");
      }
      const readableOriginal = await renderToReadableStream(element, Object.assign({ onError, signal: controller.signal }, options.streamOptions));
      const { allReady } = readableOriginal;
      let promiseResolved = false;
      allReady.catch((err) => {
        (0, common_1.debugFlow)("react bug");
        didError = true;
        firstErr = firstErr || err;
        reactBug = err;
        if (reactBug !== firstErr || promiseResolved) {
          console.error(reactBug);
        }
      });
      if (didError)
        throw firstErr;
      if (disable)
        await allReady;
      if (didError)
        throw firstErr;
      const { readableForUser, streamEnd, injectToStream, hasStreamEnded } = (0, createReadableWrapper_1.createReadableWrapper)(readableOriginal, clearTimeouts, doNotClosePromise);
      promiseResolved = true;
      return {
        readable: readableForUser,
        pipe: null,
        abort: controller.abort,
        streamEnd: (0, common_1.wrapStreamEnd)(streamEnd, didError),
        injectToStream,
        hasStreamEnded
      };
    }
    exports.renderToWebStream = renderToWebStream;
  }
});

// node_modules/react-streaming/dist/cjs/server/index.node-and-web.js
var require_index_node_and_web = __commonJS({
  "node_modules/react-streaming/dist/cjs/server/index.node-and-web.js"(exports) {
    "use strict";
    var __createBinding2 = exports && exports.__createBinding || (Object.create ? function(o8, m12, k5, k22) {
      if (k22 === void 0) k22 = k5;
      var desc = Object.getOwnPropertyDescriptor(m12, k5);
      if (!desc || ("get" in desc ? !m12.__esModule : desc.writable || desc.configurable)) {
        desc = { enumerable: true, get: function() {
          return m12[k5];
        } };
      }
      Object.defineProperty(o8, k22, desc);
    } : function(o8, m12, k5, k22) {
      if (k22 === void 0) k22 = k5;
      o8[k22] = m12[k5];
    });
    var __exportStar2 = exports && exports.__exportStar || function(m12, exports2) {
      for (var p11 in m12) if (p11 !== "default" && !Object.prototype.hasOwnProperty.call(exports2, p11)) __createBinding2(exports2, m12, p11);
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    __exportStar2(require_server(), exports);
    var renderToNodeStream_1 = require_renderToNodeStream();
    var renderToStream_1 = require_renderToStream();
    (0, renderToStream_1.renderToNodeStream_set)(renderToNodeStream_1.renderToNodeStream);
    var renderToWebStream_1 = require_renderToWebStream();
    var renderToStream_2 = require_renderToStream();
    (0, renderToStream_2.renderToWebStream_set)(renderToWebStream_1.renderToWebStream);
  }
});

// node_modules/vike-react/dist/utils/getGlobalObject.js
function getGlobalObject2(key, defaultValue2) {
  const globalObjectsAll = globalThis[projectKey2] = globalThis[projectKey2] || {};
  const globalObject12 = globalObjectsAll[key] = globalObjectsAll[key] || defaultValue2;
  return globalObject12;
}
var projectKey2;
var init_getGlobalObject2 = __esm({
  "node_modules/vike-react/dist/utils/getGlobalObject.js"() {
    projectKey2 = "_vike_react";
  }
});

// node_modules/vike-react/dist/hooks/usePageContext.js
function PageContextProvider({ pageContext, children }) {
  const { reactContext } = globalObject3;
  return import_react2.default.createElement(reactContext.Provider, { value: pageContext }, children);
}
function usePageContext() {
  const { reactContext } = globalObject3;
  const pageContext = (0, import_react2.useContext)(reactContext);
  return pageContext;
}
var import_react2, globalObject3;
var init_usePageContext = __esm({
  "node_modules/vike-react/dist/hooks/usePageContext.js"() {
    import_react2 = __toESM(require_react(), 1);
    init_getGlobalObject2();
    globalObject3 = getGlobalObject2("PageContextProvider.ts", {
      reactContext: import_react2.default.createContext(void 0)
    });
  }
});

// node_modules/vike-react/dist/utils/isCallable.js
function isCallable2(thing) {
  return thing instanceof Function || typeof thing === "function";
}
var init_isCallable2 = __esm({
  "node_modules/vike-react/dist/utils/isCallable.js"() {
  }
});

// node_modules/vike-react/dist/hooks/useConfig/configsCumulative.js
var configsCumulative;
var init_configsCumulative = __esm({
  "node_modules/vike-react/dist/hooks/useConfig/configsCumulative.js"() {
    configsCumulative = ["Head", "bodyAttributes", "htmlAttributes"];
  }
});

// node_modules/vike-react/dist/utils/includes.js
function includes(values2, x3) {
  return values2.includes(x3);
}
var init_includes = __esm({
  "node_modules/vike-react/dist/utils/includes.js"() {
  }
});

// node_modules/vike-react/dist/integration/getHeadSetting.js
function getHeadSetting(configName, pageContext) {
  const valFromUseConfig = pageContext._configFromHook?.[configName];
  const valFromConfig = pageContext.config[configName];
  const getCallable = (val) => isCallable2(val) ? val(pageContext) : val;
  if (!includes(configsCumulative, configName)) {
    if (valFromUseConfig !== void 0)
      return valFromUseConfig;
    return getCallable(valFromConfig);
  } else {
    return [
      //
      ...(valFromConfig ?? []).map(getCallable),
      ...valFromUseConfig ?? []
    ];
  }
}
var init_getHeadSetting = __esm({
  "node_modules/vike-react/dist/integration/getHeadSetting.js"() {
    init_isCallable2();
    init_configsCumulative();
    init_includes();
  }
});

// node_modules/vike-react/dist/integration/getPageElement.js
function getPageElement(pageContext) {
  const { Page: Page6, config: { Loading } } = pageContext;
  let page = Page6 ? import_react3.default.createElement(Page6, null) : null;
  const addSuspense = (el2) => {
    if (!Loading?.layout)
      return el2;
    return import_react3.default.createElement(import_react3.Suspense, { fallback: import_react3.default.createElement(Loading.layout, null) }, page);
  };
  page = addSuspense(page);
  [
    // Inner wrapping
    ...pageContext.config.Layout || [],
    // Outer wrapping
    ...pageContext.config.Wrapper || []
  ].forEach((Wrap) => {
    page = import_react3.default.createElement(Wrap, null, page);
    page = addSuspense(page);
  });
  page = import_react3.default.createElement(PageContextProvider, { pageContext }, page);
  let renderPromiseResolve;
  let renderPromise = new Promise((r7) => renderPromiseResolve = r7);
  page = import_react3.default.createElement(RenderPromiseProvider, { renderPromiseResolve }, page);
  if (pageContext.config.reactStrictMode !== false) {
    page = import_react3.default.createElement(import_react3.default.StrictMode, null, page);
  }
  return { page, renderPromise };
}
function RenderPromiseProvider({ children, renderPromiseResolve }) {
  (0, import_react3.useEffect)(renderPromiseResolve);
  return children;
}
var import_react3;
var init_getPageElement = __esm({
  "node_modules/vike-react/dist/integration/getPageElement.js"() {
    import_react3 = __toESM(require_react(), 1);
    init_usePageContext();
  }
});

// node_modules/vike-react/dist/utils/isReactElement.js
function isReactElement(value) {
  return (0, import_react4.isValidElement)(value) || Array.isArray(value);
}
var import_react4;
var init_isReactElement = __esm({
  "node_modules/vike-react/dist/utils/isReactElement.js"() {
    import_react4 = __toESM(require_react(), 1);
  }
});

// node_modules/vike-react/dist/utils/getTagAttributesString.js
function getTagAttributesString(tagAttributes) {
  const tagAttributesString = Object.entries(tagAttributes).filter(([_key, value]) => value !== false && value !== null && value !== void 0).map(([key, value]) => `${ensureIsValidAttributeName(key)}=${JSON.stringify(String(value))}`).join(" ");
  if (tagAttributesString.length === 0)
    return "";
  return ` ${tagAttributesString}`;
}
function ensureIsValidAttributeName(str) {
  if (/^[a-z][a-z0-9\-]*$/i.test(str) && !str.endsWith("-"))
    return str;
  throw new Error(`Invalid HTML tag attribute name ${JSON.stringify(str)}`);
}
var init_getTagAttributesString = __esm({
  "node_modules/vike-react/dist/utils/getTagAttributesString.js"() {
  }
});

// node_modules/vike/dist/esm/utils/humanizeTime.js
function humanizeTime(milliseconds) {
  const seconds = milliseconds / 1e3;
  if (seconds < 120) {
    const n13 = round(seconds);
    return `${n13} second${plural(n13)}`;
  }
  {
    const minutes = seconds / 60;
    const n13 = round(minutes);
    return `${n13} minute${plural(n13)}`;
  }
}
function round(n13) {
  let rounded = n13.toFixed(1);
  if (rounded.endsWith(".0"))
    rounded = rounded.slice(0, -2);
  return rounded;
}
function plural(n13) {
  return n13 === "1" ? "" : "s";
}
var init_humanizeTime = __esm({
  "node_modules/vike/dist/esm/utils/humanizeTime.js"() {
  }
});

// node_modules/vike/dist/esm/shared/hooks/executeHook.js
function isUserHookError(err) {
  if (!isObject(err))
    return false;
  return globalObject4.userHookErrors.get(err) ?? false;
}
function executeHook(hookFnCaller, hook, pageContext) {
  const { hookName, hookFilePath, hookTimeout: { error: timeoutErr, warning: timeoutWarn } } = hook;
  let resolve;
  let reject;
  const promise = new Promise((resolve_, reject_) => {
    resolve = (ret) => {
      clearTimeouts();
      resolve_(ret);
    };
    reject = (err) => {
      clearTimeouts();
      reject_(err);
    };
  });
  const clearTimeouts = () => {
    if (currentTimeoutWarn)
      clearTimeout(currentTimeoutWarn);
    if (currentTimeoutErr)
      clearTimeout(currentTimeoutErr);
  };
  const currentTimeoutWarn = isNotDisabled(timeoutWarn) && setTimeout(() => {
    assertWarning2(false, `The ${hookName}() hook defined by ${hookFilePath} is slow: it's taking more than ${humanizeTime(timeoutWarn)} (https://vike.dev/hooksTimeout)`, { onlyOnce: false });
  }, timeoutWarn);
  const currentTimeoutErr = isNotDisabled(timeoutErr) && setTimeout(() => {
    const err = getProjectError(`The ${hookName}() hook defined by ${hookFilePath} timed out: it didn't finish after ${humanizeTime(timeoutErr)} (https://vike.dev/hooksTimeout)`);
    reject(err);
  }, timeoutErr);
  (async () => {
    try {
      providePageContext(pageContext);
      const ret = await hookFnCaller();
      resolve(ret);
    } catch (err) {
      if (isObject(err)) {
        globalObject4.userHookErrors.set(err, { hookName, hookFilePath });
      }
      reject(err);
    }
  })();
  return promise;
}
function isNotDisabled(timeout) {
  return !!timeout && timeout !== Infinity;
}
function providePageContext(pageContext) {
  globalObject4.pageContext = pageContext;
  Promise.resolve().then(() => {
    globalObject4.pageContext = null;
  });
}
var globalObject4;
var init_executeHook = __esm({
  "node_modules/vike/dist/esm/shared/hooks/executeHook.js"() {
    init_assert();
    init_getGlobalObject();
    init_humanizeTime();
    init_isObject();
    globalObject4 = getGlobalObject("utils/executeHook.ts", {
      userHookErrors: /* @__PURE__ */ new WeakMap(),
      pageContext: null
    });
  }
});

// node_modules/vike/dist/esm/shared/getPageContext.js
var init_getPageContext = __esm({
  "node_modules/vike/dist/esm/shared/getPageContext.js"() {
    init_executeHook();
  }
});

// node_modules/vike-react/dist/utils/callCumulativeHooks.js
async function callCumulativeHooks(values2, pageContext) {
  if (!values2)
    return [];
  const valuesPromises = values2.map((val) => {
    if (typeof val === "function") {
      providePageContext(pageContext);
      return val(pageContext);
    } else {
      return val;
    }
  });
  const valuesResolved = await Promise.all(valuesPromises);
  return valuesResolved;
}
var init_callCumulativeHooks = __esm({
  "node_modules/vike-react/dist/utils/callCumulativeHooks.js"() {
    init_getPageContext();
  }
});

// node_modules/vike-react/dist/integration/onRenderHtml.js
async function getPageHtml(pageContext) {
  if (pageContext.Page)
    pageContext.page = getPageElement(pageContext).page;
  await callCumulativeHooks(pageContext.config.onBeforeRenderHtml, pageContext);
  let pageHtml = "";
  if (pageContext.page) {
    const { stream, streamIsRequired } = pageContext.config;
    if (!stream && !streamIsRequired) {
      const pageHtmlString = (0, import_server.renderToString)(pageContext.page);
      pageContext.pageHtmlString = pageHtmlString;
      pageHtml = dangerouslySkipEscape(pageHtmlString);
    } else {
      const pageHtmlStream = await (0, import_server2.renderToStream)(pageContext.page, {
        webStream: typeof stream === "string" ? stream === "web" : void 0,
        userAgent: pageContext.headers?.["user-agent"] || // TODO/eventually: remove old way of acccessing the User Agent header.
        // @ts-ignore
        pageContext.userAgent,
        disable: stream === false ? true : void 0
      });
      pageContext.pageHtmlStream = pageHtmlStream;
      pageHtml = pageHtmlStream;
    }
  }
  await callCumulativeHooks(pageContext.config.onAfterRenderHtml, pageContext);
  return pageHtml;
}
function getHeadHtml(pageContext) {
  pageContext._headAlreadySet = true;
  const favicon = getHeadSetting("favicon", pageContext);
  const title3 = getHeadSetting("title", pageContext);
  const description = getHeadSetting("description", pageContext);
  const image2 = getHeadSetting("image", pageContext);
  const faviconTag = !favicon ? "" : escapeInject`<link rel="icon" href="${favicon}" />`;
  const titleTags = !title3 ? "" : escapeInject`<title>${title3}</title><meta property="og:title" content="${title3}" />`;
  const descriptionTags = !description ? "" : escapeInject`<meta name="description" content="${description}" /><meta property="og:description" content="${description}" />`;
  const imageTags = !image2 ? "" : escapeInject`<meta property="og:image" content="${image2}"><meta name="twitter:card" content="summary_large_image">`;
  const viewportTag = dangerouslySkipEscape(getViewportTag(getHeadSetting("viewport", pageContext)));
  const headElementsHtml = dangerouslySkipEscape([
    // Added by +Head
    ...pageContext.config.Head ?? [],
    // Added by useConfig()
    ...pageContext._configFromHook?.Head ?? []
  ].filter((Head) => Head !== null && Head !== void 0).map((Head) => getHeadElementHtml(Head, pageContext)).join("\n"));
  const headHtml = escapeInject`
    ${titleTags}
    ${viewportTag}
    ${headElementsHtml}
    ${faviconTag}
    ${descriptionTags}
    ${imageTags}
  `;
  return headHtml;
}
function getHeadElementHtml(Head, pageContext) {
  let headElement;
  if (isReactElement(Head)) {
    headElement = Head;
  } else {
    headElement = import_react5.default.createElement(
      PageContextProvider,
      { pageContext },
      import_react5.default.createElement(Head, null)
    );
  }
  if (pageContext.config.reactStrictMode !== false) {
    headElement = import_react5.default.createElement(import_react5.default.StrictMode, null, headElement);
  }
  return (0, import_server.renderToStaticMarkup)(headElement);
}
function getTagAttributes(pageContext) {
  let lang = getHeadSetting("lang", pageContext);
  if (lang === void 0)
    lang = "en";
  const bodyAttributes = mergeTagAttributesList(getHeadSetting("bodyAttributes", pageContext));
  const htmlAttributes = mergeTagAttributesList(getHeadSetting("htmlAttributes", pageContext));
  const bodyAttributesString = getTagAttributesString(bodyAttributes);
  const htmlAttributesString = getTagAttributesString({ ...htmlAttributes, lang: lang ?? htmlAttributes.lang });
  return { htmlAttributesString, bodyAttributesString };
}
function mergeTagAttributesList(tagAttributesList = []) {
  const tagAttributes = {};
  tagAttributesList.forEach((tagAttrs) => Object.assign(tagAttributes, tagAttrs));
  return tagAttributes;
}
function getViewportTag(viewport) {
  if (viewport === "responsive" || viewport === void 0) {
    return '<meta name="viewport" content="width=device-width,initial-scale=1">';
  }
  if (typeof viewport === "number") {
    return `<meta name="viewport" content="width=${viewport}">`;
  }
  return "";
}
function addEcosystemStamp() {
  const g7 = globalThis;
  g7._isVikeReactApp = /* Don't set to true so that consumers do `!!globalThis._isVikeApp` instead of `globalThis._isVikeApp === true`.
  true
  */
  // We use an object so that we can eventually, in the future, add helpful information as needed. (E.g. the vike-react version.)
  {};
}
async function getBodyHtmlBoundary(pageContext) {
  const bodyHtmlBegin = dangerouslySkipEscape((await callCumulativeHooks(pageContext.config.bodyHtmlBegin, pageContext)).join(""));
  const bodyHtmlEnd = dangerouslySkipEscape((await callCumulativeHooks(pageContext.config.bodyHtmlEnd, pageContext)).join(""));
  return { bodyHtmlBegin, bodyHtmlEnd };
}
var import_react5, import_server, import_server2, onRenderHtml;
var init_onRenderHtml = __esm({
  "node_modules/vike-react/dist/integration/onRenderHtml.js"() {
    import_react5 = __toESM(require_react(), 1);
    import_server = __toESM(require_server_node(), 1);
    import_server2 = __toESM(require_index_node_and_web(), 1);
    init_runtime();
    init_usePageContext();
    init_getHeadSetting();
    init_getPageElement();
    init_isReactElement();
    init_getTagAttributesString();
    init_callCumulativeHooks();
    addEcosystemStamp();
    onRenderHtml = async (pageContext) => {
      const pageHtml = await getPageHtml(pageContext);
      const headHtml = getHeadHtml(pageContext);
      const { bodyHtmlBegin, bodyHtmlEnd } = await getBodyHtmlBoundary(pageContext);
      const { htmlAttributesString, bodyAttributesString } = getTagAttributes(pageContext);
      delete pageContext._configFromHook;
      return escapeInject`<!DOCTYPE html>
    <html${dangerouslySkipEscape(htmlAttributesString)}>
      <head>
        <meta charset="UTF-8" />
        ${headHtml}
      </head>
      <body${dangerouslySkipEscape(bodyAttributesString)}>
        ${bodyHtmlBegin}
        <div id="root">${pageHtml}</div>
        ${bodyHtmlEnd}
      </body>
    </html>`;
    };
  }
});

// node_modules/react/cjs/react-jsx-runtime.production.min.js
var require_react_jsx_runtime_production_min = __commonJS({
  "node_modules/react/cjs/react-jsx-runtime.production.min.js"(exports) {
    "use strict";
    var f15 = require_react();
    var k5 = Symbol.for("react.element");
    var l12 = Symbol.for("react.fragment");
    var m12 = Object.prototype.hasOwnProperty;
    var n13 = f15.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED.ReactCurrentOwner;
    var p11 = { key: true, ref: true, __self: true, __source: true };
    function q2(c15, a18, g7) {
      var b4, d12 = {}, e14 = null, h6 = null;
      void 0 !== g7 && (e14 = "" + g7);
      void 0 !== a18.key && (e14 = "" + a18.key);
      void 0 !== a18.ref && (h6 = a18.ref);
      for (b4 in a18) m12.call(a18, b4) && !p11.hasOwnProperty(b4) && (d12[b4] = a18[b4]);
      if (c15 && c15.defaultProps) for (b4 in a18 = c15.defaultProps, a18) void 0 === d12[b4] && (d12[b4] = a18[b4]);
      return { $$typeof: k5, type: c15, key: e14, ref: h6, props: d12, _owner: n13.current };
    }
    exports.Fragment = l12;
    exports.jsx = q2;
    exports.jsxs = q2;
  }
});

// node_modules/react/jsx-runtime.js
var require_jsx_runtime = __commonJS({
  "node_modules/react/jsx-runtime.js"(exports, module) {
    "use strict";
    if (true) {
      module.exports = require_react_jsx_runtime_production_min();
    } else {
      module.exports = null;
    }
  }
});

// node_modules/tslib/tslib.es6.mjs
var tslib_es6_exports = {};
__export(tslib_es6_exports, {
  __addDisposableResource: () => __addDisposableResource,
  __assign: () => __assign,
  __asyncDelegator: () => __asyncDelegator,
  __asyncGenerator: () => __asyncGenerator,
  __asyncValues: () => __asyncValues,
  __await: () => __await,
  __awaiter: () => __awaiter,
  __classPrivateFieldGet: () => __classPrivateFieldGet,
  __classPrivateFieldIn: () => __classPrivateFieldIn,
  __classPrivateFieldSet: () => __classPrivateFieldSet,
  __createBinding: () => __createBinding,
  __decorate: () => __decorate,
  __disposeResources: () => __disposeResources,
  __esDecorate: () => __esDecorate,
  __exportStar: () => __exportStar,
  __extends: () => __extends,
  __generator: () => __generator,
  __importDefault: () => __importDefault,
  __importStar: () => __importStar,
  __makeTemplateObject: () => __makeTemplateObject,
  __metadata: () => __metadata,
  __param: () => __param,
  __propKey: () => __propKey,
  __read: () => __read,
  __rest: () => __rest,
  __rewriteRelativeImportExtension: () => __rewriteRelativeImportExtension,
  __runInitializers: () => __runInitializers,
  __setFunctionName: () => __setFunctionName,
  __spread: () => __spread,
  __spreadArray: () => __spreadArray,
  __spreadArrays: () => __spreadArrays,
  __values: () => __values,
  default: () => tslib_es6_default
});
function __extends(d12, b4) {
  if (typeof b4 !== "function" && b4 !== null)
    throw new TypeError("Class extends value " + String(b4) + " is not a constructor or null");
  extendStatics(d12, b4);
  function __() {
    this.constructor = d12;
  }
  d12.prototype = b4 === null ? Object.create(b4) : (__.prototype = b4.prototype, new __());
}
function __rest(s36, e14) {
  var t20 = {};
  for (var p11 in s36) if (Object.prototype.hasOwnProperty.call(s36, p11) && e14.indexOf(p11) < 0)
    t20[p11] = s36[p11];
  if (s36 != null && typeof Object.getOwnPropertySymbols === "function")
    for (var i13 = 0, p11 = Object.getOwnPropertySymbols(s36); i13 < p11.length; i13++) {
      if (e14.indexOf(p11[i13]) < 0 && Object.prototype.propertyIsEnumerable.call(s36, p11[i13]))
        t20[p11[i13]] = s36[p11[i13]];
    }
  return t20;
}
function __decorate(decorators, target, key, desc) {
  var c15 = arguments.length, r7 = c15 < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d12;
  if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r7 = Reflect.decorate(decorators, target, key, desc);
  else for (var i13 = decorators.length - 1; i13 >= 0; i13--) if (d12 = decorators[i13]) r7 = (c15 < 3 ? d12(r7) : c15 > 3 ? d12(target, key, r7) : d12(target, key)) || r7;
  return c15 > 3 && r7 && Object.defineProperty(target, key, r7), r7;
}
function __param(paramIndex, decorator) {
  return function(target, key) {
    decorator(target, key, paramIndex);
  };
}
function __esDecorate(ctor, descriptorIn, decorators, contextIn, initializers, extraInitializers) {
  function accept(f15) {
    if (f15 !== void 0 && typeof f15 !== "function") throw new TypeError("Function expected");
    return f15;
  }
  var kind = contextIn.kind, key = kind === "getter" ? "get" : kind === "setter" ? "set" : "value";
  var target = !descriptorIn && ctor ? contextIn["static"] ? ctor : ctor.prototype : null;
  var descriptor = descriptorIn || (target ? Object.getOwnPropertyDescriptor(target, contextIn.name) : {});
  var _2, done = false;
  for (var i13 = decorators.length - 1; i13 >= 0; i13--) {
    var context = {};
    for (var p11 in contextIn) context[p11] = p11 === "access" ? {} : contextIn[p11];
    for (var p11 in contextIn.access) context.access[p11] = contextIn.access[p11];
    context.addInitializer = function(f15) {
      if (done) throw new TypeError("Cannot add initializers after decoration has completed");
      extraInitializers.push(accept(f15 || null));
    };
    var result = (0, decorators[i13])(kind === "accessor" ? { get: descriptor.get, set: descriptor.set } : descriptor[key], context);
    if (kind === "accessor") {
      if (result === void 0) continue;
      if (result === null || typeof result !== "object") throw new TypeError("Object expected");
      if (_2 = accept(result.get)) descriptor.get = _2;
      if (_2 = accept(result.set)) descriptor.set = _2;
      if (_2 = accept(result.init)) initializers.unshift(_2);
    } else if (_2 = accept(result)) {
      if (kind === "field") initializers.unshift(_2);
      else descriptor[key] = _2;
    }
  }
  if (target) Object.defineProperty(target, contextIn.name, descriptor);
  done = true;
}
function __runInitializers(thisArg, initializers, value) {
  var useValue = arguments.length > 2;
  for (var i13 = 0; i13 < initializers.length; i13++) {
    value = useValue ? initializers[i13].call(thisArg, value) : initializers[i13].call(thisArg);
  }
  return useValue ? value : void 0;
}
function __propKey(x3) {
  return typeof x3 === "symbol" ? x3 : "".concat(x3);
}
function __setFunctionName(f15, name2, prefix) {
  if (typeof name2 === "symbol") name2 = name2.description ? "[".concat(name2.description, "]") : "";
  return Object.defineProperty(f15, "name", { configurable: true, value: prefix ? "".concat(prefix, " ", name2) : name2 });
}
function __metadata(metadataKey, metadataValue) {
  if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(metadataKey, metadataValue);
}
function __awaiter(thisArg, _arguments, P4, generator) {
  function adopt(value) {
    return value instanceof P4 ? value : new P4(function(resolve) {
      resolve(value);
    });
  }
  return new (P4 || (P4 = Promise))(function(resolve, reject) {
    function fulfilled(value) {
      try {
        step(generator.next(value));
      } catch (e14) {
        reject(e14);
      }
    }
    function rejected(value) {
      try {
        step(generator["throw"](value));
      } catch (e14) {
        reject(e14);
      }
    }
    function step(result) {
      result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
    }
    step((generator = generator.apply(thisArg, _arguments || [])).next());
  });
}
function __generator(thisArg, body) {
  var _2 = { label: 0, sent: function() {
    if (t20[0] & 1) throw t20[1];
    return t20[1];
  }, trys: [], ops: [] }, f15, y4, t20, g7 = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
  return g7.next = verb(0), g7["throw"] = verb(1), g7["return"] = verb(2), typeof Symbol === "function" && (g7[Symbol.iterator] = function() {
    return this;
  }), g7;
  function verb(n13) {
    return function(v5) {
      return step([n13, v5]);
    };
  }
  function step(op) {
    if (f15) throw new TypeError("Generator is already executing.");
    while (g7 && (g7 = 0, op[0] && (_2 = 0)), _2) try {
      if (f15 = 1, y4 && (t20 = op[0] & 2 ? y4["return"] : op[0] ? y4["throw"] || ((t20 = y4["return"]) && t20.call(y4), 0) : y4.next) && !(t20 = t20.call(y4, op[1])).done) return t20;
      if (y4 = 0, t20) op = [op[0] & 2, t20.value];
      switch (op[0]) {
        case 0:
        case 1:
          t20 = op;
          break;
        case 4:
          _2.label++;
          return { value: op[1], done: false };
        case 5:
          _2.label++;
          y4 = op[1];
          op = [0];
          continue;
        case 7:
          op = _2.ops.pop();
          _2.trys.pop();
          continue;
        default:
          if (!(t20 = _2.trys, t20 = t20.length > 0 && t20[t20.length - 1]) && (op[0] === 6 || op[0] === 2)) {
            _2 = 0;
            continue;
          }
          if (op[0] === 3 && (!t20 || op[1] > t20[0] && op[1] < t20[3])) {
            _2.label = op[1];
            break;
          }
          if (op[0] === 6 && _2.label < t20[1]) {
            _2.label = t20[1];
            t20 = op;
            break;
          }
          if (t20 && _2.label < t20[2]) {
            _2.label = t20[2];
            _2.ops.push(op);
            break;
          }
          if (t20[2]) _2.ops.pop();
          _2.trys.pop();
          continue;
      }
      op = body.call(thisArg, _2);
    } catch (e14) {
      op = [6, e14];
      y4 = 0;
    } finally {
      f15 = t20 = 0;
    }
    if (op[0] & 5) throw op[1];
    return { value: op[0] ? op[1] : void 0, done: true };
  }
}
function __exportStar(m12, o8) {
  for (var p11 in m12) if (p11 !== "default" && !Object.prototype.hasOwnProperty.call(o8, p11)) __createBinding(o8, m12, p11);
}
function __values(o8) {
  var s36 = typeof Symbol === "function" && Symbol.iterator, m12 = s36 && o8[s36], i13 = 0;
  if (m12) return m12.call(o8);
  if (o8 && typeof o8.length === "number") return {
    next: function() {
      if (o8 && i13 >= o8.length) o8 = void 0;
      return { value: o8 && o8[i13++], done: !o8 };
    }
  };
  throw new TypeError(s36 ? "Object is not iterable." : "Symbol.iterator is not defined.");
}
function __read(o8, n13) {
  var m12 = typeof Symbol === "function" && o8[Symbol.iterator];
  if (!m12) return o8;
  var i13 = m12.call(o8), r7, ar2 = [], e14;
  try {
    while ((n13 === void 0 || n13-- > 0) && !(r7 = i13.next()).done) ar2.push(r7.value);
  } catch (error) {
    e14 = { error };
  } finally {
    try {
      if (r7 && !r7.done && (m12 = i13["return"])) m12.call(i13);
    } finally {
      if (e14) throw e14.error;
    }
  }
  return ar2;
}
function __spread() {
  for (var ar2 = [], i13 = 0; i13 < arguments.length; i13++)
    ar2 = ar2.concat(__read(arguments[i13]));
  return ar2;
}
function __spreadArrays() {
  for (var s36 = 0, i13 = 0, il2 = arguments.length; i13 < il2; i13++) s36 += arguments[i13].length;
  for (var r7 = Array(s36), k5 = 0, i13 = 0; i13 < il2; i13++)
    for (var a18 = arguments[i13], j3 = 0, jl = a18.length; j3 < jl; j3++, k5++)
      r7[k5] = a18[j3];
  return r7;
}
function __spreadArray(to, from, pack) {
  if (pack || arguments.length === 2) for (var i13 = 0, l12 = from.length, ar2; i13 < l12; i13++) {
    if (ar2 || !(i13 in from)) {
      if (!ar2) ar2 = Array.prototype.slice.call(from, 0, i13);
      ar2[i13] = from[i13];
    }
  }
  return to.concat(ar2 || Array.prototype.slice.call(from));
}
function __await(v5) {
  return this instanceof __await ? (this.v = v5, this) : new __await(v5);
}
function __asyncGenerator(thisArg, _arguments, generator) {
  if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
  var g7 = generator.apply(thisArg, _arguments || []), i13, q2 = [];
  return i13 = Object.create((typeof AsyncIterator === "function" ? AsyncIterator : Object).prototype), verb("next"), verb("throw"), verb("return", awaitReturn), i13[Symbol.asyncIterator] = function() {
    return this;
  }, i13;
  function awaitReturn(f15) {
    return function(v5) {
      return Promise.resolve(v5).then(f15, reject);
    };
  }
  function verb(n13, f15) {
    if (g7[n13]) {
      i13[n13] = function(v5) {
        return new Promise(function(a18, b4) {
          q2.push([n13, v5, a18, b4]) > 1 || resume(n13, v5);
        });
      };
      if (f15) i13[n13] = f15(i13[n13]);
    }
  }
  function resume(n13, v5) {
    try {
      step(g7[n13](v5));
    } catch (e14) {
      settle(q2[0][3], e14);
    }
  }
  function step(r7) {
    r7.value instanceof __await ? Promise.resolve(r7.value.v).then(fulfill, reject) : settle(q2[0][2], r7);
  }
  function fulfill(value) {
    resume("next", value);
  }
  function reject(value) {
    resume("throw", value);
  }
  function settle(f15, v5) {
    if (f15(v5), q2.shift(), q2.length) resume(q2[0][0], q2[0][1]);
  }
}
function __asyncDelegator(o8) {
  var i13, p11;
  return i13 = {}, verb("next"), verb("throw", function(e14) {
    throw e14;
  }), verb("return"), i13[Symbol.iterator] = function() {
    return this;
  }, i13;
  function verb(n13, f15) {
    i13[n13] = o8[n13] ? function(v5) {
      return (p11 = !p11) ? { value: __await(o8[n13](v5)), done: false } : f15 ? f15(v5) : v5;
    } : f15;
  }
}
function __asyncValues(o8) {
  if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
  var m12 = o8[Symbol.asyncIterator], i13;
  return m12 ? m12.call(o8) : (o8 = typeof __values === "function" ? __values(o8) : o8[Symbol.iterator](), i13 = {}, verb("next"), verb("throw"), verb("return"), i13[Symbol.asyncIterator] = function() {
    return this;
  }, i13);
  function verb(n13) {
    i13[n13] = o8[n13] && function(v5) {
      return new Promise(function(resolve, reject) {
        v5 = o8[n13](v5), settle(resolve, reject, v5.done, v5.value);
      });
    };
  }
  function settle(resolve, reject, d12, v5) {
    Promise.resolve(v5).then(function(v6) {
      resolve({ value: v6, done: d12 });
    }, reject);
  }
}
function __makeTemplateObject(cooked, raw) {
  if (Object.defineProperty) {
    Object.defineProperty(cooked, "raw", { value: raw });
  } else {
    cooked.raw = raw;
  }
  return cooked;
}
function __importStar(mod) {
  if (mod && mod.__esModule) return mod;
  var result = {};
  if (mod != null) {
    for (var k5 = ownKeys(mod), i13 = 0; i13 < k5.length; i13++) if (k5[i13] !== "default") __createBinding(result, mod, k5[i13]);
  }
  __setModuleDefault(result, mod);
  return result;
}
function __importDefault(mod) {
  return mod && mod.__esModule ? mod : { default: mod };
}
function __classPrivateFieldGet(receiver, state2, kind, f15) {
  if (kind === "a" && !f15) throw new TypeError("Private accessor was defined without a getter");
  if (typeof state2 === "function" ? receiver !== state2 || !f15 : !state2.has(receiver)) throw new TypeError("Cannot read private member from an object whose class did not declare it");
  return kind === "m" ? f15 : kind === "a" ? f15.call(receiver) : f15 ? f15.value : state2.get(receiver);
}
function __classPrivateFieldSet(receiver, state2, value, kind, f15) {
  if (kind === "m") throw new TypeError("Private method is not writable");
  if (kind === "a" && !f15) throw new TypeError("Private accessor was defined without a setter");
  if (typeof state2 === "function" ? receiver !== state2 || !f15 : !state2.has(receiver)) throw new TypeError("Cannot write private member to an object whose class did not declare it");
  return kind === "a" ? f15.call(receiver, value) : f15 ? f15.value = value : state2.set(receiver, value), value;
}
function __classPrivateFieldIn(state2, receiver) {
  if (receiver === null || typeof receiver !== "object" && typeof receiver !== "function") throw new TypeError("Cannot use 'in' operator on non-object");
  return typeof state2 === "function" ? receiver === state2 : state2.has(receiver);
}
function __addDisposableResource(env2, value, async) {
  if (value !== null && value !== void 0) {
    if (typeof value !== "object" && typeof value !== "function") throw new TypeError("Object expected.");
    var dispose, inner3;
    if (async) {
      if (!Symbol.asyncDispose) throw new TypeError("Symbol.asyncDispose is not defined.");
      dispose = value[Symbol.asyncDispose];
    }
    if (dispose === void 0) {
      if (!Symbol.dispose) throw new TypeError("Symbol.dispose is not defined.");
      dispose = value[Symbol.dispose];
      if (async) inner3 = dispose;
    }
    if (typeof dispose !== "function") throw new TypeError("Object not disposable.");
    if (inner3) dispose = function() {
      try {
        inner3.call(this);
      } catch (e14) {
        return Promise.reject(e14);
      }
    };
    env2.stack.push({ value, dispose, async });
  } else if (async) {
    env2.stack.push({ async: true });
  }
  return value;
}
function __disposeResources(env2) {
  function fail(e14) {
    env2.error = env2.hasError ? new _SuppressedError(e14, env2.error, "An error was suppressed during disposal.") : e14;
    env2.hasError = true;
  }
  var r7, s36 = 0;
  function next2() {
    while (r7 = env2.stack.pop()) {
      try {
        if (!r7.async && s36 === 1) return s36 = 0, env2.stack.push(r7), Promise.resolve().then(next2);
        if (r7.dispose) {
          var result = r7.dispose.call(r7.value);
          if (r7.async) return s36 |= 2, Promise.resolve(result).then(next2, function(e14) {
            fail(e14);
            return next2();
          });
        } else s36 |= 1;
      } catch (e14) {
        fail(e14);
      }
    }
    if (s36 === 1) return env2.hasError ? Promise.reject(env2.error) : Promise.resolve();
    if (env2.hasError) throw env2.error;
  }
  return next2();
}
function __rewriteRelativeImportExtension(path, preserveJsx) {
  if (typeof path === "string" && /^\.\.?\//.test(path)) {
    return path.replace(/\.(tsx)$|((?:\.d)?)((?:\.[^./]+?)?)\.([cm]?)ts$/i, function(m12, tsx, d12, ext, cm) {
      return tsx ? preserveJsx ? ".jsx" : ".js" : d12 && (!ext || !cm) ? m12 : d12 + ext + "." + cm.toLowerCase() + "js";
    });
  }
  return path;
}
var extendStatics, __assign, __createBinding, __setModuleDefault, ownKeys, _SuppressedError, tslib_es6_default;
var init_tslib_es6 = __esm({
  "node_modules/tslib/tslib.es6.mjs"() {
    extendStatics = function(d12, b4) {
      extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d13, b5) {
        d13.__proto__ = b5;
      } || function(d13, b5) {
        for (var p11 in b5) if (Object.prototype.hasOwnProperty.call(b5, p11)) d13[p11] = b5[p11];
      };
      return extendStatics(d12, b4);
    };
    __assign = function() {
      __assign = Object.assign || function __assign2(t20) {
        for (var s36, i13 = 1, n13 = arguments.length; i13 < n13; i13++) {
          s36 = arguments[i13];
          for (var p11 in s36) if (Object.prototype.hasOwnProperty.call(s36, p11)) t20[p11] = s36[p11];
        }
        return t20;
      };
      return __assign.apply(this, arguments);
    };
    __createBinding = Object.create ? function(o8, m12, k5, k22) {
      if (k22 === void 0) k22 = k5;
      var desc = Object.getOwnPropertyDescriptor(m12, k5);
      if (!desc || ("get" in desc ? !m12.__esModule : desc.writable || desc.configurable)) {
        desc = { enumerable: true, get: function() {
          return m12[k5];
        } };
      }
      Object.defineProperty(o8, k22, desc);
    } : function(o8, m12, k5, k22) {
      if (k22 === void 0) k22 = k5;
      o8[k22] = m12[k5];
    };
    __setModuleDefault = Object.create ? function(o8, v5) {
      Object.defineProperty(o8, "default", { enumerable: true, value: v5 });
    } : function(o8, v5) {
      o8["default"] = v5;
    };
    ownKeys = function(o8) {
      ownKeys = Object.getOwnPropertyNames || function(o9) {
        var ar2 = [];
        for (var k5 in o9) if (Object.prototype.hasOwnProperty.call(o9, k5)) ar2[ar2.length] = k5;
        return ar2;
      };
      return ownKeys(o8);
    };
    _SuppressedError = typeof SuppressedError === "function" ? SuppressedError : function(error, suppressed, message) {
      var e14 = new Error(message);
      return e14.name = "SuppressedError", e14.error = error, e14.suppressed = suppressed, e14;
    };
    tslib_es6_default = {
      __extends,
      __assign,
      __rest,
      __decorate,
      __param,
      __esDecorate,
      __runInitializers,
      __propKey,
      __setFunctionName,
      __metadata,
      __awaiter,
      __generator,
      __createBinding,
      __exportStar,
      __values,
      __read,
      __spread,
      __spreadArrays,
      __spreadArray,
      __await,
      __asyncGenerator,
      __asyncDelegator,
      __asyncValues,
      __makeTemplateObject,
      __importStar,
      __importDefault,
      __classPrivateFieldGet,
      __classPrivateFieldSet,
      __classPrivateFieldIn,
      __addDisposableResource,
      __disposeResources,
      __rewriteRelativeImportExtension
    };
  }
});

// node_modules/react-remove-scroll-bar/dist/es5/constants.js
var require_constants = __commonJS({
  "node_modules/react-remove-scroll-bar/dist/es5/constants.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.removedBarSizeVariable = exports.noScrollbarsClassName = exports.fullWidthClassName = exports.zeroRightClassName = void 0;
    exports.zeroRightClassName = "right-scroll-bar-position";
    exports.fullWidthClassName = "width-before-scroll-bar";
    exports.noScrollbarsClassName = "with-scroll-bars-hidden";
    exports.removedBarSizeVariable = "--removed-body-scroll-bar-size";
  }
});

// node_modules/use-callback-ref/dist/es5/assignRef.js
var require_assignRef = __commonJS({
  "node_modules/use-callback-ref/dist/es5/assignRef.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.assignRef = void 0;
    function assignRef2(ref, value) {
      if (typeof ref === "function") {
        ref(value);
      } else if (ref) {
        ref.current = value;
      }
      return ref;
    }
    exports.assignRef = assignRef2;
  }
});

// node_modules/use-callback-ref/dist/es5/useRef.js
var require_useRef = __commonJS({
  "node_modules/use-callback-ref/dist/es5/useRef.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.useCallbackRef = void 0;
    var react_1 = require_react();
    function useCallbackRef(initialValue, callback) {
      var ref = (0, react_1.useState)(function() {
        return {
          // value
          value: initialValue,
          // last callback
          callback,
          // "memoized" public interface
          facade: {
            get current() {
              return ref.value;
            },
            set current(value) {
              var last = ref.value;
              if (last !== value) {
                ref.value = value;
                ref.callback(value, last);
              }
            }
          }
        };
      })[0];
      ref.callback = callback;
      return ref.facade;
    }
    exports.useCallbackRef = useCallbackRef;
  }
});

// node_modules/use-callback-ref/dist/es5/createRef.js
var require_createRef = __commonJS({
  "node_modules/use-callback-ref/dist/es5/createRef.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createCallbackRef = void 0;
    function createCallbackRef(callback) {
      var current = null;
      return {
        get current() {
          return current;
        },
        set current(value) {
          var last = current;
          if (last !== value) {
            current = value;
            callback(value, last);
          }
        }
      };
    }
    exports.createCallbackRef = createCallbackRef;
  }
});

// node_modules/use-callback-ref/dist/es5/mergeRef.js
var require_mergeRef = __commonJS({
  "node_modules/use-callback-ref/dist/es5/mergeRef.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.mergeRefs = void 0;
    var assignRef_1 = require_assignRef();
    var createRef_1 = require_createRef();
    function mergeRefs2(refs) {
      return (0, createRef_1.createCallbackRef)(function(newValue) {
        return refs.forEach(function(ref) {
          return (0, assignRef_1.assignRef)(ref, newValue);
        });
      });
    }
    exports.mergeRefs = mergeRefs2;
  }
});

// node_modules/use-callback-ref/dist/es5/useMergeRef.js
var require_useMergeRef = __commonJS({
  "node_modules/use-callback-ref/dist/es5/useMergeRef.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.useMergeRefs = void 0;
    var tslib_1 = (init_tslib_es6(), __toCommonJS(tslib_es6_exports));
    var React10 = tslib_1.__importStar(require_react());
    var assignRef_1 = require_assignRef();
    var useRef_1 = require_useRef();
    var useIsomorphicLayoutEffect3 = typeof window !== "undefined" ? React10.useLayoutEffect : React10.useEffect;
    var currentValues = /* @__PURE__ */ new WeakMap();
    function useMergeRefs(refs, defaultValue2) {
      var callbackRef = (0, useRef_1.useCallbackRef)(defaultValue2 || null, function(newValue) {
        return refs.forEach(function(ref) {
          return (0, assignRef_1.assignRef)(ref, newValue);
        });
      });
      useIsomorphicLayoutEffect3(function() {
        var oldValue = currentValues.get(callbackRef);
        if (oldValue) {
          var prevRefs_1 = new Set(oldValue);
          var nextRefs_1 = new Set(refs);
          var current_1 = callbackRef.current;
          prevRefs_1.forEach(function(ref) {
            if (!nextRefs_1.has(ref)) {
              (0, assignRef_1.assignRef)(ref, null);
            }
          });
          nextRefs_1.forEach(function(ref) {
            if (!prevRefs_1.has(ref)) {
              (0, assignRef_1.assignRef)(ref, current_1);
            }
          });
        }
        currentValues.set(callbackRef, refs);
      }, [refs]);
      return callbackRef;
    }
    exports.useMergeRefs = useMergeRefs;
  }
});

// node_modules/use-callback-ref/dist/es5/useTransformRef.js
var require_useTransformRef = __commonJS({
  "node_modules/use-callback-ref/dist/es5/useTransformRef.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.useTransformRef = void 0;
    var assignRef_1 = require_assignRef();
    var useRef_1 = require_useRef();
    function useTransformRef(ref, transformer) {
      return (0, useRef_1.useCallbackRef)(null, function(value) {
        return (0, assignRef_1.assignRef)(ref, transformer(value));
      });
    }
    exports.useTransformRef = useTransformRef;
  }
});

// node_modules/use-callback-ref/dist/es5/transformRef.js
var require_transformRef = __commonJS({
  "node_modules/use-callback-ref/dist/es5/transformRef.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.transformRef = void 0;
    var assignRef_1 = require_assignRef();
    var createRef_1 = require_createRef();
    function transformRef(ref, transformer) {
      return (0, createRef_1.createCallbackRef)(function(value) {
        return (0, assignRef_1.assignRef)(ref, transformer(value));
      });
    }
    exports.transformRef = transformRef;
  }
});

// node_modules/use-callback-ref/dist/es5/refToCallback.js
var require_refToCallback = __commonJS({
  "node_modules/use-callback-ref/dist/es5/refToCallback.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.useRefToCallback = exports.refToCallback = void 0;
    function refToCallback(ref) {
      return function(newValue) {
        if (typeof ref === "function") {
          ref(newValue);
        } else if (ref) {
          ref.current = newValue;
        }
      };
    }
    exports.refToCallback = refToCallback;
    var nullCallback = function() {
      return null;
    };
    var weakMem = /* @__PURE__ */ new WeakMap();
    var weakMemoize = function(ref) {
      var usedRef = ref || nullCallback;
      var storedRef = weakMem.get(usedRef);
      if (storedRef) {
        return storedRef;
      }
      var cb = refToCallback(usedRef);
      weakMem.set(usedRef, cb);
      return cb;
    };
    function useRefToCallback(ref) {
      return weakMemoize(ref);
    }
    exports.useRefToCallback = useRefToCallback;
  }
});

// node_modules/use-callback-ref/dist/es5/index.js
var require_es5 = __commonJS({
  "node_modules/use-callback-ref/dist/es5/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.useRefToCallback = exports.refToCallback = exports.transformRef = exports.useTransformRef = exports.useMergeRefs = exports.mergeRefs = exports.createCallbackRef = exports.useCallbackRef = exports.assignRef = void 0;
    var assignRef_1 = require_assignRef();
    Object.defineProperty(exports, "assignRef", { enumerable: true, get: function() {
      return assignRef_1.assignRef;
    } });
    var useRef_1 = require_useRef();
    Object.defineProperty(exports, "useCallbackRef", { enumerable: true, get: function() {
      return useRef_1.useCallbackRef;
    } });
    var createRef_1 = require_createRef();
    Object.defineProperty(exports, "createCallbackRef", { enumerable: true, get: function() {
      return createRef_1.createCallbackRef;
    } });
    var mergeRef_1 = require_mergeRef();
    Object.defineProperty(exports, "mergeRefs", { enumerable: true, get: function() {
      return mergeRef_1.mergeRefs;
    } });
    var useMergeRef_1 = require_useMergeRef();
    Object.defineProperty(exports, "useMergeRefs", { enumerable: true, get: function() {
      return useMergeRef_1.useMergeRefs;
    } });
    var useTransformRef_1 = require_useTransformRef();
    Object.defineProperty(exports, "useTransformRef", { enumerable: true, get: function() {
      return useTransformRef_1.useTransformRef;
    } });
    var transformRef_1 = require_transformRef();
    Object.defineProperty(exports, "transformRef", { enumerable: true, get: function() {
      return transformRef_1.transformRef;
    } });
    var refToCallback_1 = require_refToCallback();
    Object.defineProperty(exports, "refToCallback", { enumerable: true, get: function() {
      return refToCallback_1.refToCallback;
    } });
    Object.defineProperty(exports, "useRefToCallback", { enumerable: true, get: function() {
      return refToCallback_1.useRefToCallback;
    } });
  }
});

// node_modules/detect-node-es/es5/node.js
var require_node = __commonJS({
  "node_modules/detect-node-es/es5/node.js"(exports, module) {
    module.exports.isNode = Object.prototype.toString.call(typeof process !== "undefined" ? process : 0) === "[object process]";
  }
});

// node_modules/use-sidecar/dist/es5/env.js
var require_env = __commonJS({
  "node_modules/use-sidecar/dist/es5/env.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.env = void 0;
    var detect_node_es_1 = require_node();
    exports.env = {
      isNode: detect_node_es_1.isNode,
      forceCache: false
    };
  }
});

// node_modules/use-sidecar/dist/es5/hook.js
var require_hook = __commonJS({
  "node_modules/use-sidecar/dist/es5/hook.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.useSidecar = void 0;
    var react_1 = require_react();
    var env_1 = require_env();
    var cache2 = /* @__PURE__ */ new WeakMap();
    var NO_OPTIONS = {};
    function useSidecar(importer, effect) {
      var options = effect && effect.options || NO_OPTIONS;
      if (env_1.env.isNode && !options.ssr) {
        return [null, null];
      }
      return useRealSidecar(importer, effect);
    }
    exports.useSidecar = useSidecar;
    function useRealSidecar(importer, effect) {
      var options = effect && effect.options || NO_OPTIONS;
      var couldUseCache = env_1.env.forceCache || env_1.env.isNode && !!options.ssr || !options.async;
      var _a = (0, react_1.useState)(couldUseCache ? function() {
        return cache2.get(importer);
      } : void 0), Car = _a[0], setCar = _a[1];
      var _b = (0, react_1.useState)(null), error = _b[0], setError = _b[1];
      (0, react_1.useEffect)(function() {
        if (!Car) {
          importer().then(function(car) {
            var resolved = effect ? effect.read() : car.default || car;
            if (!resolved) {
              console.error("Sidecar error: with importer", importer);
              var error_1;
              if (effect) {
                console.error("Sidecar error: with medium", effect);
                error_1 = new Error("Sidecar medium was not found");
              } else {
                error_1 = new Error("Sidecar was not found in exports");
              }
              setError(function() {
                return error_1;
              });
              throw error_1;
            }
            cache2.set(importer, resolved);
            setCar(function() {
              return resolved;
            });
          }, function(e14) {
            return setError(function() {
              return e14;
            });
          });
        }
      }, []);
      return [Car, error];
    }
  }
});

// node_modules/use-sidecar/dist/es5/hoc.js
var require_hoc = __commonJS({
  "node_modules/use-sidecar/dist/es5/hoc.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.sidecar = void 0;
    var tslib_1 = (init_tslib_es6(), __toCommonJS(tslib_es6_exports));
    var React10 = tslib_1.__importStar(require_react());
    var hook_1 = require_hook();
    function sidecar(importer, errorComponent) {
      var ErrorCase = function() {
        return errorComponent;
      };
      return function Sidecar(props) {
        var _a = (0, hook_1.useSidecar)(importer, props.sideCar), Car = _a[0], error = _a[1];
        if (error && errorComponent) {
          return ErrorCase;
        }
        return Car ? React10.createElement(Car, tslib_1.__assign({}, props)) : null;
      };
    }
    exports.sidecar = sidecar;
  }
});

// node_modules/use-sidecar/dist/es5/config.js
var require_config = __commonJS({
  "node_modules/use-sidecar/dist/es5/config.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.setConfig = exports.config = void 0;
    exports.config = {
      onError: function(e14) {
        return console.error(e14);
      }
    };
    var setConfig = function(conf) {
      Object.assign(exports.config, conf);
    };
    exports.setConfig = setConfig;
  }
});

// node_modules/use-sidecar/dist/es5/medium.js
var require_medium = __commonJS({
  "node_modules/use-sidecar/dist/es5/medium.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createSidecarMedium = exports.createMedium = void 0;
    var tslib_1 = (init_tslib_es6(), __toCommonJS(tslib_es6_exports));
    function ItoI(a18) {
      return a18;
    }
    function innerCreateMedium(defaults, middleware) {
      if (middleware === void 0) {
        middleware = ItoI;
      }
      var buffer = [];
      var assigned = false;
      var medium = {
        read: function() {
          if (assigned) {
            throw new Error("Sidecar: could not `read` from an `assigned` medium. `read` could be used only with `useMedium`.");
          }
          if (buffer.length) {
            return buffer[buffer.length - 1];
          }
          return defaults;
        },
        useMedium: function(data) {
          var item2 = middleware(data, assigned);
          buffer.push(item2);
          return function() {
            buffer = buffer.filter(function(x3) {
              return x3 !== item2;
            });
          };
        },
        assignSyncMedium: function(cb) {
          assigned = true;
          while (buffer.length) {
            var cbs = buffer;
            buffer = [];
            cbs.forEach(cb);
          }
          buffer = {
            push: function(x3) {
              return cb(x3);
            },
            filter: function() {
              return buffer;
            }
          };
        },
        assignMedium: function(cb) {
          assigned = true;
          var pendingQueue = [];
          if (buffer.length) {
            var cbs = buffer;
            buffer = [];
            cbs.forEach(cb);
            pendingQueue = buffer;
          }
          var executeQueue = function() {
            var cbs2 = pendingQueue;
            pendingQueue = [];
            cbs2.forEach(cb);
          };
          var cycle = function() {
            return Promise.resolve().then(executeQueue);
          };
          cycle();
          buffer = {
            push: function(x3) {
              pendingQueue.push(x3);
              cycle();
            },
            filter: function(filter2) {
              pendingQueue = pendingQueue.filter(filter2);
              return buffer;
            }
          };
        }
      };
      return medium;
    }
    function createMedium(defaults, middleware) {
      if (middleware === void 0) {
        middleware = ItoI;
      }
      return innerCreateMedium(defaults, middleware);
    }
    exports.createMedium = createMedium;
    function createSidecarMedium(options) {
      if (options === void 0) {
        options = {};
      }
      var medium = innerCreateMedium(null);
      medium.options = tslib_1.__assign({ async: true, ssr: false }, options);
      return medium;
    }
    exports.createSidecarMedium = createSidecarMedium;
  }
});

// node_modules/use-sidecar/dist/es5/renderProp.js
var require_renderProp = __commonJS({
  "node_modules/use-sidecar/dist/es5/renderProp.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.renderCar = void 0;
    var tslib_1 = (init_tslib_es6(), __toCommonJS(tslib_es6_exports));
    var React10 = tslib_1.__importStar(require_react());
    var react_1 = require_react();
    function renderCar(WrappedComponent, defaults) {
      function State(_a) {
        var stateRef = _a.stateRef, props = _a.props;
        var renderTarget = (0, react_1.useCallback)(function SideTarget() {
          var args = [];
          for (var _i2 = 0; _i2 < arguments.length; _i2++) {
            args[_i2] = arguments[_i2];
          }
          (0, react_1.useLayoutEffect)(function() {
            stateRef.current(args);
          });
          return null;
        }, []);
        return React10.createElement(WrappedComponent, tslib_1.__assign({}, props, { children: renderTarget }));
      }
      var Children2 = React10.memo(function(_a) {
        var stateRef = _a.stateRef, defaultState = _a.defaultState, children = _a.children;
        var _b = (0, react_1.useState)(defaultState.current), state2 = _b[0], setState = _b[1];
        (0, react_1.useEffect)(function() {
          stateRef.current = setState;
        }, []);
        return children.apply(void 0, state2);
      }, function() {
        return true;
      });
      return function Combiner(props) {
        var defaultState = React10.useRef(defaults(props));
        var ref = React10.useRef(function(state2) {
          return defaultState.current = state2;
        });
        return React10.createElement(
          React10.Fragment,
          null,
          React10.createElement(State, { stateRef: ref, props }),
          React10.createElement(Children2, { stateRef: ref, defaultState, children: props.children })
        );
      };
    }
    exports.renderCar = renderCar;
  }
});

// node_modules/use-sidecar/dist/es5/exports.js
var require_exports = __commonJS({
  "node_modules/use-sidecar/dist/es5/exports.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.exportSidecar = void 0;
    var tslib_1 = (init_tslib_es6(), __toCommonJS(tslib_es6_exports));
    var React10 = tslib_1.__importStar(require_react());
    var SideCar = function(_a) {
      var sideCar = _a.sideCar, rest = tslib_1.__rest(_a, ["sideCar"]);
      if (!sideCar) {
        throw new Error("Sidecar: please provide `sideCar` property to import the right car");
      }
      var Target = sideCar.read();
      if (!Target) {
        throw new Error("Sidecar medium not found");
      }
      return React10.createElement(Target, tslib_1.__assign({}, rest));
    };
    SideCar.isSideCarExport = true;
    function exportSidecar(medium, exported) {
      medium.useMedium(exported);
      return SideCar;
    }
    exports.exportSidecar = exportSidecar;
  }
});

// node_modules/use-sidecar/dist/es5/index.js
var require_es52 = __commonJS({
  "node_modules/use-sidecar/dist/es5/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.exportSidecar = exports.renderCar = exports.createSidecarMedium = exports.createMedium = exports.setConfig = exports.useSidecar = exports.sidecar = void 0;
    var hoc_1 = require_hoc();
    Object.defineProperty(exports, "sidecar", { enumerable: true, get: function() {
      return hoc_1.sidecar;
    } });
    var hook_1 = require_hook();
    Object.defineProperty(exports, "useSidecar", { enumerable: true, get: function() {
      return hook_1.useSidecar;
    } });
    var config_1 = require_config();
    Object.defineProperty(exports, "setConfig", { enumerable: true, get: function() {
      return config_1.setConfig;
    } });
    var medium_1 = require_medium();
    Object.defineProperty(exports, "createMedium", { enumerable: true, get: function() {
      return medium_1.createMedium;
    } });
    Object.defineProperty(exports, "createSidecarMedium", { enumerable: true, get: function() {
      return medium_1.createSidecarMedium;
    } });
    var renderProp_1 = require_renderProp();
    Object.defineProperty(exports, "renderCar", { enumerable: true, get: function() {
      return renderProp_1.renderCar;
    } });
    var exports_1 = require_exports();
    Object.defineProperty(exports, "exportSidecar", { enumerable: true, get: function() {
      return exports_1.exportSidecar;
    } });
  }
});

// node_modules/react-remove-scroll/dist/es5/medium.js
var require_medium2 = __commonJS({
  "node_modules/react-remove-scroll/dist/es5/medium.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.effectCar = void 0;
    var use_sidecar_1 = require_es52();
    exports.effectCar = (0, use_sidecar_1.createSidecarMedium)();
  }
});

// node_modules/react-remove-scroll/dist/es5/UI.js
var require_UI = __commonJS({
  "node_modules/react-remove-scroll/dist/es5/UI.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.RemoveScroll = void 0;
    var tslib_1 = (init_tslib_es6(), __toCommonJS(tslib_es6_exports));
    var React10 = tslib_1.__importStar(require_react());
    var constants_1 = require_constants();
    var use_callback_ref_1 = require_es5();
    var medium_1 = require_medium2();
    var nothing = function() {
      return;
    };
    var RemoveScroll3 = React10.forwardRef(function(props, parentRef) {
      var ref = React10.useRef(null);
      var _a = React10.useState({
        onScrollCapture: nothing,
        onWheelCapture: nothing,
        onTouchMoveCapture: nothing
      }), callbacks = _a[0], setCallbacks = _a[1];
      var forwardProps = props.forwardProps, children = props.children, className = props.className, removeScrollBar = props.removeScrollBar, enabled = props.enabled, shards = props.shards, sideCar = props.sideCar, noIsolation = props.noIsolation, inert = props.inert, allowPinchZoom = props.allowPinchZoom, _b = props.as, Container2 = _b === void 0 ? "div" : _b, gapMode = props.gapMode, rest = tslib_1.__rest(props, ["forwardProps", "children", "className", "removeScrollBar", "enabled", "shards", "sideCar", "noIsolation", "inert", "allowPinchZoom", "as", "gapMode"]);
      var SideCar = sideCar;
      var containerRef = (0, use_callback_ref_1.useMergeRefs)([ref, parentRef]);
      var containerProps = tslib_1.__assign(tslib_1.__assign({}, rest), callbacks);
      return React10.createElement(
        React10.Fragment,
        null,
        enabled && React10.createElement(SideCar, { sideCar: medium_1.effectCar, removeScrollBar, shards, noIsolation, inert, setCallbacks, allowPinchZoom: !!allowPinchZoom, lockRef: ref, gapMode }),
        forwardProps ? React10.cloneElement(React10.Children.only(children), tslib_1.__assign(tslib_1.__assign({}, containerProps), { ref: containerRef })) : React10.createElement(Container2, tslib_1.__assign({}, containerProps, { className, ref: containerRef }), children)
      );
    });
    exports.RemoveScroll = RemoveScroll3;
    RemoveScroll3.defaultProps = {
      enabled: true,
      removeScrollBar: true,
      inert: false
    };
    RemoveScroll3.classNames = {
      fullWidth: constants_1.fullWidthClassName,
      zeroRight: constants_1.zeroRightClassName
    };
  }
});

// node_modules/get-nonce/dist/es5/index.js
var require_es53 = __commonJS({
  "node_modules/get-nonce/dist/es5/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var currentNonce;
    exports.setNonce = function(nonce) {
      currentNonce = nonce;
    };
    exports.getNonce = function() {
      if (currentNonce) {
        return currentNonce;
      }
      if (typeof __webpack_nonce__ !== "undefined") {
        return __webpack_nonce__;
      }
      return void 0;
    };
  }
});

// node_modules/react-style-singleton/dist/es5/singleton.js
var require_singleton = __commonJS({
  "node_modules/react-style-singleton/dist/es5/singleton.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.stylesheetSingleton = void 0;
    var get_nonce_1 = require_es53();
    function makeStyleTag() {
      if (!document)
        return null;
      var tag2 = document.createElement("style");
      tag2.type = "text/css";
      var nonce = (0, get_nonce_1.getNonce)();
      if (nonce) {
        tag2.setAttribute("nonce", nonce);
      }
      return tag2;
    }
    function injectStyles(tag2, css) {
      if (tag2.styleSheet) {
        tag2.styleSheet.cssText = css;
      } else {
        tag2.appendChild(document.createTextNode(css));
      }
    }
    function insertStyleTag(tag2) {
      var head = document.head || document.getElementsByTagName("head")[0];
      head.appendChild(tag2);
    }
    var stylesheetSingleton = function() {
      var counter = 0;
      var stylesheet = null;
      return {
        add: function(style) {
          if (counter == 0) {
            if (stylesheet = makeStyleTag()) {
              injectStyles(stylesheet, style);
              insertStyleTag(stylesheet);
            }
          }
          counter++;
        },
        remove: function() {
          counter--;
          if (!counter && stylesheet) {
            stylesheet.parentNode && stylesheet.parentNode.removeChild(stylesheet);
            stylesheet = null;
          }
        }
      };
    };
    exports.stylesheetSingleton = stylesheetSingleton;
  }
});

// node_modules/react-style-singleton/dist/es5/hook.js
var require_hook2 = __commonJS({
  "node_modules/react-style-singleton/dist/es5/hook.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.styleHookSingleton = void 0;
    var tslib_1 = (init_tslib_es6(), __toCommonJS(tslib_es6_exports));
    var React10 = tslib_1.__importStar(require_react());
    var singleton_1 = require_singleton();
    var styleHookSingleton = function() {
      var sheet = (0, singleton_1.stylesheetSingleton)();
      return function(styles, isDynamic) {
        React10.useEffect(function() {
          sheet.add(styles);
          return function() {
            sheet.remove();
          };
        }, [styles && isDynamic]);
      };
    };
    exports.styleHookSingleton = styleHookSingleton;
  }
});

// node_modules/react-style-singleton/dist/es5/component.js
var require_component = __commonJS({
  "node_modules/react-style-singleton/dist/es5/component.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.styleSingleton = void 0;
    var hook_1 = require_hook2();
    var styleSingleton = function() {
      var useStyle2 = (0, hook_1.styleHookSingleton)();
      var Sheet = function(_a) {
        var styles = _a.styles, dynamic = _a.dynamic;
        useStyle2(styles, dynamic);
        return null;
      };
      return Sheet;
    };
    exports.styleSingleton = styleSingleton;
  }
});

// node_modules/react-style-singleton/dist/es5/index.js
var require_es54 = __commonJS({
  "node_modules/react-style-singleton/dist/es5/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.styleHookSingleton = exports.stylesheetSingleton = exports.styleSingleton = void 0;
    var component_1 = require_component();
    Object.defineProperty(exports, "styleSingleton", { enumerable: true, get: function() {
      return component_1.styleSingleton;
    } });
    var singleton_1 = require_singleton();
    Object.defineProperty(exports, "stylesheetSingleton", { enumerable: true, get: function() {
      return singleton_1.stylesheetSingleton;
    } });
    var hook_1 = require_hook2();
    Object.defineProperty(exports, "styleHookSingleton", { enumerable: true, get: function() {
      return hook_1.styleHookSingleton;
    } });
  }
});

// node_modules/react-remove-scroll-bar/dist/es5/utils.js
var require_utils5 = __commonJS({
  "node_modules/react-remove-scroll-bar/dist/es5/utils.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.getGapWidth = exports.zeroGap = void 0;
    exports.zeroGap = {
      left: 0,
      top: 0,
      right: 0,
      gap: 0
    };
    var parse3 = function(x3) {
      return parseInt(x3 || "", 10) || 0;
    };
    var getOffset = function(gapMode) {
      var cs = window.getComputedStyle(document.body);
      var left = cs[gapMode === "padding" ? "paddingLeft" : "marginLeft"];
      var top2 = cs[gapMode === "padding" ? "paddingTop" : "marginTop"];
      var right = cs[gapMode === "padding" ? "paddingRight" : "marginRight"];
      return [parse3(left), parse3(top2), parse3(right)];
    };
    var getGapWidth = function(gapMode) {
      if (gapMode === void 0) {
        gapMode = "margin";
      }
      if (typeof window === "undefined") {
        return exports.zeroGap;
      }
      var offsets = getOffset(gapMode);
      var documentWidth = document.documentElement.clientWidth;
      var windowWidth = window.innerWidth;
      return {
        left: offsets[0],
        top: offsets[1],
        right: offsets[2],
        gap: Math.max(0, windowWidth - documentWidth + offsets[2] - offsets[0])
      };
    };
    exports.getGapWidth = getGapWidth;
  }
});

// node_modules/react-remove-scroll-bar/dist/es5/component.js
var require_component2 = __commonJS({
  "node_modules/react-remove-scroll-bar/dist/es5/component.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.RemoveScrollBar = exports.useLockAttribute = exports.lockAttribute = void 0;
    var tslib_1 = (init_tslib_es6(), __toCommonJS(tslib_es6_exports));
    var React10 = tslib_1.__importStar(require_react());
    var react_style_singleton_1 = require_es54();
    var constants_1 = require_constants();
    var utils_1 = require_utils5();
    var Style = (0, react_style_singleton_1.styleSingleton)();
    exports.lockAttribute = "data-scroll-locked";
    var getStyles = function(_a, allowRelative, gapMode, important) {
      var left = _a.left, top2 = _a.top, right = _a.right, gap = _a.gap;
      if (gapMode === void 0) {
        gapMode = "margin";
      }
      return "\n  .".concat(constants_1.noScrollbarsClassName, " {\n   overflow: hidden ").concat(important, ";\n   padding-right: ").concat(gap, "px ").concat(important, ";\n  }\n  body[").concat(exports.lockAttribute, "] {\n    overflow: hidden ").concat(important, ";\n    overscroll-behavior: contain;\n    ").concat([
        allowRelative && "position: relative ".concat(important, ";"),
        gapMode === "margin" && "\n    padding-left: ".concat(left, "px;\n    padding-top: ").concat(top2, "px;\n    padding-right: ").concat(right, "px;\n    margin-left:0;\n    margin-top:0;\n    margin-right: ").concat(gap, "px ").concat(important, ";\n    "),
        gapMode === "padding" && "padding-right: ".concat(gap, "px ").concat(important, ";")
      ].filter(Boolean).join(""), "\n  }\n  \n  .").concat(constants_1.zeroRightClassName, " {\n    right: ").concat(gap, "px ").concat(important, ";\n  }\n  \n  .").concat(constants_1.fullWidthClassName, " {\n    margin-right: ").concat(gap, "px ").concat(important, ";\n  }\n  \n  .").concat(constants_1.zeroRightClassName, " .").concat(constants_1.zeroRightClassName, " {\n    right: 0 ").concat(important, ";\n  }\n  \n  .").concat(constants_1.fullWidthClassName, " .").concat(constants_1.fullWidthClassName, " {\n    margin-right: 0 ").concat(important, ";\n  }\n  \n  body[").concat(exports.lockAttribute, "] {\n    ").concat(constants_1.removedBarSizeVariable, ": ").concat(gap, "px;\n  }\n");
    };
    var getCurrentUseCounter = function() {
      var counter = parseInt(document.body.getAttribute(exports.lockAttribute) || "0", 10);
      return isFinite(counter) ? counter : 0;
    };
    var useLockAttribute = function() {
      React10.useEffect(function() {
        document.body.setAttribute(exports.lockAttribute, (getCurrentUseCounter() + 1).toString());
        return function() {
          var newCounter = getCurrentUseCounter() - 1;
          if (newCounter <= 0) {
            document.body.removeAttribute(exports.lockAttribute);
          } else {
            document.body.setAttribute(exports.lockAttribute, newCounter.toString());
          }
        };
      }, []);
    };
    exports.useLockAttribute = useLockAttribute;
    var RemoveScrollBar = function(_a) {
      var noRelative = _a.noRelative, noImportant = _a.noImportant, _b = _a.gapMode, gapMode = _b === void 0 ? "margin" : _b;
      (0, exports.useLockAttribute)();
      var gap = React10.useMemo(function() {
        return (0, utils_1.getGapWidth)(gapMode);
      }, [gapMode]);
      return React10.createElement(Style, { styles: getStyles(gap, !noRelative, gapMode, !noImportant ? "!important" : "") });
    };
    exports.RemoveScrollBar = RemoveScrollBar;
  }
});

// node_modules/react-remove-scroll-bar/dist/es5/index.js
var require_es55 = __commonJS({
  "node_modules/react-remove-scroll-bar/dist/es5/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.getGapWidth = exports.removedBarSizeVariable = exports.noScrollbarsClassName = exports.fullWidthClassName = exports.zeroRightClassName = exports.RemoveScrollBar = void 0;
    var component_1 = require_component2();
    Object.defineProperty(exports, "RemoveScrollBar", { enumerable: true, get: function() {
      return component_1.RemoveScrollBar;
    } });
    var constants_1 = require_constants();
    Object.defineProperty(exports, "zeroRightClassName", { enumerable: true, get: function() {
      return constants_1.zeroRightClassName;
    } });
    Object.defineProperty(exports, "fullWidthClassName", { enumerable: true, get: function() {
      return constants_1.fullWidthClassName;
    } });
    Object.defineProperty(exports, "noScrollbarsClassName", { enumerable: true, get: function() {
      return constants_1.noScrollbarsClassName;
    } });
    Object.defineProperty(exports, "removedBarSizeVariable", { enumerable: true, get: function() {
      return constants_1.removedBarSizeVariable;
    } });
    var utils_1 = require_utils5();
    Object.defineProperty(exports, "getGapWidth", { enumerable: true, get: function() {
      return utils_1.getGapWidth;
    } });
  }
});

// node_modules/react-remove-scroll/dist/es5/aggresiveCapture.js
var require_aggresiveCapture = __commonJS({
  "node_modules/react-remove-scroll/dist/es5/aggresiveCapture.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.nonPassive = void 0;
    var passiveSupported = false;
    if (typeof window !== "undefined") {
      try {
        options = Object.defineProperty({}, "passive", {
          get: function() {
            passiveSupported = true;
            return true;
          }
        });
        window.addEventListener("test", options, options);
        window.removeEventListener("test", options, options);
      } catch (err) {
        passiveSupported = false;
      }
    }
    var options;
    exports.nonPassive = passiveSupported ? { passive: false } : false;
  }
});

// node_modules/react-remove-scroll/dist/es5/handleScroll.js
var require_handleScroll = __commonJS({
  "node_modules/react-remove-scroll/dist/es5/handleScroll.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.handleScroll = exports.locationCouldBeScrolled = void 0;
    var alwaysContainsScroll = function(node) {
      return node.tagName === "TEXTAREA";
    };
    var elementCanBeScrolled = function(node, overflow) {
      if (!(node instanceof Element)) {
        return false;
      }
      var styles = window.getComputedStyle(node);
      return (
        // not-not-scrollable
        styles[overflow] !== "hidden" && // contains scroll inside self
        !(styles.overflowY === styles.overflowX && !alwaysContainsScroll(node) && styles[overflow] === "visible")
      );
    };
    var elementCouldBeVScrolled = function(node) {
      return elementCanBeScrolled(node, "overflowY");
    };
    var elementCouldBeHScrolled = function(node) {
      return elementCanBeScrolled(node, "overflowX");
    };
    var locationCouldBeScrolled = function(axis, node) {
      var ownerDocument = node.ownerDocument;
      var current = node;
      do {
        if (typeof ShadowRoot !== "undefined" && current instanceof ShadowRoot) {
          current = current.host;
        }
        var isScrollable = elementCouldBeScrolled(axis, current);
        if (isScrollable) {
          var _a = getScrollVariables(axis, current), scrollHeight = _a[1], clientHeight = _a[2];
          if (scrollHeight > clientHeight) {
            return true;
          }
        }
        current = current.parentNode;
      } while (current && current !== ownerDocument.body);
      return false;
    };
    exports.locationCouldBeScrolled = locationCouldBeScrolled;
    var getVScrollVariables = function(_a) {
      var scrollTop = _a.scrollTop, scrollHeight = _a.scrollHeight, clientHeight = _a.clientHeight;
      return [
        scrollTop,
        scrollHeight,
        clientHeight
      ];
    };
    var getHScrollVariables = function(_a) {
      var scrollLeft = _a.scrollLeft, scrollWidth = _a.scrollWidth, clientWidth = _a.clientWidth;
      return [
        scrollLeft,
        scrollWidth,
        clientWidth
      ];
    };
    var elementCouldBeScrolled = function(axis, node) {
      return axis === "v" ? elementCouldBeVScrolled(node) : elementCouldBeHScrolled(node);
    };
    var getScrollVariables = function(axis, node) {
      return axis === "v" ? getVScrollVariables(node) : getHScrollVariables(node);
    };
    var getDirectionFactor = function(axis, direction) {
      return axis === "h" && direction === "rtl" ? -1 : 1;
    };
    var handleScroll = function(axis, endTarget, event, sourceDelta, noOverscroll) {
      var directionFactor = getDirectionFactor(axis, window.getComputedStyle(endTarget).direction);
      var delta = directionFactor * sourceDelta;
      var target = event.target;
      var targetInLock = endTarget.contains(target);
      var shouldCancelScroll = false;
      var isDeltaPositive = delta > 0;
      var availableScroll = 0;
      var availableScrollTop = 0;
      do {
        var _a = getScrollVariables(axis, target), position = _a[0], scroll_1 = _a[1], capacity = _a[2];
        var elementScroll = scroll_1 - capacity - directionFactor * position;
        if (position || elementScroll) {
          if (elementCouldBeScrolled(axis, target)) {
            availableScroll += elementScroll;
            availableScrollTop += position;
          }
        }
        if (target instanceof ShadowRoot) {
          target = target.host;
        } else {
          target = target.parentNode;
        }
      } while (
        // portaled content
        !targetInLock && target !== document.body || // self content
        targetInLock && (endTarget.contains(target) || endTarget === target)
      );
      if (isDeltaPositive && (noOverscroll && Math.abs(availableScroll) < 1 || !noOverscroll && delta > availableScroll)) {
        shouldCancelScroll = true;
      } else if (!isDeltaPositive && (noOverscroll && Math.abs(availableScrollTop) < 1 || !noOverscroll && -delta > availableScrollTop)) {
        shouldCancelScroll = true;
      }
      return shouldCancelScroll;
    };
    exports.handleScroll = handleScroll;
  }
});

// node_modules/react-remove-scroll/dist/es5/SideEffect.js
var require_SideEffect = __commonJS({
  "node_modules/react-remove-scroll/dist/es5/SideEffect.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.RemoveScrollSideCar = exports.getDeltaXY = exports.getTouchXY = void 0;
    var tslib_1 = (init_tslib_es6(), __toCommonJS(tslib_es6_exports));
    var React10 = tslib_1.__importStar(require_react());
    var react_remove_scroll_bar_1 = require_es55();
    var react_style_singleton_1 = require_es54();
    var aggresiveCapture_1 = require_aggresiveCapture();
    var handleScroll_1 = require_handleScroll();
    var getTouchXY = function(event) {
      return "changedTouches" in event ? [event.changedTouches[0].clientX, event.changedTouches[0].clientY] : [0, 0];
    };
    exports.getTouchXY = getTouchXY;
    var getDeltaXY = function(event) {
      return [event.deltaX, event.deltaY];
    };
    exports.getDeltaXY = getDeltaXY;
    var extractRef = function(ref) {
      return ref && "current" in ref ? ref.current : ref;
    };
    var deltaCompare = function(x3, y4) {
      return x3[0] === y4[0] && x3[1] === y4[1];
    };
    var generateStyle = function(id3) {
      return "\n  .block-interactivity-".concat(id3, " {pointer-events: none;}\n  .allow-interactivity-").concat(id3, " {pointer-events: all;}\n");
    };
    var idCounter = 0;
    var lockStack = [];
    function RemoveScrollSideCar(props) {
      var shouldPreventQueue = React10.useRef([]);
      var touchStartRef = React10.useRef([0, 0]);
      var activeAxis = React10.useRef();
      var id3 = React10.useState(idCounter++)[0];
      var Style = React10.useState(react_style_singleton_1.styleSingleton)[0];
      var lastProps = React10.useRef(props);
      React10.useEffect(function() {
        lastProps.current = props;
      }, [props]);
      React10.useEffect(function() {
        if (props.inert) {
          document.body.classList.add("block-interactivity-".concat(id3));
          var allow_1 = tslib_1.__spreadArray([props.lockRef.current], (props.shards || []).map(extractRef), true).filter(Boolean);
          allow_1.forEach(function(el2) {
            return el2.classList.add("allow-interactivity-".concat(id3));
          });
          return function() {
            document.body.classList.remove("block-interactivity-".concat(id3));
            allow_1.forEach(function(el2) {
              return el2.classList.remove("allow-interactivity-".concat(id3));
            });
          };
        }
        return;
      }, [props.inert, props.lockRef.current, props.shards]);
      var shouldCancelEvent = React10.useCallback(function(event, parent) {
        if ("touches" in event && event.touches.length === 2 || event.type === "wheel" && event.ctrlKey) {
          return !lastProps.current.allowPinchZoom;
        }
        var touch = (0, exports.getTouchXY)(event);
        var touchStart = touchStartRef.current;
        var deltaX = "deltaX" in event ? event.deltaX : touchStart[0] - touch[0];
        var deltaY = "deltaY" in event ? event.deltaY : touchStart[1] - touch[1];
        var currentAxis;
        var target = event.target;
        var moveDirection = Math.abs(deltaX) > Math.abs(deltaY) ? "h" : "v";
        if ("touches" in event && moveDirection === "h" && target.type === "range") {
          return false;
        }
        var canBeScrolledInMainDirection = (0, handleScroll_1.locationCouldBeScrolled)(moveDirection, target);
        if (!canBeScrolledInMainDirection) {
          return true;
        }
        if (canBeScrolledInMainDirection) {
          currentAxis = moveDirection;
        } else {
          currentAxis = moveDirection === "v" ? "h" : "v";
          canBeScrolledInMainDirection = (0, handleScroll_1.locationCouldBeScrolled)(moveDirection, target);
        }
        if (!canBeScrolledInMainDirection) {
          return false;
        }
        if (!activeAxis.current && "changedTouches" in event && (deltaX || deltaY)) {
          activeAxis.current = currentAxis;
        }
        if (!currentAxis) {
          return true;
        }
        var cancelingAxis = activeAxis.current || currentAxis;
        return (0, handleScroll_1.handleScroll)(cancelingAxis, parent, event, cancelingAxis === "h" ? deltaX : deltaY, true);
      }, []);
      var shouldPrevent = React10.useCallback(function(_event) {
        var event = _event;
        if (!lockStack.length || lockStack[lockStack.length - 1] !== Style) {
          return;
        }
        var delta = "deltaY" in event ? (0, exports.getDeltaXY)(event) : (0, exports.getTouchXY)(event);
        var sourceEvent = shouldPreventQueue.current.filter(function(e14) {
          return e14.name === event.type && (e14.target === event.target || event.target === e14.shadowParent) && deltaCompare(e14.delta, delta);
        })[0];
        if (sourceEvent && sourceEvent.should) {
          if (event.cancelable) {
            event.preventDefault();
          }
          return;
        }
        if (!sourceEvent) {
          var shardNodes = (lastProps.current.shards || []).map(extractRef).filter(Boolean).filter(function(node) {
            return node.contains(event.target);
          });
          var shouldStop = shardNodes.length > 0 ? shouldCancelEvent(event, shardNodes[0]) : !lastProps.current.noIsolation;
          if (shouldStop) {
            if (event.cancelable) {
              event.preventDefault();
            }
          }
        }
      }, []);
      var shouldCancel = React10.useCallback(function(name2, delta, target, should) {
        var event = { name: name2, delta, target, should, shadowParent: getOutermostShadowParent(target) };
        shouldPreventQueue.current.push(event);
        setTimeout(function() {
          shouldPreventQueue.current = shouldPreventQueue.current.filter(function(e14) {
            return e14 !== event;
          });
        }, 1);
      }, []);
      var scrollTouchStart = React10.useCallback(function(event) {
        touchStartRef.current = (0, exports.getTouchXY)(event);
        activeAxis.current = void 0;
      }, []);
      var scrollWheel = React10.useCallback(function(event) {
        shouldCancel(event.type, (0, exports.getDeltaXY)(event), event.target, shouldCancelEvent(event, props.lockRef.current));
      }, []);
      var scrollTouchMove = React10.useCallback(function(event) {
        shouldCancel(event.type, (0, exports.getTouchXY)(event), event.target, shouldCancelEvent(event, props.lockRef.current));
      }, []);
      React10.useEffect(function() {
        lockStack.push(Style);
        props.setCallbacks({
          onScrollCapture: scrollWheel,
          onWheelCapture: scrollWheel,
          onTouchMoveCapture: scrollTouchMove
        });
        document.addEventListener("wheel", shouldPrevent, aggresiveCapture_1.nonPassive);
        document.addEventListener("touchmove", shouldPrevent, aggresiveCapture_1.nonPassive);
        document.addEventListener("touchstart", scrollTouchStart, aggresiveCapture_1.nonPassive);
        return function() {
          lockStack = lockStack.filter(function(inst) {
            return inst !== Style;
          });
          document.removeEventListener("wheel", shouldPrevent, aggresiveCapture_1.nonPassive);
          document.removeEventListener("touchmove", shouldPrevent, aggresiveCapture_1.nonPassive);
          document.removeEventListener("touchstart", scrollTouchStart, aggresiveCapture_1.nonPassive);
        };
      }, []);
      var removeScrollBar = props.removeScrollBar, inert = props.inert;
      return React10.createElement(
        React10.Fragment,
        null,
        inert ? React10.createElement(Style, { styles: generateStyle(id3) }) : null,
        removeScrollBar ? React10.createElement(react_remove_scroll_bar_1.RemoveScrollBar, { gapMode: props.gapMode }) : null
      );
    }
    exports.RemoveScrollSideCar = RemoveScrollSideCar;
    function getOutermostShadowParent(node) {
      var shadowParent = null;
      while (node !== null) {
        if (node instanceof ShadowRoot) {
          shadowParent = node.host;
          node = node.host;
        }
        node = node.parentNode;
      }
      return shadowParent;
    }
  }
});

// node_modules/react-remove-scroll/dist/es5/sidecar.js
var require_sidecar = __commonJS({
  "node_modules/react-remove-scroll/dist/es5/sidecar.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var use_sidecar_1 = require_es52();
    var SideEffect_1 = require_SideEffect();
    var medium_1 = require_medium2();
    exports.default = (0, use_sidecar_1.exportSidecar)(medium_1.effectCar, SideEffect_1.RemoveScrollSideCar);
  }
});

// node_modules/react-remove-scroll/dist/es5/Combination.js
var require_Combination = __commonJS({
  "node_modules/react-remove-scroll/dist/es5/Combination.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var tslib_1 = (init_tslib_es6(), __toCommonJS(tslib_es6_exports));
    var React10 = tslib_1.__importStar(require_react());
    var UI_1 = require_UI();
    var sidecar_1 = tslib_1.__importDefault(require_sidecar());
    var ReactRemoveScroll = React10.forwardRef(function(props, ref) {
      return React10.createElement(UI_1.RemoveScroll, tslib_1.__assign({}, props, { ref, sideCar: sidecar_1.default }));
    });
    ReactRemoveScroll.classNames = UI_1.RemoveScroll.classNames;
    exports.default = ReactRemoveScroll;
  }
});

// node_modules/react-remove-scroll/dist/es5/index.js
var require_es56 = __commonJS({
  "node_modules/react-remove-scroll/dist/es5/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.RemoveScroll = void 0;
    var tslib_1 = (init_tslib_es6(), __toCommonJS(tslib_es6_exports));
    var Combination_1 = tslib_1.__importDefault(require_Combination());
    exports.RemoveScroll = Combination_1.default;
  }
});

// node_modules/@mantine/core/esm/core/utils/keys/keys.mjs
function keys(object) {
  return Object.keys(object);
}
var init_keys = __esm({
  "node_modules/@mantine/core/esm/core/utils/keys/keys.mjs"() {
    "use client";
  }
});

// node_modules/@mantine/core/esm/core/utils/deep-merge/deep-merge.mjs
function isObject2(item2) {
  return item2 && typeof item2 === "object" && !Array.isArray(item2);
}
function deepMerge(target, source) {
  const result = { ...target };
  const _source = source;
  if (isObject2(target) && isObject2(source)) {
    Object.keys(source).forEach((key) => {
      if (isObject2(_source[key])) {
        if (!(key in target)) {
          result[key] = _source[key];
        } else {
          result[key] = deepMerge(result[key], _source[key]);
        }
      } else {
        result[key] = _source[key];
      }
    });
  }
  return result;
}
var init_deep_merge = __esm({
  "node_modules/@mantine/core/esm/core/utils/deep-merge/deep-merge.mjs"() {
  }
});

// node_modules/@mantine/core/esm/core/utils/camel-to-kebab-case/camel-to-kebab-case.mjs
function camelToKebabCase(value) {
  return value.replace(/[A-Z]/g, (letter) => `-${letter.toLowerCase()}`);
}
var init_camel_to_kebab_case = __esm({
  "node_modules/@mantine/core/esm/core/utils/camel-to-kebab-case/camel-to-kebab-case.mjs"() {
    "use client";
  }
});

// node_modules/@mantine/core/esm/core/utils/units-converters/px.mjs
function getTransformedScaledValue(value) {
  if (typeof value !== "string" || !value.includes("var(--mantine-scale)")) {
    return value;
  }
  return value.match(/^calc\((.*?)\)$/)?.[1].split("*")[0].trim();
}
function px(value) {
  const transformedValue = getTransformedScaledValue(value);
  if (typeof transformedValue === "number") {
    return transformedValue;
  }
  if (typeof transformedValue === "string") {
    if (transformedValue.includes("calc") || transformedValue.includes("var")) {
      return transformedValue;
    }
    if (transformedValue.includes("px")) {
      return Number(transformedValue.replace("px", ""));
    }
    if (transformedValue.includes("rem")) {
      return Number(transformedValue.replace("rem", "")) * 16;
    }
    if (transformedValue.includes("em")) {
      return Number(transformedValue.replace("em", "")) * 16;
    }
    return Number(transformedValue);
  }
  return NaN;
}
var init_px = __esm({
  "node_modules/@mantine/core/esm/core/utils/units-converters/px.mjs"() {
  }
});

// node_modules/@mantine/core/esm/core/utils/units-converters/rem.mjs
function scaleRem(remValue) {
  if (remValue === "0rem") {
    return "0rem";
  }
  return `calc(${remValue} * var(--mantine-scale))`;
}
function createConverter(units, { shouldScale = false } = {}) {
  function converter(value) {
    if (value === 0 || value === "0") {
      return `0${units}`;
    }
    if (typeof value === "number") {
      const val = `${value / 16}${units}`;
      return shouldScale ? scaleRem(val) : val;
    }
    if (typeof value === "string") {
      if (value === "") {
        return value;
      }
      if (value.startsWith("calc(") || value.startsWith("clamp(") || value.includes("rgba(")) {
        return value;
      }
      if (value.includes(",")) {
        return value.split(",").map((val) => converter(val)).join(",");
      }
      if (value.includes(" ")) {
        return value.split(" ").map((val) => converter(val)).join(" ");
      }
      if (value.includes(units)) {
        return shouldScale ? scaleRem(value) : value;
      }
      const replaced = value.replace("px", "");
      if (!Number.isNaN(Number(replaced))) {
        const val = `${Number(replaced) / 16}${units}`;
        return shouldScale ? scaleRem(val) : val;
      }
    }
    return value;
  }
  return converter;
}
var rem, em;
var init_rem = __esm({
  "node_modules/@mantine/core/esm/core/utils/units-converters/rem.mjs"() {
    rem = createConverter("rem", { shouldScale: true });
    em = createConverter("em");
  }
});

// node_modules/@mantine/core/esm/core/utils/filter-props/filter-props.mjs
function filterProps(props) {
  return Object.keys(props).reduce((acc, key) => {
    if (props[key] !== void 0) {
      acc[key] = props[key];
    }
    return acc;
  }, {});
}
var init_filter_props = __esm({
  "node_modules/@mantine/core/esm/core/utils/filter-props/filter-props.mjs"() {
    "use client";
  }
});

// node_modules/@mantine/core/esm/core/utils/is-number-like/is-number-like.mjs
function isNumberLike(value) {
  if (typeof value === "number") {
    return true;
  }
  if (typeof value === "string") {
    if (value.startsWith("calc(") || value.startsWith("var(") || value.includes(" ") && value.trim() !== "") {
      return true;
    }
    const cssUnitsRegex = /^[+-]?[0-9]+(\.[0-9]+)?(px|em|rem|ex|ch|lh|rlh|vw|vh|vmin|vmax|vb|vi|svw|svh|lvw|lvh|dvw|dvh|cm|mm|in|pt|pc|q|cqw|cqh|cqi|cqb|cqmin|cqmax|%)?$/;
    const values2 = value.trim().split(/\s+/);
    return values2.every((val) => cssUnitsRegex.test(val));
  }
  return false;
}
var init_is_number_like = __esm({
  "node_modules/@mantine/core/esm/core/utils/is-number-like/is-number-like.mjs"() {
    "use client";
  }
});

// node_modules/@mantine/core/esm/core/utils/is-element/is-element.mjs
function isElement(value) {
  if (Array.isArray(value) || value === null) {
    return false;
  }
  if (typeof value === "object") {
    if (value.type === import_react6.Fragment) {
      return false;
    }
    return true;
  }
  return false;
}
var import_react6;
var init_is_element = __esm({
  "node_modules/@mantine/core/esm/core/utils/is-element/is-element.mjs"() {
    "use client";
    import_react6 = __toESM(require_react(), 1);
  }
});

// node_modules/@mantine/core/esm/core/utils/create-safe-context/create-safe-context.mjs
function createSafeContext(errorMessage) {
  const Context = (0, import_react7.createContext)(null);
  const useSafeContext = () => {
    const ctx = (0, import_react7.useContext)(Context);
    if (ctx === null) {
      throw new Error(errorMessage);
    }
    return ctx;
  };
  const Provider = ({ children, value }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Context.Provider, { value, children });
  return [Provider, useSafeContext];
}
var import_jsx_runtime, import_react7;
var init_create_safe_context = __esm({
  "node_modules/@mantine/core/esm/core/utils/create-safe-context/create-safe-context.mjs"() {
    "use client";
    import_jsx_runtime = __toESM(require_jsx_runtime(), 1);
    import_react7 = __toESM(require_react(), 1);
  }
});

// node_modules/@mantine/core/esm/core/utils/create-optional-context/create-optional-context.mjs
function createOptionalContext(initialValue = null) {
  const Context = (0, import_react8.createContext)(initialValue);
  const useOptionalContext = () => (0, import_react8.useContext)(Context);
  const Provider = ({ children, value }) => /* @__PURE__ */ (0, import_jsx_runtime2.jsx)(Context.Provider, { value, children });
  return [Provider, useOptionalContext];
}
var import_jsx_runtime2, import_react8;
var init_create_optional_context = __esm({
  "node_modules/@mantine/core/esm/core/utils/create-optional-context/create-optional-context.mjs"() {
    "use client";
    import_jsx_runtime2 = __toESM(require_jsx_runtime(), 1);
    import_react8 = __toESM(require_react(), 1);
  }
});

// node_modules/@mantine/core/esm/core/utils/find-element-ancestor/find-element-ancestor.mjs
function findElementAncestor(element, selector) {
  let _element = element;
  while ((_element = _element.parentElement) && !_element.matches(selector)) {
  }
  return _element;
}
var init_find_element_ancestor = __esm({
  "node_modules/@mantine/core/esm/core/utils/find-element-ancestor/find-element-ancestor.mjs"() {
    "use client";
  }
});

// node_modules/@mantine/core/esm/core/utils/create-scoped-keydown-handler/create-scoped-keydown-handler.mjs
function getPreviousIndex(current, elements, loop) {
  for (let i13 = current - 1; i13 >= 0; i13 -= 1) {
    if (!elements[i13].disabled) {
      return i13;
    }
  }
  if (loop) {
    for (let i13 = elements.length - 1; i13 > -1; i13 -= 1) {
      if (!elements[i13].disabled) {
        return i13;
      }
    }
  }
  return current;
}
function getNextIndex(current, elements, loop) {
  for (let i13 = current + 1; i13 < elements.length; i13 += 1) {
    if (!elements[i13].disabled) {
      return i13;
    }
  }
  if (loop) {
    for (let i13 = 0; i13 < elements.length; i13 += 1) {
      if (!elements[i13].disabled) {
        return i13;
      }
    }
  }
  return current;
}
function onSameLevel(target, sibling, parentSelector) {
  return findElementAncestor(target, parentSelector) === findElementAncestor(sibling, parentSelector);
}
function createScopedKeydownHandler({
  parentSelector,
  siblingSelector,
  onKeyDown,
  loop = true,
  activateOnFocus = false,
  dir = "rtl",
  orientation
}) {
  return (event) => {
    onKeyDown?.(event);
    const elements = Array.from(
      findElementAncestor(event.currentTarget, parentSelector)?.querySelectorAll(
        siblingSelector
      ) || []
    ).filter((node) => onSameLevel(event.currentTarget, node, parentSelector));
    const current = elements.findIndex((el2) => event.currentTarget === el2);
    const _nextIndex = getNextIndex(current, elements, loop);
    const _previousIndex = getPreviousIndex(current, elements, loop);
    const nextIndex = dir === "rtl" ? _previousIndex : _nextIndex;
    const previousIndex = dir === "rtl" ? _nextIndex : _previousIndex;
    switch (event.key) {
      case "ArrowRight": {
        if (orientation === "horizontal") {
          event.stopPropagation();
          event.preventDefault();
          elements[nextIndex].focus();
          activateOnFocus && elements[nextIndex].click();
        }
        break;
      }
      case "ArrowLeft": {
        if (orientation === "horizontal") {
          event.stopPropagation();
          event.preventDefault();
          elements[previousIndex].focus();
          activateOnFocus && elements[previousIndex].click();
        }
        break;
      }
      case "ArrowUp": {
        if (orientation === "vertical") {
          event.stopPropagation();
          event.preventDefault();
          elements[_previousIndex].focus();
          activateOnFocus && elements[_previousIndex].click();
        }
        break;
      }
      case "ArrowDown": {
        if (orientation === "vertical") {
          event.stopPropagation();
          event.preventDefault();
          elements[_nextIndex].focus();
          activateOnFocus && elements[_nextIndex].click();
        }
        break;
      }
      case "Home": {
        event.stopPropagation();
        event.preventDefault();
        !elements[0].disabled && elements[0].focus();
        break;
      }
      case "End": {
        event.stopPropagation();
        event.preventDefault();
        const last = elements.length - 1;
        !elements[last].disabled && elements[last].focus();
        break;
      }
    }
  };
}
var init_create_scoped_keydown_handler = __esm({
  "node_modules/@mantine/core/esm/core/utils/create-scoped-keydown-handler/create-scoped-keydown-handler.mjs"() {
    "use client";
    init_find_element_ancestor();
  }
});

// node_modules/@mantine/core/esm/core/utils/get-default-z-index/get-default-z-index.mjs
function getDefaultZIndex(level) {
  return elevations[level];
}
var elevations;
var init_get_default_z_index = __esm({
  "node_modules/@mantine/core/esm/core/utils/get-default-z-index/get-default-z-index.mjs"() {
    "use client";
    elevations = {
      app: 100,
      modal: 200,
      popover: 300,
      overlay: 400,
      max: 9999
    };
  }
});

// node_modules/@mantine/core/esm/core/utils/noop/noop.mjs
var noop;
var init_noop = __esm({
  "node_modules/@mantine/core/esm/core/utils/noop/noop.mjs"() {
    "use client";
    noop = () => {
    };
  }
});

// node_modules/@mantine/core/esm/core/utils/close-on-escape/close-on-escape.mjs
function closeOnEscape(callback, options = { active: true }) {
  if (typeof callback !== "function" || !options.active) {
    return options.onKeyDown || noop;
  }
  return (event) => {
    if (event.key === "Escape") {
      callback(event);
      options.onTrigger?.();
    }
  };
}
var init_close_on_escape = __esm({
  "node_modules/@mantine/core/esm/core/utils/close-on-escape/close-on-escape.mjs"() {
    "use client";
    init_noop();
  }
});

// node_modules/@mantine/core/esm/core/utils/get-size/get-size.mjs
function getSize(size4, prefix = "size", convertToRem = true) {
  if (size4 === void 0) {
    return void 0;
  }
  return isNumberLike(size4) ? convertToRem ? rem(size4) : size4 : `var(--${prefix}-${size4})`;
}
function getSpacing(size4) {
  return getSize(size4, "mantine-spacing");
}
function getRadius(size4) {
  if (size4 === void 0) {
    return "var(--mantine-radius-default)";
  }
  return getSize(size4, "mantine-radius");
}
function getFontSize(size4) {
  return getSize(size4, "mantine-font-size");
}
function getLineHeight(size4) {
  return getSize(size4, "mantine-line-height", false);
}
function getShadow(size4) {
  if (!size4) {
    return void 0;
  }
  return getSize(size4, "mantine-shadow", false);
}
var init_get_size = __esm({
  "node_modules/@mantine/core/esm/core/utils/get-size/get-size.mjs"() {
    "use client";
    init_is_number_like();
    init_rem();
  }
});

// node_modules/@mantine/core/esm/core/utils/create-event-handler/create-event-handler.mjs
function createEventHandler(parentEventHandler, eventHandler) {
  return (event) => {
    parentEventHandler?.(event);
    eventHandler?.(event);
  };
}
var init_create_event_handler = __esm({
  "node_modules/@mantine/core/esm/core/utils/create-event-handler/create-event-handler.mjs"() {
    "use client";
  }
});

// node_modules/@mantine/core/esm/core/utils/get-context-item-index/get-context-item-index.mjs
function getContextItemIndex(elementSelector, parentSelector, node) {
  if (!node) {
    return null;
  }
  return Array.from(
    findElementAncestor(node, parentSelector)?.querySelectorAll(elementSelector) || []
  ).findIndex((element) => element === node);
}
var init_get_context_item_index = __esm({
  "node_modules/@mantine/core/esm/core/utils/get-context-item-index/get-context-item-index.mjs"() {
    "use client";
    init_find_element_ancestor();
  }
});

// node_modules/@mantine/core/esm/core/utils/use-hovered/use-hovered.mjs
function useHovered() {
  const [hovered, setHovered] = (0, import_react9.useState)(-1);
  const resetHovered = () => setHovered(-1);
  return [hovered, { setHovered, resetHovered }];
}
var import_react9;
var init_use_hovered = __esm({
  "node_modules/@mantine/core/esm/core/utils/use-hovered/use-hovered.mjs"() {
    "use client";
    import_react9 = __toESM(require_react(), 1);
  }
});

// node_modules/@mantine/hooks/esm/utils/clamp/clamp.mjs
function clamp(value, min2, max2) {
  if (min2 === void 0 && max2 === void 0) {
    return value;
  }
  if (min2 !== void 0 && max2 === void 0) {
    return Math.max(value, min2);
  }
  if (min2 === void 0 && max2 !== void 0) {
    return Math.min(value, max2);
  }
  return Math.min(Math.max(value, min2), max2);
}
var init_clamp = __esm({
  "node_modules/@mantine/hooks/esm/utils/clamp/clamp.mjs"() {
    "use client";
  }
});

// node_modules/@mantine/hooks/esm/utils/random-id/random-id.mjs
function randomId() {
  return `mantine-${Math.random().toString(36).slice(2, 11)}`;
}
var init_random_id = __esm({
  "node_modules/@mantine/hooks/esm/utils/random-id/random-id.mjs"() {
    "use client";
  }
});

// node_modules/@mantine/hooks/esm/use-click-outside/use-click-outside.mjs
function useClickOutside(handler2, events, nodes) {
  const ref = (0, import_react10.useRef)();
  (0, import_react10.useEffect)(() => {
    const listener = (event) => {
      const { target } = event ?? {};
      if (Array.isArray(nodes)) {
        const shouldIgnore = target?.hasAttribute("data-ignore-outside-clicks") || !document.body.contains(target) && target.tagName !== "HTML";
        const shouldTrigger = nodes.every((node) => !!node && !event.composedPath().includes(node));
        shouldTrigger && !shouldIgnore && handler2();
      } else if (ref.current && !ref.current.contains(target)) {
        handler2();
      }
    };
    (events || DEFAULT_EVENTS).forEach((fn) => document.addEventListener(fn, listener));
    return () => {
      (events || DEFAULT_EVENTS).forEach((fn) => document.removeEventListener(fn, listener));
    };
  }, [ref, handler2, nodes]);
  return ref;
}
var import_react10, DEFAULT_EVENTS;
var init_use_click_outside = __esm({
  "node_modules/@mantine/hooks/esm/use-click-outside/use-click-outside.mjs"() {
    "use client";
    import_react10 = __toESM(require_react(), 1);
    DEFAULT_EVENTS = ["mousedown", "touchstart"];
  }
});

// node_modules/@mantine/hooks/esm/use-media-query/use-media-query.mjs
function attachMediaListener(query2, callback) {
  try {
    query2.addEventListener("change", callback);
    return () => query2.removeEventListener("change", callback);
  } catch (e14) {
    query2.addListener(callback);
    return () => query2.removeListener(callback);
  }
}
function getInitialValue(query2, initialValue) {
  if (typeof window !== "undefined" && "matchMedia" in window) {
    return window.matchMedia(query2).matches;
  }
  return false;
}
function useMediaQuery(query2, initialValue, { getInitialValueInEffect } = {
  getInitialValueInEffect: true
}) {
  const [matches, setMatches] = (0, import_react11.useState)(
    getInitialValueInEffect ? initialValue : getInitialValue(query2)
  );
  const queryRef = (0, import_react11.useRef)();
  (0, import_react11.useEffect)(() => {
    if ("matchMedia" in window) {
      queryRef.current = window.matchMedia(query2);
      setMatches(queryRef.current.matches);
      return attachMediaListener(queryRef.current, (event) => setMatches(event.matches));
    }
    return void 0;
  }, [query2]);
  return matches;
}
var import_react11;
var init_use_media_query = __esm({
  "node_modules/@mantine/hooks/esm/use-media-query/use-media-query.mjs"() {
    "use client";
    import_react11 = __toESM(require_react(), 1);
  }
});

// node_modules/@mantine/hooks/esm/use-debounced-value/use-debounced-value.mjs
function useDebouncedValue(value, wait, options = { leading: false }) {
  const [_value, setValue] = (0, import_react12.useState)(value);
  const mountedRef = (0, import_react12.useRef)(false);
  const timeoutRef = (0, import_react12.useRef)(null);
  const cooldownRef = (0, import_react12.useRef)(false);
  const cancel = () => window.clearTimeout(timeoutRef.current);
  (0, import_react12.useEffect)(() => {
    if (mountedRef.current) {
      if (!cooldownRef.current && options.leading) {
        cooldownRef.current = true;
        setValue(value);
      } else {
        cancel();
        timeoutRef.current = window.setTimeout(() => {
          cooldownRef.current = false;
          setValue(value);
        }, wait);
      }
    }
  }, [value, options.leading, wait]);
  (0, import_react12.useEffect)(() => {
    mountedRef.current = true;
    return cancel;
  }, []);
  return [_value, cancel];
}
var import_react12;
var init_use_debounced_value = __esm({
  "node_modules/@mantine/hooks/esm/use-debounced-value/use-debounced-value.mjs"() {
    "use client";
    import_react12 = __toESM(require_react(), 1);
  }
});

// node_modules/@mantine/hooks/esm/use-isomorphic-effect/use-isomorphic-effect.mjs
var import_react13, useIsomorphicEffect;
var init_use_isomorphic_effect = __esm({
  "node_modules/@mantine/hooks/esm/use-isomorphic-effect/use-isomorphic-effect.mjs"() {
    "use client";
    import_react13 = __toESM(require_react(), 1);
    useIsomorphicEffect = typeof document !== "undefined" ? import_react13.useLayoutEffect : import_react13.useEffect;
  }
});

// node_modules/@mantine/hooks/esm/use-did-update/use-did-update.mjs
function useDidUpdate(fn, dependencies) {
  const mounted = (0, import_react14.useRef)(false);
  (0, import_react14.useEffect)(
    () => () => {
      mounted.current = false;
    },
    []
  );
  (0, import_react14.useEffect)(() => {
    if (mounted.current) {
      return fn();
    }
    mounted.current = true;
    return void 0;
  }, dependencies);
}
var import_react14;
var init_use_did_update = __esm({
  "node_modules/@mantine/hooks/esm/use-did-update/use-did-update.mjs"() {
    "use client";
    import_react14 = __toESM(require_react(), 1);
  }
});

// node_modules/@mantine/hooks/esm/use-focus-return/use-focus-return.mjs
function useFocusReturn({ opened, shouldReturnFocus = true }) {
  const lastActiveElement = (0, import_react15.useRef)();
  const returnFocus = () => {
    if (lastActiveElement.current && "focus" in lastActiveElement.current && typeof lastActiveElement.current.focus === "function") {
      lastActiveElement.current?.focus({ preventScroll: true });
    }
  };
  useDidUpdate(() => {
    let timeout = -1;
    const clearFocusTimeout = (event) => {
      if (event.key === "Tab") {
        window.clearTimeout(timeout);
      }
    };
    document.addEventListener("keydown", clearFocusTimeout);
    if (opened) {
      lastActiveElement.current = document.activeElement;
    } else if (shouldReturnFocus) {
      timeout = window.setTimeout(returnFocus, 10);
    }
    return () => {
      window.clearTimeout(timeout);
      document.removeEventListener("keydown", clearFocusTimeout);
    };
  }, [opened, shouldReturnFocus]);
  return returnFocus;
}
var import_react15;
var init_use_focus_return = __esm({
  "node_modules/@mantine/hooks/esm/use-focus-return/use-focus-return.mjs"() {
    "use client";
    import_react15 = __toESM(require_react(), 1);
    init_use_did_update();
  }
});

// node_modules/@mantine/hooks/esm/use-focus-trap/tabbable.mjs
function hidden(element) {
  if (false) {
    return false;
  }
  return element.style.display === "none";
}
function visible(element) {
  const isHidden = element.getAttribute("aria-hidden") || element.getAttribute("hidden") || element.getAttribute("type") === "hidden";
  if (isHidden) {
    return false;
  }
  let parentElement = element;
  while (parentElement) {
    if (parentElement === document.body || parentElement.nodeType === 11) {
      break;
    }
    if (hidden(parentElement)) {
      return false;
    }
    parentElement = parentElement.parentNode;
  }
  return true;
}
function getElementTabIndex(element) {
  let tabIndex = element.getAttribute("tabindex");
  if (tabIndex === null) {
    tabIndex = void 0;
  }
  return parseInt(tabIndex, 10);
}
function focusable(element) {
  const nodeName = element.nodeName.toLowerCase();
  const isTabIndexNotNaN = !Number.isNaN(getElementTabIndex(element));
  const res = (
    // @ts-expect-error function accepts any html element but if it is a button, it should not be disabled to trigger the condition
    TABBABLE_NODES.test(nodeName) && !element.disabled || (element instanceof HTMLAnchorElement ? element.href || isTabIndexNotNaN : isTabIndexNotNaN)
  );
  return res && visible(element);
}
function tabbable(element) {
  const tabIndex = getElementTabIndex(element);
  const isTabIndexNaN = Number.isNaN(tabIndex);
  return (isTabIndexNaN || tabIndex >= 0) && focusable(element);
}
function findTabbableDescendants(element) {
  return Array.from(element.querySelectorAll(FOCUS_SELECTOR)).filter(tabbable);
}
var TABBABLE_NODES, FOCUS_SELECTOR;
var init_tabbable = __esm({
  "node_modules/@mantine/hooks/esm/use-focus-trap/tabbable.mjs"() {
    "use client";
    TABBABLE_NODES = /input|select|textarea|button|object/;
    FOCUS_SELECTOR = "a, input, select, textarea, button, object, [tabindex]";
  }
});

// node_modules/@mantine/hooks/esm/use-focus-trap/scope-tab.mjs
function scopeTab(node, event) {
  const tabbable2 = findTabbableDescendants(node);
  if (!tabbable2.length) {
    event.preventDefault();
    return;
  }
  const finalTabbable = tabbable2[event.shiftKey ? 0 : tabbable2.length - 1];
  const root5 = node.getRootNode();
  let leavingFinalTabbable = finalTabbable === root5.activeElement || node === root5.activeElement;
  const activeElement = root5.activeElement;
  const activeElementIsRadio = activeElement.tagName === "INPUT" && activeElement.getAttribute("type") === "radio";
  if (activeElementIsRadio) {
    const activeRadioGroup = tabbable2.filter(
      (element) => element.getAttribute("type") === "radio" && element.getAttribute("name") === activeElement.getAttribute("name")
    );
    leavingFinalTabbable = activeRadioGroup.includes(finalTabbable);
  }
  if (!leavingFinalTabbable) {
    return;
  }
  event.preventDefault();
  const target = tabbable2[event.shiftKey ? tabbable2.length - 1 : 0];
  if (target) {
    target.focus();
  }
}
var init_scope_tab = __esm({
  "node_modules/@mantine/hooks/esm/use-focus-trap/scope-tab.mjs"() {
    "use client";
    init_tabbable();
  }
});

// node_modules/@mantine/hooks/esm/use-focus-trap/use-focus-trap.mjs
function useFocusTrap(active = true) {
  const ref = (0, import_react16.useRef)();
  const focusNode = (node) => {
    let focusElement = node.querySelector("[data-autofocus]");
    if (!focusElement) {
      const children = Array.from(node.querySelectorAll(FOCUS_SELECTOR));
      focusElement = children.find(tabbable) || children.find(focusable) || null;
      if (!focusElement && focusable(node)) {
        focusElement = node;
      }
    }
    if (focusElement) {
      focusElement.focus({ preventScroll: true });
    } else if (false) {
      console.warn(
        "[@mantine/hooks/use-focus-trap] Failed to find focusable element within provided node",
        node
      );
    }
  };
  const setRef = (0, import_react16.useCallback)(
    (node) => {
      if (!active) {
        return;
      }
      if (node === null) {
        return;
      }
      if (ref.current === node) {
        return;
      }
      if (node) {
        setTimeout(() => {
          if (node.getRootNode()) {
            focusNode(node);
          } else if (false) {
            console.warn("[@mantine/hooks/use-focus-trap] Ref node is not part of the dom", node);
          }
        });
        ref.current = node;
      } else {
        ref.current = null;
      }
    },
    [active]
  );
  (0, import_react16.useEffect)(() => {
    if (!active) {
      return void 0;
    }
    ref.current && setTimeout(() => focusNode(ref.current));
    const handleKeyDown = (event) => {
      if (event.key === "Tab" && ref.current) {
        scopeTab(ref.current, event);
      }
    };
    document.addEventListener("keydown", handleKeyDown);
    return () => document.removeEventListener("keydown", handleKeyDown);
  }, [active]);
  return setRef;
}
var import_react16;
var init_use_focus_trap = __esm({
  "node_modules/@mantine/hooks/esm/use-focus-trap/use-focus-trap.mjs"() {
    "use client";
    import_react16 = __toESM(require_react(), 1);
    init_scope_tab();
    init_tabbable();
  }
});

// node_modules/@mantine/hooks/esm/use-force-update/use-force-update.mjs
function useForceUpdate() {
  const [, update] = (0, import_react17.useReducer)(reducer, 0);
  return update;
}
var import_react17, reducer;
var init_use_force_update = __esm({
  "node_modules/@mantine/hooks/esm/use-force-update/use-force-update.mjs"() {
    "use client";
    import_react17 = __toESM(require_react(), 1);
    reducer = (value) => (value + 1) % 1e6;
  }
});

// node_modules/@mantine/hooks/esm/use-id/use-react-id.mjs
function useReactId() {
  const id3 = __useId();
  return id3 ? `mantine-${id3.replace(/:/g, "")}` : "";
}
var import_react18, __useId;
var init_use_react_id = __esm({
  "node_modules/@mantine/hooks/esm/use-id/use-react-id.mjs"() {
    "use client";
    import_react18 = __toESM(require_react(), 1);
    __useId = import_react18.default["useId".toString()] || (() => void 0);
  }
});

// node_modules/@mantine/hooks/esm/use-id/use-id.mjs
function useId(staticId) {
  const reactId = useReactId();
  const [uuid, setUuid] = (0, import_react19.useState)(reactId);
  useIsomorphicEffect(() => {
    setUuid(randomId());
  }, []);
  if (typeof staticId === "string") {
    return staticId;
  }
  if (typeof window === "undefined") {
    return reactId;
  }
  return uuid;
}
var import_react19;
var init_use_id = __esm({
  "node_modules/@mantine/hooks/esm/use-id/use-id.mjs"() {
    "use client";
    import_react19 = __toESM(require_react(), 1);
    init_use_isomorphic_effect();
    init_random_id();
    init_use_react_id();
  }
});

// node_modules/@mantine/hooks/esm/use-window-event/use-window-event.mjs
function useWindowEvent(type2, listener, options) {
  (0, import_react20.useEffect)(() => {
    window.addEventListener(type2, listener, options);
    return () => window.removeEventListener(type2, listener, options);
  }, [type2, listener]);
}
var import_react20;
var init_use_window_event = __esm({
  "node_modules/@mantine/hooks/esm/use-window-event/use-window-event.mjs"() {
    "use client";
    import_react20 = __toESM(require_react(), 1);
  }
});

// node_modules/@mantine/hooks/esm/use-merged-ref/use-merged-ref.mjs
function assignRef(ref, value) {
  if (typeof ref === "function") {
    ref(value);
  } else if (typeof ref === "object" && ref !== null && "current" in ref) {
    ref.current = value;
  }
}
function mergeRefs(...refs) {
  return (node) => {
    refs.forEach((ref) => assignRef(ref, node));
  };
}
function useMergedRef(...refs) {
  return (0, import_react21.useCallback)(mergeRefs(...refs), refs);
}
var import_react21;
var init_use_merged_ref = __esm({
  "node_modules/@mantine/hooks/esm/use-merged-ref/use-merged-ref.mjs"() {
    "use client";
    import_react21 = __toESM(require_react(), 1);
  }
});

// node_modules/@mantine/hooks/esm/use-move/use-move.mjs
function useMove(onChange, handlers, dir = "ltr") {
  const ref = (0, import_react22.useRef)(null);
  const mounted = (0, import_react22.useRef)(false);
  const isSliding = (0, import_react22.useRef)(false);
  const frame2 = (0, import_react22.useRef)(0);
  const [active, setActive] = (0, import_react22.useState)(false);
  (0, import_react22.useEffect)(() => {
    mounted.current = true;
  }, []);
  (0, import_react22.useEffect)(() => {
    const onScrub = ({ x: x3, y: y4 }) => {
      cancelAnimationFrame(frame2.current);
      frame2.current = requestAnimationFrame(() => {
        if (mounted.current && ref.current) {
          ref.current.style.userSelect = "none";
          const rect = ref.current.getBoundingClientRect();
          if (rect.width && rect.height) {
            const _x = clamp((x3 - rect.left) / rect.width, 0, 1);
            onChange({
              x: dir === "ltr" ? _x : 1 - _x,
              y: clamp((y4 - rect.top) / rect.height, 0, 1)
            });
          }
        }
      });
    };
    const bindEvents = () => {
      document.addEventListener("mousemove", onMouseMove);
      document.addEventListener("mouseup", stopScrubbing);
      document.addEventListener("touchmove", onTouchMove);
      document.addEventListener("touchend", stopScrubbing);
    };
    const unbindEvents = () => {
      document.removeEventListener("mousemove", onMouseMove);
      document.removeEventListener("mouseup", stopScrubbing);
      document.removeEventListener("touchmove", onTouchMove);
      document.removeEventListener("touchend", stopScrubbing);
    };
    const startScrubbing = () => {
      if (!isSliding.current && mounted.current) {
        isSliding.current = true;
        typeof handlers?.onScrubStart === "function" && handlers.onScrubStart();
        setActive(true);
        bindEvents();
      }
    };
    const stopScrubbing = () => {
      if (isSliding.current && mounted.current) {
        isSliding.current = false;
        setActive(false);
        unbindEvents();
        setTimeout(() => {
          typeof handlers?.onScrubEnd === "function" && handlers.onScrubEnd();
        }, 0);
      }
    };
    const onMouseDown = (event) => {
      startScrubbing();
      event.preventDefault();
      onMouseMove(event);
    };
    const onMouseMove = (event) => onScrub({ x: event.clientX, y: event.clientY });
    const onTouchStart = (event) => {
      if (event.cancelable) {
        event.preventDefault();
      }
      startScrubbing();
      onTouchMove(event);
    };
    const onTouchMove = (event) => {
      if (event.cancelable) {
        event.preventDefault();
      }
      onScrub({ x: event.changedTouches[0].clientX, y: event.changedTouches[0].clientY });
    };
    ref.current?.addEventListener("mousedown", onMouseDown);
    ref.current?.addEventListener("touchstart", onTouchStart, { passive: false });
    return () => {
      if (ref.current) {
        ref.current.removeEventListener("mousedown", onMouseDown);
        ref.current.removeEventListener("touchstart", onTouchStart);
      }
    };
  }, [dir, onChange]);
  return { ref, active };
}
var import_react22;
var init_use_move = __esm({
  "node_modules/@mantine/hooks/esm/use-move/use-move.mjs"() {
    "use client";
    import_react22 = __toESM(require_react(), 1);
    init_clamp();
  }
});

// node_modules/@mantine/hooks/esm/use-uncontrolled/use-uncontrolled.mjs
function useUncontrolled({
  value,
  defaultValue: defaultValue2,
  finalValue,
  onChange = () => {
  }
}) {
  const [uncontrolledValue, setUncontrolledValue] = (0, import_react23.useState)(
    defaultValue2 !== void 0 ? defaultValue2 : finalValue
  );
  const handleUncontrolledChange = (val, ...payload) => {
    setUncontrolledValue(val);
    onChange?.(val, ...payload);
  };
  if (value !== void 0) {
    return [value, onChange, true];
  }
  return [uncontrolledValue, handleUncontrolledChange, false];
}
var import_react23;
var init_use_uncontrolled = __esm({
  "node_modules/@mantine/hooks/esm/use-uncontrolled/use-uncontrolled.mjs"() {
    "use client";
    import_react23 = __toESM(require_react(), 1);
  }
});

// node_modules/@mantine/hooks/esm/use-pagination/use-pagination.mjs
function range(start, end) {
  const length = end - start + 1;
  return Array.from({ length }, (_2, index3) => index3 + start);
}
function usePagination({
  total,
  siblings = 1,
  boundaries = 1,
  page,
  initialPage = 1,
  onChange
}) {
  const _total = Math.max(Math.trunc(total), 0);
  const [activePage, setActivePage] = useUncontrolled({
    value: page,
    onChange,
    defaultValue: initialPage,
    finalValue: initialPage
  });
  const setPage = (pageNumber) => {
    if (pageNumber <= 0) {
      setActivePage(1);
    } else if (pageNumber > _total) {
      setActivePage(_total);
    } else {
      setActivePage(pageNumber);
    }
  };
  const next2 = () => setPage(activePage + 1);
  const previous2 = () => setPage(activePage - 1);
  const first = () => setPage(1);
  const last = () => setPage(_total);
  const paginationRange = (0, import_react24.useMemo)(() => {
    const totalPageNumbers = siblings * 2 + 3 + boundaries * 2;
    if (totalPageNumbers >= _total) {
      return range(1, _total);
    }
    const leftSiblingIndex = Math.max(activePage - siblings, boundaries);
    const rightSiblingIndex = Math.min(activePage + siblings, _total - boundaries);
    const shouldShowLeftDots = leftSiblingIndex > boundaries + 2;
    const shouldShowRightDots = rightSiblingIndex < _total - (boundaries + 1);
    if (!shouldShowLeftDots && shouldShowRightDots) {
      const leftItemCount = siblings * 2 + boundaries + 2;
      return [...range(1, leftItemCount), DOTS, ...range(_total - (boundaries - 1), _total)];
    }
    if (shouldShowLeftDots && !shouldShowRightDots) {
      const rightItemCount = boundaries + 1 + 2 * siblings;
      return [...range(1, boundaries), DOTS, ...range(_total - rightItemCount, _total)];
    }
    return [
      ...range(1, boundaries),
      DOTS,
      ...range(leftSiblingIndex, rightSiblingIndex),
      DOTS,
      ...range(_total - boundaries + 1, _total)
    ];
  }, [_total, siblings, activePage]);
  return {
    range: paginationRange,
    active: activePage,
    setPage,
    next: next2,
    previous: previous2,
    first,
    last
  };
}
var import_react24, DOTS;
var init_use_pagination = __esm({
  "node_modules/@mantine/hooks/esm/use-pagination/use-pagination.mjs"() {
    "use client";
    import_react24 = __toESM(require_react(), 1);
    init_use_uncontrolled();
    DOTS = "dots";
  }
});

// node_modules/@mantine/hooks/esm/use-reduced-motion/use-reduced-motion.mjs
function useReducedMotion(initialValue, options) {
  return useMediaQuery("(prefers-reduced-motion: reduce)", initialValue, options);
}
var init_use_reduced_motion = __esm({
  "node_modules/@mantine/hooks/esm/use-reduced-motion/use-reduced-motion.mjs"() {
    "use client";
    init_use_media_query();
  }
});

// node_modules/@mantine/hooks/esm/use-disclosure/use-disclosure.mjs
function useDisclosure(initialState = false, callbacks) {
  const { onOpen, onClose } = callbacks || {};
  const [opened, setOpened] = (0, import_react25.useState)(initialState);
  const open = (0, import_react25.useCallback)(() => {
    setOpened((isOpened) => {
      if (!isOpened) {
        onOpen?.();
        return true;
      }
      return isOpened;
    });
  }, [onOpen]);
  const close = (0, import_react25.useCallback)(() => {
    setOpened((isOpened) => {
      if (isOpened) {
        onClose?.();
        return false;
      }
      return isOpened;
    });
  }, [onClose]);
  const toggle = (0, import_react25.useCallback)(() => {
    opened ? close() : open();
  }, [close, open, opened]);
  return [opened, { open, close, toggle }];
}
var import_react25;
var init_use_disclosure = __esm({
  "node_modules/@mantine/hooks/esm/use-disclosure/use-disclosure.mjs"() {
    "use client";
    import_react25 = __toESM(require_react(), 1);
  }
});

// node_modules/@mantine/hooks/esm/index.mjs
var init_esm = __esm({
  "node_modules/@mantine/hooks/esm/index.mjs"() {
    init_clamp();
    init_random_id();
    init_use_click_outside();
    init_use_debounced_value();
    init_use_focus_return();
    init_use_did_update();
    init_use_focus_trap();
    init_use_force_update();
    init_use_id();
    init_use_isomorphic_effect();
    init_use_media_query();
    init_use_merged_ref();
    init_use_move();
    init_use_pagination();
    init_use_reduced_motion();
    init_use_uncontrolled();
    init_use_window_event();
    init_use_disclosure();
  }
});

// node_modules/@mantine/core/esm/core/utils/find-closest-number/find-closest-number.mjs
function findClosestNumber(value, numbers) {
  if (numbers.length === 0) {
    return value;
  }
  return numbers.reduce(
    (prev2, curr) => Math.abs(curr - value) < Math.abs(prev2 - value) ? curr : prev2
  );
}
var init_find_closest_number = __esm({
  "node_modules/@mantine/core/esm/core/utils/find-closest-number/find-closest-number.mjs"() {
    "use client";
  }
});

// node_modules/@mantine/core/esm/core/utils/get-ref-prop/get-ref-prop.mjs
function getRefProp(element) {
  const version = import_react26.default.version;
  if (typeof import_react26.default.version !== "string") {
    return element?.ref;
  }
  if (version.startsWith("18.")) {
    return element?.ref;
  }
  return element?.props?.ref;
}
var import_react26;
var init_get_ref_prop = __esm({
  "node_modules/@mantine/core/esm/core/utils/get-ref-prop/get-ref-prop.mjs"() {
    "use client";
    import_react26 = __toESM(require_react(), 1);
  }
});

// node_modules/@mantine/core/esm/core/styles-api/create-vars-resolver/create-vars-resolver.mjs
function createVarsResolver(resolver) {
  return resolver;
}
var init_create_vars_resolver = __esm({
  "node_modules/@mantine/core/esm/core/styles-api/create-vars-resolver/create-vars-resolver.mjs"() {
    "use client";
  }
});

// node_modules/clsx/dist/clsx.mjs
function r(e14) {
  var t20, f15, n13 = "";
  if ("string" == typeof e14 || "number" == typeof e14) n13 += e14;
  else if ("object" == typeof e14) if (Array.isArray(e14)) {
    var o8 = e14.length;
    for (t20 = 0; t20 < o8; t20++) e14[t20] && (f15 = r(e14[t20])) && (n13 && (n13 += " "), n13 += f15);
  } else for (f15 in e14) e14[f15] && (n13 && (n13 += " "), n13 += f15);
  return n13;
}
function clsx() {
  for (var e14, t20, f15 = 0, n13 = "", o8 = arguments.length; f15 < o8; f15++) (e14 = arguments[f15]) && (t20 = r(e14)) && (n13 && (n13 += " "), n13 += t20);
  return n13;
}
var clsx_default;
var init_clsx = __esm({
  "node_modules/clsx/dist/clsx.mjs"() {
    clsx_default = clsx;
  }
});

// node_modules/@mantine/core/esm/core/styles-api/use-styles/get-class-name/resolve-class-names/resolve-class-names.mjs
function mergeClassNames(objects) {
  const merged = {};
  objects.forEach((obj) => {
    Object.entries(obj).forEach(([key, value]) => {
      if (merged[key]) {
        merged[key] = clsx_default(merged[key], value);
      } else {
        merged[key] = value;
      }
    });
  });
  return merged;
}
function resolveClassNames({ theme: theme2, classNames, props, stylesCtx }) {
  const arrayClassNames = Array.isArray(classNames) ? classNames : [classNames];
  const resolvedClassNames = arrayClassNames.map(
    (item2) => typeof item2 === "function" ? item2(theme2, props, stylesCtx) : item2 || EMPTY_CLASS_NAMES
  );
  return mergeClassNames(resolvedClassNames);
}
var EMPTY_CLASS_NAMES;
var init_resolve_class_names = __esm({
  "node_modules/@mantine/core/esm/core/styles-api/use-styles/get-class-name/resolve-class-names/resolve-class-names.mjs"() {
    "use client";
    init_clsx();
    EMPTY_CLASS_NAMES = {};
  }
});

// node_modules/@mantine/core/esm/core/styles-api/use-styles/get-style/resolve-styles/resolve-styles.mjs
function resolveStyles({ theme: theme2, styles, props, stylesCtx }) {
  const arrayStyles = Array.isArray(styles) ? styles : [styles];
  return arrayStyles.reduce((acc, style) => {
    if (typeof style === "function") {
      return { ...acc, ...style(theme2, props, stylesCtx) };
    }
    return { ...acc, ...style };
  }, {});
}
var init_resolve_styles = __esm({
  "node_modules/@mantine/core/esm/core/styles-api/use-styles/get-style/resolve-styles/resolve-styles.mjs"() {
    "use client";
  }
});

// node_modules/@mantine/core/esm/core/MantineProvider/Mantine.context.mjs
function useMantineContext() {
  const ctx = (0, import_react27.useContext)(MantineContext);
  if (!ctx) {
    throw new Error("[@mantine/core] MantineProvider was not found in tree");
  }
  return ctx;
}
function useMantineCssVariablesResolver() {
  return useMantineContext().cssVariablesResolver;
}
function useMantineClassNamesPrefix() {
  return useMantineContext().classNamesPrefix;
}
function useMantineStyleNonce() {
  return useMantineContext().getStyleNonce;
}
function useMantineWithStaticClasses() {
  return useMantineContext().withStaticClasses;
}
function useMantineIsHeadless() {
  return useMantineContext().headless;
}
function useMantineSxTransform() {
  return useMantineContext().stylesTransform?.sx;
}
function useMantineStylesTransform() {
  return useMantineContext().stylesTransform?.styles;
}
var import_react27, MantineContext;
var init_Mantine_context = __esm({
  "node_modules/@mantine/core/esm/core/MantineProvider/Mantine.context.mjs"() {
    "use client";
    import_react27 = __toESM(require_react(), 1);
    MantineContext = (0, import_react27.createContext)(null);
  }
});

// node_modules/@mantine/core/esm/core/MantineProvider/color-functions/to-rgba/to-rgba.mjs
function isHexColor(hex2) {
  const HEX_REGEXP = /^#?([0-9A-F]{3}){1,2}([0-9A-F]{2})?$/i;
  return HEX_REGEXP.test(hex2);
}
function hexToRgba(color2) {
  let hexString = color2.replace("#", "");
  if (hexString.length === 3) {
    const shorthandHex = hexString.split("");
    hexString = [
      shorthandHex[0],
      shorthandHex[0],
      shorthandHex[1],
      shorthandHex[1],
      shorthandHex[2],
      shorthandHex[2]
    ].join("");
  }
  if (hexString.length === 8) {
    const alpha3 = parseInt(hexString.slice(6, 8), 16) / 255;
    return {
      r: parseInt(hexString.slice(0, 2), 16),
      g: parseInt(hexString.slice(2, 4), 16),
      b: parseInt(hexString.slice(4, 6), 16),
      a: alpha3
    };
  }
  const parsed = parseInt(hexString, 16);
  const r7 = parsed >> 16 & 255;
  const g7 = parsed >> 8 & 255;
  const b4 = parsed & 255;
  return {
    r: r7,
    g: g7,
    b: b4,
    a: 1
  };
}
function rgbStringToRgba(color2) {
  const [r7, g7, b4, a18] = color2.replace(/[^0-9,./]/g, "").split(/[/,]/).map(Number);
  return { r: r7, g: g7, b: b4, a: a18 || 1 };
}
function hslStringToRgba(hslaString) {
  const hslaRegex = /^hsla?\(\s*(\d+)\s*,\s*(\d+%)\s*,\s*(\d+%)\s*(,\s*(0?\.\d+|\d+(\.\d+)?))?\s*\)$/i;
  const matches = hslaString.match(hslaRegex);
  if (!matches) {
    return {
      r: 0,
      g: 0,
      b: 0,
      a: 1
    };
  }
  const h6 = parseInt(matches[1], 10);
  const s36 = parseInt(matches[2], 10) / 100;
  const l12 = parseInt(matches[3], 10) / 100;
  const a18 = matches[5] ? parseFloat(matches[5]) : void 0;
  const chroma = (1 - Math.abs(2 * l12 - 1)) * s36;
  const huePrime = h6 / 60;
  const x3 = chroma * (1 - Math.abs(huePrime % 2 - 1));
  const m12 = l12 - chroma / 2;
  let r7;
  let g7;
  let b4;
  if (huePrime >= 0 && huePrime < 1) {
    r7 = chroma;
    g7 = x3;
    b4 = 0;
  } else if (huePrime >= 1 && huePrime < 2) {
    r7 = x3;
    g7 = chroma;
    b4 = 0;
  } else if (huePrime >= 2 && huePrime < 3) {
    r7 = 0;
    g7 = chroma;
    b4 = x3;
  } else if (huePrime >= 3 && huePrime < 4) {
    r7 = 0;
    g7 = x3;
    b4 = chroma;
  } else if (huePrime >= 4 && huePrime < 5) {
    r7 = x3;
    g7 = 0;
    b4 = chroma;
  } else {
    r7 = chroma;
    g7 = 0;
    b4 = x3;
  }
  return {
    r: Math.round((r7 + m12) * 255),
    g: Math.round((g7 + m12) * 255),
    b: Math.round((b4 + m12) * 255),
    a: a18 || 1
  };
}
function toRgba(color2) {
  if (isHexColor(color2)) {
    return hexToRgba(color2);
  }
  if (color2.startsWith("rgb")) {
    return rgbStringToRgba(color2);
  }
  if (color2.startsWith("hsl")) {
    return hslStringToRgba(color2);
  }
  return {
    r: 0,
    g: 0,
    b: 0,
    a: 1
  };
}
var init_to_rgba = __esm({
  "node_modules/@mantine/core/esm/core/MantineProvider/color-functions/to-rgba/to-rgba.mjs"() {
  }
});

// node_modules/@mantine/core/esm/core/MantineProvider/color-functions/darken/darken.mjs
function darken(color2, alpha3) {
  if (color2.startsWith("var(")) {
    return `color-mix(in srgb, ${color2}, black ${alpha3 * 100}%)`;
  }
  const { r: r7, g: g7, b: b4, a: a18 } = toRgba(color2);
  const f15 = 1 - alpha3;
  const dark = (input2) => Math.round(input2 * f15);
  return `rgba(${dark(r7)}, ${dark(g7)}, ${dark(b4)}, ${a18})`;
}
var init_darken = __esm({
  "node_modules/@mantine/core/esm/core/MantineProvider/color-functions/darken/darken.mjs"() {
    init_to_rgba();
  }
});

// node_modules/@mantine/core/esm/core/MantineProvider/color-functions/get-primary-shade/get-primary-shade.mjs
function getPrimaryShade(theme2, colorScheme) {
  if (typeof theme2.primaryShade === "number") {
    return theme2.primaryShade;
  }
  if (colorScheme === "dark") {
    return theme2.primaryShade.dark;
  }
  return theme2.primaryShade.light;
}
var init_get_primary_shade = __esm({
  "node_modules/@mantine/core/esm/core/MantineProvider/color-functions/get-primary-shade/get-primary-shade.mjs"() {
    "use client";
  }
});

// node_modules/@mantine/core/esm/core/MantineProvider/color-functions/luminance/luminance.mjs
function gammaCorrect(c15) {
  return c15 <= 0.03928 ? c15 / 12.92 : ((c15 + 0.055) / 1.055) ** 2.4;
}
function getLightnessFromOklch(oklchColor) {
  const match = oklchColor.match(/oklch\((.*?)%\s/);
  return match ? parseFloat(match[1]) : null;
}
function luminance(color2) {
  if (color2.startsWith("oklch(")) {
    return (getLightnessFromOklch(color2) || 0) / 100;
  }
  const { r: r7, g: g7, b: b4 } = toRgba(color2);
  const sR = r7 / 255;
  const sG = g7 / 255;
  const sB = b4 / 255;
  const rLinear = gammaCorrect(sR);
  const gLinear = gammaCorrect(sG);
  const bLinear = gammaCorrect(sB);
  return 0.2126 * rLinear + 0.7152 * gLinear + 0.0722 * bLinear;
}
function isLightColor(color2, luminanceThreshold = 0.179) {
  if (color2.startsWith("var(")) {
    return false;
  }
  return luminance(color2) > luminanceThreshold;
}
var init_luminance = __esm({
  "node_modules/@mantine/core/esm/core/MantineProvider/color-functions/luminance/luminance.mjs"() {
    "use client";
    init_to_rgba();
  }
});

// node_modules/@mantine/core/esm/core/MantineProvider/color-functions/parse-theme-color/parse-theme-color.mjs
function parseThemeColor({
  color: color2,
  theme: theme2,
  colorScheme
}) {
  if (typeof color2 !== "string") {
    throw new Error(
      `[@mantine/core] Failed to parse color. Expected color to be a string, instead got ${typeof color2}`
    );
  }
  if (color2 === "bright") {
    return {
      color: color2,
      value: colorScheme === "dark" ? theme2.white : theme2.black,
      shade: void 0,
      isThemeColor: false,
      isLight: isLightColor(
        colorScheme === "dark" ? theme2.white : theme2.black,
        theme2.luminanceThreshold
      ),
      variable: "--mantine-color-bright"
    };
  }
  if (color2 === "dimmed") {
    return {
      color: color2,
      value: colorScheme === "dark" ? theme2.colors.dark[2] : theme2.colors.gray[7],
      shade: void 0,
      isThemeColor: false,
      isLight: isLightColor(
        colorScheme === "dark" ? theme2.colors.dark[2] : theme2.colors.gray[6],
        theme2.luminanceThreshold
      ),
      variable: "--mantine-color-dimmed"
    };
  }
  if (color2 === "white" || color2 === "black") {
    return {
      color: color2,
      value: color2 === "white" ? theme2.white : theme2.black,
      shade: void 0,
      isThemeColor: false,
      isLight: isLightColor(
        color2 === "white" ? theme2.white : theme2.black,
        theme2.luminanceThreshold
      ),
      variable: `--mantine-color-${color2}`
    };
  }
  const [_color, shade] = color2.split(".");
  const colorShade = shade ? Number(shade) : void 0;
  const isThemeColor = _color in theme2.colors;
  if (isThemeColor) {
    const colorValue = colorShade !== void 0 ? theme2.colors[_color][colorShade] : theme2.colors[_color][getPrimaryShade(theme2, colorScheme || "light")];
    return {
      color: _color,
      value: colorValue,
      shade: colorShade,
      isThemeColor,
      isLight: isLightColor(colorValue, theme2.luminanceThreshold),
      variable: shade ? `--mantine-color-${_color}-${colorShade}` : `--mantine-color-${_color}-filled`
    };
  }
  return {
    color: color2,
    value: color2,
    isThemeColor,
    isLight: isLightColor(color2, theme2.luminanceThreshold),
    shade: colorShade,
    variable: void 0
  };
}
var init_parse_theme_color = __esm({
  "node_modules/@mantine/core/esm/core/MantineProvider/color-functions/parse-theme-color/parse-theme-color.mjs"() {
    "use client";
    init_get_primary_shade();
    init_luminance();
  }
});

// node_modules/@mantine/core/esm/core/MantineProvider/color-functions/get-theme-color/get-theme-color.mjs
function getThemeColor(color2, theme2) {
  const parsed = parseThemeColor({ color: color2 || theme2.primaryColor, theme: theme2 });
  return parsed.variable ? `var(${parsed.variable})` : color2;
}
var init_get_theme_color = __esm({
  "node_modules/@mantine/core/esm/core/MantineProvider/color-functions/get-theme-color/get-theme-color.mjs"() {
    "use client";
    init_parse_theme_color();
  }
});

// node_modules/@mantine/core/esm/core/MantineProvider/color-functions/get-gradient/get-gradient.mjs
function getGradient(gradient, theme2) {
  const merged = {
    from: gradient?.from || theme2.defaultGradient.from,
    to: gradient?.to || theme2.defaultGradient.to,
    deg: gradient?.deg || theme2.defaultGradient.deg || 0
  };
  const fromColor = getThemeColor(merged.from, theme2);
  const toColor = getThemeColor(merged.to, theme2);
  return `linear-gradient(${merged.deg}deg, ${fromColor} 0%, ${toColor} 100%)`;
}
var init_get_gradient = __esm({
  "node_modules/@mantine/core/esm/core/MantineProvider/color-functions/get-gradient/get-gradient.mjs"() {
    "use client";
    init_get_theme_color();
  }
});

// node_modules/@mantine/core/esm/core/MantineProvider/color-functions/rgba/rgba.mjs
function rgba(color2, alpha22) {
  if (typeof color2 !== "string" || alpha22 > 1 || alpha22 < 0) {
    return "rgba(0, 0, 0, 1)";
  }
  if (color2.startsWith("var(")) {
    const mixPercentage = (1 - alpha22) * 100;
    return `color-mix(in srgb, ${color2}, transparent ${mixPercentage}%)`;
  }
  if (color2.startsWith("oklch")) {
    if (color2.includes("/")) {
      return color2.replace(/\/\s*[\d.]+\s*\)/, `/ ${alpha22})`);
    }
    return color2.replace(")", ` / ${alpha22})`);
  }
  const { r: r7, g: g7, b: b4 } = toRgba(color2);
  return `rgba(${r7}, ${g7}, ${b4}, ${alpha22})`;
}
var alpha;
var init_rgba = __esm({
  "node_modules/@mantine/core/esm/core/MantineProvider/color-functions/rgba/rgba.mjs"() {
    init_to_rgba();
    alpha = rgba;
  }
});

// node_modules/@mantine/core/esm/core/MantineProvider/color-functions/default-variant-colors-resolver/default-variant-colors-resolver.mjs
var import_react28, import_jsx_runtime3, defaultVariantColorsResolver;
var init_default_variant_colors_resolver = __esm({
  "node_modules/@mantine/core/esm/core/MantineProvider/color-functions/default-variant-colors-resolver/default-variant-colors-resolver.mjs"() {
    "use client";
    init_rem();
    import_react28 = __toESM(require_react(), 1);
    import_jsx_runtime3 = __toESM(require_jsx_runtime(), 1);
    init_darken();
    init_get_gradient();
    init_parse_theme_color();
    init_rgba();
    defaultVariantColorsResolver = ({
      color: color2,
      theme: theme2,
      variant,
      gradient,
      autoContrast
    }) => {
      const parsed = parseThemeColor({ color: color2, theme: theme2 });
      const _autoContrast = typeof autoContrast === "boolean" ? autoContrast : theme2.autoContrast;
      if (variant === "filled") {
        const textColor = _autoContrast ? parsed.isLight ? "var(--mantine-color-black)" : "var(--mantine-color-white)" : "var(--mantine-color-white)";
        if (parsed.isThemeColor) {
          if (parsed.shade === void 0) {
            return {
              background: `var(--mantine-color-${color2}-filled)`,
              hover: `var(--mantine-color-${color2}-filled-hover)`,
              color: textColor,
              border: `${rem(1)} solid transparent`
            };
          }
          return {
            background: `var(--mantine-color-${parsed.color}-${parsed.shade})`,
            hover: `var(--mantine-color-${parsed.color}-${parsed.shade === 9 ? 8 : parsed.shade + 1})`,
            color: textColor,
            border: `${rem(1)} solid transparent`
          };
        }
        return {
          background: color2,
          hover: darken(color2, 0.1),
          color: textColor,
          border: `${rem(1)} solid transparent`
        };
      }
      if (variant === "light") {
        if (parsed.isThemeColor) {
          if (parsed.shade === void 0) {
            return {
              background: `var(--mantine-color-${color2}-light)`,
              hover: `var(--mantine-color-${color2}-light-hover)`,
              color: `var(--mantine-color-${color2}-light-color)`,
              border: `${rem(1)} solid transparent`
            };
          }
          const parsedColor = theme2.colors[parsed.color][parsed.shade];
          return {
            background: rgba(parsedColor, 0.1),
            hover: rgba(parsedColor, 0.12),
            color: `var(--mantine-color-${parsed.color}-${Math.min(parsed.shade, 6)})`,
            border: `${rem(1)} solid transparent`
          };
        }
        return {
          background: rgba(color2, 0.1),
          hover: rgba(color2, 0.12),
          color: color2,
          border: `${rem(1)} solid transparent`
        };
      }
      if (variant === "outline") {
        if (parsed.isThemeColor) {
          if (parsed.shade === void 0) {
            return {
              background: "transparent",
              hover: `var(--mantine-color-${color2}-outline-hover)`,
              color: `var(--mantine-color-${color2}-outline)`,
              border: `${rem(1)} solid var(--mantine-color-${color2}-outline)`
            };
          }
          return {
            background: "transparent",
            hover: rgba(theme2.colors[parsed.color][parsed.shade], 0.05),
            color: `var(--mantine-color-${parsed.color}-${parsed.shade})`,
            border: `${rem(1)} solid var(--mantine-color-${parsed.color}-${parsed.shade})`
          };
        }
        return {
          background: "transparent",
          hover: rgba(color2, 0.05),
          color: color2,
          border: `${rem(1)} solid ${color2}`
        };
      }
      if (variant === "subtle") {
        if (parsed.isThemeColor) {
          if (parsed.shade === void 0) {
            return {
              background: "transparent",
              hover: `var(--mantine-color-${color2}-light-hover)`,
              color: `var(--mantine-color-${color2}-light-color)`,
              border: `${rem(1)} solid transparent`
            };
          }
          const parsedColor = theme2.colors[parsed.color][parsed.shade];
          return {
            background: "transparent",
            hover: rgba(parsedColor, 0.12),
            color: `var(--mantine-color-${parsed.color}-${Math.min(parsed.shade, 6)})`,
            border: `${rem(1)} solid transparent`
          };
        }
        return {
          background: "transparent",
          hover: rgba(color2, 0.12),
          color: color2,
          border: `${rem(1)} solid transparent`
        };
      }
      if (variant === "transparent") {
        if (parsed.isThemeColor) {
          if (parsed.shade === void 0) {
            return {
              background: "transparent",
              hover: "transparent",
              color: `var(--mantine-color-${color2}-light-color)`,
              border: `${rem(1)} solid transparent`
            };
          }
          return {
            background: "transparent",
            hover: "transparent",
            color: `var(--mantine-color-${parsed.color}-${Math.min(parsed.shade, 6)})`,
            border: `${rem(1)} solid transparent`
          };
        }
        return {
          background: "transparent",
          hover: "transparent",
          color: color2,
          border: `${rem(1)} solid transparent`
        };
      }
      if (variant === "white") {
        if (parsed.isThemeColor) {
          if (parsed.shade === void 0) {
            return {
              background: "var(--mantine-color-white)",
              hover: darken(theme2.white, 0.01),
              color: `var(--mantine-color-${color2}-filled)`,
              border: `${rem(1)} solid transparent`
            };
          }
          return {
            background: "var(--mantine-color-white)",
            hover: darken(theme2.white, 0.01),
            color: `var(--mantine-color-${parsed.color}-${parsed.shade})`,
            border: `${rem(1)} solid transparent`
          };
        }
        return {
          background: "var(--mantine-color-white)",
          hover: darken(theme2.white, 0.01),
          color: color2,
          border: `${rem(1)} solid transparent`
        };
      }
      if (variant === "gradient") {
        return {
          background: getGradient(gradient, theme2),
          hover: getGradient(gradient, theme2),
          color: "var(--mantine-color-white)",
          border: "none"
        };
      }
      if (variant === "default") {
        return {
          background: "var(--mantine-color-default)",
          hover: "var(--mantine-color-default-hover)",
          color: "var(--mantine-color-default-color)",
          border: `${rem(1)} solid var(--mantine-color-default-border)`
        };
      }
      return {};
    };
  }
});

// node_modules/@mantine/core/esm/core/MantineProvider/default-colors.mjs
var DEFAULT_COLORS;
var init_default_colors = __esm({
  "node_modules/@mantine/core/esm/core/MantineProvider/default-colors.mjs"() {
    DEFAULT_COLORS = {
      dark: [
        "#C9C9C9",
        "#b8b8b8",
        "#828282",
        "#696969",
        "#424242",
        "#3b3b3b",
        "#2e2e2e",
        "#242424",
        "#1f1f1f",
        "#141414"
      ],
      gray: [
        "#f8f9fa",
        "#f1f3f5",
        "#e9ecef",
        "#dee2e6",
        "#ced4da",
        "#adb5bd",
        "#868e96",
        "#495057",
        "#343a40",
        "#212529"
      ],
      red: [
        "#fff5f5",
        "#ffe3e3",
        "#ffc9c9",
        "#ffa8a8",
        "#ff8787",
        "#ff6b6b",
        "#fa5252",
        "#f03e3e",
        "#e03131",
        "#c92a2a"
      ],
      pink: [
        "#fff0f6",
        "#ffdeeb",
        "#fcc2d7",
        "#faa2c1",
        "#f783ac",
        "#f06595",
        "#e64980",
        "#d6336c",
        "#c2255c",
        "#a61e4d"
      ],
      grape: [
        "#f8f0fc",
        "#f3d9fa",
        "#eebefa",
        "#e599f7",
        "#da77f2",
        "#cc5de8",
        "#be4bdb",
        "#ae3ec9",
        "#9c36b5",
        "#862e9c"
      ],
      violet: [
        "#f3f0ff",
        "#e5dbff",
        "#d0bfff",
        "#b197fc",
        "#9775fa",
        "#845ef7",
        "#7950f2",
        "#7048e8",
        "#6741d9",
        "#5f3dc4"
      ],
      indigo: [
        "#edf2ff",
        "#dbe4ff",
        "#bac8ff",
        "#91a7ff",
        "#748ffc",
        "#5c7cfa",
        "#4c6ef5",
        "#4263eb",
        "#3b5bdb",
        "#364fc7"
      ],
      blue: [
        "#e7f5ff",
        "#d0ebff",
        "#a5d8ff",
        "#74c0fc",
        "#4dabf7",
        "#339af0",
        "#228be6",
        "#1c7ed6",
        "#1971c2",
        "#1864ab"
      ],
      cyan: [
        "#e3fafc",
        "#c5f6fa",
        "#99e9f2",
        "#66d9e8",
        "#3bc9db",
        "#22b8cf",
        "#15aabf",
        "#1098ad",
        "#0c8599",
        "#0b7285"
      ],
      teal: [
        "#e6fcf5",
        "#c3fae8",
        "#96f2d7",
        "#63e6be",
        "#38d9a9",
        "#20c997",
        "#12b886",
        "#0ca678",
        "#099268",
        "#087f5b"
      ],
      green: [
        "#ebfbee",
        "#d3f9d8",
        "#b2f2bb",
        "#8ce99a",
        "#69db7c",
        "#51cf66",
        "#40c057",
        "#37b24d",
        "#2f9e44",
        "#2b8a3e"
      ],
      lime: [
        "#f4fce3",
        "#e9fac8",
        "#d8f5a2",
        "#c0eb75",
        "#a9e34b",
        "#94d82d",
        "#82c91e",
        "#74b816",
        "#66a80f",
        "#5c940d"
      ],
      yellow: [
        "#fff9db",
        "#fff3bf",
        "#ffec99",
        "#ffe066",
        "#ffd43b",
        "#fcc419",
        "#fab005",
        "#f59f00",
        "#f08c00",
        "#e67700"
      ],
      orange: [
        "#fff4e6",
        "#ffe8cc",
        "#ffd8a8",
        "#ffc078",
        "#ffa94d",
        "#ff922b",
        "#fd7e14",
        "#f76707",
        "#e8590c",
        "#d9480f"
      ]
    };
  }
});

// node_modules/@mantine/core/esm/core/MantineProvider/default-theme.mjs
var import_react29, import_jsx_runtime4, DEFAULT_FONT_FAMILY, DEFAULT_THEME;
var init_default_theme = __esm({
  "node_modules/@mantine/core/esm/core/MantineProvider/default-theme.mjs"() {
    init_rem();
    import_react29 = __toESM(require_react(), 1);
    import_jsx_runtime4 = __toESM(require_jsx_runtime(), 1);
    init_default_variant_colors_resolver();
    init_default_colors();
    DEFAULT_FONT_FAMILY = "-apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Helvetica, Arial, sans-serif, Apple Color Emoji, Segoe UI Emoji";
    DEFAULT_THEME = {
      scale: 1,
      fontSmoothing: true,
      focusRing: "auto",
      white: "#fff",
      black: "#000",
      colors: DEFAULT_COLORS,
      primaryShade: { light: 6, dark: 8 },
      primaryColor: "blue",
      variantColorResolver: defaultVariantColorsResolver,
      autoContrast: false,
      luminanceThreshold: 0.3,
      fontFamily: DEFAULT_FONT_FAMILY,
      fontFamilyMonospace: "ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, Liberation Mono, Courier New, monospace",
      respectReducedMotion: false,
      cursorType: "default",
      defaultGradient: { from: "blue", to: "cyan", deg: 45 },
      defaultRadius: "sm",
      activeClassName: "mantine-active",
      focusClassName: "",
      headings: {
        fontFamily: DEFAULT_FONT_FAMILY,
        fontWeight: "700",
        textWrap: "wrap",
        sizes: {
          h1: { fontSize: rem(34), lineHeight: "1.3" },
          h2: { fontSize: rem(26), lineHeight: "1.35" },
          h3: { fontSize: rem(22), lineHeight: "1.4" },
          h4: { fontSize: rem(18), lineHeight: "1.45" },
          h5: { fontSize: rem(16), lineHeight: "1.5" },
          h6: { fontSize: rem(14), lineHeight: "1.5" }
        }
      },
      fontSizes: {
        xs: rem(12),
        sm: rem(14),
        md: rem(16),
        lg: rem(18),
        xl: rem(20)
      },
      lineHeights: {
        xs: "1.4",
        sm: "1.45",
        md: "1.55",
        lg: "1.6",
        xl: "1.65"
      },
      radius: {
        xs: rem(2),
        sm: rem(4),
        md: rem(8),
        lg: rem(16),
        xl: rem(32)
      },
      spacing: {
        xs: rem(10),
        sm: rem(12),
        md: rem(16),
        lg: rem(20),
        xl: rem(32)
      },
      breakpoints: {
        xs: "36em",
        sm: "48em",
        md: "62em",
        lg: "75em",
        xl: "88em"
      },
      shadows: {
        xs: `0 ${rem(1)} ${rem(3)} rgba(0, 0, 0, 0.05), 0 ${rem(1)} ${rem(2)} rgba(0, 0, 0, 0.1)`,
        sm: `0 ${rem(1)} ${rem(3)} rgba(0, 0, 0, 0.05), rgba(0, 0, 0, 0.05) 0 ${rem(10)} ${rem(
          15
        )} ${rem(-5)}, rgba(0, 0, 0, 0.04) 0 ${rem(7)} ${rem(7)} ${rem(-5)}`,
        md: `0 ${rem(1)} ${rem(3)} rgba(0, 0, 0, 0.05), rgba(0, 0, 0, 0.05) 0 ${rem(20)} ${rem(
          25
        )} ${rem(-5)}, rgba(0, 0, 0, 0.04) 0 ${rem(10)} ${rem(10)} ${rem(-5)}`,
        lg: `0 ${rem(1)} ${rem(3)} rgba(0, 0, 0, 0.05), rgba(0, 0, 0, 0.05) 0 ${rem(28)} ${rem(
          23
        )} ${rem(-7)}, rgba(0, 0, 0, 0.04) 0 ${rem(12)} ${rem(12)} ${rem(-7)}`,
        xl: `0 ${rem(1)} ${rem(3)} rgba(0, 0, 0, 0.05), rgba(0, 0, 0, 0.05) 0 ${rem(36)} ${rem(
          28
        )} ${rem(-7)}, rgba(0, 0, 0, 0.04) 0 ${rem(17)} ${rem(17)} ${rem(-7)}`
      },
      other: {},
      components: {}
    };
  }
});

// node_modules/@mantine/core/esm/core/MantineProvider/color-scheme-managers/is-mantine-color-scheme.mjs
function isMantineColorScheme(value) {
  return value === "auto" || value === "dark" || value === "light";
}
var init_is_mantine_color_scheme = __esm({
  "node_modules/@mantine/core/esm/core/MantineProvider/color-scheme-managers/is-mantine-color-scheme.mjs"() {
    "use client";
  }
});

// node_modules/@mantine/core/esm/core/MantineProvider/color-scheme-managers/local-storage-manager.mjs
function localStorageColorSchemeManager({
  key = "mantine-color-scheme-value"
} = {}) {
  let handleStorageEvent;
  return {
    get: (defaultValue2) => {
      if (typeof window === "undefined") {
        return defaultValue2;
      }
      try {
        const storedColorScheme = window.localStorage.getItem(key);
        return isMantineColorScheme(storedColorScheme) ? storedColorScheme : defaultValue2;
      } catch {
        return defaultValue2;
      }
    },
    set: (value) => {
      try {
        window.localStorage.setItem(key, value);
      } catch (error) {
        console.warn(
          "[@mantine/core] Local storage color scheme manager was unable to save color scheme.",
          error
        );
      }
    },
    subscribe: (onUpdate) => {
      handleStorageEvent = (event) => {
        if (event.storageArea === window.localStorage && event.key === key) {
          isMantineColorScheme(event.newValue) && onUpdate(event.newValue);
        }
      };
      window.addEventListener("storage", handleStorageEvent);
    },
    unsubscribe: () => {
      window.removeEventListener("storage", handleStorageEvent);
    },
    clear: () => {
      window.localStorage.removeItem(key);
    }
  };
}
var init_local_storage_manager = __esm({
  "node_modules/@mantine/core/esm/core/MantineProvider/color-scheme-managers/local-storage-manager.mjs"() {
    "use client";
    init_is_mantine_color_scheme();
  }
});

// node_modules/@mantine/core/esm/core/MantineProvider/merge-mantine-theme/merge-mantine-theme.mjs
function isValidPrimaryShade(shade) {
  if (shade < 0 || shade > 9) {
    return false;
  }
  return parseInt(shade.toString(), 10) === shade;
}
function validateMantineTheme(theme2) {
  if (!(theme2.primaryColor in theme2.colors)) {
    throw new Error(INVALID_PRIMARY_COLOR_ERROR);
  }
  if (typeof theme2.primaryShade === "object") {
    if (!isValidPrimaryShade(theme2.primaryShade.dark) || !isValidPrimaryShade(theme2.primaryShade.light)) {
      throw new Error(INVALID_PRIMARY_SHADE_ERROR);
    }
  }
  if (typeof theme2.primaryShade === "number" && !isValidPrimaryShade(theme2.primaryShade)) {
    throw new Error(INVALID_PRIMARY_SHADE_ERROR);
  }
}
function mergeMantineTheme(currentTheme, themeOverride) {
  if (!themeOverride) {
    validateMantineTheme(currentTheme);
    return currentTheme;
  }
  const result = deepMerge(currentTheme, themeOverride);
  if (themeOverride.fontFamily && !themeOverride.headings?.fontFamily) {
    result.headings.fontFamily = themeOverride.fontFamily;
  }
  validateMantineTheme(result);
  return result;
}
var import_react30, import_jsx_runtime5, INVALID_PRIMARY_COLOR_ERROR, INVALID_PRIMARY_SHADE_ERROR;
var init_merge_mantine_theme = __esm({
  "node_modules/@mantine/core/esm/core/MantineProvider/merge-mantine-theme/merge-mantine-theme.mjs"() {
    init_deep_merge();
    import_react30 = __toESM(require_react(), 1);
    import_jsx_runtime5 = __toESM(require_jsx_runtime(), 1);
    INVALID_PRIMARY_COLOR_ERROR = "[@mantine/core] MantineProvider: Invalid theme.primaryColor, it accepts only key of theme.colors, learn more \u2013 https://mantine.dev/theming/colors/#primary-color";
    INVALID_PRIMARY_SHADE_ERROR = "[@mantine/core] MantineProvider: Invalid theme.primaryShade, it accepts only 0-9 integers or an object { light: 0-9, dark: 0-9 }";
  }
});

// node_modules/@mantine/core/esm/core/MantineProvider/MantineThemeProvider/MantineThemeProvider.mjs
function useMantineTheme() {
  const ctx = (0, import_react31.useContext)(MantineThemeContext);
  if (!ctx) {
    throw new Error(
      "@mantine/core: MantineProvider was not found in component tree, make sure you have it in your app"
    );
  }
  return ctx;
}
function MantineThemeProvider({
  theme: theme2,
  children,
  inherit = true
}) {
  const parentTheme = useSafeMantineTheme();
  const mergedTheme = (0, import_react31.useMemo)(
    () => mergeMantineTheme(inherit ? parentTheme : DEFAULT_THEME, theme2),
    [theme2, parentTheme, inherit]
  );
  return /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(MantineThemeContext.Provider, { value: mergedTheme, children });
}
var import_jsx_runtime6, import_react31, MantineThemeContext, useSafeMantineTheme;
var init_MantineThemeProvider = __esm({
  "node_modules/@mantine/core/esm/core/MantineProvider/MantineThemeProvider/MantineThemeProvider.mjs"() {
    "use client";
    import_jsx_runtime6 = __toESM(require_jsx_runtime(), 1);
    import_react31 = __toESM(require_react(), 1);
    init_default_theme();
    init_merge_mantine_theme();
    MantineThemeContext = (0, import_react31.createContext)(null);
    useSafeMantineTheme = () => (0, import_react31.useContext)(MantineThemeContext) || DEFAULT_THEME;
    MantineThemeProvider.displayName = "@mantine/core/MantineThemeProvider";
  }
});

// node_modules/@mantine/core/esm/core/MantineProvider/MantineClasses/MantineClasses.mjs
function MantineClasses() {
  const theme2 = useMantineTheme();
  const nonce = useMantineStyleNonce();
  const classes42 = keys(theme2.breakpoints).reduce((acc, breakpoint) => {
    const isPxBreakpoint = theme2.breakpoints[breakpoint].includes("px");
    const pxValue = px(theme2.breakpoints[breakpoint]);
    const maxWidthBreakpoint = isPxBreakpoint ? `${pxValue - 0.1}px` : em(pxValue - 0.1);
    const minWidthBreakpoint = isPxBreakpoint ? `${pxValue}px` : em(pxValue);
    return `${acc}@media (max-width: ${maxWidthBreakpoint}) {.mantine-visible-from-${breakpoint} {display: none !important;}}@media (min-width: ${minWidthBreakpoint}) {.mantine-hidden-from-${breakpoint} {display: none !important;}}`;
  }, "");
  return /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(
    "style",
    {
      "data-mantine-styles": "classes",
      nonce: nonce?.(),
      dangerouslySetInnerHTML: { __html: classes42 }
    }
  );
}
var import_jsx_runtime7, import_react32;
var init_MantineClasses = __esm({
  "node_modules/@mantine/core/esm/core/MantineProvider/MantineClasses/MantineClasses.mjs"() {
    "use client";
    import_jsx_runtime7 = __toESM(require_jsx_runtime(), 1);
    init_keys();
    init_px();
    init_rem();
    import_react32 = __toESM(require_react(), 1);
    init_Mantine_context();
    init_MantineThemeProvider();
  }
});

// node_modules/@mantine/core/esm/core/MantineProvider/convert-css-variables/css-variables-object-to-string.mjs
function cssVariablesObjectToString(variables) {
  return Object.entries(variables).map(([name2, value]) => `${name2}: ${value};`).join("");
}
var init_css_variables_object_to_string = __esm({
  "node_modules/@mantine/core/esm/core/MantineProvider/convert-css-variables/css-variables-object-to-string.mjs"() {
    "use client";
  }
});

// node_modules/@mantine/core/esm/core/MantineProvider/convert-css-variables/wrap-with-selector.mjs
function wrapWithSelector(selectors, code) {
  const _selectors = Array.isArray(selectors) ? selectors : [selectors];
  return _selectors.reduce((acc, selector) => `${selector}{${acc}}`, code);
}
var init_wrap_with_selector = __esm({
  "node_modules/@mantine/core/esm/core/MantineProvider/convert-css-variables/wrap-with-selector.mjs"() {
    "use client";
  }
});

// node_modules/@mantine/core/esm/core/MantineProvider/convert-css-variables/convert-css-variables.mjs
function convertCssVariables(input2, selector) {
  const sharedVariables = cssVariablesObjectToString(input2.variables);
  const shared = sharedVariables ? wrapWithSelector(selector, sharedVariables) : "";
  const dark = cssVariablesObjectToString(input2.dark);
  const light = cssVariablesObjectToString(input2.light);
  const darkForced = dark ? selector === ":host" ? wrapWithSelector(`${selector}([data-mantine-color-scheme="dark"])`, dark) : wrapWithSelector(`${selector}[data-mantine-color-scheme="dark"]`, dark) : "";
  const lightForced = light ? selector === ":host" ? wrapWithSelector(`${selector}([data-mantine-color-scheme="light"])`, light) : wrapWithSelector(`${selector}[data-mantine-color-scheme="light"]`, light) : "";
  return `${shared}${darkForced}${lightForced}`;
}
var init_convert_css_variables = __esm({
  "node_modules/@mantine/core/esm/core/MantineProvider/convert-css-variables/convert-css-variables.mjs"() {
    "use client";
    init_css_variables_object_to_string();
    init_wrap_with_selector();
  }
});

// node_modules/@mantine/core/esm/core/MantineProvider/color-functions/get-contrast-color/get-contrast-color.mjs
function getContrastColor({ color: color2, theme: theme2, autoContrast }) {
  const _autoContrast = typeof autoContrast === "boolean" ? autoContrast : theme2.autoContrast;
  if (!_autoContrast) {
    return "var(--mantine-color-white)";
  }
  const parsed = parseThemeColor({ color: color2 || theme2.primaryColor, theme: theme2 });
  return parsed.isLight ? "var(--mantine-color-black)" : "var(--mantine-color-white)";
}
function getPrimaryContrastColor(theme2, colorScheme) {
  return getContrastColor({
    color: theme2.colors[theme2.primaryColor][getPrimaryShade(theme2, colorScheme)],
    theme: theme2,
    autoContrast: null
  });
}
var init_get_contrast_color = __esm({
  "node_modules/@mantine/core/esm/core/MantineProvider/color-functions/get-contrast-color/get-contrast-color.mjs"() {
    "use client";
    init_get_primary_shade();
    init_parse_theme_color();
  }
});

// node_modules/@mantine/core/esm/core/MantineProvider/MantineCssVariables/get-css-color-variables.mjs
function getCSSColorVariables({
  theme: theme2,
  color: color2,
  colorScheme,
  name: name2 = color2,
  withColorValues = true
}) {
  if (!theme2.colors[color2]) {
    return {};
  }
  if (colorScheme === "light") {
    const primaryShade2 = getPrimaryShade(theme2, "light");
    const dynamicVariables2 = {
      [`--mantine-color-${name2}-text`]: `var(--mantine-color-${name2}-filled)`,
      [`--mantine-color-${name2}-filled`]: `var(--mantine-color-${name2}-${primaryShade2})`,
      [`--mantine-color-${name2}-filled-hover`]: `var(--mantine-color-${name2}-${primaryShade2 === 9 ? 8 : primaryShade2 + 1})`,
      [`--mantine-color-${name2}-light`]: alpha(theme2.colors[color2][primaryShade2], 0.1),
      [`--mantine-color-${name2}-light-hover`]: alpha(theme2.colors[color2][primaryShade2], 0.12),
      [`--mantine-color-${name2}-light-color`]: `var(--mantine-color-${name2}-${primaryShade2})`,
      [`--mantine-color-${name2}-outline`]: `var(--mantine-color-${name2}-${primaryShade2})`,
      [`--mantine-color-${name2}-outline-hover`]: alpha(theme2.colors[color2][primaryShade2], 0.05)
    };
    if (!withColorValues) {
      return dynamicVariables2;
    }
    return {
      [`--mantine-color-${name2}-0`]: theme2.colors[color2][0],
      [`--mantine-color-${name2}-1`]: theme2.colors[color2][1],
      [`--mantine-color-${name2}-2`]: theme2.colors[color2][2],
      [`--mantine-color-${name2}-3`]: theme2.colors[color2][3],
      [`--mantine-color-${name2}-4`]: theme2.colors[color2][4],
      [`--mantine-color-${name2}-5`]: theme2.colors[color2][5],
      [`--mantine-color-${name2}-6`]: theme2.colors[color2][6],
      [`--mantine-color-${name2}-7`]: theme2.colors[color2][7],
      [`--mantine-color-${name2}-8`]: theme2.colors[color2][8],
      [`--mantine-color-${name2}-9`]: theme2.colors[color2][9],
      ...dynamicVariables2
    };
  }
  const primaryShade = getPrimaryShade(theme2, "dark");
  const dynamicVariables = {
    [`--mantine-color-${name2}-text`]: `var(--mantine-color-${name2}-4)`,
    [`--mantine-color-${name2}-filled`]: `var(--mantine-color-${name2}-${primaryShade})`,
    [`--mantine-color-${name2}-filled-hover`]: `var(--mantine-color-${name2}-${primaryShade === 9 ? 8 : primaryShade + 1})`,
    [`--mantine-color-${name2}-light`]: alpha(
      theme2.colors[color2][Math.max(0, primaryShade - 2)],
      0.15
    ),
    [`--mantine-color-${name2}-light-hover`]: alpha(
      theme2.colors[color2][Math.max(0, primaryShade - 2)],
      0.2
    ),
    [`--mantine-color-${name2}-light-color`]: `var(--mantine-color-${name2}-${Math.max(primaryShade - 5, 0)})`,
    [`--mantine-color-${name2}-outline`]: `var(--mantine-color-${name2}-${Math.max(primaryShade - 4, 0)})`,
    [`--mantine-color-${name2}-outline-hover`]: alpha(
      theme2.colors[color2][Math.max(primaryShade - 4, 0)],
      0.05
    )
  };
  if (!withColorValues) {
    return dynamicVariables;
  }
  return {
    [`--mantine-color-${name2}-0`]: theme2.colors[color2][0],
    [`--mantine-color-${name2}-1`]: theme2.colors[color2][1],
    [`--mantine-color-${name2}-2`]: theme2.colors[color2][2],
    [`--mantine-color-${name2}-3`]: theme2.colors[color2][3],
    [`--mantine-color-${name2}-4`]: theme2.colors[color2][4],
    [`--mantine-color-${name2}-5`]: theme2.colors[color2][5],
    [`--mantine-color-${name2}-6`]: theme2.colors[color2][6],
    [`--mantine-color-${name2}-7`]: theme2.colors[color2][7],
    [`--mantine-color-${name2}-8`]: theme2.colors[color2][8],
    [`--mantine-color-${name2}-9`]: theme2.colors[color2][9],
    ...dynamicVariables
  };
}
var import_react33, import_jsx_runtime8;
var init_get_css_color_variables = __esm({
  "node_modules/@mantine/core/esm/core/MantineProvider/MantineCssVariables/get-css-color-variables.mjs"() {
    "use client";
    init_get_primary_shade();
    import_react33 = __toESM(require_react(), 1);
    import_jsx_runtime8 = __toESM(require_jsx_runtime(), 1);
    init_rgba();
  }
});

// node_modules/@mantine/core/esm/core/MantineProvider/MantineCssVariables/virtual-color/virtual-color.mjs
function isVirtualColor(value) {
  return !!value && typeof value === "object" && "mantine-virtual-color" in value;
}
var import_react34, import_jsx_runtime9;
var init_virtual_color = __esm({
  "node_modules/@mantine/core/esm/core/MantineProvider/MantineCssVariables/virtual-color/virtual-color.mjs"() {
    import_react34 = __toESM(require_react(), 1);
    import_jsx_runtime9 = __toESM(require_jsx_runtime(), 1);
  }
});

// node_modules/@mantine/core/esm/core/MantineProvider/MantineCssVariables/default-css-variables-resolver.mjs
function assignSizeVariables(variables, sizes2, name2) {
  keys(sizes2).forEach(
    (size4) => Object.assign(variables, { [`--mantine-${name2}-${size4}`]: sizes2[size4] })
  );
}
var import_react35, import_jsx_runtime10, defaultCssVariablesResolver;
var init_default_css_variables_resolver = __esm({
  "node_modules/@mantine/core/esm/core/MantineProvider/MantineCssVariables/default-css-variables-resolver.mjs"() {
    "use client";
    init_keys();
    init_rem();
    import_react35 = __toESM(require_react(), 1);
    import_jsx_runtime10 = __toESM(require_jsx_runtime(), 1);
    init_get_primary_shade();
    init_get_contrast_color();
    init_get_css_color_variables();
    init_virtual_color();
    defaultCssVariablesResolver = (theme2) => {
      const lightPrimaryShade = getPrimaryShade(theme2, "light");
      const defaultRadius = theme2.defaultRadius in theme2.radius ? theme2.radius[theme2.defaultRadius] : rem(theme2.defaultRadius);
      const result = {
        variables: {
          "--mantine-scale": theme2.scale.toString(),
          "--mantine-cursor-type": theme2.cursorType,
          "--mantine-color-scheme": "light dark",
          "--mantine-webkit-font-smoothing": theme2.fontSmoothing ? "antialiased" : "unset",
          "--mantine-moz-font-smoothing": theme2.fontSmoothing ? "grayscale" : "unset",
          "--mantine-color-white": theme2.white,
          "--mantine-color-black": theme2.black,
          "--mantine-line-height": theme2.lineHeights.md,
          "--mantine-font-family": theme2.fontFamily,
          "--mantine-font-family-monospace": theme2.fontFamilyMonospace,
          "--mantine-font-family-headings": theme2.headings.fontFamily,
          "--mantine-heading-font-weight": theme2.headings.fontWeight,
          "--mantine-heading-text-wrap": theme2.headings.textWrap,
          "--mantine-radius-default": defaultRadius,
          // Primary colors
          "--mantine-primary-color-filled": `var(--mantine-color-${theme2.primaryColor}-filled)`,
          "--mantine-primary-color-filled-hover": `var(--mantine-color-${theme2.primaryColor}-filled-hover)`,
          "--mantine-primary-color-light": `var(--mantine-color-${theme2.primaryColor}-light)`,
          "--mantine-primary-color-light-hover": `var(--mantine-color-${theme2.primaryColor}-light-hover)`,
          "--mantine-primary-color-light-color": `var(--mantine-color-${theme2.primaryColor}-light-color)`
        },
        light: {
          "--mantine-primary-color-contrast": getPrimaryContrastColor(theme2, "light"),
          "--mantine-color-bright": "var(--mantine-color-black)",
          "--mantine-color-text": theme2.black,
          "--mantine-color-body": theme2.white,
          "--mantine-color-error": "var(--mantine-color-red-6)",
          "--mantine-color-placeholder": "var(--mantine-color-gray-5)",
          "--mantine-color-anchor": `var(--mantine-color-${theme2.primaryColor}-${lightPrimaryShade})`,
          "--mantine-color-default": "var(--mantine-color-white)",
          "--mantine-color-default-hover": "var(--mantine-color-gray-0)",
          "--mantine-color-default-color": "var(--mantine-color-black)",
          "--mantine-color-default-border": "var(--mantine-color-gray-4)",
          "--mantine-color-dimmed": "var(--mantine-color-gray-6)"
        },
        dark: {
          "--mantine-primary-color-contrast": getPrimaryContrastColor(theme2, "dark"),
          "--mantine-color-bright": "var(--mantine-color-white)",
          "--mantine-color-text": "var(--mantine-color-dark-0)",
          "--mantine-color-body": "var(--mantine-color-dark-7)",
          "--mantine-color-error": "var(--mantine-color-red-8)",
          "--mantine-color-placeholder": "var(--mantine-color-dark-3)",
          "--mantine-color-anchor": `var(--mantine-color-${theme2.primaryColor}-4)`,
          "--mantine-color-default": "var(--mantine-color-dark-6)",
          "--mantine-color-default-hover": "var(--mantine-color-dark-5)",
          "--mantine-color-default-color": "var(--mantine-color-white)",
          "--mantine-color-default-border": "var(--mantine-color-dark-4)",
          "--mantine-color-dimmed": "var(--mantine-color-dark-2)"
        }
      };
      assignSizeVariables(result.variables, theme2.breakpoints, "breakpoint");
      assignSizeVariables(result.variables, theme2.spacing, "spacing");
      assignSizeVariables(result.variables, theme2.fontSizes, "font-size");
      assignSizeVariables(result.variables, theme2.lineHeights, "line-height");
      assignSizeVariables(result.variables, theme2.shadows, "shadow");
      assignSizeVariables(result.variables, theme2.radius, "radius");
      theme2.colors[theme2.primaryColor].forEach((_2, index3) => {
        result.variables[`--mantine-primary-color-${index3}`] = `var(--mantine-color-${theme2.primaryColor}-${index3})`;
      });
      keys(theme2.colors).forEach((color2) => {
        const value = theme2.colors[color2];
        if (isVirtualColor(value)) {
          Object.assign(
            result.light,
            getCSSColorVariables({
              theme: theme2,
              name: value.name,
              color: value.light,
              colorScheme: "light",
              withColorValues: true
            })
          );
          Object.assign(
            result.dark,
            getCSSColorVariables({
              theme: theme2,
              name: value.name,
              color: value.dark,
              colorScheme: "dark",
              withColorValues: true
            })
          );
          return;
        }
        value.forEach((shade, index3) => {
          result.variables[`--mantine-color-${color2}-${index3}`] = shade;
        });
        Object.assign(
          result.light,
          getCSSColorVariables({
            theme: theme2,
            color: color2,
            colorScheme: "light",
            withColorValues: false
          })
        );
        Object.assign(
          result.dark,
          getCSSColorVariables({
            theme: theme2,
            color: color2,
            colorScheme: "dark",
            withColorValues: false
          })
        );
      });
      const headings4 = theme2.headings.sizes;
      keys(headings4).forEach((heading) => {
        result.variables[`--mantine-${heading}-font-size`] = headings4[heading].fontSize;
        result.variables[`--mantine-${heading}-line-height`] = headings4[heading].lineHeight;
        result.variables[`--mantine-${heading}-font-weight`] = headings4[heading].fontWeight || theme2.headings.fontWeight;
      });
      return result;
    };
  }
});

// node_modules/@mantine/core/esm/core/MantineProvider/MantineCssVariables/get-merged-variables.mjs
function getMergedVariables({ theme: theme2, generator }) {
  const defaultResolver = defaultCssVariablesResolver(theme2);
  const providerGenerator = generator?.(theme2);
  return providerGenerator ? deepMerge(defaultResolver, providerGenerator) : defaultResolver;
}
var import_react36, import_jsx_runtime11;
var init_get_merged_variables = __esm({
  "node_modules/@mantine/core/esm/core/MantineProvider/MantineCssVariables/get-merged-variables.mjs"() {
    "use client";
    init_deep_merge();
    import_react36 = __toESM(require_react(), 1);
    import_jsx_runtime11 = __toESM(require_jsx_runtime(), 1);
    init_default_css_variables_resolver();
  }
});

// node_modules/@mantine/core/esm/core/MantineProvider/MantineCssVariables/remove-default-variables.mjs
function removeDefaultVariables(input2) {
  const cleaned = {
    variables: {},
    light: {},
    dark: {}
  };
  keys(input2.variables).forEach((key) => {
    if (defaultCssVariables.variables[key] !== input2.variables[key]) {
      cleaned.variables[key] = input2.variables[key];
    }
  });
  keys(input2.light).forEach((key) => {
    if (defaultCssVariables.light[key] !== input2.light[key]) {
      cleaned.light[key] = input2.light[key];
    }
  });
  keys(input2.dark).forEach((key) => {
    if (defaultCssVariables.dark[key] !== input2.dark[key]) {
      cleaned.dark[key] = input2.dark[key];
    }
  });
  return cleaned;
}
var import_react37, import_jsx_runtime12, defaultCssVariables;
var init_remove_default_variables = __esm({
  "node_modules/@mantine/core/esm/core/MantineProvider/MantineCssVariables/remove-default-variables.mjs"() {
    "use client";
    init_keys();
    import_react37 = __toESM(require_react(), 1);
    import_jsx_runtime12 = __toESM(require_jsx_runtime(), 1);
    init_default_theme();
    init_default_css_variables_resolver();
    defaultCssVariables = defaultCssVariablesResolver(DEFAULT_THEME);
  }
});

// node_modules/@mantine/core/esm/core/MantineProvider/MantineCssVariables/MantineCssVariables.mjs
function getColorSchemeCssVariables(selector) {
  return `
  ${selector}[data-mantine-color-scheme="dark"] { --mantine-color-scheme: dark; }
  ${selector}[data-mantine-color-scheme="light"] { --mantine-color-scheme: light; }
`;
}
function MantineCssVariables({
  cssVariablesSelector,
  deduplicateCssVariables
}) {
  const theme2 = useMantineTheme();
  const nonce = useMantineStyleNonce();
  const generator = useMantineCssVariablesResolver();
  const mergedVariables = getMergedVariables({ theme: theme2, generator });
  const shouldCleanVariables = cssVariablesSelector === ":root" && deduplicateCssVariables;
  const cleanedVariables = shouldCleanVariables ? removeDefaultVariables(mergedVariables) : mergedVariables;
  const css = convertCssVariables(cleanedVariables, cssVariablesSelector);
  if (css) {
    return /* @__PURE__ */ (0, import_jsx_runtime13.jsx)(
      "style",
      {
        "data-mantine-styles": true,
        nonce: nonce?.(),
        dangerouslySetInnerHTML: {
          __html: `${css}${shouldCleanVariables ? "" : getColorSchemeCssVariables(cssVariablesSelector)}`
        }
      }
    );
  }
  return null;
}
var import_jsx_runtime13;
var init_MantineCssVariables = __esm({
  "node_modules/@mantine/core/esm/core/MantineProvider/MantineCssVariables/MantineCssVariables.mjs"() {
    "use client";
    import_jsx_runtime13 = __toESM(require_jsx_runtime(), 1);
    init_convert_css_variables();
    init_Mantine_context();
    init_MantineThemeProvider();
    init_get_merged_variables();
    init_remove_default_variables();
    MantineCssVariables.displayName = "@mantine/CssVariables";
  }
});

// node_modules/@mantine/core/esm/core/MantineProvider/suppress-nextjs-warning.mjs
function suppressNextjsWarning() {
  const originalError = console.error;
  console.error = (...args) => {
    if (args.length > 1 && typeof args[0] === "string" && args[0].toLowerCase().includes("extra attributes from the server") && typeof args[1] === "string" && args[1].toLowerCase().includes("data-mantine-color-scheme")) ;
    else {
      originalError(...args);
    }
  };
}
var init_suppress_nextjs_warning = __esm({
  "node_modules/@mantine/core/esm/core/MantineProvider/suppress-nextjs-warning.mjs"() {
    "use client";
  }
});

// node_modules/@mantine/core/esm/core/MantineProvider/use-mantine-color-scheme/use-provider-color-scheme.mjs
function setColorSchemeAttribute(colorScheme, getRootElement) {
  const hasDarkColorScheme = typeof window !== "undefined" && "matchMedia" in window && window.matchMedia("(prefers-color-scheme: dark)").matches;
  const computedColorScheme = colorScheme !== "auto" ? colorScheme : hasDarkColorScheme ? "dark" : "light";
  getRootElement()?.setAttribute("data-mantine-color-scheme", computedColorScheme);
}
function useProviderColorScheme({
  manager,
  defaultColorScheme,
  getRootElement,
  forceColorScheme
}) {
  const media = (0, import_react38.useRef)();
  const [value, setValue] = (0, import_react38.useState)(() => manager.get(defaultColorScheme));
  const colorSchemeValue = forceColorScheme || value;
  const setColorScheme = (0, import_react38.useCallback)(
    (colorScheme) => {
      if (!forceColorScheme) {
        setColorSchemeAttribute(colorScheme, getRootElement);
        setValue(colorScheme);
        manager.set(colorScheme);
      }
    },
    [manager.set, colorSchemeValue, forceColorScheme]
  );
  const clearColorScheme = (0, import_react38.useCallback)(() => {
    setValue(defaultColorScheme);
    setColorSchemeAttribute(defaultColorScheme, getRootElement);
    manager.clear();
  }, [manager.clear, defaultColorScheme]);
  (0, import_react38.useEffect)(() => {
    manager.subscribe(setColorScheme);
    return manager.unsubscribe;
  }, [manager.subscribe, manager.unsubscribe]);
  useIsomorphicEffect(() => {
    setColorSchemeAttribute(manager.get(defaultColorScheme), getRootElement);
  }, []);
  (0, import_react38.useEffect)(() => {
    if (forceColorScheme) {
      setColorSchemeAttribute(forceColorScheme, getRootElement);
      return () => {
      };
    }
    if (forceColorScheme === void 0) {
      setColorSchemeAttribute(value, getRootElement);
    }
    if (typeof window !== "undefined" && "matchMedia" in window) {
      media.current = window.matchMedia("(prefers-color-scheme: dark)");
    }
    const listener = (event) => {
      if (value === "auto") {
        setColorSchemeAttribute(event.matches ? "dark" : "light", getRootElement);
      }
    };
    media.current?.addEventListener("change", listener);
    return () => media.current?.removeEventListener("change", listener);
  }, [value, forceColorScheme]);
  return { colorScheme: colorSchemeValue, setColorScheme, clearColorScheme };
}
var import_react38;
var init_use_provider_color_scheme = __esm({
  "node_modules/@mantine/core/esm/core/MantineProvider/use-mantine-color-scheme/use-provider-color-scheme.mjs"() {
    "use client";
    import_react38 = __toESM(require_react(), 1);
    init_esm();
  }
});

// node_modules/@mantine/core/esm/core/MantineProvider/use-respect-reduce-motion/use-respect-reduce-motion.mjs
function useRespectReduceMotion({
  respectReducedMotion,
  getRootElement
}) {
  useIsomorphicEffect(() => {
    if (respectReducedMotion) {
      getRootElement()?.setAttribute("data-respect-reduced-motion", "true");
    }
  }, [respectReducedMotion]);
}
var init_use_respect_reduce_motion = __esm({
  "node_modules/@mantine/core/esm/core/MantineProvider/use-respect-reduce-motion/use-respect-reduce-motion.mjs"() {
    "use client";
    init_esm();
  }
});

// node_modules/@mantine/core/esm/core/MantineProvider/MantineProvider.mjs
function MantineProvider({
  theme: theme2,
  children,
  getStyleNonce,
  withStaticClasses = true,
  withGlobalClasses = true,
  deduplicateCssVariables = true,
  withCssVariables = true,
  cssVariablesSelector = ":root",
  classNamesPrefix = "mantine",
  colorSchemeManager = localStorageColorSchemeManager(),
  defaultColorScheme = "light",
  getRootElement = () => document.documentElement,
  cssVariablesResolver,
  forceColorScheme,
  stylesTransform
}) {
  const { colorScheme, setColorScheme, clearColorScheme } = useProviderColorScheme({
    defaultColorScheme,
    forceColorScheme,
    manager: colorSchemeManager,
    getRootElement
  });
  useRespectReduceMotion({
    respectReducedMotion: theme2?.respectReducedMotion || false,
    getRootElement
  });
  return /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(
    MantineContext.Provider,
    {
      value: {
        colorScheme,
        setColorScheme,
        clearColorScheme,
        getRootElement,
        classNamesPrefix,
        getStyleNonce,
        cssVariablesResolver,
        cssVariablesSelector,
        withStaticClasses,
        stylesTransform
      },
      children: /* @__PURE__ */ (0, import_jsx_runtime14.jsxs)(MantineThemeProvider, { theme: theme2, children: [
        withCssVariables && /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(
          MantineCssVariables,
          {
            cssVariablesSelector,
            deduplicateCssVariables
          }
        ),
        withGlobalClasses && /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(MantineClasses, {}),
        children
      ] })
    }
  );
}
function HeadlessMantineProvider({ children, theme: theme2 }) {
  return /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(
    MantineContext.Provider,
    {
      value: {
        colorScheme: "auto",
        setColorScheme: () => {
        },
        clearColorScheme: () => {
        },
        getRootElement: () => document.documentElement,
        classNamesPrefix: "mantine",
        cssVariablesSelector: ":root",
        withStaticClasses: false,
        headless: true
      },
      children: /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(MantineThemeProvider, { theme: theme2, children })
    }
  );
}
var import_jsx_runtime14, import_react39;
var init_MantineProvider = __esm({
  "node_modules/@mantine/core/esm/core/MantineProvider/MantineProvider.mjs"() {
    "use client";
    import_jsx_runtime14 = __toESM(require_jsx_runtime(), 1);
    init_local_storage_manager();
    init_Mantine_context();
    init_MantineClasses();
    init_MantineCssVariables();
    import_react39 = __toESM(require_react(), 1);
    init_MantineThemeProvider();
    init_suppress_nextjs_warning();
    init_use_provider_color_scheme();
    init_use_respect_reduce_motion();
    suppressNextjsWarning();
    MantineProvider.displayName = "@mantine/core/MantineProvider";
    HeadlessMantineProvider.displayName = "@mantine/core/HeadlessMantineProvider";
  }
});

// node_modules/@mantine/core/esm/core/styles-api/use-resolved-styles-api/use-resolved-styles-api.mjs
function useResolvedStylesApi({
  classNames,
  styles,
  props,
  stylesCtx
}) {
  const theme2 = useMantineTheme();
  return {
    resolvedClassNames: resolveClassNames({
      theme: theme2,
      classNames,
      props,
      stylesCtx: stylesCtx || void 0
    }),
    resolvedStyles: resolveStyles({
      theme: theme2,
      styles,
      props,
      stylesCtx: stylesCtx || void 0
    })
  };
}
var import_react40, import_jsx_runtime15;
var init_use_resolved_styles_api = __esm({
  "node_modules/@mantine/core/esm/core/styles-api/use-resolved-styles-api/use-resolved-styles-api.mjs"() {
    "use client";
    import_react40 = __toESM(require_react(), 1);
    import_jsx_runtime15 = __toESM(require_jsx_runtime(), 1);
    init_MantineThemeProvider();
    init_resolve_class_names();
    init_resolve_styles();
  }
});

// node_modules/@mantine/core/esm/core/styles-api/use-styles/get-class-name/get-global-class-names/get-global-class-names.mjs
function getGlobalClassNames({ theme: theme2, options, unstyled }) {
  return clsx_default(
    options?.focusable && !unstyled && (theme2.focusClassName || FOCUS_CLASS_NAMES[theme2.focusRing]),
    options?.active && !unstyled && theme2.activeClassName
  );
}
var FOCUS_CLASS_NAMES;
var init_get_global_class_names = __esm({
  "node_modules/@mantine/core/esm/core/styles-api/use-styles/get-class-name/get-global-class-names/get-global-class-names.mjs"() {
    "use client";
    init_clsx();
    FOCUS_CLASS_NAMES = {
      always: "mantine-focus-always",
      auto: "mantine-focus-auto",
      never: "mantine-focus-never"
    };
  }
});

// node_modules/@mantine/core/esm/core/styles-api/use-styles/get-class-name/get-options-class-names/get-options-class-names.mjs
function getOptionsClassNames({
  selector,
  stylesCtx,
  options,
  props,
  theme: theme2
}) {
  return resolveClassNames({
    theme: theme2,
    classNames: options?.classNames,
    props: options?.props || props,
    stylesCtx
  })[selector];
}
var init_get_options_class_names = __esm({
  "node_modules/@mantine/core/esm/core/styles-api/use-styles/get-class-name/get-options-class-names/get-options-class-names.mjs"() {
    "use client";
    init_resolve_class_names();
  }
});

// node_modules/@mantine/core/esm/core/styles-api/use-styles/get-class-name/get-resolved-class-names/get-resolved-class-names.mjs
function getResolvedClassNames({
  selector,
  stylesCtx,
  theme: theme2,
  classNames,
  props
}) {
  return resolveClassNames({ theme: theme2, classNames, props, stylesCtx })[selector];
}
var init_get_resolved_class_names = __esm({
  "node_modules/@mantine/core/esm/core/styles-api/use-styles/get-class-name/get-resolved-class-names/get-resolved-class-names.mjs"() {
    "use client";
    init_resolve_class_names();
  }
});

// node_modules/@mantine/core/esm/core/styles-api/use-styles/get-class-name/get-root-class-name/get-root-class-name.mjs
function getRootClassName({ rootSelector, selector, className }) {
  return rootSelector === selector ? className : void 0;
}
var init_get_root_class_name = __esm({
  "node_modules/@mantine/core/esm/core/styles-api/use-styles/get-class-name/get-root-class-name/get-root-class-name.mjs"() {
    "use client";
  }
});

// node_modules/@mantine/core/esm/core/styles-api/use-styles/get-class-name/get-selector-class-name/get-selector-class-name.mjs
function getSelectorClassName({ selector, classes: classes42, unstyled }) {
  return unstyled ? void 0 : classes42[selector];
}
var init_get_selector_class_name = __esm({
  "node_modules/@mantine/core/esm/core/styles-api/use-styles/get-class-name/get-selector-class-name/get-selector-class-name.mjs"() {
    "use client";
  }
});

// node_modules/@mantine/core/esm/core/styles-api/use-styles/get-class-name/get-static-class-names/get-static-class-names.mjs
function getStaticClassNames({
  themeName,
  classNamesPrefix,
  selector,
  withStaticClass
}) {
  if (withStaticClass === false) {
    return [];
  }
  return themeName.map((n13) => `${classNamesPrefix}-${n13}-${selector}`);
}
var init_get_static_class_names = __esm({
  "node_modules/@mantine/core/esm/core/styles-api/use-styles/get-class-name/get-static-class-names/get-static-class-names.mjs"() {
    "use client";
  }
});

// node_modules/@mantine/core/esm/core/styles-api/use-styles/get-class-name/get-theme-class-names/get-theme-class-names.mjs
function getThemeClassNames({
  themeName,
  theme: theme2,
  selector,
  props,
  stylesCtx
}) {
  return themeName.map(
    (n13) => resolveClassNames({
      theme: theme2,
      classNames: theme2.components[n13]?.classNames,
      props,
      stylesCtx
    })?.[selector]
  );
}
var init_get_theme_class_names = __esm({
  "node_modules/@mantine/core/esm/core/styles-api/use-styles/get-class-name/get-theme-class-names/get-theme-class-names.mjs"() {
    "use client";
    init_resolve_class_names();
  }
});

// node_modules/@mantine/core/esm/core/styles-api/use-styles/get-class-name/get-variant-class-name/get-variant-class-name.mjs
function getVariantClassName({
  options,
  classes: classes42,
  selector,
  unstyled
}) {
  return options?.variant && !unstyled ? classes42[`${selector}--${options.variant}`] : void 0;
}
var init_get_variant_class_name = __esm({
  "node_modules/@mantine/core/esm/core/styles-api/use-styles/get-class-name/get-variant-class-name/get-variant-class-name.mjs"() {
    "use client";
  }
});

// node_modules/@mantine/core/esm/core/styles-api/use-styles/get-class-name/get-class-name.mjs
function getClassName({
  theme: theme2,
  options,
  themeName,
  selector,
  classNamesPrefix,
  classNames,
  classes: classes42,
  unstyled,
  className,
  rootSelector,
  props,
  stylesCtx,
  withStaticClasses,
  headless,
  transformedStyles
}) {
  return clsx_default(
    getGlobalClassNames({ theme: theme2, options, unstyled: unstyled || headless }),
    getThemeClassNames({ theme: theme2, themeName, selector, props, stylesCtx }),
    getVariantClassName({ options, classes: classes42, selector, unstyled }),
    getResolvedClassNames({ selector, stylesCtx, theme: theme2, classNames, props }),
    getResolvedClassNames({ selector, stylesCtx, theme: theme2, classNames: transformedStyles, props }),
    getOptionsClassNames({ selector, stylesCtx, options, props, theme: theme2 }),
    getRootClassName({ rootSelector, selector, className }),
    getSelectorClassName({ selector, classes: classes42, unstyled: unstyled || headless }),
    withStaticClasses && !headless && getStaticClassNames({
      themeName,
      classNamesPrefix,
      selector,
      withStaticClass: options?.withStaticClass
    }),
    options?.className
  );
}
var init_get_class_name = __esm({
  "node_modules/@mantine/core/esm/core/styles-api/use-styles/get-class-name/get-class-name.mjs"() {
    "use client";
    init_clsx();
    init_get_global_class_names();
    init_get_options_class_names();
    init_get_resolved_class_names();
    init_get_root_class_name();
    init_get_selector_class_name();
    init_get_static_class_names();
    init_get_theme_class_names();
    init_get_variant_class_name();
  }
});

// node_modules/@mantine/core/esm/core/styles-api/use-styles/get-style/get-theme-styles/get-theme-styles.mjs
function getThemeStyles({
  theme: theme2,
  themeName,
  props,
  stylesCtx,
  selector
}) {
  return themeName.map(
    (n13) => resolveStyles({
      theme: theme2,
      styles: theme2.components[n13]?.styles,
      props,
      stylesCtx
    })[selector]
  ).reduce((acc, val) => ({ ...acc, ...val }), {});
}
var init_get_theme_styles = __esm({
  "node_modules/@mantine/core/esm/core/styles-api/use-styles/get-style/get-theme-styles/get-theme-styles.mjs"() {
    "use client";
    init_resolve_styles();
  }
});

// node_modules/@mantine/core/esm/core/styles-api/use-styles/get-style/resolve-style/resolve-style.mjs
function resolveStyle({ style, theme: theme2 }) {
  if (Array.isArray(style)) {
    return [...style].reduce(
      (acc, item2) => ({ ...acc, ...resolveStyle({ style: item2, theme: theme2 }) }),
      {}
    );
  }
  if (typeof style === "function") {
    return style(theme2);
  }
  if (style == null) {
    return {};
  }
  return style;
}
var init_resolve_style = __esm({
  "node_modules/@mantine/core/esm/core/styles-api/use-styles/get-style/resolve-style/resolve-style.mjs"() {
    "use client";
  }
});

// node_modules/@mantine/core/esm/core/styles-api/use-styles/get-style/resolve-vars/merge-vars.mjs
function mergeVars(vars) {
  return vars.reduce((acc, current) => {
    if (current) {
      Object.keys(current).forEach((key) => {
        acc[key] = { ...acc[key], ...filterProps(current[key]) };
      });
    }
    return acc;
  }, {});
}
var import_react41, import_jsx_runtime16;
var init_merge_vars = __esm({
  "node_modules/@mantine/core/esm/core/styles-api/use-styles/get-style/resolve-vars/merge-vars.mjs"() {
    "use client";
    init_filter_props();
    import_react41 = __toESM(require_react(), 1);
    import_jsx_runtime16 = __toESM(require_jsx_runtime(), 1);
  }
});

// node_modules/@mantine/core/esm/core/styles-api/use-styles/get-style/resolve-vars/resolve-vars.mjs
function resolveVars({
  vars,
  varsResolver: varsResolver40,
  theme: theme2,
  props,
  stylesCtx,
  selector,
  themeName,
  headless
}) {
  return mergeVars([
    headless ? {} : varsResolver40?.(theme2, props, stylesCtx),
    ...themeName.map((name2) => theme2.components?.[name2]?.vars?.(theme2, props, stylesCtx)),
    vars?.(theme2, props, stylesCtx)
  ])?.[selector];
}
var init_resolve_vars = __esm({
  "node_modules/@mantine/core/esm/core/styles-api/use-styles/get-style/resolve-vars/resolve-vars.mjs"() {
    "use client";
    init_merge_vars();
  }
});

// node_modules/@mantine/core/esm/core/styles-api/use-styles/get-style/get-style.mjs
function getStyle({
  theme: theme2,
  themeName,
  selector,
  options,
  props,
  stylesCtx,
  rootSelector,
  styles,
  style,
  vars,
  varsResolver: varsResolver40,
  headless,
  withStylesTransform
}) {
  return {
    ...!withStylesTransform && getThemeStyles({ theme: theme2, themeName, props, stylesCtx, selector }),
    ...!withStylesTransform && resolveStyles({ theme: theme2, styles, props, stylesCtx })[selector],
    ...!withStylesTransform && resolveStyles({ theme: theme2, styles: options?.styles, props: options?.props || props, stylesCtx })[selector],
    ...resolveVars({ theme: theme2, props, stylesCtx, vars, varsResolver: varsResolver40, selector, themeName, headless }),
    ...rootSelector === selector ? resolveStyle({ style, theme: theme2 }) : null,
    ...resolveStyle({ style: options?.style, theme: theme2 })
  };
}
var init_get_style = __esm({
  "node_modules/@mantine/core/esm/core/styles-api/use-styles/get-style/get-style.mjs"() {
    "use client";
    init_get_theme_styles();
    init_resolve_style();
    init_resolve_styles();
    init_resolve_vars();
  }
});

// node_modules/@mantine/core/esm/core/styles-api/use-styles/use-transformed-styles.mjs
function useStylesTransform({ props, stylesCtx, themeName }) {
  const theme2 = useMantineTheme();
  const stylesTransform = useMantineStylesTransform()?.();
  const getTransformedStyles = (styles) => {
    if (!stylesTransform) {
      return [];
    }
    const transformedStyles = styles.map(
      (style) => stylesTransform(style, { props, theme: theme2, ctx: stylesCtx })
    );
    return [
      ...transformedStyles,
      ...themeName.map(
        (n13) => stylesTransform(theme2.components[n13]?.styles, { props, theme: theme2, ctx: stylesCtx })
      )
    ].filter(Boolean);
  };
  return {
    getTransformedStyles,
    withStylesTransform: !!stylesTransform
  };
}
var import_react42, import_jsx_runtime17;
var init_use_transformed_styles = __esm({
  "node_modules/@mantine/core/esm/core/styles-api/use-styles/use-transformed-styles.mjs"() {
    "use client";
    import_react42 = __toESM(require_react(), 1);
    import_jsx_runtime17 = __toESM(require_jsx_runtime(), 1);
    init_Mantine_context();
    init_MantineThemeProvider();
  }
});

// node_modules/@mantine/core/esm/core/styles-api/use-styles/use-styles.mjs
function useStyles({
  name: name2,
  classes: classes42,
  props,
  stylesCtx,
  className,
  style,
  rootSelector = "root",
  unstyled,
  classNames,
  styles,
  vars,
  varsResolver: varsResolver40
}) {
  const theme2 = useMantineTheme();
  const classNamesPrefix = useMantineClassNamesPrefix();
  const withStaticClasses = useMantineWithStaticClasses();
  const headless = useMantineIsHeadless();
  const themeName = (Array.isArray(name2) ? name2 : [name2]).filter((n13) => n13);
  const { withStylesTransform, getTransformedStyles } = useStylesTransform({
    props,
    stylesCtx,
    themeName
  });
  return (selector, options) => ({
    className: getClassName({
      theme: theme2,
      options,
      themeName,
      selector,
      classNamesPrefix,
      classNames,
      classes: classes42,
      unstyled,
      className,
      rootSelector,
      props,
      stylesCtx,
      withStaticClasses,
      headless,
      transformedStyles: getTransformedStyles([options?.styles, styles])
    }),
    style: getStyle({
      theme: theme2,
      themeName,
      selector,
      options,
      props,
      stylesCtx,
      rootSelector,
      styles,
      style,
      vars,
      varsResolver: varsResolver40,
      headless,
      withStylesTransform
    })
  });
}
var import_react43, import_jsx_runtime18;
var init_use_styles = __esm({
  "node_modules/@mantine/core/esm/core/styles-api/use-styles/use-styles.mjs"() {
    "use client";
    import_react43 = __toESM(require_react(), 1);
    import_jsx_runtime18 = __toESM(require_jsx_runtime(), 1);
    init_Mantine_context();
    init_MantineThemeProvider();
    init_get_class_name();
    init_get_style();
    init_use_transformed_styles();
  }
});

// node_modules/@mantine/core/esm/core/MantineProvider/color-functions/get-auto-contrast-value/get-auto-contrast-value.mjs
function getAutoContrastValue(autoContrast, theme2) {
  return typeof autoContrast === "boolean" ? autoContrast : theme2.autoContrast;
}
var init_get_auto_contrast_value = __esm({
  "node_modules/@mantine/core/esm/core/MantineProvider/color-functions/get-auto-contrast-value/get-auto-contrast-value.mjs"() {
    "use client";
  }
});

// node_modules/@mantine/core/esm/core/MantineProvider/ColorSchemeScript/ColorSchemeScript.mjs
function ColorSchemeScript({
  defaultColorScheme = "light",
  localStorageKey = "mantine-color-scheme-value",
  forceColorScheme,
  ...others
}) {
  const _defaultColorScheme = ["light", "dark", "auto"].includes(defaultColorScheme) ? defaultColorScheme : "light";
  return /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(
    "script",
    {
      ...others,
      "data-mantine-script": true,
      dangerouslySetInnerHTML: {
        __html: getScript({
          defaultColorScheme: _defaultColorScheme,
          localStorageKey,
          forceColorScheme
        })
      }
    }
  );
}
var import_jsx_runtime19, getScript;
var init_ColorSchemeScript = __esm({
  "node_modules/@mantine/core/esm/core/MantineProvider/ColorSchemeScript/ColorSchemeScript.mjs"() {
    "use client";
    import_jsx_runtime19 = __toESM(require_jsx_runtime(), 1);
    getScript = ({
      defaultColorScheme,
      localStorageKey,
      forceColorScheme
    }) => forceColorScheme ? `document.documentElement.setAttribute("data-mantine-color-scheme", '${forceColorScheme}');` : `try {
  var _colorScheme = window.localStorage.getItem("${localStorageKey}");
  var colorScheme = _colorScheme === "light" || _colorScheme === "dark" || _colorScheme === "auto" ? _colorScheme : "${defaultColorScheme}";
  var computedColorScheme = colorScheme !== "auto" ? colorScheme : window.matchMedia("(prefers-color-scheme: dark)").matches ? "dark" : "light";
  document.documentElement.setAttribute("data-mantine-color-scheme", computedColorScheme);
} catch (e) {}
`;
  }
});

// node_modules/@mantine/core/esm/core/MantineProvider/use-props/use-props.mjs
function useProps(component, defaultProps79, props) {
  const theme2 = useMantineTheme();
  const contextPropsPayload = theme2.components[component]?.defaultProps;
  const contextProps = typeof contextPropsPayload === "function" ? contextPropsPayload(theme2) : contextPropsPayload;
  return { ...defaultProps79, ...contextProps, ...filterProps(props) };
}
var import_react44, import_jsx_runtime20;
var init_use_props = __esm({
  "node_modules/@mantine/core/esm/core/MantineProvider/use-props/use-props.mjs"() {
    "use client";
    init_filter_props();
    import_react44 = __toESM(require_react(), 1);
    import_jsx_runtime20 = __toESM(require_jsx_runtime(), 1);
    init_MantineThemeProvider();
  }
});

// node_modules/@mantine/core/esm/core/MantineProvider/create-theme/create-theme.mjs
function createTheme(theme2) {
  return theme2;
}
var init_create_theme = __esm({
  "node_modules/@mantine/core/esm/core/MantineProvider/create-theme/create-theme.mjs"() {
  }
});

// node_modules/@mantine/core/esm/core/InlineStyles/css-object-to-string/css-object-to-string.mjs
function cssObjectToString(css) {
  return keys(css).reduce(
    (acc, rule) => css[rule] !== void 0 ? `${acc}${camelToKebabCase(rule)}:${css[rule]};` : acc,
    ""
  ).trim();
}
var import_react45, import_jsx_runtime21;
var init_css_object_to_string = __esm({
  "node_modules/@mantine/core/esm/core/InlineStyles/css-object-to-string/css-object-to-string.mjs"() {
    "use client";
    init_keys();
    init_camel_to_kebab_case();
    import_react45 = __toESM(require_react(), 1);
    import_jsx_runtime21 = __toESM(require_jsx_runtime(), 1);
  }
});

// node_modules/@mantine/core/esm/core/InlineStyles/styles-to-string/styles-to-string.mjs
function stylesToString({ selector, styles, media, container: container4 }) {
  const baseStyles = styles ? cssObjectToString(styles) : "";
  const mediaQueryStyles = !Array.isArray(media) ? [] : media.map((item2) => `@media${item2.query}{${selector}{${cssObjectToString(item2.styles)}}}`);
  const containerStyles = !Array.isArray(container4) ? [] : container4.map(
    (item2) => `@container ${item2.query}{${selector}{${cssObjectToString(item2.styles)}}}`
  );
  return `${baseStyles ? `${selector}{${baseStyles}}` : ""}${mediaQueryStyles.join("")}${containerStyles.join("")}`.trim();
}
var init_styles_to_string = __esm({
  "node_modules/@mantine/core/esm/core/InlineStyles/styles-to-string/styles-to-string.mjs"() {
    "use client";
    init_css_object_to_string();
  }
});

// node_modules/@mantine/core/esm/core/InlineStyles/InlineStyles.mjs
function InlineStyles(props) {
  const nonce = useMantineStyleNonce();
  return /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(
    "style",
    {
      "data-mantine-styles": "inline",
      nonce: nonce?.(),
      dangerouslySetInnerHTML: { __html: stylesToString(props) }
    }
  );
}
var import_jsx_runtime22, import_react46;
var init_InlineStyles = __esm({
  "node_modules/@mantine/core/esm/core/InlineStyles/InlineStyles.mjs"() {
    "use client";
    import_jsx_runtime22 = __toESM(require_jsx_runtime(), 1);
    import_react46 = __toESM(require_react(), 1);
    init_Mantine_context();
    init_styles_to_string();
  }
});

// node_modules/@mantine/core/esm/core/Box/style-props/extract-style-props/extract-style-props.mjs
function extractStyleProps(others) {
  const {
    m: m12,
    mx,
    my,
    mt: mt2,
    mb,
    ml: ml2,
    mr: mr3,
    me: me4,
    ms,
    p: p11,
    px: px3,
    py,
    pt: pt4,
    pb,
    pl: pl2,
    pr: pr3,
    pe: pe4,
    ps,
    bd,
    bg,
    c: c15,
    opacity,
    ff,
    fz,
    fw,
    lts,
    ta,
    lh,
    fs,
    tt: tt2,
    td,
    w: w11,
    miw,
    maw,
    h: h6,
    mih,
    mah,
    bgsz,
    bgp,
    bgr,
    bga,
    pos,
    top: top2,
    left,
    bottom,
    right,
    inset,
    display,
    flex,
    hiddenFrom,
    visibleFrom,
    lightHidden,
    darkHidden,
    sx,
    ...rest
  } = others;
  const styleProps = filterProps({
    m: m12,
    mx,
    my,
    mt: mt2,
    mb,
    ml: ml2,
    mr: mr3,
    me: me4,
    ms,
    p: p11,
    px: px3,
    py,
    pt: pt4,
    pb,
    pl: pl2,
    pr: pr3,
    pe: pe4,
    ps,
    bd,
    bg,
    c: c15,
    opacity,
    ff,
    fz,
    fw,
    lts,
    ta,
    lh,
    fs,
    tt: tt2,
    td,
    w: w11,
    miw,
    maw,
    h: h6,
    mih,
    mah,
    bgsz,
    bgp,
    bgr,
    bga,
    pos,
    top: top2,
    left,
    bottom,
    right,
    inset,
    display,
    flex,
    hiddenFrom,
    visibleFrom,
    lightHidden,
    darkHidden,
    sx
  });
  return { styleProps, rest };
}
var import_react47, import_jsx_runtime23;
var init_extract_style_props = __esm({
  "node_modules/@mantine/core/esm/core/Box/style-props/extract-style-props/extract-style-props.mjs"() {
    "use client";
    init_filter_props();
    import_react47 = __toESM(require_react(), 1);
    import_jsx_runtime23 = __toESM(require_jsx_runtime(), 1);
  }
});

// node_modules/@mantine/core/esm/core/Box/style-props/style-props-data.mjs
var STYlE_PROPS_DATA;
var init_style_props_data = __esm({
  "node_modules/@mantine/core/esm/core/Box/style-props/style-props-data.mjs"() {
    "use client";
    STYlE_PROPS_DATA = {
      m: { type: "spacing", property: "margin" },
      mt: { type: "spacing", property: "marginTop" },
      mb: { type: "spacing", property: "marginBottom" },
      ml: { type: "spacing", property: "marginLeft" },
      mr: { type: "spacing", property: "marginRight" },
      ms: { type: "spacing", property: "marginInlineStart" },
      me: { type: "spacing", property: "marginInlineEnd" },
      mx: { type: "spacing", property: "marginInline" },
      my: { type: "spacing", property: "marginBlock" },
      p: { type: "spacing", property: "padding" },
      pt: { type: "spacing", property: "paddingTop" },
      pb: { type: "spacing", property: "paddingBottom" },
      pl: { type: "spacing", property: "paddingLeft" },
      pr: { type: "spacing", property: "paddingRight" },
      ps: { type: "spacing", property: "paddingInlineStart" },
      pe: { type: "spacing", property: "paddingInlineEnd" },
      px: { type: "spacing", property: "paddingInline" },
      py: { type: "spacing", property: "paddingBlock" },
      bd: { type: "border", property: "border" },
      bg: { type: "color", property: "background" },
      c: { type: "textColor", property: "color" },
      opacity: { type: "identity", property: "opacity" },
      ff: { type: "fontFamily", property: "fontFamily" },
      fz: { type: "fontSize", property: "fontSize" },
      fw: { type: "identity", property: "fontWeight" },
      lts: { type: "size", property: "letterSpacing" },
      ta: { type: "identity", property: "textAlign" },
      lh: { type: "lineHeight", property: "lineHeight" },
      fs: { type: "identity", property: "fontStyle" },
      tt: { type: "identity", property: "textTransform" },
      td: { type: "identity", property: "textDecoration" },
      w: { type: "spacing", property: "width" },
      miw: { type: "spacing", property: "minWidth" },
      maw: { type: "spacing", property: "maxWidth" },
      h: { type: "spacing", property: "height" },
      mih: { type: "spacing", property: "minHeight" },
      mah: { type: "spacing", property: "maxHeight" },
      bgsz: { type: "size", property: "backgroundSize" },
      bgp: { type: "identity", property: "backgroundPosition" },
      bgr: { type: "identity", property: "backgroundRepeat" },
      bga: { type: "identity", property: "backgroundAttachment" },
      pos: { type: "identity", property: "position" },
      top: { type: "size", property: "top" },
      left: { type: "size", property: "left" },
      bottom: { type: "size", property: "bottom" },
      right: { type: "size", property: "right" },
      inset: { type: "size", property: "inset" },
      display: { type: "identity", property: "display" },
      flex: { type: "identity", property: "flex" }
    };
  }
});

// node_modules/@mantine/core/esm/core/Box/style-props/resolvers/color-resolver/color-resolver.mjs
function colorResolver(color2, theme2) {
  const parsedColor = parseThemeColor({ color: color2, theme: theme2 });
  if (parsedColor.color === "dimmed") {
    return "var(--mantine-color-dimmed)";
  }
  if (parsedColor.color === "bright") {
    return "var(--mantine-color-bright)";
  }
  return parsedColor.variable ? `var(${parsedColor.variable})` : parsedColor.color;
}
function textColorResolver(color2, theme2) {
  const parsedColor = parseThemeColor({ color: color2, theme: theme2 });
  if (parsedColor.isThemeColor && parsedColor.shade === void 0) {
    return `var(--mantine-color-${parsedColor.color}-text)`;
  }
  return colorResolver(color2, theme2);
}
var import_react48, import_jsx_runtime24;
var init_color_resolver = __esm({
  "node_modules/@mantine/core/esm/core/Box/style-props/resolvers/color-resolver/color-resolver.mjs"() {
    "use client";
    init_parse_theme_color();
    import_react48 = __toESM(require_react(), 1);
    import_jsx_runtime24 = __toESM(require_jsx_runtime(), 1);
  }
});

// node_modules/@mantine/core/esm/core/Box/style-props/resolvers/border-resolver/border-resolver.mjs
function borderResolver(value, theme2) {
  if (typeof value === "number") {
    return rem(value);
  }
  if (typeof value === "string") {
    const [size4, style, ...colorTuple] = value.split(" ").filter((val) => val.trim() !== "");
    let result = `${rem(size4)}`;
    style && (result += ` ${style}`);
    colorTuple.length > 0 && (result += ` ${colorResolver(colorTuple.join(" "), theme2)}`);
    return result.trim();
  }
  return value;
}
var import_react49, import_jsx_runtime25;
var init_border_resolver = __esm({
  "node_modules/@mantine/core/esm/core/Box/style-props/resolvers/border-resolver/border-resolver.mjs"() {
    "use client";
    init_rem();
    import_react49 = __toESM(require_react(), 1);
    import_jsx_runtime25 = __toESM(require_jsx_runtime(), 1);
    init_color_resolver();
  }
});

// node_modules/@mantine/core/esm/core/Box/style-props/resolvers/font-family-resolver/font-family-resolver.mjs
function fontFamilyResolver(fontFamily) {
  if (typeof fontFamily === "string" && fontFamily in values) {
    return values[fontFamily];
  }
  return fontFamily;
}
var values;
var init_font_family_resolver = __esm({
  "node_modules/@mantine/core/esm/core/Box/style-props/resolvers/font-family-resolver/font-family-resolver.mjs"() {
    "use client";
    values = {
      text: "var(--mantine-font-family)",
      mono: "var(--mantine-font-family-monospace)",
      monospace: "var(--mantine-font-family-monospace)",
      heading: "var(--mantine-font-family-headings)",
      headings: "var(--mantine-font-family-headings)"
    };
  }
});

// node_modules/@mantine/core/esm/core/Box/style-props/resolvers/font-size-resolver/font-size-resolver.mjs
function fontSizeResolver(value, theme2) {
  if (typeof value === "string" && value in theme2.fontSizes) {
    return `var(--mantine-font-size-${value})`;
  }
  if (typeof value === "string" && headings.includes(value)) {
    return `var(--mantine-${value}-font-size)`;
  }
  if (typeof value === "number") {
    return rem(value);
  }
  if (typeof value === "string") {
    return rem(value);
  }
  return value;
}
var import_react50, import_jsx_runtime26, headings;
var init_font_size_resolver = __esm({
  "node_modules/@mantine/core/esm/core/Box/style-props/resolvers/font-size-resolver/font-size-resolver.mjs"() {
    "use client";
    init_rem();
    import_react50 = __toESM(require_react(), 1);
    import_jsx_runtime26 = __toESM(require_jsx_runtime(), 1);
    headings = ["h1", "h2", "h3", "h4", "h5", "h6"];
  }
});

// node_modules/@mantine/core/esm/core/Box/style-props/resolvers/identity-resolver/identity-resolver.mjs
function identityResolver(value) {
  return value;
}
var init_identity_resolver = __esm({
  "node_modules/@mantine/core/esm/core/Box/style-props/resolvers/identity-resolver/identity-resolver.mjs"() {
    "use client";
  }
});

// node_modules/@mantine/core/esm/core/Box/style-props/resolvers/line-height-resolver/line-height-resolver.mjs
function lineHeightResolver(value, theme2) {
  if (typeof value === "string" && value in theme2.lineHeights) {
    return `var(--mantine-line-height-${value})`;
  }
  if (typeof value === "string" && headings2.includes(value)) {
    return `var(--mantine-${value}-line-height)`;
  }
  return value;
}
var headings2;
var init_line_height_resolver = __esm({
  "node_modules/@mantine/core/esm/core/Box/style-props/resolvers/line-height-resolver/line-height-resolver.mjs"() {
    "use client";
    headings2 = ["h1", "h2", "h3", "h4", "h5", "h6"];
  }
});

// node_modules/@mantine/core/esm/core/Box/style-props/resolvers/size-resolver/size-resolver.mjs
function sizeResolver(value) {
  if (typeof value === "number") {
    return rem(value);
  }
  return value;
}
var import_react51, import_jsx_runtime27;
var init_size_resolver = __esm({
  "node_modules/@mantine/core/esm/core/Box/style-props/resolvers/size-resolver/size-resolver.mjs"() {
    "use client";
    init_rem();
    import_react51 = __toESM(require_react(), 1);
    import_jsx_runtime27 = __toESM(require_jsx_runtime(), 1);
  }
});

// node_modules/@mantine/core/esm/core/Box/style-props/resolvers/spacing-resolver/spacing-resolver.mjs
function spacingResolver(value, theme2) {
  if (typeof value === "number") {
    return rem(value);
  }
  if (typeof value === "string") {
    const mod = value.replace("-", "");
    if (!(mod in theme2.spacing)) {
      return rem(value);
    }
    const variable = `--mantine-spacing-${mod}`;
    return value.startsWith("-") ? `calc(var(${variable}) * -1)` : `var(${variable})`;
  }
  return value;
}
var import_react52, import_jsx_runtime28;
var init_spacing_resolver = __esm({
  "node_modules/@mantine/core/esm/core/Box/style-props/resolvers/spacing-resolver/spacing-resolver.mjs"() {
    "use client";
    init_rem();
    import_react52 = __toESM(require_react(), 1);
    import_jsx_runtime28 = __toESM(require_jsx_runtime(), 1);
  }
});

// node_modules/@mantine/core/esm/core/Box/style-props/resolvers/index.mjs
var resolvers;
var init_resolvers = __esm({
  "node_modules/@mantine/core/esm/core/Box/style-props/resolvers/index.mjs"() {
    "use client";
    init_border_resolver();
    init_color_resolver();
    init_font_family_resolver();
    init_font_size_resolver();
    init_identity_resolver();
    init_line_height_resolver();
    init_size_resolver();
    init_spacing_resolver();
    resolvers = {
      color: colorResolver,
      textColor: textColorResolver,
      fontSize: fontSizeResolver,
      spacing: spacingResolver,
      identity: identityResolver,
      size: sizeResolver,
      lineHeight: lineHeightResolver,
      fontFamily: fontFamilyResolver,
      border: borderResolver
    };
  }
});

// node_modules/@mantine/core/esm/core/Box/style-props/parse-style-props/sort-media-queries.mjs
function replaceMediaQuery(query2) {
  return query2.replace("(min-width: ", "").replace("em)", "");
}
function sortMediaQueries({
  media,
  ...props
}) {
  const breakpoints = Object.keys(media);
  const sortedMedia = breakpoints.sort((a18, b4) => Number(replaceMediaQuery(a18)) - Number(replaceMediaQuery(b4))).map((query2) => ({ query: query2, styles: media[query2] }));
  return { ...props, media: sortedMedia };
}
var init_sort_media_queries = __esm({
  "node_modules/@mantine/core/esm/core/Box/style-props/parse-style-props/sort-media-queries.mjs"() {
    "use client";
  }
});

// node_modules/@mantine/core/esm/core/Box/style-props/parse-style-props/parse-style-props.mjs
function hasResponsiveStyles(styleProp) {
  if (typeof styleProp !== "object" || styleProp === null) {
    return false;
  }
  const breakpoints = Object.keys(styleProp);
  if (breakpoints.length === 1 && breakpoints[0] === "base") {
    return false;
  }
  return true;
}
function getBaseValue(value) {
  if (typeof value === "object" && value !== null) {
    if ("base" in value) {
      return value.base;
    }
    return void 0;
  }
  return value;
}
function getBreakpointKeys(value) {
  if (typeof value === "object" && value !== null) {
    return keys(value).filter((key) => key !== "base");
  }
  return [];
}
function getBreakpointValue(value, breakpoint) {
  if (typeof value === "object" && value !== null && breakpoint in value) {
    return value[breakpoint];
  }
  return value;
}
function parseStyleProps({
  styleProps,
  data,
  theme: theme2
}) {
  return sortMediaQueries(
    keys(styleProps).reduce(
      (acc, styleProp) => {
        if (styleProp === "hiddenFrom" || styleProp === "visibleFrom" || styleProp === "sx") {
          return acc;
        }
        const propertyData = data[styleProp];
        const properties = Array.isArray(propertyData.property) ? propertyData.property : [propertyData.property];
        const baseValue = getBaseValue(styleProps[styleProp]);
        if (!hasResponsiveStyles(styleProps[styleProp])) {
          properties.forEach((property) => {
            acc.inlineStyles[property] = resolvers[propertyData.type](baseValue, theme2);
          });
          return acc;
        }
        acc.hasResponsiveStyles = true;
        const breakpoints = getBreakpointKeys(styleProps[styleProp]);
        properties.forEach((property) => {
          if (baseValue) {
            acc.styles[property] = resolvers[propertyData.type](baseValue, theme2);
          }
          breakpoints.forEach((breakpoint) => {
            const bp = `(min-width: ${theme2.breakpoints[breakpoint]})`;
            acc.media[bp] = {
              ...acc.media[bp],
              [property]: resolvers[propertyData.type](
                getBreakpointValue(styleProps[styleProp], breakpoint),
                theme2
              )
            };
          });
        });
        return acc;
      },
      {
        hasResponsiveStyles: false,
        styles: {},
        inlineStyles: {},
        media: {}
      }
    )
  );
}
var import_react53, import_jsx_runtime29;
var init_parse_style_props = __esm({
  "node_modules/@mantine/core/esm/core/Box/style-props/parse-style-props/parse-style-props.mjs"() {
    "use client";
    init_keys();
    import_react53 = __toESM(require_react(), 1);
    import_jsx_runtime29 = __toESM(require_jsx_runtime(), 1);
    init_resolvers();
    init_sort_media_queries();
  }
});

// node_modules/@mantine/core/esm/core/Box/use-random-classname/use-random-classname.mjs
function useRandomClassName() {
  const id3 = (0, import_react54.useId)().replace(/:/g, "");
  return `__m__-${id3}`;
}
var import_react54;
var init_use_random_classname = __esm({
  "node_modules/@mantine/core/esm/core/Box/use-random-classname/use-random-classname.mjs"() {
    "use client";
    import_react54 = __toESM(require_react(), 1);
  }
});

// node_modules/@mantine/core/esm/core/factory/create-polymorphic-component.mjs
function createPolymorphicComponent(component) {
  return component;
}
var init_create_polymorphic_component = __esm({
  "node_modules/@mantine/core/esm/core/factory/create-polymorphic-component.mjs"() {
  }
});

// node_modules/@mantine/core/esm/core/Box/get-box-mod/get-box-mod.mjs
function transformModKey(key) {
  return key.startsWith("data-") ? key : `data-${key}`;
}
function getMod(props) {
  return Object.keys(props).reduce((acc, key) => {
    const value = props[key];
    if (value === void 0 || value === "" || value === false || value === null) {
      return acc;
    }
    acc[transformModKey(key)] = props[key];
    return acc;
  }, {});
}
function getBoxMod(mod) {
  if (!mod) {
    return null;
  }
  if (typeof mod === "string") {
    return { [transformModKey(mod)]: true };
  }
  if (Array.isArray(mod)) {
    return [...mod].reduce(
      (acc, value) => ({ ...acc, ...getBoxMod(value) }),
      {}
    );
  }
  return getMod(mod);
}
var init_get_box_mod = __esm({
  "node_modules/@mantine/core/esm/core/Box/get-box-mod/get-box-mod.mjs"() {
    "use client";
  }
});

// node_modules/@mantine/core/esm/core/Box/get-box-style/get-box-style.mjs
function mergeStyles(styles, theme2) {
  if (Array.isArray(styles)) {
    return [...styles].reduce(
      (acc, item2) => ({ ...acc, ...mergeStyles(item2, theme2) }),
      {}
    );
  }
  if (typeof styles === "function") {
    return styles(theme2);
  }
  if (styles == null) {
    return {};
  }
  return styles;
}
function getBoxStyle({
  theme: theme2,
  style,
  vars,
  styleProps
}) {
  const _style = mergeStyles(style, theme2);
  const _vars = mergeStyles(vars, theme2);
  return { ..._style, ..._vars, ...styleProps };
}
var init_get_box_style = __esm({
  "node_modules/@mantine/core/esm/core/Box/get-box-style/get-box-style.mjs"() {
    "use client";
  }
});

// node_modules/@mantine/core/esm/core/Box/Box.mjs
var import_jsx_runtime30, import_react55, _Box, Box;
var init_Box = __esm({
  "node_modules/@mantine/core/esm/core/Box/Box.mjs"() {
    "use client";
    import_jsx_runtime30 = __toESM(require_jsx_runtime(), 1);
    import_react55 = __toESM(require_react(), 1);
    init_clsx();
    init_create_polymorphic_component();
    init_InlineStyles();
    init_is_number_like();
    init_Mantine_context();
    init_MantineThemeProvider();
    init_get_box_mod();
    init_get_box_style();
    init_extract_style_props();
    init_style_props_data();
    init_parse_style_props();
    init_use_random_classname();
    _Box = (0, import_react55.forwardRef)(
      ({
        component,
        style,
        __vars,
        className,
        variant,
        mod,
        size: size4,
        hiddenFrom,
        visibleFrom,
        lightHidden,
        darkHidden,
        renderRoot,
        __size,
        ...others
      }, ref) => {
        const theme2 = useMantineTheme();
        const Element3 = component || "div";
        const { styleProps, rest } = extractStyleProps(others);
        const useSxTransform = useMantineSxTransform();
        const transformedSx = useSxTransform?.()?.(styleProps.sx);
        const responsiveClassName = useRandomClassName();
        const parsedStyleProps = parseStyleProps({
          styleProps,
          theme: theme2,
          data: STYlE_PROPS_DATA
        });
        const props = {
          ref,
          style: getBoxStyle({
            theme: theme2,
            style,
            vars: __vars,
            styleProps: parsedStyleProps.inlineStyles
          }),
          className: clsx_default(className, transformedSx, {
            [responsiveClassName]: parsedStyleProps.hasResponsiveStyles,
            "mantine-light-hidden": lightHidden,
            "mantine-dark-hidden": darkHidden,
            [`mantine-hidden-from-${hiddenFrom}`]: hiddenFrom,
            [`mantine-visible-from-${visibleFrom}`]: visibleFrom
          }),
          "data-variant": variant,
          "data-size": isNumberLike(size4) ? void 0 : size4 || void 0,
          size: __size,
          ...getBoxMod(mod),
          ...rest
        };
        return /* @__PURE__ */ (0, import_jsx_runtime30.jsxs)(import_jsx_runtime30.Fragment, { children: [
          parsedStyleProps.hasResponsiveStyles && /* @__PURE__ */ (0, import_jsx_runtime30.jsx)(
            InlineStyles,
            {
              selector: `.${responsiveClassName}`,
              styles: parsedStyleProps.styles,
              media: parsedStyleProps.media
            }
          ),
          typeof renderRoot === "function" ? renderRoot(props) : /* @__PURE__ */ (0, import_jsx_runtime30.jsx)(Element3, { ...props })
        ] });
      }
    );
    _Box.displayName = "@mantine/core/Box";
    Box = createPolymorphicComponent(_Box);
  }
});

// node_modules/@mantine/core/esm/core/factory/factory.mjs
function identity(value) {
  return value;
}
function factory(ui) {
  const Component2 = (0, import_react56.forwardRef)(ui);
  Component2.extend = identity;
  Component2.withProps = (fixedProps) => {
    const Extended = (0, import_react56.forwardRef)((props, ref) => /* @__PURE__ */ (0, import_jsx_runtime31.jsx)(Component2, { ...fixedProps, ...props, ref }));
    Extended.extend = Component2.extend;
    Extended.displayName = `WithProps(${Component2.displayName})`;
    return Extended;
  };
  return Component2;
}
var import_jsx_runtime31, import_react56;
var init_factory = __esm({
  "node_modules/@mantine/core/esm/core/factory/factory.mjs"() {
    "use client";
    import_jsx_runtime31 = __toESM(require_jsx_runtime(), 1);
    import_react56 = __toESM(require_react(), 1);
  }
});

// node_modules/@mantine/core/esm/core/factory/polymorphic-factory.mjs
function polymorphicFactory(ui) {
  const Component2 = (0, import_react57.forwardRef)(ui);
  Component2.withProps = (fixedProps) => {
    const Extended = (0, import_react57.forwardRef)((props, ref) => /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(Component2, { ...fixedProps, ...props, ref }));
    Extended.extend = Component2.extend;
    Extended.displayName = `WithProps(${Component2.displayName})`;
    return Extended;
  };
  Component2.extend = identity;
  return Component2;
}
var import_jsx_runtime32, import_react57;
var init_polymorphic_factory = __esm({
  "node_modules/@mantine/core/esm/core/factory/polymorphic-factory.mjs"() {
    "use client";
    import_jsx_runtime32 = __toESM(require_jsx_runtime(), 1);
    import_react57 = __toESM(require_react(), 1);
    init_factory();
  }
});

// node_modules/@mantine/core/esm/core/DirectionProvider/DirectionProvider.mjs
function useDirection() {
  return (0, import_react58.useContext)(DirectionContext);
}
var import_jsx_runtime33, import_react58, DirectionContext;
var init_DirectionProvider = __esm({
  "node_modules/@mantine/core/esm/core/DirectionProvider/DirectionProvider.mjs"() {
    "use client";
    import_jsx_runtime33 = __toESM(require_jsx_runtime(), 1);
    import_react58 = __toESM(require_react(), 1);
    DirectionContext = (0, import_react58.createContext)({
      dir: "ltr",
      toggleDirection: () => {
      },
      setDirection: () => {
      }
    });
  }
});

// node_modules/scheduler/cjs/scheduler.production.min.js
var require_scheduler_production_min = __commonJS({
  "node_modules/scheduler/cjs/scheduler.production.min.js"(exports) {
    "use strict";
    function f15(a18, b4) {
      var c15 = a18.length;
      a18.push(b4);
      a: for (; 0 < c15; ) {
        var d12 = c15 - 1 >>> 1, e14 = a18[d12];
        if (0 < g7(e14, b4)) a18[d12] = b4, a18[c15] = e14, c15 = d12;
        else break a;
      }
    }
    function h6(a18) {
      return 0 === a18.length ? null : a18[0];
    }
    function k5(a18) {
      if (0 === a18.length) return null;
      var b4 = a18[0], c15 = a18.pop();
      if (c15 !== b4) {
        a18[0] = c15;
        a: for (var d12 = 0, e14 = a18.length, w11 = e14 >>> 1; d12 < w11; ) {
          var m12 = 2 * (d12 + 1) - 1, C5 = a18[m12], n13 = m12 + 1, x3 = a18[n13];
          if (0 > g7(C5, c15)) n13 < e14 && 0 > g7(x3, C5) ? (a18[d12] = x3, a18[n13] = c15, d12 = n13) : (a18[d12] = C5, a18[m12] = c15, d12 = m12);
          else if (n13 < e14 && 0 > g7(x3, c15)) a18[d12] = x3, a18[n13] = c15, d12 = n13;
          else break a;
        }
      }
      return b4;
    }
    function g7(a18, b4) {
      var c15 = a18.sortIndex - b4.sortIndex;
      return 0 !== c15 ? c15 : a18.id - b4.id;
    }
    if ("object" === typeof performance && "function" === typeof performance.now) {
      l12 = performance;
      exports.unstable_now = function() {
        return l12.now();
      };
    } else {
      p11 = Date, q2 = p11.now();
      exports.unstable_now = function() {
        return p11.now() - q2;
      };
    }
    var l12;
    var p11;
    var q2;
    var r7 = [];
    var t20 = [];
    var u4 = 1;
    var v5 = null;
    var y4 = 3;
    var z2 = false;
    var A4 = false;
    var B4 = false;
    var D5 = "function" === typeof setTimeout ? setTimeout : null;
    var E6 = "function" === typeof clearTimeout ? clearTimeout : null;
    var F4 = "undefined" !== typeof setImmediate ? setImmediate : null;
    "undefined" !== typeof navigator && void 0 !== navigator.scheduling && void 0 !== navigator.scheduling.isInputPending && navigator.scheduling.isInputPending.bind(navigator.scheduling);
    function G3(a18) {
      for (var b4 = h6(t20); null !== b4; ) {
        if (null === b4.callback) k5(t20);
        else if (b4.startTime <= a18) k5(t20), b4.sortIndex = b4.expirationTime, f15(r7, b4);
        else break;
        b4 = h6(t20);
      }
    }
    function H4(a18) {
      B4 = false;
      G3(a18);
      if (!A4) if (null !== h6(r7)) A4 = true, I6(J2);
      else {
        var b4 = h6(t20);
        null !== b4 && K3(H4, b4.startTime - a18);
      }
    }
    function J2(a18, b4) {
      A4 = false;
      B4 && (B4 = false, E6(L3), L3 = -1);
      z2 = true;
      var c15 = y4;
      try {
        G3(b4);
        for (v5 = h6(r7); null !== v5 && (!(v5.expirationTime > b4) || a18 && !M6()); ) {
          var d12 = v5.callback;
          if ("function" === typeof d12) {
            v5.callback = null;
            y4 = v5.priorityLevel;
            var e14 = d12(v5.expirationTime <= b4);
            b4 = exports.unstable_now();
            "function" === typeof e14 ? v5.callback = e14 : v5 === h6(r7) && k5(r7);
            G3(b4);
          } else k5(r7);
          v5 = h6(r7);
        }
        if (null !== v5) var w11 = true;
        else {
          var m12 = h6(t20);
          null !== m12 && K3(H4, m12.startTime - b4);
          w11 = false;
        }
        return w11;
      } finally {
        v5 = null, y4 = c15, z2 = false;
      }
    }
    var N4 = false;
    var O5 = null;
    var L3 = -1;
    var P4 = 5;
    var Q4 = -1;
    function M6() {
      return exports.unstable_now() - Q4 < P4 ? false : true;
    }
    function R10() {
      if (null !== O5) {
        var a18 = exports.unstable_now();
        Q4 = a18;
        var b4 = true;
        try {
          b4 = O5(true, a18);
        } finally {
          b4 ? S2() : (N4 = false, O5 = null);
        }
      } else N4 = false;
    }
    var S2;
    if ("function" === typeof F4) S2 = function() {
      F4(R10);
    };
    else if ("undefined" !== typeof MessageChannel) {
      T3 = new MessageChannel(), U3 = T3.port2;
      T3.port1.onmessage = R10;
      S2 = function() {
        U3.postMessage(null);
      };
    } else S2 = function() {
      D5(R10, 0);
    };
    var T3;
    var U3;
    function I6(a18) {
      O5 = a18;
      N4 || (N4 = true, S2());
    }
    function K3(a18, b4) {
      L3 = D5(function() {
        a18(exports.unstable_now());
      }, b4);
    }
    exports.unstable_IdlePriority = 5;
    exports.unstable_ImmediatePriority = 1;
    exports.unstable_LowPriority = 4;
    exports.unstable_NormalPriority = 3;
    exports.unstable_Profiling = null;
    exports.unstable_UserBlockingPriority = 2;
    exports.unstable_cancelCallback = function(a18) {
      a18.callback = null;
    };
    exports.unstable_continueExecution = function() {
      A4 || z2 || (A4 = true, I6(J2));
    };
    exports.unstable_forceFrameRate = function(a18) {
      0 > a18 || 125 < a18 ? console.error("forceFrameRate takes a positive int between 0 and 125, forcing frame rates higher than 125 fps is not supported") : P4 = 0 < a18 ? Math.floor(1e3 / a18) : 5;
    };
    exports.unstable_getCurrentPriorityLevel = function() {
      return y4;
    };
    exports.unstable_getFirstCallbackNode = function() {
      return h6(r7);
    };
    exports.unstable_next = function(a18) {
      switch (y4) {
        case 1:
        case 2:
        case 3:
          var b4 = 3;
          break;
        default:
          b4 = y4;
      }
      var c15 = y4;
      y4 = b4;
      try {
        return a18();
      } finally {
        y4 = c15;
      }
    };
    exports.unstable_pauseExecution = function() {
    };
    exports.unstable_requestPaint = function() {
    };
    exports.unstable_runWithPriority = function(a18, b4) {
      switch (a18) {
        case 1:
        case 2:
        case 3:
        case 4:
        case 5:
          break;
        default:
          a18 = 3;
      }
      var c15 = y4;
      y4 = a18;
      try {
        return b4();
      } finally {
        y4 = c15;
      }
    };
    exports.unstable_scheduleCallback = function(a18, b4, c15) {
      var d12 = exports.unstable_now();
      "object" === typeof c15 && null !== c15 ? (c15 = c15.delay, c15 = "number" === typeof c15 && 0 < c15 ? d12 + c15 : d12) : c15 = d12;
      switch (a18) {
        case 1:
          var e14 = -1;
          break;
        case 2:
          e14 = 250;
          break;
        case 5:
          e14 = 1073741823;
          break;
        case 4:
          e14 = 1e4;
          break;
        default:
          e14 = 5e3;
      }
      e14 = c15 + e14;
      a18 = { id: u4++, callback: b4, priorityLevel: a18, startTime: c15, expirationTime: e14, sortIndex: -1 };
      c15 > d12 ? (a18.sortIndex = c15, f15(t20, a18), null === h6(r7) && a18 === h6(t20) && (B4 ? (E6(L3), L3 = -1) : B4 = true, K3(H4, c15 - d12))) : (a18.sortIndex = e14, f15(r7, a18), A4 || z2 || (A4 = true, I6(J2)));
      return a18;
    };
    exports.unstable_shouldYield = M6;
    exports.unstable_wrapCallback = function(a18) {
      var b4 = y4;
      return function() {
        var c15 = y4;
        y4 = b4;
        try {
          return a18.apply(this, arguments);
        } finally {
          y4 = c15;
        }
      };
    };
  }
});

// node_modules/scheduler/index.js
var require_scheduler = __commonJS({
  "node_modules/scheduler/index.js"(exports, module) {
    "use strict";
    if (true) {
      module.exports = require_scheduler_production_min();
    } else {
      module.exports = null;
    }
  }
});

// node_modules/react-dom/cjs/react-dom.production.min.js
var require_react_dom_production_min = __commonJS({
  "node_modules/react-dom/cjs/react-dom.production.min.js"(exports) {
    "use strict";
    var aa = require_react();
    var ca = require_scheduler();
    function p11(a18) {
      for (var b4 = "https://reactjs.org/docs/error-decoder.html?invariant=" + a18, c15 = 1; c15 < arguments.length; c15++) b4 += "&args[]=" + encodeURIComponent(arguments[c15]);
      return "Minified React error #" + a18 + "; visit " + b4 + " for the full message or use the non-minified dev environment for full errors and additional helpful warnings.";
    }
    var da = /* @__PURE__ */ new Set();
    var ea = {};
    function fa(a18, b4) {
      ha(a18, b4);
      ha(a18 + "Capture", b4);
    }
    function ha(a18, b4) {
      ea[a18] = b4;
      for (a18 = 0; a18 < b4.length; a18++) da.add(b4[a18]);
    }
    var ia = !("undefined" === typeof window || "undefined" === typeof window.document || "undefined" === typeof window.document.createElement);
    var ja = Object.prototype.hasOwnProperty;
    var ka = /^[:A-Z_a-z\u00C0-\u00D6\u00D8-\u00F6\u00F8-\u02FF\u0370-\u037D\u037F-\u1FFF\u200C-\u200D\u2070-\u218F\u2C00-\u2FEF\u3001-\uD7FF\uF900-\uFDCF\uFDF0-\uFFFD][:A-Z_a-z\u00C0-\u00D6\u00D8-\u00F6\u00F8-\u02FF\u0370-\u037D\u037F-\u1FFF\u200C-\u200D\u2070-\u218F\u2C00-\u2FEF\u3001-\uD7FF\uF900-\uFDCF\uFDF0-\uFFFD\-.0-9\u00B7\u0300-\u036F\u203F-\u2040]*$/;
    var la = {};
    var ma = {};
    function oa(a18) {
      if (ja.call(ma, a18)) return true;
      if (ja.call(la, a18)) return false;
      if (ka.test(a18)) return ma[a18] = true;
      la[a18] = true;
      return false;
    }
    function pa(a18, b4, c15, d12) {
      if (null !== c15 && 0 === c15.type) return false;
      switch (typeof b4) {
        case "function":
        case "symbol":
          return true;
        case "boolean":
          if (d12) return false;
          if (null !== c15) return !c15.acceptsBooleans;
          a18 = a18.toLowerCase().slice(0, 5);
          return "data-" !== a18 && "aria-" !== a18;
        default:
          return false;
      }
    }
    function qa(a18, b4, c15, d12) {
      if (null === b4 || "undefined" === typeof b4 || pa(a18, b4, c15, d12)) return true;
      if (d12) return false;
      if (null !== c15) switch (c15.type) {
        case 3:
          return !b4;
        case 4:
          return false === b4;
        case 5:
          return isNaN(b4);
        case 6:
          return isNaN(b4) || 1 > b4;
      }
      return false;
    }
    function v5(a18, b4, c15, d12, e14, f15, g7) {
      this.acceptsBooleans = 2 === b4 || 3 === b4 || 4 === b4;
      this.attributeName = d12;
      this.attributeNamespace = e14;
      this.mustUseProperty = c15;
      this.propertyName = a18;
      this.type = b4;
      this.sanitizeURL = f15;
      this.removeEmptyString = g7;
    }
    var z2 = {};
    "children dangerouslySetInnerHTML defaultValue defaultChecked innerHTML suppressContentEditableWarning suppressHydrationWarning style".split(" ").forEach(function(a18) {
      z2[a18] = new v5(a18, 0, false, a18, null, false, false);
    });
    [["acceptCharset", "accept-charset"], ["className", "class"], ["htmlFor", "for"], ["httpEquiv", "http-equiv"]].forEach(function(a18) {
      var b4 = a18[0];
      z2[b4] = new v5(b4, 1, false, a18[1], null, false, false);
    });
    ["contentEditable", "draggable", "spellCheck", "value"].forEach(function(a18) {
      z2[a18] = new v5(a18, 2, false, a18.toLowerCase(), null, false, false);
    });
    ["autoReverse", "externalResourcesRequired", "focusable", "preserveAlpha"].forEach(function(a18) {
      z2[a18] = new v5(a18, 2, false, a18, null, false, false);
    });
    "allowFullScreen async autoFocus autoPlay controls default defer disabled disablePictureInPicture disableRemotePlayback formNoValidate hidden loop noModule noValidate open playsInline readOnly required reversed scoped seamless itemScope".split(" ").forEach(function(a18) {
      z2[a18] = new v5(a18, 3, false, a18.toLowerCase(), null, false, false);
    });
    ["checked", "multiple", "muted", "selected"].forEach(function(a18) {
      z2[a18] = new v5(a18, 3, true, a18, null, false, false);
    });
    ["capture", "download"].forEach(function(a18) {
      z2[a18] = new v5(a18, 4, false, a18, null, false, false);
    });
    ["cols", "rows", "size", "span"].forEach(function(a18) {
      z2[a18] = new v5(a18, 6, false, a18, null, false, false);
    });
    ["rowSpan", "start"].forEach(function(a18) {
      z2[a18] = new v5(a18, 5, false, a18.toLowerCase(), null, false, false);
    });
    var ra = /[\-:]([a-z])/g;
    function sa(a18) {
      return a18[1].toUpperCase();
    }
    "accent-height alignment-baseline arabic-form baseline-shift cap-height clip-path clip-rule color-interpolation color-interpolation-filters color-profile color-rendering dominant-baseline enable-background fill-opacity fill-rule flood-color flood-opacity font-family font-size font-size-adjust font-stretch font-style font-variant font-weight glyph-name glyph-orientation-horizontal glyph-orientation-vertical horiz-adv-x horiz-origin-x image-rendering letter-spacing lighting-color marker-end marker-mid marker-start overline-position overline-thickness paint-order panose-1 pointer-events rendering-intent shape-rendering stop-color stop-opacity strikethrough-position strikethrough-thickness stroke-dasharray stroke-dashoffset stroke-linecap stroke-linejoin stroke-miterlimit stroke-opacity stroke-width text-anchor text-decoration text-rendering underline-position underline-thickness unicode-bidi unicode-range units-per-em v-alphabetic v-hanging v-ideographic v-mathematical vector-effect vert-adv-y vert-origin-x vert-origin-y word-spacing writing-mode xmlns:xlink x-height".split(" ").forEach(function(a18) {
      var b4 = a18.replace(
        ra,
        sa
      );
      z2[b4] = new v5(b4, 1, false, a18, null, false, false);
    });
    "xlink:actuate xlink:arcrole xlink:role xlink:show xlink:title xlink:type".split(" ").forEach(function(a18) {
      var b4 = a18.replace(ra, sa);
      z2[b4] = new v5(b4, 1, false, a18, "http://www.w3.org/1999/xlink", false, false);
    });
    ["xml:base", "xml:lang", "xml:space"].forEach(function(a18) {
      var b4 = a18.replace(ra, sa);
      z2[b4] = new v5(b4, 1, false, a18, "http://www.w3.org/XML/1998/namespace", false, false);
    });
    ["tabIndex", "crossOrigin"].forEach(function(a18) {
      z2[a18] = new v5(a18, 1, false, a18.toLowerCase(), null, false, false);
    });
    z2.xlinkHref = new v5("xlinkHref", 1, false, "xlink:href", "http://www.w3.org/1999/xlink", true, false);
    ["src", "href", "action", "formAction"].forEach(function(a18) {
      z2[a18] = new v5(a18, 1, false, a18.toLowerCase(), null, true, true);
    });
    function ta(a18, b4, c15, d12) {
      var e14 = z2.hasOwnProperty(b4) ? z2[b4] : null;
      if (null !== e14 ? 0 !== e14.type : d12 || !(2 < b4.length) || "o" !== b4[0] && "O" !== b4[0] || "n" !== b4[1] && "N" !== b4[1]) qa(b4, c15, e14, d12) && (c15 = null), d12 || null === e14 ? oa(b4) && (null === c15 ? a18.removeAttribute(b4) : a18.setAttribute(b4, "" + c15)) : e14.mustUseProperty ? a18[e14.propertyName] = null === c15 ? 3 === e14.type ? false : "" : c15 : (b4 = e14.attributeName, d12 = e14.attributeNamespace, null === c15 ? a18.removeAttribute(b4) : (e14 = e14.type, c15 = 3 === e14 || 4 === e14 && true === c15 ? "" : "" + c15, d12 ? a18.setAttributeNS(d12, b4, c15) : a18.setAttribute(b4, c15)));
    }
    var ua = aa.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED;
    var va = Symbol.for("react.element");
    var wa = Symbol.for("react.portal");
    var ya = Symbol.for("react.fragment");
    var za = Symbol.for("react.strict_mode");
    var Aa = Symbol.for("react.profiler");
    var Ba = Symbol.for("react.provider");
    var Ca = Symbol.for("react.context");
    var Da = Symbol.for("react.forward_ref");
    var Ea = Symbol.for("react.suspense");
    var Fa = Symbol.for("react.suspense_list");
    var Ga = Symbol.for("react.memo");
    var Ha = Symbol.for("react.lazy");
    Symbol.for("react.scope");
    Symbol.for("react.debug_trace_mode");
    var Ia = Symbol.for("react.offscreen");
    Symbol.for("react.legacy_hidden");
    Symbol.for("react.cache");
    Symbol.for("react.tracing_marker");
    var Ja = Symbol.iterator;
    function Ka(a18) {
      if (null === a18 || "object" !== typeof a18) return null;
      a18 = Ja && a18[Ja] || a18["@@iterator"];
      return "function" === typeof a18 ? a18 : null;
    }
    var A4 = Object.assign;
    var La;
    function Ma(a18) {
      if (void 0 === La) try {
        throw Error();
      } catch (c15) {
        var b4 = c15.stack.trim().match(/\n( *(at )?)/);
        La = b4 && b4[1] || "";
      }
      return "\n" + La + a18;
    }
    var Na = false;
    function Oa(a18, b4) {
      if (!a18 || Na) return "";
      Na = true;
      var c15 = Error.prepareStackTrace;
      Error.prepareStackTrace = void 0;
      try {
        if (b4) if (b4 = function() {
          throw Error();
        }, Object.defineProperty(b4.prototype, "props", { set: function() {
          throw Error();
        } }), "object" === typeof Reflect && Reflect.construct) {
          try {
            Reflect.construct(b4, []);
          } catch (l12) {
            var d12 = l12;
          }
          Reflect.construct(a18, [], b4);
        } else {
          try {
            b4.call();
          } catch (l12) {
            d12 = l12;
          }
          a18.call(b4.prototype);
        }
        else {
          try {
            throw Error();
          } catch (l12) {
            d12 = l12;
          }
          a18();
        }
      } catch (l12) {
        if (l12 && d12 && "string" === typeof l12.stack) {
          for (var e14 = l12.stack.split("\n"), f15 = d12.stack.split("\n"), g7 = e14.length - 1, h6 = f15.length - 1; 1 <= g7 && 0 <= h6 && e14[g7] !== f15[h6]; ) h6--;
          for (; 1 <= g7 && 0 <= h6; g7--, h6--) if (e14[g7] !== f15[h6]) {
            if (1 !== g7 || 1 !== h6) {
              do
                if (g7--, h6--, 0 > h6 || e14[g7] !== f15[h6]) {
                  var k5 = "\n" + e14[g7].replace(" at new ", " at ");
                  a18.displayName && k5.includes("<anonymous>") && (k5 = k5.replace("<anonymous>", a18.displayName));
                  return k5;
                }
              while (1 <= g7 && 0 <= h6);
            }
            break;
          }
        }
      } finally {
        Na = false, Error.prepareStackTrace = c15;
      }
      return (a18 = a18 ? a18.displayName || a18.name : "") ? Ma(a18) : "";
    }
    function Pa(a18) {
      switch (a18.tag) {
        case 5:
          return Ma(a18.type);
        case 16:
          return Ma("Lazy");
        case 13:
          return Ma("Suspense");
        case 19:
          return Ma("SuspenseList");
        case 0:
        case 2:
        case 15:
          return a18 = Oa(a18.type, false), a18;
        case 11:
          return a18 = Oa(a18.type.render, false), a18;
        case 1:
          return a18 = Oa(a18.type, true), a18;
        default:
          return "";
      }
    }
    function Qa(a18) {
      if (null == a18) return null;
      if ("function" === typeof a18) return a18.displayName || a18.name || null;
      if ("string" === typeof a18) return a18;
      switch (a18) {
        case ya:
          return "Fragment";
        case wa:
          return "Portal";
        case Aa:
          return "Profiler";
        case za:
          return "StrictMode";
        case Ea:
          return "Suspense";
        case Fa:
          return "SuspenseList";
      }
      if ("object" === typeof a18) switch (a18.$$typeof) {
        case Ca:
          return (a18.displayName || "Context") + ".Consumer";
        case Ba:
          return (a18._context.displayName || "Context") + ".Provider";
        case Da:
          var b4 = a18.render;
          a18 = a18.displayName;
          a18 || (a18 = b4.displayName || b4.name || "", a18 = "" !== a18 ? "ForwardRef(" + a18 + ")" : "ForwardRef");
          return a18;
        case Ga:
          return b4 = a18.displayName || null, null !== b4 ? b4 : Qa(a18.type) || "Memo";
        case Ha:
          b4 = a18._payload;
          a18 = a18._init;
          try {
            return Qa(a18(b4));
          } catch (c15) {
          }
      }
      return null;
    }
    function Ra(a18) {
      var b4 = a18.type;
      switch (a18.tag) {
        case 24:
          return "Cache";
        case 9:
          return (b4.displayName || "Context") + ".Consumer";
        case 10:
          return (b4._context.displayName || "Context") + ".Provider";
        case 18:
          return "DehydratedFragment";
        case 11:
          return a18 = b4.render, a18 = a18.displayName || a18.name || "", b4.displayName || ("" !== a18 ? "ForwardRef(" + a18 + ")" : "ForwardRef");
        case 7:
          return "Fragment";
        case 5:
          return b4;
        case 4:
          return "Portal";
        case 3:
          return "Root";
        case 6:
          return "Text";
        case 16:
          return Qa(b4);
        case 8:
          return b4 === za ? "StrictMode" : "Mode";
        case 22:
          return "Offscreen";
        case 12:
          return "Profiler";
        case 21:
          return "Scope";
        case 13:
          return "Suspense";
        case 19:
          return "SuspenseList";
        case 25:
          return "TracingMarker";
        case 1:
        case 0:
        case 17:
        case 2:
        case 14:
        case 15:
          if ("function" === typeof b4) return b4.displayName || b4.name || null;
          if ("string" === typeof b4) return b4;
      }
      return null;
    }
    function Sa(a18) {
      switch (typeof a18) {
        case "boolean":
        case "number":
        case "string":
        case "undefined":
          return a18;
        case "object":
          return a18;
        default:
          return "";
      }
    }
    function Ta(a18) {
      var b4 = a18.type;
      return (a18 = a18.nodeName) && "input" === a18.toLowerCase() && ("checkbox" === b4 || "radio" === b4);
    }
    function Ua(a18) {
      var b4 = Ta(a18) ? "checked" : "value", c15 = Object.getOwnPropertyDescriptor(a18.constructor.prototype, b4), d12 = "" + a18[b4];
      if (!a18.hasOwnProperty(b4) && "undefined" !== typeof c15 && "function" === typeof c15.get && "function" === typeof c15.set) {
        var e14 = c15.get, f15 = c15.set;
        Object.defineProperty(a18, b4, { configurable: true, get: function() {
          return e14.call(this);
        }, set: function(a19) {
          d12 = "" + a19;
          f15.call(this, a19);
        } });
        Object.defineProperty(a18, b4, { enumerable: c15.enumerable });
        return { getValue: function() {
          return d12;
        }, setValue: function(a19) {
          d12 = "" + a19;
        }, stopTracking: function() {
          a18._valueTracker = null;
          delete a18[b4];
        } };
      }
    }
    function Va(a18) {
      a18._valueTracker || (a18._valueTracker = Ua(a18));
    }
    function Wa(a18) {
      if (!a18) return false;
      var b4 = a18._valueTracker;
      if (!b4) return true;
      var c15 = b4.getValue();
      var d12 = "";
      a18 && (d12 = Ta(a18) ? a18.checked ? "true" : "false" : a18.value);
      a18 = d12;
      return a18 !== c15 ? (b4.setValue(a18), true) : false;
    }
    function Xa(a18) {
      a18 = a18 || ("undefined" !== typeof document ? document : void 0);
      if ("undefined" === typeof a18) return null;
      try {
        return a18.activeElement || a18.body;
      } catch (b4) {
        return a18.body;
      }
    }
    function Ya(a18, b4) {
      var c15 = b4.checked;
      return A4({}, b4, { defaultChecked: void 0, defaultValue: void 0, value: void 0, checked: null != c15 ? c15 : a18._wrapperState.initialChecked });
    }
    function Za(a18, b4) {
      var c15 = null == b4.defaultValue ? "" : b4.defaultValue, d12 = null != b4.checked ? b4.checked : b4.defaultChecked;
      c15 = Sa(null != b4.value ? b4.value : c15);
      a18._wrapperState = { initialChecked: d12, initialValue: c15, controlled: "checkbox" === b4.type || "radio" === b4.type ? null != b4.checked : null != b4.value };
    }
    function ab(a18, b4) {
      b4 = b4.checked;
      null != b4 && ta(a18, "checked", b4, false);
    }
    function bb(a18, b4) {
      ab(a18, b4);
      var c15 = Sa(b4.value), d12 = b4.type;
      if (null != c15) if ("number" === d12) {
        if (0 === c15 && "" === a18.value || a18.value != c15) a18.value = "" + c15;
      } else a18.value !== "" + c15 && (a18.value = "" + c15);
      else if ("submit" === d12 || "reset" === d12) {
        a18.removeAttribute("value");
        return;
      }
      b4.hasOwnProperty("value") ? cb(a18, b4.type, c15) : b4.hasOwnProperty("defaultValue") && cb(a18, b4.type, Sa(b4.defaultValue));
      null == b4.checked && null != b4.defaultChecked && (a18.defaultChecked = !!b4.defaultChecked);
    }
    function db(a18, b4, c15) {
      if (b4.hasOwnProperty("value") || b4.hasOwnProperty("defaultValue")) {
        var d12 = b4.type;
        if (!("submit" !== d12 && "reset" !== d12 || void 0 !== b4.value && null !== b4.value)) return;
        b4 = "" + a18._wrapperState.initialValue;
        c15 || b4 === a18.value || (a18.value = b4);
        a18.defaultValue = b4;
      }
      c15 = a18.name;
      "" !== c15 && (a18.name = "");
      a18.defaultChecked = !!a18._wrapperState.initialChecked;
      "" !== c15 && (a18.name = c15);
    }
    function cb(a18, b4, c15) {
      if ("number" !== b4 || Xa(a18.ownerDocument) !== a18) null == c15 ? a18.defaultValue = "" + a18._wrapperState.initialValue : a18.defaultValue !== "" + c15 && (a18.defaultValue = "" + c15);
    }
    var eb = Array.isArray;
    function fb(a18, b4, c15, d12) {
      a18 = a18.options;
      if (b4) {
        b4 = {};
        for (var e14 = 0; e14 < c15.length; e14++) b4["$" + c15[e14]] = true;
        for (c15 = 0; c15 < a18.length; c15++) e14 = b4.hasOwnProperty("$" + a18[c15].value), a18[c15].selected !== e14 && (a18[c15].selected = e14), e14 && d12 && (a18[c15].defaultSelected = true);
      } else {
        c15 = "" + Sa(c15);
        b4 = null;
        for (e14 = 0; e14 < a18.length; e14++) {
          if (a18[e14].value === c15) {
            a18[e14].selected = true;
            d12 && (a18[e14].defaultSelected = true);
            return;
          }
          null !== b4 || a18[e14].disabled || (b4 = a18[e14]);
        }
        null !== b4 && (b4.selected = true);
      }
    }
    function gb(a18, b4) {
      if (null != b4.dangerouslySetInnerHTML) throw Error(p11(91));
      return A4({}, b4, { value: void 0, defaultValue: void 0, children: "" + a18._wrapperState.initialValue });
    }
    function hb(a18, b4) {
      var c15 = b4.value;
      if (null == c15) {
        c15 = b4.children;
        b4 = b4.defaultValue;
        if (null != c15) {
          if (null != b4) throw Error(p11(92));
          if (eb(c15)) {
            if (1 < c15.length) throw Error(p11(93));
            c15 = c15[0];
          }
          b4 = c15;
        }
        null == b4 && (b4 = "");
        c15 = b4;
      }
      a18._wrapperState = { initialValue: Sa(c15) };
    }
    function ib(a18, b4) {
      var c15 = Sa(b4.value), d12 = Sa(b4.defaultValue);
      null != c15 && (c15 = "" + c15, c15 !== a18.value && (a18.value = c15), null == b4.defaultValue && a18.defaultValue !== c15 && (a18.defaultValue = c15));
      null != d12 && (a18.defaultValue = "" + d12);
    }
    function jb(a18) {
      var b4 = a18.textContent;
      b4 === a18._wrapperState.initialValue && "" !== b4 && null !== b4 && (a18.value = b4);
    }
    function kb(a18) {
      switch (a18) {
        case "svg":
          return "http://www.w3.org/2000/svg";
        case "math":
          return "http://www.w3.org/1998/Math/MathML";
        default:
          return "http://www.w3.org/1999/xhtml";
      }
    }
    function lb(a18, b4) {
      return null == a18 || "http://www.w3.org/1999/xhtml" === a18 ? kb(b4) : "http://www.w3.org/2000/svg" === a18 && "foreignObject" === b4 ? "http://www.w3.org/1999/xhtml" : a18;
    }
    var mb;
    var nb = function(a18) {
      return "undefined" !== typeof MSApp && MSApp.execUnsafeLocalFunction ? function(b4, c15, d12, e14) {
        MSApp.execUnsafeLocalFunction(function() {
          return a18(b4, c15, d12, e14);
        });
      } : a18;
    }(function(a18, b4) {
      if ("http://www.w3.org/2000/svg" !== a18.namespaceURI || "innerHTML" in a18) a18.innerHTML = b4;
      else {
        mb = mb || document.createElement("div");
        mb.innerHTML = "<svg>" + b4.valueOf().toString() + "</svg>";
        for (b4 = mb.firstChild; a18.firstChild; ) a18.removeChild(a18.firstChild);
        for (; b4.firstChild; ) a18.appendChild(b4.firstChild);
      }
    });
    function ob(a18, b4) {
      if (b4) {
        var c15 = a18.firstChild;
        if (c15 && c15 === a18.lastChild && 3 === c15.nodeType) {
          c15.nodeValue = b4;
          return;
        }
      }
      a18.textContent = b4;
    }
    var pb = {
      animationIterationCount: true,
      aspectRatio: true,
      borderImageOutset: true,
      borderImageSlice: true,
      borderImageWidth: true,
      boxFlex: true,
      boxFlexGroup: true,
      boxOrdinalGroup: true,
      columnCount: true,
      columns: true,
      flex: true,
      flexGrow: true,
      flexPositive: true,
      flexShrink: true,
      flexNegative: true,
      flexOrder: true,
      gridArea: true,
      gridRow: true,
      gridRowEnd: true,
      gridRowSpan: true,
      gridRowStart: true,
      gridColumn: true,
      gridColumnEnd: true,
      gridColumnSpan: true,
      gridColumnStart: true,
      fontWeight: true,
      lineClamp: true,
      lineHeight: true,
      opacity: true,
      order: true,
      orphans: true,
      tabSize: true,
      widows: true,
      zIndex: true,
      zoom: true,
      fillOpacity: true,
      floodOpacity: true,
      stopOpacity: true,
      strokeDasharray: true,
      strokeDashoffset: true,
      strokeMiterlimit: true,
      strokeOpacity: true,
      strokeWidth: true
    };
    var qb = ["Webkit", "ms", "Moz", "O"];
    Object.keys(pb).forEach(function(a18) {
      qb.forEach(function(b4) {
        b4 = b4 + a18.charAt(0).toUpperCase() + a18.substring(1);
        pb[b4] = pb[a18];
      });
    });
    function rb(a18, b4, c15) {
      return null == b4 || "boolean" === typeof b4 || "" === b4 ? "" : c15 || "number" !== typeof b4 || 0 === b4 || pb.hasOwnProperty(a18) && pb[a18] ? ("" + b4).trim() : b4 + "px";
    }
    function sb(a18, b4) {
      a18 = a18.style;
      for (var c15 in b4) if (b4.hasOwnProperty(c15)) {
        var d12 = 0 === c15.indexOf("--"), e14 = rb(c15, b4[c15], d12);
        "float" === c15 && (c15 = "cssFloat");
        d12 ? a18.setProperty(c15, e14) : a18[c15] = e14;
      }
    }
    var tb = A4({ menuitem: true }, { area: true, base: true, br: true, col: true, embed: true, hr: true, img: true, input: true, keygen: true, link: true, meta: true, param: true, source: true, track: true, wbr: true });
    function ub(a18, b4) {
      if (b4) {
        if (tb[a18] && (null != b4.children || null != b4.dangerouslySetInnerHTML)) throw Error(p11(137, a18));
        if (null != b4.dangerouslySetInnerHTML) {
          if (null != b4.children) throw Error(p11(60));
          if ("object" !== typeof b4.dangerouslySetInnerHTML || !("__html" in b4.dangerouslySetInnerHTML)) throw Error(p11(61));
        }
        if (null != b4.style && "object" !== typeof b4.style) throw Error(p11(62));
      }
    }
    function vb(a18, b4) {
      if (-1 === a18.indexOf("-")) return "string" === typeof b4.is;
      switch (a18) {
        case "annotation-xml":
        case "color-profile":
        case "font-face":
        case "font-face-src":
        case "font-face-uri":
        case "font-face-format":
        case "font-face-name":
        case "missing-glyph":
          return false;
        default:
          return true;
      }
    }
    var wb = null;
    function xb(a18) {
      a18 = a18.target || a18.srcElement || window;
      a18.correspondingUseElement && (a18 = a18.correspondingUseElement);
      return 3 === a18.nodeType ? a18.parentNode : a18;
    }
    var yb = null;
    var zb = null;
    var Ab = null;
    function Bb(a18) {
      if (a18 = Cb(a18)) {
        if ("function" !== typeof yb) throw Error(p11(280));
        var b4 = a18.stateNode;
        b4 && (b4 = Db(b4), yb(a18.stateNode, a18.type, b4));
      }
    }
    function Eb(a18) {
      zb ? Ab ? Ab.push(a18) : Ab = [a18] : zb = a18;
    }
    function Fb() {
      if (zb) {
        var a18 = zb, b4 = Ab;
        Ab = zb = null;
        Bb(a18);
        if (b4) for (a18 = 0; a18 < b4.length; a18++) Bb(b4[a18]);
      }
    }
    function Gb(a18, b4) {
      return a18(b4);
    }
    function Hb() {
    }
    var Ib = false;
    function Jb(a18, b4, c15) {
      if (Ib) return a18(b4, c15);
      Ib = true;
      try {
        return Gb(a18, b4, c15);
      } finally {
        if (Ib = false, null !== zb || null !== Ab) Hb(), Fb();
      }
    }
    function Kb(a18, b4) {
      var c15 = a18.stateNode;
      if (null === c15) return null;
      var d12 = Db(c15);
      if (null === d12) return null;
      c15 = d12[b4];
      a: switch (b4) {
        case "onClick":
        case "onClickCapture":
        case "onDoubleClick":
        case "onDoubleClickCapture":
        case "onMouseDown":
        case "onMouseDownCapture":
        case "onMouseMove":
        case "onMouseMoveCapture":
        case "onMouseUp":
        case "onMouseUpCapture":
        case "onMouseEnter":
          (d12 = !d12.disabled) || (a18 = a18.type, d12 = !("button" === a18 || "input" === a18 || "select" === a18 || "textarea" === a18));
          a18 = !d12;
          break a;
        default:
          a18 = false;
      }
      if (a18) return null;
      if (c15 && "function" !== typeof c15) throw Error(p11(231, b4, typeof c15));
      return c15;
    }
    var Lb = false;
    if (ia) try {
      Mb = {};
      Object.defineProperty(Mb, "passive", { get: function() {
        Lb = true;
      } });
      window.addEventListener("test", Mb, Mb);
      window.removeEventListener("test", Mb, Mb);
    } catch (a18) {
      Lb = false;
    }
    var Mb;
    function Nb(a18, b4, c15, d12, e14, f15, g7, h6, k5) {
      var l12 = Array.prototype.slice.call(arguments, 3);
      try {
        b4.apply(c15, l12);
      } catch (m12) {
        this.onError(m12);
      }
    }
    var Ob = false;
    var Pb = null;
    var Qb = false;
    var Rb = null;
    var Sb = { onError: function(a18) {
      Ob = true;
      Pb = a18;
    } };
    function Tb(a18, b4, c15, d12, e14, f15, g7, h6, k5) {
      Ob = false;
      Pb = null;
      Nb.apply(Sb, arguments);
    }
    function Ub(a18, b4, c15, d12, e14, f15, g7, h6, k5) {
      Tb.apply(this, arguments);
      if (Ob) {
        if (Ob) {
          var l12 = Pb;
          Ob = false;
          Pb = null;
        } else throw Error(p11(198));
        Qb || (Qb = true, Rb = l12);
      }
    }
    function Vb(a18) {
      var b4 = a18, c15 = a18;
      if (a18.alternate) for (; b4.return; ) b4 = b4.return;
      else {
        a18 = b4;
        do
          b4 = a18, 0 !== (b4.flags & 4098) && (c15 = b4.return), a18 = b4.return;
        while (a18);
      }
      return 3 === b4.tag ? c15 : null;
    }
    function Wb(a18) {
      if (13 === a18.tag) {
        var b4 = a18.memoizedState;
        null === b4 && (a18 = a18.alternate, null !== a18 && (b4 = a18.memoizedState));
        if (null !== b4) return b4.dehydrated;
      }
      return null;
    }
    function Xb(a18) {
      if (Vb(a18) !== a18) throw Error(p11(188));
    }
    function Yb(a18) {
      var b4 = a18.alternate;
      if (!b4) {
        b4 = Vb(a18);
        if (null === b4) throw Error(p11(188));
        return b4 !== a18 ? null : a18;
      }
      for (var c15 = a18, d12 = b4; ; ) {
        var e14 = c15.return;
        if (null === e14) break;
        var f15 = e14.alternate;
        if (null === f15) {
          d12 = e14.return;
          if (null !== d12) {
            c15 = d12;
            continue;
          }
          break;
        }
        if (e14.child === f15.child) {
          for (f15 = e14.child; f15; ) {
            if (f15 === c15) return Xb(e14), a18;
            if (f15 === d12) return Xb(e14), b4;
            f15 = f15.sibling;
          }
          throw Error(p11(188));
        }
        if (c15.return !== d12.return) c15 = e14, d12 = f15;
        else {
          for (var g7 = false, h6 = e14.child; h6; ) {
            if (h6 === c15) {
              g7 = true;
              c15 = e14;
              d12 = f15;
              break;
            }
            if (h6 === d12) {
              g7 = true;
              d12 = e14;
              c15 = f15;
              break;
            }
            h6 = h6.sibling;
          }
          if (!g7) {
            for (h6 = f15.child; h6; ) {
              if (h6 === c15) {
                g7 = true;
                c15 = f15;
                d12 = e14;
                break;
              }
              if (h6 === d12) {
                g7 = true;
                d12 = f15;
                c15 = e14;
                break;
              }
              h6 = h6.sibling;
            }
            if (!g7) throw Error(p11(189));
          }
        }
        if (c15.alternate !== d12) throw Error(p11(190));
      }
      if (3 !== c15.tag) throw Error(p11(188));
      return c15.stateNode.current === c15 ? a18 : b4;
    }
    function Zb(a18) {
      a18 = Yb(a18);
      return null !== a18 ? $b(a18) : null;
    }
    function $b(a18) {
      if (5 === a18.tag || 6 === a18.tag) return a18;
      for (a18 = a18.child; null !== a18; ) {
        var b4 = $b(a18);
        if (null !== b4) return b4;
        a18 = a18.sibling;
      }
      return null;
    }
    var ac = ca.unstable_scheduleCallback;
    var bc = ca.unstable_cancelCallback;
    var cc = ca.unstable_shouldYield;
    var dc = ca.unstable_requestPaint;
    var B4 = ca.unstable_now;
    var ec = ca.unstable_getCurrentPriorityLevel;
    var fc = ca.unstable_ImmediatePriority;
    var gc = ca.unstable_UserBlockingPriority;
    var hc = ca.unstable_NormalPriority;
    var ic = ca.unstable_LowPriority;
    var jc = ca.unstable_IdlePriority;
    var kc = null;
    var lc = null;
    function mc(a18) {
      if (lc && "function" === typeof lc.onCommitFiberRoot) try {
        lc.onCommitFiberRoot(kc, a18, void 0, 128 === (a18.current.flags & 128));
      } catch (b4) {
      }
    }
    var oc = Math.clz32 ? Math.clz32 : nc;
    var pc43 = Math.log;
    var qc = Math.LN2;
    function nc(a18) {
      a18 >>>= 0;
      return 0 === a18 ? 32 : 31 - (pc43(a18) / qc | 0) | 0;
    }
    var rc = 64;
    var sc = 4194304;
    function tc(a18) {
      switch (a18 & -a18) {
        case 1:
          return 1;
        case 2:
          return 2;
        case 4:
          return 4;
        case 8:
          return 8;
        case 16:
          return 16;
        case 32:
          return 32;
        case 64:
        case 128:
        case 256:
        case 512:
        case 1024:
        case 2048:
        case 4096:
        case 8192:
        case 16384:
        case 32768:
        case 65536:
        case 131072:
        case 262144:
        case 524288:
        case 1048576:
        case 2097152:
          return a18 & 4194240;
        case 4194304:
        case 8388608:
        case 16777216:
        case 33554432:
        case 67108864:
          return a18 & 130023424;
        case 134217728:
          return 134217728;
        case 268435456:
          return 268435456;
        case 536870912:
          return 536870912;
        case 1073741824:
          return 1073741824;
        default:
          return a18;
      }
    }
    function uc(a18, b4) {
      var c15 = a18.pendingLanes;
      if (0 === c15) return 0;
      var d12 = 0, e14 = a18.suspendedLanes, f15 = a18.pingedLanes, g7 = c15 & 268435455;
      if (0 !== g7) {
        var h6 = g7 & ~e14;
        0 !== h6 ? d12 = tc(h6) : (f15 &= g7, 0 !== f15 && (d12 = tc(f15)));
      } else g7 = c15 & ~e14, 0 !== g7 ? d12 = tc(g7) : 0 !== f15 && (d12 = tc(f15));
      if (0 === d12) return 0;
      if (0 !== b4 && b4 !== d12 && 0 === (b4 & e14) && (e14 = d12 & -d12, f15 = b4 & -b4, e14 >= f15 || 16 === e14 && 0 !== (f15 & 4194240))) return b4;
      0 !== (d12 & 4) && (d12 |= c15 & 16);
      b4 = a18.entangledLanes;
      if (0 !== b4) for (a18 = a18.entanglements, b4 &= d12; 0 < b4; ) c15 = 31 - oc(b4), e14 = 1 << c15, d12 |= a18[c15], b4 &= ~e14;
      return d12;
    }
    function vc(a18, b4) {
      switch (a18) {
        case 1:
        case 2:
        case 4:
          return b4 + 250;
        case 8:
        case 16:
        case 32:
        case 64:
        case 128:
        case 256:
        case 512:
        case 1024:
        case 2048:
        case 4096:
        case 8192:
        case 16384:
        case 32768:
        case 65536:
        case 131072:
        case 262144:
        case 524288:
        case 1048576:
        case 2097152:
          return b4 + 5e3;
        case 4194304:
        case 8388608:
        case 16777216:
        case 33554432:
        case 67108864:
          return -1;
        case 134217728:
        case 268435456:
        case 536870912:
        case 1073741824:
          return -1;
        default:
          return -1;
      }
    }
    function wc(a18, b4) {
      for (var c15 = a18.suspendedLanes, d12 = a18.pingedLanes, e14 = a18.expirationTimes, f15 = a18.pendingLanes; 0 < f15; ) {
        var g7 = 31 - oc(f15), h6 = 1 << g7, k5 = e14[g7];
        if (-1 === k5) {
          if (0 === (h6 & c15) || 0 !== (h6 & d12)) e14[g7] = vc(h6, b4);
        } else k5 <= b4 && (a18.expiredLanes |= h6);
        f15 &= ~h6;
      }
    }
    function xc(a18) {
      a18 = a18.pendingLanes & -1073741825;
      return 0 !== a18 ? a18 : a18 & 1073741824 ? 1073741824 : 0;
    }
    function yc() {
      var a18 = rc;
      rc <<= 1;
      0 === (rc & 4194240) && (rc = 64);
      return a18;
    }
    function zc(a18) {
      for (var b4 = [], c15 = 0; 31 > c15; c15++) b4.push(a18);
      return b4;
    }
    function Ac(a18, b4, c15) {
      a18.pendingLanes |= b4;
      536870912 !== b4 && (a18.suspendedLanes = 0, a18.pingedLanes = 0);
      a18 = a18.eventTimes;
      b4 = 31 - oc(b4);
      a18[b4] = c15;
    }
    function Bc(a18, b4) {
      var c15 = a18.pendingLanes & ~b4;
      a18.pendingLanes = b4;
      a18.suspendedLanes = 0;
      a18.pingedLanes = 0;
      a18.expiredLanes &= b4;
      a18.mutableReadLanes &= b4;
      a18.entangledLanes &= b4;
      b4 = a18.entanglements;
      var d12 = a18.eventTimes;
      for (a18 = a18.expirationTimes; 0 < c15; ) {
        var e14 = 31 - oc(c15), f15 = 1 << e14;
        b4[e14] = 0;
        d12[e14] = -1;
        a18[e14] = -1;
        c15 &= ~f15;
      }
    }
    function Cc(a18, b4) {
      var c15 = a18.entangledLanes |= b4;
      for (a18 = a18.entanglements; c15; ) {
        var d12 = 31 - oc(c15), e14 = 1 << d12;
        e14 & b4 | a18[d12] & b4 && (a18[d12] |= b4);
        c15 &= ~e14;
      }
    }
    var C5 = 0;
    function Dc(a18) {
      a18 &= -a18;
      return 1 < a18 ? 4 < a18 ? 0 !== (a18 & 268435455) ? 16 : 536870912 : 4 : 1;
    }
    var Ec;
    var Fc;
    var Gc;
    var Hc;
    var Ic;
    var Jc = false;
    var Kc = [];
    var Lc = null;
    var Mc = null;
    var Nc = null;
    var Oc = /* @__PURE__ */ new Map();
    var Pc = /* @__PURE__ */ new Map();
    var Qc = [];
    var Rc = "mousedown mouseup touchcancel touchend touchstart auxclick dblclick pointercancel pointerdown pointerup dragend dragstart drop compositionend compositionstart keydown keypress keyup input textInput copy cut paste click change contextmenu reset submit".split(" ");
    function Sc(a18, b4) {
      switch (a18) {
        case "focusin":
        case "focusout":
          Lc = null;
          break;
        case "dragenter":
        case "dragleave":
          Mc = null;
          break;
        case "mouseover":
        case "mouseout":
          Nc = null;
          break;
        case "pointerover":
        case "pointerout":
          Oc.delete(b4.pointerId);
          break;
        case "gotpointercapture":
        case "lostpointercapture":
          Pc.delete(b4.pointerId);
      }
    }
    function Tc(a18, b4, c15, d12, e14, f15) {
      if (null === a18 || a18.nativeEvent !== f15) return a18 = { blockedOn: b4, domEventName: c15, eventSystemFlags: d12, nativeEvent: f15, targetContainers: [e14] }, null !== b4 && (b4 = Cb(b4), null !== b4 && Fc(b4)), a18;
      a18.eventSystemFlags |= d12;
      b4 = a18.targetContainers;
      null !== e14 && -1 === b4.indexOf(e14) && b4.push(e14);
      return a18;
    }
    function Uc(a18, b4, c15, d12, e14) {
      switch (b4) {
        case "focusin":
          return Lc = Tc(Lc, a18, b4, c15, d12, e14), true;
        case "dragenter":
          return Mc = Tc(Mc, a18, b4, c15, d12, e14), true;
        case "mouseover":
          return Nc = Tc(Nc, a18, b4, c15, d12, e14), true;
        case "pointerover":
          var f15 = e14.pointerId;
          Oc.set(f15, Tc(Oc.get(f15) || null, a18, b4, c15, d12, e14));
          return true;
        case "gotpointercapture":
          return f15 = e14.pointerId, Pc.set(f15, Tc(Pc.get(f15) || null, a18, b4, c15, d12, e14)), true;
      }
      return false;
    }
    function Vc(a18) {
      var b4 = Wc(a18.target);
      if (null !== b4) {
        var c15 = Vb(b4);
        if (null !== c15) {
          if (b4 = c15.tag, 13 === b4) {
            if (b4 = Wb(c15), null !== b4) {
              a18.blockedOn = b4;
              Ic(a18.priority, function() {
                Gc(c15);
              });
              return;
            }
          } else if (3 === b4 && c15.stateNode.current.memoizedState.isDehydrated) {
            a18.blockedOn = 3 === c15.tag ? c15.stateNode.containerInfo : null;
            return;
          }
        }
      }
      a18.blockedOn = null;
    }
    function Xc(a18) {
      if (null !== a18.blockedOn) return false;
      for (var b4 = a18.targetContainers; 0 < b4.length; ) {
        var c15 = Yc(a18.domEventName, a18.eventSystemFlags, b4[0], a18.nativeEvent);
        if (null === c15) {
          c15 = a18.nativeEvent;
          var d12 = new c15.constructor(c15.type, c15);
          wb = d12;
          c15.target.dispatchEvent(d12);
          wb = null;
        } else return b4 = Cb(c15), null !== b4 && Fc(b4), a18.blockedOn = c15, false;
        b4.shift();
      }
      return true;
    }
    function Zc(a18, b4, c15) {
      Xc(a18) && c15.delete(b4);
    }
    function $c() {
      Jc = false;
      null !== Lc && Xc(Lc) && (Lc = null);
      null !== Mc && Xc(Mc) && (Mc = null);
      null !== Nc && Xc(Nc) && (Nc = null);
      Oc.forEach(Zc);
      Pc.forEach(Zc);
    }
    function ad(a18, b4) {
      a18.blockedOn === b4 && (a18.blockedOn = null, Jc || (Jc = true, ca.unstable_scheduleCallback(ca.unstable_NormalPriority, $c)));
    }
    function bd(a18) {
      function b4(b5) {
        return ad(b5, a18);
      }
      if (0 < Kc.length) {
        ad(Kc[0], a18);
        for (var c15 = 1; c15 < Kc.length; c15++) {
          var d12 = Kc[c15];
          d12.blockedOn === a18 && (d12.blockedOn = null);
        }
      }
      null !== Lc && ad(Lc, a18);
      null !== Mc && ad(Mc, a18);
      null !== Nc && ad(Nc, a18);
      Oc.forEach(b4);
      Pc.forEach(b4);
      for (c15 = 0; c15 < Qc.length; c15++) d12 = Qc[c15], d12.blockedOn === a18 && (d12.blockedOn = null);
      for (; 0 < Qc.length && (c15 = Qc[0], null === c15.blockedOn); ) Vc(c15), null === c15.blockedOn && Qc.shift();
    }
    var cd = ua.ReactCurrentBatchConfig;
    var dd = true;
    function ed(a18, b4, c15, d12) {
      var e14 = C5, f15 = cd.transition;
      cd.transition = null;
      try {
        C5 = 1, fd(a18, b4, c15, d12);
      } finally {
        C5 = e14, cd.transition = f15;
      }
    }
    function gd(a18, b4, c15, d12) {
      var e14 = C5, f15 = cd.transition;
      cd.transition = null;
      try {
        C5 = 4, fd(a18, b4, c15, d12);
      } finally {
        C5 = e14, cd.transition = f15;
      }
    }
    function fd(a18, b4, c15, d12) {
      if (dd) {
        var e14 = Yc(a18, b4, c15, d12);
        if (null === e14) hd(a18, b4, d12, id3, c15), Sc(a18, d12);
        else if (Uc(e14, a18, b4, c15, d12)) d12.stopPropagation();
        else if (Sc(a18, d12), b4 & 4 && -1 < Rc.indexOf(a18)) {
          for (; null !== e14; ) {
            var f15 = Cb(e14);
            null !== f15 && Ec(f15);
            f15 = Yc(a18, b4, c15, d12);
            null === f15 && hd(a18, b4, d12, id3, c15);
            if (f15 === e14) break;
            e14 = f15;
          }
          null !== e14 && d12.stopPropagation();
        } else hd(a18, b4, d12, null, c15);
      }
    }
    var id3 = null;
    function Yc(a18, b4, c15, d12) {
      id3 = null;
      a18 = xb(d12);
      a18 = Wc(a18);
      if (null !== a18) if (b4 = Vb(a18), null === b4) a18 = null;
      else if (c15 = b4.tag, 13 === c15) {
        a18 = Wb(b4);
        if (null !== a18) return a18;
        a18 = null;
      } else if (3 === c15) {
        if (b4.stateNode.current.memoizedState.isDehydrated) return 3 === b4.tag ? b4.stateNode.containerInfo : null;
        a18 = null;
      } else b4 !== a18 && (a18 = null);
      id3 = a18;
      return null;
    }
    function jd(a18) {
      switch (a18) {
        case "cancel":
        case "click":
        case "close":
        case "contextmenu":
        case "copy":
        case "cut":
        case "auxclick":
        case "dblclick":
        case "dragend":
        case "dragstart":
        case "drop":
        case "focusin":
        case "focusout":
        case "input":
        case "invalid":
        case "keydown":
        case "keypress":
        case "keyup":
        case "mousedown":
        case "mouseup":
        case "paste":
        case "pause":
        case "play":
        case "pointercancel":
        case "pointerdown":
        case "pointerup":
        case "ratechange":
        case "reset":
        case "resize":
        case "seeked":
        case "submit":
        case "touchcancel":
        case "touchend":
        case "touchstart":
        case "volumechange":
        case "change":
        case "selectionchange":
        case "textInput":
        case "compositionstart":
        case "compositionend":
        case "compositionupdate":
        case "beforeblur":
        case "afterblur":
        case "beforeinput":
        case "blur":
        case "fullscreenchange":
        case "focus":
        case "hashchange":
        case "popstate":
        case "select":
        case "selectstart":
          return 1;
        case "drag":
        case "dragenter":
        case "dragexit":
        case "dragleave":
        case "dragover":
        case "mousemove":
        case "mouseout":
        case "mouseover":
        case "pointermove":
        case "pointerout":
        case "pointerover":
        case "scroll":
        case "toggle":
        case "touchmove":
        case "wheel":
        case "mouseenter":
        case "mouseleave":
        case "pointerenter":
        case "pointerleave":
          return 4;
        case "message":
          switch (ec()) {
            case fc:
              return 1;
            case gc:
              return 4;
            case hc:
            case ic:
              return 16;
            case jc:
              return 536870912;
            default:
              return 16;
          }
        default:
          return 16;
      }
    }
    var kd = null;
    var ld = null;
    var md = null;
    function nd() {
      if (md) return md;
      var a18, b4 = ld, c15 = b4.length, d12, e14 = "value" in kd ? kd.value : kd.textContent, f15 = e14.length;
      for (a18 = 0; a18 < c15 && b4[a18] === e14[a18]; a18++) ;
      var g7 = c15 - a18;
      for (d12 = 1; d12 <= g7 && b4[c15 - d12] === e14[f15 - d12]; d12++) ;
      return md = e14.slice(a18, 1 < d12 ? 1 - d12 : void 0);
    }
    function od(a18) {
      var b4 = a18.keyCode;
      "charCode" in a18 ? (a18 = a18.charCode, 0 === a18 && 13 === b4 && (a18 = 13)) : a18 = b4;
      10 === a18 && (a18 = 13);
      return 32 <= a18 || 13 === a18 ? a18 : 0;
    }
    function pd() {
      return true;
    }
    function qd() {
      return false;
    }
    function rd(a18) {
      function b4(b5, d12, e14, f15, g7) {
        this._reactName = b5;
        this._targetInst = e14;
        this.type = d12;
        this.nativeEvent = f15;
        this.target = g7;
        this.currentTarget = null;
        for (var c15 in a18) a18.hasOwnProperty(c15) && (b5 = a18[c15], this[c15] = b5 ? b5(f15) : f15[c15]);
        this.isDefaultPrevented = (null != f15.defaultPrevented ? f15.defaultPrevented : false === f15.returnValue) ? pd : qd;
        this.isPropagationStopped = qd;
        return this;
      }
      A4(b4.prototype, { preventDefault: function() {
        this.defaultPrevented = true;
        var a19 = this.nativeEvent;
        a19 && (a19.preventDefault ? a19.preventDefault() : "unknown" !== typeof a19.returnValue && (a19.returnValue = false), this.isDefaultPrevented = pd);
      }, stopPropagation: function() {
        var a19 = this.nativeEvent;
        a19 && (a19.stopPropagation ? a19.stopPropagation() : "unknown" !== typeof a19.cancelBubble && (a19.cancelBubble = true), this.isPropagationStopped = pd);
      }, persist: function() {
      }, isPersistent: pd });
      return b4;
    }
    var sd = { eventPhase: 0, bubbles: 0, cancelable: 0, timeStamp: function(a18) {
      return a18.timeStamp || Date.now();
    }, defaultPrevented: 0, isTrusted: 0 };
    var td = rd(sd);
    var ud = A4({}, sd, { view: 0, detail: 0 });
    var vd = rd(ud);
    var wd;
    var xd;
    var yd;
    var Ad = A4({}, ud, { screenX: 0, screenY: 0, clientX: 0, clientY: 0, pageX: 0, pageY: 0, ctrlKey: 0, shiftKey: 0, altKey: 0, metaKey: 0, getModifierState: zd, button: 0, buttons: 0, relatedTarget: function(a18) {
      return void 0 === a18.relatedTarget ? a18.fromElement === a18.srcElement ? a18.toElement : a18.fromElement : a18.relatedTarget;
    }, movementX: function(a18) {
      if ("movementX" in a18) return a18.movementX;
      a18 !== yd && (yd && "mousemove" === a18.type ? (wd = a18.screenX - yd.screenX, xd = a18.screenY - yd.screenY) : xd = wd = 0, yd = a18);
      return wd;
    }, movementY: function(a18) {
      return "movementY" in a18 ? a18.movementY : xd;
    } });
    var Bd = rd(Ad);
    var Cd = A4({}, Ad, { dataTransfer: 0 });
    var Dd = rd(Cd);
    var Ed = A4({}, ud, { relatedTarget: 0 });
    var Fd = rd(Ed);
    var Gd = A4({}, sd, { animationName: 0, elapsedTime: 0, pseudoElement: 0 });
    var Hd = rd(Gd);
    var Id = A4({}, sd, { clipboardData: function(a18) {
      return "clipboardData" in a18 ? a18.clipboardData : window.clipboardData;
    } });
    var Jd = rd(Id);
    var Kd = A4({}, sd, { data: 0 });
    var Ld = rd(Kd);
    var Md = {
      Esc: "Escape",
      Spacebar: " ",
      Left: "ArrowLeft",
      Up: "ArrowUp",
      Right: "ArrowRight",
      Down: "ArrowDown",
      Del: "Delete",
      Win: "OS",
      Menu: "ContextMenu",
      Apps: "ContextMenu",
      Scroll: "ScrollLock",
      MozPrintableKey: "Unidentified"
    };
    var Nd = {
      8: "Backspace",
      9: "Tab",
      12: "Clear",
      13: "Enter",
      16: "Shift",
      17: "Control",
      18: "Alt",
      19: "Pause",
      20: "CapsLock",
      27: "Escape",
      32: " ",
      33: "PageUp",
      34: "PageDown",
      35: "End",
      36: "Home",
      37: "ArrowLeft",
      38: "ArrowUp",
      39: "ArrowRight",
      40: "ArrowDown",
      45: "Insert",
      46: "Delete",
      112: "F1",
      113: "F2",
      114: "F3",
      115: "F4",
      116: "F5",
      117: "F6",
      118: "F7",
      119: "F8",
      120: "F9",
      121: "F10",
      122: "F11",
      123: "F12",
      144: "NumLock",
      145: "ScrollLock",
      224: "Meta"
    };
    var Od = { Alt: "altKey", Control: "ctrlKey", Meta: "metaKey", Shift: "shiftKey" };
    function Pd(a18) {
      var b4 = this.nativeEvent;
      return b4.getModifierState ? b4.getModifierState(a18) : (a18 = Od[a18]) ? !!b4[a18] : false;
    }
    function zd() {
      return Pd;
    }
    var Qd = A4({}, ud, { key: function(a18) {
      if (a18.key) {
        var b4 = Md[a18.key] || a18.key;
        if ("Unidentified" !== b4) return b4;
      }
      return "keypress" === a18.type ? (a18 = od(a18), 13 === a18 ? "Enter" : String.fromCharCode(a18)) : "keydown" === a18.type || "keyup" === a18.type ? Nd[a18.keyCode] || "Unidentified" : "";
    }, code: 0, location: 0, ctrlKey: 0, shiftKey: 0, altKey: 0, metaKey: 0, repeat: 0, locale: 0, getModifierState: zd, charCode: function(a18) {
      return "keypress" === a18.type ? od(a18) : 0;
    }, keyCode: function(a18) {
      return "keydown" === a18.type || "keyup" === a18.type ? a18.keyCode : 0;
    }, which: function(a18) {
      return "keypress" === a18.type ? od(a18) : "keydown" === a18.type || "keyup" === a18.type ? a18.keyCode : 0;
    } });
    var Rd = rd(Qd);
    var Sd = A4({}, Ad, { pointerId: 0, width: 0, height: 0, pressure: 0, tangentialPressure: 0, tiltX: 0, tiltY: 0, twist: 0, pointerType: 0, isPrimary: 0 });
    var Td = rd(Sd);
    var Ud = A4({}, ud, { touches: 0, targetTouches: 0, changedTouches: 0, altKey: 0, metaKey: 0, ctrlKey: 0, shiftKey: 0, getModifierState: zd });
    var Vd = rd(Ud);
    var Wd = A4({}, sd, { propertyName: 0, elapsedTime: 0, pseudoElement: 0 });
    var Xd = rd(Wd);
    var Yd = A4({}, Ad, {
      deltaX: function(a18) {
        return "deltaX" in a18 ? a18.deltaX : "wheelDeltaX" in a18 ? -a18.wheelDeltaX : 0;
      },
      deltaY: function(a18) {
        return "deltaY" in a18 ? a18.deltaY : "wheelDeltaY" in a18 ? -a18.wheelDeltaY : "wheelDelta" in a18 ? -a18.wheelDelta : 0;
      },
      deltaZ: 0,
      deltaMode: 0
    });
    var Zd = rd(Yd);
    var $d = [9, 13, 27, 32];
    var ae3 = ia && "CompositionEvent" in window;
    var be5 = null;
    ia && "documentMode" in document && (be5 = document.documentMode);
    var ce3 = ia && "TextEvent" in window && !be5;
    var de3 = ia && (!ae3 || be5 && 8 < be5 && 11 >= be5);
    var ee4 = String.fromCharCode(32);
    var fe2 = false;
    function ge3(a18, b4) {
      switch (a18) {
        case "keyup":
          return -1 !== $d.indexOf(b4.keyCode);
        case "keydown":
          return 229 !== b4.keyCode;
        case "keypress":
        case "mousedown":
        case "focusout":
          return true;
        default:
          return false;
      }
    }
    function he2(a18) {
      a18 = a18.detail;
      return "object" === typeof a18 && "data" in a18 ? a18.data : null;
    }
    var ie3 = false;
    function je4(a18, b4) {
      switch (a18) {
        case "compositionend":
          return he2(b4);
        case "keypress":
          if (32 !== b4.which) return null;
          fe2 = true;
          return ee4;
        case "textInput":
          return a18 = b4.data, a18 === ee4 && fe2 ? null : a18;
        default:
          return null;
      }
    }
    function ke3(a18, b4) {
      if (ie3) return "compositionend" === a18 || !ae3 && ge3(a18, b4) ? (a18 = nd(), md = ld = kd = null, ie3 = false, a18) : null;
      switch (a18) {
        case "paste":
          return null;
        case "keypress":
          if (!(b4.ctrlKey || b4.altKey || b4.metaKey) || b4.ctrlKey && b4.altKey) {
            if (b4.char && 1 < b4.char.length) return b4.char;
            if (b4.which) return String.fromCharCode(b4.which);
          }
          return null;
        case "compositionend":
          return de3 && "ko" !== b4.locale ? null : b4.data;
        default:
          return null;
      }
    }
    var le5 = { color: true, date: true, datetime: true, "datetime-local": true, email: true, month: true, number: true, password: true, range: true, search: true, tel: true, text: true, time: true, url: true, week: true };
    function me4(a18) {
      var b4 = a18 && a18.nodeName && a18.nodeName.toLowerCase();
      return "input" === b4 ? !!le5[a18.type] : "textarea" === b4 ? true : false;
    }
    function ne3(a18, b4, c15, d12) {
      Eb(d12);
      b4 = oe2(b4, "onChange");
      0 < b4.length && (c15 = new td("onChange", "change", null, c15, d12), a18.push({ event: c15, listeners: b4 }));
    }
    var pe4 = null;
    var qe4 = null;
    function re3(a18) {
      se3(a18, 0);
    }
    function te4(a18) {
      var b4 = ue4(a18);
      if (Wa(b4)) return a18;
    }
    function ve4(a18, b4) {
      if ("change" === a18) return b4;
    }
    var we4 = false;
    if (ia) {
      if (ia) {
        ye3 = "oninput" in document;
        if (!ye3) {
          ze3 = document.createElement("div");
          ze3.setAttribute("oninput", "return;");
          ye3 = "function" === typeof ze3.oninput;
        }
        xe5 = ye3;
      } else xe5 = false;
      we4 = xe5 && (!document.documentMode || 9 < document.documentMode);
    }
    var xe5;
    var ye3;
    var ze3;
    function Ae4() {
      pe4 && (pe4.detachEvent("onpropertychange", Be2), qe4 = pe4 = null);
    }
    function Be2(a18) {
      if ("value" === a18.propertyName && te4(qe4)) {
        var b4 = [];
        ne3(b4, qe4, a18, xb(a18));
        Jb(re3, b4);
      }
    }
    function Ce4(a18, b4, c15) {
      "focusin" === a18 ? (Ae4(), pe4 = b4, qe4 = c15, pe4.attachEvent("onpropertychange", Be2)) : "focusout" === a18 && Ae4();
    }
    function De4(a18) {
      if ("selectionchange" === a18 || "keyup" === a18 || "keydown" === a18) return te4(qe4);
    }
    function Ee2(a18, b4) {
      if ("click" === a18) return te4(b4);
    }
    function Fe3(a18, b4) {
      if ("input" === a18 || "change" === a18) return te4(b4);
    }
    function Ge3(a18, b4) {
      return a18 === b4 && (0 !== a18 || 1 / a18 === 1 / b4) || a18 !== a18 && b4 !== b4;
    }
    var He5 = "function" === typeof Object.is ? Object.is : Ge3;
    function Ie3(a18, b4) {
      if (He5(a18, b4)) return true;
      if ("object" !== typeof a18 || null === a18 || "object" !== typeof b4 || null === b4) return false;
      var c15 = Object.keys(a18), d12 = Object.keys(b4);
      if (c15.length !== d12.length) return false;
      for (d12 = 0; d12 < c15.length; d12++) {
        var e14 = c15[d12];
        if (!ja.call(b4, e14) || !He5(a18[e14], b4[e14])) return false;
      }
      return true;
    }
    function Je3(a18) {
      for (; a18 && a18.firstChild; ) a18 = a18.firstChild;
      return a18;
    }
    function Ke4(a18, b4) {
      var c15 = Je3(a18);
      a18 = 0;
      for (var d12; c15; ) {
        if (3 === c15.nodeType) {
          d12 = a18 + c15.textContent.length;
          if (a18 <= b4 && d12 >= b4) return { node: c15, offset: b4 - a18 };
          a18 = d12;
        }
        a: {
          for (; c15; ) {
            if (c15.nextSibling) {
              c15 = c15.nextSibling;
              break a;
            }
            c15 = c15.parentNode;
          }
          c15 = void 0;
        }
        c15 = Je3(c15);
      }
    }
    function Le4(a18, b4) {
      return a18 && b4 ? a18 === b4 ? true : a18 && 3 === a18.nodeType ? false : b4 && 3 === b4.nodeType ? Le4(a18, b4.parentNode) : "contains" in a18 ? a18.contains(b4) : a18.compareDocumentPosition ? !!(a18.compareDocumentPosition(b4) & 16) : false : false;
    }
    function Me3() {
      for (var a18 = window, b4 = Xa(); b4 instanceof a18.HTMLIFrameElement; ) {
        try {
          var c15 = "string" === typeof b4.contentWindow.location.href;
        } catch (d12) {
          c15 = false;
        }
        if (c15) a18 = b4.contentWindow;
        else break;
        b4 = Xa(a18.document);
      }
      return b4;
    }
    function Ne4(a18) {
      var b4 = a18 && a18.nodeName && a18.nodeName.toLowerCase();
      return b4 && ("input" === b4 && ("text" === a18.type || "search" === a18.type || "tel" === a18.type || "url" === a18.type || "password" === a18.type) || "textarea" === b4 || "true" === a18.contentEditable);
    }
    function Oe3(a18) {
      var b4 = Me3(), c15 = a18.focusedElem, d12 = a18.selectionRange;
      if (b4 !== c15 && c15 && c15.ownerDocument && Le4(c15.ownerDocument.documentElement, c15)) {
        if (null !== d12 && Ne4(c15)) {
          if (b4 = d12.start, a18 = d12.end, void 0 === a18 && (a18 = b4), "selectionStart" in c15) c15.selectionStart = b4, c15.selectionEnd = Math.min(a18, c15.value.length);
          else if (a18 = (b4 = c15.ownerDocument || document) && b4.defaultView || window, a18.getSelection) {
            a18 = a18.getSelection();
            var e14 = c15.textContent.length, f15 = Math.min(d12.start, e14);
            d12 = void 0 === d12.end ? f15 : Math.min(d12.end, e14);
            !a18.extend && f15 > d12 && (e14 = d12, d12 = f15, f15 = e14);
            e14 = Ke4(c15, f15);
            var g7 = Ke4(
              c15,
              d12
            );
            e14 && g7 && (1 !== a18.rangeCount || a18.anchorNode !== e14.node || a18.anchorOffset !== e14.offset || a18.focusNode !== g7.node || a18.focusOffset !== g7.offset) && (b4 = b4.createRange(), b4.setStart(e14.node, e14.offset), a18.removeAllRanges(), f15 > d12 ? (a18.addRange(b4), a18.extend(g7.node, g7.offset)) : (b4.setEnd(g7.node, g7.offset), a18.addRange(b4)));
          }
        }
        b4 = [];
        for (a18 = c15; a18 = a18.parentNode; ) 1 === a18.nodeType && b4.push({ element: a18, left: a18.scrollLeft, top: a18.scrollTop });
        "function" === typeof c15.focus && c15.focus();
        for (c15 = 0; c15 < b4.length; c15++) a18 = b4[c15], a18.element.scrollLeft = a18.left, a18.element.scrollTop = a18.top;
      }
    }
    var Pe3 = ia && "documentMode" in document && 11 >= document.documentMode;
    var Qe4 = null;
    var Re4 = null;
    var Se2 = null;
    var Te4 = false;
    function Ue3(a18, b4, c15) {
      var d12 = c15.window === c15 ? c15.document : 9 === c15.nodeType ? c15 : c15.ownerDocument;
      Te4 || null == Qe4 || Qe4 !== Xa(d12) || (d12 = Qe4, "selectionStart" in d12 && Ne4(d12) ? d12 = { start: d12.selectionStart, end: d12.selectionEnd } : (d12 = (d12.ownerDocument && d12.ownerDocument.defaultView || window).getSelection(), d12 = { anchorNode: d12.anchorNode, anchorOffset: d12.anchorOffset, focusNode: d12.focusNode, focusOffset: d12.focusOffset }), Se2 && Ie3(Se2, d12) || (Se2 = d12, d12 = oe2(Re4, "onSelect"), 0 < d12.length && (b4 = new td("onSelect", "select", null, b4, c15), a18.push({ event: b4, listeners: d12 }), b4.target = Qe4)));
    }
    function Ve5(a18, b4) {
      var c15 = {};
      c15[a18.toLowerCase()] = b4.toLowerCase();
      c15["Webkit" + a18] = "webkit" + b4;
      c15["Moz" + a18] = "moz" + b4;
      return c15;
    }
    var We4 = { animationend: Ve5("Animation", "AnimationEnd"), animationiteration: Ve5("Animation", "AnimationIteration"), animationstart: Ve5("Animation", "AnimationStart"), transitionend: Ve5("Transition", "TransitionEnd") };
    var Xe3 = {};
    var Ye3 = {};
    ia && (Ye3 = document.createElement("div").style, "AnimationEvent" in window || (delete We4.animationend.animation, delete We4.animationiteration.animation, delete We4.animationstart.animation), "TransitionEvent" in window || delete We4.transitionend.transition);
    function Ze4(a18) {
      if (Xe3[a18]) return Xe3[a18];
      if (!We4[a18]) return a18;
      var b4 = We4[a18], c15;
      for (c15 in b4) if (b4.hasOwnProperty(c15) && c15 in Ye3) return Xe3[a18] = b4[c15];
      return a18;
    }
    var $e4 = Ze4("animationend");
    var af = Ze4("animationiteration");
    var bf = Ze4("animationstart");
    var cf = Ze4("transitionend");
    var df = /* @__PURE__ */ new Map();
    var ef = "abort auxClick cancel canPlay canPlayThrough click close contextMenu copy cut drag dragEnd dragEnter dragExit dragLeave dragOver dragStart drop durationChange emptied encrypted ended error gotPointerCapture input invalid keyDown keyPress keyUp load loadedData loadedMetadata loadStart lostPointerCapture mouseDown mouseMove mouseOut mouseOver mouseUp paste pause play playing pointerCancel pointerDown pointerMove pointerOut pointerOver pointerUp progress rateChange reset resize seeked seeking stalled submit suspend timeUpdate touchCancel touchEnd touchStart volumeChange scroll toggle touchMove waiting wheel".split(" ");
    function ff(a18, b4) {
      df.set(a18, b4);
      fa(b4, [a18]);
    }
    for (gf = 0; gf < ef.length; gf++) {
      hf = ef[gf], jf = hf.toLowerCase(), kf = hf[0].toUpperCase() + hf.slice(1);
      ff(jf, "on" + kf);
    }
    var hf;
    var jf;
    var kf;
    var gf;
    ff($e4, "onAnimationEnd");
    ff(af, "onAnimationIteration");
    ff(bf, "onAnimationStart");
    ff("dblclick", "onDoubleClick");
    ff("focusin", "onFocus");
    ff("focusout", "onBlur");
    ff(cf, "onTransitionEnd");
    ha("onMouseEnter", ["mouseout", "mouseover"]);
    ha("onMouseLeave", ["mouseout", "mouseover"]);
    ha("onPointerEnter", ["pointerout", "pointerover"]);
    ha("onPointerLeave", ["pointerout", "pointerover"]);
    fa("onChange", "change click focusin focusout input keydown keyup selectionchange".split(" "));
    fa("onSelect", "focusout contextmenu dragend focusin keydown keyup mousedown mouseup selectionchange".split(" "));
    fa("onBeforeInput", ["compositionend", "keypress", "textInput", "paste"]);
    fa("onCompositionEnd", "compositionend focusout keydown keypress keyup mousedown".split(" "));
    fa("onCompositionStart", "compositionstart focusout keydown keypress keyup mousedown".split(" "));
    fa("onCompositionUpdate", "compositionupdate focusout keydown keypress keyup mousedown".split(" "));
    var lf = "abort canplay canplaythrough durationchange emptied encrypted ended error loadeddata loadedmetadata loadstart pause play playing progress ratechange resize seeked seeking stalled suspend timeupdate volumechange waiting".split(" ");
    var mf = new Set("cancel close invalid load scroll toggle".split(" ").concat(lf));
    function nf(a18, b4, c15) {
      var d12 = a18.type || "unknown-event";
      a18.currentTarget = c15;
      Ub(d12, b4, void 0, a18);
      a18.currentTarget = null;
    }
    function se3(a18, b4) {
      b4 = 0 !== (b4 & 4);
      for (var c15 = 0; c15 < a18.length; c15++) {
        var d12 = a18[c15], e14 = d12.event;
        d12 = d12.listeners;
        a: {
          var f15 = void 0;
          if (b4) for (var g7 = d12.length - 1; 0 <= g7; g7--) {
            var h6 = d12[g7], k5 = h6.instance, l12 = h6.currentTarget;
            h6 = h6.listener;
            if (k5 !== f15 && e14.isPropagationStopped()) break a;
            nf(e14, h6, l12);
            f15 = k5;
          }
          else for (g7 = 0; g7 < d12.length; g7++) {
            h6 = d12[g7];
            k5 = h6.instance;
            l12 = h6.currentTarget;
            h6 = h6.listener;
            if (k5 !== f15 && e14.isPropagationStopped()) break a;
            nf(e14, h6, l12);
            f15 = k5;
          }
        }
      }
      if (Qb) throw a18 = Rb, Qb = false, Rb = null, a18;
    }
    function D5(a18, b4) {
      var c15 = b4[of];
      void 0 === c15 && (c15 = b4[of] = /* @__PURE__ */ new Set());
      var d12 = a18 + "__bubble";
      c15.has(d12) || (pf(b4, a18, 2, false), c15.add(d12));
    }
    function qf(a18, b4, c15) {
      var d12 = 0;
      b4 && (d12 |= 4);
      pf(c15, a18, d12, b4);
    }
    var rf = "_reactListening" + Math.random().toString(36).slice(2);
    function sf(a18) {
      if (!a18[rf]) {
        a18[rf] = true;
        da.forEach(function(b5) {
          "selectionchange" !== b5 && (mf.has(b5) || qf(b5, false, a18), qf(b5, true, a18));
        });
        var b4 = 9 === a18.nodeType ? a18 : a18.ownerDocument;
        null === b4 || b4[rf] || (b4[rf] = true, qf("selectionchange", false, b4));
      }
    }
    function pf(a18, b4, c15, d12) {
      switch (jd(b4)) {
        case 1:
          var e14 = ed;
          break;
        case 4:
          e14 = gd;
          break;
        default:
          e14 = fd;
      }
      c15 = e14.bind(null, b4, c15, a18);
      e14 = void 0;
      !Lb || "touchstart" !== b4 && "touchmove" !== b4 && "wheel" !== b4 || (e14 = true);
      d12 ? void 0 !== e14 ? a18.addEventListener(b4, c15, { capture: true, passive: e14 }) : a18.addEventListener(b4, c15, true) : void 0 !== e14 ? a18.addEventListener(b4, c15, { passive: e14 }) : a18.addEventListener(b4, c15, false);
    }
    function hd(a18, b4, c15, d12, e14) {
      var f15 = d12;
      if (0 === (b4 & 1) && 0 === (b4 & 2) && null !== d12) a: for (; ; ) {
        if (null === d12) return;
        var g7 = d12.tag;
        if (3 === g7 || 4 === g7) {
          var h6 = d12.stateNode.containerInfo;
          if (h6 === e14 || 8 === h6.nodeType && h6.parentNode === e14) break;
          if (4 === g7) for (g7 = d12.return; null !== g7; ) {
            var k5 = g7.tag;
            if (3 === k5 || 4 === k5) {
              if (k5 = g7.stateNode.containerInfo, k5 === e14 || 8 === k5.nodeType && k5.parentNode === e14) return;
            }
            g7 = g7.return;
          }
          for (; null !== h6; ) {
            g7 = Wc(h6);
            if (null === g7) return;
            k5 = g7.tag;
            if (5 === k5 || 6 === k5) {
              d12 = f15 = g7;
              continue a;
            }
            h6 = h6.parentNode;
          }
        }
        d12 = d12.return;
      }
      Jb(function() {
        var d13 = f15, e15 = xb(c15), g8 = [];
        a: {
          var h7 = df.get(a18);
          if (void 0 !== h7) {
            var k6 = td, n13 = a18;
            switch (a18) {
              case "keypress":
                if (0 === od(c15)) break a;
              case "keydown":
              case "keyup":
                k6 = Rd;
                break;
              case "focusin":
                n13 = "focus";
                k6 = Fd;
                break;
              case "focusout":
                n13 = "blur";
                k6 = Fd;
                break;
              case "beforeblur":
              case "afterblur":
                k6 = Fd;
                break;
              case "click":
                if (2 === c15.button) break a;
              case "auxclick":
              case "dblclick":
              case "mousedown":
              case "mousemove":
              case "mouseup":
              case "mouseout":
              case "mouseover":
              case "contextmenu":
                k6 = Bd;
                break;
              case "drag":
              case "dragend":
              case "dragenter":
              case "dragexit":
              case "dragleave":
              case "dragover":
              case "dragstart":
              case "drop":
                k6 = Dd;
                break;
              case "touchcancel":
              case "touchend":
              case "touchmove":
              case "touchstart":
                k6 = Vd;
                break;
              case $e4:
              case af:
              case bf:
                k6 = Hd;
                break;
              case cf:
                k6 = Xd;
                break;
              case "scroll":
                k6 = vd;
                break;
              case "wheel":
                k6 = Zd;
                break;
              case "copy":
              case "cut":
              case "paste":
                k6 = Jd;
                break;
              case "gotpointercapture":
              case "lostpointercapture":
              case "pointercancel":
              case "pointerdown":
              case "pointermove":
              case "pointerout":
              case "pointerover":
              case "pointerup":
                k6 = Td;
            }
            var t20 = 0 !== (b4 & 4), J2 = !t20 && "scroll" === a18, x3 = t20 ? null !== h7 ? h7 + "Capture" : null : h7;
            t20 = [];
            for (var w11 = d13, u4; null !== w11; ) {
              u4 = w11;
              var F4 = u4.stateNode;
              5 === u4.tag && null !== F4 && (u4 = F4, null !== x3 && (F4 = Kb(w11, x3), null != F4 && t20.push(tf(w11, F4, u4))));
              if (J2) break;
              w11 = w11.return;
            }
            0 < t20.length && (h7 = new k6(h7, n13, null, c15, e15), g8.push({ event: h7, listeners: t20 }));
          }
        }
        if (0 === (b4 & 7)) {
          a: {
            h7 = "mouseover" === a18 || "pointerover" === a18;
            k6 = "mouseout" === a18 || "pointerout" === a18;
            if (h7 && c15 !== wb && (n13 = c15.relatedTarget || c15.fromElement) && (Wc(n13) || n13[uf])) break a;
            if (k6 || h7) {
              h7 = e15.window === e15 ? e15 : (h7 = e15.ownerDocument) ? h7.defaultView || h7.parentWindow : window;
              if (k6) {
                if (n13 = c15.relatedTarget || c15.toElement, k6 = d13, n13 = n13 ? Wc(n13) : null, null !== n13 && (J2 = Vb(n13), n13 !== J2 || 5 !== n13.tag && 6 !== n13.tag)) n13 = null;
              } else k6 = null, n13 = d13;
              if (k6 !== n13) {
                t20 = Bd;
                F4 = "onMouseLeave";
                x3 = "onMouseEnter";
                w11 = "mouse";
                if ("pointerout" === a18 || "pointerover" === a18) t20 = Td, F4 = "onPointerLeave", x3 = "onPointerEnter", w11 = "pointer";
                J2 = null == k6 ? h7 : ue4(k6);
                u4 = null == n13 ? h7 : ue4(n13);
                h7 = new t20(F4, w11 + "leave", k6, c15, e15);
                h7.target = J2;
                h7.relatedTarget = u4;
                F4 = null;
                Wc(e15) === d13 && (t20 = new t20(x3, w11 + "enter", n13, c15, e15), t20.target = u4, t20.relatedTarget = J2, F4 = t20);
                J2 = F4;
                if (k6 && n13) b: {
                  t20 = k6;
                  x3 = n13;
                  w11 = 0;
                  for (u4 = t20; u4; u4 = vf(u4)) w11++;
                  u4 = 0;
                  for (F4 = x3; F4; F4 = vf(F4)) u4++;
                  for (; 0 < w11 - u4; ) t20 = vf(t20), w11--;
                  for (; 0 < u4 - w11; ) x3 = vf(x3), u4--;
                  for (; w11--; ) {
                    if (t20 === x3 || null !== x3 && t20 === x3.alternate) break b;
                    t20 = vf(t20);
                    x3 = vf(x3);
                  }
                  t20 = null;
                }
                else t20 = null;
                null !== k6 && wf(g8, h7, k6, t20, false);
                null !== n13 && null !== J2 && wf(g8, J2, n13, t20, true);
              }
            }
          }
          a: {
            h7 = d13 ? ue4(d13) : window;
            k6 = h7.nodeName && h7.nodeName.toLowerCase();
            if ("select" === k6 || "input" === k6 && "file" === h7.type) var na = ve4;
            else if (me4(h7)) if (we4) na = Fe3;
            else {
              na = De4;
              var xa = Ce4;
            }
            else (k6 = h7.nodeName) && "input" === k6.toLowerCase() && ("checkbox" === h7.type || "radio" === h7.type) && (na = Ee2);
            if (na && (na = na(a18, d13))) {
              ne3(g8, na, c15, e15);
              break a;
            }
            xa && xa(a18, h7, d13);
            "focusout" === a18 && (xa = h7._wrapperState) && xa.controlled && "number" === h7.type && cb(h7, "number", h7.value);
          }
          xa = d13 ? ue4(d13) : window;
          switch (a18) {
            case "focusin":
              if (me4(xa) || "true" === xa.contentEditable) Qe4 = xa, Re4 = d13, Se2 = null;
              break;
            case "focusout":
              Se2 = Re4 = Qe4 = null;
              break;
            case "mousedown":
              Te4 = true;
              break;
            case "contextmenu":
            case "mouseup":
            case "dragend":
              Te4 = false;
              Ue3(g8, c15, e15);
              break;
            case "selectionchange":
              if (Pe3) break;
            case "keydown":
            case "keyup":
              Ue3(g8, c15, e15);
          }
          var $a;
          if (ae3) b: {
            switch (a18) {
              case "compositionstart":
                var ba = "onCompositionStart";
                break b;
              case "compositionend":
                ba = "onCompositionEnd";
                break b;
              case "compositionupdate":
                ba = "onCompositionUpdate";
                break b;
            }
            ba = void 0;
          }
          else ie3 ? ge3(a18, c15) && (ba = "onCompositionEnd") : "keydown" === a18 && 229 === c15.keyCode && (ba = "onCompositionStart");
          ba && (de3 && "ko" !== c15.locale && (ie3 || "onCompositionStart" !== ba ? "onCompositionEnd" === ba && ie3 && ($a = nd()) : (kd = e15, ld = "value" in kd ? kd.value : kd.textContent, ie3 = true)), xa = oe2(d13, ba), 0 < xa.length && (ba = new Ld(ba, a18, null, c15, e15), g8.push({ event: ba, listeners: xa }), $a ? ba.data = $a : ($a = he2(c15), null !== $a && (ba.data = $a))));
          if ($a = ce3 ? je4(a18, c15) : ke3(a18, c15)) d13 = oe2(d13, "onBeforeInput"), 0 < d13.length && (e15 = new Ld("onBeforeInput", "beforeinput", null, c15, e15), g8.push({ event: e15, listeners: d13 }), e15.data = $a);
        }
        se3(g8, b4);
      });
    }
    function tf(a18, b4, c15) {
      return { instance: a18, listener: b4, currentTarget: c15 };
    }
    function oe2(a18, b4) {
      for (var c15 = b4 + "Capture", d12 = []; null !== a18; ) {
        var e14 = a18, f15 = e14.stateNode;
        5 === e14.tag && null !== f15 && (e14 = f15, f15 = Kb(a18, c15), null != f15 && d12.unshift(tf(a18, f15, e14)), f15 = Kb(a18, b4), null != f15 && d12.push(tf(a18, f15, e14)));
        a18 = a18.return;
      }
      return d12;
    }
    function vf(a18) {
      if (null === a18) return null;
      do
        a18 = a18.return;
      while (a18 && 5 !== a18.tag);
      return a18 ? a18 : null;
    }
    function wf(a18, b4, c15, d12, e14) {
      for (var f15 = b4._reactName, g7 = []; null !== c15 && c15 !== d12; ) {
        var h6 = c15, k5 = h6.alternate, l12 = h6.stateNode;
        if (null !== k5 && k5 === d12) break;
        5 === h6.tag && null !== l12 && (h6 = l12, e14 ? (k5 = Kb(c15, f15), null != k5 && g7.unshift(tf(c15, k5, h6))) : e14 || (k5 = Kb(c15, f15), null != k5 && g7.push(tf(c15, k5, h6))));
        c15 = c15.return;
      }
      0 !== g7.length && a18.push({ event: b4, listeners: g7 });
    }
    var xf = /\r\n?/g;
    var yf = /\u0000|\uFFFD/g;
    function zf(a18) {
      return ("string" === typeof a18 ? a18 : "" + a18).replace(xf, "\n").replace(yf, "");
    }
    function Af(a18, b4, c15) {
      b4 = zf(b4);
      if (zf(a18) !== b4 && c15) throw Error(p11(425));
    }
    function Bf() {
    }
    var Cf = null;
    var Df = null;
    function Ef(a18, b4) {
      return "textarea" === a18 || "noscript" === a18 || "string" === typeof b4.children || "number" === typeof b4.children || "object" === typeof b4.dangerouslySetInnerHTML && null !== b4.dangerouslySetInnerHTML && null != b4.dangerouslySetInnerHTML.__html;
    }
    var Ff = "function" === typeof setTimeout ? setTimeout : void 0;
    var Gf = "function" === typeof clearTimeout ? clearTimeout : void 0;
    var Hf = "function" === typeof Promise ? Promise : void 0;
    var Jf = "function" === typeof queueMicrotask ? queueMicrotask : "undefined" !== typeof Hf ? function(a18) {
      return Hf.resolve(null).then(a18).catch(If);
    } : Ff;
    function If(a18) {
      setTimeout(function() {
        throw a18;
      });
    }
    function Kf(a18, b4) {
      var c15 = b4, d12 = 0;
      do {
        var e14 = c15.nextSibling;
        a18.removeChild(c15);
        if (e14 && 8 === e14.nodeType) if (c15 = e14.data, "/$" === c15) {
          if (0 === d12) {
            a18.removeChild(e14);
            bd(b4);
            return;
          }
          d12--;
        } else "$" !== c15 && "$?" !== c15 && "$!" !== c15 || d12++;
        c15 = e14;
      } while (c15);
      bd(b4);
    }
    function Lf(a18) {
      for (; null != a18; a18 = a18.nextSibling) {
        var b4 = a18.nodeType;
        if (1 === b4 || 3 === b4) break;
        if (8 === b4) {
          b4 = a18.data;
          if ("$" === b4 || "$!" === b4 || "$?" === b4) break;
          if ("/$" === b4) return null;
        }
      }
      return a18;
    }
    function Mf(a18) {
      a18 = a18.previousSibling;
      for (var b4 = 0; a18; ) {
        if (8 === a18.nodeType) {
          var c15 = a18.data;
          if ("$" === c15 || "$!" === c15 || "$?" === c15) {
            if (0 === b4) return a18;
            b4--;
          } else "/$" === c15 && b4++;
        }
        a18 = a18.previousSibling;
      }
      return null;
    }
    var Nf = Math.random().toString(36).slice(2);
    var Of = "__reactFiber$" + Nf;
    var Pf = "__reactProps$" + Nf;
    var uf = "__reactContainer$" + Nf;
    var of = "__reactEvents$" + Nf;
    var Qf = "__reactListeners$" + Nf;
    var Rf = "__reactHandles$" + Nf;
    function Wc(a18) {
      var b4 = a18[Of];
      if (b4) return b4;
      for (var c15 = a18.parentNode; c15; ) {
        if (b4 = c15[uf] || c15[Of]) {
          c15 = b4.alternate;
          if (null !== b4.child || null !== c15 && null !== c15.child) for (a18 = Mf(a18); null !== a18; ) {
            if (c15 = a18[Of]) return c15;
            a18 = Mf(a18);
          }
          return b4;
        }
        a18 = c15;
        c15 = a18.parentNode;
      }
      return null;
    }
    function Cb(a18) {
      a18 = a18[Of] || a18[uf];
      return !a18 || 5 !== a18.tag && 6 !== a18.tag && 13 !== a18.tag && 3 !== a18.tag ? null : a18;
    }
    function ue4(a18) {
      if (5 === a18.tag || 6 === a18.tag) return a18.stateNode;
      throw Error(p11(33));
    }
    function Db(a18) {
      return a18[Pf] || null;
    }
    var Sf = [];
    var Tf = -1;
    function Uf(a18) {
      return { current: a18 };
    }
    function E6(a18) {
      0 > Tf || (a18.current = Sf[Tf], Sf[Tf] = null, Tf--);
    }
    function G3(a18, b4) {
      Tf++;
      Sf[Tf] = a18.current;
      a18.current = b4;
    }
    var Vf = {};
    var H4 = Uf(Vf);
    var Wf = Uf(false);
    var Xf = Vf;
    function Yf(a18, b4) {
      var c15 = a18.type.contextTypes;
      if (!c15) return Vf;
      var d12 = a18.stateNode;
      if (d12 && d12.__reactInternalMemoizedUnmaskedChildContext === b4) return d12.__reactInternalMemoizedMaskedChildContext;
      var e14 = {}, f15;
      for (f15 in c15) e14[f15] = b4[f15];
      d12 && (a18 = a18.stateNode, a18.__reactInternalMemoizedUnmaskedChildContext = b4, a18.__reactInternalMemoizedMaskedChildContext = e14);
      return e14;
    }
    function Zf(a18) {
      a18 = a18.childContextTypes;
      return null !== a18 && void 0 !== a18;
    }
    function $f() {
      E6(Wf);
      E6(H4);
    }
    function ag(a18, b4, c15) {
      if (H4.current !== Vf) throw Error(p11(168));
      G3(H4, b4);
      G3(Wf, c15);
    }
    function bg(a18, b4, c15) {
      var d12 = a18.stateNode;
      b4 = b4.childContextTypes;
      if ("function" !== typeof d12.getChildContext) return c15;
      d12 = d12.getChildContext();
      for (var e14 in d12) if (!(e14 in b4)) throw Error(p11(108, Ra(a18) || "Unknown", e14));
      return A4({}, c15, d12);
    }
    function cg(a18) {
      a18 = (a18 = a18.stateNode) && a18.__reactInternalMemoizedMergedChildContext || Vf;
      Xf = H4.current;
      G3(H4, a18);
      G3(Wf, Wf.current);
      return true;
    }
    function dg(a18, b4, c15) {
      var d12 = a18.stateNode;
      if (!d12) throw Error(p11(169));
      c15 ? (a18 = bg(a18, b4, Xf), d12.__reactInternalMemoizedMergedChildContext = a18, E6(Wf), E6(H4), G3(H4, a18)) : E6(Wf);
      G3(Wf, c15);
    }
    var eg = null;
    var fg = false;
    var gg = false;
    function hg(a18) {
      null === eg ? eg = [a18] : eg.push(a18);
    }
    function ig(a18) {
      fg = true;
      hg(a18);
    }
    function jg() {
      if (!gg && null !== eg) {
        gg = true;
        var a18 = 0, b4 = C5;
        try {
          var c15 = eg;
          for (C5 = 1; a18 < c15.length; a18++) {
            var d12 = c15[a18];
            do
              d12 = d12(true);
            while (null !== d12);
          }
          eg = null;
          fg = false;
        } catch (e14) {
          throw null !== eg && (eg = eg.slice(a18 + 1)), ac(fc, jg), e14;
        } finally {
          C5 = b4, gg = false;
        }
      }
      return null;
    }
    var kg = [];
    var lg = 0;
    var mg = null;
    var ng = 0;
    var og = [];
    var pg = 0;
    var qg = null;
    var rg = 1;
    var sg = "";
    function tg(a18, b4) {
      kg[lg++] = ng;
      kg[lg++] = mg;
      mg = a18;
      ng = b4;
    }
    function ug(a18, b4, c15) {
      og[pg++] = rg;
      og[pg++] = sg;
      og[pg++] = qg;
      qg = a18;
      var d12 = rg;
      a18 = sg;
      var e14 = 32 - oc(d12) - 1;
      d12 &= ~(1 << e14);
      c15 += 1;
      var f15 = 32 - oc(b4) + e14;
      if (30 < f15) {
        var g7 = e14 - e14 % 5;
        f15 = (d12 & (1 << g7) - 1).toString(32);
        d12 >>= g7;
        e14 -= g7;
        rg = 1 << 32 - oc(b4) + e14 | c15 << e14 | d12;
        sg = f15 + a18;
      } else rg = 1 << f15 | c15 << e14 | d12, sg = a18;
    }
    function vg(a18) {
      null !== a18.return && (tg(a18, 1), ug(a18, 1, 0));
    }
    function wg(a18) {
      for (; a18 === mg; ) mg = kg[--lg], kg[lg] = null, ng = kg[--lg], kg[lg] = null;
      for (; a18 === qg; ) qg = og[--pg], og[pg] = null, sg = og[--pg], og[pg] = null, rg = og[--pg], og[pg] = null;
    }
    var xg = null;
    var yg = null;
    var I6 = false;
    var zg = null;
    function Ag(a18, b4) {
      var c15 = Bg(5, null, null, 0);
      c15.elementType = "DELETED";
      c15.stateNode = b4;
      c15.return = a18;
      b4 = a18.deletions;
      null === b4 ? (a18.deletions = [c15], a18.flags |= 16) : b4.push(c15);
    }
    function Cg(a18, b4) {
      switch (a18.tag) {
        case 5:
          var c15 = a18.type;
          b4 = 1 !== b4.nodeType || c15.toLowerCase() !== b4.nodeName.toLowerCase() ? null : b4;
          return null !== b4 ? (a18.stateNode = b4, xg = a18, yg = Lf(b4.firstChild), true) : false;
        case 6:
          return b4 = "" === a18.pendingProps || 3 !== b4.nodeType ? null : b4, null !== b4 ? (a18.stateNode = b4, xg = a18, yg = null, true) : false;
        case 13:
          return b4 = 8 !== b4.nodeType ? null : b4, null !== b4 ? (c15 = null !== qg ? { id: rg, overflow: sg } : null, a18.memoizedState = { dehydrated: b4, treeContext: c15, retryLane: 1073741824 }, c15 = Bg(18, null, null, 0), c15.stateNode = b4, c15.return = a18, a18.child = c15, xg = a18, yg = null, true) : false;
        default:
          return false;
      }
    }
    function Dg(a18) {
      return 0 !== (a18.mode & 1) && 0 === (a18.flags & 128);
    }
    function Eg(a18) {
      if (I6) {
        var b4 = yg;
        if (b4) {
          var c15 = b4;
          if (!Cg(a18, b4)) {
            if (Dg(a18)) throw Error(p11(418));
            b4 = Lf(c15.nextSibling);
            var d12 = xg;
            b4 && Cg(a18, b4) ? Ag(d12, c15) : (a18.flags = a18.flags & -4097 | 2, I6 = false, xg = a18);
          }
        } else {
          if (Dg(a18)) throw Error(p11(418));
          a18.flags = a18.flags & -4097 | 2;
          I6 = false;
          xg = a18;
        }
      }
    }
    function Fg(a18) {
      for (a18 = a18.return; null !== a18 && 5 !== a18.tag && 3 !== a18.tag && 13 !== a18.tag; ) a18 = a18.return;
      xg = a18;
    }
    function Gg(a18) {
      if (a18 !== xg) return false;
      if (!I6) return Fg(a18), I6 = true, false;
      var b4;
      (b4 = 3 !== a18.tag) && !(b4 = 5 !== a18.tag) && (b4 = a18.type, b4 = "head" !== b4 && "body" !== b4 && !Ef(a18.type, a18.memoizedProps));
      if (b4 && (b4 = yg)) {
        if (Dg(a18)) throw Hg(), Error(p11(418));
        for (; b4; ) Ag(a18, b4), b4 = Lf(b4.nextSibling);
      }
      Fg(a18);
      if (13 === a18.tag) {
        a18 = a18.memoizedState;
        a18 = null !== a18 ? a18.dehydrated : null;
        if (!a18) throw Error(p11(317));
        a: {
          a18 = a18.nextSibling;
          for (b4 = 0; a18; ) {
            if (8 === a18.nodeType) {
              var c15 = a18.data;
              if ("/$" === c15) {
                if (0 === b4) {
                  yg = Lf(a18.nextSibling);
                  break a;
                }
                b4--;
              } else "$" !== c15 && "$!" !== c15 && "$?" !== c15 || b4++;
            }
            a18 = a18.nextSibling;
          }
          yg = null;
        }
      } else yg = xg ? Lf(a18.stateNode.nextSibling) : null;
      return true;
    }
    function Hg() {
      for (var a18 = yg; a18; ) a18 = Lf(a18.nextSibling);
    }
    function Ig() {
      yg = xg = null;
      I6 = false;
    }
    function Jg(a18) {
      null === zg ? zg = [a18] : zg.push(a18);
    }
    var Kg = ua.ReactCurrentBatchConfig;
    function Lg(a18, b4, c15) {
      a18 = c15.ref;
      if (null !== a18 && "function" !== typeof a18 && "object" !== typeof a18) {
        if (c15._owner) {
          c15 = c15._owner;
          if (c15) {
            if (1 !== c15.tag) throw Error(p11(309));
            var d12 = c15.stateNode;
          }
          if (!d12) throw Error(p11(147, a18));
          var e14 = d12, f15 = "" + a18;
          if (null !== b4 && null !== b4.ref && "function" === typeof b4.ref && b4.ref._stringRef === f15) return b4.ref;
          b4 = function(a19) {
            var b5 = e14.refs;
            null === a19 ? delete b5[f15] : b5[f15] = a19;
          };
          b4._stringRef = f15;
          return b4;
        }
        if ("string" !== typeof a18) throw Error(p11(284));
        if (!c15._owner) throw Error(p11(290, a18));
      }
      return a18;
    }
    function Mg(a18, b4) {
      a18 = Object.prototype.toString.call(b4);
      throw Error(p11(31, "[object Object]" === a18 ? "object with keys {" + Object.keys(b4).join(", ") + "}" : a18));
    }
    function Ng(a18) {
      var b4 = a18._init;
      return b4(a18._payload);
    }
    function Og(a18) {
      function b4(b5, c16) {
        if (a18) {
          var d13 = b5.deletions;
          null === d13 ? (b5.deletions = [c16], b5.flags |= 16) : d13.push(c16);
        }
      }
      function c15(c16, d13) {
        if (!a18) return null;
        for (; null !== d13; ) b4(c16, d13), d13 = d13.sibling;
        return null;
      }
      function d12(a19, b5) {
        for (a19 = /* @__PURE__ */ new Map(); null !== b5; ) null !== b5.key ? a19.set(b5.key, b5) : a19.set(b5.index, b5), b5 = b5.sibling;
        return a19;
      }
      function e14(a19, b5) {
        a19 = Pg(a19, b5);
        a19.index = 0;
        a19.sibling = null;
        return a19;
      }
      function f15(b5, c16, d13) {
        b5.index = d13;
        if (!a18) return b5.flags |= 1048576, c16;
        d13 = b5.alternate;
        if (null !== d13) return d13 = d13.index, d13 < c16 ? (b5.flags |= 2, c16) : d13;
        b5.flags |= 2;
        return c16;
      }
      function g7(b5) {
        a18 && null === b5.alternate && (b5.flags |= 2);
        return b5;
      }
      function h6(a19, b5, c16, d13) {
        if (null === b5 || 6 !== b5.tag) return b5 = Qg(c16, a19.mode, d13), b5.return = a19, b5;
        b5 = e14(b5, c16);
        b5.return = a19;
        return b5;
      }
      function k5(a19, b5, c16, d13) {
        var f16 = c16.type;
        if (f16 === ya) return m12(a19, b5, c16.props.children, d13, c16.key);
        if (null !== b5 && (b5.elementType === f16 || "object" === typeof f16 && null !== f16 && f16.$$typeof === Ha && Ng(f16) === b5.type)) return d13 = e14(b5, c16.props), d13.ref = Lg(a19, b5, c16), d13.return = a19, d13;
        d13 = Rg(c16.type, c16.key, c16.props, null, a19.mode, d13);
        d13.ref = Lg(a19, b5, c16);
        d13.return = a19;
        return d13;
      }
      function l12(a19, b5, c16, d13) {
        if (null === b5 || 4 !== b5.tag || b5.stateNode.containerInfo !== c16.containerInfo || b5.stateNode.implementation !== c16.implementation) return b5 = Sg(c16, a19.mode, d13), b5.return = a19, b5;
        b5 = e14(b5, c16.children || []);
        b5.return = a19;
        return b5;
      }
      function m12(a19, b5, c16, d13, f16) {
        if (null === b5 || 7 !== b5.tag) return b5 = Tg(c16, a19.mode, d13, f16), b5.return = a19, b5;
        b5 = e14(b5, c16);
        b5.return = a19;
        return b5;
      }
      function q2(a19, b5, c16) {
        if ("string" === typeof b5 && "" !== b5 || "number" === typeof b5) return b5 = Qg("" + b5, a19.mode, c16), b5.return = a19, b5;
        if ("object" === typeof b5 && null !== b5) {
          switch (b5.$$typeof) {
            case va:
              return c16 = Rg(b5.type, b5.key, b5.props, null, a19.mode, c16), c16.ref = Lg(a19, null, b5), c16.return = a19, c16;
            case wa:
              return b5 = Sg(b5, a19.mode, c16), b5.return = a19, b5;
            case Ha:
              var d13 = b5._init;
              return q2(a19, d13(b5._payload), c16);
          }
          if (eb(b5) || Ka(b5)) return b5 = Tg(b5, a19.mode, c16, null), b5.return = a19, b5;
          Mg(a19, b5);
        }
        return null;
      }
      function r7(a19, b5, c16, d13) {
        var e15 = null !== b5 ? b5.key : null;
        if ("string" === typeof c16 && "" !== c16 || "number" === typeof c16) return null !== e15 ? null : h6(a19, b5, "" + c16, d13);
        if ("object" === typeof c16 && null !== c16) {
          switch (c16.$$typeof) {
            case va:
              return c16.key === e15 ? k5(a19, b5, c16, d13) : null;
            case wa:
              return c16.key === e15 ? l12(a19, b5, c16, d13) : null;
            case Ha:
              return e15 = c16._init, r7(
                a19,
                b5,
                e15(c16._payload),
                d13
              );
          }
          if (eb(c16) || Ka(c16)) return null !== e15 ? null : m12(a19, b5, c16, d13, null);
          Mg(a19, c16);
        }
        return null;
      }
      function y4(a19, b5, c16, d13, e15) {
        if ("string" === typeof d13 && "" !== d13 || "number" === typeof d13) return a19 = a19.get(c16) || null, h6(b5, a19, "" + d13, e15);
        if ("object" === typeof d13 && null !== d13) {
          switch (d13.$$typeof) {
            case va:
              return a19 = a19.get(null === d13.key ? c16 : d13.key) || null, k5(b5, a19, d13, e15);
            case wa:
              return a19 = a19.get(null === d13.key ? c16 : d13.key) || null, l12(b5, a19, d13, e15);
            case Ha:
              var f16 = d13._init;
              return y4(a19, b5, c16, f16(d13._payload), e15);
          }
          if (eb(d13) || Ka(d13)) return a19 = a19.get(c16) || null, m12(b5, a19, d13, e15, null);
          Mg(b5, d13);
        }
        return null;
      }
      function n13(e15, g8, h7, k6) {
        for (var l13 = null, m13 = null, u4 = g8, w11 = g8 = 0, x3 = null; null !== u4 && w11 < h7.length; w11++) {
          u4.index > w11 ? (x3 = u4, u4 = null) : x3 = u4.sibling;
          var n14 = r7(e15, u4, h7[w11], k6);
          if (null === n14) {
            null === u4 && (u4 = x3);
            break;
          }
          a18 && u4 && null === n14.alternate && b4(e15, u4);
          g8 = f15(n14, g8, w11);
          null === m13 ? l13 = n14 : m13.sibling = n14;
          m13 = n14;
          u4 = x3;
        }
        if (w11 === h7.length) return c15(e15, u4), I6 && tg(e15, w11), l13;
        if (null === u4) {
          for (; w11 < h7.length; w11++) u4 = q2(e15, h7[w11], k6), null !== u4 && (g8 = f15(u4, g8, w11), null === m13 ? l13 = u4 : m13.sibling = u4, m13 = u4);
          I6 && tg(e15, w11);
          return l13;
        }
        for (u4 = d12(e15, u4); w11 < h7.length; w11++) x3 = y4(u4, e15, w11, h7[w11], k6), null !== x3 && (a18 && null !== x3.alternate && u4.delete(null === x3.key ? w11 : x3.key), g8 = f15(x3, g8, w11), null === m13 ? l13 = x3 : m13.sibling = x3, m13 = x3);
        a18 && u4.forEach(function(a19) {
          return b4(e15, a19);
        });
        I6 && tg(e15, w11);
        return l13;
      }
      function t20(e15, g8, h7, k6) {
        var l13 = Ka(h7);
        if ("function" !== typeof l13) throw Error(p11(150));
        h7 = l13.call(h7);
        if (null == h7) throw Error(p11(151));
        for (var u4 = l13 = null, m13 = g8, w11 = g8 = 0, x3 = null, n14 = h7.next(); null !== m13 && !n14.done; w11++, n14 = h7.next()) {
          m13.index > w11 ? (x3 = m13, m13 = null) : x3 = m13.sibling;
          var t21 = r7(e15, m13, n14.value, k6);
          if (null === t21) {
            null === m13 && (m13 = x3);
            break;
          }
          a18 && m13 && null === t21.alternate && b4(e15, m13);
          g8 = f15(t21, g8, w11);
          null === u4 ? l13 = t21 : u4.sibling = t21;
          u4 = t21;
          m13 = x3;
        }
        if (n14.done) return c15(
          e15,
          m13
        ), I6 && tg(e15, w11), l13;
        if (null === m13) {
          for (; !n14.done; w11++, n14 = h7.next()) n14 = q2(e15, n14.value, k6), null !== n14 && (g8 = f15(n14, g8, w11), null === u4 ? l13 = n14 : u4.sibling = n14, u4 = n14);
          I6 && tg(e15, w11);
          return l13;
        }
        for (m13 = d12(e15, m13); !n14.done; w11++, n14 = h7.next()) n14 = y4(m13, e15, w11, n14.value, k6), null !== n14 && (a18 && null !== n14.alternate && m13.delete(null === n14.key ? w11 : n14.key), g8 = f15(n14, g8, w11), null === u4 ? l13 = n14 : u4.sibling = n14, u4 = n14);
        a18 && m13.forEach(function(a19) {
          return b4(e15, a19);
        });
        I6 && tg(e15, w11);
        return l13;
      }
      function J2(a19, d13, f16, h7) {
        "object" === typeof f16 && null !== f16 && f16.type === ya && null === f16.key && (f16 = f16.props.children);
        if ("object" === typeof f16 && null !== f16) {
          switch (f16.$$typeof) {
            case va:
              a: {
                for (var k6 = f16.key, l13 = d13; null !== l13; ) {
                  if (l13.key === k6) {
                    k6 = f16.type;
                    if (k6 === ya) {
                      if (7 === l13.tag) {
                        c15(a19, l13.sibling);
                        d13 = e14(l13, f16.props.children);
                        d13.return = a19;
                        a19 = d13;
                        break a;
                      }
                    } else if (l13.elementType === k6 || "object" === typeof k6 && null !== k6 && k6.$$typeof === Ha && Ng(k6) === l13.type) {
                      c15(a19, l13.sibling);
                      d13 = e14(l13, f16.props);
                      d13.ref = Lg(a19, l13, f16);
                      d13.return = a19;
                      a19 = d13;
                      break a;
                    }
                    c15(a19, l13);
                    break;
                  } else b4(a19, l13);
                  l13 = l13.sibling;
                }
                f16.type === ya ? (d13 = Tg(f16.props.children, a19.mode, h7, f16.key), d13.return = a19, a19 = d13) : (h7 = Rg(f16.type, f16.key, f16.props, null, a19.mode, h7), h7.ref = Lg(a19, d13, f16), h7.return = a19, a19 = h7);
              }
              return g7(a19);
            case wa:
              a: {
                for (l13 = f16.key; null !== d13; ) {
                  if (d13.key === l13) if (4 === d13.tag && d13.stateNode.containerInfo === f16.containerInfo && d13.stateNode.implementation === f16.implementation) {
                    c15(a19, d13.sibling);
                    d13 = e14(d13, f16.children || []);
                    d13.return = a19;
                    a19 = d13;
                    break a;
                  } else {
                    c15(a19, d13);
                    break;
                  }
                  else b4(a19, d13);
                  d13 = d13.sibling;
                }
                d13 = Sg(f16, a19.mode, h7);
                d13.return = a19;
                a19 = d13;
              }
              return g7(a19);
            case Ha:
              return l13 = f16._init, J2(a19, d13, l13(f16._payload), h7);
          }
          if (eb(f16)) return n13(a19, d13, f16, h7);
          if (Ka(f16)) return t20(a19, d13, f16, h7);
          Mg(a19, f16);
        }
        return "string" === typeof f16 && "" !== f16 || "number" === typeof f16 ? (f16 = "" + f16, null !== d13 && 6 === d13.tag ? (c15(a19, d13.sibling), d13 = e14(d13, f16), d13.return = a19, a19 = d13) : (c15(a19, d13), d13 = Qg(f16, a19.mode, h7), d13.return = a19, a19 = d13), g7(a19)) : c15(a19, d13);
      }
      return J2;
    }
    var Ug = Og(true);
    var Vg = Og(false);
    var Wg = Uf(null);
    var Xg = null;
    var Yg = null;
    var Zg = null;
    function $g() {
      Zg = Yg = Xg = null;
    }
    function ah(a18) {
      var b4 = Wg.current;
      E6(Wg);
      a18._currentValue = b4;
    }
    function bh(a18, b4, c15) {
      for (; null !== a18; ) {
        var d12 = a18.alternate;
        (a18.childLanes & b4) !== b4 ? (a18.childLanes |= b4, null !== d12 && (d12.childLanes |= b4)) : null !== d12 && (d12.childLanes & b4) !== b4 && (d12.childLanes |= b4);
        if (a18 === c15) break;
        a18 = a18.return;
      }
    }
    function ch(a18, b4) {
      Xg = a18;
      Zg = Yg = null;
      a18 = a18.dependencies;
      null !== a18 && null !== a18.firstContext && (0 !== (a18.lanes & b4) && (dh = true), a18.firstContext = null);
    }
    function eh(a18) {
      var b4 = a18._currentValue;
      if (Zg !== a18) if (a18 = { context: a18, memoizedValue: b4, next: null }, null === Yg) {
        if (null === Xg) throw Error(p11(308));
        Yg = a18;
        Xg.dependencies = { lanes: 0, firstContext: a18 };
      } else Yg = Yg.next = a18;
      return b4;
    }
    var fh = null;
    function gh(a18) {
      null === fh ? fh = [a18] : fh.push(a18);
    }
    function hh(a18, b4, c15, d12) {
      var e14 = b4.interleaved;
      null === e14 ? (c15.next = c15, gh(b4)) : (c15.next = e14.next, e14.next = c15);
      b4.interleaved = c15;
      return ih(a18, d12);
    }
    function ih(a18, b4) {
      a18.lanes |= b4;
      var c15 = a18.alternate;
      null !== c15 && (c15.lanes |= b4);
      c15 = a18;
      for (a18 = a18.return; null !== a18; ) a18.childLanes |= b4, c15 = a18.alternate, null !== c15 && (c15.childLanes |= b4), c15 = a18, a18 = a18.return;
      return 3 === c15.tag ? c15.stateNode : null;
    }
    var jh = false;
    function kh(a18) {
      a18.updateQueue = { baseState: a18.memoizedState, firstBaseUpdate: null, lastBaseUpdate: null, shared: { pending: null, interleaved: null, lanes: 0 }, effects: null };
    }
    function lh(a18, b4) {
      a18 = a18.updateQueue;
      b4.updateQueue === a18 && (b4.updateQueue = { baseState: a18.baseState, firstBaseUpdate: a18.firstBaseUpdate, lastBaseUpdate: a18.lastBaseUpdate, shared: a18.shared, effects: a18.effects });
    }
    function mh(a18, b4) {
      return { eventTime: a18, lane: b4, tag: 0, payload: null, callback: null, next: null };
    }
    function nh(a18, b4, c15) {
      var d12 = a18.updateQueue;
      if (null === d12) return null;
      d12 = d12.shared;
      if (0 !== (K3 & 2)) {
        var e14 = d12.pending;
        null === e14 ? b4.next = b4 : (b4.next = e14.next, e14.next = b4);
        d12.pending = b4;
        return ih(a18, c15);
      }
      e14 = d12.interleaved;
      null === e14 ? (b4.next = b4, gh(d12)) : (b4.next = e14.next, e14.next = b4);
      d12.interleaved = b4;
      return ih(a18, c15);
    }
    function oh(a18, b4, c15) {
      b4 = b4.updateQueue;
      if (null !== b4 && (b4 = b4.shared, 0 !== (c15 & 4194240))) {
        var d12 = b4.lanes;
        d12 &= a18.pendingLanes;
        c15 |= d12;
        b4.lanes = c15;
        Cc(a18, c15);
      }
    }
    function ph(a18, b4) {
      var c15 = a18.updateQueue, d12 = a18.alternate;
      if (null !== d12 && (d12 = d12.updateQueue, c15 === d12)) {
        var e14 = null, f15 = null;
        c15 = c15.firstBaseUpdate;
        if (null !== c15) {
          do {
            var g7 = { eventTime: c15.eventTime, lane: c15.lane, tag: c15.tag, payload: c15.payload, callback: c15.callback, next: null };
            null === f15 ? e14 = f15 = g7 : f15 = f15.next = g7;
            c15 = c15.next;
          } while (null !== c15);
          null === f15 ? e14 = f15 = b4 : f15 = f15.next = b4;
        } else e14 = f15 = b4;
        c15 = { baseState: d12.baseState, firstBaseUpdate: e14, lastBaseUpdate: f15, shared: d12.shared, effects: d12.effects };
        a18.updateQueue = c15;
        return;
      }
      a18 = c15.lastBaseUpdate;
      null === a18 ? c15.firstBaseUpdate = b4 : a18.next = b4;
      c15.lastBaseUpdate = b4;
    }
    function qh(a18, b4, c15, d12) {
      var e14 = a18.updateQueue;
      jh = false;
      var f15 = e14.firstBaseUpdate, g7 = e14.lastBaseUpdate, h6 = e14.shared.pending;
      if (null !== h6) {
        e14.shared.pending = null;
        var k5 = h6, l12 = k5.next;
        k5.next = null;
        null === g7 ? f15 = l12 : g7.next = l12;
        g7 = k5;
        var m12 = a18.alternate;
        null !== m12 && (m12 = m12.updateQueue, h6 = m12.lastBaseUpdate, h6 !== g7 && (null === h6 ? m12.firstBaseUpdate = l12 : h6.next = l12, m12.lastBaseUpdate = k5));
      }
      if (null !== f15) {
        var q2 = e14.baseState;
        g7 = 0;
        m12 = l12 = k5 = null;
        h6 = f15;
        do {
          var r7 = h6.lane, y4 = h6.eventTime;
          if ((d12 & r7) === r7) {
            null !== m12 && (m12 = m12.next = {
              eventTime: y4,
              lane: 0,
              tag: h6.tag,
              payload: h6.payload,
              callback: h6.callback,
              next: null
            });
            a: {
              var n13 = a18, t20 = h6;
              r7 = b4;
              y4 = c15;
              switch (t20.tag) {
                case 1:
                  n13 = t20.payload;
                  if ("function" === typeof n13) {
                    q2 = n13.call(y4, q2, r7);
                    break a;
                  }
                  q2 = n13;
                  break a;
                case 3:
                  n13.flags = n13.flags & -65537 | 128;
                case 0:
                  n13 = t20.payload;
                  r7 = "function" === typeof n13 ? n13.call(y4, q2, r7) : n13;
                  if (null === r7 || void 0 === r7) break a;
                  q2 = A4({}, q2, r7);
                  break a;
                case 2:
                  jh = true;
              }
            }
            null !== h6.callback && 0 !== h6.lane && (a18.flags |= 64, r7 = e14.effects, null === r7 ? e14.effects = [h6] : r7.push(h6));
          } else y4 = { eventTime: y4, lane: r7, tag: h6.tag, payload: h6.payload, callback: h6.callback, next: null }, null === m12 ? (l12 = m12 = y4, k5 = q2) : m12 = m12.next = y4, g7 |= r7;
          h6 = h6.next;
          if (null === h6) if (h6 = e14.shared.pending, null === h6) break;
          else r7 = h6, h6 = r7.next, r7.next = null, e14.lastBaseUpdate = r7, e14.shared.pending = null;
        } while (1);
        null === m12 && (k5 = q2);
        e14.baseState = k5;
        e14.firstBaseUpdate = l12;
        e14.lastBaseUpdate = m12;
        b4 = e14.shared.interleaved;
        if (null !== b4) {
          e14 = b4;
          do
            g7 |= e14.lane, e14 = e14.next;
          while (e14 !== b4);
        } else null === f15 && (e14.shared.lanes = 0);
        rh |= g7;
        a18.lanes = g7;
        a18.memoizedState = q2;
      }
    }
    function sh(a18, b4, c15) {
      a18 = b4.effects;
      b4.effects = null;
      if (null !== a18) for (b4 = 0; b4 < a18.length; b4++) {
        var d12 = a18[b4], e14 = d12.callback;
        if (null !== e14) {
          d12.callback = null;
          d12 = c15;
          if ("function" !== typeof e14) throw Error(p11(191, e14));
          e14.call(d12);
        }
      }
    }
    var th = {};
    var uh = Uf(th);
    var vh2 = Uf(th);
    var wh = Uf(th);
    function xh(a18) {
      if (a18 === th) throw Error(p11(174));
      return a18;
    }
    function yh(a18, b4) {
      G3(wh, b4);
      G3(vh2, a18);
      G3(uh, th);
      a18 = b4.nodeType;
      switch (a18) {
        case 9:
        case 11:
          b4 = (b4 = b4.documentElement) ? b4.namespaceURI : lb(null, "");
          break;
        default:
          a18 = 8 === a18 ? b4.parentNode : b4, b4 = a18.namespaceURI || null, a18 = a18.tagName, b4 = lb(b4, a18);
      }
      E6(uh);
      G3(uh, b4);
    }
    function zh() {
      E6(uh);
      E6(vh2);
      E6(wh);
    }
    function Ah(a18) {
      xh(wh.current);
      var b4 = xh(uh.current);
      var c15 = lb(b4, a18.type);
      b4 !== c15 && (G3(vh2, a18), G3(uh, c15));
    }
    function Bh(a18) {
      vh2.current === a18 && (E6(uh), E6(vh2));
    }
    var L3 = Uf(0);
    function Ch(a18) {
      for (var b4 = a18; null !== b4; ) {
        if (13 === b4.tag) {
          var c15 = b4.memoizedState;
          if (null !== c15 && (c15 = c15.dehydrated, null === c15 || "$?" === c15.data || "$!" === c15.data)) return b4;
        } else if (19 === b4.tag && void 0 !== b4.memoizedProps.revealOrder) {
          if (0 !== (b4.flags & 128)) return b4;
        } else if (null !== b4.child) {
          b4.child.return = b4;
          b4 = b4.child;
          continue;
        }
        if (b4 === a18) break;
        for (; null === b4.sibling; ) {
          if (null === b4.return || b4.return === a18) return null;
          b4 = b4.return;
        }
        b4.sibling.return = b4.return;
        b4 = b4.sibling;
      }
      return null;
    }
    var Dh = [];
    function Eh() {
      for (var a18 = 0; a18 < Dh.length; a18++) Dh[a18]._workInProgressVersionPrimary = null;
      Dh.length = 0;
    }
    var Fh = ua.ReactCurrentDispatcher;
    var Gh = ua.ReactCurrentBatchConfig;
    var Hh = 0;
    var M6 = null;
    var N4 = null;
    var O5 = null;
    var Ih = false;
    var Jh = false;
    var Kh = 0;
    var Lh = 0;
    function P4() {
      throw Error(p11(321));
    }
    function Mh(a18, b4) {
      if (null === b4) return false;
      for (var c15 = 0; c15 < b4.length && c15 < a18.length; c15++) if (!He5(a18[c15], b4[c15])) return false;
      return true;
    }
    function Nh(a18, b4, c15, d12, e14, f15) {
      Hh = f15;
      M6 = b4;
      b4.memoizedState = null;
      b4.updateQueue = null;
      b4.lanes = 0;
      Fh.current = null === a18 || null === a18.memoizedState ? Oh : Ph;
      a18 = c15(d12, e14);
      if (Jh) {
        f15 = 0;
        do {
          Jh = false;
          Kh = 0;
          if (25 <= f15) throw Error(p11(301));
          f15 += 1;
          O5 = N4 = null;
          b4.updateQueue = null;
          Fh.current = Qh;
          a18 = c15(d12, e14);
        } while (Jh);
      }
      Fh.current = Rh;
      b4 = null !== N4 && null !== N4.next;
      Hh = 0;
      O5 = N4 = M6 = null;
      Ih = false;
      if (b4) throw Error(p11(300));
      return a18;
    }
    function Sh() {
      var a18 = 0 !== Kh;
      Kh = 0;
      return a18;
    }
    function Th() {
      var a18 = { memoizedState: null, baseState: null, baseQueue: null, queue: null, next: null };
      null === O5 ? M6.memoizedState = O5 = a18 : O5 = O5.next = a18;
      return O5;
    }
    function Uh() {
      if (null === N4) {
        var a18 = M6.alternate;
        a18 = null !== a18 ? a18.memoizedState : null;
      } else a18 = N4.next;
      var b4 = null === O5 ? M6.memoizedState : O5.next;
      if (null !== b4) O5 = b4, N4 = a18;
      else {
        if (null === a18) throw Error(p11(310));
        N4 = a18;
        a18 = { memoizedState: N4.memoizedState, baseState: N4.baseState, baseQueue: N4.baseQueue, queue: N4.queue, next: null };
        null === O5 ? M6.memoizedState = O5 = a18 : O5 = O5.next = a18;
      }
      return O5;
    }
    function Vh(a18, b4) {
      return "function" === typeof b4 ? b4(a18) : b4;
    }
    function Wh(a18) {
      var b4 = Uh(), c15 = b4.queue;
      if (null === c15) throw Error(p11(311));
      c15.lastRenderedReducer = a18;
      var d12 = N4, e14 = d12.baseQueue, f15 = c15.pending;
      if (null !== f15) {
        if (null !== e14) {
          var g7 = e14.next;
          e14.next = f15.next;
          f15.next = g7;
        }
        d12.baseQueue = e14 = f15;
        c15.pending = null;
      }
      if (null !== e14) {
        f15 = e14.next;
        d12 = d12.baseState;
        var h6 = g7 = null, k5 = null, l12 = f15;
        do {
          var m12 = l12.lane;
          if ((Hh & m12) === m12) null !== k5 && (k5 = k5.next = { lane: 0, action: l12.action, hasEagerState: l12.hasEagerState, eagerState: l12.eagerState, next: null }), d12 = l12.hasEagerState ? l12.eagerState : a18(d12, l12.action);
          else {
            var q2 = {
              lane: m12,
              action: l12.action,
              hasEagerState: l12.hasEagerState,
              eagerState: l12.eagerState,
              next: null
            };
            null === k5 ? (h6 = k5 = q2, g7 = d12) : k5 = k5.next = q2;
            M6.lanes |= m12;
            rh |= m12;
          }
          l12 = l12.next;
        } while (null !== l12 && l12 !== f15);
        null === k5 ? g7 = d12 : k5.next = h6;
        He5(d12, b4.memoizedState) || (dh = true);
        b4.memoizedState = d12;
        b4.baseState = g7;
        b4.baseQueue = k5;
        c15.lastRenderedState = d12;
      }
      a18 = c15.interleaved;
      if (null !== a18) {
        e14 = a18;
        do
          f15 = e14.lane, M6.lanes |= f15, rh |= f15, e14 = e14.next;
        while (e14 !== a18);
      } else null === e14 && (c15.lanes = 0);
      return [b4.memoizedState, c15.dispatch];
    }
    function Xh(a18) {
      var b4 = Uh(), c15 = b4.queue;
      if (null === c15) throw Error(p11(311));
      c15.lastRenderedReducer = a18;
      var d12 = c15.dispatch, e14 = c15.pending, f15 = b4.memoizedState;
      if (null !== e14) {
        c15.pending = null;
        var g7 = e14 = e14.next;
        do
          f15 = a18(f15, g7.action), g7 = g7.next;
        while (g7 !== e14);
        He5(f15, b4.memoizedState) || (dh = true);
        b4.memoizedState = f15;
        null === b4.baseQueue && (b4.baseState = f15);
        c15.lastRenderedState = f15;
      }
      return [f15, d12];
    }
    function Yh() {
    }
    function Zh(a18, b4) {
      var c15 = M6, d12 = Uh(), e14 = b4(), f15 = !He5(d12.memoizedState, e14);
      f15 && (d12.memoizedState = e14, dh = true);
      d12 = d12.queue;
      $h(ai.bind(null, c15, d12, a18), [a18]);
      if (d12.getSnapshot !== b4 || f15 || null !== O5 && O5.memoizedState.tag & 1) {
        c15.flags |= 2048;
        bi2(9, ci2.bind(null, c15, d12, e14, b4), void 0, null);
        if (null === Q4) throw Error(p11(349));
        0 !== (Hh & 30) || di2(c15, b4, e14);
      }
      return e14;
    }
    function di2(a18, b4, c15) {
      a18.flags |= 16384;
      a18 = { getSnapshot: b4, value: c15 };
      b4 = M6.updateQueue;
      null === b4 ? (b4 = { lastEffect: null, stores: null }, M6.updateQueue = b4, b4.stores = [a18]) : (c15 = b4.stores, null === c15 ? b4.stores = [a18] : c15.push(a18));
    }
    function ci2(a18, b4, c15, d12) {
      b4.value = c15;
      b4.getSnapshot = d12;
      ei(b4) && fi2(a18);
    }
    function ai(a18, b4, c15) {
      return c15(function() {
        ei(b4) && fi2(a18);
      });
    }
    function ei(a18) {
      var b4 = a18.getSnapshot;
      a18 = a18.value;
      try {
        var c15 = b4();
        return !He5(a18, c15);
      } catch (d12) {
        return true;
      }
    }
    function fi2(a18) {
      var b4 = ih(a18, 1);
      null !== b4 && gi2(b4, a18, 1, -1);
    }
    function hi2(a18) {
      var b4 = Th();
      "function" === typeof a18 && (a18 = a18());
      b4.memoizedState = b4.baseState = a18;
      a18 = { pending: null, interleaved: null, lanes: 0, dispatch: null, lastRenderedReducer: Vh, lastRenderedState: a18 };
      b4.queue = a18;
      a18 = a18.dispatch = ii.bind(null, M6, a18);
      return [b4.memoizedState, a18];
    }
    function bi2(a18, b4, c15, d12) {
      a18 = { tag: a18, create: b4, destroy: c15, deps: d12, next: null };
      b4 = M6.updateQueue;
      null === b4 ? (b4 = { lastEffect: null, stores: null }, M6.updateQueue = b4, b4.lastEffect = a18.next = a18) : (c15 = b4.lastEffect, null === c15 ? b4.lastEffect = a18.next = a18 : (d12 = c15.next, c15.next = a18, a18.next = d12, b4.lastEffect = a18));
      return a18;
    }
    function ji2() {
      return Uh().memoizedState;
    }
    function ki2(a18, b4, c15, d12) {
      var e14 = Th();
      M6.flags |= a18;
      e14.memoizedState = bi2(1 | b4, c15, void 0, void 0 === d12 ? null : d12);
    }
    function li(a18, b4, c15, d12) {
      var e14 = Uh();
      d12 = void 0 === d12 ? null : d12;
      var f15 = void 0;
      if (null !== N4) {
        var g7 = N4.memoizedState;
        f15 = g7.destroy;
        if (null !== d12 && Mh(d12, g7.deps)) {
          e14.memoizedState = bi2(b4, c15, f15, d12);
          return;
        }
      }
      M6.flags |= a18;
      e14.memoizedState = bi2(1 | b4, c15, f15, d12);
    }
    function mi2(a18, b4) {
      return ki2(8390656, 8, a18, b4);
    }
    function $h(a18, b4) {
      return li(2048, 8, a18, b4);
    }
    function ni(a18, b4) {
      return li(4, 2, a18, b4);
    }
    function oi(a18, b4) {
      return li(4, 4, a18, b4);
    }
    function pi2(a18, b4) {
      if ("function" === typeof b4) return a18 = a18(), b4(a18), function() {
        b4(null);
      };
      if (null !== b4 && void 0 !== b4) return a18 = a18(), b4.current = a18, function() {
        b4.current = null;
      };
    }
    function qi2(a18, b4, c15) {
      c15 = null !== c15 && void 0 !== c15 ? c15.concat([a18]) : null;
      return li(4, 4, pi2.bind(null, b4, a18), c15);
    }
    function ri() {
    }
    function si(a18, b4) {
      var c15 = Uh();
      b4 = void 0 === b4 ? null : b4;
      var d12 = c15.memoizedState;
      if (null !== d12 && null !== b4 && Mh(b4, d12[1])) return d12[0];
      c15.memoizedState = [a18, b4];
      return a18;
    }
    function ti(a18, b4) {
      var c15 = Uh();
      b4 = void 0 === b4 ? null : b4;
      var d12 = c15.memoizedState;
      if (null !== d12 && null !== b4 && Mh(b4, d12[1])) return d12[0];
      a18 = a18();
      c15.memoizedState = [a18, b4];
      return a18;
    }
    function ui(a18, b4, c15) {
      if (0 === (Hh & 21)) return a18.baseState && (a18.baseState = false, dh = true), a18.memoizedState = c15;
      He5(c15, b4) || (c15 = yc(), M6.lanes |= c15, rh |= c15, a18.baseState = true);
      return b4;
    }
    function vi2(a18, b4) {
      var c15 = C5;
      C5 = 0 !== c15 && 4 > c15 ? c15 : 4;
      a18(true);
      var d12 = Gh.transition;
      Gh.transition = {};
      try {
        a18(false), b4();
      } finally {
        C5 = c15, Gh.transition = d12;
      }
    }
    function wi2() {
      return Uh().memoizedState;
    }
    function xi(a18, b4, c15) {
      var d12 = yi2(a18);
      c15 = { lane: d12, action: c15, hasEagerState: false, eagerState: null, next: null };
      if (zi2(a18)) Ai2(b4, c15);
      else if (c15 = hh(a18, b4, c15, d12), null !== c15) {
        var e14 = R10();
        gi2(c15, a18, d12, e14);
        Bi2(c15, b4, d12);
      }
    }
    function ii(a18, b4, c15) {
      var d12 = yi2(a18), e14 = { lane: d12, action: c15, hasEagerState: false, eagerState: null, next: null };
      if (zi2(a18)) Ai2(b4, e14);
      else {
        var f15 = a18.alternate;
        if (0 === a18.lanes && (null === f15 || 0 === f15.lanes) && (f15 = b4.lastRenderedReducer, null !== f15)) try {
          var g7 = b4.lastRenderedState, h6 = f15(g7, c15);
          e14.hasEagerState = true;
          e14.eagerState = h6;
          if (He5(h6, g7)) {
            var k5 = b4.interleaved;
            null === k5 ? (e14.next = e14, gh(b4)) : (e14.next = k5.next, k5.next = e14);
            b4.interleaved = e14;
            return;
          }
        } catch (l12) {
        } finally {
        }
        c15 = hh(a18, b4, e14, d12);
        null !== c15 && (e14 = R10(), gi2(c15, a18, d12, e14), Bi2(c15, b4, d12));
      }
    }
    function zi2(a18) {
      var b4 = a18.alternate;
      return a18 === M6 || null !== b4 && b4 === M6;
    }
    function Ai2(a18, b4) {
      Jh = Ih = true;
      var c15 = a18.pending;
      null === c15 ? b4.next = b4 : (b4.next = c15.next, c15.next = b4);
      a18.pending = b4;
    }
    function Bi2(a18, b4, c15) {
      if (0 !== (c15 & 4194240)) {
        var d12 = b4.lanes;
        d12 &= a18.pendingLanes;
        c15 |= d12;
        b4.lanes = c15;
        Cc(a18, c15);
      }
    }
    var Rh = { readContext: eh, useCallback: P4, useContext: P4, useEffect: P4, useImperativeHandle: P4, useInsertionEffect: P4, useLayoutEffect: P4, useMemo: P4, useReducer: P4, useRef: P4, useState: P4, useDebugValue: P4, useDeferredValue: P4, useTransition: P4, useMutableSource: P4, useSyncExternalStore: P4, useId: P4, unstable_isNewReconciler: false };
    var Oh = { readContext: eh, useCallback: function(a18, b4) {
      Th().memoizedState = [a18, void 0 === b4 ? null : b4];
      return a18;
    }, useContext: eh, useEffect: mi2, useImperativeHandle: function(a18, b4, c15) {
      c15 = null !== c15 && void 0 !== c15 ? c15.concat([a18]) : null;
      return ki2(
        4194308,
        4,
        pi2.bind(null, b4, a18),
        c15
      );
    }, useLayoutEffect: function(a18, b4) {
      return ki2(4194308, 4, a18, b4);
    }, useInsertionEffect: function(a18, b4) {
      return ki2(4, 2, a18, b4);
    }, useMemo: function(a18, b4) {
      var c15 = Th();
      b4 = void 0 === b4 ? null : b4;
      a18 = a18();
      c15.memoizedState = [a18, b4];
      return a18;
    }, useReducer: function(a18, b4, c15) {
      var d12 = Th();
      b4 = void 0 !== c15 ? c15(b4) : b4;
      d12.memoizedState = d12.baseState = b4;
      a18 = { pending: null, interleaved: null, lanes: 0, dispatch: null, lastRenderedReducer: a18, lastRenderedState: b4 };
      d12.queue = a18;
      a18 = a18.dispatch = xi.bind(null, M6, a18);
      return [d12.memoizedState, a18];
    }, useRef: function(a18) {
      var b4 = Th();
      a18 = { current: a18 };
      return b4.memoizedState = a18;
    }, useState: hi2, useDebugValue: ri, useDeferredValue: function(a18) {
      return Th().memoizedState = a18;
    }, useTransition: function() {
      var a18 = hi2(false), b4 = a18[0];
      a18 = vi2.bind(null, a18[1]);
      Th().memoizedState = a18;
      return [b4, a18];
    }, useMutableSource: function() {
    }, useSyncExternalStore: function(a18, b4, c15) {
      var d12 = M6, e14 = Th();
      if (I6) {
        if (void 0 === c15) throw Error(p11(407));
        c15 = c15();
      } else {
        c15 = b4();
        if (null === Q4) throw Error(p11(349));
        0 !== (Hh & 30) || di2(d12, b4, c15);
      }
      e14.memoizedState = c15;
      var f15 = { value: c15, getSnapshot: b4 };
      e14.queue = f15;
      mi2(ai.bind(
        null,
        d12,
        f15,
        a18
      ), [a18]);
      d12.flags |= 2048;
      bi2(9, ci2.bind(null, d12, f15, c15, b4), void 0, null);
      return c15;
    }, useId: function() {
      var a18 = Th(), b4 = Q4.identifierPrefix;
      if (I6) {
        var c15 = sg;
        var d12 = rg;
        c15 = (d12 & ~(1 << 32 - oc(d12) - 1)).toString(32) + c15;
        b4 = ":" + b4 + "R" + c15;
        c15 = Kh++;
        0 < c15 && (b4 += "H" + c15.toString(32));
        b4 += ":";
      } else c15 = Lh++, b4 = ":" + b4 + "r" + c15.toString(32) + ":";
      return a18.memoizedState = b4;
    }, unstable_isNewReconciler: false };
    var Ph = {
      readContext: eh,
      useCallback: si,
      useContext: eh,
      useEffect: $h,
      useImperativeHandle: qi2,
      useInsertionEffect: ni,
      useLayoutEffect: oi,
      useMemo: ti,
      useReducer: Wh,
      useRef: ji2,
      useState: function() {
        return Wh(Vh);
      },
      useDebugValue: ri,
      useDeferredValue: function(a18) {
        var b4 = Uh();
        return ui(b4, N4.memoizedState, a18);
      },
      useTransition: function() {
        var a18 = Wh(Vh)[0], b4 = Uh().memoizedState;
        return [a18, b4];
      },
      useMutableSource: Yh,
      useSyncExternalStore: Zh,
      useId: wi2,
      unstable_isNewReconciler: false
    };
    var Qh = { readContext: eh, useCallback: si, useContext: eh, useEffect: $h, useImperativeHandle: qi2, useInsertionEffect: ni, useLayoutEffect: oi, useMemo: ti, useReducer: Xh, useRef: ji2, useState: function() {
      return Xh(Vh);
    }, useDebugValue: ri, useDeferredValue: function(a18) {
      var b4 = Uh();
      return null === N4 ? b4.memoizedState = a18 : ui(b4, N4.memoizedState, a18);
    }, useTransition: function() {
      var a18 = Xh(Vh)[0], b4 = Uh().memoizedState;
      return [a18, b4];
    }, useMutableSource: Yh, useSyncExternalStore: Zh, useId: wi2, unstable_isNewReconciler: false };
    function Ci2(a18, b4) {
      if (a18 && a18.defaultProps) {
        b4 = A4({}, b4);
        a18 = a18.defaultProps;
        for (var c15 in a18) void 0 === b4[c15] && (b4[c15] = a18[c15]);
        return b4;
      }
      return b4;
    }
    function Di(a18, b4, c15, d12) {
      b4 = a18.memoizedState;
      c15 = c15(d12, b4);
      c15 = null === c15 || void 0 === c15 ? b4 : A4({}, b4, c15);
      a18.memoizedState = c15;
      0 === a18.lanes && (a18.updateQueue.baseState = c15);
    }
    var Ei2 = { isMounted: function(a18) {
      return (a18 = a18._reactInternals) ? Vb(a18) === a18 : false;
    }, enqueueSetState: function(a18, b4, c15) {
      a18 = a18._reactInternals;
      var d12 = R10(), e14 = yi2(a18), f15 = mh(d12, e14);
      f15.payload = b4;
      void 0 !== c15 && null !== c15 && (f15.callback = c15);
      b4 = nh(a18, f15, e14);
      null !== b4 && (gi2(b4, a18, e14, d12), oh(b4, a18, e14));
    }, enqueueReplaceState: function(a18, b4, c15) {
      a18 = a18._reactInternals;
      var d12 = R10(), e14 = yi2(a18), f15 = mh(d12, e14);
      f15.tag = 1;
      f15.payload = b4;
      void 0 !== c15 && null !== c15 && (f15.callback = c15);
      b4 = nh(a18, f15, e14);
      null !== b4 && (gi2(b4, a18, e14, d12), oh(b4, a18, e14));
    }, enqueueForceUpdate: function(a18, b4) {
      a18 = a18._reactInternals;
      var c15 = R10(), d12 = yi2(a18), e14 = mh(c15, d12);
      e14.tag = 2;
      void 0 !== b4 && null !== b4 && (e14.callback = b4);
      b4 = nh(a18, e14, d12);
      null !== b4 && (gi2(b4, a18, d12, c15), oh(b4, a18, d12));
    } };
    function Fi2(a18, b4, c15, d12, e14, f15, g7) {
      a18 = a18.stateNode;
      return "function" === typeof a18.shouldComponentUpdate ? a18.shouldComponentUpdate(d12, f15, g7) : b4.prototype && b4.prototype.isPureReactComponent ? !Ie3(c15, d12) || !Ie3(e14, f15) : true;
    }
    function Gi(a18, b4, c15) {
      var d12 = false, e14 = Vf;
      var f15 = b4.contextType;
      "object" === typeof f15 && null !== f15 ? f15 = eh(f15) : (e14 = Zf(b4) ? Xf : H4.current, d12 = b4.contextTypes, f15 = (d12 = null !== d12 && void 0 !== d12) ? Yf(a18, e14) : Vf);
      b4 = new b4(c15, f15);
      a18.memoizedState = null !== b4.state && void 0 !== b4.state ? b4.state : null;
      b4.updater = Ei2;
      a18.stateNode = b4;
      b4._reactInternals = a18;
      d12 && (a18 = a18.stateNode, a18.__reactInternalMemoizedUnmaskedChildContext = e14, a18.__reactInternalMemoizedMaskedChildContext = f15);
      return b4;
    }
    function Hi(a18, b4, c15, d12) {
      a18 = b4.state;
      "function" === typeof b4.componentWillReceiveProps && b4.componentWillReceiveProps(c15, d12);
      "function" === typeof b4.UNSAFE_componentWillReceiveProps && b4.UNSAFE_componentWillReceiveProps(c15, d12);
      b4.state !== a18 && Ei2.enqueueReplaceState(b4, b4.state, null);
    }
    function Ii2(a18, b4, c15, d12) {
      var e14 = a18.stateNode;
      e14.props = c15;
      e14.state = a18.memoizedState;
      e14.refs = {};
      kh(a18);
      var f15 = b4.contextType;
      "object" === typeof f15 && null !== f15 ? e14.context = eh(f15) : (f15 = Zf(b4) ? Xf : H4.current, e14.context = Yf(a18, f15));
      e14.state = a18.memoizedState;
      f15 = b4.getDerivedStateFromProps;
      "function" === typeof f15 && (Di(a18, b4, f15, c15), e14.state = a18.memoizedState);
      "function" === typeof b4.getDerivedStateFromProps || "function" === typeof e14.getSnapshotBeforeUpdate || "function" !== typeof e14.UNSAFE_componentWillMount && "function" !== typeof e14.componentWillMount || (b4 = e14.state, "function" === typeof e14.componentWillMount && e14.componentWillMount(), "function" === typeof e14.UNSAFE_componentWillMount && e14.UNSAFE_componentWillMount(), b4 !== e14.state && Ei2.enqueueReplaceState(e14, e14.state, null), qh(a18, c15, e14, d12), e14.state = a18.memoizedState);
      "function" === typeof e14.componentDidMount && (a18.flags |= 4194308);
    }
    function Ji(a18, b4) {
      try {
        var c15 = "", d12 = b4;
        do
          c15 += Pa(d12), d12 = d12.return;
        while (d12);
        var e14 = c15;
      } catch (f15) {
        e14 = "\nError generating stack: " + f15.message + "\n" + f15.stack;
      }
      return { value: a18, source: b4, stack: e14, digest: null };
    }
    function Ki(a18, b4, c15) {
      return { value: a18, source: null, stack: null != c15 ? c15 : null, digest: null != b4 ? b4 : null };
    }
    function Li(a18, b4) {
      try {
        console.error(b4.value);
      } catch (c15) {
        setTimeout(function() {
          throw c15;
        });
      }
    }
    var Mi = "function" === typeof WeakMap ? WeakMap : Map;
    function Ni(a18, b4, c15) {
      c15 = mh(-1, c15);
      c15.tag = 3;
      c15.payload = { element: null };
      var d12 = b4.value;
      c15.callback = function() {
        Oi2 || (Oi2 = true, Pi2 = d12);
        Li(a18, b4);
      };
      return c15;
    }
    function Qi(a18, b4, c15) {
      c15 = mh(-1, c15);
      c15.tag = 3;
      var d12 = a18.type.getDerivedStateFromError;
      if ("function" === typeof d12) {
        var e14 = b4.value;
        c15.payload = function() {
          return d12(e14);
        };
        c15.callback = function() {
          Li(a18, b4);
        };
      }
      var f15 = a18.stateNode;
      null !== f15 && "function" === typeof f15.componentDidCatch && (c15.callback = function() {
        Li(a18, b4);
        "function" !== typeof d12 && (null === Ri2 ? Ri2 = /* @__PURE__ */ new Set([this]) : Ri2.add(this));
        var c16 = b4.stack;
        this.componentDidCatch(b4.value, { componentStack: null !== c16 ? c16 : "" });
      });
      return c15;
    }
    function Si2(a18, b4, c15) {
      var d12 = a18.pingCache;
      if (null === d12) {
        d12 = a18.pingCache = new Mi();
        var e14 = /* @__PURE__ */ new Set();
        d12.set(b4, e14);
      } else e14 = d12.get(b4), void 0 === e14 && (e14 = /* @__PURE__ */ new Set(), d12.set(b4, e14));
      e14.has(c15) || (e14.add(c15), a18 = Ti2.bind(null, a18, b4, c15), b4.then(a18, a18));
    }
    function Ui(a18) {
      do {
        var b4;
        if (b4 = 13 === a18.tag) b4 = a18.memoizedState, b4 = null !== b4 ? null !== b4.dehydrated ? true : false : true;
        if (b4) return a18;
        a18 = a18.return;
      } while (null !== a18);
      return null;
    }
    function Vi(a18, b4, c15, d12, e14) {
      if (0 === (a18.mode & 1)) return a18 === b4 ? a18.flags |= 65536 : (a18.flags |= 128, c15.flags |= 131072, c15.flags &= -52805, 1 === c15.tag && (null === c15.alternate ? c15.tag = 17 : (b4 = mh(-1, 1), b4.tag = 2, nh(c15, b4, 1))), c15.lanes |= 1), a18;
      a18.flags |= 65536;
      a18.lanes = e14;
      return a18;
    }
    var Wi2 = ua.ReactCurrentOwner;
    var dh = false;
    function Xi(a18, b4, c15, d12) {
      b4.child = null === a18 ? Vg(b4, null, c15, d12) : Ug(b4, a18.child, c15, d12);
    }
    function Yi(a18, b4, c15, d12, e14) {
      c15 = c15.render;
      var f15 = b4.ref;
      ch(b4, e14);
      d12 = Nh(a18, b4, c15, d12, f15, e14);
      c15 = Sh();
      if (null !== a18 && !dh) return b4.updateQueue = a18.updateQueue, b4.flags &= -2053, a18.lanes &= ~e14, Zi(a18, b4, e14);
      I6 && c15 && vg(b4);
      b4.flags |= 1;
      Xi(a18, b4, d12, e14);
      return b4.child;
    }
    function $i(a18, b4, c15, d12, e14) {
      if (null === a18) {
        var f15 = c15.type;
        if ("function" === typeof f15 && !aj(f15) && void 0 === f15.defaultProps && null === c15.compare && void 0 === c15.defaultProps) return b4.tag = 15, b4.type = f15, bj(a18, b4, f15, d12, e14);
        a18 = Rg(c15.type, null, d12, b4, b4.mode, e14);
        a18.ref = b4.ref;
        a18.return = b4;
        return b4.child = a18;
      }
      f15 = a18.child;
      if (0 === (a18.lanes & e14)) {
        var g7 = f15.memoizedProps;
        c15 = c15.compare;
        c15 = null !== c15 ? c15 : Ie3;
        if (c15(g7, d12) && a18.ref === b4.ref) return Zi(a18, b4, e14);
      }
      b4.flags |= 1;
      a18 = Pg(f15, d12);
      a18.ref = b4.ref;
      a18.return = b4;
      return b4.child = a18;
    }
    function bj(a18, b4, c15, d12, e14) {
      if (null !== a18) {
        var f15 = a18.memoizedProps;
        if (Ie3(f15, d12) && a18.ref === b4.ref) if (dh = false, b4.pendingProps = d12 = f15, 0 !== (a18.lanes & e14)) 0 !== (a18.flags & 131072) && (dh = true);
        else return b4.lanes = a18.lanes, Zi(a18, b4, e14);
      }
      return cj(a18, b4, c15, d12, e14);
    }
    function dj(a18, b4, c15) {
      var d12 = b4.pendingProps, e14 = d12.children, f15 = null !== a18 ? a18.memoizedState : null;
      if ("hidden" === d12.mode) if (0 === (b4.mode & 1)) b4.memoizedState = { baseLanes: 0, cachePool: null, transitions: null }, G3(ej, fj), fj |= c15;
      else {
        if (0 === (c15 & 1073741824)) return a18 = null !== f15 ? f15.baseLanes | c15 : c15, b4.lanes = b4.childLanes = 1073741824, b4.memoizedState = { baseLanes: a18, cachePool: null, transitions: null }, b4.updateQueue = null, G3(ej, fj), fj |= a18, null;
        b4.memoizedState = { baseLanes: 0, cachePool: null, transitions: null };
        d12 = null !== f15 ? f15.baseLanes : c15;
        G3(ej, fj);
        fj |= d12;
      }
      else null !== f15 ? (d12 = f15.baseLanes | c15, b4.memoizedState = null) : d12 = c15, G3(ej, fj), fj |= d12;
      Xi(a18, b4, e14, c15);
      return b4.child;
    }
    function gj(a18, b4) {
      var c15 = b4.ref;
      if (null === a18 && null !== c15 || null !== a18 && a18.ref !== c15) b4.flags |= 512, b4.flags |= 2097152;
    }
    function cj(a18, b4, c15, d12, e14) {
      var f15 = Zf(c15) ? Xf : H4.current;
      f15 = Yf(b4, f15);
      ch(b4, e14);
      c15 = Nh(a18, b4, c15, d12, f15, e14);
      d12 = Sh();
      if (null !== a18 && !dh) return b4.updateQueue = a18.updateQueue, b4.flags &= -2053, a18.lanes &= ~e14, Zi(a18, b4, e14);
      I6 && d12 && vg(b4);
      b4.flags |= 1;
      Xi(a18, b4, c15, e14);
      return b4.child;
    }
    function hj(a18, b4, c15, d12, e14) {
      if (Zf(c15)) {
        var f15 = true;
        cg(b4);
      } else f15 = false;
      ch(b4, e14);
      if (null === b4.stateNode) ij(a18, b4), Gi(b4, c15, d12), Ii2(b4, c15, d12, e14), d12 = true;
      else if (null === a18) {
        var g7 = b4.stateNode, h6 = b4.memoizedProps;
        g7.props = h6;
        var k5 = g7.context, l12 = c15.contextType;
        "object" === typeof l12 && null !== l12 ? l12 = eh(l12) : (l12 = Zf(c15) ? Xf : H4.current, l12 = Yf(b4, l12));
        var m12 = c15.getDerivedStateFromProps, q2 = "function" === typeof m12 || "function" === typeof g7.getSnapshotBeforeUpdate;
        q2 || "function" !== typeof g7.UNSAFE_componentWillReceiveProps && "function" !== typeof g7.componentWillReceiveProps || (h6 !== d12 || k5 !== l12) && Hi(b4, g7, d12, l12);
        jh = false;
        var r7 = b4.memoizedState;
        g7.state = r7;
        qh(b4, d12, g7, e14);
        k5 = b4.memoizedState;
        h6 !== d12 || r7 !== k5 || Wf.current || jh ? ("function" === typeof m12 && (Di(b4, c15, m12, d12), k5 = b4.memoizedState), (h6 = jh || Fi2(b4, c15, h6, d12, r7, k5, l12)) ? (q2 || "function" !== typeof g7.UNSAFE_componentWillMount && "function" !== typeof g7.componentWillMount || ("function" === typeof g7.componentWillMount && g7.componentWillMount(), "function" === typeof g7.UNSAFE_componentWillMount && g7.UNSAFE_componentWillMount()), "function" === typeof g7.componentDidMount && (b4.flags |= 4194308)) : ("function" === typeof g7.componentDidMount && (b4.flags |= 4194308), b4.memoizedProps = d12, b4.memoizedState = k5), g7.props = d12, g7.state = k5, g7.context = l12, d12 = h6) : ("function" === typeof g7.componentDidMount && (b4.flags |= 4194308), d12 = false);
      } else {
        g7 = b4.stateNode;
        lh(a18, b4);
        h6 = b4.memoizedProps;
        l12 = b4.type === b4.elementType ? h6 : Ci2(b4.type, h6);
        g7.props = l12;
        q2 = b4.pendingProps;
        r7 = g7.context;
        k5 = c15.contextType;
        "object" === typeof k5 && null !== k5 ? k5 = eh(k5) : (k5 = Zf(c15) ? Xf : H4.current, k5 = Yf(b4, k5));
        var y4 = c15.getDerivedStateFromProps;
        (m12 = "function" === typeof y4 || "function" === typeof g7.getSnapshotBeforeUpdate) || "function" !== typeof g7.UNSAFE_componentWillReceiveProps && "function" !== typeof g7.componentWillReceiveProps || (h6 !== q2 || r7 !== k5) && Hi(b4, g7, d12, k5);
        jh = false;
        r7 = b4.memoizedState;
        g7.state = r7;
        qh(b4, d12, g7, e14);
        var n13 = b4.memoizedState;
        h6 !== q2 || r7 !== n13 || Wf.current || jh ? ("function" === typeof y4 && (Di(b4, c15, y4, d12), n13 = b4.memoizedState), (l12 = jh || Fi2(b4, c15, l12, d12, r7, n13, k5) || false) ? (m12 || "function" !== typeof g7.UNSAFE_componentWillUpdate && "function" !== typeof g7.componentWillUpdate || ("function" === typeof g7.componentWillUpdate && g7.componentWillUpdate(d12, n13, k5), "function" === typeof g7.UNSAFE_componentWillUpdate && g7.UNSAFE_componentWillUpdate(d12, n13, k5)), "function" === typeof g7.componentDidUpdate && (b4.flags |= 4), "function" === typeof g7.getSnapshotBeforeUpdate && (b4.flags |= 1024)) : ("function" !== typeof g7.componentDidUpdate || h6 === a18.memoizedProps && r7 === a18.memoizedState || (b4.flags |= 4), "function" !== typeof g7.getSnapshotBeforeUpdate || h6 === a18.memoizedProps && r7 === a18.memoizedState || (b4.flags |= 1024), b4.memoizedProps = d12, b4.memoizedState = n13), g7.props = d12, g7.state = n13, g7.context = k5, d12 = l12) : ("function" !== typeof g7.componentDidUpdate || h6 === a18.memoizedProps && r7 === a18.memoizedState || (b4.flags |= 4), "function" !== typeof g7.getSnapshotBeforeUpdate || h6 === a18.memoizedProps && r7 === a18.memoizedState || (b4.flags |= 1024), d12 = false);
      }
      return jj(a18, b4, c15, d12, f15, e14);
    }
    function jj(a18, b4, c15, d12, e14, f15) {
      gj(a18, b4);
      var g7 = 0 !== (b4.flags & 128);
      if (!d12 && !g7) return e14 && dg(b4, c15, false), Zi(a18, b4, f15);
      d12 = b4.stateNode;
      Wi2.current = b4;
      var h6 = g7 && "function" !== typeof c15.getDerivedStateFromError ? null : d12.render();
      b4.flags |= 1;
      null !== a18 && g7 ? (b4.child = Ug(b4, a18.child, null, f15), b4.child = Ug(b4, null, h6, f15)) : Xi(a18, b4, h6, f15);
      b4.memoizedState = d12.state;
      e14 && dg(b4, c15, true);
      return b4.child;
    }
    function kj(a18) {
      var b4 = a18.stateNode;
      b4.pendingContext ? ag(a18, b4.pendingContext, b4.pendingContext !== b4.context) : b4.context && ag(a18, b4.context, false);
      yh(a18, b4.containerInfo);
    }
    function lj(a18, b4, c15, d12, e14) {
      Ig();
      Jg(e14);
      b4.flags |= 256;
      Xi(a18, b4, c15, d12);
      return b4.child;
    }
    var mj = { dehydrated: null, treeContext: null, retryLane: 0 };
    function nj(a18) {
      return { baseLanes: a18, cachePool: null, transitions: null };
    }
    function oj(a18, b4, c15) {
      var d12 = b4.pendingProps, e14 = L3.current, f15 = false, g7 = 0 !== (b4.flags & 128), h6;
      (h6 = g7) || (h6 = null !== a18 && null === a18.memoizedState ? false : 0 !== (e14 & 2));
      if (h6) f15 = true, b4.flags &= -129;
      else if (null === a18 || null !== a18.memoizedState) e14 |= 1;
      G3(L3, e14 & 1);
      if (null === a18) {
        Eg(b4);
        a18 = b4.memoizedState;
        if (null !== a18 && (a18 = a18.dehydrated, null !== a18)) return 0 === (b4.mode & 1) ? b4.lanes = 1 : "$!" === a18.data ? b4.lanes = 8 : b4.lanes = 1073741824, null;
        g7 = d12.children;
        a18 = d12.fallback;
        return f15 ? (d12 = b4.mode, f15 = b4.child, g7 = { mode: "hidden", children: g7 }, 0 === (d12 & 1) && null !== f15 ? (f15.childLanes = 0, f15.pendingProps = g7) : f15 = pj(g7, d12, 0, null), a18 = Tg(a18, d12, c15, null), f15.return = b4, a18.return = b4, f15.sibling = a18, b4.child = f15, b4.child.memoizedState = nj(c15), b4.memoizedState = mj, a18) : qj(b4, g7);
      }
      e14 = a18.memoizedState;
      if (null !== e14 && (h6 = e14.dehydrated, null !== h6)) return rj(a18, b4, g7, d12, h6, e14, c15);
      if (f15) {
        f15 = d12.fallback;
        g7 = b4.mode;
        e14 = a18.child;
        h6 = e14.sibling;
        var k5 = { mode: "hidden", children: d12.children };
        0 === (g7 & 1) && b4.child !== e14 ? (d12 = b4.child, d12.childLanes = 0, d12.pendingProps = k5, b4.deletions = null) : (d12 = Pg(e14, k5), d12.subtreeFlags = e14.subtreeFlags & 14680064);
        null !== h6 ? f15 = Pg(h6, f15) : (f15 = Tg(f15, g7, c15, null), f15.flags |= 2);
        f15.return = b4;
        d12.return = b4;
        d12.sibling = f15;
        b4.child = d12;
        d12 = f15;
        f15 = b4.child;
        g7 = a18.child.memoizedState;
        g7 = null === g7 ? nj(c15) : { baseLanes: g7.baseLanes | c15, cachePool: null, transitions: g7.transitions };
        f15.memoizedState = g7;
        f15.childLanes = a18.childLanes & ~c15;
        b4.memoizedState = mj;
        return d12;
      }
      f15 = a18.child;
      a18 = f15.sibling;
      d12 = Pg(f15, { mode: "visible", children: d12.children });
      0 === (b4.mode & 1) && (d12.lanes = c15);
      d12.return = b4;
      d12.sibling = null;
      null !== a18 && (c15 = b4.deletions, null === c15 ? (b4.deletions = [a18], b4.flags |= 16) : c15.push(a18));
      b4.child = d12;
      b4.memoizedState = null;
      return d12;
    }
    function qj(a18, b4) {
      b4 = pj({ mode: "visible", children: b4 }, a18.mode, 0, null);
      b4.return = a18;
      return a18.child = b4;
    }
    function sj(a18, b4, c15, d12) {
      null !== d12 && Jg(d12);
      Ug(b4, a18.child, null, c15);
      a18 = qj(b4, b4.pendingProps.children);
      a18.flags |= 2;
      b4.memoizedState = null;
      return a18;
    }
    function rj(a18, b4, c15, d12, e14, f15, g7) {
      if (c15) {
        if (b4.flags & 256) return b4.flags &= -257, d12 = Ki(Error(p11(422))), sj(a18, b4, g7, d12);
        if (null !== b4.memoizedState) return b4.child = a18.child, b4.flags |= 128, null;
        f15 = d12.fallback;
        e14 = b4.mode;
        d12 = pj({ mode: "visible", children: d12.children }, e14, 0, null);
        f15 = Tg(f15, e14, g7, null);
        f15.flags |= 2;
        d12.return = b4;
        f15.return = b4;
        d12.sibling = f15;
        b4.child = d12;
        0 !== (b4.mode & 1) && Ug(b4, a18.child, null, g7);
        b4.child.memoizedState = nj(g7);
        b4.memoizedState = mj;
        return f15;
      }
      if (0 === (b4.mode & 1)) return sj(a18, b4, g7, null);
      if ("$!" === e14.data) {
        d12 = e14.nextSibling && e14.nextSibling.dataset;
        if (d12) var h6 = d12.dgst;
        d12 = h6;
        f15 = Error(p11(419));
        d12 = Ki(f15, d12, void 0);
        return sj(a18, b4, g7, d12);
      }
      h6 = 0 !== (g7 & a18.childLanes);
      if (dh || h6) {
        d12 = Q4;
        if (null !== d12) {
          switch (g7 & -g7) {
            case 4:
              e14 = 2;
              break;
            case 16:
              e14 = 8;
              break;
            case 64:
            case 128:
            case 256:
            case 512:
            case 1024:
            case 2048:
            case 4096:
            case 8192:
            case 16384:
            case 32768:
            case 65536:
            case 131072:
            case 262144:
            case 524288:
            case 1048576:
            case 2097152:
            case 4194304:
            case 8388608:
            case 16777216:
            case 33554432:
            case 67108864:
              e14 = 32;
              break;
            case 536870912:
              e14 = 268435456;
              break;
            default:
              e14 = 0;
          }
          e14 = 0 !== (e14 & (d12.suspendedLanes | g7)) ? 0 : e14;
          0 !== e14 && e14 !== f15.retryLane && (f15.retryLane = e14, ih(a18, e14), gi2(d12, a18, e14, -1));
        }
        tj();
        d12 = Ki(Error(p11(421)));
        return sj(a18, b4, g7, d12);
      }
      if ("$?" === e14.data) return b4.flags |= 128, b4.child = a18.child, b4 = uj.bind(null, a18), e14._reactRetry = b4, null;
      a18 = f15.treeContext;
      yg = Lf(e14.nextSibling);
      xg = b4;
      I6 = true;
      zg = null;
      null !== a18 && (og[pg++] = rg, og[pg++] = sg, og[pg++] = qg, rg = a18.id, sg = a18.overflow, qg = b4);
      b4 = qj(b4, d12.children);
      b4.flags |= 4096;
      return b4;
    }
    function vj(a18, b4, c15) {
      a18.lanes |= b4;
      var d12 = a18.alternate;
      null !== d12 && (d12.lanes |= b4);
      bh(a18.return, b4, c15);
    }
    function wj(a18, b4, c15, d12, e14) {
      var f15 = a18.memoizedState;
      null === f15 ? a18.memoizedState = { isBackwards: b4, rendering: null, renderingStartTime: 0, last: d12, tail: c15, tailMode: e14 } : (f15.isBackwards = b4, f15.rendering = null, f15.renderingStartTime = 0, f15.last = d12, f15.tail = c15, f15.tailMode = e14);
    }
    function xj(a18, b4, c15) {
      var d12 = b4.pendingProps, e14 = d12.revealOrder, f15 = d12.tail;
      Xi(a18, b4, d12.children, c15);
      d12 = L3.current;
      if (0 !== (d12 & 2)) d12 = d12 & 1 | 2, b4.flags |= 128;
      else {
        if (null !== a18 && 0 !== (a18.flags & 128)) a: for (a18 = b4.child; null !== a18; ) {
          if (13 === a18.tag) null !== a18.memoizedState && vj(a18, c15, b4);
          else if (19 === a18.tag) vj(a18, c15, b4);
          else if (null !== a18.child) {
            a18.child.return = a18;
            a18 = a18.child;
            continue;
          }
          if (a18 === b4) break a;
          for (; null === a18.sibling; ) {
            if (null === a18.return || a18.return === b4) break a;
            a18 = a18.return;
          }
          a18.sibling.return = a18.return;
          a18 = a18.sibling;
        }
        d12 &= 1;
      }
      G3(L3, d12);
      if (0 === (b4.mode & 1)) b4.memoizedState = null;
      else switch (e14) {
        case "forwards":
          c15 = b4.child;
          for (e14 = null; null !== c15; ) a18 = c15.alternate, null !== a18 && null === Ch(a18) && (e14 = c15), c15 = c15.sibling;
          c15 = e14;
          null === c15 ? (e14 = b4.child, b4.child = null) : (e14 = c15.sibling, c15.sibling = null);
          wj(b4, false, e14, c15, f15);
          break;
        case "backwards":
          c15 = null;
          e14 = b4.child;
          for (b4.child = null; null !== e14; ) {
            a18 = e14.alternate;
            if (null !== a18 && null === Ch(a18)) {
              b4.child = e14;
              break;
            }
            a18 = e14.sibling;
            e14.sibling = c15;
            c15 = e14;
            e14 = a18;
          }
          wj(b4, true, c15, null, f15);
          break;
        case "together":
          wj(b4, false, null, null, void 0);
          break;
        default:
          b4.memoizedState = null;
      }
      return b4.child;
    }
    function ij(a18, b4) {
      0 === (b4.mode & 1) && null !== a18 && (a18.alternate = null, b4.alternate = null, b4.flags |= 2);
    }
    function Zi(a18, b4, c15) {
      null !== a18 && (b4.dependencies = a18.dependencies);
      rh |= b4.lanes;
      if (0 === (c15 & b4.childLanes)) return null;
      if (null !== a18 && b4.child !== a18.child) throw Error(p11(153));
      if (null !== b4.child) {
        a18 = b4.child;
        c15 = Pg(a18, a18.pendingProps);
        b4.child = c15;
        for (c15.return = b4; null !== a18.sibling; ) a18 = a18.sibling, c15 = c15.sibling = Pg(a18, a18.pendingProps), c15.return = b4;
        c15.sibling = null;
      }
      return b4.child;
    }
    function yj(a18, b4, c15) {
      switch (b4.tag) {
        case 3:
          kj(b4);
          Ig();
          break;
        case 5:
          Ah(b4);
          break;
        case 1:
          Zf(b4.type) && cg(b4);
          break;
        case 4:
          yh(b4, b4.stateNode.containerInfo);
          break;
        case 10:
          var d12 = b4.type._context, e14 = b4.memoizedProps.value;
          G3(Wg, d12._currentValue);
          d12._currentValue = e14;
          break;
        case 13:
          d12 = b4.memoizedState;
          if (null !== d12) {
            if (null !== d12.dehydrated) return G3(L3, L3.current & 1), b4.flags |= 128, null;
            if (0 !== (c15 & b4.child.childLanes)) return oj(a18, b4, c15);
            G3(L3, L3.current & 1);
            a18 = Zi(a18, b4, c15);
            return null !== a18 ? a18.sibling : null;
          }
          G3(L3, L3.current & 1);
          break;
        case 19:
          d12 = 0 !== (c15 & b4.childLanes);
          if (0 !== (a18.flags & 128)) {
            if (d12) return xj(a18, b4, c15);
            b4.flags |= 128;
          }
          e14 = b4.memoizedState;
          null !== e14 && (e14.rendering = null, e14.tail = null, e14.lastEffect = null);
          G3(L3, L3.current);
          if (d12) break;
          else return null;
        case 22:
        case 23:
          return b4.lanes = 0, dj(a18, b4, c15);
      }
      return Zi(a18, b4, c15);
    }
    var zj;
    var Aj;
    var Bj;
    var Cj;
    zj = function(a18, b4) {
      for (var c15 = b4.child; null !== c15; ) {
        if (5 === c15.tag || 6 === c15.tag) a18.appendChild(c15.stateNode);
        else if (4 !== c15.tag && null !== c15.child) {
          c15.child.return = c15;
          c15 = c15.child;
          continue;
        }
        if (c15 === b4) break;
        for (; null === c15.sibling; ) {
          if (null === c15.return || c15.return === b4) return;
          c15 = c15.return;
        }
        c15.sibling.return = c15.return;
        c15 = c15.sibling;
      }
    };
    Aj = function() {
    };
    Bj = function(a18, b4, c15, d12) {
      var e14 = a18.memoizedProps;
      if (e14 !== d12) {
        a18 = b4.stateNode;
        xh(uh.current);
        var f15 = null;
        switch (c15) {
          case "input":
            e14 = Ya(a18, e14);
            d12 = Ya(a18, d12);
            f15 = [];
            break;
          case "select":
            e14 = A4({}, e14, { value: void 0 });
            d12 = A4({}, d12, { value: void 0 });
            f15 = [];
            break;
          case "textarea":
            e14 = gb(a18, e14);
            d12 = gb(a18, d12);
            f15 = [];
            break;
          default:
            "function" !== typeof e14.onClick && "function" === typeof d12.onClick && (a18.onclick = Bf);
        }
        ub(c15, d12);
        var g7;
        c15 = null;
        for (l12 in e14) if (!d12.hasOwnProperty(l12) && e14.hasOwnProperty(l12) && null != e14[l12]) if ("style" === l12) {
          var h6 = e14[l12];
          for (g7 in h6) h6.hasOwnProperty(g7) && (c15 || (c15 = {}), c15[g7] = "");
        } else "dangerouslySetInnerHTML" !== l12 && "children" !== l12 && "suppressContentEditableWarning" !== l12 && "suppressHydrationWarning" !== l12 && "autoFocus" !== l12 && (ea.hasOwnProperty(l12) ? f15 || (f15 = []) : (f15 = f15 || []).push(l12, null));
        for (l12 in d12) {
          var k5 = d12[l12];
          h6 = null != e14 ? e14[l12] : void 0;
          if (d12.hasOwnProperty(l12) && k5 !== h6 && (null != k5 || null != h6)) if ("style" === l12) if (h6) {
            for (g7 in h6) !h6.hasOwnProperty(g7) || k5 && k5.hasOwnProperty(g7) || (c15 || (c15 = {}), c15[g7] = "");
            for (g7 in k5) k5.hasOwnProperty(g7) && h6[g7] !== k5[g7] && (c15 || (c15 = {}), c15[g7] = k5[g7]);
          } else c15 || (f15 || (f15 = []), f15.push(
            l12,
            c15
          )), c15 = k5;
          else "dangerouslySetInnerHTML" === l12 ? (k5 = k5 ? k5.__html : void 0, h6 = h6 ? h6.__html : void 0, null != k5 && h6 !== k5 && (f15 = f15 || []).push(l12, k5)) : "children" === l12 ? "string" !== typeof k5 && "number" !== typeof k5 || (f15 = f15 || []).push(l12, "" + k5) : "suppressContentEditableWarning" !== l12 && "suppressHydrationWarning" !== l12 && (ea.hasOwnProperty(l12) ? (null != k5 && "onScroll" === l12 && D5("scroll", a18), f15 || h6 === k5 || (f15 = [])) : (f15 = f15 || []).push(l12, k5));
        }
        c15 && (f15 = f15 || []).push("style", c15);
        var l12 = f15;
        if (b4.updateQueue = l12) b4.flags |= 4;
      }
    };
    Cj = function(a18, b4, c15, d12) {
      c15 !== d12 && (b4.flags |= 4);
    };
    function Dj(a18, b4) {
      if (!I6) switch (a18.tailMode) {
        case "hidden":
          b4 = a18.tail;
          for (var c15 = null; null !== b4; ) null !== b4.alternate && (c15 = b4), b4 = b4.sibling;
          null === c15 ? a18.tail = null : c15.sibling = null;
          break;
        case "collapsed":
          c15 = a18.tail;
          for (var d12 = null; null !== c15; ) null !== c15.alternate && (d12 = c15), c15 = c15.sibling;
          null === d12 ? b4 || null === a18.tail ? a18.tail = null : a18.tail.sibling = null : d12.sibling = null;
      }
    }
    function S2(a18) {
      var b4 = null !== a18.alternate && a18.alternate.child === a18.child, c15 = 0, d12 = 0;
      if (b4) for (var e14 = a18.child; null !== e14; ) c15 |= e14.lanes | e14.childLanes, d12 |= e14.subtreeFlags & 14680064, d12 |= e14.flags & 14680064, e14.return = a18, e14 = e14.sibling;
      else for (e14 = a18.child; null !== e14; ) c15 |= e14.lanes | e14.childLanes, d12 |= e14.subtreeFlags, d12 |= e14.flags, e14.return = a18, e14 = e14.sibling;
      a18.subtreeFlags |= d12;
      a18.childLanes = c15;
      return b4;
    }
    function Ej(a18, b4, c15) {
      var d12 = b4.pendingProps;
      wg(b4);
      switch (b4.tag) {
        case 2:
        case 16:
        case 15:
        case 0:
        case 11:
        case 7:
        case 8:
        case 12:
        case 9:
        case 14:
          return S2(b4), null;
        case 1:
          return Zf(b4.type) && $f(), S2(b4), null;
        case 3:
          d12 = b4.stateNode;
          zh();
          E6(Wf);
          E6(H4);
          Eh();
          d12.pendingContext && (d12.context = d12.pendingContext, d12.pendingContext = null);
          if (null === a18 || null === a18.child) Gg(b4) ? b4.flags |= 4 : null === a18 || a18.memoizedState.isDehydrated && 0 === (b4.flags & 256) || (b4.flags |= 1024, null !== zg && (Fj(zg), zg = null));
          Aj(a18, b4);
          S2(b4);
          return null;
        case 5:
          Bh(b4);
          var e14 = xh(wh.current);
          c15 = b4.type;
          if (null !== a18 && null != b4.stateNode) Bj(a18, b4, c15, d12, e14), a18.ref !== b4.ref && (b4.flags |= 512, b4.flags |= 2097152);
          else {
            if (!d12) {
              if (null === b4.stateNode) throw Error(p11(166));
              S2(b4);
              return null;
            }
            a18 = xh(uh.current);
            if (Gg(b4)) {
              d12 = b4.stateNode;
              c15 = b4.type;
              var f15 = b4.memoizedProps;
              d12[Of] = b4;
              d12[Pf] = f15;
              a18 = 0 !== (b4.mode & 1);
              switch (c15) {
                case "dialog":
                  D5("cancel", d12);
                  D5("close", d12);
                  break;
                case "iframe":
                case "object":
                case "embed":
                  D5("load", d12);
                  break;
                case "video":
                case "audio":
                  for (e14 = 0; e14 < lf.length; e14++) D5(lf[e14], d12);
                  break;
                case "source":
                  D5("error", d12);
                  break;
                case "img":
                case "image":
                case "link":
                  D5(
                    "error",
                    d12
                  );
                  D5("load", d12);
                  break;
                case "details":
                  D5("toggle", d12);
                  break;
                case "input":
                  Za(d12, f15);
                  D5("invalid", d12);
                  break;
                case "select":
                  d12._wrapperState = { wasMultiple: !!f15.multiple };
                  D5("invalid", d12);
                  break;
                case "textarea":
                  hb(d12, f15), D5("invalid", d12);
              }
              ub(c15, f15);
              e14 = null;
              for (var g7 in f15) if (f15.hasOwnProperty(g7)) {
                var h6 = f15[g7];
                "children" === g7 ? "string" === typeof h6 ? d12.textContent !== h6 && (true !== f15.suppressHydrationWarning && Af(d12.textContent, h6, a18), e14 = ["children", h6]) : "number" === typeof h6 && d12.textContent !== "" + h6 && (true !== f15.suppressHydrationWarning && Af(
                  d12.textContent,
                  h6,
                  a18
                ), e14 = ["children", "" + h6]) : ea.hasOwnProperty(g7) && null != h6 && "onScroll" === g7 && D5("scroll", d12);
              }
              switch (c15) {
                case "input":
                  Va(d12);
                  db(d12, f15, true);
                  break;
                case "textarea":
                  Va(d12);
                  jb(d12);
                  break;
                case "select":
                case "option":
                  break;
                default:
                  "function" === typeof f15.onClick && (d12.onclick = Bf);
              }
              d12 = e14;
              b4.updateQueue = d12;
              null !== d12 && (b4.flags |= 4);
            } else {
              g7 = 9 === e14.nodeType ? e14 : e14.ownerDocument;
              "http://www.w3.org/1999/xhtml" === a18 && (a18 = kb(c15));
              "http://www.w3.org/1999/xhtml" === a18 ? "script" === c15 ? (a18 = g7.createElement("div"), a18.innerHTML = "<script></script>", a18 = a18.removeChild(a18.firstChild)) : "string" === typeof d12.is ? a18 = g7.createElement(c15, { is: d12.is }) : (a18 = g7.createElement(c15), "select" === c15 && (g7 = a18, d12.multiple ? g7.multiple = true : d12.size && (g7.size = d12.size))) : a18 = g7.createElementNS(a18, c15);
              a18[Of] = b4;
              a18[Pf] = d12;
              zj(a18, b4, false, false);
              b4.stateNode = a18;
              a: {
                g7 = vb(c15, d12);
                switch (c15) {
                  case "dialog":
                    D5("cancel", a18);
                    D5("close", a18);
                    e14 = d12;
                    break;
                  case "iframe":
                  case "object":
                  case "embed":
                    D5("load", a18);
                    e14 = d12;
                    break;
                  case "video":
                  case "audio":
                    for (e14 = 0; e14 < lf.length; e14++) D5(lf[e14], a18);
                    e14 = d12;
                    break;
                  case "source":
                    D5("error", a18);
                    e14 = d12;
                    break;
                  case "img":
                  case "image":
                  case "link":
                    D5(
                      "error",
                      a18
                    );
                    D5("load", a18);
                    e14 = d12;
                    break;
                  case "details":
                    D5("toggle", a18);
                    e14 = d12;
                    break;
                  case "input":
                    Za(a18, d12);
                    e14 = Ya(a18, d12);
                    D5("invalid", a18);
                    break;
                  case "option":
                    e14 = d12;
                    break;
                  case "select":
                    a18._wrapperState = { wasMultiple: !!d12.multiple };
                    e14 = A4({}, d12, { value: void 0 });
                    D5("invalid", a18);
                    break;
                  case "textarea":
                    hb(a18, d12);
                    e14 = gb(a18, d12);
                    D5("invalid", a18);
                    break;
                  default:
                    e14 = d12;
                }
                ub(c15, e14);
                h6 = e14;
                for (f15 in h6) if (h6.hasOwnProperty(f15)) {
                  var k5 = h6[f15];
                  "style" === f15 ? sb(a18, k5) : "dangerouslySetInnerHTML" === f15 ? (k5 = k5 ? k5.__html : void 0, null != k5 && nb(a18, k5)) : "children" === f15 ? "string" === typeof k5 ? ("textarea" !== c15 || "" !== k5) && ob(a18, k5) : "number" === typeof k5 && ob(a18, "" + k5) : "suppressContentEditableWarning" !== f15 && "suppressHydrationWarning" !== f15 && "autoFocus" !== f15 && (ea.hasOwnProperty(f15) ? null != k5 && "onScroll" === f15 && D5("scroll", a18) : null != k5 && ta(a18, f15, k5, g7));
                }
                switch (c15) {
                  case "input":
                    Va(a18);
                    db(a18, d12, false);
                    break;
                  case "textarea":
                    Va(a18);
                    jb(a18);
                    break;
                  case "option":
                    null != d12.value && a18.setAttribute("value", "" + Sa(d12.value));
                    break;
                  case "select":
                    a18.multiple = !!d12.multiple;
                    f15 = d12.value;
                    null != f15 ? fb(a18, !!d12.multiple, f15, false) : null != d12.defaultValue && fb(
                      a18,
                      !!d12.multiple,
                      d12.defaultValue,
                      true
                    );
                    break;
                  default:
                    "function" === typeof e14.onClick && (a18.onclick = Bf);
                }
                switch (c15) {
                  case "button":
                  case "input":
                  case "select":
                  case "textarea":
                    d12 = !!d12.autoFocus;
                    break a;
                  case "img":
                    d12 = true;
                    break a;
                  default:
                    d12 = false;
                }
              }
              d12 && (b4.flags |= 4);
            }
            null !== b4.ref && (b4.flags |= 512, b4.flags |= 2097152);
          }
          S2(b4);
          return null;
        case 6:
          if (a18 && null != b4.stateNode) Cj(a18, b4, a18.memoizedProps, d12);
          else {
            if ("string" !== typeof d12 && null === b4.stateNode) throw Error(p11(166));
            c15 = xh(wh.current);
            xh(uh.current);
            if (Gg(b4)) {
              d12 = b4.stateNode;
              c15 = b4.memoizedProps;
              d12[Of] = b4;
              if (f15 = d12.nodeValue !== c15) {
                if (a18 = xg, null !== a18) switch (a18.tag) {
                  case 3:
                    Af(d12.nodeValue, c15, 0 !== (a18.mode & 1));
                    break;
                  case 5:
                    true !== a18.memoizedProps.suppressHydrationWarning && Af(d12.nodeValue, c15, 0 !== (a18.mode & 1));
                }
              }
              f15 && (b4.flags |= 4);
            } else d12 = (9 === c15.nodeType ? c15 : c15.ownerDocument).createTextNode(d12), d12[Of] = b4, b4.stateNode = d12;
          }
          S2(b4);
          return null;
        case 13:
          E6(L3);
          d12 = b4.memoizedState;
          if (null === a18 || null !== a18.memoizedState && null !== a18.memoizedState.dehydrated) {
            if (I6 && null !== yg && 0 !== (b4.mode & 1) && 0 === (b4.flags & 128)) Hg(), Ig(), b4.flags |= 98560, f15 = false;
            else if (f15 = Gg(b4), null !== d12 && null !== d12.dehydrated) {
              if (null === a18) {
                if (!f15) throw Error(p11(318));
                f15 = b4.memoizedState;
                f15 = null !== f15 ? f15.dehydrated : null;
                if (!f15) throw Error(p11(317));
                f15[Of] = b4;
              } else Ig(), 0 === (b4.flags & 128) && (b4.memoizedState = null), b4.flags |= 4;
              S2(b4);
              f15 = false;
            } else null !== zg && (Fj(zg), zg = null), f15 = true;
            if (!f15) return b4.flags & 65536 ? b4 : null;
          }
          if (0 !== (b4.flags & 128)) return b4.lanes = c15, b4;
          d12 = null !== d12;
          d12 !== (null !== a18 && null !== a18.memoizedState) && d12 && (b4.child.flags |= 8192, 0 !== (b4.mode & 1) && (null === a18 || 0 !== (L3.current & 1) ? 0 === T3 && (T3 = 3) : tj()));
          null !== b4.updateQueue && (b4.flags |= 4);
          S2(b4);
          return null;
        case 4:
          return zh(), Aj(a18, b4), null === a18 && sf(b4.stateNode.containerInfo), S2(b4), null;
        case 10:
          return ah(b4.type._context), S2(b4), null;
        case 17:
          return Zf(b4.type) && $f(), S2(b4), null;
        case 19:
          E6(L3);
          f15 = b4.memoizedState;
          if (null === f15) return S2(b4), null;
          d12 = 0 !== (b4.flags & 128);
          g7 = f15.rendering;
          if (null === g7) if (d12) Dj(f15, false);
          else {
            if (0 !== T3 || null !== a18 && 0 !== (a18.flags & 128)) for (a18 = b4.child; null !== a18; ) {
              g7 = Ch(a18);
              if (null !== g7) {
                b4.flags |= 128;
                Dj(f15, false);
                d12 = g7.updateQueue;
                null !== d12 && (b4.updateQueue = d12, b4.flags |= 4);
                b4.subtreeFlags = 0;
                d12 = c15;
                for (c15 = b4.child; null !== c15; ) f15 = c15, a18 = d12, f15.flags &= 14680066, g7 = f15.alternate, null === g7 ? (f15.childLanes = 0, f15.lanes = a18, f15.child = null, f15.subtreeFlags = 0, f15.memoizedProps = null, f15.memoizedState = null, f15.updateQueue = null, f15.dependencies = null, f15.stateNode = null) : (f15.childLanes = g7.childLanes, f15.lanes = g7.lanes, f15.child = g7.child, f15.subtreeFlags = 0, f15.deletions = null, f15.memoizedProps = g7.memoizedProps, f15.memoizedState = g7.memoizedState, f15.updateQueue = g7.updateQueue, f15.type = g7.type, a18 = g7.dependencies, f15.dependencies = null === a18 ? null : { lanes: a18.lanes, firstContext: a18.firstContext }), c15 = c15.sibling;
                G3(L3, L3.current & 1 | 2);
                return b4.child;
              }
              a18 = a18.sibling;
            }
            null !== f15.tail && B4() > Gj && (b4.flags |= 128, d12 = true, Dj(f15, false), b4.lanes = 4194304);
          }
          else {
            if (!d12) if (a18 = Ch(g7), null !== a18) {
              if (b4.flags |= 128, d12 = true, c15 = a18.updateQueue, null !== c15 && (b4.updateQueue = c15, b4.flags |= 4), Dj(f15, true), null === f15.tail && "hidden" === f15.tailMode && !g7.alternate && !I6) return S2(b4), null;
            } else 2 * B4() - f15.renderingStartTime > Gj && 1073741824 !== c15 && (b4.flags |= 128, d12 = true, Dj(f15, false), b4.lanes = 4194304);
            f15.isBackwards ? (g7.sibling = b4.child, b4.child = g7) : (c15 = f15.last, null !== c15 ? c15.sibling = g7 : b4.child = g7, f15.last = g7);
          }
          if (null !== f15.tail) return b4 = f15.tail, f15.rendering = b4, f15.tail = b4.sibling, f15.renderingStartTime = B4(), b4.sibling = null, c15 = L3.current, G3(L3, d12 ? c15 & 1 | 2 : c15 & 1), b4;
          S2(b4);
          return null;
        case 22:
        case 23:
          return Hj(), d12 = null !== b4.memoizedState, null !== a18 && null !== a18.memoizedState !== d12 && (b4.flags |= 8192), d12 && 0 !== (b4.mode & 1) ? 0 !== (fj & 1073741824) && (S2(b4), b4.subtreeFlags & 6 && (b4.flags |= 8192)) : S2(b4), null;
        case 24:
          return null;
        case 25:
          return null;
      }
      throw Error(p11(156, b4.tag));
    }
    function Ij(a18, b4) {
      wg(b4);
      switch (b4.tag) {
        case 1:
          return Zf(b4.type) && $f(), a18 = b4.flags, a18 & 65536 ? (b4.flags = a18 & -65537 | 128, b4) : null;
        case 3:
          return zh(), E6(Wf), E6(H4), Eh(), a18 = b4.flags, 0 !== (a18 & 65536) && 0 === (a18 & 128) ? (b4.flags = a18 & -65537 | 128, b4) : null;
        case 5:
          return Bh(b4), null;
        case 13:
          E6(L3);
          a18 = b4.memoizedState;
          if (null !== a18 && null !== a18.dehydrated) {
            if (null === b4.alternate) throw Error(p11(340));
            Ig();
          }
          a18 = b4.flags;
          return a18 & 65536 ? (b4.flags = a18 & -65537 | 128, b4) : null;
        case 19:
          return E6(L3), null;
        case 4:
          return zh(), null;
        case 10:
          return ah(b4.type._context), null;
        case 22:
        case 23:
          return Hj(), null;
        case 24:
          return null;
        default:
          return null;
      }
    }
    var Jj = false;
    var U3 = false;
    var Kj = "function" === typeof WeakSet ? WeakSet : Set;
    var V3 = null;
    function Lj(a18, b4) {
      var c15 = a18.ref;
      if (null !== c15) if ("function" === typeof c15) try {
        c15(null);
      } catch (d12) {
        W4(a18, b4, d12);
      }
      else c15.current = null;
    }
    function Mj(a18, b4, c15) {
      try {
        c15();
      } catch (d12) {
        W4(a18, b4, d12);
      }
    }
    var Nj = false;
    function Oj(a18, b4) {
      Cf = dd;
      a18 = Me3();
      if (Ne4(a18)) {
        if ("selectionStart" in a18) var c15 = { start: a18.selectionStart, end: a18.selectionEnd };
        else a: {
          c15 = (c15 = a18.ownerDocument) && c15.defaultView || window;
          var d12 = c15.getSelection && c15.getSelection();
          if (d12 && 0 !== d12.rangeCount) {
            c15 = d12.anchorNode;
            var e14 = d12.anchorOffset, f15 = d12.focusNode;
            d12 = d12.focusOffset;
            try {
              c15.nodeType, f15.nodeType;
            } catch (F4) {
              c15 = null;
              break a;
            }
            var g7 = 0, h6 = -1, k5 = -1, l12 = 0, m12 = 0, q2 = a18, r7 = null;
            b: for (; ; ) {
              for (var y4; ; ) {
                q2 !== c15 || 0 !== e14 && 3 !== q2.nodeType || (h6 = g7 + e14);
                q2 !== f15 || 0 !== d12 && 3 !== q2.nodeType || (k5 = g7 + d12);
                3 === q2.nodeType && (g7 += q2.nodeValue.length);
                if (null === (y4 = q2.firstChild)) break;
                r7 = q2;
                q2 = y4;
              }
              for (; ; ) {
                if (q2 === a18) break b;
                r7 === c15 && ++l12 === e14 && (h6 = g7);
                r7 === f15 && ++m12 === d12 && (k5 = g7);
                if (null !== (y4 = q2.nextSibling)) break;
                q2 = r7;
                r7 = q2.parentNode;
              }
              q2 = y4;
            }
            c15 = -1 === h6 || -1 === k5 ? null : { start: h6, end: k5 };
          } else c15 = null;
        }
        c15 = c15 || { start: 0, end: 0 };
      } else c15 = null;
      Df = { focusedElem: a18, selectionRange: c15 };
      dd = false;
      for (V3 = b4; null !== V3; ) if (b4 = V3, a18 = b4.child, 0 !== (b4.subtreeFlags & 1028) && null !== a18) a18.return = b4, V3 = a18;
      else for (; null !== V3; ) {
        b4 = V3;
        try {
          var n13 = b4.alternate;
          if (0 !== (b4.flags & 1024)) switch (b4.tag) {
            case 0:
            case 11:
            case 15:
              break;
            case 1:
              if (null !== n13) {
                var t20 = n13.memoizedProps, J2 = n13.memoizedState, x3 = b4.stateNode, w11 = x3.getSnapshotBeforeUpdate(b4.elementType === b4.type ? t20 : Ci2(b4.type, t20), J2);
                x3.__reactInternalSnapshotBeforeUpdate = w11;
              }
              break;
            case 3:
              var u4 = b4.stateNode.containerInfo;
              1 === u4.nodeType ? u4.textContent = "" : 9 === u4.nodeType && u4.documentElement && u4.removeChild(u4.documentElement);
              break;
            case 5:
            case 6:
            case 4:
            case 17:
              break;
            default:
              throw Error(p11(163));
          }
        } catch (F4) {
          W4(b4, b4.return, F4);
        }
        a18 = b4.sibling;
        if (null !== a18) {
          a18.return = b4.return;
          V3 = a18;
          break;
        }
        V3 = b4.return;
      }
      n13 = Nj;
      Nj = false;
      return n13;
    }
    function Pj(a18, b4, c15) {
      var d12 = b4.updateQueue;
      d12 = null !== d12 ? d12.lastEffect : null;
      if (null !== d12) {
        var e14 = d12 = d12.next;
        do {
          if ((e14.tag & a18) === a18) {
            var f15 = e14.destroy;
            e14.destroy = void 0;
            void 0 !== f15 && Mj(b4, c15, f15);
          }
          e14 = e14.next;
        } while (e14 !== d12);
      }
    }
    function Qj(a18, b4) {
      b4 = b4.updateQueue;
      b4 = null !== b4 ? b4.lastEffect : null;
      if (null !== b4) {
        var c15 = b4 = b4.next;
        do {
          if ((c15.tag & a18) === a18) {
            var d12 = c15.create;
            c15.destroy = d12();
          }
          c15 = c15.next;
        } while (c15 !== b4);
      }
    }
    function Rj(a18) {
      var b4 = a18.ref;
      if (null !== b4) {
        var c15 = a18.stateNode;
        switch (a18.tag) {
          case 5:
            a18 = c15;
            break;
          default:
            a18 = c15;
        }
        "function" === typeof b4 ? b4(a18) : b4.current = a18;
      }
    }
    function Sj(a18) {
      var b4 = a18.alternate;
      null !== b4 && (a18.alternate = null, Sj(b4));
      a18.child = null;
      a18.deletions = null;
      a18.sibling = null;
      5 === a18.tag && (b4 = a18.stateNode, null !== b4 && (delete b4[Of], delete b4[Pf], delete b4[of], delete b4[Qf], delete b4[Rf]));
      a18.stateNode = null;
      a18.return = null;
      a18.dependencies = null;
      a18.memoizedProps = null;
      a18.memoizedState = null;
      a18.pendingProps = null;
      a18.stateNode = null;
      a18.updateQueue = null;
    }
    function Tj(a18) {
      return 5 === a18.tag || 3 === a18.tag || 4 === a18.tag;
    }
    function Uj(a18) {
      a: for (; ; ) {
        for (; null === a18.sibling; ) {
          if (null === a18.return || Tj(a18.return)) return null;
          a18 = a18.return;
        }
        a18.sibling.return = a18.return;
        for (a18 = a18.sibling; 5 !== a18.tag && 6 !== a18.tag && 18 !== a18.tag; ) {
          if (a18.flags & 2) continue a;
          if (null === a18.child || 4 === a18.tag) continue a;
          else a18.child.return = a18, a18 = a18.child;
        }
        if (!(a18.flags & 2)) return a18.stateNode;
      }
    }
    function Vj(a18, b4, c15) {
      var d12 = a18.tag;
      if (5 === d12 || 6 === d12) a18 = a18.stateNode, b4 ? 8 === c15.nodeType ? c15.parentNode.insertBefore(a18, b4) : c15.insertBefore(a18, b4) : (8 === c15.nodeType ? (b4 = c15.parentNode, b4.insertBefore(a18, c15)) : (b4 = c15, b4.appendChild(a18)), c15 = c15._reactRootContainer, null !== c15 && void 0 !== c15 || null !== b4.onclick || (b4.onclick = Bf));
      else if (4 !== d12 && (a18 = a18.child, null !== a18)) for (Vj(a18, b4, c15), a18 = a18.sibling; null !== a18; ) Vj(a18, b4, c15), a18 = a18.sibling;
    }
    function Wj(a18, b4, c15) {
      var d12 = a18.tag;
      if (5 === d12 || 6 === d12) a18 = a18.stateNode, b4 ? c15.insertBefore(a18, b4) : c15.appendChild(a18);
      else if (4 !== d12 && (a18 = a18.child, null !== a18)) for (Wj(a18, b4, c15), a18 = a18.sibling; null !== a18; ) Wj(a18, b4, c15), a18 = a18.sibling;
    }
    var X4 = null;
    var Xj = false;
    function Yj(a18, b4, c15) {
      for (c15 = c15.child; null !== c15; ) Zj(a18, b4, c15), c15 = c15.sibling;
    }
    function Zj(a18, b4, c15) {
      if (lc && "function" === typeof lc.onCommitFiberUnmount) try {
        lc.onCommitFiberUnmount(kc, c15);
      } catch (h6) {
      }
      switch (c15.tag) {
        case 5:
          U3 || Lj(c15, b4);
        case 6:
          var d12 = X4, e14 = Xj;
          X4 = null;
          Yj(a18, b4, c15);
          X4 = d12;
          Xj = e14;
          null !== X4 && (Xj ? (a18 = X4, c15 = c15.stateNode, 8 === a18.nodeType ? a18.parentNode.removeChild(c15) : a18.removeChild(c15)) : X4.removeChild(c15.stateNode));
          break;
        case 18:
          null !== X4 && (Xj ? (a18 = X4, c15 = c15.stateNode, 8 === a18.nodeType ? Kf(a18.parentNode, c15) : 1 === a18.nodeType && Kf(a18, c15), bd(a18)) : Kf(X4, c15.stateNode));
          break;
        case 4:
          d12 = X4;
          e14 = Xj;
          X4 = c15.stateNode.containerInfo;
          Xj = true;
          Yj(a18, b4, c15);
          X4 = d12;
          Xj = e14;
          break;
        case 0:
        case 11:
        case 14:
        case 15:
          if (!U3 && (d12 = c15.updateQueue, null !== d12 && (d12 = d12.lastEffect, null !== d12))) {
            e14 = d12 = d12.next;
            do {
              var f15 = e14, g7 = f15.destroy;
              f15 = f15.tag;
              void 0 !== g7 && (0 !== (f15 & 2) ? Mj(c15, b4, g7) : 0 !== (f15 & 4) && Mj(c15, b4, g7));
              e14 = e14.next;
            } while (e14 !== d12);
          }
          Yj(a18, b4, c15);
          break;
        case 1:
          if (!U3 && (Lj(c15, b4), d12 = c15.stateNode, "function" === typeof d12.componentWillUnmount)) try {
            d12.props = c15.memoizedProps, d12.state = c15.memoizedState, d12.componentWillUnmount();
          } catch (h6) {
            W4(c15, b4, h6);
          }
          Yj(a18, b4, c15);
          break;
        case 21:
          Yj(a18, b4, c15);
          break;
        case 22:
          c15.mode & 1 ? (U3 = (d12 = U3) || null !== c15.memoizedState, Yj(a18, b4, c15), U3 = d12) : Yj(a18, b4, c15);
          break;
        default:
          Yj(a18, b4, c15);
      }
    }
    function ak(a18) {
      var b4 = a18.updateQueue;
      if (null !== b4) {
        a18.updateQueue = null;
        var c15 = a18.stateNode;
        null === c15 && (c15 = a18.stateNode = new Kj());
        b4.forEach(function(b5) {
          var d12 = bk.bind(null, a18, b5);
          c15.has(b5) || (c15.add(b5), b5.then(d12, d12));
        });
      }
    }
    function ck(a18, b4) {
      var c15 = b4.deletions;
      if (null !== c15) for (var d12 = 0; d12 < c15.length; d12++) {
        var e14 = c15[d12];
        try {
          var f15 = a18, g7 = b4, h6 = g7;
          a: for (; null !== h6; ) {
            switch (h6.tag) {
              case 5:
                X4 = h6.stateNode;
                Xj = false;
                break a;
              case 3:
                X4 = h6.stateNode.containerInfo;
                Xj = true;
                break a;
              case 4:
                X4 = h6.stateNode.containerInfo;
                Xj = true;
                break a;
            }
            h6 = h6.return;
          }
          if (null === X4) throw Error(p11(160));
          Zj(f15, g7, e14);
          X4 = null;
          Xj = false;
          var k5 = e14.alternate;
          null !== k5 && (k5.return = null);
          e14.return = null;
        } catch (l12) {
          W4(e14, b4, l12);
        }
      }
      if (b4.subtreeFlags & 12854) for (b4 = b4.child; null !== b4; ) dk(b4, a18), b4 = b4.sibling;
    }
    function dk(a18, b4) {
      var c15 = a18.alternate, d12 = a18.flags;
      switch (a18.tag) {
        case 0:
        case 11:
        case 14:
        case 15:
          ck(b4, a18);
          ek(a18);
          if (d12 & 4) {
            try {
              Pj(3, a18, a18.return), Qj(3, a18);
            } catch (t20) {
              W4(a18, a18.return, t20);
            }
            try {
              Pj(5, a18, a18.return);
            } catch (t20) {
              W4(a18, a18.return, t20);
            }
          }
          break;
        case 1:
          ck(b4, a18);
          ek(a18);
          d12 & 512 && null !== c15 && Lj(c15, c15.return);
          break;
        case 5:
          ck(b4, a18);
          ek(a18);
          d12 & 512 && null !== c15 && Lj(c15, c15.return);
          if (a18.flags & 32) {
            var e14 = a18.stateNode;
            try {
              ob(e14, "");
            } catch (t20) {
              W4(a18, a18.return, t20);
            }
          }
          if (d12 & 4 && (e14 = a18.stateNode, null != e14)) {
            var f15 = a18.memoizedProps, g7 = null !== c15 ? c15.memoizedProps : f15, h6 = a18.type, k5 = a18.updateQueue;
            a18.updateQueue = null;
            if (null !== k5) try {
              "input" === h6 && "radio" === f15.type && null != f15.name && ab(e14, f15);
              vb(h6, g7);
              var l12 = vb(h6, f15);
              for (g7 = 0; g7 < k5.length; g7 += 2) {
                var m12 = k5[g7], q2 = k5[g7 + 1];
                "style" === m12 ? sb(e14, q2) : "dangerouslySetInnerHTML" === m12 ? nb(e14, q2) : "children" === m12 ? ob(e14, q2) : ta(e14, m12, q2, l12);
              }
              switch (h6) {
                case "input":
                  bb(e14, f15);
                  break;
                case "textarea":
                  ib(e14, f15);
                  break;
                case "select":
                  var r7 = e14._wrapperState.wasMultiple;
                  e14._wrapperState.wasMultiple = !!f15.multiple;
                  var y4 = f15.value;
                  null != y4 ? fb(e14, !!f15.multiple, y4, false) : r7 !== !!f15.multiple && (null != f15.defaultValue ? fb(
                    e14,
                    !!f15.multiple,
                    f15.defaultValue,
                    true
                  ) : fb(e14, !!f15.multiple, f15.multiple ? [] : "", false));
              }
              e14[Pf] = f15;
            } catch (t20) {
              W4(a18, a18.return, t20);
            }
          }
          break;
        case 6:
          ck(b4, a18);
          ek(a18);
          if (d12 & 4) {
            if (null === a18.stateNode) throw Error(p11(162));
            e14 = a18.stateNode;
            f15 = a18.memoizedProps;
            try {
              e14.nodeValue = f15;
            } catch (t20) {
              W4(a18, a18.return, t20);
            }
          }
          break;
        case 3:
          ck(b4, a18);
          ek(a18);
          if (d12 & 4 && null !== c15 && c15.memoizedState.isDehydrated) try {
            bd(b4.containerInfo);
          } catch (t20) {
            W4(a18, a18.return, t20);
          }
          break;
        case 4:
          ck(b4, a18);
          ek(a18);
          break;
        case 13:
          ck(b4, a18);
          ek(a18);
          e14 = a18.child;
          e14.flags & 8192 && (f15 = null !== e14.memoizedState, e14.stateNode.isHidden = f15, !f15 || null !== e14.alternate && null !== e14.alternate.memoizedState || (fk = B4()));
          d12 & 4 && ak(a18);
          break;
        case 22:
          m12 = null !== c15 && null !== c15.memoizedState;
          a18.mode & 1 ? (U3 = (l12 = U3) || m12, ck(b4, a18), U3 = l12) : ck(b4, a18);
          ek(a18);
          if (d12 & 8192) {
            l12 = null !== a18.memoizedState;
            if ((a18.stateNode.isHidden = l12) && !m12 && 0 !== (a18.mode & 1)) for (V3 = a18, m12 = a18.child; null !== m12; ) {
              for (q2 = V3 = m12; null !== V3; ) {
                r7 = V3;
                y4 = r7.child;
                switch (r7.tag) {
                  case 0:
                  case 11:
                  case 14:
                  case 15:
                    Pj(4, r7, r7.return);
                    break;
                  case 1:
                    Lj(r7, r7.return);
                    var n13 = r7.stateNode;
                    if ("function" === typeof n13.componentWillUnmount) {
                      d12 = r7;
                      c15 = r7.return;
                      try {
                        b4 = d12, n13.props = b4.memoizedProps, n13.state = b4.memoizedState, n13.componentWillUnmount();
                      } catch (t20) {
                        W4(d12, c15, t20);
                      }
                    }
                    break;
                  case 5:
                    Lj(r7, r7.return);
                    break;
                  case 22:
                    if (null !== r7.memoizedState) {
                      gk(q2);
                      continue;
                    }
                }
                null !== y4 ? (y4.return = r7, V3 = y4) : gk(q2);
              }
              m12 = m12.sibling;
            }
            a: for (m12 = null, q2 = a18; ; ) {
              if (5 === q2.tag) {
                if (null === m12) {
                  m12 = q2;
                  try {
                    e14 = q2.stateNode, l12 ? (f15 = e14.style, "function" === typeof f15.setProperty ? f15.setProperty("display", "none", "important") : f15.display = "none") : (h6 = q2.stateNode, k5 = q2.memoizedProps.style, g7 = void 0 !== k5 && null !== k5 && k5.hasOwnProperty("display") ? k5.display : null, h6.style.display = rb("display", g7));
                  } catch (t20) {
                    W4(a18, a18.return, t20);
                  }
                }
              } else if (6 === q2.tag) {
                if (null === m12) try {
                  q2.stateNode.nodeValue = l12 ? "" : q2.memoizedProps;
                } catch (t20) {
                  W4(a18, a18.return, t20);
                }
              } else if ((22 !== q2.tag && 23 !== q2.tag || null === q2.memoizedState || q2 === a18) && null !== q2.child) {
                q2.child.return = q2;
                q2 = q2.child;
                continue;
              }
              if (q2 === a18) break a;
              for (; null === q2.sibling; ) {
                if (null === q2.return || q2.return === a18) break a;
                m12 === q2 && (m12 = null);
                q2 = q2.return;
              }
              m12 === q2 && (m12 = null);
              q2.sibling.return = q2.return;
              q2 = q2.sibling;
            }
          }
          break;
        case 19:
          ck(b4, a18);
          ek(a18);
          d12 & 4 && ak(a18);
          break;
        case 21:
          break;
        default:
          ck(
            b4,
            a18
          ), ek(a18);
      }
    }
    function ek(a18) {
      var b4 = a18.flags;
      if (b4 & 2) {
        try {
          a: {
            for (var c15 = a18.return; null !== c15; ) {
              if (Tj(c15)) {
                var d12 = c15;
                break a;
              }
              c15 = c15.return;
            }
            throw Error(p11(160));
          }
          switch (d12.tag) {
            case 5:
              var e14 = d12.stateNode;
              d12.flags & 32 && (ob(e14, ""), d12.flags &= -33);
              var f15 = Uj(a18);
              Wj(a18, f15, e14);
              break;
            case 3:
            case 4:
              var g7 = d12.stateNode.containerInfo, h6 = Uj(a18);
              Vj(a18, h6, g7);
              break;
            default:
              throw Error(p11(161));
          }
        } catch (k5) {
          W4(a18, a18.return, k5);
        }
        a18.flags &= -3;
      }
      b4 & 4096 && (a18.flags &= -4097);
    }
    function hk(a18, b4, c15) {
      V3 = a18;
      ik(a18, b4, c15);
    }
    function ik(a18, b4, c15) {
      for (var d12 = 0 !== (a18.mode & 1); null !== V3; ) {
        var e14 = V3, f15 = e14.child;
        if (22 === e14.tag && d12) {
          var g7 = null !== e14.memoizedState || Jj;
          if (!g7) {
            var h6 = e14.alternate, k5 = null !== h6 && null !== h6.memoizedState || U3;
            h6 = Jj;
            var l12 = U3;
            Jj = g7;
            if ((U3 = k5) && !l12) for (V3 = e14; null !== V3; ) g7 = V3, k5 = g7.child, 22 === g7.tag && null !== g7.memoizedState ? jk(e14) : null !== k5 ? (k5.return = g7, V3 = k5) : jk(e14);
            for (; null !== f15; ) V3 = f15, ik(f15, b4, c15), f15 = f15.sibling;
            V3 = e14;
            Jj = h6;
            U3 = l12;
          }
          kk(a18, b4, c15);
        } else 0 !== (e14.subtreeFlags & 8772) && null !== f15 ? (f15.return = e14, V3 = f15) : kk(a18, b4, c15);
      }
    }
    function kk(a18) {
      for (; null !== V3; ) {
        var b4 = V3;
        if (0 !== (b4.flags & 8772)) {
          var c15 = b4.alternate;
          try {
            if (0 !== (b4.flags & 8772)) switch (b4.tag) {
              case 0:
              case 11:
              case 15:
                U3 || Qj(5, b4);
                break;
              case 1:
                var d12 = b4.stateNode;
                if (b4.flags & 4 && !U3) if (null === c15) d12.componentDidMount();
                else {
                  var e14 = b4.elementType === b4.type ? c15.memoizedProps : Ci2(b4.type, c15.memoizedProps);
                  d12.componentDidUpdate(e14, c15.memoizedState, d12.__reactInternalSnapshotBeforeUpdate);
                }
                var f15 = b4.updateQueue;
                null !== f15 && sh(b4, f15, d12);
                break;
              case 3:
                var g7 = b4.updateQueue;
                if (null !== g7) {
                  c15 = null;
                  if (null !== b4.child) switch (b4.child.tag) {
                    case 5:
                      c15 = b4.child.stateNode;
                      break;
                    case 1:
                      c15 = b4.child.stateNode;
                  }
                  sh(b4, g7, c15);
                }
                break;
              case 5:
                var h6 = b4.stateNode;
                if (null === c15 && b4.flags & 4) {
                  c15 = h6;
                  var k5 = b4.memoizedProps;
                  switch (b4.type) {
                    case "button":
                    case "input":
                    case "select":
                    case "textarea":
                      k5.autoFocus && c15.focus();
                      break;
                    case "img":
                      k5.src && (c15.src = k5.src);
                  }
                }
                break;
              case 6:
                break;
              case 4:
                break;
              case 12:
                break;
              case 13:
                if (null === b4.memoizedState) {
                  var l12 = b4.alternate;
                  if (null !== l12) {
                    var m12 = l12.memoizedState;
                    if (null !== m12) {
                      var q2 = m12.dehydrated;
                      null !== q2 && bd(q2);
                    }
                  }
                }
                break;
              case 19:
              case 17:
              case 21:
              case 22:
              case 23:
              case 25:
                break;
              default:
                throw Error(p11(163));
            }
            U3 || b4.flags & 512 && Rj(b4);
          } catch (r7) {
            W4(b4, b4.return, r7);
          }
        }
        if (b4 === a18) {
          V3 = null;
          break;
        }
        c15 = b4.sibling;
        if (null !== c15) {
          c15.return = b4.return;
          V3 = c15;
          break;
        }
        V3 = b4.return;
      }
    }
    function gk(a18) {
      for (; null !== V3; ) {
        var b4 = V3;
        if (b4 === a18) {
          V3 = null;
          break;
        }
        var c15 = b4.sibling;
        if (null !== c15) {
          c15.return = b4.return;
          V3 = c15;
          break;
        }
        V3 = b4.return;
      }
    }
    function jk(a18) {
      for (; null !== V3; ) {
        var b4 = V3;
        try {
          switch (b4.tag) {
            case 0:
            case 11:
            case 15:
              var c15 = b4.return;
              try {
                Qj(4, b4);
              } catch (k5) {
                W4(b4, c15, k5);
              }
              break;
            case 1:
              var d12 = b4.stateNode;
              if ("function" === typeof d12.componentDidMount) {
                var e14 = b4.return;
                try {
                  d12.componentDidMount();
                } catch (k5) {
                  W4(b4, e14, k5);
                }
              }
              var f15 = b4.return;
              try {
                Rj(b4);
              } catch (k5) {
                W4(b4, f15, k5);
              }
              break;
            case 5:
              var g7 = b4.return;
              try {
                Rj(b4);
              } catch (k5) {
                W4(b4, g7, k5);
              }
          }
        } catch (k5) {
          W4(b4, b4.return, k5);
        }
        if (b4 === a18) {
          V3 = null;
          break;
        }
        var h6 = b4.sibling;
        if (null !== h6) {
          h6.return = b4.return;
          V3 = h6;
          break;
        }
        V3 = b4.return;
      }
    }
    var lk = Math.ceil;
    var mk = ua.ReactCurrentDispatcher;
    var nk = ua.ReactCurrentOwner;
    var ok = ua.ReactCurrentBatchConfig;
    var K3 = 0;
    var Q4 = null;
    var Y3 = null;
    var Z5 = 0;
    var fj = 0;
    var ej = Uf(0);
    var T3 = 0;
    var pk = null;
    var rh = 0;
    var qk = 0;
    var rk = 0;
    var sk = null;
    var tk = null;
    var fk = 0;
    var Gj = Infinity;
    var uk = null;
    var Oi2 = false;
    var Pi2 = null;
    var Ri2 = null;
    var vk = false;
    var wk = null;
    var xk = 0;
    var yk = 0;
    var zk = null;
    var Ak = -1;
    var Bk = 0;
    function R10() {
      return 0 !== (K3 & 6) ? B4() : -1 !== Ak ? Ak : Ak = B4();
    }
    function yi2(a18) {
      if (0 === (a18.mode & 1)) return 1;
      if (0 !== (K3 & 2) && 0 !== Z5) return Z5 & -Z5;
      if (null !== Kg.transition) return 0 === Bk && (Bk = yc()), Bk;
      a18 = C5;
      if (0 !== a18) return a18;
      a18 = window.event;
      a18 = void 0 === a18 ? 16 : jd(a18.type);
      return a18;
    }
    function gi2(a18, b4, c15, d12) {
      if (50 < yk) throw yk = 0, zk = null, Error(p11(185));
      Ac(a18, c15, d12);
      if (0 === (K3 & 2) || a18 !== Q4) a18 === Q4 && (0 === (K3 & 2) && (qk |= c15), 4 === T3 && Ck(a18, Z5)), Dk(a18, d12), 1 === c15 && 0 === K3 && 0 === (b4.mode & 1) && (Gj = B4() + 500, fg && jg());
    }
    function Dk(a18, b4) {
      var c15 = a18.callbackNode;
      wc(a18, b4);
      var d12 = uc(a18, a18 === Q4 ? Z5 : 0);
      if (0 === d12) null !== c15 && bc(c15), a18.callbackNode = null, a18.callbackPriority = 0;
      else if (b4 = d12 & -d12, a18.callbackPriority !== b4) {
        null != c15 && bc(c15);
        if (1 === b4) 0 === a18.tag ? ig(Ek.bind(null, a18)) : hg(Ek.bind(null, a18)), Jf(function() {
          0 === (K3 & 6) && jg();
        }), c15 = null;
        else {
          switch (Dc(d12)) {
            case 1:
              c15 = fc;
              break;
            case 4:
              c15 = gc;
              break;
            case 16:
              c15 = hc;
              break;
            case 536870912:
              c15 = jc;
              break;
            default:
              c15 = hc;
          }
          c15 = Fk(c15, Gk.bind(null, a18));
        }
        a18.callbackPriority = b4;
        a18.callbackNode = c15;
      }
    }
    function Gk(a18, b4) {
      Ak = -1;
      Bk = 0;
      if (0 !== (K3 & 6)) throw Error(p11(327));
      var c15 = a18.callbackNode;
      if (Hk() && a18.callbackNode !== c15) return null;
      var d12 = uc(a18, a18 === Q4 ? Z5 : 0);
      if (0 === d12) return null;
      if (0 !== (d12 & 30) || 0 !== (d12 & a18.expiredLanes) || b4) b4 = Ik(a18, d12);
      else {
        b4 = d12;
        var e14 = K3;
        K3 |= 2;
        var f15 = Jk();
        if (Q4 !== a18 || Z5 !== b4) uk = null, Gj = B4() + 500, Kk(a18, b4);
        do
          try {
            Lk();
            break;
          } catch (h6) {
            Mk(a18, h6);
          }
        while (1);
        $g();
        mk.current = f15;
        K3 = e14;
        null !== Y3 ? b4 = 0 : (Q4 = null, Z5 = 0, b4 = T3);
      }
      if (0 !== b4) {
        2 === b4 && (e14 = xc(a18), 0 !== e14 && (d12 = e14, b4 = Nk(a18, e14)));
        if (1 === b4) throw c15 = pk, Kk(a18, 0), Ck(a18, d12), Dk(a18, B4()), c15;
        if (6 === b4) Ck(a18, d12);
        else {
          e14 = a18.current.alternate;
          if (0 === (d12 & 30) && !Ok(e14) && (b4 = Ik(a18, d12), 2 === b4 && (f15 = xc(a18), 0 !== f15 && (d12 = f15, b4 = Nk(a18, f15))), 1 === b4)) throw c15 = pk, Kk(a18, 0), Ck(a18, d12), Dk(a18, B4()), c15;
          a18.finishedWork = e14;
          a18.finishedLanes = d12;
          switch (b4) {
            case 0:
            case 1:
              throw Error(p11(345));
            case 2:
              Pk(a18, tk, uk);
              break;
            case 3:
              Ck(a18, d12);
              if ((d12 & 130023424) === d12 && (b4 = fk + 500 - B4(), 10 < b4)) {
                if (0 !== uc(a18, 0)) break;
                e14 = a18.suspendedLanes;
                if ((e14 & d12) !== d12) {
                  R10();
                  a18.pingedLanes |= a18.suspendedLanes & e14;
                  break;
                }
                a18.timeoutHandle = Ff(Pk.bind(null, a18, tk, uk), b4);
                break;
              }
              Pk(a18, tk, uk);
              break;
            case 4:
              Ck(a18, d12);
              if ((d12 & 4194240) === d12) break;
              b4 = a18.eventTimes;
              for (e14 = -1; 0 < d12; ) {
                var g7 = 31 - oc(d12);
                f15 = 1 << g7;
                g7 = b4[g7];
                g7 > e14 && (e14 = g7);
                d12 &= ~f15;
              }
              d12 = e14;
              d12 = B4() - d12;
              d12 = (120 > d12 ? 120 : 480 > d12 ? 480 : 1080 > d12 ? 1080 : 1920 > d12 ? 1920 : 3e3 > d12 ? 3e3 : 4320 > d12 ? 4320 : 1960 * lk(d12 / 1960)) - d12;
              if (10 < d12) {
                a18.timeoutHandle = Ff(Pk.bind(null, a18, tk, uk), d12);
                break;
              }
              Pk(a18, tk, uk);
              break;
            case 5:
              Pk(a18, tk, uk);
              break;
            default:
              throw Error(p11(329));
          }
        }
      }
      Dk(a18, B4());
      return a18.callbackNode === c15 ? Gk.bind(null, a18) : null;
    }
    function Nk(a18, b4) {
      var c15 = sk;
      a18.current.memoizedState.isDehydrated && (Kk(a18, b4).flags |= 256);
      a18 = Ik(a18, b4);
      2 !== a18 && (b4 = tk, tk = c15, null !== b4 && Fj(b4));
      return a18;
    }
    function Fj(a18) {
      null === tk ? tk = a18 : tk.push.apply(tk, a18);
    }
    function Ok(a18) {
      for (var b4 = a18; ; ) {
        if (b4.flags & 16384) {
          var c15 = b4.updateQueue;
          if (null !== c15 && (c15 = c15.stores, null !== c15)) for (var d12 = 0; d12 < c15.length; d12++) {
            var e14 = c15[d12], f15 = e14.getSnapshot;
            e14 = e14.value;
            try {
              if (!He5(f15(), e14)) return false;
            } catch (g7) {
              return false;
            }
          }
        }
        c15 = b4.child;
        if (b4.subtreeFlags & 16384 && null !== c15) c15.return = b4, b4 = c15;
        else {
          if (b4 === a18) break;
          for (; null === b4.sibling; ) {
            if (null === b4.return || b4.return === a18) return true;
            b4 = b4.return;
          }
          b4.sibling.return = b4.return;
          b4 = b4.sibling;
        }
      }
      return true;
    }
    function Ck(a18, b4) {
      b4 &= ~rk;
      b4 &= ~qk;
      a18.suspendedLanes |= b4;
      a18.pingedLanes &= ~b4;
      for (a18 = a18.expirationTimes; 0 < b4; ) {
        var c15 = 31 - oc(b4), d12 = 1 << c15;
        a18[c15] = -1;
        b4 &= ~d12;
      }
    }
    function Ek(a18) {
      if (0 !== (K3 & 6)) throw Error(p11(327));
      Hk();
      var b4 = uc(a18, 0);
      if (0 === (b4 & 1)) return Dk(a18, B4()), null;
      var c15 = Ik(a18, b4);
      if (0 !== a18.tag && 2 === c15) {
        var d12 = xc(a18);
        0 !== d12 && (b4 = d12, c15 = Nk(a18, d12));
      }
      if (1 === c15) throw c15 = pk, Kk(a18, 0), Ck(a18, b4), Dk(a18, B4()), c15;
      if (6 === c15) throw Error(p11(345));
      a18.finishedWork = a18.current.alternate;
      a18.finishedLanes = b4;
      Pk(a18, tk, uk);
      Dk(a18, B4());
      return null;
    }
    function Qk(a18, b4) {
      var c15 = K3;
      K3 |= 1;
      try {
        return a18(b4);
      } finally {
        K3 = c15, 0 === K3 && (Gj = B4() + 500, fg && jg());
      }
    }
    function Rk(a18) {
      null !== wk && 0 === wk.tag && 0 === (K3 & 6) && Hk();
      var b4 = K3;
      K3 |= 1;
      var c15 = ok.transition, d12 = C5;
      try {
        if (ok.transition = null, C5 = 1, a18) return a18();
      } finally {
        C5 = d12, ok.transition = c15, K3 = b4, 0 === (K3 & 6) && jg();
      }
    }
    function Hj() {
      fj = ej.current;
      E6(ej);
    }
    function Kk(a18, b4) {
      a18.finishedWork = null;
      a18.finishedLanes = 0;
      var c15 = a18.timeoutHandle;
      -1 !== c15 && (a18.timeoutHandle = -1, Gf(c15));
      if (null !== Y3) for (c15 = Y3.return; null !== c15; ) {
        var d12 = c15;
        wg(d12);
        switch (d12.tag) {
          case 1:
            d12 = d12.type.childContextTypes;
            null !== d12 && void 0 !== d12 && $f();
            break;
          case 3:
            zh();
            E6(Wf);
            E6(H4);
            Eh();
            break;
          case 5:
            Bh(d12);
            break;
          case 4:
            zh();
            break;
          case 13:
            E6(L3);
            break;
          case 19:
            E6(L3);
            break;
          case 10:
            ah(d12.type._context);
            break;
          case 22:
          case 23:
            Hj();
        }
        c15 = c15.return;
      }
      Q4 = a18;
      Y3 = a18 = Pg(a18.current, null);
      Z5 = fj = b4;
      T3 = 0;
      pk = null;
      rk = qk = rh = 0;
      tk = sk = null;
      if (null !== fh) {
        for (b4 = 0; b4 < fh.length; b4++) if (c15 = fh[b4], d12 = c15.interleaved, null !== d12) {
          c15.interleaved = null;
          var e14 = d12.next, f15 = c15.pending;
          if (null !== f15) {
            var g7 = f15.next;
            f15.next = e14;
            d12.next = g7;
          }
          c15.pending = d12;
        }
        fh = null;
      }
      return a18;
    }
    function Mk(a18, b4) {
      do {
        var c15 = Y3;
        try {
          $g();
          Fh.current = Rh;
          if (Ih) {
            for (var d12 = M6.memoizedState; null !== d12; ) {
              var e14 = d12.queue;
              null !== e14 && (e14.pending = null);
              d12 = d12.next;
            }
            Ih = false;
          }
          Hh = 0;
          O5 = N4 = M6 = null;
          Jh = false;
          Kh = 0;
          nk.current = null;
          if (null === c15 || null === c15.return) {
            T3 = 1;
            pk = b4;
            Y3 = null;
            break;
          }
          a: {
            var f15 = a18, g7 = c15.return, h6 = c15, k5 = b4;
            b4 = Z5;
            h6.flags |= 32768;
            if (null !== k5 && "object" === typeof k5 && "function" === typeof k5.then) {
              var l12 = k5, m12 = h6, q2 = m12.tag;
              if (0 === (m12.mode & 1) && (0 === q2 || 11 === q2 || 15 === q2)) {
                var r7 = m12.alternate;
                r7 ? (m12.updateQueue = r7.updateQueue, m12.memoizedState = r7.memoizedState, m12.lanes = r7.lanes) : (m12.updateQueue = null, m12.memoizedState = null);
              }
              var y4 = Ui(g7);
              if (null !== y4) {
                y4.flags &= -257;
                Vi(y4, g7, h6, f15, b4);
                y4.mode & 1 && Si2(f15, l12, b4);
                b4 = y4;
                k5 = l12;
                var n13 = b4.updateQueue;
                if (null === n13) {
                  var t20 = /* @__PURE__ */ new Set();
                  t20.add(k5);
                  b4.updateQueue = t20;
                } else n13.add(k5);
                break a;
              } else {
                if (0 === (b4 & 1)) {
                  Si2(f15, l12, b4);
                  tj();
                  break a;
                }
                k5 = Error(p11(426));
              }
            } else if (I6 && h6.mode & 1) {
              var J2 = Ui(g7);
              if (null !== J2) {
                0 === (J2.flags & 65536) && (J2.flags |= 256);
                Vi(J2, g7, h6, f15, b4);
                Jg(Ji(k5, h6));
                break a;
              }
            }
            f15 = k5 = Ji(k5, h6);
            4 !== T3 && (T3 = 2);
            null === sk ? sk = [f15] : sk.push(f15);
            f15 = g7;
            do {
              switch (f15.tag) {
                case 3:
                  f15.flags |= 65536;
                  b4 &= -b4;
                  f15.lanes |= b4;
                  var x3 = Ni(f15, k5, b4);
                  ph(f15, x3);
                  break a;
                case 1:
                  h6 = k5;
                  var w11 = f15.type, u4 = f15.stateNode;
                  if (0 === (f15.flags & 128) && ("function" === typeof w11.getDerivedStateFromError || null !== u4 && "function" === typeof u4.componentDidCatch && (null === Ri2 || !Ri2.has(u4)))) {
                    f15.flags |= 65536;
                    b4 &= -b4;
                    f15.lanes |= b4;
                    var F4 = Qi(f15, h6, b4);
                    ph(f15, F4);
                    break a;
                  }
              }
              f15 = f15.return;
            } while (null !== f15);
          }
          Sk(c15);
        } catch (na) {
          b4 = na;
          Y3 === c15 && null !== c15 && (Y3 = c15 = c15.return);
          continue;
        }
        break;
      } while (1);
    }
    function Jk() {
      var a18 = mk.current;
      mk.current = Rh;
      return null === a18 ? Rh : a18;
    }
    function tj() {
      if (0 === T3 || 3 === T3 || 2 === T3) T3 = 4;
      null === Q4 || 0 === (rh & 268435455) && 0 === (qk & 268435455) || Ck(Q4, Z5);
    }
    function Ik(a18, b4) {
      var c15 = K3;
      K3 |= 2;
      var d12 = Jk();
      if (Q4 !== a18 || Z5 !== b4) uk = null, Kk(a18, b4);
      do
        try {
          Tk();
          break;
        } catch (e14) {
          Mk(a18, e14);
        }
      while (1);
      $g();
      K3 = c15;
      mk.current = d12;
      if (null !== Y3) throw Error(p11(261));
      Q4 = null;
      Z5 = 0;
      return T3;
    }
    function Tk() {
      for (; null !== Y3; ) Uk(Y3);
    }
    function Lk() {
      for (; null !== Y3 && !cc(); ) Uk(Y3);
    }
    function Uk(a18) {
      var b4 = Vk(a18.alternate, a18, fj);
      a18.memoizedProps = a18.pendingProps;
      null === b4 ? Sk(a18) : Y3 = b4;
      nk.current = null;
    }
    function Sk(a18) {
      var b4 = a18;
      do {
        var c15 = b4.alternate;
        a18 = b4.return;
        if (0 === (b4.flags & 32768)) {
          if (c15 = Ej(c15, b4, fj), null !== c15) {
            Y3 = c15;
            return;
          }
        } else {
          c15 = Ij(c15, b4);
          if (null !== c15) {
            c15.flags &= 32767;
            Y3 = c15;
            return;
          }
          if (null !== a18) a18.flags |= 32768, a18.subtreeFlags = 0, a18.deletions = null;
          else {
            T3 = 6;
            Y3 = null;
            return;
          }
        }
        b4 = b4.sibling;
        if (null !== b4) {
          Y3 = b4;
          return;
        }
        Y3 = b4 = a18;
      } while (null !== b4);
      0 === T3 && (T3 = 5);
    }
    function Pk(a18, b4, c15) {
      var d12 = C5, e14 = ok.transition;
      try {
        ok.transition = null, C5 = 1, Wk(a18, b4, c15, d12);
      } finally {
        ok.transition = e14, C5 = d12;
      }
      return null;
    }
    function Wk(a18, b4, c15, d12) {
      do
        Hk();
      while (null !== wk);
      if (0 !== (K3 & 6)) throw Error(p11(327));
      c15 = a18.finishedWork;
      var e14 = a18.finishedLanes;
      if (null === c15) return null;
      a18.finishedWork = null;
      a18.finishedLanes = 0;
      if (c15 === a18.current) throw Error(p11(177));
      a18.callbackNode = null;
      a18.callbackPriority = 0;
      var f15 = c15.lanes | c15.childLanes;
      Bc(a18, f15);
      a18 === Q4 && (Y3 = Q4 = null, Z5 = 0);
      0 === (c15.subtreeFlags & 2064) && 0 === (c15.flags & 2064) || vk || (vk = true, Fk(hc, function() {
        Hk();
        return null;
      }));
      f15 = 0 !== (c15.flags & 15990);
      if (0 !== (c15.subtreeFlags & 15990) || f15) {
        f15 = ok.transition;
        ok.transition = null;
        var g7 = C5;
        C5 = 1;
        var h6 = K3;
        K3 |= 4;
        nk.current = null;
        Oj(a18, c15);
        dk(c15, a18);
        Oe3(Df);
        dd = !!Cf;
        Df = Cf = null;
        a18.current = c15;
        hk(c15, a18, e14);
        dc();
        K3 = h6;
        C5 = g7;
        ok.transition = f15;
      } else a18.current = c15;
      vk && (vk = false, wk = a18, xk = e14);
      f15 = a18.pendingLanes;
      0 === f15 && (Ri2 = null);
      mc(c15.stateNode, d12);
      Dk(a18, B4());
      if (null !== b4) for (d12 = a18.onRecoverableError, c15 = 0; c15 < b4.length; c15++) e14 = b4[c15], d12(e14.value, { componentStack: e14.stack, digest: e14.digest });
      if (Oi2) throw Oi2 = false, a18 = Pi2, Pi2 = null, a18;
      0 !== (xk & 1) && 0 !== a18.tag && Hk();
      f15 = a18.pendingLanes;
      0 !== (f15 & 1) ? a18 === zk ? yk++ : (yk = 0, zk = a18) : yk = 0;
      jg();
      return null;
    }
    function Hk() {
      if (null !== wk) {
        var a18 = Dc(xk), b4 = ok.transition, c15 = C5;
        try {
          ok.transition = null;
          C5 = 16 > a18 ? 16 : a18;
          if (null === wk) var d12 = false;
          else {
            a18 = wk;
            wk = null;
            xk = 0;
            if (0 !== (K3 & 6)) throw Error(p11(331));
            var e14 = K3;
            K3 |= 4;
            for (V3 = a18.current; null !== V3; ) {
              var f15 = V3, g7 = f15.child;
              if (0 !== (V3.flags & 16)) {
                var h6 = f15.deletions;
                if (null !== h6) {
                  for (var k5 = 0; k5 < h6.length; k5++) {
                    var l12 = h6[k5];
                    for (V3 = l12; null !== V3; ) {
                      var m12 = V3;
                      switch (m12.tag) {
                        case 0:
                        case 11:
                        case 15:
                          Pj(8, m12, f15);
                      }
                      var q2 = m12.child;
                      if (null !== q2) q2.return = m12, V3 = q2;
                      else for (; null !== V3; ) {
                        m12 = V3;
                        var r7 = m12.sibling, y4 = m12.return;
                        Sj(m12);
                        if (m12 === l12) {
                          V3 = null;
                          break;
                        }
                        if (null !== r7) {
                          r7.return = y4;
                          V3 = r7;
                          break;
                        }
                        V3 = y4;
                      }
                    }
                  }
                  var n13 = f15.alternate;
                  if (null !== n13) {
                    var t20 = n13.child;
                    if (null !== t20) {
                      n13.child = null;
                      do {
                        var J2 = t20.sibling;
                        t20.sibling = null;
                        t20 = J2;
                      } while (null !== t20);
                    }
                  }
                  V3 = f15;
                }
              }
              if (0 !== (f15.subtreeFlags & 2064) && null !== g7) g7.return = f15, V3 = g7;
              else b: for (; null !== V3; ) {
                f15 = V3;
                if (0 !== (f15.flags & 2048)) switch (f15.tag) {
                  case 0:
                  case 11:
                  case 15:
                    Pj(9, f15, f15.return);
                }
                var x3 = f15.sibling;
                if (null !== x3) {
                  x3.return = f15.return;
                  V3 = x3;
                  break b;
                }
                V3 = f15.return;
              }
            }
            var w11 = a18.current;
            for (V3 = w11; null !== V3; ) {
              g7 = V3;
              var u4 = g7.child;
              if (0 !== (g7.subtreeFlags & 2064) && null !== u4) u4.return = g7, V3 = u4;
              else b: for (g7 = w11; null !== V3; ) {
                h6 = V3;
                if (0 !== (h6.flags & 2048)) try {
                  switch (h6.tag) {
                    case 0:
                    case 11:
                    case 15:
                      Qj(9, h6);
                  }
                } catch (na) {
                  W4(h6, h6.return, na);
                }
                if (h6 === g7) {
                  V3 = null;
                  break b;
                }
                var F4 = h6.sibling;
                if (null !== F4) {
                  F4.return = h6.return;
                  V3 = F4;
                  break b;
                }
                V3 = h6.return;
              }
            }
            K3 = e14;
            jg();
            if (lc && "function" === typeof lc.onPostCommitFiberRoot) try {
              lc.onPostCommitFiberRoot(kc, a18);
            } catch (na) {
            }
            d12 = true;
          }
          return d12;
        } finally {
          C5 = c15, ok.transition = b4;
        }
      }
      return false;
    }
    function Xk(a18, b4, c15) {
      b4 = Ji(c15, b4);
      b4 = Ni(a18, b4, 1);
      a18 = nh(a18, b4, 1);
      b4 = R10();
      null !== a18 && (Ac(a18, 1, b4), Dk(a18, b4));
    }
    function W4(a18, b4, c15) {
      if (3 === a18.tag) Xk(a18, a18, c15);
      else for (; null !== b4; ) {
        if (3 === b4.tag) {
          Xk(b4, a18, c15);
          break;
        } else if (1 === b4.tag) {
          var d12 = b4.stateNode;
          if ("function" === typeof b4.type.getDerivedStateFromError || "function" === typeof d12.componentDidCatch && (null === Ri2 || !Ri2.has(d12))) {
            a18 = Ji(c15, a18);
            a18 = Qi(b4, a18, 1);
            b4 = nh(b4, a18, 1);
            a18 = R10();
            null !== b4 && (Ac(b4, 1, a18), Dk(b4, a18));
            break;
          }
        }
        b4 = b4.return;
      }
    }
    function Ti2(a18, b4, c15) {
      var d12 = a18.pingCache;
      null !== d12 && d12.delete(b4);
      b4 = R10();
      a18.pingedLanes |= a18.suspendedLanes & c15;
      Q4 === a18 && (Z5 & c15) === c15 && (4 === T3 || 3 === T3 && (Z5 & 130023424) === Z5 && 500 > B4() - fk ? Kk(a18, 0) : rk |= c15);
      Dk(a18, b4);
    }
    function Yk(a18, b4) {
      0 === b4 && (0 === (a18.mode & 1) ? b4 = 1 : (b4 = sc, sc <<= 1, 0 === (sc & 130023424) && (sc = 4194304)));
      var c15 = R10();
      a18 = ih(a18, b4);
      null !== a18 && (Ac(a18, b4, c15), Dk(a18, c15));
    }
    function uj(a18) {
      var b4 = a18.memoizedState, c15 = 0;
      null !== b4 && (c15 = b4.retryLane);
      Yk(a18, c15);
    }
    function bk(a18, b4) {
      var c15 = 0;
      switch (a18.tag) {
        case 13:
          var d12 = a18.stateNode;
          var e14 = a18.memoizedState;
          null !== e14 && (c15 = e14.retryLane);
          break;
        case 19:
          d12 = a18.stateNode;
          break;
        default:
          throw Error(p11(314));
      }
      null !== d12 && d12.delete(b4);
      Yk(a18, c15);
    }
    var Vk;
    Vk = function(a18, b4, c15) {
      if (null !== a18) if (a18.memoizedProps !== b4.pendingProps || Wf.current) dh = true;
      else {
        if (0 === (a18.lanes & c15) && 0 === (b4.flags & 128)) return dh = false, yj(a18, b4, c15);
        dh = 0 !== (a18.flags & 131072) ? true : false;
      }
      else dh = false, I6 && 0 !== (b4.flags & 1048576) && ug(b4, ng, b4.index);
      b4.lanes = 0;
      switch (b4.tag) {
        case 2:
          var d12 = b4.type;
          ij(a18, b4);
          a18 = b4.pendingProps;
          var e14 = Yf(b4, H4.current);
          ch(b4, c15);
          e14 = Nh(null, b4, d12, a18, e14, c15);
          var f15 = Sh();
          b4.flags |= 1;
          "object" === typeof e14 && null !== e14 && "function" === typeof e14.render && void 0 === e14.$$typeof ? (b4.tag = 1, b4.memoizedState = null, b4.updateQueue = null, Zf(d12) ? (f15 = true, cg(b4)) : f15 = false, b4.memoizedState = null !== e14.state && void 0 !== e14.state ? e14.state : null, kh(b4), e14.updater = Ei2, b4.stateNode = e14, e14._reactInternals = b4, Ii2(b4, d12, a18, c15), b4 = jj(null, b4, d12, true, f15, c15)) : (b4.tag = 0, I6 && f15 && vg(b4), Xi(null, b4, e14, c15), b4 = b4.child);
          return b4;
        case 16:
          d12 = b4.elementType;
          a: {
            ij(a18, b4);
            a18 = b4.pendingProps;
            e14 = d12._init;
            d12 = e14(d12._payload);
            b4.type = d12;
            e14 = b4.tag = Zk(d12);
            a18 = Ci2(d12, a18);
            switch (e14) {
              case 0:
                b4 = cj(null, b4, d12, a18, c15);
                break a;
              case 1:
                b4 = hj(null, b4, d12, a18, c15);
                break a;
              case 11:
                b4 = Yi(null, b4, d12, a18, c15);
                break a;
              case 14:
                b4 = $i(null, b4, d12, Ci2(d12.type, a18), c15);
                break a;
            }
            throw Error(p11(
              306,
              d12,
              ""
            ));
          }
          return b4;
        case 0:
          return d12 = b4.type, e14 = b4.pendingProps, e14 = b4.elementType === d12 ? e14 : Ci2(d12, e14), cj(a18, b4, d12, e14, c15);
        case 1:
          return d12 = b4.type, e14 = b4.pendingProps, e14 = b4.elementType === d12 ? e14 : Ci2(d12, e14), hj(a18, b4, d12, e14, c15);
        case 3:
          a: {
            kj(b4);
            if (null === a18) throw Error(p11(387));
            d12 = b4.pendingProps;
            f15 = b4.memoizedState;
            e14 = f15.element;
            lh(a18, b4);
            qh(b4, d12, null, c15);
            var g7 = b4.memoizedState;
            d12 = g7.element;
            if (f15.isDehydrated) if (f15 = { element: d12, isDehydrated: false, cache: g7.cache, pendingSuspenseBoundaries: g7.pendingSuspenseBoundaries, transitions: g7.transitions }, b4.updateQueue.baseState = f15, b4.memoizedState = f15, b4.flags & 256) {
              e14 = Ji(Error(p11(423)), b4);
              b4 = lj(a18, b4, d12, c15, e14);
              break a;
            } else if (d12 !== e14) {
              e14 = Ji(Error(p11(424)), b4);
              b4 = lj(a18, b4, d12, c15, e14);
              break a;
            } else for (yg = Lf(b4.stateNode.containerInfo.firstChild), xg = b4, I6 = true, zg = null, c15 = Vg(b4, null, d12, c15), b4.child = c15; c15; ) c15.flags = c15.flags & -3 | 4096, c15 = c15.sibling;
            else {
              Ig();
              if (d12 === e14) {
                b4 = Zi(a18, b4, c15);
                break a;
              }
              Xi(a18, b4, d12, c15);
            }
            b4 = b4.child;
          }
          return b4;
        case 5:
          return Ah(b4), null === a18 && Eg(b4), d12 = b4.type, e14 = b4.pendingProps, f15 = null !== a18 ? a18.memoizedProps : null, g7 = e14.children, Ef(d12, e14) ? g7 = null : null !== f15 && Ef(d12, f15) && (b4.flags |= 32), gj(a18, b4), Xi(a18, b4, g7, c15), b4.child;
        case 6:
          return null === a18 && Eg(b4), null;
        case 13:
          return oj(a18, b4, c15);
        case 4:
          return yh(b4, b4.stateNode.containerInfo), d12 = b4.pendingProps, null === a18 ? b4.child = Ug(b4, null, d12, c15) : Xi(a18, b4, d12, c15), b4.child;
        case 11:
          return d12 = b4.type, e14 = b4.pendingProps, e14 = b4.elementType === d12 ? e14 : Ci2(d12, e14), Yi(a18, b4, d12, e14, c15);
        case 7:
          return Xi(a18, b4, b4.pendingProps, c15), b4.child;
        case 8:
          return Xi(a18, b4, b4.pendingProps.children, c15), b4.child;
        case 12:
          return Xi(a18, b4, b4.pendingProps.children, c15), b4.child;
        case 10:
          a: {
            d12 = b4.type._context;
            e14 = b4.pendingProps;
            f15 = b4.memoizedProps;
            g7 = e14.value;
            G3(Wg, d12._currentValue);
            d12._currentValue = g7;
            if (null !== f15) if (He5(f15.value, g7)) {
              if (f15.children === e14.children && !Wf.current) {
                b4 = Zi(a18, b4, c15);
                break a;
              }
            } else for (f15 = b4.child, null !== f15 && (f15.return = b4); null !== f15; ) {
              var h6 = f15.dependencies;
              if (null !== h6) {
                g7 = f15.child;
                for (var k5 = h6.firstContext; null !== k5; ) {
                  if (k5.context === d12) {
                    if (1 === f15.tag) {
                      k5 = mh(-1, c15 & -c15);
                      k5.tag = 2;
                      var l12 = f15.updateQueue;
                      if (null !== l12) {
                        l12 = l12.shared;
                        var m12 = l12.pending;
                        null === m12 ? k5.next = k5 : (k5.next = m12.next, m12.next = k5);
                        l12.pending = k5;
                      }
                    }
                    f15.lanes |= c15;
                    k5 = f15.alternate;
                    null !== k5 && (k5.lanes |= c15);
                    bh(
                      f15.return,
                      c15,
                      b4
                    );
                    h6.lanes |= c15;
                    break;
                  }
                  k5 = k5.next;
                }
              } else if (10 === f15.tag) g7 = f15.type === b4.type ? null : f15.child;
              else if (18 === f15.tag) {
                g7 = f15.return;
                if (null === g7) throw Error(p11(341));
                g7.lanes |= c15;
                h6 = g7.alternate;
                null !== h6 && (h6.lanes |= c15);
                bh(g7, c15, b4);
                g7 = f15.sibling;
              } else g7 = f15.child;
              if (null !== g7) g7.return = f15;
              else for (g7 = f15; null !== g7; ) {
                if (g7 === b4) {
                  g7 = null;
                  break;
                }
                f15 = g7.sibling;
                if (null !== f15) {
                  f15.return = g7.return;
                  g7 = f15;
                  break;
                }
                g7 = g7.return;
              }
              f15 = g7;
            }
            Xi(a18, b4, e14.children, c15);
            b4 = b4.child;
          }
          return b4;
        case 9:
          return e14 = b4.type, d12 = b4.pendingProps.children, ch(b4, c15), e14 = eh(e14), d12 = d12(e14), b4.flags |= 1, Xi(a18, b4, d12, c15), b4.child;
        case 14:
          return d12 = b4.type, e14 = Ci2(d12, b4.pendingProps), e14 = Ci2(d12.type, e14), $i(a18, b4, d12, e14, c15);
        case 15:
          return bj(a18, b4, b4.type, b4.pendingProps, c15);
        case 17:
          return d12 = b4.type, e14 = b4.pendingProps, e14 = b4.elementType === d12 ? e14 : Ci2(d12, e14), ij(a18, b4), b4.tag = 1, Zf(d12) ? (a18 = true, cg(b4)) : a18 = false, ch(b4, c15), Gi(b4, d12, e14), Ii2(b4, d12, e14, c15), jj(null, b4, d12, true, a18, c15);
        case 19:
          return xj(a18, b4, c15);
        case 22:
          return dj(a18, b4, c15);
      }
      throw Error(p11(156, b4.tag));
    };
    function Fk(a18, b4) {
      return ac(a18, b4);
    }
    function $k(a18, b4, c15, d12) {
      this.tag = a18;
      this.key = c15;
      this.sibling = this.child = this.return = this.stateNode = this.type = this.elementType = null;
      this.index = 0;
      this.ref = null;
      this.pendingProps = b4;
      this.dependencies = this.memoizedState = this.updateQueue = this.memoizedProps = null;
      this.mode = d12;
      this.subtreeFlags = this.flags = 0;
      this.deletions = null;
      this.childLanes = this.lanes = 0;
      this.alternate = null;
    }
    function Bg(a18, b4, c15, d12) {
      return new $k(a18, b4, c15, d12);
    }
    function aj(a18) {
      a18 = a18.prototype;
      return !(!a18 || !a18.isReactComponent);
    }
    function Zk(a18) {
      if ("function" === typeof a18) return aj(a18) ? 1 : 0;
      if (void 0 !== a18 && null !== a18) {
        a18 = a18.$$typeof;
        if (a18 === Da) return 11;
        if (a18 === Ga) return 14;
      }
      return 2;
    }
    function Pg(a18, b4) {
      var c15 = a18.alternate;
      null === c15 ? (c15 = Bg(a18.tag, b4, a18.key, a18.mode), c15.elementType = a18.elementType, c15.type = a18.type, c15.stateNode = a18.stateNode, c15.alternate = a18, a18.alternate = c15) : (c15.pendingProps = b4, c15.type = a18.type, c15.flags = 0, c15.subtreeFlags = 0, c15.deletions = null);
      c15.flags = a18.flags & 14680064;
      c15.childLanes = a18.childLanes;
      c15.lanes = a18.lanes;
      c15.child = a18.child;
      c15.memoizedProps = a18.memoizedProps;
      c15.memoizedState = a18.memoizedState;
      c15.updateQueue = a18.updateQueue;
      b4 = a18.dependencies;
      c15.dependencies = null === b4 ? null : { lanes: b4.lanes, firstContext: b4.firstContext };
      c15.sibling = a18.sibling;
      c15.index = a18.index;
      c15.ref = a18.ref;
      return c15;
    }
    function Rg(a18, b4, c15, d12, e14, f15) {
      var g7 = 2;
      d12 = a18;
      if ("function" === typeof a18) aj(a18) && (g7 = 1);
      else if ("string" === typeof a18) g7 = 5;
      else a: switch (a18) {
        case ya:
          return Tg(c15.children, e14, f15, b4);
        case za:
          g7 = 8;
          e14 |= 8;
          break;
        case Aa:
          return a18 = Bg(12, c15, b4, e14 | 2), a18.elementType = Aa, a18.lanes = f15, a18;
        case Ea:
          return a18 = Bg(13, c15, b4, e14), a18.elementType = Ea, a18.lanes = f15, a18;
        case Fa:
          return a18 = Bg(19, c15, b4, e14), a18.elementType = Fa, a18.lanes = f15, a18;
        case Ia:
          return pj(c15, e14, f15, b4);
        default:
          if ("object" === typeof a18 && null !== a18) switch (a18.$$typeof) {
            case Ba:
              g7 = 10;
              break a;
            case Ca:
              g7 = 9;
              break a;
            case Da:
              g7 = 11;
              break a;
            case Ga:
              g7 = 14;
              break a;
            case Ha:
              g7 = 16;
              d12 = null;
              break a;
          }
          throw Error(p11(130, null == a18 ? a18 : typeof a18, ""));
      }
      b4 = Bg(g7, c15, b4, e14);
      b4.elementType = a18;
      b4.type = d12;
      b4.lanes = f15;
      return b4;
    }
    function Tg(a18, b4, c15, d12) {
      a18 = Bg(7, a18, d12, b4);
      a18.lanes = c15;
      return a18;
    }
    function pj(a18, b4, c15, d12) {
      a18 = Bg(22, a18, d12, b4);
      a18.elementType = Ia;
      a18.lanes = c15;
      a18.stateNode = { isHidden: false };
      return a18;
    }
    function Qg(a18, b4, c15) {
      a18 = Bg(6, a18, null, b4);
      a18.lanes = c15;
      return a18;
    }
    function Sg(a18, b4, c15) {
      b4 = Bg(4, null !== a18.children ? a18.children : [], a18.key, b4);
      b4.lanes = c15;
      b4.stateNode = { containerInfo: a18.containerInfo, pendingChildren: null, implementation: a18.implementation };
      return b4;
    }
    function al2(a18, b4, c15, d12, e14) {
      this.tag = b4;
      this.containerInfo = a18;
      this.finishedWork = this.pingCache = this.current = this.pendingChildren = null;
      this.timeoutHandle = -1;
      this.callbackNode = this.pendingContext = this.context = null;
      this.callbackPriority = 0;
      this.eventTimes = zc(0);
      this.expirationTimes = zc(-1);
      this.entangledLanes = this.finishedLanes = this.mutableReadLanes = this.expiredLanes = this.pingedLanes = this.suspendedLanes = this.pendingLanes = 0;
      this.entanglements = zc(0);
      this.identifierPrefix = d12;
      this.onRecoverableError = e14;
      this.mutableSourceEagerHydrationData = null;
    }
    function bl2(a18, b4, c15, d12, e14, f15, g7, h6, k5) {
      a18 = new al2(a18, b4, c15, h6, k5);
      1 === b4 ? (b4 = 1, true === f15 && (b4 |= 8)) : b4 = 0;
      f15 = Bg(3, null, null, b4);
      a18.current = f15;
      f15.stateNode = a18;
      f15.memoizedState = { element: d12, isDehydrated: c15, cache: null, transitions: null, pendingSuspenseBoundaries: null };
      kh(f15);
      return a18;
    }
    function cl2(a18, b4, c15) {
      var d12 = 3 < arguments.length && void 0 !== arguments[3] ? arguments[3] : null;
      return { $$typeof: wa, key: null == d12 ? null : "" + d12, children: a18, containerInfo: b4, implementation: c15 };
    }
    function dl2(a18) {
      if (!a18) return Vf;
      a18 = a18._reactInternals;
      a: {
        if (Vb(a18) !== a18 || 1 !== a18.tag) throw Error(p11(170));
        var b4 = a18;
        do {
          switch (b4.tag) {
            case 3:
              b4 = b4.stateNode.context;
              break a;
            case 1:
              if (Zf(b4.type)) {
                b4 = b4.stateNode.__reactInternalMemoizedMergedChildContext;
                break a;
              }
          }
          b4 = b4.return;
        } while (null !== b4);
        throw Error(p11(171));
      }
      if (1 === a18.tag) {
        var c15 = a18.type;
        if (Zf(c15)) return bg(a18, c15, b4);
      }
      return b4;
    }
    function el2(a18, b4, c15, d12, e14, f15, g7, h6, k5) {
      a18 = bl2(c15, d12, true, a18, e14, f15, g7, h6, k5);
      a18.context = dl2(null);
      c15 = a18.current;
      d12 = R10();
      e14 = yi2(c15);
      f15 = mh(d12, e14);
      f15.callback = void 0 !== b4 && null !== b4 ? b4 : null;
      nh(c15, f15, e14);
      a18.current.lanes = e14;
      Ac(a18, e14, d12);
      Dk(a18, d12);
      return a18;
    }
    function fl2(a18, b4, c15, d12) {
      var e14 = b4.current, f15 = R10(), g7 = yi2(e14);
      c15 = dl2(c15);
      null === b4.context ? b4.context = c15 : b4.pendingContext = c15;
      b4 = mh(f15, g7);
      b4.payload = { element: a18 };
      d12 = void 0 === d12 ? null : d12;
      null !== d12 && (b4.callback = d12);
      a18 = nh(e14, b4, g7);
      null !== a18 && (gi2(a18, e14, g7, f15), oh(a18, e14, g7));
      return g7;
    }
    function gl2(a18) {
      a18 = a18.current;
      if (!a18.child) return null;
      switch (a18.child.tag) {
        case 5:
          return a18.child.stateNode;
        default:
          return a18.child.stateNode;
      }
    }
    function hl2(a18, b4) {
      a18 = a18.memoizedState;
      if (null !== a18 && null !== a18.dehydrated) {
        var c15 = a18.retryLane;
        a18.retryLane = 0 !== c15 && c15 < b4 ? c15 : b4;
      }
    }
    function il2(a18, b4) {
      hl2(a18, b4);
      (a18 = a18.alternate) && hl2(a18, b4);
    }
    function jl() {
      return null;
    }
    var kl = "function" === typeof reportError ? reportError : function(a18) {
      console.error(a18);
    };
    function ll2(a18) {
      this._internalRoot = a18;
    }
    ml2.prototype.render = ll2.prototype.render = function(a18) {
      var b4 = this._internalRoot;
      if (null === b4) throw Error(p11(409));
      fl2(a18, b4, null, null);
    };
    ml2.prototype.unmount = ll2.prototype.unmount = function() {
      var a18 = this._internalRoot;
      if (null !== a18) {
        this._internalRoot = null;
        var b4 = a18.containerInfo;
        Rk(function() {
          fl2(null, a18, null, null);
        });
        b4[uf] = null;
      }
    };
    function ml2(a18) {
      this._internalRoot = a18;
    }
    ml2.prototype.unstable_scheduleHydration = function(a18) {
      if (a18) {
        var b4 = Hc();
        a18 = { blockedOn: null, target: a18, priority: b4 };
        for (var c15 = 0; c15 < Qc.length && 0 !== b4 && b4 < Qc[c15].priority; c15++) ;
        Qc.splice(c15, 0, a18);
        0 === c15 && Vc(a18);
      }
    };
    function nl2(a18) {
      return !(!a18 || 1 !== a18.nodeType && 9 !== a18.nodeType && 11 !== a18.nodeType);
    }
    function ol2(a18) {
      return !(!a18 || 1 !== a18.nodeType && 9 !== a18.nodeType && 11 !== a18.nodeType && (8 !== a18.nodeType || " react-mount-point-unstable " !== a18.nodeValue));
    }
    function pl2() {
    }
    function ql(a18, b4, c15, d12, e14) {
      if (e14) {
        if ("function" === typeof d12) {
          var f15 = d12;
          d12 = function() {
            var a19 = gl2(g7);
            f15.call(a19);
          };
        }
        var g7 = el2(b4, d12, a18, 0, null, false, false, "", pl2);
        a18._reactRootContainer = g7;
        a18[uf] = g7.current;
        sf(8 === a18.nodeType ? a18.parentNode : a18);
        Rk();
        return g7;
      }
      for (; e14 = a18.lastChild; ) a18.removeChild(e14);
      if ("function" === typeof d12) {
        var h6 = d12;
        d12 = function() {
          var a19 = gl2(k5);
          h6.call(a19);
        };
      }
      var k5 = bl2(a18, 0, false, null, null, false, false, "", pl2);
      a18._reactRootContainer = k5;
      a18[uf] = k5.current;
      sf(8 === a18.nodeType ? a18.parentNode : a18);
      Rk(function() {
        fl2(b4, k5, c15, d12);
      });
      return k5;
    }
    function rl2(a18, b4, c15, d12, e14) {
      var f15 = c15._reactRootContainer;
      if (f15) {
        var g7 = f15;
        if ("function" === typeof e14) {
          var h6 = e14;
          e14 = function() {
            var a19 = gl2(g7);
            h6.call(a19);
          };
        }
        fl2(b4, g7, a18, e14);
      } else g7 = ql(c15, b4, a18, e14, d12);
      return gl2(g7);
    }
    Ec = function(a18) {
      switch (a18.tag) {
        case 3:
          var b4 = a18.stateNode;
          if (b4.current.memoizedState.isDehydrated) {
            var c15 = tc(b4.pendingLanes);
            0 !== c15 && (Cc(b4, c15 | 1), Dk(b4, B4()), 0 === (K3 & 6) && (Gj = B4() + 500, jg()));
          }
          break;
        case 13:
          Rk(function() {
            var b5 = ih(a18, 1);
            if (null !== b5) {
              var c16 = R10();
              gi2(b5, a18, 1, c16);
            }
          }), il2(a18, 1);
      }
    };
    Fc = function(a18) {
      if (13 === a18.tag) {
        var b4 = ih(a18, 134217728);
        if (null !== b4) {
          var c15 = R10();
          gi2(b4, a18, 134217728, c15);
        }
        il2(a18, 134217728);
      }
    };
    Gc = function(a18) {
      if (13 === a18.tag) {
        var b4 = yi2(a18), c15 = ih(a18, b4);
        if (null !== c15) {
          var d12 = R10();
          gi2(c15, a18, b4, d12);
        }
        il2(a18, b4);
      }
    };
    Hc = function() {
      return C5;
    };
    Ic = function(a18, b4) {
      var c15 = C5;
      try {
        return C5 = a18, b4();
      } finally {
        C5 = c15;
      }
    };
    yb = function(a18, b4, c15) {
      switch (b4) {
        case "input":
          bb(a18, c15);
          b4 = c15.name;
          if ("radio" === c15.type && null != b4) {
            for (c15 = a18; c15.parentNode; ) c15 = c15.parentNode;
            c15 = c15.querySelectorAll("input[name=" + JSON.stringify("" + b4) + '][type="radio"]');
            for (b4 = 0; b4 < c15.length; b4++) {
              var d12 = c15[b4];
              if (d12 !== a18 && d12.form === a18.form) {
                var e14 = Db(d12);
                if (!e14) throw Error(p11(90));
                Wa(d12);
                bb(d12, e14);
              }
            }
          }
          break;
        case "textarea":
          ib(a18, c15);
          break;
        case "select":
          b4 = c15.value, null != b4 && fb(a18, !!c15.multiple, b4, false);
      }
    };
    Gb = Qk;
    Hb = Rk;
    var sl2 = { usingClientEntryPoint: false, Events: [Cb, ue4, Db, Eb, Fb, Qk] };
    var tl2 = { findFiberByHostInstance: Wc, bundleType: 0, version: "18.3.1", rendererPackageName: "react-dom" };
    var ul2 = { bundleType: tl2.bundleType, version: tl2.version, rendererPackageName: tl2.rendererPackageName, rendererConfig: tl2.rendererConfig, overrideHookState: null, overrideHookStateDeletePath: null, overrideHookStateRenamePath: null, overrideProps: null, overridePropsDeletePath: null, overridePropsRenamePath: null, setErrorHandler: null, setSuspenseHandler: null, scheduleUpdate: null, currentDispatcherRef: ua.ReactCurrentDispatcher, findHostInstanceByFiber: function(a18) {
      a18 = Zb(a18);
      return null === a18 ? null : a18.stateNode;
    }, findFiberByHostInstance: tl2.findFiberByHostInstance || jl, findHostInstancesForRefresh: null, scheduleRefresh: null, scheduleRoot: null, setRefreshHandler: null, getCurrentFiber: null, reconcilerVersion: "18.3.1-next-f1338f8080-20240426" };
    if ("undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__) {
      vl2 = __REACT_DEVTOOLS_GLOBAL_HOOK__;
      if (!vl2.isDisabled && vl2.supportsFiber) try {
        kc = vl2.inject(ul2), lc = vl2;
      } catch (a18) {
      }
    }
    var vl2;
    exports.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED = sl2;
    exports.createPortal = function(a18, b4) {
      var c15 = 2 < arguments.length && void 0 !== arguments[2] ? arguments[2] : null;
      if (!nl2(b4)) throw Error(p11(200));
      return cl2(a18, b4, null, c15);
    };
    exports.createRoot = function(a18, b4) {
      if (!nl2(a18)) throw Error(p11(299));
      var c15 = false, d12 = "", e14 = kl;
      null !== b4 && void 0 !== b4 && (true === b4.unstable_strictMode && (c15 = true), void 0 !== b4.identifierPrefix && (d12 = b4.identifierPrefix), void 0 !== b4.onRecoverableError && (e14 = b4.onRecoverableError));
      b4 = bl2(a18, 1, false, null, null, c15, false, d12, e14);
      a18[uf] = b4.current;
      sf(8 === a18.nodeType ? a18.parentNode : a18);
      return new ll2(b4);
    };
    exports.findDOMNode = function(a18) {
      if (null == a18) return null;
      if (1 === a18.nodeType) return a18;
      var b4 = a18._reactInternals;
      if (void 0 === b4) {
        if ("function" === typeof a18.render) throw Error(p11(188));
        a18 = Object.keys(a18).join(",");
        throw Error(p11(268, a18));
      }
      a18 = Zb(b4);
      a18 = null === a18 ? null : a18.stateNode;
      return a18;
    };
    exports.flushSync = function(a18) {
      return Rk(a18);
    };
    exports.hydrate = function(a18, b4, c15) {
      if (!ol2(b4)) throw Error(p11(200));
      return rl2(null, a18, b4, true, c15);
    };
    exports.hydrateRoot = function(a18, b4, c15) {
      if (!nl2(a18)) throw Error(p11(405));
      var d12 = null != c15 && c15.hydratedSources || null, e14 = false, f15 = "", g7 = kl;
      null !== c15 && void 0 !== c15 && (true === c15.unstable_strictMode && (e14 = true), void 0 !== c15.identifierPrefix && (f15 = c15.identifierPrefix), void 0 !== c15.onRecoverableError && (g7 = c15.onRecoverableError));
      b4 = el2(b4, null, a18, 1, null != c15 ? c15 : null, e14, false, f15, g7);
      a18[uf] = b4.current;
      sf(a18);
      if (d12) for (a18 = 0; a18 < d12.length; a18++) c15 = d12[a18], e14 = c15._getVersion, e14 = e14(c15._source), null == b4.mutableSourceEagerHydrationData ? b4.mutableSourceEagerHydrationData = [c15, e14] : b4.mutableSourceEagerHydrationData.push(
        c15,
        e14
      );
      return new ml2(b4);
    };
    exports.render = function(a18, b4, c15) {
      if (!ol2(b4)) throw Error(p11(200));
      return rl2(null, a18, b4, false, c15);
    };
    exports.unmountComponentAtNode = function(a18) {
      if (!ol2(a18)) throw Error(p11(40));
      return a18._reactRootContainer ? (Rk(function() {
        rl2(null, null, a18, false, function() {
          a18._reactRootContainer = null;
          a18[uf] = null;
        });
      }), true) : false;
    };
    exports.unstable_batchedUpdates = Qk;
    exports.unstable_renderSubtreeIntoContainer = function(a18, b4, c15, d12) {
      if (!ol2(c15)) throw Error(p11(200));
      if (null == a18 || void 0 === a18._reactInternals) throw Error(p11(38));
      return rl2(a18, b4, c15, false, d12);
    };
    exports.version = "18.3.1-next-f1338f8080-20240426";
  }
});

// node_modules/react-dom/index.js
var require_react_dom = __commonJS({
  "node_modules/react-dom/index.js"(exports, module) {
    "use strict";
    function checkDCE() {
      if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ === "undefined" || typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE !== "function") {
        return;
      }
      if (false) {
        throw new Error("^_^");
      }
      try {
        __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE(checkDCE);
      } catch (err) {
        console.error(err);
      }
    }
    if (true) {
      checkDCE();
      module.exports = require_react_dom_production_min();
    } else {
      module.exports = null;
    }
  }
});

// node_modules/@mantine/core/esm/components/UnstyledButton/UnstyledButton.module.css.mjs
var classes;
var init_UnstyledButton_module_css = __esm({
  "node_modules/@mantine/core/esm/components/UnstyledButton/UnstyledButton.module.css.mjs"() {
    "use client";
    classes = { "root": "m_87cf2631" };
  }
});

// node_modules/@mantine/core/esm/components/UnstyledButton/UnstyledButton.mjs
var import_jsx_runtime34, import_react59, defaultProps, UnstyledButton;
var init_UnstyledButton = __esm({
  "node_modules/@mantine/core/esm/components/UnstyledButton/UnstyledButton.mjs"() {
    "use client";
    import_jsx_runtime34 = __toESM(require_jsx_runtime(), 1);
    import_react59 = __toESM(require_react(), 1);
    init_clsx();
    init_use_props();
    init_use_styles();
    init_Box();
    init_polymorphic_factory();
    init_UnstyledButton_module_css();
    defaultProps = {
      __staticSelector: "UnstyledButton"
    };
    UnstyledButton = polymorphicFactory(
      (_props, ref) => {
        const props = useProps("UnstyledButton", defaultProps, _props);
        const {
          className,
          component = "button",
          __staticSelector,
          unstyled,
          classNames,
          styles,
          style,
          ...others
        } = props;
        const getStyles = useStyles({
          name: __staticSelector,
          props,
          classes,
          className,
          style,
          classNames,
          styles,
          unstyled
        });
        return /* @__PURE__ */ (0, import_jsx_runtime34.jsx)(
          Box,
          {
            ...getStyles("root", { focusable: true }),
            component,
            ref,
            type: component === "button" ? "button" : void 0,
            ...others
          }
        );
      }
    );
    UnstyledButton.classes = classes;
    UnstyledButton.displayName = "@mantine/core/UnstyledButton";
  }
});

// node_modules/@mantine/core/esm/components/VisuallyHidden/VisuallyHidden.module.css.mjs
var classes2;
var init_VisuallyHidden_module_css = __esm({
  "node_modules/@mantine/core/esm/components/VisuallyHidden/VisuallyHidden.module.css.mjs"() {
    "use client";
    classes2 = { "root": "m_515a97f8" };
  }
});

// node_modules/@mantine/core/esm/components/VisuallyHidden/VisuallyHidden.mjs
var import_jsx_runtime35, import_react60, defaultProps2, VisuallyHidden;
var init_VisuallyHidden = __esm({
  "node_modules/@mantine/core/esm/components/VisuallyHidden/VisuallyHidden.mjs"() {
    "use client";
    import_jsx_runtime35 = __toESM(require_jsx_runtime(), 1);
    import_react60 = __toESM(require_react(), 1);
    init_clsx();
    init_use_props();
    init_use_styles();
    init_Box();
    init_factory();
    init_VisuallyHidden_module_css();
    defaultProps2 = {};
    VisuallyHidden = factory((_props, ref) => {
      const props = useProps("VisuallyHidden", defaultProps2, _props);
      const { classNames, className, style, styles, unstyled, vars, ...others } = props;
      const getStyles = useStyles({
        name: "VisuallyHidden",
        classes: classes2,
        props,
        className,
        style,
        classNames,
        styles,
        unstyled
      });
      return /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(Box, { component: "span", ref, ...getStyles("root"), ...others });
    });
    VisuallyHidden.classes = classes2;
    VisuallyHidden.displayName = "@mantine/core/VisuallyHidden";
  }
});

// node_modules/@mantine/core/esm/components/Paper/Paper.module.css.mjs
var classes3;
var init_Paper_module_css = __esm({
  "node_modules/@mantine/core/esm/components/Paper/Paper.module.css.mjs"() {
    "use client";
    classes3 = { "root": "m_1b7284a3" };
  }
});

// node_modules/@mantine/core/esm/components/Paper/Paper.mjs
var import_jsx_runtime36, import_react61, defaultProps3, varsResolver, Paper;
var init_Paper = __esm({
  "node_modules/@mantine/core/esm/components/Paper/Paper.mjs"() {
    "use client";
    import_jsx_runtime36 = __toESM(require_jsx_runtime(), 1);
    import_react61 = __toESM(require_react(), 1);
    init_get_size();
    init_create_vars_resolver();
    init_clsx();
    init_use_props();
    init_use_styles();
    init_Box();
    init_polymorphic_factory();
    init_Paper_module_css();
    defaultProps3 = {};
    varsResolver = createVarsResolver((_2, { radius, shadow }) => ({
      root: {
        "--paper-radius": radius === void 0 ? void 0 : getRadius(radius),
        "--paper-shadow": getShadow(shadow)
      }
    }));
    Paper = polymorphicFactory((_props, ref) => {
      const props = useProps("Paper", defaultProps3, _props);
      const {
        classNames,
        className,
        style,
        styles,
        unstyled,
        withBorder,
        vars,
        radius,
        shadow,
        variant,
        mod,
        ...others
      } = props;
      const getStyles = useStyles({
        name: "Paper",
        props,
        classes: classes3,
        className,
        style,
        classNames,
        styles,
        unstyled,
        vars,
        varsResolver
      });
      return /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(
        Box,
        {
          ref,
          mod: [{ "data-with-border": withBorder }, mod],
          ...getStyles("root"),
          variant,
          ...others
        }
      );
    });
    Paper.classes = classes3;
    Paper.displayName = "@mantine/core/Paper";
  }
});

// node_modules/@floating-ui/utils/dist/floating-ui.utils.dom.mjs
function hasWindow() {
  return typeof window !== "undefined";
}
function getNodeName(node) {
  if (isNode(node)) {
    return (node.nodeName || "").toLowerCase();
  }
  return "#document";
}
function getWindow(node) {
  var _node$ownerDocument;
  return (node == null || (_node$ownerDocument = node.ownerDocument) == null ? void 0 : _node$ownerDocument.defaultView) || window;
}
function getDocumentElement(node) {
  var _ref;
  return (_ref = (isNode(node) ? node.ownerDocument : node.document) || window.document) == null ? void 0 : _ref.documentElement;
}
function isNode(value) {
  if (!hasWindow()) {
    return false;
  }
  return value instanceof Node || value instanceof getWindow(value).Node;
}
function isElement2(value) {
  if (!hasWindow()) {
    return false;
  }
  return value instanceof Element || value instanceof getWindow(value).Element;
}
function isHTMLElement(value) {
  if (!hasWindow()) {
    return false;
  }
  return value instanceof HTMLElement || value instanceof getWindow(value).HTMLElement;
}
function isShadowRoot(value) {
  if (!hasWindow() || typeof ShadowRoot === "undefined") {
    return false;
  }
  return value instanceof ShadowRoot || value instanceof getWindow(value).ShadowRoot;
}
function isOverflowElement(element) {
  const {
    overflow,
    overflowX,
    overflowY,
    display
  } = getComputedStyle2(element);
  return /auto|scroll|overlay|hidden|clip/.test(overflow + overflowY + overflowX) && !["inline", "contents"].includes(display);
}
function isTableElement(element) {
  return ["table", "td", "th"].includes(getNodeName(element));
}
function isTopLayer(element) {
  return [":popover-open", ":modal"].some((selector) => {
    try {
      return element.matches(selector);
    } catch (e14) {
      return false;
    }
  });
}
function isContainingBlock(elementOrCss) {
  const webkit = isWebKit();
  const css = isElement2(elementOrCss) ? getComputedStyle2(elementOrCss) : elementOrCss;
  return css.transform !== "none" || css.perspective !== "none" || (css.containerType ? css.containerType !== "normal" : false) || !webkit && (css.backdropFilter ? css.backdropFilter !== "none" : false) || !webkit && (css.filter ? css.filter !== "none" : false) || ["transform", "perspective", "filter"].some((value) => (css.willChange || "").includes(value)) || ["paint", "layout", "strict", "content"].some((value) => (css.contain || "").includes(value));
}
function getContainingBlock(element) {
  let currentNode = getParentNode(element);
  while (isHTMLElement(currentNode) && !isLastTraversableNode(currentNode)) {
    if (isContainingBlock(currentNode)) {
      return currentNode;
    } else if (isTopLayer(currentNode)) {
      return null;
    }
    currentNode = getParentNode(currentNode);
  }
  return null;
}
function isWebKit() {
  if (typeof CSS === "undefined" || !CSS.supports) return false;
  return CSS.supports("-webkit-backdrop-filter", "none");
}
function isLastTraversableNode(node) {
  return ["html", "body", "#document"].includes(getNodeName(node));
}
function getComputedStyle2(element) {
  return getWindow(element).getComputedStyle(element);
}
function getNodeScroll(element) {
  if (isElement2(element)) {
    return {
      scrollLeft: element.scrollLeft,
      scrollTop: element.scrollTop
    };
  }
  return {
    scrollLeft: element.scrollX,
    scrollTop: element.scrollY
  };
}
function getParentNode(node) {
  if (getNodeName(node) === "html") {
    return node;
  }
  const result = (
    // Step into the shadow DOM of the parent of a slotted node.
    node.assignedSlot || // DOM Element detected.
    node.parentNode || // ShadowRoot detected.
    isShadowRoot(node) && node.host || // Fallback.
    getDocumentElement(node)
  );
  return isShadowRoot(result) ? result.host : result;
}
function getNearestOverflowAncestor(node) {
  const parentNode = getParentNode(node);
  if (isLastTraversableNode(parentNode)) {
    return node.ownerDocument ? node.ownerDocument.body : node.body;
  }
  if (isHTMLElement(parentNode) && isOverflowElement(parentNode)) {
    return parentNode;
  }
  return getNearestOverflowAncestor(parentNode);
}
function getOverflowAncestors(node, list, traverseIframes) {
  var _node$ownerDocument2;
  if (list === void 0) {
    list = [];
  }
  if (traverseIframes === void 0) {
    traverseIframes = true;
  }
  const scrollableAncestor = getNearestOverflowAncestor(node);
  const isBody = scrollableAncestor === ((_node$ownerDocument2 = node.ownerDocument) == null ? void 0 : _node$ownerDocument2.body);
  const win = getWindow(scrollableAncestor);
  if (isBody) {
    const frameElement = getFrameElement(win);
    return list.concat(win, win.visualViewport || [], isOverflowElement(scrollableAncestor) ? scrollableAncestor : [], frameElement && traverseIframes ? getOverflowAncestors(frameElement) : []);
  }
  return list.concat(scrollableAncestor, getOverflowAncestors(scrollableAncestor, [], traverseIframes));
}
function getFrameElement(win) {
  return win.parent && Object.getPrototypeOf(win.parent) ? win.frameElement : null;
}
var init_floating_ui_utils_dom = __esm({
  "node_modules/@floating-ui/utils/dist/floating-ui.utils.dom.mjs"() {
  }
});

// node_modules/@floating-ui/utils/dist/floating-ui.utils.mjs
function clamp2(start, value, end) {
  return max(start, min(value, end));
}
function evaluate(value, param) {
  return typeof value === "function" ? value(param) : value;
}
function getSide(placement) {
  return placement.split("-")[0];
}
function getAlignment(placement) {
  return placement.split("-")[1];
}
function getOppositeAxis(axis) {
  return axis === "x" ? "y" : "x";
}
function getAxisLength(axis) {
  return axis === "y" ? "height" : "width";
}
function getSideAxis(placement) {
  return ["top", "bottom"].includes(getSide(placement)) ? "y" : "x";
}
function getAlignmentAxis(placement) {
  return getOppositeAxis(getSideAxis(placement));
}
function getAlignmentSides(placement, rects, rtl) {
  if (rtl === void 0) {
    rtl = false;
  }
  const alignment = getAlignment(placement);
  const alignmentAxis = getAlignmentAxis(placement);
  const length = getAxisLength(alignmentAxis);
  let mainAlignmentSide = alignmentAxis === "x" ? alignment === (rtl ? "end" : "start") ? "right" : "left" : alignment === "start" ? "bottom" : "top";
  if (rects.reference[length] > rects.floating[length]) {
    mainAlignmentSide = getOppositePlacement(mainAlignmentSide);
  }
  return [mainAlignmentSide, getOppositePlacement(mainAlignmentSide)];
}
function getExpandedPlacements(placement) {
  const oppositePlacement = getOppositePlacement(placement);
  return [getOppositeAlignmentPlacement(placement), oppositePlacement, getOppositeAlignmentPlacement(oppositePlacement)];
}
function getOppositeAlignmentPlacement(placement) {
  return placement.replace(/start|end/g, (alignment) => oppositeAlignmentMap[alignment]);
}
function getSideList(side, isStart, rtl) {
  const lr = ["left", "right"];
  const rl2 = ["right", "left"];
  const tb = ["top", "bottom"];
  const bt3 = ["bottom", "top"];
  switch (side) {
    case "top":
    case "bottom":
      if (rtl) return isStart ? rl2 : lr;
      return isStart ? lr : rl2;
    case "left":
    case "right":
      return isStart ? tb : bt3;
    default:
      return [];
  }
}
function getOppositeAxisPlacements(placement, flipAlignment, direction, rtl) {
  const alignment = getAlignment(placement);
  let list = getSideList(getSide(placement), direction === "start", rtl);
  if (alignment) {
    list = list.map((side) => side + "-" + alignment);
    if (flipAlignment) {
      list = list.concat(list.map(getOppositeAlignmentPlacement));
    }
  }
  return list;
}
function getOppositePlacement(placement) {
  return placement.replace(/left|right|bottom|top/g, (side) => oppositeSideMap[side]);
}
function expandPaddingObject(padding) {
  return {
    top: 0,
    right: 0,
    bottom: 0,
    left: 0,
    ...padding
  };
}
function getPaddingObject(padding) {
  return typeof padding !== "number" ? expandPaddingObject(padding) : {
    top: padding,
    right: padding,
    bottom: padding,
    left: padding
  };
}
function rectToClientRect(rect) {
  const {
    x: x3,
    y: y4,
    width,
    height
  } = rect;
  return {
    width,
    height,
    top: y4,
    left: x3,
    right: x3 + width,
    bottom: y4 + height,
    x: x3,
    y: y4
  };
}
var min, max, round2, floor, createCoords, oppositeSideMap, oppositeAlignmentMap;
var init_floating_ui_utils = __esm({
  "node_modules/@floating-ui/utils/dist/floating-ui.utils.mjs"() {
    min = Math.min;
    max = Math.max;
    round2 = Math.round;
    floor = Math.floor;
    createCoords = (v5) => ({
      x: v5,
      y: v5
    });
    oppositeSideMap = {
      left: "right",
      right: "left",
      bottom: "top",
      top: "bottom"
    };
    oppositeAlignmentMap = {
      start: "end",
      end: "start"
    };
  }
});

// node_modules/@floating-ui/core/dist/floating-ui.core.mjs
function computeCoordsFromPlacement(_ref, placement, rtl) {
  let {
    reference,
    floating
  } = _ref;
  const sideAxis = getSideAxis(placement);
  const alignmentAxis = getAlignmentAxis(placement);
  const alignLength = getAxisLength(alignmentAxis);
  const side = getSide(placement);
  const isVertical = sideAxis === "y";
  const commonX = reference.x + reference.width / 2 - floating.width / 2;
  const commonY = reference.y + reference.height / 2 - floating.height / 2;
  const commonAlign = reference[alignLength] / 2 - floating[alignLength] / 2;
  let coords;
  switch (side) {
    case "top":
      coords = {
        x: commonX,
        y: reference.y - floating.height
      };
      break;
    case "bottom":
      coords = {
        x: commonX,
        y: reference.y + reference.height
      };
      break;
    case "right":
      coords = {
        x: reference.x + reference.width,
        y: commonY
      };
      break;
    case "left":
      coords = {
        x: reference.x - floating.width,
        y: commonY
      };
      break;
    default:
      coords = {
        x: reference.x,
        y: reference.y
      };
  }
  switch (getAlignment(placement)) {
    case "start":
      coords[alignmentAxis] -= commonAlign * (rtl && isVertical ? -1 : 1);
      break;
    case "end":
      coords[alignmentAxis] += commonAlign * (rtl && isVertical ? -1 : 1);
      break;
  }
  return coords;
}
async function detectOverflow(state2, options) {
  var _await$platform$isEle;
  if (options === void 0) {
    options = {};
  }
  const {
    x: x3,
    y: y4,
    platform: platform2,
    rects,
    elements,
    strategy
  } = state2;
  const {
    boundary = "clippingAncestors",
    rootBoundary = "viewport",
    elementContext = "floating",
    altBoundary = false,
    padding = 0
  } = evaluate(options, state2);
  const paddingObject = getPaddingObject(padding);
  const altContext = elementContext === "floating" ? "reference" : "floating";
  const element = elements[altBoundary ? altContext : elementContext];
  const clippingClientRect = rectToClientRect(await platform2.getClippingRect({
    element: ((_await$platform$isEle = await (platform2.isElement == null ? void 0 : platform2.isElement(element))) != null ? _await$platform$isEle : true) ? element : element.contextElement || await (platform2.getDocumentElement == null ? void 0 : platform2.getDocumentElement(elements.floating)),
    boundary,
    rootBoundary,
    strategy
  }));
  const rect = elementContext === "floating" ? {
    x: x3,
    y: y4,
    width: rects.floating.width,
    height: rects.floating.height
  } : rects.reference;
  const offsetParent = await (platform2.getOffsetParent == null ? void 0 : platform2.getOffsetParent(elements.floating));
  const offsetScale = await (platform2.isElement == null ? void 0 : platform2.isElement(offsetParent)) ? await (platform2.getScale == null ? void 0 : platform2.getScale(offsetParent)) || {
    x: 1,
    y: 1
  } : {
    x: 1,
    y: 1
  };
  const elementClientRect = rectToClientRect(platform2.convertOffsetParentRelativeRectToViewportRelativeRect ? await platform2.convertOffsetParentRelativeRectToViewportRelativeRect({
    elements,
    rect,
    offsetParent,
    strategy
  }) : rect);
  return {
    top: (clippingClientRect.top - elementClientRect.top + paddingObject.top) / offsetScale.y,
    bottom: (elementClientRect.bottom - clippingClientRect.bottom + paddingObject.bottom) / offsetScale.y,
    left: (clippingClientRect.left - elementClientRect.left + paddingObject.left) / offsetScale.x,
    right: (elementClientRect.right - clippingClientRect.right + paddingObject.right) / offsetScale.x
  };
}
function getBoundingRect(rects) {
  const minX = min(...rects.map((rect) => rect.left));
  const minY = min(...rects.map((rect) => rect.top));
  const maxX = max(...rects.map((rect) => rect.right));
  const maxY = max(...rects.map((rect) => rect.bottom));
  return {
    x: minX,
    y: minY,
    width: maxX - minX,
    height: maxY - minY
  };
}
function getRectsByLine(rects) {
  const sortedRects = rects.slice().sort((a18, b4) => a18.y - b4.y);
  const groups = [];
  let prevRect = null;
  for (let i13 = 0; i13 < sortedRects.length; i13++) {
    const rect = sortedRects[i13];
    if (!prevRect || rect.y - prevRect.y > prevRect.height / 2) {
      groups.push([rect]);
    } else {
      groups[groups.length - 1].push(rect);
    }
    prevRect = rect;
  }
  return groups.map((rect) => rectToClientRect(getBoundingRect(rect)));
}
async function convertValueToCoords(state2, options) {
  const {
    placement,
    platform: platform2,
    elements
  } = state2;
  const rtl = await (platform2.isRTL == null ? void 0 : platform2.isRTL(elements.floating));
  const side = getSide(placement);
  const alignment = getAlignment(placement);
  const isVertical = getSideAxis(placement) === "y";
  const mainAxisMulti = ["left", "top"].includes(side) ? -1 : 1;
  const crossAxisMulti = rtl && isVertical ? -1 : 1;
  const rawValue = evaluate(options, state2);
  let {
    mainAxis,
    crossAxis,
    alignmentAxis
  } = typeof rawValue === "number" ? {
    mainAxis: rawValue,
    crossAxis: 0,
    alignmentAxis: null
  } : {
    mainAxis: rawValue.mainAxis || 0,
    crossAxis: rawValue.crossAxis || 0,
    alignmentAxis: rawValue.alignmentAxis
  };
  if (alignment && typeof alignmentAxis === "number") {
    crossAxis = alignment === "end" ? alignmentAxis * -1 : alignmentAxis;
  }
  return isVertical ? {
    x: crossAxis * crossAxisMulti,
    y: mainAxis * mainAxisMulti
  } : {
    x: mainAxis * mainAxisMulti,
    y: crossAxis * crossAxisMulti
  };
}
var computePosition, arrow, flip, inline, offset, shift, limitShift, size;
var init_floating_ui_core = __esm({
  "node_modules/@floating-ui/core/dist/floating-ui.core.mjs"() {
    init_floating_ui_utils();
    init_floating_ui_utils();
    computePosition = async (reference, floating, config) => {
      const {
        placement = "bottom",
        strategy = "absolute",
        middleware = [],
        platform: platform2
      } = config;
      const validMiddleware = middleware.filter(Boolean);
      const rtl = await (platform2.isRTL == null ? void 0 : platform2.isRTL(floating));
      let rects = await platform2.getElementRects({
        reference,
        floating,
        strategy
      });
      let {
        x: x3,
        y: y4
      } = computeCoordsFromPlacement(rects, placement, rtl);
      let statefulPlacement = placement;
      let middlewareData = {};
      let resetCount = 0;
      for (let i13 = 0; i13 < validMiddleware.length; i13++) {
        const {
          name: name2,
          fn
        } = validMiddleware[i13];
        const {
          x: nextX,
          y: nextY,
          data,
          reset: reset2
        } = await fn({
          x: x3,
          y: y4,
          initialPlacement: placement,
          placement: statefulPlacement,
          strategy,
          middlewareData,
          rects,
          platform: platform2,
          elements: {
            reference,
            floating
          }
        });
        x3 = nextX != null ? nextX : x3;
        y4 = nextY != null ? nextY : y4;
        middlewareData = {
          ...middlewareData,
          [name2]: {
            ...middlewareData[name2],
            ...data
          }
        };
        if (reset2 && resetCount <= 50) {
          resetCount++;
          if (typeof reset2 === "object") {
            if (reset2.placement) {
              statefulPlacement = reset2.placement;
            }
            if (reset2.rects) {
              rects = reset2.rects === true ? await platform2.getElementRects({
                reference,
                floating,
                strategy
              }) : reset2.rects;
            }
            ({
              x: x3,
              y: y4
            } = computeCoordsFromPlacement(rects, statefulPlacement, rtl));
          }
          i13 = -1;
        }
      }
      return {
        x: x3,
        y: y4,
        placement: statefulPlacement,
        strategy,
        middlewareData
      };
    };
    arrow = (options) => ({
      name: "arrow",
      options,
      async fn(state2) {
        const {
          x: x3,
          y: y4,
          placement,
          rects,
          platform: platform2,
          elements,
          middlewareData
        } = state2;
        const {
          element,
          padding = 0
        } = evaluate(options, state2) || {};
        if (element == null) {
          return {};
        }
        const paddingObject = getPaddingObject(padding);
        const coords = {
          x: x3,
          y: y4
        };
        const axis = getAlignmentAxis(placement);
        const length = getAxisLength(axis);
        const arrowDimensions = await platform2.getDimensions(element);
        const isYAxis = axis === "y";
        const minProp = isYAxis ? "top" : "left";
        const maxProp = isYAxis ? "bottom" : "right";
        const clientProp = isYAxis ? "clientHeight" : "clientWidth";
        const endDiff = rects.reference[length] + rects.reference[axis] - coords[axis] - rects.floating[length];
        const startDiff = coords[axis] - rects.reference[axis];
        const arrowOffsetParent = await (platform2.getOffsetParent == null ? void 0 : platform2.getOffsetParent(element));
        let clientSize = arrowOffsetParent ? arrowOffsetParent[clientProp] : 0;
        if (!clientSize || !await (platform2.isElement == null ? void 0 : platform2.isElement(arrowOffsetParent))) {
          clientSize = elements.floating[clientProp] || rects.floating[length];
        }
        const centerToReference = endDiff / 2 - startDiff / 2;
        const largestPossiblePadding = clientSize / 2 - arrowDimensions[length] / 2 - 1;
        const minPadding = min(paddingObject[minProp], largestPossiblePadding);
        const maxPadding = min(paddingObject[maxProp], largestPossiblePadding);
        const min$1 = minPadding;
        const max2 = clientSize - arrowDimensions[length] - maxPadding;
        const center = clientSize / 2 - arrowDimensions[length] / 2 + centerToReference;
        const offset4 = clamp2(min$1, center, max2);
        const shouldAddOffset = !middlewareData.arrow && getAlignment(placement) != null && center !== offset4 && rects.reference[length] / 2 - (center < min$1 ? minPadding : maxPadding) - arrowDimensions[length] / 2 < 0;
        const alignmentOffset = shouldAddOffset ? center < min$1 ? center - min$1 : center - max2 : 0;
        return {
          [axis]: coords[axis] + alignmentOffset,
          data: {
            [axis]: offset4,
            centerOffset: center - offset4 - alignmentOffset,
            ...shouldAddOffset && {
              alignmentOffset
            }
          },
          reset: shouldAddOffset
        };
      }
    });
    flip = function(options) {
      if (options === void 0) {
        options = {};
      }
      return {
        name: "flip",
        options,
        async fn(state2) {
          var _middlewareData$arrow, _middlewareData$flip;
          const {
            placement,
            middlewareData,
            rects,
            initialPlacement,
            platform: platform2,
            elements
          } = state2;
          const {
            mainAxis: checkMainAxis = true,
            crossAxis: checkCrossAxis = true,
            fallbackPlacements: specifiedFallbackPlacements,
            fallbackStrategy = "bestFit",
            fallbackAxisSideDirection = "none",
            flipAlignment = true,
            ...detectOverflowOptions
          } = evaluate(options, state2);
          if ((_middlewareData$arrow = middlewareData.arrow) != null && _middlewareData$arrow.alignmentOffset) {
            return {};
          }
          const side = getSide(placement);
          const initialSideAxis = getSideAxis(initialPlacement);
          const isBasePlacement = getSide(initialPlacement) === initialPlacement;
          const rtl = await (platform2.isRTL == null ? void 0 : platform2.isRTL(elements.floating));
          const fallbackPlacements = specifiedFallbackPlacements || (isBasePlacement || !flipAlignment ? [getOppositePlacement(initialPlacement)] : getExpandedPlacements(initialPlacement));
          const hasFallbackAxisSideDirection = fallbackAxisSideDirection !== "none";
          if (!specifiedFallbackPlacements && hasFallbackAxisSideDirection) {
            fallbackPlacements.push(...getOppositeAxisPlacements(initialPlacement, flipAlignment, fallbackAxisSideDirection, rtl));
          }
          const placements2 = [initialPlacement, ...fallbackPlacements];
          const overflow = await detectOverflow(state2, detectOverflowOptions);
          const overflows = [];
          let overflowsData = ((_middlewareData$flip = middlewareData.flip) == null ? void 0 : _middlewareData$flip.overflows) || [];
          if (checkMainAxis) {
            overflows.push(overflow[side]);
          }
          if (checkCrossAxis) {
            const sides2 = getAlignmentSides(placement, rects, rtl);
            overflows.push(overflow[sides2[0]], overflow[sides2[1]]);
          }
          overflowsData = [...overflowsData, {
            placement,
            overflows
          }];
          if (!overflows.every((side2) => side2 <= 0)) {
            var _middlewareData$flip2, _overflowsData$filter;
            const nextIndex = (((_middlewareData$flip2 = middlewareData.flip) == null ? void 0 : _middlewareData$flip2.index) || 0) + 1;
            const nextPlacement = placements2[nextIndex];
            if (nextPlacement) {
              return {
                data: {
                  index: nextIndex,
                  overflows: overflowsData
                },
                reset: {
                  placement: nextPlacement
                }
              };
            }
            let resetPlacement = (_overflowsData$filter = overflowsData.filter((d12) => d12.overflows[0] <= 0).sort((a18, b4) => a18.overflows[1] - b4.overflows[1])[0]) == null ? void 0 : _overflowsData$filter.placement;
            if (!resetPlacement) {
              switch (fallbackStrategy) {
                case "bestFit": {
                  var _overflowsData$filter2;
                  const placement2 = (_overflowsData$filter2 = overflowsData.filter((d12) => {
                    if (hasFallbackAxisSideDirection) {
                      const currentSideAxis = getSideAxis(d12.placement);
                      return currentSideAxis === initialSideAxis || // Create a bias to the `y` side axis due to horizontal
                      // reading directions favoring greater width.
                      currentSideAxis === "y";
                    }
                    return true;
                  }).map((d12) => [d12.placement, d12.overflows.filter((overflow2) => overflow2 > 0).reduce((acc, overflow2) => acc + overflow2, 0)]).sort((a18, b4) => a18[1] - b4[1])[0]) == null ? void 0 : _overflowsData$filter2[0];
                  if (placement2) {
                    resetPlacement = placement2;
                  }
                  break;
                }
                case "initialPlacement":
                  resetPlacement = initialPlacement;
                  break;
              }
            }
            if (placement !== resetPlacement) {
              return {
                reset: {
                  placement: resetPlacement
                }
              };
            }
          }
          return {};
        }
      };
    };
    inline = function(options) {
      if (options === void 0) {
        options = {};
      }
      return {
        name: "inline",
        options,
        async fn(state2) {
          const {
            placement,
            elements,
            rects,
            platform: platform2,
            strategy
          } = state2;
          const {
            padding = 2,
            x: x3,
            y: y4
          } = evaluate(options, state2);
          const nativeClientRects = Array.from(await (platform2.getClientRects == null ? void 0 : platform2.getClientRects(elements.reference)) || []);
          const clientRects = getRectsByLine(nativeClientRects);
          const fallback = rectToClientRect(getBoundingRect(nativeClientRects));
          const paddingObject = getPaddingObject(padding);
          function getBoundingClientRect2() {
            if (clientRects.length === 2 && clientRects[0].left > clientRects[1].right && x3 != null && y4 != null) {
              return clientRects.find((rect) => x3 > rect.left - paddingObject.left && x3 < rect.right + paddingObject.right && y4 > rect.top - paddingObject.top && y4 < rect.bottom + paddingObject.bottom) || fallback;
            }
            if (clientRects.length >= 2) {
              if (getSideAxis(placement) === "y") {
                const firstRect = clientRects[0];
                const lastRect = clientRects[clientRects.length - 1];
                const isTop = getSide(placement) === "top";
                const top3 = firstRect.top;
                const bottom2 = lastRect.bottom;
                const left2 = isTop ? firstRect.left : lastRect.left;
                const right2 = isTop ? firstRect.right : lastRect.right;
                const width2 = right2 - left2;
                const height2 = bottom2 - top3;
                return {
                  top: top3,
                  bottom: bottom2,
                  left: left2,
                  right: right2,
                  width: width2,
                  height: height2,
                  x: left2,
                  y: top3
                };
              }
              const isLeftSide = getSide(placement) === "left";
              const maxRight = max(...clientRects.map((rect) => rect.right));
              const minLeft = min(...clientRects.map((rect) => rect.left));
              const measureRects = clientRects.filter((rect) => isLeftSide ? rect.left === minLeft : rect.right === maxRight);
              const top2 = measureRects[0].top;
              const bottom = measureRects[measureRects.length - 1].bottom;
              const left = minLeft;
              const right = maxRight;
              const width = right - left;
              const height = bottom - top2;
              return {
                top: top2,
                bottom,
                left,
                right,
                width,
                height,
                x: left,
                y: top2
              };
            }
            return fallback;
          }
          const resetRects = await platform2.getElementRects({
            reference: {
              getBoundingClientRect: getBoundingClientRect2
            },
            floating: elements.floating,
            strategy
          });
          if (rects.reference.x !== resetRects.reference.x || rects.reference.y !== resetRects.reference.y || rects.reference.width !== resetRects.reference.width || rects.reference.height !== resetRects.reference.height) {
            return {
              reset: {
                rects: resetRects
              }
            };
          }
          return {};
        }
      };
    };
    offset = function(options) {
      if (options === void 0) {
        options = 0;
      }
      return {
        name: "offset",
        options,
        async fn(state2) {
          var _middlewareData$offse, _middlewareData$arrow;
          const {
            x: x3,
            y: y4,
            placement,
            middlewareData
          } = state2;
          const diffCoords = await convertValueToCoords(state2, options);
          if (placement === ((_middlewareData$offse = middlewareData.offset) == null ? void 0 : _middlewareData$offse.placement) && (_middlewareData$arrow = middlewareData.arrow) != null && _middlewareData$arrow.alignmentOffset) {
            return {};
          }
          return {
            x: x3 + diffCoords.x,
            y: y4 + diffCoords.y,
            data: {
              ...diffCoords,
              placement
            }
          };
        }
      };
    };
    shift = function(options) {
      if (options === void 0) {
        options = {};
      }
      return {
        name: "shift",
        options,
        async fn(state2) {
          const {
            x: x3,
            y: y4,
            placement
          } = state2;
          const {
            mainAxis: checkMainAxis = true,
            crossAxis: checkCrossAxis = false,
            limiter = {
              fn: (_ref) => {
                let {
                  x: x4,
                  y: y5
                } = _ref;
                return {
                  x: x4,
                  y: y5
                };
              }
            },
            ...detectOverflowOptions
          } = evaluate(options, state2);
          const coords = {
            x: x3,
            y: y4
          };
          const overflow = await detectOverflow(state2, detectOverflowOptions);
          const crossAxis = getSideAxis(getSide(placement));
          const mainAxis = getOppositeAxis(crossAxis);
          let mainAxisCoord = coords[mainAxis];
          let crossAxisCoord = coords[crossAxis];
          if (checkMainAxis) {
            const minSide = mainAxis === "y" ? "top" : "left";
            const maxSide = mainAxis === "y" ? "bottom" : "right";
            const min2 = mainAxisCoord + overflow[minSide];
            const max2 = mainAxisCoord - overflow[maxSide];
            mainAxisCoord = clamp2(min2, mainAxisCoord, max2);
          }
          if (checkCrossAxis) {
            const minSide = crossAxis === "y" ? "top" : "left";
            const maxSide = crossAxis === "y" ? "bottom" : "right";
            const min2 = crossAxisCoord + overflow[minSide];
            const max2 = crossAxisCoord - overflow[maxSide];
            crossAxisCoord = clamp2(min2, crossAxisCoord, max2);
          }
          const limitedCoords = limiter.fn({
            ...state2,
            [mainAxis]: mainAxisCoord,
            [crossAxis]: crossAxisCoord
          });
          return {
            ...limitedCoords,
            data: {
              x: limitedCoords.x - x3,
              y: limitedCoords.y - y4,
              enabled: {
                [mainAxis]: checkMainAxis,
                [crossAxis]: checkCrossAxis
              }
            }
          };
        }
      };
    };
    limitShift = function(options) {
      if (options === void 0) {
        options = {};
      }
      return {
        options,
        fn(state2) {
          const {
            x: x3,
            y: y4,
            placement,
            rects,
            middlewareData
          } = state2;
          const {
            offset: offset4 = 0,
            mainAxis: checkMainAxis = true,
            crossAxis: checkCrossAxis = true
          } = evaluate(options, state2);
          const coords = {
            x: x3,
            y: y4
          };
          const crossAxis = getSideAxis(placement);
          const mainAxis = getOppositeAxis(crossAxis);
          let mainAxisCoord = coords[mainAxis];
          let crossAxisCoord = coords[crossAxis];
          const rawOffset = evaluate(offset4, state2);
          const computedOffset = typeof rawOffset === "number" ? {
            mainAxis: rawOffset,
            crossAxis: 0
          } : {
            mainAxis: 0,
            crossAxis: 0,
            ...rawOffset
          };
          if (checkMainAxis) {
            const len = mainAxis === "y" ? "height" : "width";
            const limitMin = rects.reference[mainAxis] - rects.floating[len] + computedOffset.mainAxis;
            const limitMax = rects.reference[mainAxis] + rects.reference[len] - computedOffset.mainAxis;
            if (mainAxisCoord < limitMin) {
              mainAxisCoord = limitMin;
            } else if (mainAxisCoord > limitMax) {
              mainAxisCoord = limitMax;
            }
          }
          if (checkCrossAxis) {
            var _middlewareData$offse, _middlewareData$offse2;
            const len = mainAxis === "y" ? "width" : "height";
            const isOriginSide = ["top", "left"].includes(getSide(placement));
            const limitMin = rects.reference[crossAxis] - rects.floating[len] + (isOriginSide ? ((_middlewareData$offse = middlewareData.offset) == null ? void 0 : _middlewareData$offse[crossAxis]) || 0 : 0) + (isOriginSide ? 0 : computedOffset.crossAxis);
            const limitMax = rects.reference[crossAxis] + rects.reference[len] + (isOriginSide ? 0 : ((_middlewareData$offse2 = middlewareData.offset) == null ? void 0 : _middlewareData$offse2[crossAxis]) || 0) - (isOriginSide ? computedOffset.crossAxis : 0);
            if (crossAxisCoord < limitMin) {
              crossAxisCoord = limitMin;
            } else if (crossAxisCoord > limitMax) {
              crossAxisCoord = limitMax;
            }
          }
          return {
            [mainAxis]: mainAxisCoord,
            [crossAxis]: crossAxisCoord
          };
        }
      };
    };
    size = function(options) {
      if (options === void 0) {
        options = {};
      }
      return {
        name: "size",
        options,
        async fn(state2) {
          var _state$middlewareData, _state$middlewareData2;
          const {
            placement,
            rects,
            platform: platform2,
            elements
          } = state2;
          const {
            apply = () => {
            },
            ...detectOverflowOptions
          } = evaluate(options, state2);
          const overflow = await detectOverflow(state2, detectOverflowOptions);
          const side = getSide(placement);
          const alignment = getAlignment(placement);
          const isYAxis = getSideAxis(placement) === "y";
          const {
            width,
            height
          } = rects.floating;
          let heightSide;
          let widthSide;
          if (side === "top" || side === "bottom") {
            heightSide = side;
            widthSide = alignment === (await (platform2.isRTL == null ? void 0 : platform2.isRTL(elements.floating)) ? "start" : "end") ? "left" : "right";
          } else {
            widthSide = side;
            heightSide = alignment === "end" ? "top" : "bottom";
          }
          const maximumClippingHeight = height - overflow.top - overflow.bottom;
          const maximumClippingWidth = width - overflow.left - overflow.right;
          const overflowAvailableHeight = min(height - overflow[heightSide], maximumClippingHeight);
          const overflowAvailableWidth = min(width - overflow[widthSide], maximumClippingWidth);
          const noShift = !state2.middlewareData.shift;
          let availableHeight = overflowAvailableHeight;
          let availableWidth = overflowAvailableWidth;
          if ((_state$middlewareData = state2.middlewareData.shift) != null && _state$middlewareData.enabled.x) {
            availableWidth = maximumClippingWidth;
          }
          if ((_state$middlewareData2 = state2.middlewareData.shift) != null && _state$middlewareData2.enabled.y) {
            availableHeight = maximumClippingHeight;
          }
          if (noShift && !alignment) {
            const xMin = max(overflow.left, 0);
            const xMax = max(overflow.right, 0);
            const yMin = max(overflow.top, 0);
            const yMax = max(overflow.bottom, 0);
            if (isYAxis) {
              availableWidth = width - 2 * (xMin !== 0 || xMax !== 0 ? xMin + xMax : max(overflow.left, overflow.right));
            } else {
              availableHeight = height - 2 * (yMin !== 0 || yMax !== 0 ? yMin + yMax : max(overflow.top, overflow.bottom));
            }
          }
          await apply({
            ...state2,
            availableWidth,
            availableHeight
          });
          const nextDimensions = await platform2.getDimensions(elements.floating);
          if (width !== nextDimensions.width || height !== nextDimensions.height) {
            return {
              reset: {
                rects: true
              }
            };
          }
          return {};
        }
      };
    };
  }
});

// node_modules/@floating-ui/dom/dist/floating-ui.dom.mjs
function getCssDimensions(element) {
  const css = getComputedStyle2(element);
  let width = parseFloat(css.width) || 0;
  let height = parseFloat(css.height) || 0;
  const hasOffset = isHTMLElement(element);
  const offsetWidth = hasOffset ? element.offsetWidth : width;
  const offsetHeight = hasOffset ? element.offsetHeight : height;
  const shouldFallback = round2(width) !== offsetWidth || round2(height) !== offsetHeight;
  if (shouldFallback) {
    width = offsetWidth;
    height = offsetHeight;
  }
  return {
    width,
    height,
    $: shouldFallback
  };
}
function unwrapElement(element) {
  return !isElement2(element) ? element.contextElement : element;
}
function getScale(element) {
  const domElement = unwrapElement(element);
  if (!isHTMLElement(domElement)) {
    return createCoords(1);
  }
  const rect = domElement.getBoundingClientRect();
  const {
    width,
    height,
    $: $5
  } = getCssDimensions(domElement);
  let x3 = ($5 ? round2(rect.width) : rect.width) / width;
  let y4 = ($5 ? round2(rect.height) : rect.height) / height;
  if (!x3 || !Number.isFinite(x3)) {
    x3 = 1;
  }
  if (!y4 || !Number.isFinite(y4)) {
    y4 = 1;
  }
  return {
    x: x3,
    y: y4
  };
}
function getVisualOffsets(element) {
  const win = getWindow(element);
  if (!isWebKit() || !win.visualViewport) {
    return noOffsets;
  }
  return {
    x: win.visualViewport.offsetLeft,
    y: win.visualViewport.offsetTop
  };
}
function shouldAddVisualOffsets(element, isFixed, floatingOffsetParent) {
  if (isFixed === void 0) {
    isFixed = false;
  }
  if (!floatingOffsetParent || isFixed && floatingOffsetParent !== getWindow(element)) {
    return false;
  }
  return isFixed;
}
function getBoundingClientRect(element, includeScale, isFixedStrategy, offsetParent) {
  if (includeScale === void 0) {
    includeScale = false;
  }
  if (isFixedStrategy === void 0) {
    isFixedStrategy = false;
  }
  const clientRect = element.getBoundingClientRect();
  const domElement = unwrapElement(element);
  let scale2 = createCoords(1);
  if (includeScale) {
    if (offsetParent) {
      if (isElement2(offsetParent)) {
        scale2 = getScale(offsetParent);
      }
    } else {
      scale2 = getScale(element);
    }
  }
  const visualOffsets = shouldAddVisualOffsets(domElement, isFixedStrategy, offsetParent) ? getVisualOffsets(domElement) : createCoords(0);
  let x3 = (clientRect.left + visualOffsets.x) / scale2.x;
  let y4 = (clientRect.top + visualOffsets.y) / scale2.y;
  let width = clientRect.width / scale2.x;
  let height = clientRect.height / scale2.y;
  if (domElement) {
    const win = getWindow(domElement);
    const offsetWin = offsetParent && isElement2(offsetParent) ? getWindow(offsetParent) : offsetParent;
    let currentWin = win;
    let currentIFrame = getFrameElement(currentWin);
    while (currentIFrame && offsetParent && offsetWin !== currentWin) {
      const iframeScale = getScale(currentIFrame);
      const iframeRect = currentIFrame.getBoundingClientRect();
      const css = getComputedStyle2(currentIFrame);
      const left = iframeRect.left + (currentIFrame.clientLeft + parseFloat(css.paddingLeft)) * iframeScale.x;
      const top2 = iframeRect.top + (currentIFrame.clientTop + parseFloat(css.paddingTop)) * iframeScale.y;
      x3 *= iframeScale.x;
      y4 *= iframeScale.y;
      width *= iframeScale.x;
      height *= iframeScale.y;
      x3 += left;
      y4 += top2;
      currentWin = getWindow(currentIFrame);
      currentIFrame = getFrameElement(currentWin);
    }
  }
  return rectToClientRect({
    width,
    height,
    x: x3,
    y: y4
  });
}
function getWindowScrollBarX(element, rect) {
  const leftScroll = getNodeScroll(element).scrollLeft;
  if (!rect) {
    return getBoundingClientRect(getDocumentElement(element)).left + leftScroll;
  }
  return rect.left + leftScroll;
}
function getHTMLOffset(documentElement, scroll, ignoreScrollbarX) {
  if (ignoreScrollbarX === void 0) {
    ignoreScrollbarX = false;
  }
  const htmlRect = documentElement.getBoundingClientRect();
  const x3 = htmlRect.left + scroll.scrollLeft - (ignoreScrollbarX ? 0 : (
    // RTL <body> scrollbar.
    getWindowScrollBarX(documentElement, htmlRect)
  ));
  const y4 = htmlRect.top + scroll.scrollTop;
  return {
    x: x3,
    y: y4
  };
}
function convertOffsetParentRelativeRectToViewportRelativeRect(_ref) {
  let {
    elements,
    rect,
    offsetParent,
    strategy
  } = _ref;
  const isFixed = strategy === "fixed";
  const documentElement = getDocumentElement(offsetParent);
  const topLayer = elements ? isTopLayer(elements.floating) : false;
  if (offsetParent === documentElement || topLayer && isFixed) {
    return rect;
  }
  let scroll = {
    scrollLeft: 0,
    scrollTop: 0
  };
  let scale2 = createCoords(1);
  const offsets = createCoords(0);
  const isOffsetParentAnElement = isHTMLElement(offsetParent);
  if (isOffsetParentAnElement || !isOffsetParentAnElement && !isFixed) {
    if (getNodeName(offsetParent) !== "body" || isOverflowElement(documentElement)) {
      scroll = getNodeScroll(offsetParent);
    }
    if (isHTMLElement(offsetParent)) {
      const offsetRect = getBoundingClientRect(offsetParent);
      scale2 = getScale(offsetParent);
      offsets.x = offsetRect.x + offsetParent.clientLeft;
      offsets.y = offsetRect.y + offsetParent.clientTop;
    }
  }
  const htmlOffset = documentElement && !isOffsetParentAnElement && !isFixed ? getHTMLOffset(documentElement, scroll, true) : createCoords(0);
  return {
    width: rect.width * scale2.x,
    height: rect.height * scale2.y,
    x: rect.x * scale2.x - scroll.scrollLeft * scale2.x + offsets.x + htmlOffset.x,
    y: rect.y * scale2.y - scroll.scrollTop * scale2.y + offsets.y + htmlOffset.y
  };
}
function getClientRects(element) {
  return Array.from(element.getClientRects());
}
function getDocumentRect(element) {
  const html = getDocumentElement(element);
  const scroll = getNodeScroll(element);
  const body = element.ownerDocument.body;
  const width = max(html.scrollWidth, html.clientWidth, body.scrollWidth, body.clientWidth);
  const height = max(html.scrollHeight, html.clientHeight, body.scrollHeight, body.clientHeight);
  let x3 = -scroll.scrollLeft + getWindowScrollBarX(element);
  const y4 = -scroll.scrollTop;
  if (getComputedStyle2(body).direction === "rtl") {
    x3 += max(html.clientWidth, body.clientWidth) - width;
  }
  return {
    width,
    height,
    x: x3,
    y: y4
  };
}
function getViewportRect(element, strategy) {
  const win = getWindow(element);
  const html = getDocumentElement(element);
  const visualViewport = win.visualViewport;
  let width = html.clientWidth;
  let height = html.clientHeight;
  let x3 = 0;
  let y4 = 0;
  if (visualViewport) {
    width = visualViewport.width;
    height = visualViewport.height;
    const visualViewportBased = isWebKit();
    if (!visualViewportBased || visualViewportBased && strategy === "fixed") {
      x3 = visualViewport.offsetLeft;
      y4 = visualViewport.offsetTop;
    }
  }
  return {
    width,
    height,
    x: x3,
    y: y4
  };
}
function getInnerBoundingClientRect(element, strategy) {
  const clientRect = getBoundingClientRect(element, true, strategy === "fixed");
  const top2 = clientRect.top + element.clientTop;
  const left = clientRect.left + element.clientLeft;
  const scale2 = isHTMLElement(element) ? getScale(element) : createCoords(1);
  const width = element.clientWidth * scale2.x;
  const height = element.clientHeight * scale2.y;
  const x3 = left * scale2.x;
  const y4 = top2 * scale2.y;
  return {
    width,
    height,
    x: x3,
    y: y4
  };
}
function getClientRectFromClippingAncestor(element, clippingAncestor, strategy) {
  let rect;
  if (clippingAncestor === "viewport") {
    rect = getViewportRect(element, strategy);
  } else if (clippingAncestor === "document") {
    rect = getDocumentRect(getDocumentElement(element));
  } else if (isElement2(clippingAncestor)) {
    rect = getInnerBoundingClientRect(clippingAncestor, strategy);
  } else {
    const visualOffsets = getVisualOffsets(element);
    rect = {
      x: clippingAncestor.x - visualOffsets.x,
      y: clippingAncestor.y - visualOffsets.y,
      width: clippingAncestor.width,
      height: clippingAncestor.height
    };
  }
  return rectToClientRect(rect);
}
function hasFixedPositionAncestor(element, stopNode) {
  const parentNode = getParentNode(element);
  if (parentNode === stopNode || !isElement2(parentNode) || isLastTraversableNode(parentNode)) {
    return false;
  }
  return getComputedStyle2(parentNode).position === "fixed" || hasFixedPositionAncestor(parentNode, stopNode);
}
function getClippingElementAncestors(element, cache2) {
  const cachedResult = cache2.get(element);
  if (cachedResult) {
    return cachedResult;
  }
  let result = getOverflowAncestors(element, [], false).filter((el2) => isElement2(el2) && getNodeName(el2) !== "body");
  let currentContainingBlockComputedStyle = null;
  const elementIsFixed = getComputedStyle2(element).position === "fixed";
  let currentNode = elementIsFixed ? getParentNode(element) : element;
  while (isElement2(currentNode) && !isLastTraversableNode(currentNode)) {
    const computedStyle = getComputedStyle2(currentNode);
    const currentNodeIsContaining = isContainingBlock(currentNode);
    if (!currentNodeIsContaining && computedStyle.position === "fixed") {
      currentContainingBlockComputedStyle = null;
    }
    const shouldDropCurrentNode = elementIsFixed ? !currentNodeIsContaining && !currentContainingBlockComputedStyle : !currentNodeIsContaining && computedStyle.position === "static" && !!currentContainingBlockComputedStyle && ["absolute", "fixed"].includes(currentContainingBlockComputedStyle.position) || isOverflowElement(currentNode) && !currentNodeIsContaining && hasFixedPositionAncestor(element, currentNode);
    if (shouldDropCurrentNode) {
      result = result.filter((ancestor) => ancestor !== currentNode);
    } else {
      currentContainingBlockComputedStyle = computedStyle;
    }
    currentNode = getParentNode(currentNode);
  }
  cache2.set(element, result);
  return result;
}
function getClippingRect(_ref) {
  let {
    element,
    boundary,
    rootBoundary,
    strategy
  } = _ref;
  const elementClippingAncestors = boundary === "clippingAncestors" ? isTopLayer(element) ? [] : getClippingElementAncestors(element, this._c) : [].concat(boundary);
  const clippingAncestors = [...elementClippingAncestors, rootBoundary];
  const firstClippingAncestor = clippingAncestors[0];
  const clippingRect = clippingAncestors.reduce((accRect, clippingAncestor) => {
    const rect = getClientRectFromClippingAncestor(element, clippingAncestor, strategy);
    accRect.top = max(rect.top, accRect.top);
    accRect.right = min(rect.right, accRect.right);
    accRect.bottom = min(rect.bottom, accRect.bottom);
    accRect.left = max(rect.left, accRect.left);
    return accRect;
  }, getClientRectFromClippingAncestor(element, firstClippingAncestor, strategy));
  return {
    width: clippingRect.right - clippingRect.left,
    height: clippingRect.bottom - clippingRect.top,
    x: clippingRect.left,
    y: clippingRect.top
  };
}
function getDimensions(element) {
  const {
    width,
    height
  } = getCssDimensions(element);
  return {
    width,
    height
  };
}
function getRectRelativeToOffsetParent(element, offsetParent, strategy) {
  const isOffsetParentAnElement = isHTMLElement(offsetParent);
  const documentElement = getDocumentElement(offsetParent);
  const isFixed = strategy === "fixed";
  const rect = getBoundingClientRect(element, true, isFixed, offsetParent);
  let scroll = {
    scrollLeft: 0,
    scrollTop: 0
  };
  const offsets = createCoords(0);
  if (isOffsetParentAnElement || !isOffsetParentAnElement && !isFixed) {
    if (getNodeName(offsetParent) !== "body" || isOverflowElement(documentElement)) {
      scroll = getNodeScroll(offsetParent);
    }
    if (isOffsetParentAnElement) {
      const offsetRect = getBoundingClientRect(offsetParent, true, isFixed, offsetParent);
      offsets.x = offsetRect.x + offsetParent.clientLeft;
      offsets.y = offsetRect.y + offsetParent.clientTop;
    } else if (documentElement) {
      offsets.x = getWindowScrollBarX(documentElement);
    }
  }
  const htmlOffset = documentElement && !isOffsetParentAnElement && !isFixed ? getHTMLOffset(documentElement, scroll) : createCoords(0);
  const x3 = rect.left + scroll.scrollLeft - offsets.x - htmlOffset.x;
  const y4 = rect.top + scroll.scrollTop - offsets.y - htmlOffset.y;
  return {
    x: x3,
    y: y4,
    width: rect.width,
    height: rect.height
  };
}
function isStaticPositioned(element) {
  return getComputedStyle2(element).position === "static";
}
function getTrueOffsetParent(element, polyfill) {
  if (!isHTMLElement(element) || getComputedStyle2(element).position === "fixed") {
    return null;
  }
  if (polyfill) {
    return polyfill(element);
  }
  let rawOffsetParent = element.offsetParent;
  if (getDocumentElement(element) === rawOffsetParent) {
    rawOffsetParent = rawOffsetParent.ownerDocument.body;
  }
  return rawOffsetParent;
}
function getOffsetParent(element, polyfill) {
  const win = getWindow(element);
  if (isTopLayer(element)) {
    return win;
  }
  if (!isHTMLElement(element)) {
    let svgOffsetParent = getParentNode(element);
    while (svgOffsetParent && !isLastTraversableNode(svgOffsetParent)) {
      if (isElement2(svgOffsetParent) && !isStaticPositioned(svgOffsetParent)) {
        return svgOffsetParent;
      }
      svgOffsetParent = getParentNode(svgOffsetParent);
    }
    return win;
  }
  let offsetParent = getTrueOffsetParent(element, polyfill);
  while (offsetParent && isTableElement(offsetParent) && isStaticPositioned(offsetParent)) {
    offsetParent = getTrueOffsetParent(offsetParent, polyfill);
  }
  if (offsetParent && isLastTraversableNode(offsetParent) && isStaticPositioned(offsetParent) && !isContainingBlock(offsetParent)) {
    return win;
  }
  return offsetParent || getContainingBlock(element) || win;
}
function isRTL(element) {
  return getComputedStyle2(element).direction === "rtl";
}
function observeMove(element, onMove) {
  let io = null;
  let timeoutId;
  const root5 = getDocumentElement(element);
  function cleanup() {
    var _io;
    clearTimeout(timeoutId);
    (_io = io) == null || _io.disconnect();
    io = null;
  }
  function refresh(skip, threshold) {
    if (skip === void 0) {
      skip = false;
    }
    if (threshold === void 0) {
      threshold = 1;
    }
    cleanup();
    const {
      left,
      top: top2,
      width,
      height
    } = element.getBoundingClientRect();
    if (!skip) {
      onMove();
    }
    if (!width || !height) {
      return;
    }
    const insetTop = floor(top2);
    const insetRight = floor(root5.clientWidth - (left + width));
    const insetBottom = floor(root5.clientHeight - (top2 + height));
    const insetLeft = floor(left);
    const rootMargin = -insetTop + "px " + -insetRight + "px " + -insetBottom + "px " + -insetLeft + "px";
    const options = {
      rootMargin,
      threshold: max(0, min(1, threshold)) || 1
    };
    let isFirstUpdate = true;
    function handleObserve(entries) {
      const ratio = entries[0].intersectionRatio;
      if (ratio !== threshold) {
        if (!isFirstUpdate) {
          return refresh();
        }
        if (!ratio) {
          timeoutId = setTimeout(() => {
            refresh(false, 1e-7);
          }, 1e3);
        } else {
          refresh(false, ratio);
        }
      }
      isFirstUpdate = false;
    }
    try {
      io = new IntersectionObserver(handleObserve, {
        ...options,
        // Handle <iframe>s
        root: root5.ownerDocument
      });
    } catch (e14) {
      io = new IntersectionObserver(handleObserve, options);
    }
    io.observe(element);
  }
  refresh(true);
  return cleanup;
}
function autoUpdate(reference, floating, update, options) {
  if (options === void 0) {
    options = {};
  }
  const {
    ancestorScroll = true,
    ancestorResize = true,
    elementResize = typeof ResizeObserver === "function",
    layoutShift = typeof IntersectionObserver === "function",
    animationFrame = false
  } = options;
  const referenceEl = unwrapElement(reference);
  const ancestors = ancestorScroll || ancestorResize ? [...referenceEl ? getOverflowAncestors(referenceEl) : [], ...getOverflowAncestors(floating)] : [];
  ancestors.forEach((ancestor) => {
    ancestorScroll && ancestor.addEventListener("scroll", update, {
      passive: true
    });
    ancestorResize && ancestor.addEventListener("resize", update);
  });
  const cleanupIo = referenceEl && layoutShift ? observeMove(referenceEl, update) : null;
  let reobserveFrame = -1;
  let resizeObserver = null;
  if (elementResize) {
    resizeObserver = new ResizeObserver((_ref) => {
      let [firstEntry] = _ref;
      if (firstEntry && firstEntry.target === referenceEl && resizeObserver) {
        resizeObserver.unobserve(floating);
        cancelAnimationFrame(reobserveFrame);
        reobserveFrame = requestAnimationFrame(() => {
          var _resizeObserver;
          (_resizeObserver = resizeObserver) == null || _resizeObserver.observe(floating);
        });
      }
      update();
    });
    if (referenceEl && !animationFrame) {
      resizeObserver.observe(referenceEl);
    }
    resizeObserver.observe(floating);
  }
  let frameId;
  let prevRefRect = animationFrame ? getBoundingClientRect(reference) : null;
  if (animationFrame) {
    frameLoop();
  }
  function frameLoop() {
    const nextRefRect = getBoundingClientRect(reference);
    if (prevRefRect && (nextRefRect.x !== prevRefRect.x || nextRefRect.y !== prevRefRect.y || nextRefRect.width !== prevRefRect.width || nextRefRect.height !== prevRefRect.height)) {
      update();
    }
    prevRefRect = nextRefRect;
    frameId = requestAnimationFrame(frameLoop);
  }
  update();
  return () => {
    var _resizeObserver2;
    ancestors.forEach((ancestor) => {
      ancestorScroll && ancestor.removeEventListener("scroll", update);
      ancestorResize && ancestor.removeEventListener("resize", update);
    });
    cleanupIo == null || cleanupIo();
    (_resizeObserver2 = resizeObserver) == null || _resizeObserver2.disconnect();
    resizeObserver = null;
    if (animationFrame) {
      cancelAnimationFrame(frameId);
    }
  };
}
var noOffsets, getElementRects, platform, offset2, shift2, flip2, size2, arrow2, inline2, limitShift2, computePosition2;
var init_floating_ui_dom = __esm({
  "node_modules/@floating-ui/dom/dist/floating-ui.dom.mjs"() {
    init_floating_ui_core();
    init_floating_ui_utils();
    init_floating_ui_utils_dom();
    noOffsets = /* @__PURE__ */ createCoords(0);
    getElementRects = async function(data) {
      const getOffsetParentFn = this.getOffsetParent || getOffsetParent;
      const getDimensionsFn = this.getDimensions;
      const floatingDimensions = await getDimensionsFn(data.floating);
      return {
        reference: getRectRelativeToOffsetParent(data.reference, await getOffsetParentFn(data.floating), data.strategy),
        floating: {
          x: 0,
          y: 0,
          width: floatingDimensions.width,
          height: floatingDimensions.height
        }
      };
    };
    platform = {
      convertOffsetParentRelativeRectToViewportRelativeRect,
      getDocumentElement,
      getClippingRect,
      getOffsetParent,
      getElementRects,
      getClientRects,
      getDimensions,
      getScale,
      isElement: isElement2,
      isRTL
    };
    offset2 = offset;
    shift2 = shift;
    flip2 = flip;
    size2 = size;
    arrow2 = arrow;
    inline2 = inline;
    limitShift2 = limitShift;
    computePosition2 = (reference, floating, options) => {
      const cache2 = /* @__PURE__ */ new Map();
      const mergedOptions = {
        platform,
        ...options
      };
      const platformWithCache = {
        ...mergedOptions.platform,
        _c: cache2
      };
      return computePosition(reference, floating, {
        ...mergedOptions,
        platform: platformWithCache
      });
    };
  }
});

// node_modules/@floating-ui/react-dom/dist/floating-ui.react-dom.mjs
function deepEqual(a18, b4) {
  if (a18 === b4) {
    return true;
  }
  if (typeof a18 !== typeof b4) {
    return false;
  }
  if (typeof a18 === "function" && a18.toString() === b4.toString()) {
    return true;
  }
  let length;
  let i13;
  let keys2;
  if (a18 && b4 && typeof a18 === "object") {
    if (Array.isArray(a18)) {
      length = a18.length;
      if (length !== b4.length) return false;
      for (i13 = length; i13-- !== 0; ) {
        if (!deepEqual(a18[i13], b4[i13])) {
          return false;
        }
      }
      return true;
    }
    keys2 = Object.keys(a18);
    length = keys2.length;
    if (length !== Object.keys(b4).length) {
      return false;
    }
    for (i13 = length; i13-- !== 0; ) {
      if (!{}.hasOwnProperty.call(b4, keys2[i13])) {
        return false;
      }
    }
    for (i13 = length; i13-- !== 0; ) {
      const key = keys2[i13];
      if (key === "_owner" && a18.$$typeof) {
        continue;
      }
      if (!deepEqual(a18[key], b4[key])) {
        return false;
      }
    }
    return true;
  }
  return a18 !== a18 && b4 !== b4;
}
function getDPR(element) {
  if (typeof window === "undefined") {
    return 1;
  }
  const win = element.ownerDocument.defaultView || window;
  return win.devicePixelRatio || 1;
}
function roundByDPR(element, value) {
  const dpr = getDPR(element);
  return Math.round(value * dpr) / dpr;
}
function useLatestRef(value) {
  const ref = React7.useRef(value);
  index(() => {
    ref.current = value;
  });
  return ref;
}
function useFloating(options) {
  if (options === void 0) {
    options = {};
  }
  const {
    placement = "bottom",
    strategy = "absolute",
    middleware = [],
    platform: platform2,
    elements: {
      reference: externalReference,
      floating: externalFloating
    } = {},
    transform = true,
    whileElementsMounted,
    open
  } = options;
  const [data, setData] = React7.useState({
    x: 0,
    y: 0,
    strategy,
    placement,
    middlewareData: {},
    isPositioned: false
  });
  const [latestMiddleware, setLatestMiddleware] = React7.useState(middleware);
  if (!deepEqual(latestMiddleware, middleware)) {
    setLatestMiddleware(middleware);
  }
  const [_reference, _setReference] = React7.useState(null);
  const [_floating, _setFloating] = React7.useState(null);
  const setReference = React7.useCallback((node) => {
    if (node !== referenceRef.current) {
      referenceRef.current = node;
      _setReference(node);
    }
  }, []);
  const setFloating = React7.useCallback((node) => {
    if (node !== floatingRef.current) {
      floatingRef.current = node;
      _setFloating(node);
    }
  }, []);
  const referenceEl = externalReference || _reference;
  const floatingEl = externalFloating || _floating;
  const referenceRef = React7.useRef(null);
  const floatingRef = React7.useRef(null);
  const dataRef = React7.useRef(data);
  const hasWhileElementsMounted = whileElementsMounted != null;
  const whileElementsMountedRef = useLatestRef(whileElementsMounted);
  const platformRef = useLatestRef(platform2);
  const openRef = useLatestRef(open);
  const update = React7.useCallback(() => {
    if (!referenceRef.current || !floatingRef.current) {
      return;
    }
    const config = {
      placement,
      strategy,
      middleware: latestMiddleware
    };
    if (platformRef.current) {
      config.platform = platformRef.current;
    }
    computePosition2(referenceRef.current, floatingRef.current, config).then((data2) => {
      const fullData = {
        ...data2,
        // The floating element's position may be recomputed while it's closed
        // but still mounted (such as when transitioning out). To ensure
        // `isPositioned` will be `false` initially on the next open, avoid
        // setting it to `true` when `open === false` (must be specified).
        isPositioned: openRef.current !== false
      };
      if (isMountedRef.current && !deepEqual(dataRef.current, fullData)) {
        dataRef.current = fullData;
        ReactDOM.flushSync(() => {
          setData(fullData);
        });
      }
    });
  }, [latestMiddleware, placement, strategy, platformRef, openRef]);
  index(() => {
    if (open === false && dataRef.current.isPositioned) {
      dataRef.current.isPositioned = false;
      setData((data2) => ({
        ...data2,
        isPositioned: false
      }));
    }
  }, [open]);
  const isMountedRef = React7.useRef(false);
  index(() => {
    isMountedRef.current = true;
    return () => {
      isMountedRef.current = false;
    };
  }, []);
  index(() => {
    if (referenceEl) referenceRef.current = referenceEl;
    if (floatingEl) floatingRef.current = floatingEl;
    if (referenceEl && floatingEl) {
      if (whileElementsMountedRef.current) {
        return whileElementsMountedRef.current(referenceEl, floatingEl, update);
      }
      update();
    }
  }, [referenceEl, floatingEl, update, whileElementsMountedRef, hasWhileElementsMounted]);
  const refs = React7.useMemo(() => ({
    reference: referenceRef,
    floating: floatingRef,
    setReference,
    setFloating
  }), [setReference, setFloating]);
  const elements = React7.useMemo(() => ({
    reference: referenceEl,
    floating: floatingEl
  }), [referenceEl, floatingEl]);
  const floatingStyles = React7.useMemo(() => {
    const initialStyles = {
      position: strategy,
      left: 0,
      top: 0
    };
    if (!elements.floating) {
      return initialStyles;
    }
    const x3 = roundByDPR(elements.floating, data.x);
    const y4 = roundByDPR(elements.floating, data.y);
    if (transform) {
      return {
        ...initialStyles,
        transform: "translate(" + x3 + "px, " + y4 + "px)",
        ...getDPR(elements.floating) >= 1.5 && {
          willChange: "transform"
        }
      };
    }
    return {
      position: strategy,
      left: x3,
      top: y4
    };
  }, [strategy, transform, elements.floating, data.x, data.y]);
  return React7.useMemo(() => ({
    ...data,
    update,
    refs,
    elements,
    floatingStyles
  }), [data, update, refs, elements, floatingStyles]);
}
var React7, import_react62, ReactDOM, index, arrow$1, offset3, shift3, limitShift3, flip3, size3, inline3, arrow3;
var init_floating_ui_react_dom = __esm({
  "node_modules/@floating-ui/react-dom/dist/floating-ui.react-dom.mjs"() {
    init_floating_ui_dom();
    init_floating_ui_dom();
    React7 = __toESM(require_react(), 1);
    import_react62 = __toESM(require_react(), 1);
    ReactDOM = __toESM(require_react_dom(), 1);
    index = typeof document !== "undefined" ? import_react62.useLayoutEffect : import_react62.useEffect;
    arrow$1 = (options) => {
      function isRef(value) {
        return {}.hasOwnProperty.call(value, "current");
      }
      return {
        name: "arrow",
        options,
        fn(state2) {
          const {
            element,
            padding
          } = typeof options === "function" ? options(state2) : options;
          if (element && isRef(element)) {
            if (element.current != null) {
              return arrow2({
                element: element.current,
                padding
              }).fn(state2);
            }
            return {};
          }
          if (element) {
            return arrow2({
              element,
              padding
            }).fn(state2);
          }
          return {};
        }
      };
    };
    offset3 = (options, deps) => ({
      ...offset2(options),
      options: [options, deps]
    });
    shift3 = (options, deps) => ({
      ...shift2(options),
      options: [options, deps]
    });
    limitShift3 = (options, deps) => ({
      ...limitShift2(options),
      options: [options, deps]
    });
    flip3 = (options, deps) => ({
      ...flip2(options),
      options: [options, deps]
    });
    size3 = (options, deps) => ({
      ...size2(options),
      options: [options, deps]
    });
    inline3 = (options, deps) => ({
      ...inline2(options),
      options: [options, deps]
    });
    arrow3 = (options, deps) => ({
      ...arrow$1(options),
      options: [options, deps]
    });
  }
});

// node_modules/@floating-ui/react/dist/floating-ui.react.mjs
function useEffectEvent(callback) {
  const ref = React8.useRef(() => {
    if (false) {
      throw new Error("Cannot call an event handler while rendering.");
    }
  });
  useSafeInsertionEffect(() => {
    ref.current = callback;
  });
  return React8.useCallback(function() {
    for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }
    return ref.current == null ? void 0 : ref.current(...args);
  }, []);
}
function useFloatingId() {
  const [id3, setId] = React8.useState(() => serverHandoffComplete ? genId() : void 0);
  index2(() => {
    if (id3 == null) {
      setId(genId());
    }
  }, []);
  React8.useEffect(() => {
    serverHandoffComplete = true;
  }, []);
  return id3;
}
function createPubSub() {
  const map = /* @__PURE__ */ new Map();
  return {
    emit(event, data) {
      var _map$get;
      (_map$get = map.get(event)) == null || _map$get.forEach((handler2) => handler2(data));
    },
    on(event, listener) {
      map.set(event, [...map.get(event) || [], listener]);
    },
    off(event, listener) {
      var _map$get2;
      map.set(event, ((_map$get2 = map.get(event)) == null ? void 0 : _map$get2.filter((l12) => l12 !== listener)) || []);
    }
  };
}
function useFloatingRootContext(options) {
  const {
    open = false,
    onOpenChange: onOpenChangeProp,
    elements: elementsProp
  } = options;
  const floatingId = useId3();
  const dataRef = React8.useRef({});
  const [events] = React8.useState(() => createPubSub());
  const nested = useFloatingParentNodeId() != null;
  if (false) {
    const optionDomReference = elementsProp.reference;
    if (optionDomReference && !isElement2(optionDomReference)) {
      error("Cannot pass a virtual element to the `elements.reference` option,", "as it must be a real DOM element. Use `refs.setPositionReference()`", "instead.");
    }
  }
  const [positionReference, setPositionReference] = React8.useState(elementsProp.reference);
  const onOpenChange = useEffectEvent((open2, event, reason) => {
    dataRef.current.openEvent = open2 ? event : void 0;
    events.emit("openchange", {
      open: open2,
      event,
      reason,
      nested
    });
    onOpenChangeProp == null || onOpenChangeProp(open2, event, reason);
  });
  const refs = React8.useMemo(() => ({
    setPositionReference
  }), []);
  const elements = React8.useMemo(() => ({
    reference: positionReference || elementsProp.reference || null,
    floating: elementsProp.floating || null,
    domReference: elementsProp.reference
  }), [positionReference, elementsProp.reference, elementsProp.floating]);
  return React8.useMemo(() => ({
    dataRef,
    open,
    onOpenChange,
    elements,
    events,
    floatingId,
    refs
  }), [open, onOpenChange, elements, events, floatingId, refs]);
}
function useFloating2(options) {
  if (options === void 0) {
    options = {};
  }
  const {
    nodeId
  } = options;
  const internalRootContext = useFloatingRootContext({
    ...options,
    elements: {
      reference: null,
      floating: null,
      ...options.elements
    }
  });
  const rootContext = options.rootContext || internalRootContext;
  const computedElements = rootContext.elements;
  const [_domReference, setDomReference] = React8.useState(null);
  const [positionReference, _setPositionReference] = React8.useState(null);
  const optionDomReference = computedElements == null ? void 0 : computedElements.domReference;
  const domReference = optionDomReference || _domReference;
  const domReferenceRef = React8.useRef(null);
  const tree = useFloatingTree();
  index2(() => {
    if (domReference) {
      domReferenceRef.current = domReference;
    }
  }, [domReference]);
  const position = useFloating({
    ...options,
    elements: {
      ...computedElements,
      ...positionReference && {
        reference: positionReference
      }
    }
  });
  const setPositionReference = React8.useCallback((node) => {
    const computedPositionReference = isElement2(node) ? {
      getBoundingClientRect: () => node.getBoundingClientRect(),
      contextElement: node
    } : node;
    _setPositionReference(computedPositionReference);
    position.refs.setReference(computedPositionReference);
  }, [position.refs]);
  const setReference = React8.useCallback((node) => {
    if (isElement2(node) || node === null) {
      domReferenceRef.current = node;
      setDomReference(node);
    }
    if (isElement2(position.refs.reference.current) || position.refs.reference.current === null || // Don't allow setting virtual elements using the old technique back to
    // `null` to support `positionReference` + an unstable `reference`
    // callback ref.
    node !== null && !isElement2(node)) {
      position.refs.setReference(node);
    }
  }, [position.refs]);
  const refs = React8.useMemo(() => ({
    ...position.refs,
    setReference,
    setPositionReference,
    domReference: domReferenceRef
  }), [position.refs, setReference, setPositionReference]);
  const elements = React8.useMemo(() => ({
    ...position.elements,
    domReference
  }), [position.elements, domReference]);
  const context = React8.useMemo(() => ({
    ...position,
    ...rootContext,
    refs,
    elements,
    nodeId
  }), [position, refs, elements, nodeId, rootContext]);
  index2(() => {
    rootContext.dataRef.current.floatingContext = context;
    const node = tree == null ? void 0 : tree.nodesRef.current.find((node2) => node2.id === nodeId);
    if (node) {
      node.context = context;
    }
  });
  return React8.useMemo(() => ({
    ...position,
    context,
    refs,
    elements
  }), [position, refs, elements, context]);
}
var React8, import_react63, ReactDOM2, SafeReact, useInsertionEffect, useSafeInsertionEffect, ARROW_UP, ARROW_DOWN, ARROW_LEFT, ARROW_RIGHT, index2, horizontalKeys, verticalKeys, allKeys, serverHandoffComplete, count, genId, useReactId2, useId3, FloatingNodeContext, FloatingTreeContext, useFloatingParentNodeId, useFloatingTree;
var init_floating_ui_react = __esm({
  "node_modules/@floating-ui/react/dist/floating-ui.react.mjs"() {
    React8 = __toESM(require_react(), 1);
    import_react63 = __toESM(require_react(), 1);
    init_floating_ui_utils_dom();
    ReactDOM2 = __toESM(require_react_dom(), 1);
    init_floating_ui_react_dom();
    init_floating_ui_react_dom();
    SafeReact = {
      ...React8
    };
    useInsertionEffect = SafeReact.useInsertionEffect;
    useSafeInsertionEffect = useInsertionEffect || ((fn) => fn());
    ARROW_UP = "ArrowUp";
    ARROW_DOWN = "ArrowDown";
    ARROW_LEFT = "ArrowLeft";
    ARROW_RIGHT = "ArrowRight";
    index2 = typeof document !== "undefined" ? import_react63.useLayoutEffect : import_react63.useEffect;
    horizontalKeys = [ARROW_LEFT, ARROW_RIGHT];
    verticalKeys = [ARROW_UP, ARROW_DOWN];
    allKeys = [...horizontalKeys, ...verticalKeys];
    serverHandoffComplete = false;
    count = 0;
    genId = () => (
      // Ensure the id is unique with multiple independent versions of Floating UI
      // on <React 18
      "floating-ui-" + Math.random().toString(36).slice(2, 6) + count++
    );
    useReactId2 = SafeReact.useId;
    useId3 = useReactId2 || useFloatingId;
    if (false) {
      devMessageSet = /* @__PURE__ */ new Set();
    }
    FloatingNodeContext = /* @__PURE__ */ React8.createContext(null);
    FloatingTreeContext = /* @__PURE__ */ React8.createContext(null);
    useFloatingParentNodeId = () => {
      var _React$useContext;
      return ((_React$useContext = React8.useContext(FloatingNodeContext)) == null ? void 0 : _React$useContext.id) || null;
    };
    useFloatingTree = () => React8.useContext(FloatingTreeContext);
  }
});

// node_modules/@mantine/core/esm/components/Floating/get-floating-position/get-floating-position.mjs
function getFloatingPosition(dir, position) {
  if (dir === "rtl" && (position.includes("right") || position.includes("left"))) {
    const [side, placement] = position.split("-");
    const flippedPosition = side === "right" ? "left" : "right";
    return placement === void 0 ? flippedPosition : `${flippedPosition}-${placement}`;
  }
  return position;
}
var init_get_floating_position = __esm({
  "node_modules/@mantine/core/esm/components/Floating/get-floating-position/get-floating-position.mjs"() {
    "use client";
  }
});

// node_modules/@mantine/core/esm/components/Floating/FloatingArrow/get-arrow-position-styles.mjs
function horizontalSide(placement, arrowY, arrowOffset, arrowPosition) {
  if (placement === "center" || arrowPosition === "center") {
    return { top: arrowY };
  }
  if (placement === "end") {
    return { bottom: arrowOffset };
  }
  if (placement === "start") {
    return { top: arrowOffset };
  }
  return {};
}
function verticalSide(placement, arrowX, arrowOffset, arrowPosition, dir) {
  if (placement === "center" || arrowPosition === "center") {
    return { left: arrowX };
  }
  if (placement === "end") {
    return { [dir === "ltr" ? "right" : "left"]: arrowOffset };
  }
  if (placement === "start") {
    return { [dir === "ltr" ? "left" : "right"]: arrowOffset };
  }
  return {};
}
function getArrowPositionStyles({
  position,
  arrowSize,
  arrowOffset,
  arrowRadius,
  arrowPosition,
  arrowX,
  arrowY,
  dir
}) {
  const [side, placement = "center"] = position.split("-");
  const baseStyles = {
    width: arrowSize,
    height: arrowSize,
    transform: "rotate(45deg)",
    position: "absolute",
    [radiusByFloatingSide[side]]: arrowRadius
  };
  const arrowPlacement = -arrowSize / 2;
  if (side === "left") {
    return {
      ...baseStyles,
      ...horizontalSide(placement, arrowY, arrowOffset, arrowPosition),
      right: arrowPlacement,
      borderLeftColor: "transparent",
      borderBottomColor: "transparent",
      clipPath: "polygon(100% 0, 0 0, 100% 100%)"
    };
  }
  if (side === "right") {
    return {
      ...baseStyles,
      ...horizontalSide(placement, arrowY, arrowOffset, arrowPosition),
      left: arrowPlacement,
      borderRightColor: "transparent",
      borderTopColor: "transparent",
      clipPath: "polygon(0 100%, 0 0, 100% 100%)"
    };
  }
  if (side === "top") {
    return {
      ...baseStyles,
      ...verticalSide(placement, arrowX, arrowOffset, arrowPosition, dir),
      bottom: arrowPlacement,
      borderTopColor: "transparent",
      borderLeftColor: "transparent",
      clipPath: "polygon(0 100%, 100% 100%, 100% 0)"
    };
  }
  if (side === "bottom") {
    return {
      ...baseStyles,
      ...verticalSide(placement, arrowX, arrowOffset, arrowPosition, dir),
      top: arrowPlacement,
      borderBottomColor: "transparent",
      borderRightColor: "transparent",
      clipPath: "polygon(0 100%, 0 0, 100% 0)"
    };
  }
  return {};
}
var radiusByFloatingSide;
var init_get_arrow_position_styles = __esm({
  "node_modules/@mantine/core/esm/components/Floating/FloatingArrow/get-arrow-position-styles.mjs"() {
    "use client";
    radiusByFloatingSide = {
      bottom: "borderTopLeftRadius",
      left: "borderTopRightRadius",
      right: "borderBottomLeftRadius",
      top: "borderBottomRightRadius"
    };
  }
});

// node_modules/@mantine/core/esm/components/Floating/FloatingArrow/FloatingArrow.mjs
var import_jsx_runtime37, import_react64, FloatingArrow;
var init_FloatingArrow = __esm({
  "node_modules/@mantine/core/esm/components/Floating/FloatingArrow/FloatingArrow.mjs"() {
    "use client";
    import_jsx_runtime37 = __toESM(require_jsx_runtime(), 1);
    import_react64 = __toESM(require_react(), 1);
    init_clsx();
    init_DirectionProvider();
    init_get_arrow_position_styles();
    FloatingArrow = (0, import_react64.forwardRef)(
      ({
        position,
        arrowSize,
        arrowOffset,
        arrowRadius,
        arrowPosition,
        visible: visible2,
        arrowX,
        arrowY,
        style,
        ...others
      }, ref) => {
        const { dir } = useDirection();
        if (!visible2) {
          return null;
        }
        return /* @__PURE__ */ (0, import_jsx_runtime37.jsx)(
          "div",
          {
            ...others,
            ref,
            style: {
              ...style,
              ...getArrowPositionStyles({
                position,
                arrowSize,
                arrowOffset,
                arrowRadius,
                arrowPosition,
                dir,
                arrowX,
                arrowY
              })
            }
          }
        );
      }
    );
    FloatingArrow.displayName = "@mantine/core/FloatingArrow";
  }
});

// node_modules/@mantine/core/esm/components/Popover/Popover.context.mjs
var import_react65, import_jsx_runtime38, PopoverContextProvider, usePopoverContext;
var init_Popover_context = __esm({
  "node_modules/@mantine/core/esm/components/Popover/Popover.context.mjs"() {
    "use client";
    import_react65 = __toESM(require_react(), 1);
    init_create_safe_context();
    import_jsx_runtime38 = __toESM(require_jsx_runtime(), 1);
    init_clsx();
    [PopoverContextProvider, usePopoverContext] = createSafeContext(
      "Popover component was not found in the tree"
    );
  }
});

// node_modules/@mantine/core/esm/components/FocusTrap/FocusTrap.mjs
function FocusTrap({
  children,
  active = true,
  refProp = "ref",
  innerRef
}) {
  const focusTrapRef = useFocusTrap(active);
  const ref = useMergedRef(focusTrapRef, innerRef);
  if (!isElement(children)) {
    return children;
  }
  return (0, import_react66.cloneElement)(children, { [refProp]: ref });
}
function FocusTrapInitialFocus(props) {
  return /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(VisuallyHidden, { tabIndex: -1, "data-autofocus": true, ...props });
}
var import_jsx_runtime39, import_react66;
var init_FocusTrap = __esm({
  "node_modules/@mantine/core/esm/components/FocusTrap/FocusTrap.mjs"() {
    "use client";
    import_jsx_runtime39 = __toESM(require_jsx_runtime(), 1);
    import_react66 = __toESM(require_react(), 1);
    init_esm();
    init_is_element();
    init_clsx();
    init_VisuallyHidden();
    FocusTrap.displayName = "@mantine/core/FocusTrap";
    FocusTrapInitialFocus.displayName = "@mantine/core/FocusTrapInitialFocus";
    FocusTrap.InitialFocus = FocusTrapInitialFocus;
  }
});

// node_modules/@mantine/core/esm/components/Portal/Portal.mjs
function createPortalNode(props) {
  const node = document.createElement("div");
  node.setAttribute("data-portal", "true");
  typeof props.className === "string" && node.classList.add(...props.className.split(" ").filter(Boolean));
  typeof props.style === "object" && Object.assign(node.style, props.style);
  typeof props.id === "string" && node.setAttribute("id", props.id);
  return node;
}
var import_jsx_runtime40, import_react67, import_react_dom3, defaultProps4, Portal;
var init_Portal = __esm({
  "node_modules/@mantine/core/esm/components/Portal/Portal.mjs"() {
    "use client";
    import_jsx_runtime40 = __toESM(require_jsx_runtime(), 1);
    import_react67 = __toESM(require_react(), 1);
    import_react_dom3 = __toESM(require_react_dom(), 1);
    init_esm();
    init_clsx();
    init_use_props();
    defaultProps4 = {};
    Portal = (0, import_react67.forwardRef)((props, ref) => {
      const { children, target, ...others } = useProps("Portal", defaultProps4, props);
      const [mounted, setMounted] = (0, import_react67.useState)(false);
      const nodeRef = (0, import_react67.useRef)(null);
      useIsomorphicEffect(() => {
        setMounted(true);
        nodeRef.current = !target ? createPortalNode(others) : typeof target === "string" ? document.querySelector(target) : target;
        assignRef(ref, nodeRef.current);
        if (!target && nodeRef.current) {
          document.body.appendChild(nodeRef.current);
        }
        return () => {
          if (!target && nodeRef.current) {
            document.body.removeChild(nodeRef.current);
          }
        };
      }, [target]);
      if (!mounted || !nodeRef.current) {
        return null;
      }
      return (0, import_react_dom3.createPortal)(/* @__PURE__ */ (0, import_jsx_runtime40.jsx)(import_jsx_runtime40.Fragment, { children }), nodeRef.current);
    });
    Portal.displayName = "@mantine/core/Portal";
  }
});

// node_modules/@mantine/core/esm/components/Portal/OptionalPortal.mjs
function OptionalPortal({ withinPortal = true, children, ...others }) {
  if (withinPortal) {
    return /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(Portal, { ...others, children });
  }
  return /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(import_jsx_runtime41.Fragment, { children });
}
var import_jsx_runtime41;
var init_OptionalPortal = __esm({
  "node_modules/@mantine/core/esm/components/Portal/OptionalPortal.mjs"() {
    "use client";
    import_jsx_runtime41 = __toESM(require_jsx_runtime(), 1);
    init_Portal();
    OptionalPortal.displayName = "@mantine/core/OptionalPortal";
  }
});

// node_modules/@mantine/core/esm/components/Transition/transitions.mjs
var import_react68, import_jsx_runtime42, popIn, transitions;
var init_transitions = __esm({
  "node_modules/@mantine/core/esm/components/Transition/transitions.mjs"() {
    "use client";
    init_rem();
    import_react68 = __toESM(require_react(), 1);
    import_jsx_runtime42 = __toESM(require_jsx_runtime(), 1);
    init_clsx();
    popIn = (from) => ({
      in: { opacity: 1, transform: "scale(1)" },
      out: { opacity: 0, transform: `scale(.9) translateY(${rem(from === "bottom" ? 10 : -10)})` },
      transitionProperty: "transform, opacity"
    });
    transitions = {
      fade: {
        in: { opacity: 1 },
        out: { opacity: 0 },
        transitionProperty: "opacity"
      },
      "fade-up": {
        in: { opacity: 1, transform: "translateY(0)" },
        out: { opacity: 0, transform: `translateY(${rem(30)}` },
        transitionProperty: "opacity, transform"
      },
      "fade-down": {
        in: { opacity: 1, transform: "translateY(0)" },
        out: { opacity: 0, transform: `translateY(${rem(-30)}` },
        transitionProperty: "opacity, transform"
      },
      "fade-left": {
        in: { opacity: 1, transform: "translateX(0)" },
        out: { opacity: 0, transform: `translateX(${rem(30)}` },
        transitionProperty: "opacity, transform"
      },
      "fade-right": {
        in: { opacity: 1, transform: "translateX(0)" },
        out: { opacity: 0, transform: `translateX(${rem(-30)}` },
        transitionProperty: "opacity, transform"
      },
      scale: {
        in: { opacity: 1, transform: "scale(1)" },
        out: { opacity: 0, transform: "scale(0)" },
        common: { transformOrigin: "top" },
        transitionProperty: "transform, opacity"
      },
      "scale-y": {
        in: { opacity: 1, transform: "scaleY(1)" },
        out: { opacity: 0, transform: "scaleY(0)" },
        common: { transformOrigin: "top" },
        transitionProperty: "transform, opacity"
      },
      "scale-x": {
        in: { opacity: 1, transform: "scaleX(1)" },
        out: { opacity: 0, transform: "scaleX(0)" },
        common: { transformOrigin: "left" },
        transitionProperty: "transform, opacity"
      },
      "skew-up": {
        in: { opacity: 1, transform: "translateY(0) skew(0deg, 0deg)" },
        out: { opacity: 0, transform: `translateY(${rem(-20)}) skew(-10deg, -5deg)` },
        common: { transformOrigin: "top" },
        transitionProperty: "transform, opacity"
      },
      "skew-down": {
        in: { opacity: 1, transform: "translateY(0) skew(0deg, 0deg)" },
        out: { opacity: 0, transform: `translateY(${rem(20)}) skew(-10deg, -5deg)` },
        common: { transformOrigin: "bottom" },
        transitionProperty: "transform, opacity"
      },
      "rotate-left": {
        in: { opacity: 1, transform: "translateY(0) rotate(0deg)" },
        out: { opacity: 0, transform: `translateY(${rem(20)}) rotate(-5deg)` },
        common: { transformOrigin: "bottom" },
        transitionProperty: "transform, opacity"
      },
      "rotate-right": {
        in: { opacity: 1, transform: "translateY(0) rotate(0deg)" },
        out: { opacity: 0, transform: `translateY(${rem(20)}) rotate(5deg)` },
        common: { transformOrigin: "top" },
        transitionProperty: "transform, opacity"
      },
      "slide-down": {
        in: { opacity: 1, transform: "translateY(0)" },
        out: { opacity: 0, transform: "translateY(-100%)" },
        common: { transformOrigin: "top" },
        transitionProperty: "transform, opacity"
      },
      "slide-up": {
        in: { opacity: 1, transform: "translateY(0)" },
        out: { opacity: 0, transform: "translateY(100%)" },
        common: { transformOrigin: "bottom" },
        transitionProperty: "transform, opacity"
      },
      "slide-left": {
        in: { opacity: 1, transform: "translateX(0)" },
        out: { opacity: 0, transform: "translateX(100%)" },
        common: { transformOrigin: "left" },
        transitionProperty: "transform, opacity"
      },
      "slide-right": {
        in: { opacity: 1, transform: "translateX(0)" },
        out: { opacity: 0, transform: "translateX(-100%)" },
        common: { transformOrigin: "right" },
        transitionProperty: "transform, opacity"
      },
      pop: {
        ...popIn("bottom"),
        common: { transformOrigin: "center center" }
      },
      "pop-bottom-left": {
        ...popIn("bottom"),
        common: { transformOrigin: "bottom left" }
      },
      "pop-bottom-right": {
        ...popIn("bottom"),
        common: { transformOrigin: "bottom right" }
      },
      "pop-top-left": {
        ...popIn("top"),
        common: { transformOrigin: "top left" }
      },
      "pop-top-right": {
        ...popIn("top"),
        common: { transformOrigin: "top right" }
      }
    };
  }
});

// node_modules/@mantine/core/esm/components/Transition/get-transition-styles/get-transition-styles.mjs
function getTransitionStyles({
  transition,
  state: state2,
  duration,
  timingFunction
}) {
  const shared = {
    transitionDuration: `${duration}ms`,
    transitionTimingFunction: timingFunction
  };
  if (typeof transition === "string") {
    if (!(transition in transitions)) {
      return {};
    }
    return {
      transitionProperty: transitions[transition].transitionProperty,
      ...shared,
      ...transitions[transition].common,
      ...transitions[transition][transitionStatuses[state2]]
    };
  }
  return {
    transitionProperty: transition.transitionProperty,
    ...shared,
    ...transition.common,
    ...transition[transitionStatuses[state2]]
  };
}
var transitionStatuses;
var init_get_transition_styles = __esm({
  "node_modules/@mantine/core/esm/components/Transition/get-transition-styles/get-transition-styles.mjs"() {
    "use client";
    init_transitions();
    transitionStatuses = {
      entering: "in",
      entered: "in",
      exiting: "out",
      exited: "out",
      "pre-exiting": "out",
      "pre-entering": "out"
    };
  }
});

// node_modules/@mantine/core/esm/components/Transition/use-transition.mjs
function useTransition({
  duration,
  exitDuration,
  timingFunction,
  mounted,
  onEnter,
  onExit,
  onEntered,
  onExited,
  enterDelay,
  exitDelay
}) {
  const theme2 = useMantineTheme();
  const shouldReduceMotion = useReducedMotion();
  const reduceMotion = theme2.respectReducedMotion ? shouldReduceMotion : false;
  const [transitionDuration, setTransitionDuration] = (0, import_react69.useState)(reduceMotion ? 0 : duration);
  const [transitionStatus, setStatus] = (0, import_react69.useState)(mounted ? "entered" : "exited");
  const transitionTimeoutRef = (0, import_react69.useRef)(-1);
  const delayTimeoutRef = (0, import_react69.useRef)(-1);
  const rafRef = (0, import_react69.useRef)(-1);
  const handleStateChange = (shouldMount) => {
    const preHandler = shouldMount ? onEnter : onExit;
    const handler2 = shouldMount ? onEntered : onExited;
    window.clearTimeout(transitionTimeoutRef.current);
    const newTransitionDuration = reduceMotion ? 0 : shouldMount ? duration : exitDuration;
    setTransitionDuration(newTransitionDuration);
    if (newTransitionDuration === 0) {
      typeof preHandler === "function" && preHandler();
      typeof handler2 === "function" && handler2();
      setStatus(shouldMount ? "entered" : "exited");
    } else {
      rafRef.current = requestAnimationFrame(() => {
        import_react_dom4.default.flushSync(() => {
          setStatus(shouldMount ? "pre-entering" : "pre-exiting");
        });
        rafRef.current = requestAnimationFrame(() => {
          typeof preHandler === "function" && preHandler();
          setStatus(shouldMount ? "entering" : "exiting");
          transitionTimeoutRef.current = window.setTimeout(() => {
            typeof handler2 === "function" && handler2();
            setStatus(shouldMount ? "entered" : "exited");
          }, newTransitionDuration);
        });
      });
    }
  };
  const handleTransitionWithDelay = (shouldMount) => {
    window.clearTimeout(delayTimeoutRef.current);
    const delay3 = shouldMount ? enterDelay : exitDelay;
    if (typeof delay3 !== "number") {
      handleStateChange(shouldMount);
      return;
    }
    delayTimeoutRef.current = window.setTimeout(
      () => {
        handleStateChange(shouldMount);
      },
      shouldMount ? enterDelay : exitDelay
    );
  };
  useDidUpdate(() => {
    handleTransitionWithDelay(mounted);
  }, [mounted]);
  (0, import_react69.useEffect)(
    () => () => {
      window.clearTimeout(transitionTimeoutRef.current);
      cancelAnimationFrame(rafRef.current);
    },
    []
  );
  return {
    transitionDuration,
    transitionStatus,
    transitionTimingFunction: timingFunction || "ease"
  };
}
var import_react69, import_react_dom4, import_jsx_runtime43;
var init_use_transition = __esm({
  "node_modules/@mantine/core/esm/components/Transition/use-transition.mjs"() {
    "use client";
    import_react69 = __toESM(require_react(), 1);
    import_react_dom4 = __toESM(require_react_dom(), 1);
    init_esm();
    import_jsx_runtime43 = __toESM(require_jsx_runtime(), 1);
    init_clsx();
    init_MantineThemeProvider();
  }
});

// node_modules/@mantine/core/esm/components/Transition/Transition.mjs
function Transition({
  keepMounted,
  transition = "fade",
  duration = 250,
  exitDuration = duration,
  mounted,
  children,
  timingFunction = "ease",
  onExit,
  onEntered,
  onEnter,
  onExited,
  enterDelay,
  exitDelay
}) {
  const { transitionDuration, transitionStatus, transitionTimingFunction } = useTransition({
    mounted,
    exitDuration,
    duration,
    timingFunction,
    onExit,
    onEntered,
    onEnter,
    onExited,
    enterDelay,
    exitDelay
  });
  if (transitionDuration === 0) {
    return mounted ? /* @__PURE__ */ (0, import_jsx_runtime44.jsx)(import_jsx_runtime44.Fragment, { children: children({}) }) : keepMounted ? children({ display: "none" }) : null;
  }
  return transitionStatus === "exited" ? keepMounted ? children({ display: "none" }) : null : /* @__PURE__ */ (0, import_jsx_runtime44.jsx)(import_jsx_runtime44.Fragment, { children: children(
    getTransitionStyles({
      transition,
      duration: transitionDuration,
      state: transitionStatus,
      timingFunction: transitionTimingFunction
    })
  ) });
}
var import_jsx_runtime44;
var init_Transition = __esm({
  "node_modules/@mantine/core/esm/components/Transition/Transition.mjs"() {
    "use client";
    import_jsx_runtime44 = __toESM(require_jsx_runtime(), 1);
    init_get_transition_styles();
    init_use_transition();
    Transition.displayName = "@mantine/core/Transition";
  }
});

// node_modules/@mantine/core/esm/components/Popover/Popover.module.css.mjs
var classes4;
var init_Popover_module_css = __esm({
  "node_modules/@mantine/core/esm/components/Popover/Popover.module.css.mjs"() {
    "use client";
    classes4 = { "dropdown": "m_38a85659", "arrow": "m_a31dc6c1" };
  }
});

// node_modules/@mantine/core/esm/components/Popover/PopoverDropdown/PopoverDropdown.mjs
var import_jsx_runtime45, import_react70, defaultProps5, PopoverDropdown;
var init_PopoverDropdown = __esm({
  "node_modules/@mantine/core/esm/components/Popover/PopoverDropdown/PopoverDropdown.mjs"() {
    "use client";
    import_jsx_runtime45 = __toESM(require_jsx_runtime(), 1);
    init_esm();
    init_rem();
    import_react70 = __toESM(require_react(), 1);
    init_close_on_escape();
    init_clsx();
    init_use_props();
    init_Box();
    init_factory();
    init_FloatingArrow();
    init_FocusTrap();
    init_OptionalPortal();
    init_Transition();
    init_Popover_context();
    init_Popover_module_css();
    defaultProps5 = {};
    PopoverDropdown = factory((_props, ref) => {
      const props = useProps("PopoverDropdown", defaultProps5, _props);
      const {
        className,
        style,
        vars,
        children,
        onKeyDownCapture,
        variant,
        classNames,
        styles,
        ...others
      } = props;
      const ctx = usePopoverContext();
      const returnFocus = useFocusReturn({
        opened: ctx.opened,
        shouldReturnFocus: ctx.returnFocus
      });
      const accessibleProps = ctx.withRoles ? {
        "aria-labelledby": ctx.getTargetId(),
        id: ctx.getDropdownId(),
        role: "dialog",
        tabIndex: -1
      } : {};
      const mergedRef = useMergedRef(ref, ctx.floating);
      if (ctx.disabled) {
        return null;
      }
      return /* @__PURE__ */ (0, import_jsx_runtime45.jsx)(OptionalPortal, { ...ctx.portalProps, withinPortal: ctx.withinPortal, children: /* @__PURE__ */ (0, import_jsx_runtime45.jsx)(
        Transition,
        {
          mounted: ctx.opened,
          ...ctx.transitionProps,
          transition: ctx.transitionProps?.transition || "fade",
          duration: ctx.transitionProps?.duration ?? 150,
          keepMounted: ctx.keepMounted,
          exitDuration: typeof ctx.transitionProps?.exitDuration === "number" ? ctx.transitionProps.exitDuration : ctx.transitionProps?.duration,
          children: (transitionStyles) => /* @__PURE__ */ (0, import_jsx_runtime45.jsx)(FocusTrap, { active: ctx.trapFocus && ctx.opened, innerRef: mergedRef, children: /* @__PURE__ */ (0, import_jsx_runtime45.jsxs)(
            Box,
            {
              ...accessibleProps,
              ...others,
              variant,
              onKeyDownCapture: closeOnEscape(ctx.onClose, {
                active: ctx.closeOnEscape,
                onTrigger: returnFocus,
                onKeyDown: onKeyDownCapture
              }),
              "data-position": ctx.placement,
              "data-fixed": ctx.floatingStrategy === "fixed" || void 0,
              ...ctx.getStyles("dropdown", {
                className,
                props,
                classNames,
                styles,
                style: [
                  {
                    ...transitionStyles,
                    zIndex: ctx.zIndex,
                    top: ctx.y ?? 0,
                    left: ctx.x ?? 0,
                    width: ctx.width === "target" ? void 0 : rem(ctx.width)
                  },
                  ctx.resolvedStyles.dropdown,
                  styles?.dropdown,
                  style
                ]
              }),
              children: [
                children,
                /* @__PURE__ */ (0, import_jsx_runtime45.jsx)(
                  FloatingArrow,
                  {
                    ref: ctx.arrowRef,
                    arrowX: ctx.arrowX,
                    arrowY: ctx.arrowY,
                    visible: ctx.withArrow,
                    position: ctx.placement,
                    arrowSize: ctx.arrowSize,
                    arrowRadius: ctx.arrowRadius,
                    arrowOffset: ctx.arrowOffset,
                    arrowPosition: ctx.arrowPosition,
                    ...ctx.getStyles("arrow", {
                      props,
                      classNames,
                      styles
                    })
                  }
                )
              ]
            }
          ) })
        }
      ) });
    });
    PopoverDropdown.classes = classes4;
    PopoverDropdown.displayName = "@mantine/core/PopoverDropdown";
  }
});

// node_modules/@mantine/core/esm/components/Popover/PopoverTarget/PopoverTarget.mjs
var import_react71, import_jsx_runtime46, defaultProps6, PopoverTarget;
var init_PopoverTarget = __esm({
  "node_modules/@mantine/core/esm/components/Popover/PopoverTarget/PopoverTarget.mjs"() {
    "use client";
    import_react71 = __toESM(require_react(), 1);
    init_clsx();
    init_esm();
    init_is_element();
    import_jsx_runtime46 = __toESM(require_jsx_runtime(), 1);
    init_get_ref_prop();
    init_use_props();
    init_factory();
    init_Popover_context();
    defaultProps6 = {
      refProp: "ref",
      popupType: "dialog"
    };
    PopoverTarget = factory((props, ref) => {
      const { children, refProp, popupType, ...others } = useProps(
        "PopoverTarget",
        defaultProps6,
        props
      );
      if (!isElement(children)) {
        throw new Error(
          "Popover.Target component children should be an element or a component that accepts ref. Fragments, strings, numbers and other primitive values are not supported"
        );
      }
      const forwardedProps = others;
      const ctx = usePopoverContext();
      const targetRef = useMergedRef(ctx.reference, getRefProp(children), ref);
      const accessibleProps = ctx.withRoles ? {
        "aria-haspopup": popupType,
        "aria-expanded": ctx.opened,
        "aria-controls": ctx.getDropdownId(),
        id: ctx.getTargetId()
      } : {};
      return (0, import_react71.cloneElement)(children, {
        ...forwardedProps,
        ...accessibleProps,
        ...ctx.targetProps,
        className: clsx_default(ctx.targetProps.className, forwardedProps.className, children.props.className),
        [refProp]: targetRef,
        ...!ctx.controlled ? { onClick: ctx.onToggle } : null
      });
    });
    PopoverTarget.displayName = "@mantine/core/PopoverTarget";
  }
});

// node_modules/@mantine/core/esm/components/Floating/use-floating-auto-update.mjs
function useFloatingAutoUpdate({
  opened,
  floating,
  position,
  positionDependencies
}) {
  const [delayedUpdate, setDelayedUpdate] = (0, import_react72.useState)(0);
  (0, import_react72.useEffect)(() => {
    if (floating.refs.reference.current && floating.refs.floating.current && opened) {
      return autoUpdate(
        floating.refs.reference.current,
        floating.refs.floating.current,
        floating.update
      );
    }
    return void 0;
  }, [
    floating.refs.reference.current,
    floating.refs.floating.current,
    opened,
    delayedUpdate,
    position
  ]);
  useDidUpdate(() => {
    floating.update();
  }, positionDependencies);
  useDidUpdate(() => {
    setDelayedUpdate((c15) => c15 + 1);
  }, [opened]);
}
var import_react72;
var init_use_floating_auto_update = __esm({
  "node_modules/@mantine/core/esm/components/Floating/use-floating-auto-update.mjs"() {
    "use client";
    import_react72 = __toESM(require_react(), 1);
    init_floating_ui_react();
    init_esm();
  }
});

// node_modules/@mantine/core/esm/components/Popover/use-popover.mjs
function getDefaultMiddlewares(middlewares) {
  if (middlewares === void 0) {
    return { shift: true, flip: true };
  }
  const result = { ...middlewares };
  if (middlewares.shift === void 0) {
    result.shift = true;
  }
  if (middlewares.flip === void 0) {
    result.flip = true;
  }
  return result;
}
function getPopoverMiddlewares(options, getFloating) {
  const middlewaresOptions = getDefaultMiddlewares(options.middlewares);
  const middlewares = [offset3(options.offset)];
  if (middlewaresOptions.shift) {
    middlewares.push(
      shift3(
        typeof middlewaresOptions.shift === "boolean" ? { limiter: limitShift3(), padding: 5 } : { limiter: limitShift3(), padding: 5, ...middlewaresOptions.shift }
      )
    );
  }
  if (middlewaresOptions.flip) {
    middlewares.push(
      typeof middlewaresOptions.flip === "boolean" ? flip3() : flip3(middlewaresOptions.flip)
    );
  }
  if (middlewaresOptions.inline) {
    middlewares.push(
      typeof middlewaresOptions.inline === "boolean" ? inline3() : inline3(middlewaresOptions.inline)
    );
  }
  middlewares.push(arrow3({ element: options.arrowRef, padding: options.arrowOffset }));
  if (middlewaresOptions.size || options.width === "target") {
    middlewares.push(
      size3({
        ...typeof middlewaresOptions.size === "boolean" ? {} : middlewaresOptions.size,
        apply({ rects, availableWidth, availableHeight, ...rest }) {
          const floating = getFloating();
          const styles = floating.refs.floating.current?.style ?? {};
          if (middlewaresOptions.size) {
            if (typeof middlewaresOptions.size === "object" && !!middlewaresOptions.size.apply) {
              middlewaresOptions.size.apply({ rects, availableWidth, availableHeight, ...rest });
            } else {
              Object.assign(styles, {
                maxWidth: `${availableWidth}px`,
                maxHeight: `${availableHeight}px`
              });
            }
          }
          if (options.width === "target") {
            Object.assign(styles, {
              width: `${rects.reference.width}px`
            });
          }
        }
      })
    );
  }
  return middlewares;
}
function usePopover(options) {
  const [_opened, setOpened] = useUncontrolled({
    value: options.opened,
    defaultValue: options.defaultOpened,
    finalValue: false,
    onChange: options.onChange
  });
  const previouslyOpened = (0, import_react74.useRef)(_opened);
  const onClose = () => {
    if (_opened) {
      setOpened(false);
    }
  };
  const onToggle = () => setOpened(!_opened);
  const floating = useFloating2({
    strategy: options.strategy,
    placement: options.position,
    middleware: getPopoverMiddlewares(options, () => floating)
  });
  useFloatingAutoUpdate({
    opened: _opened,
    position: options.position,
    positionDependencies: options.positionDependencies || [],
    floating
  });
  useDidUpdate(() => {
    options.onPositionChange?.(floating.placement);
  }, [floating.placement]);
  useDidUpdate(() => {
    if (_opened !== previouslyOpened.current) {
      if (!_opened) {
        options.onClose?.();
      } else {
        options.onOpen?.();
      }
    }
    previouslyOpened.current = _opened;
  }, [_opened, options.onClose, options.onOpen]);
  return {
    floating,
    controlled: typeof options.opened === "boolean",
    opened: _opened,
    onClose,
    onToggle
  };
}
var import_react74;
var init_use_popover = __esm({
  "node_modules/@mantine/core/esm/components/Popover/use-popover.mjs"() {
    "use client";
    import_react74 = __toESM(require_react(), 1);
    init_floating_ui_react();
    init_esm();
    init_use_floating_auto_update();
  }
});

// node_modules/@mantine/core/esm/components/Popover/Popover.mjs
function Popover(_props) {
  const props = useProps("Popover", defaultProps7, _props);
  const {
    children,
    position,
    offset: offset4,
    onPositionChange,
    positionDependencies,
    opened,
    transitionProps,
    onExitTransitionEnd,
    onEnterTransitionEnd,
    width,
    middlewares,
    withArrow,
    arrowSize,
    arrowOffset,
    arrowRadius,
    arrowPosition,
    unstyled,
    classNames,
    styles,
    closeOnClickOutside,
    withinPortal,
    portalProps,
    closeOnEscape: closeOnEscape2,
    clickOutsideEvents,
    trapFocus,
    onClose,
    onOpen,
    onChange,
    zIndex,
    radius,
    shadow,
    id: id3,
    defaultOpened,
    __staticSelector,
    withRoles,
    disabled,
    returnFocus,
    variant,
    keepMounted,
    vars,
    floatingStrategy,
    ...others
  } = props;
  const getStyles = useStyles({
    name: __staticSelector,
    props,
    classes: classes4,
    classNames,
    styles,
    unstyled,
    rootSelector: "dropdown",
    vars,
    varsResolver: varsResolver2
  });
  const { resolvedStyles } = useResolvedStylesApi({ classNames, styles, props });
  const arrowRef = (0, import_react76.useRef)(null);
  const [targetNode, setTargetNode] = (0, import_react76.useState)(null);
  const [dropdownNode, setDropdownNode] = (0, import_react76.useState)(null);
  const { dir } = useDirection();
  const uid = useId(id3);
  const popover = usePopover({
    middlewares,
    width,
    position: getFloatingPosition(dir, position),
    offset: typeof offset4 === "number" ? offset4 + (withArrow ? arrowSize / 2 : 0) : offset4,
    arrowRef,
    arrowOffset,
    onPositionChange,
    positionDependencies,
    opened,
    defaultOpened,
    onChange,
    onOpen,
    onClose,
    strategy: floatingStrategy
  });
  useClickOutside(() => closeOnClickOutside && popover.onClose(), clickOutsideEvents, [
    targetNode,
    dropdownNode
  ]);
  const reference = (0, import_react76.useCallback)(
    (node) => {
      setTargetNode(node);
      popover.floating.refs.setReference(node);
    },
    [popover.floating.refs.setReference]
  );
  const floating = (0, import_react76.useCallback)(
    (node) => {
      setDropdownNode(node);
      popover.floating.refs.setFloating(node);
    },
    [popover.floating.refs.setFloating]
  );
  const onExited = (0, import_react76.useCallback)(() => {
    transitionProps?.onExited?.();
    onExitTransitionEnd?.();
  }, [transitionProps?.onExited, onExitTransitionEnd]);
  const onEntered = (0, import_react76.useCallback)(() => {
    transitionProps?.onEntered?.();
    onEnterTransitionEnd?.();
  }, [transitionProps?.onEntered, onEnterTransitionEnd]);
  return /* @__PURE__ */ (0, import_jsx_runtime47.jsx)(
    PopoverContextProvider,
    {
      value: {
        returnFocus,
        disabled,
        controlled: popover.controlled,
        reference,
        floating,
        x: popover.floating.x,
        y: popover.floating.y,
        arrowX: popover.floating?.middlewareData?.arrow?.x,
        arrowY: popover.floating?.middlewareData?.arrow?.y,
        opened: popover.opened,
        arrowRef,
        transitionProps: { ...transitionProps, onExited, onEntered },
        width,
        withArrow,
        arrowSize,
        arrowOffset,
        arrowRadius,
        arrowPosition,
        placement: popover.floating.placement,
        trapFocus,
        withinPortal,
        portalProps,
        zIndex,
        radius,
        shadow,
        closeOnEscape: closeOnEscape2,
        onClose: popover.onClose,
        onToggle: popover.onToggle,
        getTargetId: () => `${uid}-target`,
        getDropdownId: () => `${uid}-dropdown`,
        withRoles,
        targetProps: others,
        __staticSelector,
        classNames,
        styles,
        unstyled,
        variant,
        keepMounted,
        getStyles,
        resolvedStyles,
        floatingStrategy
      },
      children
    }
  );
}
var import_jsx_runtime47, import_react76, defaultProps7, varsResolver2;
var init_Popover = __esm({
  "node_modules/@mantine/core/esm/components/Popover/Popover.mjs"() {
    "use client";
    import_jsx_runtime47 = __toESM(require_jsx_runtime(), 1);
    import_react76 = __toESM(require_react(), 1);
    init_esm();
    init_get_default_z_index();
    init_get_size();
    init_create_vars_resolver();
    init_clsx();
    init_use_resolved_styles_api();
    init_use_styles();
    init_use_props();
    init_DirectionProvider();
    init_get_floating_position();
    init_Popover_context();
    init_PopoverDropdown();
    init_PopoverTarget();
    init_use_popover();
    init_Popover_module_css();
    defaultProps7 = {
      position: "bottom",
      offset: 8,
      positionDependencies: [],
      transitionProps: { transition: "fade", duration: 150 },
      middlewares: { flip: true, shift: true, inline: false },
      arrowSize: 7,
      arrowOffset: 5,
      arrowRadius: 0,
      arrowPosition: "side",
      closeOnClickOutside: true,
      withinPortal: true,
      closeOnEscape: true,
      trapFocus: false,
      withRoles: true,
      returnFocus: false,
      clickOutsideEvents: ["mousedown", "touchstart"],
      zIndex: getDefaultZIndex("popover"),
      __staticSelector: "Popover",
      width: "max-content"
    };
    varsResolver2 = createVarsResolver((_2, { radius, shadow }) => ({
      dropdown: {
        "--popover-radius": radius === void 0 ? void 0 : getRadius(radius),
        "--popover-shadow": getShadow(shadow)
      }
    }));
    Popover.Target = PopoverTarget;
    Popover.Dropdown = PopoverDropdown;
    Popover.displayName = "@mantine/core/Popover";
    Popover.extend = (input2) => input2;
  }
});

// node_modules/@mantine/core/esm/components/Loader/Loader.module.css.mjs
var classes5;
var init_Loader_module_css = __esm({
  "node_modules/@mantine/core/esm/components/Loader/Loader.module.css.mjs"() {
    "use client";
    classes5 = { "root": "m_5ae2e3c", "barsLoader": "m_7a2bd4cd", "bar": "m_870bb79", "bars-loader-animation": "m_5d2b3b9d", "dotsLoader": "m_4e3f22d7", "dot": "m_870c4af", "loader-dots-animation": "m_aac34a1", "ovalLoader": "m_b34414df", "oval-loader-animation": "m_f8e89c4b" };
  }
});

// node_modules/@mantine/core/esm/components/Loader/loaders/Bars.mjs
var import_jsx_runtime48, import_react77, Bars;
var init_Bars = __esm({
  "node_modules/@mantine/core/esm/components/Loader/loaders/Bars.mjs"() {
    "use client";
    import_jsx_runtime48 = __toESM(require_jsx_runtime(), 1);
    import_react77 = __toESM(require_react(), 1);
    init_clsx();
    init_Box();
    init_Loader_module_css();
    Bars = (0, import_react77.forwardRef)(({ className, ...others }, ref) => /* @__PURE__ */ (0, import_jsx_runtime48.jsxs)(Box, { component: "span", className: clsx_default(classes5.barsLoader, className), ...others, ref, children: [
      /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("span", { className: classes5.bar }),
      /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("span", { className: classes5.bar }),
      /* @__PURE__ */ (0, import_jsx_runtime48.jsx)("span", { className: classes5.bar })
    ] }));
    Bars.displayName = "@mantine/core/Bars";
  }
});

// node_modules/@mantine/core/esm/components/Loader/loaders/Dots.mjs
var import_jsx_runtime49, import_react78, Dots;
var init_Dots = __esm({
  "node_modules/@mantine/core/esm/components/Loader/loaders/Dots.mjs"() {
    "use client";
    import_jsx_runtime49 = __toESM(require_jsx_runtime(), 1);
    import_react78 = __toESM(require_react(), 1);
    init_clsx();
    init_Box();
    init_Loader_module_css();
    Dots = (0, import_react78.forwardRef)(({ className, ...others }, ref) => /* @__PURE__ */ (0, import_jsx_runtime49.jsxs)(Box, { component: "span", className: clsx_default(classes5.dotsLoader, className), ...others, ref, children: [
      /* @__PURE__ */ (0, import_jsx_runtime49.jsx)("span", { className: classes5.dot }),
      /* @__PURE__ */ (0, import_jsx_runtime49.jsx)("span", { className: classes5.dot }),
      /* @__PURE__ */ (0, import_jsx_runtime49.jsx)("span", { className: classes5.dot })
    ] }));
    Dots.displayName = "@mantine/core/Dots";
  }
});

// node_modules/@mantine/core/esm/components/Loader/loaders/Oval.mjs
var import_jsx_runtime50, import_react79, Oval;
var init_Oval = __esm({
  "node_modules/@mantine/core/esm/components/Loader/loaders/Oval.mjs"() {
    "use client";
    import_jsx_runtime50 = __toESM(require_jsx_runtime(), 1);
    import_react79 = __toESM(require_react(), 1);
    init_clsx();
    init_Box();
    init_Loader_module_css();
    Oval = (0, import_react79.forwardRef)(({ className, ...others }, ref) => /* @__PURE__ */ (0, import_jsx_runtime50.jsx)(Box, { component: "span", className: clsx_default(classes5.ovalLoader, className), ...others, ref }));
    Oval.displayName = "@mantine/core/Oval";
  }
});

// node_modules/@mantine/core/esm/components/Loader/Loader.mjs
var import_jsx_runtime51, import_react80, defaultLoaders, defaultProps8, varsResolver3, Loader;
var init_Loader = __esm({
  "node_modules/@mantine/core/esm/components/Loader/Loader.mjs"() {
    "use client";
    import_jsx_runtime51 = __toESM(require_jsx_runtime(), 1);
    import_react80 = __toESM(require_react(), 1);
    init_get_size();
    init_create_vars_resolver();
    init_clsx();
    init_get_theme_color();
    init_use_props();
    init_use_styles();
    init_Box();
    init_factory();
    init_Bars();
    init_Dots();
    init_Oval();
    init_Loader_module_css();
    defaultLoaders = {
      bars: Bars,
      oval: Oval,
      dots: Dots
    };
    defaultProps8 = {
      loaders: defaultLoaders,
      type: "oval"
    };
    varsResolver3 = createVarsResolver((theme2, { size: size4, color: color2 }) => ({
      root: {
        "--loader-size": getSize(size4, "loader-size"),
        "--loader-color": color2 ? getThemeColor(color2, theme2) : void 0
      }
    }));
    Loader = factory((_props, ref) => {
      const props = useProps("Loader", defaultProps8, _props);
      const {
        size: size4,
        color: color2,
        type: type2,
        vars,
        className,
        style,
        classNames,
        styles,
        unstyled,
        loaders,
        variant,
        children,
        ...others
      } = props;
      const getStyles = useStyles({
        name: "Loader",
        props,
        classes: classes5,
        className,
        style,
        classNames,
        styles,
        unstyled,
        vars,
        varsResolver: varsResolver3
      });
      if (children) {
        return /* @__PURE__ */ (0, import_jsx_runtime51.jsx)(Box, { ...getStyles("root"), ref, ...others, children });
      }
      return /* @__PURE__ */ (0, import_jsx_runtime51.jsx)(
        Box,
        {
          ...getStyles("root"),
          ref,
          component: loaders[type2],
          variant,
          size: size4,
          ...others
        }
      );
    });
    Loader.defaultLoaders = defaultLoaders;
    Loader.classes = classes5;
    Loader.displayName = "@mantine/core/Loader";
  }
});

// node_modules/@mantine/core/esm/components/ActionIcon/ActionIcon.module.css.mjs
var classes6;
var init_ActionIcon_module_css = __esm({
  "node_modules/@mantine/core/esm/components/ActionIcon/ActionIcon.module.css.mjs"() {
    "use client";
    classes6 = { "root": "m_8d3f4000", "icon": "m_8d3afb97", "loader": "m_302b9fb1", "group": "m_1a0f1b21" };
  }
});

// node_modules/@mantine/core/esm/components/ActionIcon/ActionIconGroup/ActionIconGroup.mjs
var import_jsx_runtime52, import_react81, defaultProps9, varsResolver4, ActionIconGroup;
var init_ActionIconGroup = __esm({
  "node_modules/@mantine/core/esm/components/ActionIcon/ActionIconGroup/ActionIconGroup.mjs"() {
    "use client";
    import_jsx_runtime52 = __toESM(require_jsx_runtime(), 1);
    init_rem();
    import_react81 = __toESM(require_react(), 1);
    init_create_vars_resolver();
    init_clsx();
    init_use_props();
    init_use_styles();
    init_Box();
    init_factory();
    init_ActionIcon_module_css();
    defaultProps9 = {
      orientation: "horizontal"
    };
    varsResolver4 = createVarsResolver((_2, { borderWidth }) => ({
      group: { "--ai-border-width": rem(borderWidth) }
    }));
    ActionIconGroup = factory((_props, ref) => {
      const props = useProps("ActionIconGroup", defaultProps9, _props);
      const {
        className,
        style,
        classNames,
        styles,
        unstyled,
        orientation,
        vars,
        borderWidth,
        variant,
        mod,
        ...others
      } = useProps("ActionIconGroup", defaultProps9, _props);
      const getStyles = useStyles({
        name: "ActionIconGroup",
        props,
        classes: classes6,
        className,
        style,
        classNames,
        styles,
        unstyled,
        vars,
        varsResolver: varsResolver4,
        rootSelector: "group"
      });
      return /* @__PURE__ */ (0, import_jsx_runtime52.jsx)(
        Box,
        {
          ...getStyles("group"),
          ref,
          variant,
          mod: [{ "data-orientation": orientation }, mod],
          role: "group",
          ...others
        }
      );
    });
    ActionIconGroup.classes = classes6;
    ActionIconGroup.displayName = "@mantine/core/ActionIconGroup";
  }
});

// node_modules/@mantine/core/esm/components/ActionIcon/ActionIcon.mjs
var import_jsx_runtime53, import_react82, defaultProps10, varsResolver5, ActionIcon;
var init_ActionIcon = __esm({
  "node_modules/@mantine/core/esm/components/ActionIcon/ActionIcon.mjs"() {
    "use client";
    import_jsx_runtime53 = __toESM(require_jsx_runtime(), 1);
    import_react82 = __toESM(require_react(), 1);
    init_get_size();
    init_create_vars_resolver();
    init_clsx();
    init_use_props();
    init_use_styles();
    init_Box();
    init_polymorphic_factory();
    init_Loader();
    init_Transition();
    init_UnstyledButton();
    init_ActionIconGroup();
    init_ActionIcon_module_css();
    defaultProps10 = {};
    varsResolver5 = createVarsResolver(
      (theme2, { size: size4, radius, variant, gradient, color: color2, autoContrast }) => {
        const colors = theme2.variantColorResolver({
          color: color2 || theme2.primaryColor,
          theme: theme2,
          gradient,
          variant: variant || "filled",
          autoContrast
        });
        return {
          root: {
            "--ai-size": getSize(size4, "ai-size"),
            "--ai-radius": radius === void 0 ? void 0 : getRadius(radius),
            "--ai-bg": color2 || variant ? colors.background : void 0,
            "--ai-hover": color2 || variant ? colors.hover : void 0,
            "--ai-hover-color": color2 || variant ? colors.hoverColor : void 0,
            "--ai-color": colors.color,
            "--ai-bd": color2 || variant ? colors.border : void 0
          }
        };
      }
    );
    ActionIcon = polymorphicFactory((_props, ref) => {
      const props = useProps("ActionIcon", defaultProps10, _props);
      const {
        className,
        unstyled,
        variant,
        classNames,
        styles,
        style,
        loading,
        loaderProps,
        size: size4,
        color: color2,
        radius,
        __staticSelector,
        gradient,
        vars,
        children,
        disabled,
        "data-disabled": dataDisabled,
        autoContrast,
        mod,
        ...others
      } = props;
      const getStyles = useStyles({
        name: ["ActionIcon", __staticSelector],
        props,
        className,
        style,
        classes: classes6,
        classNames,
        styles,
        unstyled,
        vars,
        varsResolver: varsResolver5
      });
      return /* @__PURE__ */ (0, import_jsx_runtime53.jsxs)(
        UnstyledButton,
        {
          ...getStyles("root", { active: !disabled && !loading && !dataDisabled }),
          ...others,
          unstyled,
          variant,
          size: size4,
          disabled: disabled || loading,
          ref,
          mod: [{ loading, disabled: disabled || dataDisabled }, mod],
          children: [
            /* @__PURE__ */ (0, import_jsx_runtime53.jsx)(Transition, { mounted: !!loading, transition: "slide-down", duration: 150, children: (transitionStyles) => /* @__PURE__ */ (0, import_jsx_runtime53.jsx)(Box, { component: "span", ...getStyles("loader", { style: transitionStyles }), "aria-hidden": true, children: /* @__PURE__ */ (0, import_jsx_runtime53.jsx)(Loader, { color: "var(--ai-color)", size: "calc(var(--ai-size) * 0.55)", ...loaderProps }) }) }),
            /* @__PURE__ */ (0, import_jsx_runtime53.jsx)(Box, { component: "span", mod: { loading }, ...getStyles("icon"), children })
          ]
        }
      );
    });
    ActionIcon.classes = classes6;
    ActionIcon.displayName = "@mantine/core/ActionIcon";
    ActionIcon.Group = ActionIconGroup;
  }
});

// node_modules/@mantine/core/esm/components/CloseButton/CloseIcon.mjs
var import_jsx_runtime54, import_react83, CloseIcon;
var init_CloseIcon = __esm({
  "node_modules/@mantine/core/esm/components/CloseButton/CloseIcon.mjs"() {
    "use client";
    import_jsx_runtime54 = __toESM(require_jsx_runtime(), 1);
    import_react83 = __toESM(require_react(), 1);
    CloseIcon = (0, import_react83.forwardRef)(
      ({ size: size4 = "var(--cb-icon-size, 70%)", style, ...others }, ref) => /* @__PURE__ */ (0, import_jsx_runtime54.jsx)(
        "svg",
        {
          viewBox: "0 0 15 15",
          fill: "none",
          xmlns: "http://www.w3.org/2000/svg",
          style: { ...style, width: size4, height: size4 },
          ref,
          ...others,
          children: /* @__PURE__ */ (0, import_jsx_runtime54.jsx)(
            "path",
            {
              d: "M11.7816 4.03157C12.0062 3.80702 12.0062 3.44295 11.7816 3.2184C11.5571 2.99385 11.193 2.99385 10.9685 3.2184L7.50005 6.68682L4.03164 3.2184C3.80708 2.99385 3.44301 2.99385 3.21846 3.2184C2.99391 3.44295 2.99391 3.80702 3.21846 4.03157L6.68688 7.49999L3.21846 10.9684C2.99391 11.193 2.99391 11.557 3.21846 11.7816C3.44301 12.0061 3.80708 12.0061 4.03164 11.7816L7.50005 8.31316L10.9685 11.7816C11.193 12.0061 11.5571 12.0061 11.7816 11.7816C12.0062 11.557 12.0062 11.193 11.7816 10.9684L8.31322 7.49999L11.7816 4.03157Z",
              fill: "currentColor",
              fillRule: "evenodd",
              clipRule: "evenodd"
            }
          )
        }
      )
    );
    CloseIcon.displayName = "@mantine/core/CloseIcon";
  }
});

// node_modules/@mantine/core/esm/components/CloseButton/CloseButton.module.css.mjs
var classes7;
var init_CloseButton_module_css = __esm({
  "node_modules/@mantine/core/esm/components/CloseButton/CloseButton.module.css.mjs"() {
    "use client";
    classes7 = { "root": "m_86a44da5", "root--subtle": "m_220c80f2" };
  }
});

// node_modules/@mantine/core/esm/components/CloseButton/CloseButton.mjs
var import_jsx_runtime55, import_react84, defaultProps11, varsResolver6, CloseButton;
var init_CloseButton = __esm({
  "node_modules/@mantine/core/esm/components/CloseButton/CloseButton.mjs"() {
    "use client";
    import_jsx_runtime55 = __toESM(require_jsx_runtime(), 1);
    init_rem();
    import_react84 = __toESM(require_react(), 1);
    init_get_size();
    init_create_vars_resolver();
    init_clsx();
    init_use_props();
    init_use_styles();
    init_polymorphic_factory();
    init_UnstyledButton();
    init_CloseIcon();
    init_CloseButton_module_css();
    defaultProps11 = {
      variant: "subtle"
    };
    varsResolver6 = createVarsResolver((_2, { size: size4, radius, iconSize }) => ({
      root: {
        "--cb-size": getSize(size4, "cb-size"),
        "--cb-radius": radius === void 0 ? void 0 : getRadius(radius),
        "--cb-icon-size": rem(iconSize)
      }
    }));
    CloseButton = polymorphicFactory((_props, ref) => {
      const props = useProps("CloseButton", defaultProps11, _props);
      const {
        iconSize,
        children,
        vars,
        radius,
        className,
        classNames,
        style,
        styles,
        unstyled,
        "data-disabled": dataDisabled,
        disabled,
        variant,
        icon: icon2,
        mod,
        ...others
      } = props;
      const getStyles = useStyles({
        name: "CloseButton",
        props,
        className,
        style,
        classes: classes7,
        classNames,
        styles,
        unstyled,
        vars,
        varsResolver: varsResolver6
      });
      return /* @__PURE__ */ (0, import_jsx_runtime55.jsxs)(
        UnstyledButton,
        {
          ref,
          ...others,
          unstyled,
          variant,
          disabled,
          mod: [{ disabled: disabled || dataDisabled }, mod],
          ...getStyles("root", { variant, active: !disabled && !dataDisabled }),
          children: [
            icon2 || /* @__PURE__ */ (0, import_jsx_runtime55.jsx)(CloseIcon, {}),
            children
          ]
        }
      );
    });
    CloseButton.classes = classes7;
    CloseButton.displayName = "@mantine/core/CloseButton";
  }
});

// node_modules/@mantine/core/esm/components/Group/filter-falsy-children/filter-falsy-children.mjs
function filterFalsyChildren(children) {
  return import_react85.Children.toArray(children).filter(Boolean);
}
var import_react85;
var init_filter_falsy_children = __esm({
  "node_modules/@mantine/core/esm/components/Group/filter-falsy-children/filter-falsy-children.mjs"() {
    "use client";
    import_react85 = __toESM(require_react(), 1);
  }
});

// node_modules/@mantine/core/esm/components/Group/Group.module.css.mjs
var classes8;
var init_Group_module_css = __esm({
  "node_modules/@mantine/core/esm/components/Group/Group.module.css.mjs"() {
    "use client";
    classes8 = { "root": "m_4081bf90" };
  }
});

// node_modules/@mantine/core/esm/components/Group/Group.mjs
var import_jsx_runtime56, import_react86, defaultProps12, varsResolver7, Group;
var init_Group = __esm({
  "node_modules/@mantine/core/esm/components/Group/Group.mjs"() {
    "use client";
    import_jsx_runtime56 = __toESM(require_jsx_runtime(), 1);
    import_react86 = __toESM(require_react(), 1);
    init_get_size();
    init_create_vars_resolver();
    init_clsx();
    init_use_props();
    init_use_styles();
    init_Box();
    init_factory();
    init_filter_falsy_children();
    init_Group_module_css();
    defaultProps12 = {
      preventGrowOverflow: true,
      gap: "md",
      align: "center",
      justify: "flex-start",
      wrap: "wrap"
    };
    varsResolver7 = createVarsResolver(
      (_2, { grow, preventGrowOverflow, gap, align, justify, wrap: wrap2 }, { childWidth }) => ({
        root: {
          "--group-child-width": grow && preventGrowOverflow ? childWidth : void 0,
          "--group-gap": getSpacing(gap),
          "--group-align": align,
          "--group-justify": justify,
          "--group-wrap": wrap2
        }
      })
    );
    Group = factory((_props, ref) => {
      const props = useProps("Group", defaultProps12, _props);
      const {
        classNames,
        className,
        style,
        styles,
        unstyled,
        children,
        gap,
        align,
        justify,
        wrap: wrap2,
        grow,
        preventGrowOverflow,
        vars,
        variant,
        __size,
        mod,
        ...others
      } = props;
      const filteredChildren = filterFalsyChildren(children);
      const childrenCount = filteredChildren.length;
      const resolvedGap = getSpacing(gap ?? "md");
      const childWidth = `calc(${100 / childrenCount}% - (${resolvedGap} - ${resolvedGap} / ${childrenCount}))`;
      const stylesCtx = { childWidth };
      const getStyles = useStyles({
        name: "Group",
        props,
        stylesCtx,
        className,
        style,
        classes: classes8,
        classNames,
        styles,
        unstyled,
        vars,
        varsResolver: varsResolver7
      });
      return /* @__PURE__ */ (0, import_jsx_runtime56.jsx)(
        Box,
        {
          ...getStyles("root"),
          ref,
          variant,
          mod: [{ grow }, mod],
          size: __size,
          ...others,
          children: filteredChildren
        }
      );
    });
    Group.classes = classes8;
    Group.displayName = "@mantine/core/Group";
  }
});

// node_modules/@mantine/core/esm/components/Overlay/Overlay.module.css.mjs
var classes9;
var init_Overlay_module_css = __esm({
  "node_modules/@mantine/core/esm/components/Overlay/Overlay.module.css.mjs"() {
    "use client";
    classes9 = { "root": "m_9814e45f" };
  }
});

// node_modules/@mantine/core/esm/components/Overlay/Overlay.mjs
var import_jsx_runtime57, import_react87, defaultProps13, varsResolver8, Overlay;
var init_Overlay = __esm({
  "node_modules/@mantine/core/esm/components/Overlay/Overlay.mjs"() {
    "use client";
    import_jsx_runtime57 = __toESM(require_jsx_runtime(), 1);
    init_rem();
    import_react87 = __toESM(require_react(), 1);
    init_get_default_z_index();
    init_get_size();
    init_create_vars_resolver();
    init_clsx();
    init_rgba();
    init_use_props();
    init_use_styles();
    init_Box();
    init_polymorphic_factory();
    init_Overlay_module_css();
    defaultProps13 = {
      zIndex: getDefaultZIndex("modal")
    };
    varsResolver8 = createVarsResolver(
      (_2, { gradient, color: color2, backgroundOpacity, blur, radius, zIndex }) => ({
        root: {
          "--overlay-bg": gradient || (color2 !== void 0 || backgroundOpacity !== void 0) && rgba(color2 || "#000", backgroundOpacity ?? 0.6) || void 0,
          "--overlay-filter": blur ? `blur(${rem(blur)})` : void 0,
          "--overlay-radius": radius === void 0 ? void 0 : getRadius(radius),
          "--overlay-z-index": zIndex?.toString()
        }
      })
    );
    Overlay = polymorphicFactory((_props, ref) => {
      const props = useProps("Overlay", defaultProps13, _props);
      const {
        classNames,
        className,
        style,
        styles,
        unstyled,
        vars,
        fixed,
        center,
        children,
        radius,
        zIndex,
        gradient,
        blur,
        color: color2,
        backgroundOpacity,
        mod,
        ...others
      } = props;
      const getStyles = useStyles({
        name: "Overlay",
        props,
        classes: classes9,
        className,
        style,
        classNames,
        styles,
        unstyled,
        vars,
        varsResolver: varsResolver8
      });
      return /* @__PURE__ */ (0, import_jsx_runtime57.jsx)(Box, { ref, ...getStyles("root"), mod: [{ center, fixed }, mod], ...others, children });
    });
    Overlay.classes = classes9;
    Overlay.displayName = "@mantine/core/Overlay";
  }
});

// node_modules/@mantine/core/esm/components/ModalBase/ModalBase.context.mjs
var import_react88, import_jsx_runtime58, ModalBaseProvider, useModalBaseContext;
var init_ModalBase_context = __esm({
  "node_modules/@mantine/core/esm/components/ModalBase/ModalBase.context.mjs"() {
    "use client";
    import_react88 = __toESM(require_react(), 1);
    init_create_safe_context();
    import_jsx_runtime58 = __toESM(require_jsx_runtime(), 1);
    init_clsx();
    [ModalBaseProvider, useModalBaseContext] = createSafeContext(
      "ModalBase component was not found in tree"
    );
  }
});

// node_modules/@mantine/core/esm/components/ModalBase/use-lock-scroll.mjs
function useLockScroll({ opened, transitionDuration }) {
  const [shouldLockScroll, setShouldLockScroll] = (0, import_react89.useState)(opened);
  const timeout = (0, import_react89.useRef)();
  const reduceMotion = useReducedMotion();
  const _transitionDuration = reduceMotion ? 0 : transitionDuration;
  (0, import_react89.useEffect)(() => {
    if (opened) {
      setShouldLockScroll(true);
      window.clearTimeout(timeout.current);
    } else if (_transitionDuration === 0) {
      setShouldLockScroll(false);
    } else {
      timeout.current = window.setTimeout(() => setShouldLockScroll(false), _transitionDuration);
    }
    return () => window.clearTimeout(timeout.current);
  }, [opened, _transitionDuration]);
  return shouldLockScroll;
}
var import_react89;
var init_use_lock_scroll = __esm({
  "node_modules/@mantine/core/esm/components/ModalBase/use-lock-scroll.mjs"() {
    "use client";
    import_react89 = __toESM(require_react(), 1);
    init_esm();
  }
});

// node_modules/@mantine/core/esm/components/ModalBase/use-modal.mjs
function useModal({
  id: id3,
  transitionProps,
  opened,
  trapFocus,
  closeOnEscape: closeOnEscape2,
  onClose,
  returnFocus
}) {
  const _id = useId(id3);
  const [titleMounted, setTitleMounted] = (0, import_react90.useState)(false);
  const [bodyMounted, setBodyMounted] = (0, import_react90.useState)(false);
  const transitionDuration = typeof transitionProps?.duration === "number" ? transitionProps?.duration : 200;
  const shouldLockScroll = useLockScroll({ opened, transitionDuration });
  useWindowEvent(
    "keydown",
    (event) => {
      if (event.key === "Escape" && closeOnEscape2 && opened) {
        const shouldTrigger = event.target?.getAttribute("data-mantine-stop-propagation") !== "true";
        shouldTrigger && onClose();
      }
    },
    { capture: true }
  );
  useFocusReturn({ opened, shouldReturnFocus: trapFocus && returnFocus });
  return {
    _id,
    titleMounted,
    bodyMounted,
    shouldLockScroll,
    setTitleMounted,
    setBodyMounted
  };
}
var import_react90;
var init_use_modal = __esm({
  "node_modules/@mantine/core/esm/components/ModalBase/use-modal.mjs"() {
    "use client";
    import_react90 = __toESM(require_react(), 1);
    init_esm();
    init_use_lock_scroll();
  }
});

// node_modules/@mantine/core/esm/components/ModalBase/ModalBase.mjs
var import_jsx_runtime59, import_react91, import_react_remove_scroll, ModalBase;
var init_ModalBase = __esm({
  "node_modules/@mantine/core/esm/components/ModalBase/ModalBase.mjs"() {
    "use client";
    import_jsx_runtime59 = __toESM(require_jsx_runtime(), 1);
    import_react91 = __toESM(require_react(), 1);
    import_react_remove_scroll = __toESM(require_es56(), 1);
    init_get_default_z_index();
    init_get_size();
    init_clsx();
    init_Box();
    init_OptionalPortal();
    init_ModalBase_context();
    init_use_modal();
    ModalBase = (0, import_react91.forwardRef)(
      ({
        keepMounted,
        opened,
        onClose,
        id: id3,
        transitionProps,
        onExitTransitionEnd,
        onEnterTransitionEnd,
        trapFocus,
        closeOnEscape: closeOnEscape2,
        returnFocus,
        closeOnClickOutside,
        withinPortal,
        portalProps,
        lockScroll,
        children,
        zIndex,
        shadow,
        padding,
        __vars,
        unstyled,
        removeScrollProps,
        ...others
      }, ref) => {
        const { _id, titleMounted, bodyMounted, shouldLockScroll, setTitleMounted, setBodyMounted } = useModal({ id: id3, transitionProps, opened, trapFocus, closeOnEscape: closeOnEscape2, onClose, returnFocus });
        const { key: removeScrollKey, ...otherRemoveScrollProps } = removeScrollProps || {};
        return /* @__PURE__ */ (0, import_jsx_runtime59.jsx)(OptionalPortal, { ...portalProps, withinPortal, children: /* @__PURE__ */ (0, import_jsx_runtime59.jsx)(
          ModalBaseProvider,
          {
            value: {
              opened,
              onClose,
              closeOnClickOutside,
              onExitTransitionEnd,
              onEnterTransitionEnd,
              transitionProps: { ...transitionProps, keepMounted },
              getTitleId: () => `${_id}-title`,
              getBodyId: () => `${_id}-body`,
              titleMounted,
              bodyMounted,
              setTitleMounted,
              setBodyMounted,
              trapFocus,
              closeOnEscape: closeOnEscape2,
              zIndex,
              unstyled
            },
            children: /* @__PURE__ */ (0, import_jsx_runtime59.jsx)(
              import_react_remove_scroll.RemoveScroll,
              {
                enabled: shouldLockScroll && lockScroll,
                ...otherRemoveScrollProps,
                children: /* @__PURE__ */ (0, import_jsx_runtime59.jsx)(
                  Box,
                  {
                    ref,
                    ...others,
                    __vars: {
                      ...__vars,
                      "--mb-z-index": (zIndex || getDefaultZIndex("modal")).toString(),
                      "--mb-shadow": getShadow(shadow),
                      "--mb-padding": getSpacing(padding)
                    },
                    children
                  }
                )
              },
              removeScrollKey
            )
          }
        ) });
      }
    );
    ModalBase.displayName = "@mantine/core/ModalBase";
  }
});

// node_modules/@mantine/core/esm/components/ModalBase/use-modal-body-id.mjs
function useModalBodyId() {
  const ctx = useModalBaseContext();
  (0, import_react92.useEffect)(() => {
    ctx.setBodyMounted(true);
    return () => ctx.setBodyMounted(false);
  }, []);
  return ctx.getBodyId();
}
var import_react92;
var init_use_modal_body_id = __esm({
  "node_modules/@mantine/core/esm/components/ModalBase/use-modal-body-id.mjs"() {
    "use client";
    import_react92 = __toESM(require_react(), 1);
    init_ModalBase_context();
  }
});

// node_modules/@mantine/core/esm/components/ModalBase/ModalBase.module.css.mjs
var classes10;
var init_ModalBase_module_css = __esm({
  "node_modules/@mantine/core/esm/components/ModalBase/ModalBase.module.css.mjs"() {
    "use client";
    classes10 = { "title": "m_615af6c9", "header": "m_b5489c3c", "inner": "m_60c222c7", "content": "m_fd1ab0aa", "close": "m_606cb269", "body": "m_5df29311" };
  }
});

// node_modules/@mantine/core/esm/components/ModalBase/ModalBaseBody.mjs
var import_jsx_runtime60, import_react93, ModalBaseBody;
var init_ModalBaseBody = __esm({
  "node_modules/@mantine/core/esm/components/ModalBase/ModalBaseBody.mjs"() {
    "use client";
    import_jsx_runtime60 = __toESM(require_jsx_runtime(), 1);
    import_react93 = __toESM(require_react(), 1);
    init_clsx();
    init_Box();
    init_ModalBase_context();
    init_use_modal_body_id();
    init_ModalBase_module_css();
    ModalBaseBody = (0, import_react93.forwardRef)(
      ({ className, ...others }, ref) => {
        const bodyId = useModalBodyId();
        const ctx = useModalBaseContext();
        return /* @__PURE__ */ (0, import_jsx_runtime60.jsx)(
          Box,
          {
            ref,
            ...others,
            id: bodyId,
            className: clsx_default({ [classes10.body]: !ctx.unstyled }, className)
          }
        );
      }
    );
    ModalBaseBody.displayName = "@mantine/core/ModalBaseBody";
  }
});

// node_modules/@mantine/core/esm/components/ModalBase/ModalBaseCloseButton.mjs
var import_jsx_runtime61, import_react94, ModalBaseCloseButton;
var init_ModalBaseCloseButton = __esm({
  "node_modules/@mantine/core/esm/components/ModalBase/ModalBaseCloseButton.mjs"() {
    "use client";
    import_jsx_runtime61 = __toESM(require_jsx_runtime(), 1);
    import_react94 = __toESM(require_react(), 1);
    init_clsx();
    init_CloseButton();
    init_ModalBase_context();
    init_ModalBase_module_css();
    ModalBaseCloseButton = (0, import_react94.forwardRef)(
      ({ className, onClick, ...others }, ref) => {
        const ctx = useModalBaseContext();
        return /* @__PURE__ */ (0, import_jsx_runtime61.jsx)(
          CloseButton,
          {
            ref,
            ...others,
            onClick: (event) => {
              ctx.onClose();
              onClick?.(event);
            },
            className: clsx_default({ [classes10.close]: !ctx.unstyled }, className),
            unstyled: ctx.unstyled
          }
        );
      }
    );
    ModalBaseCloseButton.displayName = "@mantine/core/ModalBaseCloseButton";
  }
});

// node_modules/@mantine/core/esm/components/ModalBase/ModalBaseContent.mjs
var import_jsx_runtime62, import_react95, ModalBaseContent;
var init_ModalBaseContent = __esm({
  "node_modules/@mantine/core/esm/components/ModalBase/ModalBaseContent.mjs"() {
    "use client";
    import_jsx_runtime62 = __toESM(require_jsx_runtime(), 1);
    import_react95 = __toESM(require_react(), 1);
    init_clsx();
    init_FocusTrap();
    init_Paper();
    init_Transition();
    init_ModalBase_context();
    init_ModalBase_module_css();
    ModalBaseContent = (0, import_react95.forwardRef)(
      ({ transitionProps, className, innerProps, onKeyDown, style, ...others }, ref) => {
        const ctx = useModalBaseContext();
        return /* @__PURE__ */ (0, import_jsx_runtime62.jsx)(
          Transition,
          {
            mounted: ctx.opened,
            transition: "pop",
            ...ctx.transitionProps,
            onExited: () => {
              ctx.onExitTransitionEnd?.();
              ctx.transitionProps?.onExited?.();
            },
            onEntered: () => {
              ctx.onEnterTransitionEnd?.();
              ctx.transitionProps?.onEntered?.();
            },
            ...transitionProps,
            children: (transitionStyles) => /* @__PURE__ */ (0, import_jsx_runtime62.jsx)(
              "div",
              {
                ...innerProps,
                className: clsx_default({ [classes10.inner]: !ctx.unstyled }, innerProps.className),
                children: /* @__PURE__ */ (0, import_jsx_runtime62.jsx)(FocusTrap, { active: ctx.opened && ctx.trapFocus, innerRef: ref, children: /* @__PURE__ */ (0, import_jsx_runtime62.jsx)(
                  Paper,
                  {
                    ...others,
                    component: "section",
                    role: "dialog",
                    tabIndex: -1,
                    "aria-modal": true,
                    "aria-describedby": ctx.bodyMounted ? ctx.getBodyId() : void 0,
                    "aria-labelledby": ctx.titleMounted ? ctx.getTitleId() : void 0,
                    style: [style, transitionStyles],
                    className: clsx_default({ [classes10.content]: !ctx.unstyled }, className),
                    unstyled: ctx.unstyled,
                    children: others.children
                  }
                ) })
              }
            )
          }
        );
      }
    );
    ModalBaseContent.displayName = "@mantine/core/ModalBaseContent";
  }
});

// node_modules/@mantine/core/esm/components/ModalBase/ModalBaseHeader.mjs
var import_jsx_runtime63, import_react96, ModalBaseHeader;
var init_ModalBaseHeader = __esm({
  "node_modules/@mantine/core/esm/components/ModalBase/ModalBaseHeader.mjs"() {
    "use client";
    import_jsx_runtime63 = __toESM(require_jsx_runtime(), 1);
    import_react96 = __toESM(require_react(), 1);
    init_clsx();
    init_Box();
    init_ModalBase_context();
    init_ModalBase_module_css();
    ModalBaseHeader = (0, import_react96.forwardRef)(
      ({ className, ...others }, ref) => {
        const ctx = useModalBaseContext();
        return /* @__PURE__ */ (0, import_jsx_runtime63.jsx)(
          Box,
          {
            component: "header",
            ref,
            className: clsx_default({ [classes10.header]: !ctx.unstyled }, className),
            ...others
          }
        );
      }
    );
    ModalBaseHeader.displayName = "@mantine/core/ModalBaseHeader";
  }
});

// node_modules/@mantine/core/esm/components/ModalBase/use-modal-transition.mjs
function useModalTransition(transitionOverride) {
  const ctx = useModalBaseContext();
  return { ...DEFAULT_TRANSITION, ...ctx.transitionProps, ...transitionOverride };
}
var DEFAULT_TRANSITION;
var init_use_modal_transition = __esm({
  "node_modules/@mantine/core/esm/components/ModalBase/use-modal-transition.mjs"() {
    "use client";
    init_ModalBase_context();
    DEFAULT_TRANSITION = {
      duration: 200,
      timingFunction: "ease",
      transition: "fade"
    };
  }
});

// node_modules/@mantine/core/esm/components/ModalBase/ModalBaseOverlay.mjs
var import_jsx_runtime64, import_react97, ModalBaseOverlay;
var init_ModalBaseOverlay = __esm({
  "node_modules/@mantine/core/esm/components/ModalBase/ModalBaseOverlay.mjs"() {
    "use client";
    import_jsx_runtime64 = __toESM(require_jsx_runtime(), 1);
    import_react97 = __toESM(require_react(), 1);
    init_Overlay();
    init_Transition();
    init_ModalBase_context();
    init_use_modal_transition();
    ModalBaseOverlay = (0, import_react97.forwardRef)(
      ({ onClick, transitionProps, style, visible: visible2, ...others }, ref) => {
        const ctx = useModalBaseContext();
        const transition = useModalTransition(transitionProps);
        return /* @__PURE__ */ (0, import_jsx_runtime64.jsx)(
          Transition,
          {
            mounted: visible2 !== void 0 ? visible2 : ctx.opened,
            ...transition,
            transition: "fade",
            children: (transitionStyles) => /* @__PURE__ */ (0, import_jsx_runtime64.jsx)(
              Overlay,
              {
                ref,
                fixed: true,
                style: [style, transitionStyles],
                zIndex: ctx.zIndex,
                unstyled: ctx.unstyled,
                onClick: (event) => {
                  onClick?.(event);
                  ctx.closeOnClickOutside && ctx.onClose();
                },
                ...others
              }
            )
          }
        );
      }
    );
    ModalBaseOverlay.displayName = "@mantine/core/ModalBaseOverlay";
  }
});

// node_modules/@mantine/core/esm/components/ModalBase/use-modal-title-id.mjs
function useModalTitle() {
  const ctx = useModalBaseContext();
  (0, import_react98.useEffect)(() => {
    ctx.setTitleMounted(true);
    return () => ctx.setTitleMounted(false);
  }, []);
  return ctx.getTitleId();
}
var import_react98;
var init_use_modal_title_id = __esm({
  "node_modules/@mantine/core/esm/components/ModalBase/use-modal-title-id.mjs"() {
    "use client";
    import_react98 = __toESM(require_react(), 1);
    init_ModalBase_context();
  }
});

// node_modules/@mantine/core/esm/components/ModalBase/ModalBaseTitle.mjs
var import_jsx_runtime65, import_react99, ModalBaseTitle;
var init_ModalBaseTitle = __esm({
  "node_modules/@mantine/core/esm/components/ModalBase/ModalBaseTitle.mjs"() {
    "use client";
    import_jsx_runtime65 = __toESM(require_jsx_runtime(), 1);
    import_react99 = __toESM(require_react(), 1);
    init_clsx();
    init_Box();
    init_ModalBase_context();
    init_use_modal_title_id();
    init_ModalBase_module_css();
    ModalBaseTitle = (0, import_react99.forwardRef)(
      ({ className, ...others }, ref) => {
        const id3 = useModalTitle();
        const ctx = useModalBaseContext();
        return /* @__PURE__ */ (0, import_jsx_runtime65.jsx)(
          Box,
          {
            component: "h2",
            ref,
            className: clsx_default({ [classes10.title]: !ctx.unstyled }, className),
            ...others,
            id: id3
          }
        );
      }
    );
    ModalBaseTitle.displayName = "@mantine/core/ModalBaseTitle";
  }
});

// node_modules/@mantine/core/esm/components/ModalBase/NativeScrollArea.mjs
function NativeScrollArea({ children }) {
  return /* @__PURE__ */ (0, import_jsx_runtime66.jsx)(import_jsx_runtime66.Fragment, { children });
}
var import_jsx_runtime66;
var init_NativeScrollArea = __esm({
  "node_modules/@mantine/core/esm/components/ModalBase/NativeScrollArea.mjs"() {
    "use client";
    import_jsx_runtime66 = __toESM(require_jsx_runtime(), 1);
  }
});

// node_modules/@mantine/core/esm/components/Input/InputWrapper.context.mjs
var import_react100, import_jsx_runtime67, InputWrapperProvider, useInputWrapperContext;
var init_InputWrapper_context = __esm({
  "node_modules/@mantine/core/esm/components/Input/InputWrapper.context.mjs"() {
    "use client";
    import_react100 = __toESM(require_react(), 1);
    import_jsx_runtime67 = __toESM(require_jsx_runtime(), 1);
    init_create_optional_context();
    init_clsx();
    [InputWrapperProvider, useInputWrapperContext] = createOptionalContext({
      offsetBottom: false,
      offsetTop: false,
      describedBy: void 0,
      getStyles: null,
      inputId: void 0,
      labelId: void 0
    });
  }
});

// node_modules/@mantine/core/esm/components/Input/Input.module.css.mjs
var classes11;
var init_Input_module_css = __esm({
  "node_modules/@mantine/core/esm/components/Input/Input.module.css.mjs"() {
    "use client";
    classes11 = { "wrapper": "m_6c018570", "input": "m_8fb7ebe7", "section": "m_82577fc2", "placeholder": "m_88bacfd0", "root": "m_46b77525", "label": "m_8fdc1311", "required": "m_78a94662", "error": "m_8f816625", "description": "m_fe47ce59" };
  }
});

// node_modules/@mantine/core/esm/components/Input/InputDescription/InputDescription.mjs
var import_jsx_runtime68, import_react101, defaultProps14, varsResolver9, InputDescription;
var init_InputDescription = __esm({
  "node_modules/@mantine/core/esm/components/Input/InputDescription/InputDescription.mjs"() {
    "use client";
    import_jsx_runtime68 = __toESM(require_jsx_runtime(), 1);
    init_rem();
    import_react101 = __toESM(require_react(), 1);
    init_get_size();
    init_create_vars_resolver();
    init_clsx();
    init_use_props();
    init_use_styles();
    init_Box();
    init_factory();
    init_InputWrapper_context();
    init_Input_module_css();
    defaultProps14 = {};
    varsResolver9 = createVarsResolver((_2, { size: size4 }) => ({
      description: {
        "--input-description-size": size4 === void 0 ? void 0 : `calc(${getFontSize(size4)} - ${rem(2)})`
      }
    }));
    InputDescription = factory((_props, ref) => {
      const props = useProps("InputDescription", defaultProps14, _props);
      const {
        classNames,
        className,
        style,
        styles,
        unstyled,
        vars,
        size: size4,
        __staticSelector,
        __inheritStyles = true,
        variant,
        ...others
      } = useProps("InputDescription", defaultProps14, props);
      const ctx = useInputWrapperContext();
      const _getStyles = useStyles({
        name: ["InputWrapper", __staticSelector],
        props,
        classes: classes11,
        className,
        style,
        classNames,
        styles,
        unstyled,
        rootSelector: "description",
        vars,
        varsResolver: varsResolver9
      });
      const getStyles = __inheritStyles && ctx?.getStyles || _getStyles;
      return /* @__PURE__ */ (0, import_jsx_runtime68.jsx)(
        Box,
        {
          component: "p",
          ref,
          variant,
          size: size4,
          ...getStyles("description", ctx?.getStyles ? { className, style } : void 0),
          ...others
        }
      );
    });
    InputDescription.classes = classes11;
    InputDescription.displayName = "@mantine/core/InputDescription";
  }
});

// node_modules/@mantine/core/esm/components/Input/InputError/InputError.mjs
var import_jsx_runtime69, import_react102, defaultProps15, varsResolver10, InputError;
var init_InputError = __esm({
  "node_modules/@mantine/core/esm/components/Input/InputError/InputError.mjs"() {
    "use client";
    import_jsx_runtime69 = __toESM(require_jsx_runtime(), 1);
    init_rem();
    import_react102 = __toESM(require_react(), 1);
    init_get_size();
    init_create_vars_resolver();
    init_clsx();
    init_use_props();
    init_use_styles();
    init_Box();
    init_factory();
    init_InputWrapper_context();
    init_Input_module_css();
    defaultProps15 = {};
    varsResolver10 = createVarsResolver((_2, { size: size4 }) => ({
      error: {
        "--input-error-size": size4 === void 0 ? void 0 : `calc(${getFontSize(size4)} - ${rem(2)})`
      }
    }));
    InputError = factory((_props, ref) => {
      const props = useProps("InputError", defaultProps15, _props);
      const {
        classNames,
        className,
        style,
        styles,
        unstyled,
        vars,
        size: size4,
        __staticSelector,
        __inheritStyles = true,
        variant,
        ...others
      } = props;
      const _getStyles = useStyles({
        name: ["InputWrapper", __staticSelector],
        props,
        classes: classes11,
        className,
        style,
        classNames,
        styles,
        unstyled,
        rootSelector: "error",
        vars,
        varsResolver: varsResolver10
      });
      const ctx = useInputWrapperContext();
      const getStyles = __inheritStyles && ctx?.getStyles || _getStyles;
      return /* @__PURE__ */ (0, import_jsx_runtime69.jsx)(
        Box,
        {
          component: "p",
          ref,
          variant,
          size: size4,
          ...getStyles("error", ctx?.getStyles ? { className, style } : void 0),
          ...others
        }
      );
    });
    InputError.classes = classes11;
    InputError.displayName = "@mantine/core/InputError";
  }
});

// node_modules/@mantine/core/esm/components/Input/InputLabel/InputLabel.mjs
var import_jsx_runtime70, import_react103, defaultProps16, varsResolver11, InputLabel;
var init_InputLabel = __esm({
  "node_modules/@mantine/core/esm/components/Input/InputLabel/InputLabel.mjs"() {
    "use client";
    import_jsx_runtime70 = __toESM(require_jsx_runtime(), 1);
    import_react103 = __toESM(require_react(), 1);
    init_get_size();
    init_create_vars_resolver();
    init_clsx();
    init_use_props();
    init_use_styles();
    init_Box();
    init_factory();
    init_InputWrapper_context();
    init_Input_module_css();
    defaultProps16 = {
      labelElement: "label"
    };
    varsResolver11 = createVarsResolver((_2, { size: size4 }) => ({
      label: {
        "--input-label-size": getFontSize(size4),
        "--input-asterisk-color": void 0
      }
    }));
    InputLabel = factory((_props, ref) => {
      const props = useProps("InputLabel", defaultProps16, _props);
      const {
        classNames,
        className,
        style,
        styles,
        unstyled,
        vars,
        labelElement,
        size: size4,
        required: required2,
        htmlFor,
        onMouseDown,
        children,
        __staticSelector,
        variant,
        mod,
        ...others
      } = useProps("InputLabel", defaultProps16, props);
      const _getStyles = useStyles({
        name: ["InputWrapper", __staticSelector],
        props,
        classes: classes11,
        className,
        style,
        classNames,
        styles,
        unstyled,
        rootSelector: "label",
        vars,
        varsResolver: varsResolver11
      });
      const ctx = useInputWrapperContext();
      const getStyles = ctx?.getStyles || _getStyles;
      return /* @__PURE__ */ (0, import_jsx_runtime70.jsxs)(
        Box,
        {
          ...getStyles("label", ctx?.getStyles ? { className, style } : void 0),
          component: labelElement,
          variant,
          size: size4,
          ref,
          htmlFor: labelElement === "label" ? htmlFor : void 0,
          mod: [{ required: required2 }, mod],
          onMouseDown: (event) => {
            onMouseDown?.(event);
            if (!event.defaultPrevented && event.detail > 1) {
              event.preventDefault();
            }
          },
          ...others,
          children: [
            children,
            required2 && /* @__PURE__ */ (0, import_jsx_runtime70.jsx)("span", { ...getStyles("required"), "aria-hidden": true, children: " *" })
          ]
        }
      );
    });
    InputLabel.classes = classes11;
    InputLabel.displayName = "@mantine/core/InputLabel";
  }
});

// node_modules/@mantine/core/esm/components/Input/InputPlaceholder/InputPlaceholder.mjs
var import_jsx_runtime71, import_react104, defaultProps17, InputPlaceholder;
var init_InputPlaceholder = __esm({
  "node_modules/@mantine/core/esm/components/Input/InputPlaceholder/InputPlaceholder.mjs"() {
    "use client";
    import_jsx_runtime71 = __toESM(require_jsx_runtime(), 1);
    import_react104 = __toESM(require_react(), 1);
    init_clsx();
    init_use_props();
    init_use_styles();
    init_Box();
    init_factory();
    init_Input_module_css();
    defaultProps17 = {};
    InputPlaceholder = factory((_props, ref) => {
      const props = useProps("InputPlaceholder", defaultProps17, _props);
      const {
        classNames,
        className,
        style,
        styles,
        unstyled,
        vars,
        __staticSelector,
        variant,
        error,
        mod,
        ...others
      } = useProps("InputPlaceholder", defaultProps17, props);
      const getStyles = useStyles({
        name: ["InputPlaceholder", __staticSelector],
        props,
        classes: classes11,
        className,
        style,
        classNames,
        styles,
        unstyled,
        rootSelector: "placeholder"
      });
      return /* @__PURE__ */ (0, import_jsx_runtime71.jsx)(
        Box,
        {
          ...getStyles("placeholder"),
          mod: [{ error: !!error }, mod],
          component: "span",
          variant,
          ref,
          ...others
        }
      );
    });
    InputPlaceholder.classes = classes11;
    InputPlaceholder.displayName = "@mantine/core/InputPlaceholder";
  }
});

// node_modules/@mantine/core/esm/components/Input/InputWrapper/get-input-offsets/get-input-offsets.mjs
function getInputOffsets(inputWrapperOrder, { hasDescription, hasError }) {
  const inputIndex = inputWrapperOrder.findIndex((part) => part === "input");
  const aboveInput = inputWrapperOrder.slice(0, inputIndex);
  const belowInput = inputWrapperOrder.slice(inputIndex + 1);
  const offsetTop = hasDescription && aboveInput.includes("description") || hasError && aboveInput.includes("error");
  const offsetBottom = hasDescription && belowInput.includes("description") || hasError && belowInput.includes("error");
  return { offsetBottom, offsetTop };
}
var init_get_input_offsets = __esm({
  "node_modules/@mantine/core/esm/components/Input/InputWrapper/get-input-offsets/get-input-offsets.mjs"() {
    "use client";
  }
});

// node_modules/@mantine/core/esm/components/Input/InputWrapper/InputWrapper.mjs
var import_jsx_runtime72, import_react105, defaultProps18, varsResolver12, InputWrapper;
var init_InputWrapper = __esm({
  "node_modules/@mantine/core/esm/components/Input/InputWrapper/InputWrapper.mjs"() {
    "use client";
    import_jsx_runtime72 = __toESM(require_jsx_runtime(), 1);
    import_react105 = __toESM(require_react(), 1);
    init_esm();
    init_rem();
    init_get_size();
    init_create_vars_resolver();
    init_clsx();
    init_use_props();
    init_use_styles();
    init_Box();
    init_factory();
    init_InputDescription();
    init_InputError();
    init_InputLabel();
    init_InputWrapper_context();
    init_get_input_offsets();
    init_Input_module_css();
    defaultProps18 = {
      labelElement: "label",
      inputContainer: (children) => children,
      inputWrapperOrder: ["label", "description", "input", "error"]
    };
    varsResolver12 = createVarsResolver((_2, { size: size4 }) => ({
      label: {
        "--input-label-size": getFontSize(size4),
        "--input-asterisk-color": void 0
      },
      error: {
        "--input-error-size": size4 === void 0 ? void 0 : `calc(${getFontSize(size4)} - ${rem(2)})`
      },
      description: {
        "--input-description-size": size4 === void 0 ? void 0 : `calc(${getFontSize(size4)} - ${rem(2)})`
      }
    }));
    InputWrapper = factory((_props, ref) => {
      const props = useProps("InputWrapper", defaultProps18, _props);
      const {
        classNames,
        className,
        style,
        styles,
        unstyled,
        vars,
        size: size4,
        variant,
        __staticSelector,
        inputContainer,
        inputWrapperOrder,
        label: label4,
        error,
        description,
        labelProps,
        descriptionProps,
        errorProps,
        labelElement,
        children,
        withAsterisk,
        id: id3,
        required: required2,
        __stylesApiProps,
        mod,
        ...others
      } = props;
      const getStyles = useStyles({
        name: ["InputWrapper", __staticSelector],
        props: __stylesApiProps || props,
        classes: classes11,
        className,
        style,
        classNames,
        styles,
        unstyled,
        vars,
        varsResolver: varsResolver12
      });
      const sharedProps = {
        size: size4,
        variant,
        __staticSelector
      };
      const idBase3 = useId(id3);
      const isRequired = typeof withAsterisk === "boolean" ? withAsterisk : required2;
      const errorId = errorProps?.id || `${idBase3}-error`;
      const descriptionId = descriptionProps?.id || `${idBase3}-description`;
      const inputId = idBase3;
      const hasError = !!error && typeof error !== "boolean";
      const hasDescription = !!description;
      const _describedBy = `${hasError ? errorId : ""} ${hasDescription ? descriptionId : ""}`;
      const describedBy = _describedBy.trim().length > 0 ? _describedBy.trim() : void 0;
      const labelId = labelProps?.id || `${idBase3}-label`;
      const _label = label4 && /* @__PURE__ */ (0, import_jsx_runtime72.jsx)(
        InputLabel,
        {
          labelElement,
          id: labelId,
          htmlFor: inputId,
          required: isRequired,
          ...sharedProps,
          ...labelProps,
          children: label4
        },
        "label"
      );
      const _description = hasDescription && /* @__PURE__ */ (0, import_jsx_runtime72.jsx)(
        InputDescription,
        {
          ...descriptionProps,
          ...sharedProps,
          size: descriptionProps?.size || sharedProps.size,
          id: descriptionProps?.id || descriptionId,
          children: description
        },
        "description"
      );
      const _input = /* @__PURE__ */ (0, import_jsx_runtime72.jsx)(import_react105.Fragment, { children: inputContainer(children) }, "input");
      const _error = hasError && /* @__PURE__ */ (0, import_react105.createElement)(
        InputError,
        {
          ...errorProps,
          ...sharedProps,
          size: errorProps?.size || sharedProps.size,
          key: "error",
          id: errorProps?.id || errorId
        },
        error
      );
      const content = inputWrapperOrder.map((part) => {
        switch (part) {
          case "label":
            return _label;
          case "input":
            return _input;
          case "description":
            return _description;
          case "error":
            return _error;
          default:
            return null;
        }
      });
      return /* @__PURE__ */ (0, import_jsx_runtime72.jsx)(
        InputWrapperProvider,
        {
          value: {
            getStyles,
            describedBy,
            inputId,
            labelId,
            ...getInputOffsets(inputWrapperOrder, { hasDescription, hasError })
          },
          children: /* @__PURE__ */ (0, import_jsx_runtime72.jsx)(
            Box,
            {
              ref,
              variant,
              size: size4,
              mod: [{ error: !!error }, mod],
              ...getStyles("root"),
              ...others,
              children: content
            }
          )
        }
      );
    });
    InputWrapper.classes = classes11;
    InputWrapper.displayName = "@mantine/core/InputWrapper";
  }
});

// node_modules/@mantine/core/esm/components/Input/Input.mjs
var import_jsx_runtime73, import_react106, defaultProps19, varsResolver13, Input;
var init_Input = __esm({
  "node_modules/@mantine/core/esm/components/Input/Input.mjs"() {
    "use client";
    import_jsx_runtime73 = __toESM(require_jsx_runtime(), 1);
    init_rem();
    import_react106 = __toESM(require_react(), 1);
    init_get_size();
    init_create_vars_resolver();
    init_clsx();
    init_use_props();
    init_use_styles();
    init_extract_style_props();
    init_Box();
    init_polymorphic_factory();
    init_InputDescription();
    init_InputError();
    init_InputLabel();
    init_InputPlaceholder();
    init_InputWrapper_context();
    init_InputWrapper();
    init_Input_module_css();
    defaultProps19 = {
      variant: "default",
      leftSectionPointerEvents: "none",
      rightSectionPointerEvents: "none",
      withAria: true,
      withErrorStyles: true
    };
    varsResolver13 = createVarsResolver((_2, props, ctx) => ({
      wrapper: {
        "--input-margin-top": ctx.offsetTop ? "calc(var(--mantine-spacing-xs) / 2)" : void 0,
        "--input-margin-bottom": ctx.offsetBottom ? "calc(var(--mantine-spacing-xs) / 2)" : void 0,
        "--input-height": getSize(props.size, "input-height"),
        "--input-fz": getFontSize(props.size),
        "--input-radius": props.radius === void 0 ? void 0 : getRadius(props.radius),
        "--input-left-section-width": props.leftSectionWidth !== void 0 ? rem(props.leftSectionWidth) : void 0,
        "--input-right-section-width": props.rightSectionWidth !== void 0 ? rem(props.rightSectionWidth) : void 0,
        "--input-padding-y": props.multiline ? getSize(props.size, "input-padding-y") : void 0,
        "--input-left-section-pointer-events": props.leftSectionPointerEvents,
        "--input-right-section-pointer-events": props.rightSectionPointerEvents
      }
    }));
    Input = polymorphicFactory((_props, ref) => {
      const props = useProps("Input", defaultProps19, _props);
      const {
        classNames,
        className,
        style,
        styles,
        unstyled,
        required: required2,
        __staticSelector,
        __stylesApiProps,
        size: size4,
        wrapperProps,
        error,
        disabled,
        leftSection,
        leftSectionProps,
        leftSectionWidth,
        rightSection,
        rightSectionProps,
        rightSectionWidth,
        rightSectionPointerEvents,
        leftSectionPointerEvents,
        variant,
        vars,
        pointer,
        multiline,
        radius,
        id: id3,
        withAria,
        withErrorStyles,
        mod,
        inputSize,
        ...others
      } = props;
      const { styleProps, rest } = extractStyleProps(others);
      const ctx = useInputWrapperContext();
      const stylesCtx = { offsetBottom: ctx?.offsetBottom, offsetTop: ctx?.offsetTop };
      const getStyles = useStyles({
        name: ["Input", __staticSelector],
        props: __stylesApiProps || props,
        classes: classes11,
        className,
        style,
        classNames,
        styles,
        unstyled,
        stylesCtx,
        rootSelector: "wrapper",
        vars,
        varsResolver: varsResolver13
      });
      const ariaAttributes = withAria ? {
        required: required2,
        disabled,
        "aria-invalid": !!error,
        "aria-describedby": ctx?.describedBy,
        id: ctx?.inputId || id3
      } : {};
      return /* @__PURE__ */ (0, import_jsx_runtime73.jsxs)(
        Box,
        {
          ...getStyles("wrapper"),
          ...styleProps,
          ...wrapperProps,
          mod: [
            {
              error: !!error && withErrorStyles,
              pointer,
              disabled,
              multiline,
              "data-with-right-section": !!rightSection,
              "data-with-left-section": !!leftSection
            },
            mod
          ],
          variant,
          size: size4,
          children: [
            leftSection && /* @__PURE__ */ (0, import_jsx_runtime73.jsx)(
              "div",
              {
                ...leftSectionProps,
                "data-position": "left",
                ...getStyles("section", {
                  className: leftSectionProps?.className,
                  style: leftSectionProps?.style
                }),
                children: leftSection
              }
            ),
            /* @__PURE__ */ (0, import_jsx_runtime73.jsx)(
              Box,
              {
                component: "input",
                ...rest,
                ...ariaAttributes,
                ref,
                required: required2,
                mod: { disabled, error: !!error && withErrorStyles },
                variant,
                __size: inputSize,
                ...getStyles("input")
              }
            ),
            rightSection && /* @__PURE__ */ (0, import_jsx_runtime73.jsx)(
              "div",
              {
                ...rightSectionProps,
                "data-position": "right",
                ...getStyles("section", {
                  className: rightSectionProps?.className,
                  style: rightSectionProps?.style
                }),
                children: rightSection
              }
            )
          ]
        }
      );
    });
    Input.classes = classes11;
    Input.Wrapper = InputWrapper;
    Input.Label = InputLabel;
    Input.Error = InputError;
    Input.Description = InputDescription;
    Input.Placeholder = InputPlaceholder;
    Input.displayName = "@mantine/core/Input";
  }
});

// node_modules/@mantine/core/esm/components/Input/use-input-props.mjs
function useInputProps(component, defaultProps79, _props) {
  const props = useProps(component, defaultProps79, _props);
  const {
    label: label4,
    description,
    error,
    required: required2,
    classNames,
    styles,
    className,
    unstyled,
    __staticSelector,
    __stylesApiProps,
    errorProps,
    labelProps,
    descriptionProps,
    wrapperProps: _wrapperProps,
    id: id3,
    size: size4,
    style,
    inputContainer,
    inputWrapperOrder,
    withAsterisk,
    variant,
    vars,
    mod,
    ...others
  } = props;
  const { styleProps, rest } = extractStyleProps(others);
  const wrapperProps = {
    label: label4,
    description,
    error,
    required: required2,
    classNames,
    className,
    __staticSelector,
    __stylesApiProps: __stylesApiProps || props,
    errorProps,
    labelProps,
    descriptionProps,
    unstyled,
    styles,
    size: size4,
    style,
    inputContainer,
    inputWrapperOrder,
    withAsterisk,
    variant,
    id: id3,
    mod,
    ..._wrapperProps
  };
  return {
    ...rest,
    classNames,
    styles,
    unstyled,
    wrapperProps: { ...wrapperProps, ...styleProps },
    inputProps: {
      required: required2,
      classNames,
      styles,
      unstyled,
      size: size4,
      __staticSelector,
      __stylesApiProps: __stylesApiProps || props,
      error,
      variant,
      id: id3
    }
  };
}
var import_react107, import_jsx_runtime74;
var init_use_input_props = __esm({
  "node_modules/@mantine/core/esm/components/Input/use-input-props.mjs"() {
    "use client";
    import_react107 = __toESM(require_react(), 1);
    import_jsx_runtime74 = __toESM(require_jsx_runtime(), 1);
    init_clsx();
    init_use_props();
    init_extract_style_props();
  }
});

// node_modules/@mantine/core/esm/components/InputBase/InputBase.mjs
var import_jsx_runtime75, import_react108, defaultProps20, InputBase;
var init_InputBase = __esm({
  "node_modules/@mantine/core/esm/components/InputBase/InputBase.mjs"() {
    "use client";
    import_jsx_runtime75 = __toESM(require_jsx_runtime(), 1);
    import_react108 = __toESM(require_react(), 1);
    init_clsx();
    init_polymorphic_factory();
    init_Input();
    init_use_input_props();
    defaultProps20 = {
      __staticSelector: "InputBase",
      withAria: true
    };
    InputBase = polymorphicFactory((props, ref) => {
      const { inputProps, wrapperProps, ...others } = useInputProps("InputBase", defaultProps20, props);
      return /* @__PURE__ */ (0, import_jsx_runtime75.jsx)(Input.Wrapper, { ...wrapperProps, children: /* @__PURE__ */ (0, import_jsx_runtime75.jsx)(Input, { ...inputProps, ...others, ref }) });
    });
    InputBase.classes = { ...Input.classes, ...Input.Wrapper.classes };
    InputBase.displayName = "@mantine/core/InputBase";
  }
});

// node_modules/@mantine/core/esm/components/Flex/flex-props.mjs
var FLEX_STYLE_PROPS_DATA;
var init_flex_props = __esm({
  "node_modules/@mantine/core/esm/components/Flex/flex-props.mjs"() {
    "use client";
    FLEX_STYLE_PROPS_DATA = {
      gap: { type: "spacing", property: "gap" },
      rowGap: { type: "spacing", property: "rowGap" },
      columnGap: { type: "spacing", property: "columnGap" },
      align: { type: "identity", property: "alignItems" },
      justify: { type: "identity", property: "justifyContent" },
      wrap: { type: "identity", property: "flexWrap" },
      direction: { type: "identity", property: "flexDirection" }
    };
  }
});

// node_modules/@mantine/core/esm/components/Flex/Flex.module.css.mjs
var classes12;
var init_Flex_module_css = __esm({
  "node_modules/@mantine/core/esm/components/Flex/Flex.module.css.mjs"() {
    "use client";
    classes12 = { "root": "m_8bffd616" };
  }
});

// node_modules/@mantine/core/esm/components/Flex/Flex.mjs
var import_jsx_runtime76, import_react109, defaultProps21, Flex;
var init_Flex = __esm({
  "node_modules/@mantine/core/esm/components/Flex/Flex.mjs"() {
    "use client";
    import_jsx_runtime76 = __toESM(require_jsx_runtime(), 1);
    init_filter_props();
    import_react109 = __toESM(require_react(), 1);
    init_clsx();
    init_MantineThemeProvider();
    init_use_props();
    init_use_styles();
    init_InlineStyles();
    init_parse_style_props();
    init_use_random_classname();
    init_Box();
    init_polymorphic_factory();
    init_flex_props();
    init_Flex_module_css();
    defaultProps21 = {};
    Flex = polymorphicFactory((_props, ref) => {
      const props = useProps("Flex", defaultProps21, _props);
      const {
        classNames,
        className,
        style,
        styles,
        unstyled,
        vars,
        gap,
        rowGap,
        columnGap,
        align,
        justify,
        wrap: wrap2,
        direction,
        ...others
      } = props;
      const getStyles = useStyles({
        name: "Flex",
        classes: classes12,
        props,
        className,
        style,
        classNames,
        styles,
        unstyled,
        vars
      });
      const theme2 = useMantineTheme();
      const responsiveClassName = useRandomClassName();
      const parsedStyleProps = parseStyleProps({
        styleProps: { gap, rowGap, columnGap, align, justify, wrap: wrap2, direction },
        theme: theme2,
        data: FLEX_STYLE_PROPS_DATA
      });
      return /* @__PURE__ */ (0, import_jsx_runtime76.jsxs)(import_jsx_runtime76.Fragment, { children: [
        parsedStyleProps.hasResponsiveStyles && /* @__PURE__ */ (0, import_jsx_runtime76.jsx)(
          InlineStyles,
          {
            selector: `.${responsiveClassName}`,
            styles: parsedStyleProps.styles,
            media: parsedStyleProps.media
          }
        ),
        /* @__PURE__ */ (0, import_jsx_runtime76.jsx)(
          Box,
          {
            ref,
            ...getStyles("root", {
              className: responsiveClassName,
              style: filterProps(parsedStyleProps.inlineStyles)
            }),
            ...others
          }
        )
      ] });
    });
    Flex.classes = classes12;
    Flex.displayName = "@mantine/core/Flex";
  }
});

// node_modules/@mantine/core/esm/components/Text/Text.module.css.mjs
var classes13;
var init_Text_module_css = __esm({
  "node_modules/@mantine/core/esm/components/Text/Text.module.css.mjs"() {
    "use client";
    classes13 = { "root": "m_b6d8b162" };
  }
});

// node_modules/@mantine/core/esm/components/Text/Text.mjs
function getTextTruncate(truncate) {
  if (truncate === "start") {
    return "start";
  }
  if (truncate === "end" || truncate) {
    return "end";
  }
  return void 0;
}
var import_jsx_runtime77, import_react110, defaultProps22, varsResolver14, Text;
var init_Text = __esm({
  "node_modules/@mantine/core/esm/components/Text/Text.mjs"() {
    "use client";
    import_jsx_runtime77 = __toESM(require_jsx_runtime(), 1);
    import_react110 = __toESM(require_react(), 1);
    init_get_size();
    init_create_vars_resolver();
    init_clsx();
    init_get_theme_color();
    init_get_gradient();
    init_use_props();
    init_use_styles();
    init_Box();
    init_polymorphic_factory();
    init_Text_module_css();
    defaultProps22 = {
      inherit: false
    };
    varsResolver14 = createVarsResolver(
      (theme2, { variant, lineClamp, gradient, size: size4, color: color2 }) => ({
        root: {
          "--text-fz": getFontSize(size4),
          "--text-lh": getLineHeight(size4),
          "--text-gradient": variant === "gradient" ? getGradient(gradient, theme2) : void 0,
          "--text-line-clamp": typeof lineClamp === "number" ? lineClamp.toString() : void 0,
          "--text-color": color2 ? getThemeColor(color2, theme2) : void 0
        }
      })
    );
    Text = polymorphicFactory((_props, ref) => {
      const props = useProps("Text", defaultProps22, _props);
      const {
        lineClamp,
        truncate,
        inline: inline4,
        inherit,
        gradient,
        span,
        __staticSelector,
        vars,
        className,
        style,
        classNames,
        styles,
        unstyled,
        variant,
        mod,
        size: size4,
        ...others
      } = props;
      const getStyles = useStyles({
        name: ["Text", __staticSelector],
        props,
        classes: classes13,
        className,
        style,
        classNames,
        styles,
        unstyled,
        vars,
        varsResolver: varsResolver14
      });
      return /* @__PURE__ */ (0, import_jsx_runtime77.jsx)(
        Box,
        {
          ...getStyles("root", { focusable: true }),
          ref,
          component: span ? "span" : "p",
          variant,
          mod: [
            {
              "data-truncate": getTextTruncate(truncate),
              "data-line-clamp": typeof lineClamp === "number",
              "data-inline": inline4,
              "data-inherit": inherit
            },
            mod
          ],
          size: size4,
          ...others
        }
      );
    });
    Text.classes = classes13;
    Text.displayName = "@mantine/core/Text";
  }
});

// node_modules/@mantine/core/esm/components/InlineInput/InlineInput.module.css.mjs
var classes14;
var init_InlineInput_module_css = __esm({
  "node_modules/@mantine/core/esm/components/InlineInput/InlineInput.module.css.mjs"() {
    "use client";
    classes14 = { "root": "m_5f75b09e", "body": "m_5f6e695e", "labelWrapper": "m_d3ea56bb", "label": "m_8ee546b8", "description": "m_328f68c0", "error": "m_8e8a99cc" };
  }
});

// node_modules/@mantine/core/esm/components/InlineInput/InlineInput.mjs
var import_jsx_runtime78, import_react111, InlineInputClasses, InlineInput;
var init_InlineInput = __esm({
  "node_modules/@mantine/core/esm/components/InlineInput/InlineInput.mjs"() {
    "use client";
    import_jsx_runtime78 = __toESM(require_jsx_runtime(), 1);
    import_react111 = __toESM(require_react(), 1);
    init_get_size();
    init_clsx();
    init_use_styles();
    init_Box();
    init_Input();
    init_InlineInput_module_css();
    InlineInputClasses = classes14;
    InlineInput = (0, import_react111.forwardRef)(
      ({
        __staticSelector,
        __stylesApiProps,
        className,
        classNames,
        styles,
        unstyled,
        children,
        label: label4,
        description,
        id: id3,
        disabled,
        error,
        size: size4,
        labelPosition = "left",
        bodyElement = "div",
        labelElement = "label",
        variant,
        style,
        vars,
        mod,
        ...others
      }, ref) => {
        const getStyles = useStyles({
          name: __staticSelector,
          props: __stylesApiProps,
          className,
          style,
          classes: classes14,
          classNames,
          styles,
          unstyled
        });
        return /* @__PURE__ */ (0, import_jsx_runtime78.jsx)(
          Box,
          {
            ...getStyles("root"),
            ref,
            __vars: {
              "--label-fz": getFontSize(size4),
              "--label-lh": getSize(size4, "label-lh")
            },
            mod: [{ "label-position": labelPosition }, mod],
            variant,
            size: size4,
            ...others,
            children: /* @__PURE__ */ (0, import_jsx_runtime78.jsxs)(
              Box,
              {
                component: bodyElement,
                htmlFor: bodyElement === "label" ? id3 : void 0,
                ...getStyles("body"),
                children: [
                  children,
                  /* @__PURE__ */ (0, import_jsx_runtime78.jsxs)("div", { ...getStyles("labelWrapper"), "data-disabled": disabled || void 0, children: [
                    label4 && /* @__PURE__ */ (0, import_jsx_runtime78.jsx)(
                      Box,
                      {
                        component: labelElement,
                        htmlFor: labelElement === "label" ? id3 : void 0,
                        ...getStyles("label"),
                        "data-disabled": disabled || void 0,
                        children: label4
                      }
                    ),
                    description && /* @__PURE__ */ (0, import_jsx_runtime78.jsx)(Input.Description, { size: size4, __inheritStyles: false, ...getStyles("description"), children: description }),
                    error && typeof error !== "boolean" && /* @__PURE__ */ (0, import_jsx_runtime78.jsx)(Input.Error, { size: size4, __inheritStyles: false, ...getStyles("error"), children: error })
                  ] })
                ]
              }
            )
          }
        );
      }
    );
    InlineInput.displayName = "@mantine/core/InlineInput";
  }
});

// node_modules/@mantine/core/esm/components/Checkbox/CheckboxGroup.context.mjs
var import_react112, CheckboxGroupContext, CheckboxGroupProvider, useCheckboxGroupContext;
var init_CheckboxGroup_context = __esm({
  "node_modules/@mantine/core/esm/components/Checkbox/CheckboxGroup.context.mjs"() {
    "use client";
    import_react112 = __toESM(require_react(), 1);
    CheckboxGroupContext = (0, import_react112.createContext)(null);
    CheckboxGroupProvider = CheckboxGroupContext.Provider;
    useCheckboxGroupContext = () => (0, import_react112.useContext)(CheckboxGroupContext);
  }
});

// node_modules/@mantine/core/esm/components/Checkbox/CheckboxCard/CheckboxCard.context.mjs
var import_react113, import_jsx_runtime79, CheckboxCardProvider, useCheckboxCardContext;
var init_CheckboxCard_context = __esm({
  "node_modules/@mantine/core/esm/components/Checkbox/CheckboxCard/CheckboxCard.context.mjs"() {
    "use client";
    import_react113 = __toESM(require_react(), 1);
    import_jsx_runtime79 = __toESM(require_jsx_runtime(), 1);
    init_create_optional_context();
    init_clsx();
    [CheckboxCardProvider, useCheckboxCardContext] = createOptionalContext();
  }
});

// node_modules/@mantine/core/esm/components/Checkbox/CheckboxCard/CheckboxCard.module.css.mjs
var classes15;
var init_CheckboxCard_module_css = __esm({
  "node_modules/@mantine/core/esm/components/Checkbox/CheckboxCard/CheckboxCard.module.css.mjs"() {
    "use client";
    classes15 = { "card": "m_26775b0a" };
  }
});

// node_modules/@mantine/core/esm/components/Checkbox/CheckboxCard/CheckboxCard.mjs
var import_jsx_runtime80, import_react114, defaultProps23, varsResolver15, CheckboxCard;
var init_CheckboxCard = __esm({
  "node_modules/@mantine/core/esm/components/Checkbox/CheckboxCard/CheckboxCard.mjs"() {
    "use client";
    import_jsx_runtime80 = __toESM(require_jsx_runtime(), 1);
    init_esm();
    import_react114 = __toESM(require_react(), 1);
    init_get_size();
    init_create_vars_resolver();
    init_clsx();
    init_use_props();
    init_use_styles();
    init_factory();
    init_UnstyledButton();
    init_CheckboxGroup_context();
    init_CheckboxCard_context();
    init_CheckboxCard_module_css();
    defaultProps23 = {
      withBorder: true
    };
    varsResolver15 = createVarsResolver((_2, { radius }) => ({
      card: {
        "--card-radius": getRadius(radius)
      }
    }));
    CheckboxCard = factory((_props, ref) => {
      const props = useProps("CheckboxCard", defaultProps23, _props);
      const {
        classNames,
        className,
        style,
        styles,
        unstyled,
        vars,
        checked,
        mod,
        withBorder,
        value,
        onClick,
        defaultChecked,
        onChange,
        ...others
      } = props;
      const getStyles = useStyles({
        name: "CheckboxCard",
        classes: classes15,
        props,
        className,
        style,
        classNames,
        styles,
        unstyled,
        vars,
        varsResolver: varsResolver15,
        rootSelector: "card"
      });
      const ctx = useCheckboxGroupContext();
      const _checked = typeof checked === "boolean" ? checked : ctx?.value.includes(value || "") || void 0;
      const [_value, setValue] = useUncontrolled({
        value: _checked,
        defaultValue: defaultChecked,
        finalValue: false,
        onChange
      });
      return /* @__PURE__ */ (0, import_jsx_runtime80.jsx)(CheckboxCardProvider, { value: { checked: _value }, children: /* @__PURE__ */ (0, import_jsx_runtime80.jsx)(
        UnstyledButton,
        {
          ref,
          mod: [{ "with-border": withBorder, checked: _value }, mod],
          ...getStyles("card"),
          ...others,
          role: "checkbox",
          "aria-checked": _value,
          onClick: (event) => {
            onClick?.(event);
            ctx?.onChange(value || "");
            setValue(!_value);
          }
        }
      ) });
    });
    CheckboxCard.displayName = "@mantine/core/CheckboxCard";
    CheckboxCard.classes = classes15;
  }
});

// node_modules/@mantine/core/esm/components/InputsGroupFieldset/InputsGroupFieldset.mjs
function InputsGroupFieldset({ children, role }) {
  const ctx = useInputWrapperContext();
  if (!ctx) {
    return /* @__PURE__ */ (0, import_jsx_runtime81.jsx)(import_jsx_runtime81.Fragment, { children });
  }
  return /* @__PURE__ */ (0, import_jsx_runtime81.jsx)("div", { role, "aria-labelledby": ctx.labelId, "aria-describedby": ctx.describedBy, children });
}
var import_jsx_runtime81, import_react115;
var init_InputsGroupFieldset = __esm({
  "node_modules/@mantine/core/esm/components/InputsGroupFieldset/InputsGroupFieldset.mjs"() {
    "use client";
    import_jsx_runtime81 = __toESM(require_jsx_runtime(), 1);
    import_react115 = __toESM(require_react(), 1);
    init_clsx();
    init_InputWrapper_context();
  }
});

// node_modules/@mantine/core/esm/components/Checkbox/CheckboxGroup/CheckboxGroup.mjs
var import_jsx_runtime82, import_react116, defaultProps24, CheckboxGroup;
var init_CheckboxGroup = __esm({
  "node_modules/@mantine/core/esm/components/Checkbox/CheckboxGroup/CheckboxGroup.mjs"() {
    "use client";
    import_jsx_runtime82 = __toESM(require_jsx_runtime(), 1);
    init_esm();
    import_react116 = __toESM(require_react(), 1);
    init_clsx();
    init_use_props();
    init_factory();
    init_Input();
    init_InputsGroupFieldset();
    init_CheckboxGroup_context();
    defaultProps24 = {};
    CheckboxGroup = factory((props, ref) => {
      const { value, defaultValue: defaultValue2, onChange, size: size4, wrapperProps, children, readOnly, ...others } = useProps("CheckboxGroup", defaultProps24, props);
      const [_value, setValue] = useUncontrolled({
        value,
        defaultValue: defaultValue2,
        finalValue: [],
        onChange
      });
      const handleChange = (event) => {
        const itemValue = typeof event === "string" ? event : event.currentTarget.value;
        !readOnly && setValue(
          _value.includes(itemValue) ? _value.filter((item2) => item2 !== itemValue) : [..._value, itemValue]
        );
      };
      return /* @__PURE__ */ (0, import_jsx_runtime82.jsx)(CheckboxGroupProvider, { value: { value: _value, onChange: handleChange, size: size4 }, children: /* @__PURE__ */ (0, import_jsx_runtime82.jsx)(
        Input.Wrapper,
        {
          size: size4,
          ref,
          ...wrapperProps,
          ...others,
          labelElement: "div",
          __staticSelector: "CheckboxGroup",
          children: /* @__PURE__ */ (0, import_jsx_runtime82.jsx)(InputsGroupFieldset, { role: "group", children })
        }
      ) });
    });
    CheckboxGroup.classes = Input.Wrapper.classes;
    CheckboxGroup.displayName = "@mantine/core/CheckboxGroup";
  }
});

// node_modules/@mantine/core/esm/components/Checkbox/CheckIcon.mjs
function CheckIcon({ size: size4, style, ...others }) {
  const _style = size4 !== void 0 ? { width: rem(size4), height: rem(size4), ...style } : style;
  return /* @__PURE__ */ (0, import_jsx_runtime83.jsx)(
    "svg",
    {
      viewBox: "0 0 10 7",
      fill: "none",
      xmlns: "http://www.w3.org/2000/svg",
      style: _style,
      "aria-hidden": true,
      ...others,
      children: /* @__PURE__ */ (0, import_jsx_runtime83.jsx)(
        "path",
        {
          d: "M4 4.586L1.707 2.293A1 1 0 1 0 .293 3.707l3 3a.997.997 0 0 0 1.414 0l5-5A1 1 0 1 0 8.293.293L4 4.586z",
          fill: "currentColor",
          fillRule: "evenodd",
          clipRule: "evenodd"
        }
      )
    }
  );
}
function CheckboxIcon({ indeterminate, ...others }) {
  if (indeterminate) {
    return /* @__PURE__ */ (0, import_jsx_runtime83.jsx)(
      "svg",
      {
        xmlns: "http://www.w3.org/2000/svg",
        fill: "none",
        viewBox: "0 0 32 6",
        "aria-hidden": true,
        ...others,
        children: /* @__PURE__ */ (0, import_jsx_runtime83.jsx)("rect", { width: "32", height: "6", fill: "currentColor", rx: "3" })
      }
    );
  }
  return /* @__PURE__ */ (0, import_jsx_runtime83.jsx)(CheckIcon, { ...others });
}
var import_jsx_runtime83, import_react117;
var init_CheckIcon = __esm({
  "node_modules/@mantine/core/esm/components/Checkbox/CheckIcon.mjs"() {
    "use client";
    import_jsx_runtime83 = __toESM(require_jsx_runtime(), 1);
    init_rem();
    import_react117 = __toESM(require_react(), 1);
    init_clsx();
  }
});

// node_modules/@mantine/core/esm/components/Checkbox/CheckboxIndicator/CheckboxIndicator.module.css.mjs
var classes16;
var init_CheckboxIndicator_module_css = __esm({
  "node_modules/@mantine/core/esm/components/Checkbox/CheckboxIndicator/CheckboxIndicator.module.css.mjs"() {
    "use client";
    classes16 = { "indicator": "m_5e5256ee", "icon": "m_1b1c543a", "indicator--outline": "m_76e20374" };
  }
});

// node_modules/@mantine/core/esm/components/Checkbox/CheckboxIndicator/CheckboxIndicator.mjs
var import_jsx_runtime84, import_react118, defaultProps25, varsResolver16, CheckboxIndicator;
var init_CheckboxIndicator = __esm({
  "node_modules/@mantine/core/esm/components/Checkbox/CheckboxIndicator/CheckboxIndicator.mjs"() {
    "use client";
    import_jsx_runtime84 = __toESM(require_jsx_runtime(), 1);
    import_react118 = __toESM(require_react(), 1);
    init_get_size();
    init_create_vars_resolver();
    init_clsx();
    init_parse_theme_color();
    init_get_theme_color();
    init_get_contrast_color();
    init_get_auto_contrast_value();
    init_use_props();
    init_use_styles();
    init_Box();
    init_factory();
    init_CheckboxCard_context();
    init_CheckIcon();
    init_CheckboxIndicator_module_css();
    defaultProps25 = {
      icon: CheckboxIcon
    };
    varsResolver16 = createVarsResolver(
      (theme2, { radius, color: color2, size: size4, iconColor, variant, autoContrast }) => {
        const parsedColor = parseThemeColor({ color: color2 || theme2.primaryColor, theme: theme2 });
        const outlineColor = parsedColor.isThemeColor && parsedColor.shade === void 0 ? `var(--mantine-color-${parsedColor.color}-outline)` : parsedColor.color;
        return {
          indicator: {
            "--checkbox-size": getSize(size4, "checkbox-size"),
            "--checkbox-radius": radius === void 0 ? void 0 : getRadius(radius),
            "--checkbox-color": variant === "outline" ? outlineColor : getThemeColor(color2, theme2),
            "--checkbox-icon-color": iconColor ? getThemeColor(iconColor, theme2) : getAutoContrastValue(autoContrast, theme2) ? getContrastColor({ color: color2, theme: theme2, autoContrast }) : void 0
          }
        };
      }
    );
    CheckboxIndicator = factory((_props, ref) => {
      const props = useProps("CheckboxIndicator", defaultProps25, _props);
      const {
        classNames,
        className,
        style,
        styles,
        unstyled,
        vars,
        icon: icon2,
        indeterminate,
        radius,
        color: color2,
        iconColor,
        autoContrast,
        checked,
        mod,
        variant,
        disabled,
        ...others
      } = props;
      const Icon = icon2;
      const getStyles = useStyles({
        name: "CheckboxIndicator",
        classes: classes16,
        props,
        className,
        style,
        classNames,
        styles,
        unstyled,
        vars,
        varsResolver: varsResolver16,
        rootSelector: "indicator"
      });
      const ctx = useCheckboxCardContext();
      const _checked = typeof checked === "boolean" || typeof indeterminate === "boolean" ? checked || indeterminate : ctx?.checked || false;
      return /* @__PURE__ */ (0, import_jsx_runtime84.jsx)(
        Box,
        {
          ref,
          ...getStyles("indicator", { variant }),
          variant,
          mod: [{ checked: _checked, disabled }, mod],
          ...others,
          children: /* @__PURE__ */ (0, import_jsx_runtime84.jsx)(Icon, { indeterminate, ...getStyles("icon") })
        }
      );
    });
    CheckboxIndicator.displayName = "@mantine/core/CheckboxIndicator";
    CheckboxIndicator.classes = classes16;
  }
});

// node_modules/@mantine/core/esm/components/Checkbox/Checkbox.module.css.mjs
var classes17;
var init_Checkbox_module_css = __esm({
  "node_modules/@mantine/core/esm/components/Checkbox/Checkbox.module.css.mjs"() {
    "use client";
    classes17 = { "root": "m_bf2d988c", "inner": "m_26062bec", "input": "m_26063560", "icon": "m_bf295423", "input--outline": "m_215c4542" };
  }
});

// node_modules/@mantine/core/esm/components/Checkbox/Checkbox.mjs
var import_jsx_runtime85, import_react119, defaultProps26, varsResolver17, Checkbox;
var init_Checkbox = __esm({
  "node_modules/@mantine/core/esm/components/Checkbox/Checkbox.mjs"() {
    "use client";
    import_jsx_runtime85 = __toESM(require_jsx_runtime(), 1);
    init_esm();
    import_react119 = __toESM(require_react(), 1);
    init_get_size();
    init_create_vars_resolver();
    init_clsx();
    init_parse_theme_color();
    init_get_theme_color();
    init_get_contrast_color();
    init_get_auto_contrast_value();
    init_use_props();
    init_use_styles();
    init_extract_style_props();
    init_Box();
    init_factory();
    init_InlineInput();
    init_CheckboxCard();
    init_CheckboxGroup_context();
    init_CheckboxGroup();
    init_CheckboxIndicator();
    init_CheckIcon();
    init_Checkbox_module_css();
    defaultProps26 = {
      labelPosition: "right",
      icon: CheckboxIcon
    };
    varsResolver17 = createVarsResolver(
      (theme2, { radius, color: color2, size: size4, iconColor, variant, autoContrast }) => {
        const parsedColor = parseThemeColor({ color: color2 || theme2.primaryColor, theme: theme2 });
        const outlineColor = parsedColor.isThemeColor && parsedColor.shade === void 0 ? `var(--mantine-color-${parsedColor.color}-outline)` : parsedColor.color;
        return {
          root: {
            "--checkbox-size": getSize(size4, "checkbox-size"),
            "--checkbox-radius": radius === void 0 ? void 0 : getRadius(radius),
            "--checkbox-color": variant === "outline" ? outlineColor : getThemeColor(color2, theme2),
            "--checkbox-icon-color": iconColor ? getThemeColor(iconColor, theme2) : getAutoContrastValue(autoContrast, theme2) ? getContrastColor({ color: color2, theme: theme2, autoContrast }) : void 0
          }
        };
      }
    );
    Checkbox = factory((_props, ref) => {
      const props = useProps("Checkbox", defaultProps26, _props);
      const {
        classNames,
        className,
        style,
        styles,
        unstyled,
        vars,
        color: color2,
        label: label4,
        id: id3,
        size: size4,
        radius,
        wrapperProps,
        checked,
        labelPosition,
        description,
        error,
        disabled,
        variant,
        indeterminate,
        icon: icon2,
        rootRef,
        iconColor,
        onChange,
        autoContrast,
        mod,
        ...others
      } = props;
      const ctx = useCheckboxGroupContext();
      const _size = size4 || ctx?.size;
      const Icon = icon2;
      const getStyles = useStyles({
        name: "Checkbox",
        props,
        classes: classes17,
        className,
        style,
        classNames,
        styles,
        unstyled,
        vars,
        varsResolver: varsResolver17
      });
      const { styleProps, rest } = extractStyleProps(others);
      const uuid = useId(id3);
      const contextProps = ctx ? {
        checked: ctx.value.includes(rest.value),
        onChange: (event) => {
          ctx.onChange(event);
          onChange?.(event);
        }
      } : {};
      return /* @__PURE__ */ (0, import_jsx_runtime85.jsx)(
        InlineInput,
        {
          ...getStyles("root"),
          __staticSelector: "Checkbox",
          __stylesApiProps: props,
          id: uuid,
          size: _size,
          labelPosition,
          label: label4,
          description,
          error,
          disabled,
          classNames,
          styles,
          unstyled,
          "data-checked": contextProps.checked || checked || void 0,
          variant,
          ref: rootRef,
          mod,
          ...styleProps,
          ...wrapperProps,
          children: /* @__PURE__ */ (0, import_jsx_runtime85.jsxs)(Box, { ...getStyles("inner"), mod: { "data-label-position": labelPosition }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime85.jsx)(
              Box,
              {
                component: "input",
                id: uuid,
                ref,
                checked,
                disabled,
                mod: { error: !!error, indeterminate },
                ...getStyles("input", { focusable: true, variant }),
                onChange,
                ...rest,
                ...contextProps,
                type: "checkbox"
              }
            ),
            /* @__PURE__ */ (0, import_jsx_runtime85.jsx)(Icon, { indeterminate, ...getStyles("icon") })
          ] })
        }
      );
    });
    Checkbox.classes = { ...classes17, ...InlineInputClasses };
    Checkbox.displayName = "@mantine/core/Checkbox";
    Checkbox.Group = CheckboxGroup;
    Checkbox.Indicator = CheckboxIndicator;
    Checkbox.Card = CheckboxCard;
  }
});

// node_modules/@mantine/core/esm/components/Blockquote/Blockquote.module.css.mjs
var classes18;
var init_Blockquote_module_css = __esm({
  "node_modules/@mantine/core/esm/components/Blockquote/Blockquote.module.css.mjs"() {
    "use client";
    classes18 = { "root": "m_ddec01c0", "icon": "m_dde7bd57", "cite": "m_dde51a35" };
  }
});

// node_modules/@mantine/core/esm/components/Blockquote/Blockquote.mjs
var import_jsx_runtime86, import_react120, defaultProps27, varsResolver18, Blockquote;
var init_Blockquote = __esm({
  "node_modules/@mantine/core/esm/components/Blockquote/Blockquote.mjs"() {
    "use client";
    import_jsx_runtime86 = __toESM(require_jsx_runtime(), 1);
    init_rem();
    import_react120 = __toESM(require_react(), 1);
    init_get_size();
    init_create_vars_resolver();
    init_clsx();
    init_parse_theme_color();
    init_get_theme_color();
    init_rgba();
    init_use_props();
    init_use_styles();
    init_Box();
    init_factory();
    init_Blockquote_module_css();
    defaultProps27 = {
      iconSize: 48
    };
    varsResolver18 = createVarsResolver((theme2, { color: color2, iconSize, radius }) => {
      const darkParsed = parseThemeColor({
        color: color2 || theme2.primaryColor,
        theme: theme2,
        colorScheme: "dark"
      });
      const lightParsed = parseThemeColor({
        color: color2 || theme2.primaryColor,
        theme: theme2,
        colorScheme: "light"
      });
      return {
        root: {
          "--bq-bg-light": rgba(lightParsed.value, 0.07),
          "--bq-bg-dark": rgba(darkParsed.value, 0.06),
          "--bq-bd": getThemeColor(color2, theme2),
          "--bq-icon-size": rem(iconSize),
          "--bq-radius": getRadius(radius)
        }
      };
    });
    Blockquote = factory((_props, ref) => {
      const props = useProps("Blockquote", defaultProps27, _props);
      const {
        classNames,
        className,
        style,
        styles,
        unstyled,
        vars,
        children,
        icon: icon2,
        iconSize,
        cite,
        ...others
      } = props;
      const getStyles = useStyles({
        name: "Blockquote",
        classes: classes18,
        props,
        className,
        style,
        classNames,
        styles,
        unstyled,
        vars,
        varsResolver: varsResolver18
      });
      return /* @__PURE__ */ (0, import_jsx_runtime86.jsxs)(Box, { component: "blockquote", ref, ...getStyles("root"), ...others, children: [
        icon2 && /* @__PURE__ */ (0, import_jsx_runtime86.jsx)("span", { ...getStyles("icon"), children: icon2 }),
        children,
        cite && /* @__PURE__ */ (0, import_jsx_runtime86.jsx)("cite", { ...getStyles("cite"), children: cite })
      ] });
    });
    Blockquote.classes = classes18;
    Blockquote.displayName = "@mantine/core/Blockquote";
  }
});

// node_modules/@mantine/core/esm/components/Burger/Burger.module.css.mjs
var classes19;
var init_Burger_module_css = __esm({
  "node_modules/@mantine/core/esm/components/Burger/Burger.module.css.mjs"() {
    "use client";
    classes19 = { "root": "m_fea6bf1a", "burger": "m_d4fb9cad" };
  }
});

// node_modules/@mantine/core/esm/components/Burger/Burger.mjs
var import_jsx_runtime87, import_react121, defaultProps28, varsResolver19, Burger;
var init_Burger = __esm({
  "node_modules/@mantine/core/esm/components/Burger/Burger.mjs"() {
    "use client";
    import_jsx_runtime87 = __toESM(require_jsx_runtime(), 1);
    init_rem();
    import_react121 = __toESM(require_react(), 1);
    init_get_size();
    init_create_vars_resolver();
    init_clsx();
    init_get_theme_color();
    init_use_props();
    init_use_styles();
    init_Box();
    init_factory();
    init_UnstyledButton();
    init_Burger_module_css();
    defaultProps28 = {};
    varsResolver19 = createVarsResolver(
      (theme2, { color: color2, size: size4, lineSize, transitionDuration, transitionTimingFunction }) => ({
        root: {
          "--burger-color": color2 ? getThemeColor(color2, theme2) : void 0,
          "--burger-size": getSize(size4, "burger-size"),
          "--burger-line-size": lineSize ? rem(lineSize) : void 0,
          "--burger-transition-duration": transitionDuration === void 0 ? void 0 : `${transitionDuration}ms`,
          "--burger-transition-timing-function": transitionTimingFunction
        }
      })
    );
    Burger = factory((_props, ref) => {
      const props = useProps("Burger", defaultProps28, _props);
      const {
        classNames,
        className,
        style,
        styles,
        unstyled,
        vars,
        opened,
        children,
        transitionDuration,
        transitionTimingFunction,
        lineSize,
        ...others
      } = props;
      const getStyles = useStyles({
        name: "Burger",
        classes: classes19,
        props,
        className,
        style,
        classNames,
        styles,
        unstyled,
        vars,
        varsResolver: varsResolver19
      });
      return /* @__PURE__ */ (0, import_jsx_runtime87.jsxs)(UnstyledButton, { ...getStyles("root"), ref, ...others, children: [
        /* @__PURE__ */ (0, import_jsx_runtime87.jsx)(Box, { mod: ["reduce-motion", { opened }], ...getStyles("burger") }),
        children
      ] });
    });
    Burger.classes = classes19;
    Burger.displayName = "@mantine/core/Burger";
  }
});

// node_modules/@mantine/core/esm/components/Button/Button.module.css.mjs
var classes20;
var init_Button_module_css = __esm({
  "node_modules/@mantine/core/esm/components/Button/Button.module.css.mjs"() {
    "use client";
    classes20 = { "root": "m_77c9d27d", "inner": "m_80f1301b", "label": "m_811560b9", "section": "m_a74036a", "loader": "m_a25b86ee", "group": "m_80d6d844" };
  }
});

// node_modules/@mantine/core/esm/components/Button/ButtonGroup/ButtonGroup.mjs
var import_jsx_runtime88, import_react122, defaultProps29, varsResolver20, ButtonGroup;
var init_ButtonGroup = __esm({
  "node_modules/@mantine/core/esm/components/Button/ButtonGroup/ButtonGroup.mjs"() {
    "use client";
    import_jsx_runtime88 = __toESM(require_jsx_runtime(), 1);
    init_rem();
    import_react122 = __toESM(require_react(), 1);
    init_create_vars_resolver();
    init_clsx();
    init_use_props();
    init_use_styles();
    init_Box();
    init_factory();
    init_Button_module_css();
    defaultProps29 = {
      orientation: "horizontal"
    };
    varsResolver20 = createVarsResolver((_2, { borderWidth }) => ({
      group: { "--button-border-width": rem(borderWidth) }
    }));
    ButtonGroup = factory((_props, ref) => {
      const props = useProps("ButtonGroup", defaultProps29, _props);
      const {
        className,
        style,
        classNames,
        styles,
        unstyled,
        orientation,
        vars,
        borderWidth,
        variant,
        mod,
        ...others
      } = useProps("ButtonGroup", defaultProps29, _props);
      const getStyles = useStyles({
        name: "ButtonGroup",
        props,
        classes: classes20,
        className,
        style,
        classNames,
        styles,
        unstyled,
        vars,
        varsResolver: varsResolver20,
        rootSelector: "group"
      });
      return /* @__PURE__ */ (0, import_jsx_runtime88.jsx)(
        Box,
        {
          ...getStyles("group"),
          ref,
          variant,
          mod: [{ "data-orientation": orientation }, mod],
          role: "group",
          ...others
        }
      );
    });
    ButtonGroup.classes = classes20;
    ButtonGroup.displayName = "@mantine/core/ButtonGroup";
  }
});

// node_modules/@mantine/core/esm/components/Button/Button.mjs
var import_jsx_runtime89, import_react123, loaderTransition, defaultProps30, varsResolver21, Button;
var init_Button = __esm({
  "node_modules/@mantine/core/esm/components/Button/Button.mjs"() {
    "use client";
    import_jsx_runtime89 = __toESM(require_jsx_runtime(), 1);
    init_rem();
    import_react123 = __toESM(require_react(), 1);
    init_get_size();
    init_create_vars_resolver();
    init_clsx();
    init_use_props();
    init_use_styles();
    init_Box();
    init_polymorphic_factory();
    init_Loader();
    init_Transition();
    init_UnstyledButton();
    init_ButtonGroup();
    init_Button_module_css();
    loaderTransition = {
      in: { opacity: 1, transform: `translate(-50%, calc(-50% + ${rem(1)}))` },
      out: { opacity: 0, transform: "translate(-50%, -200%)" },
      common: { transformOrigin: "center" },
      transitionProperty: "transform, opacity"
    };
    defaultProps30 = {};
    varsResolver21 = createVarsResolver(
      (theme2, { radius, color: color2, gradient, variant, size: size4, justify, autoContrast }) => {
        const colors = theme2.variantColorResolver({
          color: color2 || theme2.primaryColor,
          theme: theme2,
          gradient,
          variant: variant || "filled",
          autoContrast
        });
        return {
          root: {
            "--button-justify": justify,
            "--button-height": getSize(size4, "button-height"),
            "--button-padding-x": getSize(size4, "button-padding-x"),
            "--button-fz": size4?.includes("compact") ? getFontSize(size4.replace("compact-", "")) : getFontSize(size4),
            "--button-radius": radius === void 0 ? void 0 : getRadius(radius),
            "--button-bg": color2 || variant ? colors.background : void 0,
            "--button-hover": color2 || variant ? colors.hover : void 0,
            "--button-color": colors.color,
            "--button-bd": color2 || variant ? colors.border : void 0,
            "--button-hover-color": color2 || variant ? colors.hoverColor : void 0
          }
        };
      }
    );
    Button = polymorphicFactory((_props, ref) => {
      const props = useProps("Button", defaultProps30, _props);
      const {
        style,
        vars,
        className,
        color: color2,
        disabled,
        children,
        leftSection,
        rightSection,
        fullWidth,
        variant,
        radius,
        loading,
        loaderProps,
        gradient,
        classNames,
        styles,
        unstyled,
        "data-disabled": dataDisabled,
        autoContrast,
        mod,
        ...others
      } = props;
      const getStyles = useStyles({
        name: "Button",
        props,
        classes: classes20,
        className,
        style,
        classNames,
        styles,
        unstyled,
        vars,
        varsResolver: varsResolver21
      });
      const hasLeftSection = !!leftSection;
      const hasRightSection = !!rightSection;
      return /* @__PURE__ */ (0, import_jsx_runtime89.jsxs)(
        UnstyledButton,
        {
          ref,
          ...getStyles("root", { active: !disabled && !loading && !dataDisabled }),
          unstyled,
          variant,
          disabled: disabled || loading,
          mod: [
            {
              disabled: disabled || dataDisabled,
              loading,
              block: fullWidth,
              "with-left-section": hasLeftSection,
              "with-right-section": hasRightSection
            },
            mod
          ],
          ...others,
          children: [
            /* @__PURE__ */ (0, import_jsx_runtime89.jsx)(Transition, { mounted: !!loading, transition: loaderTransition, duration: 150, children: (transitionStyles) => /* @__PURE__ */ (0, import_jsx_runtime89.jsx)(Box, { component: "span", ...getStyles("loader", { style: transitionStyles }), "aria-hidden": true, children: /* @__PURE__ */ (0, import_jsx_runtime89.jsx)(
              Loader,
              {
                color: "var(--button-color)",
                size: "calc(var(--button-height) / 1.8)",
                ...loaderProps
              }
            ) }) }),
            /* @__PURE__ */ (0, import_jsx_runtime89.jsxs)("span", { ...getStyles("inner"), children: [
              leftSection && /* @__PURE__ */ (0, import_jsx_runtime89.jsx)(Box, { component: "span", ...getStyles("section"), mod: { position: "left" }, children: leftSection }),
              /* @__PURE__ */ (0, import_jsx_runtime89.jsx)(Box, { component: "span", mod: { loading }, ...getStyles("label"), children }),
              rightSection && /* @__PURE__ */ (0, import_jsx_runtime89.jsx)(Box, { component: "span", ...getStyles("section"), mod: { position: "right" }, children: rightSection })
            ] })
          ]
        }
      );
    });
    Button.classes = classes20;
    Button.displayName = "@mantine/core/Button";
    Button.Group = ButtonGroup;
  }
});

// node_modules/@mantine/core/esm/components/Center/Center.module.css.mjs
var classes21;
var init_Center_module_css = __esm({
  "node_modules/@mantine/core/esm/components/Center/Center.module.css.mjs"() {
    "use client";
    classes21 = { "root": "m_4451eb3a" };
  }
});

// node_modules/@mantine/core/esm/components/Center/Center.mjs
var import_jsx_runtime90, import_react124, defaultProps31, Center;
var init_Center = __esm({
  "node_modules/@mantine/core/esm/components/Center/Center.mjs"() {
    "use client";
    import_jsx_runtime90 = __toESM(require_jsx_runtime(), 1);
    import_react124 = __toESM(require_react(), 1);
    init_clsx();
    init_use_props();
    init_use_styles();
    init_Box();
    init_polymorphic_factory();
    init_Center_module_css();
    defaultProps31 = {};
    Center = polymorphicFactory((_props, ref) => {
      const props = useProps("Center", defaultProps31, _props);
      const { classNames, className, style, styles, unstyled, vars, inline: inline4, mod, ...others } = props;
      const getStyles = useStyles({
        name: "Center",
        props,
        classes: classes21,
        className,
        style,
        classNames,
        styles,
        unstyled,
        vars
      });
      return /* @__PURE__ */ (0, import_jsx_runtime90.jsx)(Box, { ref, mod: [{ inline: inline4 }, mod], ...getStyles("root"), ...others });
    });
    Center.classes = classes21;
    Center.displayName = "@mantine/core/Center";
  }
});

// node_modules/@mantine/core/esm/components/Container/Container.module.css.mjs
var classes22;
var init_Container_module_css = __esm({
  "node_modules/@mantine/core/esm/components/Container/Container.module.css.mjs"() {
    "use client";
    classes22 = { "root": "m_7485cace" };
  }
});

// node_modules/@mantine/core/esm/components/Container/Container.mjs
var import_jsx_runtime91, import_react125, defaultProps32, varsResolver22, Container;
var init_Container = __esm({
  "node_modules/@mantine/core/esm/components/Container/Container.mjs"() {
    "use client";
    import_jsx_runtime91 = __toESM(require_jsx_runtime(), 1);
    import_react125 = __toESM(require_react(), 1);
    init_get_size();
    init_create_vars_resolver();
    init_clsx();
    init_use_props();
    init_use_styles();
    init_Box();
    init_factory();
    init_Container_module_css();
    defaultProps32 = {};
    varsResolver22 = createVarsResolver((_2, { size: size4, fluid }) => ({
      root: {
        "--container-size": fluid ? void 0 : getSize(size4, "container-size")
      }
    }));
    Container = factory((_props, ref) => {
      const props = useProps("Container", defaultProps32, _props);
      const { classNames, className, style, styles, unstyled, vars, fluid, mod, ...others } = props;
      const getStyles = useStyles({
        name: "Container",
        classes: classes22,
        props,
        className,
        style,
        classNames,
        styles,
        unstyled,
        vars,
        varsResolver: varsResolver22
      });
      return /* @__PURE__ */ (0, import_jsx_runtime91.jsx)(Box, { ref, mod: [{ fluid }, mod], ...getStyles("root"), ...others });
    });
    Container.classes = classes22;
    Container.displayName = "@mantine/core/Container";
  }
});

// node_modules/@mantine/core/esm/components/Divider/Divider.module.css.mjs
var classes23;
var init_Divider_module_css = __esm({
  "node_modules/@mantine/core/esm/components/Divider/Divider.module.css.mjs"() {
    "use client";
    classes23 = { "root": "m_3eebeb36", "label": "m_9e365f20" };
  }
});

// node_modules/@mantine/core/esm/components/Divider/Divider.mjs
var import_jsx_runtime92, import_react126, defaultProps33, varsResolver23, Divider;
var init_Divider = __esm({
  "node_modules/@mantine/core/esm/components/Divider/Divider.mjs"() {
    "use client";
    import_jsx_runtime92 = __toESM(require_jsx_runtime(), 1);
    import_react126 = __toESM(require_react(), 1);
    init_get_size();
    init_create_vars_resolver();
    init_clsx();
    init_get_theme_color();
    init_use_props();
    init_use_styles();
    init_Box();
    init_factory();
    init_Divider_module_css();
    defaultProps33 = {
      orientation: "horizontal"
    };
    varsResolver23 = createVarsResolver((theme2, { color: color2, variant, size: size4 }) => ({
      root: {
        "--divider-color": color2 ? getThemeColor(color2, theme2) : void 0,
        "--divider-border-style": variant,
        "--divider-size": getSize(size4, "divider-size")
      }
    }));
    Divider = factory((_props, ref) => {
      const props = useProps("Divider", defaultProps33, _props);
      const {
        classNames,
        className,
        style,
        styles,
        unstyled,
        vars,
        color: color2,
        orientation,
        label: label4,
        labelPosition,
        mod,
        ...others
      } = props;
      const getStyles = useStyles({
        name: "Divider",
        classes: classes23,
        props,
        className,
        style,
        classNames,
        styles,
        unstyled,
        vars,
        varsResolver: varsResolver23
      });
      return /* @__PURE__ */ (0, import_jsx_runtime92.jsx)(
        Box,
        {
          ref,
          mod: [{ orientation, "with-label": !!label4 }, mod],
          ...getStyles("root"),
          ...others,
          role: "separator",
          children: label4 && /* @__PURE__ */ (0, import_jsx_runtime92.jsx)(Box, { component: "span", mod: { position: labelPosition }, ...getStyles("label"), children: label4 })
        }
      );
    });
    Divider.classes = classes23;
    Divider.displayName = "@mantine/core/Divider";
  }
});

// node_modules/@mantine/core/esm/components/Drawer/Drawer.context.mjs
var import_react127, import_jsx_runtime93, DrawerProvider, useDrawerContext;
var init_Drawer_context = __esm({
  "node_modules/@mantine/core/esm/components/Drawer/Drawer.context.mjs"() {
    "use client";
    import_react127 = __toESM(require_react(), 1);
    init_create_safe_context();
    import_jsx_runtime93 = __toESM(require_jsx_runtime(), 1);
    init_clsx();
    [DrawerProvider, useDrawerContext] = createSafeContext(
      "Drawer component was not found in tree"
    );
  }
});

// node_modules/@mantine/core/esm/components/Drawer/Drawer.module.css.mjs
var classes24;
var init_Drawer_module_css = __esm({
  "node_modules/@mantine/core/esm/components/Drawer/Drawer.module.css.mjs"() {
    "use client";
    classes24 = { "root": "m_f11b401e", "header": "m_5a7c2c9", "content": "m_b8a05bbd", "inner": "m_31cd769a" };
  }
});

// node_modules/@mantine/core/esm/components/Drawer/DrawerBody.mjs
var import_jsx_runtime94, import_react128, defaultProps34, DrawerBody;
var init_DrawerBody = __esm({
  "node_modules/@mantine/core/esm/components/Drawer/DrawerBody.mjs"() {
    "use client";
    import_jsx_runtime94 = __toESM(require_jsx_runtime(), 1);
    import_react128 = __toESM(require_react(), 1);
    init_clsx();
    init_use_props();
    init_factory();
    init_ModalBaseBody();
    init_Drawer_context();
    init_Drawer_module_css();
    defaultProps34 = {};
    DrawerBody = factory((_props, ref) => {
      const props = useProps("DrawerBody", defaultProps34, _props);
      const { classNames, className, style, styles, vars, ...others } = props;
      const ctx = useDrawerContext();
      return /* @__PURE__ */ (0, import_jsx_runtime94.jsx)(
        ModalBaseBody,
        {
          ref,
          ...ctx.getStyles("body", { classNames, style, styles, className }),
          ...others
        }
      );
    });
    DrawerBody.classes = classes24;
    DrawerBody.displayName = "@mantine/core/DrawerBody";
  }
});

// node_modules/@mantine/core/esm/components/Drawer/DrawerCloseButton.mjs
var import_jsx_runtime95, import_react129, defaultProps35, DrawerCloseButton;
var init_DrawerCloseButton = __esm({
  "node_modules/@mantine/core/esm/components/Drawer/DrawerCloseButton.mjs"() {
    "use client";
    import_jsx_runtime95 = __toESM(require_jsx_runtime(), 1);
    import_react129 = __toESM(require_react(), 1);
    init_clsx();
    init_use_props();
    init_factory();
    init_ModalBaseCloseButton();
    init_Drawer_context();
    init_Drawer_module_css();
    defaultProps35 = {};
    DrawerCloseButton = factory((_props, ref) => {
      const props = useProps("DrawerCloseButton", defaultProps35, _props);
      const { classNames, className, style, styles, vars, ...others } = props;
      const ctx = useDrawerContext();
      return /* @__PURE__ */ (0, import_jsx_runtime95.jsx)(
        ModalBaseCloseButton,
        {
          ref,
          ...ctx.getStyles("close", { classNames, style, styles, className }),
          ...others
        }
      );
    });
    DrawerCloseButton.classes = classes24;
    DrawerCloseButton.displayName = "@mantine/core/DrawerCloseButton";
  }
});

// node_modules/@mantine/core/esm/components/Drawer/DrawerContent.mjs
var import_jsx_runtime96, import_react130, defaultProps36, DrawerContent;
var init_DrawerContent = __esm({
  "node_modules/@mantine/core/esm/components/Drawer/DrawerContent.mjs"() {
    "use client";
    import_jsx_runtime96 = __toESM(require_jsx_runtime(), 1);
    import_react130 = __toESM(require_react(), 1);
    init_clsx();
    init_use_props();
    init_factory();
    init_ModalBaseContent();
    init_NativeScrollArea();
    init_Drawer_context();
    init_Drawer_module_css();
    defaultProps36 = {};
    DrawerContent = factory((_props, ref) => {
      const props = useProps("DrawerContent", defaultProps36, _props);
      const { classNames, className, style, styles, vars, children, radius, __hidden, ...others } = props;
      const ctx = useDrawerContext();
      const Scroll = ctx.scrollAreaComponent || NativeScrollArea;
      return /* @__PURE__ */ (0, import_jsx_runtime96.jsx)(
        ModalBaseContent,
        {
          ...ctx.getStyles("content", { className, style, styles, classNames }),
          innerProps: ctx.getStyles("inner", { className, style, styles, classNames }),
          ref,
          ...others,
          radius: radius || ctx.radius || 0,
          "data-hidden": __hidden || void 0,
          children: /* @__PURE__ */ (0, import_jsx_runtime96.jsx)(Scroll, { style: { height: "calc(100vh - var(--drawer-offset) * 2)" }, children })
        }
      );
    });
    DrawerContent.classes = classes24;
    DrawerContent.displayName = "@mantine/core/DrawerContent";
  }
});

// node_modules/@mantine/core/esm/components/Drawer/DrawerHeader.mjs
var import_jsx_runtime97, import_react131, defaultProps37, DrawerHeader;
var init_DrawerHeader = __esm({
  "node_modules/@mantine/core/esm/components/Drawer/DrawerHeader.mjs"() {
    "use client";
    import_jsx_runtime97 = __toESM(require_jsx_runtime(), 1);
    import_react131 = __toESM(require_react(), 1);
    init_clsx();
    init_use_props();
    init_factory();
    init_ModalBaseHeader();
    init_Drawer_context();
    init_Drawer_module_css();
    defaultProps37 = {};
    DrawerHeader = factory((_props, ref) => {
      const props = useProps("DrawerHeader", defaultProps37, _props);
      const { classNames, className, style, styles, vars, ...others } = props;
      const ctx = useDrawerContext();
      return /* @__PURE__ */ (0, import_jsx_runtime97.jsx)(
        ModalBaseHeader,
        {
          ref,
          ...ctx.getStyles("header", { classNames, style, styles, className }),
          ...others
        }
      );
    });
    DrawerHeader.classes = classes24;
    DrawerHeader.displayName = "@mantine/core/DrawerHeader";
  }
});

// node_modules/@mantine/core/esm/components/Drawer/DrawerOverlay.mjs
var import_jsx_runtime98, import_react132, defaultProps38, DrawerOverlay;
var init_DrawerOverlay = __esm({
  "node_modules/@mantine/core/esm/components/Drawer/DrawerOverlay.mjs"() {
    "use client";
    import_jsx_runtime98 = __toESM(require_jsx_runtime(), 1);
    import_react132 = __toESM(require_react(), 1);
    init_clsx();
    init_use_props();
    init_factory();
    init_ModalBaseOverlay();
    init_Drawer_context();
    init_Drawer_module_css();
    defaultProps38 = {};
    DrawerOverlay = factory((_props, ref) => {
      const props = useProps("DrawerOverlay", defaultProps38, _props);
      const { classNames, className, style, styles, vars, ...others } = props;
      const ctx = useDrawerContext();
      return /* @__PURE__ */ (0, import_jsx_runtime98.jsx)(
        ModalBaseOverlay,
        {
          ref,
          ...ctx.getStyles("overlay", { classNames, style, styles, className }),
          ...others
        }
      );
    });
    DrawerOverlay.classes = classes24;
    DrawerOverlay.displayName = "@mantine/core/DrawerOverlay";
  }
});

// node_modules/@mantine/core/esm/components/Drawer/DrawerRoot.mjs
function getDrawerAlign(position) {
  switch (position) {
    case "top":
      return "flex-start";
    case "bottom":
      return "flex-end";
    default:
      return void 0;
  }
}
function getDrawerFlex(position) {
  if (position === "top" || position === "bottom") {
    return "0 0 calc(100% - var(--drawer-offset, 0rem) * 2)";
  }
  return void 0;
}
var import_jsx_runtime99, import_react133, transitions2, rtlTransitions, defaultProps39, varsResolver24, DrawerRoot;
var init_DrawerRoot = __esm({
  "node_modules/@mantine/core/esm/components/Drawer/DrawerRoot.mjs"() {
    "use client";
    import_jsx_runtime99 = __toESM(require_jsx_runtime(), 1);
    init_rem();
    import_react133 = __toESM(require_react(), 1);
    init_get_default_z_index();
    init_get_size();
    init_create_vars_resolver();
    init_clsx();
    init_use_props();
    init_use_styles();
    init_factory();
    init_DirectionProvider();
    init_ModalBase();
    init_Drawer_context();
    init_Drawer_module_css();
    transitions2 = {
      top: "slide-down",
      bottom: "slide-up",
      left: "slide-right",
      right: "slide-left"
    };
    rtlTransitions = {
      top: "slide-down",
      bottom: "slide-up",
      right: "slide-right",
      left: "slide-left"
    };
    defaultProps39 = {
      closeOnClickOutside: true,
      withinPortal: true,
      lockScroll: true,
      trapFocus: true,
      returnFocus: true,
      closeOnEscape: true,
      keepMounted: false,
      zIndex: getDefaultZIndex("modal"),
      position: "left"
    };
    varsResolver24 = createVarsResolver((_2, { position, size: size4, offset: offset4 }) => ({
      root: {
        "--drawer-size": getSize(size4, "drawer-size"),
        "--drawer-flex": getDrawerFlex(position),
        "--drawer-height": position === "left" || position === "right" ? void 0 : "var(--drawer-size)",
        "--drawer-align": getDrawerAlign(position),
        "--drawer-justify": position === "right" ? "flex-end" : void 0,
        "--drawer-offset": rem(offset4)
      }
    }));
    DrawerRoot = factory((_props, ref) => {
      const props = useProps("DrawerRoot", defaultProps39, _props);
      const {
        classNames,
        className,
        style,
        styles,
        unstyled,
        vars,
        scrollAreaComponent,
        position,
        transitionProps,
        radius,
        ...others
      } = props;
      const { dir } = useDirection();
      const getStyles = useStyles({
        name: "Drawer",
        classes: classes24,
        props,
        className,
        style,
        classNames,
        styles,
        unstyled,
        vars,
        varsResolver: varsResolver24
      });
      const drawerTransition = (dir === "rtl" ? rtlTransitions : transitions2)[position];
      return /* @__PURE__ */ (0, import_jsx_runtime99.jsx)(DrawerProvider, { value: { scrollAreaComponent, getStyles, radius }, children: /* @__PURE__ */ (0, import_jsx_runtime99.jsx)(
        ModalBase,
        {
          ref,
          ...getStyles("root"),
          transitionProps: { transition: drawerTransition, ...transitionProps },
          unstyled,
          ...others
        }
      ) });
    });
    DrawerRoot.classes = classes24;
    DrawerRoot.displayName = "@mantine/core/DrawerRoot";
  }
});

// node_modules/@mantine/core/esm/components/Drawer/DrawerStack.mjs
function DrawerStack({ children }) {
  const [stack3, setStack] = (0, import_react134.useState)([]);
  const [maxZIndex, setMaxZIndex] = (0, import_react134.useState)(getDefaultZIndex("modal"));
  return /* @__PURE__ */ (0, import_jsx_runtime100.jsx)(
    DrawerStackProvider,
    {
      value: {
        stack: stack3,
        addModal: (id3, zIndex) => {
          setStack((current) => [.../* @__PURE__ */ new Set([...current, id3])]);
          setMaxZIndex(
            (current) => typeof zIndex === "number" && typeof current === "number" ? Math.max(current, zIndex) : current
          );
        },
        removeModal: (id3) => setStack((current) => current.filter((currentId) => currentId !== id3)),
        getZIndex: (id3) => `calc(${maxZIndex} + ${stack3.indexOf(id3)} + 1)`,
        currentId: stack3[stack3.length - 1],
        maxZIndex
      },
      children
    }
  );
}
var import_jsx_runtime100, import_react134, DrawerStackProvider, useDrawerStackContext;
var init_DrawerStack = __esm({
  "node_modules/@mantine/core/esm/components/Drawer/DrawerStack.mjs"() {
    "use client";
    import_jsx_runtime100 = __toESM(require_jsx_runtime(), 1);
    import_react134 = __toESM(require_react(), 1);
    init_create_optional_context();
    init_get_default_z_index();
    init_clsx();
    [DrawerStackProvider, useDrawerStackContext] = createOptionalContext();
    DrawerStack.displayName = "@mantine/core/DrawerStack";
  }
});

// node_modules/@mantine/core/esm/components/Drawer/DrawerTitle.mjs
var import_jsx_runtime101, import_react135, defaultProps40, DrawerTitle;
var init_DrawerTitle = __esm({
  "node_modules/@mantine/core/esm/components/Drawer/DrawerTitle.mjs"() {
    "use client";
    import_jsx_runtime101 = __toESM(require_jsx_runtime(), 1);
    import_react135 = __toESM(require_react(), 1);
    init_clsx();
    init_use_props();
    init_factory();
    init_ModalBaseTitle();
    init_Drawer_context();
    init_Drawer_module_css();
    defaultProps40 = {};
    DrawerTitle = factory((_props, ref) => {
      const props = useProps("DrawerTitle", defaultProps40, _props);
      const { classNames, className, style, styles, vars, ...others } = props;
      const ctx = useDrawerContext();
      return /* @__PURE__ */ (0, import_jsx_runtime101.jsx)(
        ModalBaseTitle,
        {
          ref,
          ...ctx.getStyles("title", { classNames, style, styles, className }),
          ...others
        }
      );
    });
    DrawerTitle.classes = classes24;
    DrawerTitle.displayName = "@mantine/core/DrawerTitle";
  }
});

// node_modules/@mantine/core/esm/components/Drawer/Drawer.mjs
var import_jsx_runtime102, import_react136, defaultProps41, Drawer;
var init_Drawer = __esm({
  "node_modules/@mantine/core/esm/components/Drawer/Drawer.mjs"() {
    "use client";
    import_jsx_runtime102 = __toESM(require_jsx_runtime(), 1);
    import_react136 = __toESM(require_react(), 1);
    init_get_default_z_index();
    init_clsx();
    init_use_props();
    init_factory();
    init_DrawerBody();
    init_DrawerCloseButton();
    init_DrawerContent();
    init_DrawerHeader();
    init_DrawerOverlay();
    init_DrawerRoot();
    init_DrawerStack();
    init_DrawerTitle();
    init_Drawer_module_css();
    defaultProps41 = {
      closeOnClickOutside: true,
      withinPortal: true,
      lockScroll: true,
      trapFocus: true,
      returnFocus: true,
      closeOnEscape: true,
      keepMounted: false,
      zIndex: getDefaultZIndex("modal"),
      withOverlay: true,
      withCloseButton: true
    };
    Drawer = factory((_props, ref) => {
      const {
        title: title3,
        withOverlay,
        overlayProps,
        withCloseButton,
        closeButtonProps,
        children,
        opened,
        stackId,
        zIndex,
        ...others
      } = useProps("Drawer", defaultProps41, _props);
      const ctx = useDrawerStackContext();
      const hasHeader = !!title3 || withCloseButton;
      const stackProps = ctx && stackId ? {
        closeOnEscape: ctx.currentId === stackId,
        trapFocus: ctx.currentId === stackId,
        zIndex: ctx.getZIndex(stackId)
      } : {};
      const overlayVisible = withOverlay === false ? false : stackId && ctx ? ctx.currentId === stackId : opened;
      (0, import_react136.useEffect)(() => {
        if (ctx && stackId) {
          opened ? ctx.addModal(stackId, zIndex || getDefaultZIndex("modal")) : ctx.removeModal(stackId);
        }
      }, [opened, stackId, zIndex]);
      return /* @__PURE__ */ (0, import_jsx_runtime102.jsxs)(
        DrawerRoot,
        {
          ref,
          opened,
          zIndex: ctx && stackId ? ctx.getZIndex(stackId) : zIndex,
          ...others,
          ...stackProps,
          children: [
            withOverlay && /* @__PURE__ */ (0, import_jsx_runtime102.jsx)(
              DrawerOverlay,
              {
                visible: overlayVisible,
                transitionProps: ctx && stackId ? { duration: 0 } : void 0,
                ...overlayProps
              }
            ),
            /* @__PURE__ */ (0, import_jsx_runtime102.jsxs)(DrawerContent, { __hidden: ctx && stackId && opened ? stackId !== ctx.currentId : false, children: [
              hasHeader && /* @__PURE__ */ (0, import_jsx_runtime102.jsxs)(DrawerHeader, { children: [
                title3 && /* @__PURE__ */ (0, import_jsx_runtime102.jsx)(DrawerTitle, { children: title3 }),
                withCloseButton && /* @__PURE__ */ (0, import_jsx_runtime102.jsx)(DrawerCloseButton, { ...closeButtonProps })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime102.jsx)(DrawerBody, { children })
            ] })
          ]
        }
      );
    });
    Drawer.classes = classes24;
    Drawer.displayName = "@mantine/core/Drawer";
    Drawer.Root = DrawerRoot;
    Drawer.Overlay = DrawerOverlay;
    Drawer.Content = DrawerContent;
    Drawer.Body = DrawerBody;
    Drawer.Header = DrawerHeader;
    Drawer.Title = DrawerTitle;
    Drawer.CloseButton = DrawerCloseButton;
    Drawer.Stack = DrawerStack;
  }
});

// node_modules/@mantine/core/esm/components/Floating/use-delayed-hover.mjs
function useDelayedHover({ open, close, openDelay, closeDelay }) {
  const openTimeout = (0, import_react137.useRef)(-1);
  const closeTimeout = (0, import_react137.useRef)(-1);
  const clearTimeouts = () => {
    window.clearTimeout(openTimeout.current);
    window.clearTimeout(closeTimeout.current);
  };
  const openDropdown = () => {
    clearTimeouts();
    if (openDelay === 0 || openDelay === void 0) {
      open();
    } else {
      openTimeout.current = window.setTimeout(open, openDelay);
    }
  };
  const closeDropdown = () => {
    clearTimeouts();
    if (closeDelay === 0 || closeDelay === void 0) {
      close();
    } else {
      closeTimeout.current = window.setTimeout(close, closeDelay);
    }
  };
  (0, import_react137.useEffect)(() => clearTimeouts, []);
  return { openDropdown, closeDropdown };
}
var import_react137;
var init_use_delayed_hover = __esm({
  "node_modules/@mantine/core/esm/components/Floating/use-delayed-hover.mjs"() {
    "use client";
    import_react137 = __toESM(require_react(), 1);
  }
});

// node_modules/@mantine/core/esm/components/Image/Image.module.css.mjs
var classes25;
var init_Image_module_css = __esm({
  "node_modules/@mantine/core/esm/components/Image/Image.module.css.mjs"() {
    "use client";
    classes25 = { "root": "m_9e117634" };
  }
});

// node_modules/@mantine/core/esm/components/Image/Image.mjs
var import_jsx_runtime103, import_react138, defaultProps42, varsResolver25, Image;
var init_Image = __esm({
  "node_modules/@mantine/core/esm/components/Image/Image.mjs"() {
    "use client";
    import_jsx_runtime103 = __toESM(require_jsx_runtime(), 1);
    import_react138 = __toESM(require_react(), 1);
    init_get_size();
    init_create_vars_resolver();
    init_clsx();
    init_use_props();
    init_use_styles();
    init_Box();
    init_polymorphic_factory();
    init_Image_module_css();
    defaultProps42 = {};
    varsResolver25 = createVarsResolver((_2, { radius, fit }) => ({
      root: {
        "--image-radius": radius === void 0 ? void 0 : getRadius(radius),
        "--image-object-fit": fit
      }
    }));
    Image = polymorphicFactory((_props, ref) => {
      const props = useProps("Image", defaultProps42, _props);
      const {
        classNames,
        className,
        style,
        styles,
        unstyled,
        vars,
        onError,
        src,
        radius,
        fit,
        fallbackSrc,
        mod,
        ...others
      } = props;
      const [error, setError] = (0, import_react138.useState)(!src);
      (0, import_react138.useEffect)(() => setError(!src), [src]);
      const getStyles = useStyles({
        name: "Image",
        classes: classes25,
        props,
        className,
        style,
        classNames,
        styles,
        unstyled,
        vars,
        varsResolver: varsResolver25
      });
      if (error && fallbackSrc) {
        return /* @__PURE__ */ (0, import_jsx_runtime103.jsx)(
          Box,
          {
            component: "img",
            ref,
            src: fallbackSrc,
            ...getStyles("root"),
            onError,
            mod: ["fallback", mod],
            ...others
          }
        );
      }
      return /* @__PURE__ */ (0, import_jsx_runtime103.jsx)(
        Box,
        {
          component: "img",
          ref,
          ...getStyles("root"),
          src,
          onError: (event) => {
            onError?.(event);
            setError(true);
          },
          mod,
          ...others
        }
      );
    });
    Image.classes = classes25;
    Image.displayName = "@mantine/core/Image";
  }
});

// node_modules/@mantine/core/esm/components/List/List.context.mjs
var import_react139, import_jsx_runtime104, ListProvider, useListContext;
var init_List_context = __esm({
  "node_modules/@mantine/core/esm/components/List/List.context.mjs"() {
    "use client";
    import_react139 = __toESM(require_react(), 1);
    init_create_safe_context();
    import_jsx_runtime104 = __toESM(require_jsx_runtime(), 1);
    init_clsx();
    [ListProvider, useListContext] = createSafeContext(
      "List component was not found in tree"
    );
  }
});

// node_modules/@mantine/core/esm/components/List/List.module.css.mjs
var classes26;
var init_List_module_css = __esm({
  "node_modules/@mantine/core/esm/components/List/List.module.css.mjs"() {
    "use client";
    classes26 = { "root": "m_abbac491", "item": "m_abb6bec2", "itemWrapper": "m_75cd9f71", "itemIcon": "m_60f83e5b" };
  }
});

// node_modules/@mantine/core/esm/components/List/ListItem/ListItem.mjs
var import_jsx_runtime105, import_react140, defaultProps43, ListItem;
var init_ListItem = __esm({
  "node_modules/@mantine/core/esm/components/List/ListItem/ListItem.mjs"() {
    "use client";
    import_jsx_runtime105 = __toESM(require_jsx_runtime(), 1);
    import_react140 = __toESM(require_react(), 1);
    init_clsx();
    init_use_props();
    init_Box();
    init_factory();
    init_List_context();
    init_List_module_css();
    defaultProps43 = {};
    ListItem = factory((_props, ref) => {
      const props = useProps("ListItem", defaultProps43, _props);
      const { classNames, className, style, styles, vars, icon: icon2, children, mod, ...others } = props;
      const ctx = useListContext();
      const _icon = icon2 || ctx.icon;
      const stylesApiProps = { classNames, styles };
      return /* @__PURE__ */ (0, import_jsx_runtime105.jsx)(
        Box,
        {
          ...ctx.getStyles("item", { ...stylesApiProps, className, style }),
          component: "li",
          mod: [{ "with-icon": !!_icon, centered: ctx.center }, mod],
          ref,
          ...others,
          children: /* @__PURE__ */ (0, import_jsx_runtime105.jsxs)("div", { ...ctx.getStyles("itemWrapper", stylesApiProps), children: [
            _icon && /* @__PURE__ */ (0, import_jsx_runtime105.jsx)("span", { ...ctx.getStyles("itemIcon", stylesApiProps), children: _icon }),
            /* @__PURE__ */ (0, import_jsx_runtime105.jsx)("span", { ...ctx.getStyles("itemLabel", stylesApiProps), children })
          ] })
        }
      );
    });
    ListItem.classes = classes26;
    ListItem.displayName = "@mantine/core/ListItem";
  }
});

// node_modules/@mantine/core/esm/components/List/List.mjs
var import_jsx_runtime106, import_react141, defaultProps44, varsResolver26, List;
var init_List = __esm({
  "node_modules/@mantine/core/esm/components/List/List.mjs"() {
    "use client";
    import_jsx_runtime106 = __toESM(require_jsx_runtime(), 1);
    import_react141 = __toESM(require_react(), 1);
    init_get_size();
    init_create_vars_resolver();
    init_clsx();
    init_use_props();
    init_use_styles();
    init_Box();
    init_factory();
    init_List_context();
    init_ListItem();
    init_List_module_css();
    defaultProps44 = {
      type: "unordered"
    };
    varsResolver26 = createVarsResolver((_2, { size: size4, spacing }) => ({
      root: {
        "--list-fz": getFontSize(size4),
        "--list-lh": getLineHeight(size4),
        "--list-spacing": getSpacing(spacing)
      }
    }));
    List = factory((_props, ref) => {
      const props = useProps("List", defaultProps44, _props);
      const {
        classNames,
        className,
        style,
        styles,
        unstyled,
        vars,
        children,
        type: type2,
        withPadding,
        icon: icon2,
        spacing,
        center,
        listStyleType,
        mod,
        ...others
      } = props;
      const getStyles = useStyles({
        name: "List",
        classes: classes26,
        props,
        className,
        style,
        classNames,
        styles,
        unstyled,
        vars,
        varsResolver: varsResolver26
      });
      return /* @__PURE__ */ (0, import_jsx_runtime106.jsx)(ListProvider, { value: { center, icon: icon2, getStyles }, children: /* @__PURE__ */ (0, import_jsx_runtime106.jsx)(
        Box,
        {
          ...getStyles("root", { style: { listStyleType } }),
          component: type2 === "unordered" ? "ul" : "ol",
          mod: [{ "with-padding": withPadding }, mod],
          ref,
          ...others,
          children
        }
      ) });
    });
    List.classes = classes26;
    List.displayName = "@mantine/core/List";
    List.Item = ListItem;
  }
});

// node_modules/@mantine/core/esm/components/Menu/Menu.context.mjs
var import_react142, import_jsx_runtime107, MenuContextProvider, useMenuContext;
var init_Menu_context = __esm({
  "node_modules/@mantine/core/esm/components/Menu/Menu.context.mjs"() {
    "use client";
    import_react142 = __toESM(require_react(), 1);
    init_create_safe_context();
    import_jsx_runtime107 = __toESM(require_jsx_runtime(), 1);
    init_clsx();
    [MenuContextProvider, useMenuContext] = createSafeContext(
      "Menu component was not found in the tree"
    );
  }
});

// node_modules/@mantine/core/esm/components/Menu/Menu.module.css.mjs
var classes27;
var init_Menu_module_css = __esm({
  "node_modules/@mantine/core/esm/components/Menu/Menu.module.css.mjs"() {
    "use client";
    classes27 = { "dropdown": "m_dc9b7c9f", "label": "m_9bfac126", "divider": "m_efdf90cb", "item": "m_99ac2aa1", "itemLabel": "m_5476e0d3", "itemSection": "m_8b75e504" };
  }
});

// node_modules/@mantine/core/esm/components/Menu/MenuDivider/MenuDivider.mjs
var import_jsx_runtime108, import_react143, defaultProps45, MenuDivider;
var init_MenuDivider = __esm({
  "node_modules/@mantine/core/esm/components/Menu/MenuDivider/MenuDivider.mjs"() {
    "use client";
    import_jsx_runtime108 = __toESM(require_jsx_runtime(), 1);
    import_react143 = __toESM(require_react(), 1);
    init_clsx();
    init_use_props();
    init_Box();
    init_factory();
    init_Menu_context();
    init_Menu_module_css();
    defaultProps45 = {};
    MenuDivider = factory((props, ref) => {
      const { classNames, className, style, styles, vars, ...others } = useProps(
        "MenuDivider",
        defaultProps45,
        props
      );
      const ctx = useMenuContext();
      return /* @__PURE__ */ (0, import_jsx_runtime108.jsx)(
        Box,
        {
          ref,
          ...ctx.getStyles("divider", { className, style, styles, classNames }),
          ...others
        }
      );
    });
    MenuDivider.classes = classes27;
    MenuDivider.displayName = "@mantine/core/MenuDivider";
  }
});

// node_modules/@mantine/core/esm/components/Menu/MenuDropdown/MenuDropdown.mjs
var import_jsx_runtime109, import_react144, defaultProps46, MenuDropdown;
var init_MenuDropdown = __esm({
  "node_modules/@mantine/core/esm/components/Menu/MenuDropdown/MenuDropdown.mjs"() {
    "use client";
    import_jsx_runtime109 = __toESM(require_jsx_runtime(), 1);
    import_react144 = __toESM(require_react(), 1);
    init_esm();
    init_create_event_handler();
    init_clsx();
    init_use_props();
    init_factory();
    init_Popover();
    init_Menu_context();
    init_Menu_module_css();
    defaultProps46 = {};
    MenuDropdown = factory((props, ref) => {
      const {
        classNames,
        className,
        style,
        styles,
        vars,
        onMouseEnter,
        onMouseLeave,
        onKeyDown,
        children,
        ...others
      } = useProps("MenuDropdown", defaultProps46, props);
      const wrapperRef = (0, import_react144.useRef)(null);
      const ctx = useMenuContext();
      const handleKeyDown = createEventHandler(onKeyDown, (event) => {
        if (event.key === "ArrowUp" || event.key === "ArrowDown") {
          event.preventDefault();
          wrapperRef.current?.querySelectorAll("[data-menu-item]:not(:disabled)")[0]?.focus();
        }
      });
      const handleMouseEnter = createEventHandler(
        onMouseEnter,
        () => (ctx.trigger === "hover" || ctx.trigger === "click-hover") && ctx.openDropdown()
      );
      const handleMouseLeave = createEventHandler(
        onMouseLeave,
        () => (ctx.trigger === "hover" || ctx.trigger === "click-hover") && ctx.closeDropdown()
      );
      return /* @__PURE__ */ (0, import_jsx_runtime109.jsxs)(
        Popover.Dropdown,
        {
          ...others,
          onMouseEnter: handleMouseEnter,
          onMouseLeave: handleMouseLeave,
          role: "menu",
          "aria-orientation": "vertical",
          ref: useMergedRef(ref, wrapperRef),
          ...ctx.getStyles("dropdown", {
            className,
            style,
            styles,
            classNames,
            withStaticClass: false
          }),
          tabIndex: -1,
          "data-menu-dropdown": true,
          onKeyDown: handleKeyDown,
          children: [
            /* @__PURE__ */ (0, import_jsx_runtime109.jsx)("div", { tabIndex: -1, "data-autofocus": true, "data-mantine-stop-propagation": true, style: { outline: 0 } }),
            children
          ]
        }
      );
    });
    MenuDropdown.classes = classes27;
    MenuDropdown.displayName = "@mantine/core/MenuDropdown";
  }
});

// node_modules/@mantine/core/esm/components/Menu/MenuItem/MenuItem.mjs
var import_jsx_runtime110, import_react145, defaultProps47, MenuItem;
var init_MenuItem = __esm({
  "node_modules/@mantine/core/esm/components/Menu/MenuItem/MenuItem.mjs"() {
    "use client";
    import_jsx_runtime110 = __toESM(require_jsx_runtime(), 1);
    import_react145 = __toESM(require_react(), 1);
    init_esm();
    init_create_scoped_keydown_handler();
    init_create_event_handler();
    init_clsx();
    init_parse_theme_color();
    init_MantineThemeProvider();
    init_use_props();
    init_polymorphic_factory();
    init_DirectionProvider();
    init_UnstyledButton();
    init_Menu_context();
    init_Menu_module_css();
    defaultProps47 = {};
    MenuItem = polymorphicFactory((props, ref) => {
      const {
        classNames,
        className,
        style,
        styles,
        vars,
        color: color2,
        closeMenuOnClick,
        leftSection,
        rightSection,
        children,
        disabled,
        ...others
      } = useProps("MenuItem", defaultProps47, props);
      const ctx = useMenuContext();
      const theme2 = useMantineTheme();
      const { dir } = useDirection();
      const itemRef = (0, import_react145.useRef)();
      const itemIndex = ctx.getItemIndex(itemRef.current);
      const _others = others;
      const handleMouseLeave = createEventHandler(_others.onMouseLeave, () => ctx.setHovered(-1));
      const handleMouseEnter = createEventHandler(
        _others.onMouseEnter,
        () => ctx.setHovered(ctx.getItemIndex(itemRef.current))
      );
      const handleClick = createEventHandler(_others.onClick, () => {
        if (typeof closeMenuOnClick === "boolean") {
          closeMenuOnClick && ctx.closeDropdownImmediately();
        } else {
          ctx.closeOnItemClick && ctx.closeDropdownImmediately();
        }
      });
      const handleFocus = createEventHandler(
        _others.onFocus,
        () => ctx.setHovered(ctx.getItemIndex(itemRef.current))
      );
      const colors = color2 ? theme2.variantColorResolver({ color: color2, theme: theme2, variant: "light" }) : void 0;
      const parsedThemeColor = color2 ? parseThemeColor({ color: color2, theme: theme2 }) : null;
      return /* @__PURE__ */ (0, import_jsx_runtime110.jsxs)(
        UnstyledButton,
        {
          ...others,
          unstyled: ctx.unstyled,
          tabIndex: ctx.menuItemTabIndex,
          onFocus: handleFocus,
          ...ctx.getStyles("item", { className, style, styles, classNames }),
          ref: useMergedRef(itemRef, ref),
          role: "menuitem",
          disabled,
          "data-menu-item": true,
          "data-disabled": disabled || void 0,
          "data-hovered": ctx.hovered === itemIndex ? true : void 0,
          "data-mantine-stop-propagation": true,
          onMouseEnter: handleMouseEnter,
          onMouseLeave: handleMouseLeave,
          onClick: handleClick,
          onKeyDown: createScopedKeydownHandler({
            siblingSelector: "[data-menu-item]",
            parentSelector: "[data-menu-dropdown]",
            activateOnFocus: false,
            loop: ctx.loop,
            dir,
            orientation: "vertical",
            onKeyDown: _others.onKeyDown
          }),
          __vars: {
            "--menu-item-color": parsedThemeColor?.isThemeColor && parsedThemeColor?.shade === void 0 ? `var(--mantine-color-${parsedThemeColor.color}-6)` : colors?.color,
            "--menu-item-hover": colors?.hover
          },
          children: [
            leftSection && /* @__PURE__ */ (0, import_jsx_runtime110.jsx)("div", { ...ctx.getStyles("itemSection", { styles, classNames }), "data-position": "left", children: leftSection }),
            children && /* @__PURE__ */ (0, import_jsx_runtime110.jsx)("div", { ...ctx.getStyles("itemLabel", { styles, classNames }), children }),
            rightSection && /* @__PURE__ */ (0, import_jsx_runtime110.jsx)("div", { ...ctx.getStyles("itemSection", { styles, classNames }), "data-position": "right", children: rightSection })
          ]
        }
      );
    });
    MenuItem.classes = classes27;
    MenuItem.displayName = "@mantine/core/MenuItem";
  }
});

// node_modules/@mantine/core/esm/components/Menu/MenuLabel/MenuLabel.mjs
var import_jsx_runtime111, import_react146, defaultProps48, MenuLabel;
var init_MenuLabel = __esm({
  "node_modules/@mantine/core/esm/components/Menu/MenuLabel/MenuLabel.mjs"() {
    "use client";
    import_jsx_runtime111 = __toESM(require_jsx_runtime(), 1);
    import_react146 = __toESM(require_react(), 1);
    init_clsx();
    init_use_props();
    init_Box();
    init_factory();
    init_Menu_context();
    init_Menu_module_css();
    defaultProps48 = {};
    MenuLabel = factory((props, ref) => {
      const { classNames, className, style, styles, vars, ...others } = useProps(
        "MenuLabel",
        defaultProps48,
        props
      );
      const ctx = useMenuContext();
      return /* @__PURE__ */ (0, import_jsx_runtime111.jsx)(
        Box,
        {
          ref,
          ...ctx.getStyles("label", { className, style, styles, classNames }),
          ...others
        }
      );
    });
    MenuLabel.classes = classes27;
    MenuLabel.displayName = "@mantine/core/MenuLabel";
  }
});

// node_modules/@mantine/core/esm/components/Menu/MenuTarget/MenuTarget.mjs
var import_jsx_runtime112, import_react147, defaultProps49, MenuTarget;
var init_MenuTarget = __esm({
  "node_modules/@mantine/core/esm/components/Menu/MenuTarget/MenuTarget.mjs"() {
    "use client";
    import_jsx_runtime112 = __toESM(require_jsx_runtime(), 1);
    import_react147 = __toESM(require_react(), 1);
    init_is_element();
    init_create_event_handler();
    init_clsx();
    init_use_props();
    init_Popover();
    init_Menu_context();
    defaultProps49 = {
      refProp: "ref"
    };
    MenuTarget = (0, import_react147.forwardRef)((props, ref) => {
      const { children, refProp, ...others } = useProps("MenuTarget", defaultProps49, props);
      if (!isElement(children)) {
        throw new Error(
          "Menu.Target component children should be an element or a component that accepts ref. Fragments, strings, numbers and other primitive values are not supported"
        );
      }
      const ctx = useMenuContext();
      const onClick = createEventHandler(children.props.onClick, () => {
        if (ctx.trigger === "click") {
          ctx.toggleDropdown();
        } else if (ctx.trigger === "click-hover") {
          ctx.setOpenedViaClick(true);
          if (!ctx.opened) {
            ctx.openDropdown();
          }
        }
      });
      const onMouseEnter = createEventHandler(
        children.props.onMouseEnter,
        () => (ctx.trigger === "hover" || ctx.trigger === "click-hover") && ctx.openDropdown()
      );
      const onMouseLeave = createEventHandler(children.props.onMouseLeave, () => {
        if (ctx.trigger === "hover") {
          ctx.closeDropdown();
        } else if (ctx.trigger === "click-hover" && !ctx.openedViaClick) {
          ctx.closeDropdown();
        }
      });
      return /* @__PURE__ */ (0, import_jsx_runtime112.jsx)(Popover.Target, { refProp, popupType: "menu", ref, ...others, children: (0, import_react147.cloneElement)(children, {
        onClick,
        onMouseEnter,
        onMouseLeave,
        "data-expanded": ctx.opened ? true : void 0
      }) });
    });
    MenuTarget.displayName = "@mantine/core/MenuTarget";
  }
});

// node_modules/@mantine/core/esm/components/Menu/Menu.mjs
function Menu(_props) {
  const props = useProps("Menu", defaultProps50, _props);
  const {
    children,
    onOpen,
    onClose,
    opened,
    defaultOpened,
    trapFocus,
    onChange,
    closeOnItemClick,
    loop,
    closeOnEscape: closeOnEscape2,
    trigger,
    openDelay,
    closeDelay,
    classNames,
    styles,
    unstyled,
    variant,
    vars,
    menuItemTabIndex,
    keepMounted,
    ...others
  } = props;
  const getStyles = useStyles({
    name: "Menu",
    classes: classes27,
    props,
    classNames,
    styles,
    unstyled
  });
  const [hovered, { setHovered, resetHovered }] = useHovered();
  const [_opened, setOpened] = useUncontrolled({
    value: opened,
    defaultValue: defaultOpened,
    finalValue: false,
    onChange
  });
  const [openedViaClick, setOpenedViaClick] = (0, import_react148.useState)(false);
  const close = () => {
    setOpened(false);
    setOpenedViaClick(false);
    _opened && onClose?.();
  };
  const open = () => {
    setOpened(true);
    !_opened && onOpen?.();
  };
  const toggleDropdown = () => {
    _opened ? close() : open();
  };
  const { openDropdown, closeDropdown } = useDelayedHover({ open, close, closeDelay, openDelay });
  const getItemIndex = (node) => getContextItemIndex("[data-menu-item]", "[data-menu-dropdown]", node);
  const { resolvedClassNames, resolvedStyles } = useResolvedStylesApi({
    classNames,
    styles,
    props
  });
  useDidUpdate(() => {
    resetHovered();
  }, [_opened]);
  return /* @__PURE__ */ (0, import_jsx_runtime113.jsx)(
    MenuContextProvider,
    {
      value: {
        getStyles,
        opened: _opened,
        toggleDropdown,
        getItemIndex,
        hovered,
        setHovered,
        openedViaClick,
        setOpenedViaClick,
        closeOnItemClick,
        closeDropdown: trigger === "click" ? close : closeDropdown,
        openDropdown: trigger === "click" ? open : openDropdown,
        closeDropdownImmediately: close,
        loop,
        trigger,
        unstyled,
        menuItemTabIndex
      },
      children: /* @__PURE__ */ (0, import_jsx_runtime113.jsx)(
        Popover,
        {
          ...others,
          opened: _opened,
          onChange: toggleDropdown,
          defaultOpened,
          trapFocus: keepMounted ? false : trapFocus,
          closeOnEscape: closeOnEscape2,
          __staticSelector: "Menu",
          classNames: resolvedClassNames,
          styles: resolvedStyles,
          unstyled,
          variant,
          keepMounted,
          children
        }
      )
    }
  );
}
var import_jsx_runtime113, import_react148, defaultProps50;
var init_Menu = __esm({
  "node_modules/@mantine/core/esm/components/Menu/Menu.mjs"() {
    "use client";
    import_jsx_runtime113 = __toESM(require_jsx_runtime(), 1);
    import_react148 = __toESM(require_react(), 1);
    init_esm();
    init_get_context_item_index();
    init_use_hovered();
    init_clsx();
    init_use_resolved_styles_api();
    init_use_styles();
    init_use_props();
    init_use_delayed_hover();
    init_Popover();
    init_Menu_context();
    init_MenuDivider();
    init_MenuDropdown();
    init_MenuItem();
    init_MenuLabel();
    init_MenuTarget();
    init_Menu_module_css();
    defaultProps50 = {
      trapFocus: true,
      closeOnItemClick: true,
      clickOutsideEvents: ["mousedown", "touchstart", "keydown"],
      loop: true,
      trigger: "click",
      openDelay: 0,
      closeDelay: 100,
      menuItemTabIndex: -1
    };
    Menu.extend = (input2) => input2;
    Menu.classes = classes27;
    Menu.displayName = "@mantine/core/Menu";
    Menu.Item = MenuItem;
    Menu.Label = MenuLabel;
    Menu.Dropdown = MenuDropdown;
    Menu.Target = MenuTarget;
    Menu.Divider = MenuDivider;
  }
});

// node_modules/@mantine/core/esm/components/Modal/Modal.context.mjs
var import_react149, import_jsx_runtime114, ModalProvider, useModalContext;
var init_Modal_context = __esm({
  "node_modules/@mantine/core/esm/components/Modal/Modal.context.mjs"() {
    "use client";
    import_react149 = __toESM(require_react(), 1);
    init_create_safe_context();
    import_jsx_runtime114 = __toESM(require_jsx_runtime(), 1);
    init_clsx();
    [ModalProvider, useModalContext] = createSafeContext(
      "Modal component was not found in tree"
    );
  }
});

// node_modules/@mantine/core/esm/components/Modal/Modal.module.css.mjs
var classes28;
var init_Modal_module_css = __esm({
  "node_modules/@mantine/core/esm/components/Modal/Modal.module.css.mjs"() {
    "use client";
    classes28 = { "root": "m_9df02822", "content": "m_54c44539", "inner": "m_1f958f16", "header": "m_d0e2b9cd" };
  }
});

// node_modules/@mantine/core/esm/components/Modal/ModalBody.mjs
var import_jsx_runtime115, import_react150, defaultProps51, ModalBody;
var init_ModalBody = __esm({
  "node_modules/@mantine/core/esm/components/Modal/ModalBody.mjs"() {
    "use client";
    import_jsx_runtime115 = __toESM(require_jsx_runtime(), 1);
    import_react150 = __toESM(require_react(), 1);
    init_clsx();
    init_use_props();
    init_factory();
    init_ModalBaseBody();
    init_Modal_context();
    init_Modal_module_css();
    defaultProps51 = {};
    ModalBody = factory((_props, ref) => {
      const props = useProps("ModalBody", defaultProps51, _props);
      const { classNames, className, style, styles, vars, ...others } = props;
      const ctx = useModalContext();
      return /* @__PURE__ */ (0, import_jsx_runtime115.jsx)(
        ModalBaseBody,
        {
          ref,
          ...ctx.getStyles("body", { classNames, style, styles, className }),
          ...others
        }
      );
    });
    ModalBody.classes = classes28;
    ModalBody.displayName = "@mantine/core/ModalBody";
  }
});

// node_modules/@mantine/core/esm/components/Modal/ModalCloseButton.mjs
var import_jsx_runtime116, import_react151, defaultProps52, ModalCloseButton;
var init_ModalCloseButton = __esm({
  "node_modules/@mantine/core/esm/components/Modal/ModalCloseButton.mjs"() {
    "use client";
    import_jsx_runtime116 = __toESM(require_jsx_runtime(), 1);
    import_react151 = __toESM(require_react(), 1);
    init_clsx();
    init_use_props();
    init_factory();
    init_ModalBaseCloseButton();
    init_Modal_context();
    init_Modal_module_css();
    defaultProps52 = {};
    ModalCloseButton = factory((_props, ref) => {
      const props = useProps("ModalCloseButton", defaultProps52, _props);
      const { classNames, className, style, styles, vars, ...others } = props;
      const ctx = useModalContext();
      return /* @__PURE__ */ (0, import_jsx_runtime116.jsx)(
        ModalBaseCloseButton,
        {
          ref,
          ...ctx.getStyles("close", { classNames, style, styles, className }),
          ...others
        }
      );
    });
    ModalCloseButton.classes = classes28;
    ModalCloseButton.displayName = "@mantine/core/ModalCloseButton";
  }
});

// node_modules/@mantine/core/esm/components/Modal/ModalContent.mjs
var import_jsx_runtime117, import_react152, defaultProps53, ModalContent;
var init_ModalContent = __esm({
  "node_modules/@mantine/core/esm/components/Modal/ModalContent.mjs"() {
    "use client";
    import_jsx_runtime117 = __toESM(require_jsx_runtime(), 1);
    init_rem();
    import_react152 = __toESM(require_react(), 1);
    init_clsx();
    init_use_props();
    init_factory();
    init_ModalBaseContent();
    init_NativeScrollArea();
    init_Modal_context();
    init_Modal_module_css();
    defaultProps53 = {};
    ModalContent = factory((_props, ref) => {
      const props = useProps("ModalContent", defaultProps53, _props);
      const { classNames, className, style, styles, vars, children, __hidden, ...others } = props;
      const ctx = useModalContext();
      const Scroll = ctx.scrollAreaComponent || NativeScrollArea;
      return /* @__PURE__ */ (0, import_jsx_runtime117.jsx)(
        ModalBaseContent,
        {
          ...ctx.getStyles("content", { className, style, styles, classNames }),
          innerProps: ctx.getStyles("inner", { className, style, styles, classNames }),
          "data-full-screen": ctx.fullScreen || void 0,
          "data-modal-content": true,
          "data-hidden": __hidden || void 0,
          ref,
          ...others,
          children: /* @__PURE__ */ (0, import_jsx_runtime117.jsx)(
            Scroll,
            {
              style: {
                maxHeight: ctx.fullScreen ? "100dvh" : `calc(100dvh - (${rem(ctx.yOffset)} * 2))`
              },
              children
            }
          )
        }
      );
    });
    ModalContent.classes = classes28;
    ModalContent.displayName = "@mantine/core/ModalContent";
  }
});

// node_modules/@mantine/core/esm/components/Modal/ModalHeader.mjs
var import_jsx_runtime118, import_react153, defaultProps54, ModalHeader;
var init_ModalHeader = __esm({
  "node_modules/@mantine/core/esm/components/Modal/ModalHeader.mjs"() {
    "use client";
    import_jsx_runtime118 = __toESM(require_jsx_runtime(), 1);
    import_react153 = __toESM(require_react(), 1);
    init_clsx();
    init_use_props();
    init_factory();
    init_ModalBaseHeader();
    init_Modal_context();
    init_Modal_module_css();
    defaultProps54 = {};
    ModalHeader = factory((_props, ref) => {
      const props = useProps("ModalHeader", defaultProps54, _props);
      const { classNames, className, style, styles, vars, ...others } = props;
      const ctx = useModalContext();
      return /* @__PURE__ */ (0, import_jsx_runtime118.jsx)(
        ModalBaseHeader,
        {
          ref,
          ...ctx.getStyles("header", { classNames, style, styles, className }),
          ...others
        }
      );
    });
    ModalHeader.classes = classes28;
    ModalHeader.displayName = "@mantine/core/ModalHeader";
  }
});

// node_modules/@mantine/core/esm/components/Modal/ModalOverlay.mjs
var import_jsx_runtime119, import_react154, defaultProps55, ModalOverlay;
var init_ModalOverlay = __esm({
  "node_modules/@mantine/core/esm/components/Modal/ModalOverlay.mjs"() {
    "use client";
    import_jsx_runtime119 = __toESM(require_jsx_runtime(), 1);
    import_react154 = __toESM(require_react(), 1);
    init_clsx();
    init_use_props();
    init_factory();
    init_ModalBaseOverlay();
    init_Modal_context();
    init_Modal_module_css();
    defaultProps55 = {};
    ModalOverlay = factory((_props, ref) => {
      const props = useProps("ModalOverlay", defaultProps55, _props);
      const { classNames, className, style, styles, vars, ...others } = props;
      const ctx = useModalContext();
      return /* @__PURE__ */ (0, import_jsx_runtime119.jsx)(
        ModalBaseOverlay,
        {
          ref,
          ...ctx.getStyles("overlay", { classNames, style, styles, className }),
          ...others
        }
      );
    });
    ModalOverlay.classes = classes28;
    ModalOverlay.displayName = "@mantine/core/ModalOverlay";
  }
});

// node_modules/@mantine/core/esm/components/Modal/ModalRoot.mjs
var import_jsx_runtime120, import_react155, defaultProps56, varsResolver27, ModalRoot;
var init_ModalRoot = __esm({
  "node_modules/@mantine/core/esm/components/Modal/ModalRoot.mjs"() {
    "use client";
    import_jsx_runtime120 = __toESM(require_jsx_runtime(), 1);
    init_rem();
    import_react155 = __toESM(require_react(), 1);
    init_get_default_z_index();
    init_get_size();
    init_create_vars_resolver();
    init_clsx();
    init_use_props();
    init_use_styles();
    init_factory();
    init_ModalBase();
    init_Modal_context();
    init_Modal_module_css();
    defaultProps56 = {
      __staticSelector: "Modal",
      closeOnClickOutside: true,
      withinPortal: true,
      lockScroll: true,
      trapFocus: true,
      returnFocus: true,
      closeOnEscape: true,
      keepMounted: false,
      zIndex: getDefaultZIndex("modal"),
      transitionProps: { duration: 200, transition: "fade-down" },
      yOffset: "5dvh"
    };
    varsResolver27 = createVarsResolver(
      (_2, { radius, size: size4, yOffset, xOffset }) => ({
        root: {
          "--modal-radius": radius === void 0 ? void 0 : getRadius(radius),
          "--modal-size": getSize(size4, "modal-size"),
          "--modal-y-offset": rem(yOffset),
          "--modal-x-offset": rem(xOffset)
        }
      })
    );
    ModalRoot = factory((_props, ref) => {
      const props = useProps("ModalRoot", defaultProps56, _props);
      const {
        classNames,
        className,
        style,
        styles,
        unstyled,
        vars,
        yOffset,
        scrollAreaComponent,
        radius,
        fullScreen,
        centered,
        xOffset,
        __staticSelector,
        ...others
      } = props;
      const getStyles = useStyles({
        name: __staticSelector,
        classes: classes28,
        props,
        className,
        style,
        classNames,
        styles,
        unstyled,
        vars,
        varsResolver: varsResolver27
      });
      return /* @__PURE__ */ (0, import_jsx_runtime120.jsx)(ModalProvider, { value: { yOffset, scrollAreaComponent, getStyles, fullScreen }, children: /* @__PURE__ */ (0, import_jsx_runtime120.jsx)(
        ModalBase,
        {
          ref,
          ...getStyles("root"),
          "data-full-screen": fullScreen || void 0,
          "data-centered": centered || void 0,
          unstyled,
          ...others
        }
      ) });
    });
    ModalRoot.classes = classes28;
    ModalRoot.displayName = "@mantine/core/ModalRoot";
  }
});

// node_modules/@mantine/core/esm/components/Modal/ModalStack.mjs
function ModalStack({ children }) {
  const [stack3, setStack] = (0, import_react156.useState)([]);
  const [maxZIndex, setMaxZIndex] = (0, import_react156.useState)(getDefaultZIndex("modal"));
  return /* @__PURE__ */ (0, import_jsx_runtime121.jsx)(
    ModalStackProvider,
    {
      value: {
        stack: stack3,
        addModal: (id3, zIndex) => {
          setStack((current) => [.../* @__PURE__ */ new Set([...current, id3])]);
          setMaxZIndex(
            (current) => typeof zIndex === "number" && typeof current === "number" ? Math.max(current, zIndex) : current
          );
        },
        removeModal: (id3) => setStack((current) => current.filter((currentId) => currentId !== id3)),
        getZIndex: (id3) => `calc(${maxZIndex} + ${stack3.indexOf(id3)} + 1)`,
        currentId: stack3[stack3.length - 1],
        maxZIndex
      },
      children
    }
  );
}
var import_jsx_runtime121, import_react156, ModalStackProvider, useModalStackContext;
var init_ModalStack = __esm({
  "node_modules/@mantine/core/esm/components/Modal/ModalStack.mjs"() {
    "use client";
    import_jsx_runtime121 = __toESM(require_jsx_runtime(), 1);
    import_react156 = __toESM(require_react(), 1);
    init_create_optional_context();
    init_get_default_z_index();
    init_clsx();
    [ModalStackProvider, useModalStackContext] = createOptionalContext();
    ModalStack.displayName = "@mantine/core/ModalStack";
  }
});

// node_modules/@mantine/core/esm/components/Modal/ModalTitle.mjs
var import_jsx_runtime122, import_react157, defaultProps57, ModalTitle;
var init_ModalTitle = __esm({
  "node_modules/@mantine/core/esm/components/Modal/ModalTitle.mjs"() {
    "use client";
    import_jsx_runtime122 = __toESM(require_jsx_runtime(), 1);
    import_react157 = __toESM(require_react(), 1);
    init_clsx();
    init_use_props();
    init_factory();
    init_ModalBaseTitle();
    init_Modal_context();
    init_Modal_module_css();
    defaultProps57 = {};
    ModalTitle = factory((_props, ref) => {
      const props = useProps("ModalTitle", defaultProps57, _props);
      const { classNames, className, style, styles, vars, ...others } = props;
      const ctx = useModalContext();
      return /* @__PURE__ */ (0, import_jsx_runtime122.jsx)(
        ModalBaseTitle,
        {
          ref,
          ...ctx.getStyles("title", { classNames, style, styles, className }),
          ...others
        }
      );
    });
    ModalTitle.classes = classes28;
    ModalTitle.displayName = "@mantine/core/ModalTitle";
  }
});

// node_modules/@mantine/core/esm/components/Modal/Modal.mjs
var import_jsx_runtime123, import_react158, defaultProps58, Modal;
var init_Modal = __esm({
  "node_modules/@mantine/core/esm/components/Modal/Modal.mjs"() {
    "use client";
    import_jsx_runtime123 = __toESM(require_jsx_runtime(), 1);
    import_react158 = __toESM(require_react(), 1);
    init_get_default_z_index();
    init_clsx();
    init_use_props();
    init_factory();
    init_ModalBody();
    init_ModalCloseButton();
    init_ModalContent();
    init_ModalHeader();
    init_ModalOverlay();
    init_ModalRoot();
    init_ModalStack();
    init_ModalTitle();
    init_Modal_module_css();
    defaultProps58 = {
      closeOnClickOutside: true,
      withinPortal: true,
      lockScroll: true,
      trapFocus: true,
      returnFocus: true,
      closeOnEscape: true,
      keepMounted: false,
      zIndex: getDefaultZIndex("modal"),
      transitionProps: { duration: 200, transition: "fade-down" },
      withOverlay: true,
      withCloseButton: true
    };
    Modal = factory((_props, ref) => {
      const {
        title: title3,
        withOverlay,
        overlayProps,
        withCloseButton,
        closeButtonProps,
        children,
        radius,
        opened,
        stackId,
        zIndex,
        ...others
      } = useProps("Modal", defaultProps58, _props);
      const ctx = useModalStackContext();
      const hasHeader = !!title3 || withCloseButton;
      const stackProps = ctx && stackId ? {
        closeOnEscape: ctx.currentId === stackId,
        trapFocus: ctx.currentId === stackId,
        zIndex: ctx.getZIndex(stackId)
      } : {};
      const overlayVisible = withOverlay === false ? false : stackId && ctx ? ctx.currentId === stackId : opened;
      (0, import_react158.useEffect)(() => {
        if (ctx && stackId) {
          opened ? ctx.addModal(stackId, zIndex || getDefaultZIndex("modal")) : ctx.removeModal(stackId);
        }
      }, [opened, stackId, zIndex]);
      return /* @__PURE__ */ (0, import_jsx_runtime123.jsxs)(
        ModalRoot,
        {
          ref,
          radius,
          opened,
          zIndex: ctx && stackId ? ctx.getZIndex(stackId) : zIndex,
          ...others,
          ...stackProps,
          children: [
            withOverlay && /* @__PURE__ */ (0, import_jsx_runtime123.jsx)(
              ModalOverlay,
              {
                visible: overlayVisible,
                transitionProps: ctx && stackId ? { duration: 0 } : void 0,
                ...overlayProps
              }
            ),
            /* @__PURE__ */ (0, import_jsx_runtime123.jsxs)(
              ModalContent,
              {
                radius,
                __hidden: ctx && stackId && opened ? stackId !== ctx.currentId : false,
                children: [
                  hasHeader && /* @__PURE__ */ (0, import_jsx_runtime123.jsxs)(ModalHeader, { children: [
                    title3 && /* @__PURE__ */ (0, import_jsx_runtime123.jsx)(ModalTitle, { children: title3 }),
                    withCloseButton && /* @__PURE__ */ (0, import_jsx_runtime123.jsx)(ModalCloseButton, { ...closeButtonProps })
                  ] }),
                  /* @__PURE__ */ (0, import_jsx_runtime123.jsx)(ModalBody, { children })
                ]
              }
            )
          ]
        }
      );
    });
    Modal.classes = classes28;
    Modal.displayName = "@mantine/core/Modal";
    Modal.Root = ModalRoot;
    Modal.Overlay = ModalOverlay;
    Modal.Content = ModalContent;
    Modal.Body = ModalBody;
    Modal.Header = ModalHeader;
    Modal.Title = ModalTitle;
    Modal.CloseButton = ModalCloseButton;
    Modal.Stack = ModalStack;
  }
});

// node_modules/@mantine/core/esm/components/Notification/Notification.module.css.mjs
var classes29;
var init_Notification_module_css = __esm({
  "node_modules/@mantine/core/esm/components/Notification/Notification.module.css.mjs"() {
    "use client";
    classes29 = { "root": "m_a513464", "icon": "m_a4ceffb", "loader": "m_b0920b15", "body": "m_a49ed24", "title": "m_3feedf16", "description": "m_3d733a3a", "closeButton": "m_919a4d88" };
  }
});

// node_modules/@mantine/core/esm/components/Notification/Notification.mjs
var import_jsx_runtime124, import_react159, defaultProps59, varsResolver28, Notification;
var init_Notification = __esm({
  "node_modules/@mantine/core/esm/components/Notification/Notification.mjs"() {
    "use client";
    import_jsx_runtime124 = __toESM(require_jsx_runtime(), 1);
    import_react159 = __toESM(require_react(), 1);
    init_get_size();
    init_create_vars_resolver();
    init_clsx();
    init_get_theme_color();
    init_use_props();
    init_use_styles();
    init_Box();
    init_factory();
    init_CloseButton();
    init_Loader();
    init_Notification_module_css();
    defaultProps59 = {
      withCloseButton: true
    };
    varsResolver28 = createVarsResolver((theme2, { radius, color: color2 }) => ({
      root: {
        "--notification-radius": radius === void 0 ? void 0 : getRadius(radius),
        "--notification-color": color2 ? getThemeColor(color2, theme2) : void 0
      }
    }));
    Notification = factory((_props, ref) => {
      const props = useProps("Notification", defaultProps59, _props);
      const {
        className,
        color: color2,
        radius,
        loading,
        withCloseButton,
        withBorder,
        title: title3,
        icon: icon2,
        children,
        onClose,
        closeButtonProps,
        classNames,
        style,
        styles,
        unstyled,
        variant,
        vars,
        mod,
        ...others
      } = props;
      const getStyles = useStyles({
        name: "Notification",
        classes: classes29,
        props,
        className,
        style,
        classNames,
        styles,
        unstyled,
        vars,
        varsResolver: varsResolver28
      });
      return /* @__PURE__ */ (0, import_jsx_runtime124.jsxs)(
        Box,
        {
          ...getStyles("root"),
          mod: [{ "data-with-icon": !!icon2 || loading, "data-with-border": withBorder }, mod],
          ref,
          variant,
          ...others,
          role: "alert",
          children: [
            icon2 && !loading && /* @__PURE__ */ (0, import_jsx_runtime124.jsx)("div", { ...getStyles("icon"), children: icon2 }),
            loading && /* @__PURE__ */ (0, import_jsx_runtime124.jsx)(Loader, { size: 28, color: color2, ...getStyles("loader") }),
            /* @__PURE__ */ (0, import_jsx_runtime124.jsxs)("div", { ...getStyles("body"), children: [
              title3 && /* @__PURE__ */ (0, import_jsx_runtime124.jsx)("div", { ...getStyles("title"), children: title3 }),
              /* @__PURE__ */ (0, import_jsx_runtime124.jsx)(Box, { ...getStyles("description"), mod: { "data-with-title": !!title3 }, children })
            ] }),
            withCloseButton && /* @__PURE__ */ (0, import_jsx_runtime124.jsx)(
              CloseButton,
              {
                iconSize: 16,
                color: "gray",
                ...closeButtonProps,
                unstyled,
                onClick: onClose,
                ...getStyles("closeButton")
              }
            )
          ]
        }
      );
    });
    Notification.classes = classes29;
    Notification.displayName = "@mantine/core/Notification";
  }
});

// node_modules/@mantine/core/esm/components/Pagination/Pagination.context.mjs
var import_react160, import_jsx_runtime125, PaginationProvider, usePaginationContext;
var init_Pagination_context = __esm({
  "node_modules/@mantine/core/esm/components/Pagination/Pagination.context.mjs"() {
    "use client";
    import_react160 = __toESM(require_react(), 1);
    init_create_safe_context();
    import_jsx_runtime125 = __toESM(require_jsx_runtime(), 1);
    init_clsx();
    [PaginationProvider, usePaginationContext] = createSafeContext(
      "Pagination.Root component was not found in tree"
    );
  }
});

// node_modules/@mantine/core/esm/components/Pagination/Pagination.module.css.mjs
var classes30;
var init_Pagination_module_css = __esm({
  "node_modules/@mantine/core/esm/components/Pagination/Pagination.module.css.mjs"() {
    "use client";
    classes30 = { "root": "m_4addd315", "control": "m_326d024a", "dots": "m_4ad7767d" };
  }
});

// node_modules/@mantine/core/esm/components/Pagination/PaginationControl/PaginationControl.mjs
var import_jsx_runtime126, import_react161, defaultProps60, PaginationControl;
var init_PaginationControl = __esm({
  "node_modules/@mantine/core/esm/components/Pagination/PaginationControl/PaginationControl.mjs"() {
    "use client";
    import_jsx_runtime126 = __toESM(require_jsx_runtime(), 1);
    import_react161 = __toESM(require_react(), 1);
    init_clsx();
    init_use_props();
    init_factory();
    init_UnstyledButton();
    init_Pagination_context();
    init_Pagination_module_css();
    defaultProps60 = {
      withPadding: true
    };
    PaginationControl = factory((_props, ref) => {
      const props = useProps("PaginationControl", defaultProps60, _props);
      const {
        classNames,
        className,
        style,
        styles,
        vars,
        active,
        disabled,
        withPadding,
        mod,
        ...others
      } = props;
      const ctx = usePaginationContext();
      const _disabled = disabled || ctx.disabled;
      return /* @__PURE__ */ (0, import_jsx_runtime126.jsx)(
        UnstyledButton,
        {
          ref,
          disabled: _disabled,
          mod: [{ active, disabled: _disabled, "with-padding": withPadding }, mod],
          ...ctx.getStyles("control", { className, style, classNames, styles, active: !_disabled }),
          ...others
        }
      );
    });
    PaginationControl.classes = classes30;
    PaginationControl.displayName = "@mantine/core/PaginationControl";
  }
});

// node_modules/@mantine/core/esm/components/Pagination/Pagination.icons.mjs
function PaginationIcon({ style, children, path, ...others }) {
  return /* @__PURE__ */ (0, import_jsx_runtime127.jsx)(
    "svg",
    {
      viewBox: "0 0 16 16",
      xmlns: "http://www.w3.org/2000/svg",
      style: {
        width: "calc(var(--pagination-control-size) / 1.8)",
        height: "calc(var(--pagination-control-size) / 1.8)",
        ...style
      },
      ...others,
      children: /* @__PURE__ */ (0, import_jsx_runtime127.jsx)("path", { d: path, fill: "currentColor" })
    }
  );
}
var import_jsx_runtime127, PaginationNextIcon, PaginationPreviousIcon, PaginationFirstIcon, PaginationLastIcon, PaginationDotsIcon;
var init_Pagination_icons = __esm({
  "node_modules/@mantine/core/esm/components/Pagination/Pagination.icons.mjs"() {
    "use client";
    import_jsx_runtime127 = __toESM(require_jsx_runtime(), 1);
    PaginationNextIcon = (props) => /* @__PURE__ */ (0, import_jsx_runtime127.jsx)(
      PaginationIcon,
      {
        ...props,
        path: "M8.781 8l-3.3-3.3.943-.943L10.667 8l-4.243 4.243-.943-.943 3.3-3.3z"
      }
    );
    PaginationPreviousIcon = (props) => /* @__PURE__ */ (0, import_jsx_runtime127.jsx)(
      PaginationIcon,
      {
        ...props,
        path: "M7.219 8l3.3 3.3-.943.943L5.333 8l4.243-4.243.943.943-3.3 3.3z"
      }
    );
    PaginationFirstIcon = (props) => /* @__PURE__ */ (0, import_jsx_runtime127.jsx)(
      PaginationIcon,
      {
        ...props,
        path: "M6.85355 3.85355C7.04882 3.65829 7.04882 3.34171 6.85355 3.14645C6.65829 2.95118 6.34171 2.95118 6.14645 3.14645L2.14645 7.14645C1.95118 7.34171 1.95118 7.65829 2.14645 7.85355L6.14645 11.8536C6.34171 12.0488 6.65829 12.0488 6.85355 11.8536C7.04882 11.6583 7.04882 11.3417 6.85355 11.1464L3.20711 7.5L6.85355 3.85355ZM12.8536 3.85355C13.0488 3.65829 13.0488 3.34171 12.8536 3.14645C12.6583 2.95118 12.3417 2.95118 12.1464 3.14645L8.14645 7.14645C7.95118 7.34171 7.95118 7.65829 8.14645 7.85355L12.1464 11.8536C12.3417 12.0488 12.6583 12.0488 12.8536 11.8536C13.0488 11.6583 13.0488 11.3417 12.8536 11.1464L9.20711 7.5L12.8536 3.85355Z"
      }
    );
    PaginationLastIcon = (props) => /* @__PURE__ */ (0, import_jsx_runtime127.jsx)(
      PaginationIcon,
      {
        ...props,
        path: "M2.14645 11.1464C1.95118 11.3417 1.95118 11.6583 2.14645 11.8536C2.34171 12.0488 2.65829 12.0488 2.85355 11.8536L6.85355 7.85355C7.04882 7.65829 7.04882 7.34171 6.85355 7.14645L2.85355 3.14645C2.65829 2.95118 2.34171 2.95118 2.14645 3.14645C1.95118 3.34171 1.95118 3.65829 2.14645 3.85355L5.79289 7.5L2.14645 11.1464ZM8.14645 11.1464C7.95118 11.3417 7.95118 11.6583 8.14645 11.8536C8.34171 12.0488 8.65829 12.0488 8.85355 11.8536L12.8536 7.85355C13.0488 7.65829 13.0488 7.34171 12.8536 7.14645L8.85355 3.14645C8.65829 2.95118 8.34171 2.95118 8.14645 3.14645C7.95118 3.34171 7.95118 3.65829 8.14645 3.85355L11.7929 7.5L8.14645 11.1464Z"
      }
    );
    PaginationDotsIcon = (props) => /* @__PURE__ */ (0, import_jsx_runtime127.jsx)(
      PaginationIcon,
      {
        ...props,
        path: "M2 8c0-.733.6-1.333 1.333-1.333.734 0 1.334.6 1.334 1.333s-.6 1.333-1.334 1.333C2.6 9.333 2 8.733 2 8zm9.333 0c0-.733.6-1.333 1.334-1.333C13.4 6.667 14 7.267 14 8s-.6 1.333-1.333 1.333c-.734 0-1.334-.6-1.334-1.333zM6.667 8c0-.733.6-1.333 1.333-1.333s1.333.6 1.333 1.333S8.733 9.333 8 9.333 6.667 8.733 6.667 8z"
      }
    );
  }
});

// node_modules/@mantine/core/esm/components/Pagination/PaginationDots/PaginationDots.mjs
var import_jsx_runtime128, import_react162, defaultProps61, PaginationDots;
var init_PaginationDots = __esm({
  "node_modules/@mantine/core/esm/components/Pagination/PaginationDots/PaginationDots.mjs"() {
    "use client";
    import_jsx_runtime128 = __toESM(require_jsx_runtime(), 1);
    import_react162 = __toESM(require_react(), 1);
    init_clsx();
    init_use_props();
    init_Box();
    init_factory();
    init_Pagination_context();
    init_Pagination_icons();
    init_Pagination_module_css();
    defaultProps61 = {
      icon: PaginationDotsIcon
    };
    PaginationDots = factory((_props, ref) => {
      const props = useProps("PaginationDots", defaultProps61, _props);
      const { classNames, className, style, styles, vars, icon: icon2, ...others } = props;
      const ctx = usePaginationContext();
      const Icon = icon2;
      return /* @__PURE__ */ (0, import_jsx_runtime128.jsx)(Box, { ref, ...ctx.getStyles("dots", { className, style, styles, classNames }), ...others, children: /* @__PURE__ */ (0, import_jsx_runtime128.jsx)(
        Icon,
        {
          style: {
            width: "calc(var(--pagination-control-size) / 1.8)",
            height: "calc(var(--pagination-control-size) / 1.8)"
          }
        }
      ) });
    });
    PaginationDots.classes = classes30;
    PaginationDots.displayName = "@mantine/core/PaginationDots";
  }
});

// node_modules/@mantine/core/esm/components/Pagination/PaginationEdges/PaginationEdges.mjs
function createEdgeComponent({ icon: icon2, name: name2, action, type: type2 }) {
  const defaultProps79 = { icon: icon2 };
  const Component2 = (0, import_react163.forwardRef)((props, ref) => {
    const { icon: _icon, ...others } = useProps(name2, defaultProps79, props);
    const Icon = _icon;
    const ctx = usePaginationContext();
    const disabled = type2 === "next" ? ctx.active === ctx.total : ctx.active === 1;
    return /* @__PURE__ */ (0, import_jsx_runtime129.jsx)(
      PaginationControl,
      {
        disabled: ctx.disabled || disabled,
        ref,
        onClick: ctx[action],
        withPadding: false,
        ...others,
        children: /* @__PURE__ */ (0, import_jsx_runtime129.jsx)(
          Icon,
          {
            className: "mantine-rotate-rtl",
            style: {
              width: "calc(var(--pagination-control-size) / 1.8)",
              height: "calc(var(--pagination-control-size) / 1.8)"
            }
          }
        )
      }
    );
  });
  Component2.displayName = `@mantine/core/${name2}`;
  return createPolymorphicComponent(Component2);
}
var import_jsx_runtime129, import_react163, PaginationNext, PaginationPrevious, PaginationFirst, PaginationLast;
var init_PaginationEdges = __esm({
  "node_modules/@mantine/core/esm/components/Pagination/PaginationEdges/PaginationEdges.mjs"() {
    "use client";
    import_jsx_runtime129 = __toESM(require_jsx_runtime(), 1);
    import_react163 = __toESM(require_react(), 1);
    init_clsx();
    init_use_props();
    init_create_polymorphic_component();
    init_Pagination_context();
    init_Pagination_icons();
    init_PaginationControl();
    PaginationNext = createEdgeComponent({
      icon: PaginationNextIcon,
      name: "PaginationNext",
      action: "onNext",
      type: "next"
    });
    PaginationPrevious = createEdgeComponent({
      icon: PaginationPreviousIcon,
      name: "PaginationPrevious",
      action: "onPrevious",
      type: "previous"
    });
    PaginationFirst = createEdgeComponent({
      icon: PaginationFirstIcon,
      name: "PaginationFirst",
      action: "onFirst",
      type: "previous"
    });
    PaginationLast = createEdgeComponent({
      icon: PaginationLastIcon,
      name: "PaginationLast",
      action: "onLast",
      type: "next"
    });
  }
});

// node_modules/@mantine/core/esm/components/Pagination/PaginationItems/PaginationItems.mjs
function PaginationItems({ dotsIcon }) {
  const ctx = usePaginationContext();
  const items = ctx.range.map((page, index3) => {
    if (page === "dots") {
      return /* @__PURE__ */ (0, import_jsx_runtime130.jsx)(PaginationDots, { icon: dotsIcon }, index3);
    }
    return /* @__PURE__ */ (0, import_jsx_runtime130.jsx)(
      PaginationControl,
      {
        active: page === ctx.active,
        "aria-current": page === ctx.active ? "page" : void 0,
        onClick: () => ctx.onChange(page),
        disabled: ctx.disabled,
        ...ctx.getItemProps?.(page),
        children: ctx.getItemProps?.(page)?.children ?? page
      },
      index3
    );
  });
  return /* @__PURE__ */ (0, import_jsx_runtime130.jsx)(import_jsx_runtime130.Fragment, { children: items });
}
var import_jsx_runtime130;
var init_PaginationItems = __esm({
  "node_modules/@mantine/core/esm/components/Pagination/PaginationItems/PaginationItems.mjs"() {
    "use client";
    import_jsx_runtime130 = __toESM(require_jsx_runtime(), 1);
    init_Pagination_context();
    init_PaginationControl();
    init_PaginationDots();
    PaginationItems.displayName = "@mantine/core/PaginationItems";
  }
});

// node_modules/@mantine/core/esm/components/Pagination/PaginationRoot/PaginationRoot.mjs
var import_jsx_runtime131, import_react164, defaultProps62, varsResolver29, PaginationRoot;
var init_PaginationRoot = __esm({
  "node_modules/@mantine/core/esm/components/Pagination/PaginationRoot/PaginationRoot.mjs"() {
    "use client";
    import_jsx_runtime131 = __toESM(require_jsx_runtime(), 1);
    init_esm();
    import_react164 = __toESM(require_react(), 1);
    init_get_size();
    init_create_event_handler();
    init_create_vars_resolver();
    init_clsx();
    init_get_theme_color();
    init_get_contrast_color();
    init_get_auto_contrast_value();
    init_use_props();
    init_use_styles();
    init_Box();
    init_factory();
    init_Pagination_context();
    init_Pagination_module_css();
    defaultProps62 = {
      siblings: 1,
      boundaries: 1
    };
    varsResolver29 = createVarsResolver(
      (theme2, { size: size4, radius, color: color2, autoContrast }) => ({
        root: {
          "--pagination-control-radius": radius === void 0 ? void 0 : getRadius(radius),
          "--pagination-control-size": getSize(size4, "pagination-control-size"),
          "--pagination-control-fz": getFontSize(size4),
          "--pagination-active-bg": color2 ? getThemeColor(color2, theme2) : void 0,
          "--pagination-active-color": getAutoContrastValue(autoContrast, theme2) ? getContrastColor({ color: color2, theme: theme2, autoContrast }) : void 0
        }
      })
    );
    PaginationRoot = factory((_props, ref) => {
      const props = useProps("PaginationRoot", defaultProps62, _props);
      const {
        classNames,
        className,
        style,
        styles,
        unstyled,
        vars,
        total,
        value,
        defaultValue: defaultValue2,
        onChange,
        disabled,
        siblings,
        boundaries,
        color: color2,
        radius,
        onNextPage,
        onPreviousPage,
        onFirstPage,
        onLastPage,
        getItemProps,
        autoContrast,
        ...others
      } = props;
      const getStyles = useStyles({
        name: "Pagination",
        classes: classes30,
        props,
        className,
        style,
        classNames,
        styles,
        unstyled,
        vars,
        varsResolver: varsResolver29
      });
      const { range: range2, setPage, next: next2, previous: previous2, active, first, last } = usePagination({
        page: value,
        initialPage: defaultValue2,
        onChange,
        total,
        siblings,
        boundaries
      });
      const handleNextPage = createEventHandler(onNextPage, next2);
      const handlePreviousPage = createEventHandler(onPreviousPage, previous2);
      const handleFirstPage = createEventHandler(onFirstPage, first);
      const handleLastPage = createEventHandler(onLastPage, last);
      return /* @__PURE__ */ (0, import_jsx_runtime131.jsx)(
        PaginationProvider,
        {
          value: {
            total,
            range: range2,
            active,
            disabled,
            getItemProps,
            onChange: setPage,
            onNext: handleNextPage,
            onPrevious: handlePreviousPage,
            onFirst: handleFirstPage,
            onLast: handleLastPage,
            getStyles
          },
          children: /* @__PURE__ */ (0, import_jsx_runtime131.jsx)(Box, { ref, ...getStyles("root"), ...others })
        }
      );
    });
    PaginationRoot.classes = classes30;
    PaginationRoot.displayName = "@mantine/core/PaginationRoot";
  }
});

// node_modules/@mantine/core/esm/components/Pagination/Pagination.mjs
var import_jsx_runtime132, import_react165, defaultProps63, Pagination;
var init_Pagination = __esm({
  "node_modules/@mantine/core/esm/components/Pagination/Pagination.mjs"() {
    "use client";
    import_jsx_runtime132 = __toESM(require_jsx_runtime(), 1);
    import_react165 = __toESM(require_react(), 1);
    init_clsx();
    init_use_props();
    init_factory();
    init_Group();
    init_PaginationControl();
    init_PaginationDots();
    init_PaginationEdges();
    init_PaginationItems();
    init_PaginationRoot();
    init_Pagination_module_css();
    defaultProps63 = {
      withControls: true,
      siblings: 1,
      boundaries: 1,
      gap: 8
    };
    Pagination = factory((_props, ref) => {
      const props = useProps("Pagination", defaultProps63, _props);
      const {
        withEdges,
        withControls,
        getControlProps,
        nextIcon,
        previousIcon,
        lastIcon,
        firstIcon,
        dotsIcon,
        total,
        gap,
        hideWithOnePage,
        ...others
      } = props;
      if (total <= 0 || hideWithOnePage && total === 1) {
        return null;
      }
      return /* @__PURE__ */ (0, import_jsx_runtime132.jsx)(PaginationRoot, { ref, total, ...others, children: /* @__PURE__ */ (0, import_jsx_runtime132.jsxs)(Group, { gap, children: [
        withEdges && /* @__PURE__ */ (0, import_jsx_runtime132.jsx)(PaginationFirst, { icon: firstIcon, ...getControlProps?.("first") }),
        withControls && /* @__PURE__ */ (0, import_jsx_runtime132.jsx)(PaginationPrevious, { icon: previousIcon, ...getControlProps?.("previous") }),
        /* @__PURE__ */ (0, import_jsx_runtime132.jsx)(PaginationItems, { dotsIcon }),
        withControls && /* @__PURE__ */ (0, import_jsx_runtime132.jsx)(PaginationNext, { icon: nextIcon, ...getControlProps?.("next") }),
        withEdges && /* @__PURE__ */ (0, import_jsx_runtime132.jsx)(PaginationLast, { icon: lastIcon, ...getControlProps?.("last") })
      ] }) });
    });
    Pagination.classes = classes30;
    Pagination.displayName = "@mantine/core/Pagination";
    Pagination.Root = PaginationRoot;
    Pagination.Control = PaginationControl;
    Pagination.Dots = PaginationDots;
    Pagination.First = PaginationFirst;
    Pagination.Last = PaginationLast;
    Pagination.Next = PaginationNext;
    Pagination.Previous = PaginationPrevious;
    Pagination.Items = PaginationItems;
  }
});

// node_modules/@mantine/core/esm/components/Progress/Progress.context.mjs
var import_react166, import_jsx_runtime133, ProgressProvider, useProgressContext;
var init_Progress_context = __esm({
  "node_modules/@mantine/core/esm/components/Progress/Progress.context.mjs"() {
    "use client";
    import_react166 = __toESM(require_react(), 1);
    init_create_safe_context();
    import_jsx_runtime133 = __toESM(require_jsx_runtime(), 1);
    init_clsx();
    [ProgressProvider, useProgressContext] = createSafeContext(
      "Progress.Root component was not found in tree"
    );
  }
});

// node_modules/@mantine/core/esm/components/Progress/Progress.module.css.mjs
var classes31;
var init_Progress_module_css = __esm({
  "node_modules/@mantine/core/esm/components/Progress/Progress.module.css.mjs"() {
    "use client";
    classes31 = { "root": "m_db6d6462", "section": "m_2242eb65", "stripes-animation": "m_81a374bd", "label": "m_91e40b74" };
  }
});

// node_modules/@mantine/core/esm/components/Progress/ProgressLabel/ProgressLabel.mjs
var import_jsx_runtime134, import_react167, defaultProps64, ProgressLabel;
var init_ProgressLabel = __esm({
  "node_modules/@mantine/core/esm/components/Progress/ProgressLabel/ProgressLabel.mjs"() {
    "use client";
    import_jsx_runtime134 = __toESM(require_jsx_runtime(), 1);
    import_react167 = __toESM(require_react(), 1);
    init_clsx();
    init_use_props();
    init_Box();
    init_factory();
    init_Progress_context();
    init_Progress_module_css();
    defaultProps64 = {};
    ProgressLabel = factory((props, ref) => {
      const { classNames, className, style, styles, vars, ...others } = useProps(
        "ProgressLabel",
        defaultProps64,
        props
      );
      const ctx = useProgressContext();
      return /* @__PURE__ */ (0, import_jsx_runtime134.jsx)(
        Box,
        {
          ref,
          ...ctx.getStyles("label", { className, style, classNames, styles }),
          ...others
        }
      );
    });
    ProgressLabel.classes = classes31;
    ProgressLabel.displayName = "@mantine/core/ProgressLabel";
  }
});

// node_modules/@mantine/core/esm/components/Progress/ProgressRoot/ProgressRoot.mjs
var import_jsx_runtime135, import_react168, defaultProps65, varsResolver30, ProgressRoot;
var init_ProgressRoot = __esm({
  "node_modules/@mantine/core/esm/components/Progress/ProgressRoot/ProgressRoot.mjs"() {
    "use client";
    import_jsx_runtime135 = __toESM(require_jsx_runtime(), 1);
    import_react168 = __toESM(require_react(), 1);
    init_get_size();
    init_create_vars_resolver();
    init_clsx();
    init_use_props();
    init_use_styles();
    init_Box();
    init_factory();
    init_Progress_context();
    init_Progress_module_css();
    defaultProps65 = {};
    varsResolver30 = createVarsResolver(
      (_2, { size: size4, radius, transitionDuration }) => ({
        root: {
          "--progress-size": getSize(size4, "progress-size"),
          "--progress-radius": radius === void 0 ? void 0 : getRadius(radius),
          "--progress-transition-duration": typeof transitionDuration === "number" ? `${transitionDuration}ms` : void 0
        }
      })
    );
    ProgressRoot = factory((_props, ref) => {
      const props = useProps("ProgressRoot", defaultProps65, _props);
      const {
        classNames,
        className,
        style,
        styles,
        unstyled,
        vars,
        autoContrast,
        transitionDuration,
        ...others
      } = props;
      const getStyles = useStyles({
        name: "Progress",
        classes: classes31,
        props,
        className,
        style,
        classNames,
        styles,
        unstyled,
        vars,
        varsResolver: varsResolver30
      });
      return /* @__PURE__ */ (0, import_jsx_runtime135.jsx)(ProgressProvider, { value: { getStyles, autoContrast }, children: /* @__PURE__ */ (0, import_jsx_runtime135.jsx)(Box, { ref, ...getStyles("root"), ...others }) });
    });
    ProgressRoot.classes = classes31;
    ProgressRoot.displayName = "@mantine/core/ProgressRoot";
  }
});

// node_modules/@mantine/core/esm/components/Progress/ProgressSection/ProgressSection.mjs
var import_jsx_runtime136, import_react169, defaultProps66, ProgressSection;
var init_ProgressSection = __esm({
  "node_modules/@mantine/core/esm/components/Progress/ProgressSection/ProgressSection.mjs"() {
    "use client";
    import_jsx_runtime136 = __toESM(require_jsx_runtime(), 1);
    import_react169 = __toESM(require_react(), 1);
    init_clsx();
    init_get_theme_color();
    init_get_contrast_color();
    init_get_auto_contrast_value();
    init_MantineThemeProvider();
    init_use_props();
    init_Box();
    init_factory();
    init_Progress_context();
    init_Progress_module_css();
    defaultProps66 = {
      withAria: true
    };
    ProgressSection = factory((props, ref) => {
      const {
        classNames,
        className,
        style,
        styles,
        vars,
        value,
        withAria,
        color: color2,
        striped,
        animated,
        mod,
        ...others
      } = useProps("ProgressSection", defaultProps66, props);
      const ctx = useProgressContext();
      const theme2 = useMantineTheme();
      const ariaAttributes = withAria ? {
        role: "progressbar",
        "aria-valuemax": 100,
        "aria-valuemin": 0,
        "aria-valuenow": value,
        "aria-valuetext": `${value}%`
      } : {};
      return /* @__PURE__ */ (0, import_jsx_runtime136.jsx)(
        Box,
        {
          ref,
          ...ctx.getStyles("section", { className, classNames, styles, style }),
          ...others,
          ...ariaAttributes,
          mod: [{ striped: striped || animated, animated }, mod],
          __vars: {
            "--progress-section-width": `${value}%`,
            "--progress-section-color": getThemeColor(color2, theme2),
            "--progress-label-color": getAutoContrastValue(ctx.autoContrast, theme2) ? getContrastColor({ color: color2, theme: theme2, autoContrast: ctx.autoContrast }) : void 0
          }
        }
      );
    });
    ProgressSection.classes = classes31;
    ProgressSection.displayName = "@mantine/core/ProgressSection";
  }
});

// node_modules/@mantine/core/esm/components/Progress/Progress.mjs
var import_jsx_runtime137, import_react170, defaultProps67, Progress;
var init_Progress = __esm({
  "node_modules/@mantine/core/esm/components/Progress/Progress.mjs"() {
    "use client";
    import_jsx_runtime137 = __toESM(require_jsx_runtime(), 1);
    import_react170 = __toESM(require_react(), 1);
    init_clsx();
    init_use_resolved_styles_api();
    init_use_props();
    init_factory();
    init_ProgressLabel();
    init_ProgressRoot();
    init_ProgressSection();
    init_Progress_module_css();
    defaultProps67 = {};
    Progress = factory((_props, ref) => {
      const props = useProps("Progress", defaultProps67, _props);
      const {
        value,
        classNames,
        styles,
        vars,
        color: color2,
        striped,
        animated,
        "aria-label": label4,
        ...others
      } = props;
      const { resolvedClassNames, resolvedStyles } = useResolvedStylesApi({
        classNames,
        styles,
        props
      });
      return /* @__PURE__ */ (0, import_jsx_runtime137.jsx)(
        ProgressRoot,
        {
          ref,
          classNames: resolvedClassNames,
          styles: resolvedStyles,
          vars,
          ...others,
          children: /* @__PURE__ */ (0, import_jsx_runtime137.jsx)(
            ProgressSection,
            {
              value,
              color: color2,
              striped,
              animated,
              "aria-label": label4
            }
          )
        }
      );
    });
    Progress.classes = classes31;
    Progress.displayName = "@mantine/core/Progress";
    Progress.Section = ProgressSection;
    Progress.Root = ProgressRoot;
    Progress.Label = ProgressLabel;
  }
});

// node_modules/@mantine/core/esm/components/Radio/RadioGroup.context.mjs
var import_react171, import_jsx_runtime138, RadioGroupProvider, useRadioGroupContext;
var init_RadioGroup_context = __esm({
  "node_modules/@mantine/core/esm/components/Radio/RadioGroup.context.mjs"() {
    "use client";
    import_react171 = __toESM(require_react(), 1);
    import_jsx_runtime138 = __toESM(require_jsx_runtime(), 1);
    init_create_optional_context();
    init_clsx();
    [RadioGroupProvider, useRadioGroupContext] = createOptionalContext();
  }
});

// node_modules/@mantine/core/esm/components/Radio/RadioCard/RadioCard.context.mjs
var import_react172, import_jsx_runtime139, RadioCardProvider, useRadioCardContext;
var init_RadioCard_context = __esm({
  "node_modules/@mantine/core/esm/components/Radio/RadioCard/RadioCard.context.mjs"() {
    "use client";
    import_react172 = __toESM(require_react(), 1);
    import_jsx_runtime139 = __toESM(require_jsx_runtime(), 1);
    init_create_optional_context();
    init_clsx();
    [RadioCardProvider, useRadioCardContext] = createOptionalContext();
  }
});

// node_modules/@mantine/core/esm/components/Radio/RadioCard/RadioCard.module.css.mjs
var classes32;
var init_RadioCard_module_css = __esm({
  "node_modules/@mantine/core/esm/components/Radio/RadioCard/RadioCard.module.css.mjs"() {
    "use client";
    classes32 = { "card": "m_9dc8ae12" };
  }
});

// node_modules/@mantine/core/esm/components/Radio/RadioCard/RadioCard.mjs
var import_jsx_runtime140, import_react173, defaultProps68, varsResolver31, RadioCard;
var init_RadioCard = __esm({
  "node_modules/@mantine/core/esm/components/Radio/RadioCard/RadioCard.mjs"() {
    "use client";
    import_jsx_runtime140 = __toESM(require_jsx_runtime(), 1);
    import_react173 = __toESM(require_react(), 1);
    init_get_size();
    init_create_vars_resolver();
    init_clsx();
    init_use_props();
    init_use_styles();
    init_factory();
    init_DirectionProvider();
    init_UnstyledButton();
    init_RadioGroup_context();
    init_RadioCard_context();
    init_RadioCard_module_css();
    defaultProps68 = {
      withBorder: true
    };
    varsResolver31 = createVarsResolver((_2, { radius }) => ({
      card: {
        "--card-radius": getRadius(radius)
      }
    }));
    RadioCard = factory((_props, ref) => {
      const props = useProps("RadioCard", defaultProps68, _props);
      const {
        classNames,
        className,
        style,
        styles,
        unstyled,
        vars,
        checked,
        mod,
        withBorder,
        value,
        onClick,
        name: name2,
        onKeyDown,
        ...others
      } = props;
      const getStyles = useStyles({
        name: "RadioCard",
        classes: classes32,
        props,
        className,
        style,
        classNames,
        styles,
        unstyled,
        vars,
        varsResolver: varsResolver31,
        rootSelector: "card"
      });
      const { dir } = useDirection();
      const ctx = useRadioGroupContext();
      const _checked = typeof checked === "boolean" ? checked : ctx?.value === value || false;
      const _name = name2 || ctx?.name;
      const handleKeyDown = (event) => {
        onKeyDown?.(event);
        if (["ArrowDown", "ArrowUp", "ArrowLeft", "ArrowRight"].includes(event.nativeEvent.code)) {
          event.preventDefault();
          const siblings = Array.from(
            document.querySelectorAll(
              `[role="radio"][name="${_name || "__mantine"}"]`
            )
          );
          const currentIndex = siblings.findIndex((element) => element === event.target);
          const nextIndex = currentIndex + 1 >= siblings.length ? 0 : currentIndex + 1;
          const prevIndex = currentIndex - 1 < 0 ? siblings.length - 1 : currentIndex - 1;
          if (event.nativeEvent.code === "ArrowDown") {
            siblings[nextIndex].focus();
            siblings[nextIndex].click();
          }
          if (event.nativeEvent.code === "ArrowUp") {
            siblings[prevIndex].focus();
            siblings[prevIndex].click();
          }
          if (event.nativeEvent.code === "ArrowLeft") {
            siblings[dir === "ltr" ? prevIndex : nextIndex].focus();
            siblings[dir === "ltr" ? prevIndex : nextIndex].click();
          }
          if (event.nativeEvent.code === "ArrowRight") {
            siblings[dir === "ltr" ? nextIndex : prevIndex].focus();
            siblings[dir === "ltr" ? nextIndex : prevIndex].click();
          }
        }
      };
      return /* @__PURE__ */ (0, import_jsx_runtime140.jsx)(RadioCardProvider, { value: { checked: _checked }, children: /* @__PURE__ */ (0, import_jsx_runtime140.jsx)(
        UnstyledButton,
        {
          ref,
          mod: [{ "with-border": withBorder, checked: _checked }, mod],
          ...getStyles("card"),
          ...others,
          role: "radio",
          "aria-checked": _checked,
          name: _name,
          onClick: (event) => {
            onClick?.(event);
            ctx?.onChange(value || "");
          },
          onKeyDown: handleKeyDown
        }
      ) });
    });
    RadioCard.displayName = "@mantine/core/RadioCard";
    RadioCard.classes = classes32;
  }
});

// node_modules/@mantine/core/esm/components/Radio/RadioGroup/RadioGroup.mjs
var import_jsx_runtime141, import_react174, defaultProps69, RadioGroup;
var init_RadioGroup = __esm({
  "node_modules/@mantine/core/esm/components/Radio/RadioGroup/RadioGroup.mjs"() {
    "use client";
    import_jsx_runtime141 = __toESM(require_jsx_runtime(), 1);
    init_esm();
    import_react174 = __toESM(require_react(), 1);
    init_clsx();
    init_use_props();
    init_factory();
    init_Input();
    init_InputsGroupFieldset();
    init_RadioGroup_context();
    defaultProps69 = {};
    RadioGroup = factory((props, ref) => {
      const { value, defaultValue: defaultValue2, onChange, size: size4, wrapperProps, children, name: name2, readOnly, ...others } = useProps("RadioGroup", defaultProps69, props);
      const _name = useId(name2);
      const [_value, setValue] = useUncontrolled({
        value,
        defaultValue: defaultValue2,
        finalValue: "",
        onChange
      });
      const handleChange = (event) => !readOnly && setValue(typeof event === "string" ? event : event.currentTarget.value);
      return /* @__PURE__ */ (0, import_jsx_runtime141.jsx)(RadioGroupProvider, { value: { value: _value, onChange: handleChange, size: size4, name: _name }, children: /* @__PURE__ */ (0, import_jsx_runtime141.jsx)(
        Input.Wrapper,
        {
          size: size4,
          ref,
          ...wrapperProps,
          ...others,
          labelElement: "div",
          __staticSelector: "RadioGroup",
          children: /* @__PURE__ */ (0, import_jsx_runtime141.jsx)(InputsGroupFieldset, { role: "radiogroup", children })
        }
      ) });
    });
    RadioGroup.classes = Input.Wrapper.classes;
    RadioGroup.displayName = "@mantine/core/RadioGroup";
  }
});

// node_modules/@mantine/core/esm/components/Radio/RadioIcon.mjs
function RadioIcon({ size: size4, style, ...others }) {
  return /* @__PURE__ */ (0, import_jsx_runtime142.jsx)(
    "svg",
    {
      xmlns: "http://www.w3.org/2000/svg",
      fill: "none",
      viewBox: "0 0 5 5",
      style: { width: rem(size4), height: rem(size4), ...style },
      "aria-hidden": true,
      ...others,
      children: /* @__PURE__ */ (0, import_jsx_runtime142.jsx)("circle", { cx: "2.5", cy: "2.5", r: "2.5", fill: "currentColor" })
    }
  );
}
var import_jsx_runtime142, import_react175;
var init_RadioIcon = __esm({
  "node_modules/@mantine/core/esm/components/Radio/RadioIcon.mjs"() {
    "use client";
    import_jsx_runtime142 = __toESM(require_jsx_runtime(), 1);
    init_rem();
    import_react175 = __toESM(require_react(), 1);
    init_clsx();
  }
});

// node_modules/@mantine/core/esm/components/Radio/RadioIndicator/RadioIndicator.module.css.mjs
var classes33;
var init_RadioIndicator_module_css = __esm({
  "node_modules/@mantine/core/esm/components/Radio/RadioIndicator/RadioIndicator.module.css.mjs"() {
    "use client";
    classes33 = { "indicator": "m_717d7ff6", "icon": "m_3e4da632", "indicator--outline": "m_2980836c" };
  }
});

// node_modules/@mantine/core/esm/components/Radio/RadioIndicator/RadioIndicator.mjs
var import_jsx_runtime143, import_react176, defaultProps70, varsResolver32, RadioIndicator;
var init_RadioIndicator = __esm({
  "node_modules/@mantine/core/esm/components/Radio/RadioIndicator/RadioIndicator.mjs"() {
    "use client";
    import_jsx_runtime143 = __toESM(require_jsx_runtime(), 1);
    import_react176 = __toESM(require_react(), 1);
    init_get_size();
    init_create_vars_resolver();
    init_clsx();
    init_parse_theme_color();
    init_get_theme_color();
    init_get_contrast_color();
    init_get_auto_contrast_value();
    init_use_props();
    init_use_styles();
    init_Box();
    init_factory();
    init_RadioCard_context();
    init_RadioIcon();
    init_RadioIndicator_module_css();
    defaultProps70 = {
      icon: RadioIcon
    };
    varsResolver32 = createVarsResolver(
      (theme2, { radius, color: color2, size: size4, iconColor, variant, autoContrast }) => {
        const parsedColor = parseThemeColor({ color: color2 || theme2.primaryColor, theme: theme2 });
        const outlineColor = parsedColor.isThemeColor && parsedColor.shade === void 0 ? `var(--mantine-color-${parsedColor.color}-outline)` : parsedColor.color;
        return {
          indicator: {
            "--radio-size": getSize(size4, "radio-size"),
            "--radio-radius": radius === void 0 ? void 0 : getRadius(radius),
            "--radio-color": variant === "outline" ? outlineColor : getThemeColor(color2, theme2),
            "--radio-icon-size": getSize(size4, "radio-icon-size"),
            "--radio-icon-color": iconColor ? getThemeColor(iconColor, theme2) : getAutoContrastValue(autoContrast, theme2) ? getContrastColor({ color: color2, theme: theme2, autoContrast }) : void 0
          }
        };
      }
    );
    RadioIndicator = factory((_props, ref) => {
      const props = useProps("RadioIndicator", defaultProps70, _props);
      const {
        classNames,
        className,
        style,
        styles,
        unstyled,
        vars,
        icon: icon2,
        radius,
        color: color2,
        iconColor,
        autoContrast,
        checked,
        mod,
        variant,
        disabled,
        ...others
      } = props;
      const Icon = icon2;
      const getStyles = useStyles({
        name: "RadioIndicator",
        classes: classes33,
        props,
        className,
        style,
        classNames,
        styles,
        unstyled,
        vars,
        varsResolver: varsResolver32,
        rootSelector: "indicator"
      });
      const ctx = useRadioCardContext();
      const _checked = typeof checked === "boolean" ? checked : ctx?.checked || false;
      return /* @__PURE__ */ (0, import_jsx_runtime143.jsx)(
        Box,
        {
          ref,
          ...getStyles("indicator", { variant }),
          variant,
          mod: [{ checked: _checked, disabled }, mod],
          ...others,
          children: /* @__PURE__ */ (0, import_jsx_runtime143.jsx)(Icon, { ...getStyles("icon") })
        }
      );
    });
    RadioIndicator.displayName = "@mantine/core/RadioIndicator";
    RadioIndicator.classes = classes33;
  }
});

// node_modules/@mantine/core/esm/components/Radio/Radio.module.css.mjs
var classes34;
var init_Radio_module_css = __esm({
  "node_modules/@mantine/core/esm/components/Radio/Radio.module.css.mjs"() {
    "use client";
    classes34 = { "root": "m_f3f1af94", "inner": "m_89c4f5e4", "icon": "m_f3ed6b2b", "radio": "m_8a3dbb89", "radio--outline": "m_1bfe9d39" };
  }
});

// node_modules/@mantine/core/esm/components/Radio/Radio.mjs
var import_jsx_runtime144, import_react177, defaultProps71, varsResolver33, Radio;
var init_Radio = __esm({
  "node_modules/@mantine/core/esm/components/Radio/Radio.mjs"() {
    "use client";
    import_jsx_runtime144 = __toESM(require_jsx_runtime(), 1);
    init_esm();
    import_react177 = __toESM(require_react(), 1);
    init_get_size();
    init_create_vars_resolver();
    init_clsx();
    init_parse_theme_color();
    init_get_theme_color();
    init_get_contrast_color();
    init_get_auto_contrast_value();
    init_use_props();
    init_use_styles();
    init_extract_style_props();
    init_Box();
    init_factory();
    init_InlineInput();
    init_RadioCard();
    init_RadioGroup_context();
    init_RadioGroup();
    init_RadioIcon();
    init_RadioIndicator();
    init_Radio_module_css();
    defaultProps71 = {
      labelPosition: "right"
    };
    varsResolver33 = createVarsResolver(
      (theme2, { size: size4, radius, color: color2, iconColor, variant, autoContrast }) => {
        const parsedColor = parseThemeColor({ color: color2 || theme2.primaryColor, theme: theme2 });
        const outlineColor = parsedColor.isThemeColor && parsedColor.shade === void 0 ? `var(--mantine-color-${parsedColor.color}-outline)` : parsedColor.color;
        return {
          root: {
            "--radio-size": getSize(size4, "radio-size"),
            "--radio-radius": radius === void 0 ? void 0 : getRadius(radius),
            "--radio-color": variant === "outline" ? outlineColor : getThemeColor(color2, theme2),
            "--radio-icon-color": iconColor ? getThemeColor(iconColor, theme2) : getAutoContrastValue(autoContrast, theme2) ? getContrastColor({ color: color2, theme: theme2, autoContrast }) : void 0,
            "--radio-icon-size": getSize(size4, "radio-icon-size")
          }
        };
      }
    );
    Radio = factory((_props, ref) => {
      const props = useProps("Radio", defaultProps71, _props);
      const {
        classNames,
        className,
        style,
        styles,
        unstyled,
        vars,
        id: id3,
        size: size4,
        label: label4,
        labelPosition,
        description,
        error,
        radius,
        color: color2,
        variant,
        disabled,
        wrapperProps,
        icon: Icon = RadioIcon,
        rootRef,
        iconColor,
        onChange,
        mod,
        ...others
      } = props;
      const getStyles = useStyles({
        name: "Radio",
        classes: classes34,
        props,
        className,
        style,
        classNames,
        styles,
        unstyled,
        vars,
        varsResolver: varsResolver33
      });
      const ctx = useRadioGroupContext();
      const contextSize = ctx?.size ?? size4;
      const componentSize = props.size ? size4 : contextSize;
      const { styleProps, rest } = extractStyleProps(others);
      const uuid = useId(id3);
      const contextProps = ctx ? {
        checked: ctx.value === rest.value,
        name: rest.name ?? ctx.name,
        onChange: (event) => {
          ctx.onChange(event);
          onChange?.(event);
        }
      } : {};
      return /* @__PURE__ */ (0, import_jsx_runtime144.jsx)(
        InlineInput,
        {
          ...getStyles("root"),
          __staticSelector: "Radio",
          __stylesApiProps: props,
          id: uuid,
          size: componentSize,
          labelPosition,
          label: label4,
          description,
          error,
          disabled,
          classNames,
          styles,
          unstyled,
          "data-checked": contextProps.checked || void 0,
          variant,
          ref: rootRef,
          mod,
          ...styleProps,
          ...wrapperProps,
          children: /* @__PURE__ */ (0, import_jsx_runtime144.jsxs)(Box, { ...getStyles("inner"), mod: { "label-position": labelPosition }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime144.jsx)(
              Box,
              {
                ...getStyles("radio", { focusable: true, variant }),
                onChange,
                ...rest,
                ...contextProps,
                component: "input",
                mod: { error: !!error },
                ref,
                id: uuid,
                disabled,
                type: "radio"
              }
            ),
            /* @__PURE__ */ (0, import_jsx_runtime144.jsx)(Icon, { ...getStyles("icon"), "aria-hidden": true })
          ] })
        }
      );
    });
    Radio.classes = classes34;
    Radio.displayName = "@mantine/core/Radio";
    Radio.Group = RadioGroup;
    Radio.Card = RadioCard;
    Radio.Indicator = RadioIndicator;
  }
});

// node_modules/@mantine/core/esm/components/Skeleton/Skeleton.module.css.mjs
var classes35;
var init_Skeleton_module_css = __esm({
  "node_modules/@mantine/core/esm/components/Skeleton/Skeleton.module.css.mjs"() {
    "use client";
    classes35 = { "root": "m_18320242", "skeleton-fade": "m_299c329c" };
  }
});

// node_modules/@mantine/core/esm/components/Skeleton/Skeleton.mjs
var import_jsx_runtime145, import_react178, defaultProps72, varsResolver34, Skeleton;
var init_Skeleton = __esm({
  "node_modules/@mantine/core/esm/components/Skeleton/Skeleton.mjs"() {
    "use client";
    import_jsx_runtime145 = __toESM(require_jsx_runtime(), 1);
    init_rem();
    import_react178 = __toESM(require_react(), 1);
    init_get_size();
    init_create_vars_resolver();
    init_clsx();
    init_use_props();
    init_use_styles();
    init_Box();
    init_factory();
    init_Skeleton_module_css();
    defaultProps72 = {
      visible: true,
      animate: true
    };
    varsResolver34 = createVarsResolver(
      (_2, { width, height, radius, circle }) => ({
        root: {
          "--skeleton-height": rem(height),
          "--skeleton-width": circle ? rem(height) : rem(width),
          "--skeleton-radius": circle ? "1000px" : radius === void 0 ? void 0 : getRadius(radius)
        }
      })
    );
    Skeleton = factory((_props, ref) => {
      const props = useProps("Skeleton", defaultProps72, _props);
      const {
        classNames,
        className,
        style,
        styles,
        unstyled,
        vars,
        width,
        height,
        circle,
        visible: visible2,
        radius,
        animate,
        mod,
        ...others
      } = props;
      const getStyles = useStyles({
        name: "Skeleton",
        classes: classes35,
        props,
        className,
        style,
        classNames,
        styles,
        unstyled,
        vars,
        varsResolver: varsResolver34
      });
      return /* @__PURE__ */ (0, import_jsx_runtime145.jsx)(Box, { ref, ...getStyles("root"), mod: [{ visible: visible2, animate }, mod], ...others });
    });
    Skeleton.classes = classes35;
    Skeleton.displayName = "@mantine/core/Skeleton";
  }
});

// node_modules/@mantine/core/esm/components/Slider/Slider.context.mjs
var import_react179, import_jsx_runtime146, SliderProvider, useSliderContext;
var init_Slider_context = __esm({
  "node_modules/@mantine/core/esm/components/Slider/Slider.context.mjs"() {
    "use client";
    import_react179 = __toESM(require_react(), 1);
    init_create_safe_context();
    import_jsx_runtime146 = __toESM(require_jsx_runtime(), 1);
    init_clsx();
    [SliderProvider, useSliderContext] = createSafeContext(
      "SliderProvider was not found in tree"
    );
  }
});

// node_modules/@mantine/core/esm/components/Slider/SliderRoot/SliderRoot.mjs
var import_jsx_runtime147, import_react180, SliderRoot;
var init_SliderRoot = __esm({
  "node_modules/@mantine/core/esm/components/Slider/SliderRoot/SliderRoot.mjs"() {
    "use client";
    import_jsx_runtime147 = __toESM(require_jsx_runtime(), 1);
    import_react180 = __toESM(require_react(), 1);
    init_clsx();
    init_Box();
    init_Slider_context();
    SliderRoot = (0, import_react180.forwardRef)(
      ({ size: size4, disabled, variant, color: color2, thumbSize, radius, ...others }, ref) => {
        const { getStyles } = useSliderContext();
        return /* @__PURE__ */ (0, import_jsx_runtime147.jsx)(
          Box,
          {
            tabIndex: -1,
            variant,
            size: size4,
            ref,
            ...getStyles("root"),
            ...others
          }
        );
      }
    );
    SliderRoot.displayName = "@mantine/core/SliderRoot";
  }
});

// node_modules/@mantine/core/esm/components/Slider/Thumb/Thumb.mjs
var import_jsx_runtime148, import_react181, Thumb;
var init_Thumb = __esm({
  "node_modules/@mantine/core/esm/components/Slider/Thumb/Thumb.mjs"() {
    "use client";
    import_jsx_runtime148 = __toESM(require_jsx_runtime(), 1);
    import_react181 = __toESM(require_react(), 1);
    init_clsx();
    init_Box();
    init_Transition();
    init_Slider_context();
    Thumb = (0, import_react181.forwardRef)(
      ({
        max: max2,
        min: min2,
        value,
        position,
        label: label4,
        dragging,
        onMouseDown,
        onKeyDownCapture,
        labelTransitionProps,
        labelAlwaysOn,
        thumbLabel,
        onFocus,
        onBlur,
        showLabelOnHover,
        isHovered,
        children = null,
        disabled
      }, ref) => {
        const { getStyles } = useSliderContext();
        const [focused, setFocused] = (0, import_react181.useState)(false);
        const isVisible = labelAlwaysOn || dragging || focused || showLabelOnHover && isHovered;
        return /* @__PURE__ */ (0, import_jsx_runtime148.jsxs)(
          Box,
          {
            tabIndex: 0,
            role: "slider",
            "aria-label": thumbLabel,
            "aria-valuemax": max2,
            "aria-valuemin": min2,
            "aria-valuenow": value,
            ref,
            __vars: { "--slider-thumb-offset": `${position}%` },
            ...getStyles("thumb", { focusable: true }),
            mod: { dragging, disabled },
            onFocus: () => {
              setFocused(true);
              typeof onFocus === "function" && onFocus();
            },
            onBlur: () => {
              setFocused(false);
              typeof onBlur === "function" && onBlur();
            },
            onTouchStart: onMouseDown,
            onMouseDown,
            onKeyDownCapture,
            onClick: (event) => event.stopPropagation(),
            children: [
              children,
              /* @__PURE__ */ (0, import_jsx_runtime148.jsx)(
                Transition,
                {
                  mounted: label4 != null && !!isVisible,
                  transition: "fade",
                  duration: 0,
                  ...labelTransitionProps,
                  children: (transitionStyles) => /* @__PURE__ */ (0, import_jsx_runtime148.jsx)("div", { ...getStyles("label", { style: transitionStyles }), children: label4 })
                }
              )
            ]
          }
        );
      }
    );
    Thumb.displayName = "@mantine/core/SliderThumb";
  }
});

// node_modules/@mantine/core/esm/components/Slider/utils/get-position/get-position.mjs
function getPosition({ value, min: min2, max: max2 }) {
  const position = (value - min2) / (max2 - min2) * 100;
  return Math.min(Math.max(position, 0), 100);
}
var init_get_position = __esm({
  "node_modules/@mantine/core/esm/components/Slider/utils/get-position/get-position.mjs"() {
    "use client";
  }
});

// node_modules/@mantine/core/esm/components/Slider/Marks/is-mark-filled.mjs
function isMarkFilled({ mark, offset: offset4, value, inverted = false }) {
  return inverted ? typeof offset4 === "number" ? mark.value <= offset4 || mark.value >= value : mark.value >= value : typeof offset4 === "number" ? mark.value >= offset4 && mark.value <= value : mark.value <= value;
}
var init_is_mark_filled = __esm({
  "node_modules/@mantine/core/esm/components/Slider/Marks/is-mark-filled.mjs"() {
    "use client";
  }
});

// node_modules/@mantine/core/esm/components/Slider/Marks/Marks.mjs
function Marks({ marks, min: min2, max: max2, disabled, value, offset: offset4, inverted }) {
  const { getStyles } = useSliderContext();
  if (!marks) {
    return null;
  }
  const items = marks.map((mark, index3) => /* @__PURE__ */ (0, import_react182.createElement)(
    Box,
    {
      ...getStyles("markWrapper"),
      __vars: { "--mark-offset": `${getPosition({ value: mark.value, min: min2, max: max2 })}%` },
      key: index3
    },
    /* @__PURE__ */ (0, import_jsx_runtime149.jsx)(
      Box,
      {
        ...getStyles("mark"),
        mod: { filled: isMarkFilled({ mark, value, offset: offset4, inverted }), disabled }
      }
    ),
    mark.label && /* @__PURE__ */ (0, import_jsx_runtime149.jsx)("div", { ...getStyles("markLabel"), children: mark.label })
  ));
  return /* @__PURE__ */ (0, import_jsx_runtime149.jsx)("div", { children: items });
}
var import_jsx_runtime149, import_react182;
var init_Marks = __esm({
  "node_modules/@mantine/core/esm/components/Slider/Marks/Marks.mjs"() {
    "use client";
    import_jsx_runtime149 = __toESM(require_jsx_runtime(), 1);
    import_react182 = __toESM(require_react(), 1);
    init_clsx();
    init_Box();
    init_Slider_context();
    init_get_position();
    init_is_mark_filled();
    Marks.displayName = "@mantine/core/SliderMarks";
  }
});

// node_modules/@mantine/core/esm/components/Slider/Track/Track.mjs
function Track({
  filled,
  children,
  offset: offset4,
  disabled,
  marksOffset,
  inverted,
  containerProps,
  ...others
}) {
  const { getStyles } = useSliderContext();
  return /* @__PURE__ */ (0, import_jsx_runtime150.jsx)(Box, { ...getStyles("trackContainer"), mod: { disabled }, ...containerProps, children: /* @__PURE__ */ (0, import_jsx_runtime150.jsxs)(Box, { ...getStyles("track"), mod: { inverted, disabled }, children: [
    /* @__PURE__ */ (0, import_jsx_runtime150.jsx)(
      Box,
      {
        mod: { inverted, disabled },
        __vars: {
          "--slider-bar-width": `calc(${filled}% + var(--slider-size))`,
          "--slider-bar-offset": `calc(${offset4}% - var(--slider-size))`
        },
        ...getStyles("bar")
      }
    ),
    children,
    /* @__PURE__ */ (0, import_jsx_runtime150.jsx)(Marks, { ...others, offset: marksOffset, disabled, inverted })
  ] }) });
}
var import_jsx_runtime150, import_react183;
var init_Track = __esm({
  "node_modules/@mantine/core/esm/components/Slider/Track/Track.mjs"() {
    "use client";
    import_jsx_runtime150 = __toESM(require_jsx_runtime(), 1);
    import_react183 = __toESM(require_react(), 1);
    init_clsx();
    init_Box();
    init_Marks();
    init_Slider_context();
    Track.displayName = "@mantine/core/SliderTrack";
  }
});

// node_modules/@mantine/core/esm/components/Slider/utils/get-change-value/get-change-value.mjs
function getChangeValue({
  value,
  containerWidth,
  min: min2,
  max: max2,
  step,
  precision
}) {
  const left = !containerWidth ? value : Math.min(Math.max(value, 0), containerWidth) / containerWidth;
  const dx = left * (max2 - min2);
  const nextValue = (dx !== 0 ? Math.round(dx / step) * step : 0) + min2;
  const nextValueWithinStep = Math.max(nextValue, min2);
  if (precision !== void 0) {
    return Number(nextValueWithinStep.toFixed(precision));
  }
  return nextValueWithinStep;
}
var init_get_change_value = __esm({
  "node_modules/@mantine/core/esm/components/Slider/utils/get-change-value/get-change-value.mjs"() {
    "use client";
  }
});

// node_modules/@mantine/core/esm/components/Slider/utils/get-floating-value/get-gloating-value.mjs
function getFloatingValue(value, precision) {
  return parseFloat(value.toFixed(precision));
}
var init_get_gloating_value = __esm({
  "node_modules/@mantine/core/esm/components/Slider/utils/get-floating-value/get-gloating-value.mjs"() {
    "use client";
  }
});

// node_modules/@mantine/core/esm/components/Slider/utils/get-precision/get-precision.mjs
function getPrecision(step) {
  if (!step) {
    return 0;
  }
  const split = step.toString().split(".");
  return split.length > 1 ? split[1].length : 0;
}
var init_get_precision = __esm({
  "node_modules/@mantine/core/esm/components/Slider/utils/get-precision/get-precision.mjs"() {
    "use client";
  }
});

// node_modules/@mantine/core/esm/components/Slider/utils/get-step-mark-value/get-step-mark-value.mjs
function getNextMarkValue(currentValue, marks) {
  const sortedMarks = [...marks].sort((a18, b4) => a18.value - b4.value);
  const nextMark = sortedMarks.find((mark) => mark.value > currentValue);
  return nextMark ? nextMark.value : currentValue;
}
function getPreviousMarkValue(currentValue, marks) {
  const sortedMarks = [...marks].sort((a18, b4) => b4.value - a18.value);
  const previousMark = sortedMarks.find((mark) => mark.value < currentValue);
  return previousMark ? previousMark.value : currentValue;
}
function getFirstMarkValue(marks) {
  const sortedMarks = [...marks].sort((a18, b4) => b4.value - a18.value);
  return sortedMarks.length > 0 ? sortedMarks[0].value : 0;
}
function getLastMarkValue(marks) {
  const sortedMarks = [...marks].sort((a18, b4) => a18.value - b4.value);
  return sortedMarks.length > 0 ? sortedMarks[sortedMarks.length - 1].value : 100;
}
var init_get_step_mark_value = __esm({
  "node_modules/@mantine/core/esm/components/Slider/utils/get-step-mark-value/get-step-mark-value.mjs"() {
    "use client";
  }
});

// node_modules/@mantine/core/esm/components/Slider/Slider.module.css.mjs
var classes36;
var init_Slider_module_css = __esm({
  "node_modules/@mantine/core/esm/components/Slider/Slider.module.css.mjs"() {
    "use client";
    classes36 = { "root": "m_dd36362e", "label": "m_c9357328", "thumb": "m_c9a9a60a", "trackContainer": "m_a8645c2", "track": "m_c9ade57f", "bar": "m_38aeed47", "markWrapper": "m_b7b0423a", "mark": "m_dd33bc19", "markLabel": "m_68c77a5b" };
  }
});

// node_modules/@mantine/core/esm/components/Slider/Slider/Slider.mjs
var import_jsx_runtime151, import_react184, defaultProps73, varsResolver35, Slider;
var init_Slider = __esm({
  "node_modules/@mantine/core/esm/components/Slider/Slider/Slider.mjs"() {
    "use client";
    import_jsx_runtime151 = __toESM(require_jsx_runtime(), 1);
    import_react184 = __toESM(require_react(), 1);
    init_esm();
    init_rem();
    init_get_size();
    init_find_closest_number();
    init_create_vars_resolver();
    init_clsx();
    init_get_theme_color();
    init_use_props();
    init_use_styles();
    init_factory();
    init_DirectionProvider();
    init_Slider_context();
    init_SliderRoot();
    init_Thumb();
    init_Track();
    init_get_change_value();
    init_get_gloating_value();
    init_get_position();
    init_get_precision();
    init_get_step_mark_value();
    init_Slider_module_css();
    defaultProps73 = {
      radius: "xl",
      min: 0,
      max: 100,
      step: 1,
      marks: [],
      label: (f15) => f15,
      labelTransitionProps: { transition: "fade", duration: 0 },
      labelAlwaysOn: false,
      thumbLabel: "",
      showLabelOnHover: true,
      disabled: false,
      scale: (v5) => v5
    };
    varsResolver35 = createVarsResolver(
      (theme2, { size: size4, color: color2, thumbSize, radius }) => ({
        root: {
          "--slider-size": getSize(size4, "slider-size"),
          "--slider-color": color2 ? getThemeColor(color2, theme2) : void 0,
          "--slider-radius": radius === void 0 ? void 0 : getRadius(radius),
          "--slider-thumb-size": thumbSize !== void 0 ? rem(thumbSize) : "calc(var(--slider-size) * 2)"
        }
      })
    );
    Slider = factory((_props, ref) => {
      const props = useProps("Slider", defaultProps73, _props);
      const {
        classNames,
        styles,
        value,
        onChange,
        onChangeEnd,
        size: size4,
        min: min2,
        max: max2,
        step,
        precision: _precision,
        defaultValue: defaultValue2,
        name: name2,
        marks,
        label: label4,
        labelTransitionProps,
        labelAlwaysOn,
        thumbLabel,
        showLabelOnHover,
        thumbChildren,
        disabled,
        unstyled,
        scale: scale2,
        inverted,
        className,
        style,
        vars,
        hiddenInputProps,
        restrictToMarks,
        ...others
      } = props;
      const getStyles = useStyles({
        name: "Slider",
        props,
        classes: classes36,
        classNames,
        className,
        styles,
        style,
        vars,
        varsResolver: varsResolver35,
        unstyled
      });
      const { dir } = useDirection();
      const [hovered, setHovered] = (0, import_react184.useState)(false);
      const [_value, setValue] = useUncontrolled({
        value: typeof value === "number" ? clamp(value, min2, max2) : value,
        defaultValue: typeof defaultValue2 === "number" ? clamp(defaultValue2, min2, max2) : defaultValue2,
        finalValue: clamp(0, min2, max2),
        onChange
      });
      const valueRef = (0, import_react184.useRef)(_value);
      const root5 = (0, import_react184.useRef)();
      const thumb2 = (0, import_react184.useRef)();
      const position = getPosition({ value: _value, min: min2, max: max2 });
      const scaledValue = scale2(_value);
      const _label = typeof label4 === "function" ? label4(scaledValue) : label4;
      const precision = _precision ?? getPrecision(step);
      const handleChange = (0, import_react184.useCallback)(
        ({ x: x3 }) => {
          if (!disabled) {
            const nextValue = getChangeValue({
              value: x3,
              min: min2,
              max: max2,
              step,
              precision
            });
            setValue(
              restrictToMarks && marks?.length ? findClosestNumber(
                nextValue,
                marks.map((mark) => mark.value)
              ) : nextValue
            );
            valueRef.current = nextValue;
          }
        },
        [disabled, min2, max2, step, precision, setValue, marks, restrictToMarks]
      );
      const { ref: container4, active } = useMove(
        handleChange,
        {
          onScrubEnd: () => onChangeEnd?.(
            restrictToMarks && marks?.length ? findClosestNumber(
              valueRef.current,
              marks.map((mark) => mark.value)
            ) : valueRef.current
          )
        },
        dir
      );
      const handleTrackKeydownCapture = (event) => {
        if (!disabled) {
          switch (event.key) {
            case "ArrowUp": {
              event.preventDefault();
              thumb2.current?.focus();
              if (restrictToMarks && marks) {
                const nextValue2 = getNextMarkValue(_value, marks);
                setValue(nextValue2);
                onChangeEnd?.(nextValue2);
                break;
              }
              const nextValue = getFloatingValue(
                Math.min(Math.max(_value + step, min2), max2),
                precision
              );
              setValue(nextValue);
              onChangeEnd?.(nextValue);
              break;
            }
            case "ArrowRight": {
              event.preventDefault();
              thumb2.current?.focus();
              if (restrictToMarks && marks) {
                const nextValue2 = dir === "rtl" ? getPreviousMarkValue(_value, marks) : getNextMarkValue(_value, marks);
                setValue(nextValue2);
                onChangeEnd?.(nextValue2);
                break;
              }
              const nextValue = getFloatingValue(
                Math.min(Math.max(dir === "rtl" ? _value - step : _value + step, min2), max2),
                precision
              );
              setValue(nextValue);
              onChangeEnd?.(nextValue);
              break;
            }
            case "ArrowDown": {
              event.preventDefault();
              thumb2.current?.focus();
              if (restrictToMarks && marks) {
                const nextValue2 = getPreviousMarkValue(_value, marks);
                setValue(nextValue2);
                onChangeEnd?.(nextValue2);
                break;
              }
              const nextValue = getFloatingValue(
                Math.min(Math.max(_value - step, min2), max2),
                precision
              );
              setValue(nextValue);
              onChangeEnd?.(nextValue);
              break;
            }
            case "ArrowLeft": {
              event.preventDefault();
              thumb2.current?.focus();
              if (restrictToMarks && marks) {
                const nextValue2 = dir === "rtl" ? getNextMarkValue(_value, marks) : getPreviousMarkValue(_value, marks);
                setValue(nextValue2);
                onChangeEnd?.(nextValue2);
                break;
              }
              const nextValue = getFloatingValue(
                Math.min(Math.max(dir === "rtl" ? _value + step : _value - step, min2), max2),
                precision
              );
              setValue(nextValue);
              onChangeEnd?.(nextValue);
              break;
            }
            case "Home": {
              event.preventDefault();
              thumb2.current?.focus();
              if (restrictToMarks && marks) {
                setValue(getFirstMarkValue(marks));
                onChangeEnd?.(getFirstMarkValue(marks));
                break;
              }
              setValue(min2);
              onChangeEnd?.(min2);
              break;
            }
            case "End": {
              event.preventDefault();
              thumb2.current?.focus();
              if (restrictToMarks && marks) {
                setValue(getLastMarkValue(marks));
                onChangeEnd?.(getLastMarkValue(marks));
                break;
              }
              setValue(max2);
              onChangeEnd?.(max2);
              break;
            }
          }
        }
      };
      return /* @__PURE__ */ (0, import_jsx_runtime151.jsx)(SliderProvider, { value: { getStyles }, children: /* @__PURE__ */ (0, import_jsx_runtime151.jsxs)(
        SliderRoot,
        {
          ...others,
          ref: useMergedRef(ref, root5),
          onKeyDownCapture: handleTrackKeydownCapture,
          onMouseDownCapture: () => root5.current?.focus(),
          size: size4,
          disabled,
          children: [
            /* @__PURE__ */ (0, import_jsx_runtime151.jsx)(
              Track,
              {
                inverted,
                offset: 0,
                filled: position,
                marks,
                min: min2,
                max: max2,
                value: scaledValue,
                disabled,
                containerProps: {
                  ref: container4,
                  onMouseEnter: showLabelOnHover ? () => setHovered(true) : void 0,
                  onMouseLeave: showLabelOnHover ? () => setHovered(false) : void 0
                },
                children: /* @__PURE__ */ (0, import_jsx_runtime151.jsx)(
                  Thumb,
                  {
                    max: max2,
                    min: min2,
                    value: scaledValue,
                    position,
                    dragging: active,
                    label: _label,
                    ref: thumb2,
                    labelTransitionProps,
                    labelAlwaysOn,
                    thumbLabel,
                    showLabelOnHover,
                    isHovered: hovered,
                    disabled,
                    children: thumbChildren
                  }
                )
              }
            ),
            /* @__PURE__ */ (0, import_jsx_runtime151.jsx)("input", { type: "hidden", name: name2, value: scaledValue, ...hiddenInputProps })
          ]
        }
      ) });
    });
    Slider.classes = classes36;
    Slider.displayName = "@mantine/core/Slider";
  }
});

// node_modules/@mantine/core/esm/components/Stack/Stack.module.css.mjs
var classes37;
var init_Stack_module_css = __esm({
  "node_modules/@mantine/core/esm/components/Stack/Stack.module.css.mjs"() {
    "use client";
    classes37 = { "root": "m_6d731127" };
  }
});

// node_modules/@mantine/core/esm/components/Stack/Stack.mjs
var import_jsx_runtime152, import_react185, defaultProps74, varsResolver36, Stack;
var init_Stack = __esm({
  "node_modules/@mantine/core/esm/components/Stack/Stack.mjs"() {
    "use client";
    import_jsx_runtime152 = __toESM(require_jsx_runtime(), 1);
    import_react185 = __toESM(require_react(), 1);
    init_get_size();
    init_create_vars_resolver();
    init_clsx();
    init_use_props();
    init_use_styles();
    init_Box();
    init_factory();
    init_Stack_module_css();
    defaultProps74 = {
      gap: "md",
      align: "stretch",
      justify: "flex-start"
    };
    varsResolver36 = createVarsResolver((_2, { gap, align, justify }) => ({
      root: {
        "--stack-gap": getSpacing(gap),
        "--stack-align": align,
        "--stack-justify": justify
      }
    }));
    Stack = factory((_props, ref) => {
      const props = useProps("Stack", defaultProps74, _props);
      const {
        classNames,
        className,
        style,
        styles,
        unstyled,
        vars,
        align,
        justify,
        gap,
        variant,
        ...others
      } = props;
      const getStyles = useStyles({
        name: "Stack",
        props,
        classes: classes37,
        className,
        style,
        classNames,
        styles,
        unstyled,
        vars,
        varsResolver: varsResolver36
      });
      return /* @__PURE__ */ (0, import_jsx_runtime152.jsx)(Box, { ref, ...getStyles("root"), variant, ...others });
    });
    Stack.classes = classes37;
    Stack.displayName = "@mantine/core/Stack";
  }
});

// node_modules/@mantine/core/esm/components/TextInput/TextInput.mjs
var import_jsx_runtime153, import_react186, defaultProps75, TextInput;
var init_TextInput = __esm({
  "node_modules/@mantine/core/esm/components/TextInput/TextInput.mjs"() {
    "use client";
    import_jsx_runtime153 = __toESM(require_jsx_runtime(), 1);
    import_react186 = __toESM(require_react(), 1);
    init_clsx();
    init_use_props();
    init_factory();
    init_InputBase();
    defaultProps75 = {};
    TextInput = factory((props, ref) => {
      const _props = useProps("TextInput", defaultProps75, props);
      return /* @__PURE__ */ (0, import_jsx_runtime153.jsx)(InputBase, { component: "input", ref, ..._props, __staticSelector: "TextInput" });
    });
    TextInput.classes = InputBase.classes;
    TextInput.displayName = "@mantine/core/TextInput";
  }
});

// node_modules/@mantine/core/esm/components/ThemeIcon/ThemeIcon.module.css.mjs
var classes38;
var init_ThemeIcon_module_css = __esm({
  "node_modules/@mantine/core/esm/components/ThemeIcon/ThemeIcon.module.css.mjs"() {
    "use client";
    classes38 = { "root": "m_7341320d" };
  }
});

// node_modules/@mantine/core/esm/components/ThemeIcon/ThemeIcon.mjs
var import_jsx_runtime154, import_react187, defaultProps76, varsResolver37, ThemeIcon;
var init_ThemeIcon = __esm({
  "node_modules/@mantine/core/esm/components/ThemeIcon/ThemeIcon.mjs"() {
    "use client";
    import_jsx_runtime154 = __toESM(require_jsx_runtime(), 1);
    import_react187 = __toESM(require_react(), 1);
    init_get_size();
    init_create_vars_resolver();
    init_clsx();
    init_use_props();
    init_use_styles();
    init_Box();
    init_factory();
    init_ThemeIcon_module_css();
    defaultProps76 = {};
    varsResolver37 = createVarsResolver(
      (theme2, { size: size4, radius, variant, gradient, color: color2, autoContrast }) => {
        const colors = theme2.variantColorResolver({
          color: color2 || theme2.primaryColor,
          theme: theme2,
          gradient,
          variant: variant || "filled",
          autoContrast
        });
        return {
          root: {
            "--ti-size": getSize(size4, "ti-size"),
            "--ti-radius": radius === void 0 ? void 0 : getRadius(radius),
            "--ti-bg": color2 || variant ? colors.background : void 0,
            "--ti-color": color2 || variant ? colors.color : void 0,
            "--ti-bd": color2 || variant ? colors.border : void 0
          }
        };
      }
    );
    ThemeIcon = factory((_props, ref) => {
      const props = useProps("ThemeIcon", defaultProps76, _props);
      const { classNames, className, style, styles, unstyled, vars, autoContrast, ...others } = props;
      const getStyles = useStyles({
        name: "ThemeIcon",
        classes: classes38,
        props,
        className,
        style,
        classNames,
        styles,
        unstyled,
        vars,
        varsResolver: varsResolver37
      });
      return /* @__PURE__ */ (0, import_jsx_runtime154.jsx)(Box, { ref, ...getStyles("root"), ...others });
    });
    ThemeIcon.classes = classes38;
    ThemeIcon.displayName = "@mantine/core/ThemeIcon";
  }
});

// node_modules/@mantine/core/esm/components/Title/get-title-size.mjs
function getTitleSize(order, size4) {
  const titleSize = size4 !== void 0 ? size4 : `h${order}`;
  if (headings3.includes(titleSize)) {
    return {
      fontSize: `var(--mantine-${titleSize}-font-size)`,
      fontWeight: `var(--mantine-${titleSize}-font-weight)`,
      lineHeight: `var(--mantine-${titleSize}-line-height)`
    };
  } else if (sizes.includes(titleSize)) {
    return {
      fontSize: `var(--mantine-font-size-${titleSize})`,
      fontWeight: `var(--mantine-h${order}-font-weight)`,
      lineHeight: `var(--mantine-h${order}-line-height)`
    };
  }
  return {
    fontSize: rem(titleSize),
    fontWeight: `var(--mantine-h${order}-font-weight)`,
    lineHeight: `var(--mantine-h${order}-line-height)`
  };
}
var import_react188, import_jsx_runtime155, headings3, sizes;
var init_get_title_size = __esm({
  "node_modules/@mantine/core/esm/components/Title/get-title-size.mjs"() {
    "use client";
    init_rem();
    import_react188 = __toESM(require_react(), 1);
    import_jsx_runtime155 = __toESM(require_jsx_runtime(), 1);
    init_clsx();
    headings3 = ["h1", "h2", "h3", "h4", "h5", "h6"];
    sizes = ["xs", "sm", "md", "lg", "xl"];
  }
});

// node_modules/@mantine/core/esm/components/Title/Title.module.css.mjs
var classes39;
var init_Title_module_css = __esm({
  "node_modules/@mantine/core/esm/components/Title/Title.module.css.mjs"() {
    "use client";
    classes39 = { "root": "m_8a5d1357" };
  }
});

// node_modules/@mantine/core/esm/components/Title/Title.mjs
var import_jsx_runtime156, import_react189, defaultProps77, varsResolver38, Title;
var init_Title = __esm({
  "node_modules/@mantine/core/esm/components/Title/Title.mjs"() {
    "use client";
    import_jsx_runtime156 = __toESM(require_jsx_runtime(), 1);
    import_react189 = __toESM(require_react(), 1);
    init_create_vars_resolver();
    init_clsx();
    init_use_props();
    init_use_styles();
    init_Box();
    init_factory();
    init_get_title_size();
    init_Title_module_css();
    defaultProps77 = {
      order: 1
    };
    varsResolver38 = createVarsResolver((_2, { order, size: size4, lineClamp, textWrap }) => {
      const sizeVariables = getTitleSize(order, size4);
      return {
        root: {
          "--title-fw": sizeVariables.fontWeight,
          "--title-lh": sizeVariables.lineHeight,
          "--title-fz": sizeVariables.fontSize,
          "--title-line-clamp": typeof lineClamp === "number" ? lineClamp.toString() : void 0,
          "--title-text-wrap": textWrap
        }
      };
    });
    Title = factory((_props, ref) => {
      const props = useProps("Title", defaultProps77, _props);
      const {
        classNames,
        className,
        style,
        styles,
        unstyled,
        order,
        vars,
        size: size4,
        variant,
        lineClamp,
        textWrap,
        mod,
        ...others
      } = props;
      const getStyles = useStyles({
        name: "Title",
        props,
        classes: classes39,
        className,
        style,
        classNames,
        styles,
        unstyled,
        vars,
        varsResolver: varsResolver38
      });
      if (![1, 2, 3, 4, 5, 6].includes(order)) {
        return null;
      }
      return /* @__PURE__ */ (0, import_jsx_runtime156.jsx)(
        Box,
        {
          ...getStyles("root"),
          component: `h${order}`,
          variant,
          ref,
          mod: [{ order, "data-line-clamp": typeof lineClamp === "number" }, mod],
          size: size4,
          ...others
        }
      );
    });
    Title.classes = classes39;
    Title.displayName = "@mantine/core/Title";
  }
});

// node_modules/@mantine/core/esm/index.mjs
var import_react_remove_scroll2;
var init_esm2 = __esm({
  "node_modules/@mantine/core/esm/index.mjs"() {
    import_react_remove_scroll2 = __toESM(require_es56(), 1);
    init_rem();
    init_get_default_z_index();
    init_create_vars_resolver();
    init_use_styles();
    init_ColorSchemeScript();
    init_MantineProvider();
    init_MantineThemeProvider();
    init_use_props();
    init_create_theme();
    init_Box();
    init_factory();
    init_Paper();
    init_Popover();
    init_ActionIcon();
    init_Group();
    init_Loader();
    init_Flex();
    init_Blockquote();
    init_Burger();
    init_Button();
    init_Center();
    init_Checkbox();
    init_Container();
    init_Divider();
    init_Drawer();
    init_Image();
    init_List();
    init_Menu();
    init_Modal();
    init_Notification();
    init_Pagination();
    init_OptionalPortal();
    init_Progress();
    init_Radio();
    init_Skeleton();
    init_Slider();
    init_Stack();
    init_Text();
    init_TextInput();
    init_ThemeIcon();
    init_Title();
    init_Transition();
  }
});

// node_modules/@mantine/store/esm/store.mjs
function createStore(initialState) {
  let state2 = initialState;
  let initialized = false;
  const listeners = /* @__PURE__ */ new Set();
  return {
    getState() {
      return state2;
    },
    updateState(value) {
      state2 = typeof value === "function" ? value(state2) : value;
    },
    setState(value) {
      this.updateState(value);
      listeners.forEach((listener) => listener(state2));
    },
    initialize(value) {
      if (!initialized) {
        state2 = value;
        initialized = true;
      }
    },
    subscribe(callback) {
      listeners.add(callback);
      return () => listeners.delete(callback);
    }
  };
}
function useStore(store) {
  return (0, import_react190.useSyncExternalStore)(
    store.subscribe,
    () => store.getState(),
    () => store.getState()
  );
}
var import_react190;
var init_store = __esm({
  "node_modules/@mantine/store/esm/store.mjs"() {
    "use client";
    import_react190 = __toESM(require_react(), 1);
  }
});

// node_modules/@mantine/store/esm/index.mjs
var init_esm3 = __esm({
  "node_modules/@mantine/store/esm/index.mjs"() {
    init_store();
  }
});

// node_modules/@mantine/nprogress/esm/nprogress.store.mjs
function getIntervalProgressValue(currentProgress) {
  let next2 = 0.5;
  if (currentProgress >= 0 && currentProgress <= 20) {
    next2 = 10;
  } else if (currentProgress >= 20 && currentProgress <= 50) {
    next2 = 4;
  } else if (currentProgress >= 50 && currentProgress <= 80) {
    next2 = 2;
  } else if (currentProgress >= 80 && currentProgress <= 99) {
    next2 = 1;
  } else if (currentProgress >= 99 && currentProgress <= 100) {
    next2 = 0;
  }
  return currentProgress + next2;
}
function updateNavigationProgressStateAction(update, store) {
  const state2 = store.getState();
  store.setState({ ...state2, ...update(store.getState()) });
}
function decrementNavigationProgressAction(store) {
  updateNavigationProgressStateAction(
    (state2) => ({ progress: Math.max(state2.progress - state2.step, 0) }),
    store
  );
}
function setNavigationProgressAction(value, store) {
  updateNavigationProgressStateAction(
    () => ({ progress: clamp(value, 0, 100), mounted: true }),
    store
  );
}
function cleanupNavigationProgressAction(store) {
  updateNavigationProgressStateAction((state2) => {
    window.clearInterval(state2.interval);
    state2.timeouts.forEach((timeout) => window.clearTimeout(timeout));
    return { timeouts: [] };
  }, store);
}
function completeNavigationProgressAction(store) {
  cleanupNavigationProgressAction(store);
  updateNavigationProgressStateAction((state2) => {
    const mountedTimeout = window.setTimeout(() => {
      updateNavigationProgressStateAction(() => ({ mounted: false }), store);
    }, 50);
    const resetTimeout = window.setTimeout(() => {
      updateNavigationProgressStateAction(() => ({ progress: 0 }), store);
    }, state2.stepInterval + 50);
    return { progress: 100, timeouts: [mountedTimeout, resetTimeout] };
  }, store);
}
function startNavigationProgressAction(store) {
  updateNavigationProgressStateAction(
    (s36) => ({ progress: getIntervalProgressValue(s36.progress), mounted: true }),
    store
  );
  updateNavigationProgressStateAction((state2) => {
    window.clearInterval(state2.interval);
    const interval2 = window.setInterval(() => {
      updateNavigationProgressStateAction(
        (s36) => ({ progress: getIntervalProgressValue(s36.progress), mounted: true }),
        store
      );
    }, state2.stepInterval);
    return { interval: interval2, mounted: true };
  }, store);
}
function stopNavigationProgressAction(store) {
  updateNavigationProgressStateAction((state2) => {
    window.clearInterval(state2.interval);
    return { interval: -1 };
  }, store);
}
function resetNavigationProgressAction(store) {
  cleanupNavigationProgressAction(store);
  stopNavigationProgressAction(store);
  updateNavigationProgressStateAction(() => ({ progress: 0, mounted: false }), store);
}
function incrementNavigationProgressAction(store) {
  updateNavigationProgressStateAction((state2) => {
    const nextValue = Math.min(state2.progress + state2.step, 100);
    const nextMounted = nextValue !== 100 && nextValue !== 0;
    if (!nextMounted) {
      const timeout = window.setTimeout(
        () => resetNavigationProgressAction(store),
        state2.stepInterval + 50
      );
      return {
        progress: nextValue,
        mounted: nextMounted,
        timeouts: [...state2.timeouts, timeout]
      };
    }
    return {
      progress: nextValue,
      mounted: nextMounted
    };
  }, store);
}
function createNprogress() {
  const store = createNprogressStore();
  const actions = {
    start: () => startNavigationProgressAction(store),
    stop: () => stopNavigationProgressAction(store),
    reset: () => resetNavigationProgressAction(store),
    set: (value) => setNavigationProgressAction(value, store),
    increment: () => incrementNavigationProgressAction(store),
    decrement: () => decrementNavigationProgressAction(store),
    complete: () => completeNavigationProgressAction(store),
    cleanup: () => cleanupNavigationProgressAction(store)
  };
  return [store, actions];
}
var createNprogressStore, useNprogress, nprogressStore, nprogress, startNavigationProgress, stopNavigationProgress, resetNavigationProgress, setNavigationProgress, incrementNavigationProgress, decrementNavigationProgress, completeNavigationProgress, cleanupNavigationProgress;
var init_nprogress_store = __esm({
  "node_modules/@mantine/nprogress/esm/nprogress.store.mjs"() {
    "use client";
    init_esm();
    init_esm3();
    createNprogressStore = () => createStore({
      mounted: false,
      progress: 0,
      interval: 0,
      step: 1,
      stepInterval: 100,
      timeouts: []
    });
    useNprogress = (store) => useStore(store);
    [nprogressStore, nprogress] = createNprogress();
    ({
      start: startNavigationProgress,
      stop: stopNavigationProgress,
      reset: resetNavigationProgress,
      set: setNavigationProgress,
      increment: incrementNavigationProgress,
      decrement: decrementNavigationProgress,
      complete: completeNavigationProgress,
      cleanup: cleanupNavigationProgress
    } = nprogress);
  }
});

// node_modules/@mantine/nprogress/esm/NavigationProgress.module.css.mjs
var classes40;
var init_NavigationProgress_module_css = __esm({
  "node_modules/@mantine/nprogress/esm/NavigationProgress.module.css.mjs"() {
    "use client";
    classes40 = { "root": "m_8f2832ae", "section": "m_7a0fe999" };
  }
});

// node_modules/@mantine/nprogress/esm/NavigationProgress.mjs
function NavigationProgress({
  initialProgress = 0,
  color: color2,
  size: size4 = 3,
  stepInterval = 500,
  withinPortal = true,
  portalProps,
  zIndex = getDefaultZIndex("max"),
  store = nprogressStore,
  ...others
}) {
  store.initialize({
    mounted: false,
    progress: initialProgress,
    interval: -1,
    step: 1,
    stepInterval,
    timeouts: []
  });
  const state2 = useNprogress(store);
  (0, import_react191.useEffect)(() => () => resetNavigationProgressAction(store), [store]);
  return /* @__PURE__ */ (0, import_jsx_runtime157.jsx)(OptionalPortal, { ...portalProps, withinPortal, children: /* @__PURE__ */ (0, import_jsx_runtime157.jsx)(
    Progress,
    {
      radius: 0,
      value: state2.progress,
      size: size4,
      color: color2,
      classNames: classes40,
      "data-mounted": state2.mounted || void 0,
      __vars: { "--nprogress-z-index": zIndex?.toString() },
      ...others
    }
  ) });
}
var import_jsx_runtime157, import_react191;
var init_NavigationProgress = __esm({
  "node_modules/@mantine/nprogress/esm/NavigationProgress.mjs"() {
    "use client";
    import_jsx_runtime157 = __toESM(require_jsx_runtime(), 1);
    import_react191 = __toESM(require_react(), 1);
    init_esm2();
    init_nprogress_store();
    init_NavigationProgress_module_css();
    NavigationProgress.displayName = "@mantine/nprogress/NavigationProgress";
  }
});

// node_modules/@mantine/nprogress/esm/index.mjs
var init_esm4 = __esm({
  "node_modules/@mantine/nprogress/esm/index.mjs"() {
    init_NavigationProgress();
  }
});

// node_modules/@mantine/notifications/esm/notifications.store.mjs
function getDistributedNotifications(data, defaultPosition, limit) {
  const queue = [];
  const notifications2 = [];
  const count2 = {};
  for (const item2 of data) {
    const position = item2.position || defaultPosition;
    count2[position] = count2[position] || 0;
    count2[position] += 1;
    if (count2[position] <= limit) {
      notifications2.push(item2);
    } else {
      queue.push(item2);
    }
  }
  return { notifications: notifications2, queue };
}
function updateNotificationsState(store, update) {
  const state2 = store.getState();
  const notifications2 = update([...state2.notifications, ...state2.queue]);
  const updated = getDistributedNotifications(notifications2, state2.defaultPosition, state2.limit);
  store.setState({
    notifications: updated.notifications,
    queue: updated.queue,
    limit: state2.limit,
    defaultPosition: state2.defaultPosition
  });
}
function showNotification(notification, store = notificationsStore) {
  const id3 = notification.id || randomId();
  updateNotificationsState(store, (notifications2) => {
    if (notification.id && notifications2.some((n13) => n13.id === notification.id)) {
      return notifications2;
    }
    return [...notifications2, { ...notification, id: id3 }];
  });
  return id3;
}
function hideNotification(id3, store = notificationsStore) {
  updateNotificationsState(
    store,
    (notifications2) => notifications2.filter((notification) => {
      if (notification.id === id3) {
        notification.onClose?.(notification);
        return false;
      }
      return true;
    })
  );
  return id3;
}
function updateNotification(notification, store = notificationsStore) {
  updateNotificationsState(
    store,
    (notifications2) => notifications2.map((item2) => {
      if (item2.id === notification.id) {
        return { ...item2, ...notification };
      }
      return item2;
    })
  );
  return notification.id;
}
function cleanNotifications(store = notificationsStore) {
  updateNotificationsState(store, () => []);
}
function cleanNotificationsQueue(store = notificationsStore) {
  updateNotificationsState(
    store,
    (notifications2) => notifications2.slice(0, store.getState().limit)
  );
}
var createNotificationsStore, notificationsStore, useNotifications, notifications;
var init_notifications_store = __esm({
  "node_modules/@mantine/notifications/esm/notifications.store.mjs"() {
    "use client";
    init_esm();
    init_esm3();
    createNotificationsStore = () => createStore({
      notifications: [],
      queue: [],
      defaultPosition: "bottom-right",
      limit: 5
    });
    notificationsStore = createNotificationsStore();
    useNotifications = (store = notificationsStore) => useStore(store);
    notifications = {
      show: showNotification,
      hide: hideNotification,
      update: updateNotification,
      clean: cleanNotifications,
      cleanQueue: cleanNotificationsQueue,
      updateState: updateNotificationsState
    };
  }
});

// node_modules/prop-types/lib/ReactPropTypesSecret.js
var require_ReactPropTypesSecret = __commonJS({
  "node_modules/prop-types/lib/ReactPropTypesSecret.js"(exports, module) {
    "use strict";
    var ReactPropTypesSecret = "SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED";
    module.exports = ReactPropTypesSecret;
  }
});

// node_modules/prop-types/factoryWithThrowingShims.js
var require_factoryWithThrowingShims = __commonJS({
  "node_modules/prop-types/factoryWithThrowingShims.js"(exports, module) {
    "use strict";
    var ReactPropTypesSecret = require_ReactPropTypesSecret();
    function emptyFunction() {
    }
    function emptyFunctionWithReset() {
    }
    emptyFunctionWithReset.resetWarningCache = emptyFunction;
    module.exports = function() {
      function shim(props, propName, componentName, location, propFullName, secret) {
        if (secret === ReactPropTypesSecret) {
          return;
        }
        var err = new Error(
          "Calling PropTypes validators directly is not supported by the `prop-types` package. Use PropTypes.checkPropTypes() to call them. Read more at http://fb.me/use-check-prop-types"
        );
        err.name = "Invariant Violation";
        throw err;
      }
      ;
      shim.isRequired = shim;
      function getShim() {
        return shim;
      }
      ;
      var ReactPropTypes = {
        array: shim,
        bigint: shim,
        bool: shim,
        func: shim,
        number: shim,
        object: shim,
        string: shim,
        symbol: shim,
        any: shim,
        arrayOf: getShim,
        element: shim,
        elementType: shim,
        instanceOf: getShim,
        node: shim,
        objectOf: getShim,
        oneOf: getShim,
        oneOfType: getShim,
        shape: getShim,
        exact: getShim,
        checkPropTypes: emptyFunctionWithReset,
        resetWarningCache: emptyFunction
      };
      ReactPropTypes.PropTypes = ReactPropTypes;
      return ReactPropTypes;
    };
  }
});

// node_modules/prop-types/index.js
var require_prop_types = __commonJS({
  "node_modules/prop-types/index.js"(exports, module) {
    if (false) {
      ReactIs = null;
      throwOnDirectAccess = true;
      module.exports = null(ReactIs.isElement, throwOnDirectAccess);
    } else {
      module.exports = require_factoryWithThrowingShims()();
    }
    var ReactIs;
    var throwOnDirectAccess;
  }
});

// node_modules/@babel/runtime/helpers/interopRequireDefault.js
var require_interopRequireDefault = __commonJS({
  "node_modules/@babel/runtime/helpers/interopRequireDefault.js"(exports, module) {
    function _interopRequireDefault(e14) {
      return e14 && e14.__esModule ? e14 : {
        "default": e14
      };
    }
    module.exports = _interopRequireDefault, module.exports.__esModule = true, module.exports["default"] = module.exports;
  }
});

// node_modules/dom-helpers/cjs/hasClass.js
var require_hasClass = __commonJS({
  "node_modules/dom-helpers/cjs/hasClass.js"(exports, module) {
    "use strict";
    exports.__esModule = true;
    exports.default = hasClass;
    function hasClass(element, className) {
      if (element.classList) return !!className && element.classList.contains(className);
      return (" " + (element.className.baseVal || element.className) + " ").indexOf(" " + className + " ") !== -1;
    }
    module.exports = exports["default"];
  }
});

// node_modules/dom-helpers/cjs/addClass.js
var require_addClass = __commonJS({
  "node_modules/dom-helpers/cjs/addClass.js"(exports, module) {
    "use strict";
    var _interopRequireDefault = require_interopRequireDefault();
    exports.__esModule = true;
    exports.default = addClass;
    var _hasClass = _interopRequireDefault(require_hasClass());
    function addClass(element, className) {
      if (element.classList) element.classList.add(className);
      else if (!(0, _hasClass.default)(element, className)) if (typeof element.className === "string") element.className = element.className + " " + className;
      else element.setAttribute("class", (element.className && element.className.baseVal || "") + " " + className);
    }
    module.exports = exports["default"];
  }
});

// node_modules/dom-helpers/cjs/removeClass.js
var require_removeClass = __commonJS({
  "node_modules/dom-helpers/cjs/removeClass.js"(exports, module) {
    "use strict";
    exports.__esModule = true;
    exports.default = removeClass;
    function replaceClassName(origClass, classToRemove) {
      return origClass.replace(new RegExp("(^|\\s)" + classToRemove + "(?:\\s|$)", "g"), "$1").replace(/\s+/g, " ").replace(/^\s*|\s*$/g, "");
    }
    function removeClass(element, className) {
      if (element.classList) {
        element.classList.remove(className);
      } else if (typeof element.className === "string") {
        element.className = replaceClassName(element.className, className);
      } else {
        element.setAttribute("class", replaceClassName(element.className && element.className.baseVal || "", className));
      }
    }
    module.exports = exports["default"];
  }
});

// node_modules/react-transition-group/cjs/config.js
var require_config2 = __commonJS({
  "node_modules/react-transition-group/cjs/config.js"(exports, module) {
    "use strict";
    exports.__esModule = true;
    exports.default = void 0;
    var _default = {
      disabled: false
    };
    exports.default = _default;
    module.exports = exports.default;
  }
});

// node_modules/react-transition-group/cjs/utils/PropTypes.js
var require_PropTypes = __commonJS({
  "node_modules/react-transition-group/cjs/utils/PropTypes.js"(exports) {
    "use strict";
    exports.__esModule = true;
    exports.classNamesShape = exports.timeoutsShape = void 0;
    var _propTypes = _interopRequireDefault(require_prop_types());
    function _interopRequireDefault(obj) {
      return obj && obj.__esModule ? obj : { default: obj };
    }
    var timeoutsShape = false ? _propTypes.default.oneOfType([_propTypes.default.number, _propTypes.default.shape({
      enter: _propTypes.default.number,
      exit: _propTypes.default.number,
      appear: _propTypes.default.number
    }).isRequired]) : null;
    exports.timeoutsShape = timeoutsShape;
    var classNamesShape = false ? _propTypes.default.oneOfType([_propTypes.default.string, _propTypes.default.shape({
      enter: _propTypes.default.string,
      exit: _propTypes.default.string,
      active: _propTypes.default.string
    }), _propTypes.default.shape({
      enter: _propTypes.default.string,
      enterDone: _propTypes.default.string,
      enterActive: _propTypes.default.string,
      exit: _propTypes.default.string,
      exitDone: _propTypes.default.string,
      exitActive: _propTypes.default.string
    })]) : null;
    exports.classNamesShape = classNamesShape;
  }
});

// node_modules/react-transition-group/cjs/TransitionGroupContext.js
var require_TransitionGroupContext = __commonJS({
  "node_modules/react-transition-group/cjs/TransitionGroupContext.js"(exports, module) {
    "use strict";
    exports.__esModule = true;
    exports.default = void 0;
    var _react = _interopRequireDefault(require_react());
    function _interopRequireDefault(obj) {
      return obj && obj.__esModule ? obj : { default: obj };
    }
    var _default = _react.default.createContext(null);
    exports.default = _default;
    module.exports = exports.default;
  }
});

// node_modules/react-transition-group/cjs/utils/reflow.js
var require_reflow = __commonJS({
  "node_modules/react-transition-group/cjs/utils/reflow.js"(exports) {
    "use strict";
    exports.__esModule = true;
    exports.forceReflow = void 0;
    var forceReflow = function forceReflow2(node) {
      return node.scrollTop;
    };
    exports.forceReflow = forceReflow;
  }
});

// node_modules/react-transition-group/cjs/Transition.js
var require_Transition = __commonJS({
  "node_modules/react-transition-group/cjs/Transition.js"(exports) {
    "use strict";
    exports.__esModule = true;
    exports.default = exports.EXITING = exports.ENTERED = exports.ENTERING = exports.EXITED = exports.UNMOUNTED = void 0;
    var _propTypes = _interopRequireDefault(require_prop_types());
    var _react = _interopRequireDefault(require_react());
    var _reactDom = _interopRequireDefault(require_react_dom());
    var _config = _interopRequireDefault(require_config2());
    var _PropTypes = require_PropTypes();
    var _TransitionGroupContext = _interopRequireDefault(require_TransitionGroupContext());
    var _reflow = require_reflow();
    function _interopRequireDefault(obj) {
      return obj && obj.__esModule ? obj : { default: obj };
    }
    function _objectWithoutPropertiesLoose(source, excluded) {
      if (source == null) return {};
      var target = {};
      var sourceKeys = Object.keys(source);
      var key, i13;
      for (i13 = 0; i13 < sourceKeys.length; i13++) {
        key = sourceKeys[i13];
        if (excluded.indexOf(key) >= 0) continue;
        target[key] = source[key];
      }
      return target;
    }
    function _inheritsLoose(subClass, superClass) {
      subClass.prototype = Object.create(superClass.prototype);
      subClass.prototype.constructor = subClass;
      subClass.__proto__ = superClass;
    }
    var UNMOUNTED = "unmounted";
    exports.UNMOUNTED = UNMOUNTED;
    var EXITED = "exited";
    exports.EXITED = EXITED;
    var ENTERING = "entering";
    exports.ENTERING = ENTERING;
    var ENTERED = "entered";
    exports.ENTERED = ENTERED;
    var EXITING = "exiting";
    exports.EXITING = EXITING;
    var Transition3 = /* @__PURE__ */ function(_React$Component) {
      _inheritsLoose(Transition4, _React$Component);
      function Transition4(props, context) {
        var _this;
        _this = _React$Component.call(this, props, context) || this;
        var parentGroup = context;
        var appear = parentGroup && !parentGroup.isMounting ? props.enter : props.appear;
        var initialStatus;
        _this.appearStatus = null;
        if (props.in) {
          if (appear) {
            initialStatus = EXITED;
            _this.appearStatus = ENTERING;
          } else {
            initialStatus = ENTERED;
          }
        } else {
          if (props.unmountOnExit || props.mountOnEnter) {
            initialStatus = UNMOUNTED;
          } else {
            initialStatus = EXITED;
          }
        }
        _this.state = {
          status: initialStatus
        };
        _this.nextCallback = null;
        return _this;
      }
      Transition4.getDerivedStateFromProps = function getDerivedStateFromProps(_ref, prevState) {
        var nextIn = _ref.in;
        if (nextIn && prevState.status === UNMOUNTED) {
          return {
            status: EXITED
          };
        }
        return null;
      };
      var _proto = Transition4.prototype;
      _proto.componentDidMount = function componentDidMount() {
        this.updateStatus(true, this.appearStatus);
      };
      _proto.componentDidUpdate = function componentDidUpdate(prevProps) {
        var nextStatus = null;
        if (prevProps !== this.props) {
          var status2 = this.state.status;
          if (this.props.in) {
            if (status2 !== ENTERING && status2 !== ENTERED) {
              nextStatus = ENTERING;
            }
          } else {
            if (status2 === ENTERING || status2 === ENTERED) {
              nextStatus = EXITING;
            }
          }
        }
        this.updateStatus(false, nextStatus);
      };
      _proto.componentWillUnmount = function componentWillUnmount() {
        this.cancelNextCallback();
      };
      _proto.getTimeouts = function getTimeouts() {
        var timeout = this.props.timeout;
        var exit, enter, appear;
        exit = enter = appear = timeout;
        if (timeout != null && typeof timeout !== "number") {
          exit = timeout.exit;
          enter = timeout.enter;
          appear = timeout.appear !== void 0 ? timeout.appear : enter;
        }
        return {
          exit,
          enter,
          appear
        };
      };
      _proto.updateStatus = function updateStatus(mounting, nextStatus) {
        if (mounting === void 0) {
          mounting = false;
        }
        if (nextStatus !== null) {
          this.cancelNextCallback();
          if (nextStatus === ENTERING) {
            if (this.props.unmountOnExit || this.props.mountOnEnter) {
              var node = this.props.nodeRef ? this.props.nodeRef.current : _reactDom.default.findDOMNode(this);
              if (node) (0, _reflow.forceReflow)(node);
            }
            this.performEnter(mounting);
          } else {
            this.performExit();
          }
        } else if (this.props.unmountOnExit && this.state.status === EXITED) {
          this.setState({
            status: UNMOUNTED
          });
        }
      };
      _proto.performEnter = function performEnter(mounting) {
        var _this2 = this;
        var enter = this.props.enter;
        var appearing = this.context ? this.context.isMounting : mounting;
        var _ref2 = this.props.nodeRef ? [appearing] : [_reactDom.default.findDOMNode(this), appearing], maybeNode = _ref2[0], maybeAppearing = _ref2[1];
        var timeouts = this.getTimeouts();
        var enterTimeout = appearing ? timeouts.appear : timeouts.enter;
        if (!mounting && !enter || _config.default.disabled) {
          this.safeSetState({
            status: ENTERED
          }, function() {
            _this2.props.onEntered(maybeNode);
          });
          return;
        }
        this.props.onEnter(maybeNode, maybeAppearing);
        this.safeSetState({
          status: ENTERING
        }, function() {
          _this2.props.onEntering(maybeNode, maybeAppearing);
          _this2.onTransitionEnd(enterTimeout, function() {
            _this2.safeSetState({
              status: ENTERED
            }, function() {
              _this2.props.onEntered(maybeNode, maybeAppearing);
            });
          });
        });
      };
      _proto.performExit = function performExit() {
        var _this3 = this;
        var exit = this.props.exit;
        var timeouts = this.getTimeouts();
        var maybeNode = this.props.nodeRef ? void 0 : _reactDom.default.findDOMNode(this);
        if (!exit || _config.default.disabled) {
          this.safeSetState({
            status: EXITED
          }, function() {
            _this3.props.onExited(maybeNode);
          });
          return;
        }
        this.props.onExit(maybeNode);
        this.safeSetState({
          status: EXITING
        }, function() {
          _this3.props.onExiting(maybeNode);
          _this3.onTransitionEnd(timeouts.exit, function() {
            _this3.safeSetState({
              status: EXITED
            }, function() {
              _this3.props.onExited(maybeNode);
            });
          });
        });
      };
      _proto.cancelNextCallback = function cancelNextCallback() {
        if (this.nextCallback !== null) {
          this.nextCallback.cancel();
          this.nextCallback = null;
        }
      };
      _proto.safeSetState = function safeSetState(nextState, callback) {
        callback = this.setNextCallback(callback);
        this.setState(nextState, callback);
      };
      _proto.setNextCallback = function setNextCallback(callback) {
        var _this4 = this;
        var active = true;
        this.nextCallback = function(event) {
          if (active) {
            active = false;
            _this4.nextCallback = null;
            callback(event);
          }
        };
        this.nextCallback.cancel = function() {
          active = false;
        };
        return this.nextCallback;
      };
      _proto.onTransitionEnd = function onTransitionEnd(timeout, handler2) {
        this.setNextCallback(handler2);
        var node = this.props.nodeRef ? this.props.nodeRef.current : _reactDom.default.findDOMNode(this);
        var doesNotHaveTimeoutOrListener = timeout == null && !this.props.addEndListener;
        if (!node || doesNotHaveTimeoutOrListener) {
          setTimeout(this.nextCallback, 0);
          return;
        }
        if (this.props.addEndListener) {
          var _ref3 = this.props.nodeRef ? [this.nextCallback] : [node, this.nextCallback], maybeNode = _ref3[0], maybeNextCallback = _ref3[1];
          this.props.addEndListener(maybeNode, maybeNextCallback);
        }
        if (timeout != null) {
          setTimeout(this.nextCallback, timeout);
        }
      };
      _proto.render = function render() {
        var status2 = this.state.status;
        if (status2 === UNMOUNTED) {
          return null;
        }
        var _this$props = this.props, children = _this$props.children, _in = _this$props.in, _mountOnEnter = _this$props.mountOnEnter, _unmountOnExit = _this$props.unmountOnExit, _appear = _this$props.appear, _enter = _this$props.enter, _exit = _this$props.exit, _timeout = _this$props.timeout, _addEndListener = _this$props.addEndListener, _onEnter = _this$props.onEnter, _onEntering = _this$props.onEntering, _onEntered = _this$props.onEntered, _onExit = _this$props.onExit, _onExiting = _this$props.onExiting, _onExited = _this$props.onExited, _nodeRef = _this$props.nodeRef, childProps = _objectWithoutPropertiesLoose(_this$props, ["children", "in", "mountOnEnter", "unmountOnExit", "appear", "enter", "exit", "timeout", "addEndListener", "onEnter", "onEntering", "onEntered", "onExit", "onExiting", "onExited", "nodeRef"]);
        return (
          // allows for nested Transitions
          /* @__PURE__ */ _react.default.createElement(_TransitionGroupContext.default.Provider, {
            value: null
          }, typeof children === "function" ? children(status2, childProps) : _react.default.cloneElement(_react.default.Children.only(children), childProps))
        );
      };
      return Transition4;
    }(_react.default.Component);
    Transition3.contextType = _TransitionGroupContext.default;
    Transition3.propTypes = false ? {
      /**
       * A React reference to DOM element that need to transition:
       * https://stackoverflow.com/a/51127130/4671932
       *
       *   - When `nodeRef` prop is used, `node` is not passed to callback functions
       *      (e.g. `onEnter`) because user already has direct access to the node.
       *   - When changing `key` prop of `Transition` in a `TransitionGroup` a new
       *     `nodeRef` need to be provided to `Transition` with changed `key` prop
       *     (see
       *     [test/CSSTransition-test.js](https://github.com/reactjs/react-transition-group/blob/13435f897b3ab71f6e19d724f145596f5910581c/test/CSSTransition-test.js#L362-L437)).
       */
      nodeRef: _propTypes.default.shape({
        current: typeof Element === "undefined" ? _propTypes.default.any : function(propValue, key, componentName, location, propFullName, secret) {
          var value = propValue[key];
          return _propTypes.default.instanceOf(value && "ownerDocument" in value ? value.ownerDocument.defaultView.Element : Element)(propValue, key, componentName, location, propFullName, secret);
        }
      }),
      /**
       * A `function` child can be used instead of a React element. This function is
       * called with the current transition status (`'entering'`, `'entered'`,
       * `'exiting'`, `'exited'`), which can be used to apply context
       * specific props to a component.
       *
       * ```jsx
       * <Transition in={this.state.in} timeout={150}>
       *   {state => (
       *     <MyComponent className={`fade fade-${state}`} />
       *   )}
       * </Transition>
       * ```
       */
      children: _propTypes.default.oneOfType([_propTypes.default.func.isRequired, _propTypes.default.element.isRequired]).isRequired,
      /**
       * Show the component; triggers the enter or exit states
       */
      in: _propTypes.default.bool,
      /**
       * By default the child component is mounted immediately along with
       * the parent `Transition` component. If you want to "lazy mount" the component on the
       * first `in={true}` you can set `mountOnEnter`. After the first enter transition the component will stay
       * mounted, even on "exited", unless you also specify `unmountOnExit`.
       */
      mountOnEnter: _propTypes.default.bool,
      /**
       * By default the child component stays mounted after it reaches the `'exited'` state.
       * Set `unmountOnExit` if you'd prefer to unmount the component after it finishes exiting.
       */
      unmountOnExit: _propTypes.default.bool,
      /**
       * By default the child component does not perform the enter transition when
       * it first mounts, regardless of the value of `in`. If you want this
       * behavior, set both `appear` and `in` to `true`.
       *
       * > **Note**: there are no special appear states like `appearing`/`appeared`, this prop
       * > only adds an additional enter transition. However, in the
       * > `<CSSTransition>` component that first enter transition does result in
       * > additional `.appear-*` classes, that way you can choose to style it
       * > differently.
       */
      appear: _propTypes.default.bool,
      /**
       * Enable or disable enter transitions.
       */
      enter: _propTypes.default.bool,
      /**
       * Enable or disable exit transitions.
       */
      exit: _propTypes.default.bool,
      /**
       * The duration of the transition, in milliseconds.
       * Required unless `addEndListener` is provided.
       *
       * You may specify a single timeout for all transitions:
       *
       * ```jsx
       * timeout={500}
       * ```
       *
       * or individually:
       *
       * ```jsx
       * timeout={{
       *  appear: 500,
       *  enter: 300,
       *  exit: 500,
       * }}
       * ```
       *
       * - `appear` defaults to the value of `enter`
       * - `enter` defaults to `0`
       * - `exit` defaults to `0`
       *
       * @type {number | { enter?: number, exit?: number, appear?: number }}
       */
      timeout: function timeout(props) {
        var pt4 = _PropTypes.timeoutsShape;
        if (!props.addEndListener) pt4 = pt4.isRequired;
        for (var _len = arguments.length, args = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
          args[_key - 1] = arguments[_key];
        }
        return pt4.apply(void 0, [props].concat(args));
      },
      /**
       * Add a custom transition end trigger. Called with the transitioning
       * DOM node and a `done` callback. Allows for more fine grained transition end
       * logic. Timeouts are still used as a fallback if provided.
       *
       * **Note**: when `nodeRef` prop is passed, `node` is not passed.
       *
       * ```jsx
       * addEndListener={(node, done) => {
       *   // use the css transitionend event to mark the finish of a transition
       *   node.addEventListener('transitionend', done, false);
       * }}
       * ```
       */
      addEndListener: _propTypes.default.func,
      /**
       * Callback fired before the "entering" status is applied. An extra parameter
       * `isAppearing` is supplied to indicate if the enter stage is occurring on the initial mount
       *
       * **Note**: when `nodeRef` prop is passed, `node` is not passed.
       *
       * @type Function(node: HtmlElement, isAppearing: bool) -> void
       */
      onEnter: _propTypes.default.func,
      /**
       * Callback fired after the "entering" status is applied. An extra parameter
       * `isAppearing` is supplied to indicate if the enter stage is occurring on the initial mount
       *
       * **Note**: when `nodeRef` prop is passed, `node` is not passed.
       *
       * @type Function(node: HtmlElement, isAppearing: bool)
       */
      onEntering: _propTypes.default.func,
      /**
       * Callback fired after the "entered" status is applied. An extra parameter
       * `isAppearing` is supplied to indicate if the enter stage is occurring on the initial mount
       *
       * **Note**: when `nodeRef` prop is passed, `node` is not passed.
       *
       * @type Function(node: HtmlElement, isAppearing: bool) -> void
       */
      onEntered: _propTypes.default.func,
      /**
       * Callback fired before the "exiting" status is applied.
       *
       * **Note**: when `nodeRef` prop is passed, `node` is not passed.
       *
       * @type Function(node: HtmlElement) -> void
       */
      onExit: _propTypes.default.func,
      /**
       * Callback fired after the "exiting" status is applied.
       *
       * **Note**: when `nodeRef` prop is passed, `node` is not passed.
       *
       * @type Function(node: HtmlElement) -> void
       */
      onExiting: _propTypes.default.func,
      /**
       * Callback fired after the "exited" status is applied.
       *
       * **Note**: when `nodeRef` prop is passed, `node` is not passed
       *
       * @type Function(node: HtmlElement) -> void
       */
      onExited: _propTypes.default.func
    } : {};
    function noop3() {
    }
    Transition3.defaultProps = {
      in: false,
      mountOnEnter: false,
      unmountOnExit: false,
      appear: false,
      enter: true,
      exit: true,
      onEnter: noop3,
      onEntering: noop3,
      onEntered: noop3,
      onExit: noop3,
      onExiting: noop3,
      onExited: noop3
    };
    Transition3.UNMOUNTED = UNMOUNTED;
    Transition3.EXITED = EXITED;
    Transition3.ENTERING = ENTERING;
    Transition3.ENTERED = ENTERED;
    Transition3.EXITING = EXITING;
    var _default = Transition3;
    exports.default = _default;
  }
});

// node_modules/react-transition-group/cjs/CSSTransition.js
var require_CSSTransition = __commonJS({
  "node_modules/react-transition-group/cjs/CSSTransition.js"(exports, module) {
    "use strict";
    exports.__esModule = true;
    exports.default = void 0;
    var _propTypes = _interopRequireDefault(require_prop_types());
    var _addClass2 = _interopRequireDefault(require_addClass());
    var _removeClass = _interopRequireDefault(require_removeClass());
    var _react = _interopRequireDefault(require_react());
    var _Transition = _interopRequireDefault(require_Transition());
    var _PropTypes = require_PropTypes();
    var _reflow = require_reflow();
    function _interopRequireDefault(obj) {
      return obj && obj.__esModule ? obj : { default: obj };
    }
    function _extends() {
      _extends = Object.assign || function(target) {
        for (var i13 = 1; i13 < arguments.length; i13++) {
          var source = arguments[i13];
          for (var key in source) {
            if (Object.prototype.hasOwnProperty.call(source, key)) {
              target[key] = source[key];
            }
          }
        }
        return target;
      };
      return _extends.apply(this, arguments);
    }
    function _objectWithoutPropertiesLoose(source, excluded) {
      if (source == null) return {};
      var target = {};
      var sourceKeys = Object.keys(source);
      var key, i13;
      for (i13 = 0; i13 < sourceKeys.length; i13++) {
        key = sourceKeys[i13];
        if (excluded.indexOf(key) >= 0) continue;
        target[key] = source[key];
      }
      return target;
    }
    function _inheritsLoose(subClass, superClass) {
      subClass.prototype = Object.create(superClass.prototype);
      subClass.prototype.constructor = subClass;
      subClass.__proto__ = superClass;
    }
    var _addClass = function addClass(node, classes42) {
      return node && classes42 && classes42.split(" ").forEach(function(c15) {
        return (0, _addClass2.default)(node, c15);
      });
    };
    var removeClass = function removeClass2(node, classes42) {
      return node && classes42 && classes42.split(" ").forEach(function(c15) {
        return (0, _removeClass.default)(node, c15);
      });
    };
    var CSSTransition = /* @__PURE__ */ function(_React$Component) {
      _inheritsLoose(CSSTransition2, _React$Component);
      function CSSTransition2() {
        var _this;
        for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
          args[_key] = arguments[_key];
        }
        _this = _React$Component.call.apply(_React$Component, [this].concat(args)) || this;
        _this.appliedClasses = {
          appear: {},
          enter: {},
          exit: {}
        };
        _this.onEnter = function(maybeNode, maybeAppearing) {
          var _this$resolveArgument = _this.resolveArguments(maybeNode, maybeAppearing), node = _this$resolveArgument[0], appearing = _this$resolveArgument[1];
          _this.removeClasses(node, "exit");
          _this.addClass(node, appearing ? "appear" : "enter", "base");
          if (_this.props.onEnter) {
            _this.props.onEnter(maybeNode, maybeAppearing);
          }
        };
        _this.onEntering = function(maybeNode, maybeAppearing) {
          var _this$resolveArgument2 = _this.resolveArguments(maybeNode, maybeAppearing), node = _this$resolveArgument2[0], appearing = _this$resolveArgument2[1];
          var type2 = appearing ? "appear" : "enter";
          _this.addClass(node, type2, "active");
          if (_this.props.onEntering) {
            _this.props.onEntering(maybeNode, maybeAppearing);
          }
        };
        _this.onEntered = function(maybeNode, maybeAppearing) {
          var _this$resolveArgument3 = _this.resolveArguments(maybeNode, maybeAppearing), node = _this$resolveArgument3[0], appearing = _this$resolveArgument3[1];
          var type2 = appearing ? "appear" : "enter";
          _this.removeClasses(node, type2);
          _this.addClass(node, type2, "done");
          if (_this.props.onEntered) {
            _this.props.onEntered(maybeNode, maybeAppearing);
          }
        };
        _this.onExit = function(maybeNode) {
          var _this$resolveArgument4 = _this.resolveArguments(maybeNode), node = _this$resolveArgument4[0];
          _this.removeClasses(node, "appear");
          _this.removeClasses(node, "enter");
          _this.addClass(node, "exit", "base");
          if (_this.props.onExit) {
            _this.props.onExit(maybeNode);
          }
        };
        _this.onExiting = function(maybeNode) {
          var _this$resolveArgument5 = _this.resolveArguments(maybeNode), node = _this$resolveArgument5[0];
          _this.addClass(node, "exit", "active");
          if (_this.props.onExiting) {
            _this.props.onExiting(maybeNode);
          }
        };
        _this.onExited = function(maybeNode) {
          var _this$resolveArgument6 = _this.resolveArguments(maybeNode), node = _this$resolveArgument6[0];
          _this.removeClasses(node, "exit");
          _this.addClass(node, "exit", "done");
          if (_this.props.onExited) {
            _this.props.onExited(maybeNode);
          }
        };
        _this.resolveArguments = function(maybeNode, maybeAppearing) {
          return _this.props.nodeRef ? [_this.props.nodeRef.current, maybeNode] : [maybeNode, maybeAppearing];
        };
        _this.getClassNames = function(type2) {
          var classNames = _this.props.classNames;
          var isStringClassNames = typeof classNames === "string";
          var prefix = isStringClassNames && classNames ? classNames + "-" : "";
          var baseClassName = isStringClassNames ? "" + prefix + type2 : classNames[type2];
          var activeClassName = isStringClassNames ? baseClassName + "-active" : classNames[type2 + "Active"];
          var doneClassName = isStringClassNames ? baseClassName + "-done" : classNames[type2 + "Done"];
          return {
            baseClassName,
            activeClassName,
            doneClassName
          };
        };
        return _this;
      }
      var _proto = CSSTransition2.prototype;
      _proto.addClass = function addClass(node, type2, phase) {
        var className = this.getClassNames(type2)[phase + "ClassName"];
        var _this$getClassNames = this.getClassNames("enter"), doneClassName = _this$getClassNames.doneClassName;
        if (type2 === "appear" && phase === "done" && doneClassName) {
          className += " " + doneClassName;
        }
        if (phase === "active") {
          if (node) (0, _reflow.forceReflow)(node);
        }
        if (className) {
          this.appliedClasses[type2][phase] = className;
          _addClass(node, className);
        }
      };
      _proto.removeClasses = function removeClasses(node, type2) {
        var _this$appliedClasses$ = this.appliedClasses[type2], baseClassName = _this$appliedClasses$.base, activeClassName = _this$appliedClasses$.active, doneClassName = _this$appliedClasses$.done;
        this.appliedClasses[type2] = {};
        if (baseClassName) {
          removeClass(node, baseClassName);
        }
        if (activeClassName) {
          removeClass(node, activeClassName);
        }
        if (doneClassName) {
          removeClass(node, doneClassName);
        }
      };
      _proto.render = function render() {
        var _this$props = this.props, _2 = _this$props.classNames, props = _objectWithoutPropertiesLoose(_this$props, ["classNames"]);
        return /* @__PURE__ */ _react.default.createElement(_Transition.default, _extends({}, props, {
          onEnter: this.onEnter,
          onEntered: this.onEntered,
          onEntering: this.onEntering,
          onExit: this.onExit,
          onExiting: this.onExiting,
          onExited: this.onExited
        }));
      };
      return CSSTransition2;
    }(_react.default.Component);
    CSSTransition.defaultProps = {
      classNames: ""
    };
    CSSTransition.propTypes = false ? _extends({}, _Transition.default.propTypes, {
      /**
       * The animation classNames applied to the component as it appears, enters,
       * exits or has finished the transition. A single name can be provided, which
       * will be suffixed for each stage, e.g. `classNames="fade"` applies:
       *
       * - `fade-appear`, `fade-appear-active`, `fade-appear-done`
       * - `fade-enter`, `fade-enter-active`, `fade-enter-done`
       * - `fade-exit`, `fade-exit-active`, `fade-exit-done`
       *
       * A few details to note about how these classes are applied:
       *
       * 1. They are _joined_ with the ones that are already defined on the child
       *    component, so if you want to add some base styles, you can use
       *    `className` without worrying that it will be overridden.
       *
       * 2. If the transition component mounts with `in={false}`, no classes are
       *    applied yet. You might be expecting `*-exit-done`, but if you think
       *    about it, a component cannot finish exiting if it hasn't entered yet.
       *
       * 2. `fade-appear-done` and `fade-enter-done` will _both_ be applied. This
       *    allows you to define different behavior for when appearing is done and
       *    when regular entering is done, using selectors like
       *    `.fade-enter-done:not(.fade-appear-done)`. For example, you could apply
       *    an epic entrance animation when element first appears in the DOM using
       *    [Animate.css](https://daneden.github.io/animate.css/). Otherwise you can
       *    simply use `fade-enter-done` for defining both cases.
       *
       * Each individual classNames can also be specified independently like:
       *
       * ```js
       * classNames={{
       *  appear: 'my-appear',
       *  appearActive: 'my-active-appear',
       *  appearDone: 'my-done-appear',
       *  enter: 'my-enter',
       *  enterActive: 'my-active-enter',
       *  enterDone: 'my-done-enter',
       *  exit: 'my-exit',
       *  exitActive: 'my-active-exit',
       *  exitDone: 'my-done-exit',
       * }}
       * ```
       *
       * If you want to set these classes using CSS Modules:
       *
       * ```js
       * import styles from './styles.css';
       * ```
       *
       * you might want to use camelCase in your CSS file, that way could simply
       * spread them instead of listing them one by one:
       *
       * ```js
       * classNames={{ ...styles }}
       * ```
       *
       * @type {string | {
       *  appear?: string,
       *  appearActive?: string,
       *  appearDone?: string,
       *  enter?: string,
       *  enterActive?: string,
       *  enterDone?: string,
       *  exit?: string,
       *  exitActive?: string,
       *  exitDone?: string,
       * }}
       */
      classNames: _PropTypes.classNamesShape,
      /**
       * A `<Transition>` callback fired immediately after the 'enter' or 'appear' class is
       * applied.
       *
       * **Note**: when `nodeRef` prop is passed, `node` is not passed.
       *
       * @type Function(node: HtmlElement, isAppearing: bool)
       */
      onEnter: _propTypes.default.func,
      /**
       * A `<Transition>` callback fired immediately after the 'enter-active' or
       * 'appear-active' class is applied.
       *
       * **Note**: when `nodeRef` prop is passed, `node` is not passed.
       *
       * @type Function(node: HtmlElement, isAppearing: bool)
       */
      onEntering: _propTypes.default.func,
      /**
       * A `<Transition>` callback fired immediately after the 'enter' or
       * 'appear' classes are **removed** and the `done` class is added to the DOM node.
       *
       * **Note**: when `nodeRef` prop is passed, `node` is not passed.
       *
       * @type Function(node: HtmlElement, isAppearing: bool)
       */
      onEntered: _propTypes.default.func,
      /**
       * A `<Transition>` callback fired immediately after the 'exit' class is
       * applied.
       *
       * **Note**: when `nodeRef` prop is passed, `node` is not passed
       *
       * @type Function(node: HtmlElement)
       */
      onExit: _propTypes.default.func,
      /**
       * A `<Transition>` callback fired immediately after the 'exit-active' is applied.
       *
       * **Note**: when `nodeRef` prop is passed, `node` is not passed
       *
       * @type Function(node: HtmlElement)
       */
      onExiting: _propTypes.default.func,
      /**
       * A `<Transition>` callback fired immediately after the 'exit' classes
       * are **removed** and the `exit-done` class is added to the DOM node.
       *
       * **Note**: when `nodeRef` prop is passed, `node` is not passed
       *
       * @type Function(node: HtmlElement)
       */
      onExited: _propTypes.default.func
    }) : {};
    var _default = CSSTransition;
    exports.default = _default;
    module.exports = exports.default;
  }
});

// node_modules/react-transition-group/cjs/utils/ChildMapping.js
var require_ChildMapping = __commonJS({
  "node_modules/react-transition-group/cjs/utils/ChildMapping.js"(exports) {
    "use strict";
    exports.__esModule = true;
    exports.getChildMapping = getChildMapping;
    exports.mergeChildMappings = mergeChildMappings;
    exports.getInitialChildMapping = getInitialChildMapping;
    exports.getNextChildMapping = getNextChildMapping;
    var _react = require_react();
    function getChildMapping(children, mapFn) {
      var mapper = function mapper2(child) {
        return mapFn && (0, _react.isValidElement)(child) ? mapFn(child) : child;
      };
      var result = /* @__PURE__ */ Object.create(null);
      if (children) _react.Children.map(children, function(c15) {
        return c15;
      }).forEach(function(child) {
        result[child.key] = mapper(child);
      });
      return result;
    }
    function mergeChildMappings(prev2, next2) {
      prev2 = prev2 || {};
      next2 = next2 || {};
      function getValueForKey(key) {
        return key in next2 ? next2[key] : prev2[key];
      }
      var nextKeysPending = /* @__PURE__ */ Object.create(null);
      var pendingKeys = [];
      for (var prevKey in prev2) {
        if (prevKey in next2) {
          if (pendingKeys.length) {
            nextKeysPending[prevKey] = pendingKeys;
            pendingKeys = [];
          }
        } else {
          pendingKeys.push(prevKey);
        }
      }
      var i13;
      var childMapping = {};
      for (var nextKey in next2) {
        if (nextKeysPending[nextKey]) {
          for (i13 = 0; i13 < nextKeysPending[nextKey].length; i13++) {
            var pendingNextKey = nextKeysPending[nextKey][i13];
            childMapping[nextKeysPending[nextKey][i13]] = getValueForKey(pendingNextKey);
          }
        }
        childMapping[nextKey] = getValueForKey(nextKey);
      }
      for (i13 = 0; i13 < pendingKeys.length; i13++) {
        childMapping[pendingKeys[i13]] = getValueForKey(pendingKeys[i13]);
      }
      return childMapping;
    }
    function getProp(child, prop, props) {
      return props[prop] != null ? props[prop] : child.props[prop];
    }
    function getInitialChildMapping(props, onExited) {
      return getChildMapping(props.children, function(child) {
        return (0, _react.cloneElement)(child, {
          onExited: onExited.bind(null, child),
          in: true,
          appear: getProp(child, "appear", props),
          enter: getProp(child, "enter", props),
          exit: getProp(child, "exit", props)
        });
      });
    }
    function getNextChildMapping(nextProps, prevChildMapping, onExited) {
      var nextChildMapping = getChildMapping(nextProps.children);
      var children = mergeChildMappings(prevChildMapping, nextChildMapping);
      Object.keys(children).forEach(function(key) {
        var child = children[key];
        if (!(0, _react.isValidElement)(child)) return;
        var hasPrev = key in prevChildMapping;
        var hasNext = key in nextChildMapping;
        var prevChild = prevChildMapping[key];
        var isLeaving = (0, _react.isValidElement)(prevChild) && !prevChild.props.in;
        if (hasNext && (!hasPrev || isLeaving)) {
          children[key] = (0, _react.cloneElement)(child, {
            onExited: onExited.bind(null, child),
            in: true,
            exit: getProp(child, "exit", nextProps),
            enter: getProp(child, "enter", nextProps)
          });
        } else if (!hasNext && hasPrev && !isLeaving) {
          children[key] = (0, _react.cloneElement)(child, {
            in: false
          });
        } else if (hasNext && hasPrev && (0, _react.isValidElement)(prevChild)) {
          children[key] = (0, _react.cloneElement)(child, {
            onExited: onExited.bind(null, child),
            in: prevChild.props.in,
            exit: getProp(child, "exit", nextProps),
            enter: getProp(child, "enter", nextProps)
          });
        }
      });
      return children;
    }
  }
});

// node_modules/react-transition-group/cjs/TransitionGroup.js
var require_TransitionGroup = __commonJS({
  "node_modules/react-transition-group/cjs/TransitionGroup.js"(exports, module) {
    "use strict";
    exports.__esModule = true;
    exports.default = void 0;
    var _propTypes = _interopRequireDefault(require_prop_types());
    var _react = _interopRequireDefault(require_react());
    var _TransitionGroupContext = _interopRequireDefault(require_TransitionGroupContext());
    var _ChildMapping = require_ChildMapping();
    function _interopRequireDefault(obj) {
      return obj && obj.__esModule ? obj : { default: obj };
    }
    function _objectWithoutPropertiesLoose(source, excluded) {
      if (source == null) return {};
      var target = {};
      var sourceKeys = Object.keys(source);
      var key, i13;
      for (i13 = 0; i13 < sourceKeys.length; i13++) {
        key = sourceKeys[i13];
        if (excluded.indexOf(key) >= 0) continue;
        target[key] = source[key];
      }
      return target;
    }
    function _extends() {
      _extends = Object.assign || function(target) {
        for (var i13 = 1; i13 < arguments.length; i13++) {
          var source = arguments[i13];
          for (var key in source) {
            if (Object.prototype.hasOwnProperty.call(source, key)) {
              target[key] = source[key];
            }
          }
        }
        return target;
      };
      return _extends.apply(this, arguments);
    }
    function _assertThisInitialized(self2) {
      if (self2 === void 0) {
        throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
      }
      return self2;
    }
    function _inheritsLoose(subClass, superClass) {
      subClass.prototype = Object.create(superClass.prototype);
      subClass.prototype.constructor = subClass;
      subClass.__proto__ = superClass;
    }
    var values2 = Object.values || function(obj) {
      return Object.keys(obj).map(function(k5) {
        return obj[k5];
      });
    };
    var defaultProps79 = {
      component: "div",
      childFactory: function childFactory(child) {
        return child;
      }
    };
    var TransitionGroup2 = /* @__PURE__ */ function(_React$Component) {
      _inheritsLoose(TransitionGroup3, _React$Component);
      function TransitionGroup3(props, context) {
        var _this;
        _this = _React$Component.call(this, props, context) || this;
        var handleExited = _this.handleExited.bind(_assertThisInitialized(_this));
        _this.state = {
          contextValue: {
            isMounting: true
          },
          handleExited,
          firstRender: true
        };
        return _this;
      }
      var _proto = TransitionGroup3.prototype;
      _proto.componentDidMount = function componentDidMount() {
        this.mounted = true;
        this.setState({
          contextValue: {
            isMounting: false
          }
        });
      };
      _proto.componentWillUnmount = function componentWillUnmount() {
        this.mounted = false;
      };
      TransitionGroup3.getDerivedStateFromProps = function getDerivedStateFromProps(nextProps, _ref) {
        var prevChildMapping = _ref.children, handleExited = _ref.handleExited, firstRender = _ref.firstRender;
        return {
          children: firstRender ? (0, _ChildMapping.getInitialChildMapping)(nextProps, handleExited) : (0, _ChildMapping.getNextChildMapping)(nextProps, prevChildMapping, handleExited),
          firstRender: false
        };
      };
      _proto.handleExited = function handleExited(child, node) {
        var currentChildMapping = (0, _ChildMapping.getChildMapping)(this.props.children);
        if (child.key in currentChildMapping) return;
        if (child.props.onExited) {
          child.props.onExited(node);
        }
        if (this.mounted) {
          this.setState(function(state2) {
            var children = _extends({}, state2.children);
            delete children[child.key];
            return {
              children
            };
          });
        }
      };
      _proto.render = function render() {
        var _this$props = this.props, Component2 = _this$props.component, childFactory = _this$props.childFactory, props = _objectWithoutPropertiesLoose(_this$props, ["component", "childFactory"]);
        var contextValue = this.state.contextValue;
        var children = values2(this.state.children).map(childFactory);
        delete props.appear;
        delete props.enter;
        delete props.exit;
        if (Component2 === null) {
          return /* @__PURE__ */ _react.default.createElement(_TransitionGroupContext.default.Provider, {
            value: contextValue
          }, children);
        }
        return /* @__PURE__ */ _react.default.createElement(_TransitionGroupContext.default.Provider, {
          value: contextValue
        }, /* @__PURE__ */ _react.default.createElement(Component2, props, children));
      };
      return TransitionGroup3;
    }(_react.default.Component);
    TransitionGroup2.propTypes = false ? {
      /**
       * `<TransitionGroup>` renders a `<div>` by default. You can change this
       * behavior by providing a `component` prop.
       * If you use React v16+ and would like to avoid a wrapping `<div>` element
       * you can pass in `component={null}`. This is useful if the wrapping div
       * borks your css styles.
       */
      component: _propTypes.default.any,
      /**
       * A set of `<Transition>` components, that are toggled `in` and out as they
       * leave. the `<TransitionGroup>` will inject specific transition props, so
       * remember to spread them through if you are wrapping the `<Transition>` as
       * with our `<Fade>` example.
       *
       * While this component is meant for multiple `Transition` or `CSSTransition`
       * children, sometimes you may want to have a single transition child with
       * content that you want to be transitioned out and in when you change it
       * (e.g. routes, images etc.) In that case you can change the `key` prop of
       * the transition child as you change its content, this will cause
       * `TransitionGroup` to transition the child out and back in.
       */
      children: _propTypes.default.node,
      /**
       * A convenience prop that enables or disables appear animations
       * for all children. Note that specifying this will override any defaults set
       * on individual children Transitions.
       */
      appear: _propTypes.default.bool,
      /**
       * A convenience prop that enables or disables enter animations
       * for all children. Note that specifying this will override any defaults set
       * on individual children Transitions.
       */
      enter: _propTypes.default.bool,
      /**
       * A convenience prop that enables or disables exit animations
       * for all children. Note that specifying this will override any defaults set
       * on individual children Transitions.
       */
      exit: _propTypes.default.bool,
      /**
       * You may need to apply reactive updates to a child as it is exiting.
       * This is generally done by using `cloneElement` however in the case of an exiting
       * child the element has already been removed and not accessible to the consumer.
       *
       * If you do need to update a child as it leaves you can provide a `childFactory`
       * to wrap every child, even the ones that are leaving.
       *
       * @type Function(child: ReactElement) -> ReactElement
       */
      childFactory: _propTypes.default.func
    } : {};
    TransitionGroup2.defaultProps = defaultProps79;
    var _default = TransitionGroup2;
    exports.default = _default;
    module.exports = exports.default;
  }
});

// node_modules/react-transition-group/cjs/ReplaceTransition.js
var require_ReplaceTransition = __commonJS({
  "node_modules/react-transition-group/cjs/ReplaceTransition.js"(exports, module) {
    "use strict";
    exports.__esModule = true;
    exports.default = void 0;
    var _propTypes = _interopRequireDefault(require_prop_types());
    var _react = _interopRequireDefault(require_react());
    var _reactDom = _interopRequireDefault(require_react_dom());
    var _TransitionGroup = _interopRequireDefault(require_TransitionGroup());
    function _interopRequireDefault(obj) {
      return obj && obj.__esModule ? obj : { default: obj };
    }
    function _objectWithoutPropertiesLoose(source, excluded) {
      if (source == null) return {};
      var target = {};
      var sourceKeys = Object.keys(source);
      var key, i13;
      for (i13 = 0; i13 < sourceKeys.length; i13++) {
        key = sourceKeys[i13];
        if (excluded.indexOf(key) >= 0) continue;
        target[key] = source[key];
      }
      return target;
    }
    function _inheritsLoose(subClass, superClass) {
      subClass.prototype = Object.create(superClass.prototype);
      subClass.prototype.constructor = subClass;
      subClass.__proto__ = superClass;
    }
    var ReplaceTransition = /* @__PURE__ */ function(_React$Component) {
      _inheritsLoose(ReplaceTransition2, _React$Component);
      function ReplaceTransition2() {
        var _this;
        for (var _len = arguments.length, _args = new Array(_len), _key = 0; _key < _len; _key++) {
          _args[_key] = arguments[_key];
        }
        _this = _React$Component.call.apply(_React$Component, [this].concat(_args)) || this;
        _this.handleEnter = function() {
          for (var _len2 = arguments.length, args = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
            args[_key2] = arguments[_key2];
          }
          return _this.handleLifecycle("onEnter", 0, args);
        };
        _this.handleEntering = function() {
          for (var _len3 = arguments.length, args = new Array(_len3), _key3 = 0; _key3 < _len3; _key3++) {
            args[_key3] = arguments[_key3];
          }
          return _this.handleLifecycle("onEntering", 0, args);
        };
        _this.handleEntered = function() {
          for (var _len4 = arguments.length, args = new Array(_len4), _key4 = 0; _key4 < _len4; _key4++) {
            args[_key4] = arguments[_key4];
          }
          return _this.handleLifecycle("onEntered", 0, args);
        };
        _this.handleExit = function() {
          for (var _len5 = arguments.length, args = new Array(_len5), _key5 = 0; _key5 < _len5; _key5++) {
            args[_key5] = arguments[_key5];
          }
          return _this.handleLifecycle("onExit", 1, args);
        };
        _this.handleExiting = function() {
          for (var _len6 = arguments.length, args = new Array(_len6), _key6 = 0; _key6 < _len6; _key6++) {
            args[_key6] = arguments[_key6];
          }
          return _this.handleLifecycle("onExiting", 1, args);
        };
        _this.handleExited = function() {
          for (var _len7 = arguments.length, args = new Array(_len7), _key7 = 0; _key7 < _len7; _key7++) {
            args[_key7] = arguments[_key7];
          }
          return _this.handleLifecycle("onExited", 1, args);
        };
        return _this;
      }
      var _proto = ReplaceTransition2.prototype;
      _proto.handleLifecycle = function handleLifecycle(handler2, idx, originalArgs) {
        var _child$props;
        var children = this.props.children;
        var child = _react.default.Children.toArray(children)[idx];
        if (child.props[handler2]) (_child$props = child.props)[handler2].apply(_child$props, originalArgs);
        if (this.props[handler2]) {
          var maybeNode = child.props.nodeRef ? void 0 : _reactDom.default.findDOMNode(this);
          this.props[handler2](maybeNode);
        }
      };
      _proto.render = function render() {
        var _this$props = this.props, children = _this$props.children, inProp = _this$props.in, props = _objectWithoutPropertiesLoose(_this$props, ["children", "in"]);
        var _React$Children$toArr = _react.default.Children.toArray(children), first = _React$Children$toArr[0], second = _React$Children$toArr[1];
        delete props.onEnter;
        delete props.onEntering;
        delete props.onEntered;
        delete props.onExit;
        delete props.onExiting;
        delete props.onExited;
        return /* @__PURE__ */ _react.default.createElement(_TransitionGroup.default, props, inProp ? _react.default.cloneElement(first, {
          key: "first",
          onEnter: this.handleEnter,
          onEntering: this.handleEntering,
          onEntered: this.handleEntered
        }) : _react.default.cloneElement(second, {
          key: "second",
          onEnter: this.handleExit,
          onEntering: this.handleExiting,
          onEntered: this.handleExited
        }));
      };
      return ReplaceTransition2;
    }(_react.default.Component);
    ReplaceTransition.propTypes = false ? {
      in: _propTypes.default.bool.isRequired,
      children: function children(props, propName) {
        if (_react.default.Children.count(props[propName]) !== 2) return new Error('"' + propName + '" must be exactly two transition components.');
        return null;
      }
    } : {};
    var _default = ReplaceTransition;
    exports.default = _default;
    module.exports = exports.default;
  }
});

// node_modules/react-transition-group/cjs/SwitchTransition.js
var require_SwitchTransition = __commonJS({
  "node_modules/react-transition-group/cjs/SwitchTransition.js"(exports) {
    "use strict";
    exports.__esModule = true;
    exports.default = exports.modes = void 0;
    var _react = _interopRequireDefault(require_react());
    var _propTypes = _interopRequireDefault(require_prop_types());
    var _Transition = require_Transition();
    var _TransitionGroupContext = _interopRequireDefault(require_TransitionGroupContext());
    var _leaveRenders;
    var _enterRenders;
    function _interopRequireDefault(obj) {
      return obj && obj.__esModule ? obj : { default: obj };
    }
    function _inheritsLoose(subClass, superClass) {
      subClass.prototype = Object.create(superClass.prototype);
      subClass.prototype.constructor = subClass;
      subClass.__proto__ = superClass;
    }
    function areChildrenDifferent(oldChildren, newChildren) {
      if (oldChildren === newChildren) return false;
      if (_react.default.isValidElement(oldChildren) && _react.default.isValidElement(newChildren) && oldChildren.key != null && oldChildren.key === newChildren.key) {
        return false;
      }
      return true;
    }
    var modes = {
      out: "out-in",
      in: "in-out"
    };
    exports.modes = modes;
    var callHook = function callHook2(element, name2, cb) {
      return function() {
        var _element$props;
        element.props[name2] && (_element$props = element.props)[name2].apply(_element$props, arguments);
        cb();
      };
    };
    var leaveRenders = (_leaveRenders = {}, _leaveRenders[modes.out] = function(_ref) {
      var current = _ref.current, changeState = _ref.changeState;
      return _react.default.cloneElement(current, {
        in: false,
        onExited: callHook(current, "onExited", function() {
          changeState(_Transition.ENTERING, null);
        })
      });
    }, _leaveRenders[modes.in] = function(_ref2) {
      var current = _ref2.current, changeState = _ref2.changeState, children = _ref2.children;
      return [current, _react.default.cloneElement(children, {
        in: true,
        onEntered: callHook(children, "onEntered", function() {
          changeState(_Transition.ENTERING);
        })
      })];
    }, _leaveRenders);
    var enterRenders = (_enterRenders = {}, _enterRenders[modes.out] = function(_ref3) {
      var children = _ref3.children, changeState = _ref3.changeState;
      return _react.default.cloneElement(children, {
        in: true,
        onEntered: callHook(children, "onEntered", function() {
          changeState(_Transition.ENTERED, _react.default.cloneElement(children, {
            in: true
          }));
        })
      });
    }, _enterRenders[modes.in] = function(_ref4) {
      var current = _ref4.current, children = _ref4.children, changeState = _ref4.changeState;
      return [_react.default.cloneElement(current, {
        in: false,
        onExited: callHook(current, "onExited", function() {
          changeState(_Transition.ENTERED, _react.default.cloneElement(children, {
            in: true
          }));
        })
      }), _react.default.cloneElement(children, {
        in: true
      })];
    }, _enterRenders);
    var SwitchTransition = /* @__PURE__ */ function(_React$Component) {
      _inheritsLoose(SwitchTransition2, _React$Component);
      function SwitchTransition2() {
        var _this;
        for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
          args[_key] = arguments[_key];
        }
        _this = _React$Component.call.apply(_React$Component, [this].concat(args)) || this;
        _this.state = {
          status: _Transition.ENTERED,
          current: null
        };
        _this.appeared = false;
        _this.changeState = function(status2, current) {
          if (current === void 0) {
            current = _this.state.current;
          }
          _this.setState({
            status: status2,
            current
          });
        };
        return _this;
      }
      var _proto = SwitchTransition2.prototype;
      _proto.componentDidMount = function componentDidMount() {
        this.appeared = true;
      };
      SwitchTransition2.getDerivedStateFromProps = function getDerivedStateFromProps(props, state2) {
        if (props.children == null) {
          return {
            current: null
          };
        }
        if (state2.status === _Transition.ENTERING && props.mode === modes.in) {
          return {
            status: _Transition.ENTERING
          };
        }
        if (state2.current && areChildrenDifferent(state2.current, props.children)) {
          return {
            status: _Transition.EXITING
          };
        }
        return {
          current: _react.default.cloneElement(props.children, {
            in: true
          })
        };
      };
      _proto.render = function render() {
        var _this$props = this.props, children = _this$props.children, mode = _this$props.mode, _this$state = this.state, status2 = _this$state.status, current = _this$state.current;
        var data = {
          children,
          current,
          changeState: this.changeState,
          status: status2
        };
        var component;
        switch (status2) {
          case _Transition.ENTERING:
            component = enterRenders[mode](data);
            break;
          case _Transition.EXITING:
            component = leaveRenders[mode](data);
            break;
          case _Transition.ENTERED:
            component = current;
        }
        return /* @__PURE__ */ _react.default.createElement(_TransitionGroupContext.default.Provider, {
          value: {
            isMounting: !this.appeared
          }
        }, component);
      };
      return SwitchTransition2;
    }(_react.default.Component);
    SwitchTransition.propTypes = false ? {
      /**
       * Transition modes.
       * `out-in`: Current element transitions out first, then when complete, the new element transitions in.
       * `in-out`: New element transitions in first, then when complete, the current element transitions out.
       *
       * @type {'out-in'|'in-out'}
       */
      mode: _propTypes.default.oneOf([modes.in, modes.out]),
      /**
       * Any `Transition` or `CSSTransition` component.
       */
      children: _propTypes.default.oneOfType([_propTypes.default.element.isRequired])
    } : {};
    SwitchTransition.defaultProps = {
      mode: modes.out
    };
    var _default = SwitchTransition;
    exports.default = _default;
  }
});

// node_modules/react-transition-group/cjs/index.js
var require_cjs = __commonJS({
  "node_modules/react-transition-group/cjs/index.js"(exports) {
    "use strict";
    exports.__esModule = true;
    exports.config = exports.Transition = exports.TransitionGroup = exports.SwitchTransition = exports.ReplaceTransition = exports.CSSTransition = void 0;
    var _CSSTransition = _interopRequireDefault(require_CSSTransition());
    exports.CSSTransition = _CSSTransition.default;
    var _ReplaceTransition = _interopRequireDefault(require_ReplaceTransition());
    exports.ReplaceTransition = _ReplaceTransition.default;
    var _SwitchTransition = _interopRequireDefault(require_SwitchTransition());
    exports.SwitchTransition = _SwitchTransition.default;
    var _TransitionGroup = _interopRequireDefault(require_TransitionGroup());
    exports.TransitionGroup = _TransitionGroup.default;
    var _Transition = _interopRequireDefault(require_Transition());
    exports.Transition = _Transition.default;
    var _config = _interopRequireDefault(require_config2());
    exports.config = _config.default;
    function _interopRequireDefault(obj) {
      return obj && obj.__esModule ? obj : { default: obj };
    }
  }
});

// node_modules/@mantine/notifications/esm/get-grouped-notifications/get-grouped-notifications.mjs
function getGroupedNotifications(notifications2, defaultPosition) {
  return notifications2.reduce(
    (acc, notification) => {
      acc[notification.position || defaultPosition].push(notification);
      return acc;
    },
    positions.reduce((acc, item2) => {
      acc[item2] = [];
      return acc;
    }, {})
  );
}
var positions;
var init_get_grouped_notifications = __esm({
  "node_modules/@mantine/notifications/esm/get-grouped-notifications/get-grouped-notifications.mjs"() {
    "use client";
    positions = [
      "bottom-center",
      "bottom-left",
      "bottom-right",
      "top-center",
      "top-left",
      "top-right"
    ];
  }
});

// node_modules/@mantine/notifications/esm/get-notification-state-styles.mjs
function getNotificationStateStyles({
  state: state2,
  maxHeight,
  position,
  transitionDuration
}) {
  const [vertical, horizontal] = position.split("-");
  const property = horizontal === "center" ? `${vertical}-center` : horizontal;
  const commonStyles = {
    opacity: 0,
    maxHeight,
    transform: transforms[property],
    transitionDuration: `${transitionDuration}ms, ${transitionDuration}ms, ${transitionDuration}ms`,
    transitionTimingFunction: "cubic-bezier(.51,.3,0,1.21), cubic-bezier(.51,.3,0,1.21), linear",
    transitionProperty: "opacity, transform, max-height"
  };
  const inState = {
    opacity: 1,
    transform: noTransform[property]
  };
  const outState = {
    opacity: 0,
    maxHeight: 0,
    transform: transforms[property]
  };
  const transitionStyles = {
    entering: inState,
    entered: inState,
    exiting: outState,
    exited: outState
  };
  return { ...commonStyles, ...transitionStyles[state2] };
}
var transforms, noTransform;
var init_get_notification_state_styles = __esm({
  "node_modules/@mantine/notifications/esm/get-notification-state-styles.mjs"() {
    "use client";
    transforms = {
      left: "translateX(-100%)",
      right: "translateX(100%)",
      "top-center": "translateY(-100%)",
      "bottom-center": "translateY(100%)"
    };
    noTransform = {
      left: "translateX(0)",
      right: "translateX(0)",
      "top-center": "translateY(0)",
      "bottom-center": "translateY(0)"
    };
  }
});

// node_modules/@mantine/notifications/esm/get-auto-close/get-auto-close.mjs
function getAutoClose(autoClose, notificationAutoClose) {
  if (typeof notificationAutoClose === "number") {
    return notificationAutoClose;
  }
  if (notificationAutoClose === false || autoClose === false) {
    return false;
  }
  return autoClose;
}
var init_get_auto_close = __esm({
  "node_modules/@mantine/notifications/esm/get-auto-close/get-auto-close.mjs"() {
    "use client";
  }
});

// node_modules/@mantine/notifications/esm/NotificationContainer.mjs
var import_jsx_runtime158, import_react192, NotificationContainer;
var init_NotificationContainer = __esm({
  "node_modules/@mantine/notifications/esm/NotificationContainer.mjs"() {
    "use client";
    import_jsx_runtime158 = __toESM(require_jsx_runtime(), 1);
    import_react192 = __toESM(require_react(), 1);
    init_esm2();
    init_get_auto_close();
    NotificationContainer = (0, import_react192.forwardRef)(
      ({ data, onHide, autoClose, ...others }, ref) => {
        const { autoClose: _autoClose, message, ...notificationProps } = data;
        const autoCloseDuration = getAutoClose(autoClose, data.autoClose);
        const autoCloseTimeout = (0, import_react192.useRef)();
        const cancelAutoClose = () => window.clearTimeout(autoCloseTimeout.current);
        const handleHide = () => {
          onHide(data.id);
          cancelAutoClose();
        };
        const handleAutoClose = () => {
          if (typeof autoCloseDuration === "number") {
            autoCloseTimeout.current = window.setTimeout(handleHide, autoCloseDuration);
          }
        };
        (0, import_react192.useEffect)(() => {
          data.onOpen?.(data);
        }, []);
        (0, import_react192.useEffect)(() => {
          handleAutoClose();
          return cancelAutoClose;
        }, [autoCloseDuration]);
        return /* @__PURE__ */ (0, import_jsx_runtime158.jsx)(
          Notification,
          {
            ...others,
            ...notificationProps,
            onClose: handleHide,
            ref,
            onMouseEnter: cancelAutoClose,
            onMouseLeave: handleAutoClose,
            children: message
          }
        );
      }
    );
    NotificationContainer.displayName = "@mantine/notifications/NotificationContainer";
  }
});

// node_modules/@mantine/notifications/esm/Notifications.module.css.mjs
var classes41;
var init_Notifications_module_css = __esm({
  "node_modules/@mantine/notifications/esm/Notifications.module.css.mjs"() {
    "use client";
    classes41 = { "root": "m_b37d9ac7", "notification": "m_5ed0edd0" };
  }
});

// node_modules/@mantine/notifications/esm/Notifications.mjs
var import_jsx_runtime159, import_react193, import_react_transition_group, Transition2, defaultProps78, varsResolver39, Notifications;
var init_Notifications = __esm({
  "node_modules/@mantine/notifications/esm/Notifications.mjs"() {
    "use client";
    import_jsx_runtime159 = __toESM(require_jsx_runtime(), 1);
    import_react193 = __toESM(require_react(), 1);
    import_react_transition_group = __toESM(require_cjs(), 1);
    init_esm2();
    init_esm();
    init_get_grouped_notifications();
    init_get_notification_state_styles();
    init_NotificationContainer();
    init_notifications_store();
    init_Notifications_module_css();
    Transition2 = import_react_transition_group.Transition;
    defaultProps78 = {
      position: "bottom-right",
      autoClose: 4e3,
      transitionDuration: 250,
      containerWidth: 440,
      notificationMaxHeight: 200,
      limit: 5,
      zIndex: getDefaultZIndex("overlay"),
      store: notificationsStore,
      withinPortal: true
    };
    varsResolver39 = createVarsResolver((_2, { zIndex, containerWidth }) => ({
      root: {
        "--notifications-z-index": zIndex?.toString(),
        "--notifications-container-width": rem(containerWidth)
      }
    }));
    Notifications = factory((_props, ref) => {
      const props = useProps("Notifications", defaultProps78, _props);
      const {
        classNames,
        className,
        style,
        styles,
        unstyled,
        vars,
        position,
        autoClose,
        transitionDuration,
        containerWidth,
        notificationMaxHeight,
        limit,
        zIndex,
        store,
        portalProps,
        withinPortal,
        ...others
      } = props;
      const theme2 = useMantineTheme();
      const data = useNotifications(store);
      const forceUpdate = useForceUpdate();
      const shouldReduceMotion = useReducedMotion();
      const refs = (0, import_react193.useRef)({});
      const previousLength = (0, import_react193.useRef)(0);
      const reduceMotion = theme2.respectReducedMotion ? shouldReduceMotion : false;
      const duration = reduceMotion ? 1 : transitionDuration;
      const getStyles = useStyles({
        name: "Notifications",
        classes: classes41,
        props,
        className,
        style,
        classNames,
        styles,
        unstyled,
        vars,
        varsResolver: varsResolver39
      });
      (0, import_react193.useEffect)(() => {
        store?.updateState((current) => ({
          ...current,
          limit: limit || 5,
          defaultPosition: position
        }));
      }, [limit, position]);
      useDidUpdate(() => {
        if (data.notifications.length > previousLength.current) {
          setTimeout(() => forceUpdate(), 0);
        }
        previousLength.current = data.notifications.length;
      }, [data.notifications]);
      const grouped = getGroupedNotifications(data.notifications, position);
      const groupedComponents = positions.reduce(
        (acc, pos) => {
          acc[pos] = grouped[pos].map(({ style: notificationStyle, ...notification }) => /* @__PURE__ */ (0, import_jsx_runtime159.jsx)(
            Transition2,
            {
              timeout: duration,
              onEnter: () => refs.current[notification.id].offsetHeight,
              nodeRef: { current: refs.current[notification.id] },
              children: (state2) => /* @__PURE__ */ (0, import_jsx_runtime159.jsx)(
                NotificationContainer,
                {
                  ref: (node) => {
                    refs.current[notification.id] = node;
                  },
                  data: notification,
                  onHide: (id3) => hideNotification(id3, store),
                  autoClose,
                  ...getStyles("notification", {
                    style: {
                      ...getNotificationStateStyles({
                        state: state2,
                        position: pos,
                        transitionDuration: duration,
                        maxHeight: notificationMaxHeight
                      }),
                      ...notificationStyle
                    }
                  })
                }
              )
            },
            notification.id
          ));
          return acc;
        },
        {}
      );
      return /* @__PURE__ */ (0, import_jsx_runtime159.jsxs)(OptionalPortal, { withinPortal, ...portalProps, children: [
        /* @__PURE__ */ (0, import_jsx_runtime159.jsx)(Box, { ...getStyles("root"), "data-position": "top-center", ref, ...others, children: /* @__PURE__ */ (0, import_jsx_runtime159.jsx)(import_react_transition_group.TransitionGroup, { children: groupedComponents["top-center"] }) }),
        /* @__PURE__ */ (0, import_jsx_runtime159.jsx)(Box, { ...getStyles("root"), "data-position": "top-left", ...others, children: /* @__PURE__ */ (0, import_jsx_runtime159.jsx)(import_react_transition_group.TransitionGroup, { children: groupedComponents["top-left"] }) }),
        /* @__PURE__ */ (0, import_jsx_runtime159.jsx)(
          Box,
          {
            ...getStyles("root", { className: import_react_remove_scroll2.RemoveScroll.classNames.fullWidth }),
            "data-position": "top-right",
            ...others,
            children: /* @__PURE__ */ (0, import_jsx_runtime159.jsx)(import_react_transition_group.TransitionGroup, { children: groupedComponents["top-right"] })
          }
        ),
        /* @__PURE__ */ (0, import_jsx_runtime159.jsx)(
          Box,
          {
            ...getStyles("root", { className: import_react_remove_scroll2.RemoveScroll.classNames.fullWidth }),
            "data-position": "bottom-right",
            ...others,
            children: /* @__PURE__ */ (0, import_jsx_runtime159.jsx)(import_react_transition_group.TransitionGroup, { children: groupedComponents["bottom-right"] })
          }
        ),
        /* @__PURE__ */ (0, import_jsx_runtime159.jsx)(Box, { ...getStyles("root"), "data-position": "bottom-left", ...others, children: /* @__PURE__ */ (0, import_jsx_runtime159.jsx)(import_react_transition_group.TransitionGroup, { children: groupedComponents["bottom-left"] }) }),
        /* @__PURE__ */ (0, import_jsx_runtime159.jsx)(Box, { ...getStyles("root"), "data-position": "bottom-center", ...others, children: /* @__PURE__ */ (0, import_jsx_runtime159.jsx)(import_react_transition_group.TransitionGroup, { children: groupedComponents["bottom-center"] }) })
      ] });
    });
    Notifications.classes = classes41;
    Notifications.displayName = "@mantine/notifications/Notifications";
    Notifications.show = notifications.show;
    Notifications.hide = notifications.hide;
    Notifications.update = notifications.update;
    Notifications.clean = notifications.clean;
    Notifications.cleanQueue = notifications.cleanQueue;
    Notifications.updateState = notifications.updateState;
  }
});

// node_modules/@mantine/notifications/esm/index.mjs
var init_esm5 = __esm({
  "node_modules/@mantine/notifications/esm/index.mjs"() {
    init_notifications_store();
    init_Notifications();
  }
});

// node_modules/effector/effector.mjs
function e(e14, t20) {
  for (const r7 in e14) t20(e14[r7], r7);
}
function t(e14, t20) {
  e14.forEach(t20);
}
function r2(e14, t20, r7) {
  if (!e14) throw Error(`${r7 ? r7 + ": " : ""}${t20}`);
}
function n(e14, t20) {
  const r7 = E(e14).meta || {};
  ge = { id: E(e14).id, parent: ge, value: e14, template: r7.template || be(), sidRoot: r7.sidRoot || ge && ge.sidRoot, meta: r7 };
  try {
    return t20();
  } finally {
    ye("region"), ge = H(ge);
  }
}
function a({ node: e14 = [], from: r7, source: n13, parent: a18 = r7 || n13, to: o8, target: s36, child: i13 = o8 || s36, scope: c15 = {}, meta: l12 = {}, family: d12 = { type: "regular" }, regional: u4 } = {}) {
  const f15 = Se(a18), p11 = Se(d12.links), m12 = Se(d12.owners), h6 = [];
  t(e14, (e15) => e15 && ie(h6, e15));
  const g7 = { id: fe(), seq: h6, next: Se(i13), meta: l12, scope: c15, family: { type: d12.type || "crosslink", links: p11, owners: m12 } };
  return t(p11, (e15) => ie(V(e15), g7)), t(m12, (e15) => ie(T(e15), g7)), t(f15, (e15) => ie(e15.next, g7)), u4 && ge && we(B(ge), [g7]), g7;
}
function o(e14, r7, n13) {
  let a18, o8 = at, s36 = null, i13 = et;
  if (e14.target && (r7 = e14.params, n13 = e14.defer, a18 = e14.meta, o8 = "page" in e14 ? e14.page : o8, e14.stack && (s36 = e14.stack), i13 = U(e14) || i13, e14 = e14.target), i13 && et && i13 !== et && (et = null), Array.isArray(e14)) for (let t20 = 0; t20 < e14.length; t20++) Qe("pure", o8, E(e14[t20]), s36, r7[t20], i13, a18);
  else Qe("pure", o8, E(e14), s36, r7, i13, a18);
  if (n13 && !tt) return;
  const c15 = { isRoot: tt, currentPage: at, scope: et, isWatch: rt, isPure: nt };
  let l12, d12, u4, f15, p11, m12;
  tt = 0;
  e: for (; f15 = Ke(); ) {
    const { idx: e15, stack: r8, type: n14 } = f15;
    u4 = r8.node, at = p11 = r8.page, et = U(r8), p11 ? m12 = p11.reg : et && (m12 = et.reg);
    const a19 = !!p11, o9 = !!et, s37 = { fail: 0, scope: u4.scope };
    l12 = d12 = 0;
    for (let t20 = e15; t20 < u4.seq.length && !l12; t20++) {
      const i14 = u4.seq[t20];
      if (i14.order) {
        const { priority: a20, barrierID: o10 } = i14.order, s38 = o10 ? p11 ? `${p11.fullID}_${o10}` : o10 : 0;
        if (t20 !== e15 || n14 !== a20) {
          o10 ? Ze.has(s38) || (Ze.add(s38), Xe(t20, r8, a20, o10)) : Xe(t20, r8, a20, 0);
          continue e;
        }
        o10 && Ze.delete(s38);
      }
      switch (i14.type) {
        case "mov": {
          const e17 = i14.data;
          let t21;
          switch (e17.from) {
            case "stack":
              t21 = B(r8);
              break;
            case "a":
            case "b":
              t21 = r8[e17.from];
              break;
            case "value":
              t21 = e17.store;
              break;
            case "store":
              if (m12 && !m12[e17.store.id]) if (a19) {
                const t22 = ct(p11, e17.store.id);
                r8.page = p11 = t22, t22 ? m12 = t22.reg : o9 ? (pt(et, e17.store, 0, 1, e17.softRead), m12 = et.reg) : m12 = void 0;
              } else o9 && pt(et, e17.store, 0, 1, e17.softRead);
              t21 = Be(m12 && m12[e17.store.id] || e17.store);
          }
          switch (e17.to) {
            case "stack":
              r8.value = t21;
              break;
            case "a":
            case "b":
              r8[e17.to] = t21;
              break;
            case "store":
              lt(p11, et, e17.target, 0).current = t21;
          }
          break;
        }
        case "compute":
          const e16 = i14.data;
          if (e16.fn) {
            rt = "watch" === u4.meta.op, nt = e16.pure;
            const t21 = e16.safe ? (0, e16.fn)(B(r8), s37.scope, r8) : mt(s37, e16.fn, r8);
            e16.filter ? d12 = !t21 : r8.value = t21, rt = c15.isWatch, nt = c15.isPure;
          }
      }
      l12 = s37.fail || d12;
    }
    if (dt && dt(r8, s37), !l12) {
      const e16 = B(r8), n15 = U(r8);
      if (t(u4.next, (t20) => {
        Qe("child", p11, t20, r8, e16, n15);
      }), n15) {
        u4.meta.needFxCounter && Qe("child", p11, n15.fxCount, r8, e16, n15), u4.meta.storeChange && Qe("child", p11, n15.storeChange, r8, e16, n15), u4.meta.warnSerialize && Qe("child", p11, n15.warnSerializeNode, r8, e16, n15);
        const a20 = n15.additionalLinks[u4.id];
        a20 && t(a20, (t20) => {
          Qe("child", p11, t20, r8, e16, n15);
        });
      }
    }
  }
  tt = c15.isRoot, at = c15.currentPage, et = U(c15);
}
function s(t20, r7 = "combine") {
  let n13 = r7 + "(", a18 = "", o8 = 0;
  return e(t20, (e14) => {
    o8 < 25 && (null != e14 && (n13 += a18, n13 += Q(e14) ? K(e14).fullName : e14.toString()), o8 += 1, a18 = ", ");
  }), n13 + ")";
}
function c(e14, t20) {
  let r7, n13;
  const a18 = e14;
  if (t20) {
    const a19 = K(t20);
    0 === e14.length ? (r7 = a19.path, n13 = a19.fullName) : (r7 = a19.path.concat([e14]), n13 = 0 === a19.fullName.length ? e14 : a19.fullName + "/" + e14);
  } else r7 = 0 === e14.length ? [] : [e14], n13 = e14;
  return { shortName: a18, fullName: n13, path: r7 };
}
function l(e14, t20) {
  if (!t20) return e14;
  if (!t20.name && !t20.named && !t20.loc) return e14;
  let r7 = `[${e14}]`;
  const n13 = t20.named || t20.name;
  n13 && (r7 += ` unit '${n13}'`);
  const a18 = t20.loc;
  return !n13 && a18 && (r7 += ` (${a18.file}:${a18.line}:${a18.column})`), r7;
}
function d(e14, t20) {
  const r7 = t20 ? e14 : e14[0];
  ze(r7);
  let n13 = r7.or;
  const a18 = r7.and;
  if (a18) {
    const r8 = t20 ? a18 : a18[0];
    if ($e(r8) && "and" in r8) {
      const r9 = d(a18, t20);
      e14 = r9[0], n13 = { ...n13, ...r9[1] };
    } else e14 = a18;
  }
  return [e14, n13];
}
function u(e14) {
  const t20 = () => e14();
  return t20.unsubscribe = () => e14(), t20;
}
function f(e14, ...t20) {
  const r7 = be();
  if (r7) {
    const n13 = r7.handlers[e14];
    if (n13) return n13(r7, ...t20);
  }
}
function p(e14, t20) {
  const n13 = ht({ or: t20, and: "string" == typeof e14 ? { name: e14 } : e14 }), s36 = l("event", n13), i13 = (e15, ...t21) => (r2(!G(i13, "derived"), "call of derived event is not supported, use createEvent instead", s36), r2(!nt, "unit call from pure function is not supported, use operators like sample instead", s36), at ? ((e16, t22, r7, n14) => {
    const a18 = at;
    let o8 = null;
    if (t22) for (o8 = at; o8 && o8.template !== t22; ) o8 = H(o8);
    st(o8);
    const s37 = e16.create(r7, n14);
    return st(a18), s37;
  })(i13, c15, e15, t21) : i13.create(e15, t21)), c15 = be(), d12 = Object.assign(i13, { graphite: a({ meta: xt(n13.actualOp || "event", i13, n13), regional: 1 }), create: (e15) => (o({ target: i13, params: e15, scope: et }), e15), watch: (e15) => $t(i13, e15), map: (e15) => zt(i13, "map", e15, [Ve()]), filter: (e15) => zt(i13, "filter", e15.fn ? e15 : e15.fn, [Ve(Oe, 1)]), filterMap: (e15) => zt(i13, "filterMap", e15, [Ve(), _e((e16) => !xe(e16), 1)]), prepend(e15) {
    r2(i13.targetable, ".prepend of derived event is not supported, call source event instead", s36);
    const t21 = p("* \u2192 " + i13.shortName, { parent: H(i13) });
    return f("eventPrepend", E(t21)), wt(t21, i13, [Ve()], "prepend", e15), Mt(i13, t21), t21;
  } });
  return null != n13 && n13.domain && n13.domain.hooks.event(d12), J(d12, "id", d12.graphite.id), ye(d12.graphite), d12;
}
function m(e14, n13, a18, o8, s36) {
  return je(a18, `${s36} ${n13}`, "first argument"), r2(Me(o8), "second argument should be a function", s36), ce(!G(e14, "derived"), `${n13} in derived store`, `${n13} in store created via createStore`, s36), t(Array.isArray(a18) ? a18 : [a18], (t20) => {
    e14.off(t20), W(e14).set(t20, kt(jt(t20, e14, "on", Ie, o8)));
  }), e14;
}
function h(e14, n13) {
  const s36 = ht(n13), i13 = Le(e14), c15 = l("store", s36), d12 = p({ named: "updates", derived: 1 });
  f("storeBase", i13);
  const u4 = i13.id, g7 = "skipVoid" in s36, y4 = g7 && !s36.skipVoid;
  ce(!(g7 && s36.skipVoid), "{skipVoid: true}", "updateFilter", c15);
  const b4 = { subscribers: /* @__PURE__ */ new Map(), updates: d12, defaultState: e14, stateRef: i13, getState() {
    let e15, t20 = i13;
    if (at) {
      let t21 = at;
      for (; t21 && !t21.reg[u4]; ) t21 = H(t21);
      t21 && (e15 = t21);
    }
    return !e15 && et && (pt(et, i13, 1), e15 = et), e15 && (t20 = e15.reg[u4]), Be(t20);
  }, setState: (e15) => o({ target: b4, params: e15, defer: 1, scope: et }), reset: (...e15) => (r2(b4.targetable, ".reset of derived store is not supported", c15), t(e15, (e16) => m(b4, ".reset", e16, () => b4.defaultState, c15)), b4), on: (e15, t20) => (r2(b4.targetable, ".on of derived store is not supported", c15), m(b4, ".on", e15, t20, c15)), off(e15) {
    const t20 = W(b4).get(e15);
    return t20 && (t20(), W(b4).delete(e15)), b4;
  }, map(e15, t20) {
    let r7, n14;
    $e(e15) && (r7 = e15, e15 = e15.fn);
    const a18 = b4.getState(), o8 = xe(a18);
    be() ? n14 = null : (!o8 || o8 && y4) && (n14 = e15(a18));
    const s37 = h(n14, { name: `${b4.shortName} \u2192 *`, derived: 1, ...t20, and: r7 }), c16 = jt(b4, s37, "map", Oe, e15);
    return We(L(s37), { type: "map", fn: e15, from: i13 }), L(s37).noInit = 1, f("storeMap", i13, c16), s37;
  }, watch(e15, t20) {
    if (ce(!t20, "watch second argument", "sample", c15), !t20 || !Q(e15)) {
      const t21 = $t(b4, e15);
      return f("storeWatch", i13, e15) || e15(b4.getState()), t21;
    }
    return r2(Me(t20), "second argument should be a function", c15), e15.watch((e16) => t20(b4.getState(), e16));
  } }, v5 = xt("store", b4, s36), k5 = b4.defaultConfig.updateFilter;
  b4.graphite = a({ scope: { state: i13, fn: k5 }, node: [_e((e15, t20, r7) => (r7.scope && !r7.scope.reg[i13.id] && (r7.b = 1), e15)), Ee(i13), _e((e15, t20, { a: r7, b: n14 }) => {
    const a18 = xe(e15);
    return a18 && !g7 && console.error(`${c15}: ${At}`), (a18 && y4 || !a18) && (e15 !== r7 || n14);
  }, 1), k5 && Ve(Ne, 1), qe({ from: "stack", target: i13 })], child: d12, meta: { ...v5, defaultState: e14 }, regional: 1 }), J(b4, "id", b4.graphite.id), J(b4, "rootStateRefId", u4);
  const w11 = G(b4, "serialize"), S2 = G(b4, "derived"), $5 = "ignore" === w11, M6 = G(b4, "sid");
  M6 && (J(b4, "storeChange", 1), i13.sid = M6), M6 || $5 || S2 || J(b4, "warnSerialize", 1);
  const x3 = xe(e14);
  return r2(S2 || !x3 || x3 && y4, At, c15), S2 && x3 && !g7 && console.error(`${c15}: ${At}`), we(b4, [d12]), null != s36 && s36.domain && s36.domain.hooks.store(b4), S2 || (b4.reinit = p({ named: "reinit" }), b4.reset(b4.reinit)), i13.meta = b4.graphite.meta, ye(b4.graphite), b4;
}
function g(...e14) {
  let t20, n13, a18;
  [e14, a18] = d(e14);
  const o8 = l("combine", a18), s36 = e14[e14.length - 1], i13 = e14.length > 1 && !Y(s36) && $e(s36), c15 = i13 && s36, u4 = i13 ? e14[e14.length - 2] : s36;
  let f15, p11, m12;
  if (Me(u4) ? (n13 = e14.slice(0, i13 ? -2 : -1), t20 = u4) : n13 = e14, 1 === n13.length) {
    const e15 = n13[0];
    Y(e15) || (f15 = e15, p11 = 1);
  }
  if (!p11 && (f15 = n13, t20)) {
    m12 = 1;
    const e15 = t20;
    t20 = (t21) => e15(...t21);
  }
  return r2($e(f15), `${o8}: shape should be an object`), Ct(Array.isArray(f15), !m12, f15, a18, t20, c15);
}
function y() {
  const e14 = {};
  return e14.req = new Promise((t20, r7) => {
    e14.rs = t20, e14.rj = r7;
  }), e14.req.catch(() => {
  }), e14;
}
function b(e14, t20 = {}) {
  const n13 = ht(Me(e14) ? { handler: e14 } : e14, t20), s36 = l("effect", n13), i13 = p(Me(e14) ? { handler: e14 } : e14, { ...t20, actualOp: "effect" }), c15 = E(i13);
  J(c15, "op", i13.kind = "effect"), i13.use = (e15) => (r2(Me(e15), ".use argument should be a function", s36), b4.scope.handler = e15, i13), i13.use.getCurrent = () => b4.scope.handler;
  const d12 = i13.finally = p({ named: "finally", derived: 1 }), u4 = i13.done = d12.filterMap({ named: "done", fn({ status: e15, params: t21, result: r7 }) {
    if ("done" === e15) return { params: t21, result: r7 };
  } }), f15 = i13.fail = d12.filterMap({ named: "fail", fn({ status: e15, params: t21, error: r7 }) {
    if ("fail" === e15) return { params: t21, error: r7 };
  } }), m12 = i13.doneData = u4.map({ named: "doneData", fn: ({ result: e15 }) => e15 }), g7 = i13.failData = f15.map({ named: "failData", fn: ({ error: e15 }) => e15 }), b4 = a({ scope: { handler: i13.defaultConfig.handler || (() => r2(0, `no handler used in ${i13.compositeName.fullName}`)) }, node: [_e((e15, t21, r7) => {
    let n14 = t21.handler;
    const a18 = U(r7);
    if (a18) {
      const e16 = a18.handlers.unitMap.get(i13) || a18.handlers.sidMap[i13.sid];
      e16 && (n14 = e16);
    }
    return e15.handler = n14, e15;
  }, 0, 1), _e((e15, t21, r7) => {
    if (t21.runnerFn && !t21.runnerFn(e15, null, r7)) return;
    const { params: n14, req: a18, handler: o8, args: s37 = [n14] } = e15, i14 = It(n14, a18, 1, d12, r7), c16 = It(n14, a18, 0, d12, r7), [l12, u5] = Nt(o8, c16, s37);
    l12 && ($e(u5) && Me(u5.then) ? u5.then(i14, c16) : i14(u5));
  }, 0, 1)], meta: { op: "fx", fx: "runner" } });
  c15.scope.runner = b4, ie(c15.seq, _e((e15, { runner: t21 }, r7) => {
    const n14 = H(r7) ? { params: e15, req: { rs(e16) {
    }, rj(e16) {
    } } } : e15;
    return r7.meta || (r7.meta = { fxID: pe() }), o({ target: t21, params: n14, defer: 1, scope: U(r7), meta: r7.meta }), n14.params;
  })), i13.create = (e15) => {
    const t21 = y(), r7 = { params: e15, req: t21 };
    if (et && !rt) {
      const e16 = et;
      t21.req.finally(() => {
        ot(e16);
      }).catch(() => {
      });
    }
    return o({ target: i13, params: r7, scope: et }), t21.req;
  };
  const v5 = i13.inFlight = h(0, { serialize: "ignore", named: (G(i13, "name") || i13.graphite.id) + ".inFlight" }).on(i13, (e15) => e15 + 1).on(d12, (e15) => e15 - 1).map({ fn: (e15) => e15, named: "inFlight" });
  J(d12, "needFxCounter", "dec"), J(i13, "needFxCounter", 1);
  const k5 = i13.pending = v5.map({ fn: (e15) => e15 > 0, named: "pending" });
  return we(i13, [d12, u4, f15, m12, g7, k5, v5]), null != n13 && n13.domain && n13.domain.hooks.effect(i13), i13;
}
function v(e14) {
  let t20;
  [e14, t20] = d(e14, 1);
  const n13 = l("attach", t20);
  let { source: a18, effect: s36, mapParams: i13, domain: u4 } = e14;
  ee(s36) && r2(xe(u4), "`domain` can only be used with a plain function", n13);
  const f15 = b(e14, t20);
  J(f15, "attached", 1);
  const { runner: p11 } = E(f15).scope;
  let m12;
  const h6 = (e15, t21, r7) => {
    const { params: n14, req: s37, handler: c15 } = e15, l12 = f15.finally, d12 = It(n14, s37, 0, l12, r7), u5 = r7.a, p12 = ee(c15);
    let m13, h7 = 1;
    if (i13 ? [h7, m13] = Nt(i13, d12, [n14, u5]) : m13 = a18 && p12 ? u5 : n14, h7) {
      if (!p12) return e15.args = [u5, m13], 1;
      o({ target: c15, params: { params: m13, req: { rs: It(n14, s37, 1, l12, r7), rj: d12 } }, page: r7.page, defer: 1, meta: r7.meta });
    }
  };
  if (a18) {
    let e15;
    p11.scope.runnerFn = h6, Y(a18) ? (e15 = a18, we(e15, [f15])) : (e15 = g(a18), we(f15, [e15])), m12 = [_e((e16) => e16, 0, 1), Ee(L(e15))], delete p11.seq[1].order;
  } else m12 = [_e(h6, 1, 1)];
  p11.seq.splice(1, 0, ...m12), f15.use(s36);
  const y4 = H(s36);
  return y4 && (Object.assign(K(f15), c(f15.shortName, y4)), f15.defaultConfig.parent = y4), Mt(s36, f15, "effect"), f15;
}
function $(e14, t20) {
  const r7 = l("merge", t20);
  je(e14, r7, "first argument");
  const n13 = p({ name: s(e14, "merge"), derived: 1, and: t20 });
  return wt(e14, n13, [], "merge"), n13;
}
function M(e14, n13) {
  let a18 = 0;
  return t(Rt, (t20) => {
    t20 in e14 && (r2(null != e14[t20], Ft(n13, t20)), a18 = 1);
  }), a18;
}
function x(...e14) {
  let t20, r7, n13, a18, [[o8, s36, i13], c15] = d(e14), u4 = 1;
  const f15 = l("sample", c15);
  return xe(s36) && $e(o8) && M(o8, f15) && (s36 = o8.clock, i13 = o8.fn, "batch" in o8 ? u4 = o8.batch : (ce(!("greedy" in o8), "greedy in sample", "batch", f15), u4 = !o8.greedy), a18 = o8.filter, t20 = o8.target, r7 = o8.name, n13 = o8.sid, o8 = o8.source), qt("sample", s36, o8, a18, t20, i13, r7, c15, u4, 1, 0, n13);
}
function j(...t20) {
  const n13 = "split";
  let o8, s36, [[i13, c15], u4] = d(t20);
  const m12 = l(n13, u4), h6 = !c15;
  h6 && (o8 = i13.cases, c15 = i13.match, s36 = i13.clock, i13 = i13.source);
  const g7 = Y(c15), y4 = !Q(c15) && Me(c15), b4 = !g7 && !y4 && $e(c15);
  r2(Q(i13), "source must be a unit", m12), o8 || (o8 = {}), h6 ? e(o8, (e14, t21) => Ce(m12, e14, `cases.${t21}`)) : (r2(b4, "match should be an object", m12), e(c15, (e14, t21) => o8[t21] = p({ derived: 1, named: `cases.${t21}`, and: u4 })), o8.__ = p({ derived: 1, named: "cases.__", and: u4 }));
  const v5 = new Set([].concat(i13, s36 || [], Object.values(o8))), k5 = Object.keys(g7 || y4 ? o8 : c15);
  let w11;
  if (g7 || y4) g7 && v5.add(c15), w11 = [g7 && Ee(L(c15), 0, 1), De({ safe: g7, filter: 1, pure: !g7, fn(e14, t21, r7) {
    const n14 = String(g7 ? r7.a : c15(e14));
    _t(t21, oe(k5, n14) ? n14 : "__", e14, r7);
  } })];
  else if (b4) {
    const t21 = Le({});
    t21.type = "shape";
    const r7 = [];
    let n14;
    e(c15, (e14, a18) => {
      if (Q(e14)) {
        n14 = 1, ie(r7, a18), v5.add(e14);
        const o9 = wt(e14, [], [Ee(t21), _e((e15, t22, { a: r8 }) => r8[a18] = e15)]);
        if (Y(e14)) {
          t21.current[a18] = e14.getState();
          const r8 = L(e14);
          We(t21, { from: r8, field: a18, type: "field" }), f("splitMatchStore", r8, o9);
        }
      }
    }), n14 && f("splitBase", t21), w11 = [n14 && Ee(t21, 0, 1), Ve((e14, t22, n15) => {
      for (let a18 = 0; a18 < k5.length; a18++) {
        const o9 = k5[a18];
        if (oe(r7, o9) ? n15.a[o9] : c15[o9](e14)) return void _t(t22, o9, e14, n15);
      }
      _t(t22, "__", e14, n15);
    }, 1)];
  } else r2(0, "expect match to be unit, function or object");
  const S2 = a({ meta: { op: n13 }, parent: s36 ? [] : i13, scope: o8, node: w11, family: { owners: Array.from(v5) }, regional: 1 });
  if (s36 && qt(n13, s36, i13, null, S2, null, n13, u4, 0, 0, 0), !h6) return o8;
}
function C(e14, { scope: t20, params: r7 } = {}) {
  if (!Q(e14)) return Promise.reject(new Error("first argument should be unit"));
  if (!(ee(e14) || Z(e14) || Y(e14) || ne(e14))) return Promise.reject(new Error("first argument accepts only effects, events, stores or scopes"));
  if (ne(e14)) t20 = e14;
  else if (!te(e14)) {
    const t21 = l("allSettled", E(e14).meta);
    return Promise.reject(Error(`${t21}: unit should be targetable`));
  }
  const n13 = y();
  n13.parentFork = et;
  const { fxCount: a18 } = t20;
  ie(a18.scope.defers, n13);
  const s36 = [], i13 = [];
  return ne(e14) || (ie(s36, e14), ie(i13, ee(e14) ? { params: r7, req: { rs(e15) {
    n13.value = { status: "done", value: e15 };
  }, rj(e15) {
    n13.value = { status: "fail", value: e15 };
  } } } : r7)), ie(s36, a18), ie(i13, null), o({ target: s36, params: i13, scope: t20 }), n13.req;
}
function N(e14, r7, n13) {
  const a18 = [];
  (function e15(o8) {
    oe(a18, o8) || (ie(a18, o8), "store" === G(o8, "op") && (n13 || G(o8, "sid")) && r7(o8, G(o8, "sid")), t(o8.next, e15), t(V(o8), e15), t(T(o8), e15));
  })(e14);
}
function I(e14, n13) {
  const a18 = Array.isArray(e14) ? new Map(e14) : e14, o8 = /* @__PURE__ */ new Map();
  let s36 = 0;
  if (a18 instanceof Map) {
    const e15 = {};
    return t(a18, (t20, a19) => {
      r2(Q(a19), "Map key should be a unit"), n13 && n13(a19, t20), a19.sid && (a19.sid in e15 && (s36 = 1), e15[a19.sid] = t20), o8.set(a19, t20);
    }), { sidMap: e15, unitMap: o8, hasSidDoubles: s36 };
  }
  return { sidMap: a18, unitMap: o8 };
}
function O(e14, n13) {
  let o8, s36 = e14;
  re(e14) && (ce(0, "fork(domain)", "fork()"), o8 = e14, s36 = n13);
  const i13 = ((e15) => {
    const r7 = a({ scope: { defers: [], inFlight: 0, fxID: 0 }, node: [_e((e16, t20, r8) => {
      r8.parent ? "dec" === r8.parent.node.meta.needFxCounter ? t20.inFlight -= 1 : (t20.inFlight += 1, t20.fxID += 1) : t20.fxID += 1;
    }), De({ priority: "sampler", batch: 1 }), _e((e16, r8) => {
      const { defers: n15, fxID: a18 } = r8;
      r8.inFlight > 0 || 0 === n15.length || Promise.resolve().then(() => {
        r8.fxID === a18 && t(n15.splice(0, n15.length), (e17) => {
          ot(e17.parentFork), e17.rs(e17.value);
        });
      });
    }, 0, 1)] }), n14 = a({ node: [_e((e16, t20, r8) => {
      const n15 = r8.parent;
      if (n15) {
        const t21 = n15.node;
        if (Vt(n15)) {
          const n16 = U(r8), a18 = t21.meta.sid;
          n16.sidIdMap[a18] = t21.scope.state.id, n16.values.sidMap[a18] = e16;
          const o10 = t21.meta.serialize;
          o10 && n16.sidSerializeSettings.set(a18, "ignore" === o10 ? { ignore: 1 } : { ignore: 0, write: o10.write });
        }
      }
    })] }), o9 = a({ node: [_e((e16, t20, r8) => {
      const n15 = U(r8);
      n15 && r8.parent && Vt(r8.parent) && (n15.warnSerialize = 1);
    })] }), s37 = { cloneOf: e15, reg: {}, values: { sidMap: {}, idMap: {} }, sidIdMap: {}, sidSerializeSettings: /* @__PURE__ */ new Map(), getState(e16) {
      if ("current" in e16) return lt(at, s37, e16, 0).current;
      const t20 = E(e16);
      return lt(at, s37, t20.scope.state, 1).current;
    }, kind: "scope", graphite: a({ family: { type: "domain", links: [r7, n14, o9] }, meta: { unit: "fork" }, scope: { forkInFlightCounter: r7 } }), additionalLinks: {}, handlers: { sidMap: {}, unitMap: /* @__PURE__ */ new Map() }, fxCount: r7, storeChange: n14, warnSerializeNode: o9 };
    return s37;
  })(o8);
  if (s36) {
    if (s36.values) {
      const { sidMap: e15, unitMap: n14, hasSidDoubles: a18 } = I(s36.values, (e16) => r2(Y(e16) && te(e16), "Values map can contain only writable stores as keys"));
      Object.assign(i13.values.sidMap, e15), t(n14, (e16, t20) => {
        if (i13.values.idMap[t20.stateRef.id] = e16, i13.sidIdMap[G(t20, "sid")] = t20.stateRef.id, "ignore" === G(t20, "serialize")) {
          const e17 = G(t20, "sid");
          i13.sidSerializeSettings.set(e17, { ignore: 1 });
        }
      }), i13.fromSerialize = !(Array.isArray(s36.values) || s36.values instanceof Map), i13.hasSidDoubles = a18;
    }
    s36.handlers && (ce(s36.handlers instanceof Map || Array.isArray(s36.handlers), "object with handlers", "array"), i13.handlers = I(s36.handlers, (e15) => r2(ee(e15), "Handlers map can contain only effects as keys")));
  }
  return i13;
}
function F(e14, { scope: t20, safe: n13 } = {}) {
  r2(t20 || et || n13, "scopeBind: scope not found");
  const a18 = t20 || et;
  return (t21) => {
    function r7() {
      ot(s36);
    }
    let n14, o8 = 0;
    const s36 = et;
    ot(a18);
    try {
      n14 = e14(t21);
    } catch (e15) {
      n14 = e15, o8 = 1;
    }
    if (r7(), o8) throw n14;
    return n14 instanceof Promise && n14.then(r7, r7), n14;
  };
}
function q(t20, n13 = {}) {
  t20.warnSerialize && console.error("There is a store without sid in this scope, its value is omitted"), r2(!t20.hasSidDoubles, "duplicate sid found in this scope");
  const a18 = n13.ignore ? n13.ignore.map(({ sid: e14 }) => e14) : [], o8 = {};
  return e(t20.values.sidMap, (e14, r7) => {
    var n14;
    if (oe(a18, r7)) return;
    const s36 = t20.sidIdMap[r7], i13 = null !== (n14 = t20.sidSerializeSettings.get(r7)) && void 0 !== n14 ? n14 : { ignore: 0, write: Tt };
    i13.ignore || (o8[r7] = (0, i13.write)(s36 && s36 in t20.reg ? t20.reg[s36].current : e14));
  }), "onlyChanges" in n13 && (ce(0, "onlyChanges"), n13.onlyChanges || (r2(t20.cloneOf, "scope should be created from domain"), N(E(t20.cloneOf), (e14, r7) => {
    r7 in o8 || oe(a18, r7) || G(e14, "isCombine") || "ignore" === G(e14, "serialize") || (o8[r7] = t20.getState(e14));
  }))), o8;
}
function D({ unit: e14, fn: t20, scope: r7, batch: n13 }) {
  const o8 = [Te.run({ fn: (e15) => t20(e15) })];
  n13 && o8.unshift(Te.compute({ priority: "sampler", batch: 1 })), Y(e14) && o8.unshift(Te.mov({ store: e14.stateRef, to: "stack" }));
  const s36 = Array.isArray(e14) ? e14 : [e14];
  if (r7) {
    const e15 = [], t21 = r7.additionalLinks;
    return s36.forEach((r8) => {
      const n14 = t21[r8.graphite.id] || [];
      t21[r8.graphite.id] = n14;
      const s37 = a({ node: P(o8, r8), meta: { watchOp: r8.kind } });
      n14.push(s37), e15.push(() => {
        const e16 = n14.indexOf(s37);
        -1 !== e16 && n14.splice(e16, 1), vt(s37);
      });
    }), u(() => {
      e15.forEach((e16) => e16());
    });
  }
  {
    const e15 = a({ node: o8, parent: s36, family: { owners: s36 } });
    return u(() => {
      vt(e15);
    });
  }
}
function P(e14, t20) {
  return Y(t20) ? [Te.mov({ store: t20.stateRef, to: "stack" }), ...e14] : e14;
}
var _, E, V, T, L, B, W, H, U, G, J, K, Q, X, Y, Z, ee, te, re, ne, ae, oe, se, ie, ce, le, de, ue, fe, pe, me, ge, ye, be, ve, ke, we, Se, $e, Me, xe, ze, Ae, je, Ce, Ne, Ie, Oe, Re, Fe, qe, De, Pe, _e, Ee, Ve, Te, Le, Be, We, He, Ue, Ge, Je, Ke, Qe, Xe, Ye, Ze, et, tt, rt, nt, at, ot, st, ct, lt, dt, ft, pt, mt, ht, gt, yt, bt, vt, kt, wt, $t, Mt, xt, zt, At, jt, Ct, Nt, It, Ot, Rt, Ft, qt, Dt, Pt, _t, Vt, Tt;
var init_effector = __esm({
  "node_modules/effector/effector.mjs"() {
    _ = "undefined" != typeof Symbol && Symbol.observable || "@@observable";
    E = (e14) => e14.graphite || e14;
    V = (e14) => e14.family.owners;
    T = (e14) => e14.family.links;
    L = (e14) => e14.stateRef;
    B = (e14) => e14.value;
    W = (e14) => e14.subscribers;
    H = (e14) => e14.parent;
    U = (e14) => e14.scope;
    G = (e14, t20) => E(e14).meta[t20];
    J = (e14, t20, r7) => E(e14).meta[t20] = r7;
    K = (e14) => e14.compositeName;
    Q = (e14) => (Me(e14) || $e(e14)) && "kind" in e14;
    X = (e14) => (t20) => Q(t20) && t20.kind === e14;
    Y = X("store");
    Z = X("event");
    ee = X("effect");
    te = (e14) => Q(e14) && !!e14.targetable;
    re = X("domain");
    ne = X("scope");
    ae = { __proto__: null, unit: Q, store: Y, event: Z, effect: ee, targetable: te, domain: re, scope: ne, attached: (e14) => ee(e14) && 1 == G(e14, "attached") };
    oe = (e14, t20) => e14.includes(t20);
    se = (e14, t20) => {
      const r7 = e14.indexOf(t20);
      -1 !== r7 && e14.splice(r7, 1);
    };
    ie = (e14, t20) => e14.push(t20);
    ce = (e14, t20, r7, n13) => !e14 && console.error(`${n13 ? n13 + ": " : ""}${t20} is deprecated${r7 ? `, use ${r7} instead` : ""}`);
    le = () => {
      let e14 = 0;
      return () => "" + ++e14;
    };
    de = le();
    ue = le();
    fe = le();
    pe = le();
    ge = null;
    ye = (e14) => {
      me && me(e14, ge);
    };
    be = () => ge && ge.template;
    ve = (e14) => (e14 && ge && ge.sidRoot && (e14 = `${ge.sidRoot}|${e14}`), e14);
    ke = ({ sid: e14, name: t20, loc: r7, method: o8, fn: s36 }) => n(a({ meta: { sidRoot: ve(e14), sid: e14, name: t20, loc: r7, method: o8, type: "factory" } }), s36);
    we = (e14, r7) => {
      const n13 = E(e14);
      t(r7, (e15) => {
        const t20 = E(e15);
        "domain" !== n13.family.type && (t20.family.type = "crosslink"), ie(V(t20), n13), ie(T(n13), t20);
      });
    };
    Se = (e14 = []) => (Array.isArray(e14) ? e14 : [e14]).flat().map(E);
    $e = (e14) => "object" == typeof e14 && null !== e14;
    Me = (e14) => "function" == typeof e14;
    xe = (e14) => void 0 === e14;
    ze = (e14) => r2($e(e14) || Me(e14), "expect first argument be an object");
    Ae = (e14, t20, n13, a18) => r2(!(!$e(e14) && !Me(e14) || !("family" in e14) && !("graphite" in e14)), `${t20}: expect ${n13} to be a unit (store, event or effect)${a18}`);
    je = (e14, r7, n13) => {
      Array.isArray(e14) ? t(e14, (e15, t20) => Ae(e15, r7, `${t20} item of ${n13}`, "")) : Ae(e14, r7, n13, " or array of units");
    };
    Ce = (e14, n13, a18 = "target") => t(Se(n13), (t20) => r2(!G(t20, "derived"), `${e14}: derived unit in "${a18}" is not supported, use createStore/createEvent instead"`));
    Ne = (e14, { fn: t20 }, { a: r7 }) => t20(e14, r7);
    Ie = (e14, { fn: t20 }, { a: r7 }) => t20(r7, e14);
    Oe = (e14, { fn: t20 }) => t20(e14);
    Re = (e14, t20, r7, n13) => {
      const a18 = { id: ue(), type: e14, data: t20 };
      return r7 && (a18.order = { priority: r7 }, n13 && (a18.order.barrierID = ++Fe)), a18;
    };
    Fe = 0;
    qe = ({ from: e14 = "store", store: t20, target: r7, to: n13 = r7 ? "store" : "stack", batch: a18, priority: o8 }) => Re("mov", { from: e14, store: t20, to: n13, target: r7 }, o8, a18);
    De = ({ fn: e14, batch: t20, priority: r7, safe: n13 = 0, filter: a18 = 0, pure: o8 = 0 }) => Re("compute", { fn: e14, safe: n13, filter: a18, pure: o8 }, r7, t20);
    Pe = ({ fn: e14 }) => De({ fn: e14, priority: "effect" });
    _e = (e14, t20, r7) => De({ fn: e14, safe: 1, filter: t20, priority: r7 && "effect" });
    Ee = (e14, t20, r7) => qe({ store: e14, to: t20 ? "stack" : "a", priority: r7 && "sampler", batch: 1 });
    Ve = (e14 = Oe, t20) => De({ fn: e14, pure: 1, filter: t20 });
    Te = { mov: qe, compute: De, filter: ({ fn: e14, pure: t20 }) => De({ fn: e14, filter: 1, pure: t20 }), run: Pe };
    Le = (e14) => ({ id: ue(), current: e14, initial: e14 });
    Be = ({ current: e14 }) => e14;
    We = (e14, t20) => {
      e14.before || (e14.before = []), ie(e14.before, t20);
    };
    He = null;
    Ue = (e14, t20) => {
      if (!e14) return t20;
      if (!t20) return e14;
      let r7;
      return (e14.v.type === t20.v.type && e14.v.id > t20.v.id || Ye(e14.v.type) > Ye(t20.v.type)) && (r7 = e14, e14 = t20, t20 = r7), r7 = Ue(e14.r, t20), e14.r = e14.l, e14.l = r7, e14;
    };
    Ge = [];
    Je = 0;
    for (; Je < 6; ) ie(Ge, { first: null, last: null, size: 0 }), Je += 1;
    Ke = () => {
      for (let e14 = 0; e14 < 6; e14++) {
        const t20 = Ge[e14];
        if (t20.size > 0) {
          if (3 === e14 || 4 === e14) {
            t20.size -= 1;
            const e15 = He.v;
            return He = Ue(He.l, He.r), e15;
          }
          1 === t20.size && (t20.last = null);
          const r7 = t20.first;
          return t20.first = r7.r, t20.size -= 1, r7.v;
        }
      }
    };
    Qe = (e14, t20, r7, n13, a18, o8, s36) => Xe(0, { a: null, b: null, node: r7, parent: n13, value: a18, page: t20, scope: o8, meta: s36 }, e14, 0);
    Xe = (e14, t20, r7, n13) => {
      const a18 = Ye(r7), o8 = Ge[a18], s36 = { v: { idx: e14, stack: t20, type: r7, id: n13 }, l: null, r: null };
      3 === a18 || 4 === a18 ? He = Ue(He, s36) : (0 === o8.size ? o8.first = s36 : o8.last.r = s36, o8.last = s36), o8.size += 1;
    };
    Ye = (e14) => {
      switch (e14) {
        case "child":
          return 0;
        case "pure":
          return 1;
        case "read":
          return 2;
        case "barrier":
          return 3;
        case "sampler":
          return 4;
        case "effect":
          return 5;
        default:
          return -1;
      }
    };
    Ze = /* @__PURE__ */ new Set();
    tt = 1;
    rt = 0;
    nt = 0;
    at = null;
    ot = (e14) => {
      et = e14;
    };
    st = (e14) => {
      at = e14;
    };
    ct = (e14, t20) => {
      if (e14) {
        for (; e14 && !e14.reg[t20]; ) e14 = e14.parent;
        if (e14) return e14;
      }
      return null;
    };
    lt = (e14, t20, r7, n13) => {
      const a18 = ct(e14, r7.id);
      return a18 ? a18.reg[r7.id] : t20 ? (pt(t20, r7, n13), t20.reg[r7.id]) : r7;
    };
    ft = (e14) => e14;
    pt = (e14, r7, n13, a18, o8) => {
      const s36 = e14.reg;
      if (s36[r7.id]) return;
      const i13 = r7.sid, c15 = { id: r7.id, current: r7.initial, meta: r7.meta };
      if (c15.id in e14.values.idMap) c15.current = e14.values.idMap[c15.id];
      else if (i13 && i13 in e14.values.sidMap && !(i13 in e14.sidIdMap)) {
        var l12;
        const t20 = null == r7 || null === (l12 = r7.meta) || void 0 === l12 ? void 0 : l12.serialize;
        c15.current = (e14.fromSerialize && "ignore" !== t20 && (null == t20 ? void 0 : t20.read) || ft)(e14.values.sidMap[i13]);
      } else if (r7.before && !o8) {
        let o9 = 0;
        const i14 = n13 || !r7.noInit || a18;
        t(r7.before, (t20) => {
          switch (t20.type) {
            case "map": {
              const r8 = t20.from;
              if ((r8 || t20.fn) && (r8 && pt(e14, r8, n13, a18), i14)) {
                const e15 = r8 && s36[r8.id].current;
                c15.current = t20.fn ? t20.fn(e15) : e15;
              }
              break;
            }
            case "field":
              pt(e14, t20.from, n13, a18), o9 || (o9 = 1, c15.current = Array.isArray(c15.current) ? [...c15.current] : { ...c15.current }), i14 && (c15.current[t20.field] = s36[s36[t20.from.id].id].current);
          }
        });
      }
      i13 && (e14.sidIdMap[i13] = r7.id), s36[r7.id] = c15;
    };
    mt = (e14, t20, r7) => {
      try {
        return t20(B(r7), e14.scope, r7);
      } catch (t21) {
        console.error(t21), e14.fail = 1, e14.failReason = t21;
      }
    };
    ht = (t20, r7 = {}) => ($e(t20) && (ht(t20.or, r7), e(t20, (e14, t21) => {
      xe(e14) || "or" === t21 || "and" === t21 || (r7[t21] = e14);
    }), ht(t20.and, r7)), r7);
    gt = (e14, t20) => {
      se(e14.next, t20), se(V(e14), t20), se(T(e14), t20);
    };
    yt = (e14, t20, r7) => {
      let n13;
      e14.next.length = 0, e14.seq.length = 0, e14.scope = null;
      let a18 = T(e14);
      for (; n13 = a18.pop(); ) gt(n13, e14), (t20 || r7 && "sample" !== e14.meta.op || "crosslink" === n13.family.type) && yt(n13, t20, "on" !== n13.meta.op && r7);
      for (a18 = V(e14); n13 = a18.pop(); ) gt(n13, e14), r7 && "crosslink" === n13.family.type && yt(n13, t20, "on" !== n13.meta.op && r7);
    };
    bt = (e14) => e14.clear();
    vt = (e14, { deep: t20 } = {}) => {
      let r7 = 0;
      if (e14.ownerSet && e14.ownerSet.delete(e14), Y(e14)) bt(W(e14));
      else if (re(e14)) {
        r7 = 1;
        const t21 = e14.history;
        bt(t21.events), bt(t21.effects), bt(t21.stores), bt(t21.domains);
      }
      yt(E(e14), !!t20, r7);
    };
    kt = (e14) => u(() => vt(e14));
    wt = (e14, t20, r7, n13, o8) => a({ node: r7, parent: e14, child: t20, scope: { fn: o8 }, meta: { op: n13 }, family: { owners: [e14, t20], links: t20 }, regional: 1 });
    $t = (e14, t20) => (r2(Me(t20), ".watch argument should be a function"), kt(a({ scope: { fn: t20 }, node: [Pe({ fn: Oe })], parent: e14, meta: { op: "watch" }, family: { owners: e14 }, regional: 1 })));
    Mt = (e14, t20, r7 = "event") => {
      H(e14) && H(e14).hooks[r7](t20);
    };
    xt = (e14, t20, r7) => {
      const n13 = ht(r7), a18 = "domain" === e14, o8 = de(), { sid: s36 = null, named: i13 = null, domain: l12 = null, parent: d12 = l12 } = n13, u4 = i13 || n13.name || (a18 ? "" : o8), f15 = c(u4, d12), p11 = { op: t20.kind = e14, name: t20.shortName = u4, sid: t20.sid = ve(s36), named: i13, unitId: t20.id = o8, serialize: n13.serialize, derived: n13.derived, config: n13 };
      if (t20.targetable = !n13.derived, t20.parent = d12, t20.compositeName = f15, t20.defaultConfig = n13, t20.getType = () => (ce(0, "getType", "compositeName.fullName"), f15.fullName), !a18) {
        t20.subscribe = (e16) => (ze(e16), t20.watch(Me(e16) ? e16 : (t21) => e16.next && e16.next(t21))), t20[_] = () => t20;
        const e15 = be();
        e15 && (p11.nativeTemplate = e15);
      }
      return p11;
    };
    zt = (e14, t20, r7, n13) => {
      let a18;
      $e(r7) && (a18 = r7, r7 = r7.fn);
      const o8 = p({ name: `${e14.shortName} \u2192 *`, derived: 1, and: a18 });
      return wt(e14, o8, n13, t20, r7), o8;
    };
    At = "undefined is used to skip updates. To allow undefined as a value provide explicit { skipVoid: false } option";
    jt = (e14, t20, r7, n13, a18) => {
      const o8 = L(t20), s36 = qe({ store: o8, to: "a", priority: "read" });
      "map" === r7 && (s36.data.softRead = 1);
      const i13 = [s36, Ve(n13)];
      return f("storeOnMap", o8, i13, Y(e14) && L(e14)), wt(e14, t20, i13, r7, a18);
    };
    Ct = (t20, n13, a18, o8, i13, c15) => {
      const d12 = l("combine", o8), u4 = t20 ? (e14) => [...e14] : (e14) => ({ ...e14 }), p11 = t20 ? [] : {}, m12 = u4(p11), g7 = Le(m12), y4 = Le(1);
      g7.type = t20 ? "list" : "shape", g7.noInit = 1, f("combineBase", g7, y4);
      const b4 = h(m12, { name: s(a18), derived: 1, ...c15, and: o8 }), v5 = L(b4);
      v5.noInit = 1, J(b4, "isCombine", 1);
      const k5 = Ee(g7);
      k5.order = { priority: "barrier" };
      const w11 = qe({ store: v5, to: "b", priority: "read" });
      w11.data.softRead = 1;
      const S2 = [_e((e14, t21, r7) => (r7.scope && !r7.scope.reg[g7.id] && (r7.c = 1), e14)), k5, qe({ store: y4, to: "b" }), _e((e14, { key: t21 }, r7) => {
        if (r7.c || e14 !== r7.a[t21]) return n13 && r7.b && (r7.a = u4(r7.a)), r7.a[t21] = e14, 1;
      }, 1), qe({ from: "a", target: g7 }), qe({ from: "value", store: 0, target: y4 }), qe({ from: "value", store: 1, target: y4, priority: "barrier", batch: 1 }), Ee(g7, 1, 1), i13 && Ve(), w11];
      if (e(a18, (e14, t21) => {
        if (!Y(e14)) return r2(!Q(e14) && !xe(e14), `combine expects a store in a field ${t21}`, d12), void (m12[t21] = p11[t21] = e14);
        p11[t21] = e14.defaultState, m12[t21] = e14.getState();
        const n14 = wt(e14, b4, S2, "combine", i13);
        n14.scope.key = t21;
        const a19 = L(e14);
        We(g7, { type: "field", field: t21, from: a19 }), f("combineField", a19, n14);
      }), b4.defaultShape = a18, We(v5, { type: "map", from: g7, fn: i13 }), !be()) if (i13) {
        const e14 = i13(m12);
        !xe(e14) || c15 && "skipVoid" in c15 || console.error(`${d12}: ${At}`), v5.current = e14, v5.initial = e14, b4.defaultState = e14;
      } else b4.defaultState = p11;
      return b4;
    };
    Nt = (e14, t20, r7) => {
      try {
        return [1, e14(...r7)];
      } catch (e15) {
        return t20(e15), [0, null];
      }
    };
    It = (e14, t20, r7, n13, a18) => (s36) => {
      o({ target: [n13, Ot], params: [r7 ? { status: "done", params: e14, result: s36 } : { status: "fail", params: e14, error: s36 }, { value: s36, fn: r7 ? t20.rs : t20.rj }], defer: 1, page: a18.page, scope: a18.scope, meta: a18.meta });
    };
    Ot = a({ node: [Pe({ fn: ({ fn: e14, value: t20 }) => e14(t20) })], meta: { op: "fx", fx: "sidechain" } });
    Rt = ["source", "clock", "target"];
    Ft = (e14, t20) => e14 + `: ${t20} should be defined`;
    qt = (e14, t20, n13, a18, o8, s36, i13, c15, d12, u4, m12, y4) => {
      const b4 = l(e14, c15), v5 = !!o8;
      r2(!xe(n13) || !xe(t20), Ft(b4, "either source or clock"));
      let k5 = 0;
      xe(n13) ? k5 = 1 : Q(n13) || (n13 = g(n13)), xe(t20) ? t20 = n13 : (je(t20, b4, "clock"), Array.isArray(t20) && (t20 = $(t20))), k5 && (n13 = t20), c15 || i13 || (i13 = n13.shortName);
      let w11 = "none";
      (m12 || a18) && (Q(a18) ? w11 = "unit" : (r2(Me(a18), "`filter` should be function or unit"), w11 = "fn")), o8 ? (je(o8, b4, "target"), Ce(b4, o8)) : "none" === w11 && u4 && Y(n13) && Y(t20) ? o8 = h(s36 ? s36(Be(L(n13)), Be(L(t20))) : Be(L(n13)), { name: i13, sid: y4, or: c15 }) : (o8 = p({ name: i13, derived: 1, or: c15 }), f("sampleTarget", E(o8)));
      const S2 = Le();
      let M6 = [];
      if ("unit" === w11) {
        const [r7, n14, s37] = Pt(a18, o8, t20, S2, e14);
        s37 || M6.push(...Dt(n14)), M6.push(...Dt(r7));
      }
      const x3 = [];
      if (k5) d12 && x3.push(Ee(S2, 1, 1));
      else {
        const [r7, a19, s37] = Pt(n13, o8, t20, S2, e14);
        s37 || x3.push(...Dt(a19)), x3.push(Ee(r7, 1, d12));
      }
      const z2 = wt(t20, o8, [f("sampleSourceLoader"), qe({ from: "stack", target: S2 }), ...x3, ...M6, Ee(S2), "fn" === w11 && Ve((e15, t21, { a: r7 }) => a18(e15, r7), 1), s36 && Ve(Ne), f("sampleSourceUpward", v5)], e14, s36);
      return we(n13, [z2]), Object.assign(z2.meta, c15, { joint: 1 }), o8;
    };
    Dt = (e14) => [Ee(e14), _e((e15, t20, { a: r7 }) => r7, 1)];
    Pt = (e14, t20, r7, n13, o8) => {
      const s36 = Y(e14), i13 = s36 ? L(e14) : Le(), c15 = Le(s36);
      return s36 || a({ parent: e14, node: [qe({ from: "stack", target: i13 }), qe({ from: "value", store: 1, target: c15 })], family: { owners: [e14, t20, r7], links: t20 }, meta: { op: o8 }, regional: 1 }), f("sampleSource", c15, i13, n13), [i13, c15, s36];
    };
    _t = (e14, t20, r7, n13) => {
      const a18 = e14[t20];
      a18 && o({ target: a18, params: Array.isArray(a18) ? a18.map(() => r7) : r7, defer: 1, stack: n13 });
    };
    Vt = (e14) => !e14.node.meta.isCombine || e14.parent && "combine" !== e14.parent.node.meta.op;
    Tt = (e14) => e14;
  }
});

// node_modules/use-sync-external-store/cjs/use-sync-external-store-shim.production.js
var require_use_sync_external_store_shim_production = __commonJS({
  "node_modules/use-sync-external-store/cjs/use-sync-external-store-shim.production.js"(exports) {
    "use strict";
    var React10 = require_react();
    function is(x3, y4) {
      return x3 === y4 && (0 !== x3 || 1 / x3 === 1 / y4) || x3 !== x3 && y4 !== y4;
    }
    var objectIs = "function" === typeof Object.is ? Object.is : is;
    var useState32 = React10.useState;
    var useEffect35 = React10.useEffect;
    var useLayoutEffect7 = React10.useLayoutEffect;
    var useDebugValue = React10.useDebugValue;
    function useSyncExternalStore$2(subscribe, getSnapshot) {
      var value = getSnapshot(), _useState = useState32({ inst: { value, getSnapshot } }), inst = _useState[0].inst, forceUpdate = _useState[1];
      useLayoutEffect7(
        function() {
          inst.value = value;
          inst.getSnapshot = getSnapshot;
          checkIfSnapshotChanged(inst) && forceUpdate({ inst });
        },
        [subscribe, value, getSnapshot]
      );
      useEffect35(
        function() {
          checkIfSnapshotChanged(inst) && forceUpdate({ inst });
          return subscribe(function() {
            checkIfSnapshotChanged(inst) && forceUpdate({ inst });
          });
        },
        [subscribe]
      );
      useDebugValue(value);
      return value;
    }
    function checkIfSnapshotChanged(inst) {
      var latestGetSnapshot = inst.getSnapshot;
      inst = inst.value;
      try {
        var nextValue = latestGetSnapshot();
        return !objectIs(inst, nextValue);
      } catch (error) {
        return true;
      }
    }
    function useSyncExternalStore$1(subscribe, getSnapshot) {
      return getSnapshot();
    }
    var shim = "undefined" === typeof window || "undefined" === typeof window.document || "undefined" === typeof window.document.createElement ? useSyncExternalStore$1 : useSyncExternalStore$2;
    exports.useSyncExternalStore = void 0 !== React10.useSyncExternalStore ? React10.useSyncExternalStore : shim;
  }
});

// node_modules/use-sync-external-store/shim/index.js
var require_shim = __commonJS({
  "node_modules/use-sync-external-store/shim/index.js"(exports, module) {
    "use strict";
    if (true) {
      module.exports = require_use_sync_external_store_shim_production();
    } else {
      module.exports = null;
    }
  }
});

// node_modules/use-sync-external-store/cjs/use-sync-external-store-shim/with-selector.production.js
var require_with_selector_production = __commonJS({
  "node_modules/use-sync-external-store/cjs/use-sync-external-store-shim/with-selector.production.js"(exports) {
    "use strict";
    var React10 = require_react();
    var shim = require_shim();
    function is(x3, y4) {
      return x3 === y4 && (0 !== x3 || 1 / x3 === 1 / y4) || x3 !== x3 && y4 !== y4;
    }
    var objectIs = "function" === typeof Object.is ? Object.is : is;
    var useSyncExternalStore2 = shim.useSyncExternalStore;
    var useRef30 = React10.useRef;
    var useEffect35 = React10.useEffect;
    var useMemo11 = React10.useMemo;
    var useDebugValue = React10.useDebugValue;
    exports.useSyncExternalStoreWithSelector = function(subscribe, getSnapshot, getServerSnapshot, selector, isEqual2) {
      var instRef = useRef30(null);
      if (null === instRef.current) {
        var inst = { hasValue: false, value: null };
        instRef.current = inst;
      } else inst = instRef.current;
      instRef = useMemo11(
        function() {
          function memoizedSelector(nextSnapshot) {
            if (!hasMemo) {
              hasMemo = true;
              memoizedSnapshot = nextSnapshot;
              nextSnapshot = selector(nextSnapshot);
              if (void 0 !== isEqual2 && inst.hasValue) {
                var currentSelection = inst.value;
                if (isEqual2(currentSelection, nextSnapshot))
                  return memoizedSelection = currentSelection;
              }
              return memoizedSelection = nextSnapshot;
            }
            currentSelection = memoizedSelection;
            if (objectIs(memoizedSnapshot, nextSnapshot)) return currentSelection;
            var nextSelection = selector(nextSnapshot);
            if (void 0 !== isEqual2 && isEqual2(currentSelection, nextSelection))
              return memoizedSnapshot = nextSnapshot, currentSelection;
            memoizedSnapshot = nextSnapshot;
            return memoizedSelection = nextSelection;
          }
          var hasMemo = false, memoizedSnapshot, memoizedSelection, maybeGetServerSnapshot = void 0 === getServerSnapshot ? null : getServerSnapshot;
          return [
            function() {
              return memoizedSelector(getSnapshot());
            },
            null === maybeGetServerSnapshot ? void 0 : function() {
              return memoizedSelector(maybeGetServerSnapshot());
            }
          ];
        },
        [getSnapshot, getServerSnapshot, selector, isEqual2]
      );
      var value = useSyncExternalStore2(subscribe, instRef[0], instRef[1]);
      useEffect35(
        function() {
          inst.hasValue = true;
          inst.value = value;
        },
        [value]
      );
      useDebugValue(value);
      return value;
    };
  }
});

// node_modules/use-sync-external-store/shim/with-selector.js
var require_with_selector = __commonJS({
  "node_modules/use-sync-external-store/shim/with-selector.js"(exports, module) {
    "use strict";
    if (true) {
      module.exports = require_with_selector_production();
    } else {
      module.exports = null;
    }
  }
});

// node_modules/effector-react/effector-react.mjs
function e2(e14, t20) {
  return t20.displayName = e14, t20;
}
function t2(e14, t20) {
  const n13 = ae.unit(e14);
  let r7 = {};
  n13 ? r7 = { unit: e14 } : "@@unitShape" in e14 ? "function" == typeof e14["@@unitShape"] ? r7 = e14["@@unitShape"]() : E2("expect @@unitShape to be a function") : r7 = e14;
  const o8 = Array.isArray(r7), s36 = import_react194.default.useRef({ stale: 1, justSubscribed: 0, scope: t20 }), [u4, c15, a18, i13, l12] = import_react194.default.useMemo(() => {
    s36.current.stale = 1;
    const e15 = Array.isArray(r7) ? [] : {}, o9 = [], u5 = [], c16 = [], a19 = [];
    for (const s37 in r7) {
      if (!{}.hasOwnProperty.call(r7, s37)) continue;
      const i14 = r7[s37];
      ae.unit(i14) || E2(`expect useUnit ${n13 ? "argument" : `value in key "${s37}"`} to be a unit`), ae.event(i14) || ae.effect(i14) ? (e15[s37] = t20 ? F(i14, { scope: t20 }) : i14, c16.push(s37), a19.push(i14)) : (e15[s37] = null, o9.push(s37), u5.push(i14));
    }
    return [e15, o9, u5, c16, a19];
  }, [s36, t20, ...Object.keys(r7), ...Object.values(r7)]), f15 = import_react194.default.useRef({ value: u4, storeKeys: c15, eventKeys: i13, eventValues: l12 }), p11 = import_react194.default.useCallback((e15) => {
    const n14 = s36.current;
    return n14.justSubscribed = 1, D({ unit: a18, fn: () => {
      n14.stale || (n14.stale = 1, e15());
    }, scope: t20, batch: 1 });
  }, [a18, t20, f15, s36]), d12 = import_react194.default.useCallback(() => {
    const e15 = f15.current, r8 = s36.current;
    let p12, d13 = 0;
    const y4 = e15.value, m12 = e15.storeKeys, v5 = e15.eventKeys, b4 = e15.eventValues, h6 = t20 !== r8.scope;
    if (r8.stale || r8.justSubscribed || h6) {
      d13 = !r8.justSubscribed || h6, p12 = o8 ? [...u4] : { ...u4 }, m12.length === c15.length && v5.length === i13.length || (d13 = 1);
      for (let e16 = 0; e16 < c15.length; e16++) {
        const n14 = U2(a18[e16], t20), r9 = c15[e16];
        d13 || (d13 = m12.includes(r9) ? y4[r9] !== n14 : 1), p12[r9] = n14;
      }
      for (let e16 = 0; e16 < i13.length; e16++) {
        const t21 = l12[e16], n14 = i13[e16];
        d13 || (d13 = v5.includes(n14) ? b4[v5.indexOf(n14)] !== t21 : 1);
      }
    }
    return d13 && (e15.value = p12), e15.storeKeys = c15, e15.eventKeys = i13, e15.eventValues = l12, r8.stale = 0, r8.justSubscribed = !d13, r8.scope = t20, n13 ? e15.value.unit : e15.value;
  }, [p11, a18, l12, t20, f15, s36]);
  return O2(p11, d12, d12);
}
function n2([e14, t20], n13) {
  let r7, o8, s36, u4, c15 = K2;
  t20 ? (r7 = t20, s36 = e14, u4 = []) : { fn: r7, store: s36, keys: u4, defaultValue: o8, updateFilter: c15 = K2 } = e14, ae.store(s36) || E2("useStoreMap expects a store"), Array.isArray(u4) || E2("useStoreMap expects an array as keys"), "function" != typeof r7 && E2("useStoreMap expects a function");
  const a18 = import_react194.default.useCallback((e15) => D({ unit: s36, fn: e15, scope: n13 }), [s36, n13]), i13 = import_react194.default.useCallback(() => U2(s36, n13), [s36, n13]), l12 = import_react194.default.useRef(), f15 = import_react194.default.useRef(), p11 = import_react194.default.useRef(u4);
  return R(a18, i13, i13, (e15) => {
    if (l12.current !== e15 || !((e16, t21) => {
      if (!e16 || !t21 || e16.length !== t21.length) return 0;
      let n14 = 1;
      for (let r8 = 0; r8 < e16.length; r8++) if (e16[r8] !== t21[r8]) {
        n14 = 0;
        break;
      }
      return n14;
    })(p11.current, u4)) {
      let t21 = r7(e15, u4);
      void 0 === t21 && void 0 !== o8 && (t21 = o8), l12.current = e15, p11.current = u4, void 0 !== t21 && (f15.current = t21);
    }
    return f15.current;
  }, (e15, t21) => !c15(t21, e15));
}
function o2(e14) {
  const t20 = import_react194.default.useContext(V2);
  return e14 && !t20 && E2("No scope found, consider adding <Provider> to app root"), t20;
}
function c2(e14, n13) {
  return t2(e14, o2(null == n13 ? void 0 : n13.forceScope));
}
function a2(e14, t20) {
  return n2([e14, t20], o2(null == e14 ? void 0 : e14.forceScope));
}
function i(r7, s36, u4) {
  return ((r8, o8, s37) => {
    let u5, c15, a18, i13 = [];
    "object" == typeof o8 && null !== o8 ? (o8.keys && (i13 = o8.keys), { fn: u5, getKey: c15, placeholder: a18 } = o8) : u5 = o8, ae.store(r8) || E2("expect useList first argument to be a store"), "function" != typeof u5 && E2("expect useList's renderItem to be a function"), Array.isArray(i13) || E2("expect useList's keys to be an array");
    const l12 = import_react194.default.useMemo(() => {
      const t20 = e2(`${r8.shortName || "Unknown"}.Item`, (e14) => {
        const { index: t21, keys: o9, keyVal: u6, value: c16 } = e14;
        if (f15.current[1]) return f15.current[0](c16, u6);
        const a19 = n2([{ store: r8, keys: [t21, ...o9], fn: (e15, t22) => e15[t22[0]] }], s37);
        return f15.current[0](a19, t21);
      });
      return import_react194.default.memo(t20);
    }, [r8, s37, !!c15]), f15 = import_react194.default.useRef([u5, c15]);
    f15.current = [u5, c15];
    const p11 = import_react194.default.useMemo(() => i13, i13);
    if (c15) {
      const e14 = t2(r8, s37);
      return 0 === e14.length && a18 ? a18 : e14.map((e15) => {
        const t20 = f15.current[1](e15);
        return import_react194.default.createElement(l12, { keyVal: t20, key: t20, keys: p11, value: e15 });
      });
    }
    {
      const e14 = n2([{ store: r8, keys: [r8], fn: (e15) => e15.length }], s37);
      return 0 === e14 && a18 ? a18 : Array.from({ length: e14 }, (e15, t20) => import_react194.default.createElement(l12, { index: t20, key: t20, keys: p11 }));
    }
  })(r7, s36, o2(null == u4 ? void 0 : u4.forceScope));
}
function f2() {
  return o2() || null;
}
var import_react194, import_with_selector, import_shim, E2, M2, O2, R, U2, K2, V2, L2;
var init_effector_react = __esm({
  "node_modules/effector-react/effector-react.mjs"() {
    import_react194 = __toESM(require_react(), 1);
    init_effector();
    import_with_selector = __toESM(require_with_selector(), 1);
    import_shim = __toESM(require_shim(), 1);
    E2 = (e14) => {
      throw Error(e14);
    };
    M2 = "undefined" != typeof window ? import_react194.default.useLayoutEffect : import_react194.default.useEffect;
    ({ useSyncExternalStore: O2 } = import_shim.default);
    ({ useSyncExternalStoreWithSelector: R } = import_with_selector.default);
    U2 = (e14, t20) => t20 ? t20.getState(e14) : e14.getState();
    K2 = (e14, t20) => e14 !== t20;
    V2 = import_react194.default.createContext(null);
    ({ Provider: L2 } = V2);
  }
});

// dist/server/chunks/chunk-D3GmwNoI.js
var header, inner, logo, link, s2;
var init_chunk_D3GmwNoI = __esm({
  "dist/server/chunks/chunk-D3GmwNoI.js"() {
    "use strict";
    header = "_header_3t3eu_1";
    inner = "_inner_3t3eu_6";
    logo = "_logo_3t3eu_18";
    link = "_link_3t3eu_25";
    s2 = {
      header,
      inner,
      logo,
      link
    };
  }
});

// dist/server/chunks/chunk-Y-L1G_uK.js
function MantineProvider2({
  children
}) {
  return /* @__PURE__ */ (0, import_jsx_runtime160.jsxs)(MantineProvider, { theme, children: [
    /* @__PURE__ */ (0, import_jsx_runtime160.jsx)(Notifications, {}),
    children
  ] });
}
function getServerScope(values2) {
  return O({
    values: values2
  });
}
function HACK_injectValues(scope, values2) {
  Object.assign(scope.values.sidMap, values2);
}
function HACK_runScopeWatchers(scope, linksToRun) {
  if (linksToRun.length) {
    linksToRun.forEach((nodeId) => {
      const links = scope.additionalLinks[nodeId];
      if (links) {
        links.forEach((link2) => {
          if (link2.meta.watchOp === "store") {
            o({
              target: link2,
              params: null,
              scope
            });
          }
        });
      }
    });
  }
}
function HACK_updateScopeRefs(tscope, values2) {
  var _a, _b, _c, _d, _e3;
  const scope = tscope;
  const linksToRun = [];
  for (const id3 in scope.reg) {
    if (Object.hasOwnProperty.call(scope.reg, id3)) {
      const ref = scope.reg[id3];
      const nodeId = (_a = ref == null ? void 0 : ref.meta) == null ? void 0 : _a.id;
      if (nodeId && scope.additionalLinks[nodeId]) {
        linksToRun.push(nodeId);
      }
      if (!ref.meta || !((_b = ref.meta) == null ? void 0 : _b.named) && ((_c = ref.meta) == null ? void 0 : _c.derived)) {
        delete scope.reg[id3];
      } else {
        const sid = (_d = ref == null ? void 0 : ref.meta) == null ? void 0 : _d.sid;
        if (sid && sid in values2) {
          const serialize2 = (_e3 = ref == null ? void 0 : ref.meta) == null ? void 0 : _e3.serialize;
          const read = serialize2 && serialize2 !== "ignore" ? serialize2 == null ? void 0 : serialize2.read : null;
          ref.current = read ? read(values2[sid]) : values2[sid];
        }
      }
    }
  }
  queueMicrotask(() => {
    HACK_runScopeWatchers(scope, linksToRun);
  });
}
function INTERNAL_getClientScope(values2) {
  if (!values2 || values2 === prevValues) return currentScope;
  prevValues = values2;
  HACK_injectValues(currentScope, values2);
  HACK_updateScopeRefs(currentScope, values2);
  return currentScope;
}
function Wrapper({
  children
}) {
  return /* @__PURE__ */ (0, import_jsx_runtime160.jsxs)(MantineProvider2, { children: [
    /* @__PURE__ */ (0, import_jsx_runtime160.jsx)(NavigationProgress, {}),
    /* @__PURE__ */ (0, import_jsx_runtime160.jsx)(ScopeProvider, { children: /* @__PURE__ */ (0, import_jsx_runtime160.jsx)(EffectorProvider, { children }) })
  ] });
}
var import_jsx_runtime160, import_react195, theme, isClient, currentScope, prevValues, getScope, ScopeContext, ScopeUpdateContext, ScopeProvider, useScope, EffectorProvider, import3, onBeforeRender, import4, logoUrl$1, HeadDefault, logoUrl, APP_ROUTES, Header, RootLayout;
var init_chunk_Y_L1G_uK = __esm({
  "dist/server/chunks/chunk-Y-L1G_uK.js"() {
    "use strict";
    import_jsx_runtime160 = __toESM(require_jsx_runtime(), 1);
    init_esm4();
    init_esm2();
    init_esm5();
    import_react195 = __toESM(require_react(), 1);
    init_effector();
    init_effector_react();
    init_usePageContext();
    init_esm();
    init_chunk_D3GmwNoI();
    theme = createTheme({
      fontFamily: "Raleway, sans-serif",
      fontFamilyMonospace: "Monaco, Courier, monospace",
      headings: {
        fontFamily: "Raleway, Montserrat, system-ui, sans-serif"
      },
      spacing: {
        xxs: "4px",
        xs: "8px",
        sm: "12px",
        md: "16px",
        lg: "20px",
        xl: "24px",
        "2xl": "28px",
        "3xl": "32px",
        "4xl": "36px",
        "5xl": "40px"
      },
      radius: {
        xxs: "4px",
        xs: "8px",
        sm: "12px",
        md: "16px",
        lg: "20px",
        xl: "24px",
        "2xl": "28px",
        "3xl": "32px",
        "4xl": "36px",
        "5xl": "40px"
      },
      cursorType: "pointer",
      components: {
        Container: Container.extend({
          vars: () => ({
            root: {
              "--container-size": "1418px"
            }
          })
        })
      }
    });
    isClient = typeof document !== "undefined";
    currentScope = O();
    getScope = isClient ? INTERNAL_getClientScope : getServerScope;
    ScopeContext = (0, import_react195.createContext)(void 0);
    ScopeUpdateContext = (0, import_react195.createContext)(() => {
    });
    ScopeProvider = ({
      children
    }) => {
      const pageContext = usePageContext();
      const [scope, setScope] = (0, import_react195.useState)("scope" in pageContext ? pageContext.scope : getScope());
      const update = (0, import_react195.useCallback)((values2) => {
        setScope(getScope(values2));
      }, []);
      (0, import_react195.useEffect)(() => {
        if (!pageContext.isHydration) {
          setScope(getScope());
        }
      }, [pageContext]);
      return /* @__PURE__ */ (0, import_jsx_runtime160.jsx)(ScopeContext.Provider, { value: scope, children: /* @__PURE__ */ (0, import_jsx_runtime160.jsx)(ScopeUpdateContext.Provider, { value: update, children }) });
    };
    useScope = () => (0, import_react195.useContext)(ScopeContext);
    EffectorProvider = ({
      children
    }) => {
      const pageContext = usePageContext();
      const scope = useScope();
      (0, import_react195.useEffect)(() => {
        const firePageStarted = async () => {
          const {
            pageStarted: pageStarted2
          } = pageContext.config;
          if (pageStarted2) {
            await C(pageStarted2, {
              scope,
              params: pageContext
            });
          }
        };
        firePageStarted().catch(() => {
          throw new Error("Page start failed");
        });
      }, [pageContext]);
      return /* @__PURE__ */ (0, import_jsx_runtime160.jsx)(L2, { value: scope, children });
    };
    import3 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
      __proto__: null,
      default: Wrapper
    }, Symbol.toStringTag, { value: "Module" }));
    onBeforeRender = async (pageContext) => {
      const {
        pageInitiated: pageInitiated2
      } = pageContext.config;
      const scope = O();
      if (pageInitiated2) {
        await C(pageInitiated2, {
          scope,
          params: pageContext
        });
      }
      return {
        pageContext: {
          scope,
          scopeValues: q(scope)
        }
      };
    };
    import4 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
      __proto__: null,
      onBeforeRender
    }, Symbol.toStringTag, { value: "Module" }));
    logoUrl$1 = "/assets/static/logo.2_7Lo9tV.svg";
    HeadDefault = () => /* @__PURE__ */ (0, import_jsx_runtime160.jsxs)(import_jsx_runtime160.Fragment, { children: [
      /* @__PURE__ */ (0, import_jsx_runtime160.jsx)("meta", { name: "viewport", content: "width=device-width, initial-scale=1, maximum-scale=1" }),
      /* @__PURE__ */ (0, import_jsx_runtime160.jsx)("meta", { name: "description", content: "Cognitive Lab" }),
      /* @__PURE__ */ (0, import_jsx_runtime160.jsx)("link", { rel: "icon", href: logoUrl$1 }),
      /* @__PURE__ */ (0, import_jsx_runtime160.jsx)(ColorSchemeScript, {})
    ] });
    logoUrl = "data:image/svg+xml,%3csvg%20xmlns='http://www.w3.org/2000/svg'%20fill='none'%20viewBox='0%200%20125%2020'%3e%3cpath%20fill='%23101113'%20d='M37.986%2016.094c-.004-.205-.01-.41-.01-.616V3.835h.972c.435%200%20.87.011%201.304-.004.215-.008.348.066.48.238%201.491%201.933%202.99%203.858%204.487%205.786l.157.197V3.85h3.312c0%204.077%200%208.145-.04%2012.229-.89.016-1.74.015-2.59.015l-4.728-5.962-.059.027c0%201.96%200%203.92-.034%205.908-1.106.028-2.178.027-3.25.027ZM33.62%2016.095v-.965c-.057.012-.09.008-.11.024-1.4%201.123-2.975%201.266-4.617.766-2.333-.71-3.59-2.41-4.006-4.785-.273-1.565-.094-3.088.776-4.447C27.053%204.512%2029.076%203.6%2031.59%203.79c1.364.103%202.566.63%203.54%201.626.286.294.516.646.782.985l-2.493%201.94c-.358-.686-.895-1.13-1.573-1.405-1.274-.516-2.623-.021-3.263%201.209-.502.965-.575%201.986-.272%203.02.616%202.103%202.606%202.409%204.034%201.682.388-.198.724-.5%201.084-.754l-.044-.102h-1.994V9.58h4.949c0%202.157%200%204.304-.028%206.481-.917.032-1.805.033-2.693.034ZM22.864%206.354c2.46%203.696.767%208.482-3.392%209.64-3.445.958-7.016-1.376-7.585-4.958-.558-3.517%201.73-6.73%205.194-7.201%202.382-.324%204.32.538%205.783%202.519Zm-7.291%201.798c-.541%201.09-.563%202.215-.145%203.339.418%201.12%201.334%201.72%202.512%201.715%201.15-.005%202.03-.615%202.443-1.717.328-.88.389-1.781.122-2.687-.327-1.109-1.031-1.84-2.174-2.01-1.176-.175-2.124.238-2.758%201.36ZM91.208%208.505v2.703H86.86v1.947h5.24c0%20.994%200%201.956-.05%202.93-.645.01-1.24.01-1.836.01l-6.687-.009V3.854h8.422c0%20.96%200%201.913-.036%202.892-1.71.028-3.383.028-5.055.028v1.73h4.349ZM4.607%207.39c-1.293%201.26-1.333%203.68-.14%205.01%201.139%201.27%203.622%201.038%204.3-.683.892.541%201.78%201.08%202.741%201.665-.267.367-.486.752-.78%201.06-1.01%201.059-2.294%201.538-3.701%201.711C3.003%2016.65.172%2013.123.234%209.72c.053-2.854%202.23-5.428%205.01-5.852%201.79-.273%203.462-.01%204.925%201.157.482.384.872.85%201.162%201.423l-2.513%201.82c-.38-.893-1.068-1.353-1.98-1.476-.81-.11-1.573.01-2.231.598ZM75.433%2016.09%2070.967%203.866h3.492l2.321%207.89.057.015%202.297-7.92h3.48c-1.482%204.091-2.953%208.155-4.469%2012.232-.933.011-1.823.01-2.712.007ZM58.806%2016.101c-.016-.015-.032-.03-.05-.085l-.001-9.25h-3.47V3.855c.093-.008.17-.02.25-.02h9.815c.05%200%20.101.006.17.053.015.99.012%201.936.01%202.881%200%200%20.006.002-.022.002l-3.431.001c0%201.229-.002%202.42%200%203.612.002%201.882.006%203.764.01%205.646%200%200-.01.028-.04.046-1.1.02-2.17.022-3.241.025ZM115.487%2016.1c-.012-.08-.034-.158-.034-.237-.002-3.918-.002-7.836-.001-11.754%200-.08.009-.16.016-.259.113-.006.211-.014.309-.015%201.79%200%203.58.038%205.368-.011%201.641-.046%202.605%201.044%202.946%202.325.371%201.393-.228%202.865-1.446%203.573l-.14.084c.077.034.122.057.169.075%201.29.508%202.056%201.685%202.032%203.125-.021%201.268-.845%202.39-2.11%202.835-.321.114-.665.163-1.05.246-2.054.007-4.056.01-6.059.013Zm5.95-5.557h-4.436v4.18c1.485%200%202.958%200%204.476.005.079-.02.158-.035.235-.058.917-.278%201.511-1.191%201.432-2.2-.078-1.003-.756-1.789-1.707-1.927Zm-4.26-1.266c1.183%200%202.366-.006%203.549.002.594.004%201.095-.191%201.466-.666.561-.718.643-1.514.286-2.345-.346-.803-.984-1.113-1.828-1.093-.93.022-1.86.005-2.79.005h-.828c-.022.049-.038.067-.038.084-.003%201.276-.006%202.55-.001%203.826.001.062.084.124.184.187ZM105.701%2012.638c-.423%201.063-.852%202.123-1.263%203.191-.08.206-.176.28-.393.271-.434-.018-.868-.005-1.344-.005.168-.423.322-.82.481-1.213%201.447-3.586%202.896-7.17%204.335-10.76.085-.213.178-.311.411-.291.329.028.733-.087.969.07.225.149.279.568.401.87l4.417%2010.992c.041.102.073.207.114.325h-1.613c-.461-1.156-.923-2.314-1.406-3.516-1.664-.05-3.306-.056-4.948-.056-.054%200-.107.08-.161.122Zm2.465-6.723-2.114%205.356h4.39l-2.151-5.606c-.043.087-.073.149-.125.25ZM66.754%2016.092c-.003-1.686-.007-3.371-.008-5.057-.002-2.272%200-4.544%200-6.816v-.36h3.296c0%204.064%200%208.122-.036%2012.208-1.108.027-2.18.026-3.252.025ZM50.792%2016.098c-.008-.101-.024-.203-.024-.304V3.85h3.301c0%204.068%200%208.125-.03%2012.213-1.103.032-2.175.033-3.247.034ZM94.064%2016.09V3.853h1.511v10.853h6.721c0%20.472%200%20.92-.05%201.378-2.76.009-5.471.006-8.182.004Z'/%3e%3c/svg%3e";
    APP_ROUTES = [{
      link: "/",
      label: "\u0413\u043B\u0430\u0432\u043D\u0430\u044F"
    }, {
      link: "/test",
      label: "\u041F\u0440\u043E\u0439\u0442\u0438 \u0442\u0435\u0441\u0442"
    }];
    Header = () => {
      const [opened, {
        toggle,
        close
      }] = useDisclosure(false);
      const pageContext = usePageContext();
      const {
        urlPathname
      } = pageContext;
      const isActive = ({
        href
      }) => href === "/" ? urlPathname === href : urlPathname.startsWith(href);
      const items = APP_ROUTES.map((link2) => /* @__PURE__ */ (0, import_jsx_runtime160.jsx)("a", { href: link2.link, className: s2.link, onClick: close, ...isActive({
        href: link2.link
      }) ? {
        "data-active": true
      } : {}, children: link2.label }, link2.label));
      return /* @__PURE__ */ (0, import_jsx_runtime160.jsx)("header", { className: s2.header, children: /* @__PURE__ */ (0, import_jsx_runtime160.jsxs)("div", { className: s2.inner, children: [
        /* @__PURE__ */ (0, import_jsx_runtime160.jsx)("a", { className: s2.logo, href: "/", children: /* @__PURE__ */ (0, import_jsx_runtime160.jsx)(Image, { src: logoUrl, height: 20, width: 125, alt: "Cognitive Lab \u043B\u043E\u0433\u043E\u0442\u0438\u043F" }) }),
        /* @__PURE__ */ (0, import_jsx_runtime160.jsx)(Group, { gap: 5, visibleFrom: "sm", children: items }),
        /* @__PURE__ */ (0, import_jsx_runtime160.jsx)(Drawer, { hiddenFrom: "sm", opened, onClose: close, size: "75%", children: /* @__PURE__ */ (0, import_jsx_runtime160.jsx)(Stack, { gap: 5, children: items }) }),
        /* @__PURE__ */ (0, import_jsx_runtime160.jsx)(Burger, { opened, onClick: toggle, hiddenFrom: "sm", size: "sm" })
      ] }) });
    };
    RootLayout = ({
      children
    }) => /* @__PURE__ */ (0, import_jsx_runtime160.jsxs)(import_jsx_runtime160.Fragment, { children: [
      /* @__PURE__ */ (0, import_jsx_runtime160.jsx)(Header, {}),
      /* @__PURE__ */ (0, import_jsx_runtime160.jsx)(Divider, { visibleFrom: "md" }),
      /* @__PURE__ */ (0, import_jsx_runtime160.jsx)("main", { id: "page-content", children })
    ] });
  }
});

// node_modules/@phosphor-icons/react/dist/lib/SSRBase.mjs
var import_react196, R2, l2, n3, c3, i2, m2, w2, d2, E3;
var init_SSRBase = __esm({
  "node_modules/@phosphor-icons/react/dist/lib/SSRBase.mjs"() {
    import_react196 = __toESM(require_react(), 1);
    R2 = Object.defineProperty;
    l2 = Object.getOwnPropertySymbols;
    n3 = Object.prototype.hasOwnProperty;
    c3 = Object.prototype.propertyIsEnumerable;
    i2 = (e14, r7, t20) => r7 in e14 ? R2(e14, r7, { enumerable: true, configurable: true, writable: true, value: t20 }) : e14[r7] = t20;
    m2 = (e14, r7) => {
      for (var t20 in r7 || (r7 = {}))
        n3.call(r7, t20) && i2(e14, t20, r7[t20]);
      if (l2)
        for (var t20 of l2(r7))
          c3.call(r7, t20) && i2(e14, t20, r7[t20]);
      return e14;
    };
    w2 = (e14, r7) => {
      var t20 = {};
      for (var s36 in e14)
        n3.call(e14, s36) && r7.indexOf(s36) < 0 && (t20[s36] = e14[s36]);
      if (e14 != null && l2)
        for (var s36 of l2(e14))
          r7.indexOf(s36) < 0 && c3.call(e14, s36) && (t20[s36] = e14[s36]);
      return t20;
    };
    d2 = (0, import_react196.forwardRef)((e14, r7) => {
      const a18 = e14, {
        alt: t20,
        color: s36 = "currentColor",
        size: o8 = "1em",
        weight: f15 = "regular",
        mirrored: h6 = false,
        children: S2,
        weights: p11
      } = a18, u4 = w2(a18, [
        "alt",
        "color",
        "size",
        "weight",
        "mirrored",
        "children",
        "weights"
      ]);
      return /* @__PURE__ */ import_react196.default.createElement(
        "svg",
        m2({
          ref: r7,
          xmlns: "http://www.w3.org/2000/svg",
          width: o8,
          height: o8,
          fill: s36,
          viewBox: "0 0 256 256",
          transform: h6 ? "scale(-1, 1)" : void 0
        }, u4),
        !!t20 && /* @__PURE__ */ import_react196.default.createElement("title", null, t20),
        S2,
        p11.get(f15)
      );
    });
    d2.displayName = "SSRBase";
    E3 = d2;
  }
});

// node_modules/@phosphor-icons/react/dist/defs/ArrowLeft.mjs
var import_react197, t3;
var init_ArrowLeft = __esm({
  "node_modules/@phosphor-icons/react/dist/defs/ArrowLeft.mjs"() {
    import_react197 = __toESM(require_react(), 1);
    t3 = /* @__PURE__ */ new Map([
      [
        "bold",
        /* @__PURE__ */ import_react197.default.createElement(import_react197.default.Fragment, null, /* @__PURE__ */ import_react197.default.createElement("path", { d: "M228,128a12,12,0,0,1-12,12H69l51.52,51.51a12,12,0,0,1-17,17l-72-72a12,12,0,0,1,0-17l72-72a12,12,0,0,1,17,17L69,116H216A12,12,0,0,1,228,128Z" }))
      ],
      [
        "duotone",
        /* @__PURE__ */ import_react197.default.createElement(import_react197.default.Fragment, null, /* @__PURE__ */ import_react197.default.createElement("path", { d: "M112,56V200L40,128Z", opacity: "0.2" }), /* @__PURE__ */ import_react197.default.createElement("path", { d: "M216,120H120V56a8,8,0,0,0-13.66-5.66l-72,72a8,8,0,0,0,0,11.32l72,72A8,8,0,0,0,120,200V136h96a8,8,0,0,0,0-16ZM104,180.69,51.31,128,104,75.31Z" }))
      ],
      [
        "fill",
        /* @__PURE__ */ import_react197.default.createElement(import_react197.default.Fragment, null, /* @__PURE__ */ import_react197.default.createElement("path", { d: "M224,128a8,8,0,0,1-8,8H120v64a8,8,0,0,1-13.66,5.66l-72-72a8,8,0,0,1,0-11.32l72-72A8,8,0,0,1,120,56v64h96A8,8,0,0,1,224,128Z" }))
      ],
      [
        "light",
        /* @__PURE__ */ import_react197.default.createElement(import_react197.default.Fragment, null, /* @__PURE__ */ import_react197.default.createElement("path", { d: "M222,128a6,6,0,0,1-6,6H54.49l61.75,61.76a6,6,0,1,1-8.48,8.48l-72-72a6,6,0,0,1,0-8.48l72-72a6,6,0,0,1,8.48,8.48L54.49,122H216A6,6,0,0,1,222,128Z" }))
      ],
      [
        "regular",
        /* @__PURE__ */ import_react197.default.createElement(import_react197.default.Fragment, null, /* @__PURE__ */ import_react197.default.createElement("path", { d: "M224,128a8,8,0,0,1-8,8H59.31l58.35,58.34a8,8,0,0,1-11.32,11.32l-72-72a8,8,0,0,1,0-11.32l72-72a8,8,0,0,1,11.32,11.32L59.31,120H216A8,8,0,0,1,224,128Z" }))
      ],
      [
        "thin",
        /* @__PURE__ */ import_react197.default.createElement(import_react197.default.Fragment, null, /* @__PURE__ */ import_react197.default.createElement("path", { d: "M220,128a4,4,0,0,1-4,4H49.66l65.17,65.17a4,4,0,0,1-5.66,5.66l-72-72a4,4,0,0,1,0-5.66l72-72a4,4,0,0,1,5.66,5.66L49.66,124H216A4,4,0,0,1,220,128Z" }))
      ]
    ]);
  }
});

// node_modules/@phosphor-icons/react/dist/defs/ArrowRight.mjs
var import_react198, a3;
var init_ArrowRight = __esm({
  "node_modules/@phosphor-icons/react/dist/defs/ArrowRight.mjs"() {
    import_react198 = __toESM(require_react(), 1);
    a3 = /* @__PURE__ */ new Map([
      [
        "bold",
        /* @__PURE__ */ import_react198.default.createElement(import_react198.default.Fragment, null, /* @__PURE__ */ import_react198.default.createElement("path", { d: "M224.49,136.49l-72,72a12,12,0,0,1-17-17L187,140H40a12,12,0,0,1,0-24H187L135.51,64.48a12,12,0,0,1,17-17l72,72A12,12,0,0,1,224.49,136.49Z" }))
      ],
      [
        "duotone",
        /* @__PURE__ */ import_react198.default.createElement(import_react198.default.Fragment, null, /* @__PURE__ */ import_react198.default.createElement("path", { d: "M216,128l-72,72V56Z", opacity: "0.2" }), /* @__PURE__ */ import_react198.default.createElement("path", { d: "M221.66,122.34l-72-72A8,8,0,0,0,136,56v64H40a8,8,0,0,0,0,16h96v64a8,8,0,0,0,13.66,5.66l72-72A8,8,0,0,0,221.66,122.34ZM152,180.69V75.31L204.69,128Z" }))
      ],
      [
        "fill",
        /* @__PURE__ */ import_react198.default.createElement(import_react198.default.Fragment, null, /* @__PURE__ */ import_react198.default.createElement("path", { d: "M221.66,133.66l-72,72A8,8,0,0,1,136,200V136H40a8,8,0,0,1,0-16h96V56a8,8,0,0,1,13.66-5.66l72,72A8,8,0,0,1,221.66,133.66Z" }))
      ],
      [
        "light",
        /* @__PURE__ */ import_react198.default.createElement(import_react198.default.Fragment, null, /* @__PURE__ */ import_react198.default.createElement("path", { d: "M220.24,132.24l-72,72a6,6,0,0,1-8.48-8.48L201.51,134H40a6,6,0,0,1,0-12H201.51L139.76,60.24a6,6,0,0,1,8.48-8.48l72,72A6,6,0,0,1,220.24,132.24Z" }))
      ],
      [
        "regular",
        /* @__PURE__ */ import_react198.default.createElement(import_react198.default.Fragment, null, /* @__PURE__ */ import_react198.default.createElement("path", { d: "M221.66,133.66l-72,72a8,8,0,0,1-11.32-11.32L196.69,136H40a8,8,0,0,1,0-16H196.69L138.34,61.66a8,8,0,0,1,11.32-11.32l72,72A8,8,0,0,1,221.66,133.66Z" }))
      ],
      [
        "thin",
        /* @__PURE__ */ import_react198.default.createElement(import_react198.default.Fragment, null, /* @__PURE__ */ import_react198.default.createElement("path", { d: "M218.83,130.83l-72,72a4,4,0,0,1-5.66-5.66L206.34,132H40a4,4,0,0,1,0-8H206.34L141.17,58.83a4,4,0,0,1,5.66-5.66l72,72A4,4,0,0,1,218.83,130.83Z" }))
      ]
    ]);
  }
});

// node_modules/@phosphor-icons/react/dist/defs/ArrowsClockwise.mjs
var import_react199, t4;
var init_ArrowsClockwise = __esm({
  "node_modules/@phosphor-icons/react/dist/defs/ArrowsClockwise.mjs"() {
    import_react199 = __toESM(require_react(), 1);
    t4 = /* @__PURE__ */ new Map([
      [
        "bold",
        /* @__PURE__ */ import_react199.default.createElement(import_react199.default.Fragment, null, /* @__PURE__ */ import_react199.default.createElement("path", { d: "M228,48V96a12,12,0,0,1-12,12H168a12,12,0,0,1,0-24h19l-7.8-7.8a75.55,75.55,0,0,0-53.32-22.26h-.43A75.49,75.49,0,0,0,72.39,75.57,12,12,0,1,1,55.61,58.41a99.38,99.38,0,0,1,69.87-28.47H126A99.42,99.42,0,0,1,196.2,59.23L204,67V48a12,12,0,0,1,24,0ZM183.61,180.43a75.49,75.49,0,0,1-53.09,21.63h-.43A75.55,75.55,0,0,1,76.77,179.8L69,172H88a12,12,0,0,0,0-24H40a12,12,0,0,0-12,12v48a12,12,0,0,0,24,0V189l7.8,7.8A99.42,99.42,0,0,0,130,226.06h.56a99.38,99.38,0,0,0,69.87-28.47,12,12,0,0,0-16.78-17.16Z" }))
      ],
      [
        "duotone",
        /* @__PURE__ */ import_react199.default.createElement(import_react199.default.Fragment, null, /* @__PURE__ */ import_react199.default.createElement("path", { d: "M216,128a88,88,0,1,1-88-88A88,88,0,0,1,216,128Z", opacity: "0.2" }), /* @__PURE__ */ import_react199.default.createElement("path", { d: "M224,48V96a8,8,0,0,1-8,8H168a8,8,0,0,1,0-16h28.69L182.06,73.37a79.56,79.56,0,0,0-56.13-23.43h-.45A79.52,79.52,0,0,0,69.59,72.71,8,8,0,0,1,58.41,61.27a96,96,0,0,1,135,.79L208,76.69V48a8,8,0,0,1,16,0ZM186.41,183.29a80,80,0,0,1-112.47-.66L59.31,168H88a8,8,0,0,0,0-16H40a8,8,0,0,0-8,8v48a8,8,0,0,0,16,0V179.31l14.63,14.63A95.43,95.43,0,0,0,130,222.06h.53a95.36,95.36,0,0,0,67.07-27.33,8,8,0,0,0-11.18-11.44Z" }))
      ],
      [
        "fill",
        /* @__PURE__ */ import_react199.default.createElement(import_react199.default.Fragment, null, /* @__PURE__ */ import_react199.default.createElement("path", { d: "M224,48V96a8,8,0,0,1-8,8H168a8,8,0,0,1-5.66-13.66L180.65,72a79.48,79.48,0,0,0-54.72-22.09h-.45A79.52,79.52,0,0,0,69.59,72.71,8,8,0,0,1,58.41,61.27,96,96,0,0,1,192,60.7l18.36-18.36A8,8,0,0,1,224,48ZM186.41,183.29A80,80,0,0,1,75.35,184l18.31-18.31A8,8,0,0,0,88,152H40a8,8,0,0,0-8,8v48a8,8,0,0,0,13.66,5.66L64,195.3a95.42,95.42,0,0,0,66,26.76h.53a95.36,95.36,0,0,0,67.07-27.33,8,8,0,0,0-11.18-11.44Z" }))
      ],
      [
        "light",
        /* @__PURE__ */ import_react199.default.createElement(import_react199.default.Fragment, null, /* @__PURE__ */ import_react199.default.createElement("path", { d: "M222,48V96a6,6,0,0,1-6,6H168a6,6,0,0,1,0-12h33.52L183.47,72a81.51,81.51,0,0,0-57.53-24h-.46A81.5,81.5,0,0,0,68.19,71.28a6,6,0,1,1-8.38-8.58,93.38,93.38,0,0,1,65.67-26.76H126a93.45,93.45,0,0,1,66,27.53l18,18V48a6,6,0,0,1,12,0ZM187.81,184.72a81.5,81.5,0,0,1-57.29,23.34h-.46a81.51,81.51,0,0,1-57.53-24L54.48,166H88a6,6,0,0,0,0-12H40a6,6,0,0,0-6,6v48a6,6,0,0,0,12,0V174.48l18,18.05a93.45,93.45,0,0,0,66,27.53h.52a93.38,93.38,0,0,0,65.67-26.76,6,6,0,1,0-8.38-8.58Z" }))
      ],
      [
        "regular",
        /* @__PURE__ */ import_react199.default.createElement(import_react199.default.Fragment, null, /* @__PURE__ */ import_react199.default.createElement("path", { d: "M224,48V96a8,8,0,0,1-8,8H168a8,8,0,0,1,0-16h28.69L182.06,73.37a79.56,79.56,0,0,0-56.13-23.43h-.45A79.52,79.52,0,0,0,69.59,72.71,8,8,0,0,1,58.41,61.27a96,96,0,0,1,135,.79L208,76.69V48a8,8,0,0,1,16,0ZM186.41,183.29a80,80,0,0,1-112.47-.66L59.31,168H88a8,8,0,0,0,0-16H40a8,8,0,0,0-8,8v48a8,8,0,0,0,16,0V179.31l14.63,14.63A95.43,95.43,0,0,0,130,222.06h.53a95.36,95.36,0,0,0,67.07-27.33,8,8,0,0,0-11.18-11.44Z" }))
      ],
      [
        "thin",
        /* @__PURE__ */ import_react199.default.createElement(import_react199.default.Fragment, null, /* @__PURE__ */ import_react199.default.createElement("path", { d: "M220,48V96a4,4,0,0,1-4,4H168a4,4,0,0,1,0-8h38.34L184.89,70.54A84,84,0,0,0,66.8,69.85a4,4,0,1,1-5.6-5.72,92,92,0,0,1,129.34.76L212,86.34V48a4,4,0,0,1,8,0ZM189.2,186.15a83.44,83.44,0,0,1-58.68,23.91h-.47a83.52,83.52,0,0,1-58.94-24.6L49.66,164H88a4,4,0,0,0,0-8H40a4,4,0,0,0-4,4v48a4,4,0,0,0,8,0V169.66l21.46,21.45A91.43,91.43,0,0,0,130,218.06h.51a91.45,91.45,0,0,0,64.28-26.19,4,4,0,1,0-5.6-5.72Z" }))
      ]
    ]);
  }
});

// node_modules/@phosphor-icons/react/dist/defs/CaretDown.mjs
var import_react200, l3;
var init_CaretDown = __esm({
  "node_modules/@phosphor-icons/react/dist/defs/CaretDown.mjs"() {
    import_react200 = __toESM(require_react(), 1);
    l3 = /* @__PURE__ */ new Map([
      [
        "bold",
        /* @__PURE__ */ import_react200.default.createElement(import_react200.default.Fragment, null, /* @__PURE__ */ import_react200.default.createElement("path", { d: "M216.49,104.49l-80,80a12,12,0,0,1-17,0l-80-80a12,12,0,0,1,17-17L128,159l71.51-71.52a12,12,0,0,1,17,17Z" }))
      ],
      [
        "duotone",
        /* @__PURE__ */ import_react200.default.createElement(import_react200.default.Fragment, null, /* @__PURE__ */ import_react200.default.createElement("path", { d: "M208,96l-80,80L48,96Z", opacity: "0.2" }), /* @__PURE__ */ import_react200.default.createElement("path", { d: "M215.39,92.94A8,8,0,0,0,208,88H48a8,8,0,0,0-5.66,13.66l80,80a8,8,0,0,0,11.32,0l80-80A8,8,0,0,0,215.39,92.94ZM128,164.69,67.31,104H188.69Z" }))
      ],
      [
        "fill",
        /* @__PURE__ */ import_react200.default.createElement(import_react200.default.Fragment, null, /* @__PURE__ */ import_react200.default.createElement("path", { d: "M213.66,101.66l-80,80a8,8,0,0,1-11.32,0l-80-80A8,8,0,0,1,48,88H208a8,8,0,0,1,5.66,13.66Z" }))
      ],
      [
        "light",
        /* @__PURE__ */ import_react200.default.createElement(import_react200.default.Fragment, null, /* @__PURE__ */ import_react200.default.createElement("path", { d: "M212.24,100.24l-80,80a6,6,0,0,1-8.48,0l-80-80a6,6,0,0,1,8.48-8.48L128,167.51l75.76-75.75a6,6,0,0,1,8.48,8.48Z" }))
      ],
      [
        "regular",
        /* @__PURE__ */ import_react200.default.createElement(import_react200.default.Fragment, null, /* @__PURE__ */ import_react200.default.createElement("path", { d: "M213.66,101.66l-80,80a8,8,0,0,1-11.32,0l-80-80A8,8,0,0,1,53.66,90.34L128,164.69l74.34-74.35a8,8,0,0,1,11.32,11.32Z" }))
      ],
      [
        "thin",
        /* @__PURE__ */ import_react200.default.createElement(import_react200.default.Fragment, null, /* @__PURE__ */ import_react200.default.createElement("path", { d: "M210.83,98.83l-80,80a4,4,0,0,1-5.66,0l-80-80a4,4,0,0,1,5.66-5.66L128,170.34l77.17-77.17a4,4,0,1,1,5.66,5.66Z" }))
      ]
    ]);
  }
});

// node_modules/@phosphor-icons/react/dist/defs/Check.mjs
var import_react201, t5;
var init_Check = __esm({
  "node_modules/@phosphor-icons/react/dist/defs/Check.mjs"() {
    import_react201 = __toESM(require_react(), 1);
    t5 = /* @__PURE__ */ new Map([
      [
        "bold",
        /* @__PURE__ */ import_react201.default.createElement(import_react201.default.Fragment, null, /* @__PURE__ */ import_react201.default.createElement("path", { d: "M232.49,80.49l-128,128a12,12,0,0,1-17,0l-56-56a12,12,0,1,1,17-17L96,183,215.51,63.51a12,12,0,0,1,17,17Z" }))
      ],
      [
        "duotone",
        /* @__PURE__ */ import_react201.default.createElement(import_react201.default.Fragment, null, /* @__PURE__ */ import_react201.default.createElement(
          "path",
          {
            d: "M232,56V200a16,16,0,0,1-16,16H40a16,16,0,0,1-16-16V56A16,16,0,0,1,40,40H216A16,16,0,0,1,232,56Z",
            opacity: "0.2"
          }
        ), /* @__PURE__ */ import_react201.default.createElement("path", { d: "M205.66,85.66l-96,96a8,8,0,0,1-11.32,0l-40-40a8,8,0,0,1,11.32-11.32L104,164.69l90.34-90.35a8,8,0,0,1,11.32,11.32Z" }))
      ],
      [
        "fill",
        /* @__PURE__ */ import_react201.default.createElement(import_react201.default.Fragment, null, /* @__PURE__ */ import_react201.default.createElement("path", { d: "M216,40H40A16,16,0,0,0,24,56V200a16,16,0,0,0,16,16H216a16,16,0,0,0,16-16V56A16,16,0,0,0,216,40ZM205.66,85.66l-96,96a8,8,0,0,1-11.32,0l-40-40a8,8,0,0,1,11.32-11.32L104,164.69l90.34-90.35a8,8,0,0,1,11.32,11.32Z" }))
      ],
      [
        "light",
        /* @__PURE__ */ import_react201.default.createElement(import_react201.default.Fragment, null, /* @__PURE__ */ import_react201.default.createElement("path", { d: "M228.24,76.24l-128,128a6,6,0,0,1-8.48,0l-56-56a6,6,0,0,1,8.48-8.48L96,191.51,219.76,67.76a6,6,0,0,1,8.48,8.48Z" }))
      ],
      [
        "regular",
        /* @__PURE__ */ import_react201.default.createElement(import_react201.default.Fragment, null, /* @__PURE__ */ import_react201.default.createElement("path", { d: "M229.66,77.66l-128,128a8,8,0,0,1-11.32,0l-56-56a8,8,0,0,1,11.32-11.32L96,188.69,218.34,66.34a8,8,0,0,1,11.32,11.32Z" }))
      ],
      [
        "thin",
        /* @__PURE__ */ import_react201.default.createElement(import_react201.default.Fragment, null, /* @__PURE__ */ import_react201.default.createElement("path", { d: "M226.83,74.83l-128,128a4,4,0,0,1-5.66,0l-56-56a4,4,0,0,1,5.66-5.66L96,194.34,221.17,69.17a4,4,0,1,1,5.66,5.66Z" }))
      ]
    ]);
  }
});

// node_modules/@phosphor-icons/react/dist/ssr/Check.mjs
var import_react202, f3, i3, p2, t6, s3, h3, m3, a5, c4, n4;
var init_Check2 = __esm({
  "node_modules/@phosphor-icons/react/dist/ssr/Check.mjs"() {
    import_react202 = __toESM(require_react(), 1);
    init_SSRBase();
    init_Check();
    f3 = Object.defineProperty;
    i3 = Object.defineProperties;
    p2 = Object.getOwnPropertyDescriptors;
    t6 = Object.getOwnPropertySymbols;
    s3 = Object.prototype.hasOwnProperty;
    h3 = Object.prototype.propertyIsEnumerable;
    m3 = (r7, e14, o8) => e14 in r7 ? f3(r7, e14, { enumerable: true, configurable: true, writable: true, value: o8 }) : r7[e14] = o8;
    a5 = (r7, e14) => {
      for (var o8 in e14 || (e14 = {}))
        s3.call(e14, o8) && m3(r7, o8, e14[o8]);
      if (t6)
        for (var o8 of t6(e14))
          h3.call(e14, o8) && m3(r7, o8, e14[o8]);
      return r7;
    };
    c4 = (r7, e14) => i3(r7, p2(e14));
    n4 = (0, import_react202.forwardRef)((r7, e14) => /* @__PURE__ */ import_react202.default.createElement(E3, c4(a5({ ref: e14 }, r7), { weights: t5 })));
    n4.displayName = "Check";
  }
});

// node_modules/@phosphor-icons/react/dist/defs/CheckCircle.mjs
var import_react203, t7;
var init_CheckCircle = __esm({
  "node_modules/@phosphor-icons/react/dist/defs/CheckCircle.mjs"() {
    import_react203 = __toESM(require_react(), 1);
    t7 = /* @__PURE__ */ new Map([
      [
        "bold",
        /* @__PURE__ */ import_react203.default.createElement(import_react203.default.Fragment, null, /* @__PURE__ */ import_react203.default.createElement("path", { d: "M176.49,95.51a12,12,0,0,1,0,17l-56,56a12,12,0,0,1-17,0l-24-24a12,12,0,1,1,17-17L112,143l47.51-47.52A12,12,0,0,1,176.49,95.51ZM236,128A108,108,0,1,1,128,20,108.12,108.12,0,0,1,236,128Zm-24,0a84,84,0,1,0-84,84A84.09,84.09,0,0,0,212,128Z" }))
      ],
      [
        "duotone",
        /* @__PURE__ */ import_react203.default.createElement(import_react203.default.Fragment, null, /* @__PURE__ */ import_react203.default.createElement("path", { d: "M224,128a96,96,0,1,1-96-96A96,96,0,0,1,224,128Z", opacity: "0.2" }), /* @__PURE__ */ import_react203.default.createElement("path", { d: "M173.66,98.34a8,8,0,0,1,0,11.32l-56,56a8,8,0,0,1-11.32,0l-24-24a8,8,0,0,1,11.32-11.32L112,148.69l50.34-50.35A8,8,0,0,1,173.66,98.34ZM232,128A104,104,0,1,1,128,24,104.11,104.11,0,0,1,232,128Zm-16,0a88,88,0,1,0-88,88A88.1,88.1,0,0,0,216,128Z" }))
      ],
      [
        "fill",
        /* @__PURE__ */ import_react203.default.createElement(import_react203.default.Fragment, null, /* @__PURE__ */ import_react203.default.createElement("path", { d: "M128,24A104,104,0,1,0,232,128,104.11,104.11,0,0,0,128,24Zm45.66,85.66-56,56a8,8,0,0,1-11.32,0l-24-24a8,8,0,0,1,11.32-11.32L112,148.69l50.34-50.35a8,8,0,0,1,11.32,11.32Z" }))
      ],
      [
        "light",
        /* @__PURE__ */ import_react203.default.createElement(import_react203.default.Fragment, null, /* @__PURE__ */ import_react203.default.createElement("path", { d: "M172.24,99.76a6,6,0,0,1,0,8.48l-56,56a6,6,0,0,1-8.48,0l-24-24a6,6,0,0,1,8.48-8.48L112,151.51l51.76-51.75A6,6,0,0,1,172.24,99.76ZM230,128A102,102,0,1,1,128,26,102.12,102.12,0,0,1,230,128Zm-12,0a90,90,0,1,0-90,90A90.1,90.1,0,0,0,218,128Z" }))
      ],
      [
        "regular",
        /* @__PURE__ */ import_react203.default.createElement(import_react203.default.Fragment, null, /* @__PURE__ */ import_react203.default.createElement("path", { d: "M173.66,98.34a8,8,0,0,1,0,11.32l-56,56a8,8,0,0,1-11.32,0l-24-24a8,8,0,0,1,11.32-11.32L112,148.69l50.34-50.35A8,8,0,0,1,173.66,98.34ZM232,128A104,104,0,1,1,128,24,104.11,104.11,0,0,1,232,128Zm-16,0a88,88,0,1,0-88,88A88.1,88.1,0,0,0,216,128Z" }))
      ],
      [
        "thin",
        /* @__PURE__ */ import_react203.default.createElement(import_react203.default.Fragment, null, /* @__PURE__ */ import_react203.default.createElement("path", { d: "M170.83,101.17a4,4,0,0,1,0,5.66l-56,56a4,4,0,0,1-5.66,0l-24-24a4,4,0,0,1,5.66-5.66L112,154.34l53.17-53.17A4,4,0,0,1,170.83,101.17ZM228,128A100,100,0,1,1,128,28,100.11,100.11,0,0,1,228,128Zm-8,0a92,92,0,1,0-92,92A92.1,92.1,0,0,0,220,128Z" }))
      ]
    ]);
  }
});

// node_modules/@phosphor-icons/react/dist/ssr/CheckCircle.mjs
var import_react204, i4, f4, p3, t8, s4, l4, m4, a6, c5, k;
var init_CheckCircle2 = __esm({
  "node_modules/@phosphor-icons/react/dist/ssr/CheckCircle.mjs"() {
    import_react204 = __toESM(require_react(), 1);
    init_SSRBase();
    init_CheckCircle();
    i4 = Object.defineProperty;
    f4 = Object.defineProperties;
    p3 = Object.getOwnPropertyDescriptors;
    t8 = Object.getOwnPropertySymbols;
    s4 = Object.prototype.hasOwnProperty;
    l4 = Object.prototype.propertyIsEnumerable;
    m4 = (r7, e14, o8) => e14 in r7 ? i4(r7, e14, { enumerable: true, configurable: true, writable: true, value: o8 }) : r7[e14] = o8;
    a6 = (r7, e14) => {
      for (var o8 in e14 || (e14 = {}))
        s4.call(e14, o8) && m4(r7, o8, e14[o8]);
      if (t8)
        for (var o8 of t8(e14))
          l4.call(e14, o8) && m4(r7, o8, e14[o8]);
      return r7;
    };
    c5 = (r7, e14) => f4(r7, p3(e14));
    k = (0, import_react204.forwardRef)((r7, e14) => /* @__PURE__ */ import_react204.default.createElement(E3, c5(a6({ ref: e14 }, r7), { weights: t7 })));
    k.displayName = "CheckCircle";
  }
});

// node_modules/@phosphor-icons/react/dist/defs/EnvelopeSimple.mjs
var import_react205, t9;
var init_EnvelopeSimple = __esm({
  "node_modules/@phosphor-icons/react/dist/defs/EnvelopeSimple.mjs"() {
    import_react205 = __toESM(require_react(), 1);
    t9 = /* @__PURE__ */ new Map([
      [
        "bold",
        /* @__PURE__ */ import_react205.default.createElement(import_react205.default.Fragment, null, /* @__PURE__ */ import_react205.default.createElement("path", { d: "M224,44H32A12,12,0,0,0,20,56V192a20,20,0,0,0,20,20H216a20,20,0,0,0,20-20V56A12,12,0,0,0,224,44ZM193.15,68,128,127.72,62.85,68ZM44,188V83.28l75.89,69.57a12,12,0,0,0,16.22,0L212,83.28V188Z" }))
      ],
      [
        "duotone",
        /* @__PURE__ */ import_react205.default.createElement(import_react205.default.Fragment, null, /* @__PURE__ */ import_react205.default.createElement("path", { d: "M224,56l-96,88L32,56Z", opacity: "0.2" }), /* @__PURE__ */ import_react205.default.createElement("path", { d: "M224,48H32a8,8,0,0,0-8,8V192a16,16,0,0,0,16,16H216a16,16,0,0,0,16-16V56A8,8,0,0,0,224,48ZM203.43,64,128,133.15,52.57,64ZM216,192H40V74.19l82.59,75.71a8,8,0,0,0,10.82,0L216,74.19V192Z" }))
      ],
      [
        "fill",
        /* @__PURE__ */ import_react205.default.createElement(import_react205.default.Fragment, null, /* @__PURE__ */ import_react205.default.createElement("path", { d: "M224,48H32a8,8,0,0,0-8,8V192a16,16,0,0,0,16,16H216a16,16,0,0,0,16-16V56A8,8,0,0,0,224,48Zm-8,144H40V74.19l82.59,75.71a8,8,0,0,0,10.82,0L216,74.19V192Z" }))
      ],
      [
        "light",
        /* @__PURE__ */ import_react205.default.createElement(import_react205.default.Fragment, null, /* @__PURE__ */ import_react205.default.createElement("path", { d: "M224,50H32a6,6,0,0,0-6,6V192a14,14,0,0,0,14,14H216a14,14,0,0,0,14-14V56A6,6,0,0,0,224,50ZM208.58,62,128,135.86,47.42,62ZM216,194H40a2,2,0,0,1-2-2V69.64l86,78.78a6,6,0,0,0,8.1,0L218,69.64V192A2,2,0,0,1,216,194Z" }))
      ],
      [
        "regular",
        /* @__PURE__ */ import_react205.default.createElement(import_react205.default.Fragment, null, /* @__PURE__ */ import_react205.default.createElement("path", { d: "M224,48H32a8,8,0,0,0-8,8V192a16,16,0,0,0,16,16H216a16,16,0,0,0,16-16V56A8,8,0,0,0,224,48ZM203.43,64,128,133.15,52.57,64ZM216,192H40V74.19l82.59,75.71a8,8,0,0,0,10.82,0L216,74.19V192Z" }))
      ],
      [
        "thin",
        /* @__PURE__ */ import_react205.default.createElement(import_react205.default.Fragment, null, /* @__PURE__ */ import_react205.default.createElement("path", { d: "M224,52H32a4,4,0,0,0-4,4V192a12,12,0,0,0,12,12H216a12,12,0,0,0,12-12V56A4,4,0,0,0,224,52Zm-10.28,8L128,138.57,42.28,60ZM216,196H40a4,4,0,0,1-4-4V65.09L125.3,147a4,4,0,0,0,5.4,0L220,65.09V192A4,4,0,0,1,216,196Z" }))
      ]
    ]);
  }
});

// node_modules/@phosphor-icons/react/dist/ssr/EnvelopeSimple.mjs
var import_react206, i5, l5, f5, r3, s5, n5, p4, t10, a7, d4;
var init_EnvelopeSimple2 = __esm({
  "node_modules/@phosphor-icons/react/dist/ssr/EnvelopeSimple.mjs"() {
    import_react206 = __toESM(require_react(), 1);
    init_SSRBase();
    init_EnvelopeSimple();
    i5 = Object.defineProperty;
    l5 = Object.defineProperties;
    f5 = Object.getOwnPropertyDescriptors;
    r3 = Object.getOwnPropertySymbols;
    s5 = Object.prototype.hasOwnProperty;
    n5 = Object.prototype.propertyIsEnumerable;
    p4 = (o8, e14, m12) => e14 in o8 ? i5(o8, e14, { enumerable: true, configurable: true, writable: true, value: m12 }) : o8[e14] = m12;
    t10 = (o8, e14) => {
      for (var m12 in e14 || (e14 = {}))
        s5.call(e14, m12) && p4(o8, m12, e14[m12]);
      if (r3)
        for (var m12 of r3(e14))
          n5.call(e14, m12) && p4(o8, m12, e14[m12]);
      return o8;
    };
    a7 = (o8, e14) => l5(o8, f5(e14));
    d4 = (0, import_react206.forwardRef)((o8, e14) => /* @__PURE__ */ import_react206.default.createElement(E3, a7(t10({ ref: e14 }, o8), { weights: t9 })));
    d4.displayName = "EnvelopeSimple";
  }
});

// node_modules/@phosphor-icons/react/dist/defs/Heart.mjs
var import_react207, t11;
var init_Heart = __esm({
  "node_modules/@phosphor-icons/react/dist/defs/Heart.mjs"() {
    import_react207 = __toESM(require_react(), 1);
    t11 = /* @__PURE__ */ new Map([
      [
        "bold",
        /* @__PURE__ */ import_react207.default.createElement(import_react207.default.Fragment, null, /* @__PURE__ */ import_react207.default.createElement("path", { d: "M178,36c-20.09,0-37.92,7.93-50,21.56C115.92,43.93,98.09,36,78,36a66.08,66.08,0,0,0-66,66c0,72.34,105.81,130.14,110.31,132.57a12,12,0,0,0,11.38,0C138.19,232.14,244,174.34,244,102A66.08,66.08,0,0,0,178,36Zm-5.49,142.36A328.69,328.69,0,0,1,128,210.16a328.69,328.69,0,0,1-44.51-31.8C61.82,159.77,36,131.42,36,102A42,42,0,0,1,78,60c17.8,0,32.7,9.4,38.89,24.54a12,12,0,0,0,22.22,0C145.3,69.4,160.2,60,178,60a42,42,0,0,1,42,42C220,131.42,194.18,159.77,172.51,178.36Z" }))
      ],
      [
        "duotone",
        /* @__PURE__ */ import_react207.default.createElement(import_react207.default.Fragment, null, /* @__PURE__ */ import_react207.default.createElement(
          "path",
          {
            d: "M232,102c0,66-104,122-104,122S24,168,24,102A54,54,0,0,1,78,48c22.59,0,41.94,12.31,50,32,8.06-19.69,27.41-32,50-32A54,54,0,0,1,232,102Z",
            opacity: "0.2"
          }
        ), /* @__PURE__ */ import_react207.default.createElement("path", { d: "M178,40c-20.65,0-38.73,8.88-50,23.89C116.73,48.88,98.65,40,78,40a62.07,62.07,0,0,0-62,62c0,70,103.79,126.66,108.21,129a8,8,0,0,0,7.58,0C136.21,228.66,240,172,240,102A62.07,62.07,0,0,0,178,40ZM128,214.8C109.74,204.16,32,155.69,32,102A46.06,46.06,0,0,1,78,56c19.45,0,35.78,10.36,42.6,27a8,8,0,0,0,14.8,0c6.82-16.67,23.15-27,42.6-27a46.06,46.06,0,0,1,46,46C224,155.61,146.24,204.15,128,214.8Z" }))
      ],
      [
        "fill",
        /* @__PURE__ */ import_react207.default.createElement(import_react207.default.Fragment, null, /* @__PURE__ */ import_react207.default.createElement("path", { d: "M240,102c0,70-103.79,126.66-108.21,129a8,8,0,0,1-7.58,0C119.79,228.66,16,172,16,102A62.07,62.07,0,0,1,78,40c20.65,0,38.73,8.88,50,23.89C139.27,48.88,157.35,40,178,40A62.07,62.07,0,0,1,240,102Z" }))
      ],
      [
        "light",
        /* @__PURE__ */ import_react207.default.createElement(import_react207.default.Fragment, null, /* @__PURE__ */ import_react207.default.createElement("path", { d: "M178,42c-21,0-39.26,9.47-50,25.34C117.26,51.47,99,42,78,42a60.07,60.07,0,0,0-60,60c0,29.2,18.2,59.59,54.1,90.31a334.68,334.68,0,0,0,53.06,37,6,6,0,0,0,5.68,0,334.68,334.68,0,0,0,53.06-37C219.8,161.59,238,131.2,238,102A60.07,60.07,0,0,0,178,42ZM128,217.11C111.59,207.64,30,157.72,30,102A48.05,48.05,0,0,1,78,54c20.28,0,37.31,10.83,44.45,28.27a6,6,0,0,0,11.1,0C140.69,64.83,157.72,54,178,54a48.05,48.05,0,0,1,48,48C226,157.72,144.41,207.64,128,217.11Z" }))
      ],
      [
        "regular",
        /* @__PURE__ */ import_react207.default.createElement(import_react207.default.Fragment, null, /* @__PURE__ */ import_react207.default.createElement("path", { d: "M178,40c-20.65,0-38.73,8.88-50,23.89C116.73,48.88,98.65,40,78,40a62.07,62.07,0,0,0-62,62c0,70,103.79,126.66,108.21,129a8,8,0,0,0,7.58,0C136.21,228.66,240,172,240,102A62.07,62.07,0,0,0,178,40ZM128,214.8C109.74,204.16,32,155.69,32,102A46.06,46.06,0,0,1,78,56c19.45,0,35.78,10.36,42.6,27a8,8,0,0,0,14.8,0c6.82-16.67,23.15-27,42.6-27a46.06,46.06,0,0,1,46,46C224,155.61,146.24,204.15,128,214.8Z" }))
      ],
      [
        "thin",
        /* @__PURE__ */ import_react207.default.createElement(import_react207.default.Fragment, null, /* @__PURE__ */ import_react207.default.createElement("path", { d: "M178,44c-21.44,0-39.92,10.19-50,27.07C117.92,54.19,99.44,44,78,44a58.07,58.07,0,0,0-58,58c0,28.59,18,58.47,53.4,88.79a333.81,333.81,0,0,0,52.7,36.73,4,4,0,0,0,3.8,0,333.81,333.81,0,0,0,52.7-36.73C218,160.47,236,130.59,236,102A58.07,58.07,0,0,0,178,44ZM128,219.42c-14-8-100-59.35-100-117.42A50.06,50.06,0,0,1,78,52c21.11,0,38.85,11.31,46.3,29.51a4,4,0,0,0,7.4,0C139.15,63.31,156.89,52,178,52a50.06,50.06,0,0,1,50,50C228,160,142,211.46,128,219.42Z" }))
      ]
    ]);
  }
});

// node_modules/@phosphor-icons/react/dist/ssr/Heart.mjs
var import_react208, i6, p5, s6, a8, c7, R4, o3, m5, f6, H2;
var init_Heart2 = __esm({
  "node_modules/@phosphor-icons/react/dist/ssr/Heart.mjs"() {
    import_react208 = __toESM(require_react(), 1);
    init_SSRBase();
    init_Heart();
    i6 = Object.defineProperty;
    p5 = Object.defineProperties;
    s6 = Object.getOwnPropertyDescriptors;
    a8 = Object.getOwnPropertySymbols;
    c7 = Object.prototype.hasOwnProperty;
    R4 = Object.prototype.propertyIsEnumerable;
    o3 = (r7, e14, t20) => e14 in r7 ? i6(r7, e14, { enumerable: true, configurable: true, writable: true, value: t20 }) : r7[e14] = t20;
    m5 = (r7, e14) => {
      for (var t20 in e14 || (e14 = {}))
        c7.call(e14, t20) && o3(r7, t20, e14[t20]);
      if (a8)
        for (var t20 of a8(e14))
          R4.call(e14, t20) && o3(r7, t20, e14[t20]);
      return r7;
    };
    f6 = (r7, e14) => p5(r7, s6(e14));
    H2 = (0, import_react208.forwardRef)((r7, e14) => /* @__PURE__ */ import_react208.default.createElement(E3, f6(m5({ ref: e14 }, r7), { weights: t11 })));
    H2.displayName = "Heart";
  }
});

// node_modules/@phosphor-icons/react/dist/defs/Info.mjs
var import_react209, t12;
var init_Info = __esm({
  "node_modules/@phosphor-icons/react/dist/defs/Info.mjs"() {
    import_react209 = __toESM(require_react(), 1);
    t12 = /* @__PURE__ */ new Map([
      [
        "bold",
        /* @__PURE__ */ import_react209.default.createElement(import_react209.default.Fragment, null, /* @__PURE__ */ import_react209.default.createElement("path", { d: "M108,84a16,16,0,1,1,16,16A16,16,0,0,1,108,84Zm128,44A108,108,0,1,1,128,20,108.12,108.12,0,0,1,236,128Zm-24,0a84,84,0,1,0-84,84A84.09,84.09,0,0,0,212,128Zm-72,36.68V132a20,20,0,0,0-20-20,12,12,0,0,0-4,23.32V168a20,20,0,0,0,20,20,12,12,0,0,0,4-23.32Z" }))
      ],
      [
        "duotone",
        /* @__PURE__ */ import_react209.default.createElement(import_react209.default.Fragment, null, /* @__PURE__ */ import_react209.default.createElement("path", { d: "M224,128a96,96,0,1,1-96-96A96,96,0,0,1,224,128Z", opacity: "0.2" }), /* @__PURE__ */ import_react209.default.createElement("path", { d: "M144,176a8,8,0,0,1-8,8,16,16,0,0,1-16-16V128a8,8,0,0,1,0-16,16,16,0,0,1,16,16v40A8,8,0,0,1,144,176Zm88-48A104,104,0,1,1,128,24,104.11,104.11,0,0,1,232,128Zm-16,0a88,88,0,1,0-88,88A88.1,88.1,0,0,0,216,128ZM124,96a12,12,0,1,0-12-12A12,12,0,0,0,124,96Z" }))
      ],
      [
        "fill",
        /* @__PURE__ */ import_react209.default.createElement(import_react209.default.Fragment, null, /* @__PURE__ */ import_react209.default.createElement("path", { d: "M128,24A104,104,0,1,0,232,128,104.11,104.11,0,0,0,128,24Zm-4,48a12,12,0,1,1-12,12A12,12,0,0,1,124,72Zm12,112a16,16,0,0,1-16-16V128a8,8,0,0,1,0-16,16,16,0,0,1,16,16v40a8,8,0,0,1,0,16Z" }))
      ],
      [
        "light",
        /* @__PURE__ */ import_react209.default.createElement(import_react209.default.Fragment, null, /* @__PURE__ */ import_react209.default.createElement("path", { d: "M142,176a6,6,0,0,1-6,6,14,14,0,0,1-14-14V128a2,2,0,0,0-2-2,6,6,0,0,1,0-12,14,14,0,0,1,14,14v40a2,2,0,0,0,2,2A6,6,0,0,1,142,176ZM124,94a10,10,0,1,0-10-10A10,10,0,0,0,124,94Zm106,34A102,102,0,1,1,128,26,102.12,102.12,0,0,1,230,128Zm-12,0a90,90,0,1,0-90,90A90.1,90.1,0,0,0,218,128Z" }))
      ],
      [
        "regular",
        /* @__PURE__ */ import_react209.default.createElement(import_react209.default.Fragment, null, /* @__PURE__ */ import_react209.default.createElement("path", { d: "M128,24A104,104,0,1,0,232,128,104.11,104.11,0,0,0,128,24Zm0,192a88,88,0,1,1,88-88A88.1,88.1,0,0,1,128,216Zm16-40a8,8,0,0,1-8,8,16,16,0,0,1-16-16V128a8,8,0,0,1,0-16,16,16,0,0,1,16,16v40A8,8,0,0,1,144,176ZM112,84a12,12,0,1,1,12,12A12,12,0,0,1,112,84Z" }))
      ],
      [
        "thin",
        /* @__PURE__ */ import_react209.default.createElement(import_react209.default.Fragment, null, /* @__PURE__ */ import_react209.default.createElement("path", { d: "M140,176a4,4,0,0,1-4,4,12,12,0,0,1-12-12V128a4,4,0,0,0-4-4,4,4,0,0,1,0-8,12,12,0,0,1,12,12v40a4,4,0,0,0,4,4A4,4,0,0,1,140,176ZM124,92a8,8,0,1,0-8-8A8,8,0,0,0,124,92Zm104,36A100,100,0,1,1,128,28,100.11,100.11,0,0,1,228,128Zm-8,0a92,92,0,1,0-92,92A92.1,92.1,0,0,0,220,128Z" }))
      ]
    ]);
  }
});

// node_modules/@phosphor-icons/react/dist/ssr/Info.mjs
var import_react210, i7, p6, s7, t13, n6, c8, m6, a9, f7, w3;
var init_Info2 = __esm({
  "node_modules/@phosphor-icons/react/dist/ssr/Info.mjs"() {
    import_react210 = __toESM(require_react(), 1);
    init_SSRBase();
    init_Info();
    i7 = Object.defineProperty;
    p6 = Object.defineProperties;
    s7 = Object.getOwnPropertyDescriptors;
    t13 = Object.getOwnPropertySymbols;
    n6 = Object.prototype.hasOwnProperty;
    c8 = Object.prototype.propertyIsEnumerable;
    m6 = (e14, o8, r7) => o8 in e14 ? i7(e14, o8, { enumerable: true, configurable: true, writable: true, value: r7 }) : e14[o8] = r7;
    a9 = (e14, o8) => {
      for (var r7 in o8 || (o8 = {}))
        n6.call(o8, r7) && m6(e14, r7, o8[r7]);
      if (t13)
        for (var r7 of t13(o8))
          c8.call(o8, r7) && m6(e14, r7, o8[r7]);
      return e14;
    };
    f7 = (e14, o8) => p6(e14, s7(o8));
    w3 = (0, import_react210.forwardRef)((e14, o8) => /* @__PURE__ */ import_react210.default.createElement(E3, f7(a9({ ref: o8 }, e14), { weights: t12 })));
    w3.displayName = "Info";
  }
});

// node_modules/@phosphor-icons/react/dist/ssr/index.mjs
var init_ssr = __esm({
  "node_modules/@phosphor-icons/react/dist/ssr/index.mjs"() {
    init_Check2();
    init_CheckCircle2();
    init_EnvelopeSimple2();
    init_Heart2();
    init_Info2();
  }
});

// node_modules/@phosphor-icons/react/dist/lib/context.mjs
var import_react211, o4;
var init_context = __esm({
  "node_modules/@phosphor-icons/react/dist/lib/context.mjs"() {
    import_react211 = __toESM(require_react(), 1);
    o4 = (0, import_react211.createContext)({
      color: "currentColor",
      size: "1em",
      weight: "regular",
      mirrored: false
    });
  }
});

// node_modules/@phosphor-icons/react/dist/lib/IconBase.mjs
var import_react212, y2, c9, f8, g3, d6, l7, a10, h5, b3;
var init_IconBase = __esm({
  "node_modules/@phosphor-icons/react/dist/lib/IconBase.mjs"() {
    import_react212 = __toESM(require_react(), 1);
    init_context();
    y2 = Object.defineProperty;
    c9 = Object.getOwnPropertySymbols;
    f8 = Object.prototype.hasOwnProperty;
    g3 = Object.prototype.propertyIsEnumerable;
    d6 = (t20, o8, e14) => o8 in t20 ? y2(t20, o8, { enumerable: true, configurable: true, writable: true, value: e14 }) : t20[o8] = e14;
    l7 = (t20, o8) => {
      for (var e14 in o8 || (o8 = {}))
        f8.call(o8, e14) && d6(t20, e14, o8[e14]);
      if (c9)
        for (var e14 of c9(o8))
          g3.call(o8, e14) && d6(t20, e14, o8[e14]);
      return t20;
    };
    a10 = (t20, o8) => {
      var e14 = {};
      for (var r7 in t20)
        f8.call(t20, r7) && o8.indexOf(r7) < 0 && (e14[r7] = t20[r7]);
      if (t20 != null && c9)
        for (var r7 of c9(t20))
          o8.indexOf(r7) < 0 && g3.call(t20, r7) && (e14[r7] = t20[r7]);
      return e14;
    };
    h5 = (0, import_react212.forwardRef)((t20, o8) => {
      const m12 = t20, {
        alt: e14,
        color: r7,
        size: n13,
        weight: s36,
        mirrored: p11,
        children: u4,
        weights: C5
      } = m12, v5 = a10(m12, [
        "alt",
        "color",
        "size",
        "weight",
        "mirrored",
        "children",
        "weights"
      ]), x3 = (0, import_react212.useContext)(o4), {
        color: B4 = "currentColor",
        size: i13,
        weight: I6 = "regular",
        mirrored: E6 = false
      } = x3, R10 = a10(x3, [
        "color",
        "size",
        "weight",
        "mirrored"
      ]);
      return /* @__PURE__ */ import_react212.default.createElement(
        "svg",
        l7(l7({
          ref: o8,
          xmlns: "http://www.w3.org/2000/svg",
          width: n13 != null ? n13 : i13,
          height: n13 != null ? n13 : i13,
          fill: r7 != null ? r7 : B4,
          viewBox: "0 0 256 256",
          transform: p11 || E6 ? "scale(-1, 1)" : void 0
        }, R10), v5),
        !!e14 && /* @__PURE__ */ import_react212.default.createElement("title", null, e14),
        u4,
        C5.get(s36 != null ? s36 : I6)
      );
    });
    h5.displayName = "IconBase";
    b3 = h5;
  }
});

// node_modules/@phosphor-icons/react/dist/csr/ArrowLeft.mjs
var import_react213, i8, p7, s8, t14, c10, w5, m7, a11, f9, I3;
var init_ArrowLeft2 = __esm({
  "node_modules/@phosphor-icons/react/dist/csr/ArrowLeft.mjs"() {
    import_react213 = __toESM(require_react(), 1);
    init_IconBase();
    init_ArrowLeft();
    i8 = Object.defineProperty;
    p7 = Object.defineProperties;
    s8 = Object.getOwnPropertyDescriptors;
    t14 = Object.getOwnPropertySymbols;
    c10 = Object.prototype.hasOwnProperty;
    w5 = Object.prototype.propertyIsEnumerable;
    m7 = (e14, r7, o8) => r7 in e14 ? i8(e14, r7, { enumerable: true, configurable: true, writable: true, value: o8 }) : e14[r7] = o8;
    a11 = (e14, r7) => {
      for (var o8 in r7 || (r7 = {}))
        c10.call(r7, o8) && m7(e14, o8, r7[o8]);
      if (t14)
        for (var o8 of t14(r7))
          w5.call(r7, o8) && m7(e14, o8, r7[o8]);
      return e14;
    };
    f9 = (e14, r7) => p7(e14, s8(r7));
    I3 = (0, import_react213.forwardRef)((e14, r7) => /* @__PURE__ */ import_react213.default.createElement(b3, f9(a11({ ref: r7 }, e14), { weights: t3 })));
    I3.displayName = "ArrowLeft";
  }
});

// node_modules/@phosphor-icons/react/dist/csr/ArrowRight.mjs
var import_react214, f10, p8, s9, e11, c11, w6, m8, a12, i9, d8;
var init_ArrowRight2 = __esm({
  "node_modules/@phosphor-icons/react/dist/csr/ArrowRight.mjs"() {
    import_react214 = __toESM(require_react(), 1);
    init_IconBase();
    init_ArrowRight();
    f10 = Object.defineProperty;
    p8 = Object.defineProperties;
    s9 = Object.getOwnPropertyDescriptors;
    e11 = Object.getOwnPropertySymbols;
    c11 = Object.prototype.hasOwnProperty;
    w6 = Object.prototype.propertyIsEnumerable;
    m8 = (o8, r7, t20) => r7 in o8 ? f10(o8, r7, { enumerable: true, configurable: true, writable: true, value: t20 }) : o8[r7] = t20;
    a12 = (o8, r7) => {
      for (var t20 in r7 || (r7 = {}))
        c11.call(r7, t20) && m8(o8, t20, r7[t20]);
      if (e11)
        for (var t20 of e11(r7))
          w6.call(r7, t20) && m8(o8, t20, r7[t20]);
      return o8;
    };
    i9 = (o8, r7) => p8(o8, s9(r7));
    d8 = (0, import_react214.forwardRef)((o8, r7) => /* @__PURE__ */ import_react214.default.createElement(b3, i9(a12({ ref: r7 }, o8), { weights: a3 })));
    d8.displayName = "ArrowRight";
  }
});

// node_modules/@phosphor-icons/react/dist/csr/ArrowsClockwise.mjs
var import_react215, i10, c12, w7, s10, f11, p9, t15, m9, a13, A2;
var init_ArrowsClockwise2 = __esm({
  "node_modules/@phosphor-icons/react/dist/csr/ArrowsClockwise.mjs"() {
    import_react215 = __toESM(require_react(), 1);
    init_IconBase();
    init_ArrowsClockwise();
    i10 = Object.defineProperty;
    c12 = Object.defineProperties;
    w7 = Object.getOwnPropertyDescriptors;
    s10 = Object.getOwnPropertySymbols;
    f11 = Object.prototype.hasOwnProperty;
    p9 = Object.prototype.propertyIsEnumerable;
    t15 = (r7, o8, e14) => o8 in r7 ? i10(r7, o8, { enumerable: true, configurable: true, writable: true, value: e14 }) : r7[o8] = e14;
    m9 = (r7, o8) => {
      for (var e14 in o8 || (o8 = {}))
        f11.call(o8, e14) && t15(r7, e14, o8[e14]);
      if (s10)
        for (var e14 of s10(o8))
          p9.call(o8, e14) && t15(r7, e14, o8[e14]);
      return r7;
    };
    a13 = (r7, o8) => c12(r7, w7(o8));
    A2 = (0, import_react215.forwardRef)((r7, o8) => /* @__PURE__ */ import_react215.default.createElement(b3, a13(m9({ ref: o8 }, r7), { weights: t4 })));
    A2.displayName = "ArrowsClockwise";
  }
});

// node_modules/@phosphor-icons/react/dist/csr/CaretDown.mjs
var import_react216, i11, n9, p10, t16, s11, c13, a14, m10, f12, D2;
var init_CaretDown2 = __esm({
  "node_modules/@phosphor-icons/react/dist/csr/CaretDown.mjs"() {
    import_react216 = __toESM(require_react(), 1);
    init_IconBase();
    init_CaretDown();
    i11 = Object.defineProperty;
    n9 = Object.defineProperties;
    p10 = Object.getOwnPropertyDescriptors;
    t16 = Object.getOwnPropertySymbols;
    s11 = Object.prototype.hasOwnProperty;
    c13 = Object.prototype.propertyIsEnumerable;
    a14 = (o8, e14, r7) => e14 in o8 ? i11(o8, e14, { enumerable: true, configurable: true, writable: true, value: r7 }) : o8[e14] = r7;
    m10 = (o8, e14) => {
      for (var r7 in e14 || (e14 = {}))
        s11.call(e14, r7) && a14(o8, r7, e14[r7]);
      if (t16)
        for (var r7 of t16(e14))
          c13.call(e14, r7) && a14(o8, r7, e14[r7]);
      return o8;
    };
    f12 = (o8, e14) => n9(o8, p10(e14));
    D2 = (0, import_react216.forwardRef)((o8, e14) => /* @__PURE__ */ import_react216.default.createElement(b3, f12(m10({ ref: e14 }, o8), { weights: l3 })));
    D2.displayName = "CaretDown";
  }
});

// node_modules/@phosphor-icons/react/dist/index.mjs
var init_dist = __esm({
  "node_modules/@phosphor-icons/react/dist/index.mjs"() {
    init_ArrowLeft2();
    init_ArrowRight2();
    init_ArrowsClockwise2();
    init_CaretDown2();
  }
});

// dist/server/chunks/chunk-DQ1jpH4N.js
var back, s12;
var init_chunk_DQ1jpH4N = __esm({
  "dist/server/chunks/chunk-DQ1jpH4N.js"() {
    "use strict";
    back = "_back_14heg_1";
    s12 = {
      back
    };
  }
});

// dist/server/chunks/chunk-feoxuLkI.js
var import_jsx_runtime161, navigate, BackButton;
var init_chunk_feoxuLkI = __esm({
  "dist/server/chunks/chunk-feoxuLkI.js"() {
    "use strict";
    import_jsx_runtime161 = __toESM(require_jsx_runtime(), 1);
    init_esm2();
    init_dist();
    init_chunk_DQ1jpH4N();
    navigate = {
      back: () => window.history.back()
    };
    BackButton = ({
      ...rest
    }) => {
      return /* @__PURE__ */ (0, import_jsx_runtime161.jsx)(Button, { ...rest, c: "dark.7", component: "a", variant: "subtle", className: s12.back, onClick: navigate.back, leftSection: /* @__PURE__ */ (0, import_jsx_runtime161.jsx)(I3, {}), children: "\u041D\u0430\u0437\u0430\u0434" });
    };
  }
});

// dist/server/chunks/chunk-Vlh7XtbV.js
var import_jsx_runtime162, import_react218, MainButton, IconCheck;
var init_chunk_Vlh7XtbV = __esm({
  "dist/server/chunks/chunk-Vlh7XtbV.js"() {
    "use strict";
    init_esm2();
    import_jsx_runtime162 = __toESM(require_jsx_runtime(), 1);
    import_react218 = __toESM(require_react(), 1);
    MainButton = Button.withProps({
      size: "lg",
      radius: "lg",
      bg: "dark.6"
    });
    IconCheck = (0, import_react218.memo)(() => /* @__PURE__ */ (0, import_jsx_runtime162.jsx)("svg", { xmlns: "http://www.w3.org/2000/svg", width: "1em", height: "1em", fill: "currentColor", viewBox: "0 0 256 256", children: /* @__PURE__ */ (0, import_jsx_runtime162.jsx)("path", { d: "m232.49 80.49-128 128a12 12 0 0 1-17 0l-56-56a12 12 0 1 1 17-17L96 183 215.51 63.51a12 12 0 0 1 17 17Z" }) }));
    IconCheck.displayName = "IconCheck";
  }
});

// dist/server/chunks/chunk-CVNqduS6.js
var container, s13;
var init_chunk_CVNqduS6 = __esm({
  "dist/server/chunks/chunk-CVNqduS6.js"() {
    "use strict";
    container = "_container_wap6c_1";
    s13 = {
      container
    };
  }
});

// dist/server/chunks/chunk-B37yhB3o.js
var import_jsx_runtime163, InnerContainer;
var init_chunk_B37yhB3o = __esm({
  "dist/server/chunks/chunk-B37yhB3o.js"() {
    "use strict";
    import_jsx_runtime163 = __toESM(require_jsx_runtime(), 1);
    init_esm2();
    init_clsx();
    init_chunk_CVNqduS6();
    InnerContainer = ({
      children,
      className,
      ...rest
    }) => {
      return /* @__PURE__ */ (0, import_jsx_runtime163.jsx)(Container, { className: clsx_default(s13.container, className), ...rest, children });
    };
  }
});

// dist/server/entries/src_pages_error.mjs
var src_pages_error_exports = {};
__export(src_pages_error_exports, {
  configValuesSerialized: () => configValuesSerialized
});
function Page() {
  const {
    is404
  } = usePageContext();
  if (is404) {
    return /* @__PURE__ */ (0, import_jsx_runtime164.jsxs)(InnerContainer, { children: [
      /* @__PURE__ */ (0, import_jsx_runtime164.jsx)(BackButton, {}),
      /* @__PURE__ */ (0, import_jsx_runtime164.jsx)("h1", { children: "404 Page Not Found" }),
      /* @__PURE__ */ (0, import_jsx_runtime164.jsx)("p", { children: "This page could not be found." })
    ] });
  }
  return /* @__PURE__ */ (0, import_jsx_runtime164.jsxs)(InnerContainer, { children: [
    /* @__PURE__ */ (0, import_jsx_runtime164.jsx)(BackButton, {}),
    /* @__PURE__ */ (0, import_jsx_runtime164.jsx)("h1", { children: "500 Internal Server Error" }),
    /* @__PURE__ */ (0, import_jsx_runtime164.jsx)("p", { children: "Something went wrong." })
  ] });
}
var import_jsx_runtime164, import_react219, import7, configValuesSerialized;
var init_src_pages_error = __esm({
  "dist/server/entries/src_pages_error.mjs"() {
    "use strict";
    init_Loading();
    init_onRenderHtml();
    init_chunk_Y_L1G_uK();
    import_jsx_runtime164 = __toESM(require_jsx_runtime(), 1);
    init_usePageContext();
    import_react219 = __toESM(require_react(), 1);
    init_chunk_feoxuLkI();
    init_chunk_Vlh7XtbV();
    init_chunk_B37yhB3o();
    init_chunk_D3GmwNoI();
    init_chunk_DQ1jpH4N();
    init_clsx();
    init_chunk_CVNqduS6();
    import7 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
      __proto__: null,
      default: Page
    }, Symbol.toStringTag, { value: "Module" }));
    configValuesSerialized = {
      ["isClientRuntimeLoaded"]: {
        type: "computed",
        definedAtData: null,
        valueSerialized: {
          type: "js-serialized",
          value: true
        }
      },
      ["Loading"]: {
        type: "standard",
        definedAtData: { "filePathToShowToUser": "vike-react/__internal/integration/Loading", "fileExportPathToShowToUser": [] },
        valueSerialized: {
          type: "pointer-import",
          value: Loading_default
        }
      },
      ["onRenderHtml"]: {
        type: "standard",
        definedAtData: { "filePathToShowToUser": "vike-react/__internal/integration/onRenderHtml", "fileExportPathToShowToUser": [] },
        valueSerialized: {
          type: "pointer-import",
          value: onRenderHtml
        }
      },
      ["Wrapper"]: {
        type: "cumulative",
        definedAtData: [{ "filePathToShowToUser": "/src/pages/+Wrapper.tsx", "fileExportPathToShowToUser": [] }],
        valueSerialized: [{
          type: "plus-file",
          exportValues: import3
        }]
      },
      ["passToClient"]: {
        type: "cumulative",
        definedAtData: [{ "filePathToShowToUser": "/src/pages/+config.ts", "fileExportPathToShowToUser": ["default", "passToClient"] }, { "filePathToShowToUser": "vike-react/config", "fileExportPathToShowToUser": ["default", "passToClient"] }],
        valueSerialized: [{
          type: "js-serialized",
          value: ["scopeValues"]
        }, {
          type: "js-serialized",
          value: ["_configFromHook"]
        }]
      },
      ["cacheControl"]: {
        type: "standard",
        definedAtData: { "filePathToShowToUser": "/src/pages/+config.ts", "fileExportPathToShowToUser": ["default", "cacheControl"] },
        valueSerialized: {
          type: "js-serialized",
          value: "public, max-age=604800"
        }
      },
      ["title"]: {
        type: "standard",
        definedAtData: { "filePathToShowToUser": "/src/pages/+config.ts", "fileExportPathToShowToUser": ["default", "title"] },
        valueSerialized: {
          type: "js-serialized",
          value: "Cognitive Lab"
        }
      },
      ["lang"]: {
        type: "standard",
        definedAtData: { "filePathToShowToUser": "/src/pages/+config.ts", "fileExportPathToShowToUser": ["default", "lang"] },
        valueSerialized: {
          type: "js-serialized",
          value: "ru"
        }
      },
      ["reactStrictMode"]: {
        type: "standard",
        definedAtData: { "filePathToShowToUser": "/src/pages/+config.ts", "fileExportPathToShowToUser": ["default", "reactStrictMode"] },
        valueSerialized: {
          type: "js-serialized",
          value: false
        }
      },
      ["onBeforeRender"]: {
        type: "standard",
        definedAtData: { "filePathToShowToUser": "/src/pages/+onBeforeRender.ts", "fileExportPathToShowToUser": [] },
        valueSerialized: {
          type: "plus-file",
          exportValues: import4
        }
      },
      ["Head"]: {
        type: "cumulative",
        definedAtData: [{ "filePathToShowToUser": "/src/widgets/HeadDefault/index.tsx", "fileExportPathToShowToUser": ["HeadDefault"] }],
        valueSerialized: [{
          type: "pointer-import",
          value: HeadDefault
        }]
      },
      ["Layout"]: {
        type: "cumulative",
        definedAtData: [{ "filePathToShowToUser": "/src/widgets/RootLayout/index.tsx", "fileExportPathToShowToUser": ["RootLayout"] }],
        valueSerialized: [{
          type: "pointer-import",
          value: RootLayout
        }]
      },
      ["Page"]: {
        type: "standard",
        definedAtData: { "filePathToShowToUser": "/src/pages/_error/+Page.tsx", "fileExportPathToShowToUser": [] },
        valueSerialized: {
          type: "plus-file",
          exportValues: import7
        }
      }
    };
  }
});

// node_modules/@farfetched/core/dist/core.js
function D3({
  field: e14
}) {
  let t20;
  if (e14 === void 0)
    t20 = h((r7) => null, {
      serialize: "ignore",
      name: "ff.$target/undefined",
      sid: "ff.$target/undefined"
    });
  else if (ae.store(e14))
    t20 = g(
      e14,
      (n13) => (a18) => n13 ?? null
    );
  else if (e14 != null && e14.source && (e14 != null && e14.fn)) {
    const r7 = e14;
    t20 = g(
      r7.source,
      (n13) => (a18) => r7.fn(a18, n13) ?? null
    );
  } else if (typeof e14 == "function") {
    const r7 = e14;
    t20 = h(
      (n13) => r7(n13) ?? null,
      {
        serialize: "ignore",
        name: "ff.$target/callbackField",
        sid: "ff.$target/$callbackField"
      }
    );
  } else {
    const r7 = e14;
    t20 = h((n13) => r7 ?? null, {
      serialize: "ignore",
      name: "ff.$target/valueField",
      sid: "ff.$target/$valueField"
    });
  }
  return t20;
}
function pt2(e14) {
  let t20;
  if (e14 === void 0)
    t20 = b(async (r7) => null);
  else if (ae.store(e14))
    t20 = v({
      source: e14,
      async effect(n13, a18) {
        return n13;
      }
    });
  else if (e14.source && e14.fn) {
    const r7 = e14;
    t20 = v({
      source: r7.source,
      async effect(n13, a18) {
        return r7.fn(a18, n13);
      }
    });
  } else if (typeof e14 == "function") {
    const r7 = e14;
    t20 = b(async (n13) => r7(n13));
  } else {
    const r7 = e14;
    t20 = b(async (n13) => r7);
  }
  return t20;
}
function O3(e14) {
  return e14 ? ae.store(e14) ? e14 : h(e14, {
    serialize: "ignore",
    name: "ff.$target/valueField",
    sid: "ff.$target/$valueField"
  }) : h(null, {
    serialize: "ignore",
    name: "ff.$target/undefined",
    sid: "ff.$target/$undefined"
  });
}
function Fe2({
  clock: e14,
  timeout: t20,
  target: r7 = p()
}) {
  const n13 = b(
    ({ payload: a18, milliseconds: o8 }) => new Promise((s36) => {
      setTimeout(s36, o8, a18);
    })
  );
  return x({
    source: O3(t20),
    clock: e14,
    fn: (a18, o8) => ({
      payload: o8,
      milliseconds: a18
    }),
    target: n13
  }), x({
    clock: n13.doneData,
    target: r7
  }), r7;
}
function ce2(e14, t20) {
  let r7 = [], n13 = () => false;
  Array.isArray(e14) ? (r7 = e14, n13 = t20) : Array.isArray(e14.stores) && (r7 = e14.stores, n13 = e14.predicate);
  let a18;
  ht2(n13) ? a18 = n13 : ae.store(n13) ? a18 = n13.map((i13) => (l12) => i13 === l12) : a18 = (i13) => i13 === n13;
  const o8 = g(r7);
  return g(a18, o8, (i13, l12) => l12.every(i13));
}
function ht2(e14) {
  return typeof e14 == "function";
}
function B2(e14) {
  return e14.map((t20) => !t20);
}
function Ie2(...e14) {
  return ce2({ predicate: true, stores: e14 });
}
function Te2({
  clock: e14,
  until: t20
}) {
  const r7 = p(), n13 = h(false, {
    serialize: "ignore",
    name: "ff.$fired",
    sid: "ff.$fired"
  }).on(r7, () => true).on(e14, () => false);
  return x({
    clock: [e14, t20],
    source: e14,
    filter: Ie2(t20, B2(n13)),
    target: r7
  }), r7;
}
function ne2(e14) {
  if (e14 === "ignore")
    return "ignore";
}
function yt2({
  clock: e14
}) {
  const t20 = h(Date.now(), {
    name: "ff.$time",
    sid: "ff.$time"
  });
  return x({
    clock: e14,
    fn: () => {
    },
    target: $e2
  }), x({ clock: $e2.doneData, target: t20 }), t20;
}
function E4(e14) {
  return e14.map((t20) => t20, { skipVoid: false });
}
function _t2() {
  const e14 = {
    // eslint-disable-next-line @typescript-eslint/no-empty-function
    resolve: () => {
    },
    // eslint-disable-next-line @typescript-eslint/no-empty-function
    reject: () => {
    },
    // @ts-expect-error it will be set later
    promise: null
  };
  return e14.promise = new Promise((t20, r7) => {
    e14.resolve = t20, e14.reject = r7;
  }), e14.promise.catch(() => {
  }), e14;
}
function Z2(e14, t20) {
  try {
    if (e14 === t20 || Number.isNaN(e14) && Number.isNaN(t20))
      return true;
    const r7 = be2(e14), n13 = be2(t20);
    if (r7 !== n13)
      return false;
    if (r7 === "pure-object") {
      if (e14 === t20)
        return true;
      const a18 = Object.keys(e14), o8 = Object.keys(t20).length;
      if (a18.length !== o8)
        return false;
      for (let s36 = 0, i13 = a18.length; s36 < i13; s36++) {
        const l12 = a18[s36];
        if (!Object.prototype.hasOwnProperty.call(t20, a18[s36]))
          return false;
        const g7 = e14[l12], y4 = t20[l12];
        if (g7 === e14 || y4 === t20 || g7 === t20 || y4 === e14)
          return g7 === y4;
        if (!Z2(g7, y4))
          return false;
      }
      return true;
    } else if (r7 === "array") {
      if (e14.length === t20.length)
        for (let a18 = 0; a18 < e14.length; a18++) {
          const o8 = e14[a18], s36 = t20[a18];
          if (o8 === e14 || s36 === t20 || o8 === t20 || s36 === e14)
            return o8 === s36;
          if (!Z2(o8, s36))
            return false;
        }
      else
        return false;
      return true;
    } else if (r7 === "object" && e14.valueOf && t20.valueOf)
      return e14.valueOf() === t20.valueOf();
  } catch {
  }
  return false;
}
function vt2(e14) {
  if (Array.isArray(e14))
    return true;
  const t20 = e14.length;
  return typeof t20 == "number" && t20 > -1 ? t20 ? 0 in e14 && t20 - 1 in e14 : true : false;
}
function be2(e14) {
  const t20 = typeof e14;
  return t20 === "object" ? e14 === null ? "null" : vt2(e14) ? "array" : e14.constructor === Object || Object.getPrototypeOf(e14) === null ? "pure-object" : "object" : t20;
}
function Q2(e14) {
  return (t20) => t20[e14];
}
function De2() {
  var e14, t20;
  return ((t20 = (e14 = a({ regional: true }).family.owners[0]) == null ? void 0 : e14.meta) == null ? void 0 : t20.name) ?? void 0;
}
function ee2(e14) {
  return {
    ...e14,
    errorType: je2,
    explanation: "Response was considered as invalid against a given contract"
  };
}
function ze2() {
  return {
    errorType: Re2,
    explanation: "Request was cancelled due to concurrency policy"
  };
}
function Ce2(e14) {
  return {
    ...e14,
    errorType: qe2,
    explanation: "Request was finished with unsuccessful HTTP code"
  };
}
function Ft2(e14) {
  return {
    ...e14,
    errorType: Le2,
    explanation: "Request was failed due to network problems"
  };
}
function It2(e14) {
  return b({
    handler: ({ result: r7 }) => {
      if (!e14.isData(r7))
        throw ee2({
          validationErrors: e14.getErrorMessages(r7),
          response: r7
        });
      return r7;
    },
    sid: "ff.applyContractFx"
  });
}
function Tt2(e14) {
  return !!(e14 === true || Array.isArray(e14) && e14.length === 0 || typeof e14 == "string" && e14.length === 0);
}
function we2(e14) {
  return e14 === true ? [] : e14 === false ? ["Invalid data"] : Array.isArray(e14) ? e14.length === 0 ? [] : e14 : [e14];
}
function jt2(e14) {
  var t20;
  return ((t20 = e14.error) == null ? void 0 : t20.errorType) === Re2;
}
function Ot2() {
  ie2 = true;
}
function Rt2() {
  ie2 = false;
}
function Nt2() {
  const e14 = te2;
  return te2 = null, e14;
}
function qt2(e14) {
  const t20 = p(e14.shortName + ".internalCall"), r7 = Te.compute({
    fn: (a18) => {
      const o8 = a18.handler, s36 = Lt(o8, t20);
      return a18.handler = s36, a18;
    }
  });
  return zt2(e14).seq.splice(1, 0, r7), t20;
}
function Lt(e14, t20) {
  function r7(...n13) {
    const { result: a18, abortCallback: o8 } = Bt(e14, ...n13);
    if (a18 instanceof Promise) {
      const s36 = _t2(), i13 = Ae2(s36, o8);
      return t20(i13), a18.then(s36.resolve, s36.reject), s36.promise;
    } else {
      const s36 = Ae2(void 0, o8);
      return t20(s36), a18;
    }
  }
  return r7;
}
function Ae2(e14, t20) {
  let r7 = e14 ? "pending" : "finished";
  function n13() {
    r7 = "finished", a18.status = r7;
  }
  e14 && e14.promise.then(n13, n13);
  const a18 = {
    id: Mt2(),
    status: r7,
    abort: (o8 = ze2()) => {
      t20 == null || t20(), r7 !== "finished" && e14 && e14.reject(o8);
    },
    promise: e14 == null ? void 0 : e14.promise
  };
  return a18;
}
function zt2(e14) {
  return e14.graphite.scope.runner;
}
function Mt2() {
  return `${Ct2++}`;
}
function Bt(e14, ...t20) {
  Ut(), Ot2();
  const r7 = e14(...t20), n13 = Nt2();
  return Rt2(), {
    result: r7,
    abortCallback: n13
  };
}
function He2(e14, { clock: t20 }) {
  x({ clock: t20, source: Ve2(e14), target: le2 });
}
function Ve2(e14) {
  if (!e14.__.meta.$callObjects) {
    const t20 = h([], { serialize: "ignore" });
    x({
      clock: e14.__.lowLevelAPI.callObjectCreated,
      source: t20,
      fn: (r7, n13) => r7.filter((a18) => a18.status === "pending").concat(n13),
      target: t20
    }), x({
      clock: le2.done,
      source: t20,
      fn: (r7, { params: n13 }) => r7.filter((a18) => !n13.includes(a18)),
      target: t20
    }), e14.__.meta.$callObjects = t20;
  }
  return e14.__.meta.$callObjects;
}
function Ke2({
  name: e14,
  meta: t20,
  kind: r7,
  serialize: n13,
  enabled: a18,
  contract: o8,
  validate: s36,
  mapData: i13,
  sourced: l12,
  paramsAreMeaningless: g7
}) {
  const y4 = p(), u4 = p(), $5 = p(), f15 = p(), d12 = It2(
    o8
  ), m12 = e14 ?? "unnamed", h6 = b({
    handler: () => {
      throw new Error("Not implemented");
    },
    sid: `ff.${m12}.executeFx`,
    name: `${m12}.executeFx`
  }), w11 = qt2(h6), F4 = [{
    name: "remote_source",
    get: b(async ({ params: p11 }) => ({ result: await h6(p11), stale: false }))
  }], {
    retrieveDataFx: I6,
    notifyAboutNewValidDataFx: q2,
    notifyAboutDataInvalidationFx: C5
  } = Ht(F4), M6 = p(), v5 = p(), S2 = p();
  x({
    clock: M6,
    fn: (p11) => ({
      params: p11,
      meta: { stopErrorPropagation: false, stale: true }
    }),
    target: f15
  });
  const A4 = {
    success: p(),
    failure: p(),
    skip: p(),
    finally: p()
  }, z2 = p(), U3 = p(), H4 = p();
  j({
    source: z2,
    match: {
      aborted: ({ error: p11 }) => jt2({ error: p11 })
    },
    cases: {
      aborted: H4,
      __: A4.failure
    }
  });
  const L3 = h("initial", {
    sid: `ff.${m12}.$status`,
    name: `ff.${m12}.$status`,
    serialize: n13
  });
  x({ clock: v5, target: L3.reinit });
  const G3 = h([], {
    serialize: "ignore",
    name: `ff.${m12}.$statusHistory`,
    sid: `ff.${m12}.$statusHistory`
  });
  x({
    clock: L3.updates,
    source: G3,
    fn: (p11, b4) => [...p11, b4],
    target: G3
  });
  const V3 = O3(a18 ?? true).map(Boolean), me4 = h(void 0, {
    serialize: "ignore",
    name: `ff.${m12}.$latestParams`,
    sid: `ff.${m12}.$latestParams`,
    skipVoid: false
  }), st3 = L3.map((p11) => p11 === "initial"), ot2 = L3.map((p11) => p11 === "pending"), ct3 = L3.map((p11) => p11 === "fail"), it = L3.map((p11) => p11 === "done"), lt3 = L3.map((p11) => ["fail", "done"].includes(p11));
  x({
    clock: [
      I6.map(() => "pending"),
      A4.success.map(() => "done"),
      A4.failure.map(() => "fail"),
      x({
        clock: H4,
        source: {
          history: G3,
          retrieveDataPengind: I6.pending
        },
        fn: ({ history: p11, retrieveDataPengind: b4 }) => b4 ? "pending" : p11[p11.length - 2] ?? "initial"
      })
    ],
    target: L3
  }), x({
    clock: f15,
    filter: V3,
    fn: ({ params: p11 }) => p11,
    target: me4
  }), x({
    clock: M6,
    filter: B2(V3),
    fn(p11) {
      return {
        params: p11,
        meta: { stopErrorPropagation: false, stale: false }
      };
    },
    target: A4.skip
  }), x({
    clock: y4,
    fn: ({ params: p11 }) => ({
      params: p11,
      meta: { stopErrorPropagation: false, stale: true }
    }),
    target: C5
  }), x({
    clock: C5.finally,
    source: y4,
    filter: ({ refresh: p11 }) => p11,
    fn: ({ params: p11 }) => ({
      params: p11,
      meta: { stopErrorPropagation: false, stale: true }
    }),
    target: f15
  }), x({
    clock: f15,
    filter: V3,
    target: I6
  }), x({ clock: I6, target: S2 }), x({
    clock: I6.done,
    fn: ({ params: p11, result: b4 }) => ({
      params: p11.params,
      result: b4.result,
      meta: {
        stopErrorPropagation: b4.stopErrorPropagation ?? false,
        stale: b4.stale
      }
    }),
    filter: V3,
    target: d12
  }), x({
    clock: I6.fail,
    source: V3,
    filter: (p11, { error: b4 }) => p11 && !b4.stopErrorPropagation,
    fn: (p11, { error: b4, params: j3 }) => ({
      error: b4.error,
      params: j3.params,
      meta: { stopErrorPropagation: b4.stopErrorPropagation, stale: false }
    }),
    target: z2
  });
  const { validDataRecieved: he2, __: ye3 } = j(
    x({
      clock: d12.done,
      source: {
        partialValidator: D3({
          field: s36 ?? Dt2
        })
      },
      fn: ({ partialValidator: p11 }, {
        params: {
          /* Extract original params, it is params of params */
          params: b4,
          meta: j3
        },
        result: K3
      }) => ({
        result: K3,
        params: b4,
        validation: p11({
          result: K3,
          params: b4
        }),
        meta: j3
      })
    }),
    {
      validDataRecieved: ({ validation: p11 }) => Tt2(p11)
    }
  );
  x({
    clock: he2,
    source: {
      partialMapper: D3({
        field: i13
      })
    },
    fn: ({ partialMapper: p11 }, { params: b4, result: j3, meta: K3 }) => ({
      result: p11({ params: b4, result: j3 }),
      params: b4,
      meta: K3
    }),
    target: A4.success
  }), x({
    clock: A4.success,
    filter: ({ meta: p11 }) => p11.stale,
    fn: ({ params: p11, meta: b4 }) => ({ params: p11, meta: b4, skipStale: true }),
    target: I6
  }), x({
    clock: he2,
    filter: ({ meta: p11 }) => !p11.stale,
    target: q2
  }), x({
    clock: d12.fail,
    filter: ({ params: p11 }) => !p11.meta.stopErrorPropagation,
    fn: ({ error: p11, params: b4 }) => ({
      error: p11,
      // Extract original params, it is params of params
      params: b4.params,
      meta: b4.meta
    }),
    target: z2
  }), x({
    clock: d12.fail,
    fn: ({ error: p11, params: b4 }) => ({
      error: p11,
      // Extract original params, it is params of params
      params: b4.params,
      meta: b4.meta
    }),
    target: U3
  }), x({
    clock: ye3,
    filter: ({ meta: p11 }) => !p11.stopErrorPropagation,
    fn: ({ params: p11, validation: b4, meta: j3, result: K3 }) => ({
      params: p11,
      error: ee2({
        validationErrors: we2(b4),
        response: K3
      }),
      meta: j3
    }),
    target: z2
  }), x({
    clock: ye3,
    fn: ({ params: p11, validation: b4, meta: j3, result: K3 }) => ({
      params: p11,
      error: ee2({
        validationErrors: we2(b4),
        response: K3
      }),
      meta: j3
    }),
    target: U3
  }), x({
    clock: V3.updates,
    source: M6,
    filter: B2(V3),
    fn: (p11) => ({
      params: p11,
      meta: {
        stopErrorPropagation: false,
        stale: true
      }
    }),
    target: A4.skip
  }), x({
    clock: A4.success,
    fn: ({ params: p11, result: b4, meta: j3 }) => ({
      status: "done",
      params: p11,
      result: b4,
      meta: j3
    }),
    target: A4.finally
  }), x({
    clock: A4.failure,
    fn: ({ params: p11, error: b4, meta: j3 }) => ({
      status: "fail",
      params: p11,
      error: b4,
      meta: j3
    }),
    target: A4.finally
  }), x({
    clock: A4.skip,
    fn: ({ params: p11, meta: b4 }) => ({
      status: "skip",
      params: p11,
      meta: b4
    }),
    target: A4.finally
  });
  const ge3 = {
    start: M6,
    finished: A4,
    started: S2,
    aborted: H4,
    reset: v5,
    $status: L3,
    $idle: st3,
    $pending: ot2,
    $failed: ct3,
    $succeeded: it,
    $finished: lt3,
    $enabled: V3,
    __: {
      executeFx: h6,
      meta: { ...t20, name: m12, flags: {} },
      kind: r7,
      $latestParams: E4(me4),
      lowLevelAPI: {
        dataSources: F4,
        dataSourceRetrieverFx: I6,
        sourced: l12 ?? [],
        paramsAreMeaningless: g7 ?? false,
        revalidate: y4,
        pushError: $5,
        pushData: u4,
        startWithMeta: f15,
        callObjectCreated: w11,
        failedIgnoreSuppression: U3
      }
    }
  };
  return He2(ge3, { clock: v5 }), ge3;
}
function Ht(e14) {
  const t20 = b({
    handler: async ({ params: a18, skipStale: o8 }) => {
      for (const s36 of e14)
        try {
          const i13 = await s36.get({ params: a18 });
          if (o8 && (i13 != null && i13.stale))
            continue;
          if (i13)
            return i13;
        } catch (i13) {
          throw {
            stopErrorPropagation: false,
            error: i13
          };
        }
      throw {
        stopErrorPropagation: false,
        error: new Error("No data source returned data")
      };
    }
  }), r7 = b({
    handler: async ({ params: a18, result: o8 }) => {
      await Promise.all(
        e14.map(Q2("set")).filter(Boolean).map((s36) => s36({ params: a18, result: o8 }))
      );
    }
  }), n13 = b({
    handler: async ({ params: a18 }) => {
      await Promise.all(
        e14.map(Q2("unset")).filter(Boolean).map((o8) => o8({ params: a18 }))
      );
    }
  });
  return {
    retrieveDataFx: t20,
    notifyAboutNewValidDataFx: r7,
    notifyAboutDataInvalidationFx: n13
  };
}
function Qt(e14, t20) {
  return a({
    meta: { [Vt2]: e14, [Kt]: t20 },
    regional: true
  });
}
function ue2(e14) {
  const {
    initialData: t20,
    contract: r7,
    mapData: n13,
    enabled: a18,
    validate: o8,
    name: s36,
    serialize: i13,
    sourced: l12,
    paramsAreMeaningless: g7
  } = e14, y4 = t20 ?? null, u4 = Ke2({
    name: s36 ?? De2(),
    kind: Qe2,
    serialize: ne2(i13),
    enabled: a18,
    meta: {
      serialize: i13,
      initialData: y4,
      sid: Jt(h(null, { sid: "dummy" }))
    },
    contract: r7,
    validate: o8,
    mapData: n13,
    sourced: l12,
    paramsAreMeaningless: g7
  }), $5 = p(), f15 = h(y4, {
    sid: `ff.${u4.__.meta.name}.$data`,
    name: `${u4.__.meta.name}.$data`,
    serialize: i13,
    skipVoid: false
  }), d12 = h(null, {
    sid: `ff.${u4.__.meta.name}.$error`,
    name: `${u4.__.meta.name}.$error`,
    serialize: ne2(i13),
    skipVoid: false
  }), m12 = h(true, {
    sid: `ff.${u4.__.meta.name}.$stale`,
    name: `${u4.__.meta.name}.$stale`,
    serialize: ne2(i13),
    skipVoid: false
  });
  x({
    clock: u4.finished.success,
    fn: () => null,
    target: d12
  }), x({
    clock: u4.finished.success,
    fn: ({ result: v5 }) => v5,
    target: f15
  }), f15.reset(u4.finished.failure), x({
    clock: u4.finished.failure,
    fn: ({ error: v5 }) => v5,
    target: d12
  }), x({
    clock: u4.finished.finally,
    fn: ({ meta: v5 }) => v5.stale,
    target: m12
  }), x({
    clock: u4.__.lowLevelAPI.pushData,
    target: [f15, d12.reinit]
  }), x({
    clock: u4.__.lowLevelAPI.pushError,
    target: [d12, f15.reinit]
  });
  const h6 = Te2({
    clock: $5,
    until: u4.$enabled
  }), w11 = p(), { haveToStart: P4, __: F4 } = j(
    x({
      clock: h6,
      source: { stale: m12, latestParams: u4.__.$latestParams },
      fn: ({ stale: v5, latestParams: S2 }, A4) => ({
        haveToStart: v5 || !Z2(A4, S2),
        params: A4
      })
    }),
    {
      haveToStart: ({ haveToStart: v5 }) => v5
    }
  );
  x({
    clock: F4,
    fn: () => null,
    target: w11
  }), x({
    clock: P4,
    fn: ({ params: v5 }) => v5,
    target: u4.start
  }), x({
    clock: u4.reset,
    target: [f15.reinit, d12.reinit, m12.reinit]
  });
  const I6 = {
    data: f15,
    error: d12,
    stale: m12,
    pending: u4.$pending,
    start: u4.start
  }, q2 = () => I6, C5 = ({
    source: v5,
    mapParams: S2
  }) => {
    const A4 = ue2(e14);
    return A4.__.lowLevelAPI.dataSourceRetrieverFx.use(
      v({
        source: v5,
        mapParams: ({ params: z2, ...U3 }, H4) => ({
          params: S2 ? S2(z2, H4) : z2,
          ...U3
        }),
        effect: u4.__.lowLevelAPI.dataSourceRetrieverFx
      })
    ), A4;
  }, M6 = Qt(
    { type: "query", name: e14.name },
    { $status: u4.$status, $data: f15, $error: d12 }
  );
  return n(M6, () => ({
    refresh: $5,
    start: u4.start,
    reset: u4.reset,
    started: E4(u4.started),
    $data: E4(f15),
    $error: E4(d12),
    $status: E4(u4.$status),
    $idle: E4(u4.$idle),
    $pending: E4(u4.$pending),
    $succeeded: E4(u4.$succeeded),
    $failed: E4(u4.$failed),
    $finished: E4(u4.$finished),
    $enabled: E4(u4.$enabled),
    $stale: m12,
    aborted: E4(u4.aborted),
    finished: {
      success: E4(u4.finished.success),
      failure: E4(u4.finished.failure),
      finally: E4(u4.finished.finally),
      skip: E4(u4.finished.skip)
    },
    __: {
      ...u4.__,
      lowLevelAPI: { ...u4.__.lowLevelAPI, refreshSkipDueToFreshness: w11 },
      experimentalAPI: { attach: C5 }
    },
    "@@unitShape": q2
  }));
}
function Jt(e14) {
  const t20 = e14.sid;
  return t20 != null && t20.includes("|") ? t20 : null;
}
function We2(e14) {
  const t20 = e14;
  if (ae.effect(t20.effect))
    return t20.effect;
  if (typeof t20.handler == "function")
    return b(t20.handler);
  throw new Wt(
    "handler or effect must be passed to the config"
  );
}
function Ir(e14) {
  const t20 = ue2({
    initialData: e14.initialData ?? null,
    contract: e14.contract ?? re2,
    mapData: e14.mapData ?? (({ result: r7 }) => r7),
    enabled: e14.enabled,
    validate: e14.validate,
    name: e14.name,
    serialize: e14.serialize
  });
  return t20.__.executeFx.use(We2(e14)), t20;
}
function de2(e14) {
  const { name: t20, enabled: r7, contract: n13, validate: a18, mapData: o8 } = e14, s36 = Ke2({
    name: t20 ?? De2(),
    serialize: "ignore",
    enabled: r7,
    kind: Ze2,
    meta: null,
    contract: n13,
    validate: a18,
    mapData: o8
  }), i13 = {
    pending: s36.$pending,
    start: s36.start,
    reset: s36.reset
  }, l12 = () => i13, g7 = ({
    source: y4,
    mapParams: u4
  }) => {
    const $5 = de2(e14);
    return $5.__.lowLevelAPI.dataSourceRetrieverFx.use(
      v({
        source: y4,
        mapParams: ({ params: f15, ...d12 }, m12) => ({
          params: u4 ? u4(f15, m12) : f15,
          ...d12
        }),
        effect: s36.__.lowLevelAPI.dataSourceRetrieverFx
      })
    ), $5;
  };
  return {
    start: s36.start,
    reset: s36.reset,
    started: E4(s36.started),
    aborted: E4(s36.aborted),
    $status: E4(s36.$status),
    $idle: E4(s36.$idle),
    $pending: E4(s36.$pending),
    $succeeded: E4(s36.$succeeded),
    $failed: E4(s36.$failed),
    $finished: E4(s36.$finished),
    $enabled: E4(s36.$enabled),
    finished: {
      success: E4(s36.finished.success),
      failure: E4(s36.finished.failure),
      finally: E4(s36.finished.finally),
      skip: E4(s36.finished.skip)
    },
    __: { ...s36.__, experimentalAPI: { attach: g7 } },
    "@@unitShape": l12
  };
}
function jr(e14) {
  const t20 = de2({
    name: e14.name,
    enabled: e14.enabled,
    contract: e14.contract ?? re2,
    mapData: ({ result: r7 }) => r7
  });
  return t20.__.executeFx.use(We2(e14)), t20;
}
function W2(e14) {
  if (typeof e14 == "number")
    return e14;
  let t20 = 0;
  for (const r7 of e14.split(" "))
    switch (true) {
      case Y2(r7, ar):
        t20 += X2(r7);
        break;
      case Y2(r7, sr):
        t20 += X2(r7) * 1e3;
        break;
      case Y2(r7, or):
        t20 += X2(r7) * 6e4;
        break;
      case Y2(r7, cr):
        t20 += X2(r7) * 36e5;
        break;
    }
  return t20;
}
function Y2(e14, t20) {
  return t20.includes(ir(e14));
}
function ir(e14) {
  return e14.replace(/[0-9.]/g, "");
}
function X2(e14) {
  return e14.includes(".") ? parseFloat(e14) : parseInt(e14);
}
function Rr(e14, {
  times: t20,
  delay: r7,
  filter: n13,
  mapParams: a18,
  ...o8
}) {
  const s36 = o8.supressIntermediateErrors ?? true, i13 = O3(t20), l12 = h(1, {
    serialize: "ignore",
    name: "ff.$attempt",
    sid: "ff.$attempt"
  }), g7 = g({
    attempt: l12
  }), y4 = g(
    l12,
    i13,
    (h6, w11) => s36 && h6 <= w11
  ), u4 = p(), $5 = p(), f15 = D3({
    field: n13 ?? true
  }), { planNextAttempt: d12, __: m12 } = j(
    x({
      clock: u4,
      source: {
        maxAttempts: i13,
        attempt: l12,
        partialFilter: f15
      },
      filter: ({ partialFilter: h6 }, w11) => h6(w11),
      fn: ({ attempt: h6, maxAttempts: w11 }, { params: P4, error: F4, meta: I6 }) => ({
        params: P4,
        error: F4,
        meta: { ...I6, attempt: h6, maxAttempts: w11 }
      })
    }),
    { planNextAttempt: ({ meta: h6 }) => h6.attempt <= h6.maxAttempts }
  );
  if (x({
    clock: Fe2({
      clock: x({
        clock: d12,
        source: {
          partialMapper: D3({
            field: a18 ?? (({ params: h6 }) => h6)
          })
        },
        fn: ({ partialMapper: h6 }, w11) => h6(w11)
      }),
      timeout: g(
        {
          partialTimeout: D3({
            field: r7
          }),
          meta: g7
        },
        ({ partialTimeout: h6, meta: w11 }) => W2(h6(w11))
      )
    }),
    fn: (h6) => ({
      params: h6,
      meta: { stopErrorPropagation: false, stale: true }
    }),
    target: [$5, e14.__.lowLevelAPI.startWithMeta]
  }), l12.on($5, (h6) => h6 + 1).reset([e14.finished.success, e14.start]), o8.otherwise && x({ clock: m12, target: o8.otherwise }), s36) {
    const h6 = e14.__.lowLevelAPI.dataSourceRetrieverFx.use.getCurrent();
    x({
      clock: e14.__.lowLevelAPI.failedIgnoreSuppression,
      target: u4
    }), e14.__.lowLevelAPI.dataSourceRetrieverFx.use(
      v({
        source: { supressError: y4, partialFilter: f15 },
        mapParams: (w11, { supressError: P4, partialFilter: F4 }) => ({
          ...w11,
          supressError: P4,
          partialFilter: F4
        }),
        effect: b(async ({ supressError: w11, partialFilter: P4, ...F4 }) => {
          const I6 = F(u4, { safe: true });
          try {
            return { ...await h6(F4), stopErrorPropagation: w11 };
          } catch (q2) {
            const C5 = {
              params: F4.params,
              error: q2.error,
              meta: F4.meta
            };
            throw (
              /*
               * If filter returns false, this fail is not supposed to be retried
               * so we should not suppress this error in any case.
               *
               * If filter returns is true, we should suppress this error only if
               * supressError is true.
               */
              P4(C5) && w11 ? (I6(C5), { error: q2.error, stopErrorPropagation: true }) : q2
            );
          }
        })
      })
    );
  }
  x({ clock: e14.finished.failure, target: u4 });
}
function pe2(e14) {
  const t20 = h(e14, {
    serialize: "ignore",
    name: "ff.$cacheInstance",
    sid: "ff.$cacheInstance"
  });
  return { ...e14, __: { $instance: t20 } };
}
function rt2({
  adapter: e14,
  options: t20,
  events: r7
}) {
  t20 != null && t20.hit && x({
    clock: e14.get.done,
    filter: ({ result: n13 }) => n13 !== null,
    fn: ({ params: n13 }) => ({ key: n13.key }),
    target: t20.hit
  }), t20 != null && t20.miss && x({
    clock: e14.get.done,
    filter: ({ result: n13 }) => n13 === null,
    fn: ({ params: n13 }) => ({ key: n13.key }),
    target: t20.miss
  }), t20 != null && t20.expired && (r7 != null && r7.itemExpired) && x({
    clock: r7.itemExpired,
    fn: ({ key: n13 }) => ({ key: n13 }),
    target: t20.expired
  }), t20 != null && t20.evicted && (r7 != null && r7.itemEvicted) && x({
    clock: r7.itemEvicted,
    target: t20.evicted
  });
}
function ur(e14) {
  const { maxEntries: t20, maxAge: r7, observability: n13 } = e14 ?? {};
  let a18 = {};
  const o8 = p(), s36 = p(), i13 = p(), l12 = p(), g7 = p();
  g7.watch(() => {
    a18 = {};
  });
  const y4 = yt2({ clock: o8 }), u4 = x({
    clock: o8,
    source: { now: y4 },
    fn: ({ now: f15 }, { key: d12, value: m12 }) => fr(
      a18,
      { key: d12, entry: { value: m12, cachedAt: f15 } },
      t20
    )
  });
  if (u4.watch(({ next: f15 }) => {
    a18 = f15;
  }), x({
    clock: u4,
    filter: ({ evicted: f15 }) => !!f15,
    fn: ({ evicted: f15 }) => ({ key: f15 }),
    target: l12
  }), s36.watch(({ key: f15 }) => {
    const { [f15]: d12, ...m12 } = a18;
    a18 = m12;
  }), r7) {
    const f15 = W2(r7);
    o8.watch((d12) => {
      const m12 = F(i13, { safe: true });
      setTimeout(() => m12(d12), f15);
    }), x({
      clock: i13,
      fn: ({ key: d12 }) => ({ key: d12 }),
      target: s36
    });
  }
  const $5 = {
    get: b(({ key: f15 }) => {
      const d12 = a18[f15] ?? null;
      if (!d12)
        return null;
      if (r7) {
        const m12 = (d12 == null ? void 0 : d12.cachedAt) + W2(r7);
        if (Date.now() >= m12)
          return s36({ key: f15 }), null;
      }
      return d12;
    }),
    set: b(o8),
    unset: b(s36),
    purge: g7
  };
  return rt2({
    adapter: $5,
    options: n13,
    events: { itemExpired: i13, itemEvicted: l12 }
  }), pe2($5);
}
function fr(e14, { key: t20, entry: r7 }, n13) {
  if (n13 === void 0)
    return { next: { ...e14, [t20]: r7 }, evicted: null };
  const a18 = Object.keys(e14);
  if (a18.length < n13)
    return { next: { ...e14, [t20]: r7 }, evicted: null };
  const [o8] = a18, { [o8]: s36, ...i13 } = e14;
  return { next: { ...i13, [t20]: r7 }, evicted: o8 };
}
function dr(e14) {
  let t20 = 0, r7 = 0;
  for (; r7 < e14.length; )
    t20 = (t20 << 5) - t20 + e14.charCodeAt(r7++) | 0;
  return t20.toString(36);
}
function nt2(e14) {
  const t20 = /* @__PURE__ */ new Set();
  function r7(n13) {
    if (n13 === void 0)
      return;
    if (n13 === null)
      return "null";
    if (typeof n13 == "number")
      return isFinite(n13) ? `${n13}` : "null";
    if (typeof n13 == "function")
      throw new TypeError("Can't serialize function");
    if (typeof n13 != "object")
      return JSON.stringify(n13);
    if (t20.has(n13))
      throw new TypeError("Can't serialize cyclic structure");
    if (t20.add(n13), Array.isArray(n13)) {
      const o8 = n13.map((s36) => r7(s36) || "null").join(",");
      return t20.delete(n13), `[${o8}]`;
    }
    const a18 = Object.keys(n13).sort().map((o8) => {
      const s36 = r7(n13[o8]);
      return s36 ? `${r7(o8)}:${s36}` : "";
    }).filter(Boolean).join(",");
    return t20.delete(n13), `{${a18}}`;
  }
  return r7(e14);
}
function pr({
  sid: e14,
  params: t20 = null,
  sources: r7
}) {
  try {
    return nt2({ params: t20, sources: r7, sid: e14 }) ?? null;
  } catch {
    return null;
  }
}
function mr({
  sid: e14,
  params: t20 = null,
  sources: r7
}) {
  try {
    const n13 = nt2({ params: t20, sources: r7, sid: e14 });
    return dr(n13);
  } catch {
    return null;
  }
}
function hr(e14) {
  const t20 = yr(e14);
  if (t20)
    return t20;
  const r7 = gr(e14);
  if (r7)
    return r7;
  throw new Error(
    "Query does not have sid or uniq name, which is required for caching, read more https://effector.dev/en/explanation/sids/"
  );
}
function yr(e14) {
  return e14.__.meta.sid ?? null;
}
function gr(e14) {
  const t20 = e14.__.meta.name;
  return xe2.has(t20) ? null : (xe2.add(t20), t20);
}
function Mr(e14, t20) {
  const {
    adapter: r7,
    staleAfter: n13,
    purge: a18,
    humanReadableKeys: o8
  } = {
    adapter: (t20 == null ? void 0 : t20.adapter) ?? ur(),
    humanReadableKeys: false,
    ...t20
  }, s36 = o8 ? pr : mr, i13 = hr(e14), l12 = e14.__.lowLevelAPI.sourced.map(pt2), g7 = b(async (d12) => Promise.all(l12.map((m12) => m12(d12)))), y4 = b(async ({ instance: d12, params: m12 }) => {
    const h6 = await g7(m12), w11 = s36({
      sid: i13,
      params: e14.__.lowLevelAPI.paramsAreMeaningless ? null : m12,
      sources: h6
    });
    w11 && await d12.unset({ key: w11 });
  }), u4 = b(async ({ instance: d12, params: m12, result: h6 }) => {
    const w11 = await g7(m12), P4 = s36({
      sid: i13,
      params: e14.__.lowLevelAPI.paramsAreMeaningless ? null : m12,
      sources: w11
    });
    P4 && await d12.set({ key: P4, value: h6 });
  }), $5 = b(async ({ params: d12, instance: m12 }) => {
    const h6 = await g7(d12), w11 = s36({
      sid: i13,
      params: e14.__.lowLevelAPI.paramsAreMeaningless ? null : d12,
      sources: h6
    });
    if (!w11)
      return null;
    const P4 = await m12.get({ key: w11 });
    if (!P4)
      return null;
    const F4 = n13 ? P4.cachedAt + W2(n13) <= Date.now() : true;
    return { result: P4.value, stale: F4 };
  }), f15 = {
    name: "cache",
    get: v({
      source: {
        instance: r7.__.$instance
      },
      mapParams: ({ params: d12 }, { instance: m12 }) => ({
        params: d12,
        instance: m12
      }),
      effect: $5
    }),
    set: v({
      source: {
        instance: r7.__.$instance
      },
      mapParams: ({ params: d12, result: m12 }, { instance: h6 }) => ({
        params: d12,
        result: m12,
        instance: h6
      }),
      effect: u4
    }),
    unset: v({
      source: {
        instance: r7.__.$instance
      },
      mapParams: ({ params: d12 }, { instance: m12 }) => ({
        instance: m12,
        params: d12
      }),
      effect: y4
    })
  };
  e14.__.lowLevelAPI.dataSources.unshift(f15), a18 && x({
    clock: a18,
    source: { instance: r7.__.$instance },
    target: b(
      ({ instance: d12 }) => d12.purge()
    )
  });
}
var re2, $e2, je2, Re2, qe2, Le2, Dt2, te2, ie2, Ct2, Ut, le2, Qe2, Vt2, Kt, Wt, Zt, er, Ze2, ar, sr, or, cr, xe2;
var init_core = __esm({
  "node_modules/@farfetched/core/dist/core.js"() {
    init_effector();
    re2 = {
      isData: (e14) => true,
      getErrorMessages: () => []
    };
    $e2 = b(() => Date.now());
    je2 = "INVALID_DATA";
    Re2 = "ABORT";
    qe2 = "HTTP";
    Le2 = "NETWORK";
    Dt2 = () => true;
    te2 = null;
    ie2 = false;
    Ct2 = 0;
    Ut = p();
    le2 = b(
      (e14) => e14.forEach((t20) => t20.abort())
    );
    Qe2 = Symbol("Query");
    Vt2 = Symbol("Farfetched node meta");
    Kt = Symbol("Farfetched links meta");
    Wt = class extends Error {
      constructor(t20) {
        super(t20);
      }
    };
    Zt = b({
      sid: "ff.fetchFx",
      handler: globalThis.fetch
    });
    er = b({
      handler: async (e14) => {
        const t20 = await Zt(e14).catch((r7) => {
          throw Ft2({
            reason: (r7 == null ? void 0 : r7.message) ?? null,
            cause: r7
          });
        });
        if (!t20.ok)
          throw Ce2({
            status: t20.status,
            statusText: t20.statusText,
            response: await t20.text().catch(() => null) ?? null
          });
        return t20;
      },
      sid: "ff.requestFx"
    });
    Ze2 = Symbol("Mutation");
    ar = [
      "ms",
      "milli",
      "millisecond",
      "milliseconds"
    ];
    sr = ["s", "sec", "secs", "second", "seconds"];
    or = ["m", "min", "mins", "minute", "minutes"];
    cr = ["h", "hr", "hrs", "hour", "hours"];
    xe2 = /* @__PURE__ */ new Set();
  }
});

// node_modules/node-fetch-native/dist/shared/node-fetch-native.1a4a356d.mjs
function f13(e14) {
  return e14 && e14.__esModule && Object.prototype.hasOwnProperty.call(e14, "default") ? e14.default : e14;
}
var t17, o5, n10;
var init_node_fetch_native_1a4a356d = __esm({
  "node_modules/node-fetch-native/dist/shared/node-fetch-native.1a4a356d.mjs"() {
    t17 = Object.defineProperty;
    o5 = (e14, l12) => t17(e14, "name", { value: l12, configurable: true });
    n10 = typeof globalThis < "u" ? globalThis : typeof window < "u" ? window : typeof global < "u" ? global : typeof self < "u" ? self : {};
    o5(f13, "getDefaultExportFromCjs");
  }
});

// node_modules/node-fetch-native/dist/chunks/multipart-parser.mjs
var multipart_parser_exports = {};
__export(multipart_parser_exports, {
  toFormData: () => Z3
});
function v3(u4) {
  const a18 = u4.match(/\bfilename=("(.*?)"|([^()<>@,;:\\"/[\]?={}\s\t]+))($|;\s)/i);
  if (!a18) return;
  const n13 = a18[2] || a18[3] || "";
  let r7 = n13.slice(n13.lastIndexOf("\\") + 1);
  return r7 = r7.replace(/%22/g, '"'), r7 = r7.replace(/&#(\d{4});/g, (d12, l12) => String.fromCharCode(l12)), r7;
}
async function Z3(u4, a18) {
  if (!/multipart/i.test(a18)) throw new TypeError("Failed to fetch");
  const n13 = a18.match(/boundary=(?:"([^"]+)"|([^;]+))/i);
  if (!n13) throw new TypeError("no or bad content-type header, no multipart boundary");
  const r7 = new k3(n13[1] || n13[2]);
  let d12, l12, c15, p11, e14, i13;
  const A4 = [], H4 = new br(), O5 = E5((s36) => {
    c15 += f15.decode(s36, { stream: true });
  }, "onPartData"), y4 = E5((s36) => {
    A4.push(s36);
  }, "appendToFile"), o8 = E5(() => {
    const s36 = new On(A4, i13, { type: e14 });
    H4.append(p11, s36);
  }, "appendFileToFormData"), L3 = E5(() => {
    H4.append(p11, c15);
  }, "appendEntryToFormData"), f15 = new TextDecoder("utf-8");
  f15.decode(), r7.onPartBegin = function() {
    r7.onPartData = O5, r7.onPartEnd = L3, d12 = "", l12 = "", c15 = "", p11 = "", e14 = "", i13 = null, A4.length = 0;
  }, r7.onHeaderField = function(s36) {
    d12 += f15.decode(s36, { stream: true });
  }, r7.onHeaderValue = function(s36) {
    l12 += f15.decode(s36, { stream: true });
  }, r7.onHeaderEnd = function() {
    if (l12 += f15.decode(), d12 = d12.toLowerCase(), d12 === "content-disposition") {
      const s36 = l12.match(/\bname=("([^"]*)"|([^()<>@,;:\\"/[\]?={}\s\t]+))/i);
      s36 && (p11 = s36[2] || s36[3] || ""), i13 = v3(l12), i13 && (r7.onPartData = y4, r7.onPartEnd = o8);
    } else d12 === "content-type" && (e14 = l12);
    l12 = "", d12 = "";
  };
  for await (const s36 of u4) r7.write(s36);
  return r7.end(), H4;
}
var B3, E5, D4, t18, w9, R7, g5, N3, x2, P2, C3, I4, M4, $2, m11, F2, k3;
var init_multipart_parser = __esm({
  "node_modules/node-fetch-native/dist/chunks/multipart-parser.mjs"() {
    init_node();
    init_node_fetch_native_1a4a356d();
    B3 = Object.defineProperty;
    E5 = (u4, a18) => B3(u4, "name", { value: a18, configurable: true });
    D4 = 0;
    t18 = { START_BOUNDARY: D4++, HEADER_FIELD_START: D4++, HEADER_FIELD: D4++, HEADER_VALUE_START: D4++, HEADER_VALUE: D4++, HEADER_VALUE_ALMOST_DONE: D4++, HEADERS_ALMOST_DONE: D4++, PART_DATA_START: D4++, PART_DATA: D4++, END: D4++ };
    w9 = 1;
    R7 = { PART_BOUNDARY: w9, LAST_BOUNDARY: w9 *= 2 };
    g5 = 10;
    N3 = 13;
    x2 = 32;
    P2 = 45;
    C3 = 58;
    I4 = 97;
    M4 = 122;
    $2 = E5((u4) => u4 | 32, "lower");
    m11 = E5(() => {
    }, "noop");
    F2 = class F3 {
      constructor(a18) {
        this.index = 0, this.flags = 0, this.onHeaderEnd = m11, this.onHeaderField = m11, this.onHeadersEnd = m11, this.onHeaderValue = m11, this.onPartBegin = m11, this.onPartData = m11, this.onPartEnd = m11, this.boundaryChars = {}, a18 = `\r
--` + a18;
        const n13 = new Uint8Array(a18.length);
        for (let r7 = 0; r7 < a18.length; r7++) n13[r7] = a18.charCodeAt(r7), this.boundaryChars[n13[r7]] = true;
        this.boundary = n13, this.lookbehind = new Uint8Array(this.boundary.length + 8), this.state = t18.START_BOUNDARY;
      }
      write(a18) {
        let n13 = 0;
        const r7 = a18.length;
        let d12 = this.index, { lookbehind: l12, boundary: c15, boundaryChars: p11, index: e14, state: i13, flags: A4 } = this;
        const H4 = this.boundary.length, O5 = H4 - 1, y4 = a18.length;
        let o8, L3;
        const f15 = E5((h6) => {
          this[h6 + "Mark"] = n13;
        }, "mark"), s36 = E5((h6) => {
          delete this[h6 + "Mark"];
        }, "clear"), T3 = E5((h6, S2, _2, U3) => {
          (S2 === void 0 || S2 !== _2) && this[h6](U3 && U3.subarray(S2, _2));
        }, "callback"), b4 = E5((h6, S2) => {
          const _2 = h6 + "Mark";
          _2 in this && (S2 ? (T3(h6, this[_2], n13, a18), delete this[_2]) : (T3(h6, this[_2], a18.length, a18), this[_2] = 0));
        }, "dataCallback");
        for (n13 = 0; n13 < r7; n13++) switch (o8 = a18[n13], i13) {
          case t18.START_BOUNDARY:
            if (e14 === c15.length - 2) {
              if (o8 === P2) A4 |= R7.LAST_BOUNDARY;
              else if (o8 !== N3) return;
              e14++;
              break;
            } else if (e14 - 1 === c15.length - 2) {
              if (A4 & R7.LAST_BOUNDARY && o8 === P2) i13 = t18.END, A4 = 0;
              else if (!(A4 & R7.LAST_BOUNDARY) && o8 === g5) e14 = 0, T3("onPartBegin"), i13 = t18.HEADER_FIELD_START;
              else return;
              break;
            }
            o8 !== c15[e14 + 2] && (e14 = -2), o8 === c15[e14 + 2] && e14++;
            break;
          case t18.HEADER_FIELD_START:
            i13 = t18.HEADER_FIELD, f15("onHeaderField"), e14 = 0;
          case t18.HEADER_FIELD:
            if (o8 === N3) {
              s36("onHeaderField"), i13 = t18.HEADERS_ALMOST_DONE;
              break;
            }
            if (e14++, o8 === P2) break;
            if (o8 === C3) {
              if (e14 === 1) return;
              b4("onHeaderField", true), i13 = t18.HEADER_VALUE_START;
              break;
            }
            if (L3 = $2(o8), L3 < I4 || L3 > M4) return;
            break;
          case t18.HEADER_VALUE_START:
            if (o8 === x2) break;
            f15("onHeaderValue"), i13 = t18.HEADER_VALUE;
          case t18.HEADER_VALUE:
            o8 === N3 && (b4("onHeaderValue", true), T3("onHeaderEnd"), i13 = t18.HEADER_VALUE_ALMOST_DONE);
            break;
          case t18.HEADER_VALUE_ALMOST_DONE:
            if (o8 !== g5) return;
            i13 = t18.HEADER_FIELD_START;
            break;
          case t18.HEADERS_ALMOST_DONE:
            if (o8 !== g5) return;
            T3("onHeadersEnd"), i13 = t18.PART_DATA_START;
            break;
          case t18.PART_DATA_START:
            i13 = t18.PART_DATA, f15("onPartData");
          case t18.PART_DATA:
            if (d12 = e14, e14 === 0) {
              for (n13 += O5; n13 < y4 && !(a18[n13] in p11); ) n13 += H4;
              n13 -= O5, o8 = a18[n13];
            }
            if (e14 < c15.length) c15[e14] === o8 ? (e14 === 0 && b4("onPartData", true), e14++) : e14 = 0;
            else if (e14 === c15.length) e14++, o8 === N3 ? A4 |= R7.PART_BOUNDARY : o8 === P2 ? A4 |= R7.LAST_BOUNDARY : e14 = 0;
            else if (e14 - 1 === c15.length) if (A4 & R7.PART_BOUNDARY) {
              if (e14 = 0, o8 === g5) {
                A4 &= ~R7.PART_BOUNDARY, T3("onPartEnd"), T3("onPartBegin"), i13 = t18.HEADER_FIELD_START;
                break;
              }
            } else A4 & R7.LAST_BOUNDARY && o8 === P2 ? (T3("onPartEnd"), i13 = t18.END, A4 = 0) : e14 = 0;
            if (e14 > 0) l12[e14 - 1] = o8;
            else if (d12 > 0) {
              const h6 = new Uint8Array(l12.buffer, l12.byteOffset, l12.byteLength);
              T3("onPartData", 0, d12, h6), d12 = 0, f15("onPartData"), n13--;
            }
            break;
          case t18.END:
            break;
          default:
            throw new Error(`Unexpected state entered: ${i13}`);
        }
        b4("onHeaderField"), b4("onHeaderValue"), b4("onPartData"), this.index = e14, this.state = i13, this.flags = A4;
      }
      end() {
        if (this.state === t18.HEADER_FIELD_START && this.index === 0 || this.state === t18.PART_DATA && this.index === this.boundary.length) this.onPartEnd();
        else if (this.state !== t18.END) throw new Error("MultipartParser.end(): stream ended unexpectedly");
      }
    };
    E5(F2, "MultipartParser");
    k3 = F2;
    E5(v3, "_fileName");
    E5(Z3, "toFormData");
  }
});

// node_modules/node-fetch-native/dist/node.mjs
import vt3 from "node:http";
import Bs from "node:https";
import st2 from "node:zlib";
import me2, { PassThrough as cr2, pipeline as lt2 } from "node:stream";
import { Buffer as M5 } from "node:buffer";
import { types as dr2, promisify as ks, deprecate as hr2 } from "node:util";
import { format as qs } from "node:url";
import { isIP as Os } from "node:net";
import { statSync as ci, createReadStream as zs, promises as Is } from "node:fs";
import { basename as Fs } from "node:path";
function js(i13) {
  if (!/^data:/i.test(i13)) throw new TypeError('`uri` does not appear to be a Data URI (must begin with "data:")');
  i13 = i13.replace(/\r?\n/g, "");
  const o8 = i13.indexOf(",");
  if (o8 === -1 || o8 <= 4) throw new TypeError("malformed data: URI");
  const a18 = i13.substring(5, o8).split(";");
  let u4 = "", l12 = false;
  const p11 = a18[0] || "text/plain";
  let h6 = p11;
  for (let E6 = 1; E6 < a18.length; E6++) a18[E6] === "base64" ? l12 = true : a18[E6] && (h6 += `;${a18[E6]}`, a18[E6].indexOf("charset=") === 0 && (u4 = a18[E6].substring(8)));
  !a18[0] && !u4.length && (h6 += ";charset=US-ASCII", u4 = "US-ASCII");
  const g7 = l12 ? "base64" : "ascii", A4 = unescape(i13.substring(o8 + 1)), w11 = Buffer.from(A4, g7);
  return w11.type = p11, w11.typeFull = h6, w11.charset = u4, w11;
}
function Ls() {
  return di || (di = 1, function(i13, o8) {
    (function(a18, u4) {
      u4(o8);
    })(n10, function(a18) {
      function u4() {
      }
      n11(u4, "noop");
      function l12(e14) {
        return typeof e14 == "object" && e14 !== null || typeof e14 == "function";
      }
      n11(l12, "typeIsObject");
      const p11 = u4;
      function h6(e14, t20) {
        try {
          Object.defineProperty(e14, "name", { value: t20, configurable: true });
        } catch {
        }
      }
      n11(h6, "setFunctionName");
      const g7 = Promise, A4 = Promise.prototype.then, w11 = Promise.reject.bind(g7);
      function E6(e14) {
        return new g7(e14);
      }
      n11(E6, "newPromise");
      function T3(e14) {
        return E6((t20) => t20(e14));
      }
      n11(T3, "promiseResolvedWith");
      function b4(e14) {
        return w11(e14);
      }
      n11(b4, "promiseRejectedWith");
      function q2(e14, t20, r7) {
        return A4.call(e14, t20, r7);
      }
      n11(q2, "PerformPromiseThen");
      function _2(e14, t20, r7) {
        q2(q2(e14, t20, r7), void 0, p11);
      }
      n11(_2, "uponPromise");
      function V3(e14, t20) {
        _2(e14, t20);
      }
      n11(V3, "uponFulfillment");
      function I6(e14, t20) {
        _2(e14, void 0, t20);
      }
      n11(I6, "uponRejection");
      function F4(e14, t20, r7) {
        return q2(e14, t20, r7);
      }
      n11(F4, "transformPromiseWith");
      function Q4(e14) {
        q2(e14, void 0, p11);
      }
      n11(Q4, "setPromiseIsHandledToTrue");
      let ge3 = n11((e14) => {
        if (typeof queueMicrotask == "function") ge3 = queueMicrotask;
        else {
          const t20 = T3(void 0);
          ge3 = n11((r7) => q2(t20, r7), "_queueMicrotask");
        }
        return ge3(e14);
      }, "_queueMicrotask");
      function z2(e14, t20, r7) {
        if (typeof e14 != "function") throw new TypeError("Argument is not a function");
        return Function.prototype.apply.call(e14, t20, r7);
      }
      n11(z2, "reflectCall");
      function j3(e14, t20, r7) {
        try {
          return T3(z2(e14, t20, r7));
        } catch (s36) {
          return b4(s36);
        }
      }
      n11(j3, "promiseCall");
      const U3 = 16384, bn = class bn {
        constructor() {
          this._cursor = 0, this._size = 0, this._front = { _elements: [], _next: void 0 }, this._back = this._front, this._cursor = 0, this._size = 0;
        }
        get length() {
          return this._size;
        }
        push(t20) {
          const r7 = this._back;
          let s36 = r7;
          r7._elements.length === U3 - 1 && (s36 = { _elements: [], _next: void 0 }), r7._elements.push(t20), s36 !== r7 && (this._back = s36, r7._next = s36), ++this._size;
        }
        shift() {
          const t20 = this._front;
          let r7 = t20;
          const s36 = this._cursor;
          let f15 = s36 + 1;
          const c15 = t20._elements, d12 = c15[s36];
          return f15 === U3 && (r7 = t20._next, f15 = 0), --this._size, this._cursor = f15, t20 !== r7 && (this._front = r7), c15[s36] = void 0, d12;
        }
        forEach(t20) {
          let r7 = this._cursor, s36 = this._front, f15 = s36._elements;
          for (; (r7 !== f15.length || s36._next !== void 0) && !(r7 === f15.length && (s36 = s36._next, f15 = s36._elements, r7 = 0, f15.length === 0)); ) t20(f15[r7]), ++r7;
        }
        peek() {
          const t20 = this._front, r7 = this._cursor;
          return t20._elements[r7];
        }
      };
      n11(bn, "SimpleQueue");
      let D5 = bn;
      const Ft3 = Symbol("[[AbortSteps]]"), Qn = Symbol("[[ErrorSteps]]"), Ar = Symbol("[[CancelSteps]]"), Br = Symbol("[[PullSteps]]"), kr = Symbol("[[ReleaseSteps]]");
      function Yn(e14, t20) {
        e14._ownerReadableStream = t20, t20._reader = e14, t20._state === "readable" ? qr(e14) : t20._state === "closed" ? Li(e14) : Gn(e14, t20._storedError);
      }
      n11(Yn, "ReadableStreamReaderGenericInitialize");
      function Wr(e14, t20) {
        const r7 = e14._ownerReadableStream;
        return ie3(r7, t20);
      }
      n11(Wr, "ReadableStreamReaderGenericCancel");
      function _e3(e14) {
        const t20 = e14._ownerReadableStream;
        t20._state === "readable" ? Or(e14, new TypeError("Reader was released and can no longer be used to monitor the stream's closedness")) : $i(e14, new TypeError("Reader was released and can no longer be used to monitor the stream's closedness")), t20._readableStreamController[kr](), t20._reader = void 0, e14._ownerReadableStream = void 0;
      }
      n11(_e3, "ReadableStreamReaderGenericRelease");
      function jt3(e14) {
        return new TypeError("Cannot " + e14 + " a stream using a released reader");
      }
      n11(jt3, "readerLockException");
      function qr(e14) {
        e14._closedPromise = E6((t20, r7) => {
          e14._closedPromise_resolve = t20, e14._closedPromise_reject = r7;
        });
      }
      n11(qr, "defaultReaderClosedPromiseInitialize");
      function Gn(e14, t20) {
        qr(e14), Or(e14, t20);
      }
      n11(Gn, "defaultReaderClosedPromiseInitializeAsRejected");
      function Li(e14) {
        qr(e14), Zn(e14);
      }
      n11(Li, "defaultReaderClosedPromiseInitializeAsResolved");
      function Or(e14, t20) {
        e14._closedPromise_reject !== void 0 && (Q4(e14._closedPromise), e14._closedPromise_reject(t20), e14._closedPromise_resolve = void 0, e14._closedPromise_reject = void 0);
      }
      n11(Or, "defaultReaderClosedPromiseReject");
      function $i(e14, t20) {
        Gn(e14, t20);
      }
      n11($i, "defaultReaderClosedPromiseResetToRejected");
      function Zn(e14) {
        e14._closedPromise_resolve !== void 0 && (e14._closedPromise_resolve(void 0), e14._closedPromise_resolve = void 0, e14._closedPromise_reject = void 0);
      }
      n11(Zn, "defaultReaderClosedPromiseResolve");
      const Kn = Number.isFinite || function(e14) {
        return typeof e14 == "number" && isFinite(e14);
      }, Di = Math.trunc || function(e14) {
        return e14 < 0 ? Math.ceil(e14) : Math.floor(e14);
      };
      function Mi(e14) {
        return typeof e14 == "object" || typeof e14 == "function";
      }
      n11(Mi, "isDictionary");
      function ue4(e14, t20) {
        if (e14 !== void 0 && !Mi(e14)) throw new TypeError(`${t20} is not an object.`);
      }
      n11(ue4, "assertDictionary");
      function Z5(e14, t20) {
        if (typeof e14 != "function") throw new TypeError(`${t20} is not a function.`);
      }
      n11(Z5, "assertFunction");
      function Ui(e14) {
        return typeof e14 == "object" && e14 !== null || typeof e14 == "function";
      }
      n11(Ui, "isObject");
      function Jn(e14, t20) {
        if (!Ui(e14)) throw new TypeError(`${t20} is not an object.`);
      }
      n11(Jn, "assertObject");
      function Se2(e14, t20, r7) {
        if (e14 === void 0) throw new TypeError(`Parameter ${t20} is required in '${r7}'.`);
      }
      n11(Se2, "assertRequiredArgument");
      function zr(e14, t20, r7) {
        if (e14 === void 0) throw new TypeError(`${t20} is required in '${r7}'.`);
      }
      n11(zr, "assertRequiredField");
      function Ir2(e14) {
        return Number(e14);
      }
      n11(Ir2, "convertUnrestrictedDouble");
      function Xn(e14) {
        return e14 === 0 ? 0 : e14;
      }
      n11(Xn, "censorNegativeZero");
      function xi(e14) {
        return Xn(Di(e14));
      }
      n11(xi, "integerPart");
      function Fr(e14, t20) {
        const s36 = Number.MAX_SAFE_INTEGER;
        let f15 = Number(e14);
        if (f15 = Xn(f15), !Kn(f15)) throw new TypeError(`${t20} is not a finite number`);
        if (f15 = xi(f15), f15 < 0 || f15 > s36) throw new TypeError(`${t20} is outside the accepted range of 0 to ${s36}, inclusive`);
        return !Kn(f15) || f15 === 0 ? 0 : f15;
      }
      n11(Fr, "convertUnsignedLongLongWithEnforceRange");
      function jr2(e14, t20) {
        if (!We4(e14)) throw new TypeError(`${t20} is not a ReadableStream.`);
      }
      n11(jr2, "assertReadableStream");
      function Qe4(e14) {
        return new fe2(e14);
      }
      n11(Qe4, "AcquireReadableStreamDefaultReader");
      function eo(e14, t20) {
        e14._reader._readRequests.push(t20);
      }
      n11(eo, "ReadableStreamAddReadRequest");
      function Lr(e14, t20, r7) {
        const f15 = e14._reader._readRequests.shift();
        r7 ? f15._closeSteps() : f15._chunkSteps(t20);
      }
      n11(Lr, "ReadableStreamFulfillReadRequest");
      function Lt2(e14) {
        return e14._reader._readRequests.length;
      }
      n11(Lt2, "ReadableStreamGetNumReadRequests");
      function to(e14) {
        const t20 = e14._reader;
        return !(t20 === void 0 || !Ee2(t20));
      }
      n11(to, "ReadableStreamHasDefaultReader");
      const mn = class mn {
        constructor(t20) {
          if (Se2(t20, 1, "ReadableStreamDefaultReader"), jr2(t20, "First parameter"), qe4(t20)) throw new TypeError("This stream has already been locked for exclusive reading by another reader");
          Yn(this, t20), this._readRequests = new D5();
        }
        get closed() {
          return Ee2(this) ? this._closedPromise : b4($t2("closed"));
        }
        cancel(t20 = void 0) {
          return Ee2(this) ? this._ownerReadableStream === void 0 ? b4(jt3("cancel")) : Wr(this, t20) : b4($t2("cancel"));
        }
        read() {
          if (!Ee2(this)) return b4($t2("read"));
          if (this._ownerReadableStream === void 0) return b4(jt3("read from"));
          let t20, r7;
          const s36 = E6((c15, d12) => {
            t20 = c15, r7 = d12;
          });
          return mt2(this, { _chunkSteps: (c15) => t20({ value: c15, done: false }), _closeSteps: () => t20({ value: void 0, done: true }), _errorSteps: (c15) => r7(c15) }), s36;
        }
        releaseLock() {
          if (!Ee2(this)) throw $t2("releaseLock");
          this._ownerReadableStream !== void 0 && Ni(this);
        }
      };
      n11(mn, "ReadableStreamDefaultReader");
      let fe2 = mn;
      Object.defineProperties(fe2.prototype, { cancel: { enumerable: true }, read: { enumerable: true }, releaseLock: { enumerable: true }, closed: { enumerable: true } }), h6(fe2.prototype.cancel, "cancel"), h6(fe2.prototype.read, "read"), h6(fe2.prototype.releaseLock, "releaseLock"), typeof Symbol.toStringTag == "symbol" && Object.defineProperty(fe2.prototype, Symbol.toStringTag, { value: "ReadableStreamDefaultReader", configurable: true });
      function Ee2(e14) {
        return !l12(e14) || !Object.prototype.hasOwnProperty.call(e14, "_readRequests") ? false : e14 instanceof fe2;
      }
      n11(Ee2, "IsReadableStreamDefaultReader");
      function mt2(e14, t20) {
        const r7 = e14._ownerReadableStream;
        r7._disturbed = true, r7._state === "closed" ? t20._closeSteps() : r7._state === "errored" ? t20._errorSteps(r7._storedError) : r7._readableStreamController[Br](t20);
      }
      n11(mt2, "ReadableStreamDefaultReaderRead");
      function Ni(e14) {
        _e3(e14);
        const t20 = new TypeError("Reader was released");
        ro(e14, t20);
      }
      n11(Ni, "ReadableStreamDefaultReaderRelease");
      function ro(e14, t20) {
        const r7 = e14._readRequests;
        e14._readRequests = new D5(), r7.forEach((s36) => {
          s36._errorSteps(t20);
        });
      }
      n11(ro, "ReadableStreamDefaultReaderErrorReadRequests");
      function $t2(e14) {
        return new TypeError(`ReadableStreamDefaultReader.prototype.${e14} can only be used on a ReadableStreamDefaultReader`);
      }
      n11($t2, "defaultReaderBrandCheckException");
      const Hi = Object.getPrototypeOf(Object.getPrototypeOf(async function* () {
      }).prototype), yn = class yn {
        constructor(t20, r7) {
          this._ongoingPromise = void 0, this._isFinished = false, this._reader = t20, this._preventCancel = r7;
        }
        next() {
          const t20 = n11(() => this._nextSteps(), "nextSteps");
          return this._ongoingPromise = this._ongoingPromise ? F4(this._ongoingPromise, t20, t20) : t20(), this._ongoingPromise;
        }
        return(t20) {
          const r7 = n11(() => this._returnSteps(t20), "returnSteps");
          return this._ongoingPromise ? F4(this._ongoingPromise, r7, r7) : r7();
        }
        _nextSteps() {
          if (this._isFinished) return Promise.resolve({ value: void 0, done: true });
          const t20 = this._reader;
          let r7, s36;
          const f15 = E6((d12, m12) => {
            r7 = d12, s36 = m12;
          });
          return mt2(t20, { _chunkSteps: (d12) => {
            this._ongoingPromise = void 0, ge3(() => r7({ value: d12, done: false }));
          }, _closeSteps: () => {
            this._ongoingPromise = void 0, this._isFinished = true, _e3(t20), r7({ value: void 0, done: true });
          }, _errorSteps: (d12) => {
            this._ongoingPromise = void 0, this._isFinished = true, _e3(t20), s36(d12);
          } }), f15;
        }
        _returnSteps(t20) {
          if (this._isFinished) return Promise.resolve({ value: t20, done: true });
          this._isFinished = true;
          const r7 = this._reader;
          if (!this._preventCancel) {
            const s36 = Wr(r7, t20);
            return _e3(r7), F4(s36, () => ({ value: t20, done: true }));
          }
          return _e3(r7), T3({ value: t20, done: true });
        }
      };
      n11(yn, "ReadableStreamAsyncIteratorImpl");
      let Dt3 = yn;
      const no = { next() {
        return oo(this) ? this._asyncIteratorImpl.next() : b4(io("next"));
      }, return(e14) {
        return oo(this) ? this._asyncIteratorImpl.return(e14) : b4(io("return"));
      } };
      Object.setPrototypeOf(no, Hi);
      function Vi(e14, t20) {
        const r7 = Qe4(e14), s36 = new Dt3(r7, t20), f15 = Object.create(no);
        return f15._asyncIteratorImpl = s36, f15;
      }
      n11(Vi, "AcquireReadableStreamAsyncIterator");
      function oo(e14) {
        if (!l12(e14) || !Object.prototype.hasOwnProperty.call(e14, "_asyncIteratorImpl")) return false;
        try {
          return e14._asyncIteratorImpl instanceof Dt3;
        } catch {
          return false;
        }
      }
      n11(oo, "IsReadableStreamAsyncIterator");
      function io(e14) {
        return new TypeError(`ReadableStreamAsyncIterator.${e14} can only be used on a ReadableSteamAsyncIterator`);
      }
      n11(io, "streamAsyncIteratorBrandCheckException");
      const ao = Number.isNaN || function(e14) {
        return e14 !== e14;
      };
      var $r, Dr, Mr2;
      function yt3(e14) {
        return e14.slice();
      }
      n11(yt3, "CreateArrayFromList");
      function so(e14, t20, r7, s36, f15) {
        new Uint8Array(e14).set(new Uint8Array(r7, s36, f15), t20);
      }
      n11(so, "CopyDataBlockBytes");
      let we4 = n11((e14) => (typeof e14.transfer == "function" ? we4 = n11((t20) => t20.transfer(), "TransferArrayBuffer") : typeof structuredClone == "function" ? we4 = n11((t20) => structuredClone(t20, { transfer: [t20] }), "TransferArrayBuffer") : we4 = n11((t20) => t20, "TransferArrayBuffer"), we4(e14)), "TransferArrayBuffer"), Ae4 = n11((e14) => (typeof e14.detached == "boolean" ? Ae4 = n11((t20) => t20.detached, "IsDetachedBuffer") : Ae4 = n11((t20) => t20.byteLength === 0, "IsDetachedBuffer"), Ae4(e14)), "IsDetachedBuffer");
      function lo(e14, t20, r7) {
        if (e14.slice) return e14.slice(t20, r7);
        const s36 = r7 - t20, f15 = new ArrayBuffer(s36);
        return so(f15, 0, e14, t20, s36), f15;
      }
      n11(lo, "ArrayBufferSlice");
      function Mt3(e14, t20) {
        const r7 = e14[t20];
        if (r7 != null) {
          if (typeof r7 != "function") throw new TypeError(`${String(t20)} is not a function`);
          return r7;
        }
      }
      n11(Mt3, "GetMethod");
      function Qi(e14) {
        const t20 = { [Symbol.iterator]: () => e14.iterator }, r7 = async function* () {
          return yield* t20;
        }(), s36 = r7.next;
        return { iterator: r7, nextMethod: s36, done: false };
      }
      n11(Qi, "CreateAsyncFromSyncIterator");
      const Ur = (Mr2 = ($r = Symbol.asyncIterator) !== null && $r !== void 0 ? $r : (Dr = Symbol.for) === null || Dr === void 0 ? void 0 : Dr.call(Symbol, "Symbol.asyncIterator")) !== null && Mr2 !== void 0 ? Mr2 : "@@asyncIterator";
      function uo(e14, t20 = "sync", r7) {
        if (r7 === void 0) if (t20 === "async") {
          if (r7 = Mt3(e14, Ur), r7 === void 0) {
            const c15 = Mt3(e14, Symbol.iterator), d12 = uo(e14, "sync", c15);
            return Qi(d12);
          }
        } else r7 = Mt3(e14, Symbol.iterator);
        if (r7 === void 0) throw new TypeError("The object is not iterable");
        const s36 = z2(r7, e14, []);
        if (!l12(s36)) throw new TypeError("The iterator method must return an object");
        const f15 = s36.next;
        return { iterator: s36, nextMethod: f15, done: false };
      }
      n11(uo, "GetIterator");
      function Yi(e14) {
        const t20 = z2(e14.nextMethod, e14.iterator, []);
        if (!l12(t20)) throw new TypeError("The iterator.next() method must return an object");
        return t20;
      }
      n11(Yi, "IteratorNext");
      function Gi(e14) {
        return !!e14.done;
      }
      n11(Gi, "IteratorComplete");
      function Zi(e14) {
        return e14.value;
      }
      n11(Zi, "IteratorValue");
      function Ki(e14) {
        return !(typeof e14 != "number" || ao(e14) || e14 < 0);
      }
      n11(Ki, "IsNonNegativeNumber");
      function fo(e14) {
        const t20 = lo(e14.buffer, e14.byteOffset, e14.byteOffset + e14.byteLength);
        return new Uint8Array(t20);
      }
      n11(fo, "CloneAsUint8Array");
      function xr(e14) {
        const t20 = e14._queue.shift();
        return e14._queueTotalSize -= t20.size, e14._queueTotalSize < 0 && (e14._queueTotalSize = 0), t20.value;
      }
      n11(xr, "DequeueValue");
      function Nr(e14, t20, r7) {
        if (!Ki(r7) || r7 === 1 / 0) throw new RangeError("Size must be a finite, non-NaN, non-negative number.");
        e14._queue.push({ value: t20, size: r7 }), e14._queueTotalSize += r7;
      }
      n11(Nr, "EnqueueValueWithSize");
      function Ji(e14) {
        return e14._queue.peek().value;
      }
      n11(Ji, "PeekQueueValue");
      function Be2(e14) {
        e14._queue = new D5(), e14._queueTotalSize = 0;
      }
      n11(Be2, "ResetQueue");
      function co(e14) {
        return e14 === DataView;
      }
      n11(co, "isDataViewConstructor");
      function Xi(e14) {
        return co(e14.constructor);
      }
      n11(Xi, "isDataView");
      function ea(e14) {
        return co(e14) ? 1 : e14.BYTES_PER_ELEMENT;
      }
      n11(ea, "arrayBufferViewElementSize");
      const gn = class gn {
        constructor() {
          throw new TypeError("Illegal constructor");
        }
        get view() {
          if (!Hr(this)) throw Zr("view");
          return this._view;
        }
        respond(t20) {
          if (!Hr(this)) throw Zr("respond");
          if (Se2(t20, 1, "respond"), t20 = Fr(t20, "First parameter"), this._associatedReadableByteStreamController === void 0) throw new TypeError("This BYOB request has been invalidated");
          if (Ae4(this._view.buffer)) throw new TypeError("The BYOB request's buffer has been detached and so cannot be used as a response");
          Ht2(this._associatedReadableByteStreamController, t20);
        }
        respondWithNewView(t20) {
          if (!Hr(this)) throw Zr("respondWithNewView");
          if (Se2(t20, 1, "respondWithNewView"), !ArrayBuffer.isView(t20)) throw new TypeError("You can only respond with array buffer views");
          if (this._associatedReadableByteStreamController === void 0) throw new TypeError("This BYOB request has been invalidated");
          if (Ae4(t20.buffer)) throw new TypeError("The given view's buffer has been detached and so cannot be used as a response");
          Vt3(this._associatedReadableByteStreamController, t20);
        }
      };
      n11(gn, "ReadableStreamBYOBRequest");
      let Re4 = gn;
      Object.defineProperties(Re4.prototype, { respond: { enumerable: true }, respondWithNewView: { enumerable: true }, view: { enumerable: true } }), h6(Re4.prototype.respond, "respond"), h6(Re4.prototype.respondWithNewView, "respondWithNewView"), typeof Symbol.toStringTag == "symbol" && Object.defineProperty(Re4.prototype, Symbol.toStringTag, { value: "ReadableStreamBYOBRequest", configurable: true });
      const _n = class _n {
        constructor() {
          throw new TypeError("Illegal constructor");
        }
        get byobRequest() {
          if (!ze3(this)) throw _t3("byobRequest");
          return Gr(this);
        }
        get desiredSize() {
          if (!ze3(this)) throw _t3("desiredSize");
          return Ro(this);
        }
        close() {
          if (!ze3(this)) throw _t3("close");
          if (this._closeRequested) throw new TypeError("The stream has already been closed; do not close it again!");
          const t20 = this._controlledReadableByteStream._state;
          if (t20 !== "readable") throw new TypeError(`The stream (in ${t20} state) is not in the readable state and cannot be closed`);
          gt2(this);
        }
        enqueue(t20) {
          if (!ze3(this)) throw _t3("enqueue");
          if (Se2(t20, 1, "enqueue"), !ArrayBuffer.isView(t20)) throw new TypeError("chunk must be an array buffer view");
          if (t20.byteLength === 0) throw new TypeError("chunk must have non-zero byteLength");
          if (t20.buffer.byteLength === 0) throw new TypeError("chunk's buffer must have non-zero byteLength");
          if (this._closeRequested) throw new TypeError("stream is closed or draining");
          const r7 = this._controlledReadableByteStream._state;
          if (r7 !== "readable") throw new TypeError(`The stream (in ${r7} state) is not in the readable state and cannot be enqueued to`);
          Nt3(this, t20);
        }
        error(t20 = void 0) {
          if (!ze3(this)) throw _t3("error");
          K3(this, t20);
        }
        [Ar](t20) {
          ho(this), Be2(this);
          const r7 = this._cancelAlgorithm(t20);
          return xt2(this), r7;
        }
        [Br](t20) {
          const r7 = this._controlledReadableByteStream;
          if (this._queueTotalSize > 0) {
            wo(this, t20);
            return;
          }
          const s36 = this._autoAllocateChunkSize;
          if (s36 !== void 0) {
            let f15;
            try {
              f15 = new ArrayBuffer(s36);
            } catch (d12) {
              t20._errorSteps(d12);
              return;
            }
            const c15 = { buffer: f15, bufferByteLength: s36, byteOffset: 0, byteLength: s36, bytesFilled: 0, minimumFill: 1, elementSize: 1, viewConstructor: Uint8Array, readerType: "default" };
            this._pendingPullIntos.push(c15);
          }
          eo(r7, t20), Ie3(this);
        }
        [kr]() {
          if (this._pendingPullIntos.length > 0) {
            const t20 = this._pendingPullIntos.peek();
            t20.readerType = "none", this._pendingPullIntos = new D5(), this._pendingPullIntos.push(t20);
          }
        }
      };
      n11(_n, "ReadableByteStreamController");
      let te4 = _n;
      Object.defineProperties(te4.prototype, { close: { enumerable: true }, enqueue: { enumerable: true }, error: { enumerable: true }, byobRequest: { enumerable: true }, desiredSize: { enumerable: true } }), h6(te4.prototype.close, "close"), h6(te4.prototype.enqueue, "enqueue"), h6(te4.prototype.error, "error"), typeof Symbol.toStringTag == "symbol" && Object.defineProperty(te4.prototype, Symbol.toStringTag, { value: "ReadableByteStreamController", configurable: true });
      function ze3(e14) {
        return !l12(e14) || !Object.prototype.hasOwnProperty.call(e14, "_controlledReadableByteStream") ? false : e14 instanceof te4;
      }
      n11(ze3, "IsReadableByteStreamController");
      function Hr(e14) {
        return !l12(e14) || !Object.prototype.hasOwnProperty.call(e14, "_associatedReadableByteStreamController") ? false : e14 instanceof Re4;
      }
      n11(Hr, "IsReadableStreamBYOBRequest");
      function Ie3(e14) {
        if (!ia(e14)) return;
        if (e14._pulling) {
          e14._pullAgain = true;
          return;
        }
        e14._pulling = true;
        const r7 = e14._pullAlgorithm();
        _2(r7, () => (e14._pulling = false, e14._pullAgain && (e14._pullAgain = false, Ie3(e14)), null), (s36) => (K3(e14, s36), null));
      }
      n11(Ie3, "ReadableByteStreamControllerCallPullIfNeeded");
      function ho(e14) {
        Qr(e14), e14._pendingPullIntos = new D5();
      }
      n11(ho, "ReadableByteStreamControllerClearPendingPullIntos");
      function Vr(e14, t20) {
        let r7 = false;
        e14._state === "closed" && (r7 = true);
        const s36 = po(t20);
        t20.readerType === "default" ? Lr(e14, s36, r7) : ca(e14, s36, r7);
      }
      n11(Vr, "ReadableByteStreamControllerCommitPullIntoDescriptor");
      function po(e14) {
        const t20 = e14.bytesFilled, r7 = e14.elementSize;
        return new e14.viewConstructor(e14.buffer, e14.byteOffset, t20 / r7);
      }
      n11(po, "ReadableByteStreamControllerConvertPullIntoDescriptor");
      function Ut2(e14, t20, r7, s36) {
        e14._queue.push({ buffer: t20, byteOffset: r7, byteLength: s36 }), e14._queueTotalSize += s36;
      }
      n11(Ut2, "ReadableByteStreamControllerEnqueueChunkToQueue");
      function bo(e14, t20, r7, s36) {
        let f15;
        try {
          f15 = lo(t20, r7, r7 + s36);
        } catch (c15) {
          throw K3(e14, c15), c15;
        }
        Ut2(e14, f15, 0, s36);
      }
      n11(bo, "ReadableByteStreamControllerEnqueueClonedChunkToQueue");
      function mo(e14, t20) {
        t20.bytesFilled > 0 && bo(e14, t20.buffer, t20.byteOffset, t20.bytesFilled), Ye3(e14);
      }
      n11(mo, "ReadableByteStreamControllerEnqueueDetachedPullIntoToQueue");
      function yo(e14, t20) {
        const r7 = Math.min(e14._queueTotalSize, t20.byteLength - t20.bytesFilled), s36 = t20.bytesFilled + r7;
        let f15 = r7, c15 = false;
        const d12 = s36 % t20.elementSize, m12 = s36 - d12;
        m12 >= t20.minimumFill && (f15 = m12 - t20.bytesFilled, c15 = true);
        const R10 = e14._queue;
        for (; f15 > 0; ) {
          const y4 = R10.peek(), C5 = Math.min(f15, y4.byteLength), P4 = t20.byteOffset + t20.bytesFilled;
          so(t20.buffer, P4, y4.buffer, y4.byteOffset, C5), y4.byteLength === C5 ? R10.shift() : (y4.byteOffset += C5, y4.byteLength -= C5), e14._queueTotalSize -= C5, go(e14, C5, t20), f15 -= C5;
        }
        return c15;
      }
      n11(yo, "ReadableByteStreamControllerFillPullIntoDescriptorFromQueue");
      function go(e14, t20, r7) {
        r7.bytesFilled += t20;
      }
      n11(go, "ReadableByteStreamControllerFillHeadPullIntoDescriptor");
      function _o(e14) {
        e14._queueTotalSize === 0 && e14._closeRequested ? (xt2(e14), Pt2(e14._controlledReadableByteStream)) : Ie3(e14);
      }
      n11(_o, "ReadableByteStreamControllerHandleQueueDrain");
      function Qr(e14) {
        e14._byobRequest !== null && (e14._byobRequest._associatedReadableByteStreamController = void 0, e14._byobRequest._view = null, e14._byobRequest = null);
      }
      n11(Qr, "ReadableByteStreamControllerInvalidateBYOBRequest");
      function Yr(e14) {
        for (; e14._pendingPullIntos.length > 0; ) {
          if (e14._queueTotalSize === 0) return;
          const t20 = e14._pendingPullIntos.peek();
          yo(e14, t20) && (Ye3(e14), Vr(e14._controlledReadableByteStream, t20));
        }
      }
      n11(Yr, "ReadableByteStreamControllerProcessPullIntoDescriptorsUsingQueue");
      function ta(e14) {
        const t20 = e14._controlledReadableByteStream._reader;
        for (; t20._readRequests.length > 0; ) {
          if (e14._queueTotalSize === 0) return;
          const r7 = t20._readRequests.shift();
          wo(e14, r7);
        }
      }
      n11(ta, "ReadableByteStreamControllerProcessReadRequestsUsingQueue");
      function ra(e14, t20, r7, s36) {
        const f15 = e14._controlledReadableByteStream, c15 = t20.constructor, d12 = ea(c15), { byteOffset: m12, byteLength: R10 } = t20, y4 = r7 * d12;
        let C5;
        try {
          C5 = we4(t20.buffer);
        } catch (B4) {
          s36._errorSteps(B4);
          return;
        }
        const P4 = { buffer: C5, bufferByteLength: C5.byteLength, byteOffset: m12, byteLength: R10, bytesFilled: 0, minimumFill: y4, elementSize: d12, viewConstructor: c15, readerType: "byob" };
        if (e14._pendingPullIntos.length > 0) {
          e14._pendingPullIntos.push(P4), Po(f15, s36);
          return;
        }
        if (f15._state === "closed") {
          const B4 = new c15(P4.buffer, P4.byteOffset, 0);
          s36._closeSteps(B4);
          return;
        }
        if (e14._queueTotalSize > 0) {
          if (yo(e14, P4)) {
            const B4 = po(P4);
            _o(e14), s36._chunkSteps(B4);
            return;
          }
          if (e14._closeRequested) {
            const B4 = new TypeError("Insufficient bytes to fill elements in the given buffer");
            K3(e14, B4), s36._errorSteps(B4);
            return;
          }
        }
        e14._pendingPullIntos.push(P4), Po(f15, s36), Ie3(e14);
      }
      n11(ra, "ReadableByteStreamControllerPullInto");
      function na(e14, t20) {
        t20.readerType === "none" && Ye3(e14);
        const r7 = e14._controlledReadableByteStream;
        if (Kr(r7)) for (; vo(r7) > 0; ) {
          const s36 = Ye3(e14);
          Vr(r7, s36);
        }
      }
      n11(na, "ReadableByteStreamControllerRespondInClosedState");
      function oa(e14, t20, r7) {
        if (go(e14, t20, r7), r7.readerType === "none") {
          mo(e14, r7), Yr(e14);
          return;
        }
        if (r7.bytesFilled < r7.minimumFill) return;
        Ye3(e14);
        const s36 = r7.bytesFilled % r7.elementSize;
        if (s36 > 0) {
          const f15 = r7.byteOffset + r7.bytesFilled;
          bo(e14, r7.buffer, f15 - s36, s36);
        }
        r7.bytesFilled -= s36, Vr(e14._controlledReadableByteStream, r7), Yr(e14);
      }
      n11(oa, "ReadableByteStreamControllerRespondInReadableState");
      function So(e14, t20) {
        const r7 = e14._pendingPullIntos.peek();
        Qr(e14), e14._controlledReadableByteStream._state === "closed" ? na(e14, r7) : oa(e14, t20, r7), Ie3(e14);
      }
      n11(So, "ReadableByteStreamControllerRespondInternal");
      function Ye3(e14) {
        return e14._pendingPullIntos.shift();
      }
      n11(Ye3, "ReadableByteStreamControllerShiftPendingPullInto");
      function ia(e14) {
        const t20 = e14._controlledReadableByteStream;
        return t20._state !== "readable" || e14._closeRequested || !e14._started ? false : !!(to(t20) && Lt2(t20) > 0 || Kr(t20) && vo(t20) > 0 || Ro(e14) > 0);
      }
      n11(ia, "ReadableByteStreamControllerShouldCallPull");
      function xt2(e14) {
        e14._pullAlgorithm = void 0, e14._cancelAlgorithm = void 0;
      }
      n11(xt2, "ReadableByteStreamControllerClearAlgorithms");
      function gt2(e14) {
        const t20 = e14._controlledReadableByteStream;
        if (!(e14._closeRequested || t20._state !== "readable")) {
          if (e14._queueTotalSize > 0) {
            e14._closeRequested = true;
            return;
          }
          if (e14._pendingPullIntos.length > 0) {
            const r7 = e14._pendingPullIntos.peek();
            if (r7.bytesFilled % r7.elementSize !== 0) {
              const s36 = new TypeError("Insufficient bytes to fill elements in the given buffer");
              throw K3(e14, s36), s36;
            }
          }
          xt2(e14), Pt2(t20);
        }
      }
      n11(gt2, "ReadableByteStreamControllerClose");
      function Nt3(e14, t20) {
        const r7 = e14._controlledReadableByteStream;
        if (e14._closeRequested || r7._state !== "readable") return;
        const { buffer: s36, byteOffset: f15, byteLength: c15 } = t20;
        if (Ae4(s36)) throw new TypeError("chunk's buffer is detached and so cannot be enqueued");
        const d12 = we4(s36);
        if (e14._pendingPullIntos.length > 0) {
          const m12 = e14._pendingPullIntos.peek();
          if (Ae4(m12.buffer)) throw new TypeError("The BYOB request's buffer has been detached and so cannot be filled with an enqueued chunk");
          Qr(e14), m12.buffer = we4(m12.buffer), m12.readerType === "none" && mo(e14, m12);
        }
        if (to(r7)) if (ta(e14), Lt2(r7) === 0) Ut2(e14, d12, f15, c15);
        else {
          e14._pendingPullIntos.length > 0 && Ye3(e14);
          const m12 = new Uint8Array(d12, f15, c15);
          Lr(r7, m12, false);
        }
        else Kr(r7) ? (Ut2(e14, d12, f15, c15), Yr(e14)) : Ut2(e14, d12, f15, c15);
        Ie3(e14);
      }
      n11(Nt3, "ReadableByteStreamControllerEnqueue");
      function K3(e14, t20) {
        const r7 = e14._controlledReadableByteStream;
        r7._state === "readable" && (ho(e14), Be2(e14), xt2(e14), Zo(r7, t20));
      }
      n11(K3, "ReadableByteStreamControllerError");
      function wo(e14, t20) {
        const r7 = e14._queue.shift();
        e14._queueTotalSize -= r7.byteLength, _o(e14);
        const s36 = new Uint8Array(r7.buffer, r7.byteOffset, r7.byteLength);
        t20._chunkSteps(s36);
      }
      n11(wo, "ReadableByteStreamControllerFillReadRequestFromQueue");
      function Gr(e14) {
        if (e14._byobRequest === null && e14._pendingPullIntos.length > 0) {
          const t20 = e14._pendingPullIntos.peek(), r7 = new Uint8Array(t20.buffer, t20.byteOffset + t20.bytesFilled, t20.byteLength - t20.bytesFilled), s36 = Object.create(Re4.prototype);
          sa(s36, e14, r7), e14._byobRequest = s36;
        }
        return e14._byobRequest;
      }
      n11(Gr, "ReadableByteStreamControllerGetBYOBRequest");
      function Ro(e14) {
        const t20 = e14._controlledReadableByteStream._state;
        return t20 === "errored" ? null : t20 === "closed" ? 0 : e14._strategyHWM - e14._queueTotalSize;
      }
      n11(Ro, "ReadableByteStreamControllerGetDesiredSize");
      function Ht2(e14, t20) {
        const r7 = e14._pendingPullIntos.peek();
        if (e14._controlledReadableByteStream._state === "closed") {
          if (t20 !== 0) throw new TypeError("bytesWritten must be 0 when calling respond() on a closed stream");
        } else {
          if (t20 === 0) throw new TypeError("bytesWritten must be greater than 0 when calling respond() on a readable stream");
          if (r7.bytesFilled + t20 > r7.byteLength) throw new RangeError("bytesWritten out of range");
        }
        r7.buffer = we4(r7.buffer), So(e14, t20);
      }
      n11(Ht2, "ReadableByteStreamControllerRespond");
      function Vt3(e14, t20) {
        const r7 = e14._pendingPullIntos.peek();
        if (e14._controlledReadableByteStream._state === "closed") {
          if (t20.byteLength !== 0) throw new TypeError("The view's length must be 0 when calling respondWithNewView() on a closed stream");
        } else if (t20.byteLength === 0) throw new TypeError("The view's length must be greater than 0 when calling respondWithNewView() on a readable stream");
        if (r7.byteOffset + r7.bytesFilled !== t20.byteOffset) throw new RangeError("The region specified by view does not match byobRequest");
        if (r7.bufferByteLength !== t20.buffer.byteLength) throw new RangeError("The buffer of view has different capacity than byobRequest");
        if (r7.bytesFilled + t20.byteLength > r7.byteLength) throw new RangeError("The region specified by view is larger than byobRequest");
        const f15 = t20.byteLength;
        r7.buffer = we4(t20.buffer), So(e14, f15);
      }
      n11(Vt3, "ReadableByteStreamControllerRespondWithNewView");
      function To(e14, t20, r7, s36, f15, c15, d12) {
        t20._controlledReadableByteStream = e14, t20._pullAgain = false, t20._pulling = false, t20._byobRequest = null, t20._queue = t20._queueTotalSize = void 0, Be2(t20), t20._closeRequested = false, t20._started = false, t20._strategyHWM = c15, t20._pullAlgorithm = s36, t20._cancelAlgorithm = f15, t20._autoAllocateChunkSize = d12, t20._pendingPullIntos = new D5(), e14._readableStreamController = t20;
        const m12 = r7();
        _2(T3(m12), () => (t20._started = true, Ie3(t20), null), (R10) => (K3(t20, R10), null));
      }
      n11(To, "SetUpReadableByteStreamController");
      function aa(e14, t20, r7) {
        const s36 = Object.create(te4.prototype);
        let f15, c15, d12;
        t20.start !== void 0 ? f15 = n11(() => t20.start(s36), "startAlgorithm") : f15 = n11(() => {
        }, "startAlgorithm"), t20.pull !== void 0 ? c15 = n11(() => t20.pull(s36), "pullAlgorithm") : c15 = n11(() => T3(void 0), "pullAlgorithm"), t20.cancel !== void 0 ? d12 = n11((R10) => t20.cancel(R10), "cancelAlgorithm") : d12 = n11(() => T3(void 0), "cancelAlgorithm");
        const m12 = t20.autoAllocateChunkSize;
        if (m12 === 0) throw new TypeError("autoAllocateChunkSize must be greater than 0");
        To(e14, s36, f15, c15, d12, r7, m12);
      }
      n11(aa, "SetUpReadableByteStreamControllerFromUnderlyingSource");
      function sa(e14, t20, r7) {
        e14._associatedReadableByteStreamController = t20, e14._view = r7;
      }
      n11(sa, "SetUpReadableStreamBYOBRequest");
      function Zr(e14) {
        return new TypeError(`ReadableStreamBYOBRequest.prototype.${e14} can only be used on a ReadableStreamBYOBRequest`);
      }
      n11(Zr, "byobRequestBrandCheckException");
      function _t3(e14) {
        return new TypeError(`ReadableByteStreamController.prototype.${e14} can only be used on a ReadableByteStreamController`);
      }
      n11(_t3, "byteStreamControllerBrandCheckException");
      function la(e14, t20) {
        ue4(e14, t20);
        const r7 = e14?.mode;
        return { mode: r7 === void 0 ? void 0 : ua(r7, `${t20} has member 'mode' that`) };
      }
      n11(la, "convertReaderOptions");
      function ua(e14, t20) {
        if (e14 = `${e14}`, e14 !== "byob") throw new TypeError(`${t20} '${e14}' is not a valid enumeration value for ReadableStreamReaderMode`);
        return e14;
      }
      n11(ua, "convertReadableStreamReaderMode");
      function fa(e14, t20) {
        var r7;
        ue4(e14, t20);
        const s36 = (r7 = e14?.min) !== null && r7 !== void 0 ? r7 : 1;
        return { min: Fr(s36, `${t20} has member 'min' that`) };
      }
      n11(fa, "convertByobReadOptions");
      function Co(e14) {
        return new ce3(e14);
      }
      n11(Co, "AcquireReadableStreamBYOBReader");
      function Po(e14, t20) {
        e14._reader._readIntoRequests.push(t20);
      }
      n11(Po, "ReadableStreamAddReadIntoRequest");
      function ca(e14, t20, r7) {
        const f15 = e14._reader._readIntoRequests.shift();
        r7 ? f15._closeSteps(t20) : f15._chunkSteps(t20);
      }
      n11(ca, "ReadableStreamFulfillReadIntoRequest");
      function vo(e14) {
        return e14._reader._readIntoRequests.length;
      }
      n11(vo, "ReadableStreamGetNumReadIntoRequests");
      function Kr(e14) {
        const t20 = e14._reader;
        return !(t20 === void 0 || !Fe3(t20));
      }
      n11(Kr, "ReadableStreamHasBYOBReader");
      const Sn = class Sn {
        constructor(t20) {
          if (Se2(t20, 1, "ReadableStreamBYOBReader"), jr2(t20, "First parameter"), qe4(t20)) throw new TypeError("This stream has already been locked for exclusive reading by another reader");
          if (!ze3(t20._readableStreamController)) throw new TypeError("Cannot construct a ReadableStreamBYOBReader for a stream not constructed with a byte source");
          Yn(this, t20), this._readIntoRequests = new D5();
        }
        get closed() {
          return Fe3(this) ? this._closedPromise : b4(Qt2("closed"));
        }
        cancel(t20 = void 0) {
          return Fe3(this) ? this._ownerReadableStream === void 0 ? b4(jt3("cancel")) : Wr(this, t20) : b4(Qt2("cancel"));
        }
        read(t20, r7 = {}) {
          if (!Fe3(this)) return b4(Qt2("read"));
          if (!ArrayBuffer.isView(t20)) return b4(new TypeError("view must be an array buffer view"));
          if (t20.byteLength === 0) return b4(new TypeError("view must have non-zero byteLength"));
          if (t20.buffer.byteLength === 0) return b4(new TypeError("view's buffer must have non-zero byteLength"));
          if (Ae4(t20.buffer)) return b4(new TypeError("view's buffer has been detached"));
          let s36;
          try {
            s36 = fa(r7, "options");
          } catch (y4) {
            return b4(y4);
          }
          const f15 = s36.min;
          if (f15 === 0) return b4(new TypeError("options.min must be greater than 0"));
          if (Xi(t20)) {
            if (f15 > t20.byteLength) return b4(new RangeError("options.min must be less than or equal to view's byteLength"));
          } else if (f15 > t20.length) return b4(new RangeError("options.min must be less than or equal to view's length"));
          if (this._ownerReadableStream === void 0) return b4(jt3("read from"));
          let c15, d12;
          const m12 = E6((y4, C5) => {
            c15 = y4, d12 = C5;
          });
          return Eo(this, t20, f15, { _chunkSteps: (y4) => c15({ value: y4, done: false }), _closeSteps: (y4) => c15({ value: y4, done: true }), _errorSteps: (y4) => d12(y4) }), m12;
        }
        releaseLock() {
          if (!Fe3(this)) throw Qt2("releaseLock");
          this._ownerReadableStream !== void 0 && da(this);
        }
      };
      n11(Sn, "ReadableStreamBYOBReader");
      let ce3 = Sn;
      Object.defineProperties(ce3.prototype, { cancel: { enumerable: true }, read: { enumerable: true }, releaseLock: { enumerable: true }, closed: { enumerable: true } }), h6(ce3.prototype.cancel, "cancel"), h6(ce3.prototype.read, "read"), h6(ce3.prototype.releaseLock, "releaseLock"), typeof Symbol.toStringTag == "symbol" && Object.defineProperty(ce3.prototype, Symbol.toStringTag, { value: "ReadableStreamBYOBReader", configurable: true });
      function Fe3(e14) {
        return !l12(e14) || !Object.prototype.hasOwnProperty.call(e14, "_readIntoRequests") ? false : e14 instanceof ce3;
      }
      n11(Fe3, "IsReadableStreamBYOBReader");
      function Eo(e14, t20, r7, s36) {
        const f15 = e14._ownerReadableStream;
        f15._disturbed = true, f15._state === "errored" ? s36._errorSteps(f15._storedError) : ra(f15._readableStreamController, t20, r7, s36);
      }
      n11(Eo, "ReadableStreamBYOBReaderRead");
      function da(e14) {
        _e3(e14);
        const t20 = new TypeError("Reader was released");
        Ao(e14, t20);
      }
      n11(da, "ReadableStreamBYOBReaderRelease");
      function Ao(e14, t20) {
        const r7 = e14._readIntoRequests;
        e14._readIntoRequests = new D5(), r7.forEach((s36) => {
          s36._errorSteps(t20);
        });
      }
      n11(Ao, "ReadableStreamBYOBReaderErrorReadIntoRequests");
      function Qt2(e14) {
        return new TypeError(`ReadableStreamBYOBReader.prototype.${e14} can only be used on a ReadableStreamBYOBReader`);
      }
      n11(Qt2, "byobReaderBrandCheckException");
      function St(e14, t20) {
        const { highWaterMark: r7 } = e14;
        if (r7 === void 0) return t20;
        if (ao(r7) || r7 < 0) throw new RangeError("Invalid highWaterMark");
        return r7;
      }
      n11(St, "ExtractHighWaterMark");
      function Yt(e14) {
        const { size: t20 } = e14;
        return t20 || (() => 1);
      }
      n11(Yt, "ExtractSizeAlgorithm");
      function Gt(e14, t20) {
        ue4(e14, t20);
        const r7 = e14?.highWaterMark, s36 = e14?.size;
        return { highWaterMark: r7 === void 0 ? void 0 : Ir2(r7), size: s36 === void 0 ? void 0 : ha(s36, `${t20} has member 'size' that`) };
      }
      n11(Gt, "convertQueuingStrategy");
      function ha(e14, t20) {
        return Z5(e14, t20), (r7) => Ir2(e14(r7));
      }
      n11(ha, "convertQueuingStrategySize");
      function pa(e14, t20) {
        ue4(e14, t20);
        const r7 = e14?.abort, s36 = e14?.close, f15 = e14?.start, c15 = e14?.type, d12 = e14?.write;
        return { abort: r7 === void 0 ? void 0 : ba(r7, e14, `${t20} has member 'abort' that`), close: s36 === void 0 ? void 0 : ma(s36, e14, `${t20} has member 'close' that`), start: f15 === void 0 ? void 0 : ya(f15, e14, `${t20} has member 'start' that`), write: d12 === void 0 ? void 0 : ga(d12, e14, `${t20} has member 'write' that`), type: c15 };
      }
      n11(pa, "convertUnderlyingSink");
      function ba(e14, t20, r7) {
        return Z5(e14, r7), (s36) => j3(e14, t20, [s36]);
      }
      n11(ba, "convertUnderlyingSinkAbortCallback");
      function ma(e14, t20, r7) {
        return Z5(e14, r7), () => j3(e14, t20, []);
      }
      n11(ma, "convertUnderlyingSinkCloseCallback");
      function ya(e14, t20, r7) {
        return Z5(e14, r7), (s36) => z2(e14, t20, [s36]);
      }
      n11(ya, "convertUnderlyingSinkStartCallback");
      function ga(e14, t20, r7) {
        return Z5(e14, r7), (s36, f15) => j3(e14, t20, [s36, f15]);
      }
      n11(ga, "convertUnderlyingSinkWriteCallback");
      function Bo(e14, t20) {
        if (!Ge3(e14)) throw new TypeError(`${t20} is not a WritableStream.`);
      }
      n11(Bo, "assertWritableStream");
      function _a(e14) {
        if (typeof e14 != "object" || e14 === null) return false;
        try {
          return typeof e14.aborted == "boolean";
        } catch {
          return false;
        }
      }
      n11(_a, "isAbortSignal");
      const Sa = typeof AbortController == "function";
      function wa() {
        if (Sa) return new AbortController();
      }
      n11(wa, "createAbortController");
      const wn = class wn {
        constructor(t20 = {}, r7 = {}) {
          t20 === void 0 ? t20 = null : Jn(t20, "First parameter");
          const s36 = Gt(r7, "Second parameter"), f15 = pa(t20, "First parameter");
          if (Wo(this), f15.type !== void 0) throw new RangeError("Invalid type is specified");
          const d12 = Yt(s36), m12 = St(s36, 1);
          Ia(this, f15, m12, d12);
        }
        get locked() {
          if (!Ge3(this)) throw er3("locked");
          return Ze4(this);
        }
        abort(t20 = void 0) {
          return Ge3(this) ? Ze4(this) ? b4(new TypeError("Cannot abort a stream that already has a writer")) : Zt2(this, t20) : b4(er3("abort"));
        }
        close() {
          return Ge3(this) ? Ze4(this) ? b4(new TypeError("Cannot close a stream that already has a writer")) : he2(this) ? b4(new TypeError("Cannot close an already-closing stream")) : qo(this) : b4(er3("close"));
        }
        getWriter() {
          if (!Ge3(this)) throw er3("getWriter");
          return ko(this);
        }
      };
      n11(wn, "WritableStream");
      let de3 = wn;
      Object.defineProperties(de3.prototype, { abort: { enumerable: true }, close: { enumerable: true }, getWriter: { enumerable: true }, locked: { enumerable: true } }), h6(de3.prototype.abort, "abort"), h6(de3.prototype.close, "close"), h6(de3.prototype.getWriter, "getWriter"), typeof Symbol.toStringTag == "symbol" && Object.defineProperty(de3.prototype, Symbol.toStringTag, { value: "WritableStream", configurable: true });
      function ko(e14) {
        return new re3(e14);
      }
      n11(ko, "AcquireWritableStreamDefaultWriter");
      function Ra(e14, t20, r7, s36, f15 = 1, c15 = () => 1) {
        const d12 = Object.create(de3.prototype);
        Wo(d12);
        const m12 = Object.create(ke3.prototype);
        return Lo(d12, m12, e14, t20, r7, s36, f15, c15), d12;
      }
      n11(Ra, "CreateWritableStream");
      function Wo(e14) {
        e14._state = "writable", e14._storedError = void 0, e14._writer = void 0, e14._writableStreamController = void 0, e14._writeRequests = new D5(), e14._inFlightWriteRequest = void 0, e14._closeRequest = void 0, e14._inFlightCloseRequest = void 0, e14._pendingAbortRequest = void 0, e14._backpressure = false;
      }
      n11(Wo, "InitializeWritableStream");
      function Ge3(e14) {
        return !l12(e14) || !Object.prototype.hasOwnProperty.call(e14, "_writableStreamController") ? false : e14 instanceof de3;
      }
      n11(Ge3, "IsWritableStream");
      function Ze4(e14) {
        return e14._writer !== void 0;
      }
      n11(Ze4, "IsWritableStreamLocked");
      function Zt2(e14, t20) {
        var r7;
        if (e14._state === "closed" || e14._state === "errored") return T3(void 0);
        e14._writableStreamController._abortReason = t20, (r7 = e14._writableStreamController._abortController) === null || r7 === void 0 || r7.abort(t20);
        const s36 = e14._state;
        if (s36 === "closed" || s36 === "errored") return T3(void 0);
        if (e14._pendingAbortRequest !== void 0) return e14._pendingAbortRequest._promise;
        let f15 = false;
        s36 === "erroring" && (f15 = true, t20 = void 0);
        const c15 = E6((d12, m12) => {
          e14._pendingAbortRequest = { _promise: void 0, _resolve: d12, _reject: m12, _reason: t20, _wasAlreadyErroring: f15 };
        });
        return e14._pendingAbortRequest._promise = c15, f15 || Xr(e14, t20), c15;
      }
      n11(Zt2, "WritableStreamAbort");
      function qo(e14) {
        const t20 = e14._state;
        if (t20 === "closed" || t20 === "errored") return b4(new TypeError(`The stream (in ${t20} state) is not in the writable state and cannot be closed`));
        const r7 = E6((f15, c15) => {
          const d12 = { _resolve: f15, _reject: c15 };
          e14._closeRequest = d12;
        }), s36 = e14._writer;
        return s36 !== void 0 && e14._backpressure && t20 === "writable" && ln(s36), Fa(e14._writableStreamController), r7;
      }
      n11(qo, "WritableStreamClose");
      function Ta(e14) {
        return E6((r7, s36) => {
          const f15 = { _resolve: r7, _reject: s36 };
          e14._writeRequests.push(f15);
        });
      }
      n11(Ta, "WritableStreamAddWriteRequest");
      function Jr(e14, t20) {
        if (e14._state === "writable") {
          Xr(e14, t20);
          return;
        }
        en(e14);
      }
      n11(Jr, "WritableStreamDealWithRejection");
      function Xr(e14, t20) {
        const r7 = e14._writableStreamController;
        e14._state = "erroring", e14._storedError = t20;
        const s36 = e14._writer;
        s36 !== void 0 && zo(s36, t20), !Aa(e14) && r7._started && en(e14);
      }
      n11(Xr, "WritableStreamStartErroring");
      function en(e14) {
        e14._state = "errored", e14._writableStreamController[Qn]();
        const t20 = e14._storedError;
        if (e14._writeRequests.forEach((f15) => {
          f15._reject(t20);
        }), e14._writeRequests = new D5(), e14._pendingAbortRequest === void 0) {
          Kt2(e14);
          return;
        }
        const r7 = e14._pendingAbortRequest;
        if (e14._pendingAbortRequest = void 0, r7._wasAlreadyErroring) {
          r7._reject(t20), Kt2(e14);
          return;
        }
        const s36 = e14._writableStreamController[Ft3](r7._reason);
        _2(s36, () => (r7._resolve(), Kt2(e14), null), (f15) => (r7._reject(f15), Kt2(e14), null));
      }
      n11(en, "WritableStreamFinishErroring");
      function Ca(e14) {
        e14._inFlightWriteRequest._resolve(void 0), e14._inFlightWriteRequest = void 0;
      }
      n11(Ca, "WritableStreamFinishInFlightWrite");
      function Pa(e14, t20) {
        e14._inFlightWriteRequest._reject(t20), e14._inFlightWriteRequest = void 0, Jr(e14, t20);
      }
      n11(Pa, "WritableStreamFinishInFlightWriteWithError");
      function va(e14) {
        e14._inFlightCloseRequest._resolve(void 0), e14._inFlightCloseRequest = void 0, e14._state === "erroring" && (e14._storedError = void 0, e14._pendingAbortRequest !== void 0 && (e14._pendingAbortRequest._resolve(), e14._pendingAbortRequest = void 0)), e14._state = "closed";
        const r7 = e14._writer;
        r7 !== void 0 && Uo(r7);
      }
      n11(va, "WritableStreamFinishInFlightClose");
      function Ea(e14, t20) {
        e14._inFlightCloseRequest._reject(t20), e14._inFlightCloseRequest = void 0, e14._pendingAbortRequest !== void 0 && (e14._pendingAbortRequest._reject(t20), e14._pendingAbortRequest = void 0), Jr(e14, t20);
      }
      n11(Ea, "WritableStreamFinishInFlightCloseWithError");
      function he2(e14) {
        return !(e14._closeRequest === void 0 && e14._inFlightCloseRequest === void 0);
      }
      n11(he2, "WritableStreamCloseQueuedOrInFlight");
      function Aa(e14) {
        return !(e14._inFlightWriteRequest === void 0 && e14._inFlightCloseRequest === void 0);
      }
      n11(Aa, "WritableStreamHasOperationMarkedInFlight");
      function Ba(e14) {
        e14._inFlightCloseRequest = e14._closeRequest, e14._closeRequest = void 0;
      }
      n11(Ba, "WritableStreamMarkCloseRequestInFlight");
      function ka(e14) {
        e14._inFlightWriteRequest = e14._writeRequests.shift();
      }
      n11(ka, "WritableStreamMarkFirstWriteRequestInFlight");
      function Kt2(e14) {
        e14._closeRequest !== void 0 && (e14._closeRequest._reject(e14._storedError), e14._closeRequest = void 0);
        const t20 = e14._writer;
        t20 !== void 0 && an(t20, e14._storedError);
      }
      n11(Kt2, "WritableStreamRejectCloseAndClosedPromiseIfNeeded");
      function tn(e14, t20) {
        const r7 = e14._writer;
        r7 !== void 0 && t20 !== e14._backpressure && (t20 ? xa(r7) : ln(r7)), e14._backpressure = t20;
      }
      n11(tn, "WritableStreamUpdateBackpressure");
      const Rn = class Rn {
        constructor(t20) {
          if (Se2(t20, 1, "WritableStreamDefaultWriter"), Bo(t20, "First parameter"), Ze4(t20)) throw new TypeError("This stream has already been locked for exclusive writing by another writer");
          this._ownerWritableStream = t20, t20._writer = this;
          const r7 = t20._state;
          if (r7 === "writable") !he2(t20) && t20._backpressure ? rr2(this) : xo(this), tr2(this);
          else if (r7 === "erroring") sn(this, t20._storedError), tr2(this);
          else if (r7 === "closed") xo(this), Ma(this);
          else {
            const s36 = t20._storedError;
            sn(this, s36), Mo(this, s36);
          }
        }
        get closed() {
          return je4(this) ? this._closedPromise : b4(Le4("closed"));
        }
        get desiredSize() {
          if (!je4(this)) throw Le4("desiredSize");
          if (this._ownerWritableStream === void 0) throw Rt3("desiredSize");
          return za(this);
        }
        get ready() {
          return je4(this) ? this._readyPromise : b4(Le4("ready"));
        }
        abort(t20 = void 0) {
          return je4(this) ? this._ownerWritableStream === void 0 ? b4(Rt3("abort")) : Wa(this, t20) : b4(Le4("abort"));
        }
        close() {
          if (!je4(this)) return b4(Le4("close"));
          const t20 = this._ownerWritableStream;
          return t20 === void 0 ? b4(Rt3("close")) : he2(t20) ? b4(new TypeError("Cannot close an already-closing stream")) : Oo(this);
        }
        releaseLock() {
          if (!je4(this)) throw Le4("releaseLock");
          this._ownerWritableStream !== void 0 && Io(this);
        }
        write(t20 = void 0) {
          return je4(this) ? this._ownerWritableStream === void 0 ? b4(Rt3("write to")) : Fo(this, t20) : b4(Le4("write"));
        }
      };
      n11(Rn, "WritableStreamDefaultWriter");
      let re3 = Rn;
      Object.defineProperties(re3.prototype, { abort: { enumerable: true }, close: { enumerable: true }, releaseLock: { enumerable: true }, write: { enumerable: true }, closed: { enumerable: true }, desiredSize: { enumerable: true }, ready: { enumerable: true } }), h6(re3.prototype.abort, "abort"), h6(re3.prototype.close, "close"), h6(re3.prototype.releaseLock, "releaseLock"), h6(re3.prototype.write, "write"), typeof Symbol.toStringTag == "symbol" && Object.defineProperty(re3.prototype, Symbol.toStringTag, { value: "WritableStreamDefaultWriter", configurable: true });
      function je4(e14) {
        return !l12(e14) || !Object.prototype.hasOwnProperty.call(e14, "_ownerWritableStream") ? false : e14 instanceof re3;
      }
      n11(je4, "IsWritableStreamDefaultWriter");
      function Wa(e14, t20) {
        const r7 = e14._ownerWritableStream;
        return Zt2(r7, t20);
      }
      n11(Wa, "WritableStreamDefaultWriterAbort");
      function Oo(e14) {
        const t20 = e14._ownerWritableStream;
        return qo(t20);
      }
      n11(Oo, "WritableStreamDefaultWriterClose");
      function qa(e14) {
        const t20 = e14._ownerWritableStream, r7 = t20._state;
        return he2(t20) || r7 === "closed" ? T3(void 0) : r7 === "errored" ? b4(t20._storedError) : Oo(e14);
      }
      n11(qa, "WritableStreamDefaultWriterCloseWithErrorPropagation");
      function Oa(e14, t20) {
        e14._closedPromiseState === "pending" ? an(e14, t20) : Ua(e14, t20);
      }
      n11(Oa, "WritableStreamDefaultWriterEnsureClosedPromiseRejected");
      function zo(e14, t20) {
        e14._readyPromiseState === "pending" ? No(e14, t20) : Na(e14, t20);
      }
      n11(zo, "WritableStreamDefaultWriterEnsureReadyPromiseRejected");
      function za(e14) {
        const t20 = e14._ownerWritableStream, r7 = t20._state;
        return r7 === "errored" || r7 === "erroring" ? null : r7 === "closed" ? 0 : $o(t20._writableStreamController);
      }
      n11(za, "WritableStreamDefaultWriterGetDesiredSize");
      function Io(e14) {
        const t20 = e14._ownerWritableStream, r7 = new TypeError("Writer was released and can no longer be used to monitor the stream's closedness");
        zo(e14, r7), Oa(e14, r7), t20._writer = void 0, e14._ownerWritableStream = void 0;
      }
      n11(Io, "WritableStreamDefaultWriterRelease");
      function Fo(e14, t20) {
        const r7 = e14._ownerWritableStream, s36 = r7._writableStreamController, f15 = ja(s36, t20);
        if (r7 !== e14._ownerWritableStream) return b4(Rt3("write to"));
        const c15 = r7._state;
        if (c15 === "errored") return b4(r7._storedError);
        if (he2(r7) || c15 === "closed") return b4(new TypeError("The stream is closing or closed and cannot be written to"));
        if (c15 === "erroring") return b4(r7._storedError);
        const d12 = Ta(r7);
        return La(s36, t20, f15), d12;
      }
      n11(Fo, "WritableStreamDefaultWriterWrite");
      const jo = {}, Tn = class Tn {
        constructor() {
          throw new TypeError("Illegal constructor");
        }
        get abortReason() {
          if (!rn(this)) throw on("abortReason");
          return this._abortReason;
        }
        get signal() {
          if (!rn(this)) throw on("signal");
          if (this._abortController === void 0) throw new TypeError("WritableStreamDefaultController.prototype.signal is not supported");
          return this._abortController.signal;
        }
        error(t20 = void 0) {
          if (!rn(this)) throw on("error");
          this._controlledWritableStream._state === "writable" && Do(this, t20);
        }
        [Ft3](t20) {
          const r7 = this._abortAlgorithm(t20);
          return Jt2(this), r7;
        }
        [Qn]() {
          Be2(this);
        }
      };
      n11(Tn, "WritableStreamDefaultController");
      let ke3 = Tn;
      Object.defineProperties(ke3.prototype, { abortReason: { enumerable: true }, signal: { enumerable: true }, error: { enumerable: true } }), typeof Symbol.toStringTag == "symbol" && Object.defineProperty(ke3.prototype, Symbol.toStringTag, { value: "WritableStreamDefaultController", configurable: true });
      function rn(e14) {
        return !l12(e14) || !Object.prototype.hasOwnProperty.call(e14, "_controlledWritableStream") ? false : e14 instanceof ke3;
      }
      n11(rn, "IsWritableStreamDefaultController");
      function Lo(e14, t20, r7, s36, f15, c15, d12, m12) {
        t20._controlledWritableStream = e14, e14._writableStreamController = t20, t20._queue = void 0, t20._queueTotalSize = void 0, Be2(t20), t20._abortReason = void 0, t20._abortController = wa(), t20._started = false, t20._strategySizeAlgorithm = m12, t20._strategyHWM = d12, t20._writeAlgorithm = s36, t20._closeAlgorithm = f15, t20._abortAlgorithm = c15;
        const R10 = nn(t20);
        tn(e14, R10);
        const y4 = r7(), C5 = T3(y4);
        _2(C5, () => (t20._started = true, Xt(t20), null), (P4) => (t20._started = true, Jr(e14, P4), null));
      }
      n11(Lo, "SetUpWritableStreamDefaultController");
      function Ia(e14, t20, r7, s36) {
        const f15 = Object.create(ke3.prototype);
        let c15, d12, m12, R10;
        t20.start !== void 0 ? c15 = n11(() => t20.start(f15), "startAlgorithm") : c15 = n11(() => {
        }, "startAlgorithm"), t20.write !== void 0 ? d12 = n11((y4) => t20.write(y4, f15), "writeAlgorithm") : d12 = n11(() => T3(void 0), "writeAlgorithm"), t20.close !== void 0 ? m12 = n11(() => t20.close(), "closeAlgorithm") : m12 = n11(() => T3(void 0), "closeAlgorithm"), t20.abort !== void 0 ? R10 = n11((y4) => t20.abort(y4), "abortAlgorithm") : R10 = n11(() => T3(void 0), "abortAlgorithm"), Lo(e14, f15, c15, d12, m12, R10, r7, s36);
      }
      n11(Ia, "SetUpWritableStreamDefaultControllerFromUnderlyingSink");
      function Jt2(e14) {
        e14._writeAlgorithm = void 0, e14._closeAlgorithm = void 0, e14._abortAlgorithm = void 0, e14._strategySizeAlgorithm = void 0;
      }
      n11(Jt2, "WritableStreamDefaultControllerClearAlgorithms");
      function Fa(e14) {
        Nr(e14, jo, 0), Xt(e14);
      }
      n11(Fa, "WritableStreamDefaultControllerClose");
      function ja(e14, t20) {
        try {
          return e14._strategySizeAlgorithm(t20);
        } catch (r7) {
          return wt2(e14, r7), 1;
        }
      }
      n11(ja, "WritableStreamDefaultControllerGetChunkSize");
      function $o(e14) {
        return e14._strategyHWM - e14._queueTotalSize;
      }
      n11($o, "WritableStreamDefaultControllerGetDesiredSize");
      function La(e14, t20, r7) {
        try {
          Nr(e14, t20, r7);
        } catch (f15) {
          wt2(e14, f15);
          return;
        }
        const s36 = e14._controlledWritableStream;
        if (!he2(s36) && s36._state === "writable") {
          const f15 = nn(e14);
          tn(s36, f15);
        }
        Xt(e14);
      }
      n11(La, "WritableStreamDefaultControllerWrite");
      function Xt(e14) {
        const t20 = e14._controlledWritableStream;
        if (!e14._started || t20._inFlightWriteRequest !== void 0) return;
        if (t20._state === "erroring") {
          en(t20);
          return;
        }
        if (e14._queue.length === 0) return;
        const s36 = Ji(e14);
        s36 === jo ? $a(e14) : Da(e14, s36);
      }
      n11(Xt, "WritableStreamDefaultControllerAdvanceQueueIfNeeded");
      function wt2(e14, t20) {
        e14._controlledWritableStream._state === "writable" && Do(e14, t20);
      }
      n11(wt2, "WritableStreamDefaultControllerErrorIfNeeded");
      function $a(e14) {
        const t20 = e14._controlledWritableStream;
        Ba(t20), xr(e14);
        const r7 = e14._closeAlgorithm();
        Jt2(e14), _2(r7, () => (va(t20), null), (s36) => (Ea(t20, s36), null));
      }
      n11($a, "WritableStreamDefaultControllerProcessClose");
      function Da(e14, t20) {
        const r7 = e14._controlledWritableStream;
        ka(r7);
        const s36 = e14._writeAlgorithm(t20);
        _2(s36, () => {
          Ca(r7);
          const f15 = r7._state;
          if (xr(e14), !he2(r7) && f15 === "writable") {
            const c15 = nn(e14);
            tn(r7, c15);
          }
          return Xt(e14), null;
        }, (f15) => (r7._state === "writable" && Jt2(e14), Pa(r7, f15), null));
      }
      n11(Da, "WritableStreamDefaultControllerProcessWrite");
      function nn(e14) {
        return $o(e14) <= 0;
      }
      n11(nn, "WritableStreamDefaultControllerGetBackpressure");
      function Do(e14, t20) {
        const r7 = e14._controlledWritableStream;
        Jt2(e14), Xr(r7, t20);
      }
      n11(Do, "WritableStreamDefaultControllerError");
      function er3(e14) {
        return new TypeError(`WritableStream.prototype.${e14} can only be used on a WritableStream`);
      }
      n11(er3, "streamBrandCheckException$2");
      function on(e14) {
        return new TypeError(`WritableStreamDefaultController.prototype.${e14} can only be used on a WritableStreamDefaultController`);
      }
      n11(on, "defaultControllerBrandCheckException$2");
      function Le4(e14) {
        return new TypeError(`WritableStreamDefaultWriter.prototype.${e14} can only be used on a WritableStreamDefaultWriter`);
      }
      n11(Le4, "defaultWriterBrandCheckException");
      function Rt3(e14) {
        return new TypeError("Cannot " + e14 + " a stream using a released writer");
      }
      n11(Rt3, "defaultWriterLockException");
      function tr2(e14) {
        e14._closedPromise = E6((t20, r7) => {
          e14._closedPromise_resolve = t20, e14._closedPromise_reject = r7, e14._closedPromiseState = "pending";
        });
      }
      n11(tr2, "defaultWriterClosedPromiseInitialize");
      function Mo(e14, t20) {
        tr2(e14), an(e14, t20);
      }
      n11(Mo, "defaultWriterClosedPromiseInitializeAsRejected");
      function Ma(e14) {
        tr2(e14), Uo(e14);
      }
      n11(Ma, "defaultWriterClosedPromiseInitializeAsResolved");
      function an(e14, t20) {
        e14._closedPromise_reject !== void 0 && (Q4(e14._closedPromise), e14._closedPromise_reject(t20), e14._closedPromise_resolve = void 0, e14._closedPromise_reject = void 0, e14._closedPromiseState = "rejected");
      }
      n11(an, "defaultWriterClosedPromiseReject");
      function Ua(e14, t20) {
        Mo(e14, t20);
      }
      n11(Ua, "defaultWriterClosedPromiseResetToRejected");
      function Uo(e14) {
        e14._closedPromise_resolve !== void 0 && (e14._closedPromise_resolve(void 0), e14._closedPromise_resolve = void 0, e14._closedPromise_reject = void 0, e14._closedPromiseState = "resolved");
      }
      n11(Uo, "defaultWriterClosedPromiseResolve");
      function rr2(e14) {
        e14._readyPromise = E6((t20, r7) => {
          e14._readyPromise_resolve = t20, e14._readyPromise_reject = r7;
        }), e14._readyPromiseState = "pending";
      }
      n11(rr2, "defaultWriterReadyPromiseInitialize");
      function sn(e14, t20) {
        rr2(e14), No(e14, t20);
      }
      n11(sn, "defaultWriterReadyPromiseInitializeAsRejected");
      function xo(e14) {
        rr2(e14), ln(e14);
      }
      n11(xo, "defaultWriterReadyPromiseInitializeAsResolved");
      function No(e14, t20) {
        e14._readyPromise_reject !== void 0 && (Q4(e14._readyPromise), e14._readyPromise_reject(t20), e14._readyPromise_resolve = void 0, e14._readyPromise_reject = void 0, e14._readyPromiseState = "rejected");
      }
      n11(No, "defaultWriterReadyPromiseReject");
      function xa(e14) {
        rr2(e14);
      }
      n11(xa, "defaultWriterReadyPromiseReset");
      function Na(e14, t20) {
        sn(e14, t20);
      }
      n11(Na, "defaultWriterReadyPromiseResetToRejected");
      function ln(e14) {
        e14._readyPromise_resolve !== void 0 && (e14._readyPromise_resolve(void 0), e14._readyPromise_resolve = void 0, e14._readyPromise_reject = void 0, e14._readyPromiseState = "fulfilled");
      }
      n11(ln, "defaultWriterReadyPromiseResolve");
      function Ha() {
        if (typeof globalThis < "u") return globalThis;
        if (typeof self < "u") return self;
        if (typeof n10 < "u") return n10;
      }
      n11(Ha, "getGlobals");
      const un = Ha();
      function Va(e14) {
        if (!(typeof e14 == "function" || typeof e14 == "object") || e14.name !== "DOMException") return false;
        try {
          return new e14(), true;
        } catch {
          return false;
        }
      }
      n11(Va, "isDOMExceptionConstructor");
      function Qa() {
        const e14 = un?.DOMException;
        return Va(e14) ? e14 : void 0;
      }
      n11(Qa, "getFromGlobal");
      function Ya() {
        const e14 = n11(function(r7, s36) {
          this.message = r7 || "", this.name = s36 || "Error", Error.captureStackTrace && Error.captureStackTrace(this, this.constructor);
        }, "DOMException");
        return h6(e14, "DOMException"), e14.prototype = Object.create(Error.prototype), Object.defineProperty(e14.prototype, "constructor", { value: e14, writable: true, configurable: true }), e14;
      }
      n11(Ya, "createPolyfill");
      const Ga = Qa() || Ya();
      function Ho(e14, t20, r7, s36, f15, c15) {
        const d12 = Qe4(e14), m12 = ko(t20);
        e14._disturbed = true;
        let R10 = false, y4 = T3(void 0);
        return E6((C5, P4) => {
          let B4;
          if (c15 !== void 0) {
            if (B4 = n11(() => {
              const S2 = c15.reason !== void 0 ? c15.reason : new Ga("Aborted", "AbortError"), v5 = [];
              s36 || v5.push(() => t20._state === "writable" ? Zt2(t20, S2) : T3(void 0)), f15 || v5.push(() => e14._state === "readable" ? ie3(e14, S2) : T3(void 0)), N4(() => Promise.all(v5.map((k5) => k5())), true, S2);
            }, "abortAlgorithm"), c15.aborted) {
              B4();
              return;
            }
            c15.addEventListener("abort", B4);
          }
          function ae3() {
            return E6((S2, v5) => {
              function k5(Y3) {
                Y3 ? S2() : q2(nt3(), k5, v5);
              }
              n11(k5, "next"), k5(false);
            });
          }
          n11(ae3, "pipeLoop");
          function nt3() {
            return R10 ? T3(true) : q2(m12._readyPromise, () => E6((S2, v5) => {
              mt2(d12, { _chunkSteps: (k5) => {
                y4 = q2(Fo(m12, k5), void 0, u4), S2(false);
              }, _closeSteps: () => S2(true), _errorSteps: v5 });
            }));
          }
          if (n11(nt3, "pipeStep"), Te4(e14, d12._closedPromise, (S2) => (s36 ? J2(true, S2) : N4(() => Zt2(t20, S2), true, S2), null)), Te4(t20, m12._closedPromise, (S2) => (f15 ? J2(true, S2) : N4(() => ie3(e14, S2), true, S2), null)), x3(e14, d12._closedPromise, () => (r7 ? J2() : N4(() => qa(m12)), null)), he2(t20) || t20._state === "closed") {
            const S2 = new TypeError("the destination writable stream closed before all data could be piped to it");
            f15 ? J2(true, S2) : N4(() => ie3(e14, S2), true, S2);
          }
          Q4(ae3());
          function Oe3() {
            const S2 = y4;
            return q2(y4, () => S2 !== y4 ? Oe3() : void 0);
          }
          n11(Oe3, "waitForWritesToFinish");
          function Te4(S2, v5, k5) {
            S2._state === "errored" ? k5(S2._storedError) : I6(v5, k5);
          }
          n11(Te4, "isOrBecomesErrored");
          function x3(S2, v5, k5) {
            S2._state === "closed" ? k5() : V3(v5, k5);
          }
          n11(x3, "isOrBecomesClosed");
          function N4(S2, v5, k5) {
            if (R10) return;
            R10 = true, t20._state === "writable" && !he2(t20) ? V3(Oe3(), Y3) : Y3();
            function Y3() {
              return _2(S2(), () => Ce4(v5, k5), (ot2) => Ce4(true, ot2)), null;
            }
            n11(Y3, "doTheRest");
          }
          n11(N4, "shutdownWithAction");
          function J2(S2, v5) {
            R10 || (R10 = true, t20._state === "writable" && !he2(t20) ? V3(Oe3(), () => Ce4(S2, v5)) : Ce4(S2, v5));
          }
          n11(J2, "shutdown");
          function Ce4(S2, v5) {
            return Io(m12), _e3(d12), c15 !== void 0 && c15.removeEventListener("abort", B4), S2 ? P4(v5) : C5(void 0), null;
          }
          n11(Ce4, "finalize");
        });
      }
      n11(Ho, "ReadableStreamPipeTo");
      const Cn = class Cn {
        constructor() {
          throw new TypeError("Illegal constructor");
        }
        get desiredSize() {
          if (!nr(this)) throw ir2("desiredSize");
          return fn(this);
        }
        close() {
          if (!nr(this)) throw ir2("close");
          if (!Je3(this)) throw new TypeError("The stream is not in a state that permits close");
          $e4(this);
        }
        enqueue(t20 = void 0) {
          if (!nr(this)) throw ir2("enqueue");
          if (!Je3(this)) throw new TypeError("The stream is not in a state that permits enqueue");
          return Ke4(this, t20);
        }
        error(t20 = void 0) {
          if (!nr(this)) throw ir2("error");
          oe2(this, t20);
        }
        [Ar](t20) {
          Be2(this);
          const r7 = this._cancelAlgorithm(t20);
          return or4(this), r7;
        }
        [Br](t20) {
          const r7 = this._controlledReadableStream;
          if (this._queue.length > 0) {
            const s36 = xr(this);
            this._closeRequested && this._queue.length === 0 ? (or4(this), Pt2(r7)) : Tt3(this), t20._chunkSteps(s36);
          } else eo(r7, t20), Tt3(this);
        }
        [kr]() {
        }
      };
      n11(Cn, "ReadableStreamDefaultController");
      let ne3 = Cn;
      Object.defineProperties(ne3.prototype, { close: { enumerable: true }, enqueue: { enumerable: true }, error: { enumerable: true }, desiredSize: { enumerable: true } }), h6(ne3.prototype.close, "close"), h6(ne3.prototype.enqueue, "enqueue"), h6(ne3.prototype.error, "error"), typeof Symbol.toStringTag == "symbol" && Object.defineProperty(ne3.prototype, Symbol.toStringTag, { value: "ReadableStreamDefaultController", configurable: true });
      function nr(e14) {
        return !l12(e14) || !Object.prototype.hasOwnProperty.call(e14, "_controlledReadableStream") ? false : e14 instanceof ne3;
      }
      n11(nr, "IsReadableStreamDefaultController");
      function Tt3(e14) {
        if (!Vo(e14)) return;
        if (e14._pulling) {
          e14._pullAgain = true;
          return;
        }
        e14._pulling = true;
        const r7 = e14._pullAlgorithm();
        _2(r7, () => (e14._pulling = false, e14._pullAgain && (e14._pullAgain = false, Tt3(e14)), null), (s36) => (oe2(e14, s36), null));
      }
      n11(Tt3, "ReadableStreamDefaultControllerCallPullIfNeeded");
      function Vo(e14) {
        const t20 = e14._controlledReadableStream;
        return !Je3(e14) || !e14._started ? false : !!(qe4(t20) && Lt2(t20) > 0 || fn(e14) > 0);
      }
      n11(Vo, "ReadableStreamDefaultControllerShouldCallPull");
      function or4(e14) {
        e14._pullAlgorithm = void 0, e14._cancelAlgorithm = void 0, e14._strategySizeAlgorithm = void 0;
      }
      n11(or4, "ReadableStreamDefaultControllerClearAlgorithms");
      function $e4(e14) {
        if (!Je3(e14)) return;
        const t20 = e14._controlledReadableStream;
        e14._closeRequested = true, e14._queue.length === 0 && (or4(e14), Pt2(t20));
      }
      n11($e4, "ReadableStreamDefaultControllerClose");
      function Ke4(e14, t20) {
        if (!Je3(e14)) return;
        const r7 = e14._controlledReadableStream;
        if (qe4(r7) && Lt2(r7) > 0) Lr(r7, t20, false);
        else {
          let s36;
          try {
            s36 = e14._strategySizeAlgorithm(t20);
          } catch (f15) {
            throw oe2(e14, f15), f15;
          }
          try {
            Nr(e14, t20, s36);
          } catch (f15) {
            throw oe2(e14, f15), f15;
          }
        }
        Tt3(e14);
      }
      n11(Ke4, "ReadableStreamDefaultControllerEnqueue");
      function oe2(e14, t20) {
        const r7 = e14._controlledReadableStream;
        r7._state === "readable" && (Be2(e14), or4(e14), Zo(r7, t20));
      }
      n11(oe2, "ReadableStreamDefaultControllerError");
      function fn(e14) {
        const t20 = e14._controlledReadableStream._state;
        return t20 === "errored" ? null : t20 === "closed" ? 0 : e14._strategyHWM - e14._queueTotalSize;
      }
      n11(fn, "ReadableStreamDefaultControllerGetDesiredSize");
      function Za(e14) {
        return !Vo(e14);
      }
      n11(Za, "ReadableStreamDefaultControllerHasBackpressure");
      function Je3(e14) {
        const t20 = e14._controlledReadableStream._state;
        return !e14._closeRequested && t20 === "readable";
      }
      n11(Je3, "ReadableStreamDefaultControllerCanCloseOrEnqueue");
      function Qo(e14, t20, r7, s36, f15, c15, d12) {
        t20._controlledReadableStream = e14, t20._queue = void 0, t20._queueTotalSize = void 0, Be2(t20), t20._started = false, t20._closeRequested = false, t20._pullAgain = false, t20._pulling = false, t20._strategySizeAlgorithm = d12, t20._strategyHWM = c15, t20._pullAlgorithm = s36, t20._cancelAlgorithm = f15, e14._readableStreamController = t20;
        const m12 = r7();
        _2(T3(m12), () => (t20._started = true, Tt3(t20), null), (R10) => (oe2(t20, R10), null));
      }
      n11(Qo, "SetUpReadableStreamDefaultController");
      function Ka(e14, t20, r7, s36) {
        const f15 = Object.create(ne3.prototype);
        let c15, d12, m12;
        t20.start !== void 0 ? c15 = n11(() => t20.start(f15), "startAlgorithm") : c15 = n11(() => {
        }, "startAlgorithm"), t20.pull !== void 0 ? d12 = n11(() => t20.pull(f15), "pullAlgorithm") : d12 = n11(() => T3(void 0), "pullAlgorithm"), t20.cancel !== void 0 ? m12 = n11((R10) => t20.cancel(R10), "cancelAlgorithm") : m12 = n11(() => T3(void 0), "cancelAlgorithm"), Qo(e14, f15, c15, d12, m12, r7, s36);
      }
      n11(Ka, "SetUpReadableStreamDefaultControllerFromUnderlyingSource");
      function ir2(e14) {
        return new TypeError(`ReadableStreamDefaultController.prototype.${e14} can only be used on a ReadableStreamDefaultController`);
      }
      n11(ir2, "defaultControllerBrandCheckException$1");
      function Ja(e14, t20) {
        return ze3(e14._readableStreamController) ? es(e14) : Xa(e14);
      }
      n11(Ja, "ReadableStreamTee");
      function Xa(e14, t20) {
        const r7 = Qe4(e14);
        let s36 = false, f15 = false, c15 = false, d12 = false, m12, R10, y4, C5, P4;
        const B4 = E6((x3) => {
          P4 = x3;
        });
        function ae3() {
          return s36 ? (f15 = true, T3(void 0)) : (s36 = true, mt2(r7, { _chunkSteps: (N4) => {
            ge3(() => {
              f15 = false;
              const J2 = N4, Ce4 = N4;
              c15 || Ke4(y4._readableStreamController, J2), d12 || Ke4(C5._readableStreamController, Ce4), s36 = false, f15 && ae3();
            });
          }, _closeSteps: () => {
            s36 = false, c15 || $e4(y4._readableStreamController), d12 || $e4(C5._readableStreamController), (!c15 || !d12) && P4(void 0);
          }, _errorSteps: () => {
            s36 = false;
          } }), T3(void 0));
        }
        n11(ae3, "pullAlgorithm");
        function nt3(x3) {
          if (c15 = true, m12 = x3, d12) {
            const N4 = yt3([m12, R10]), J2 = ie3(e14, N4);
            P4(J2);
          }
          return B4;
        }
        n11(nt3, "cancel1Algorithm");
        function Oe3(x3) {
          if (d12 = true, R10 = x3, c15) {
            const N4 = yt3([m12, R10]), J2 = ie3(e14, N4);
            P4(J2);
          }
          return B4;
        }
        n11(Oe3, "cancel2Algorithm");
        function Te4() {
        }
        return n11(Te4, "startAlgorithm"), y4 = Ct3(Te4, ae3, nt3), C5 = Ct3(Te4, ae3, Oe3), I6(r7._closedPromise, (x3) => (oe2(y4._readableStreamController, x3), oe2(C5._readableStreamController, x3), (!c15 || !d12) && P4(void 0), null)), [y4, C5];
      }
      n11(Xa, "ReadableStreamDefaultTee");
      function es(e14) {
        let t20 = Qe4(e14), r7 = false, s36 = false, f15 = false, c15 = false, d12 = false, m12, R10, y4, C5, P4;
        const B4 = E6((S2) => {
          P4 = S2;
        });
        function ae3(S2) {
          I6(S2._closedPromise, (v5) => (S2 !== t20 || (K3(y4._readableStreamController, v5), K3(C5._readableStreamController, v5), (!c15 || !d12) && P4(void 0)), null));
        }
        n11(ae3, "forwardReaderError");
        function nt3() {
          Fe3(t20) && (_e3(t20), t20 = Qe4(e14), ae3(t20)), mt2(t20, { _chunkSteps: (v5) => {
            ge3(() => {
              s36 = false, f15 = false;
              const k5 = v5;
              let Y3 = v5;
              if (!c15 && !d12) try {
                Y3 = fo(v5);
              } catch (ot2) {
                K3(y4._readableStreamController, ot2), K3(C5._readableStreamController, ot2), P4(ie3(e14, ot2));
                return;
              }
              c15 || Nt3(y4._readableStreamController, k5), d12 || Nt3(C5._readableStreamController, Y3), r7 = false, s36 ? Te4() : f15 && x3();
            });
          }, _closeSteps: () => {
            r7 = false, c15 || gt2(y4._readableStreamController), d12 || gt2(C5._readableStreamController), y4._readableStreamController._pendingPullIntos.length > 0 && Ht2(y4._readableStreamController, 0), C5._readableStreamController._pendingPullIntos.length > 0 && Ht2(C5._readableStreamController, 0), (!c15 || !d12) && P4(void 0);
          }, _errorSteps: () => {
            r7 = false;
          } });
        }
        n11(nt3, "pullWithDefaultReader");
        function Oe3(S2, v5) {
          Ee2(t20) && (_e3(t20), t20 = Co(e14), ae3(t20));
          const k5 = v5 ? C5 : y4, Y3 = v5 ? y4 : C5;
          Eo(t20, S2, 1, { _chunkSteps: (it) => {
            ge3(() => {
              s36 = false, f15 = false;
              const at2 = v5 ? d12 : c15;
              if (v5 ? c15 : d12) at2 || Vt3(k5._readableStreamController, it);
              else {
                let ui;
                try {
                  ui = fo(it);
                } catch (kn) {
                  K3(k5._readableStreamController, kn), K3(Y3._readableStreamController, kn), P4(ie3(e14, kn));
                  return;
                }
                at2 || Vt3(k5._readableStreamController, it), Nt3(Y3._readableStreamController, ui);
              }
              r7 = false, s36 ? Te4() : f15 && x3();
            });
          }, _closeSteps: (it) => {
            r7 = false;
            const at2 = v5 ? d12 : c15, fr2 = v5 ? c15 : d12;
            at2 || gt2(k5._readableStreamController), fr2 || gt2(Y3._readableStreamController), it !== void 0 && (at2 || Vt3(k5._readableStreamController, it), !fr2 && Y3._readableStreamController._pendingPullIntos.length > 0 && Ht2(Y3._readableStreamController, 0)), (!at2 || !fr2) && P4(void 0);
          }, _errorSteps: () => {
            r7 = false;
          } });
        }
        n11(Oe3, "pullWithBYOBReader");
        function Te4() {
          if (r7) return s36 = true, T3(void 0);
          r7 = true;
          const S2 = Gr(y4._readableStreamController);
          return S2 === null ? nt3() : Oe3(S2._view, false), T3(void 0);
        }
        n11(Te4, "pull1Algorithm");
        function x3() {
          if (r7) return f15 = true, T3(void 0);
          r7 = true;
          const S2 = Gr(C5._readableStreamController);
          return S2 === null ? nt3() : Oe3(S2._view, true), T3(void 0);
        }
        n11(x3, "pull2Algorithm");
        function N4(S2) {
          if (c15 = true, m12 = S2, d12) {
            const v5 = yt3([m12, R10]), k5 = ie3(e14, v5);
            P4(k5);
          }
          return B4;
        }
        n11(N4, "cancel1Algorithm");
        function J2(S2) {
          if (d12 = true, R10 = S2, c15) {
            const v5 = yt3([m12, R10]), k5 = ie3(e14, v5);
            P4(k5);
          }
          return B4;
        }
        n11(J2, "cancel2Algorithm");
        function Ce4() {
        }
        return n11(Ce4, "startAlgorithm"), y4 = Go(Ce4, Te4, N4), C5 = Go(Ce4, x3, J2), ae3(t20), [y4, C5];
      }
      n11(es, "ReadableByteStreamTee");
      function ts(e14) {
        return l12(e14) && typeof e14.getReader < "u";
      }
      n11(ts, "isReadableStreamLike");
      function rs(e14) {
        return ts(e14) ? os(e14.getReader()) : ns(e14);
      }
      n11(rs, "ReadableStreamFrom");
      function ns(e14) {
        let t20;
        const r7 = uo(e14, "async"), s36 = u4;
        function f15() {
          let d12;
          try {
            d12 = Yi(r7);
          } catch (R10) {
            return b4(R10);
          }
          const m12 = T3(d12);
          return F4(m12, (R10) => {
            if (!l12(R10)) throw new TypeError("The promise returned by the iterator.next() method must fulfill with an object");
            if (Gi(R10)) $e4(t20._readableStreamController);
            else {
              const C5 = Zi(R10);
              Ke4(t20._readableStreamController, C5);
            }
          });
        }
        n11(f15, "pullAlgorithm");
        function c15(d12) {
          const m12 = r7.iterator;
          let R10;
          try {
            R10 = Mt3(m12, "return");
          } catch (P4) {
            return b4(P4);
          }
          if (R10 === void 0) return T3(void 0);
          let y4;
          try {
            y4 = z2(R10, m12, [d12]);
          } catch (P4) {
            return b4(P4);
          }
          const C5 = T3(y4);
          return F4(C5, (P4) => {
            if (!l12(P4)) throw new TypeError("The promise returned by the iterator.return() method must fulfill with an object");
          });
        }
        return n11(c15, "cancelAlgorithm"), t20 = Ct3(s36, f15, c15, 0), t20;
      }
      n11(ns, "ReadableStreamFromIterable");
      function os(e14) {
        let t20;
        const r7 = u4;
        function s36() {
          let c15;
          try {
            c15 = e14.read();
          } catch (d12) {
            return b4(d12);
          }
          return F4(c15, (d12) => {
            if (!l12(d12)) throw new TypeError("The promise returned by the reader.read() method must fulfill with an object");
            if (d12.done) $e4(t20._readableStreamController);
            else {
              const m12 = d12.value;
              Ke4(t20._readableStreamController, m12);
            }
          });
        }
        n11(s36, "pullAlgorithm");
        function f15(c15) {
          try {
            return T3(e14.cancel(c15));
          } catch (d12) {
            return b4(d12);
          }
        }
        return n11(f15, "cancelAlgorithm"), t20 = Ct3(r7, s36, f15, 0), t20;
      }
      n11(os, "ReadableStreamFromDefaultReader");
      function is(e14, t20) {
        ue4(e14, t20);
        const r7 = e14, s36 = r7?.autoAllocateChunkSize, f15 = r7?.cancel, c15 = r7?.pull, d12 = r7?.start, m12 = r7?.type;
        return { autoAllocateChunkSize: s36 === void 0 ? void 0 : Fr(s36, `${t20} has member 'autoAllocateChunkSize' that`), cancel: f15 === void 0 ? void 0 : as(f15, r7, `${t20} has member 'cancel' that`), pull: c15 === void 0 ? void 0 : ss(c15, r7, `${t20} has member 'pull' that`), start: d12 === void 0 ? void 0 : ls(d12, r7, `${t20} has member 'start' that`), type: m12 === void 0 ? void 0 : us(m12, `${t20} has member 'type' that`) };
      }
      n11(is, "convertUnderlyingDefaultOrByteSource");
      function as(e14, t20, r7) {
        return Z5(e14, r7), (s36) => j3(e14, t20, [s36]);
      }
      n11(as, "convertUnderlyingSourceCancelCallback");
      function ss(e14, t20, r7) {
        return Z5(e14, r7), (s36) => j3(e14, t20, [s36]);
      }
      n11(ss, "convertUnderlyingSourcePullCallback");
      function ls(e14, t20, r7) {
        return Z5(e14, r7), (s36) => z2(e14, t20, [s36]);
      }
      n11(ls, "convertUnderlyingSourceStartCallback");
      function us(e14, t20) {
        if (e14 = `${e14}`, e14 !== "bytes") throw new TypeError(`${t20} '${e14}' is not a valid enumeration value for ReadableStreamType`);
        return e14;
      }
      n11(us, "convertReadableStreamType");
      function fs(e14, t20) {
        return ue4(e14, t20), { preventCancel: !!e14?.preventCancel };
      }
      n11(fs, "convertIteratorOptions");
      function Yo(e14, t20) {
        ue4(e14, t20);
        const r7 = e14?.preventAbort, s36 = e14?.preventCancel, f15 = e14?.preventClose, c15 = e14?.signal;
        return c15 !== void 0 && cs(c15, `${t20} has member 'signal' that`), { preventAbort: !!r7, preventCancel: !!s36, preventClose: !!f15, signal: c15 };
      }
      n11(Yo, "convertPipeOptions");
      function cs(e14, t20) {
        if (!_a(e14)) throw new TypeError(`${t20} is not an AbortSignal.`);
      }
      n11(cs, "assertAbortSignal");
      function ds(e14, t20) {
        ue4(e14, t20);
        const r7 = e14?.readable;
        zr(r7, "readable", "ReadableWritablePair"), jr2(r7, `${t20} has member 'readable' that`);
        const s36 = e14?.writable;
        return zr(s36, "writable", "ReadableWritablePair"), Bo(s36, `${t20} has member 'writable' that`), { readable: r7, writable: s36 };
      }
      n11(ds, "convertReadableWritablePair");
      const Pn = class Pn {
        constructor(t20 = {}, r7 = {}) {
          t20 === void 0 ? t20 = null : Jn(t20, "First parameter");
          const s36 = Gt(r7, "Second parameter"), f15 = is(t20, "First parameter");
          if (cn(this), f15.type === "bytes") {
            if (s36.size !== void 0) throw new RangeError("The strategy for a byte stream cannot have a size function");
            const c15 = St(s36, 0);
            aa(this, f15, c15);
          } else {
            const c15 = Yt(s36), d12 = St(s36, 1);
            Ka(this, f15, d12, c15);
          }
        }
        get locked() {
          if (!We4(this)) throw De4("locked");
          return qe4(this);
        }
        cancel(t20 = void 0) {
          return We4(this) ? qe4(this) ? b4(new TypeError("Cannot cancel a stream that already has a reader")) : ie3(this, t20) : b4(De4("cancel"));
        }
        getReader(t20 = void 0) {
          if (!We4(this)) throw De4("getReader");
          return la(t20, "First parameter").mode === void 0 ? Qe4(this) : Co(this);
        }
        pipeThrough(t20, r7 = {}) {
          if (!We4(this)) throw De4("pipeThrough");
          Se2(t20, 1, "pipeThrough");
          const s36 = ds(t20, "First parameter"), f15 = Yo(r7, "Second parameter");
          if (qe4(this)) throw new TypeError("ReadableStream.prototype.pipeThrough cannot be used on a locked ReadableStream");
          if (Ze4(s36.writable)) throw new TypeError("ReadableStream.prototype.pipeThrough cannot be used on a locked WritableStream");
          const c15 = Ho(this, s36.writable, f15.preventClose, f15.preventAbort, f15.preventCancel, f15.signal);
          return Q4(c15), s36.readable;
        }
        pipeTo(t20, r7 = {}) {
          if (!We4(this)) return b4(De4("pipeTo"));
          if (t20 === void 0) return b4("Parameter 1 is required in 'pipeTo'.");
          if (!Ge3(t20)) return b4(new TypeError("ReadableStream.prototype.pipeTo's first argument must be a WritableStream"));
          let s36;
          try {
            s36 = Yo(r7, "Second parameter");
          } catch (f15) {
            return b4(f15);
          }
          return qe4(this) ? b4(new TypeError("ReadableStream.prototype.pipeTo cannot be used on a locked ReadableStream")) : Ze4(t20) ? b4(new TypeError("ReadableStream.prototype.pipeTo cannot be used on a locked WritableStream")) : Ho(this, t20, s36.preventClose, s36.preventAbort, s36.preventCancel, s36.signal);
        }
        tee() {
          if (!We4(this)) throw De4("tee");
          const t20 = Ja(this);
          return yt3(t20);
        }
        values(t20 = void 0) {
          if (!We4(this)) throw De4("values");
          const r7 = fs(t20, "First parameter");
          return Vi(this, r7.preventCancel);
        }
        [Ur](t20) {
          return this.values(t20);
        }
        static from(t20) {
          return rs(t20);
        }
      };
      n11(Pn, "ReadableStream");
      let L3 = Pn;
      Object.defineProperties(L3, { from: { enumerable: true } }), Object.defineProperties(L3.prototype, { cancel: { enumerable: true }, getReader: { enumerable: true }, pipeThrough: { enumerable: true }, pipeTo: { enumerable: true }, tee: { enumerable: true }, values: { enumerable: true }, locked: { enumerable: true } }), h6(L3.from, "from"), h6(L3.prototype.cancel, "cancel"), h6(L3.prototype.getReader, "getReader"), h6(L3.prototype.pipeThrough, "pipeThrough"), h6(L3.prototype.pipeTo, "pipeTo"), h6(L3.prototype.tee, "tee"), h6(L3.prototype.values, "values"), typeof Symbol.toStringTag == "symbol" && Object.defineProperty(L3.prototype, Symbol.toStringTag, { value: "ReadableStream", configurable: true }), Object.defineProperty(L3.prototype, Ur, { value: L3.prototype.values, writable: true, configurable: true });
      function Ct3(e14, t20, r7, s36 = 1, f15 = () => 1) {
        const c15 = Object.create(L3.prototype);
        cn(c15);
        const d12 = Object.create(ne3.prototype);
        return Qo(c15, d12, e14, t20, r7, s36, f15), c15;
      }
      n11(Ct3, "CreateReadableStream");
      function Go(e14, t20, r7) {
        const s36 = Object.create(L3.prototype);
        cn(s36);
        const f15 = Object.create(te4.prototype);
        return To(s36, f15, e14, t20, r7, 0, void 0), s36;
      }
      n11(Go, "CreateReadableByteStream");
      function cn(e14) {
        e14._state = "readable", e14._reader = void 0, e14._storedError = void 0, e14._disturbed = false;
      }
      n11(cn, "InitializeReadableStream");
      function We4(e14) {
        return !l12(e14) || !Object.prototype.hasOwnProperty.call(e14, "_readableStreamController") ? false : e14 instanceof L3;
      }
      n11(We4, "IsReadableStream");
      function qe4(e14) {
        return e14._reader !== void 0;
      }
      n11(qe4, "IsReadableStreamLocked");
      function ie3(e14, t20) {
        if (e14._disturbed = true, e14._state === "closed") return T3(void 0);
        if (e14._state === "errored") return b4(e14._storedError);
        Pt2(e14);
        const r7 = e14._reader;
        if (r7 !== void 0 && Fe3(r7)) {
          const f15 = r7._readIntoRequests;
          r7._readIntoRequests = new D5(), f15.forEach((c15) => {
            c15._closeSteps(void 0);
          });
        }
        const s36 = e14._readableStreamController[Ar](t20);
        return F4(s36, u4);
      }
      n11(ie3, "ReadableStreamCancel");
      function Pt2(e14) {
        e14._state = "closed";
        const t20 = e14._reader;
        if (t20 !== void 0 && (Zn(t20), Ee2(t20))) {
          const r7 = t20._readRequests;
          t20._readRequests = new D5(), r7.forEach((s36) => {
            s36._closeSteps();
          });
        }
      }
      n11(Pt2, "ReadableStreamClose");
      function Zo(e14, t20) {
        e14._state = "errored", e14._storedError = t20;
        const r7 = e14._reader;
        r7 !== void 0 && (Or(r7, t20), Ee2(r7) ? ro(r7, t20) : Ao(r7, t20));
      }
      n11(Zo, "ReadableStreamError");
      function De4(e14) {
        return new TypeError(`ReadableStream.prototype.${e14} can only be used on a ReadableStream`);
      }
      n11(De4, "streamBrandCheckException$1");
      function Ko(e14, t20) {
        ue4(e14, t20);
        const r7 = e14?.highWaterMark;
        return zr(r7, "highWaterMark", "QueuingStrategyInit"), { highWaterMark: Ir2(r7) };
      }
      n11(Ko, "convertQueuingStrategyInit");
      const Jo = n11((e14) => e14.byteLength, "byteLengthSizeFunction");
      h6(Jo, "size");
      const vn = class vn {
        constructor(t20) {
          Se2(t20, 1, "ByteLengthQueuingStrategy"), t20 = Ko(t20, "First parameter"), this._byteLengthQueuingStrategyHighWaterMark = t20.highWaterMark;
        }
        get highWaterMark() {
          if (!ei(this)) throw Xo("highWaterMark");
          return this._byteLengthQueuingStrategyHighWaterMark;
        }
        get size() {
          if (!ei(this)) throw Xo("size");
          return Jo;
        }
      };
      n11(vn, "ByteLengthQueuingStrategy");
      let Xe3 = vn;
      Object.defineProperties(Xe3.prototype, { highWaterMark: { enumerable: true }, size: { enumerable: true } }), typeof Symbol.toStringTag == "symbol" && Object.defineProperty(Xe3.prototype, Symbol.toStringTag, { value: "ByteLengthQueuingStrategy", configurable: true });
      function Xo(e14) {
        return new TypeError(`ByteLengthQueuingStrategy.prototype.${e14} can only be used on a ByteLengthQueuingStrategy`);
      }
      n11(Xo, "byteLengthBrandCheckException");
      function ei(e14) {
        return !l12(e14) || !Object.prototype.hasOwnProperty.call(e14, "_byteLengthQueuingStrategyHighWaterMark") ? false : e14 instanceof Xe3;
      }
      n11(ei, "IsByteLengthQueuingStrategy");
      const ti = n11(() => 1, "countSizeFunction");
      h6(ti, "size");
      const En = class En {
        constructor(t20) {
          Se2(t20, 1, "CountQueuingStrategy"), t20 = Ko(t20, "First parameter"), this._countQueuingStrategyHighWaterMark = t20.highWaterMark;
        }
        get highWaterMark() {
          if (!ni(this)) throw ri("highWaterMark");
          return this._countQueuingStrategyHighWaterMark;
        }
        get size() {
          if (!ni(this)) throw ri("size");
          return ti;
        }
      };
      n11(En, "CountQueuingStrategy");
      let et2 = En;
      Object.defineProperties(et2.prototype, { highWaterMark: { enumerable: true }, size: { enumerable: true } }), typeof Symbol.toStringTag == "symbol" && Object.defineProperty(et2.prototype, Symbol.toStringTag, { value: "CountQueuingStrategy", configurable: true });
      function ri(e14) {
        return new TypeError(`CountQueuingStrategy.prototype.${e14} can only be used on a CountQueuingStrategy`);
      }
      n11(ri, "countBrandCheckException");
      function ni(e14) {
        return !l12(e14) || !Object.prototype.hasOwnProperty.call(e14, "_countQueuingStrategyHighWaterMark") ? false : e14 instanceof et2;
      }
      n11(ni, "IsCountQueuingStrategy");
      function hs(e14, t20) {
        ue4(e14, t20);
        const r7 = e14?.cancel, s36 = e14?.flush, f15 = e14?.readableType, c15 = e14?.start, d12 = e14?.transform, m12 = e14?.writableType;
        return { cancel: r7 === void 0 ? void 0 : ys(r7, e14, `${t20} has member 'cancel' that`), flush: s36 === void 0 ? void 0 : ps(s36, e14, `${t20} has member 'flush' that`), readableType: f15, start: c15 === void 0 ? void 0 : bs(c15, e14, `${t20} has member 'start' that`), transform: d12 === void 0 ? void 0 : ms(d12, e14, `${t20} has member 'transform' that`), writableType: m12 };
      }
      n11(hs, "convertTransformer");
      function ps(e14, t20, r7) {
        return Z5(e14, r7), (s36) => j3(e14, t20, [s36]);
      }
      n11(ps, "convertTransformerFlushCallback");
      function bs(e14, t20, r7) {
        return Z5(e14, r7), (s36) => z2(e14, t20, [s36]);
      }
      n11(bs, "convertTransformerStartCallback");
      function ms(e14, t20, r7) {
        return Z5(e14, r7), (s36, f15) => j3(e14, t20, [s36, f15]);
      }
      n11(ms, "convertTransformerTransformCallback");
      function ys(e14, t20, r7) {
        return Z5(e14, r7), (s36) => j3(e14, t20, [s36]);
      }
      n11(ys, "convertTransformerCancelCallback");
      const An = class An {
        constructor(t20 = {}, r7 = {}, s36 = {}) {
          t20 === void 0 && (t20 = null);
          const f15 = Gt(r7, "Second parameter"), c15 = Gt(s36, "Third parameter"), d12 = hs(t20, "First parameter");
          if (d12.readableType !== void 0) throw new RangeError("Invalid readableType specified");
          if (d12.writableType !== void 0) throw new RangeError("Invalid writableType specified");
          const m12 = St(c15, 0), R10 = Yt(c15), y4 = St(f15, 1), C5 = Yt(f15);
          let P4;
          const B4 = E6((ae3) => {
            P4 = ae3;
          });
          gs(this, B4, y4, C5, m12, R10), Ss(this, d12), d12.start !== void 0 ? P4(d12.start(this._transformStreamController)) : P4(void 0);
        }
        get readable() {
          if (!oi(this)) throw li("readable");
          return this._readable;
        }
        get writable() {
          if (!oi(this)) throw li("writable");
          return this._writable;
        }
      };
      n11(An, "TransformStream");
      let tt2 = An;
      Object.defineProperties(tt2.prototype, { readable: { enumerable: true }, writable: { enumerable: true } }), typeof Symbol.toStringTag == "symbol" && Object.defineProperty(tt2.prototype, Symbol.toStringTag, { value: "TransformStream", configurable: true });
      function gs(e14, t20, r7, s36, f15, c15) {
        function d12() {
          return t20;
        }
        n11(d12, "startAlgorithm");
        function m12(B4) {
          return Ts(e14, B4);
        }
        n11(m12, "writeAlgorithm");
        function R10(B4) {
          return Cs(e14, B4);
        }
        n11(R10, "abortAlgorithm");
        function y4() {
          return Ps(e14);
        }
        n11(y4, "closeAlgorithm"), e14._writable = Ra(d12, m12, y4, R10, r7, s36);
        function C5() {
          return vs(e14);
        }
        n11(C5, "pullAlgorithm");
        function P4(B4) {
          return Es(e14, B4);
        }
        n11(P4, "cancelAlgorithm"), e14._readable = Ct3(d12, C5, P4, f15, c15), e14._backpressure = void 0, e14._backpressureChangePromise = void 0, e14._backpressureChangePromise_resolve = void 0, ar2(e14, true), e14._transformStreamController = void 0;
      }
      n11(gs, "InitializeTransformStream");
      function oi(e14) {
        return !l12(e14) || !Object.prototype.hasOwnProperty.call(e14, "_transformStreamController") ? false : e14 instanceof tt2;
      }
      n11(oi, "IsTransformStream");
      function ii(e14, t20) {
        oe2(e14._readable._readableStreamController, t20), dn(e14, t20);
      }
      n11(ii, "TransformStreamError");
      function dn(e14, t20) {
        lr(e14._transformStreamController), wt2(e14._writable._writableStreamController, t20), hn(e14);
      }
      n11(dn, "TransformStreamErrorWritableAndUnblockWrite");
      function hn(e14) {
        e14._backpressure && ar2(e14, false);
      }
      n11(hn, "TransformStreamUnblockWrite");
      function ar2(e14, t20) {
        e14._backpressureChangePromise !== void 0 && e14._backpressureChangePromise_resolve(), e14._backpressureChangePromise = E6((r7) => {
          e14._backpressureChangePromise_resolve = r7;
        }), e14._backpressure = t20;
      }
      n11(ar2, "TransformStreamSetBackpressure");
      const Bn = class Bn {
        constructor() {
          throw new TypeError("Illegal constructor");
        }
        get desiredSize() {
          if (!sr2(this)) throw ur2("desiredSize");
          const t20 = this._controlledTransformStream._readable._readableStreamController;
          return fn(t20);
        }
        enqueue(t20 = void 0) {
          if (!sr2(this)) throw ur2("enqueue");
          ai(this, t20);
        }
        error(t20 = void 0) {
          if (!sr2(this)) throw ur2("error");
          ws(this, t20);
        }
        terminate() {
          if (!sr2(this)) throw ur2("terminate");
          Rs(this);
        }
      };
      n11(Bn, "TransformStreamDefaultController");
      let pe4 = Bn;
      Object.defineProperties(pe4.prototype, { enqueue: { enumerable: true }, error: { enumerable: true }, terminate: { enumerable: true }, desiredSize: { enumerable: true } }), h6(pe4.prototype.enqueue, "enqueue"), h6(pe4.prototype.error, "error"), h6(pe4.prototype.terminate, "terminate"), typeof Symbol.toStringTag == "symbol" && Object.defineProperty(pe4.prototype, Symbol.toStringTag, { value: "TransformStreamDefaultController", configurable: true });
      function sr2(e14) {
        return !l12(e14) || !Object.prototype.hasOwnProperty.call(e14, "_controlledTransformStream") ? false : e14 instanceof pe4;
      }
      n11(sr2, "IsTransformStreamDefaultController");
      function _s(e14, t20, r7, s36, f15) {
        t20._controlledTransformStream = e14, e14._transformStreamController = t20, t20._transformAlgorithm = r7, t20._flushAlgorithm = s36, t20._cancelAlgorithm = f15, t20._finishPromise = void 0, t20._finishPromise_resolve = void 0, t20._finishPromise_reject = void 0;
      }
      n11(_s, "SetUpTransformStreamDefaultController");
      function Ss(e14, t20) {
        const r7 = Object.create(pe4.prototype);
        let s36, f15, c15;
        t20.transform !== void 0 ? s36 = n11((d12) => t20.transform(d12, r7), "transformAlgorithm") : s36 = n11((d12) => {
          try {
            return ai(r7, d12), T3(void 0);
          } catch (m12) {
            return b4(m12);
          }
        }, "transformAlgorithm"), t20.flush !== void 0 ? f15 = n11(() => t20.flush(r7), "flushAlgorithm") : f15 = n11(() => T3(void 0), "flushAlgorithm"), t20.cancel !== void 0 ? c15 = n11((d12) => t20.cancel(d12), "cancelAlgorithm") : c15 = n11(() => T3(void 0), "cancelAlgorithm"), _s(e14, r7, s36, f15, c15);
      }
      n11(Ss, "SetUpTransformStreamDefaultControllerFromTransformer");
      function lr(e14) {
        e14._transformAlgorithm = void 0, e14._flushAlgorithm = void 0, e14._cancelAlgorithm = void 0;
      }
      n11(lr, "TransformStreamDefaultControllerClearAlgorithms");
      function ai(e14, t20) {
        const r7 = e14._controlledTransformStream, s36 = r7._readable._readableStreamController;
        if (!Je3(s36)) throw new TypeError("Readable side is not in a state that permits enqueue");
        try {
          Ke4(s36, t20);
        } catch (c15) {
          throw dn(r7, c15), r7._readable._storedError;
        }
        Za(s36) !== r7._backpressure && ar2(r7, true);
      }
      n11(ai, "TransformStreamDefaultControllerEnqueue");
      function ws(e14, t20) {
        ii(e14._controlledTransformStream, t20);
      }
      n11(ws, "TransformStreamDefaultControllerError");
      function si(e14, t20) {
        const r7 = e14._transformAlgorithm(t20);
        return F4(r7, void 0, (s36) => {
          throw ii(e14._controlledTransformStream, s36), s36;
        });
      }
      n11(si, "TransformStreamDefaultControllerPerformTransform");
      function Rs(e14) {
        const t20 = e14._controlledTransformStream, r7 = t20._readable._readableStreamController;
        $e4(r7);
        const s36 = new TypeError("TransformStream terminated");
        dn(t20, s36);
      }
      n11(Rs, "TransformStreamDefaultControllerTerminate");
      function Ts(e14, t20) {
        const r7 = e14._transformStreamController;
        if (e14._backpressure) {
          const s36 = e14._backpressureChangePromise;
          return F4(s36, () => {
            const f15 = e14._writable;
            if (f15._state === "erroring") throw f15._storedError;
            return si(r7, t20);
          });
        }
        return si(r7, t20);
      }
      n11(Ts, "TransformStreamDefaultSinkWriteAlgorithm");
      function Cs(e14, t20) {
        const r7 = e14._transformStreamController;
        if (r7._finishPromise !== void 0) return r7._finishPromise;
        const s36 = e14._readable;
        r7._finishPromise = E6((c15, d12) => {
          r7._finishPromise_resolve = c15, r7._finishPromise_reject = d12;
        });
        const f15 = r7._cancelAlgorithm(t20);
        return lr(r7), _2(f15, () => (s36._state === "errored" ? rt3(r7, s36._storedError) : (oe2(s36._readableStreamController, t20), pn(r7)), null), (c15) => (oe2(s36._readableStreamController, c15), rt3(r7, c15), null)), r7._finishPromise;
      }
      n11(Cs, "TransformStreamDefaultSinkAbortAlgorithm");
      function Ps(e14) {
        const t20 = e14._transformStreamController;
        if (t20._finishPromise !== void 0) return t20._finishPromise;
        const r7 = e14._readable;
        t20._finishPromise = E6((f15, c15) => {
          t20._finishPromise_resolve = f15, t20._finishPromise_reject = c15;
        });
        const s36 = t20._flushAlgorithm();
        return lr(t20), _2(s36, () => (r7._state === "errored" ? rt3(t20, r7._storedError) : ($e4(r7._readableStreamController), pn(t20)), null), (f15) => (oe2(r7._readableStreamController, f15), rt3(t20, f15), null)), t20._finishPromise;
      }
      n11(Ps, "TransformStreamDefaultSinkCloseAlgorithm");
      function vs(e14) {
        return ar2(e14, false), e14._backpressureChangePromise;
      }
      n11(vs, "TransformStreamDefaultSourcePullAlgorithm");
      function Es(e14, t20) {
        const r7 = e14._transformStreamController;
        if (r7._finishPromise !== void 0) return r7._finishPromise;
        const s36 = e14._writable;
        r7._finishPromise = E6((c15, d12) => {
          r7._finishPromise_resolve = c15, r7._finishPromise_reject = d12;
        });
        const f15 = r7._cancelAlgorithm(t20);
        return lr(r7), _2(f15, () => (s36._state === "errored" ? rt3(r7, s36._storedError) : (wt2(s36._writableStreamController, t20), hn(e14), pn(r7)), null), (c15) => (wt2(s36._writableStreamController, c15), hn(e14), rt3(r7, c15), null)), r7._finishPromise;
      }
      n11(Es, "TransformStreamDefaultSourceCancelAlgorithm");
      function ur2(e14) {
        return new TypeError(`TransformStreamDefaultController.prototype.${e14} can only be used on a TransformStreamDefaultController`);
      }
      n11(ur2, "defaultControllerBrandCheckException");
      function pn(e14) {
        e14._finishPromise_resolve !== void 0 && (e14._finishPromise_resolve(), e14._finishPromise_resolve = void 0, e14._finishPromise_reject = void 0);
      }
      n11(pn, "defaultControllerFinishPromiseResolve");
      function rt3(e14, t20) {
        e14._finishPromise_reject !== void 0 && (Q4(e14._finishPromise), e14._finishPromise_reject(t20), e14._finishPromise_resolve = void 0, e14._finishPromise_reject = void 0);
      }
      n11(rt3, "defaultControllerFinishPromiseReject");
      function li(e14) {
        return new TypeError(`TransformStream.prototype.${e14} can only be used on a TransformStream`);
      }
      n11(li, "streamBrandCheckException"), a18.ByteLengthQueuingStrategy = Xe3, a18.CountQueuingStrategy = et2, a18.ReadableByteStreamController = te4, a18.ReadableStream = L3, a18.ReadableStreamBYOBReader = ce3, a18.ReadableStreamBYOBRequest = Re4, a18.ReadableStreamDefaultController = ne3, a18.ReadableStreamDefaultReader = fe2, a18.TransformStream = tt2, a18.TransformStreamDefaultController = pe4, a18.WritableStream = de3, a18.WritableStreamDefaultController = ke3, a18.WritableStreamDefaultWriter = re3;
    });
  }(pr2, pr2.exports)), pr2.exports;
}
async function* qn(i13, o8 = true) {
  for (const a18 of i13) if ("stream" in a18) yield* a18.stream();
  else if (ArrayBuffer.isView(a18)) if (o8) {
    let u4 = a18.byteOffset;
    const l12 = a18.byteOffset + a18.byteLength;
    for (; u4 !== l12; ) {
      const p11 = Math.min(l12 - u4, hi), h6 = a18.buffer.slice(u4, u4 + p11);
      u4 += h6.byteLength, yield new Uint8Array(h6);
    }
  } else yield a18;
  else {
    let u4 = 0, l12 = a18;
    for (; u4 !== l12.size; ) {
      const h6 = await l12.slice(u4, Math.min(l12.size, u4 + hi)).arrayBuffer();
      u4 += h6.byteLength, yield new Uint8Array(h6);
    }
  }
}
function Vs(i13, o8 = ut) {
  var a18 = `${bi()}${bi()}`.replace(/\./g, "").slice(-28).padStart(32, "-"), u4 = [], l12 = `--${a18}\r
Content-Disposition: form-data; name="`;
  return i13.forEach((p11, h6) => typeof p11 == "string" ? u4.push(l12 + zn(h6) + `"\r
\r
${p11.replace(/\r(?!\n)|(?<!\r)\n/g, `\r
`)}\r
`) : u4.push(l12 + zn(h6) + `"; filename="${zn(p11.name, 1)}"\r
Content-Type: ${p11.type || "application/octet-stream"}\r
\r
`, p11, `\r
`)), u4.push(`--${a18}--`), new o8(u4, { type: "multipart/form-data; boundary=" + a18 });
}
async function In(i13) {
  if (i13[H3].disturbed) throw new TypeError(`body used already for: ${i13.url}`);
  if (i13[H3].disturbed = true, i13[H3].error) throw i13[H3].error;
  const { body: o8 } = i13;
  if (o8 === null) return M5.alloc(0);
  if (!(o8 instanceof me2)) return M5.alloc(0);
  const a18 = [];
  let u4 = 0;
  try {
    for await (const l12 of o8) {
      if (i13.size > 0 && u4 + l12.length > i13.size) {
        const p11 = new G2(`content size at ${i13.url} over limit: ${i13.size}`, "max-size");
        throw o8.destroy(p11), p11;
      }
      u4 += l12.length, a18.push(l12);
    }
  } catch (l12) {
    throw l12 instanceof ft2 ? l12 : new G2(`Invalid response body while trying to fetch ${i13.url}: ${l12.message}`, "system", l12);
  }
  if (o8.readableEnded === true || o8._readableState.ended === true) try {
    return a18.every((l12) => typeof l12 == "string") ? M5.from(a18.join("")) : M5.concat(a18, u4);
  } catch (l12) {
    throw new G2(`Could not create Buffer from response body for ${i13.url}: ${l12.message}`, "system", l12);
  }
  else throw new G2(`Premature close of server response while trying to fetch ${i13.url}`);
}
function el(i13 = []) {
  return new ye2(i13.reduce((o8, a18, u4, l12) => (u4 % 2 === 0 && o8.push(l12.slice(u4, u4 + 2)), o8), []).filter(([o8, a18]) => {
    try {
      return gr2(o8), jn(o8, String(a18)), true;
    } catch {
      return false;
    }
  }));
}
function _i(i13, o8 = false) {
  return i13 == null || (i13 = new URL(i13), /^(about|blob|data):$/.test(i13.protocol)) ? "no-referrer" : (i13.username = "", i13.password = "", i13.hash = "", o8 && (i13.pathname = "", i13.search = ""), i13);
}
function ol(i13) {
  if (!Si.has(i13)) throw new TypeError(`Invalid referrerPolicy: ${i13}`);
  return i13;
}
function il(i13) {
  if (/^(http|ws)s:$/.test(i13.protocol)) return true;
  const o8 = i13.host.replace(/(^\[)|(]$)/g, ""), a18 = Os(o8);
  return a18 === 4 && /^127\./.test(o8) || a18 === 6 && /^(((0+:){7})|(::(0+:){0,6}))0*1$/.test(o8) ? true : i13.host === "localhost" || i13.host.endsWith(".localhost") ? false : i13.protocol === "file:";
}
function ct2(i13) {
  return /^about:(blank|srcdoc)$/.test(i13) || i13.protocol === "data:" || /^(blob|filesystem):$/.test(i13.protocol) ? true : il(i13);
}
function al(i13, { referrerURLCallback: o8, referrerOriginCallback: a18 } = {}) {
  if (i13.referrer === "no-referrer" || i13.referrerPolicy === "") return null;
  const u4 = i13.referrerPolicy;
  if (i13.referrer === "about:client") return "no-referrer";
  const l12 = i13.referrer;
  let p11 = _i(l12), h6 = _i(l12, true);
  p11.toString().length > 4096 && (p11 = h6), o8 && (p11 = o8(p11)), a18 && (h6 = a18(h6));
  const g7 = new URL(i13.url);
  switch (u4) {
    case "no-referrer":
      return "no-referrer";
    case "origin":
      return h6;
    case "unsafe-url":
      return p11;
    case "strict-origin":
      return ct2(p11) && !ct2(g7) ? "no-referrer" : h6.toString();
    case "strict-origin-when-cross-origin":
      return p11.origin === g7.origin ? p11 : ct2(p11) && !ct2(g7) ? "no-referrer" : h6;
    case "same-origin":
      return p11.origin === g7.origin ? p11 : "no-referrer";
    case "origin-when-cross-origin":
      return p11.origin === g7.origin ? p11 : h6;
    case "no-referrer-when-downgrade":
      return ct2(p11) && !ct2(g7) ? "no-referrer" : p11;
    default:
      throw new TypeError(`Invalid referrerPolicy: ${u4}`);
  }
}
function sl(i13) {
  const o8 = (i13.get("referrer-policy") || "").split(/[,\s]+/);
  let a18 = "";
  for (const u4 of o8) u4 && Si.has(u4) && (a18 = u4);
  return a18;
}
async function Ti(i13, o8) {
  return new Promise((a18, u4) => {
    const l12 = new dt2(i13, o8), { parsedURL: p11, options: h6 } = ul(l12);
    if (!ml.has(p11.protocol)) throw new TypeError(`node-fetch cannot load ${i13}. URL scheme "${p11.protocol.replace(/:$/, "")}" is not supported.`);
    if (p11.protocol === "data:") {
      const _2 = js(l12.url), V3 = new le3(_2, { headers: { "Content-Type": _2.typeFull } });
      a18(V3);
      return;
    }
    const g7 = (p11.protocol === "https:" ? Bs : vt3).request, { signal: A4 } = l12;
    let w11 = null;
    const E6 = n11(() => {
      const _2 = new _r("The operation was aborted.");
      u4(_2), l12.body && l12.body instanceof me2.Readable && l12.body.destroy(_2), !(!w11 || !w11.body) && w11.body.emit("error", _2);
    }, "abort");
    if (A4 && A4.aborted) {
      E6();
      return;
    }
    const T3 = n11(() => {
      E6(), q2();
    }, "abortAndFinalize"), b4 = g7(p11.toString(), h6);
    A4 && A4.addEventListener("abort", T3);
    const q2 = n11(() => {
      b4.abort(), A4 && A4.removeEventListener("abort", T3);
    }, "finalize");
    b4.on("error", (_2) => {
      u4(new G2(`request to ${l12.url} failed, reason: ${_2.message}`, "system", _2)), q2();
    }), yl(b4, (_2) => {
      w11 && w11.body && w11.body.destroy(_2);
    }), process.version < "v14" && b4.on("socket", (_2) => {
      let V3;
      _2.prependListener("end", () => {
        V3 = _2._eventsCount;
      }), _2.prependListener("close", (I6) => {
        if (w11 && V3 < _2._eventsCount && !I6) {
          const F4 = new Error("Premature close");
          F4.code = "ERR_STREAM_PREMATURE_CLOSE", w11.body.emit("error", F4);
        }
      });
    }), b4.on("response", (_2) => {
      b4.setTimeout(0);
      const V3 = el(_2.rawHeaders);
      if (Ln(_2.statusCode)) {
        const z2 = V3.get("Location");
        let j3 = null;
        try {
          j3 = z2 === null ? null : new URL(z2, l12.url);
        } catch {
          if (l12.redirect !== "manual") {
            u4(new G2(`uri requested responds with an invalid redirect URL: ${z2}`, "invalid-redirect")), q2();
            return;
          }
        }
        switch (l12.redirect) {
          case "error":
            u4(new G2(`uri requested responds with a redirect, redirect mode is set to error: ${l12.url}`, "no-redirect")), q2();
            return;
          case "manual":
            break;
          case "follow": {
            if (j3 === null) break;
            if (l12.counter >= l12.follow) {
              u4(new G2(`maximum redirect reached at: ${l12.url}`, "max-redirect")), q2();
              return;
            }
            const U3 = { headers: new ye2(l12.headers), follow: l12.follow, counter: l12.counter + 1, agent: l12.agent, compress: l12.compress, method: l12.method, body: Fn(l12), signal: l12.signal, size: l12.size, referrer: l12.referrer, referrerPolicy: l12.referrerPolicy };
            if (!Ys(l12.url, j3) || !Gs(l12.url, j3)) for (const Ft3 of ["authorization", "www-authenticate", "cookie", "cookie2"]) U3.headers.delete(Ft3);
            if (_2.statusCode !== 303 && l12.body && o8.body instanceof me2.Readable) {
              u4(new G2("Cannot follow redirect with body being a readable stream", "unsupported-redirect")), q2();
              return;
            }
            (_2.statusCode === 303 || (_2.statusCode === 301 || _2.statusCode === 302) && l12.method === "POST") && (U3.method = "GET", U3.body = void 0, U3.headers.delete("content-length"));
            const D5 = sl(V3);
            D5 && (U3.referrerPolicy = D5), a18(Ti(new dt2(j3, U3))), q2();
            return;
          }
          default:
            return u4(new TypeError(`Redirect option '${l12.redirect}' is not a valid value of RequestRedirect`));
        }
      }
      A4 && _2.once("end", () => {
        A4.removeEventListener("abort", T3);
      });
      let I6 = lt2(_2, new cr2(), (z2) => {
        z2 && u4(z2);
      });
      process.version < "v12.10" && _2.on("aborted", T3);
      const F4 = { url: l12.url, status: _2.statusCode, statusText: _2.statusMessage, headers: V3, size: l12.size, counter: l12.counter, highWaterMark: l12.highWaterMark }, Q4 = V3.get("Content-Encoding");
      if (!l12.compress || l12.method === "HEAD" || Q4 === null || _2.statusCode === 204 || _2.statusCode === 304) {
        w11 = new le3(I6, F4), a18(w11);
        return;
      }
      const ge3 = { flush: st2.Z_SYNC_FLUSH, finishFlush: st2.Z_SYNC_FLUSH };
      if (Q4 === "gzip" || Q4 === "x-gzip") {
        I6 = lt2(I6, st2.createGunzip(ge3), (z2) => {
          z2 && u4(z2);
        }), w11 = new le3(I6, F4), a18(w11);
        return;
      }
      if (Q4 === "deflate" || Q4 === "x-deflate") {
        const z2 = lt2(_2, new cr2(), (j3) => {
          j3 && u4(j3);
        });
        z2.once("data", (j3) => {
          (j3[0] & 15) === 8 ? I6 = lt2(I6, st2.createInflate(), (U3) => {
            U3 && u4(U3);
          }) : I6 = lt2(I6, st2.createInflateRaw(), (U3) => {
            U3 && u4(U3);
          }), w11 = new le3(I6, F4), a18(w11);
        }), z2.once("end", () => {
          w11 || (w11 = new le3(I6, F4), a18(w11));
        });
        return;
      }
      if (Q4 === "br") {
        I6 = lt2(I6, st2.createBrotliDecompress(), (z2) => {
          z2 && u4(z2);
        }), w11 = new le3(I6, F4), a18(w11);
        return;
      }
      w11 = new le3(I6, F4), a18(w11);
    }), Xs(b4, l12).catch(u4);
  });
}
function yl(i13, o8) {
  const a18 = M5.from(`0\r
\r
`);
  let u4 = false, l12 = false, p11;
  i13.on("response", (h6) => {
    const { headers: g7 } = h6;
    u4 = g7["transfer-encoding"] === "chunked" && !g7["content-length"];
  }), i13.on("socket", (h6) => {
    const g7 = n11(() => {
      if (u4 && !l12) {
        const w11 = new Error("Premature close");
        w11.code = "ERR_STREAM_PREMATURE_CLOSE", o8(w11);
      }
    }, "onSocketClose"), A4 = n11((w11) => {
      l12 = M5.compare(w11.slice(-5), a18) === 0, !l12 && p11 && (l12 = M5.compare(p11.slice(-3), a18.slice(0, 3)) === 0 && M5.compare(w11.slice(-2), a18.slice(3)) === 0), p11 = w11;
    }, "onData");
    h6.prependListener("close", g7), h6.on("data", A4), i13.on("close", () => {
      h6.removeListener("close", g7), h6.removeListener("data", A4);
    });
  });
}
function W3(i13) {
  const o8 = Ci.get(i13);
  return console.assert(o8 != null, "'this' is expected an Event object, but got", i13), o8;
}
function Pi(i13) {
  if (i13.passiveListener != null) {
    typeof console < "u" && typeof console.error == "function" && console.error("Unable to preventDefault inside passive event listener invocation.", i13.passiveListener);
    return;
  }
  i13.event.cancelable && (i13.canceled = true, typeof i13.event.preventDefault == "function" && i13.event.preventDefault());
}
function ht3(i13, o8) {
  Ci.set(this, { eventTarget: i13, event: o8, eventPhase: 2, currentTarget: i13, canceled: false, stopped: false, immediateStopped: false, passiveListener: null, timeStamp: o8.timeStamp || Date.now() }), Object.defineProperty(this, "isTrusted", { value: false, enumerable: true });
  const a18 = Object.keys(o8);
  for (let u4 = 0; u4 < a18.length; ++u4) {
    const l12 = a18[u4];
    l12 in this || Object.defineProperty(this, l12, vi(l12));
  }
}
function vi(i13) {
  return { get() {
    return W3(this).event[i13];
  }, set(o8) {
    W3(this).event[i13] = o8;
  }, configurable: true, enumerable: true };
}
function gl(i13) {
  return { value() {
    const o8 = W3(this).event;
    return o8[i13].apply(o8, arguments);
  }, configurable: true, enumerable: true };
}
function _l(i13, o8) {
  const a18 = Object.keys(o8);
  if (a18.length === 0) return i13;
  function u4(l12, p11) {
    i13.call(this, l12, p11);
  }
  n11(u4, "CustomEvent"), u4.prototype = Object.create(i13.prototype, { constructor: { value: u4, configurable: true, writable: true } });
  for (let l12 = 0; l12 < a18.length; ++l12) {
    const p11 = a18[l12];
    if (!(p11 in i13.prototype)) {
      const g7 = typeof Object.getOwnPropertyDescriptor(o8, p11).value == "function";
      Object.defineProperty(u4.prototype, p11, g7 ? gl(p11) : vi(p11));
    }
  }
  return u4;
}
function Ei(i13) {
  if (i13 == null || i13 === Object.prototype) return ht3;
  let o8 = Dn.get(i13);
  return o8 == null && (o8 = _l(Ei(Object.getPrototypeOf(i13)), i13), Dn.set(i13, o8)), o8;
}
function Sl(i13, o8) {
  const a18 = Ei(Object.getPrototypeOf(o8));
  return new a18(i13, o8);
}
function wl(i13) {
  return W3(i13).immediateStopped;
}
function Rl(i13, o8) {
  W3(i13).eventPhase = o8;
}
function Tl(i13, o8) {
  W3(i13).currentTarget = o8;
}
function Ai(i13, o8) {
  W3(i13).passiveListener = o8;
}
function Rr2(i13) {
  return i13 !== null && typeof i13 == "object";
}
function Bt2(i13) {
  const o8 = Bi.get(i13);
  if (o8 == null) throw new TypeError("'this' is expected an EventTarget object, but got another value.");
  return o8;
}
function Cl(i13) {
  return { get() {
    let a18 = Bt2(this).get(i13);
    for (; a18 != null; ) {
      if (a18.listenerType === wr) return a18.listener;
      a18 = a18.next;
    }
    return null;
  }, set(o8) {
    typeof o8 != "function" && !Rr2(o8) && (o8 = null);
    const a18 = Bt2(this);
    let u4 = null, l12 = a18.get(i13);
    for (; l12 != null; ) l12.listenerType === wr ? u4 !== null ? u4.next = l12.next : l12.next !== null ? a18.set(i13, l12.next) : a18.delete(i13) : u4 = l12, l12 = l12.next;
    if (o8 !== null) {
      const p11 = { listener: o8, listenerType: wr, passive: false, once: false, next: null };
      u4 === null ? a18.set(i13, p11) : u4.next = p11;
    }
  }, configurable: true, enumerable: true };
}
function qi(i13, o8) {
  Object.defineProperty(i13, `on${o8}`, Cl(o8));
}
function Oi(i13) {
  function o8() {
    Pe2.call(this);
  }
  n11(o8, "CustomEventTarget"), o8.prototype = Object.create(Pe2.prototype, { constructor: { value: o8, configurable: true, writable: true } });
  for (let a18 = 0; a18 < i13.length; ++a18) qi(o8.prototype, i13[a18]);
  return o8;
}
function Pe2() {
  if (this instanceof Pe2) {
    Bi.set(this, /* @__PURE__ */ new Map());
    return;
  }
  if (arguments.length === 1 && Array.isArray(arguments[0])) return Oi(arguments[0]);
  if (arguments.length > 0) {
    const i13 = new Array(arguments.length);
    for (let o8 = 0; o8 < arguments.length; ++o8) i13[o8] = arguments[o8];
    return Oi(i13);
  }
  throw new TypeError("Cannot call a class as a function");
}
function Pl() {
  const i13 = Object.create(pt3.prototype);
  return Pe2.call(i13), Tr.set(i13, false), i13;
}
function vl(i13) {
  Tr.get(i13) === false && (Tr.set(i13, true), i13.dispatchEvent({ type: "abort" }));
}
function Ii(i13) {
  const o8 = zi.get(i13);
  if (o8 == null) throw new TypeError(`Expected 'this' to be an 'AbortController' object, but got ${i13 === null ? "null" : typeof i13}`);
  return o8;
}
function ji() {
  !globalThis.process?.versions?.node && !globalThis.process?.env.DISABLE_NODE_FETCH_NATIVE_WARN && console.warn("[node-fetch-native] Node.js compatible build of `node-fetch-native` is being used in a non-Node.js environment. Please make sure you are using proper export conditions or report this issue to https://github.com/unjs/node-fetch-native. You can set `process.env.DISABLE_NODE_FETCH_NATIVE_WARN` to disable this warning.");
}
var As, n11, fi, O4, be3, X3, ve2, kt2, bt2, Cr, Ve3, Wt2, qt3, Ot3, ee3, zt3, Ne2, He3, It3, pr2, di, $s, hi, pi, Ds, ut, Ms, Us, On, Et, xs, Ns, bi, Hs, mi, zn, Me2, br, Un, ft2, xn, G2, mr2, yi, yr2, Qs, Ys, Gs, Zs, H3, Nn, Ue2, Fn, Ks, gi, Js, Xs, gr2, jn, Pr, ye2, tl, Ln, se2, xe3, le3, rl, Si, nl, $3, At2, ll, vr, dt2, ul, Hn, _r, fl, cl, $n, dl, hl, pl, bl, wi, Ri, Er, Sr, ml, Ci, Dn, Bi, ki, Wi, wr, Vn, pt3, Tr, Mn, zi, El, Al, Fi;
var init_node = __esm({
  "node_modules/node-fetch-native/dist/node.mjs"() {
    init_node_fetch_native_1a4a356d();
    As = Object.defineProperty;
    n11 = (i13, o8) => As(i13, "name", { value: o8, configurable: true });
    fi = (i13, o8, a18) => {
      if (!o8.has(i13)) throw TypeError("Cannot " + a18);
    };
    O4 = (i13, o8, a18) => (fi(i13, o8, "read from private field"), a18 ? a18.call(i13) : o8.get(i13));
    be3 = (i13, o8, a18) => {
      if (o8.has(i13)) throw TypeError("Cannot add the same private member more than once");
      o8 instanceof WeakSet ? o8.add(i13) : o8.set(i13, a18);
    };
    X3 = (i13, o8, a18, u4) => (fi(i13, o8, "write to private field"), u4 ? u4.call(i13, a18) : o8.set(i13, a18), a18);
    n11(js, "dataUriToBuffer");
    pr2 = { exports: {} };
    n11(Ls, "requirePonyfill_es2018");
    $s = 65536;
    if (!globalThis.ReadableStream) try {
      const i13 = __require("node:process"), { emitWarning: o8 } = i13;
      try {
        i13.emitWarning = () => {
        }, Object.assign(globalThis, __require("node:stream/web")), i13.emitWarning = o8;
      } catch (a18) {
        throw i13.emitWarning = o8, a18;
      }
    } catch {
      Object.assign(globalThis, Ls());
    }
    try {
      const { Blob: i13 } = __require("buffer");
      i13 && !i13.prototype.stream && (i13.prototype.stream = n11(function(a18) {
        let u4 = 0;
        const l12 = this;
        return new ReadableStream({ type: "bytes", async pull(p11) {
          const g7 = await l12.slice(u4, Math.min(l12.size, u4 + $s)).arrayBuffer();
          u4 += g7.byteLength, p11.enqueue(new Uint8Array(g7)), u4 === l12.size && p11.close();
        } });
      }, "name"));
    } catch {
    }
    hi = 65536;
    n11(qn, "toIterator");
    pi = (Ve3 = class {
      constructor(o8 = [], a18 = {}) {
        be3(this, ve2, []);
        be3(this, kt2, "");
        be3(this, bt2, 0);
        be3(this, Cr, "transparent");
        if (typeof o8 != "object" || o8 === null) throw new TypeError("Failed to construct 'Blob': The provided value cannot be converted to a sequence.");
        if (typeof o8[Symbol.iterator] != "function") throw new TypeError("Failed to construct 'Blob': The object must have a callable @@iterator property.");
        if (typeof a18 != "object" && typeof a18 != "function") throw new TypeError("Failed to construct 'Blob': parameter 2 cannot convert to dictionary.");
        a18 === null && (a18 = {});
        const u4 = new TextEncoder();
        for (const p11 of o8) {
          let h6;
          ArrayBuffer.isView(p11) ? h6 = new Uint8Array(p11.buffer.slice(p11.byteOffset, p11.byteOffset + p11.byteLength)) : p11 instanceof ArrayBuffer ? h6 = new Uint8Array(p11.slice(0)) : p11 instanceof Ve3 ? h6 = p11 : h6 = u4.encode(`${p11}`), X3(this, bt2, O4(this, bt2) + (ArrayBuffer.isView(h6) ? h6.byteLength : h6.size)), O4(this, ve2).push(h6);
        }
        X3(this, Cr, `${a18.endings === void 0 ? "transparent" : a18.endings}`);
        const l12 = a18.type === void 0 ? "" : String(a18.type);
        X3(this, kt2, /^[\x20-\x7E]*$/.test(l12) ? l12 : "");
      }
      get size() {
        return O4(this, bt2);
      }
      get type() {
        return O4(this, kt2);
      }
      async text() {
        const o8 = new TextDecoder();
        let a18 = "";
        for await (const u4 of qn(O4(this, ve2), false)) a18 += o8.decode(u4, { stream: true });
        return a18 += o8.decode(), a18;
      }
      async arrayBuffer() {
        const o8 = new Uint8Array(this.size);
        let a18 = 0;
        for await (const u4 of qn(O4(this, ve2), false)) o8.set(u4, a18), a18 += u4.length;
        return o8.buffer;
      }
      stream() {
        const o8 = qn(O4(this, ve2), true);
        return new globalThis.ReadableStream({ type: "bytes", async pull(a18) {
          const u4 = await o8.next();
          u4.done ? a18.close() : a18.enqueue(u4.value);
        }, async cancel() {
          await o8.return();
        } });
      }
      slice(o8 = 0, a18 = this.size, u4 = "") {
        const { size: l12 } = this;
        let p11 = o8 < 0 ? Math.max(l12 + o8, 0) : Math.min(o8, l12), h6 = a18 < 0 ? Math.max(l12 + a18, 0) : Math.min(a18, l12);
        const g7 = Math.max(h6 - p11, 0), A4 = O4(this, ve2), w11 = [];
        let E6 = 0;
        for (const b4 of A4) {
          if (E6 >= g7) break;
          const q2 = ArrayBuffer.isView(b4) ? b4.byteLength : b4.size;
          if (p11 && q2 <= p11) p11 -= q2, h6 -= q2;
          else {
            let _2;
            ArrayBuffer.isView(b4) ? (_2 = b4.subarray(p11, Math.min(q2, h6)), E6 += _2.byteLength) : (_2 = b4.slice(p11, Math.min(q2, h6)), E6 += _2.size), h6 -= q2, w11.push(_2), p11 = 0;
          }
        }
        const T3 = new Ve3([], { type: String(u4).toLowerCase() });
        return X3(T3, bt2, g7), X3(T3, ve2, w11), T3;
      }
      get [Symbol.toStringTag]() {
        return "Blob";
      }
      static [Symbol.hasInstance](o8) {
        return o8 && typeof o8 == "object" && typeof o8.constructor == "function" && (typeof o8.stream == "function" || typeof o8.arrayBuffer == "function") && /^(Blob|File)$/.test(o8[Symbol.toStringTag]);
      }
    }, ve2 = /* @__PURE__ */ new WeakMap(), kt2 = /* @__PURE__ */ new WeakMap(), bt2 = /* @__PURE__ */ new WeakMap(), Cr = /* @__PURE__ */ new WeakMap(), n11(Ve3, "Blob"), Ve3);
    Object.defineProperties(pi.prototype, { size: { enumerable: true }, type: { enumerable: true }, slice: { enumerable: true } });
    Ds = pi;
    ut = Ds;
    Ms = (Ot3 = class extends ut {
      constructor(a18, u4, l12 = {}) {
        if (arguments.length < 2) throw new TypeError(`Failed to construct 'File': 2 arguments required, but only ${arguments.length} present.`);
        super(a18, l12);
        be3(this, Wt2, 0);
        be3(this, qt3, "");
        l12 === null && (l12 = {});
        const p11 = l12.lastModified === void 0 ? Date.now() : Number(l12.lastModified);
        Number.isNaN(p11) || X3(this, Wt2, p11), X3(this, qt3, String(u4));
      }
      get name() {
        return O4(this, qt3);
      }
      get lastModified() {
        return O4(this, Wt2);
      }
      get [Symbol.toStringTag]() {
        return "File";
      }
      static [Symbol.hasInstance](a18) {
        return !!a18 && a18 instanceof ut && /^(File)$/.test(a18[Symbol.toStringTag]);
      }
    }, Wt2 = /* @__PURE__ */ new WeakMap(), qt3 = /* @__PURE__ */ new WeakMap(), n11(Ot3, "File"), Ot3);
    Us = Ms;
    On = Us;
    ({ toStringTag: Et, iterator: xs, hasInstance: Ns } = Symbol);
    bi = Math.random;
    Hs = "append,set,get,getAll,delete,keys,values,entries,forEach,constructor".split(",");
    mi = n11((i13, o8, a18) => (i13 += "", /^(Blob|File)$/.test(o8 && o8[Et]) ? [(a18 = a18 !== void 0 ? a18 + "" : o8[Et] == "File" ? o8.name : "blob", i13), o8.name !== a18 || o8[Et] == "blob" ? new On([o8], a18, o8) : o8] : [i13, o8 + ""]), "f");
    zn = n11((i13, o8) => (o8 ? i13 : i13.replace(/\r?\n|\r/g, `\r
`)).replace(/\n/g, "%0A").replace(/\r/g, "%0D").replace(/"/g, "%22"), "e$1");
    Me2 = n11((i13, o8, a18) => {
      if (o8.length < a18) throw new TypeError(`Failed to execute '${i13}' on 'FormData': ${a18} arguments required, but only ${o8.length} present.`);
    }, "x");
    br = (zt3 = class {
      constructor(...o8) {
        be3(this, ee3, []);
        if (o8.length) throw new TypeError("Failed to construct 'FormData': parameter 1 is not of type 'HTMLFormElement'.");
      }
      get [Et]() {
        return "FormData";
      }
      [xs]() {
        return this.entries();
      }
      static [Ns](o8) {
        return o8 && typeof o8 == "object" && o8[Et] === "FormData" && !Hs.some((a18) => typeof o8[a18] != "function");
      }
      append(...o8) {
        Me2("append", arguments, 2), O4(this, ee3).push(mi(...o8));
      }
      delete(o8) {
        Me2("delete", arguments, 1), o8 += "", X3(this, ee3, O4(this, ee3).filter(([a18]) => a18 !== o8));
      }
      get(o8) {
        Me2("get", arguments, 1), o8 += "";
        for (var a18 = O4(this, ee3), u4 = a18.length, l12 = 0; l12 < u4; l12++) if (a18[l12][0] === o8) return a18[l12][1];
        return null;
      }
      getAll(o8, a18) {
        return Me2("getAll", arguments, 1), a18 = [], o8 += "", O4(this, ee3).forEach((u4) => u4[0] === o8 && a18.push(u4[1])), a18;
      }
      has(o8) {
        return Me2("has", arguments, 1), o8 += "", O4(this, ee3).some((a18) => a18[0] === o8);
      }
      forEach(o8, a18) {
        Me2("forEach", arguments, 1);
        for (var [u4, l12] of this) o8.call(a18, l12, u4, this);
      }
      set(...o8) {
        Me2("set", arguments, 2);
        var a18 = [], u4 = true;
        o8 = mi(...o8), O4(this, ee3).forEach((l12) => {
          l12[0] === o8[0] ? u4 && (u4 = !a18.push(o8)) : a18.push(l12);
        }), u4 && a18.push(o8), X3(this, ee3, a18);
      }
      *entries() {
        yield* O4(this, ee3);
      }
      *keys() {
        for (var [o8] of this) yield o8;
      }
      *values() {
        for (var [, o8] of this) yield o8;
      }
    }, ee3 = /* @__PURE__ */ new WeakMap(), n11(zt3, "FormData"), zt3);
    n11(Vs, "formDataToBlob");
    Un = class Un2 extends Error {
      constructor(o8, a18) {
        super(o8), Error.captureStackTrace(this, this.constructor), this.type = a18;
      }
      get name() {
        return this.constructor.name;
      }
      get [Symbol.toStringTag]() {
        return this.constructor.name;
      }
    };
    n11(Un, "FetchBaseError");
    ft2 = Un;
    xn = class xn2 extends ft2 {
      constructor(o8, a18, u4) {
        super(o8, a18), u4 && (this.code = this.errno = u4.code, this.erroredSysCall = u4.syscall);
      }
    };
    n11(xn, "FetchError");
    G2 = xn;
    mr2 = Symbol.toStringTag;
    yi = n11((i13) => typeof i13 == "object" && typeof i13.append == "function" && typeof i13.delete == "function" && typeof i13.get == "function" && typeof i13.getAll == "function" && typeof i13.has == "function" && typeof i13.set == "function" && typeof i13.sort == "function" && i13[mr2] === "URLSearchParams", "isURLSearchParameters");
    yr2 = n11((i13) => i13 && typeof i13 == "object" && typeof i13.arrayBuffer == "function" && typeof i13.type == "string" && typeof i13.stream == "function" && typeof i13.constructor == "function" && /^(Blob|File)$/.test(i13[mr2]), "isBlob");
    Qs = n11((i13) => typeof i13 == "object" && (i13[mr2] === "AbortSignal" || i13[mr2] === "EventTarget"), "isAbortSignal");
    Ys = n11((i13, o8) => {
      const a18 = new URL(o8).hostname, u4 = new URL(i13).hostname;
      return a18 === u4 || a18.endsWith(`.${u4}`);
    }, "isDomainOrSubdomain");
    Gs = n11((i13, o8) => {
      const a18 = new URL(o8).protocol, u4 = new URL(i13).protocol;
      return a18 === u4;
    }, "isSameProtocol");
    Zs = ks(me2.pipeline);
    H3 = Symbol("Body internals");
    Nn = class Nn2 {
      constructor(o8, { size: a18 = 0 } = {}) {
        let u4 = null;
        o8 === null ? o8 = null : yi(o8) ? o8 = M5.from(o8.toString()) : yr2(o8) || M5.isBuffer(o8) || (dr2.isAnyArrayBuffer(o8) ? o8 = M5.from(o8) : ArrayBuffer.isView(o8) ? o8 = M5.from(o8.buffer, o8.byteOffset, o8.byteLength) : o8 instanceof me2 || (o8 instanceof br ? (o8 = Vs(o8), u4 = o8.type.split("=")[1]) : o8 = M5.from(String(o8))));
        let l12 = o8;
        M5.isBuffer(o8) ? l12 = me2.Readable.from(o8) : yr2(o8) && (l12 = me2.Readable.from(o8.stream())), this[H3] = { body: o8, stream: l12, boundary: u4, disturbed: false, error: null }, this.size = a18, o8 instanceof me2 && o8.on("error", (p11) => {
          const h6 = p11 instanceof ft2 ? p11 : new G2(`Invalid response body while trying to fetch ${this.url}: ${p11.message}`, "system", p11);
          this[H3].error = h6;
        });
      }
      get body() {
        return this[H3].stream;
      }
      get bodyUsed() {
        return this[H3].disturbed;
      }
      async arrayBuffer() {
        const { buffer: o8, byteOffset: a18, byteLength: u4 } = await In(this);
        return o8.slice(a18, a18 + u4);
      }
      async formData() {
        const o8 = this.headers.get("content-type");
        if (o8.startsWith("application/x-www-form-urlencoded")) {
          const u4 = new br(), l12 = new URLSearchParams(await this.text());
          for (const [p11, h6] of l12) u4.append(p11, h6);
          return u4;
        }
        const { toFormData: a18 } = await Promise.resolve().then(() => (init_multipart_parser(), multipart_parser_exports));
        return a18(this.body, o8);
      }
      async blob() {
        const o8 = this.headers && this.headers.get("content-type") || this[H3].body && this[H3].body.type || "", a18 = await this.arrayBuffer();
        return new ut([a18], { type: o8 });
      }
      async json() {
        const o8 = await this.text();
        return JSON.parse(o8);
      }
      async text() {
        const o8 = await In(this);
        return new TextDecoder().decode(o8);
      }
      buffer() {
        return In(this);
      }
    };
    n11(Nn, "Body");
    Ue2 = Nn;
    Ue2.prototype.buffer = hr2(Ue2.prototype.buffer, "Please use 'response.arrayBuffer()' instead of 'response.buffer()'", "node-fetch#buffer"), Object.defineProperties(Ue2.prototype, { body: { enumerable: true }, bodyUsed: { enumerable: true }, arrayBuffer: { enumerable: true }, blob: { enumerable: true }, json: { enumerable: true }, text: { enumerable: true }, data: { get: hr2(() => {
    }, "data doesn't exist, use json(), text(), arrayBuffer(), or body instead", "https://github.com/node-fetch/node-fetch/issues/1000 (response)") } });
    n11(In, "consumeBody");
    Fn = n11((i13, o8) => {
      let a18, u4, { body: l12 } = i13[H3];
      if (i13.bodyUsed) throw new Error("cannot clone body after it is used");
      return l12 instanceof me2 && typeof l12.getBoundary != "function" && (a18 = new cr2({ highWaterMark: o8 }), u4 = new cr2({ highWaterMark: o8 }), l12.pipe(a18), l12.pipe(u4), i13[H3].stream = a18, l12 = u4), l12;
    }, "clone");
    Ks = hr2((i13) => i13.getBoundary(), "form-data doesn't follow the spec and requires special treatment. Use alternative package", "https://github.com/node-fetch/node-fetch/issues/1167");
    gi = n11((i13, o8) => i13 === null ? null : typeof i13 == "string" ? "text/plain;charset=UTF-8" : yi(i13) ? "application/x-www-form-urlencoded;charset=UTF-8" : yr2(i13) ? i13.type || null : M5.isBuffer(i13) || dr2.isAnyArrayBuffer(i13) || ArrayBuffer.isView(i13) ? null : i13 instanceof br ? `multipart/form-data; boundary=${o8[H3].boundary}` : i13 && typeof i13.getBoundary == "function" ? `multipart/form-data;boundary=${Ks(i13)}` : i13 instanceof me2 ? null : "text/plain;charset=UTF-8", "extractContentType");
    Js = n11((i13) => {
      const { body: o8 } = i13[H3];
      return o8 === null ? 0 : yr2(o8) ? o8.size : M5.isBuffer(o8) ? o8.length : o8 && typeof o8.getLengthSync == "function" && o8.hasKnownLength && o8.hasKnownLength() ? o8.getLengthSync() : null;
    }, "getTotalBytes");
    Xs = n11(async (i13, { body: o8 }) => {
      o8 === null ? i13.end() : await Zs(o8, i13);
    }, "writeToStream");
    gr2 = typeof vt3.validateHeaderName == "function" ? vt3.validateHeaderName : (i13) => {
      if (!/^[\^`\-\w!#$%&'*+.|~]+$/.test(i13)) {
        const o8 = new TypeError(`Header name must be a valid HTTP token [${i13}]`);
        throw Object.defineProperty(o8, "code", { value: "ERR_INVALID_HTTP_TOKEN" }), o8;
      }
    };
    jn = typeof vt3.validateHeaderValue == "function" ? vt3.validateHeaderValue : (i13, o8) => {
      if (/[^\t\u0020-\u007E\u0080-\u00FF]/.test(o8)) {
        const a18 = new TypeError(`Invalid character in header content ["${i13}"]`);
        throw Object.defineProperty(a18, "code", { value: "ERR_INVALID_CHAR" }), a18;
      }
    };
    Pr = class Pr2 extends URLSearchParams {
      constructor(o8) {
        let a18 = [];
        if (o8 instanceof Pr2) {
          const u4 = o8.raw();
          for (const [l12, p11] of Object.entries(u4)) a18.push(...p11.map((h6) => [l12, h6]));
        } else if (o8 != null) if (typeof o8 == "object" && !dr2.isBoxedPrimitive(o8)) {
          const u4 = o8[Symbol.iterator];
          if (u4 == null) a18.push(...Object.entries(o8));
          else {
            if (typeof u4 != "function") throw new TypeError("Header pairs must be iterable");
            a18 = [...o8].map((l12) => {
              if (typeof l12 != "object" || dr2.isBoxedPrimitive(l12)) throw new TypeError("Each header pair must be an iterable object");
              return [...l12];
            }).map((l12) => {
              if (l12.length !== 2) throw new TypeError("Each header pair must be a name/value tuple");
              return [...l12];
            });
          }
        } else throw new TypeError("Failed to construct 'Headers': The provided value is not of type '(sequence<sequence<ByteString>> or record<ByteString, ByteString>)");
        return a18 = a18.length > 0 ? a18.map(([u4, l12]) => (gr2(u4), jn(u4, String(l12)), [String(u4).toLowerCase(), String(l12)])) : void 0, super(a18), new Proxy(this, { get(u4, l12, p11) {
          switch (l12) {
            case "append":
            case "set":
              return (h6, g7) => (gr2(h6), jn(h6, String(g7)), URLSearchParams.prototype[l12].call(u4, String(h6).toLowerCase(), String(g7)));
            case "delete":
            case "has":
            case "getAll":
              return (h6) => (gr2(h6), URLSearchParams.prototype[l12].call(u4, String(h6).toLowerCase()));
            case "keys":
              return () => (u4.sort(), new Set(URLSearchParams.prototype.keys.call(u4)).keys());
            default:
              return Reflect.get(u4, l12, p11);
          }
        } });
      }
      get [Symbol.toStringTag]() {
        return this.constructor.name;
      }
      toString() {
        return Object.prototype.toString.call(this);
      }
      get(o8) {
        const a18 = this.getAll(o8);
        if (a18.length === 0) return null;
        let u4 = a18.join(", ");
        return /^content-encoding$/i.test(o8) && (u4 = u4.toLowerCase()), u4;
      }
      forEach(o8, a18 = void 0) {
        for (const u4 of this.keys()) Reflect.apply(o8, a18, [this.get(u4), u4, this]);
      }
      *values() {
        for (const o8 of this.keys()) yield this.get(o8);
      }
      *entries() {
        for (const o8 of this.keys()) yield [o8, this.get(o8)];
      }
      [Symbol.iterator]() {
        return this.entries();
      }
      raw() {
        return [...this.keys()].reduce((o8, a18) => (o8[a18] = this.getAll(a18), o8), {});
      }
      [Symbol.for("nodejs.util.inspect.custom")]() {
        return [...this.keys()].reduce((o8, a18) => {
          const u4 = this.getAll(a18);
          return a18 === "host" ? o8[a18] = u4[0] : o8[a18] = u4.length > 1 ? u4 : u4[0], o8;
        }, {});
      }
    };
    n11(Pr, "Headers");
    ye2 = Pr;
    Object.defineProperties(ye2.prototype, ["get", "entries", "forEach", "values"].reduce((i13, o8) => (i13[o8] = { enumerable: true }, i13), {}));
    n11(el, "fromRawHeaders");
    tl = /* @__PURE__ */ new Set([301, 302, 303, 307, 308]);
    Ln = n11((i13) => tl.has(i13), "isRedirect");
    se2 = Symbol("Response internals");
    xe3 = class xe4 extends Ue2 {
      constructor(o8 = null, a18 = {}) {
        super(o8, a18);
        const u4 = a18.status != null ? a18.status : 200, l12 = new ye2(a18.headers);
        if (o8 !== null && !l12.has("Content-Type")) {
          const p11 = gi(o8, this);
          p11 && l12.append("Content-Type", p11);
        }
        this[se2] = { type: "default", url: a18.url, status: u4, statusText: a18.statusText || "", headers: l12, counter: a18.counter, highWaterMark: a18.highWaterMark };
      }
      get type() {
        return this[se2].type;
      }
      get url() {
        return this[se2].url || "";
      }
      get status() {
        return this[se2].status;
      }
      get ok() {
        return this[se2].status >= 200 && this[se2].status < 300;
      }
      get redirected() {
        return this[se2].counter > 0;
      }
      get statusText() {
        return this[se2].statusText;
      }
      get headers() {
        return this[se2].headers;
      }
      get highWaterMark() {
        return this[se2].highWaterMark;
      }
      clone() {
        return new xe4(Fn(this, this.highWaterMark), { type: this.type, url: this.url, status: this.status, statusText: this.statusText, headers: this.headers, ok: this.ok, redirected: this.redirected, size: this.size, highWaterMark: this.highWaterMark });
      }
      static redirect(o8, a18 = 302) {
        if (!Ln(a18)) throw new RangeError('Failed to execute "redirect" on "response": Invalid status code');
        return new xe4(null, { headers: { location: new URL(o8).toString() }, status: a18 });
      }
      static error() {
        const o8 = new xe4(null, { status: 0, statusText: "" });
        return o8[se2].type = "error", o8;
      }
      static json(o8 = void 0, a18 = {}) {
        const u4 = JSON.stringify(o8);
        if (u4 === void 0) throw new TypeError("data is not JSON serializable");
        const l12 = new ye2(a18 && a18.headers);
        return l12.has("content-type") || l12.set("content-type", "application/json"), new xe4(u4, { ...a18, headers: l12 });
      }
      get [Symbol.toStringTag]() {
        return "Response";
      }
    };
    n11(xe3, "Response");
    le3 = xe3;
    Object.defineProperties(le3.prototype, { type: { enumerable: true }, url: { enumerable: true }, status: { enumerable: true }, ok: { enumerable: true }, redirected: { enumerable: true }, statusText: { enumerable: true }, headers: { enumerable: true }, clone: { enumerable: true } });
    rl = n11((i13) => {
      if (i13.search) return i13.search;
      const o8 = i13.href.length - 1, a18 = i13.hash || (i13.href[o8] === "#" ? "#" : "");
      return i13.href[o8 - a18.length] === "?" ? "?" : "";
    }, "getSearch");
    n11(_i, "stripURLForUseAsAReferrer");
    Si = /* @__PURE__ */ new Set(["", "no-referrer", "no-referrer-when-downgrade", "same-origin", "origin", "strict-origin", "origin-when-cross-origin", "strict-origin-when-cross-origin", "unsafe-url"]);
    nl = "strict-origin-when-cross-origin";
    n11(ol, "validateReferrerPolicy");
    n11(il, "isOriginPotentiallyTrustworthy");
    n11(ct2, "isUrlPotentiallyTrustworthy");
    n11(al, "determineRequestsReferrer");
    n11(sl, "parseReferrerPolicyFromHeader");
    $3 = Symbol("Request internals");
    At2 = n11((i13) => typeof i13 == "object" && typeof i13[$3] == "object", "isRequest");
    ll = hr2(() => {
    }, ".data is not a valid RequestInit property, use .body instead", "https://github.com/node-fetch/node-fetch/issues/1000 (request)");
    vr = class vr2 extends Ue2 {
      constructor(o8, a18 = {}) {
        let u4;
        if (At2(o8) ? u4 = new URL(o8.url) : (u4 = new URL(o8), o8 = {}), u4.username !== "" || u4.password !== "") throw new TypeError(`${u4} is an url with embedded credentials.`);
        let l12 = a18.method || o8.method || "GET";
        if (/^(delete|get|head|options|post|put)$/i.test(l12) && (l12 = l12.toUpperCase()), !At2(a18) && "data" in a18 && ll(), (a18.body != null || At2(o8) && o8.body !== null) && (l12 === "GET" || l12 === "HEAD")) throw new TypeError("Request with GET/HEAD method cannot have body");
        const p11 = a18.body ? a18.body : At2(o8) && o8.body !== null ? Fn(o8) : null;
        super(p11, { size: a18.size || o8.size || 0 });
        const h6 = new ye2(a18.headers || o8.headers || {});
        if (p11 !== null && !h6.has("Content-Type")) {
          const w11 = gi(p11, this);
          w11 && h6.set("Content-Type", w11);
        }
        let g7 = At2(o8) ? o8.signal : null;
        if ("signal" in a18 && (g7 = a18.signal), g7 != null && !Qs(g7)) throw new TypeError("Expected signal to be an instanceof AbortSignal or EventTarget");
        let A4 = a18.referrer == null ? o8.referrer : a18.referrer;
        if (A4 === "") A4 = "no-referrer";
        else if (A4) {
          const w11 = new URL(A4);
          A4 = /^about:(\/\/)?client$/.test(w11) ? "client" : w11;
        } else A4 = void 0;
        this[$3] = { method: l12, redirect: a18.redirect || o8.redirect || "follow", headers: h6, parsedURL: u4, signal: g7, referrer: A4 }, this.follow = a18.follow === void 0 ? o8.follow === void 0 ? 20 : o8.follow : a18.follow, this.compress = a18.compress === void 0 ? o8.compress === void 0 ? true : o8.compress : a18.compress, this.counter = a18.counter || o8.counter || 0, this.agent = a18.agent || o8.agent, this.highWaterMark = a18.highWaterMark || o8.highWaterMark || 16384, this.insecureHTTPParser = a18.insecureHTTPParser || o8.insecureHTTPParser || false, this.referrerPolicy = a18.referrerPolicy || o8.referrerPolicy || "";
      }
      get method() {
        return this[$3].method;
      }
      get url() {
        return qs(this[$3].parsedURL);
      }
      get headers() {
        return this[$3].headers;
      }
      get redirect() {
        return this[$3].redirect;
      }
      get signal() {
        return this[$3].signal;
      }
      get referrer() {
        if (this[$3].referrer === "no-referrer") return "";
        if (this[$3].referrer === "client") return "about:client";
        if (this[$3].referrer) return this[$3].referrer.toString();
      }
      get referrerPolicy() {
        return this[$3].referrerPolicy;
      }
      set referrerPolicy(o8) {
        this[$3].referrerPolicy = ol(o8);
      }
      clone() {
        return new vr2(this);
      }
      get [Symbol.toStringTag]() {
        return "Request";
      }
    };
    n11(vr, "Request");
    dt2 = vr;
    Object.defineProperties(dt2.prototype, { method: { enumerable: true }, url: { enumerable: true }, headers: { enumerable: true }, redirect: { enumerable: true }, clone: { enumerable: true }, signal: { enumerable: true }, referrer: { enumerable: true }, referrerPolicy: { enumerable: true } });
    ul = n11((i13) => {
      const { parsedURL: o8 } = i13[$3], a18 = new ye2(i13[$3].headers);
      a18.has("Accept") || a18.set("Accept", "*/*");
      let u4 = null;
      if (i13.body === null && /^(post|put)$/i.test(i13.method) && (u4 = "0"), i13.body !== null) {
        const g7 = Js(i13);
        typeof g7 == "number" && !Number.isNaN(g7) && (u4 = String(g7));
      }
      u4 && a18.set("Content-Length", u4), i13.referrerPolicy === "" && (i13.referrerPolicy = nl), i13.referrer && i13.referrer !== "no-referrer" ? i13[$3].referrer = al(i13) : i13[$3].referrer = "no-referrer", i13[$3].referrer instanceof URL && a18.set("Referer", i13.referrer), a18.has("User-Agent") || a18.set("User-Agent", "node-fetch"), i13.compress && !a18.has("Accept-Encoding") && a18.set("Accept-Encoding", "gzip, deflate, br");
      let { agent: l12 } = i13;
      typeof l12 == "function" && (l12 = l12(o8));
      const p11 = rl(o8), h6 = { path: o8.pathname + p11, method: i13.method, headers: a18[Symbol.for("nodejs.util.inspect.custom")](), insecureHTTPParser: i13.insecureHTTPParser, agent: l12 };
      return { parsedURL: o8, options: h6 };
    }, "getNodeRequestOptions");
    Hn = class Hn2 extends ft2 {
      constructor(o8, a18 = "aborted") {
        super(o8, a18);
      }
    };
    n11(Hn, "AbortError");
    _r = Hn;
    if (!globalThis.DOMException) try {
      const { MessageChannel: i13 } = __require("worker_threads"), o8 = new i13().port1, a18 = new ArrayBuffer();
      o8.postMessage(a18, [a18, a18]);
    } catch (i13) {
      i13.constructor.name === "DOMException" && (globalThis.DOMException = i13.constructor);
    }
    fl = globalThis.DOMException;
    cl = f13(fl);
    ({ stat: $n } = Is);
    dl = n11((i13, o8) => wi(ci(i13), i13, o8), "blobFromSync");
    hl = n11((i13, o8) => $n(i13).then((a18) => wi(a18, i13, o8)), "blobFrom");
    pl = n11((i13, o8) => $n(i13).then((a18) => Ri(a18, i13, o8)), "fileFrom");
    bl = n11((i13, o8) => Ri(ci(i13), i13, o8), "fileFromSync");
    wi = n11((i13, o8, a18 = "") => new ut([new Sr({ path: o8, size: i13.size, lastModified: i13.mtimeMs, start: 0 })], { type: a18 }), "fromBlob");
    Ri = n11((i13, o8, a18 = "") => new On([new Sr({ path: o8, size: i13.size, lastModified: i13.mtimeMs, start: 0 })], Fs(o8), { type: a18, lastModified: i13.mtimeMs }), "fromFile");
    Er = class Er2 {
      constructor(o8) {
        be3(this, Ne2, void 0);
        be3(this, He3, void 0);
        X3(this, Ne2, o8.path), X3(this, He3, o8.start), this.size = o8.size, this.lastModified = o8.lastModified;
      }
      slice(o8, a18) {
        return new Er2({ path: O4(this, Ne2), lastModified: this.lastModified, size: a18 - o8, start: O4(this, He3) + o8 });
      }
      async *stream() {
        const { mtimeMs: o8 } = await $n(O4(this, Ne2));
        if (o8 > this.lastModified) throw new cl("The requested file could not be read, typically due to permission problems that have occurred after a reference to a file was acquired.", "NotReadableError");
        yield* zs(O4(this, Ne2), { start: O4(this, He3), end: O4(this, He3) + this.size - 1 });
      }
      get [Symbol.toStringTag]() {
        return "Blob";
      }
    };
    Ne2 = /* @__PURE__ */ new WeakMap(), He3 = /* @__PURE__ */ new WeakMap(), n11(Er, "BlobDataItem");
    Sr = Er;
    ml = /* @__PURE__ */ new Set(["data:", "http:", "https:"]);
    n11(Ti, "fetch$1");
    n11(yl, "fixResponseChunkedTransferBadEnding");
    Ci = /* @__PURE__ */ new WeakMap();
    Dn = /* @__PURE__ */ new WeakMap();
    n11(W3, "pd");
    n11(Pi, "setCancelFlag");
    n11(ht3, "Event"), ht3.prototype = { get type() {
      return W3(this).event.type;
    }, get target() {
      return W3(this).eventTarget;
    }, get currentTarget() {
      return W3(this).currentTarget;
    }, composedPath() {
      const i13 = W3(this).currentTarget;
      return i13 == null ? [] : [i13];
    }, get NONE() {
      return 0;
    }, get CAPTURING_PHASE() {
      return 1;
    }, get AT_TARGET() {
      return 2;
    }, get BUBBLING_PHASE() {
      return 3;
    }, get eventPhase() {
      return W3(this).eventPhase;
    }, stopPropagation() {
      const i13 = W3(this);
      i13.stopped = true, typeof i13.event.stopPropagation == "function" && i13.event.stopPropagation();
    }, stopImmediatePropagation() {
      const i13 = W3(this);
      i13.stopped = true, i13.immediateStopped = true, typeof i13.event.stopImmediatePropagation == "function" && i13.event.stopImmediatePropagation();
    }, get bubbles() {
      return !!W3(this).event.bubbles;
    }, get cancelable() {
      return !!W3(this).event.cancelable;
    }, preventDefault() {
      Pi(W3(this));
    }, get defaultPrevented() {
      return W3(this).canceled;
    }, get composed() {
      return !!W3(this).event.composed;
    }, get timeStamp() {
      return W3(this).timeStamp;
    }, get srcElement() {
      return W3(this).eventTarget;
    }, get cancelBubble() {
      return W3(this).stopped;
    }, set cancelBubble(i13) {
      if (!i13) return;
      const o8 = W3(this);
      o8.stopped = true, typeof o8.event.cancelBubble == "boolean" && (o8.event.cancelBubble = true);
    }, get returnValue() {
      return !W3(this).canceled;
    }, set returnValue(i13) {
      i13 || Pi(W3(this));
    }, initEvent() {
    } }, Object.defineProperty(ht3.prototype, "constructor", { value: ht3, configurable: true, writable: true }), typeof window < "u" && typeof window.Event < "u" && (Object.setPrototypeOf(ht3.prototype, window.Event.prototype), Dn.set(window.Event.prototype, ht3));
    n11(vi, "defineRedirectDescriptor");
    n11(gl, "defineCallDescriptor");
    n11(_l, "defineWrapper");
    n11(Ei, "getWrapper");
    n11(Sl, "wrapEvent");
    n11(wl, "isStopped");
    n11(Rl, "setEventPhase");
    n11(Tl, "setCurrentTarget");
    n11(Ai, "setPassiveListener");
    Bi = /* @__PURE__ */ new WeakMap();
    ki = 1;
    Wi = 2;
    wr = 3;
    n11(Rr2, "isObject");
    n11(Bt2, "getListeners");
    n11(Cl, "defineEventAttributeDescriptor");
    n11(qi, "defineEventAttribute");
    n11(Oi, "defineCustomEventTarget");
    n11(Pe2, "EventTarget"), Pe2.prototype = { addEventListener(i13, o8, a18) {
      if (o8 == null) return;
      if (typeof o8 != "function" && !Rr2(o8)) throw new TypeError("'listener' should be a function or an object.");
      const u4 = Bt2(this), l12 = Rr2(a18), h6 = (l12 ? !!a18.capture : !!a18) ? ki : Wi, g7 = { listener: o8, listenerType: h6, passive: l12 && !!a18.passive, once: l12 && !!a18.once, next: null };
      let A4 = u4.get(i13);
      if (A4 === void 0) {
        u4.set(i13, g7);
        return;
      }
      let w11 = null;
      for (; A4 != null; ) {
        if (A4.listener === o8 && A4.listenerType === h6) return;
        w11 = A4, A4 = A4.next;
      }
      w11.next = g7;
    }, removeEventListener(i13, o8, a18) {
      if (o8 == null) return;
      const u4 = Bt2(this), p11 = (Rr2(a18) ? !!a18.capture : !!a18) ? ki : Wi;
      let h6 = null, g7 = u4.get(i13);
      for (; g7 != null; ) {
        if (g7.listener === o8 && g7.listenerType === p11) {
          h6 !== null ? h6.next = g7.next : g7.next !== null ? u4.set(i13, g7.next) : u4.delete(i13);
          return;
        }
        h6 = g7, g7 = g7.next;
      }
    }, dispatchEvent(i13) {
      if (i13 == null || typeof i13.type != "string") throw new TypeError('"event.type" should be a string.');
      const o8 = Bt2(this), a18 = i13.type;
      let u4 = o8.get(a18);
      if (u4 == null) return true;
      const l12 = Sl(this, i13);
      let p11 = null;
      for (; u4 != null; ) {
        if (u4.once ? p11 !== null ? p11.next = u4.next : u4.next !== null ? o8.set(a18, u4.next) : o8.delete(a18) : p11 = u4, Ai(l12, u4.passive ? u4.listener : null), typeof u4.listener == "function") try {
          u4.listener.call(this, l12);
        } catch (h6) {
          typeof console < "u" && typeof console.error == "function" && console.error(h6);
        }
        else u4.listenerType !== wr && typeof u4.listener.handleEvent == "function" && u4.listener.handleEvent(l12);
        if (wl(l12)) break;
        u4 = u4.next;
      }
      return Ai(l12, null), Rl(l12, 0), Tl(l12, null), !l12.defaultPrevented;
    } }, Object.defineProperty(Pe2.prototype, "constructor", { value: Pe2, configurable: true, writable: true }), typeof window < "u" && typeof window.EventTarget < "u" && Object.setPrototypeOf(Pe2.prototype, window.EventTarget.prototype);
    Vn = class Vn2 extends Pe2 {
      constructor() {
        throw super(), new TypeError("AbortSignal cannot be constructed directly");
      }
      get aborted() {
        const o8 = Tr.get(this);
        if (typeof o8 != "boolean") throw new TypeError(`Expected 'this' to be an 'AbortSignal' object, but got ${this === null ? "null" : typeof this}`);
        return o8;
      }
    };
    n11(Vn, "AbortSignal");
    pt3 = Vn;
    qi(pt3.prototype, "abort");
    n11(Pl, "createAbortSignal");
    n11(vl, "abortSignal");
    Tr = /* @__PURE__ */ new WeakMap();
    Object.defineProperties(pt3.prototype, { aborted: { enumerable: true } }), typeof Symbol == "function" && typeof Symbol.toStringTag == "symbol" && Object.defineProperty(pt3.prototype, Symbol.toStringTag, { configurable: true, value: "AbortSignal" });
    Mn = (It3 = class {
      constructor() {
        zi.set(this, Pl());
      }
      get signal() {
        return Ii(this);
      }
      abort() {
        vl(Ii(this));
      }
    }, n11(It3, "AbortController"), It3);
    zi = /* @__PURE__ */ new WeakMap();
    n11(Ii, "getSignal"), Object.defineProperties(Mn.prototype, { signal: { enumerable: true }, abort: { enumerable: true } }), typeof Symbol == "function" && typeof Symbol.toStringTag == "symbol" && Object.defineProperty(Mn.prototype, Symbol.toStringTag, { configurable: true, value: "AbortController" });
    El = Object.defineProperty;
    Al = n11((i13, o8) => El(i13, "name", { value: o8, configurable: true }), "e");
    Fi = Ti;
    ji();
    n11(ji, "s"), Al(ji, "checkNodeEnvironment");
  }
});

// node_modules/node-fetch-native/dist/index.mjs
function l9() {
  return !o6 && globalThis.fetch ? globalThis.fetch : Fi;
}
var a15, t19, f14, g6, o6, s14, T2, R8, u2, d10, $4, C4, A3;
var init_dist2 = __esm({
  "node_modules/node-fetch-native/dist/index.mjs"() {
    init_node();
    init_node();
    init_node_fetch_native_1a4a356d();
    a15 = Object.defineProperty;
    t19 = (e14, r7) => a15(e14, "name", { value: r7, configurable: true });
    f14 = Object.defineProperty;
    g6 = t19((e14, r7) => f14(e14, "name", { value: r7, configurable: true }), "e");
    o6 = !!globalThis.process?.env?.FORCE_NODE_FETCH;
    t19(l9, "p"), g6(l9, "_getFetch");
    s14 = l9();
    T2 = !o6 && globalThis.Blob || ut;
    R8 = !o6 && globalThis.File || On;
    u2 = !o6 && globalThis.FormData || br;
    d10 = !o6 && globalThis.Headers || ye2;
    $4 = !o6 && globalThis.Request || dt2;
    C4 = !o6 && globalThis.Response || le3;
    A3 = !o6 && globalThis.AbortController || Mn;
  }
});

// node_modules/destr/dist/index.mjs
function jsonParseTransform(key, value) {
  if (key === "__proto__" || key === "constructor" && value && typeof value === "object" && "prototype" in value) {
    warnKeyDropped(key);
    return;
  }
  return value;
}
function warnKeyDropped(key) {
  console.warn(`[destr] Dropping "${key}" key to prevent prototype pollution.`);
}
function destr(value, options = {}) {
  if (typeof value !== "string") {
    return value;
  }
  const _value = value.trim();
  if (
    // eslint-disable-next-line unicorn/prefer-at
    value[0] === '"' && value.endsWith('"') && !value.includes("\\")
  ) {
    return _value.slice(1, -1);
  }
  if (_value.length <= 9) {
    const _lval = _value.toLowerCase();
    if (_lval === "true") {
      return true;
    }
    if (_lval === "false") {
      return false;
    }
    if (_lval === "undefined") {
      return void 0;
    }
    if (_lval === "null") {
      return null;
    }
    if (_lval === "nan") {
      return Number.NaN;
    }
    if (_lval === "infinity") {
      return Number.POSITIVE_INFINITY;
    }
    if (_lval === "-infinity") {
      return Number.NEGATIVE_INFINITY;
    }
  }
  if (!JsonSigRx.test(value)) {
    if (options.strict) {
      throw new SyntaxError("[destr] Invalid JSON");
    }
    return value;
  }
  try {
    if (suspectProtoRx.test(value) || suspectConstructorRx.test(value)) {
      if (options.strict) {
        throw new Error("[destr] Possible prototype pollution");
      }
      return JSON.parse(value, jsonParseTransform);
    }
    return JSON.parse(value);
  } catch (error) {
    if (options.strict) {
      throw error;
    }
    return value;
  }
}
var suspectProtoRx, suspectConstructorRx, JsonSigRx;
var init_dist3 = __esm({
  "node_modules/destr/dist/index.mjs"() {
    suspectProtoRx = /"(?:_|\\u0{2}5[Ff]){2}(?:p|\\u0{2}70)(?:r|\\u0{2}72)(?:o|\\u0{2}6[Ff])(?:t|\\u0{2}74)(?:o|\\u0{2}6[Ff])(?:_|\\u0{2}5[Ff]){2}"\s*:/;
    suspectConstructorRx = /"(?:c|\\u0063)(?:o|\\u006[Ff])(?:n|\\u006[Ee])(?:s|\\u0073)(?:t|\\u0074)(?:r|\\u0072)(?:u|\\u0075)(?:c|\\u0063)(?:t|\\u0074)(?:o|\\u006[Ff])(?:r|\\u0072)"\s*:/;
    JsonSigRx = /^\s*["[{]|^\s*-?\d{1,16}(\.\d{1,17})?([Ee][+-]?\d+)?\s*$/;
  }
});

// node_modules/ufo/dist/index.mjs
function encode(text4) {
  return encodeURI("" + text4).replace(ENC_PIPE_RE, "|");
}
function encodeQueryValue(input2) {
  return encode(typeof input2 === "string" ? input2 : JSON.stringify(input2)).replace(PLUS_RE, "%2B").replace(ENC_SPACE_RE, "+").replace(HASH_RE, "%23").replace(AMPERSAND_RE, "%26").replace(ENC_BACKTICK_RE, "`").replace(ENC_CARET_RE, "^").replace(SLASH_RE, "%2F");
}
function encodeQueryKey(text4) {
  return encodeQueryValue(text4).replace(EQUAL_RE, "%3D");
}
function decode(text4 = "") {
  try {
    return decodeURIComponent("" + text4);
  } catch {
    return "" + text4;
  }
}
function decodeQueryKey(text4) {
  return decode(text4.replace(PLUS_RE, " "));
}
function decodeQueryValue(text4) {
  return decode(text4.replace(PLUS_RE, " "));
}
function parseQuery(parametersString = "") {
  const object = {};
  if (parametersString[0] === "?") {
    parametersString = parametersString.slice(1);
  }
  for (const parameter of parametersString.split("&")) {
    const s36 = parameter.match(/([^=]+)=?(.*)/) || [];
    if (s36.length < 2) {
      continue;
    }
    const key = decodeQueryKey(s36[1]);
    if (key === "__proto__" || key === "constructor") {
      continue;
    }
    const value = decodeQueryValue(s36[2] || "");
    if (object[key] === void 0) {
      object[key] = value;
    } else if (Array.isArray(object[key])) {
      object[key].push(value);
    } else {
      object[key] = [object[key], value];
    }
  }
  return object;
}
function encodeQueryItem(key, value) {
  if (typeof value === "number" || typeof value === "boolean") {
    value = String(value);
  }
  if (!value) {
    return encodeQueryKey(key);
  }
  if (Array.isArray(value)) {
    return value.map((_value) => `${encodeQueryKey(key)}=${encodeQueryValue(_value)}`).join("&");
  }
  return `${encodeQueryKey(key)}=${encodeQueryValue(value)}`;
}
function stringifyQuery(query2) {
  return Object.keys(query2).filter((k5) => query2[k5] !== void 0).map((k5) => encodeQueryItem(k5, query2[k5])).filter(Boolean).join("&");
}
function hasProtocol(inputString, opts = {}) {
  if (typeof opts === "boolean") {
    opts = { acceptRelative: opts };
  }
  if (opts.strict) {
    return PROTOCOL_STRICT_REGEX.test(inputString);
  }
  return PROTOCOL_REGEX.test(inputString) || (opts.acceptRelative ? PROTOCOL_RELATIVE_REGEX.test(inputString) : false);
}
function hasTrailingSlash(input2 = "", respectQueryAndFragment) {
  if (!respectQueryAndFragment) {
    return input2.endsWith("/");
  }
  return TRAILING_SLASH_RE.test(input2);
}
function withoutTrailingSlash(input2 = "", respectQueryAndFragment) {
  if (!respectQueryAndFragment) {
    return (hasTrailingSlash(input2) ? input2.slice(0, -1) : input2) || "/";
  }
  if (!hasTrailingSlash(input2, true)) {
    return input2 || "/";
  }
  let path = input2;
  let fragment = "";
  const fragmentIndex = input2.indexOf("#");
  if (fragmentIndex >= 0) {
    path = input2.slice(0, fragmentIndex);
    fragment = input2.slice(fragmentIndex);
  }
  const [s0, ...s36] = path.split("?");
  const cleanPath = s0.endsWith("/") ? s0.slice(0, -1) : s0;
  return (cleanPath || "/") + (s36.length > 0 ? `?${s36.join("?")}` : "") + fragment;
}
function withTrailingSlash(input2 = "", respectQueryAndFragment) {
  if (!respectQueryAndFragment) {
    return input2.endsWith("/") ? input2 : input2 + "/";
  }
  if (hasTrailingSlash(input2, true)) {
    return input2 || "/";
  }
  let path = input2;
  let fragment = "";
  const fragmentIndex = input2.indexOf("#");
  if (fragmentIndex >= 0) {
    path = input2.slice(0, fragmentIndex);
    fragment = input2.slice(fragmentIndex);
    if (!path) {
      return fragment;
    }
  }
  const [s0, ...s36] = path.split("?");
  return s0 + "/" + (s36.length > 0 ? `?${s36.join("?")}` : "") + fragment;
}
function withBase(input2, base) {
  if (isEmptyURL(base) || hasProtocol(input2)) {
    return input2;
  }
  const _base = withoutTrailingSlash(base);
  if (input2.startsWith(_base)) {
    return input2;
  }
  return joinURL(_base, input2);
}
function withQuery(input2, query2) {
  const parsed = parseURL(input2);
  const mergedQuery = { ...parseQuery(parsed.search), ...query2 };
  parsed.search = stringifyQuery(mergedQuery);
  return stringifyParsedURL(parsed);
}
function isEmptyURL(url) {
  return !url || url === "/";
}
function isNonEmptyURL(url) {
  return url && url !== "/";
}
function joinURL(base, ...input2) {
  let url = base || "";
  for (const segment of input2.filter((url2) => isNonEmptyURL(url2))) {
    if (url) {
      const _segment = segment.replace(JOIN_LEADING_SLASH_RE, "");
      url = withTrailingSlash(url) + _segment;
    } else {
      url = segment;
    }
  }
  return url;
}
function parseURL(input2 = "", defaultProto) {
  const _specialProtoMatch = input2.match(
    /^[\s\0]*(blob:|data:|javascript:|vbscript:)(.*)/i
  );
  if (_specialProtoMatch) {
    const [, _proto, _pathname = ""] = _specialProtoMatch;
    return {
      protocol: _proto.toLowerCase(),
      pathname: _pathname,
      href: _proto + _pathname,
      auth: "",
      host: "",
      search: "",
      hash: ""
    };
  }
  if (!hasProtocol(input2, { acceptRelative: true })) {
    return defaultProto ? parseURL(defaultProto + input2) : parsePath(input2);
  }
  const [, protocol = "", auth, hostAndPath = ""] = input2.replace(/\\/g, "/").match(/^[\s\0]*([\w+.-]{2,}:)?\/\/([^/@]+@)?(.*)/) || [];
  let [, host = "", path = ""] = hostAndPath.match(/([^#/?]*)(.*)?/) || [];
  if (protocol === "file:") {
    path = path.replace(/\/(?=[A-Za-z]:)/, "");
  }
  const { pathname, search, hash } = parsePath(path);
  return {
    protocol: protocol.toLowerCase(),
    auth: auth ? auth.slice(0, Math.max(0, auth.length - 1)) : "",
    host,
    pathname,
    search,
    hash,
    [protocolRelative]: !protocol
  };
}
function parsePath(input2 = "") {
  const [pathname = "", search = "", hash = ""] = (input2.match(/([^#?]*)(\?[^#]*)?(#.*)?/) || []).splice(1);
  return {
    pathname,
    search,
    hash
  };
}
function stringifyParsedURL(parsed) {
  const pathname = parsed.pathname || "";
  const search = parsed.search ? (parsed.search.startsWith("?") ? "" : "?") + parsed.search : "";
  const hash = parsed.hash || "";
  const auth = parsed.auth ? parsed.auth + "@" : "";
  const host = parsed.host || "";
  const proto = parsed.protocol || parsed[protocolRelative] ? (parsed.protocol || "") + "//" : "";
  return proto + auth + host + pathname + search + hash;
}
var r5, HASH_RE, AMPERSAND_RE, SLASH_RE, EQUAL_RE, PLUS_RE, ENC_CARET_RE, ENC_BACKTICK_RE, ENC_PIPE_RE, ENC_SPACE_RE, PROTOCOL_STRICT_REGEX, PROTOCOL_REGEX, PROTOCOL_RELATIVE_REGEX, TRAILING_SLASH_RE, JOIN_LEADING_SLASH_RE, protocolRelative;
var init_dist4 = __esm({
  "node_modules/ufo/dist/index.mjs"() {
    r5 = String.fromCharCode;
    HASH_RE = /#/g;
    AMPERSAND_RE = /&/g;
    SLASH_RE = /\//g;
    EQUAL_RE = /=/g;
    PLUS_RE = /\+/g;
    ENC_CARET_RE = /%5e/gi;
    ENC_BACKTICK_RE = /%60/gi;
    ENC_PIPE_RE = /%7c/gi;
    ENC_SPACE_RE = /%20/gi;
    PROTOCOL_STRICT_REGEX = /^[\s\w\0+.-]{2,}:([/\\]{1,2})/;
    PROTOCOL_REGEX = /^[\s\w\0+.-]{2,}:([/\\]{2})?/;
    PROTOCOL_RELATIVE_REGEX = /^([/\\]\s*){2,}[^/\\]/;
    TRAILING_SLASH_RE = /\/$|\/\?|\/#/;
    JOIN_LEADING_SLASH_RE = /^\.?\//;
    protocolRelative = Symbol.for("ufo:protocolRelative");
  }
});

// node_modules/ofetch/dist/shared/ofetch.03887fc3.mjs
function createFetchError(ctx) {
  const errorMessage = ctx.error?.message || ctx.error?.toString() || "";
  const method = ctx.request?.method || ctx.options?.method || "GET";
  const url = ctx.request?.url || String(ctx.request) || "/";
  const requestStr = `[${method}] ${JSON.stringify(url)}`;
  const statusStr = ctx.response ? `${ctx.response.status} ${ctx.response.statusText}` : "<no response>";
  const message = `${requestStr}: ${statusStr}${errorMessage ? ` ${errorMessage}` : ""}`;
  const fetchError = new FetchError(
    message,
    ctx.error ? { cause: ctx.error } : void 0
  );
  for (const key of ["request", "options", "response"]) {
    Object.defineProperty(fetchError, key, {
      get() {
        return ctx[key];
      }
    });
  }
  for (const [key, refKey] of [
    ["data", "_data"],
    ["status", "status"],
    ["statusCode", "status"],
    ["statusText", "statusText"],
    ["statusMessage", "statusText"]
  ]) {
    Object.defineProperty(fetchError, key, {
      get() {
        return ctx.response && ctx.response[refKey];
      }
    });
  }
  return fetchError;
}
function isPayloadMethod(method = "GET") {
  return payloadMethods.has(method.toUpperCase());
}
function isJSONSerializable(value) {
  if (value === void 0) {
    return false;
  }
  const t20 = typeof value;
  if (t20 === "string" || t20 === "number" || t20 === "boolean" || t20 === null) {
    return true;
  }
  if (t20 !== "object") {
    return false;
  }
  if (Array.isArray(value)) {
    return true;
  }
  if (value.buffer) {
    return false;
  }
  return value.constructor && value.constructor.name === "Object" || typeof value.toJSON === "function";
}
function detectResponseType(_contentType = "") {
  if (!_contentType) {
    return "json";
  }
  const contentType = _contentType.split(";").shift() || "";
  if (JSON_RE.test(contentType)) {
    return "json";
  }
  if (textTypes.has(contentType) || contentType.startsWith("text/")) {
    return "text";
  }
  return "blob";
}
function resolveFetchOptions(request, input2, defaults, Headers3) {
  const headers = mergeHeaders(
    input2?.headers ?? request?.headers,
    defaults?.headers,
    Headers3
  );
  let query2;
  if (defaults?.query || defaults?.params || input2?.params || input2?.query) {
    query2 = {
      ...defaults?.params,
      ...defaults?.query,
      ...input2?.params,
      ...input2?.query
    };
  }
  return {
    ...defaults,
    ...input2,
    query: query2,
    params: query2,
    headers
  };
}
function mergeHeaders(input2, defaults, Headers3) {
  if (!defaults) {
    return new Headers3(input2);
  }
  const headers = new Headers3(defaults);
  if (input2) {
    for (const [key, value] of Symbol.iterator in input2 || Array.isArray(input2) ? input2 : new Headers3(input2)) {
      headers.set(key, value);
    }
  }
  return headers;
}
async function callHooks(context, hooks) {
  if (hooks) {
    if (Array.isArray(hooks)) {
      for (const hook of hooks) {
        await hook(context);
      }
    } else {
      await hooks(context);
    }
  }
}
function createFetch(globalOptions = {}) {
  const {
    fetch: fetch2 = globalThis.fetch,
    Headers: Headers3 = globalThis.Headers,
    AbortController: AbortController3 = globalThis.AbortController
  } = globalOptions;
  async function onError(context) {
    const isAbort = context.error && context.error.name === "AbortError" && !context.options.timeout || false;
    if (context.options.retry !== false && !isAbort) {
      let retries;
      if (typeof context.options.retry === "number") {
        retries = context.options.retry;
      } else {
        retries = isPayloadMethod(context.options.method) ? 0 : 1;
      }
      const responseCode = context.response && context.response.status || 500;
      if (retries > 0 && (Array.isArray(context.options.retryStatusCodes) ? context.options.retryStatusCodes.includes(responseCode) : retryStatusCodes.has(responseCode))) {
        const retryDelay = typeof context.options.retryDelay === "function" ? context.options.retryDelay(context) : context.options.retryDelay || 0;
        if (retryDelay > 0) {
          await new Promise((resolve) => setTimeout(resolve, retryDelay));
        }
        return $fetchRaw(context.request, {
          ...context.options,
          retry: retries - 1
        });
      }
    }
    const error = createFetchError(context);
    if (Error.captureStackTrace) {
      Error.captureStackTrace(error, $fetchRaw);
    }
    throw error;
  }
  const $fetchRaw = async function $fetchRaw2(_request, _options = {}) {
    const context = {
      request: _request,
      options: resolveFetchOptions(
        _request,
        _options,
        globalOptions.defaults,
        Headers3
      ),
      response: void 0,
      error: void 0
    };
    if (context.options.method) {
      context.options.method = context.options.method.toUpperCase();
    }
    if (context.options.onRequest) {
      await callHooks(context, context.options.onRequest);
    }
    if (typeof context.request === "string") {
      if (context.options.baseURL) {
        context.request = withBase(context.request, context.options.baseURL);
      }
      if (context.options.query) {
        context.request = withQuery(context.request, context.options.query);
        delete context.options.query;
      }
      if ("query" in context.options) {
        delete context.options.query;
      }
      if ("params" in context.options) {
        delete context.options.params;
      }
    }
    if (context.options.body && isPayloadMethod(context.options.method)) {
      if (isJSONSerializable(context.options.body)) {
        context.options.body = typeof context.options.body === "string" ? context.options.body : JSON.stringify(context.options.body);
        context.options.headers = new Headers3(context.options.headers || {});
        if (!context.options.headers.has("content-type")) {
          context.options.headers.set("content-type", "application/json");
        }
        if (!context.options.headers.has("accept")) {
          context.options.headers.set("accept", "application/json");
        }
      } else if (
        // ReadableStream Body
        "pipeTo" in context.options.body && typeof context.options.body.pipeTo === "function" || // Node.js Stream Body
        typeof context.options.body.pipe === "function"
      ) {
        if (!("duplex" in context.options)) {
          context.options.duplex = "half";
        }
      }
    }
    let abortTimeout;
    if (!context.options.signal && context.options.timeout) {
      const controller = new AbortController3();
      abortTimeout = setTimeout(() => {
        const error = new Error(
          "[TimeoutError]: The operation was aborted due to timeout"
        );
        error.name = "TimeoutError";
        error.code = 23;
        controller.abort(error);
      }, context.options.timeout);
      context.options.signal = controller.signal;
    }
    try {
      context.response = await fetch2(
        context.request,
        context.options
      );
    } catch (error) {
      context.error = error;
      if (context.options.onRequestError) {
        await callHooks(
          context,
          context.options.onRequestError
        );
      }
      return await onError(context);
    } finally {
      if (abortTimeout) {
        clearTimeout(abortTimeout);
      }
    }
    const hasBody = (context.response.body || // https://github.com/unjs/ofetch/issues/324
    // https://github.com/unjs/ofetch/issues/294
    // https://github.com/JakeChampion/fetch/issues/1454
    context.response._bodyInit) && !nullBodyResponses.has(context.response.status) && context.options.method !== "HEAD";
    if (hasBody) {
      const responseType = (context.options.parseResponse ? "json" : context.options.responseType) || detectResponseType(context.response.headers.get("content-type") || "");
      switch (responseType) {
        case "json": {
          const data = await context.response.text();
          const parseFunction = context.options.parseResponse || destr;
          context.response._data = parseFunction(data);
          break;
        }
        case "stream": {
          context.response._data = context.response.body || context.response._bodyInit;
          break;
        }
        default: {
          context.response._data = await context.response[responseType]();
        }
      }
    }
    if (context.options.onResponse) {
      await callHooks(
        context,
        context.options.onResponse
      );
    }
    if (!context.options.ignoreResponseError && context.response.status >= 400 && context.response.status < 600) {
      if (context.options.onResponseError) {
        await callHooks(
          context,
          context.options.onResponseError
        );
      }
      return await onError(context);
    }
    return context.response;
  };
  const $fetch = async function $fetch2(request, options) {
    const r7 = await $fetchRaw(request, options);
    return r7._data;
  };
  $fetch.raw = $fetchRaw;
  $fetch.native = (...args) => fetch2(...args);
  $fetch.create = (defaultOptions = {}, customGlobalOptions = {}) => createFetch({
    ...globalOptions,
    ...customGlobalOptions,
    defaults: {
      ...globalOptions.defaults,
      ...customGlobalOptions.defaults,
      ...defaultOptions
    }
  });
  return $fetch;
}
var FetchError, payloadMethods, textTypes, JSON_RE, retryStatusCodes, nullBodyResponses;
var init_ofetch_03887fc3 = __esm({
  "node_modules/ofetch/dist/shared/ofetch.03887fc3.mjs"() {
    init_dist3();
    init_dist4();
    FetchError = class extends Error {
      constructor(message, opts) {
        super(message, opts);
        this.name = "FetchError";
        if (opts?.cause && !this.cause) {
          this.cause = opts.cause;
        }
      }
    };
    payloadMethods = new Set(
      Object.freeze(["PATCH", "POST", "PUT", "DELETE"])
    );
    textTypes = /* @__PURE__ */ new Set([
      "image/svg",
      "application/xml",
      "application/xhtml",
      "application/html"
    ]);
    JSON_RE = /^application\/(?:[\w!#$%&*.^`~-]*\+)?json(;.+)?$/i;
    retryStatusCodes = /* @__PURE__ */ new Set([
      408,
      // Request Timeout
      409,
      // Conflict
      425,
      // Too Early (Experimental)
      429,
      // Too Many Requests
      500,
      // Internal Server Error
      502,
      // Bad Gateway
      503,
      // Service Unavailable
      504
      // Gateway Timeout
    ]);
    nullBodyResponses = /* @__PURE__ */ new Set([101, 204, 205, 304]);
  }
});

// node_modules/ofetch/dist/node.mjs
import http from "node:http";
import https from "node:https";
function createNodeFetch() {
  const useKeepAlive = JSON.parse(process.env.FETCH_KEEP_ALIVE || "false");
  if (!useKeepAlive) {
    return s14;
  }
  const agentOptions = { keepAlive: true };
  const httpAgent = new http.Agent(agentOptions);
  const httpsAgent = new https.Agent(agentOptions);
  const nodeFetchOptions = {
    agent(parsedURL) {
      return parsedURL.protocol === "http:" ? httpAgent : httpsAgent;
    }
  };
  return function nodeFetchWithKeepAlive(input2, init) {
    return s14(input2, { ...nodeFetchOptions, ...init });
  };
}
var fetch, Headers2, AbortController2, ofetch;
var init_node2 = __esm({
  "node_modules/ofetch/dist/node.mjs"() {
    init_dist2();
    init_ofetch_03887fc3();
    init_dist3();
    fetch = globalThis.fetch ? (...args) => globalThis.fetch(...args) : createNodeFetch();
    Headers2 = globalThis.Headers || d10;
    AbortController2 = globalThis.AbortController || A3;
    ofetch = createFetch({ fetch, Headers: Headers2, AbortController: AbortController2 });
  }
});

// dist/server/chunks/chunk-BDsA7YFO.js
function getConfig(payload, params) {
  return typeof payload === "function" ? payload(params) : payload;
}
var createRequestInstance, createRequestFx, createInternalRequestFx, createCommonRequestFx, HTTP_METHODS;
var init_chunk_BDsA7YFO = __esm({
  "dist/server/chunks/chunk-BDsA7YFO.js"() {
    "use strict";
    init_effector();
    init_node2();
    createRequestInstance = ({
      baseURL,
      headers,
      payload,
      withTokenInHeaders
    }) => b((params) => {
      var _a;
      const {
        url,
        ...fetchOptions
      } = getConfig(payload, params);
      const newHeaders = new Headers(headers);
      if (withTokenInHeaders) {
        newHeaders.append("Authorization", `Token ${(_a = localStorage.getItem("$uuid")) == null ? void 0 : _a.replaceAll('"', "")}`);
      }
      return ofetch(url, {
        ...fetchOptions,
        headers: newHeaders,
        baseURL
      });
    }, {
      name: "createRequestInstance",
      sid: "-2cl3s"
    });
    createRequestFx = (params) => (payload) => createRequestInstance({
      ...params,
      payload
    });
    createInternalRequestFx = createRequestFx({
      baseURL: "https://api.dev.cognitivelab.ru",
      withTokenInHeaders: true
    });
    createCommonRequestFx = createRequestFx({
      baseURL: "https://api.dev.cognitivelab.ru"
    });
    HTTP_METHODS = {
      POST: "POST",
      PUT: "PUT",
      PATCH: "PATCH",
      DELETE: "DELETE",
      GET: "GET"
    };
  }
});

// dist/server/chunks/chunk-CiwkTWKi.js
var import_jsx_runtime165, API, getPersonalityTypesQuery, getFreeResultQuery, $freeResult, $freeContent, $navigationIconMap, manImage;
var init_chunk_CiwkTWKi = __esm({
  "dist/server/chunks/chunk-CiwkTWKi.js"() {
    "use strict";
    import_jsx_runtime165 = __toESM(require_jsx_runtime(), 1);
    init_ssr();
    init_effector();
    init_core();
    init_chunk_BDsA7YFO();
    API = {
      URL: "https://api.dev.cognitivelab.ru",
      PERSONALITY_TYPES: "/api/v1/surveys/personality-types",
      PERSONALITY_TYPE: (type2) => `/api/v1/surveys/personality-types/${type2}`,
      SEND_FREE_EMAIL: "/api/v1/surveys/free-report/email",
      GET_REGULAR_PRICE: "/api/v1/payments/regular-price",
      GET_PROMO_PRICE: (promocode) => `/api/v1/payments/promo-code-price?promo_code=${promocode}`,
      PURCHASE_REPORT: "/api/v1/payments/purchase-report"
    };
    getPersonalityTypesQuery = ke({
      sid: "-6flptg",
      fn: () => Ir({
        effect: createCommonRequestFx(() => ({
          url: API.PERSONALITY_TYPES
        })),
        mapData: ({
          result
        }) => result.map((el2) => el2.types).flat()
      }),
      name: "getPersonalityTypesQuery",
      method: "createQuery"
    });
    ke({
      sid: "vyqxv0",
      fn: () => Ir({
        effect: createCommonRequestFx((type2) => ({
          url: API.PERSONALITY_TYPE(type2)
        }))
      }),
      name: "getPersonalityTypeQuery",
      method: "createQuery"
    });
    getFreeResultQuery = ke({
      sid: "3ardgj",
      fn: () => Ir({
        effect: createInternalRequestFx(() => ({
          url: `/api/v1/surveys/free-report`
        }))
      }),
      name: "getFreeResultQuery",
      method: "createQuery"
    });
    $freeResult = h(null, {
      name: "$freeResult",
      sid: "v8m1c0"
    });
    $freeContent = h([], {
      name: "$freeContent",
      sid: "-g3cled"
    });
    $navigationIconMap = h({
      \u0412\u0432\u0435\u0434\u0435\u043D\u0438\u0435: /* @__PURE__ */ (0, import_jsx_runtime165.jsx)(H2, { size: 20 }),
      "\u0412\u0430\u0448 \u043F\u0441\u0438\u0445\u043E\u043B\u043E\u0433\u0438\u0447\u0435\u0441\u043A\u0438\u0439 \u043F\u043E\u0440\u0442\u0440\u0435\u0442": /* @__PURE__ */ (0, import_jsx_runtime165.jsx)(H2, { size: 20 }),
      "\u041A\u043B\u044E\u0447\u0435\u0432\u044B\u0435 \u0447\u0435\u0440\u0442\u044B \u0432\u0430\u0448\u0435\u0433\u043E \u0445\u0430\u0440\u0430\u043A\u0442\u0435\u0440\u0430": /* @__PURE__ */ (0, import_jsx_runtime165.jsx)(H2, { size: 20 }),
      "\u041A\u0430\u043A \u0432\u044B \u0434\u0443\u043C\u0430\u0435\u0442\u0435 \u0438 \u043F\u0440\u0438\u043D\u0438\u043C\u0430\u0435\u0442\u0435 \u0440\u0435\u0448\u0435\u043D\u0438\u044F": /* @__PURE__ */ (0, import_jsx_runtime165.jsx)(H2, { size: 20 }),
      "\u0412\u0430\u0448 \u043A\u0430\u0440\u044C\u0435\u0440\u043D\u044B\u0439 \u043F\u0443\u0442\u044C": /* @__PURE__ */ (0, import_jsx_runtime165.jsx)(H2, { size: 20 }),
      "\u0421\u0438\u043B\u044C\u043D\u044B\u0435 \u0438 \u0441\u043B\u0430\u0431\u044B\u0435 \u0441\u0442\u043E\u0440\u043E\u043D\u044B": /* @__PURE__ */ (0, import_jsx_runtime165.jsx)(H2, { size: 20 }),
      "\u0412\u0430\u0448 \u0441\u0442\u0438\u043B\u044C \u043E\u0431\u0449\u0435\u043D\u0438\u044F \u0438 \u0432\u0437\u0430\u0438\u043C\u043E\u0434\u0435\u0439\u0441\u0442\u0432\u0438\u044F \u0441 \u0434\u0440\u0443\u0433\u0438\u043C\u0438": /* @__PURE__ */ (0, import_jsx_runtime165.jsx)(H2, { size: 20 }),
      "\u041B\u043E\u0432\u0443\u0448\u043A\u0438 \u0432\u0430\u0448\u0435\u0433\u043E \u0440\u0430\u0437\u0443\u043C\u0430 - \u043A\u0430\u043A \u0438\u0445 \u0440\u0430\u0441\u043F\u043E\u0437\u043D\u0430\u0442\u044C \u0438 \u043F\u0440\u0435\u043E\u0434\u043E\u043B\u0435\u0442\u044C": /* @__PURE__ */ (0, import_jsx_runtime165.jsx)(H2, { size: 20 }),
      "\u041A\u0430\u043A \u0441\u0442\u0440\u0435\u0441\u0441 \u0432\u043B\u0438\u044F\u0435\u0442 \u043D\u0430 \u0432\u0430\u0448\u0438 \u0440\u0435\u0448\u0435\u043D\u0438\u044F": /* @__PURE__ */ (0, import_jsx_runtime165.jsx)(H2, { size: 20 }),
      "\u041A\u0430\u043A \u0441\u043E\u0445\u0440\u0430\u043D\u0438\u0442\u044C \u0432\u043D\u0443\u0442\u0440\u0435\u043D\u043D\u0438\u0439 \u0431\u0430\u043B\u0430\u043D\u0441 \u0438 \u044D\u043D\u0435\u0440\u0433\u0438\u044E": /* @__PURE__ */ (0, import_jsx_runtime165.jsx)(H2, { size: 20 }),
      "\u0417\u043D\u0430\u043C\u0435\u043D\u0438\u0442\u043E\u0441\u0442\u0438, \u043F\u043E\u0445\u043E\u0436\u0438\u0435 \u043D\u0430 \u0432\u0430\u0441": /* @__PURE__ */ (0, import_jsx_runtime165.jsx)(H2, { size: 20 }),
      "\u041A\u0430\u043A \u0432\u044B \u0441\u0442\u0440\u043E\u0438\u0442\u0435 \u043E\u0442\u043D\u043E\u0448\u0435\u043D\u0438\u044F \u0441 \u043E\u043A\u0440\u0443\u0436\u0430\u044E\u0449\u0438\u043C\u0438": /* @__PURE__ */ (0, import_jsx_runtime165.jsx)(H2, { size: 20 }),
      "\u041B\u0438\u0447\u043D\u044B\u0439 \u043F\u043B\u0430\u043D \u0440\u0430\u0437\u0432\u0438\u0442\u0438\u044F": /* @__PURE__ */ (0, import_jsx_runtime165.jsx)(H2, { size: 20 }),
      \u0417\u0430\u043A\u043B\u044E\u0447\u0435\u043D\u0438\u0435: /* @__PURE__ */ (0, import_jsx_runtime165.jsx)(H2, { size: 20 })
    }, {
      name: "$navigationIconMap",
      sid: "-rplmts"
    });
    x({
      and: [{
        clock: $freeResult,
        fn: (result) => {
          if (!result) return [];
          return result.content.map((item2) => ({
            content: item2.content,
            title: item2.title
          })).flat().map((el2) => ({
            content: el2.content.map((el22) => el22.content).flat(),
            title: el2.title
          })).flat();
        },
        target: $freeContent
      }],
      or: {
        sid: "f67boq"
      }
    });
    x({
      and: [{
        clock: getFreeResultQuery.finished.success,
        fn: ({
          result
        }) => result,
        target: $freeResult
      }],
      or: {
        sid: "g0g0bl"
      }
    });
    manImage = "/assets/static/man-temp_large.C17Lahl-.webp";
  }
});

// node_modules/patronum/and/index.js
var init_and = __esm({
  "node_modules/patronum/and/index.js"() {
  }
});

// node_modules/patronum/combine-events/index.js
function ownKeys2(object, enumerableOnly) {
  var keys2 = Object.keys(object);
  if (Object.getOwnPropertySymbols) {
    var symbols = Object.getOwnPropertySymbols(object);
    enumerableOnly && (symbols = symbols.filter(function(sym) {
      return Object.getOwnPropertyDescriptor(object, sym).enumerable;
    })), keys2.push.apply(keys2, symbols);
  }
  return keys2;
}
function _objectSpread(target) {
  for (var i13 = 1; i13 < arguments.length; i13++) {
    var source = null != arguments[i13] ? arguments[i13] : {};
    i13 % 2 ? ownKeys2(Object(source), true).forEach(function(key) {
      _defineProperty(target, key, source[key]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(target, Object.getOwnPropertyDescriptors(source)) : ownKeys2(Object(source)).forEach(function(key) {
      Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key));
    });
  }
  return target;
}
function _defineProperty(obj, key, value) {
  key = _toPropertyKey(key);
  if (key in obj) {
    Object.defineProperty(obj, key, { value, enumerable: true, configurable: true, writable: true });
  } else {
    obj[key] = value;
  }
  return obj;
}
function _toPropertyKey(arg) {
  var key = _toPrimitive(arg, "string");
  return typeof key === "symbol" ? key : String(key);
}
function _toPrimitive(input2, hint3) {
  if (typeof input2 !== "object" || input2 === null) return input2;
  var prim = input2[Symbol.toPrimitive];
  if (prim !== void 0) {
    var res = prim.call(input2, hint3 || "default");
    if (typeof res !== "object") return res;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (hint3 === "string" ? String : Number)(input2);
}
function combineEvents(args) {
  var argsShape = isEventsShape(args) ? {
    events: args
  } : args;
  var {
    events,
    reset: reset2,
    target = p({
      name: "target",
      sid: "p73fff"
    })
  } = argsShape;
  if (!(ae.unit(target) && ae.targetable(target))) throwError("target should be a targetable unit");
  if (reset2 && !ae.unit(reset2)) throwError("reset should be a unit");
  n(target, () => {
    var keys2 = Object.keys(events);
    var defaultShape = Array.isArray(events) ? [...keys2].fill("") : {};
    var $counter = h(keys2.length, {
      and: {
        serialize: "ignore"
      },
      name: "$counter",
      sid: "-ahoahx"
    });
    var $results = h(defaultShape, {
      and: {
        serialize: "ignore"
      },
      name: "$results",
      sid: "oy8iyn"
    });
    x({
      and: [{
        source: target,
        target: $counter.reinit
      }],
      or: {
        sid: "blex0c"
      }
    });
    $results.reset(target);
    if (reset2) {
      x({
        and: [{
          source: reset2,
          target: $counter.reinit
        }],
        or: {
          sid: "bnm3fi"
        }
      });
      $results.reset(reset2);
    }
    var _loop = function _loop2(key2) {
      var $isDone = h(false, {
        and: {
          serialize: "ignore"
        },
        name: "$isDone",
        sid: "-rf2fac"
      }).on(events[key2], () => true).reset(target);
      if (reset2) {
        $isDone.reset(reset2);
      }
      $counter.on($isDone, (value) => value - 1);
      $results.on(events[key2], (shape, payload) => {
        var newShape = Array.isArray(shape) ? [...shape] : _objectSpread({}, shape);
        newShape[key2] = payload;
        return newShape;
      });
    };
    for (var key of keys2) {
      _loop(key);
    }
    var eventsTrriggered = x({
      and: [{
        source: $results,
        clock: [...Object.values(events)]
      }],
      or: {
        name: "eventsTrriggered",
        sid: "-lro8bq"
      }
    });
    x({
      and: [{
        source: eventsTrriggered,
        filter: $counter.map((value) => value === 0, {
          skipVoid: false
        }),
        target
      }],
      or: {
        sid: "-dlkr2s"
      }
    });
  });
  return target;
}
function isEventsShape(args) {
  return Object.keys(args).some((key) => !["events", "reset", "target"].includes(key) && ae.unit(args[key]));
}
function throwError(message) {
  throw new Error(message);
}
var init_combine_events = __esm({
  "node_modules/patronum/combine-events/index.js"() {
    init_effector();
  }
});

// node_modules/patronum/condition/index.js
var init_condition = __esm({
  "node_modules/patronum/condition/index.js"() {
  }
});

// node_modules/patronum/debounce/index.js
var init_debounce = __esm({
  "node_modules/patronum/debounce/index.js"() {
  }
});

// node_modules/patronum/debug/index.js
function ownKeys3(object, enumerableOnly) {
  var keys2 = Object.keys(object);
  if (Object.getOwnPropertySymbols) {
    var symbols = Object.getOwnPropertySymbols(object);
    enumerableOnly && (symbols = symbols.filter(function(sym) {
      return Object.getOwnPropertyDescriptor(object, sym).enumerable;
    })), keys2.push.apply(keys2, symbols);
  }
  return keys2;
}
function _objectSpread2(target) {
  for (var i13 = 1; i13 < arguments.length; i13++) {
    var source = null != arguments[i13] ? arguments[i13] : {};
    i13 % 2 ? ownKeys3(Object(source), true).forEach(function(key) {
      _defineProperty2(target, key, source[key]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(target, Object.getOwnPropertyDescriptors(source)) : ownKeys3(Object(source)).forEach(function(key) {
      Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key));
    });
  }
  return target;
}
function _defineProperty2(obj, key, value) {
  key = _toPropertyKey2(key);
  if (key in obj) {
    Object.defineProperty(obj, key, { value, enumerable: true, configurable: true, writable: true });
  } else {
    obj[key] = value;
  }
  return obj;
}
function _toPropertyKey2(arg) {
  var key = _toPrimitive2(arg, "string");
  return typeof key === "symbol" ? key : String(key);
}
function _toPrimitive2(input2, hint3) {
  if (typeof input2 !== "object" || input2 === null) return input2;
  var prim = input2[Symbol.toPrimitive];
  if (prim !== void 0) {
    var res = prim.call(input2, hint3 || "default");
    if (typeof res !== "object") return res;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (hint3 === "string" ? String : Number)(input2);
}
function debug3() {
  var {
    config,
    units
  } = resolveParams(...arguments);
  units.forEach((unit) => {
    if (ae.store(unit, {
      sid: "-foynff"
    }) || ae.event(unit, {
      sid: "apy28p"
    }) || ae.effect(unit, {
      sid: "apy3od"
    })) {
      watchUnit(unit, config);
    } else if (ae.domain(unit, {
      sid: "-rsqe9t"
    })) {
      watchDomain(unit, config);
    } else {
    }
  });
}
function watchDomain(domain, config) {
  domain.onCreateStore((store) => watchUnit(store, config));
  domain.onCreateEvent((event) => watchUnit(event, config));
  domain.onCreateEffect((effect) => watchUnit(effect, config));
  domain.onCreateDomain((domain2) => watchDomain(domain2, config));
}
function watchUnit(unit, config) {
  if (ae.store(unit, {
    sid: "w7ds2s"
  })) {
    watchStoreInit(unit, config);
    watch(unit, config);
  } else if (ae.event(unit, {
    sid: "fretyd"
  })) {
    watch(unit, config);
  } else if (ae.effect(unit, {
    sid: "gpi2qr"
  })) {
    watch(unit, config);
    watch(unit.finally, config);
    watch(unit.done, config);
    watch(unit.fail, config);
  }
}
function watch(unit, config) {
  var watcher = a({
    parent: [unit],
    // debug watchers should behave like normal watchers
    meta: {
      op: "watch"
    },
    family: {
      owners: unit
    },
    regional: true,
    // node only gets all required data
    node: [Te.run({
      fn(value, _internal, stack3) {
        var _stack$scope;
        var scope = (_stack$scope = stack3 === null || stack3 === void 0 ? void 0 : stack3.scope) !== null && _stack$scope !== void 0 ? _stack$scope : null;
        var context = {
          logType: "update",
          scope,
          scopeName: getScopeName(scope),
          node: getNode(unit),
          kind: getType(unit),
          value,
          name: getName(unit),
          loc: getLoc(unit),
          // Use stack meta of actual unit, not of debug node
          stackMeta: getStackMeta(stack3.parent),
          trace: config.trace ? collectTrace(stack3) : []
        };
        if (!config.handler) {
          throw Error("patronum/debug must have the handler");
        }
        config.handler(context);
      }
    })]
  });
  return () => vt(watcher);
}
function collectTrace(stack3) {
  var trace = [];
  var parent = stack3 === null || stack3 === void 0 ? void 0 : stack3.parent;
  while (parent) {
    var {
      node,
      value
    } = parent;
    var entry = {
      node,
      value,
      name: getName(node),
      loc: getLoc(node),
      kind: getType(node),
      stackMeta: getStackMeta(parent)
    };
    trace.push(entry);
    parent = parent.parent;
  }
  return trace;
}
function watchStoreInit(store, config) {
  if (!config.handler) {
    throw Error("patronum/debug must have the handler");
  }
  var node = getNode(store);
  var context = {
    logType: "initial",
    scope: null,
    scopeName: null,
    node,
    kind: getType(store),
    value: store.getState(),
    name: getName(store),
    loc: getLoc(store),
    // nothing to trace for store.getState() - it is one-step call
    trace: [],
    // no stackMeta for initial state
    stackMeta: {}
  };
  config.handler(context);
  scopes.forEach((scope) => watchStoreInitInScope(store, config, scope));
  watchScopeRegister((newScope) => watchStoreInitInScope(store, config, newScope));
}
function watchStoreInitInScope(store, config, scope) {
  if (!config.handler) {
    throw Error("patronum/debug must have the handler");
  }
  var node = getNode(store);
  var context = {
    logType: "initial",
    scope,
    scopeName: getScopeName(scope),
    node,
    kind: getType(store),
    value: scope.getState(store),
    name: getName(store),
    loc: getLoc(store),
    // nothing to trace for scope.getState(store) - it is one-step call
    trace: [],
    // no stackMeta for initial state
    stackMeta: {}
  };
  config.handler(context);
}
function resolveParams() {
  var config = defaultConfig;
  for (var _len = arguments.length, entry = new Array(_len), _key = 0; _key < _len; _key++) {
    entry[_key] = arguments[_key];
  }
  var [maybeConfig, ...restUnits] = entry;
  var units = [];
  if (isConfig(maybeConfig)) {
    config = _objectSpread2(_objectSpread2({}, defaultConfig), maybeConfig);
  } else if (!ae.unit(maybeConfig)) {
    for (var [name2, unit] of Object.entries(maybeConfig)) {
      customNames.set(getGraph(unit).id, name2);
      units.push(unit);
    }
  } else {
    units.push(maybeConfig);
  }
  for (var maybeUnit of restUnits) {
    if (ae.unit(maybeUnit)) {
      units.push(maybeUnit);
    } else {
      for (var [_name, _unit] of Object.entries(maybeUnit)) {
        customNames.set(getGraph(_unit).id, _name);
        units.push(_unit);
      }
    }
  }
  return {
    config,
    units
  };
}
function isConfig(maybeConfig) {
  if (!ae.unit(maybeConfig)) {
    return !Object.values(maybeConfig).every(ae.unit);
  }
  return false;
}
function registerScope(scope, config) {
  scopes.save(scope, {
    name: config.name
  });
  watchers.forEach((cb) => cb(scope));
  return () => {
    scopes.delete(scope);
  };
}
function unregisterAllScopes() {
  scopes.clear();
}
function getDefaultScopeName() {
  unknownScopes += 1;
  return "unknown_".concat(unknownScopes);
}
function getScopeName(scope) {
  if (!scope) return null;
  var meta = scopes.get(scope);
  if (!meta) {
    var fallbackId = scope._debugId || (scope._debugId = getDefaultScopeName());
    return fallbackId;
  }
  return meta.name;
}
function isEffectChild(node) {
  var actualNode = getNode(node);
  var {
    sid,
    named
  } = actualNode.meta;
  return Boolean(!sid && (named === "finally" || named === "done" || named === "doneData" || named === "fail" || named === "failData" || named === "inFlight" || named === "pending"));
}
function isStoreOn(node) {
  var actualNode = getNode(node);
  var {
    op
  } = actualNode.meta;
  if (op === "on") return true;
  return false;
}
function getType(unit) {
  if (ae.store(unit, {
    sid: "4o7s04"
  })) {
    return "store";
  }
  if (ae.effect(unit, {
    sid: "4pv5s7"
  }) || isEffectChild(unit)) {
    return "effect";
  }
  if (ae.event(unit, {
    sid: "4rijka"
  })) {
    return "event";
  }
  if (ae.domain(unit, {
    sid: "54plsy"
  })) {
    return "domain";
  }
  if (ae.unit(unit)) {
    return "unit";
  }
  var node = getNode(unit);
  if (node.meta.op) {
    return node.meta.op;
  }
  return "unknown";
}
function getName(unit) {
  var _getNode, _getNode$meta;
  var custom2 = customNames.get(getGraph(unit).id);
  if (custom2) {
    return custom2;
  }
  if (isEffectChild(unit)) {
    var node = getNode(unit);
    var parentEffect = node.family.owners.find((n13) => n13.meta.op === "effect");
    if (parentEffect) {
      var closestParentDomainName = getOwningDomainName(parentEffect);
      var formattedDomainName = closestParentDomainName ? "".concat(closestParentDomainName, "/") : "";
      return "".concat(formattedDomainName).concat(getName(parentEffect), ".").concat(node.meta.named);
    }
    return node.meta.named;
  }
  if (isStoreOn(unit)) {
    var _node = getNode(unit);
    var targetStoreName = getName(_node.next[0]);
    var triggerEventName = getName(_node.family.owners[0]);
    return "".concat(targetStoreName, ".on(").concat(triggerEventName, ")");
  }
  if (ae.unit(unit)) {
    var _compositeName;
    if (unit !== null && unit !== void 0 && (_compositeName = unit.compositeName) !== null && _compositeName !== void 0 && _compositeName.fullName) {
      return unit.compositeName.fullName;
    }
    var _closestParentDomainName = getOwningDomainName(unit);
    var _formattedDomainName = _closestParentDomainName ? "".concat(_closestParentDomainName, "/") : "";
    if (unit !== null && unit !== void 0 && unit.shortName) {
      return "".concat(_formattedDomainName).concat(unit.shortName);
    }
    if (unit !== null && unit !== void 0 && unit.name) {
      return "".concat(_formattedDomainName).concat(unit.name);
    }
  }
  if ((_getNode = getNode(unit)) !== null && _getNode !== void 0 && (_getNode$meta = _getNode.meta) !== null && _getNode$meta !== void 0 && _getNode$meta.name) {
    return getNode(unit).meta.name;
  }
  return null;
}
function getOwningDomainName(unit) {
  var closestParentDomain = getNode(unit).family.owners.find((n13) => n13.meta.op === "domain");
  if (!closestParentDomain) return null;
  return getName(closestParentDomain);
}
function readLoc(_ref) {
  var {
    meta
  } = _ref;
  var loc = "config" in meta ? meta.config.loc : meta.loc;
  return loc;
}
function getLoc(unit) {
  var loc = readLoc(getNode(unit));
  if (!loc) return void 0;
  return loc;
}
function getNode(node) {
  var actualNode = "graphite" in node ? node.graphite : node;
  return actualNode;
}
function getStackMeta(stack3) {
  if (!stack3) return {};
  var meta = stack3.meta || {};
  return meta;
}
var defaultConfig, watchers, watchScopeRegister, unknownScopes, cache, scopes, getGraph, customNames;
var init_debug2 = __esm({
  "node_modules/patronum/debug/index.js"() {
    init_effector();
    defaultConfig = {
      trace: false,
      // default logger to console.info
      handler: (context) => {
        if (isEffectChild(context.node) && context.node.meta.named === "finally") {
          return;
        }
        var {
          scope,
          scopeName,
          name: name2,
          kind,
          value,
          loc,
          trace,
          node,
          logType
        } = context;
        var scopeLog = scope ? " (scope: ".concat(scopeName, ")") : "";
        var logName = name2 !== null && name2 !== void 0 ? name2 : loc ? "".concat(loc.file, ":").concat(loc.line, ":").concat(loc.column) : "";
        var logPrintType = logType === "initial" ? " [getState]" : "";
        console.info("[".concat(kind, "]").concat(scopeLog, " ").concat(logName).concat(logPrintType), value);
        if (
          // logging trace only if there is something to log
          trace && trace.length > 0 && // do not log trace for effect children, as it is always the same effect internals
          !isEffectChild(node)
        ) {
          console.groupCollapsed("[".concat(kind, "]").concat(scopeLog, " ").concat(logName, " trace"));
          trace.forEach((update) => {
            var {
              name: traceName,
              kind: kind2,
              value: value2,
              loc: loc2
            } = update;
            var logTraceName = traceName !== null && traceName !== void 0 ? traceName : loc2 ? "".concat(loc2.file, ":").concat(loc2.line, ":").concat(loc2.column) : "";
            console.info("<- [".concat(kind2, "] ").concat(logTraceName), value2);
          });
          console.groupEnd();
        }
      }
    };
    watchers = /* @__PURE__ */ new Set();
    watchScopeRegister = (cb) => {
      watchers.add(cb);
      return () => {
        watchers.delete(cb);
      };
    };
    unknownScopes = 0;
    cache = /* @__PURE__ */ new Map();
    scopes = {
      save(scope, meta) {
        if (!scopes.get(scope)) {
          cache.set(scope, meta);
        }
      },
      get(scope) {
        var _cache$get;
        if (!scope) return null;
        return (_cache$get = cache.get(scope)) !== null && _cache$get !== void 0 ? _cache$get : null;
      },
      delete(scope) {
        cache.delete(scope);
      },
      forEach(callback) {
        cache.forEach((meta, scope) => callback(scope, meta));
      },
      clear() {
        cache.clear();
      }
    };
    debug3.registerScope = registerScope;
    debug3.unregisterAllScopes = unregisterAllScopes;
    getGraph = (graph) => graph.graphite || graph;
    customNames = /* @__PURE__ */ new Map();
  }
});

// node_modules/patronum/delay/index.js
function delay() {
  var argsShape = arguments.length === 2 ? {
    source: arguments.length <= 0 ? void 0 : arguments[0],
    timeout: arguments.length <= 1 ? void 0 : arguments[1]
  } : arguments.length <= 0 ? void 0 : arguments[0];
  var {
    source,
    timeout,
    target = p({
      name: "target",
      sid: "jucb8d"
    })
  } = argsShape;
  var targets = Array.isArray(target) ? target : [target];
  if (!ae.unit(source)) throw new TypeError("source must be a unit from effector");
  if (!targets.every((unit) => ae.unit(unit))) throw new TypeError("target must be a unit from effector");
  var ms = validateTimeout(timeout);
  var timerFx = b((_ref) => {
    var {
      payload,
      milliseconds
    } = _ref;
    return new Promise((resolve) => {
      setTimeout(resolve, milliseconds, payload);
    });
  }, {
    name: "timerFx",
    sid: "-wp3hwr"
  });
  x({
    and: [{
      // ms can be Store<number> | number
      // converts object of stores or object of values to store
      source: g({
        and: [{
          milliseconds: ms
        }],
        or: {
          name: "source",
          sid: "-gaikyz"
        }
      }),
      clock: source,
      fn: (_ref2, payload) => {
        var {
          milliseconds
        } = _ref2;
        return {
          payload,
          milliseconds: typeof milliseconds === "function" ? milliseconds(payload) : milliseconds
        };
      },
      target: timerFx
    }],
    or: {
      sid: "-uuqxfw"
    }
  });
  x({
    and: [{
      clock: timerFx.doneData,
      target: targets
    }],
    or: {
      sid: "-uc1x9m"
    }
  });
  return target;
}
function validateTimeout(timeout) {
  if (ae.store(timeout, {
    sid: "-tvk3f2"
  }) || typeof timeout === "function" || typeof timeout === "number") {
    return timeout;
  }
  throw new TypeError(`'timeout' argument must be a function, Store, or a number. Passed "`.concat(typeof timeout, '"'));
}
var init_delay = __esm({
  "node_modules/patronum/delay/index.js"() {
    init_effector();
  }
});

// node_modules/patronum/either/index.js
var init_either = __esm({
  "node_modules/patronum/either/index.js"() {
  }
});

// node_modules/patronum/empty/index.js
var init_empty = __esm({
  "node_modules/patronum/empty/index.js"() {
  }
});

// node_modules/patronum/equals/index.js
var init_equals = __esm({
  "node_modules/patronum/equals/index.js"() {
  }
});

// node_modules/patronum/every/index.js
var init_every = __esm({
  "node_modules/patronum/every/index.js"() {
  }
});

// node_modules/patronum/format/index.js
var init_format = __esm({
  "node_modules/patronum/format/index.js"() {
  }
});

// node_modules/patronum/in-flight/index.js
var init_in_flight = __esm({
  "node_modules/patronum/in-flight/index.js"() {
  }
});

// node_modules/patronum/interval/index.js
var init_interval = __esm({
  "node_modules/patronum/interval/index.js"() {
  }
});

// node_modules/patronum/not/index.js
var init_not = __esm({
  "node_modules/patronum/not/index.js"() {
  }
});

// node_modules/patronum/once/index.js
function once(unitOrConfig) {
  var source;
  var reset2;
  if (ae.unit(unitOrConfig)) {
    source = unitOrConfig;
  } else {
    ({
      source,
      reset: reset2
    } = unitOrConfig);
  }
  var $canTrigger = h(true, {
    name: "$canTrigger",
    sid: "ys2jl0"
  });
  var trigger = x({
    and: [{
      source,
      filter: $canTrigger
    }],
    or: {
      name: "trigger",
      sid: "e3xto3"
    }
  });
  $canTrigger.on(trigger, () => false);
  if (reset2) {
    $canTrigger.reset(reset2);
  }
  return trigger;
}
var init_once = __esm({
  "node_modules/patronum/once/index.js"() {
    init_effector();
  }
});

// node_modules/patronum/or/index.js
function or2() {
  for (var _len = arguments.length, stores = new Array(_len), _key = 0; _key < _len; _key++) {
    stores[_key] = arguments[_key];
  }
  return g({
    and: [stores, (values2) => {
      for (var value of values2) {
        if (value) {
          return true;
        }
      }
      return false;
    }, {
      skipVoid: false
    }],
    or: {
      sid: "-44722k"
    }
  });
}
var init_or = __esm({
  "node_modules/patronum/or/index.js"() {
    init_effector();
  }
});

// node_modules/patronum/pending/index.js
var init_pending = __esm({
  "node_modules/patronum/pending/index.js"() {
  }
});

// node_modules/patronum/previous/index.js
var init_previous = __esm({
  "node_modules/patronum/previous/index.js"() {
  }
});

// node_modules/patronum/readonly/index.js
var init_readonly = __esm({
  "node_modules/patronum/readonly/index.js"() {
  }
});

// node_modules/patronum/reset/index.js
var init_reset = __esm({
  "node_modules/patronum/reset/index.js"() {
  }
});

// node_modules/patronum/reshape/index.js
var init_reshape = __esm({
  "node_modules/patronum/reshape/index.js"() {
  }
});

// node_modules/patronum/snapshot/index.js
var init_snapshot = __esm({
  "node_modules/patronum/snapshot/index.js"() {
  }
});

// node_modules/patronum/some/index.js
var init_some = __esm({
  "node_modules/patronum/some/index.js"() {
  }
});

// node_modules/patronum/split-map/index.js
var init_split_map = __esm({
  "node_modules/patronum/split-map/index.js"() {
  }
});

// node_modules/patronum/spread/index.js
function spread(args) {
  var argsShape = isTargets(args) ? {
    targets: args
  } : args;
  var {
    targets,
    source = p({
      name: "source",
      sid: "-u92i8l"
    })
  } = argsShape;
  var _loop = function _loop2(targetKey2) {
    if (hasOwnProp(targets, targetKey2)) {
      var currentTarget = targets[targetKey2];
      x({
        and: [{
          source,
          filter: (object) => {
            return typeof object === "object" && object !== null && targetKey2 in object;
          },
          fn: (object) => object[targetKey2],
          target: currentTarget,
          batch: false
        }],
        or: {
          sid: "motx3n"
        }
      });
    }
  };
  for (var targetKey in targets) {
    _loop(targetKey);
  }
  return source;
}
function isTargets(args) {
  return !Object.keys(args).some((key) => ["targets", "source"].includes(key));
}
var hasPropBase, hasOwnProp;
var init_spread = __esm({
  "node_modules/patronum/spread/index.js"() {
    init_effector();
    hasPropBase = {}.hasOwnProperty;
    hasOwnProp = (object, key) => hasPropBase.call(object, key);
  }
});

// node_modules/patronum/status/index.js
var init_status = __esm({
  "node_modules/patronum/status/index.js"() {
  }
});

// node_modules/patronum/throttle/index.js
var init_throttle = __esm({
  "node_modules/patronum/throttle/index.js"() {
  }
});

// node_modules/patronum/time/index.js
var init_time = __esm({
  "node_modules/patronum/time/index.js"() {
  }
});

// node_modules/patronum/index.js
var init_patronum = __esm({
  "node_modules/patronum/index.js"() {
    init_and();
    init_combine_events();
    init_condition();
    init_debounce();
    init_debug2();
    init_delay();
    init_either();
    init_empty();
    init_equals();
    init_every();
    init_format();
    init_in_flight();
    init_interval();
    init_not();
    init_once();
    init_or();
    init_pending();
    init_previous();
    init_readonly();
    init_reset();
    init_reshape();
    init_snapshot();
    init_some();
    init_split_map();
    init_spread();
    init_status();
    init_throttle();
    init_time();
  }
});

// node_modules/@effector-reform/core/dist/index.js
function je3(r7, t20) {
  const e14 = {
    ...Te3,
    ...t20
  }, o8 = h(r7, {
    and: {
      name: "<field value>"
    },
    name: "$value",
    sid: "-odukqc"
  }), s36 = h(null, {
    and: {
      name: "<inner field error>"
    },
    name: "$innerError",
    sid: "-bm1sfb"
  }), c15 = h((t20 == null ? void 0 : t20.error) ?? null, {
    and: {
      name: "<outer field error>"
    },
    name: "$outerError",
    sid: "-q0yxv4"
  }), l12 = g({
    and: [{
      innerError: s36,
      outerError: c15
    }],
    or: {
      name: "$error",
      sid: "1v51k4"
    }
  }).map(($5) => {
    let {
      innerError: D5,
      outerError: R10
    } = $5;
    return R10 || D5;
  }), d12 = l12.map(($5) => $5 === null), n13 = h(false, {
    name: "$isFocused",
    sid: "-tfwbg6"
  }), g7 = h(e14.meta, {
    name: "$meta",
    sid: "8313b6"
  }), y4 = p({
    name: "changeMeta",
    sid: "yu4u8t"
  }), E6 = p({
    name: "metaChanged",
    sid: "-143jij"
  });
  x({
    and: [{
      clock: y4,
      target: g7
    }],
    or: {
      sid: "5g0d5w"
    }
  }), x({
    and: [{
      clock: g7,
      target: E6
    }],
    or: {
      sid: "5irc4p"
    }
  });
  const S2 = p("<field change>", {
    name: "change",
    sid: "-jsyjht"
  }), k5 = p("<field changed>", {
    name: "changed",
    sid: "-gkuyjz"
  }), h6 = p({
    name: "blur",
    sid: "-p5ms63"
  }), F4 = p({
    name: "blurred",
    sid: "-14fc3g"
  }), V3 = p({
    name: "focus",
    sid: "x0dsbh"
  }), b4 = p({
    name: "focused",
    sid: "-26ufxj"
  }), p11 = p("<field setError>", {
    name: "changeError",
    sid: "-tne5lt"
  }), I6 = p("<field error changed>", {
    name: "errorChanged",
    sid: "-izkoun"
  }), B4 = p({
    name: "setInnerError",
    sid: "r2bo7q"
  }), X4 = p({
    name: "setOuterError",
    sid: "b89nlc"
  }), L3 = p("<field reset>", {
    name: "reset",
    sid: "4w68vf"
  }), O5 = p("<field reset completed>", {
    name: "resetCompleted",
    sid: "-unnp98"
  }), N4 = p({
    name: "batchedSetInnerError",
    sid: "d93elg"
  }), z2 = p({
    name: "batchedSetOuterError",
    sid: "-2kym0y"
  }), U3 = p({
    name: "batchedSetValue",
    sid: "t0ffop"
  }), M6 = p({
    name: "batchedReset",
    sid: "-g5tm4n"
  });
  return x({
    and: [{
      clock: h6,
      fn: () => false,
      target: n13
    }],
    or: {
      sid: "6xw7ba"
    }
  }), x({
    and: [{
      clock: V3,
      fn: () => true,
      target: n13
    }],
    or: {
      sid: "79zod8"
    }
  }), x({
    and: [{
      clock: n13,
      filter: ($5) => $5,
      target: b4
    }],
    or: {
      sid: "7b39jy"
    }
  }), x({
    and: [{
      clock: n13,
      filter: ($5) => !$5,
      target: F4
    }],
    or: {
      sid: "7bn25b"
    }
  }), x({
    and: [{
      clock: B4,
      target: s36
    }],
    or: {
      sid: "7cqnc1"
    }
  }), x({
    and: [{
      clock: p11,
      target: c15
    }],
    or: {
      sid: "-xt9yfq"
    }
  }), x({
    and: [{
      clock: S2,
      target: o8
    }],
    or: {
      sid: "-xqizgx"
    }
  }), x({
    and: [{
      clock: U3,
      fn: ($5) => $5.value,
      target: o8
    }],
    or: {
      sid: "-xpfea7"
    }
  }), x({
    and: [{
      clock: N4,
      fn: ($5) => $5.value,
      target: s36
    }],
    or: {
      sid: "-xaky9g"
    }
  }), x({
    and: [{
      clock: z2,
      fn: ($5) => $5.value,
      target: c15
    }],
    or: {
      sid: "-x7a6pa"
    }
  }), x({
    and: [{
      clock: o8,
      target: k5
    }],
    or: {
      sid: "-wsfqoj"
    }
  }), x({
    and: [{
      clock: p11,
      target: c15
    }],
    or: {
      sid: "-wrvy36"
    }
  }), x({
    and: [{
      clock: l12,
      target: I6
    }],
    or: {
      sid: "-wqscwg"
    }
  }), x({
    and: [{
      clock: B4,
      target: s36
    }],
    or: {
      sid: "-wq8kb3"
    }
  }), x({
    and: [{
      clock: [L3, M6],
      fn: () => ({
        value: r7,
        outerError: (t20 == null ? void 0 : t20.error) ?? null,
        completed: {
          value: r7,
          error: (t20 == null ? void 0 : t20.error) ?? null
        },
        innerError: null
      }),
      target: ke({
        sid: "-s8o1lm",
        fn: () => spread({
          value: o8,
          completed: O5,
          outerError: c15,
          innerError: s36
        }),
        name: "target",
        method: "spread"
      })
    }],
    or: {
      sid: "-wdlans"
    }
  }), {
    "@@type": te3,
    batchedSetInnerError: N4,
    batchedSetOuterError: z2,
    batchedSetValue: U3,
    batchedReset: M6,
    $meta: g7,
    $value: o8,
    $outerError: c15,
    $innerError: s36,
    $error: l12,
    $isValid: d12,
    $isFocused: n13,
    changeMeta: y4,
    metaChanged: E6,
    blur: h6,
    blurred: F4,
    focus: V3,
    focused: b4,
    change: S2,
    changed: k5,
    changeError: p11,
    errorChanged: I6,
    reset: L3,
    resetCompleted: O5,
    setInnerError: B4,
    setOuterError: X4,
    copyOnCreateForm: e14.copyOnCreateForm,
    "@@unitShape": () => ({
      value: o8,
      error: l12,
      meta: g7,
      isValid: d12,
      isFocused: n13,
      changeMeta: y4,
      blur: h6,
      blurred: F4,
      focus: V3,
      focused: b4,
      changeError: p11,
      change: S2,
      reset: L3
    })
  };
}
function le4(r7) {
  let t20 = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : false;
  for (const e14 of r7)
    vt(e14, t20 ? {
      deep: t20
    } : void 0);
}
function w10(r7) {
  const t20 = [], e14 = h(-1, {
    name: "$lastIndex",
    sid: "-8wc724"
  }), o8 = h([...t20], {
    name: "$payloads",
    sid: "i6kdoz"
  }), s36 = p({
    name: "reset",
    sid: "1ok3o"
  }), c15 = p({
    name: "target",
    sid: "-ppag00"
  }), l12 = r7;
  x({
    and: [{
      clock: c15,
      target: s36
    }],
    or: {
      sid: "2gntpx"
    }
  }), x({
    and: [{
      clock: s36,
      fn: () => ({
        lastIndex: -1,
        payloads: [...t20]
      }),
      target: ke({
        sid: "-a2sx3w",
        fn: () => spread({
          lastIndex: e14,
          payloads: o8
        }),
        name: "target",
        method: "spread"
      })
    }],
    or: {
      sid: "2uyh5b"
    }
  });
  for (let d12 = 0; d12 < l12.length; d12++) {
    const n13 = l12[d12];
    x({
      and: [{
        clock: n13,
        source: e14,
        filter: (g7) => d12 - g7 !== 1,
        target: s36
      }],
      or: {
        sid: "3d3ory"
      }
    }), d12 === l12.length - 1 ? x({
      and: [{
        clock: n13,
        source: [e14, o8],
        filter: (g7) => {
          let [y4] = g7;
          return d12 - y4 === 1;
        },
        fn: (g7, y4) => {
          let [, E6] = g7;
          return [...E6, y4];
        },
        target: c15
      }],
      or: {
        sid: "3t1q15"
      }
    }) : x({
      and: [{
        clock: n13,
        source: [e14, o8],
        filter: (g7) => {
          let [y4] = g7;
          return d12 - y4 === 1;
        },
        fn: (g7, y4) => {
          let [, E6] = g7;
          return {
            lastIndex: d12,
            payloads: [...E6, y4]
          };
        },
        target: ke({
          sid: "zdisu8",
          fn: () => spread({
            lastIndex: e14,
            payloads: o8
          }),
          name: "target",
          method: "spread"
        })
      }],
      or: {
        sid: "48zr8m"
      }
    });
  }
  return c15;
}
function Re3(r7) {
  return "@@type" in r7 && r7["@@type"] === te3;
}
function pe3(r7) {
  return typeof r7 == "boolean" || typeof r7 == "string" || typeof r7 == "number" || r7 === null;
}
function Q3(r7) {
  const t20 = typeof File < "u" ? r7 instanceof File : false, e14 = typeof Buffer < "u" ? r7 instanceof Buffer : false, o8 = typeof FileList < "u" ? r7 instanceof FileList : false;
  return pe3(r7) || typeof r7 == "bigint" || e14 || t20 || o8 || r7 instanceof Date || r7 instanceof Blob || r7 instanceof ArrayBuffer || r7 instanceof Int8Array || r7 instanceof Int16Array || r7 instanceof Int32Array || r7 instanceof BigInt64Array;
}
function Oe2(r7) {
  let t20 = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : false;
  const e14 = r7;
  le4([r7.$value, r7.$error, r7.$meta, r7.change, r7.changeError, r7.errorChanged, r7.changed, r7.reset, r7.resetCompleted, r7.metaChanged, e14.$outerError, e14.$innerError, e14.setInnerError, e14.batchedSetValue, e14.batchedSetOuterError, e14.batchedSetInnerError, e14.batchedReset], t20);
}
function De3(r7, t20) {
  return r7.reduce((e14, o8, s36) => (t20.includes(o8) || (e14.nodes.push(o8), e14.indexes.push(s36)), e14), {
    nodes: [],
    indexes: []
  });
}
function Ce3(r7) {
  let t20 = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : false;
  const e14 = r7;
  le4([e14.$values, e14.$error, e14.$meta, e14.change, e14.changed, e14.changeError, e14.errorChanged, e14.insert, e14.inserted, e14.move, e14.moved, e14.pop, e14.popped, e14.push, e14.pushed, e14.remove, e14.removed, e14.replace, e14.replaced, e14.swap, e14.swapped, e14.unshift, e14.unshifted, e14.clear, e14.cleared, e14.reset, e14.resetCompleted, e14.setInnerError, e14.batchedSetValue, e14.batchedSetOuterError, e14.batchedSetInnerError, e14.batchedClear, e14.batchedReset, e14.metaChanged], t20);
}
function ge2(r7) {
  if (!Q3(r7))
    for (const t20 in r7) {
      const e14 = r7[t20];
      switch (e14["@@type"]) {
        case ae2: {
          const o8 = e14.$values.getState();
          for (const s36 of o8) {
            if (Q3(s36))
              break;
            ge2(s36);
          }
          Ce3(e14);
          break;
        }
        case te3: {
          Oe2(e14);
          break;
        }
        default: {
          ge2(e14);
          break;
        }
      }
    }
}
function Ke3(r7) {
  return "@@type" in r7 && r7["@@type"] === ae2;
}
function _e2() {
  const r7 = p("<schema updated>", {
    name: "schemaUpdated",
    sid: "ogwkmf"
  }), t20 = p("<blurred>", {
    name: "blurred",
    sid: "-vtbsm2"
  }), e14 = p("<focused>", {
    name: "focused",
    sid: "r8rv7c"
  }), o8 = p({
    name: "metaChanged",
    sid: "p0ft9i"
  });
  return {
    blurred: t20,
    focused: e14,
    schemaUpdated: r7,
    metaChanged: o8
  };
}
function ue3(r7, t20) {
  return {
    id: Math.random().toString(),
    type: t20,
    fields: r7
  };
}
function me3(r7) {
  return Object.values(r7).reduce((t20, e14) => (e14.fields.length > 0 || (t20.tasks.push(e14), e14.type === "values" && t20.updateType !== "all" ? t20.updateType = t20.updateType === "error" ? "all" : "value" : e14.type === "errors" && t20.updateType !== "all" ? t20.updateType = t20.updateType === "value" ? "all" : "error" : (e14.type === "reset" || e14.type === "all") && (t20.updateType = "all")), t20), {
    tasks: [],
    updateType: "none"
  });
}
function Le3(r7) {
  const t20 = h({}, {
    and: {
      name: "<batch info>"
    },
    name: "$tasks",
    sid: "-c2xgvr"
  }), e14 = p({
    name: "batchedSchemaUpdated",
    sid: "-o4wxhp"
  }), o8 = p("<add batch task>", {
    name: "addBatchTask",
    sid: "bogptp"
  });
  return x({
    and: [{
      clock: o8,
      source: t20,
      fn: (s36, c15) => ({
        ...s36,
        [c15.id]: c15
      }),
      target: t20
    }],
    or: {
      sid: "-44ej8y"
    }
  }), x({
    and: [{
      clock: e14,
      source: t20,
      filter: (s36, c15) => {
        let {
          "@@batchInfo": l12
        } = c15;
        return !!s36[l12.id];
      },
      fn: (s36, c15) => {
        let {
          fieldPath: l12,
          "@@batchInfo": d12
        } = c15;
        const n13 = s36[d12.id];
        return n13.fields = n13.fields.filter((g7) => g7 !== l12), {
          ...s36
        };
      },
      target: t20
    }],
    or: {
      sid: "-40jz3f"
    }
  }), x({
    and: [{
      clock: t20,
      filter: (s36) => me3(s36).tasks.length > 0,
      fn: (s36) => {
        const {
          tasks: c15,
          updateType: l12
        } = me3(s36);
        return {
          fieldPath: c15.map((d12) => d12.fields).join(" "),
          type: l12
        };
      },
      target: r7
    }],
    or: {
      sid: "-35rhv7"
    }
  }), x({
    and: [{
      clock: t20,
      fn: (s36) => {
        const {
          tasks: c15
        } = me3(s36);
        if (c15.length === 0)
          return s36;
        const l12 = {
          ...s36
        };
        for (const d12 of c15)
          delete l12[d12.id];
        return l12;
      },
      target: t20
    }],
    or: {
      sid: "-2mip3k"
    }
  }), {
    addBatchTask: o8,
    batchedSchemaUpdated: e14
  };
}
function he(r7, t20) {
  const e14 = r7[t20];
  if (!e14)
    throw new Error(`unknown field with path ${t20}`);
  if (e14.type !== "array-field")
    throw new Error(`expected field type "array-field", received "${e14.type}"`);
  return e14;
}
function Ne3(r7) {
  var t20 = this;
  let {
    field: e14,
    path: o8,
    key: s36,
    resultErrorsNode: c15,
    resultValuesNode: l12,
    map: d12
  } = r7;
  const n13 = e14, g7 = n13.$values.getState(), y4 = [...o8, s36].join(".");
  l12[s36] = [], c15[s36] = {
    error: n13.$error.getState(),
    errors: []
  };
  const E6 = {
    type: "array-field",
    isValid: !c15[s36].error,
    reset: n13.reset,
    batchedSetValue: n13.batchedSetValue,
    batchedSetOuterError: n13.batchedSetOuterError,
    batchedSetInnerError: n13.batchedSetInnerError,
    batchedReset: n13.batchedReset,
    batchedClear: n13.batchedClear,
    clearMemory: function() {
      (arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : false) && Ce3(n13), le4([h6, k5, F4, V3]), delete t20.api[y4];
    },
    clearValuesMemory: () => {
      const b4 = Object.keys(this.api).filter((p11) => p11.startsWith(y4)).filter((p11) => p11 !== y4);
      for (const p11 of b4)
        this.api[p11].clearMemory();
    },
    clearInnerError: n13.setInnerError.prepend(() => null),
    clearOuterError: n13.changeError.prepend(() => null),
    setInnerError: n13.setInnerError,
    setOuterError: n13.changeError,
    setValue: n13.change
  };
  this.api[y4] = E6, c15[s36].error && (this.isValid = false);
  const S2 = (b4) => {
    b4.map((p11, I6) => {
      Q3(p11) ? l12[s36].push(p11) : (l12[s36].push({}), c15[s36].errors.push({}), d12.call(this, p11, l12[s36][I6], c15[s36].errors[I6], [...o8, s36, I6.toString()]));
    });
  };
  S2(g7);
  const k5 = b((b4) => {
    let {
      values: p11
    } = b4;
    he(this.api, y4).clearValuesMemory(), l12[s36] = [], c15[s36].errors = [], S2(p11);
  }, {
    name: "changeValuesFx",
    sid: "yd7x97"
  }), h6 = b((b4) => {
    let {
      error: p11
    } = b4;
    c15[s36].error = p11, E6.isValid = !c15[s36].error, c15[s36].error && (this.isValid = false);
  }, {
    name: "changeErrorFx",
    sid: "-35p6u6"
  }), F4 = b(() => {
    he(this.api, y4).clearValuesMemory(), l12[s36] = [], c15[s36] = {
      error: null,
      errors: []
    }, E6.isValid = !c15[s36].error;
  }, {
    name: "clearFx",
    sid: "-aodjm7"
  }), V3 = b((b4) => {
    let {
      error: p11,
      values: I6
    } = b4;
    he(this.api, y4).clearValuesMemory(), l12[s36] = [], c15[s36] = {
      error: p11,
      errors: []
    }, E6.isValid = !c15[s36].error, S2(I6);
  }, {
    name: "resetFx",
    sid: "pbjlrx"
  });
  x({
    and: [{
      clock: [w10([n13.setInnerError, n13.errorChanged]), w10([n13.changeError, n13.errorChanged])],
      source: n13.$error,
      fn: (b4) => ({
        error: b4
      }),
      target: h6
    }],
    or: {
      sid: "-ki7upl"
    }
  }), x({
    and: [{
      clock: [w10([n13.change, n13.changed]), w10([n13.pushed, n13.changed]), w10([n13.swapped, n13.changed]), w10([n13.moved, n13.changed]), w10([n13.inserted, n13.changed]), w10([n13.unshifted, n13.changed]), w10([n13.removed, n13.changed]), w10([n13.popped, n13.changed]), w10([n13.replaced, n13.changed])],
      fn: (b4) => {
        let [, p11] = b4;
        return {
          values: p11
        };
      },
      target: k5
    }],
    or: {
      sid: "-k168be"
    }
  }), x({
    and: [{
      clock: w10([n13.reset, n13.resetCompleted]),
      fn: (b4) => {
        let [, {
          values: p11,
          error: I6
        }] = b4;
        return {
          values: p11,
          error: I6
        };
      },
      target: V3
    }],
    or: {
      sid: "-97c3md"
    }
  }), x({
    and: [{
      clock: w10([n13.clear, n13.cleared]),
      fn: () => ({}),
      target: F4
    }],
    or: {
      sid: "-92dya4"
    }
  }), x({
    and: [{
      clock: n13.batchedSetValue,
      filter: (b4) => {
        let {
          value: p11
        } = b4;
        return l12[s36] === p11;
      },
      fn: (b4) => {
        let {
          "@@batchInfo": p11
        } = b4;
        return {
          fieldPath: y4,
          "@@batchInfo": p11
        };
      },
      target: this.batchedSchemaUpdated
    }],
    or: {
      sid: "-8mzpo0"
    }
  }), x({
    and: [{
      clock: w10([n13.batchedClear, n13.cleared]),
      fn: (b4) => {
        let [{
          "@@batchInfo": p11
        }] = b4;
        return {
          batchInfo: p11
        };
      },
      target: F4
    }],
    or: {
      sid: "-85eaog"
    }
  }), x({
    and: [{
      clock: w10([n13.batchedReset, n13.resetCompleted]),
      fn: (b4) => {
        let [{
          "@@batchInfo": p11
        }, {
          values: I6,
          error: B4
        }] = b4;
        return {
          values: I6,
          error: B4,
          batchInfo: p11
        };
      },
      target: V3
    }],
    or: {
      sid: "-7qjunp"
    }
  }), x({
    and: [{
      clock: w10([n13.batchedSetValue, n13.changed]),
      source: n13.$values,
      fn: (b4, p11) => {
        let [{
          "@@batchInfo": I6
        }] = p11;
        return {
          values: b4,
          batchInfo: I6
        };
      },
      target: k5
    }],
    or: {
      sid: "-79i89i"
    }
  }), x({
    and: [{
      clock: n13.batchedSetInnerError,
      source: n13.$outerError,
      fn: (b4, p11) => {
        let {
          value: I6,
          "@@batchInfo": B4
        } = p11;
        return {
          error: b4 ?? I6,
          batchInfo: B4
        };
      },
      target: h6
    }],
    or: {
      sid: "-6u3zne"
    }
  }), x({
    and: [{
      clock: n13.batchedSetOuterError,
      fn: (b4) => {
        let {
          value: p11,
          "@@batchInfo": I6
        } = b4;
        return {
          error: p11,
          batchInfo: I6
        };
      },
      target: h6
    }],
    or: {
      sid: "-6d2d97"
    }
  }), x({
    and: [{
      clock: h6.done,
      filter: (b4) => {
        let {
          params: p11
        } = b4;
        return !p11.batchInfo;
      },
      fn: () => ({
        fieldPath: y4,
        type: "error"
      }),
      target: this.schemaUpdated
    }],
    or: {
      sid: "-69rlp1"
    }
  }), x({
    and: [{
      clock: [k5.done, h6.done, V3.done, F4.done],
      filter: (b4) => {
        let {
          params: p11
        } = b4;
        return !!p11.batchInfo;
      },
      fn: (b4) => {
        let {
          params: p11
        } = b4;
        return {
          fieldPath: y4,
          "@@batchInfo": p11.batchInfo
        };
      },
      target: this.batchedSchemaUpdated
    }],
    or: {
      sid: "-5spzau"
    }
  }), x({
    and: [{
      clock: k5.done,
      filter: (b4) => {
        let {
          params: p11
        } = b4;
        return !p11.batchInfo;
      },
      fn: () => ({
        fieldPath: y4,
        type: "value"
      }),
      target: this.schemaUpdated
    }],
    or: {
      sid: "-4xdph9"
    }
  }), x({
    and: [{
      clock: n13.metaChanged,
      fn: (b4) => ({
        fieldPath: y4,
        meta: b4
      }),
      target: this.metaChanged
    }],
    or: {
      sid: "5hlz71"
    }
  });
}
function Ge2(r7) {
  var t20 = this;
  let {
    resultValuesNode: e14,
    resultErrorsNode: o8,
    field: s36,
    key: c15,
    path: l12
  } = r7;
  const d12 = s36;
  e14[c15] = d12.$value.getState(), o8[c15] = d12.$error.getState();
  const n13 = [...l12, c15].join("."), g7 = {
    type: "primitive-field",
    isValid: !o8[c15],
    reset: d12.reset,
    clearMemory: function() {
      (arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : false) && Oe2(d12), le4([y4, E6, S2]), delete t20.api[n13];
    },
    batchedSetValue: d12.batchedSetValue,
    batchedSetOuterError: d12.batchedSetOuterError,
    batchedSetInnerError: d12.batchedSetInnerError,
    batchedReset: d12.batchedReset,
    clearInnerError: d12.setInnerError.prepend(() => null),
    clearOuterError: d12.changeError.prepend(() => null),
    setInnerError: d12.setInnerError,
    setOuterError: d12.changeError,
    setValue: d12.change
  };
  this.api[n13] = g7, o8[c15] && (this.isValid = false);
  const y4 = b((k5) => {
    let {
      value: h6
    } = k5;
    e14[c15] = h6;
  }, {
    name: "changeValueFx",
    sid: "7ka07y"
  }), E6 = b((k5) => {
    let {
      error: h6
    } = k5;
    o8[c15] = h6, g7.isValid = !o8[c15], o8[c15] && (this.isValid = false);
  }, {
    name: "changeErrorFx",
    sid: "o72mb0"
  }), S2 = b((k5) => {
    let {
      value: h6,
      error: F4
    } = k5;
    e14[c15] = h6, o8[c15] = F4, g7.isValid = !o8[c15], o8[c15] && (this.isValid = false);
  }, {
    name: "resetFx",
    sid: "-xcik7l"
  });
  x({
    and: [{
      clock: w10([d12.changeError, d12.errorChanged]),
      fn: (k5) => {
        let [h6] = k5;
        return {
          error: h6
        };
      },
      target: E6
    }],
    or: {
      sid: "-5jpzph"
    }
  }), x({
    and: [{
      clock: w10([d12.change, d12.changed]),
      fn: (k5) => {
        let [h6] = k5;
        return {
          value: h6
        };
      },
      target: y4
    }],
    or: {
      sid: "-5gf85b"
    }
  }), x({
    and: [{
      clock: w10([d12.reset, d12.resetCompleted]),
      fn: (k5) => {
        let [, {
          value: h6,
          error: F4
        }] = k5;
        return {
          value: h6,
          error: F4
        };
      },
      target: S2
    }],
    or: {
      sid: "-51ks4k"
    }
  }), x({
    and: [{
      clock: y4.done,
      filter: (k5) => {
        let {
          params: h6
        } = k5;
        return !h6.batchInfo;
      },
      fn: () => ({
        fieldPath: n13,
        type: "value"
      }),
      target: this.schemaUpdated
    }],
    or: {
      sid: "-4l2ybq"
    }
  }), x({
    and: [{
      clock: E6.done,
      filter: (k5) => {
        let {
          params: h6
        } = k5;
        return !h6.batchInfo;
      },
      fn: () => ({
        fieldPath: n13,
        type: "error"
      }),
      target: this.schemaUpdated
    }],
    or: {
      sid: "-441bxj"
    }
  }), x({
    and: [{
      clock: S2.done,
      filter: (k5) => {
        let {
          params: h6
        } = k5;
        return !h6.batchInfo;
      },
      fn: () => ({
        fieldPath: n13,
        type: "all"
      }),
      target: this.schemaUpdated
    }],
    or: {
      sid: "-3mzpjc"
    }
  }), x({
    and: [{
      clock: d12.batchedSetValue,
      filter: (k5) => {
        let {
          value: h6
        } = k5;
        return h6 === e14[c15];
      },
      fn: (k5) => {
        let {
          "@@batchInfo": h6
        } = k5;
        return {
          fieldPath: n13,
          "@@batchInfo": h6
        };
      },
      target: this.batchedSchemaUpdated
    }],
    or: {
      sid: "-35eajs"
    }
  }), x({
    and: [{
      clock: d12.batchedSetInnerError,
      source: d12.$outerError,
      fn: (k5, h6) => {
        let {
          value: F4,
          "@@batchInfo": V3
        } = h6;
        return {
          error: k5 ?? F4,
          batchInfo: V3
        };
      },
      target: E6
    }],
    or: {
      sid: "-2nsvk8"
    }
  }), x({
    and: [{
      clock: d12.batchedSetOuterError,
      source: d12.$innerError,
      fn: (k5, h6) => {
        let {
          value: F4,
          "@@batchInfo": V3
        } = h6;
        return {
          error: F4 ?? k5,
          batchInfo: V3
        };
      },
      target: E6
    }],
    or: {
      sid: "-26r961"
    }
  }), x({
    and: [{
      clock: w10([d12.batchedSetValue, d12.changed]),
      source: d12.$value,
      fn: (k5, h6) => {
        let [{
          "@@batchInfo": F4
        }] = h6;
        return {
          value: k5,
          batchInfo: F4
        };
      },
      target: y4
    }],
    or: {
      sid: "-1ppmru"
    }
  }), x({
    and: [{
      clock: w10([d12.batchedReset, d12.resetCompleted]),
      fn: (k5) => {
        let [{
          "@@batchInfo": h6
        }, {
          value: F4,
          error: V3
        }] = k5;
        return {
          value: F4,
          error: V3,
          batchInfo: h6
        };
      },
      target: S2
    }],
    or: {
      sid: "-18o0dn"
    }
  }), x({
    and: [{
      clock: [y4.done, E6.done, S2.done],
      filter: (k5) => {
        let {
          params: h6
        } = k5;
        return !!h6.batchInfo;
      },
      fn: (k5) => {
        let {
          params: h6
        } = k5;
        return {
          fieldPath: n13,
          "@@batchInfo": h6.batchInfo
        };
      },
      target: this.batchedSchemaUpdated
    }],
    or: {
      sid: "96boan"
    }
  }), x({
    and: [{
      clock: d12.focused,
      fn: () => ({
        fieldPath: n13
      }),
      target: this.focused
    }],
    or: {
      sid: "9ndaou"
    }
  }), x({
    and: [{
      clock: d12.blurred,
      fn: () => ({
        fieldPath: n13
      }),
      target: this.blurred
    }],
    or: {
      sid: "a27qpl"
    }
  }), x({
    and: [{
      clock: d12.metaChanged,
      fn: (k5) => ({
        fieldPath: n13,
        meta: k5
      }),
      target: this.metaChanged
    }],
    or: {
      sid: "a5ii9r"
    }
  });
}
function ke2(r7, t20, e14) {
  let o8 = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : [];
  for (const s36 in r7) {
    const c15 = r7[s36];
    switch (c15["@@type"]) {
      case te3: {
        Ge2.call(this, {
          field: c15,
          resultValuesNode: t20,
          resultErrorsNode: e14,
          key: s36,
          path: o8
        });
        break;
      }
      case ae2: {
        Ne3.call(this, {
          field: c15,
          resultValuesNode: t20,
          resultErrorsNode: e14,
          key: s36,
          path: o8,
          map: ke2
        });
        break;
      }
      default: {
        t20[s36] = {}, e14[s36] = {}, ke2.call(this, c15, t20[s36], e14[s36], [...o8, s36]);
        break;
      }
    }
  }
}
function Je2(r7, t20, e14, o8, s36, c15) {
  const l12 = {}, d12 = {}, n13 = {}, g7 = {
    api: n13,
    isValid: true,
    metaChanged: t20,
    schemaUpdated: e14,
    batchedSchemaUpdated: o8,
    focused: s36,
    blurred: c15
  };
  return ke2.call(g7, r7, l12, d12), {
    api: n13,
    values: l12,
    errors: d12,
    isValid: g7.isValid
  };
}
function we3(r7, t20, e14) {
  const o8 = Object.keys(r7), s36 = ue3(o8, "errors");
  t20(s36);
  for (const c15 of o8) {
    const l12 = r7[c15];
    e14 === "inner" ? l12.batchedSetInnerError({
      value: null,
      "@@batchInfo": {
        fieldPath: c15,
        id: s36.id
      }
    }) : e14 === "outer" && l12.batchedSetOuterError({
      value: null,
      "@@batchInfo": {
        fieldPath: c15,
        id: s36.id
      }
    });
  }
}
function be4(r7, t20, e14, o8) {
  let s36 = arguments.length > 4 && arguments[4] !== void 0 ? arguments[4] : false;
  const c15 = Object.keys(t20), l12 = s36 ? c15 : Object.keys(r7);
  if (Object.keys(l12).length === 0)
    return;
  const d12 = ue3(l12, "errors");
  e14(d12);
  for (const n13 of l12) {
    const g7 = t20[n13];
    if (!g7) {
      console.error(`Unknown field with path: ${n13}`);
      continue;
    }
    switch (o8) {
      case "inner": {
        g7.batchedSetInnerError({
          value: r7[n13] ?? null,
          "@@batchInfo": {
            id: d12.id,
            fieldPath: n13
          }
        });
        break;
      }
      case "outer": {
        g7.batchedSetOuterError({
          value: r7[n13] ?? null,
          "@@batchInfo": {
            id: d12.id,
            fieldPath: n13
          }
        });
        break;
      }
    }
  }
}
function We3(r7, t20, e14) {
  const o8 = {};
  function s36(l12) {
    let d12 = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : [];
    for (const n13 in l12) {
      const g7 = l12[n13], y4 = [...d12, n13].join(".");
      if (Q3(g7) || Array.isArray(g7)) {
        const E6 = t20[y4];
        if (!E6) {
          console.error(`Unknown field with path: ${y4}`);
          continue;
        }
        o8[y4] = {
          setValue: E6.batchedSetValue,
          value: g7
        };
        continue;
      }
      typeof g7 == "object" && s36(g7, [...d12, n13]);
    }
  }
  if (s36(r7), Object.keys(o8).length === 0)
    return;
  const c15 = ue3(Object.keys(o8), "values");
  e14(c15);
  for (const l12 in o8)
    o8[l12].setValue({
      value: o8[l12].value,
      "@@batchInfo": {
        id: c15.id,
        fieldPath: l12
      }
    });
}
function He4(r7) {
  const t20 = r7.match(/[\w\s:]+expected/);
  if (!t20)
    throw new Error();
  return t20[0].replace(" expected", "").replace(/:/g, "").split(" ").join(".");
}
function Qe3(r7) {
  return r7 !== null && typeof r7 == "object" && "isData" in r7 && "getErrorMessages" in r7;
}
function Xe2(r7) {
  return (t20) => r7.isData(t20) ? null : r7.getErrorMessages(t20).reduce((e14, o8) => (e14[He4(o8)] = o8, e14), {});
}
function Ve4(r7) {
  let t20 = true;
  for (const e14 in r7)
    if (!r7[e14].isValid) {
      t20 = false;
      break;
    }
  return t20;
}
function ve3(r7) {
  const {
    schemaUpdated: t20,
    focused: e14,
    blurred: o8,
    metaChanged: s36
  } = _e2(), {
    batchedSchemaUpdated: c15,
    addBatchTask: l12
  } = Le3(t20), d12 = Je2(r7, s36, t20, c15, e14, o8), n13 = h(d12.api, {
    name: "$api",
    sid: "5ac4l6"
  }), g7 = h(d12.values, {
    name: "$values",
    sid: "4n2u7a"
  }), y4 = h(d12.errors, {
    name: "$errors",
    sid: "d6ox7g"
  }), E6 = h(true, {
    name: "$isValid",
    sid: "-hzissl"
  });
  return x({
    and: [{
      clock: t20,
      fn: (S2) => {
        switch (S2.type) {
          case "value":
            return {
              values: {
                ...d12.values
              },
              api: {
                ...d12.api
              }
            };
          case "error":
            return {
              errors: {
                ...d12.errors
              },
              isValid: Ve4(d12.api)
            };
          case "all":
            return {
              values: {
                ...d12.values
              },
              api: {
                ...d12.api
              },
              errors: {
                ...d12.errors
              },
              isValid: Ve4(d12.api)
            };
          case "none":
            return {};
        }
      },
      target: ke({
        sid: "mar1gh",
        fn: () => spread({
          api: n13,
          values: g7,
          errors: y4,
          isValid: E6
        }),
        name: "target",
        method: "spread"
      })
    }],
    or: {
      sid: "ortu1l"
    }
  }), {
    $api: n13,
    $values: g7,
    $errors: y4,
    $isValid: E6,
    addBatchTask: l12,
    focused: e14,
    blurred: o8,
    metaChanged: s36
  };
}
function qe3(r7, t20) {
  function e14() {
    return r7.map(Z4);
  }
  function o8(u4) {
    return Array.isArray(u4) ? u4.map(Z4) : [Z4(u4)];
  }
  const s36 = {
    ...Ye2,
    ...t20
  }, c15 = b((u4) => {
    let {
      nodes: v5
    } = u4;
    for (const f15 of v5) {
      if (Q3(f15))
        break;
      ge2(f15);
    }
  }, {
    name: "clearNodesFx",
    sid: "wfgbu9"
  }), l12 = h(e14(), {
    and: {
      name: "<array field values>",
      serialize: {
        read(u4) {
          if (!u4)
            throw new Error();
          if (!Array.isArray(u4))
            throw new Error();
          return u4.map((v5) => {
            const f15 = Z4(v5.values), fe2 = v5.errors, Ie3 = Z4(f15), {
              $api: Pe3,
              addBatchTask: Be2
            } = ve3(Ie3), ze3 = Pe3.getState();
            return be4(fe2, ze3, Be2, "outer"), Ie3;
          });
        },
        write(u4) {
          return u4.map((f15) => pe3(f15) ? f15 : ve3(f15)).map((f15) => pe3(f15) ? f15 : Q3(f15) ? null : {
            values: f15.$values.getState(),
            errors: f15.$errors.getState()
          }).filter(Boolean);
        }
      }
    },
    name: "$values",
    sid: "sjuv08"
  }), d12 = h(null, {
    and: {
      name: "<inner field error>"
    },
    name: "$innerError",
    sid: "-kchvpq"
  }), n13 = h((t20 == null ? void 0 : t20.error) ?? null, {
    and: {
      name: "<outer field error>"
    },
    name: "$outerError",
    sid: "1wsmhq"
  }), g7 = g({
    and: [{
      innerError: d12,
      outerError: n13
    }],
    or: {
      name: "$error",
      sid: "-rshpr3"
    }
  }).map((u4) => {
    let {
      innerError: v5,
      outerError: f15
    } = u4;
    return f15 || v5;
  }), y4 = g7.map((u4) => u4 === null), E6 = h(s36.meta, {
    name: "$meta",
    sid: "-vyojsv"
  }), S2 = p({
    name: "changeMeta",
    sid: "2shed5"
  }), k5 = p({
    name: "metaChanged",
    sid: "-ivbm3n"
  });
  x({
    and: [{
      clock: S2,
      target: E6
    }],
    or: {
      sid: "5d32m6"
    }
  }), x({
    and: [{
      clock: E6,
      target: k5
    }],
    or: {
      sid: "5rdq1k"
    }
  });
  const h6 = p({
    name: "batchedSetInnerError",
    sid: "-c6kya6"
  }), F4 = p({
    name: "batchedSetOuterError",
    sid: "-1addje"
  }), V3 = p({
    name: "batchedSetValue",
    sid: "ko31od"
  }), b4 = p({
    name: "batchedClear",
    sid: "-78eyq"
  }), p11 = p({
    name: "batchedReset",
    sid: "ifde3f"
  }), I6 = p("<field change>", {
    name: "change",
    sid: "osif4q"
  }), B4 = p("<field changed>", {
    name: "changed",
    sid: "af8itw"
  }), X4 = p("<field reset>", {
    name: "reset",
    sid: "xur9p5"
  }), L3 = p("<field resetCompleted>", {
    name: "resetCompleted",
    sid: "-jrc2gj"
  }), O5 = p("<field clear>", {
    name: "clear",
    sid: "s0d9a9"
  }), N4 = p("<field cleared>", {
    name: "cleared",
    sid: "h0c4m7"
  }), z2 = p("<field setInnerError>", {
    name: "setInnerError",
    sid: "5tiq7u"
  }), U3 = p("<field changeError>", {
    name: "changeError",
    sid: "dhpxkn"
  }), M6 = p("<field errorChanged>", {
    name: "errorChanged",
    sid: "-4dw7of"
  }), $5 = p("<field push>", {
    name: "push",
    sid: "-ftivsi"
  }), D5 = p({
    name: "pushed",
    sid: "w0fzsc"
  }), R10 = p("<field swap>", {
    name: "swap",
    sid: "5281m"
  }), T3 = p("<field swap>", {
    name: "swapped",
    sid: "5speod"
  }), G3 = p("<field move>", {
    name: "move",
    sid: "-ekptwp"
  }), ne3 = p("<field moved>", {
    name: "moved",
    sid: "-avkrzh"
  }), J2 = p("<field insert>", {
    name: "insert",
    sid: "wya78o"
  }), K3 = p("<field inserted>", {
    name: "inserted",
    sid: "nxo0na"
  }), Y3 = p("<field unshift>", {
    name: "unshift",
    sid: "-dagn2c"
  }), oe2 = p("<field unshifted>", {
    name: "unshifted",
    sid: "-5avp5n"
  }), W4 = p("<field remove>", {
    name: "remove",
    sid: "8o74l8"
  }), se3 = p("<field removed>", {
    name: "removed",
    sid: "-x5vmoy"
  }), H4 = p("<field pop>", {
    name: "pop",
    sid: "-vqhico"
  }), ee4 = p("<field popped>", {
    name: "popped",
    sid: "-py9d7d"
  }), re3 = p("<field replace>", {
    name: "replace",
    sid: "pjkkq6"
  }), de3 = p("<field replaced>", {
    name: "replaced",
    sid: "6wdzu8"
  }), C5 = v({
    and: {
      source: l12,
      effect: async (u4, v5) => (await c15(De3(u4, v5)), [...v5]),
      name: "syncFx"
    },
    or: {
      name: "syncFx",
      sid: "hlvvff"
    }
  });
  x({
    and: [{
      clock: C5.doneData,
      target: l12
    }],
    or: {
      sid: "jgl6ah"
    }
  }), x({
    and: [{
      clock: [O5, b4],
      fn: () => ({
        values: [],
        error: null
      }),
      target: ke({
        sid: "y4fl20",
        fn: () => spread({
          values: C5,
          error: n13
        }),
        name: "target",
        method: "spread"
      })
    }],
    or: {
      sid: "jhorh7"
    }
  }), x({
    and: [{
      clock: [X4, p11],
      fn: () => {
        const u4 = e14(), v5 = (t20 == null ? void 0 : t20.error) ?? null;
        return {
          sync: u4,
          completed: {
            values: u4,
            error: v5
          },
          outerError: v5,
          innerError: null
        };
      },
      target: ke({
        sid: "-80xiv9",
        fn: () => spread({
          sync: C5,
          completed: L3,
          error: n13,
          innerError: d12
        }),
        name: "target",
        method: "spread"
      })
    }],
    or: {
      sid: "jy6la1"
    }
  }), x({
    and: [{
      clock: [O5, b4],
      target: N4
    }],
    or: {
      sid: "kwtmns"
    }
  });
  const ce3 = v({
    and: {
      source: l12,
      effect: (u4, v5) => u4.concat(o8(v5)),
      name: "pushFx"
    },
    or: {
      name: "pushFx",
      sid: "9z1gk9"
    }
  }), m12 = v({
    and: {
      source: l12,
      effect: (u4, v5) => {
        const f15 = [...u4], fe2 = f15[v5.indexA];
        return f15[v5.indexA] = f15[v5.indexB], f15[v5.indexB] = fe2, f15;
      },
      name: "swapFx"
    },
    or: {
      name: "swapFx",
      sid: "lxs8tg"
    }
  }), x3 = v({
    and: {
      source: l12,
      effect: (u4, v5) => {
        const f15 = [...u4];
        return f15.splice(v5.to, 0, ...f15.splice(v5.from, 1)), f15;
      },
      name: "moveFx"
    },
    or: {
      name: "moveFx",
      sid: "tx15z6"
    }
  }), ie3 = v({
    and: {
      source: l12,
      effect: (u4, v5) => {
        const f15 = [...u4];
        return f15.splice(v5.index, 0, ...o8(v5.value)), f15;
      },
      name: "insertFx"
    },
    or: {
      name: "insertFx",
      sid: "ttx6oo"
    }
  }), Ee2 = v({
    and: {
      source: l12,
      effect: (u4, v5) => {
        const f15 = [...u4];
        return f15.unshift(...o8(v5)), f15;
      },
      name: "unshiftFx"
    },
    or: {
      name: "unshiftFx",
      sid: "2ylkuq"
    }
  }), xe5 = v({
    and: {
      source: l12,
      effect: async (u4, v5) => {
        const f15 = [...u4];
        return f15.splice(v5.index, 1)[0], f15;
      },
      name: "removeFx"
    },
    or: {
      name: "removeFx",
      sid: "-2pfpcj"
    }
  }), Fe3 = v({
    and: {
      source: l12,
      effect: (u4) => {
        const v5 = [...u4];
        return v5.pop(), v5;
      },
      name: "popFx"
    },
    or: {
      name: "popFx",
      sid: "-4q41tp"
    }
  }), Se2 = v({
    and: {
      source: l12,
      effect: (u4, v5) => {
        const f15 = [...u4];
        return f15.splice(v5.index, 1, ...o8(v5.value)), f15;
      },
      name: "replaceFx"
    },
    or: {
      name: "replaceFx",
      sid: "-8h4dzt"
    }
  });
  return x({
    and: [{
      clock: l12,
      target: B4
    }],
    or: {
      sid: "z5to9p"
    }
  }), x({
    and: [{
      clock: V3,
      fn: (u4) => o8(u4.value),
      target: C5
    }],
    or: {
      sid: "-zgzqa1"
    }
  }), x({
    and: [{
      clock: I6,
      fn: (u4) => o8(u4),
      target: C5
    }],
    or: {
      sid: "-zdoypv"
    }
  }), x({
    and: [{
      clock: $5,
      target: ce3
    }],
    or: {
      sid: "-yyuip4"
    }
  }), x({
    and: [{
      clock: ce3.done,
      fn: (u4) => {
        let {
          params: v5,
          result: f15
        } = u4;
        return {
          pushed: {
            params: v5,
            result: f15
          },
          values: f15
        };
      },
      target: ke({
        sid: "-83eqov",
        fn: () => spread({
          pushed: D5,
          values: C5
        }),
        name: "target",
        method: "spread"
      })
    }],
    or: {
      sid: "-yxqxie"
    }
  }), x({
    and: [{
      clock: R10,
      target: m12
    }],
    or: {
      sid: "-yflpxh"
    }
  }), x({
    and: [{
      clock: m12.done,
      fn: (u4) => {
        let {
          params: v5,
          result: f15
        } = u4;
        return {
          swapped: {
            params: v5,
            result: f15
          },
          values: f15
        };
      },
      target: ke({
        sid: "wl1rw3",
        fn: () => spread({
          swapped: T3,
          values: C5
        }),
        name: "target",
        method: "spread"
      })
    }],
    or: {
      sid: "-yei4qr"
    }
  }), x({
    and: [{
      clock: G3,
      target: x3
    }],
    or: {
      sid: "-nmv6f6"
    }
  }), x({
    and: [{
      clock: x3.done,
      fn: (u4) => {
        let {
          params: v5,
          result: f15
        } = u4;
        return {
          moved: {
            params: v5,
            result: f15
          },
          values: f15
        };
      },
      target: ke({
        sid: "-lvhia8",
        fn: () => spread({
          moved: ne3,
          values: C5
        }),
        name: "target",
        method: "spread"
      })
    }],
    or: {
      sid: "-nlrl8g"
    }
  }), x({
    and: [{
      clock: J2,
      target: ie3
    }],
    or: {
      sid: "-n3mdnj"
    }
  }), x({
    and: [{
      clock: ie3.done,
      fn: (u4) => {
        let {
          params: v5,
          result: f15
        } = u4;
        return {
          inserted: {
            params: v5,
            result: f15
          },
          values: f15
        };
      },
      target: ke({
        sid: "4n1bso",
        fn: () => spread({
          inserted: K3,
          values: C5
        }),
        name: "target",
        method: "spread"
      })
    }],
    or: {
      sid: "-n2isgt"
    }
  }), x({
    and: [{
      clock: Y3,
      target: Ee2
    }],
    or: {
      sid: "-mkdkvw"
    }
  }), x({
    and: [{
      clock: Ee2.done,
      fn: (u4) => {
        let {
          params: v5,
          result: f15
        } = u4;
        return {
          unshifted: {
            params: v5,
            result: f15
          },
          values: f15
        };
      },
      target: ke({
        sid: "l7m3lh",
        fn: () => spread({
          unshifted: oe2,
          values: C5
        }),
        name: "target",
        method: "spread"
      })
    }],
    or: {
      sid: "-m7qb8l"
    }
  }), x({
    and: [{
      clock: W4,
      target: xe5
    }],
    or: {
      sid: "-lpl3no"
    }
  }), x({
    and: [{
      clock: xe5.done,
      fn: (u4) => {
        let {
          params: v5,
          result: f15
        } = u4;
        return {
          removed: {
            params: v5,
            result: f15
          },
          values: f15
        };
      },
      target: ke({
        sid: "-lvu946",
        fn: () => spread({
          removed: se3,
          values: C5
        }),
        name: "target",
        method: "spread"
      })
    }],
    or: {
      sid: "-lmu4ov"
    }
  }), x({
    and: [{
      clock: H4,
      target: Fe3
    }],
    or: {
      sid: "-l4ox3y"
    }
  }), x({
    and: [{
      clock: Fe3.doneData,
      fn: (u4) => u4,
      target: [C5, ee4]
    }],
    or: {
      sid: "-kqe9ok"
    }
  }), x({
    and: [{
      clock: re3,
      target: Se2
    }],
    or: {
      sid: "-kbjtnt"
    }
  }), x({
    and: [{
      clock: Se2.done,
      fn: (u4) => {
        let {
          params: v5,
          result: f15
        } = u4;
        return {
          replaced: {
            params: v5,
            result: f15
          },
          values: f15
        };
      },
      target: ke({
        sid: "l79crj",
        fn: () => spread({
          replaced: de3,
          values: C5
        }),
        name: "target",
        method: "spread"
      })
    }],
    or: {
      sid: "-k8sup0"
    }
  }), x({
    and: [{
      clock: U3,
      target: n13
    }],
    or: {
      sid: "-jqnn43"
    }
  }), x({
    and: [{
      clock: z2,
      target: d12
    }],
    or: {
      sid: "-9eexem"
    }
  }), x({
    and: [{
      clock: U3,
      target: n13
    }],
    or: {
      sid: "-9bnyft"
    }
  }), x({
    and: [{
      clock: z2,
      target: d12
    }],
    or: {
      sid: "-8xdb0f"
    }
  }), x({
    and: [{
      clock: h6,
      fn: (u4) => u4.value,
      target: d12
    }],
    or: {
      sid: "-8umc1m"
    }
  }), x({
    and: [{
      clock: F4,
      fn: (u4) => u4.value,
      target: n13
    }],
    or: {
      sid: "-8frw0v"
    }
  }), x({
    and: [{
      clock: h6,
      fn: (u4) => u4.value,
      target: d12
    }],
    or: {
      sid: "-8ch4gp"
    }
  }), x({
    and: [{
      clock: F4,
      fn: (u4) => u4.value,
      target: n13
    }],
    or: {
      sid: "-7xmofy"
    }
  }), x({
    and: [{
      clock: g7,
      target: M6
    }],
    or: {
      sid: "-7is8f7"
    }
  }), {
    "@@type": ae2,
    batchedSetInnerError: h6,
    batchedSetOuterError: F4,
    batchedSetValue: V3,
    batchedClear: b4,
    batchedReset: p11,
    $meta: E6,
    $values: l12,
    $error: g7,
    $outerError: n13,
    $innerError: d12,
    $isValid: y4,
    changeMeta: S2,
    metaChanged: k5,
    setInnerError: z2,
    changeError: U3,
    errorChanged: M6,
    change: I6,
    changed: B4,
    push: $5,
    pushed: D5,
    swap: R10,
    swapped: T3,
    move: G3,
    moved: ne3,
    clear: O5,
    cleared: N4,
    insert: J2,
    inserted: K3,
    unshift: Y3,
    unshifted: oe2,
    remove: W4,
    removed: se3,
    pop: H4,
    popped: ee4,
    replace: re3,
    replaced: de3,
    reset: X4,
    resetCompleted: L3,
    copyOnCreateForm: s36.copyOnCreateForm,
    "@@unitShape": () => ({
      values: l12,
      error: g7,
      meta: E6,
      isValid: y4,
      changeMeta: S2,
      change: I6,
      changeError: U3,
      clear: O5,
      reset: X4,
      push: $5,
      move: G3,
      swap: R10,
      insert: J2,
      unshift: Y3,
      remove: W4,
      pop: H4,
      replace: re3
    })
  };
}
function $e3(r7) {
  switch (r7["@@type"]) {
    case ae2:
      return qe3(r7.$values.getState(), {
        error: r7.$error.getState(),
        meta: r7.$meta.getState(),
        copyOnCreateForm: r7.copyOnCreateForm
      });
    case te3:
      return je3(r7.$value.getState(), {
        error: r7.$error.getState(),
        meta: r7.$meta.getState(),
        copyOnCreateForm: r7.copyOnCreateForm
      });
  }
}
function Z4(r7) {
  const t20 = {};
  if (Q3(r7))
    return r7;
  for (const e14 in r7) {
    const o8 = r7[e14];
    if (Q3(o8)) {
      t20[e14] = je3(o8);
      continue;
    }
    if (Re3(o8) || Ke3(o8)) {
      t20[e14] = o8;
      continue;
    }
    if (Array.isArray(o8)) {
      t20[e14] = qe3(o8);
      continue;
    }
    typeof o8 == "object" && (t20[e14] = Z4(o8));
  }
  return t20;
}
function Ae3(r7) {
  const t20 = {};
  for (const e14 in r7) {
    const o8 = r7[e14];
    switch (o8["@@type"]) {
      case ae2: {
        t20[e14] = o8.copyOnCreateForm ? $e3(o8) : o8;
        break;
      }
      case te3: {
        t20[e14] = o8.copyOnCreateForm ? $e3(o8) : o8;
        break;
      }
      case void 0: {
        t20[e14] = Ae3(o8);
        break;
      }
    }
  }
  return t20;
}
function Ze3(r7, t20) {
  const e14 = Object.keys(r7).filter((s36) => !/\.[0-9]\./.test(s36)), o8 = ue3(e14, "reset");
  t20(o8);
  for (const s36 of e14)
    r7[s36].batchedReset({
      "@@batchInfo": {
        fieldPath: s36,
        id: o8.id
      }
    });
}
function er2(r7) {
  return r7 && r7.__esModule && Object.prototype.hasOwnProperty.call(r7, "default") ? r7.default : r7;
}
function or3(r7) {
  const {
    schema: t20,
    validation: e14 = () => null,
    validationStrategies: o8 = ["submit", "change", "blur", "focus"],
    clearOuterErrorsOnSubmit: s36 = o8.includes("submit")
  } = r7, c15 = Ae3(Z4(t20)), {
    $errors: l12,
    $values: d12,
    $isValid: n13,
    $api: g7,
    focused: y4,
    blurred: E6,
    addBatchTask: S2,
    metaChanged: k5
  } = ve3(c15), h6 = h(structuredClone(d12.getState()), {
    name: "$snapshot",
    sid: "-f327m3"
  }), F4 = g({
    and: [d12, h6, (m12, x3) => !tr(m12, x3)],
    or: {
      name: "$isChanged",
      sid: "14425w"
    }
  }), V3 = h({
    needSav: false,
    skipValidation: false
  }, {
    name: "$innerMeta",
    sid: "-5mc75k"
  }), b4 = v({
    and: {
      source: g7,
      effect: (m12) => we3(m12, S2, "outer")
    },
    or: {
      name: "clearOuterErrorsFx",
      sid: "zi6imm"
    }
  }), p11 = v({
    and: {
      source: g7,
      effect: (m12) => we3(m12, S2, "inner")
    },
    or: {
      name: "clearInnerErrorsFx",
      sid: "-kavetu"
    }
  }), I6 = v({
    and: {
      source: g7,
      effect: (m12) => Ze3(m12, S2)
    },
    or: {
      name: "resetFx",
      sid: "-ykxubg"
    }
  }), B4 = v({
    and: {
      source: g7,
      effect: (m12, x3) => We3(x3, m12, S2)
    },
    or: {
      name: "setValuesFx",
      sid: "-4b841x"
    }
  }), X4 = v({
    and: {
      source: g7,
      effect: (m12, x3) => be4(x3, m12, S2, "inner", true)
    },
    or: {
      name: "setInnerErrorsFx",
      sid: "v56e0n"
    }
  }), L3 = v({
    and: {
      source: g7,
      effect: (m12, x3) => be4(x3, m12, S2, "outer", true)
    },
    or: {
      name: "setOuterErrorsFx",
      sid: "-9eu2p6"
    }
  }), O5 = p("<form fill>", {
    name: "fill",
    sid: "kpzm3t"
  }), N4 = p({
    name: "filled",
    sid: "tyra75"
  }), z2 = p("<form clear outer errors>", {
    name: "clearOuterErrors",
    sid: "2zemk7"
  }), U3 = p("<form clear inner errors>", {
    name: "clearInnerErrors",
    sid: "p9rjp8"
  }), M6 = p("<form changed>", {
    name: "changed",
    sid: "-unj094"
  }), $5 = p("<form errors changed>", {
    name: "errorsChanged",
    sid: "-n0m8wf"
  }), D5 = p("<form submit>", {
    name: "submit",
    sid: "-1ynx7q"
  }), R10 = p("<form submitted>", {
    name: "submitted",
    sid: "-x9ncho"
  }), T3 = p("<form validate>", {
    name: "validate",
    sid: "-npokp9"
  }), G3 = p("<form validated>", {
    name: "validated",
    sid: "dznotw"
  }), ne3 = p("<validation failed>", {
    name: "validationFailed",
    sid: "5pt79b"
  }), J2 = p("<form validated and submitted>", {
    name: "validatedAndSubmitted",
    sid: "46hyv4"
  }), K3 = p({
    name: "changeInnerMeta",
    sid: "-7iewhc"
  }), Y3 = p({
    name: "forceUpdateSnapshot",
    sid: "-76axh9"
  }), oe2 = p({
    name: "snapshotUpdated",
    sid: "lntxx6"
  }), W4 = p("<form reset>", {
    name: "reset",
    sid: "oqr3hr"
  }), se3 = Qe3(e14) ? Xe2(e14) : e14, H4 = b(se3, {
    name: "validateFx",
    sid: "-1duodp"
  }), ee4 = H4.pending;
  x({
    and: [{
      clock: K3,
      source: V3,
      fn: (m12, x3) => ({
        ...m12,
        ...x3
      }),
      target: V3
    }],
    or: {
      sid: "-eq4qw5"
    }
  });
  const re3 = [...new Set(o8)];
  for (const m12 of re3)
    switch (m12) {
      case "submit": {
        x({
          and: [{
            clock: R10,
            target: [T3, K3.prepend(() => ({
              needSav: true
            }))]
          }],
          or: {
            sid: "-e8jbrf"
          }
        });
        break;
      }
      case "focus": {
        x({
          and: [{
            clock: y4,
            target: T3
          }],
          or: {
            sid: "-dqxwrv"
          }
        });
        break;
      }
      case "blur": {
        x({
          and: [{
            clock: E6,
            target: T3
          }],
          or: {
            sid: "-dazvke"
          }
        });
        break;
      }
      case "change": {
        x({
          and: [{
            clock: M6,
            target: T3
          }],
          or: {
            sid: "-2x3s2u"
          }
        }), x({
          and: [{
            clock: W4,
            target: K3.prepend(() => ({
              skipValidation: true
            }))
          }],
          or: {
            sid: "-2uct41"
          }
        });
        break;
      }
    }
  re3.includes("submit") || x({
    and: [{
      clock: R10,
      source: {
        isValid: n13,
        isValidationPending: ee4
      },
      filter: (m12) => {
        let {
          isValid: x3,
          isValidationPending: ie3
        } = m12;
        return x3 && !ie3;
      },
      fn: (m12, x3) => x3,
      target: J2
    }],
    or: {
      sid: "-2cre7x"
    }
  }), x({
    and: [{
      clock: [J2, Y3],
      source: d12,
      fn: (m12) => structuredClone(m12),
      target: h6
    }],
    or: {
      sid: "-1iipms"
    }
  }), x({
    and: [{
      clock: W4,
      target: I6
    }],
    or: {
      sid: "-1eo5h9"
    }
  }), x({
    and: [{
      clock: d12,
      target: M6
    }],
    or: {
      sid: "-10di1v"
    }
  }), x({
    and: [{
      clock: l12,
      target: $5
    }],
    or: {
      sid: "-xmj32"
    }
  }), x({
    and: [{
      clock: O5,
      filter: (m12) => !!m12.values,
      fn: (m12) => m12.values,
      target: B4
    }],
    or: {
      sid: "-jbvno"
    }
  }), x({
    and: [{
      clock: O5,
      filter: (m12) => !!m12.errors,
      fn: (m12) => m12.errors,
      target: L3
    }],
    or: {
      sid: "-fhbi5"
    }
  });
  const de3 = w10([O5.filter({
    fn: (m12) => !!m12.values && !m12.errors
  }), M6]), C5 = w10([O5.filter({
    fn: (m12) => !!m12.errors && !m12.values
  }), $5]), ce3 = w10([O5.filter({
    fn: (m12) => !!m12.values && !!m12.errors
  }), ke({
    sid: "-d58jjh",
    fn: () => combineEvents([M6, $5]),
    name: "formFilled",
    method: "combineEvents"
  })]);
  return x({
    and: [{
      clock: [de3, C5, ce3],
      fn: () => {
      },
      target: N4
    }],
    or: {
      sid: "yjyhq"
    }
  }), x({
    and: [{
      clock: h6,
      target: oe2
    }],
    or: {
      sid: "1deeih"
    }
  }), x({
    and: [{
      clock: D5,
      source: d12,
      target: R10
    }],
    or: {
      sid: "1g5dha"
    }
  }), x({
    and: [{
      clock: z2,
      target: b4
    }],
    or: {
      sid: "bsxvs4"
    }
  }), x({
    and: [{
      clock: U3,
      target: p11
    }],
    or: {
      sid: "bvouqx"
    }
  }), s36 && x({
    and: [{
      clock: D5,
      target: z2
    }],
    or: {
      sid: "cajate"
    }
  }), x({
    and: [{
      clock: T3,
      source: {
        values: d12,
        meta: V3
      },
      filter: (m12) => {
        let {
          meta: x3
        } = m12;
        return !x3.skipValidation;
      },
      fn: (m12) => {
        let {
          values: x3
        } = m12;
        return x3;
      },
      target: H4
    }],
    or: {
      sid: "cpdqsf"
    }
  }), x({
    and: [{
      clock: T3,
      source: V3,
      filter: (m12) => m12.skipValidation,
      fn: () => ({
        skipValidation: false
      }),
      target: K3
    }],
    or: {
      sid: "cts3jb"
    }
  }), x({
    and: [{
      clock: H4.done,
      filter: (m12) => {
        let {
          result: x3
        } = m12;
        return !x3;
      },
      fn: (m12) => {
        let {
          params: x3
        } = m12;
        return x3;
      },
      target: G3
    }],
    or: {
      sid: "d9q4qs"
    }
  }), x({
    and: [{
      clock: G3,
      target: p11
    }],
    or: {
      sid: "dp4dcw"
    }
  }), x({
    and: [{
      clock: H4.doneData,
      filter: Boolean,
      target: [X4, ne3, K3.prepend(() => ({
        needSav: false
      }))]
    }],
    or: {
      sid: "drvcbp"
    }
  }), x({
    and: [{
      clock: G3,
      source: V3,
      filter: (m12) => {
        let {
          needSav: x3
        } = m12;
        return x3;
      },
      fn: (m12, x3) => x3,
      target: [J2, K3.prepend(() => ({
        needSav: false
      }))]
    }],
    or: {
      sid: "e8wypw"
    }
  }), {
    $errors: l12,
    $values: d12,
    $snapshot: h6,
    $isValid: n13,
    $isChanged: F4,
    $isValidationPending: ee4,
    fields: c15,
    metaChanged: k5,
    changed: M6,
    errorsChanged: $5,
    submit: D5,
    submitted: R10,
    reset: W4,
    clearOuterErrors: z2,
    clearInnerErrors: U3,
    validate: T3,
    validated: G3,
    validationFailed: ne3,
    validatedAndSubmitted: J2,
    forceUpdateSnapshot: Y3,
    snapshotUpdated: oe2,
    fill: O5,
    filled: N4,
    "@@unitShape": () => ({
      errors: l12,
      values: d12,
      snapshot: h6,
      isValidationPending: ee4,
      isChanged: F4,
      isValid: n13,
      submit: D5,
      validate: T3,
      reset: W4,
      clearOuterErrors: z2,
      clearInnerErrors: U3,
      forceUpdateSnapshot: Y3,
      fill: O5
    })
  };
}
var te3, Te3, ae2, Ye2, rr, tr;
var init_dist5 = __esm({
  "node_modules/@effector-reform/core/dist/index.js"() {
    init_effector();
    init_patronum();
    te3 = Symbol("primitive-field");
    Te3 = {
      error: null,
      meta: {},
      copyOnCreateForm: true
    };
    ae2 = Symbol("array-field");
    Ye2 = {
      error: null,
      meta: {},
      copyOnCreateForm: true
    };
    rr = function r6(t20, e14) {
      if (t20 === e14) return true;
      if (t20 && e14 && typeof t20 == "object" && typeof e14 == "object") {
        if (t20.constructor !== e14.constructor) return false;
        var o8, s36, c15;
        if (Array.isArray(t20)) {
          if (o8 = t20.length, o8 != e14.length) return false;
          for (s36 = o8; s36-- !== 0; ) if (!r6(t20[s36], e14[s36])) return false;
          return true;
        }
        if (t20.constructor === RegExp) return t20.source === e14.source && t20.flags === e14.flags;
        if (t20.valueOf !== Object.prototype.valueOf) return t20.valueOf() === e14.valueOf();
        if (t20.toString !== Object.prototype.toString) return t20.toString() === e14.toString();
        if (c15 = Object.keys(t20), o8 = c15.length, o8 !== Object.keys(e14).length) return false;
        for (s36 = o8; s36-- !== 0; ) if (!Object.prototype.hasOwnProperty.call(e14, c15[s36])) return false;
        for (s36 = o8; s36-- !== 0; ) {
          var l12 = c15[s36];
          if (!r6(t20[l12], e14[l12])) return false;
        }
        return true;
      }
      return t20 !== t20 && e14 !== e14;
    };
    tr = /* @__PURE__ */ er2(rr);
  }
});

// node_modules/@effector-reform/react/dist/index.js
function i12(e14, r7) {
  return r7 ? r7.getState(e14) : e14.getState();
}
function c14(e14, r7) {
  return r7 ? F(e14, { scope: r7 }) : e14;
}
function l10(e14, r7) {
  const a18 = {}, t20 = (s36) => i12(s36, r7), o8 = (s36) => c14(s36, r7);
  for (const s36 in e14) {
    const n13 = e14[s36];
    switch (n13["@@type"]) {
      case ae2: {
        a18[s36] = {
          values: t20(n13.$values).map(
            (u4) => Q3(u4) ? u4 : l10(u4, r7)
          ),
          meta: t20(n13.$meta),
          isValid: t20(n13.$isValid),
          error: t20(n13.$error),
          onChange: o8(n13.change),
          onChangeError: o8(n13.changeError),
          onReset: o8(n13.reset),
          onPush: o8(n13.push),
          onSwap: o8(n13.swap),
          onMove: o8(n13.move),
          onInsert: o8(n13.insert),
          onUnshift: o8(n13.unshift),
          onRemove: o8(n13.remove),
          onPop: o8(n13.pop),
          onReplace: o8(n13.replace),
          onChangeMeta: o8(n13.changeMeta)
        };
        break;
      }
      case te3: {
        a18[s36] = {
          value: t20(n13.$value),
          error: t20(n13.$error),
          meta: t20(n13.$meta),
          isValid: t20(n13.$isValid),
          isFocused: t20(n13.$isFocused),
          onChangeMeta: o8(n13.changeMeta),
          onChangeError: o8(n13.changeError),
          onChange: o8(n13.change),
          onFocus: o8(n13.focus),
          onBlur: o8(n13.blur)
        };
        break;
      }
      default:
        a18[s36] = l10(
          e14[s36],
          r7
        );
    }
  }
  return a18;
}
function I5(e14, r7, a18) {
  const t20 = [];
  for (const o8 of e14)
    t20.push(
      D({
        unit: o8,
        fn: a18,
        scope: r7 ?? void 0,
        batch: true
      })
    );
  return {
    subscriptions: t20,
    unsubscribe: () => {
      for (const o8 of t20)
        o8.unsubscribe();
    }
  };
}
function j2(e14, r7) {
  const a18 = f2(), {
    values: t20,
    errors: o8,
    snapshot: s36,
    forceUpdateSnapshot: n13,
    submit: u4,
    reset: g7,
    clearOuterErrors: m12,
    clearInnerErrors: v5,
    validate: d12,
    ...b4
  } = c2(e14), [p11, f15] = (0, import_react220.useState)(
    () => l10(e14.fields, a18)
  );
  return (0, import_react220.useEffect)(() => {
    const { unsubscribe: h6 } = I5(
      [e14.$values, e14.$errors, e14.metaChanged],
      a18,
      () => f15(l10(e14.fields, a18))
    );
    return () => {
      h6(), r7 != null && r7.resetOnUnmount && g7();
    };
  }, []), {
    values: t20,
    errors: o8,
    fields: p11,
    snapshot: s36,
    onSubmit: (h6) => {
      h6.preventDefault(), u4();
    },
    onForceUpdateSnapshot: n13,
    onReset: g7,
    onValidate: d12,
    onClearOuterErrors: m12,
    onClearInnerErrors: v5,
    ...b4
  };
}
var import_react220;
var init_dist6 = __esm({
  "node_modules/@effector-reform/react/dist/index.js"() {
    init_dist5();
    init_effector_react();
    import_react220 = __toESM(require_react(), 1);
    init_effector();
  }
});

// dist/server/chunks/chunk-BzlClNuB.js
var createPageInit, createPageStart, useIsLarge, useIsHuge;
var init_chunk_BzlClNuB = __esm({
  "dist/server/chunks/chunk-BzlClNuB.js"() {
    "use strict";
    init_effector();
    init_esm();
    createPageInit = () => p({
      name: "createPageInit",
      sid: "-5a10p2"
    });
    createPageStart = () => p({
      name: "createPageStart",
      sid: "53e7vg"
    });
    useIsLarge = () => useMediaQuery("(min-width: 1200px");
    useIsHuge = () => useMediaQuery("(min-width: 1440px)");
  }
});

// node_modules/@effector-reform/zod/dist/index.js
function a16(e14) {
  return async (r7) => {
    try {
      return await e14.parseAsync(r7), null;
    } catch (u4) {
      const {
        errors: s36
      } = u4;
      return s36.reduce((t20, n13) => (t20[n13.path.join(".")] || (t20[n13.path.join(".")] = n13.message), t20), {});
    }
  };
}
var init_dist7 = __esm({
  "node_modules/@effector-reform/zod/dist/index.js"() {
  }
});

// node_modules/zod/lib/index.mjs
function setErrorMap(map) {
  overrideErrorMap = map;
}
function getErrorMap() {
  return overrideErrorMap;
}
function addIssueToContext(ctx, issueData) {
  const overrideMap = getErrorMap();
  const issue = makeIssue({
    issueData,
    data: ctx.data,
    path: ctx.path,
    errorMaps: [
      ctx.common.contextualErrorMap,
      ctx.schemaErrorMap,
      overrideMap,
      overrideMap === errorMap ? void 0 : errorMap
      // then global default map
    ].filter((x3) => !!x3)
  });
  ctx.common.issues.push(issue);
}
function __classPrivateFieldGet2(receiver, state2, kind, f15) {
  if (kind === "a" && !f15) throw new TypeError("Private accessor was defined without a getter");
  if (typeof state2 === "function" ? receiver !== state2 || !f15 : !state2.has(receiver)) throw new TypeError("Cannot read private member from an object whose class did not declare it");
  return kind === "m" ? f15 : kind === "a" ? f15.call(receiver) : f15 ? f15.value : state2.get(receiver);
}
function __classPrivateFieldSet2(receiver, state2, value, kind, f15) {
  if (kind === "m") throw new TypeError("Private method is not writable");
  if (kind === "a" && !f15) throw new TypeError("Private accessor was defined without a setter");
  if (typeof state2 === "function" ? receiver !== state2 || !f15 : !state2.has(receiver)) throw new TypeError("Cannot write private member to an object whose class did not declare it");
  return kind === "a" ? f15.call(receiver, value) : f15 ? f15.value = value : state2.set(receiver, value), value;
}
function processCreateParams(params) {
  if (!params)
    return {};
  const { errorMap: errorMap2, invalid_type_error, required_error, description } = params;
  if (errorMap2 && (invalid_type_error || required_error)) {
    throw new Error(`Can't use "invalid_type_error" or "required_error" in conjunction with custom error map.`);
  }
  if (errorMap2)
    return { errorMap: errorMap2, description };
  const customMap = (iss, ctx) => {
    var _a, _b;
    const { message } = params;
    if (iss.code === "invalid_enum_value") {
      return { message: message !== null && message !== void 0 ? message : ctx.defaultError };
    }
    if (typeof ctx.data === "undefined") {
      return { message: (_a = message !== null && message !== void 0 ? message : required_error) !== null && _a !== void 0 ? _a : ctx.defaultError };
    }
    if (iss.code !== "invalid_type")
      return { message: ctx.defaultError };
    return { message: (_b = message !== null && message !== void 0 ? message : invalid_type_error) !== null && _b !== void 0 ? _b : ctx.defaultError };
  };
  return { errorMap: customMap, description };
}
function timeRegexSource(args) {
  let regex2 = `([01]\\d|2[0-3]):[0-5]\\d:[0-5]\\d`;
  if (args.precision) {
    regex2 = `${regex2}\\.\\d{${args.precision}}`;
  } else if (args.precision == null) {
    regex2 = `${regex2}(\\.\\d+)?`;
  }
  return regex2;
}
function timeRegex(args) {
  return new RegExp(`^${timeRegexSource(args)}$`);
}
function datetimeRegex(args) {
  let regex2 = `${dateRegexSource}T${timeRegexSource(args)}`;
  const opts = [];
  opts.push(args.local ? `Z?` : `Z`);
  if (args.offset)
    opts.push(`([+-]\\d{2}:?\\d{2})`);
  regex2 = `${regex2}(${opts.join("|")})`;
  return new RegExp(`^${regex2}$`);
}
function isValidIP(ip, version) {
  if ((version === "v4" || !version) && ipv4Regex.test(ip)) {
    return true;
  }
  if ((version === "v6" || !version) && ipv6Regex.test(ip)) {
    return true;
  }
  return false;
}
function floatSafeRemainder(val, step) {
  const valDecCount = (val.toString().split(".")[1] || "").length;
  const stepDecCount = (step.toString().split(".")[1] || "").length;
  const decCount = valDecCount > stepDecCount ? valDecCount : stepDecCount;
  const valInt = parseInt(val.toFixed(decCount).replace(".", ""));
  const stepInt = parseInt(step.toFixed(decCount).replace(".", ""));
  return valInt % stepInt / Math.pow(10, decCount);
}
function deepPartialify(schema) {
  if (schema instanceof ZodObject) {
    const newShape = {};
    for (const key in schema.shape) {
      const fieldSchema = schema.shape[key];
      newShape[key] = ZodOptional.create(deepPartialify(fieldSchema));
    }
    return new ZodObject({
      ...schema._def,
      shape: () => newShape
    });
  } else if (schema instanceof ZodArray) {
    return new ZodArray({
      ...schema._def,
      type: deepPartialify(schema.element)
    });
  } else if (schema instanceof ZodOptional) {
    return ZodOptional.create(deepPartialify(schema.unwrap()));
  } else if (schema instanceof ZodNullable) {
    return ZodNullable.create(deepPartialify(schema.unwrap()));
  } else if (schema instanceof ZodTuple) {
    return ZodTuple.create(schema.items.map((item2) => deepPartialify(item2)));
  } else {
    return schema;
  }
}
function mergeValues(a18, b4) {
  const aType = getParsedType(a18);
  const bType = getParsedType(b4);
  if (a18 === b4) {
    return { valid: true, data: a18 };
  } else if (aType === ZodParsedType.object && bType === ZodParsedType.object) {
    const bKeys = util.objectKeys(b4);
    const sharedKeys = util.objectKeys(a18).filter((key) => bKeys.indexOf(key) !== -1);
    const newObj = { ...a18, ...b4 };
    for (const key of sharedKeys) {
      const sharedValue = mergeValues(a18[key], b4[key]);
      if (!sharedValue.valid) {
        return { valid: false };
      }
      newObj[key] = sharedValue.data;
    }
    return { valid: true, data: newObj };
  } else if (aType === ZodParsedType.array && bType === ZodParsedType.array) {
    if (a18.length !== b4.length) {
      return { valid: false };
    }
    const newArray = [];
    for (let index3 = 0; index3 < a18.length; index3++) {
      const itemA = a18[index3];
      const itemB = b4[index3];
      const sharedValue = mergeValues(itemA, itemB);
      if (!sharedValue.valid) {
        return { valid: false };
      }
      newArray.push(sharedValue.data);
    }
    return { valid: true, data: newArray };
  } else if (aType === ZodParsedType.date && bType === ZodParsedType.date && +a18 === +b4) {
    return { valid: true, data: a18 };
  } else {
    return { valid: false };
  }
}
function createZodEnum(values2, params) {
  return new ZodEnum({
    values: values2,
    typeName: ZodFirstPartyTypeKind.ZodEnum,
    ...processCreateParams(params)
  });
}
function custom(check2, params = {}, fatal) {
  if (check2)
    return ZodAny.create().superRefine((data, ctx) => {
      var _a, _b;
      if (!check2(data)) {
        const p11 = typeof params === "function" ? params(data) : typeof params === "string" ? { message: params } : params;
        const _fatal = (_b = (_a = p11.fatal) !== null && _a !== void 0 ? _a : fatal) !== null && _b !== void 0 ? _b : true;
        const p22 = typeof p11 === "string" ? { message: p11 } : p11;
        ctx.addIssue({ code: "custom", ...p22, fatal: _fatal });
      }
    });
  return ZodAny.create();
}
var util, objectUtil, ZodParsedType, getParsedType, ZodIssueCode, quotelessJson, ZodError, errorMap, overrideErrorMap, makeIssue, EMPTY_PATH, ParseStatus, INVALID, DIRTY, OK, isAborted, isDirty, isValid, isAsync, errorUtil, _ZodEnum_cache, _ZodNativeEnum_cache, ParseInputLazyPath, handleResult, ZodType, cuidRegex, cuid2Regex, ulidRegex, uuidRegex, nanoidRegex, durationRegex, emailRegex, _emojiRegex, emojiRegex, ipv4Regex, ipv6Regex, base64Regex, dateRegexSource, dateRegex, ZodString, ZodNumber, ZodBigInt, ZodBoolean, ZodDate, ZodSymbol, ZodUndefined, ZodNull, ZodAny, ZodUnknown, ZodNever, ZodVoid, ZodArray, ZodObject, ZodUnion, getDiscriminator, ZodDiscriminatedUnion, ZodIntersection, ZodTuple, ZodRecord, ZodMap, ZodSet, ZodFunction, ZodLazy, ZodLiteral, ZodEnum, ZodNativeEnum, ZodPromise, ZodEffects, ZodOptional, ZodNullable, ZodDefault, ZodCatch, ZodNaN, BRAND, ZodBranded, ZodPipeline, ZodReadonly, late, ZodFirstPartyTypeKind, instanceOfType, stringType, numberType, nanType, bigIntType, booleanType, dateType, symbolType, undefinedType, nullType, anyType, unknownType, neverType, voidType, arrayType, objectType, strictObjectType, unionType, discriminatedUnionType, intersectionType, tupleType, recordType, mapType, setType, functionType, lazyType, literalType, enumType, nativeEnumType, promiseType, effectsType, optionalType, nullableType, preprocessType, pipelineType, ostring, onumber, oboolean, coerce, NEVER, z;
var init_lib = __esm({
  "node_modules/zod/lib/index.mjs"() {
    (function(util2) {
      util2.assertEqual = (val) => val;
      function assertIs(_arg) {
      }
      util2.assertIs = assertIs;
      function assertNever(_x) {
        throw new Error();
      }
      util2.assertNever = assertNever;
      util2.arrayToEnum = (items) => {
        const obj = {};
        for (const item2 of items) {
          obj[item2] = item2;
        }
        return obj;
      };
      util2.getValidEnumValues = (obj) => {
        const validKeys = util2.objectKeys(obj).filter((k5) => typeof obj[obj[k5]] !== "number");
        const filtered = {};
        for (const k5 of validKeys) {
          filtered[k5] = obj[k5];
        }
        return util2.objectValues(filtered);
      };
      util2.objectValues = (obj) => {
        return util2.objectKeys(obj).map(function(e14) {
          return obj[e14];
        });
      };
      util2.objectKeys = typeof Object.keys === "function" ? (obj) => Object.keys(obj) : (object) => {
        const keys2 = [];
        for (const key in object) {
          if (Object.prototype.hasOwnProperty.call(object, key)) {
            keys2.push(key);
          }
        }
        return keys2;
      };
      util2.find = (arr, checker) => {
        for (const item2 of arr) {
          if (checker(item2))
            return item2;
        }
        return void 0;
      };
      util2.isInteger = typeof Number.isInteger === "function" ? (val) => Number.isInteger(val) : (val) => typeof val === "number" && isFinite(val) && Math.floor(val) === val;
      function joinValues(array, separator = " | ") {
        return array.map((val) => typeof val === "string" ? `'${val}'` : val).join(separator);
      }
      util2.joinValues = joinValues;
      util2.jsonStringifyReplacer = (_2, value) => {
        if (typeof value === "bigint") {
          return value.toString();
        }
        return value;
      };
    })(util || (util = {}));
    (function(objectUtil2) {
      objectUtil2.mergeShapes = (first, second) => {
        return {
          ...first,
          ...second
          // second overwrites first
        };
      };
    })(objectUtil || (objectUtil = {}));
    ZodParsedType = util.arrayToEnum([
      "string",
      "nan",
      "number",
      "integer",
      "float",
      "boolean",
      "date",
      "bigint",
      "symbol",
      "function",
      "undefined",
      "null",
      "array",
      "object",
      "unknown",
      "promise",
      "void",
      "never",
      "map",
      "set"
    ]);
    getParsedType = (data) => {
      const t20 = typeof data;
      switch (t20) {
        case "undefined":
          return ZodParsedType.undefined;
        case "string":
          return ZodParsedType.string;
        case "number":
          return isNaN(data) ? ZodParsedType.nan : ZodParsedType.number;
        case "boolean":
          return ZodParsedType.boolean;
        case "function":
          return ZodParsedType.function;
        case "bigint":
          return ZodParsedType.bigint;
        case "symbol":
          return ZodParsedType.symbol;
        case "object":
          if (Array.isArray(data)) {
            return ZodParsedType.array;
          }
          if (data === null) {
            return ZodParsedType.null;
          }
          if (data.then && typeof data.then === "function" && data.catch && typeof data.catch === "function") {
            return ZodParsedType.promise;
          }
          if (typeof Map !== "undefined" && data instanceof Map) {
            return ZodParsedType.map;
          }
          if (typeof Set !== "undefined" && data instanceof Set) {
            return ZodParsedType.set;
          }
          if (typeof Date !== "undefined" && data instanceof Date) {
            return ZodParsedType.date;
          }
          return ZodParsedType.object;
        default:
          return ZodParsedType.unknown;
      }
    };
    ZodIssueCode = util.arrayToEnum([
      "invalid_type",
      "invalid_literal",
      "custom",
      "invalid_union",
      "invalid_union_discriminator",
      "invalid_enum_value",
      "unrecognized_keys",
      "invalid_arguments",
      "invalid_return_type",
      "invalid_date",
      "invalid_string",
      "too_small",
      "too_big",
      "invalid_intersection_types",
      "not_multiple_of",
      "not_finite"
    ]);
    quotelessJson = (obj) => {
      const json = JSON.stringify(obj, null, 2);
      return json.replace(/"([^"]+)":/g, "$1:");
    };
    ZodError = class _ZodError extends Error {
      constructor(issues) {
        super();
        this.issues = [];
        this.addIssue = (sub) => {
          this.issues = [...this.issues, sub];
        };
        this.addIssues = (subs = []) => {
          this.issues = [...this.issues, ...subs];
        };
        const actualProto = new.target.prototype;
        if (Object.setPrototypeOf) {
          Object.setPrototypeOf(this, actualProto);
        } else {
          this.__proto__ = actualProto;
        }
        this.name = "ZodError";
        this.issues = issues;
      }
      get errors() {
        return this.issues;
      }
      format(_mapper) {
        const mapper = _mapper || function(issue) {
          return issue.message;
        };
        const fieldErrors = { _errors: [] };
        const processError = (error) => {
          for (const issue of error.issues) {
            if (issue.code === "invalid_union") {
              issue.unionErrors.map(processError);
            } else if (issue.code === "invalid_return_type") {
              processError(issue.returnTypeError);
            } else if (issue.code === "invalid_arguments") {
              processError(issue.argumentsError);
            } else if (issue.path.length === 0) {
              fieldErrors._errors.push(mapper(issue));
            } else {
              let curr = fieldErrors;
              let i13 = 0;
              while (i13 < issue.path.length) {
                const el2 = issue.path[i13];
                const terminal = i13 === issue.path.length - 1;
                if (!terminal) {
                  curr[el2] = curr[el2] || { _errors: [] };
                } else {
                  curr[el2] = curr[el2] || { _errors: [] };
                  curr[el2]._errors.push(mapper(issue));
                }
                curr = curr[el2];
                i13++;
              }
            }
          }
        };
        processError(this);
        return fieldErrors;
      }
      static assert(value) {
        if (!(value instanceof _ZodError)) {
          throw new Error(`Not a ZodError: ${value}`);
        }
      }
      toString() {
        return this.message;
      }
      get message() {
        return JSON.stringify(this.issues, util.jsonStringifyReplacer, 2);
      }
      get isEmpty() {
        return this.issues.length === 0;
      }
      flatten(mapper = (issue) => issue.message) {
        const fieldErrors = {};
        const formErrors = [];
        for (const sub of this.issues) {
          if (sub.path.length > 0) {
            fieldErrors[sub.path[0]] = fieldErrors[sub.path[0]] || [];
            fieldErrors[sub.path[0]].push(mapper(sub));
          } else {
            formErrors.push(mapper(sub));
          }
        }
        return { formErrors, fieldErrors };
      }
      get formErrors() {
        return this.flatten();
      }
    };
    ZodError.create = (issues) => {
      const error = new ZodError(issues);
      return error;
    };
    errorMap = (issue, _ctx) => {
      let message;
      switch (issue.code) {
        case ZodIssueCode.invalid_type:
          if (issue.received === ZodParsedType.undefined) {
            message = "Required";
          } else {
            message = `Expected ${issue.expected}, received ${issue.received}`;
          }
          break;
        case ZodIssueCode.invalid_literal:
          message = `Invalid literal value, expected ${JSON.stringify(issue.expected, util.jsonStringifyReplacer)}`;
          break;
        case ZodIssueCode.unrecognized_keys:
          message = `Unrecognized key(s) in object: ${util.joinValues(issue.keys, ", ")}`;
          break;
        case ZodIssueCode.invalid_union:
          message = `Invalid input`;
          break;
        case ZodIssueCode.invalid_union_discriminator:
          message = `Invalid discriminator value. Expected ${util.joinValues(issue.options)}`;
          break;
        case ZodIssueCode.invalid_enum_value:
          message = `Invalid enum value. Expected ${util.joinValues(issue.options)}, received '${issue.received}'`;
          break;
        case ZodIssueCode.invalid_arguments:
          message = `Invalid function arguments`;
          break;
        case ZodIssueCode.invalid_return_type:
          message = `Invalid function return type`;
          break;
        case ZodIssueCode.invalid_date:
          message = `Invalid date`;
          break;
        case ZodIssueCode.invalid_string:
          if (typeof issue.validation === "object") {
            if ("includes" in issue.validation) {
              message = `Invalid input: must include "${issue.validation.includes}"`;
              if (typeof issue.validation.position === "number") {
                message = `${message} at one or more positions greater than or equal to ${issue.validation.position}`;
              }
            } else if ("startsWith" in issue.validation) {
              message = `Invalid input: must start with "${issue.validation.startsWith}"`;
            } else if ("endsWith" in issue.validation) {
              message = `Invalid input: must end with "${issue.validation.endsWith}"`;
            } else {
              util.assertNever(issue.validation);
            }
          } else if (issue.validation !== "regex") {
            message = `Invalid ${issue.validation}`;
          } else {
            message = "Invalid";
          }
          break;
        case ZodIssueCode.too_small:
          if (issue.type === "array")
            message = `Array must contain ${issue.exact ? "exactly" : issue.inclusive ? `at least` : `more than`} ${issue.minimum} element(s)`;
          else if (issue.type === "string")
            message = `String must contain ${issue.exact ? "exactly" : issue.inclusive ? `at least` : `over`} ${issue.minimum} character(s)`;
          else if (issue.type === "number")
            message = `Number must be ${issue.exact ? `exactly equal to ` : issue.inclusive ? `greater than or equal to ` : `greater than `}${issue.minimum}`;
          else if (issue.type === "date")
            message = `Date must be ${issue.exact ? `exactly equal to ` : issue.inclusive ? `greater than or equal to ` : `greater than `}${new Date(Number(issue.minimum))}`;
          else
            message = "Invalid input";
          break;
        case ZodIssueCode.too_big:
          if (issue.type === "array")
            message = `Array must contain ${issue.exact ? `exactly` : issue.inclusive ? `at most` : `less than`} ${issue.maximum} element(s)`;
          else if (issue.type === "string")
            message = `String must contain ${issue.exact ? `exactly` : issue.inclusive ? `at most` : `under`} ${issue.maximum} character(s)`;
          else if (issue.type === "number")
            message = `Number must be ${issue.exact ? `exactly` : issue.inclusive ? `less than or equal to` : `less than`} ${issue.maximum}`;
          else if (issue.type === "bigint")
            message = `BigInt must be ${issue.exact ? `exactly` : issue.inclusive ? `less than or equal to` : `less than`} ${issue.maximum}`;
          else if (issue.type === "date")
            message = `Date must be ${issue.exact ? `exactly` : issue.inclusive ? `smaller than or equal to` : `smaller than`} ${new Date(Number(issue.maximum))}`;
          else
            message = "Invalid input";
          break;
        case ZodIssueCode.custom:
          message = `Invalid input`;
          break;
        case ZodIssueCode.invalid_intersection_types:
          message = `Intersection results could not be merged`;
          break;
        case ZodIssueCode.not_multiple_of:
          message = `Number must be a multiple of ${issue.multipleOf}`;
          break;
        case ZodIssueCode.not_finite:
          message = "Number must be finite";
          break;
        default:
          message = _ctx.defaultError;
          util.assertNever(issue);
      }
      return { message };
    };
    overrideErrorMap = errorMap;
    makeIssue = (params) => {
      const { data, path, errorMaps, issueData } = params;
      const fullPath = [...path, ...issueData.path || []];
      const fullIssue = {
        ...issueData,
        path: fullPath
      };
      if (issueData.message !== void 0) {
        return {
          ...issueData,
          path: fullPath,
          message: issueData.message
        };
      }
      let errorMessage = "";
      const maps = errorMaps.filter((m12) => !!m12).slice().reverse();
      for (const map of maps) {
        errorMessage = map(fullIssue, { data, defaultError: errorMessage }).message;
      }
      return {
        ...issueData,
        path: fullPath,
        message: errorMessage
      };
    };
    EMPTY_PATH = [];
    ParseStatus = class _ParseStatus {
      constructor() {
        this.value = "valid";
      }
      dirty() {
        if (this.value === "valid")
          this.value = "dirty";
      }
      abort() {
        if (this.value !== "aborted")
          this.value = "aborted";
      }
      static mergeArray(status2, results) {
        const arrayValue = [];
        for (const s36 of results) {
          if (s36.status === "aborted")
            return INVALID;
          if (s36.status === "dirty")
            status2.dirty();
          arrayValue.push(s36.value);
        }
        return { status: status2.value, value: arrayValue };
      }
      static async mergeObjectAsync(status2, pairs) {
        const syncPairs = [];
        for (const pair of pairs) {
          const key = await pair.key;
          const value = await pair.value;
          syncPairs.push({
            key,
            value
          });
        }
        return _ParseStatus.mergeObjectSync(status2, syncPairs);
      }
      static mergeObjectSync(status2, pairs) {
        const finalObject = {};
        for (const pair of pairs) {
          const { key, value } = pair;
          if (key.status === "aborted")
            return INVALID;
          if (value.status === "aborted")
            return INVALID;
          if (key.status === "dirty")
            status2.dirty();
          if (value.status === "dirty")
            status2.dirty();
          if (key.value !== "__proto__" && (typeof value.value !== "undefined" || pair.alwaysSet)) {
            finalObject[key.value] = value.value;
          }
        }
        return { status: status2.value, value: finalObject };
      }
    };
    INVALID = Object.freeze({
      status: "aborted"
    });
    DIRTY = (value) => ({ status: "dirty", value });
    OK = (value) => ({ status: "valid", value });
    isAborted = (x3) => x3.status === "aborted";
    isDirty = (x3) => x3.status === "dirty";
    isValid = (x3) => x3.status === "valid";
    isAsync = (x3) => typeof Promise !== "undefined" && x3 instanceof Promise;
    (function(errorUtil2) {
      errorUtil2.errToObj = (message) => typeof message === "string" ? { message } : message || {};
      errorUtil2.toString = (message) => typeof message === "string" ? message : message === null || message === void 0 ? void 0 : message.message;
    })(errorUtil || (errorUtil = {}));
    ParseInputLazyPath = class {
      constructor(parent, value, path, key) {
        this._cachedPath = [];
        this.parent = parent;
        this.data = value;
        this._path = path;
        this._key = key;
      }
      get path() {
        if (!this._cachedPath.length) {
          if (this._key instanceof Array) {
            this._cachedPath.push(...this._path, ...this._key);
          } else {
            this._cachedPath.push(...this._path, this._key);
          }
        }
        return this._cachedPath;
      }
    };
    handleResult = (ctx, result) => {
      if (isValid(result)) {
        return { success: true, data: result.value };
      } else {
        if (!ctx.common.issues.length) {
          throw new Error("Validation failed but no issues detected.");
        }
        return {
          success: false,
          get error() {
            if (this._error)
              return this._error;
            const error = new ZodError(ctx.common.issues);
            this._error = error;
            return this._error;
          }
        };
      }
    };
    ZodType = class {
      constructor(def) {
        this.spa = this.safeParseAsync;
        this._def = def;
        this.parse = this.parse.bind(this);
        this.safeParse = this.safeParse.bind(this);
        this.parseAsync = this.parseAsync.bind(this);
        this.safeParseAsync = this.safeParseAsync.bind(this);
        this.spa = this.spa.bind(this);
        this.refine = this.refine.bind(this);
        this.refinement = this.refinement.bind(this);
        this.superRefine = this.superRefine.bind(this);
        this.optional = this.optional.bind(this);
        this.nullable = this.nullable.bind(this);
        this.nullish = this.nullish.bind(this);
        this.array = this.array.bind(this);
        this.promise = this.promise.bind(this);
        this.or = this.or.bind(this);
        this.and = this.and.bind(this);
        this.transform = this.transform.bind(this);
        this.brand = this.brand.bind(this);
        this.default = this.default.bind(this);
        this.catch = this.catch.bind(this);
        this.describe = this.describe.bind(this);
        this.pipe = this.pipe.bind(this);
        this.readonly = this.readonly.bind(this);
        this.isNullable = this.isNullable.bind(this);
        this.isOptional = this.isOptional.bind(this);
      }
      get description() {
        return this._def.description;
      }
      _getType(input2) {
        return getParsedType(input2.data);
      }
      _getOrReturnCtx(input2, ctx) {
        return ctx || {
          common: input2.parent.common,
          data: input2.data,
          parsedType: getParsedType(input2.data),
          schemaErrorMap: this._def.errorMap,
          path: input2.path,
          parent: input2.parent
        };
      }
      _processInputParams(input2) {
        return {
          status: new ParseStatus(),
          ctx: {
            common: input2.parent.common,
            data: input2.data,
            parsedType: getParsedType(input2.data),
            schemaErrorMap: this._def.errorMap,
            path: input2.path,
            parent: input2.parent
          }
        };
      }
      _parseSync(input2) {
        const result = this._parse(input2);
        if (isAsync(result)) {
          throw new Error("Synchronous parse encountered promise.");
        }
        return result;
      }
      _parseAsync(input2) {
        const result = this._parse(input2);
        return Promise.resolve(result);
      }
      parse(data, params) {
        const result = this.safeParse(data, params);
        if (result.success)
          return result.data;
        throw result.error;
      }
      safeParse(data, params) {
        var _a;
        const ctx = {
          common: {
            issues: [],
            async: (_a = params === null || params === void 0 ? void 0 : params.async) !== null && _a !== void 0 ? _a : false,
            contextualErrorMap: params === null || params === void 0 ? void 0 : params.errorMap
          },
          path: (params === null || params === void 0 ? void 0 : params.path) || [],
          schemaErrorMap: this._def.errorMap,
          parent: null,
          data,
          parsedType: getParsedType(data)
        };
        const result = this._parseSync({ data, path: ctx.path, parent: ctx });
        return handleResult(ctx, result);
      }
      async parseAsync(data, params) {
        const result = await this.safeParseAsync(data, params);
        if (result.success)
          return result.data;
        throw result.error;
      }
      async safeParseAsync(data, params) {
        const ctx = {
          common: {
            issues: [],
            contextualErrorMap: params === null || params === void 0 ? void 0 : params.errorMap,
            async: true
          },
          path: (params === null || params === void 0 ? void 0 : params.path) || [],
          schemaErrorMap: this._def.errorMap,
          parent: null,
          data,
          parsedType: getParsedType(data)
        };
        const maybeAsyncResult = this._parse({ data, path: ctx.path, parent: ctx });
        const result = await (isAsync(maybeAsyncResult) ? maybeAsyncResult : Promise.resolve(maybeAsyncResult));
        return handleResult(ctx, result);
      }
      refine(check2, message) {
        const getIssueProperties = (val) => {
          if (typeof message === "string" || typeof message === "undefined") {
            return { message };
          } else if (typeof message === "function") {
            return message(val);
          } else {
            return message;
          }
        };
        return this._refinement((val, ctx) => {
          const result = check2(val);
          const setError = () => ctx.addIssue({
            code: ZodIssueCode.custom,
            ...getIssueProperties(val)
          });
          if (typeof Promise !== "undefined" && result instanceof Promise) {
            return result.then((data) => {
              if (!data) {
                setError();
                return false;
              } else {
                return true;
              }
            });
          }
          if (!result) {
            setError();
            return false;
          } else {
            return true;
          }
        });
      }
      refinement(check2, refinementData) {
        return this._refinement((val, ctx) => {
          if (!check2(val)) {
            ctx.addIssue(typeof refinementData === "function" ? refinementData(val, ctx) : refinementData);
            return false;
          } else {
            return true;
          }
        });
      }
      _refinement(refinement) {
        return new ZodEffects({
          schema: this,
          typeName: ZodFirstPartyTypeKind.ZodEffects,
          effect: { type: "refinement", refinement }
        });
      }
      superRefine(refinement) {
        return this._refinement(refinement);
      }
      optional() {
        return ZodOptional.create(this, this._def);
      }
      nullable() {
        return ZodNullable.create(this, this._def);
      }
      nullish() {
        return this.nullable().optional();
      }
      array() {
        return ZodArray.create(this, this._def);
      }
      promise() {
        return ZodPromise.create(this, this._def);
      }
      or(option) {
        return ZodUnion.create([this, option], this._def);
      }
      and(incoming) {
        return ZodIntersection.create(this, incoming, this._def);
      }
      transform(transform) {
        return new ZodEffects({
          ...processCreateParams(this._def),
          schema: this,
          typeName: ZodFirstPartyTypeKind.ZodEffects,
          effect: { type: "transform", transform }
        });
      }
      default(def) {
        const defaultValueFunc = typeof def === "function" ? def : () => def;
        return new ZodDefault({
          ...processCreateParams(this._def),
          innerType: this,
          defaultValue: defaultValueFunc,
          typeName: ZodFirstPartyTypeKind.ZodDefault
        });
      }
      brand() {
        return new ZodBranded({
          typeName: ZodFirstPartyTypeKind.ZodBranded,
          type: this,
          ...processCreateParams(this._def)
        });
      }
      catch(def) {
        const catchValueFunc = typeof def === "function" ? def : () => def;
        return new ZodCatch({
          ...processCreateParams(this._def),
          innerType: this,
          catchValue: catchValueFunc,
          typeName: ZodFirstPartyTypeKind.ZodCatch
        });
      }
      describe(description) {
        const This = this.constructor;
        return new This({
          ...this._def,
          description
        });
      }
      pipe(target) {
        return ZodPipeline.create(this, target);
      }
      readonly() {
        return ZodReadonly.create(this);
      }
      isOptional() {
        return this.safeParse(void 0).success;
      }
      isNullable() {
        return this.safeParse(null).success;
      }
    };
    cuidRegex = /^c[^\s-]{8,}$/i;
    cuid2Regex = /^[0-9a-z]+$/;
    ulidRegex = /^[0-9A-HJKMNP-TV-Z]{26}$/;
    uuidRegex = /^[0-9a-fA-F]{8}\b-[0-9a-fA-F]{4}\b-[0-9a-fA-F]{4}\b-[0-9a-fA-F]{4}\b-[0-9a-fA-F]{12}$/i;
    nanoidRegex = /^[a-z0-9_-]{21}$/i;
    durationRegex = /^[-+]?P(?!$)(?:(?:[-+]?\d+Y)|(?:[-+]?\d+[.,]\d+Y$))?(?:(?:[-+]?\d+M)|(?:[-+]?\d+[.,]\d+M$))?(?:(?:[-+]?\d+W)|(?:[-+]?\d+[.,]\d+W$))?(?:(?:[-+]?\d+D)|(?:[-+]?\d+[.,]\d+D$))?(?:T(?=[\d+-])(?:(?:[-+]?\d+H)|(?:[-+]?\d+[.,]\d+H$))?(?:(?:[-+]?\d+M)|(?:[-+]?\d+[.,]\d+M$))?(?:[-+]?\d+(?:[.,]\d+)?S)?)??$/;
    emailRegex = /^(?!\.)(?!.*\.\.)([A-Z0-9_'+\-\.]*)[A-Z0-9_+-]@([A-Z0-9][A-Z0-9\-]*\.)+[A-Z]{2,}$/i;
    _emojiRegex = `^(\\p{Extended_Pictographic}|\\p{Emoji_Component})+$`;
    ipv4Regex = /^(?:(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])\.){3}(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])$/;
    ipv6Regex = /^(([a-f0-9]{1,4}:){7}|::([a-f0-9]{1,4}:){0,6}|([a-f0-9]{1,4}:){1}:([a-f0-9]{1,4}:){0,5}|([a-f0-9]{1,4}:){2}:([a-f0-9]{1,4}:){0,4}|([a-f0-9]{1,4}:){3}:([a-f0-9]{1,4}:){0,3}|([a-f0-9]{1,4}:){4}:([a-f0-9]{1,4}:){0,2}|([a-f0-9]{1,4}:){5}:([a-f0-9]{1,4}:){0,1})([a-f0-9]{1,4}|(((25[0-5])|(2[0-4][0-9])|(1[0-9]{2})|([0-9]{1,2}))\.){3}((25[0-5])|(2[0-4][0-9])|(1[0-9]{2})|([0-9]{1,2})))$/;
    base64Regex = /^([0-9a-zA-Z+/]{4})*(([0-9a-zA-Z+/]{2}==)|([0-9a-zA-Z+/]{3}=))?$/;
    dateRegexSource = `((\\d\\d[2468][048]|\\d\\d[13579][26]|\\d\\d0[48]|[02468][048]00|[13579][26]00)-02-29|\\d{4}-((0[13578]|1[02])-(0[1-9]|[12]\\d|3[01])|(0[469]|11)-(0[1-9]|[12]\\d|30)|(02)-(0[1-9]|1\\d|2[0-8])))`;
    dateRegex = new RegExp(`^${dateRegexSource}$`);
    ZodString = class _ZodString extends ZodType {
      _parse(input2) {
        if (this._def.coerce) {
          input2.data = String(input2.data);
        }
        const parsedType = this._getType(input2);
        if (parsedType !== ZodParsedType.string) {
          const ctx2 = this._getOrReturnCtx(input2);
          addIssueToContext(ctx2, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.string,
            received: ctx2.parsedType
          });
          return INVALID;
        }
        const status2 = new ParseStatus();
        let ctx = void 0;
        for (const check2 of this._def.checks) {
          if (check2.kind === "min") {
            if (input2.data.length < check2.value) {
              ctx = this._getOrReturnCtx(input2, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.too_small,
                minimum: check2.value,
                type: "string",
                inclusive: true,
                exact: false,
                message: check2.message
              });
              status2.dirty();
            }
          } else if (check2.kind === "max") {
            if (input2.data.length > check2.value) {
              ctx = this._getOrReturnCtx(input2, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.too_big,
                maximum: check2.value,
                type: "string",
                inclusive: true,
                exact: false,
                message: check2.message
              });
              status2.dirty();
            }
          } else if (check2.kind === "length") {
            const tooBig = input2.data.length > check2.value;
            const tooSmall = input2.data.length < check2.value;
            if (tooBig || tooSmall) {
              ctx = this._getOrReturnCtx(input2, ctx);
              if (tooBig) {
                addIssueToContext(ctx, {
                  code: ZodIssueCode.too_big,
                  maximum: check2.value,
                  type: "string",
                  inclusive: true,
                  exact: true,
                  message: check2.message
                });
              } else if (tooSmall) {
                addIssueToContext(ctx, {
                  code: ZodIssueCode.too_small,
                  minimum: check2.value,
                  type: "string",
                  inclusive: true,
                  exact: true,
                  message: check2.message
                });
              }
              status2.dirty();
            }
          } else if (check2.kind === "email") {
            if (!emailRegex.test(input2.data)) {
              ctx = this._getOrReturnCtx(input2, ctx);
              addIssueToContext(ctx, {
                validation: "email",
                code: ZodIssueCode.invalid_string,
                message: check2.message
              });
              status2.dirty();
            }
          } else if (check2.kind === "emoji") {
            if (!emojiRegex) {
              emojiRegex = new RegExp(_emojiRegex, "u");
            }
            if (!emojiRegex.test(input2.data)) {
              ctx = this._getOrReturnCtx(input2, ctx);
              addIssueToContext(ctx, {
                validation: "emoji",
                code: ZodIssueCode.invalid_string,
                message: check2.message
              });
              status2.dirty();
            }
          } else if (check2.kind === "uuid") {
            if (!uuidRegex.test(input2.data)) {
              ctx = this._getOrReturnCtx(input2, ctx);
              addIssueToContext(ctx, {
                validation: "uuid",
                code: ZodIssueCode.invalid_string,
                message: check2.message
              });
              status2.dirty();
            }
          } else if (check2.kind === "nanoid") {
            if (!nanoidRegex.test(input2.data)) {
              ctx = this._getOrReturnCtx(input2, ctx);
              addIssueToContext(ctx, {
                validation: "nanoid",
                code: ZodIssueCode.invalid_string,
                message: check2.message
              });
              status2.dirty();
            }
          } else if (check2.kind === "cuid") {
            if (!cuidRegex.test(input2.data)) {
              ctx = this._getOrReturnCtx(input2, ctx);
              addIssueToContext(ctx, {
                validation: "cuid",
                code: ZodIssueCode.invalid_string,
                message: check2.message
              });
              status2.dirty();
            }
          } else if (check2.kind === "cuid2") {
            if (!cuid2Regex.test(input2.data)) {
              ctx = this._getOrReturnCtx(input2, ctx);
              addIssueToContext(ctx, {
                validation: "cuid2",
                code: ZodIssueCode.invalid_string,
                message: check2.message
              });
              status2.dirty();
            }
          } else if (check2.kind === "ulid") {
            if (!ulidRegex.test(input2.data)) {
              ctx = this._getOrReturnCtx(input2, ctx);
              addIssueToContext(ctx, {
                validation: "ulid",
                code: ZodIssueCode.invalid_string,
                message: check2.message
              });
              status2.dirty();
            }
          } else if (check2.kind === "url") {
            try {
              new URL(input2.data);
            } catch (_a) {
              ctx = this._getOrReturnCtx(input2, ctx);
              addIssueToContext(ctx, {
                validation: "url",
                code: ZodIssueCode.invalid_string,
                message: check2.message
              });
              status2.dirty();
            }
          } else if (check2.kind === "regex") {
            check2.regex.lastIndex = 0;
            const testResult = check2.regex.test(input2.data);
            if (!testResult) {
              ctx = this._getOrReturnCtx(input2, ctx);
              addIssueToContext(ctx, {
                validation: "regex",
                code: ZodIssueCode.invalid_string,
                message: check2.message
              });
              status2.dirty();
            }
          } else if (check2.kind === "trim") {
            input2.data = input2.data.trim();
          } else if (check2.kind === "includes") {
            if (!input2.data.includes(check2.value, check2.position)) {
              ctx = this._getOrReturnCtx(input2, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.invalid_string,
                validation: { includes: check2.value, position: check2.position },
                message: check2.message
              });
              status2.dirty();
            }
          } else if (check2.kind === "toLowerCase") {
            input2.data = input2.data.toLowerCase();
          } else if (check2.kind === "toUpperCase") {
            input2.data = input2.data.toUpperCase();
          } else if (check2.kind === "startsWith") {
            if (!input2.data.startsWith(check2.value)) {
              ctx = this._getOrReturnCtx(input2, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.invalid_string,
                validation: { startsWith: check2.value },
                message: check2.message
              });
              status2.dirty();
            }
          } else if (check2.kind === "endsWith") {
            if (!input2.data.endsWith(check2.value)) {
              ctx = this._getOrReturnCtx(input2, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.invalid_string,
                validation: { endsWith: check2.value },
                message: check2.message
              });
              status2.dirty();
            }
          } else if (check2.kind === "datetime") {
            const regex2 = datetimeRegex(check2);
            if (!regex2.test(input2.data)) {
              ctx = this._getOrReturnCtx(input2, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.invalid_string,
                validation: "datetime",
                message: check2.message
              });
              status2.dirty();
            }
          } else if (check2.kind === "date") {
            const regex2 = dateRegex;
            if (!regex2.test(input2.data)) {
              ctx = this._getOrReturnCtx(input2, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.invalid_string,
                validation: "date",
                message: check2.message
              });
              status2.dirty();
            }
          } else if (check2.kind === "time") {
            const regex2 = timeRegex(check2);
            if (!regex2.test(input2.data)) {
              ctx = this._getOrReturnCtx(input2, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.invalid_string,
                validation: "time",
                message: check2.message
              });
              status2.dirty();
            }
          } else if (check2.kind === "duration") {
            if (!durationRegex.test(input2.data)) {
              ctx = this._getOrReturnCtx(input2, ctx);
              addIssueToContext(ctx, {
                validation: "duration",
                code: ZodIssueCode.invalid_string,
                message: check2.message
              });
              status2.dirty();
            }
          } else if (check2.kind === "ip") {
            if (!isValidIP(input2.data, check2.version)) {
              ctx = this._getOrReturnCtx(input2, ctx);
              addIssueToContext(ctx, {
                validation: "ip",
                code: ZodIssueCode.invalid_string,
                message: check2.message
              });
              status2.dirty();
            }
          } else if (check2.kind === "base64") {
            if (!base64Regex.test(input2.data)) {
              ctx = this._getOrReturnCtx(input2, ctx);
              addIssueToContext(ctx, {
                validation: "base64",
                code: ZodIssueCode.invalid_string,
                message: check2.message
              });
              status2.dirty();
            }
          } else {
            util.assertNever(check2);
          }
        }
        return { status: status2.value, value: input2.data };
      }
      _regex(regex2, validation, message) {
        return this.refinement((data) => regex2.test(data), {
          validation,
          code: ZodIssueCode.invalid_string,
          ...errorUtil.errToObj(message)
        });
      }
      _addCheck(check2) {
        return new _ZodString({
          ...this._def,
          checks: [...this._def.checks, check2]
        });
      }
      email(message) {
        return this._addCheck({ kind: "email", ...errorUtil.errToObj(message) });
      }
      url(message) {
        return this._addCheck({ kind: "url", ...errorUtil.errToObj(message) });
      }
      emoji(message) {
        return this._addCheck({ kind: "emoji", ...errorUtil.errToObj(message) });
      }
      uuid(message) {
        return this._addCheck({ kind: "uuid", ...errorUtil.errToObj(message) });
      }
      nanoid(message) {
        return this._addCheck({ kind: "nanoid", ...errorUtil.errToObj(message) });
      }
      cuid(message) {
        return this._addCheck({ kind: "cuid", ...errorUtil.errToObj(message) });
      }
      cuid2(message) {
        return this._addCheck({ kind: "cuid2", ...errorUtil.errToObj(message) });
      }
      ulid(message) {
        return this._addCheck({ kind: "ulid", ...errorUtil.errToObj(message) });
      }
      base64(message) {
        return this._addCheck({ kind: "base64", ...errorUtil.errToObj(message) });
      }
      ip(options) {
        return this._addCheck({ kind: "ip", ...errorUtil.errToObj(options) });
      }
      datetime(options) {
        var _a, _b;
        if (typeof options === "string") {
          return this._addCheck({
            kind: "datetime",
            precision: null,
            offset: false,
            local: false,
            message: options
          });
        }
        return this._addCheck({
          kind: "datetime",
          precision: typeof (options === null || options === void 0 ? void 0 : options.precision) === "undefined" ? null : options === null || options === void 0 ? void 0 : options.precision,
          offset: (_a = options === null || options === void 0 ? void 0 : options.offset) !== null && _a !== void 0 ? _a : false,
          local: (_b = options === null || options === void 0 ? void 0 : options.local) !== null && _b !== void 0 ? _b : false,
          ...errorUtil.errToObj(options === null || options === void 0 ? void 0 : options.message)
        });
      }
      date(message) {
        return this._addCheck({ kind: "date", message });
      }
      time(options) {
        if (typeof options === "string") {
          return this._addCheck({
            kind: "time",
            precision: null,
            message: options
          });
        }
        return this._addCheck({
          kind: "time",
          precision: typeof (options === null || options === void 0 ? void 0 : options.precision) === "undefined" ? null : options === null || options === void 0 ? void 0 : options.precision,
          ...errorUtil.errToObj(options === null || options === void 0 ? void 0 : options.message)
        });
      }
      duration(message) {
        return this._addCheck({ kind: "duration", ...errorUtil.errToObj(message) });
      }
      regex(regex2, message) {
        return this._addCheck({
          kind: "regex",
          regex: regex2,
          ...errorUtil.errToObj(message)
        });
      }
      includes(value, options) {
        return this._addCheck({
          kind: "includes",
          value,
          position: options === null || options === void 0 ? void 0 : options.position,
          ...errorUtil.errToObj(options === null || options === void 0 ? void 0 : options.message)
        });
      }
      startsWith(value, message) {
        return this._addCheck({
          kind: "startsWith",
          value,
          ...errorUtil.errToObj(message)
        });
      }
      endsWith(value, message) {
        return this._addCheck({
          kind: "endsWith",
          value,
          ...errorUtil.errToObj(message)
        });
      }
      min(minLength, message) {
        return this._addCheck({
          kind: "min",
          value: minLength,
          ...errorUtil.errToObj(message)
        });
      }
      max(maxLength, message) {
        return this._addCheck({
          kind: "max",
          value: maxLength,
          ...errorUtil.errToObj(message)
        });
      }
      length(len, message) {
        return this._addCheck({
          kind: "length",
          value: len,
          ...errorUtil.errToObj(message)
        });
      }
      /**
       * @deprecated Use z.string().min(1) instead.
       * @see {@link ZodString.min}
       */
      nonempty(message) {
        return this.min(1, errorUtil.errToObj(message));
      }
      trim() {
        return new _ZodString({
          ...this._def,
          checks: [...this._def.checks, { kind: "trim" }]
        });
      }
      toLowerCase() {
        return new _ZodString({
          ...this._def,
          checks: [...this._def.checks, { kind: "toLowerCase" }]
        });
      }
      toUpperCase() {
        return new _ZodString({
          ...this._def,
          checks: [...this._def.checks, { kind: "toUpperCase" }]
        });
      }
      get isDatetime() {
        return !!this._def.checks.find((ch) => ch.kind === "datetime");
      }
      get isDate() {
        return !!this._def.checks.find((ch) => ch.kind === "date");
      }
      get isTime() {
        return !!this._def.checks.find((ch) => ch.kind === "time");
      }
      get isDuration() {
        return !!this._def.checks.find((ch) => ch.kind === "duration");
      }
      get isEmail() {
        return !!this._def.checks.find((ch) => ch.kind === "email");
      }
      get isURL() {
        return !!this._def.checks.find((ch) => ch.kind === "url");
      }
      get isEmoji() {
        return !!this._def.checks.find((ch) => ch.kind === "emoji");
      }
      get isUUID() {
        return !!this._def.checks.find((ch) => ch.kind === "uuid");
      }
      get isNANOID() {
        return !!this._def.checks.find((ch) => ch.kind === "nanoid");
      }
      get isCUID() {
        return !!this._def.checks.find((ch) => ch.kind === "cuid");
      }
      get isCUID2() {
        return !!this._def.checks.find((ch) => ch.kind === "cuid2");
      }
      get isULID() {
        return !!this._def.checks.find((ch) => ch.kind === "ulid");
      }
      get isIP() {
        return !!this._def.checks.find((ch) => ch.kind === "ip");
      }
      get isBase64() {
        return !!this._def.checks.find((ch) => ch.kind === "base64");
      }
      get minLength() {
        let min2 = null;
        for (const ch of this._def.checks) {
          if (ch.kind === "min") {
            if (min2 === null || ch.value > min2)
              min2 = ch.value;
          }
        }
        return min2;
      }
      get maxLength() {
        let max2 = null;
        for (const ch of this._def.checks) {
          if (ch.kind === "max") {
            if (max2 === null || ch.value < max2)
              max2 = ch.value;
          }
        }
        return max2;
      }
    };
    ZodString.create = (params) => {
      var _a;
      return new ZodString({
        checks: [],
        typeName: ZodFirstPartyTypeKind.ZodString,
        coerce: (_a = params === null || params === void 0 ? void 0 : params.coerce) !== null && _a !== void 0 ? _a : false,
        ...processCreateParams(params)
      });
    };
    ZodNumber = class _ZodNumber extends ZodType {
      constructor() {
        super(...arguments);
        this.min = this.gte;
        this.max = this.lte;
        this.step = this.multipleOf;
      }
      _parse(input2) {
        if (this._def.coerce) {
          input2.data = Number(input2.data);
        }
        const parsedType = this._getType(input2);
        if (parsedType !== ZodParsedType.number) {
          const ctx2 = this._getOrReturnCtx(input2);
          addIssueToContext(ctx2, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.number,
            received: ctx2.parsedType
          });
          return INVALID;
        }
        let ctx = void 0;
        const status2 = new ParseStatus();
        for (const check2 of this._def.checks) {
          if (check2.kind === "int") {
            if (!util.isInteger(input2.data)) {
              ctx = this._getOrReturnCtx(input2, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.invalid_type,
                expected: "integer",
                received: "float",
                message: check2.message
              });
              status2.dirty();
            }
          } else if (check2.kind === "min") {
            const tooSmall = check2.inclusive ? input2.data < check2.value : input2.data <= check2.value;
            if (tooSmall) {
              ctx = this._getOrReturnCtx(input2, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.too_small,
                minimum: check2.value,
                type: "number",
                inclusive: check2.inclusive,
                exact: false,
                message: check2.message
              });
              status2.dirty();
            }
          } else if (check2.kind === "max") {
            const tooBig = check2.inclusive ? input2.data > check2.value : input2.data >= check2.value;
            if (tooBig) {
              ctx = this._getOrReturnCtx(input2, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.too_big,
                maximum: check2.value,
                type: "number",
                inclusive: check2.inclusive,
                exact: false,
                message: check2.message
              });
              status2.dirty();
            }
          } else if (check2.kind === "multipleOf") {
            if (floatSafeRemainder(input2.data, check2.value) !== 0) {
              ctx = this._getOrReturnCtx(input2, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.not_multiple_of,
                multipleOf: check2.value,
                message: check2.message
              });
              status2.dirty();
            }
          } else if (check2.kind === "finite") {
            if (!Number.isFinite(input2.data)) {
              ctx = this._getOrReturnCtx(input2, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.not_finite,
                message: check2.message
              });
              status2.dirty();
            }
          } else {
            util.assertNever(check2);
          }
        }
        return { status: status2.value, value: input2.data };
      }
      gte(value, message) {
        return this.setLimit("min", value, true, errorUtil.toString(message));
      }
      gt(value, message) {
        return this.setLimit("min", value, false, errorUtil.toString(message));
      }
      lte(value, message) {
        return this.setLimit("max", value, true, errorUtil.toString(message));
      }
      lt(value, message) {
        return this.setLimit("max", value, false, errorUtil.toString(message));
      }
      setLimit(kind, value, inclusive, message) {
        return new _ZodNumber({
          ...this._def,
          checks: [
            ...this._def.checks,
            {
              kind,
              value,
              inclusive,
              message: errorUtil.toString(message)
            }
          ]
        });
      }
      _addCheck(check2) {
        return new _ZodNumber({
          ...this._def,
          checks: [...this._def.checks, check2]
        });
      }
      int(message) {
        return this._addCheck({
          kind: "int",
          message: errorUtil.toString(message)
        });
      }
      positive(message) {
        return this._addCheck({
          kind: "min",
          value: 0,
          inclusive: false,
          message: errorUtil.toString(message)
        });
      }
      negative(message) {
        return this._addCheck({
          kind: "max",
          value: 0,
          inclusive: false,
          message: errorUtil.toString(message)
        });
      }
      nonpositive(message) {
        return this._addCheck({
          kind: "max",
          value: 0,
          inclusive: true,
          message: errorUtil.toString(message)
        });
      }
      nonnegative(message) {
        return this._addCheck({
          kind: "min",
          value: 0,
          inclusive: true,
          message: errorUtil.toString(message)
        });
      }
      multipleOf(value, message) {
        return this._addCheck({
          kind: "multipleOf",
          value,
          message: errorUtil.toString(message)
        });
      }
      finite(message) {
        return this._addCheck({
          kind: "finite",
          message: errorUtil.toString(message)
        });
      }
      safe(message) {
        return this._addCheck({
          kind: "min",
          inclusive: true,
          value: Number.MIN_SAFE_INTEGER,
          message: errorUtil.toString(message)
        })._addCheck({
          kind: "max",
          inclusive: true,
          value: Number.MAX_SAFE_INTEGER,
          message: errorUtil.toString(message)
        });
      }
      get minValue() {
        let min2 = null;
        for (const ch of this._def.checks) {
          if (ch.kind === "min") {
            if (min2 === null || ch.value > min2)
              min2 = ch.value;
          }
        }
        return min2;
      }
      get maxValue() {
        let max2 = null;
        for (const ch of this._def.checks) {
          if (ch.kind === "max") {
            if (max2 === null || ch.value < max2)
              max2 = ch.value;
          }
        }
        return max2;
      }
      get isInt() {
        return !!this._def.checks.find((ch) => ch.kind === "int" || ch.kind === "multipleOf" && util.isInteger(ch.value));
      }
      get isFinite() {
        let max2 = null, min2 = null;
        for (const ch of this._def.checks) {
          if (ch.kind === "finite" || ch.kind === "int" || ch.kind === "multipleOf") {
            return true;
          } else if (ch.kind === "min") {
            if (min2 === null || ch.value > min2)
              min2 = ch.value;
          } else if (ch.kind === "max") {
            if (max2 === null || ch.value < max2)
              max2 = ch.value;
          }
        }
        return Number.isFinite(min2) && Number.isFinite(max2);
      }
    };
    ZodNumber.create = (params) => {
      return new ZodNumber({
        checks: [],
        typeName: ZodFirstPartyTypeKind.ZodNumber,
        coerce: (params === null || params === void 0 ? void 0 : params.coerce) || false,
        ...processCreateParams(params)
      });
    };
    ZodBigInt = class _ZodBigInt extends ZodType {
      constructor() {
        super(...arguments);
        this.min = this.gte;
        this.max = this.lte;
      }
      _parse(input2) {
        if (this._def.coerce) {
          input2.data = BigInt(input2.data);
        }
        const parsedType = this._getType(input2);
        if (parsedType !== ZodParsedType.bigint) {
          const ctx2 = this._getOrReturnCtx(input2);
          addIssueToContext(ctx2, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.bigint,
            received: ctx2.parsedType
          });
          return INVALID;
        }
        let ctx = void 0;
        const status2 = new ParseStatus();
        for (const check2 of this._def.checks) {
          if (check2.kind === "min") {
            const tooSmall = check2.inclusive ? input2.data < check2.value : input2.data <= check2.value;
            if (tooSmall) {
              ctx = this._getOrReturnCtx(input2, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.too_small,
                type: "bigint",
                minimum: check2.value,
                inclusive: check2.inclusive,
                message: check2.message
              });
              status2.dirty();
            }
          } else if (check2.kind === "max") {
            const tooBig = check2.inclusive ? input2.data > check2.value : input2.data >= check2.value;
            if (tooBig) {
              ctx = this._getOrReturnCtx(input2, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.too_big,
                type: "bigint",
                maximum: check2.value,
                inclusive: check2.inclusive,
                message: check2.message
              });
              status2.dirty();
            }
          } else if (check2.kind === "multipleOf") {
            if (input2.data % check2.value !== BigInt(0)) {
              ctx = this._getOrReturnCtx(input2, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.not_multiple_of,
                multipleOf: check2.value,
                message: check2.message
              });
              status2.dirty();
            }
          } else {
            util.assertNever(check2);
          }
        }
        return { status: status2.value, value: input2.data };
      }
      gte(value, message) {
        return this.setLimit("min", value, true, errorUtil.toString(message));
      }
      gt(value, message) {
        return this.setLimit("min", value, false, errorUtil.toString(message));
      }
      lte(value, message) {
        return this.setLimit("max", value, true, errorUtil.toString(message));
      }
      lt(value, message) {
        return this.setLimit("max", value, false, errorUtil.toString(message));
      }
      setLimit(kind, value, inclusive, message) {
        return new _ZodBigInt({
          ...this._def,
          checks: [
            ...this._def.checks,
            {
              kind,
              value,
              inclusive,
              message: errorUtil.toString(message)
            }
          ]
        });
      }
      _addCheck(check2) {
        return new _ZodBigInt({
          ...this._def,
          checks: [...this._def.checks, check2]
        });
      }
      positive(message) {
        return this._addCheck({
          kind: "min",
          value: BigInt(0),
          inclusive: false,
          message: errorUtil.toString(message)
        });
      }
      negative(message) {
        return this._addCheck({
          kind: "max",
          value: BigInt(0),
          inclusive: false,
          message: errorUtil.toString(message)
        });
      }
      nonpositive(message) {
        return this._addCheck({
          kind: "max",
          value: BigInt(0),
          inclusive: true,
          message: errorUtil.toString(message)
        });
      }
      nonnegative(message) {
        return this._addCheck({
          kind: "min",
          value: BigInt(0),
          inclusive: true,
          message: errorUtil.toString(message)
        });
      }
      multipleOf(value, message) {
        return this._addCheck({
          kind: "multipleOf",
          value,
          message: errorUtil.toString(message)
        });
      }
      get minValue() {
        let min2 = null;
        for (const ch of this._def.checks) {
          if (ch.kind === "min") {
            if (min2 === null || ch.value > min2)
              min2 = ch.value;
          }
        }
        return min2;
      }
      get maxValue() {
        let max2 = null;
        for (const ch of this._def.checks) {
          if (ch.kind === "max") {
            if (max2 === null || ch.value < max2)
              max2 = ch.value;
          }
        }
        return max2;
      }
    };
    ZodBigInt.create = (params) => {
      var _a;
      return new ZodBigInt({
        checks: [],
        typeName: ZodFirstPartyTypeKind.ZodBigInt,
        coerce: (_a = params === null || params === void 0 ? void 0 : params.coerce) !== null && _a !== void 0 ? _a : false,
        ...processCreateParams(params)
      });
    };
    ZodBoolean = class extends ZodType {
      _parse(input2) {
        if (this._def.coerce) {
          input2.data = Boolean(input2.data);
        }
        const parsedType = this._getType(input2);
        if (parsedType !== ZodParsedType.boolean) {
          const ctx = this._getOrReturnCtx(input2);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.boolean,
            received: ctx.parsedType
          });
          return INVALID;
        }
        return OK(input2.data);
      }
    };
    ZodBoolean.create = (params) => {
      return new ZodBoolean({
        typeName: ZodFirstPartyTypeKind.ZodBoolean,
        coerce: (params === null || params === void 0 ? void 0 : params.coerce) || false,
        ...processCreateParams(params)
      });
    };
    ZodDate = class _ZodDate extends ZodType {
      _parse(input2) {
        if (this._def.coerce) {
          input2.data = new Date(input2.data);
        }
        const parsedType = this._getType(input2);
        if (parsedType !== ZodParsedType.date) {
          const ctx2 = this._getOrReturnCtx(input2);
          addIssueToContext(ctx2, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.date,
            received: ctx2.parsedType
          });
          return INVALID;
        }
        if (isNaN(input2.data.getTime())) {
          const ctx2 = this._getOrReturnCtx(input2);
          addIssueToContext(ctx2, {
            code: ZodIssueCode.invalid_date
          });
          return INVALID;
        }
        const status2 = new ParseStatus();
        let ctx = void 0;
        for (const check2 of this._def.checks) {
          if (check2.kind === "min") {
            if (input2.data.getTime() < check2.value) {
              ctx = this._getOrReturnCtx(input2, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.too_small,
                message: check2.message,
                inclusive: true,
                exact: false,
                minimum: check2.value,
                type: "date"
              });
              status2.dirty();
            }
          } else if (check2.kind === "max") {
            if (input2.data.getTime() > check2.value) {
              ctx = this._getOrReturnCtx(input2, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.too_big,
                message: check2.message,
                inclusive: true,
                exact: false,
                maximum: check2.value,
                type: "date"
              });
              status2.dirty();
            }
          } else {
            util.assertNever(check2);
          }
        }
        return {
          status: status2.value,
          value: new Date(input2.data.getTime())
        };
      }
      _addCheck(check2) {
        return new _ZodDate({
          ...this._def,
          checks: [...this._def.checks, check2]
        });
      }
      min(minDate, message) {
        return this._addCheck({
          kind: "min",
          value: minDate.getTime(),
          message: errorUtil.toString(message)
        });
      }
      max(maxDate, message) {
        return this._addCheck({
          kind: "max",
          value: maxDate.getTime(),
          message: errorUtil.toString(message)
        });
      }
      get minDate() {
        let min2 = null;
        for (const ch of this._def.checks) {
          if (ch.kind === "min") {
            if (min2 === null || ch.value > min2)
              min2 = ch.value;
          }
        }
        return min2 != null ? new Date(min2) : null;
      }
      get maxDate() {
        let max2 = null;
        for (const ch of this._def.checks) {
          if (ch.kind === "max") {
            if (max2 === null || ch.value < max2)
              max2 = ch.value;
          }
        }
        return max2 != null ? new Date(max2) : null;
      }
    };
    ZodDate.create = (params) => {
      return new ZodDate({
        checks: [],
        coerce: (params === null || params === void 0 ? void 0 : params.coerce) || false,
        typeName: ZodFirstPartyTypeKind.ZodDate,
        ...processCreateParams(params)
      });
    };
    ZodSymbol = class extends ZodType {
      _parse(input2) {
        const parsedType = this._getType(input2);
        if (parsedType !== ZodParsedType.symbol) {
          const ctx = this._getOrReturnCtx(input2);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.symbol,
            received: ctx.parsedType
          });
          return INVALID;
        }
        return OK(input2.data);
      }
    };
    ZodSymbol.create = (params) => {
      return new ZodSymbol({
        typeName: ZodFirstPartyTypeKind.ZodSymbol,
        ...processCreateParams(params)
      });
    };
    ZodUndefined = class extends ZodType {
      _parse(input2) {
        const parsedType = this._getType(input2);
        if (parsedType !== ZodParsedType.undefined) {
          const ctx = this._getOrReturnCtx(input2);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.undefined,
            received: ctx.parsedType
          });
          return INVALID;
        }
        return OK(input2.data);
      }
    };
    ZodUndefined.create = (params) => {
      return new ZodUndefined({
        typeName: ZodFirstPartyTypeKind.ZodUndefined,
        ...processCreateParams(params)
      });
    };
    ZodNull = class extends ZodType {
      _parse(input2) {
        const parsedType = this._getType(input2);
        if (parsedType !== ZodParsedType.null) {
          const ctx = this._getOrReturnCtx(input2);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.null,
            received: ctx.parsedType
          });
          return INVALID;
        }
        return OK(input2.data);
      }
    };
    ZodNull.create = (params) => {
      return new ZodNull({
        typeName: ZodFirstPartyTypeKind.ZodNull,
        ...processCreateParams(params)
      });
    };
    ZodAny = class extends ZodType {
      constructor() {
        super(...arguments);
        this._any = true;
      }
      _parse(input2) {
        return OK(input2.data);
      }
    };
    ZodAny.create = (params) => {
      return new ZodAny({
        typeName: ZodFirstPartyTypeKind.ZodAny,
        ...processCreateParams(params)
      });
    };
    ZodUnknown = class extends ZodType {
      constructor() {
        super(...arguments);
        this._unknown = true;
      }
      _parse(input2) {
        return OK(input2.data);
      }
    };
    ZodUnknown.create = (params) => {
      return new ZodUnknown({
        typeName: ZodFirstPartyTypeKind.ZodUnknown,
        ...processCreateParams(params)
      });
    };
    ZodNever = class extends ZodType {
      _parse(input2) {
        const ctx = this._getOrReturnCtx(input2);
        addIssueToContext(ctx, {
          code: ZodIssueCode.invalid_type,
          expected: ZodParsedType.never,
          received: ctx.parsedType
        });
        return INVALID;
      }
    };
    ZodNever.create = (params) => {
      return new ZodNever({
        typeName: ZodFirstPartyTypeKind.ZodNever,
        ...processCreateParams(params)
      });
    };
    ZodVoid = class extends ZodType {
      _parse(input2) {
        const parsedType = this._getType(input2);
        if (parsedType !== ZodParsedType.undefined) {
          const ctx = this._getOrReturnCtx(input2);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.void,
            received: ctx.parsedType
          });
          return INVALID;
        }
        return OK(input2.data);
      }
    };
    ZodVoid.create = (params) => {
      return new ZodVoid({
        typeName: ZodFirstPartyTypeKind.ZodVoid,
        ...processCreateParams(params)
      });
    };
    ZodArray = class _ZodArray extends ZodType {
      _parse(input2) {
        const { ctx, status: status2 } = this._processInputParams(input2);
        const def = this._def;
        if (ctx.parsedType !== ZodParsedType.array) {
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.array,
            received: ctx.parsedType
          });
          return INVALID;
        }
        if (def.exactLength !== null) {
          const tooBig = ctx.data.length > def.exactLength.value;
          const tooSmall = ctx.data.length < def.exactLength.value;
          if (tooBig || tooSmall) {
            addIssueToContext(ctx, {
              code: tooBig ? ZodIssueCode.too_big : ZodIssueCode.too_small,
              minimum: tooSmall ? def.exactLength.value : void 0,
              maximum: tooBig ? def.exactLength.value : void 0,
              type: "array",
              inclusive: true,
              exact: true,
              message: def.exactLength.message
            });
            status2.dirty();
          }
        }
        if (def.minLength !== null) {
          if (ctx.data.length < def.minLength.value) {
            addIssueToContext(ctx, {
              code: ZodIssueCode.too_small,
              minimum: def.minLength.value,
              type: "array",
              inclusive: true,
              exact: false,
              message: def.minLength.message
            });
            status2.dirty();
          }
        }
        if (def.maxLength !== null) {
          if (ctx.data.length > def.maxLength.value) {
            addIssueToContext(ctx, {
              code: ZodIssueCode.too_big,
              maximum: def.maxLength.value,
              type: "array",
              inclusive: true,
              exact: false,
              message: def.maxLength.message
            });
            status2.dirty();
          }
        }
        if (ctx.common.async) {
          return Promise.all([...ctx.data].map((item2, i13) => {
            return def.type._parseAsync(new ParseInputLazyPath(ctx, item2, ctx.path, i13));
          })).then((result2) => {
            return ParseStatus.mergeArray(status2, result2);
          });
        }
        const result = [...ctx.data].map((item2, i13) => {
          return def.type._parseSync(new ParseInputLazyPath(ctx, item2, ctx.path, i13));
        });
        return ParseStatus.mergeArray(status2, result);
      }
      get element() {
        return this._def.type;
      }
      min(minLength, message) {
        return new _ZodArray({
          ...this._def,
          minLength: { value: minLength, message: errorUtil.toString(message) }
        });
      }
      max(maxLength, message) {
        return new _ZodArray({
          ...this._def,
          maxLength: { value: maxLength, message: errorUtil.toString(message) }
        });
      }
      length(len, message) {
        return new _ZodArray({
          ...this._def,
          exactLength: { value: len, message: errorUtil.toString(message) }
        });
      }
      nonempty(message) {
        return this.min(1, message);
      }
    };
    ZodArray.create = (schema, params) => {
      return new ZodArray({
        type: schema,
        minLength: null,
        maxLength: null,
        exactLength: null,
        typeName: ZodFirstPartyTypeKind.ZodArray,
        ...processCreateParams(params)
      });
    };
    ZodObject = class _ZodObject extends ZodType {
      constructor() {
        super(...arguments);
        this._cached = null;
        this.nonstrict = this.passthrough;
        this.augment = this.extend;
      }
      _getCached() {
        if (this._cached !== null)
          return this._cached;
        const shape = this._def.shape();
        const keys2 = util.objectKeys(shape);
        return this._cached = { shape, keys: keys2 };
      }
      _parse(input2) {
        const parsedType = this._getType(input2);
        if (parsedType !== ZodParsedType.object) {
          const ctx2 = this._getOrReturnCtx(input2);
          addIssueToContext(ctx2, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.object,
            received: ctx2.parsedType
          });
          return INVALID;
        }
        const { status: status2, ctx } = this._processInputParams(input2);
        const { shape, keys: shapeKeys } = this._getCached();
        const extraKeys = [];
        if (!(this._def.catchall instanceof ZodNever && this._def.unknownKeys === "strip")) {
          for (const key in ctx.data) {
            if (!shapeKeys.includes(key)) {
              extraKeys.push(key);
            }
          }
        }
        const pairs = [];
        for (const key of shapeKeys) {
          const keyValidator = shape[key];
          const value = ctx.data[key];
          pairs.push({
            key: { status: "valid", value: key },
            value: keyValidator._parse(new ParseInputLazyPath(ctx, value, ctx.path, key)),
            alwaysSet: key in ctx.data
          });
        }
        if (this._def.catchall instanceof ZodNever) {
          const unknownKeys = this._def.unknownKeys;
          if (unknownKeys === "passthrough") {
            for (const key of extraKeys) {
              pairs.push({
                key: { status: "valid", value: key },
                value: { status: "valid", value: ctx.data[key] }
              });
            }
          } else if (unknownKeys === "strict") {
            if (extraKeys.length > 0) {
              addIssueToContext(ctx, {
                code: ZodIssueCode.unrecognized_keys,
                keys: extraKeys
              });
              status2.dirty();
            }
          } else if (unknownKeys === "strip") ;
          else {
            throw new Error(`Internal ZodObject error: invalid unknownKeys value.`);
          }
        } else {
          const catchall = this._def.catchall;
          for (const key of extraKeys) {
            const value = ctx.data[key];
            pairs.push({
              key: { status: "valid", value: key },
              value: catchall._parse(
                new ParseInputLazyPath(ctx, value, ctx.path, key)
                //, ctx.child(key), value, getParsedType(value)
              ),
              alwaysSet: key in ctx.data
            });
          }
        }
        if (ctx.common.async) {
          return Promise.resolve().then(async () => {
            const syncPairs = [];
            for (const pair of pairs) {
              const key = await pair.key;
              const value = await pair.value;
              syncPairs.push({
                key,
                value,
                alwaysSet: pair.alwaysSet
              });
            }
            return syncPairs;
          }).then((syncPairs) => {
            return ParseStatus.mergeObjectSync(status2, syncPairs);
          });
        } else {
          return ParseStatus.mergeObjectSync(status2, pairs);
        }
      }
      get shape() {
        return this._def.shape();
      }
      strict(message) {
        errorUtil.errToObj;
        return new _ZodObject({
          ...this._def,
          unknownKeys: "strict",
          ...message !== void 0 ? {
            errorMap: (issue, ctx) => {
              var _a, _b, _c, _d;
              const defaultError = (_c = (_b = (_a = this._def).errorMap) === null || _b === void 0 ? void 0 : _b.call(_a, issue, ctx).message) !== null && _c !== void 0 ? _c : ctx.defaultError;
              if (issue.code === "unrecognized_keys")
                return {
                  message: (_d = errorUtil.errToObj(message).message) !== null && _d !== void 0 ? _d : defaultError
                };
              return {
                message: defaultError
              };
            }
          } : {}
        });
      }
      strip() {
        return new _ZodObject({
          ...this._def,
          unknownKeys: "strip"
        });
      }
      passthrough() {
        return new _ZodObject({
          ...this._def,
          unknownKeys: "passthrough"
        });
      }
      // const AugmentFactory =
      //   <Def extends ZodObjectDef>(def: Def) =>
      //   <Augmentation extends ZodRawShape>(
      //     augmentation: Augmentation
      //   ): ZodObject<
      //     extendShape<ReturnType<Def["shape"]>, Augmentation>,
      //     Def["unknownKeys"],
      //     Def["catchall"]
      //   > => {
      //     return new ZodObject({
      //       ...def,
      //       shape: () => ({
      //         ...def.shape(),
      //         ...augmentation,
      //       }),
      //     }) as any;
      //   };
      extend(augmentation) {
        return new _ZodObject({
          ...this._def,
          shape: () => ({
            ...this._def.shape(),
            ...augmentation
          })
        });
      }
      /**
       * Prior to zod@1.0.12 there was a bug in the
       * inferred type of merged objects. Please
       * upgrade if you are experiencing issues.
       */
      merge(merging) {
        const merged = new _ZodObject({
          unknownKeys: merging._def.unknownKeys,
          catchall: merging._def.catchall,
          shape: () => ({
            ...this._def.shape(),
            ...merging._def.shape()
          }),
          typeName: ZodFirstPartyTypeKind.ZodObject
        });
        return merged;
      }
      // merge<
      //   Incoming extends AnyZodObject,
      //   Augmentation extends Incoming["shape"],
      //   NewOutput extends {
      //     [k in keyof Augmentation | keyof Output]: k extends keyof Augmentation
      //       ? Augmentation[k]["_output"]
      //       : k extends keyof Output
      //       ? Output[k]
      //       : never;
      //   },
      //   NewInput extends {
      //     [k in keyof Augmentation | keyof Input]: k extends keyof Augmentation
      //       ? Augmentation[k]["_input"]
      //       : k extends keyof Input
      //       ? Input[k]
      //       : never;
      //   }
      // >(
      //   merging: Incoming
      // ): ZodObject<
      //   extendShape<T, ReturnType<Incoming["_def"]["shape"]>>,
      //   Incoming["_def"]["unknownKeys"],
      //   Incoming["_def"]["catchall"],
      //   NewOutput,
      //   NewInput
      // > {
      //   const merged: any = new ZodObject({
      //     unknownKeys: merging._def.unknownKeys,
      //     catchall: merging._def.catchall,
      //     shape: () =>
      //       objectUtil.mergeShapes(this._def.shape(), merging._def.shape()),
      //     typeName: ZodFirstPartyTypeKind.ZodObject,
      //   }) as any;
      //   return merged;
      // }
      setKey(key, schema) {
        return this.augment({ [key]: schema });
      }
      // merge<Incoming extends AnyZodObject>(
      //   merging: Incoming
      // ): //ZodObject<T & Incoming["_shape"], UnknownKeys, Catchall> = (merging) => {
      // ZodObject<
      //   extendShape<T, ReturnType<Incoming["_def"]["shape"]>>,
      //   Incoming["_def"]["unknownKeys"],
      //   Incoming["_def"]["catchall"]
      // > {
      //   // const mergedShape = objectUtil.mergeShapes(
      //   //   this._def.shape(),
      //   //   merging._def.shape()
      //   // );
      //   const merged: any = new ZodObject({
      //     unknownKeys: merging._def.unknownKeys,
      //     catchall: merging._def.catchall,
      //     shape: () =>
      //       objectUtil.mergeShapes(this._def.shape(), merging._def.shape()),
      //     typeName: ZodFirstPartyTypeKind.ZodObject,
      //   }) as any;
      //   return merged;
      // }
      catchall(index3) {
        return new _ZodObject({
          ...this._def,
          catchall: index3
        });
      }
      pick(mask) {
        const shape = {};
        util.objectKeys(mask).forEach((key) => {
          if (mask[key] && this.shape[key]) {
            shape[key] = this.shape[key];
          }
        });
        return new _ZodObject({
          ...this._def,
          shape: () => shape
        });
      }
      omit(mask) {
        const shape = {};
        util.objectKeys(this.shape).forEach((key) => {
          if (!mask[key]) {
            shape[key] = this.shape[key];
          }
        });
        return new _ZodObject({
          ...this._def,
          shape: () => shape
        });
      }
      /**
       * @deprecated
       */
      deepPartial() {
        return deepPartialify(this);
      }
      partial(mask) {
        const newShape = {};
        util.objectKeys(this.shape).forEach((key) => {
          const fieldSchema = this.shape[key];
          if (mask && !mask[key]) {
            newShape[key] = fieldSchema;
          } else {
            newShape[key] = fieldSchema.optional();
          }
        });
        return new _ZodObject({
          ...this._def,
          shape: () => newShape
        });
      }
      required(mask) {
        const newShape = {};
        util.objectKeys(this.shape).forEach((key) => {
          if (mask && !mask[key]) {
            newShape[key] = this.shape[key];
          } else {
            const fieldSchema = this.shape[key];
            let newField = fieldSchema;
            while (newField instanceof ZodOptional) {
              newField = newField._def.innerType;
            }
            newShape[key] = newField;
          }
        });
        return new _ZodObject({
          ...this._def,
          shape: () => newShape
        });
      }
      keyof() {
        return createZodEnum(util.objectKeys(this.shape));
      }
    };
    ZodObject.create = (shape, params) => {
      return new ZodObject({
        shape: () => shape,
        unknownKeys: "strip",
        catchall: ZodNever.create(),
        typeName: ZodFirstPartyTypeKind.ZodObject,
        ...processCreateParams(params)
      });
    };
    ZodObject.strictCreate = (shape, params) => {
      return new ZodObject({
        shape: () => shape,
        unknownKeys: "strict",
        catchall: ZodNever.create(),
        typeName: ZodFirstPartyTypeKind.ZodObject,
        ...processCreateParams(params)
      });
    };
    ZodObject.lazycreate = (shape, params) => {
      return new ZodObject({
        shape,
        unknownKeys: "strip",
        catchall: ZodNever.create(),
        typeName: ZodFirstPartyTypeKind.ZodObject,
        ...processCreateParams(params)
      });
    };
    ZodUnion = class extends ZodType {
      _parse(input2) {
        const { ctx } = this._processInputParams(input2);
        const options = this._def.options;
        function handleResults(results) {
          for (const result of results) {
            if (result.result.status === "valid") {
              return result.result;
            }
          }
          for (const result of results) {
            if (result.result.status === "dirty") {
              ctx.common.issues.push(...result.ctx.common.issues);
              return result.result;
            }
          }
          const unionErrors = results.map((result) => new ZodError(result.ctx.common.issues));
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_union,
            unionErrors
          });
          return INVALID;
        }
        if (ctx.common.async) {
          return Promise.all(options.map(async (option) => {
            const childCtx = {
              ...ctx,
              common: {
                ...ctx.common,
                issues: []
              },
              parent: null
            };
            return {
              result: await option._parseAsync({
                data: ctx.data,
                path: ctx.path,
                parent: childCtx
              }),
              ctx: childCtx
            };
          })).then(handleResults);
        } else {
          let dirty = void 0;
          const issues = [];
          for (const option of options) {
            const childCtx = {
              ...ctx,
              common: {
                ...ctx.common,
                issues: []
              },
              parent: null
            };
            const result = option._parseSync({
              data: ctx.data,
              path: ctx.path,
              parent: childCtx
            });
            if (result.status === "valid") {
              return result;
            } else if (result.status === "dirty" && !dirty) {
              dirty = { result, ctx: childCtx };
            }
            if (childCtx.common.issues.length) {
              issues.push(childCtx.common.issues);
            }
          }
          if (dirty) {
            ctx.common.issues.push(...dirty.ctx.common.issues);
            return dirty.result;
          }
          const unionErrors = issues.map((issues2) => new ZodError(issues2));
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_union,
            unionErrors
          });
          return INVALID;
        }
      }
      get options() {
        return this._def.options;
      }
    };
    ZodUnion.create = (types, params) => {
      return new ZodUnion({
        options: types,
        typeName: ZodFirstPartyTypeKind.ZodUnion,
        ...processCreateParams(params)
      });
    };
    getDiscriminator = (type2) => {
      if (type2 instanceof ZodLazy) {
        return getDiscriminator(type2.schema);
      } else if (type2 instanceof ZodEffects) {
        return getDiscriminator(type2.innerType());
      } else if (type2 instanceof ZodLiteral) {
        return [type2.value];
      } else if (type2 instanceof ZodEnum) {
        return type2.options;
      } else if (type2 instanceof ZodNativeEnum) {
        return util.objectValues(type2.enum);
      } else if (type2 instanceof ZodDefault) {
        return getDiscriminator(type2._def.innerType);
      } else if (type2 instanceof ZodUndefined) {
        return [void 0];
      } else if (type2 instanceof ZodNull) {
        return [null];
      } else if (type2 instanceof ZodOptional) {
        return [void 0, ...getDiscriminator(type2.unwrap())];
      } else if (type2 instanceof ZodNullable) {
        return [null, ...getDiscriminator(type2.unwrap())];
      } else if (type2 instanceof ZodBranded) {
        return getDiscriminator(type2.unwrap());
      } else if (type2 instanceof ZodReadonly) {
        return getDiscriminator(type2.unwrap());
      } else if (type2 instanceof ZodCatch) {
        return getDiscriminator(type2._def.innerType);
      } else {
        return [];
      }
    };
    ZodDiscriminatedUnion = class _ZodDiscriminatedUnion extends ZodType {
      _parse(input2) {
        const { ctx } = this._processInputParams(input2);
        if (ctx.parsedType !== ZodParsedType.object) {
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.object,
            received: ctx.parsedType
          });
          return INVALID;
        }
        const discriminator = this.discriminator;
        const discriminatorValue = ctx.data[discriminator];
        const option = this.optionsMap.get(discriminatorValue);
        if (!option) {
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_union_discriminator,
            options: Array.from(this.optionsMap.keys()),
            path: [discriminator]
          });
          return INVALID;
        }
        if (ctx.common.async) {
          return option._parseAsync({
            data: ctx.data,
            path: ctx.path,
            parent: ctx
          });
        } else {
          return option._parseSync({
            data: ctx.data,
            path: ctx.path,
            parent: ctx
          });
        }
      }
      get discriminator() {
        return this._def.discriminator;
      }
      get options() {
        return this._def.options;
      }
      get optionsMap() {
        return this._def.optionsMap;
      }
      /**
       * The constructor of the discriminated union schema. Its behaviour is very similar to that of the normal z.union() constructor.
       * However, it only allows a union of objects, all of which need to share a discriminator property. This property must
       * have a different value for each object in the union.
       * @param discriminator the name of the discriminator property
       * @param types an array of object schemas
       * @param params
       */
      static create(discriminator, options, params) {
        const optionsMap = /* @__PURE__ */ new Map();
        for (const type2 of options) {
          const discriminatorValues = getDiscriminator(type2.shape[discriminator]);
          if (!discriminatorValues.length) {
            throw new Error(`A discriminator value for key \`${discriminator}\` could not be extracted from all schema options`);
          }
          for (const value of discriminatorValues) {
            if (optionsMap.has(value)) {
              throw new Error(`Discriminator property ${String(discriminator)} has duplicate value ${String(value)}`);
            }
            optionsMap.set(value, type2);
          }
        }
        return new _ZodDiscriminatedUnion({
          typeName: ZodFirstPartyTypeKind.ZodDiscriminatedUnion,
          discriminator,
          options,
          optionsMap,
          ...processCreateParams(params)
        });
      }
    };
    ZodIntersection = class extends ZodType {
      _parse(input2) {
        const { status: status2, ctx } = this._processInputParams(input2);
        const handleParsed = (parsedLeft, parsedRight) => {
          if (isAborted(parsedLeft) || isAborted(parsedRight)) {
            return INVALID;
          }
          const merged = mergeValues(parsedLeft.value, parsedRight.value);
          if (!merged.valid) {
            addIssueToContext(ctx, {
              code: ZodIssueCode.invalid_intersection_types
            });
            return INVALID;
          }
          if (isDirty(parsedLeft) || isDirty(parsedRight)) {
            status2.dirty();
          }
          return { status: status2.value, value: merged.data };
        };
        if (ctx.common.async) {
          return Promise.all([
            this._def.left._parseAsync({
              data: ctx.data,
              path: ctx.path,
              parent: ctx
            }),
            this._def.right._parseAsync({
              data: ctx.data,
              path: ctx.path,
              parent: ctx
            })
          ]).then(([left, right]) => handleParsed(left, right));
        } else {
          return handleParsed(this._def.left._parseSync({
            data: ctx.data,
            path: ctx.path,
            parent: ctx
          }), this._def.right._parseSync({
            data: ctx.data,
            path: ctx.path,
            parent: ctx
          }));
        }
      }
    };
    ZodIntersection.create = (left, right, params) => {
      return new ZodIntersection({
        left,
        right,
        typeName: ZodFirstPartyTypeKind.ZodIntersection,
        ...processCreateParams(params)
      });
    };
    ZodTuple = class _ZodTuple extends ZodType {
      _parse(input2) {
        const { status: status2, ctx } = this._processInputParams(input2);
        if (ctx.parsedType !== ZodParsedType.array) {
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.array,
            received: ctx.parsedType
          });
          return INVALID;
        }
        if (ctx.data.length < this._def.items.length) {
          addIssueToContext(ctx, {
            code: ZodIssueCode.too_small,
            minimum: this._def.items.length,
            inclusive: true,
            exact: false,
            type: "array"
          });
          return INVALID;
        }
        const rest = this._def.rest;
        if (!rest && ctx.data.length > this._def.items.length) {
          addIssueToContext(ctx, {
            code: ZodIssueCode.too_big,
            maximum: this._def.items.length,
            inclusive: true,
            exact: false,
            type: "array"
          });
          status2.dirty();
        }
        const items = [...ctx.data].map((item2, itemIndex) => {
          const schema = this._def.items[itemIndex] || this._def.rest;
          if (!schema)
            return null;
          return schema._parse(new ParseInputLazyPath(ctx, item2, ctx.path, itemIndex));
        }).filter((x3) => !!x3);
        if (ctx.common.async) {
          return Promise.all(items).then((results) => {
            return ParseStatus.mergeArray(status2, results);
          });
        } else {
          return ParseStatus.mergeArray(status2, items);
        }
      }
      get items() {
        return this._def.items;
      }
      rest(rest) {
        return new _ZodTuple({
          ...this._def,
          rest
        });
      }
    };
    ZodTuple.create = (schemas, params) => {
      if (!Array.isArray(schemas)) {
        throw new Error("You must pass an array of schemas to z.tuple([ ... ])");
      }
      return new ZodTuple({
        items: schemas,
        typeName: ZodFirstPartyTypeKind.ZodTuple,
        rest: null,
        ...processCreateParams(params)
      });
    };
    ZodRecord = class _ZodRecord extends ZodType {
      get keySchema() {
        return this._def.keyType;
      }
      get valueSchema() {
        return this._def.valueType;
      }
      _parse(input2) {
        const { status: status2, ctx } = this._processInputParams(input2);
        if (ctx.parsedType !== ZodParsedType.object) {
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.object,
            received: ctx.parsedType
          });
          return INVALID;
        }
        const pairs = [];
        const keyType = this._def.keyType;
        const valueType = this._def.valueType;
        for (const key in ctx.data) {
          pairs.push({
            key: keyType._parse(new ParseInputLazyPath(ctx, key, ctx.path, key)),
            value: valueType._parse(new ParseInputLazyPath(ctx, ctx.data[key], ctx.path, key)),
            alwaysSet: key in ctx.data
          });
        }
        if (ctx.common.async) {
          return ParseStatus.mergeObjectAsync(status2, pairs);
        } else {
          return ParseStatus.mergeObjectSync(status2, pairs);
        }
      }
      get element() {
        return this._def.valueType;
      }
      static create(first, second, third) {
        if (second instanceof ZodType) {
          return new _ZodRecord({
            keyType: first,
            valueType: second,
            typeName: ZodFirstPartyTypeKind.ZodRecord,
            ...processCreateParams(third)
          });
        }
        return new _ZodRecord({
          keyType: ZodString.create(),
          valueType: first,
          typeName: ZodFirstPartyTypeKind.ZodRecord,
          ...processCreateParams(second)
        });
      }
    };
    ZodMap = class extends ZodType {
      get keySchema() {
        return this._def.keyType;
      }
      get valueSchema() {
        return this._def.valueType;
      }
      _parse(input2) {
        const { status: status2, ctx } = this._processInputParams(input2);
        if (ctx.parsedType !== ZodParsedType.map) {
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.map,
            received: ctx.parsedType
          });
          return INVALID;
        }
        const keyType = this._def.keyType;
        const valueType = this._def.valueType;
        const pairs = [...ctx.data.entries()].map(([key, value], index3) => {
          return {
            key: keyType._parse(new ParseInputLazyPath(ctx, key, ctx.path, [index3, "key"])),
            value: valueType._parse(new ParseInputLazyPath(ctx, value, ctx.path, [index3, "value"]))
          };
        });
        if (ctx.common.async) {
          const finalMap = /* @__PURE__ */ new Map();
          return Promise.resolve().then(async () => {
            for (const pair of pairs) {
              const key = await pair.key;
              const value = await pair.value;
              if (key.status === "aborted" || value.status === "aborted") {
                return INVALID;
              }
              if (key.status === "dirty" || value.status === "dirty") {
                status2.dirty();
              }
              finalMap.set(key.value, value.value);
            }
            return { status: status2.value, value: finalMap };
          });
        } else {
          const finalMap = /* @__PURE__ */ new Map();
          for (const pair of pairs) {
            const key = pair.key;
            const value = pair.value;
            if (key.status === "aborted" || value.status === "aborted") {
              return INVALID;
            }
            if (key.status === "dirty" || value.status === "dirty") {
              status2.dirty();
            }
            finalMap.set(key.value, value.value);
          }
          return { status: status2.value, value: finalMap };
        }
      }
    };
    ZodMap.create = (keyType, valueType, params) => {
      return new ZodMap({
        valueType,
        keyType,
        typeName: ZodFirstPartyTypeKind.ZodMap,
        ...processCreateParams(params)
      });
    };
    ZodSet = class _ZodSet extends ZodType {
      _parse(input2) {
        const { status: status2, ctx } = this._processInputParams(input2);
        if (ctx.parsedType !== ZodParsedType.set) {
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.set,
            received: ctx.parsedType
          });
          return INVALID;
        }
        const def = this._def;
        if (def.minSize !== null) {
          if (ctx.data.size < def.minSize.value) {
            addIssueToContext(ctx, {
              code: ZodIssueCode.too_small,
              minimum: def.minSize.value,
              type: "set",
              inclusive: true,
              exact: false,
              message: def.minSize.message
            });
            status2.dirty();
          }
        }
        if (def.maxSize !== null) {
          if (ctx.data.size > def.maxSize.value) {
            addIssueToContext(ctx, {
              code: ZodIssueCode.too_big,
              maximum: def.maxSize.value,
              type: "set",
              inclusive: true,
              exact: false,
              message: def.maxSize.message
            });
            status2.dirty();
          }
        }
        const valueType = this._def.valueType;
        function finalizeSet(elements2) {
          const parsedSet = /* @__PURE__ */ new Set();
          for (const element of elements2) {
            if (element.status === "aborted")
              return INVALID;
            if (element.status === "dirty")
              status2.dirty();
            parsedSet.add(element.value);
          }
          return { status: status2.value, value: parsedSet };
        }
        const elements = [...ctx.data.values()].map((item2, i13) => valueType._parse(new ParseInputLazyPath(ctx, item2, ctx.path, i13)));
        if (ctx.common.async) {
          return Promise.all(elements).then((elements2) => finalizeSet(elements2));
        } else {
          return finalizeSet(elements);
        }
      }
      min(minSize, message) {
        return new _ZodSet({
          ...this._def,
          minSize: { value: minSize, message: errorUtil.toString(message) }
        });
      }
      max(maxSize, message) {
        return new _ZodSet({
          ...this._def,
          maxSize: { value: maxSize, message: errorUtil.toString(message) }
        });
      }
      size(size4, message) {
        return this.min(size4, message).max(size4, message);
      }
      nonempty(message) {
        return this.min(1, message);
      }
    };
    ZodSet.create = (valueType, params) => {
      return new ZodSet({
        valueType,
        minSize: null,
        maxSize: null,
        typeName: ZodFirstPartyTypeKind.ZodSet,
        ...processCreateParams(params)
      });
    };
    ZodFunction = class _ZodFunction extends ZodType {
      constructor() {
        super(...arguments);
        this.validate = this.implement;
      }
      _parse(input2) {
        const { ctx } = this._processInputParams(input2);
        if (ctx.parsedType !== ZodParsedType.function) {
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.function,
            received: ctx.parsedType
          });
          return INVALID;
        }
        function makeArgsIssue(args, error) {
          return makeIssue({
            data: args,
            path: ctx.path,
            errorMaps: [
              ctx.common.contextualErrorMap,
              ctx.schemaErrorMap,
              getErrorMap(),
              errorMap
            ].filter((x3) => !!x3),
            issueData: {
              code: ZodIssueCode.invalid_arguments,
              argumentsError: error
            }
          });
        }
        function makeReturnsIssue(returns, error) {
          return makeIssue({
            data: returns,
            path: ctx.path,
            errorMaps: [
              ctx.common.contextualErrorMap,
              ctx.schemaErrorMap,
              getErrorMap(),
              errorMap
            ].filter((x3) => !!x3),
            issueData: {
              code: ZodIssueCode.invalid_return_type,
              returnTypeError: error
            }
          });
        }
        const params = { errorMap: ctx.common.contextualErrorMap };
        const fn = ctx.data;
        if (this._def.returns instanceof ZodPromise) {
          const me4 = this;
          return OK(async function(...args) {
            const error = new ZodError([]);
            const parsedArgs = await me4._def.args.parseAsync(args, params).catch((e14) => {
              error.addIssue(makeArgsIssue(args, e14));
              throw error;
            });
            const result = await Reflect.apply(fn, this, parsedArgs);
            const parsedReturns = await me4._def.returns._def.type.parseAsync(result, params).catch((e14) => {
              error.addIssue(makeReturnsIssue(result, e14));
              throw error;
            });
            return parsedReturns;
          });
        } else {
          const me4 = this;
          return OK(function(...args) {
            const parsedArgs = me4._def.args.safeParse(args, params);
            if (!parsedArgs.success) {
              throw new ZodError([makeArgsIssue(args, parsedArgs.error)]);
            }
            const result = Reflect.apply(fn, this, parsedArgs.data);
            const parsedReturns = me4._def.returns.safeParse(result, params);
            if (!parsedReturns.success) {
              throw new ZodError([makeReturnsIssue(result, parsedReturns.error)]);
            }
            return parsedReturns.data;
          });
        }
      }
      parameters() {
        return this._def.args;
      }
      returnType() {
        return this._def.returns;
      }
      args(...items) {
        return new _ZodFunction({
          ...this._def,
          args: ZodTuple.create(items).rest(ZodUnknown.create())
        });
      }
      returns(returnType) {
        return new _ZodFunction({
          ...this._def,
          returns: returnType
        });
      }
      implement(func) {
        const validatedFunc = this.parse(func);
        return validatedFunc;
      }
      strictImplement(func) {
        const validatedFunc = this.parse(func);
        return validatedFunc;
      }
      static create(args, returns, params) {
        return new _ZodFunction({
          args: args ? args : ZodTuple.create([]).rest(ZodUnknown.create()),
          returns: returns || ZodUnknown.create(),
          typeName: ZodFirstPartyTypeKind.ZodFunction,
          ...processCreateParams(params)
        });
      }
    };
    ZodLazy = class extends ZodType {
      get schema() {
        return this._def.getter();
      }
      _parse(input2) {
        const { ctx } = this._processInputParams(input2);
        const lazySchema = this._def.getter();
        return lazySchema._parse({ data: ctx.data, path: ctx.path, parent: ctx });
      }
    };
    ZodLazy.create = (getter, params) => {
      return new ZodLazy({
        getter,
        typeName: ZodFirstPartyTypeKind.ZodLazy,
        ...processCreateParams(params)
      });
    };
    ZodLiteral = class extends ZodType {
      _parse(input2) {
        if (input2.data !== this._def.value) {
          const ctx = this._getOrReturnCtx(input2);
          addIssueToContext(ctx, {
            received: ctx.data,
            code: ZodIssueCode.invalid_literal,
            expected: this._def.value
          });
          return INVALID;
        }
        return { status: "valid", value: input2.data };
      }
      get value() {
        return this._def.value;
      }
    };
    ZodLiteral.create = (value, params) => {
      return new ZodLiteral({
        value,
        typeName: ZodFirstPartyTypeKind.ZodLiteral,
        ...processCreateParams(params)
      });
    };
    ZodEnum = class _ZodEnum extends ZodType {
      constructor() {
        super(...arguments);
        _ZodEnum_cache.set(this, void 0);
      }
      _parse(input2) {
        if (typeof input2.data !== "string") {
          const ctx = this._getOrReturnCtx(input2);
          const expectedValues = this._def.values;
          addIssueToContext(ctx, {
            expected: util.joinValues(expectedValues),
            received: ctx.parsedType,
            code: ZodIssueCode.invalid_type
          });
          return INVALID;
        }
        if (!__classPrivateFieldGet2(this, _ZodEnum_cache, "f")) {
          __classPrivateFieldSet2(this, _ZodEnum_cache, new Set(this._def.values), "f");
        }
        if (!__classPrivateFieldGet2(this, _ZodEnum_cache, "f").has(input2.data)) {
          const ctx = this._getOrReturnCtx(input2);
          const expectedValues = this._def.values;
          addIssueToContext(ctx, {
            received: ctx.data,
            code: ZodIssueCode.invalid_enum_value,
            options: expectedValues
          });
          return INVALID;
        }
        return OK(input2.data);
      }
      get options() {
        return this._def.values;
      }
      get enum() {
        const enumValues = {};
        for (const val of this._def.values) {
          enumValues[val] = val;
        }
        return enumValues;
      }
      get Values() {
        const enumValues = {};
        for (const val of this._def.values) {
          enumValues[val] = val;
        }
        return enumValues;
      }
      get Enum() {
        const enumValues = {};
        for (const val of this._def.values) {
          enumValues[val] = val;
        }
        return enumValues;
      }
      extract(values2, newDef = this._def) {
        return _ZodEnum.create(values2, {
          ...this._def,
          ...newDef
        });
      }
      exclude(values2, newDef = this._def) {
        return _ZodEnum.create(this.options.filter((opt) => !values2.includes(opt)), {
          ...this._def,
          ...newDef
        });
      }
    };
    _ZodEnum_cache = /* @__PURE__ */ new WeakMap();
    ZodEnum.create = createZodEnum;
    ZodNativeEnum = class extends ZodType {
      constructor() {
        super(...arguments);
        _ZodNativeEnum_cache.set(this, void 0);
      }
      _parse(input2) {
        const nativeEnumValues = util.getValidEnumValues(this._def.values);
        const ctx = this._getOrReturnCtx(input2);
        if (ctx.parsedType !== ZodParsedType.string && ctx.parsedType !== ZodParsedType.number) {
          const expectedValues = util.objectValues(nativeEnumValues);
          addIssueToContext(ctx, {
            expected: util.joinValues(expectedValues),
            received: ctx.parsedType,
            code: ZodIssueCode.invalid_type
          });
          return INVALID;
        }
        if (!__classPrivateFieldGet2(this, _ZodNativeEnum_cache, "f")) {
          __classPrivateFieldSet2(this, _ZodNativeEnum_cache, new Set(util.getValidEnumValues(this._def.values)), "f");
        }
        if (!__classPrivateFieldGet2(this, _ZodNativeEnum_cache, "f").has(input2.data)) {
          const expectedValues = util.objectValues(nativeEnumValues);
          addIssueToContext(ctx, {
            received: ctx.data,
            code: ZodIssueCode.invalid_enum_value,
            options: expectedValues
          });
          return INVALID;
        }
        return OK(input2.data);
      }
      get enum() {
        return this._def.values;
      }
    };
    _ZodNativeEnum_cache = /* @__PURE__ */ new WeakMap();
    ZodNativeEnum.create = (values2, params) => {
      return new ZodNativeEnum({
        values: values2,
        typeName: ZodFirstPartyTypeKind.ZodNativeEnum,
        ...processCreateParams(params)
      });
    };
    ZodPromise = class extends ZodType {
      unwrap() {
        return this._def.type;
      }
      _parse(input2) {
        const { ctx } = this._processInputParams(input2);
        if (ctx.parsedType !== ZodParsedType.promise && ctx.common.async === false) {
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.promise,
            received: ctx.parsedType
          });
          return INVALID;
        }
        const promisified = ctx.parsedType === ZodParsedType.promise ? ctx.data : Promise.resolve(ctx.data);
        return OK(promisified.then((data) => {
          return this._def.type.parseAsync(data, {
            path: ctx.path,
            errorMap: ctx.common.contextualErrorMap
          });
        }));
      }
    };
    ZodPromise.create = (schema, params) => {
      return new ZodPromise({
        type: schema,
        typeName: ZodFirstPartyTypeKind.ZodPromise,
        ...processCreateParams(params)
      });
    };
    ZodEffects = class extends ZodType {
      innerType() {
        return this._def.schema;
      }
      sourceType() {
        return this._def.schema._def.typeName === ZodFirstPartyTypeKind.ZodEffects ? this._def.schema.sourceType() : this._def.schema;
      }
      _parse(input2) {
        const { status: status2, ctx } = this._processInputParams(input2);
        const effect = this._def.effect || null;
        const checkCtx = {
          addIssue: (arg) => {
            addIssueToContext(ctx, arg);
            if (arg.fatal) {
              status2.abort();
            } else {
              status2.dirty();
            }
          },
          get path() {
            return ctx.path;
          }
        };
        checkCtx.addIssue = checkCtx.addIssue.bind(checkCtx);
        if (effect.type === "preprocess") {
          const processed = effect.transform(ctx.data, checkCtx);
          if (ctx.common.async) {
            return Promise.resolve(processed).then(async (processed2) => {
              if (status2.value === "aborted")
                return INVALID;
              const result = await this._def.schema._parseAsync({
                data: processed2,
                path: ctx.path,
                parent: ctx
              });
              if (result.status === "aborted")
                return INVALID;
              if (result.status === "dirty")
                return DIRTY(result.value);
              if (status2.value === "dirty")
                return DIRTY(result.value);
              return result;
            });
          } else {
            if (status2.value === "aborted")
              return INVALID;
            const result = this._def.schema._parseSync({
              data: processed,
              path: ctx.path,
              parent: ctx
            });
            if (result.status === "aborted")
              return INVALID;
            if (result.status === "dirty")
              return DIRTY(result.value);
            if (status2.value === "dirty")
              return DIRTY(result.value);
            return result;
          }
        }
        if (effect.type === "refinement") {
          const executeRefinement = (acc) => {
            const result = effect.refinement(acc, checkCtx);
            if (ctx.common.async) {
              return Promise.resolve(result);
            }
            if (result instanceof Promise) {
              throw new Error("Async refinement encountered during synchronous parse operation. Use .parseAsync instead.");
            }
            return acc;
          };
          if (ctx.common.async === false) {
            const inner3 = this._def.schema._parseSync({
              data: ctx.data,
              path: ctx.path,
              parent: ctx
            });
            if (inner3.status === "aborted")
              return INVALID;
            if (inner3.status === "dirty")
              status2.dirty();
            executeRefinement(inner3.value);
            return { status: status2.value, value: inner3.value };
          } else {
            return this._def.schema._parseAsync({ data: ctx.data, path: ctx.path, parent: ctx }).then((inner3) => {
              if (inner3.status === "aborted")
                return INVALID;
              if (inner3.status === "dirty")
                status2.dirty();
              return executeRefinement(inner3.value).then(() => {
                return { status: status2.value, value: inner3.value };
              });
            });
          }
        }
        if (effect.type === "transform") {
          if (ctx.common.async === false) {
            const base = this._def.schema._parseSync({
              data: ctx.data,
              path: ctx.path,
              parent: ctx
            });
            if (!isValid(base))
              return base;
            const result = effect.transform(base.value, checkCtx);
            if (result instanceof Promise) {
              throw new Error(`Asynchronous transform encountered during synchronous parse operation. Use .parseAsync instead.`);
            }
            return { status: status2.value, value: result };
          } else {
            return this._def.schema._parseAsync({ data: ctx.data, path: ctx.path, parent: ctx }).then((base) => {
              if (!isValid(base))
                return base;
              return Promise.resolve(effect.transform(base.value, checkCtx)).then((result) => ({ status: status2.value, value: result }));
            });
          }
        }
        util.assertNever(effect);
      }
    };
    ZodEffects.create = (schema, effect, params) => {
      return new ZodEffects({
        schema,
        typeName: ZodFirstPartyTypeKind.ZodEffects,
        effect,
        ...processCreateParams(params)
      });
    };
    ZodEffects.createWithPreprocess = (preprocess, schema, params) => {
      return new ZodEffects({
        schema,
        effect: { type: "preprocess", transform: preprocess },
        typeName: ZodFirstPartyTypeKind.ZodEffects,
        ...processCreateParams(params)
      });
    };
    ZodOptional = class extends ZodType {
      _parse(input2) {
        const parsedType = this._getType(input2);
        if (parsedType === ZodParsedType.undefined) {
          return OK(void 0);
        }
        return this._def.innerType._parse(input2);
      }
      unwrap() {
        return this._def.innerType;
      }
    };
    ZodOptional.create = (type2, params) => {
      return new ZodOptional({
        innerType: type2,
        typeName: ZodFirstPartyTypeKind.ZodOptional,
        ...processCreateParams(params)
      });
    };
    ZodNullable = class extends ZodType {
      _parse(input2) {
        const parsedType = this._getType(input2);
        if (parsedType === ZodParsedType.null) {
          return OK(null);
        }
        return this._def.innerType._parse(input2);
      }
      unwrap() {
        return this._def.innerType;
      }
    };
    ZodNullable.create = (type2, params) => {
      return new ZodNullable({
        innerType: type2,
        typeName: ZodFirstPartyTypeKind.ZodNullable,
        ...processCreateParams(params)
      });
    };
    ZodDefault = class extends ZodType {
      _parse(input2) {
        const { ctx } = this._processInputParams(input2);
        let data = ctx.data;
        if (ctx.parsedType === ZodParsedType.undefined) {
          data = this._def.defaultValue();
        }
        return this._def.innerType._parse({
          data,
          path: ctx.path,
          parent: ctx
        });
      }
      removeDefault() {
        return this._def.innerType;
      }
    };
    ZodDefault.create = (type2, params) => {
      return new ZodDefault({
        innerType: type2,
        typeName: ZodFirstPartyTypeKind.ZodDefault,
        defaultValue: typeof params.default === "function" ? params.default : () => params.default,
        ...processCreateParams(params)
      });
    };
    ZodCatch = class extends ZodType {
      _parse(input2) {
        const { ctx } = this._processInputParams(input2);
        const newCtx = {
          ...ctx,
          common: {
            ...ctx.common,
            issues: []
          }
        };
        const result = this._def.innerType._parse({
          data: newCtx.data,
          path: newCtx.path,
          parent: {
            ...newCtx
          }
        });
        if (isAsync(result)) {
          return result.then((result2) => {
            return {
              status: "valid",
              value: result2.status === "valid" ? result2.value : this._def.catchValue({
                get error() {
                  return new ZodError(newCtx.common.issues);
                },
                input: newCtx.data
              })
            };
          });
        } else {
          return {
            status: "valid",
            value: result.status === "valid" ? result.value : this._def.catchValue({
              get error() {
                return new ZodError(newCtx.common.issues);
              },
              input: newCtx.data
            })
          };
        }
      }
      removeCatch() {
        return this._def.innerType;
      }
    };
    ZodCatch.create = (type2, params) => {
      return new ZodCatch({
        innerType: type2,
        typeName: ZodFirstPartyTypeKind.ZodCatch,
        catchValue: typeof params.catch === "function" ? params.catch : () => params.catch,
        ...processCreateParams(params)
      });
    };
    ZodNaN = class extends ZodType {
      _parse(input2) {
        const parsedType = this._getType(input2);
        if (parsedType !== ZodParsedType.nan) {
          const ctx = this._getOrReturnCtx(input2);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.nan,
            received: ctx.parsedType
          });
          return INVALID;
        }
        return { status: "valid", value: input2.data };
      }
    };
    ZodNaN.create = (params) => {
      return new ZodNaN({
        typeName: ZodFirstPartyTypeKind.ZodNaN,
        ...processCreateParams(params)
      });
    };
    BRAND = Symbol("zod_brand");
    ZodBranded = class extends ZodType {
      _parse(input2) {
        const { ctx } = this._processInputParams(input2);
        const data = ctx.data;
        return this._def.type._parse({
          data,
          path: ctx.path,
          parent: ctx
        });
      }
      unwrap() {
        return this._def.type;
      }
    };
    ZodPipeline = class _ZodPipeline extends ZodType {
      _parse(input2) {
        const { status: status2, ctx } = this._processInputParams(input2);
        if (ctx.common.async) {
          const handleAsync = async () => {
            const inResult = await this._def.in._parseAsync({
              data: ctx.data,
              path: ctx.path,
              parent: ctx
            });
            if (inResult.status === "aborted")
              return INVALID;
            if (inResult.status === "dirty") {
              status2.dirty();
              return DIRTY(inResult.value);
            } else {
              return this._def.out._parseAsync({
                data: inResult.value,
                path: ctx.path,
                parent: ctx
              });
            }
          };
          return handleAsync();
        } else {
          const inResult = this._def.in._parseSync({
            data: ctx.data,
            path: ctx.path,
            parent: ctx
          });
          if (inResult.status === "aborted")
            return INVALID;
          if (inResult.status === "dirty") {
            status2.dirty();
            return {
              status: "dirty",
              value: inResult.value
            };
          } else {
            return this._def.out._parseSync({
              data: inResult.value,
              path: ctx.path,
              parent: ctx
            });
          }
        }
      }
      static create(a18, b4) {
        return new _ZodPipeline({
          in: a18,
          out: b4,
          typeName: ZodFirstPartyTypeKind.ZodPipeline
        });
      }
    };
    ZodReadonly = class extends ZodType {
      _parse(input2) {
        const result = this._def.innerType._parse(input2);
        const freeze = (data) => {
          if (isValid(data)) {
            data.value = Object.freeze(data.value);
          }
          return data;
        };
        return isAsync(result) ? result.then((data) => freeze(data)) : freeze(result);
      }
      unwrap() {
        return this._def.innerType;
      }
    };
    ZodReadonly.create = (type2, params) => {
      return new ZodReadonly({
        innerType: type2,
        typeName: ZodFirstPartyTypeKind.ZodReadonly,
        ...processCreateParams(params)
      });
    };
    late = {
      object: ZodObject.lazycreate
    };
    (function(ZodFirstPartyTypeKind2) {
      ZodFirstPartyTypeKind2["ZodString"] = "ZodString";
      ZodFirstPartyTypeKind2["ZodNumber"] = "ZodNumber";
      ZodFirstPartyTypeKind2["ZodNaN"] = "ZodNaN";
      ZodFirstPartyTypeKind2["ZodBigInt"] = "ZodBigInt";
      ZodFirstPartyTypeKind2["ZodBoolean"] = "ZodBoolean";
      ZodFirstPartyTypeKind2["ZodDate"] = "ZodDate";
      ZodFirstPartyTypeKind2["ZodSymbol"] = "ZodSymbol";
      ZodFirstPartyTypeKind2["ZodUndefined"] = "ZodUndefined";
      ZodFirstPartyTypeKind2["ZodNull"] = "ZodNull";
      ZodFirstPartyTypeKind2["ZodAny"] = "ZodAny";
      ZodFirstPartyTypeKind2["ZodUnknown"] = "ZodUnknown";
      ZodFirstPartyTypeKind2["ZodNever"] = "ZodNever";
      ZodFirstPartyTypeKind2["ZodVoid"] = "ZodVoid";
      ZodFirstPartyTypeKind2["ZodArray"] = "ZodArray";
      ZodFirstPartyTypeKind2["ZodObject"] = "ZodObject";
      ZodFirstPartyTypeKind2["ZodUnion"] = "ZodUnion";
      ZodFirstPartyTypeKind2["ZodDiscriminatedUnion"] = "ZodDiscriminatedUnion";
      ZodFirstPartyTypeKind2["ZodIntersection"] = "ZodIntersection";
      ZodFirstPartyTypeKind2["ZodTuple"] = "ZodTuple";
      ZodFirstPartyTypeKind2["ZodRecord"] = "ZodRecord";
      ZodFirstPartyTypeKind2["ZodMap"] = "ZodMap";
      ZodFirstPartyTypeKind2["ZodSet"] = "ZodSet";
      ZodFirstPartyTypeKind2["ZodFunction"] = "ZodFunction";
      ZodFirstPartyTypeKind2["ZodLazy"] = "ZodLazy";
      ZodFirstPartyTypeKind2["ZodLiteral"] = "ZodLiteral";
      ZodFirstPartyTypeKind2["ZodEnum"] = "ZodEnum";
      ZodFirstPartyTypeKind2["ZodEffects"] = "ZodEffects";
      ZodFirstPartyTypeKind2["ZodNativeEnum"] = "ZodNativeEnum";
      ZodFirstPartyTypeKind2["ZodOptional"] = "ZodOptional";
      ZodFirstPartyTypeKind2["ZodNullable"] = "ZodNullable";
      ZodFirstPartyTypeKind2["ZodDefault"] = "ZodDefault";
      ZodFirstPartyTypeKind2["ZodCatch"] = "ZodCatch";
      ZodFirstPartyTypeKind2["ZodPromise"] = "ZodPromise";
      ZodFirstPartyTypeKind2["ZodBranded"] = "ZodBranded";
      ZodFirstPartyTypeKind2["ZodPipeline"] = "ZodPipeline";
      ZodFirstPartyTypeKind2["ZodReadonly"] = "ZodReadonly";
    })(ZodFirstPartyTypeKind || (ZodFirstPartyTypeKind = {}));
    instanceOfType = (cls, params = {
      message: `Input not instance of ${cls.name}`
    }) => custom((data) => data instanceof cls, params);
    stringType = ZodString.create;
    numberType = ZodNumber.create;
    nanType = ZodNaN.create;
    bigIntType = ZodBigInt.create;
    booleanType = ZodBoolean.create;
    dateType = ZodDate.create;
    symbolType = ZodSymbol.create;
    undefinedType = ZodUndefined.create;
    nullType = ZodNull.create;
    anyType = ZodAny.create;
    unknownType = ZodUnknown.create;
    neverType = ZodNever.create;
    voidType = ZodVoid.create;
    arrayType = ZodArray.create;
    objectType = ZodObject.create;
    strictObjectType = ZodObject.strictCreate;
    unionType = ZodUnion.create;
    discriminatedUnionType = ZodDiscriminatedUnion.create;
    intersectionType = ZodIntersection.create;
    tupleType = ZodTuple.create;
    recordType = ZodRecord.create;
    mapType = ZodMap.create;
    setType = ZodSet.create;
    functionType = ZodFunction.create;
    lazyType = ZodLazy.create;
    literalType = ZodLiteral.create;
    enumType = ZodEnum.create;
    nativeEnumType = ZodNativeEnum.create;
    promiseType = ZodPromise.create;
    effectsType = ZodEffects.create;
    optionalType = ZodOptional.create;
    nullableType = ZodNullable.create;
    preprocessType = ZodEffects.createWithPreprocess;
    pipelineType = ZodPipeline.create;
    ostring = () => stringType().optional();
    onumber = () => numberType().optional();
    oboolean = () => booleanType().optional();
    coerce = {
      string: (arg) => ZodString.create({ ...arg, coerce: true }),
      number: (arg) => ZodNumber.create({ ...arg, coerce: true }),
      boolean: (arg) => ZodBoolean.create({
        ...arg,
        coerce: true
      }),
      bigint: (arg) => ZodBigInt.create({ ...arg, coerce: true }),
      date: (arg) => ZodDate.create({ ...arg, coerce: true })
    };
    NEVER = INVALID;
    z = /* @__PURE__ */ Object.freeze({
      __proto__: null,
      defaultErrorMap: errorMap,
      setErrorMap,
      getErrorMap,
      makeIssue,
      EMPTY_PATH,
      addIssueToContext,
      ParseStatus,
      INVALID,
      DIRTY,
      OK,
      isAborted,
      isDirty,
      isValid,
      isAsync,
      get util() {
        return util;
      },
      get objectUtil() {
        return objectUtil;
      },
      ZodParsedType,
      getParsedType,
      ZodType,
      datetimeRegex,
      ZodString,
      ZodNumber,
      ZodBigInt,
      ZodBoolean,
      ZodDate,
      ZodSymbol,
      ZodUndefined,
      ZodNull,
      ZodAny,
      ZodUnknown,
      ZodNever,
      ZodVoid,
      ZodArray,
      ZodObject,
      ZodUnion,
      ZodDiscriminatedUnion,
      ZodIntersection,
      ZodTuple,
      ZodRecord,
      ZodMap,
      ZodSet,
      ZodFunction,
      ZodLazy,
      ZodLiteral,
      ZodEnum,
      ZodNativeEnum,
      ZodPromise,
      ZodEffects,
      ZodTransformer: ZodEffects,
      ZodOptional,
      ZodNullable,
      ZodDefault,
      ZodCatch,
      ZodNaN,
      BRAND,
      ZodBranded,
      ZodPipeline,
      ZodReadonly,
      custom,
      Schema: ZodType,
      ZodSchema: ZodType,
      late,
      get ZodFirstPartyTypeKind() {
        return ZodFirstPartyTypeKind;
      },
      coerce,
      any: anyType,
      array: arrayType,
      bigint: bigIntType,
      boolean: booleanType,
      date: dateType,
      discriminatedUnion: discriminatedUnionType,
      effect: effectsType,
      "enum": enumType,
      "function": functionType,
      "instanceof": instanceOfType,
      intersection: intersectionType,
      lazy: lazyType,
      literal: literalType,
      map: mapType,
      nan: nanType,
      nativeEnum: nativeEnumType,
      never: neverType,
      "null": nullType,
      nullable: nullableType,
      number: numberType,
      object: objectType,
      oboolean,
      onumber,
      optional: optionalType,
      ostring,
      pipeline: pipelineType,
      preprocess: preprocessType,
      promise: promiseType,
      record: recordType,
      set: setType,
      strictObject: strictObjectType,
      string: stringType,
      symbol: symbolType,
      transformer: effectsType,
      tuple: tupleType,
      "undefined": undefinedType,
      union: unionType,
      unknown: unknownType,
      "void": voidType,
      NEVER,
      ZodIssueCode,
      quotelessJson,
      ZodError
    });
  }
});

// dist/server/chunks/chunk-BUm1PIXW.js
var wrapper, container2, s15;
var init_chunk_BUm1PIXW = __esm({
  "dist/server/chunks/chunk-BUm1PIXW.js"() {
    "use strict";
    wrapper = "_wrapper_1vr4c_1";
    container2 = "_container_1vr4c_21";
    s15 = {
      wrapper,
      container: container2
    };
  }
});

// node_modules/lodash.throttle/index.js
var require_lodash = __commonJS({
  "node_modules/lodash.throttle/index.js"(exports, module) {
    var FUNC_ERROR_TEXT = "Expected a function";
    var NAN = 0 / 0;
    var symbolTag = "[object Symbol]";
    var reTrim = /^\s+|\s+$/g;
    var reIsBadHex = /^[-+]0x[0-9a-f]+$/i;
    var reIsBinary = /^0b[01]+$/i;
    var reIsOctal = /^0o[0-7]+$/i;
    var freeParseInt = parseInt;
    var freeGlobal2 = typeof global == "object" && global && global.Object === Object && global;
    var freeSelf2 = typeof self == "object" && self && self.Object === Object && self;
    var root5 = freeGlobal2 || freeSelf2 || Function("return this")();
    var objectProto3 = Object.prototype;
    var objectToString2 = objectProto3.toString;
    var nativeMax = Math.max;
    var nativeMin = Math.min;
    var now2 = function() {
      return root5.Date.now();
    };
    function debounce3(func, wait, options) {
      var lastArgs, lastThis, maxWait, result, timerId, lastCallTime, lastInvokeTime = 0, leading = false, maxing = false, trailing = true;
      if (typeof func != "function") {
        throw new TypeError(FUNC_ERROR_TEXT);
      }
      wait = toNumber(wait) || 0;
      if (isObject4(options)) {
        leading = !!options.leading;
        maxing = "maxWait" in options;
        maxWait = maxing ? nativeMax(toNumber(options.maxWait) || 0, wait) : maxWait;
        trailing = "trailing" in options ? !!options.trailing : trailing;
      }
      function invokeFunc(time3) {
        var args = lastArgs, thisArg = lastThis;
        lastArgs = lastThis = void 0;
        lastInvokeTime = time3;
        result = func.apply(thisArg, args);
        return result;
      }
      function leadingEdge(time3) {
        lastInvokeTime = time3;
        timerId = setTimeout(timerExpired, wait);
        return leading ? invokeFunc(time3) : result;
      }
      function remainingWait(time3) {
        var timeSinceLastCall = time3 - lastCallTime, timeSinceLastInvoke = time3 - lastInvokeTime, result2 = wait - timeSinceLastCall;
        return maxing ? nativeMin(result2, maxWait - timeSinceLastInvoke) : result2;
      }
      function shouldInvoke(time3) {
        var timeSinceLastCall = time3 - lastCallTime, timeSinceLastInvoke = time3 - lastInvokeTime;
        return lastCallTime === void 0 || timeSinceLastCall >= wait || timeSinceLastCall < 0 || maxing && timeSinceLastInvoke >= maxWait;
      }
      function timerExpired() {
        var time3 = now2();
        if (shouldInvoke(time3)) {
          return trailingEdge(time3);
        }
        timerId = setTimeout(timerExpired, remainingWait(time3));
      }
      function trailingEdge(time3) {
        timerId = void 0;
        if (trailing && lastArgs) {
          return invokeFunc(time3);
        }
        lastArgs = lastThis = void 0;
        return result;
      }
      function cancel() {
        if (timerId !== void 0) {
          clearTimeout(timerId);
        }
        lastInvokeTime = 0;
        lastArgs = lastCallTime = lastThis = timerId = void 0;
      }
      function flush() {
        return timerId === void 0 ? result : trailingEdge(now2());
      }
      function debounced() {
        var time3 = now2(), isInvoking = shouldInvoke(time3);
        lastArgs = arguments;
        lastThis = this;
        lastCallTime = time3;
        if (isInvoking) {
          if (timerId === void 0) {
            return leadingEdge(lastCallTime);
          }
          if (maxing) {
            timerId = setTimeout(timerExpired, wait);
            return invokeFunc(lastCallTime);
          }
        }
        if (timerId === void 0) {
          timerId = setTimeout(timerExpired, wait);
        }
        return result;
      }
      debounced.cancel = cancel;
      debounced.flush = flush;
      return debounced;
    }
    function throttle2(func, wait, options) {
      var leading = true, trailing = true;
      if (typeof func != "function") {
        throw new TypeError(FUNC_ERROR_TEXT);
      }
      if (isObject4(options)) {
        leading = "leading" in options ? !!options.leading : leading;
        trailing = "trailing" in options ? !!options.trailing : trailing;
      }
      return debounce3(func, wait, {
        "leading": leading,
        "maxWait": wait,
        "trailing": trailing
      });
    }
    function isObject4(value) {
      var type2 = typeof value;
      return !!value && (type2 == "object" || type2 == "function");
    }
    function isObjectLike2(value) {
      return !!value && typeof value == "object";
    }
    function isSymbol(value) {
      return typeof value == "symbol" || isObjectLike2(value) && objectToString2.call(value) == symbolTag;
    }
    function toNumber(value) {
      if (typeof value == "number") {
        return value;
      }
      if (isSymbol(value)) {
        return NAN;
      }
      if (isObject4(value)) {
        var other = typeof value.valueOf == "function" ? value.valueOf() : value;
        value = isObject4(other) ? other + "" : other;
      }
      if (typeof value != "string") {
        return value === 0 ? value : +value;
      }
      value = value.replace(reTrim, "");
      var isBinary = reIsBinary.test(value);
      return isBinary || reIsOctal.test(value) ? freeParseInt(value.slice(2), isBinary ? 2 : 8) : reIsBadHex.test(value) ? NAN : +value;
    }
    module.exports = throttle2;
  }
});

// node_modules/react-scroll/modules/mixins/passive-event-listeners.js
var require_passive_event_listeners = __commonJS({
  "node_modules/react-scroll/modules/mixins/passive-event-listeners.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    var addPassiveEventListener = exports.addPassiveEventListener = function addPassiveEventListener2(target, eventName, listener) {
      var listenerName = listener.name;
      if (!listenerName) {
        listenerName = eventName;
        console.warn("Listener must be a named function.");
      }
      if (!attachedListeners.has(eventName)) attachedListeners.set(eventName, /* @__PURE__ */ new Set());
      var listeners = attachedListeners.get(eventName);
      if (listeners.has(listenerName)) return;
      var supportsPassiveOption = function() {
        var supportsPassiveOption2 = false;
        try {
          var opts = Object.defineProperty({}, "passive", {
            get: function get() {
              supportsPassiveOption2 = true;
            }
          });
          window.addEventListener("test", null, opts);
        } catch (e14) {
        }
        return supportsPassiveOption2;
      }();
      target.addEventListener(eventName, listener, supportsPassiveOption ? { passive: true } : false);
      listeners.add(listenerName);
    };
    var removePassiveEventListener = exports.removePassiveEventListener = function removePassiveEventListener2(target, eventName, listener) {
      target.removeEventListener(eventName, listener);
      attachedListeners.get(eventName).delete(listener.name || eventName);
    };
    var attachedListeners = /* @__PURE__ */ new Map();
  }
});

// node_modules/react-scroll/modules/mixins/scroll-spy.js
var require_scroll_spy = __commonJS({
  "node_modules/react-scroll/modules/mixins/scroll-spy.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    var _lodash = require_lodash();
    var _lodash2 = _interopRequireDefault(_lodash);
    var _passiveEventListeners = require_passive_event_listeners();
    function _interopRequireDefault(obj) {
      return obj && obj.__esModule ? obj : { default: obj };
    }
    var eventThrottler = function eventThrottler2(eventHandler) {
      var throttleAmount = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 66;
      return (0, _lodash2.default)(eventHandler, throttleAmount);
    };
    var scrollSpy = {
      spyCallbacks: [],
      spySetState: [],
      scrollSpyContainers: [],
      mount: function mount(scrollSpyContainer, throttle2) {
        if (scrollSpyContainer) {
          var eventHandler = eventThrottler(function(event) {
            scrollSpy.scrollHandler(scrollSpyContainer);
          }, throttle2);
          scrollSpy.scrollSpyContainers.push(scrollSpyContainer);
          (0, _passiveEventListeners.addPassiveEventListener)(scrollSpyContainer, "scroll", eventHandler);
        }
      },
      isMounted: function isMounted(scrollSpyContainer) {
        return scrollSpy.scrollSpyContainers.indexOf(scrollSpyContainer) !== -1;
      },
      currentPositionX: function currentPositionX(scrollSpyContainer) {
        if (scrollSpyContainer === document) {
          var supportPageOffset = window.pageYOffset !== void 0;
          var isCSS1Compat = (document.compatMode || "") === "CSS1Compat";
          return supportPageOffset ? window.pageXOffset : isCSS1Compat ? document.documentElement.scrollLeft : document.body.scrollLeft;
        } else {
          return scrollSpyContainer.scrollLeft;
        }
      },
      currentPositionY: function currentPositionY(scrollSpyContainer) {
        if (scrollSpyContainer === document) {
          var supportPageOffset = window.pageXOffset !== void 0;
          var isCSS1Compat = (document.compatMode || "") === "CSS1Compat";
          return supportPageOffset ? window.pageYOffset : isCSS1Compat ? document.documentElement.scrollTop : document.body.scrollTop;
        } else {
          return scrollSpyContainer.scrollTop;
        }
      },
      scrollHandler: function scrollHandler(scrollSpyContainer) {
        var callbacks = scrollSpy.scrollSpyContainers[scrollSpy.scrollSpyContainers.indexOf(scrollSpyContainer)].spyCallbacks || [];
        callbacks.forEach(function(c15) {
          return c15(scrollSpy.currentPositionX(scrollSpyContainer), scrollSpy.currentPositionY(scrollSpyContainer));
        });
      },
      addStateHandler: function addStateHandler(handler2) {
        scrollSpy.spySetState.push(handler2);
      },
      addSpyHandler: function addSpyHandler(handler2, scrollSpyContainer) {
        var container4 = scrollSpy.scrollSpyContainers[scrollSpy.scrollSpyContainers.indexOf(scrollSpyContainer)];
        if (!container4.spyCallbacks) {
          container4.spyCallbacks = [];
        }
        container4.spyCallbacks.push(handler2);
        handler2(scrollSpy.currentPositionX(scrollSpyContainer), scrollSpy.currentPositionY(scrollSpyContainer));
      },
      updateStates: function updateStates() {
        scrollSpy.spySetState.forEach(function(s36) {
          return s36();
        });
      },
      unmount: function unmount(stateHandler, spyHandler) {
        scrollSpy.scrollSpyContainers.forEach(function(c15) {
          return c15.spyCallbacks && c15.spyCallbacks.length && c15.spyCallbacks.indexOf(spyHandler) > -1 && c15.spyCallbacks.splice(c15.spyCallbacks.indexOf(spyHandler), 1);
        });
        if (scrollSpy.spySetState && scrollSpy.spySetState.length && scrollSpy.spySetState.indexOf(stateHandler) > -1) {
          scrollSpy.spySetState.splice(scrollSpy.spySetState.indexOf(stateHandler), 1);
        }
        document.removeEventListener("scroll", scrollSpy.scrollHandler);
      },
      update: function update() {
        return scrollSpy.scrollSpyContainers.forEach(function(c15) {
          return scrollSpy.scrollHandler(c15);
        });
      }
    };
    exports.default = scrollSpy;
  }
});

// node_modules/react-scroll/modules/mixins/utils.js
var require_utils6 = __commonJS({
  "node_modules/react-scroll/modules/mixins/utils.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    var updateHash = function updateHash2(hash, historyUpdate) {
      var hashVal = hash.indexOf("#") === 0 ? hash.substring(1) : hash;
      var hashToUpdate = hashVal ? "#" + hashVal : "";
      var curLoc = window && window.location;
      var urlToPush = hashToUpdate ? curLoc.pathname + curLoc.search + hashToUpdate : curLoc.pathname + curLoc.search;
      historyUpdate ? history.pushState(history.state, "", urlToPush) : history.replaceState(history.state, "", urlToPush);
    };
    var getHash = function getHash2() {
      return window.location.hash.replace(/^#/, "");
    };
    var filterElementInContainer = function filterElementInContainer2(container4) {
      return function(element) {
        return container4.contains ? container4 != element && container4.contains(element) : !!(container4.compareDocumentPosition(element) & 16);
      };
    };
    var isPositioned = function isPositioned2(element) {
      return getComputedStyle(element).position !== "static";
    };
    var getElementOffsetInfoUntil = function getElementOffsetInfoUntil2(element, predicate) {
      var offsetTop = element.offsetTop;
      var currentOffsetParent = element.offsetParent;
      while (currentOffsetParent && !predicate(currentOffsetParent)) {
        offsetTop += currentOffsetParent.offsetTop;
        currentOffsetParent = currentOffsetParent.offsetParent;
      }
      return { offsetTop, offsetParent: currentOffsetParent };
    };
    var scrollOffset = function scrollOffset2(c15, t20, horizontal) {
      if (horizontal) {
        return c15 === document ? t20.getBoundingClientRect().left + (window.scrollX || window.pageXOffset) : getComputedStyle(c15).position !== "static" ? t20.offsetLeft : t20.offsetLeft - c15.offsetLeft;
      } else {
        if (c15 === document) {
          return t20.getBoundingClientRect().top + (window.scrollY || window.pageYOffset);
        }
        if (isPositioned(c15)) {
          if (t20.offsetParent !== c15) {
            var isContainerElementOrDocument = function isContainerElementOrDocument2(e14) {
              return e14 === c15 || e14 === document;
            };
            var _getElementOffsetInfo = getElementOffsetInfoUntil(t20, isContainerElementOrDocument), offsetTop = _getElementOffsetInfo.offsetTop, offsetParent = _getElementOffsetInfo.offsetParent;
            if (offsetParent !== c15) {
              throw new Error("Seems containerElement is not an ancestor of the Element");
            }
            return offsetTop;
          }
          return t20.offsetTop;
        }
        if (t20.offsetParent === c15.offsetParent) {
          return t20.offsetTop - c15.offsetTop;
        }
        var isDocument = function isDocument2(e14) {
          return e14 === document;
        };
        return getElementOffsetInfoUntil(t20, isDocument).offsetTop - getElementOffsetInfoUntil(c15, isDocument).offsetTop;
      }
    };
    exports.default = {
      updateHash,
      getHash,
      filterElementInContainer,
      scrollOffset
    };
  }
});

// node_modules/react-scroll/modules/mixins/smooth.js
var require_smooth = __commonJS({
  "node_modules/react-scroll/modules/mixins/smooth.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.default = {
      /*
       * https://github.com/oblador/angular-scroll (duScrollDefaultEasing)
       */
      defaultEasing: function defaultEasing2(x3) {
        if (x3 < 0.5) {
          return Math.pow(x3 * 2, 2) / 2;
        }
        return 1 - Math.pow((1 - x3) * 2, 2) / 2;
      },
      /*
       * https://gist.github.com/gre/1650294
       */
      // no easing, no acceleration
      linear: function linear(x3) {
        return x3;
      },
      // accelerating from zero velocity
      easeInQuad: function easeInQuad(x3) {
        return x3 * x3;
      },
      // decelerating to zero velocity
      easeOutQuad: function easeOutQuad(x3) {
        return x3 * (2 - x3);
      },
      // acceleration until halfway, then deceleration
      easeInOutQuad: function easeInOutQuad(x3) {
        return x3 < 0.5 ? 2 * x3 * x3 : -1 + (4 - 2 * x3) * x3;
      },
      // accelerating from zero velocity 
      easeInCubic: function easeInCubic(x3) {
        return x3 * x3 * x3;
      },
      // decelerating to zero velocity π
      easeOutCubic: function easeOutCubic(x3) {
        return --x3 * x3 * x3 + 1;
      },
      // acceleration until halfway, then deceleration 
      easeInOutCubic: function easeInOutCubic(x3) {
        return x3 < 0.5 ? 4 * x3 * x3 * x3 : (x3 - 1) * (2 * x3 - 2) * (2 * x3 - 2) + 1;
      },
      // accelerating from zero velocity 
      easeInQuart: function easeInQuart(x3) {
        return x3 * x3 * x3 * x3;
      },
      // decelerating to zero velocity 
      easeOutQuart: function easeOutQuart(x3) {
        return 1 - --x3 * x3 * x3 * x3;
      },
      // acceleration until halfway, then deceleration
      easeInOutQuart: function easeInOutQuart(x3) {
        return x3 < 0.5 ? 8 * x3 * x3 * x3 * x3 : 1 - 8 * --x3 * x3 * x3 * x3;
      },
      // accelerating from zero velocity
      easeInQuint: function easeInQuint(x3) {
        return x3 * x3 * x3 * x3 * x3;
      },
      // decelerating to zero velocity
      easeOutQuint: function easeOutQuint(x3) {
        return 1 + --x3 * x3 * x3 * x3 * x3;
      },
      // acceleration until halfway, then deceleration 
      easeInOutQuint: function easeInOutQuint(x3) {
        return x3 < 0.5 ? 16 * x3 * x3 * x3 * x3 * x3 : 1 + 16 * --x3 * x3 * x3 * x3 * x3;
      }
    };
  }
});

// node_modules/react-scroll/modules/mixins/cancel-events.js
var require_cancel_events = __commonJS({
  "node_modules/react-scroll/modules/mixins/cancel-events.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    var _passiveEventListeners = require_passive_event_listeners();
    var events = ["mousedown", "mousewheel", "touchmove", "keydown"];
    exports.default = {
      subscribe: function subscribe(cancelEvent) {
        return typeof document !== "undefined" && events.forEach(function(event) {
          return (0, _passiveEventListeners.addPassiveEventListener)(document, event, cancelEvent);
        });
      }
    };
  }
});

// node_modules/react-scroll/modules/mixins/scroll-events.js
var require_scroll_events = __commonJS({
  "node_modules/react-scroll/modules/mixins/scroll-events.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    var Events = {
      registered: {},
      scrollEvent: {
        register: function register(evtName, callback) {
          Events.registered[evtName] = callback;
        },
        remove: function remove(evtName) {
          Events.registered[evtName] = null;
        }
      }
    };
    exports.default = Events;
  }
});

// node_modules/react-scroll/modules/mixins/animate-scroll.js
var require_animate_scroll = __commonJS({
  "node_modules/react-scroll/modules/mixins/animate-scroll.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    var _extends = Object.assign || function(target) {
      for (var i13 = 1; i13 < arguments.length; i13++) {
        var source = arguments[i13];
        for (var key in source) {
          if (Object.prototype.hasOwnProperty.call(source, key)) {
            target[key] = source[key];
          }
        }
      }
      return target;
    };
    var _utils = require_utils6();
    var _utils2 = _interopRequireDefault(_utils);
    var _smooth = require_smooth();
    var _smooth2 = _interopRequireDefault(_smooth);
    var _cancelEvents = require_cancel_events();
    var _cancelEvents2 = _interopRequireDefault(_cancelEvents);
    var _scrollEvents = require_scroll_events();
    var _scrollEvents2 = _interopRequireDefault(_scrollEvents);
    function _interopRequireDefault(obj) {
      return obj && obj.__esModule ? obj : { default: obj };
    }
    var getAnimationType = function getAnimationType2(options) {
      return _smooth2.default[options.smooth] || _smooth2.default.defaultEasing;
    };
    var functionWrapper = function functionWrapper2(value) {
      return typeof value === "function" ? value : function() {
        return value;
      };
    };
    var currentWindowProperties = function currentWindowProperties2() {
      if (typeof window !== "undefined") {
        return window.requestAnimationFrame || window.webkitRequestAnimationFrame;
      }
    };
    var requestAnimationFrameHelper = function() {
      return currentWindowProperties() || function(callback, element, delay3) {
        window.setTimeout(callback, delay3 || 1e3 / 60, (/* @__PURE__ */ new Date()).getTime());
      };
    }();
    var makeData = function makeData2() {
      return {
        currentPosition: 0,
        startPosition: 0,
        targetPosition: 0,
        progress: 0,
        duration: 0,
        cancel: false,
        target: null,
        containerElement: null,
        to: null,
        start: null,
        delta: null,
        percent: null,
        delayTimeout: null
      };
    };
    var currentPositionX = function currentPositionX2(options) {
      var containerElement = options.data.containerElement;
      if (containerElement && containerElement !== document && containerElement !== document.body) {
        return containerElement.scrollLeft;
      } else {
        var supportPageOffset = window.pageXOffset !== void 0;
        var isCSS1Compat = (document.compatMode || "") === "CSS1Compat";
        return supportPageOffset ? window.pageXOffset : isCSS1Compat ? document.documentElement.scrollLeft : document.body.scrollLeft;
      }
    };
    var currentPositionY = function currentPositionY2(options) {
      var containerElement = options.data.containerElement;
      if (containerElement && containerElement !== document && containerElement !== document.body) {
        return containerElement.scrollTop;
      } else {
        var supportPageOffset = window.pageXOffset !== void 0;
        var isCSS1Compat = (document.compatMode || "") === "CSS1Compat";
        return supportPageOffset ? window.pageYOffset : isCSS1Compat ? document.documentElement.scrollTop : document.body.scrollTop;
      }
    };
    var scrollContainerWidth = function scrollContainerWidth2(options) {
      var containerElement = options.data.containerElement;
      if (containerElement && containerElement !== document && containerElement !== document.body) {
        return containerElement.scrollWidth - containerElement.offsetWidth;
      } else {
        var body = document.body;
        var html = document.documentElement;
        return Math.max(body.scrollWidth, body.offsetWidth, html.clientWidth, html.scrollWidth, html.offsetWidth);
      }
    };
    var scrollContainerHeight = function scrollContainerHeight2(options) {
      var containerElement = options.data.containerElement;
      if (containerElement && containerElement !== document && containerElement !== document.body) {
        return containerElement.scrollHeight - containerElement.offsetHeight;
      } else {
        var body = document.body;
        var html = document.documentElement;
        return Math.max(body.scrollHeight, body.offsetHeight, html.clientHeight, html.scrollHeight, html.offsetHeight);
      }
    };
    var animateScroll = function animateScroll2(easing, options, timestamp) {
      var data = options.data;
      if (!options.ignoreCancelEvents && data.cancel) {
        if (_scrollEvents2.default.registered["end"]) {
          _scrollEvents2.default.registered["end"](data.to, data.target, data.currentPositionY);
        }
        return;
      }
      ;
      data.delta = Math.round(data.targetPosition - data.startPosition);
      if (data.start === null) {
        data.start = timestamp;
      }
      data.progress = timestamp - data.start;
      data.percent = data.progress >= data.duration ? 1 : easing(data.progress / data.duration);
      data.currentPosition = data.startPosition + Math.ceil(data.delta * data.percent);
      if (data.containerElement && data.containerElement !== document && data.containerElement !== document.body) {
        if (options.horizontal) {
          data.containerElement.scrollLeft = data.currentPosition;
        } else {
          data.containerElement.scrollTop = data.currentPosition;
        }
      } else {
        if (options.horizontal) {
          window.scrollTo(data.currentPosition, 0);
        } else {
          window.scrollTo(0, data.currentPosition);
        }
      }
      if (data.percent < 1) {
        var easedAnimate = animateScroll2.bind(null, easing, options);
        requestAnimationFrameHelper.call(window, easedAnimate);
        return;
      }
      if (_scrollEvents2.default.registered["end"]) {
        _scrollEvents2.default.registered["end"](data.to, data.target, data.currentPosition);
      }
    };
    var setContainer = function setContainer2(options) {
      options.data.containerElement = !options ? null : options.containerId ? document.getElementById(options.containerId) : options.container && options.container.nodeType ? options.container : document;
    };
    var animateTopScroll = function animateTopScroll2(scrollOffset, options, to, target) {
      options.data = options.data || makeData();
      window.clearTimeout(options.data.delayTimeout);
      var setCancel = function setCancel2() {
        options.data.cancel = true;
      };
      _cancelEvents2.default.subscribe(setCancel);
      setContainer(options);
      options.data.start = null;
      options.data.cancel = false;
      options.data.startPosition = options.horizontal ? currentPositionX(options) : currentPositionY(options);
      options.data.targetPosition = options.absolute ? scrollOffset : scrollOffset + options.data.startPosition;
      if (options.data.startPosition === options.data.targetPosition) {
        if (_scrollEvents2.default.registered["end"]) {
          _scrollEvents2.default.registered["end"](options.data.to, options.data.target, options.data.currentPosition);
        }
        return;
      }
      options.data.delta = Math.round(options.data.targetPosition - options.data.startPosition);
      options.data.duration = functionWrapper(options.duration)(options.data.delta);
      options.data.duration = isNaN(parseFloat(options.data.duration)) ? 1e3 : parseFloat(options.data.duration);
      options.data.to = to;
      options.data.target = target;
      var easing = getAnimationType(options);
      var easedAnimate = animateScroll.bind(null, easing, options);
      if (options && options.delay > 0) {
        options.data.delayTimeout = window.setTimeout(function() {
          if (_scrollEvents2.default.registered["begin"]) {
            _scrollEvents2.default.registered["begin"](options.data.to, options.data.target);
          }
          requestAnimationFrameHelper.call(window, easedAnimate);
        }, options.delay);
        return;
      }
      if (_scrollEvents2.default.registered["begin"]) {
        _scrollEvents2.default.registered["begin"](options.data.to, options.data.target);
      }
      requestAnimationFrameHelper.call(window, easedAnimate);
    };
    var proceedOptions = function proceedOptions2(options) {
      options = _extends({}, options);
      options.data = options.data || makeData();
      options.absolute = true;
      return options;
    };
    var scrollToTop = function scrollToTop2(options) {
      animateTopScroll(0, proceedOptions(options));
    };
    var scrollTo = function scrollTo2(toPosition, options) {
      animateTopScroll(toPosition, proceedOptions(options));
    };
    var scrollToBottom = function scrollToBottom2(options) {
      options = proceedOptions(options);
      setContainer(options);
      animateTopScroll(options.horizontal ? scrollContainerWidth(options) : scrollContainerHeight(options), options);
    };
    var scrollMore = function scrollMore2(toPosition, options) {
      options = proceedOptions(options);
      setContainer(options);
      var currentPosition = options.horizontal ? currentPositionX(options) : currentPositionY(options);
      animateTopScroll(toPosition + currentPosition, options);
    };
    exports.default = {
      animateTopScroll,
      getAnimationType,
      scrollToTop,
      scrollToBottom,
      scrollTo,
      scrollMore
    };
  }
});

// node_modules/react-scroll/modules/mixins/scroller.js
var require_scroller = __commonJS({
  "node_modules/react-scroll/modules/mixins/scroller.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    var _extends = Object.assign || function(target) {
      for (var i13 = 1; i13 < arguments.length; i13++) {
        var source = arguments[i13];
        for (var key in source) {
          if (Object.prototype.hasOwnProperty.call(source, key)) {
            target[key] = source[key];
          }
        }
      }
      return target;
    };
    var _utils = require_utils6();
    var _utils2 = _interopRequireDefault(_utils);
    var _animateScroll = require_animate_scroll();
    var _animateScroll2 = _interopRequireDefault(_animateScroll);
    var _scrollEvents = require_scroll_events();
    var _scrollEvents2 = _interopRequireDefault(_scrollEvents);
    function _interopRequireDefault(obj) {
      return obj && obj.__esModule ? obj : { default: obj };
    }
    var __mapped = {};
    var __activeLink = void 0;
    exports.default = {
      unmount: function unmount() {
        __mapped = {};
      },
      register: function register(name2, element) {
        __mapped[name2] = element;
      },
      unregister: function unregister(name2) {
        delete __mapped[name2];
      },
      get: function get(name2) {
        return __mapped[name2] || document.getElementById(name2) || document.getElementsByName(name2)[0] || document.getElementsByClassName(name2)[0];
      },
      setActiveLink: function setActiveLink(link2) {
        return __activeLink = link2;
      },
      getActiveLink: function getActiveLink() {
        return __activeLink;
      },
      scrollTo: function scrollTo(to, props) {
        var target = this.get(to);
        if (!target) {
          console.warn("target Element not found");
          return;
        }
        props = _extends({}, props, { absolute: false });
        var containerId = props.containerId;
        var container4 = props.container;
        var containerElement = void 0;
        if (containerId) {
          containerElement = document.getElementById(containerId);
        } else if (container4 && container4.nodeType) {
          containerElement = container4;
        } else {
          containerElement = document;
        }
        props.absolute = true;
        var horizontal = props.horizontal;
        var scrollOffset = _utils2.default.scrollOffset(containerElement, target, horizontal) + (props.offset || 0);
        if (!props.smooth) {
          if (_scrollEvents2.default.registered["begin"]) {
            _scrollEvents2.default.registered["begin"](to, target);
          }
          if (containerElement === document) {
            if (props.horizontal) {
              window.scrollTo(scrollOffset, 0);
            } else {
              window.scrollTo(0, scrollOffset);
            }
          } else {
            containerElement.scrollTop = scrollOffset;
          }
          if (_scrollEvents2.default.registered["end"]) {
            _scrollEvents2.default.registered["end"](to, target);
          }
          return;
        }
        _animateScroll2.default.animateTopScroll(scrollOffset, props, to, target);
      }
    };
  }
});

// node_modules/react-scroll/modules/mixins/scroll-hash.js
var require_scroll_hash = __commonJS({
  "node_modules/react-scroll/modules/mixins/scroll-hash.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    var _passiveEventListeners = require_passive_event_listeners();
    var _utils = require_utils6();
    var _utils2 = _interopRequireDefault(_utils);
    function _interopRequireDefault(obj) {
      return obj && obj.__esModule ? obj : { default: obj };
    }
    var scrollHash = {
      mountFlag: false,
      initialized: false,
      scroller: null,
      containers: {},
      mount: function mount(scroller) {
        this.scroller = scroller;
        this.handleHashChange = this.handleHashChange.bind(this);
        window.addEventListener("hashchange", this.handleHashChange);
        this.initStateFromHash();
        this.mountFlag = true;
      },
      mapContainer: function mapContainer(to, container4) {
        this.containers[to] = container4;
      },
      isMounted: function isMounted() {
        return this.mountFlag;
      },
      isInitialized: function isInitialized() {
        return this.initialized;
      },
      initStateFromHash: function initStateFromHash() {
        var _this = this;
        var hash = this.getHash();
        if (hash) {
          window.setTimeout(function() {
            _this.scrollTo(hash, true);
            _this.initialized = true;
          }, 10);
        } else {
          this.initialized = true;
        }
      },
      scrollTo: function scrollTo(to, isInit) {
        var scroller = this.scroller;
        var element = scroller.get(to);
        if (element && (isInit || to !== scroller.getActiveLink())) {
          var container4 = this.containers[to] || document;
          scroller.scrollTo(to, { container: container4 });
        }
      },
      getHash: function getHash() {
        return _utils2.default.getHash();
      },
      changeHash: function changeHash(to, saveHashHistory) {
        if (this.isInitialized() && _utils2.default.getHash() !== to) {
          _utils2.default.updateHash(to, saveHashHistory);
        }
      },
      handleHashChange: function handleHashChange() {
        this.scrollTo(this.getHash());
      },
      unmount: function unmount() {
        this.scroller = null;
        this.containers = null;
        window.removeEventListener("hashchange", this.handleHashChange);
      }
    };
    exports.default = scrollHash;
  }
});

// node_modules/react-scroll/modules/mixins/scroll-link.js
var require_scroll_link = __commonJS({
  "node_modules/react-scroll/modules/mixins/scroll-link.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    var _extends = Object.assign || function(target) {
      for (var i13 = 1; i13 < arguments.length; i13++) {
        var source = arguments[i13];
        for (var key in source) {
          if (Object.prototype.hasOwnProperty.call(source, key)) {
            target[key] = source[key];
          }
        }
      }
      return target;
    };
    var _createClass = /* @__PURE__ */ function() {
      function defineProperties(target, props) {
        for (var i13 = 0; i13 < props.length; i13++) {
          var descriptor = props[i13];
          descriptor.enumerable = descriptor.enumerable || false;
          descriptor.configurable = true;
          if ("value" in descriptor) descriptor.writable = true;
          Object.defineProperty(target, descriptor.key, descriptor);
        }
      }
      return function(Constructor, protoProps, staticProps) {
        if (protoProps) defineProperties(Constructor.prototype, protoProps);
        if (staticProps) defineProperties(Constructor, staticProps);
        return Constructor;
      };
    }();
    var _react = require_react();
    var _react2 = _interopRequireDefault(_react);
    var _scrollSpy = require_scroll_spy();
    var _scrollSpy2 = _interopRequireDefault(_scrollSpy);
    var _scroller = require_scroller();
    var _scroller2 = _interopRequireDefault(_scroller);
    var _propTypes = require_prop_types();
    var _propTypes2 = _interopRequireDefault(_propTypes);
    var _scrollHash = require_scroll_hash();
    var _scrollHash2 = _interopRequireDefault(_scrollHash);
    function _interopRequireDefault(obj) {
      return obj && obj.__esModule ? obj : { default: obj };
    }
    function _classCallCheck(instance, Constructor) {
      if (!(instance instanceof Constructor)) {
        throw new TypeError("Cannot call a class as a function");
      }
    }
    function _possibleConstructorReturn(self2, call) {
      if (!self2) {
        throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
      }
      return call && (typeof call === "object" || typeof call === "function") ? call : self2;
    }
    function _inherits(subClass, superClass) {
      if (typeof superClass !== "function" && superClass !== null) {
        throw new TypeError("Super expression must either be null or a function, not " + typeof superClass);
      }
      subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } });
      if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass;
    }
    var protoTypes = {
      to: _propTypes2.default.string.isRequired,
      containerId: _propTypes2.default.string,
      container: _propTypes2.default.object,
      activeClass: _propTypes2.default.string,
      activeStyle: _propTypes2.default.object,
      spy: _propTypes2.default.bool,
      horizontal: _propTypes2.default.bool,
      smooth: _propTypes2.default.oneOfType([_propTypes2.default.bool, _propTypes2.default.string]),
      offset: _propTypes2.default.number,
      delay: _propTypes2.default.number,
      isDynamic: _propTypes2.default.bool,
      onClick: _propTypes2.default.func,
      duration: _propTypes2.default.oneOfType([_propTypes2.default.number, _propTypes2.default.func]),
      absolute: _propTypes2.default.bool,
      onSetActive: _propTypes2.default.func,
      onSetInactive: _propTypes2.default.func,
      ignoreCancelEvents: _propTypes2.default.bool,
      hashSpy: _propTypes2.default.bool,
      saveHashHistory: _propTypes2.default.bool,
      spyThrottle: _propTypes2.default.number
    };
    exports.default = function(Component2, customScroller) {
      var scroller = customScroller || _scroller2.default;
      var Link2 = function(_React$PureComponent) {
        _inherits(Link3, _React$PureComponent);
        function Link3(props) {
          _classCallCheck(this, Link3);
          var _this = _possibleConstructorReturn(this, (Link3.__proto__ || Object.getPrototypeOf(Link3)).call(this, props));
          _initialiseProps.call(_this);
          _this.state = {
            active: false
          };
          return _this;
        }
        _createClass(Link3, [{
          key: "getScrollSpyContainer",
          value: function getScrollSpyContainer() {
            var containerId = this.props.containerId;
            var container4 = this.props.container;
            if (containerId && !container4) {
              return document.getElementById(containerId);
            }
            if (container4 && container4.nodeType) {
              return container4;
            }
            return document;
          }
        }, {
          key: "componentDidMount",
          value: function componentDidMount() {
            if (this.props.spy || this.props.hashSpy) {
              var scrollSpyContainer = this.getScrollSpyContainer();
              if (!_scrollSpy2.default.isMounted(scrollSpyContainer)) {
                _scrollSpy2.default.mount(scrollSpyContainer, this.props.spyThrottle);
              }
              if (this.props.hashSpy) {
                if (!_scrollHash2.default.isMounted()) {
                  _scrollHash2.default.mount(scroller);
                }
                _scrollHash2.default.mapContainer(this.props.to, scrollSpyContainer);
              }
              _scrollSpy2.default.addSpyHandler(this.spyHandler, scrollSpyContainer);
              this.setState({
                container: scrollSpyContainer
              });
            }
          }
        }, {
          key: "componentWillUnmount",
          value: function componentWillUnmount() {
            _scrollSpy2.default.unmount(this.stateHandler, this.spyHandler);
          }
        }, {
          key: "render",
          value: function render() {
            var className = "";
            if (this.state && this.state.active) {
              className = ((this.props.className || "") + " " + (this.props.activeClass || "active")).trim();
            } else {
              className = this.props.className;
            }
            var style = {};
            if (this.state && this.state.active) {
              style = _extends({}, this.props.style, this.props.activeStyle);
            } else {
              style = _extends({}, this.props.style);
            }
            var props = _extends({}, this.props);
            for (var prop in protoTypes) {
              if (props.hasOwnProperty(prop)) {
                delete props[prop];
              }
            }
            props.className = className;
            props.style = style;
            props.onClick = this.handleClick;
            return _react2.default.createElement(Component2, props);
          }
        }]);
        return Link3;
      }(_react2.default.PureComponent);
      var _initialiseProps = function _initialiseProps2() {
        var _this2 = this;
        this.scrollTo = function(to, props) {
          scroller.scrollTo(to, _extends({}, _this2.state, props));
        };
        this.handleClick = function(event) {
          if (_this2.props.onClick) {
            _this2.props.onClick(event);
          }
          if (event.stopPropagation) event.stopPropagation();
          if (event.preventDefault) event.preventDefault();
          _this2.scrollTo(_this2.props.to, _this2.props);
        };
        this.spyHandler = function(x3, y4) {
          var scrollSpyContainer = _this2.getScrollSpyContainer();
          if (_scrollHash2.default.isMounted() && !_scrollHash2.default.isInitialized()) {
            return;
          }
          var horizontal = _this2.props.horizontal;
          var to = _this2.props.to;
          var element = null;
          var isInside = void 0;
          var isOutside = void 0;
          if (horizontal) {
            var elemLeftBound = 0;
            var elemRightBound = 0;
            var containerLeft = 0;
            if (scrollSpyContainer.getBoundingClientRect) {
              var containerCords = scrollSpyContainer.getBoundingClientRect();
              containerLeft = containerCords.left;
            }
            if (!element || _this2.props.isDynamic) {
              element = scroller.get(to);
              if (!element) {
                return;
              }
              var cords = element.getBoundingClientRect();
              elemLeftBound = cords.left - containerLeft + x3;
              elemRightBound = elemLeftBound + cords.width;
            }
            var offsetX = x3 - _this2.props.offset;
            isInside = offsetX >= Math.floor(elemLeftBound) && offsetX < Math.floor(elemRightBound);
            isOutside = offsetX < Math.floor(elemLeftBound) || offsetX >= Math.floor(elemRightBound);
          } else {
            var elemTopBound = 0;
            var elemBottomBound = 0;
            var containerTop = 0;
            if (scrollSpyContainer.getBoundingClientRect) {
              var _containerCords = scrollSpyContainer.getBoundingClientRect();
              containerTop = _containerCords.top;
            }
            if (!element || _this2.props.isDynamic) {
              element = scroller.get(to);
              if (!element) {
                return;
              }
              var _cords = element.getBoundingClientRect();
              elemTopBound = _cords.top - containerTop + y4;
              elemBottomBound = elemTopBound + _cords.height;
            }
            var offsetY = y4 - _this2.props.offset;
            isInside = offsetY >= Math.floor(elemTopBound) && offsetY < Math.floor(elemBottomBound);
            isOutside = offsetY < Math.floor(elemTopBound) || offsetY >= Math.floor(elemBottomBound);
          }
          var activeLink = scroller.getActiveLink();
          if (isOutside) {
            if (to === activeLink) {
              scroller.setActiveLink(void 0);
            }
            if (_this2.props.hashSpy && _scrollHash2.default.getHash() === to) {
              var _props$saveHashHistor = _this2.props.saveHashHistory, saveHashHistory = _props$saveHashHistor === void 0 ? false : _props$saveHashHistor;
              _scrollHash2.default.changeHash("", saveHashHistory);
            }
            if (_this2.props.spy && _this2.state.active) {
              _this2.setState({ active: false });
              _this2.props.onSetInactive && _this2.props.onSetInactive(to, element);
            }
          }
          if (isInside && (activeLink !== to || _this2.state.active === false)) {
            scroller.setActiveLink(to);
            var _props$saveHashHistor2 = _this2.props.saveHashHistory, _saveHashHistory = _props$saveHashHistor2 === void 0 ? false : _props$saveHashHistor2;
            _this2.props.hashSpy && _scrollHash2.default.changeHash(to, _saveHashHistory);
            if (_this2.props.spy) {
              _this2.setState({ active: true });
              _this2.props.onSetActive && _this2.props.onSetActive(to, element);
            }
          }
        };
      };
      ;
      Link2.propTypes = protoTypes;
      Link2.defaultProps = { offset: 0 };
      return Link2;
    };
  }
});

// node_modules/react-scroll/modules/components/Link.js
var require_Link = __commonJS({
  "node_modules/react-scroll/modules/components/Link.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    var _react = require_react();
    var _react2 = _interopRequireDefault(_react);
    var _scrollLink = require_scroll_link();
    var _scrollLink2 = _interopRequireDefault(_scrollLink);
    function _interopRequireDefault(obj) {
      return obj && obj.__esModule ? obj : { default: obj };
    }
    function _classCallCheck(instance, Constructor) {
      if (!(instance instanceof Constructor)) {
        throw new TypeError("Cannot call a class as a function");
      }
    }
    function _possibleConstructorReturn(self2, call) {
      if (!self2) {
        throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
      }
      return call && (typeof call === "object" || typeof call === "function") ? call : self2;
    }
    function _inherits(subClass, superClass) {
      if (typeof superClass !== "function" && superClass !== null) {
        throw new TypeError("Super expression must either be null or a function, not " + typeof superClass);
      }
      subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } });
      if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass;
    }
    var LinkElement = function(_React$Component) {
      _inherits(LinkElement2, _React$Component);
      function LinkElement2() {
        var _ref;
        var _temp, _this, _ret;
        _classCallCheck(this, LinkElement2);
        for (var _len = arguments.length, args = Array(_len), _key = 0; _key < _len; _key++) {
          args[_key] = arguments[_key];
        }
        return _ret = (_temp = (_this = _possibleConstructorReturn(this, (_ref = LinkElement2.__proto__ || Object.getPrototypeOf(LinkElement2)).call.apply(_ref, [this].concat(args))), _this), _this.render = function() {
          return _react2.default.createElement(
            "a",
            _this.props,
            _this.props.children
          );
        }, _temp), _possibleConstructorReturn(_this, _ret);
      }
      return LinkElement2;
    }(_react2.default.Component);
    exports.default = (0, _scrollLink2.default)(LinkElement);
  }
});

// node_modules/react-scroll/modules/components/Button.js
var require_Button = __commonJS({
  "node_modules/react-scroll/modules/components/Button.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    var _createClass = /* @__PURE__ */ function() {
      function defineProperties(target, props) {
        for (var i13 = 0; i13 < props.length; i13++) {
          var descriptor = props[i13];
          descriptor.enumerable = descriptor.enumerable || false;
          descriptor.configurable = true;
          if ("value" in descriptor) descriptor.writable = true;
          Object.defineProperty(target, descriptor.key, descriptor);
        }
      }
      return function(Constructor, protoProps, staticProps) {
        if (protoProps) defineProperties(Constructor.prototype, protoProps);
        if (staticProps) defineProperties(Constructor, staticProps);
        return Constructor;
      };
    }();
    var _react = require_react();
    var _react2 = _interopRequireDefault(_react);
    var _scrollLink = require_scroll_link();
    var _scrollLink2 = _interopRequireDefault(_scrollLink);
    function _interopRequireDefault(obj) {
      return obj && obj.__esModule ? obj : { default: obj };
    }
    function _classCallCheck(instance, Constructor) {
      if (!(instance instanceof Constructor)) {
        throw new TypeError("Cannot call a class as a function");
      }
    }
    function _possibleConstructorReturn(self2, call) {
      if (!self2) {
        throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
      }
      return call && (typeof call === "object" || typeof call === "function") ? call : self2;
    }
    function _inherits(subClass, superClass) {
      if (typeof superClass !== "function" && superClass !== null) {
        throw new TypeError("Super expression must either be null or a function, not " + typeof superClass);
      }
      subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } });
      if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass;
    }
    var ButtonElement = function(_React$Component) {
      _inherits(ButtonElement2, _React$Component);
      function ButtonElement2() {
        _classCallCheck(this, ButtonElement2);
        return _possibleConstructorReturn(this, (ButtonElement2.__proto__ || Object.getPrototypeOf(ButtonElement2)).apply(this, arguments));
      }
      _createClass(ButtonElement2, [{
        key: "render",
        value: function render() {
          return _react2.default.createElement(
            "button",
            this.props,
            this.props.children
          );
        }
      }]);
      return ButtonElement2;
    }(_react2.default.Component);
    exports.default = (0, _scrollLink2.default)(ButtonElement);
  }
});

// node_modules/react-scroll/modules/mixins/scroll-element.js
var require_scroll_element = __commonJS({
  "node_modules/react-scroll/modules/mixins/scroll-element.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    var _extends = Object.assign || function(target) {
      for (var i13 = 1; i13 < arguments.length; i13++) {
        var source = arguments[i13];
        for (var key in source) {
          if (Object.prototype.hasOwnProperty.call(source, key)) {
            target[key] = source[key];
          }
        }
      }
      return target;
    };
    var _createClass = /* @__PURE__ */ function() {
      function defineProperties(target, props) {
        for (var i13 = 0; i13 < props.length; i13++) {
          var descriptor = props[i13];
          descriptor.enumerable = descriptor.enumerable || false;
          descriptor.configurable = true;
          if ("value" in descriptor) descriptor.writable = true;
          Object.defineProperty(target, descriptor.key, descriptor);
        }
      }
      return function(Constructor, protoProps, staticProps) {
        if (protoProps) defineProperties(Constructor.prototype, protoProps);
        if (staticProps) defineProperties(Constructor, staticProps);
        return Constructor;
      };
    }();
    var _react = require_react();
    var _react2 = _interopRequireDefault(_react);
    var _reactDom = require_react_dom();
    var _reactDom2 = _interopRequireDefault(_reactDom);
    var _scroller = require_scroller();
    var _scroller2 = _interopRequireDefault(_scroller);
    var _propTypes = require_prop_types();
    var _propTypes2 = _interopRequireDefault(_propTypes);
    function _interopRequireDefault(obj) {
      return obj && obj.__esModule ? obj : { default: obj };
    }
    function _classCallCheck(instance, Constructor) {
      if (!(instance instanceof Constructor)) {
        throw new TypeError("Cannot call a class as a function");
      }
    }
    function _possibleConstructorReturn(self2, call) {
      if (!self2) {
        throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
      }
      return call && (typeof call === "object" || typeof call === "function") ? call : self2;
    }
    function _inherits(subClass, superClass) {
      if (typeof superClass !== "function" && superClass !== null) {
        throw new TypeError("Super expression must either be null or a function, not " + typeof superClass);
      }
      subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } });
      if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass;
    }
    exports.default = function(Component2) {
      var Element3 = function(_React$Component) {
        _inherits(Element4, _React$Component);
        function Element4(props) {
          _classCallCheck(this, Element4);
          var _this = _possibleConstructorReturn(this, (Element4.__proto__ || Object.getPrototypeOf(Element4)).call(this, props));
          _this.childBindings = {
            domNode: null
          };
          return _this;
        }
        _createClass(Element4, [{
          key: "componentDidMount",
          value: function componentDidMount() {
            if (typeof window === "undefined") {
              return false;
            }
            this.registerElems(this.props.name);
          }
        }, {
          key: "componentDidUpdate",
          value: function componentDidUpdate(prevProps) {
            if (this.props.name !== prevProps.name) {
              this.registerElems(this.props.name);
            }
          }
        }, {
          key: "componentWillUnmount",
          value: function componentWillUnmount() {
            if (typeof window === "undefined") {
              return false;
            }
            _scroller2.default.unregister(this.props.name);
          }
        }, {
          key: "registerElems",
          value: function registerElems(name2) {
            _scroller2.default.register(name2, this.childBindings.domNode);
          }
        }, {
          key: "render",
          value: function render() {
            return _react2.default.createElement(Component2, _extends({}, this.props, { parentBindings: this.childBindings }));
          }
        }]);
        return Element4;
      }(_react2.default.Component);
      ;
      Element3.propTypes = {
        name: _propTypes2.default.string,
        id: _propTypes2.default.string
      };
      return Element3;
    };
  }
});

// node_modules/react-scroll/modules/components/Element.js
var require_Element = __commonJS({
  "node_modules/react-scroll/modules/components/Element.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    var _extends = Object.assign || function(target) {
      for (var i13 = 1; i13 < arguments.length; i13++) {
        var source = arguments[i13];
        for (var key in source) {
          if (Object.prototype.hasOwnProperty.call(source, key)) {
            target[key] = source[key];
          }
        }
      }
      return target;
    };
    var _createClass = /* @__PURE__ */ function() {
      function defineProperties(target, props) {
        for (var i13 = 0; i13 < props.length; i13++) {
          var descriptor = props[i13];
          descriptor.enumerable = descriptor.enumerable || false;
          descriptor.configurable = true;
          if ("value" in descriptor) descriptor.writable = true;
          Object.defineProperty(target, descriptor.key, descriptor);
        }
      }
      return function(Constructor, protoProps, staticProps) {
        if (protoProps) defineProperties(Constructor.prototype, protoProps);
        if (staticProps) defineProperties(Constructor, staticProps);
        return Constructor;
      };
    }();
    var _react = require_react();
    var _react2 = _interopRequireDefault(_react);
    var _scrollElement = require_scroll_element();
    var _scrollElement2 = _interopRequireDefault(_scrollElement);
    var _propTypes = require_prop_types();
    var _propTypes2 = _interopRequireDefault(_propTypes);
    function _interopRequireDefault(obj) {
      return obj && obj.__esModule ? obj : { default: obj };
    }
    function _classCallCheck(instance, Constructor) {
      if (!(instance instanceof Constructor)) {
        throw new TypeError("Cannot call a class as a function");
      }
    }
    function _possibleConstructorReturn(self2, call) {
      if (!self2) {
        throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
      }
      return call && (typeof call === "object" || typeof call === "function") ? call : self2;
    }
    function _inherits(subClass, superClass) {
      if (typeof superClass !== "function" && superClass !== null) {
        throw new TypeError("Super expression must either be null or a function, not " + typeof superClass);
      }
      subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } });
      if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass;
    }
    var ElementWrapper = function(_React$Component) {
      _inherits(ElementWrapper2, _React$Component);
      function ElementWrapper2() {
        _classCallCheck(this, ElementWrapper2);
        return _possibleConstructorReturn(this, (ElementWrapper2.__proto__ || Object.getPrototypeOf(ElementWrapper2)).apply(this, arguments));
      }
      _createClass(ElementWrapper2, [{
        key: "render",
        value: function render() {
          var _this2 = this;
          var newProps = _extends({}, this.props);
          delete newProps.name;
          if (newProps.parentBindings) {
            delete newProps.parentBindings;
          }
          return _react2.default.createElement(
            "div",
            _extends({}, newProps, { ref: function ref(el2) {
              _this2.props.parentBindings.domNode = el2;
            } }),
            this.props.children
          );
        }
      }]);
      return ElementWrapper2;
    }(_react2.default.Component);
    ElementWrapper.propTypes = {
      name: _propTypes2.default.string,
      id: _propTypes2.default.string
    };
    exports.default = (0, _scrollElement2.default)(ElementWrapper);
  }
});

// node_modules/react-scroll/modules/mixins/Helpers.js
var require_Helpers = __commonJS({
  "node_modules/react-scroll/modules/mixins/Helpers.js"(exports, module) {
    "use strict";
    var _extends = Object.assign || function(target) {
      for (var i13 = 1; i13 < arguments.length; i13++) {
        var source = arguments[i13];
        for (var key in source) {
          if (Object.prototype.hasOwnProperty.call(source, key)) {
            target[key] = source[key];
          }
        }
      }
      return target;
    };
    var _createClass = /* @__PURE__ */ function() {
      function defineProperties(target, props) {
        for (var i13 = 0; i13 < props.length; i13++) {
          var descriptor = props[i13];
          descriptor.enumerable = descriptor.enumerable || false;
          descriptor.configurable = true;
          if ("value" in descriptor) descriptor.writable = true;
          Object.defineProperty(target, descriptor.key, descriptor);
        }
      }
      return function(Constructor, protoProps, staticProps) {
        if (protoProps) defineProperties(Constructor.prototype, protoProps);
        if (staticProps) defineProperties(Constructor, staticProps);
        return Constructor;
      };
    }();
    function _classCallCheck(instance, Constructor) {
      if (!(instance instanceof Constructor)) {
        throw new TypeError("Cannot call a class as a function");
      }
    }
    function _possibleConstructorReturn(self2, call) {
      if (!self2) {
        throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
      }
      return call && (typeof call === "object" || typeof call === "function") ? call : self2;
    }
    function _inherits(subClass, superClass) {
      if (typeof superClass !== "function" && superClass !== null) {
        throw new TypeError("Super expression must either be null or a function, not " + typeof superClass);
      }
      subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } });
      if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass;
    }
    var React10 = require_react();
    var ReactDOM4 = require_react_dom();
    var utils = require_utils6();
    var scrollSpy = require_scroll_spy();
    var defaultScroller = require_scroller();
    var PropTypes = require_prop_types();
    var scrollHash = require_scroll_hash();
    var protoTypes = {
      to: PropTypes.string.isRequired,
      containerId: PropTypes.string,
      container: PropTypes.object,
      activeClass: PropTypes.string,
      spy: PropTypes.bool,
      smooth: PropTypes.oneOfType([PropTypes.bool, PropTypes.string]),
      offset: PropTypes.number,
      delay: PropTypes.number,
      isDynamic: PropTypes.bool,
      onClick: PropTypes.func,
      duration: PropTypes.oneOfType([PropTypes.number, PropTypes.func]),
      absolute: PropTypes.bool,
      onSetActive: PropTypes.func,
      onSetInactive: PropTypes.func,
      ignoreCancelEvents: PropTypes.bool,
      hashSpy: PropTypes.bool,
      spyThrottle: PropTypes.number
    };
    var Helpers = {
      Scroll: function Scroll(Component2, customScroller) {
        console.warn("Helpers.Scroll is deprecated since v1.7.0");
        var scroller = customScroller || defaultScroller;
        var Scroll2 = function(_React$Component) {
          _inherits(Scroll3, _React$Component);
          function Scroll3(props) {
            _classCallCheck(this, Scroll3);
            var _this = _possibleConstructorReturn(this, (Scroll3.__proto__ || Object.getPrototypeOf(Scroll3)).call(this, props));
            _initialiseProps.call(_this);
            _this.state = {
              active: false
            };
            return _this;
          }
          _createClass(Scroll3, [{
            key: "getScrollSpyContainer",
            value: function getScrollSpyContainer() {
              var containerId = this.props.containerId;
              var container4 = this.props.container;
              if (containerId) {
                return document.getElementById(containerId);
              }
              if (container4 && container4.nodeType) {
                return container4;
              }
              return document;
            }
          }, {
            key: "componentDidMount",
            value: function componentDidMount() {
              if (this.props.spy || this.props.hashSpy) {
                var scrollSpyContainer = this.getScrollSpyContainer();
                if (!scrollSpy.isMounted(scrollSpyContainer)) {
                  scrollSpy.mount(scrollSpyContainer, this.props.spyThrottle);
                }
                if (this.props.hashSpy) {
                  if (!scrollHash.isMounted()) {
                    scrollHash.mount(scroller);
                  }
                  scrollHash.mapContainer(this.props.to, scrollSpyContainer);
                }
                if (this.props.spy) {
                  scrollSpy.addStateHandler(this.stateHandler);
                }
                scrollSpy.addSpyHandler(this.spyHandler, scrollSpyContainer);
                this.setState({
                  container: scrollSpyContainer
                });
              }
            }
          }, {
            key: "componentWillUnmount",
            value: function componentWillUnmount() {
              scrollSpy.unmount(this.stateHandler, this.spyHandler);
            }
          }, {
            key: "render",
            value: function render() {
              var className = "";
              if (this.state && this.state.active) {
                className = ((this.props.className || "") + " " + (this.props.activeClass || "active")).trim();
              } else {
                className = this.props.className;
              }
              var props = _extends({}, this.props);
              for (var prop in protoTypes) {
                if (props.hasOwnProperty(prop)) {
                  delete props[prop];
                }
              }
              props.className = className;
              props.onClick = this.handleClick;
              return React10.createElement(Component2, props);
            }
          }]);
          return Scroll3;
        }(React10.Component);
        var _initialiseProps = function _initialiseProps2() {
          var _this2 = this;
          this.scrollTo = function(to, props) {
            scroller.scrollTo(to, _extends({}, _this2.state, props));
          };
          this.handleClick = function(event) {
            if (_this2.props.onClick) {
              _this2.props.onClick(event);
            }
            if (event.stopPropagation) event.stopPropagation();
            if (event.preventDefault) event.preventDefault();
            _this2.scrollTo(_this2.props.to, _this2.props);
          };
          this.stateHandler = function() {
            if (scroller.getActiveLink() !== _this2.props.to) {
              if (_this2.state !== null && _this2.state.active && _this2.props.onSetInactive) {
                _this2.props.onSetInactive();
              }
              _this2.setState({ active: false });
            }
          };
          this.spyHandler = function(y4) {
            var scrollSpyContainer = _this2.getScrollSpyContainer();
            if (scrollHash.isMounted() && !scrollHash.isInitialized()) {
              return;
            }
            var to = _this2.props.to;
            var element = null;
            var elemTopBound = 0;
            var elemBottomBound = 0;
            var containerTop = 0;
            if (scrollSpyContainer.getBoundingClientRect) {
              var containerCords = scrollSpyContainer.getBoundingClientRect();
              containerTop = containerCords.top;
            }
            if (!element || _this2.props.isDynamic) {
              element = scroller.get(to);
              if (!element) {
                return;
              }
              var cords = element.getBoundingClientRect();
              elemTopBound = cords.top - containerTop + y4;
              elemBottomBound = elemTopBound + cords.height;
            }
            var offsetY = y4 - _this2.props.offset;
            var isInside = offsetY >= Math.floor(elemTopBound) && offsetY < Math.floor(elemBottomBound);
            var isOutside = offsetY < Math.floor(elemTopBound) || offsetY >= Math.floor(elemBottomBound);
            var activeLink = scroller.getActiveLink();
            if (isOutside) {
              if (to === activeLink) {
                scroller.setActiveLink(void 0);
              }
              if (_this2.props.hashSpy && scrollHash.getHash() === to) {
                scrollHash.changeHash();
              }
              if (_this2.props.spy && _this2.state.active) {
                _this2.setState({ active: false });
                _this2.props.onSetInactive && _this2.props.onSetInactive();
              }
              return scrollSpy.updateStates();
            }
            if (isInside && activeLink !== to) {
              scroller.setActiveLink(to);
              _this2.props.hashSpy && scrollHash.changeHash(to);
              if (_this2.props.spy) {
                _this2.setState({ active: true });
                _this2.props.onSetActive && _this2.props.onSetActive(to);
              }
              return scrollSpy.updateStates();
            }
          };
        };
        ;
        Scroll2.propTypes = protoTypes;
        Scroll2.defaultProps = { offset: 0 };
        return Scroll2;
      },
      Element: function Element3(Component2) {
        console.warn("Helpers.Element is deprecated since v1.7.0");
        var Element4 = function(_React$Component2) {
          _inherits(Element5, _React$Component2);
          function Element5(props) {
            _classCallCheck(this, Element5);
            var _this3 = _possibleConstructorReturn(this, (Element5.__proto__ || Object.getPrototypeOf(Element5)).call(this, props));
            _this3.childBindings = {
              domNode: null
            };
            return _this3;
          }
          _createClass(Element5, [{
            key: "componentDidMount",
            value: function componentDidMount() {
              if (typeof window === "undefined") {
                return false;
              }
              this.registerElems(this.props.name);
            }
          }, {
            key: "componentDidUpdate",
            value: function componentDidUpdate(prevProps) {
              if (this.props.name !== prevProps.name) {
                this.registerElems(this.props.name);
              }
            }
          }, {
            key: "componentWillUnmount",
            value: function componentWillUnmount() {
              if (typeof window === "undefined") {
                return false;
              }
              defaultScroller.unregister(this.props.name);
            }
          }, {
            key: "registerElems",
            value: function registerElems(name2) {
              defaultScroller.register(name2, this.childBindings.domNode);
            }
          }, {
            key: "render",
            value: function render() {
              return React10.createElement(Component2, _extends({}, this.props, { parentBindings: this.childBindings }));
            }
          }]);
          return Element5;
        }(React10.Component);
        ;
        Element4.propTypes = {
          name: PropTypes.string,
          id: PropTypes.string
        };
        return Element4;
      }
    };
    module.exports = Helpers;
  }
});

// node_modules/react-scroll/modules/index.js
var require_modules = __commonJS({
  "node_modules/react-scroll/modules/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.Helpers = exports.ScrollElement = exports.ScrollLink = exports.animateScroll = exports.scrollSpy = exports.Events = exports.scroller = exports.Element = exports.Button = exports.Link = void 0;
    var _Link = require_Link();
    var _Link2 = _interopRequireDefault(_Link);
    var _Button = require_Button();
    var _Button2 = _interopRequireDefault(_Button);
    var _Element = require_Element();
    var _Element2 = _interopRequireDefault(_Element);
    var _scroller = require_scroller();
    var _scroller2 = _interopRequireDefault(_scroller);
    var _scrollEvents = require_scroll_events();
    var _scrollEvents2 = _interopRequireDefault(_scrollEvents);
    var _scrollSpy = require_scroll_spy();
    var _scrollSpy2 = _interopRequireDefault(_scrollSpy);
    var _animateScroll = require_animate_scroll();
    var _animateScroll2 = _interopRequireDefault(_animateScroll);
    var _scrollLink = require_scroll_link();
    var _scrollLink2 = _interopRequireDefault(_scrollLink);
    var _scrollElement = require_scroll_element();
    var _scrollElement2 = _interopRequireDefault(_scrollElement);
    var _Helpers = require_Helpers();
    var _Helpers2 = _interopRequireDefault(_Helpers);
    function _interopRequireDefault(obj) {
      return obj && obj.__esModule ? obj : { default: obj };
    }
    exports.Link = _Link2.default;
    exports.Button = _Button2.default;
    exports.Element = _Element2.default;
    exports.scroller = _scroller2.default;
    exports.Events = _scrollEvents2.default;
    exports.scrollSpy = _scrollSpy2.default;
    exports.animateScroll = _animateScroll2.default;
    exports.ScrollLink = _scrollLink2.default;
    exports.ScrollElement = _scrollElement2.default;
    exports.Helpers = _Helpers2.default;
    exports.default = { Link: _Link2.default, Button: _Button2.default, Element: _Element2.default, scroller: _scroller2.default, Events: _scrollEvents2.default, scrollSpy: _scrollSpy2.default, animateScroll: _animateScroll2.default, ScrollLink: _scrollLink2.default, ScrollElement: _scrollElement2.default, Helpers: _Helpers2.default };
  }
});

// dist/server/chunks/chunk-BikPkIUV.js
var root, track, bar, thumb, label, icon, s16;
var init_chunk_BikPkIUV = __esm({
  "dist/server/chunks/chunk-BikPkIUV.js"() {
    "use strict";
    root = "_root_3cfb0_1";
    track = "_track_3cfb0_51";
    bar = "_bar_3cfb0_55";
    thumb = "_thumb_3cfb0_71";
    label = "_label_3cfb0_91";
    icon = "_icon_3cfb0_116";
    s16 = {
      root,
      track,
      bar,
      thumb,
      label,
      icon
    };
  }
});

// dist/server/chunks/chunk-alFISafC.js
var text, s17;
var init_chunk_alFISafC = __esm({
  "dist/server/chunks/chunk-alFISafC.js"() {
    "use strict";
    text = "_text_txqep_1";
    s17 = {
      text
    };
  }
});

// dist/server/chunks/chunk-pUEbsvnZ.js
var itemIcon, s18;
var init_chunk_pUEbsvnZ = __esm({
  "dist/server/chunks/chunk-pUEbsvnZ.js"() {
    "use strict";
    itemIcon = "_itemIcon_ir4ol_1";
    s18 = {
      itemIcon
    };
  }
});

// dist/server/chunks/chunk-D1GxHHzF.js
var itemWrapper, itemLabel, s19;
var init_chunk_D1GxHHzF = __esm({
  "dist/server/chunks/chunk-D1GxHHzF.js"() {
    "use strict";
    itemWrapper = "_itemWrapper_46u10_1";
    itemLabel = "_itemLabel_46u10_5";
    s19 = {
      itemWrapper,
      itemLabel
    };
  }
});

// dist/server/chunks/chunk-a-vQjrVb.js
var title, s20;
var init_chunk_a_vQjrVb = __esm({
  "dist/server/chunks/chunk-a-vQjrVb.js"() {
    "use strict";
    title = "_title_25esx_1";
    s20 = {
      title
    };
  }
});

// dist/server/chunks/chunk-DK20jVfX.js
var paper, stack, personalityType, name, type, image, s21;
var init_chunk_DK20jVfX = __esm({
  "dist/server/chunks/chunk-DK20jVfX.js"() {
    "use strict";
    paper = "_paper_1h3wh_1";
    stack = "_stack_1h3wh_18";
    personalityType = "_personalityType_1h3wh_29";
    name = "_name_1h3wh_37";
    type = "_type_1h3wh_45";
    image = "_image_1h3wh_53";
    s21 = {
      paper,
      stack,
      personalityType,
      name,
      type,
      image
    };
  }
});

// dist/server/chunks/chunk-CoUVVSTL.js
var inner2, label2, dropdownIcon, dropdown, item, s22;
var init_chunk_CoUVVSTL = __esm({
  "dist/server/chunks/chunk-CoUVVSTL.js"() {
    "use strict";
    inner2 = "_inner_10ovm_1";
    label2 = "_label_10ovm_2";
    dropdownIcon = "_dropdownIcon_10ovm_11";
    dropdown = "_dropdown_10ovm_11";
    item = "_item_10ovm_24";
    s22 = {
      inner: inner2,
      label: label2,
      dropdownIcon,
      dropdown,
      item
    };
  }
});

// node_modules/vike/dist/esm/node/client/router.js
function warnNoEffect(caller) {
  assertWarning2(false, `Calling ${caller}() on the server-side has no effect`, {
    showStackTrace: true,
    onlyOnce: false
  });
}
var navigate2;
var init_router = __esm({
  "node_modules/vike/dist/esm/node/client/router.js"() {
    init_assert();
    navigate2 = () => warnNoEffect("navigate");
  }
});

// dist/server/entries/src_pages_free-report.mjs
var src_pages_free_report_exports = {};
__export(src_pages_free_report_exports, {
  configValuesSerialized: () => configValuesSerialized2
});
function Page2() {
  const {
    name: name2,
    data
  } = c2({
    name: $reportName,
    data: $freeResult
  });
  const isLoading = c2($isLoadingPage);
  if (isLoading && !data) return /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(PageLoader, {});
  if (!data) return null;
  return /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Box, { component: "section", pb: 800, children: /* @__PURE__ */ (0, import_jsx_runtime166.jsxs)(Container, { mt: "xs", children: [
    /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(ReportHeader, { name: name2, type: data == null ? void 0 : data.mbti_type }),
    /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(SendReportEmail, {}),
    /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(ReportNavigation, {}),
    /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(ContentResolver, {})
  ] }) });
}
var import_jsx_runtime166, import_react221, import_react_scroll, BoldText, PageLoader, PointsBlock, sendFreeReportOnEmailMutation, $reportName, Header2, lockImage, menUrl, Top, Paywall, useBarChartViewModel, TypeToColorMap, BarChart, IconList, MainTitle, regex, Paragraph, OrderedCards, envelopeImage, Subscription, BlockquoteLine, FilledBulletList, gemImage, sparklesImage, ConclusionPaywall, SendReportSchema, sendReportForm, showUserEmailNotificationFx, SendReportEmail, keyIconImage, BuyFullReportButton, ContentResolver, circleImage, circleSmallImage, ReportHeader, ReportNavigation, pageStarted, $isLoadingPage, redirectToIndexPageFx, import72, configValuesSerialized2;
var init_src_pages_free_report = __esm({
  "dist/server/entries/src_pages_free-report.mjs"() {
    "use strict";
    init_Loading();
    init_onRenderHtml();
    init_chunk_Y_L1G_uK();
    import_jsx_runtime166 = __toESM(require_jsx_runtime(), 1);
    init_esm2();
    init_effector_react();
    init_chunk_CiwkTWKi();
    init_effector();
    init_core();
    init_chunk_BDsA7YFO();
    import_react221 = __toESM(require_react(), 1);
    init_chunk_Vlh7XtbV();
    init_clsx();
    init_dist6();
    init_ssr();
    init_chunk_BzlClNuB();
    init_chunk_B37yhB3o();
    init_dist5();
    init_dist7();
    init_esm5();
    init_lib();
    init_chunk_BUm1PIXW();
    import_react_scroll = __toESM(require_modules(), 1);
    init_chunk_BikPkIUV();
    init_chunk_alFISafC();
    init_chunk_pUEbsvnZ();
    init_chunk_D1GxHHzF();
    init_chunk_a_vQjrVb();
    init_chunk_DK20jVfX();
    init_dist();
    init_chunk_CoUVVSTL();
    init_patronum();
    init_router();
    init_usePageContext();
    init_chunk_D3GmwNoI();
    init_chunk_CVNqduS6();
    BoldText = (props) => {
      const {
        text: text4,
        boldText,
        ...rest
      } = props;
      const parts = text4 == null ? void 0 : text4.replaceAll("**", "").split(boldText ?? "");
      return /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Text, { ...rest, children: parts == null ? void 0 : parts.map((part, index3) => /* @__PURE__ */ (0, import_jsx_runtime166.jsxs)(import_react221.default.Fragment, { children: [
        part,
        index3 !== parts.length - 1 && /* @__PURE__ */ (0, import_jsx_runtime166.jsx)("strong", { style: {
          fontWeight: "bold"
        }, children: boldText == null ? void 0 : boldText.replaceAll("*", "") })
      ] }, index3)) });
    };
    PageLoader = () => /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Center, { h: "100vh", children: /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Loader, { size: "xl", color: "violet.5" }) });
    PointsBlock = ({
      points
    }) => {
      const isLarge = useIsLarge();
      if (!points || !points.length) return null;
      return /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Stack, { pos: isLarge ? "absolute" : "static", maw: isLarge ? 781 : "fit-content", left: 300, top: 60, children: points.map((item2, index3) => /* @__PURE__ */ (0, import_jsx_runtime166.jsxs)(Group, { gap: isLarge ? "sm" : "xs", children: [
        /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(n4, { size: isLarge ? 24 : 16, weight: "bold", color: "var(--mantine-color-violet-9)" }),
        /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Text, { flex: 1, fz: isLarge ? 22 : 16, lh: isLarge ? "25px" : "18px", children: item2 })
      ] }, `${item2}_${index3}`)) });
    };
    sendFreeReportOnEmailMutation = ke({
      sid: "lvc7y9",
      fn: () => jr({
        effect: createInternalRequestFx((body) => ({
          url: API.SEND_FREE_EMAIL,
          method: HTTP_METHODS.POST,
          body
        }))
      }),
      name: "sendFreeReportOnEmailMutation",
      method: "createMutation"
    });
    $reportName = h("", {
      name: "$reportName",
      sid: "suigk7"
    });
    Header2 = ({
      text: text4,
      c: c15 = "violet.9",
      ...props
    }) => {
      const isLarge = useIsLarge();
      return /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Title, { mt: c15 === "black" ? 60 : 0, lh: 1.1, order: 5, fz: isLarge ? 32 : 18, c: c15 === "primary" ? "violet.9" : c15, ...props, children: text4 });
    };
    lockImage = "data:image/webp;base64,";
    menUrl = "/assets/static/men-temp.e7uCjLN3.webp";
    Top = ({
      title: title3
    }) => {
      const isLarge = useIsLarge();
      return /* @__PURE__ */ (0, import_jsx_runtime166.jsxs)(Flex, { w: "100%", gap: isLarge ? "lg" : "xxs", align: isLarge ? "flex-start" : "center", direction: isLarge ? "row-reverse" : "row", justify: isLarge ? "flex-end" : "space-between", children: [
        /* @__PURE__ */ (0, import_jsx_runtime166.jsxs)(Group, { gap: "xs", children: [
          /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Image, { w: 32, h: 32, src: lockImage }),
          /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Title, { fz: isLarge ? 32 : 20, textWrap: "balance", maw: isLarge ? "fit-content" : 175, children: title3 })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Image, { src: isLarge ? manImage : menUrl, w: isLarge ? 351 : 126, h: isLarge ? 311 : 116 })
      ] });
    };
    Paywall = ({
      title: title3,
      buyButtonSlot,
      points
    }) => {
      const isLarge = useIsLarge();
      return /* @__PURE__ */ (0, import_jsx_runtime166.jsxs)(Stack, { gap: "lg", align: "center", pos: "relative", mt: isLarge ? 60 : 0, children: [
        /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Top, { title: title3 }),
        /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(PointsBlock, { points }),
        /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Box, { left: 332, bottom: -20, pos: isLarge ? "absolute" : "static", maw: isLarge ? "fit-content" : "100%", display: isLarge ? "block" : "contents", children: buyButtonSlot })
      ] });
    };
    useBarChartViewModel = ({
      marks
    }) => {
      const [mounted, setMounted] = (0, import_react221.useState)(false);
      const [selectedItem, setSelectedItem] = (0, import_react221.useState)(null);
      const isLarge = useIsLarge();
      const onSelectItemMouseOverHandler = (0, import_react221.useCallback)((mark) => {
        if ((selectedItem == null ? void 0 : selectedItem.label) === mark.label) return;
        setMounted(false);
        setTimeout(() => {
          setSelectedItem(mark);
          setMounted(true);
        }, 200);
      }, [selectedItem, mounted]);
      (0, import_react221.useEffect)(() => {
        if (!isLarge) return;
        setMounted(true);
        setSelectedItem(marks[0]);
      }, [isLarge]);
      return {
        onSelectItemMouseOverHandler,
        selectedItem,
        mounted
      };
    };
    TypeToColorMap = {
      "E-I": "violet",
      "J-P": "yellow",
      "S-N": "green",
      "T-F": "indigo"
    };
    BarChart = ({
      marks
    }) => {
      const isLarge = useIsLarge();
      const {
        onSelectItemMouseOverHandler,
        selectedItem,
        mounted
      } = useBarChartViewModel({
        marks
      });
      return /* @__PURE__ */ (0, import_jsx_runtime166.jsxs)(Group, { align: "stretch", gap: "lg", mb: isLarge ? 100 : 0, mt: isLarge ? "5xl" : 0, children: [
        /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Stack, { flex: 1, mt: isLarge ? 0 : 40, mb: isLarge ? 0 : 60, gap: isLarge ? 74 : 84, children: marks.map((mark, i13) => /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Box, { onMouseOver: () => isLarge && onSelectItemMouseOverHandler(mark), children: /* @__PURE__ */ (0, import_jsx_runtime166.jsxs)(Popover, { offset: 40, width: "90%", position: "top", shadow: "sm", children: [
          /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Popover.Target, { children: /* @__PURE__ */ (0, import_jsx_runtime166.jsxs)(Box, { pos: "relative", children: [
            /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(w3, { style: {
              "--offset": mark.value + "%"
            }, size: 16, className: s16.icon }),
            /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Slider, { labelAlwaysOn: true, classNames: s16, value: mark.value, "data-type": mark.label, "data-value": mark.value })
          ] }) }),
          /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Popover.Dropdown, { hidden: isLarge, bg: `${TypeToColorMap[mark.mbti_type[i13]]}.0`, children: mark.data.map((item2, idx) => {
            switch (item2.type) {
              case "paragraph":
                return /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Paragraph, { text: item2.text }, idx);
              case "header":
                return /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Header2, { text: item2.text, c: `${TypeToColorMap[mark.mbti_type[i13]]}.9` }, idx);
            }
          }) })
        ] }) }, i13)) }),
        /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Box, { flex: 1, h: "auto", visibleFrom: "lg", children: selectedItem && /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Transition, { mounted, children: (styles) => /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Paper, { py: 22, px: "3xl", h: "100%", mih: "auto", radius: 30, style: styles, visibleFrom: "md", bg: `${TypeToColorMap[selectedItem.label]}.0`, children: selectedItem == null ? void 0 : selectedItem.data.map((item2, idx) => {
          switch (item2.type) {
            case "paragraph":
              return /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Paragraph, { text: item2.text }, idx);
            case "header":
              return /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Header2, { text: item2.text, c: `${TypeToColorMap[selectedItem.label]}.9` }, idx);
          }
        }) }) }) })
      ] });
    };
    IconList = ({
      items
    }) => {
      const isLarge = useIsLarge();
      return /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(List, { classNames: s18, children: items.map((item2) => /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(List.Item, { icon: /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(ThemeIcon, { color: "transparent", c: "violet.9", size: isLarge ? 32 : 24, children: /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(k, { size: isLarge ? 32 : 24 }) }), mb: "md", children: /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Text, { fz: isLarge ? 22 : 18, lh: "21px", children: item2.text }) }, `${item2.type}_${item2.text}`)) });
    };
    MainTitle = ({
      children
    }) => {
      const isLarge = useIsLarge();
      return /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Title, { fz: isLarge ? 24 : 20, children });
    };
    regex = /\*\*(.*?)\*\*/;
    Paragraph = ({
      text: text4,
      ...rest
    }) => {
      const match = text4 == null ? void 0 : text4.match(regex);
      return /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(BoldText, { className: s17.text, text: text4, boldText: match ? match[1] : "", ...rest });
    };
    OrderedCards = ({
      items,
      color: color2
    }) => {
      const isLarge = useIsLarge();
      return /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Stack, { children: items.map((item2) => /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Paper, { px: "3xl", py: "lg", bg: color2 === "positive" ? "green.0" : "pink.0", children: /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Stack, { gap: "xs", children: /* @__PURE__ */ (0, import_jsx_runtime166.jsxs)(Title, { fz: isLarge ? 24 : 16, c: color2 === "positive" ? "green.9" : "pink.9", order: 5, children: [
        item2.order,
        ". ",
        item2.title
      ] }) }) }, item2.order)) });
    };
    envelopeImage = "/assets/static/envelope.fCgPZGsq.webp";
    Subscription = ({
      title: title3,
      text: text4,
      subscriptionFormSlot
    }) => {
      const isHuge = useIsHuge();
      const isLarge = useIsLarge();
      return /* @__PURE__ */ (0, import_jsx_runtime166.jsxs)(Paper, { radius: "lg", bg: "violet.0", pos: "relative", mt: isLarge ? 100 : 0, mx: isHuge ? -157 : 0, px: isLarge ? 153 : "md", py: isLarge ? "3xl" : "xl", children: [
        /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Image, { pos: "absolute", top: isLarge ? 32 : 5, right: isLarge ? 153 : 33, w: isLarge ? 215 : 84, h: isLarge ? 216 : 84, src: envelopeImage }),
        /* @__PURE__ */ (0, import_jsx_runtime166.jsxs)(Stack, { children: [
          /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Title, { order: 5, fz: isLarge ? 32 : 20, textWrap: "balance", maw: "50%", children: title3 }),
          /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Text, { fz: isLarge ? 24 : 18, children: text4 }),
          subscriptionFormSlot
        ] })
      ] });
    };
    BlockquoteLine = ({
      text: text4
    }) => {
      return /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Blockquote, { py: "sm", px: "md", color: "violet.9", bg: "transparent", icon: null, children: /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Paragraph, { text: text4 }) });
    };
    FilledBulletList = ({
      items
    }) => {
      const isLarge = useIsLarge();
      return /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(List, { classNames: s19, c: "violet.9", children: items.map((item2) => /* @__PURE__ */ (0, import_jsx_runtime166.jsxs)(List.Item, { mb: "md", children: [
        /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Text, { span: item2.text.startsWith(" \u2014 "), fz: isLarge ? 22 : 18, lh: "21px", children: item2.title }),
        /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Text, { span: true, c: "dark.7", fz: isLarge ? 22 : 18, lh: "21px", children: item2.text })
      ] }, `${item2.type}_${item2.title}`)) });
    };
    gemImage = "/assets/static/gem.ld0lVkQd.webp";
    sparklesImage = "data:image/webp;base64,";
    ConclusionPaywall = ({
      text: text4,
      title: title3,
      points,
      extraContentSlot,
      buyButtonSlot
    }) => {
      const isLarge = useIsLarge();
      return /* @__PURE__ */ (0, import_jsx_runtime166.jsxs)(Paper, { bg: "violet.0", pos: "relative", py: isLarge ? "3xl" : "xl", px: isLarge ? "3xl" : "md", radius: isLarge ? "lg" : "md", children: [
        /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Image, { src: gemImage, w: isLarge ? 250 : 108, h: isLarge ? 250 : 108, pos: "absolute", top: -7, right: 0 }),
        /* @__PURE__ */ (0, import_jsx_runtime166.jsxs)(Stack, { gap: isLarge ? "lg" : "md", children: [
          /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Title, { order: 5, fz: isLarge ? 32 : 20, textWrap: "balance", maw: isLarge ? "100%" : "60%", children: /* @__PURE__ */ (0, import_jsx_runtime166.jsxs)(Group, { gap: "xs", children: [
            /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Image, { visibleFrom: "md", src: sparklesImage, w: 48, h: 48 }),
            title3
          ] }) }),
          /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Text, { maw: isLarge ? 820 : "100%", fz: isLarge ? 24 : 16, children: text4 }),
          /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(PointsBlock, { points }),
          /* @__PURE__ */ (0, import_jsx_runtime166.jsxs)(Group, { gap: "sm", w: isLarge ? "fit-content" : "100%", children: [
            buyButtonSlot,
            extraContentSlot
          ] })
        ] })
      ] });
    };
    SendReportSchema = z.object({
      email: z.string().min(1, {
        message: "\u041E\u0431\u044F\u0437\u0430\u0442\u0435\u043B\u044C\u043D\u043E\u0435 \u043F\u043E\u043B\u0435"
      }).email("\u0412\u0432\u0435\u0434\u0438\u0442\u0435 \u043F\u043E\u0447\u0442\u0443 \u0432 \u0444\u043E\u0440\u043C\u0430\u0442\u0435 example@mail.org")
    });
    sendReportForm = or3({
      schema: {
        email: ""
      },
      validation: a16(SendReportSchema),
      validationStrategies: ["submit"]
    });
    showUserEmailNotificationFx = b((email) => {
      notifications.show({
        title: "\u041E\u0442\u0447\u0435\u0442 \u043E\u0442\u043F\u0440\u0430\u0432\u043B\u0435\u043D!",
        message: `\u041E\u0442\u0447\u0435\u0442 \u043E\u0442\u043F\u0440\u0430\u0432\u043B\u0435\u043D \u043D\u0430 \u043F\u043E\u0447\u0442\u0443 ${email}`
      });
    }, {
      name: "showUserEmailNotificationFx",
      sid: "bdp625"
    });
    x({
      and: [{
        clock: sendReportForm.submitted,
        filter: () => Boolean(sendReportForm.$isValid),
        fn: (values2) => ({
          email: values2.email
        }),
        target: sendFreeReportOnEmailMutation.start
      }],
      or: {
        sid: "-f18j5l"
      }
    });
    x({
      and: [{
        clock: sendFreeReportOnEmailMutation.finished.success,
        filter: ({
          params
        }) => Boolean(params.email),
        fn: ({
          params: {
            email
          }
        }) => email,
        target: showUserEmailNotificationFx
      }],
      or: {
        sid: "-eluajh"
      }
    });
    SendReportEmail = ({
      isFreeReport = true,
      type: type2 = "text"
    }) => {
      const isLarge = useIsLarge();
      const {
        fields,
        onSubmit,
        isValid: isValid2,
        onValidate
      } = j2(sendReportForm);
      const isLoading = c2(sendFreeReportOnEmailMutation.$pending);
      const isButtonDisabled = isLoading;
      const onS = (e14) => {
        e14.preventDefault();
        onValidate();
        if (!isValid2) onSubmit(e14);
      };
      return /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(InnerContainer, { className: type2 === "block" ? s15.container : "", children: /* @__PURE__ */ (0, import_jsx_runtime166.jsx)("form", { onSubmit: isFreeReport ? onS : () => {
      }, children: /* @__PURE__ */ (0, import_jsx_runtime166.jsxs)(Flex, { className: s15.wrapper, children: [
        type2 === "text" && /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Text, { fw: "bold", visibleFrom: "md", fz: 24, children: "\u041E\u0442\u043F\u0440\u0430\u0432\u044C\u0442\u0435 \u043E\u0442\u0447\u0435\u0442 \u043D\u0430 \u043F\u043E\u0447\u0442\u0443" }),
        /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(TextInput, { required: true, inputMode: "email", disabled: isLoading, placeholder: "name@mail.ru", value: fields.email.value, error: fields.email.error, miw: isLarge ? 514 : "100%", ml: type2 === "text" ? "auto" : 0, size: type2 === "block" ? "lg" : "md", bg: type2 === "block" ? "violet.0" : "white", onChange: (e14) => fields.email.onChange(e14.target.value) }),
        /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Button, { type: "submit", color: "dark.7", loading: isLoading, fullWidth: !isLarge, disabled: isButtonDisabled, size: type2 === "block" ? "lg" : "md", leftSection: /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(d4, { size: 20 }), variant: type2 === "block" ? "filled" : "outline", c: type2 === "block" && !isButtonDisabled ? "white" : "dark.7", children: isLarge ? "\u041E\u0442\u043F\u0440\u0430\u0432\u0438\u0442\u044C" : "\u041E\u0442\u043F\u0440\u0430\u0432\u0438\u0442\u044C \u043E\u0442\u0447\u0435\u0442 \u043D\u0430 \u043F\u043E\u0447\u0442\u0443" })
      ] }) }) });
    };
    keyIconImage = "data:image/webp;base64,";
    BuyFullReportButton = ({
      buttonText = "\u041A\u0443\u043F\u0438\u0442\u044C \u043F\u043E\u043B\u043D\u044B\u0439 \u043E\u0442\u0447\u0435\u0442"
    }) => {
      const isLarge = useIsLarge();
      const {
        mbti
      } = c2({
        mbti: getFreeResultQuery.$data
      });
      return /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(MainButton, { fullWidth: true, component: "a", size: isLarge ? "lg" : "md", radius: isLarge ? "md" : "sm", href: `/purchase-report?mbti=${mbti == null ? void 0 : mbti.mbti_type}`, leftSection: /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Image, { src: keyIconImage, w: 20, h: 20 }), children: buttonText });
    };
    ContentResolver = () => {
      const isLarge = useIsLarge();
      const content = c2($freeContent);
      if (!content) return null;
      const render = i($freeContent, (item2, idx) => /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Stack, { id: item2.title, gap: "md", mb: isLarge ? 100 : 60, children: /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(import_react_scroll.Element, { name: item2.title, children: /* @__PURE__ */ (0, import_jsx_runtime166.jsxs)(Stack, { children: [
        /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Title, { className: s20.title, children: item2.title }),
        /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Stack, { gap: "md", children: item2.content.map((item22, idx2) => {
          switch (item22.type) {
            case "paywall":
              return /* @__PURE__ */ (0, import_react221.createElement)(Paywall, { ...item22, key: `${item22.type}_${idx2}`, buttonText: item22.button_text, buyButtonSlot: /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(BuyFullReportButton, {}) });
            case "conclusion_paywall":
              return /* @__PURE__ */ (0, import_react221.createElement)(ConclusionPaywall, { ...item22, buyButtonSlot: /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(BuyFullReportButton, {}), key: `${item22.type}_${item22.color}_${idx2}` });
            case "blockquote_line":
              return /* @__PURE__ */ (0, import_react221.createElement)(BlockquoteLine, { ...item22, key: `${item22.type}_${idx2}` });
            case "filled_bullet_list":
              return /* @__PURE__ */ (0, import_react221.createElement)(FilledBulletList, { ...item22, key: `${item22.type}_${idx2}` });
            case "bar_chart": {
              const marks = Object.entries(item22.mbti_percentages).map((el2) => ({
                value: el2[1].positive,
                label: el2[0],
                data: item22.mbti_data[el2[0]],
                mbti_type: Object.keys(item22.mbti_data)
              }));
              return /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(BarChart, { marks }, `${item22.type}_${idx2}`);
            }
            case "paragraph":
              return /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Paragraph, { text: item22.text }, `${item22.type}_${idx2}`);
            case "title":
              return /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(MainTitle, { children: item22.text }, `${item22.type}_${idx2}`);
            case "icon_list":
              return /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(IconList, { items: item22.items }, `${item22.type}_${idx2}`);
            case "header":
              return /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Header2, { c: item22.color, text: item22.text }, `${item22.type}_${idx2}`);
            case "ordered_cards":
              return /* @__PURE__ */ (0, import_react221.createElement)(OrderedCards, { ...item22, key: `${item22.type}_${item22.color}_${idx2}` });
            case "subscription":
              return /* @__PURE__ */ (0, import_react221.createElement)(Subscription, { ...item22, subscriptionFormSlot: /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(SendReportEmail, { type: "block" }), key: `${item22.type}_${item22.color}_${idx2}` });
          }
        }) })
      ] }) }) }, idx));
      return /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(InnerContainer, { children: render });
    };
    circleImage = "data:image/svg+xml,%3csvg%20xmlns='http://www.w3.org/2000/svg'%20fill='none'%20viewBox='0%200%20656%20399'%3e%3crect%20width='739'%20height='739'%20y='-118'%20fill='%23D0BFFF'%20rx='369.5'/%3e%3crect%20width='483'%20height='483'%20x='203'%20y='84'%20fill='%23E5DBFF'%20rx='241.5'/%3e%3c/svg%3e";
    circleSmallImage = "data:image/svg+xml,%3csvg%20xmlns='http://www.w3.org/2000/svg'%20fill='none'%20viewBox='0%200%20315%20211'%3e%3crect%20width='364'%20height='364'%20fill='%23D0BFFF'%20rx='182'/%3e%3crect%20width='192'%20height='192'%20x='86'%20y='86'%20fill='%23E5DBFF'%20rx='96'/%3e%3c/svg%3e";
    ReportHeader = ({
      name: name2,
      type: type2
    }) => {
      const isLarge = useIsLarge();
      return /* @__PURE__ */ (0, import_jsx_runtime166.jsxs)(Paper, { className: s21.paper, children: [
        /* @__PURE__ */ (0, import_jsx_runtime166.jsxs)(Stack, { className: s21.stack, children: [
          /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Text, { className: s21.personalityType, children: "\u0412\u0430\u0448 \u0442\u0438\u043F \u043B\u0438\u0447\u043D\u043E\u0441\u0442\u0438" }),
          /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Title, { className: s21.name, children: name2 }),
          /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Text, { className: s21.type, children: type2 })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Image, { className: s21.image, src: isLarge ? circleImage : circleSmallImage })
      ] });
    };
    ReportNavigation = () => {
      const isLarge = useIsLarge();
      const content = a2({
        store: $freeContent,
        keys: ["title"],
        fn: (content2) => content2.map(({
          title: title3
        }) => title3)
      });
      const icons = c2($navigationIconMap);
      const [activeMenu, setActiveMenu] = (0, import_react221.useState)(content[0]);
      return /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(InnerContainer, { style: {
        zIndex: 1e3
      }, pos: "sticky", top: 0, py: "lg", pb: "md", mb: 32, bg: "white", children: /* @__PURE__ */ (0, import_jsx_runtime166.jsxs)(Menu, { offset: 16, keepMounted: true, width: "target", classNames: s22, position: "bottom", closeOnItemClick: true, middlewares: {
        flip: false
      }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Menu.Target, { children: /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Button, { px: 0, size: "lg", variant: "transparent", classNames: s22, fullWidth: true, justify: "flex-start", children: /* @__PURE__ */ (0, import_jsx_runtime166.jsxs)(Group, { style: {
          overflow: "hidden"
        }, gap: 0, w: "100%", wrap: "nowrap", align: "center", justify: isLarge ? "flex-start" : "space-between", children: [
          /* @__PURE__ */ (0, import_jsx_runtime166.jsxs)(Group, { gap: "xs", wrap: "nowrap", style: {
            overflow: "hidden"
          }, children: [
            /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Paper, { p: 10, radius: "sm", bg: "violet.1", children: /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Center, { children: icons[activeMenu] }) }),
            /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Text, { ta: "start", truncate: "end", fz: 20, fw: "bold", children: activeMenu })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(D2, { style: {
            flex: "0 1 32px"
          }, color: "var(--mantine-color-dark-9)", size: 20 })
        ] }) }) }),
        /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Menu.Dropdown, { w: "auto", children: content.map((title3) => /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Menu.Item, { leftSection: /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Paper, { p: "xxs", radius: "xs", bg: "violet.1", children: /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Center, { className: s22.dropdownIcon, children: icons[title3] }) }), children: /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(import_react_scroll.Link, { delay: 400, spy: true, offset: -100, to: title3, onSetActive: setActiveMenu, children: /* @__PURE__ */ (0, import_jsx_runtime166.jsx)(Text, { span: true, inline: true, fz: 14, fw: "bold", children: title3 }) }) }, title3)) })
      ] }, activeMenu) });
    };
    pageStarted = createPageStart();
    $isLoadingPage = ke({
      sid: "tm8un9",
      fn: () => or2(getPersonalityTypesQuery.$pending, getFreeResultQuery.$pending),
      name: "$isLoadingPage",
      method: "or"
    });
    redirectToIndexPageFx = b(async () => {
      await navigate2("/");
    }, {
      name: "redirectToIndexPageFx",
      sid: "oyzftu"
    });
    x({
      and: [{
        clock: pageStarted,
        target: getPersonalityTypesQuery.start
      }],
      or: {
        sid: "-9vc7md"
      }
    });
    x({
      and: [{
        clock: getPersonalityTypesQuery.finished.success,
        target: getFreeResultQuery.start
      }],
      or: {
        sid: "-9h1k6z"
      }
    });
    x({
      and: [{
        clock: getFreeResultQuery.finished.success,
        source: getPersonalityTypesQuery.$data,
        fn: (result, {
          result: freeResult
        }) => {
          var _a;
          return ((_a = result == null ? void 0 : result.find((el2) => el2.code === (freeResult == null ? void 0 : freeResult.mbti_type))) == null ? void 0 : _a.name) ?? "";
        },
        target: $reportName
      }],
      or: {
        sid: "-9eal86"
      }
    });
    ke({
      sid: "-8ywcm2",
      fn: () => Rr(getFreeResultQuery, {
        times: 5,
        delay: 500,
        // @ts-expect-error make issue
        otherwise: redirectToIndexPageFx
      }),
      name: "none",
      method: "retry"
    });
    ke({
      sid: "-8ji3zy",
      fn: () => Mr(getFreeResultQuery),
      name: "none",
      method: "cache"
    });
    ke({
      sid: "-8iybel",
      fn: () => Mr(getPersonalityTypesQuery),
      name: "none",
      method: "cache"
    });
    import72 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
      __proto__: null,
      default: Page2
    }, Symbol.toStringTag, { value: "Module" }));
    configValuesSerialized2 = {
      ["isClientRuntimeLoaded"]: {
        type: "computed",
        definedAtData: null,
        valueSerialized: {
          type: "js-serialized",
          value: true
        }
      },
      ["Loading"]: {
        type: "standard",
        definedAtData: { "filePathToShowToUser": "vike-react/__internal/integration/Loading", "fileExportPathToShowToUser": [] },
        valueSerialized: {
          type: "pointer-import",
          value: Loading_default
        }
      },
      ["onRenderHtml"]: {
        type: "standard",
        definedAtData: { "filePathToShowToUser": "vike-react/__internal/integration/onRenderHtml", "fileExportPathToShowToUser": [] },
        valueSerialized: {
          type: "pointer-import",
          value: onRenderHtml
        }
      },
      ["Wrapper"]: {
        type: "cumulative",
        definedAtData: [{ "filePathToShowToUser": "/src/pages/+Wrapper.tsx", "fileExportPathToShowToUser": [] }],
        valueSerialized: [{
          type: "plus-file",
          exportValues: import3
        }]
      },
      ["passToClient"]: {
        type: "cumulative",
        definedAtData: [{ "filePathToShowToUser": "/src/pages/+config.ts", "fileExportPathToShowToUser": ["default", "passToClient"] }, { "filePathToShowToUser": "vike-react/config", "fileExportPathToShowToUser": ["default", "passToClient"] }],
        valueSerialized: [{
          type: "js-serialized",
          value: ["scopeValues"]
        }, {
          type: "js-serialized",
          value: ["_configFromHook"]
        }]
      },
      ["cacheControl"]: {
        type: "standard",
        definedAtData: { "filePathToShowToUser": "/src/pages/+config.ts", "fileExportPathToShowToUser": ["default", "cacheControl"] },
        valueSerialized: {
          type: "js-serialized",
          value: "public, max-age=604800"
        }
      },
      ["title"]: {
        type: "standard",
        definedAtData: { "filePathToShowToUser": "/src/pages/+config.ts", "fileExportPathToShowToUser": ["default", "title"] },
        valueSerialized: {
          type: "js-serialized",
          value: "Cognitive Lab"
        }
      },
      ["lang"]: {
        type: "standard",
        definedAtData: { "filePathToShowToUser": "/src/pages/+config.ts", "fileExportPathToShowToUser": ["default", "lang"] },
        valueSerialized: {
          type: "js-serialized",
          value: "ru"
        }
      },
      ["reactStrictMode"]: {
        type: "standard",
        definedAtData: { "filePathToShowToUser": "/src/pages/+config.ts", "fileExportPathToShowToUser": ["default", "reactStrictMode"] },
        valueSerialized: {
          type: "js-serialized",
          value: false
        }
      },
      ["onBeforeRender"]: {
        type: "standard",
        definedAtData: { "filePathToShowToUser": "/src/pages/+onBeforeRender.ts", "fileExportPathToShowToUser": [] },
        valueSerialized: {
          type: "plus-file",
          exportValues: import4
        }
      },
      ["Head"]: {
        type: "cumulative",
        definedAtData: [{ "filePathToShowToUser": "/src/widgets/HeadDefault/index.tsx", "fileExportPathToShowToUser": ["HeadDefault"] }],
        valueSerialized: [{
          type: "pointer-import",
          value: HeadDefault
        }]
      },
      ["Layout"]: {
        type: "cumulative",
        definedAtData: [{ "filePathToShowToUser": "/src/widgets/RootLayout/index.tsx", "fileExportPathToShowToUser": ["RootLayout"] }],
        valueSerialized: [{
          type: "pointer-import",
          value: RootLayout
        }]
      },
      ["Page"]: {
        type: "standard",
        definedAtData: { "filePathToShowToUser": "/src/pages/free-report/+Page.tsx", "fileExportPathToShowToUser": [] },
        valueSerialized: {
          type: "plus-file",
          exportValues: import72
        }
      }
    };
  }
});

// dist/server/entries/src_pages_index.mjs
var src_pages_index_exports = {};
__export(src_pages_index_exports, {
  configValuesSerialized: () => configValuesSerialized3
});
function Page3() {
  return /* @__PURE__ */ (0, import_jsx_runtime167.jsx)(Container, { children: "Index" });
}
var import_jsx_runtime167, import_react224, import73, configValuesSerialized3;
var init_src_pages_index = __esm({
  "dist/server/entries/src_pages_index.mjs"() {
    "use strict";
    init_Loading();
    init_onRenderHtml();
    init_chunk_Y_L1G_uK();
    import_jsx_runtime167 = __toESM(require_jsx_runtime(), 1);
    init_esm2();
    import_react224 = __toESM(require_react(), 1);
    init_usePageContext();
    init_chunk_D3GmwNoI();
    import73 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
      __proto__: null,
      default: Page3
    }, Symbol.toStringTag, { value: "Module" }));
    configValuesSerialized3 = {
      ["isClientRuntimeLoaded"]: {
        type: "computed",
        definedAtData: null,
        valueSerialized: {
          type: "js-serialized",
          value: true
        }
      },
      ["Loading"]: {
        type: "standard",
        definedAtData: { "filePathToShowToUser": "vike-react/__internal/integration/Loading", "fileExportPathToShowToUser": [] },
        valueSerialized: {
          type: "pointer-import",
          value: Loading_default
        }
      },
      ["onRenderHtml"]: {
        type: "standard",
        definedAtData: { "filePathToShowToUser": "vike-react/__internal/integration/onRenderHtml", "fileExportPathToShowToUser": [] },
        valueSerialized: {
          type: "pointer-import",
          value: onRenderHtml
        }
      },
      ["Wrapper"]: {
        type: "cumulative",
        definedAtData: [{ "filePathToShowToUser": "/src/pages/+Wrapper.tsx", "fileExportPathToShowToUser": [] }],
        valueSerialized: [{
          type: "plus-file",
          exportValues: import3
        }]
      },
      ["passToClient"]: {
        type: "cumulative",
        definedAtData: [{ "filePathToShowToUser": "/src/pages/+config.ts", "fileExportPathToShowToUser": ["default", "passToClient"] }, { "filePathToShowToUser": "vike-react/config", "fileExportPathToShowToUser": ["default", "passToClient"] }],
        valueSerialized: [{
          type: "js-serialized",
          value: ["scopeValues"]
        }, {
          type: "js-serialized",
          value: ["_configFromHook"]
        }]
      },
      ["cacheControl"]: {
        type: "standard",
        definedAtData: { "filePathToShowToUser": "/src/pages/+config.ts", "fileExportPathToShowToUser": ["default", "cacheControl"] },
        valueSerialized: {
          type: "js-serialized",
          value: "public, max-age=604800"
        }
      },
      ["title"]: {
        type: "standard",
        definedAtData: { "filePathToShowToUser": "/src/pages/+config.ts", "fileExportPathToShowToUser": ["default", "title"] },
        valueSerialized: {
          type: "js-serialized",
          value: "Cognitive Lab"
        }
      },
      ["lang"]: {
        type: "standard",
        definedAtData: { "filePathToShowToUser": "/src/pages/+config.ts", "fileExportPathToShowToUser": ["default", "lang"] },
        valueSerialized: {
          type: "js-serialized",
          value: "ru"
        }
      },
      ["reactStrictMode"]: {
        type: "standard",
        definedAtData: { "filePathToShowToUser": "/src/pages/+config.ts", "fileExportPathToShowToUser": ["default", "reactStrictMode"] },
        valueSerialized: {
          type: "js-serialized",
          value: false
        }
      },
      ["onBeforeRender"]: {
        type: "standard",
        definedAtData: { "filePathToShowToUser": "/src/pages/+onBeforeRender.ts", "fileExportPathToShowToUser": [] },
        valueSerialized: {
          type: "plus-file",
          exportValues: import4
        }
      },
      ["Head"]: {
        type: "cumulative",
        definedAtData: [{ "filePathToShowToUser": "/src/widgets/HeadDefault/index.tsx", "fileExportPathToShowToUser": ["HeadDefault"] }],
        valueSerialized: [{
          type: "pointer-import",
          value: HeadDefault
        }]
      },
      ["Layout"]: {
        type: "cumulative",
        definedAtData: [{ "filePathToShowToUser": "/src/widgets/RootLayout/index.tsx", "fileExportPathToShowToUser": ["RootLayout"] }],
        valueSerialized: [{
          type: "pointer-import",
          value: RootLayout
        }]
      },
      ["Page"]: {
        type: "standard",
        definedAtData: { "filePathToShowToUser": "/src/pages/index/+Page.tsx", "fileExportPathToShowToUser": [] },
        valueSerialized: {
          type: "plus-file",
          exportValues: import73
        }
      }
    };
  }
});

// node_modules/lodash-es/_freeGlobal.js
var freeGlobal, freeGlobal_default;
var init_freeGlobal = __esm({
  "node_modules/lodash-es/_freeGlobal.js"() {
    freeGlobal = typeof global == "object" && global && global.Object === Object && global;
    freeGlobal_default = freeGlobal;
  }
});

// node_modules/lodash-es/_root.js
var freeSelf, root2, root_default;
var init_root = __esm({
  "node_modules/lodash-es/_root.js"() {
    init_freeGlobal();
    freeSelf = typeof self == "object" && self && self.Object === Object && self;
    root2 = freeGlobal_default || freeSelf || Function("return this")();
    root_default = root2;
  }
});

// node_modules/lodash-es/_Symbol.js
var Symbol2, Symbol_default;
var init_Symbol = __esm({
  "node_modules/lodash-es/_Symbol.js"() {
    init_root();
    Symbol2 = root_default.Symbol;
    Symbol_default = Symbol2;
  }
});

// node_modules/lodash-es/_getRawTag.js
function getRawTag(value) {
  var isOwn = hasOwnProperty.call(value, symToStringTag), tag2 = value[symToStringTag];
  try {
    value[symToStringTag] = void 0;
    var unmasked = true;
  } catch (e14) {
  }
  var result = nativeObjectToString.call(value);
  if (unmasked) {
    if (isOwn) {
      value[symToStringTag] = tag2;
    } else {
      delete value[symToStringTag];
    }
  }
  return result;
}
var objectProto, hasOwnProperty, nativeObjectToString, symToStringTag, getRawTag_default;
var init_getRawTag = __esm({
  "node_modules/lodash-es/_getRawTag.js"() {
    init_Symbol();
    objectProto = Object.prototype;
    hasOwnProperty = objectProto.hasOwnProperty;
    nativeObjectToString = objectProto.toString;
    symToStringTag = Symbol_default ? Symbol_default.toStringTag : void 0;
    getRawTag_default = getRawTag;
  }
});

// node_modules/lodash-es/_objectToString.js
function objectToString(value) {
  return nativeObjectToString2.call(value);
}
var objectProto2, nativeObjectToString2, objectToString_default;
var init_objectToString = __esm({
  "node_modules/lodash-es/_objectToString.js"() {
    objectProto2 = Object.prototype;
    nativeObjectToString2 = objectProto2.toString;
    objectToString_default = objectToString;
  }
});

// node_modules/lodash-es/_baseGetTag.js
function baseGetTag(value) {
  if (value == null) {
    return value === void 0 ? undefinedTag : nullTag;
  }
  return symToStringTag2 && symToStringTag2 in Object(value) ? getRawTag_default(value) : objectToString_default(value);
}
var nullTag, undefinedTag, symToStringTag2, baseGetTag_default;
var init_baseGetTag = __esm({
  "node_modules/lodash-es/_baseGetTag.js"() {
    init_Symbol();
    init_getRawTag();
    init_objectToString();
    nullTag = "[object Null]";
    undefinedTag = "[object Undefined]";
    symToStringTag2 = Symbol_default ? Symbol_default.toStringTag : void 0;
    baseGetTag_default = baseGetTag;
  }
});

// node_modules/lodash-es/isObjectLike.js
function isObjectLike(value) {
  return value != null && typeof value == "object";
}
var isObjectLike_default;
var init_isObjectLike = __esm({
  "node_modules/lodash-es/isObjectLike.js"() {
    isObjectLike_default = isObjectLike;
  }
});

// node_modules/lodash-es/isArray.js
var isArray2, isArray_default;
var init_isArray2 = __esm({
  "node_modules/lodash-es/isArray.js"() {
    isArray2 = Array.isArray;
    isArray_default = isArray2;
  }
});

// node_modules/lodash-es/isObject.js
function isObject3(value) {
  var type2 = typeof value;
  return value != null && (type2 == "object" || type2 == "function");
}
var isObject_default;
var init_isObject2 = __esm({
  "node_modules/lodash-es/isObject.js"() {
    isObject_default = isObject3;
  }
});

// node_modules/lodash-es/isNumber.js
function isNumber(value) {
  return typeof value == "number" || isObjectLike_default(value) && baseGetTag_default(value) == numberTag;
}
var numberTag, isNumber_default;
var init_isNumber = __esm({
  "node_modules/lodash-es/isNumber.js"() {
    init_baseGetTag();
    init_isObjectLike();
    numberTag = "[object Number]";
    isNumber_default = isNumber;
  }
});

// node_modules/lodash-es/lodash.js
var init_lodash = __esm({
  "node_modules/lodash-es/lodash.js"() {
    init_isArray2();
    init_isNumber();
    init_isObject2();
  }
});

// dist/server/chunks/chunk-Ba3xGwqT.js
var label3, required, stackWrapper, promocodeLabel, promocodeWrapper, s23;
var init_chunk_Ba3xGwqT = __esm({
  "dist/server/chunks/chunk-Ba3xGwqT.js"() {
    "use strict";
    label3 = "_label_53g0z_1";
    required = "_required_53g0z_11";
    stackWrapper = "_stackWrapper_53g0z_23";
    promocodeLabel = "_promocodeLabel_53g0z_31";
    promocodeWrapper = "_promocodeWrapper_53g0z_41";
    s23 = {
      label: label3,
      required,
      stackWrapper,
      promocodeLabel,
      promocodeWrapper
    };
  }
});

// node_modules/@mantine/form/esm/actions/actions.mjs
function validateFormName(name2) {
  if (!/^[0-9a-zA-Z-]+$/.test(name2)) {
    throw new Error(
      `[@mantine/use-form] Form name "${name2}" is invalid, it should contain only letters, numbers and dashes`
    );
  }
}
function useFormEvent(eventKey, handler2) {
  useIsomorphicEffect2(() => {
    if (eventKey) {
      window.addEventListener(eventKey, handler2);
      return () => window.removeEventListener(eventKey, handler2);
    }
    return void 0;
  }, [eventKey]);
}
function useFormActions(name2, form) {
  if (name2) {
    validateFormName(name2);
  }
  useFormEvent(
    `mantine-form:${name2}:set-field-value`,
    (event) => form.setFieldValue(event.detail.path, event.detail.value)
  );
  useFormEvent(
    `mantine-form:${name2}:set-values`,
    (event) => form.setValues(event.detail)
  );
  useFormEvent(
    `mantine-form:${name2}:set-initial-values`,
    (event) => form.setInitialValues(event.detail)
  );
  useFormEvent(
    `mantine-form:${name2}:set-errors`,
    (event) => form.setErrors(event.detail)
  );
  useFormEvent(
    `mantine-form:${name2}:set-field-error`,
    (event) => form.setFieldError(event.detail.path, event.detail.error)
  );
  useFormEvent(
    `mantine-form:${name2}:clear-field-error`,
    (event) => form.clearFieldError(event.detail)
  );
  useFormEvent(`mantine-form:${name2}:clear-errors`, form.clearErrors);
  useFormEvent(`mantine-form:${name2}:reset`, form.reset);
  useFormEvent(`mantine-form:${name2}:validate`, form.validate);
  useFormEvent(
    `mantine-form:${name2}:validate-field`,
    (event) => form.validateField(event.detail)
  );
  useFormEvent(
    `mantine-form:${name2}:reorder-list-item`,
    (event) => form.reorderListItem(event.detail.path, event.detail.payload)
  );
  useFormEvent(
    `mantine-form:${name2}:remove-list-item`,
    (event) => form.removeListItem(event.detail.path, event.detail.index)
  );
  useFormEvent(
    `mantine-form:${name2}:insert-list-item`,
    (event) => form.insertListItem(event.detail.path, event.detail.item, event.detail.index)
  );
  useFormEvent(
    `mantine-form:${name2}:set-dirty`,
    (event) => form.setDirty(event.detail)
  );
  useFormEvent(
    `mantine-form:${name2}:set-touched`,
    (event) => form.setTouched(event.detail)
  );
  useFormEvent(
    `mantine-form:${name2}:reset-dirty`,
    (event) => form.resetDirty(event.detail)
  );
  useFormEvent(`mantine-form:${name2}:reset-touched`, form.resetTouched);
}
var import_react225, useIsomorphicEffect2;
var init_actions = __esm({
  "node_modules/@mantine/form/esm/actions/actions.mjs"() {
    "use client";
    import_react225 = __toESM(require_react(), 1);
    useIsomorphicEffect2 = typeof window !== "undefined" ? import_react225.useLayoutEffect : import_react225.useEffect;
  }
});

// node_modules/@mantine/form/esm/get-input-on-change/get-input-on-change.mjs
function getInputOnChange(setValue) {
  return (val) => {
    if (!val) {
      setValue(val);
    } else if (typeof val === "function") {
      setValue(val);
    } else if (typeof val === "object" && "nativeEvent" in val) {
      const { currentTarget } = val;
      if (currentTarget instanceof HTMLInputElement) {
        if (currentTarget.type === "checkbox") {
          setValue(currentTarget.checked);
        } else {
          setValue(currentTarget.value);
        }
      } else if (currentTarget instanceof HTMLTextAreaElement || currentTarget instanceof HTMLSelectElement) {
        setValue(currentTarget.value);
      }
    } else {
      setValue(val);
    }
  };
}
var init_get_input_on_change = __esm({
  "node_modules/@mantine/form/esm/get-input-on-change/get-input-on-change.mjs"() {
    "use client";
  }
});

// node_modules/@mantine/form/esm/hooks/use-form-errors/filter-errors/filter-errors.mjs
function filterErrors(errors) {
  if (errors === null || typeof errors !== "object") {
    return {};
  }
  return Object.keys(errors).reduce((acc, key) => {
    const errorValue = errors[key];
    if (errorValue !== void 0 && errorValue !== null && errorValue !== false) {
      acc[key] = errorValue;
    }
    return acc;
  }, {});
}
var init_filter_errors = __esm({
  "node_modules/@mantine/form/esm/hooks/use-form-errors/filter-errors/filter-errors.mjs"() {
    "use client";
  }
});

// node_modules/@mantine/form/esm/hooks/use-form-errors/use-form-errors.mjs
function useFormErrors(initialErrors) {
  const [errorsState, setErrorsState] = (0, import_react226.useState)(filterErrors(initialErrors));
  const errorsRef = (0, import_react226.useRef)(errorsState);
  const setErrors = (0, import_react226.useCallback)((errors) => {
    setErrorsState((current) => {
      const newErrors = filterErrors(typeof errors === "function" ? errors(current) : errors);
      errorsRef.current = newErrors;
      return newErrors;
    });
  }, []);
  const clearErrors = (0, import_react226.useCallback)(() => setErrors({}), []);
  const clearFieldError = (0, import_react226.useCallback)(
    (path) => {
      if (errorsRef.current[path] === void 0) {
        return;
      }
      setErrors((current) => {
        const errors = { ...current };
        delete errors[path];
        return errors;
      });
    },
    [errorsState]
  );
  const setFieldError = (0, import_react226.useCallback)(
    (path, error) => {
      if (error == null || error === false) {
        clearFieldError(path);
      } else if (errorsRef.current[path] !== error) {
        setErrors((current) => ({ ...current, [path]: error }));
      }
    },
    [errorsState]
  );
  return {
    errorsState,
    setErrors,
    clearErrors,
    setFieldError,
    clearFieldError
  };
}
var import_react226;
var init_use_form_errors = __esm({
  "node_modules/@mantine/form/esm/hooks/use-form-errors/use-form-errors.mjs"() {
    "use client";
    import_react226 = __toESM(require_react(), 1);
    init_filter_errors();
  }
});

// node_modules/@mantine/form/esm/lists/clear-list-state.mjs
function clearListState(field, state2) {
  if (state2 === null || typeof state2 !== "object") {
    return {};
  }
  const clone = { ...state2 };
  Object.keys(state2).forEach((errorKey) => {
    if (errorKey.includes(`${String(field)}.`)) {
      delete clone[errorKey];
    }
  });
  return clone;
}
var init_clear_list_state = __esm({
  "node_modules/@mantine/form/esm/lists/clear-list-state.mjs"() {
    "use client";
  }
});

// node_modules/@mantine/form/esm/lists/change-error-indices.mjs
function getIndexFromKeyAfterPath(key, path) {
  const split = key.substring(path.length + 1).split(".")[0];
  return parseInt(split, 10);
}
function changeErrorIndices(path, index3, errors, change) {
  if (index3 === void 0) {
    return errors;
  }
  const pathString = `${String(path)}`;
  let clearedErrors = errors;
  if (change === -1) {
    clearedErrors = clearListState(`${pathString}.${index3}`, clearedErrors);
  }
  const cloned = { ...clearedErrors };
  const changedKeys = /* @__PURE__ */ new Set();
  Object.entries(clearedErrors).filter(([key]) => {
    if (!key.startsWith(`${pathString}.`)) {
      return false;
    }
    const currIndex = getIndexFromKeyAfterPath(key, pathString);
    if (Number.isNaN(currIndex)) {
      return false;
    }
    return currIndex >= index3;
  }).forEach(([key, value]) => {
    const currIndex = getIndexFromKeyAfterPath(key, pathString);
    const newKey = key.replace(
      `${pathString}.${currIndex}`,
      `${pathString}.${currIndex + change}`
    );
    cloned[newKey] = value;
    changedKeys.add(newKey);
    if (!changedKeys.has(key)) {
      delete cloned[key];
    }
  });
  return cloned;
}
var init_change_error_indices = __esm({
  "node_modules/@mantine/form/esm/lists/change-error-indices.mjs"() {
    "use client";
    init_clear_list_state();
  }
});

// node_modules/@mantine/form/esm/lists/reorder-errors.mjs
function reorderErrors(path, { from, to }, errors) {
  const oldKeyStart = `${path}.${from}`;
  const newKeyStart = `${path}.${to}`;
  const clone = { ...errors };
  Object.keys(errors).every((key) => {
    let oldKey;
    let newKey;
    if (key.startsWith(oldKeyStart)) {
      oldKey = key;
      newKey = key.replace(oldKeyStart, newKeyStart);
    }
    if (key.startsWith(newKeyStart)) {
      oldKey = key.replace(newKeyStart, oldKeyStart);
      newKey = key;
    }
    if (oldKey && newKey) {
      const value1 = clone[oldKey];
      const value2 = clone[newKey];
      value2 === void 0 ? delete clone[oldKey] : clone[oldKey] = value2;
      value1 === void 0 ? delete clone[newKey] : clone[newKey] = value1;
      return false;
    }
    return true;
  });
  return clone;
}
var init_reorder_errors = __esm({
  "node_modules/@mantine/form/esm/lists/reorder-errors.mjs"() {
    "use client";
  }
});

// node_modules/klona/full/index.mjs
function set(obj, key, val) {
  if (typeof val.value === "object") val.value = klona(val.value);
  if (!val.enumerable || val.get || val.set || !val.configurable || !val.writable || key === "__proto__") {
    Object.defineProperty(obj, key, val);
  } else obj[key] = val.value;
}
function klona(x3) {
  if (typeof x3 !== "object") return x3;
  var i13 = 0, k5, list, tmp, str = Object.prototype.toString.call(x3);
  if (str === "[object Object]") {
    tmp = Object.create(x3.__proto__ || null);
  } else if (str === "[object Array]") {
    tmp = Array(x3.length);
  } else if (str === "[object Set]") {
    tmp = /* @__PURE__ */ new Set();
    x3.forEach(function(val) {
      tmp.add(klona(val));
    });
  } else if (str === "[object Map]") {
    tmp = /* @__PURE__ */ new Map();
    x3.forEach(function(val, key) {
      tmp.set(klona(key), klona(val));
    });
  } else if (str === "[object Date]") {
    tmp = /* @__PURE__ */ new Date(+x3);
  } else if (str === "[object RegExp]") {
    tmp = new RegExp(x3.source, x3.flags);
  } else if (str === "[object DataView]") {
    tmp = new x3.constructor(klona(x3.buffer));
  } else if (str === "[object ArrayBuffer]") {
    tmp = x3.slice(0);
  } else if (str.slice(-6) === "Array]") {
    tmp = new x3.constructor(x3);
  }
  if (tmp) {
    for (list = Object.getOwnPropertySymbols(x3); i13 < list.length; i13++) {
      set(tmp, list[i13], Object.getOwnPropertyDescriptor(x3, list[i13]));
    }
    for (i13 = 0, list = Object.getOwnPropertyNames(x3); i13 < list.length; i13++) {
      if (Object.hasOwnProperty.call(tmp, k5 = list[i13]) && tmp[k5] === x3[k5]) continue;
      set(tmp, k5, Object.getOwnPropertyDescriptor(x3, k5));
    }
  }
  return tmp || x3;
}
var init_full = __esm({
  "node_modules/klona/full/index.mjs"() {
  }
});

// node_modules/@mantine/form/esm/paths/get-splitted-path.mjs
function getSplittedPath(path) {
  if (typeof path !== "string") {
    return [];
  }
  return path.split(".");
}
var init_get_splitted_path = __esm({
  "node_modules/@mantine/form/esm/paths/get-splitted-path.mjs"() {
    "use client";
  }
});

// node_modules/@mantine/form/esm/paths/get-path.mjs
function getPath(path, values2) {
  const splittedPath = getSplittedPath(path);
  if (splittedPath.length === 0 || typeof values2 !== "object" || values2 === null) {
    return void 0;
  }
  let value = values2[splittedPath[0]];
  for (let i13 = 1; i13 < splittedPath.length; i13 += 1) {
    if (value == null) {
      break;
    }
    value = value[splittedPath[i13]];
  }
  return value;
}
var init_get_path = __esm({
  "node_modules/@mantine/form/esm/paths/get-path.mjs"() {
    "use client";
    init_get_splitted_path();
  }
});

// node_modules/@mantine/form/esm/paths/set-path.mjs
function setPath(path, value, values2) {
  const splittedPath = getSplittedPath(path);
  if (splittedPath.length === 0) {
    return values2;
  }
  const cloned = klona(values2);
  if (splittedPath.length === 1) {
    cloned[splittedPath[0]] = value;
    return cloned;
  }
  let val = cloned[splittedPath[0]];
  for (let i13 = 1; i13 < splittedPath.length - 1; i13 += 1) {
    if (val === void 0) {
      return cloned;
    }
    val = val[splittedPath[i13]];
  }
  val[splittedPath[splittedPath.length - 1]] = value;
  return cloned;
}
var init_set_path = __esm({
  "node_modules/@mantine/form/esm/paths/set-path.mjs"() {
    "use client";
    init_full();
    init_get_splitted_path();
  }
});

// node_modules/@mantine/form/esm/paths/reorder-path.mjs
function reorderPath(path, { from, to }, values2) {
  const currentValue = getPath(path, values2);
  if (!Array.isArray(currentValue)) {
    return values2;
  }
  const cloned = [...currentValue];
  const item2 = currentValue[from];
  cloned.splice(from, 1);
  cloned.splice(to, 0, item2);
  return setPath(path, cloned, values2);
}
var init_reorder_path = __esm({
  "node_modules/@mantine/form/esm/paths/reorder-path.mjs"() {
    "use client";
    init_get_path();
    init_set_path();
  }
});

// node_modules/@mantine/form/esm/paths/insert-path.mjs
function insertPath(path, value, index3, values2) {
  const currentValue = getPath(path, values2);
  if (!Array.isArray(currentValue)) {
    return values2;
  }
  const cloned = [...currentValue];
  cloned.splice(typeof index3 === "number" ? index3 : cloned.length, 0, value);
  return setPath(path, cloned, values2);
}
var init_insert_path = __esm({
  "node_modules/@mantine/form/esm/paths/insert-path.mjs"() {
    "use client";
    init_get_path();
    init_set_path();
  }
});

// node_modules/@mantine/form/esm/paths/remove-path.mjs
function removePath(path, index3, values2) {
  const currentValue = getPath(path, values2);
  if (!Array.isArray(currentValue)) {
    return values2;
  }
  return setPath(
    path,
    currentValue.filter((_2, itemIndex) => itemIndex !== index3),
    values2
  );
}
var init_remove_path = __esm({
  "node_modules/@mantine/form/esm/paths/remove-path.mjs"() {
    "use client";
    init_get_path();
    init_set_path();
  }
});

// node_modules/@mantine/form/esm/paths/replace-path.mjs
function replacePath(path, item2, index3, values2) {
  const currentValue = getPath(path, values2);
  if (!Array.isArray(currentValue)) {
    return values2;
  }
  if (currentValue.length <= index3) {
    return values2;
  }
  const cloned = [...currentValue];
  cloned[index3] = item2;
  return setPath(path, cloned, values2);
}
var init_replace_path = __esm({
  "node_modules/@mantine/form/esm/paths/replace-path.mjs"() {
    "use client";
    init_get_path();
    init_set_path();
  }
});

// node_modules/@mantine/form/esm/hooks/use-form-list/use-form-list.mjs
function useFormList({
  $values,
  $errors,
  $status
}) {
  const reorderListItem = (0, import_react227.useCallback)((path, payload) => {
    $status.clearFieldDirty(path);
    $errors.setErrors((errs) => reorderErrors(path, payload, errs));
    $values.setValues({
      values: reorderPath(path, payload, $values.refValues.current),
      updateState: true
    });
  }, []);
  const removeListItem = (0, import_react227.useCallback)((path, index3) => {
    $status.clearFieldDirty(path);
    $errors.setErrors((errs) => changeErrorIndices(path, index3, errs, -1));
    $values.setValues({
      values: removePath(path, index3, $values.refValues.current),
      updateState: true
    });
  }, []);
  const insertListItem = (0, import_react227.useCallback)((path, item2, index3) => {
    $status.clearFieldDirty(path);
    $errors.setErrors((errs) => changeErrorIndices(path, index3, errs, 1));
    $values.setValues({
      values: insertPath(path, item2, index3, $values.refValues.current),
      updateState: true
    });
  }, []);
  const replaceListItem = (0, import_react227.useCallback)((path, index3, item2) => {
    $status.clearFieldDirty(path);
    $values.setValues({
      values: replacePath(path, item2, index3, $values.refValues.current),
      updateState: true
    });
  }, []);
  return { reorderListItem, removeListItem, insertListItem, replaceListItem };
}
var import_react227;
var init_use_form_list = __esm({
  "node_modules/@mantine/form/esm/hooks/use-form-list/use-form-list.mjs"() {
    "use client";
    import_react227 = __toESM(require_react(), 1);
    init_change_error_indices();
    init_reorder_errors();
    init_full();
    init_reorder_path();
    init_insert_path();
    init_remove_path();
    init_replace_path();
  }
});

// node_modules/fast-deep-equal/index.js
var require_fast_deep_equal = __commonJS({
  "node_modules/fast-deep-equal/index.js"(exports, module) {
    "use strict";
    module.exports = function equal(a18, b4) {
      if (a18 === b4) return true;
      if (a18 && b4 && typeof a18 == "object" && typeof b4 == "object") {
        if (a18.constructor !== b4.constructor) return false;
        var length, i13, keys2;
        if (Array.isArray(a18)) {
          length = a18.length;
          if (length != b4.length) return false;
          for (i13 = length; i13-- !== 0; )
            if (!equal(a18[i13], b4[i13])) return false;
          return true;
        }
        if (a18.constructor === RegExp) return a18.source === b4.source && a18.flags === b4.flags;
        if (a18.valueOf !== Object.prototype.valueOf) return a18.valueOf() === b4.valueOf();
        if (a18.toString !== Object.prototype.toString) return a18.toString() === b4.toString();
        keys2 = Object.keys(a18);
        length = keys2.length;
        if (length !== Object.keys(b4).length) return false;
        for (i13 = length; i13-- !== 0; )
          if (!Object.prototype.hasOwnProperty.call(b4, keys2[i13])) return false;
        for (i13 = length; i13-- !== 0; ) {
          var key = keys2[i13];
          if (!equal(a18[key], b4[key])) return false;
        }
        return true;
      }
      return a18 !== a18 && b4 !== b4;
    };
  }
});

// node_modules/@mantine/form/esm/get-status/get-status.mjs
function getStatus(status2, path) {
  const paths = Object.keys(status2);
  if (typeof path === "string") {
    const nestedPaths = paths.filter((statusPath) => statusPath.startsWith(`${path}.`));
    return status2[path] || nestedPaths.some((statusPath) => status2[statusPath]) || false;
  }
  return paths.some((statusPath) => status2[statusPath]);
}
var init_get_status = __esm({
  "node_modules/@mantine/form/esm/get-status/get-status.mjs"() {
    "use client";
  }
});

// node_modules/@mantine/form/esm/hooks/use-form-status/use-form-status.mjs
function useFormStatus({
  initialDirty,
  initialTouched,
  mode,
  $values
}) {
  const [touchedState, setTouchedState] = (0, import_react228.useState)(initialTouched);
  const [dirtyState, setDirtyState] = (0, import_react228.useState)(initialDirty);
  const touchedRef = (0, import_react228.useRef)(initialTouched);
  const dirtyRef = (0, import_react228.useRef)(initialDirty);
  const setTouched = (0, import_react228.useCallback)((values2) => {
    const resolvedValues = typeof values2 === "function" ? values2(touchedRef.current) : values2;
    touchedRef.current = resolvedValues;
    if (mode === "controlled") {
      setTouchedState(resolvedValues);
    }
  }, []);
  const setDirty = (0, import_react228.useCallback)(
    (values2, forceUpdate = false) => {
      const resolvedValues = typeof values2 === "function" ? values2(dirtyRef.current) : values2;
      dirtyRef.current = resolvedValues;
      if (mode === "controlled" || forceUpdate) {
        setDirtyState(resolvedValues);
      }
    },
    []
  );
  const resetTouched = (0, import_react228.useCallback)(() => setTouched({}), []);
  const resetDirty = (0, import_react228.useCallback)((values2) => {
    const newSnapshot = values2 ? { ...values2, ...$values.refValues.current } : $values.refValues.current;
    $values.setValuesSnapshot(newSnapshot);
    setDirty({});
  }, []);
  const setFieldTouched = (0, import_react228.useCallback)((path, touched) => {
    setTouched((currentTouched) => {
      if (getStatus(currentTouched, path) === touched) {
        return currentTouched;
      }
      return { ...currentTouched, [path]: touched };
    });
  }, []);
  const setFieldDirty = (0, import_react228.useCallback)((path, dirty, forceUpdate) => {
    setDirty((currentDirty) => {
      if (getStatus(currentDirty, path) === dirty) {
        return currentDirty;
      }
      return { ...currentDirty, [path]: dirty };
    }, forceUpdate);
  }, []);
  const setCalculatedFieldDirty = (0, import_react228.useCallback)((path, value) => {
    const currentDirty = getStatus(dirtyRef.current, path);
    const dirty = !(0, import_fast_deep_equal.default)(getPath(path, $values.getValuesSnapshot()), value);
    const clearedState = clearListState(path, dirtyRef.current);
    clearedState[path] = dirty;
    setDirty(clearedState, currentDirty !== dirty);
  }, []);
  const isTouched = (0, import_react228.useCallback)(
    (path) => getStatus(touchedRef.current, path),
    []
  );
  const clearFieldDirty = (0, import_react228.useCallback)(
    (path) => setDirty((current) => {
      if (typeof path !== "string") {
        return current;
      }
      const result = clearListState(path, current);
      delete result[path];
      if ((0, import_fast_deep_equal.default)(result, current)) {
        return current;
      }
      return result;
    }),
    []
  );
  const isDirty2 = (0, import_react228.useCallback)((path) => {
    if (path) {
      const overriddenValue = getPath(path, dirtyRef.current);
      if (typeof overriddenValue === "boolean") {
        return overriddenValue;
      }
      const sliceOfValues = getPath(path, $values.refValues.current);
      const sliceOfInitialValues = getPath(path, $values.valuesSnapshot.current);
      return !(0, import_fast_deep_equal.default)(sliceOfValues, sliceOfInitialValues);
    }
    const isOverridden = Object.keys(dirtyRef.current).length > 0;
    if (isOverridden) {
      return getStatus(dirtyRef.current);
    }
    return !(0, import_fast_deep_equal.default)($values.refValues.current, $values.valuesSnapshot.current);
  }, []);
  const getDirty = (0, import_react228.useCallback)(() => dirtyRef.current, []);
  const getTouched = (0, import_react228.useCallback)(() => touchedRef.current, []);
  return {
    touchedState,
    dirtyState,
    touchedRef,
    dirtyRef,
    setTouched,
    setDirty,
    resetDirty,
    resetTouched,
    isTouched,
    setFieldTouched,
    setFieldDirty,
    setTouchedState,
    setDirtyState,
    clearFieldDirty,
    isDirty: isDirty2,
    getDirty,
    getTouched,
    setCalculatedFieldDirty
  };
}
var import_react228, import_fast_deep_equal;
var init_use_form_status = __esm({
  "node_modules/@mantine/form/esm/hooks/use-form-status/use-form-status.mjs"() {
    "use client";
    import_react228 = __toESM(require_react(), 1);
    import_fast_deep_equal = __toESM(require_fast_deep_equal(), 1);
    init_get_status();
    init_clear_list_state();
    init_get_path();
    init_full();
  }
});

// node_modules/@mantine/form/esm/hooks/use-form-values/use-form-values.mjs
function useFormValues({
  initialValues,
  onValuesChange,
  mode
}) {
  const initialized = (0, import_react229.useRef)(false);
  const [stateValues, setStateValues] = (0, import_react229.useState)(initialValues || {});
  const refValues = (0, import_react229.useRef)(stateValues);
  const valuesSnapshot = (0, import_react229.useRef)(stateValues);
  const setValues = (0, import_react229.useCallback)(
    ({
      values: values2,
      subscribers,
      updateState = true,
      mergeWithPreviousValues = true
    }) => {
      const previousValues = refValues.current;
      const resolvedValues = values2 instanceof Function ? values2(refValues.current) : values2;
      const updatedValues = mergeWithPreviousValues ? { ...previousValues, ...resolvedValues } : resolvedValues;
      refValues.current = updatedValues;
      updateState && setStateValues(updatedValues);
      onValuesChange?.(updatedValues, previousValues);
      subscribers?.filter(Boolean).forEach((subscriber) => subscriber({ updatedValues, previousValues }));
    },
    [onValuesChange]
  );
  const setFieldValue = (0, import_react229.useCallback)(
    (payload) => {
      const currentValue = getPath(payload.path, refValues.current);
      const updatedValue = payload.value instanceof Function ? payload.value(currentValue) : payload.value;
      if (currentValue !== updatedValue) {
        const previousValues = refValues.current;
        const updatedValues = setPath(payload.path, updatedValue, refValues.current);
        setValues({ values: updatedValues, updateState: payload.updateState });
        payload.subscribers?.filter(Boolean).forEach(
          (subscriber) => subscriber({ path: payload.path, updatedValues, previousValues })
        );
      }
    },
    [setValues]
  );
  const setValuesSnapshot = (0, import_react229.useCallback)((payload) => {
    valuesSnapshot.current = payload;
  }, []);
  const initialize = (0, import_react229.useCallback)(
    (values2, onInitialize) => {
      if (!initialized.current) {
        initialized.current = true;
        setValues({ values: values2, updateState: mode === "controlled" });
        setValuesSnapshot(values2);
        onInitialize();
      }
    },
    [setValues]
  );
  const resetValues = (0, import_react229.useCallback)(() => {
    setValues({
      values: valuesSnapshot.current,
      updateState: true,
      mergeWithPreviousValues: false
    });
  }, [setValues]);
  const getValues = (0, import_react229.useCallback)(() => refValues.current, []);
  const getValuesSnapshot = (0, import_react229.useCallback)(() => valuesSnapshot.current, []);
  return {
    initialized,
    stateValues,
    refValues,
    valuesSnapshot,
    setValues,
    setFieldValue,
    resetValues,
    setValuesSnapshot,
    initialize,
    getValues,
    getValuesSnapshot
  };
}
var import_react229;
var init_use_form_values = __esm({
  "node_modules/@mantine/form/esm/hooks/use-form-values/use-form-values.mjs"() {
    "use client";
    import_react229 = __toESM(require_react(), 1);
    init_get_path();
    init_set_path();
  }
});

// node_modules/@mantine/form/esm/hooks/use-form-watch/use-form-watch.mjs
function useFormWatch({
  $status
}) {
  const subscribers = (0, import_react230.useRef)(
    {}
  );
  const watch2 = (0, import_react230.useCallback)((path, callback) => {
    (0, import_react230.useEffect)(() => {
      subscribers.current[path] = subscribers.current[path] || [];
      subscribers.current[path].push(callback);
      return () => {
        subscribers.current[path] = subscribers.current[path].filter((cb) => cb !== callback);
      };
    }, [callback]);
  }, []);
  const getFieldSubscribers = (0, import_react230.useCallback)((path) => {
    if (!subscribers.current[path]) {
      return [];
    }
    return subscribers.current[path].map(
      (callback) => (input2) => callback({
        previousValue: getPath(path, input2.previousValues),
        value: getPath(path, input2.updatedValues),
        touched: $status.isTouched(path),
        dirty: $status.isDirty(path)
      })
    );
  }, []);
  return {
    subscribers,
    watch: watch2,
    getFieldSubscribers
  };
}
var import_react230;
var init_use_form_watch = __esm({
  "node_modules/@mantine/form/esm/hooks/use-form-watch/use-form-watch.mjs"() {
    "use client";
    import_react230 = __toESM(require_react(), 1);
    init_get_path();
    init_full();
  }
});

// node_modules/@mantine/form/esm/paths/get-data-path.mjs
function getDataPath(formName, fieldPath) {
  return formName ? `${formName}-${fieldPath.toString()}` : fieldPath.toString();
}
var init_get_data_path = __esm({
  "node_modules/@mantine/form/esm/paths/get-data-path.mjs"() {
    "use client";
  }
});

// node_modules/@mantine/form/esm/validate/validate-values.mjs
function getValidationResults(errors) {
  const filteredErrors = filterErrors(errors);
  return { hasErrors: Object.keys(filteredErrors).length > 0, errors: filteredErrors };
}
function validateRulesRecord(rules, values2, path = "", errors = {}) {
  if (typeof rules !== "object" || rules === null) {
    return errors;
  }
  return Object.keys(rules).reduce((acc, ruleKey) => {
    const rule = rules[ruleKey];
    const rulePath = `${path === "" ? "" : `${path}.`}${ruleKey}`;
    const value = getPath(rulePath, values2);
    let arrayValidation = false;
    if (typeof rule === "function") {
      acc[rulePath] = rule(value, values2, rulePath);
    }
    if (typeof rule === "object" && Array.isArray(value)) {
      arrayValidation = true;
      value.forEach(
        (_item, index3) => validateRulesRecord(rule, values2, `${rulePath}.${index3}`, acc)
      );
    }
    if (typeof rule === "object" && typeof value === "object" && value !== null) {
      if (!arrayValidation) {
        validateRulesRecord(rule, values2, rulePath, acc);
      }
    }
    return acc;
  }, errors);
}
function validateValues(validate2, values2) {
  if (typeof validate2 === "function") {
    return getValidationResults(validate2(values2));
  }
  return getValidationResults(validateRulesRecord(validate2, values2));
}
var init_validate_values = __esm({
  "node_modules/@mantine/form/esm/validate/validate-values.mjs"() {
    "use client";
    init_filter_errors();
    init_get_path();
    init_full();
  }
});

// node_modules/@mantine/form/esm/validate/validate-field-value.mjs
function validateFieldValue(path, rules, values2) {
  if (typeof path !== "string") {
    return { hasError: false, error: null };
  }
  const results = validateValues(rules, values2);
  const pathInError = Object.keys(results.errors).find(
    (errorKey) => path.split(".").every((pathPart, i13) => pathPart === errorKey.split(".")[i13])
  );
  return { hasError: !!pathInError, error: pathInError ? results.errors[pathInError] : null };
}
var init_validate_field_value = __esm({
  "node_modules/@mantine/form/esm/validate/validate-field-value.mjs"() {
    "use client";
    init_validate_values();
  }
});

// node_modules/@mantine/form/esm/form-index.mjs
var FORM_INDEX;
var init_form_index = __esm({
  "node_modules/@mantine/form/esm/form-index.mjs"() {
    "use client";
    FORM_INDEX = "__MANTINE_FORM_INDEX__";
  }
});

// node_modules/@mantine/form/esm/validate/should-validate-on-change.mjs
function shouldValidateOnChange(path, validateInputOnChange) {
  if (!validateInputOnChange) {
    return false;
  }
  if (typeof validateInputOnChange === "boolean") {
    return validateInputOnChange;
  }
  if (Array.isArray(validateInputOnChange)) {
    return validateInputOnChange.includes(path.replace(/[.][0-9]+/g, `.${FORM_INDEX}`));
  }
  return false;
}
var init_should_validate_on_change = __esm({
  "node_modules/@mantine/form/esm/validate/should-validate-on-change.mjs"() {
    "use client";
    init_form_index();
  }
});

// node_modules/@mantine/form/esm/use-form.mjs
function useForm({
  name: name2,
  mode = "controlled",
  initialValues,
  initialErrors = {},
  initialDirty = {},
  initialTouched = {},
  clearInputErrorOnChange = true,
  validateInputOnChange = false,
  validateInputOnBlur = false,
  onValuesChange,
  transformValues = (values2) => values2,
  enhanceGetInputProps,
  validate: rules,
  onSubmitPreventDefault = "always",
  touchTrigger = "change"
} = {}) {
  const $errors = useFormErrors(initialErrors);
  const $values = useFormValues({ initialValues, onValuesChange, mode });
  const $status = useFormStatus({ initialDirty, initialTouched, $values, mode });
  const $list = useFormList({ $values, $errors, $status });
  const $watch = useFormWatch({ $status });
  const [formKey, setFormKey] = (0, import_react231.useState)(0);
  const [fieldKeys, setFieldKeys] = (0, import_react231.useState)({});
  const [submitting, setSubmitting] = (0, import_react231.useState)(false);
  const reset2 = (0, import_react231.useCallback)(() => {
    $values.resetValues();
    $errors.clearErrors();
    $status.resetDirty();
    $status.resetTouched();
    mode === "uncontrolled" && setFormKey((key2) => key2 + 1);
  }, []);
  const handleValuesChanges = (0, import_react231.useCallback)(
    (previousValues) => {
      clearInputErrorOnChange && $errors.clearErrors();
      mode === "uncontrolled" && setFormKey((key2) => key2 + 1);
      Object.keys($watch.subscribers.current).forEach((path) => {
        const value = getPath(path, $values.refValues.current);
        const previousValue = getPath(path, previousValues);
        if (value !== previousValue) {
          $watch.getFieldSubscribers(path).forEach((cb) => cb({ previousValues, updatedValues: $values.refValues.current }));
        }
      });
    },
    [clearInputErrorOnChange]
  );
  const initialize = (0, import_react231.useCallback)(
    (values2) => {
      const previousValues = $values.refValues.current;
      $values.initialize(values2, () => mode === "uncontrolled" && setFormKey((key2) => key2 + 1));
      handleValuesChanges(previousValues);
    },
    [handleValuesChanges]
  );
  const setFieldValue = (0, import_react231.useCallback)(
    (path, value, options) => {
      const shouldValidate = shouldValidateOnChange(path, validateInputOnChange);
      const resolvedValue = value instanceof Function ? value(getPath(path, $values.refValues.current)) : value;
      $status.setCalculatedFieldDirty(path, resolvedValue);
      touchTrigger === "change" && $status.setFieldTouched(path, true);
      !shouldValidate && clearInputErrorOnChange && $errors.clearFieldError(path);
      $values.setFieldValue({
        path,
        value,
        updateState: mode === "controlled",
        subscribers: [
          ...$watch.getFieldSubscribers(path),
          shouldValidate ? (payload) => {
            const validationResults = validateFieldValue(path, rules, payload.updatedValues);
            validationResults.hasError ? $errors.setFieldError(path, validationResults.error) : $errors.clearFieldError(path);
          } : null,
          options?.forceUpdate !== false && mode !== "controlled" ? () => setFieldKeys((keys2) => ({
            ...keys2,
            [path]: (keys2[path] || 0) + 1
          })) : null
        ]
      });
    },
    [onValuesChange, rules]
  );
  const setValues = (0, import_react231.useCallback)(
    (values2) => {
      const previousValues = $values.refValues.current;
      $values.setValues({ values: values2, updateState: mode === "controlled" });
      handleValuesChanges(previousValues);
    },
    [onValuesChange, handleValuesChanges]
  );
  const validate2 = (0, import_react231.useCallback)(() => {
    const results = validateValues(rules, $values.refValues.current);
    $errors.setErrors(results.errors);
    return results;
  }, [rules]);
  const validateField = (0, import_react231.useCallback)(
    (path) => {
      const results = validateFieldValue(path, rules, $values.refValues.current);
      results.hasError ? $errors.setFieldError(path, results.error) : $errors.clearFieldError(path);
      return results;
    },
    [rules]
  );
  const getInputProps = (path, { type: type2 = "input", withError = true, withFocus = true, ...otherOptions } = {}) => {
    const onChange = getInputOnChange(
      (value) => setFieldValue(path, value, { forceUpdate: false })
    );
    const payload = { onChange, "data-path": getDataPath(name2, path) };
    if (withError) {
      payload.error = $errors.errorsState[path];
    }
    if (type2 === "checkbox") {
      payload[mode === "controlled" ? "checked" : "defaultChecked"] = getPath(
        path,
        $values.refValues.current
      );
    } else {
      payload[mode === "controlled" ? "value" : "defaultValue"] = getPath(
        path,
        $values.refValues.current
      );
    }
    if (withFocus) {
      payload.onFocus = () => $status.setFieldTouched(path, true);
      payload.onBlur = () => {
        if (shouldValidateOnChange(path, validateInputOnBlur)) {
          const validationResults = validateFieldValue(path, rules, $values.refValues.current);
          validationResults.hasError ? $errors.setFieldError(path, validationResults.error) : $errors.clearFieldError(path);
        }
      };
    }
    return Object.assign(
      payload,
      enhanceGetInputProps?.({
        inputProps: payload,
        field: path,
        options: { type: type2, withError, withFocus, ...otherOptions },
        form
      })
    );
  };
  const onSubmit = (handleSubmit, handleValidationFailure) => (event) => {
    if (onSubmitPreventDefault === "always") {
      event?.preventDefault();
    }
    const results = validate2();
    if (results.hasErrors) {
      if (onSubmitPreventDefault === "validation-failed") {
        event?.preventDefault();
      }
      handleValidationFailure?.(results.errors, $values.refValues.current, event);
    } else {
      const submitResult = handleSubmit?.(
        transformValues($values.refValues.current),
        event
      );
      if (submitResult instanceof Promise) {
        setSubmitting(true);
        submitResult.finally(() => setSubmitting(false));
      }
    }
  };
  const getTransformedValues = (input2) => transformValues(input2 || $values.refValues.current);
  const onReset = (0, import_react231.useCallback)((event) => {
    event.preventDefault();
    reset2();
  }, []);
  const isValid2 = (0, import_react231.useCallback)(
    (path) => path ? !validateFieldValue(path, rules, $values.refValues.current).hasError : !validateValues(rules, $values.refValues.current).hasErrors,
    [rules]
  );
  const key = (path) => `${formKey}-${path}-${fieldKeys[path] || 0}`;
  const getInputNode = (0, import_react231.useCallback)(
    (path) => document.querySelector(`[data-path="${getDataPath(name2, path)}"]`),
    []
  );
  const form = {
    watch: $watch.watch,
    initialized: $values.initialized.current,
    values: $values.stateValues,
    getValues: $values.getValues,
    setInitialValues: $values.setValuesSnapshot,
    initialize,
    setValues,
    setFieldValue,
    submitting,
    setSubmitting,
    errors: $errors.errorsState,
    setErrors: $errors.setErrors,
    setFieldError: $errors.setFieldError,
    clearFieldError: $errors.clearFieldError,
    clearErrors: $errors.clearErrors,
    resetDirty: $status.resetDirty,
    setTouched: $status.setTouched,
    setDirty: $status.setDirty,
    isTouched: $status.isTouched,
    resetTouched: $status.resetTouched,
    isDirty: $status.isDirty,
    getTouched: $status.getTouched,
    getDirty: $status.getDirty,
    reorderListItem: $list.reorderListItem,
    insertListItem: $list.insertListItem,
    removeListItem: $list.removeListItem,
    replaceListItem: $list.replaceListItem,
    reset: reset2,
    validate: validate2,
    validateField,
    getInputProps,
    onSubmit,
    onReset,
    isValid: isValid2,
    getTransformedValues,
    key,
    getInputNode
  };
  useFormActions(name2, form);
  return form;
}
var import_react231;
var init_use_form = __esm({
  "node_modules/@mantine/form/esm/use-form.mjs"() {
    "use client";
    import_react231 = __toESM(require_react(), 1);
    init_actions();
    init_get_input_on_change();
    init_use_form_errors();
    init_use_form_list();
    init_use_form_status();
    init_use_form_values();
    init_use_form_watch();
    init_get_path();
    init_full();
    init_get_data_path();
    init_validate_values();
    init_validate_field_value();
    init_should_validate_on_change();
  }
});

// node_modules/@mantine/form/esm/index.mjs
var init_esm6 = __esm({
  "node_modules/@mantine/form/esm/index.mjs"() {
    init_use_form();
  }
});

// node_modules/effector-storage/core/index.js
function k4(k5) {
  var { adapter: g7, store: v5, source: y4 = v5, target: p11 = v5, clock: m12 = y4, done: h6, fail: w11 = u3, finally: x3, pickup: E6, context: P4, key: D5, keyPrefix: M6 = "", contract: b4 } = k5;
  if (!g7) throw Error("Adapter is not defined");
  if (!y4) throw Error("Store or source is not defined");
  if (!p11) throw Error("Target is not defined");
  if (!D5 && y4.shortName === y4.id) throw Error("Key or name is not defined");
  if (y4 === p11 && !ae.store(y4)) throw Error("Source must be different from target");
  void 0 === k5.def && ae.store(y4) && (k5.def = y4.defaultState);
  var S2 = "factory" in g7 ? g7(k5) : g7, z2 = D5 || y4.shortName, A4 = function(r7, t20) {
    var o8 = l11.get(r7);
    void 0 === o8 && (o8 = /* @__PURE__ */ new Map(), l11.set(r7, o8));
    var a18 = o8.get(t20);
    return void 0 !== a18 || (a18 = h(null, { serialize: "ignore" }), o8.set(t20, a18)), a18;
  }(S2.keyArea || S2, M6 + z2), N4 = a(), K3 = () => vt(N4), T3 = (e14) => ({ status: r7 = "fail", params: t20, result: o8, error: a18 }) => "done" === r7 ? { status: r7, key: z2, keyPrefix: M6, operation: e14, value: "get" === e14 ? o8 : t20 } : { status: r7, key: z2, keyPrefix: M6, operation: e14, value: "function" == typeof t20 ? void 0 : t20, error: a18 };
  return n(N4, () => {
    var t20 = h([], { serialize: "ignore" }), o8 = S2(M6 + z2, (e14) => {
      g8(e14);
    }), a18 = v({ source: t20, effect: d11(o8.get) }), c15 = v({ source: t20, effect: d11(o8.set) }), l12 = b(/* @__PURE__ */ ((e14) => (r7) => !e14 || void 0 === r7 || ("isData" in e14 ? e14.isData(r7) : e14(r7)) ? r7 : (() => {
      throw "getErrorMessages" in e14 ? e14.getErrorMessages(r7) : void 0;
    })())(b4)), u4 = p(), k6 = p(), g8 = a18;
    t20.updates.watch(() => {
      g8 = F(a18, { safe: true });
    }), x({ clock: m12, source: y4, target: k6 }), x({ clock: k6, source: A4, filter: (e14, r7) => r7 !== e14, fn: (e14, r7) => r7, target: c15 }), x({ clock: [a18.doneData, x(c15, c15.done)], filter: (e14) => void 0 !== e14, target: A4 }), x({ clock: [a18.doneData, A4], target: l12 }), x({ clock: l12.doneData, filter: (e14) => void 0 !== e14, target: p11 }), x({ clock: [a18.finally.map(T3("get")), c15.finally.map(T3("set")), l12.fail.map(T3("validate"))], target: u4 }), x3 && x({ clock: u4, target: x3 }), h6 && x({ clock: u4, filter: ({ status: e14 }) => "done" === e14, fn: ({ key: e14, keyPrefix: r7, operation: t21, value: o9 }) => ({ key: e14, keyPrefix: r7, operation: t21, value: o9 }), target: h6 }), x({ clock: u4, filter: ({ status: e14 }) => "fail" === e14, fn: ({ key: e14, keyPrefix: r7, operation: t21, error: o9, value: a19 }) => ({ key: e14, keyPrefix: r7, operation: t21, error: o9, value: a19 }), target: w11 }), P4 && t20.on(P4, ([e14], r7) => [void 0 === r7 ? e14 : r7]), E6 ? (x({ clock: E6, fn: () => {
    }, target: a18 }), t20.on(E6, ([e14], r7) => [void 0 === r7 ? e14 : r7])) : a18();
  }), K3.unsubscribe = K3;
}
var l11, d11, u3;
var init_core2 = __esm({
  "node_modules/effector-storage/core/index.js"() {
    init_effector();
    l11 = /* @__PURE__ */ new Map();
    d11 = (e14) => ([r7], t20) => e14(t20, r7);
    u3 = p();
    u3.watch((e14) => console.error(e14.error));
  }
});

// node_modules/effector-storage/nil/index.js
function e12({ keyArea: e14 = "" } = {}) {
  var r7 = () => ({ get() {
  }, set() {
  } });
  return r7.keyArea = e14, r7.noop = true, r7;
}
var init_nil = __esm({
  "node_modules/effector-storage/nil/index.js"() {
    e12.factory = true;
  }
});

// node_modules/effector-storage/storage/index.js
function e13({ storage: e14, sync: t20 = false, serialize: r7 = JSON.stringify, deserialize: n13 = JSON.parse, timeout: i13, def: o8 }) {
  var d12 = (d13, a18) => {
    var s36, u4, v5, f15 = () => v5.setItem(d13, r7(u4)), l12 = (e15) => {
      s36 = clearTimeout(s36), e15 && f15(), "undefined" != typeof removeEventListener && removeEventListener("beforeunload", l12);
    };
    return t20 && "undefined" != typeof addEventListener && addEventListener("storage", (r8) => {
      r8.storageArea === e14() && (r8.key === d13 && a18("force" === t20 ? void 0 : r8.newValue), null === r8.key && a18(null));
    }), { get(t21) {
      l12();
      var r8 = void 0 !== t21 ? t21 : e14().getItem(d13);
      return null === r8 ? void 0 !== o8 ? o8 : t21 : n13(r8);
    }, set(t21) {
      u4 = t21, v5 = e14(), void 0 === i13 ? f15() : s36 || (s36 = setTimeout(l12, i13, 1), "undefined" != typeof addEventListener && addEventListener("beforeunload", l12));
    } };
  };
  try {
    d12.keyArea = e14();
  } catch (e15) {
  }
  return d12;
}
var init_storage = __esm({
  "node_modules/effector-storage/storage/index.js"() {
    e13.factory = true;
  }
});

// node_modules/effector-storage/local/index.js
function o7(r7) {
  return function() {
    try {
      return "undefined" != typeof localStorage;
    } catch (r8) {
      return true;
    }
  }() ? e13({ storage: () => localStorage, sync: true, ...r7 }) : e12({ keyArea: "local" });
}
function n12(t20) {
  return (e14) => k4({ adapter: o7, ...t20, ...e14 });
}
var a17;
var init_local = __esm({
  "node_modules/effector-storage/local/index.js"() {
    init_core2();
    init_nil();
    init_storage();
    o7.factory = true;
    a17 = n12();
  }
});

// node_modules/uuid/dist/esm/stringify.js
function unsafeStringify(arr, offset4 = 0) {
  return (byteToHex[arr[offset4 + 0]] + byteToHex[arr[offset4 + 1]] + byteToHex[arr[offset4 + 2]] + byteToHex[arr[offset4 + 3]] + "-" + byteToHex[arr[offset4 + 4]] + byteToHex[arr[offset4 + 5]] + "-" + byteToHex[arr[offset4 + 6]] + byteToHex[arr[offset4 + 7]] + "-" + byteToHex[arr[offset4 + 8]] + byteToHex[arr[offset4 + 9]] + "-" + byteToHex[arr[offset4 + 10]] + byteToHex[arr[offset4 + 11]] + byteToHex[arr[offset4 + 12]] + byteToHex[arr[offset4 + 13]] + byteToHex[arr[offset4 + 14]] + byteToHex[arr[offset4 + 15]]).toLowerCase();
}
var byteToHex;
var init_stringify = __esm({
  "node_modules/uuid/dist/esm/stringify.js"() {
    byteToHex = [];
    for (let i13 = 0; i13 < 256; ++i13) {
      byteToHex.push((i13 + 256).toString(16).slice(1));
    }
  }
});

// node_modules/uuid/dist/esm/rng.js
import { randomFillSync } from "crypto";
function rng() {
  if (poolPtr > rnds8Pool.length - 16) {
    randomFillSync(rnds8Pool);
    poolPtr = 0;
  }
  return rnds8Pool.slice(poolPtr, poolPtr += 16);
}
var rnds8Pool, poolPtr;
var init_rng = __esm({
  "node_modules/uuid/dist/esm/rng.js"() {
    rnds8Pool = new Uint8Array(256);
    poolPtr = rnds8Pool.length;
  }
});

// node_modules/uuid/dist/esm/native.js
import { randomUUID } from "crypto";
var native_default;
var init_native = __esm({
  "node_modules/uuid/dist/esm/native.js"() {
    native_default = { randomUUID };
  }
});

// node_modules/uuid/dist/esm/v4.js
function v4(options, buf, offset4) {
  if (native_default.randomUUID && !buf && !options) {
    return native_default.randomUUID();
  }
  options = options || {};
  const rnds = options.random || (options.rng || rng)();
  rnds[6] = rnds[6] & 15 | 64;
  rnds[8] = rnds[8] & 63 | 128;
  if (buf) {
    offset4 = offset4 || 0;
    for (let i13 = 0; i13 < 16; ++i13) {
      buf[offset4 + i13] = rnds[i13];
    }
    return buf;
  }
  return unsafeStringify(rnds);
}
var v4_default;
var init_v4 = __esm({
  "node_modules/uuid/dist/esm/v4.js"() {
    init_native();
    init_rng();
    init_stringify();
    v4_default = v4;
  }
});

// node_modules/uuid/dist/esm/index.js
var init_esm7 = __esm({
  "node_modules/uuid/dist/esm/index.js"() {
    init_v4();
  }
});

// dist/server/chunks/chunk-Cbus1V3P.js
var import_jsx_runtime168, import_react232, getQuestionsQuery, submitAnswersMutation, SubmitTestModal, scaleFormFieldChanged$1, formPageChanged$1, submitScaleForm$1, submitModalStateChanged$1, TestEvents, $surveyId, $preparedQuestions$1, $currentPage$1, $currentQuestion$1, $scaleForm$2, $currentValue$1, $currentProgress, $isSubmitModalShown$1, $isLoadingState, TestStores, appStarted, appService, $uuid, delayedAppStarted, $currentPage, $preparedQuestions, $currentQuestion, $scaleForm$1, $currentValue, scaleFormFieldChanged, formPageChanged, delayed, $scaleForm, $isSubmitModalShown, submitScaleForm, submitModalStateChanged, redirectToFreeReportPageFx, showSubmitErrorFx;
var init_chunk_Cbus1V3P = __esm({
  "dist/server/chunks/chunk-Cbus1V3P.js"() {
    "use strict";
    import_jsx_runtime168 = __toESM(require_jsx_runtime(), 1);
    import_react232 = __toESM(require_react(), 1);
    init_esm2();
    init_chunk_Vlh7XtbV();
    init_clsx();
    init_effector();
    init_core();
    init_chunk_BDsA7YFO();
    init_local();
    init_lodash();
    init_patronum();
    init_esm7();
    init_esm5();
    init_router();
    getQuestionsQuery = ke({
      sid: "-idf33f",
      fn: () => Ir({
        effect: createCommonRequestFx(() => ({
          url: `/api/v1/surveys/questions`
        }))
        // TODO: @test only multiple
        // mapData: ({ result }) => result.filter((el) => el.type === 'multiple_choice'),
        // TODO: @test only single
        // mapData: ({ result }) => result.filter((el) => el.type === 'single_choice'),
        // TODO: @test only scale
        // mapData: ({ result }) => result.filter((el) => el.type === 'scale'),
      }),
      name: "getQuestionsQuery",
      method: "createQuery"
    });
    submitAnswersMutation = ke({
      sid: "-dwzmxg",
      fn: () => jr({
        effect: createInternalRequestFx((body) => ({
          url: `/api/v1/surveys/answers/submit`,
          method: HTTP_METHODS.POST,
          body
        }))
      }),
      name: "submitAnswersMutation",
      method: "createMutation"
    });
    SubmitTestModal = (0, import_react232.memo)(({
      opened,
      onClose,
      onSubmit,
      loading
    }) => /* @__PURE__ */ (0, import_jsx_runtime168.jsx)(Modal, { opened, onClose, centered: true, title: /* @__PURE__ */ (0, import_jsx_runtime168.jsx)(Text, { fz: 16, fw: "bold", children: "\u0417\u0430\u0432\u0435\u0440\u0448\u0438\u0442\u044C \u0442\u0435\u0441\u0442\u0438\u0440\u043E\u0432\u0430\u043D\u0438\u0435?" }), children: /* @__PURE__ */ (0, import_jsx_runtime168.jsxs)(Group, { gap: "sm", justify: "end", children: [
      /* @__PURE__ */ (0, import_jsx_runtime168.jsx)(Button, { size: "sm", radius: "md", variant: "outline", c: "dark.7", bd: "1px solid var(--mantine-color-dark-7)", onClick: onClose, children: "\u041E\u0442\u043C\u0435\u043D\u0438\u0442\u044C" }),
      /* @__PURE__ */ (0, import_jsx_runtime168.jsx)(MainButton, { loading, size: "sm", radius: "md", onClick: onSubmit, children: "\u0417\u0430\u0432\u0435\u0440\u0448\u0438\u0442\u044C" })
    ] }) }));
    SubmitTestModal.displayName = "SubmitTestModal";
    scaleFormFieldChanged$1 = p({
      name: "scaleFormFieldChanged",
      sid: "xa64nj"
    });
    formPageChanged$1 = p({
      name: "formPageChanged",
      sid: "jlkrlc"
    });
    submitScaleForm$1 = p({
      name: "submitScaleForm",
      sid: "-xmifwm"
    });
    submitModalStateChanged$1 = p({
      name: "submitModalStateChanged",
      sid: "-uhnbja"
    });
    TestEvents = {
      scaleFormFieldChanged: scaleFormFieldChanged$1,
      formPageChanged: formPageChanged$1,
      submitModalStateChanged: submitModalStateChanged$1,
      submitScaleForm: submitScaleForm$1
    };
    $surveyId = h(null, {
      name: "$surveyId",
      sid: "phgjfd"
    });
    $preparedQuestions$1 = h(null, {
      name: "$preparedQuestions",
      sid: "twhx53"
    });
    $currentPage$1 = h(1, {
      name: "$currentPage",
      sid: "v6oeh4"
    });
    $currentQuestion$1 = h(null, {
      name: "$currentQuestion",
      sid: "-cgeoue"
    });
    $scaleForm$2 = h({
      answers: []
    }, {
      name: "$scaleForm",
      sid: "-34awu5"
    });
    $currentValue$1 = h(null, {
      name: "$currentValue",
      sid: "vtre3e"
    });
    $currentProgress = h(0, {
      name: "$currentProgress",
      sid: "-1fd944"
    });
    $isSubmitModalShown$1 = h(false, {
      name: "$isSubmitModalShown",
      sid: "-o7do0x"
    });
    $isLoadingState = submitAnswersMutation.$pending;
    TestStores = {
      $currentPage: $currentPage$1,
      $currentValue: $currentValue$1,
      $currentQuestion: $currentQuestion$1,
      $preparedQuestions: $preparedQuestions$1,
      $isLoadingState,
      $scaleForm: $scaleForm$2,
      $isSubmitModalShown: $isSubmitModalShown$1,
      $currentProgress,
      $surveyId
    };
    appStarted = p({
      name: "appStarted",
      sid: "-ru60z1"
    });
    appService = {
      appStarted
    };
    $uuid = h("", {
      name: "$uuid",
      sid: "1mpkgt"
    });
    delayedAppStarted = ke({
      sid: "-9ea67c",
      fn: () => delay(appService.appStarted, 300),
      name: "delayedAppStarted",
      method: "delay"
    });
    a17({
      store: $uuid,
      pickup: appService.appStarted
    });
    x({
      and: [{
        clock: delayedAppStarted,
        source: $uuid,
        fn: (currentUuid) => {
          if (currentUuid.length > 0) return currentUuid;
          return v4_default();
        },
        target: $uuid
      }],
      or: {
        sid: "-xx6i4t"
      }
    });
    ({
      $currentPage,
      $preparedQuestions,
      $currentQuestion,
      $scaleForm: $scaleForm$1,
      $currentValue
    } = TestStores);
    ({
      scaleFormFieldChanged,
      formPageChanged
    } = TestEvents);
    x({
      and: [{
        clock: [$currentPage, $preparedQuestions],
        source: {
          page: $currentPage,
          questions: $preparedQuestions
        },
        fn: ({
          page,
          questions
        }) => {
          if (!questions) return null;
          return questions[page - 1];
        },
        target: $currentQuestion
      }],
      or: {
        sid: "-ponxhz"
      }
    });
    x({
      and: [{
        clock: scaleFormFieldChanged,
        source: $scaleForm$1,
        fn: (form, field) => {
          form.answers[field.index] = field;
          return {
            ...form
          };
        },
        target: $scaleForm$1
      }],
      or: {
        sid: "-p5yxbp"
      }
    });
    delayed = ke({
      sid: "bboj47",
      fn: () => delay(scaleFormFieldChanged, 1e3),
      name: "delayed",
      method: "delay"
    });
    x({
      and: [{
        clock: delayed,
        source: $currentPage,
        filter: (_2, field) => !(field.isMultiple || field.isSingle),
        fn: (page) => page + 1,
        target: formPageChanged
      }],
      or: {
        sid: "-obq8ou"
      }
    });
    x({
      and: [{
        clock: delayed,
        source: {
          form: $scaleForm$1,
          page: $currentPage
        },
        filter: (_2, field) => !(field.isMultiple || field.isSingle),
        fn: ({
          form,
          page
        }) => {
          const currentPage = page - 1;
          if (form.answers && form.answers.length > 0 && "value" in form.answers[currentPage].answer) {
            if (form.answers[currentPage].answer.value) {
              return form.answers[currentPage].answer.value;
            }
            return null;
          }
          return null;
        },
        target: $currentValue
      }],
      or: {
        sid: "-nvs7hd"
      }
    });
    x({
      and: [{
        clock: [$scaleForm$1, formPageChanged],
        source: {
          form: $scaleForm$1,
          page: $currentPage
        },
        fn: ({
          form: {
            answers
          },
          page
        }, pages) => {
          var _a;
          const currentPage = (isNumber_default(pages) ? pages : page) - 1;
          if (!((_a = answers[currentPage]) == null ? void 0 : _a.answer)) return null;
          if (answers && answers[currentPage].answer && isArray_default(answers[currentPage].answer)) {
            return answers[currentPage].answer;
          }
          if (answers && answers.length > 0 && answers[currentPage].isSingle) {
            return answers[currentPage].answer;
          }
          if (answers && answers.length > 0 && "value" in answers[currentPage].answer) {
            return answers[currentPage].answer.value;
          }
          return null;
        },
        target: $currentValue
      }],
      or: {
        sid: "-mwldi9"
      }
    });
    x({
      and: [{
        clock: formPageChanged,
        fn: (page) => {
          window.scrollTo(0, 0);
          return page;
        },
        target: $currentPage
      }],
      or: {
        sid: "-lunkkc"
      }
    });
    a17({
      store: $uuid,
      pickup: appService.appStarted
    });
    ({
      $scaleForm,
      $isSubmitModalShown
    } = TestStores);
    ({
      submitScaleForm,
      submitModalStateChanged
    } = TestEvents);
    redirectToFreeReportPageFx = b(async () => {
      await navigate2("/free-report");
    }, {
      name: "redirectToFreeReportPageFx",
      sid: "-r2e0tw"
    });
    showSubmitErrorFx = b(async (message) => {
      notifications.show({
        color: "red",
        title: "\u041E\u0448\u0438\u0431\u043A\u0430!",
        message
      });
    }, {
      name: "showSubmitErrorFx",
      sid: "icm0c9"
    });
    x({
      and: [{
        clock: submitModalStateChanged,
        source: $isSubmitModalShown,
        fn: (param) => !param,
        target: $isSubmitModalShown
      }],
      or: {
        sid: "6kdsv9"
      }
    });
    x({
      and: [{
        clock: submitScaleForm,
        source: $scaleForm,
        target: submitAnswersMutation.start
      }],
      or: {
        sid: "6zs1hd"
      }
    });
    x({
      and: [{
        clock: submitAnswersMutation.finished.success,
        fn: () => false,
        target: $isSubmitModalShown
      }],
      or: {
        sid: "732t1j"
      }
    });
    x({
      and: [{
        clock: submitAnswersMutation.finished.success,
        filter: () => !(window == null ? void 0 : window.location.origin.includes("free-report")),
        fn: ({
          result
        }) => result.id,
        target: TestStores.$surveyId
      }],
      or: {
        sid: "7hx92a"
      }
    });
    x({
      and: [{
        clock: submitAnswersMutation.finished.success,
        filter: () => !(window == null ? void 0 : window.location.origin.includes("free-report")),
        target: redirectToFreeReportPageFx
      }],
      or: {
        sid: "7xbhoe"
      }
    });
    x({
      and: [{
        clock: submitAnswersMutation.finished.failure,
        fn: (params) => {
          return params.error.message;
        },
        target: showSubmitErrorFx
      }],
      or: {
        sid: "80m98k"
      }
    });
    x({
      and: [{
        clock: TestStores.$scaleForm,
        source: {
          questions: TestStores.$preparedQuestions,
          form: TestStores.$scaleForm
        },
        fn: ({
          questions,
          form: {
            answers
          }
        }) => Number((answers.length / ((questions == null ? void 0 : questions.length) ?? 0) * 100).toFixed(0)),
        target: TestStores.$currentProgress
      }],
      or: {
        sid: "-hdmfhh"
      }
    });
    a17({
      store: TestStores.$currentPage,
      pickup: appService.appStarted
    });
    a17({
      store: TestStores.$scaleForm,
      pickup: appService.appStarted
    });
    a17({
      store: TestStores.$currentProgress,
      pickup: appService.appStarted
    });
  }
});

// dist/server/chunks/chunk-CXcew3ng.js
var wrapper2, title2, skeleton, price, s24;
var init_chunk_CXcew3ng = __esm({
  "dist/server/chunks/chunk-CXcew3ng.js"() {
    "use strict";
    wrapper2 = "_wrapper_1chve_1";
    title2 = "_title_1chve_13";
    skeleton = "_skeleton_1chve_25";
    price = "_price_1chve_35";
    s24 = {
      wrapper: wrapper2,
      title: title2,
      skeleton,
      price
    };
  }
});

// dist/server/chunks/chunk-BcQy09SU.js
var wrapper3, s25;
var init_chunk_BcQy09SU = __esm({
  "dist/server/chunks/chunk-BcQy09SU.js"() {
    "use strict";
    wrapper3 = "_wrapper_1lzpu_1";
    s25 = {
      wrapper: wrapper3
    };
  }
});

// dist/server/entries/src_pages_purchase-report.mjs
var src_pages_purchase_report_exports = {};
__export(src_pages_purchase_report_exports, {
  configValuesSerialized: () => configValuesSerialized4
});
function isErrorWithMessage(value) {
  return isObject_default(value) && "data" in value && "message" in value;
}
function Page4() {
  return /* @__PURE__ */ (0, import_jsx_runtime169.jsxs)(Container, { pb: "5xl", children: [
    /* @__PURE__ */ (0, import_jsx_runtime169.jsx)(BackButton, {}),
    /* @__PURE__ */ (0, import_jsx_runtime169.jsxs)(InnerContainer, { className: s25.wrapper, children: [
      /* @__PURE__ */ (0, import_jsx_runtime169.jsx)(Image, { src: manImage, width: 185, height: 178, flex: "auto" }),
      /* @__PURE__ */ (0, import_jsx_runtime169.jsxs)(Box, { children: [
        /* @__PURE__ */ (0, import_jsx_runtime169.jsx)(ReportPriceInfo, {}),
        /* @__PURE__ */ (0, import_jsx_runtime169.jsx)(ReportBuyForm, {}),
        /* @__PURE__ */ (0, import_jsx_runtime169.jsx)(Button, { fullWidth: true, variant: "transparent", c: "dark.7", component: "a", href: "/help", children: "\u0421\u043B\u0443\u0436\u0431\u0430 \u043F\u043E\u0434\u0434\u0435\u0440\u0436\u043A\u0438" })
      ] })
    ] })
  ] });
}
var import_jsx_runtime169, import_react233, getRegularPriceQuery, getPriceWithPromocodeQuery, purchaseReportMutation, applyPromocodeClicked, $promocodeErrorMessage, $showSuccessPromoMessage, reportPurchased, openTransactionPaywallFx, useReportBuyFormViewModel, ReportBuyForm, ReportPriceInfo, import74, configValuesSerialized4;
var init_src_pages_purchase_report = __esm({
  "dist/server/entries/src_pages_purchase-report.mjs"() {
    "use strict";
    init_Loading();
    init_onRenderHtml();
    init_chunk_Y_L1G_uK();
    import_jsx_runtime169 = __toESM(require_jsx_runtime(), 1);
    init_esm2();
    init_chunk_CiwkTWKi();
    import_react233 = __toESM(require_react(), 1);
    init_chunk_feoxuLkI();
    init_chunk_Vlh7XtbV();
    init_chunk_B37yhB3o();
    init_effector_react();
    init_effector();
    init_core();
    init_chunk_BDsA7YFO();
    init_clsx();
    init_patronum();
    init_lodash();
    init_chunk_Ba3xGwqT();
    init_esm6();
    init_chunk_Cbus1V3P();
    init_chunk_CXcew3ng();
    init_chunk_BcQy09SU();
    init_usePageContext();
    init_chunk_D3GmwNoI();
    init_chunk_DQ1jpH4N();
    init_chunk_CVNqduS6();
    init_router();
    getRegularPriceQuery = ke({
      sid: "-1tbyj3",
      fn: () => Ir({
        effect: createInternalRequestFx(() => ({
          url: API.GET_REGULAR_PRICE
        }))
      }),
      name: "getRegularPriceQuery",
      method: "createQuery"
    });
    getPriceWithPromocodeQuery = ke({
      sid: "2o5wn9",
      fn: () => Ir({
        effect: createInternalRequestFx((promocode) => ({
          url: API.GET_PROMO_PRICE(promocode)
        }))
      }),
      name: "getPriceWithPromocodeQuery",
      method: "createQuery"
    });
    purchaseReportMutation = ke({
      sid: "i7j439",
      fn: () => jr({
        effect: createInternalRequestFx((body) => ({
          url: API.PURCHASE_REPORT,
          method: HTTP_METHODS.POST,
          body
        }))
      }),
      name: "purchaseReportMutation",
      method: "createMutation"
    });
    h(0, {
      name: "$regularPrice",
      sid: "-azggkp"
    });
    applyPromocodeClicked = p({
      name: "applyPromocodeClicked",
      sid: "-94troy"
    });
    $promocodeErrorMessage = h("", {
      name: "$promocodeErrorMessage",
      sid: "-2c3qs7"
    }).on(applyPromocodeClicked, () => {
      return "";
    });
    $showSuccessPromoMessage = h(false, {
      name: "$showSuccessPromoMessage",
      sid: "-2yl3jp"
    });
    reportPurchased = p({}, {
      name: "reportPurchased",
      sid: "wwj1gz"
    });
    openTransactionPaywallFx = b(async (page) => {
      window.open(page, "_self");
    }, {
      name: "openTransactionPaywallFx",
      sid: "-cews0i"
    });
    x({
      and: [{
        clock: applyPromocodeClicked,
        target: getPriceWithPromocodeQuery.start
      }],
      or: {
        sid: "5jgo28"
      }
    });
    x({
      and: [{
        clock: getPriceWithPromocodeQuery.finished.failure,
        fn: ({
          error
        }) => isErrorWithMessage(error) ? error.data.message : "",
        target: $promocodeErrorMessage
      }],
      or: {
        sid: "5xrbhm"
      }
    });
    x({
      and: [{
        clock: getPriceWithPromocodeQuery.finished.failure,
        fn: () => false,
        target: $showSuccessPromoMessage
      }],
      or: {
        sid: "6clrid"
      }
    });
    x({
      and: [{
        clock: getPriceWithPromocodeQuery.finished.success,
        fn: () => true,
        target: $showSuccessPromoMessage
      }],
      or: {
        sid: "6fwj2j"
      }
    });
    x({
      and: [{
        clock: reportPurchased,
        target: purchaseReportMutation.start
      }],
      or: {
        sid: "6uqz3a"
      }
    });
    x({
      and: [{
        source: ke({
          sid: "-dwuloe",
          fn: () => once(purchaseReportMutation.finished.success),
          name: "source",
          method: "once"
        }),
        fn: ({
          result
        }) => result.confirmation_url,
        target: openTransactionPaywallFx
      }],
      or: {
        sid: "6xhy23"
      }
    });
    useReportBuyFormViewModel = () => {
      const {
        data
      } = c2(getFreeResultQuery);
      const surveyId = c2(TestStores.$surveyId);
      const purchaseReportHandler = c2(reportPurchased);
      const form = useForm({
        mode: "controlled",
        initialValues: {
          email: "",
          mbti_type: surveyId ? "" : (data == null ? void 0 : data.mbti_type) ?? "",
          promo_code: "",
          survey_result: surveyId ?? ""
        },
        validate: {
          email: (value) => /^\S+@\S+$/.test(value) ? null : "\u041D\u0435\u043F\u0440\u0430\u0432\u0438\u043B\u044C\u043D\u044B\u0439 email"
        }
      });
      const emailProps = {
        radius: "xs",
        name: "email",
        withAsterisk: true,
        key: form.key("email"),
        label: "\u042D\u043B\u0435\u043A\u0442\u0440\u043E\u043D\u043D\u0430\u044F \u043F\u043E\u0447\u0442\u0430",
        placeholder: "name@mail.ru",
        ...form.getInputProps("email")
      };
      const promocodeProps = {
        radius: "xs",
        name: "promo_code",
        key: form.key("promo_code"),
        placeholder: "\u0412\u0432\u0435\u0434\u0438\u0442\u0435 \u043F\u0440\u043E\u043C\u043E\u043A\u043E\u0434",
        ...form.getInputProps("promo_code")
      };
      const onSubmit = form.onSubmit((data2) => {
        const preparedData = {
          ...data2
        };
        if (surveyId) {
          delete preparedData.mbti_type;
        }
        if (!surveyId) {
          delete preparedData.survey_result;
        }
        if (!preparedData.promo_code) {
          delete preparedData.promo_code;
        }
        purchaseReportHandler(preparedData);
      });
      return {
        form,
        onSubmit,
        emailProps,
        promocodeProps
      };
    };
    ReportBuyForm = () => {
      const {
        pending: pending2
      } = c2(getPriceWithPromocodeQuery);
      const {
        applyPromoHandler,
        promocodeError,
        showSuccessMessage
      } = c2({
        applyPromoHandler: applyPromocodeClicked,
        promocodeError: $promocodeErrorMessage,
        showSuccessMessage: $showSuccessPromoMessage
      });
      const {
        onSubmit,
        promocodeProps,
        emailProps,
        form
      } = useReportBuyFormViewModel();
      return /* @__PURE__ */ (0, import_jsx_runtime169.jsx)("form", { onSubmit, children: /* @__PURE__ */ (0, import_jsx_runtime169.jsxs)(Stack, { className: s23.stackWrapper, children: [
        /* @__PURE__ */ (0, import_jsx_runtime169.jsx)(TextInput, { ...emailProps, classNames: s23 }),
        /* @__PURE__ */ (0, import_jsx_runtime169.jsxs)(Paper, { bg: "gray.0", radius: "xs", p: "md", px: "sm", children: [
          /* @__PURE__ */ (0, import_jsx_runtime169.jsx)(Text, { className: s23.promocodeLabel, children: "\u0423 \u043C\u0435\u043D\u044F \u0435\u0441\u0442\u044C \u043F\u0440\u043E\u043C\u043E\u043A\u043E\u0434" }),
          /* @__PURE__ */ (0, import_jsx_runtime169.jsxs)(Flex, { className: s23.promocodeWrapper, children: [
            /* @__PURE__ */ (0, import_jsx_runtime169.jsx)(TextInput, { ...promocodeProps, disabled: pending2, error: promocodeError, "data-success": showSuccessMessage, description: showSuccessMessage ? "\u041F\u0440\u043E\u043C\u043E\u043A\u043E\u0434 \u043F\u0440\u0438\u043C\u0435\u043D\u0435\u043D" : "", inputWrapperOrder: ["label", "input", "description", "error"] }),
            /* @__PURE__ */ (0, import_jsx_runtime169.jsx)(Button, { fullWidth: true, c: "dark.7", radius: "xs", color: "dark.7", variant: "outline", onClick: () => applyPromoHandler(form.values.promo_code.toUpperCase()), children: "\u041F\u0440\u0438\u043C\u0435\u043D\u0438\u0442\u044C" })
          ] })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime169.jsx)(MainButton, { disabled: pending2, radius: "xs", size: "md", type: "submit", children: "\u041F\u0435\u0440\u0435\u0439\u0442\u0438 \u043A \u043E\u043F\u043B\u0430\u0442\u0435" }),
        /* @__PURE__ */ (0, import_jsx_runtime169.jsxs)(Text, { fz: 12, ta: "center", mb: 12, children: [
          "\u041D\u0430\u0436\u0438\u043C\u0430\u044F \u043D\u0430 \u043A\u043D\u043E\u043F\u043A\u0443, \u0432\u044B \u043F\u043E\u0434\u0442\u0432\u0435\u0440\u0436\u0434\u0430\u0435\u0442\u0435 \u0441\u0432\u043E\u0435 \u0441\u043E\u0433\u043B\u0430\u0441\u0438\u0435 \u043D\u0430 \u0441\u0431\u043E\u0440, \u0445\u0440\u0430\u043D\u0435\u043D\u0438\u0435 \u0438 \u0438\u0441\u043F\u043E\u043B\u044C\u0437\u043E\u0432\u0430\u043D\u0438\u0435 \u0432\u0430\u0448\u0438\u0445 \u043F\u0435\u0440\u0441\u043E\u043D\u0430\u043B\u044C\u043D\u044B\u0445 \u0434\u0430\u043D\u043D\u044B\u0445 \u0441\u043E\u0433\u043B\u0430\u0441\u043D\u043E",
          " ",
          /* @__PURE__ */ (0, import_jsx_runtime169.jsx)(Text, { fz: 12, c: "blue.7", component: "a", href: "/privacy-policy", children: "\u041F\u043E\u043B\u0438\u0442\u0438\u043A\u0435 \u043A\u043E\u043D\u0444\u0438\u0434\u0435\u043D\u0446\u0438\u0430\u043B\u044C\u043D\u043E\u0441\u0442\u0438" }),
          " ",
          "\u0438 \u043F\u0440\u0438\u043D\u0438\u043C\u0430\u0435\u0442\u0435 \u0443\u0441\u043B\u043E\u0432\u0438\u044F",
          " ",
          /* @__PURE__ */ (0, import_jsx_runtime169.jsx)(Text, { fz: 12, c: "blue.7", component: "a", href: "/publisher-offer", children: "\u041F\u0443\u0431\u043B\u0438\u0447\u043D\u043E\u0439 \u043E\u0444\u0435\u0440\u0442\u044B" })
        ] })
      ] }) });
    };
    ReportPriceInfo = () => {
      const {
        data
      } = c2(getRegularPriceQuery);
      const {
        data: dataWithPromocode
      } = c2(getPriceWithPromocodeQuery);
      return /* @__PURE__ */ (0, import_jsx_runtime169.jsxs)(Stack, { className: s24.wrapper, children: [
        /* @__PURE__ */ (0, import_jsx_runtime169.jsx)(Title, { className: s24.title, order: 2, children: "\u041F\u043E\u043B\u043D\u044B\u0439 \u043E\u0442\u0447\u0435\u0442 \u043F\u043E \u0432\u0430\u0448\u0435\u043C\u0443 \u0442\u0438\u043F\u0443 \u043B\u0438\u0447\u043D\u043E\u0441\u0442\u0438" }),
        /* @__PURE__ */ (0, import_jsx_runtime169.jsx)(Skeleton, { className: s24.skeleton, hidden: Boolean(data == null ? void 0 : data.regular_price), radius: "sm" }),
        /* @__PURE__ */ (0, import_jsx_runtime169.jsxs)(Group, { gap: "lg", children: [
          /* @__PURE__ */ (0, import_jsx_runtime169.jsxs)(Text, { hidden: !data, className: s24.price, td: (dataWithPromocode == null ? void 0 : dataWithPromocode.discount) ? "line-through" : "", c: (dataWithPromocode == null ? void 0 : dataWithPromocode.discount) ? "dark.0" : "dark.7", children: [
            data == null ? void 0 : data.regular_price,
            " ",
            /* @__PURE__ */ (0, import_jsx_runtime169.jsx)(Text, { className: s24.price, span: true, ff: "system-ui", children: "\u20BD" })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime169.jsxs)(Text, { hidden: !(dataWithPromocode == null ? void 0 : dataWithPromocode.discount), className: s24.price, c: "violet.8", children: [
            dataWithPromocode == null ? void 0 : dataWithPromocode.final_price,
            " ",
            /* @__PURE__ */ (0, import_jsx_runtime169.jsx)(Text, { className: s24.price, span: true, ff: "system-ui", children: "\u20BD" })
          ] })
        ] })
      ] });
    };
    import74 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
      __proto__: null,
      default: Page4
    }, Symbol.toStringTag, { value: "Module" }));
    configValuesSerialized4 = {
      ["isClientRuntimeLoaded"]: {
        type: "computed",
        definedAtData: null,
        valueSerialized: {
          type: "js-serialized",
          value: true
        }
      },
      ["Loading"]: {
        type: "standard",
        definedAtData: { "filePathToShowToUser": "vike-react/__internal/integration/Loading", "fileExportPathToShowToUser": [] },
        valueSerialized: {
          type: "pointer-import",
          value: Loading_default
        }
      },
      ["onRenderHtml"]: {
        type: "standard",
        definedAtData: { "filePathToShowToUser": "vike-react/__internal/integration/onRenderHtml", "fileExportPathToShowToUser": [] },
        valueSerialized: {
          type: "pointer-import",
          value: onRenderHtml
        }
      },
      ["Wrapper"]: {
        type: "cumulative",
        definedAtData: [{ "filePathToShowToUser": "/src/pages/+Wrapper.tsx", "fileExportPathToShowToUser": [] }],
        valueSerialized: [{
          type: "plus-file",
          exportValues: import3
        }]
      },
      ["passToClient"]: {
        type: "cumulative",
        definedAtData: [{ "filePathToShowToUser": "/src/pages/+config.ts", "fileExportPathToShowToUser": ["default", "passToClient"] }, { "filePathToShowToUser": "vike-react/config", "fileExportPathToShowToUser": ["default", "passToClient"] }],
        valueSerialized: [{
          type: "js-serialized",
          value: ["scopeValues"]
        }, {
          type: "js-serialized",
          value: ["_configFromHook"]
        }]
      },
      ["cacheControl"]: {
        type: "standard",
        definedAtData: { "filePathToShowToUser": "/src/pages/+config.ts", "fileExportPathToShowToUser": ["default", "cacheControl"] },
        valueSerialized: {
          type: "js-serialized",
          value: "public, max-age=604800"
        }
      },
      ["title"]: {
        type: "standard",
        definedAtData: { "filePathToShowToUser": "/src/pages/+config.ts", "fileExportPathToShowToUser": ["default", "title"] },
        valueSerialized: {
          type: "js-serialized",
          value: "Cognitive Lab"
        }
      },
      ["lang"]: {
        type: "standard",
        definedAtData: { "filePathToShowToUser": "/src/pages/+config.ts", "fileExportPathToShowToUser": ["default", "lang"] },
        valueSerialized: {
          type: "js-serialized",
          value: "ru"
        }
      },
      ["reactStrictMode"]: {
        type: "standard",
        definedAtData: { "filePathToShowToUser": "/src/pages/+config.ts", "fileExportPathToShowToUser": ["default", "reactStrictMode"] },
        valueSerialized: {
          type: "js-serialized",
          value: false
        }
      },
      ["onBeforeRender"]: {
        type: "standard",
        definedAtData: { "filePathToShowToUser": "/src/pages/+onBeforeRender.ts", "fileExportPathToShowToUser": [] },
        valueSerialized: {
          type: "plus-file",
          exportValues: import4
        }
      },
      ["Head"]: {
        type: "cumulative",
        definedAtData: [{ "filePathToShowToUser": "/src/widgets/HeadDefault/index.tsx", "fileExportPathToShowToUser": ["HeadDefault"] }],
        valueSerialized: [{
          type: "pointer-import",
          value: HeadDefault
        }]
      },
      ["Layout"]: {
        type: "cumulative",
        definedAtData: [{ "filePathToShowToUser": "/src/widgets/RootLayout/index.tsx", "fileExportPathToShowToUser": ["RootLayout"] }],
        valueSerialized: [{
          type: "pointer-import",
          value: RootLayout
        }]
      },
      ["Page"]: {
        type: "standard",
        definedAtData: { "filePathToShowToUser": "/src/pages/purchase-report/+Page.tsx", "fileExportPathToShowToUser": [] },
        valueSerialized: {
          type: "plus-file",
          exportValues: import74
        }
      }
    };
  }
});

// dist/server/chunks/chunk-BUtdu6VE.js
var container3, s26;
var init_chunk_BUtdu6VE = __esm({
  "dist/server/chunks/chunk-BUtdu6VE.js"() {
    "use strict";
    container3 = "_container_56zyf_1";
    s26 = {
      container: container3
    };
  }
});

// node_modules/framer-motion/dist/es/render/components/create-proxy.mjs
function createDOMMotionComponentProxy(componentFactory) {
  if (typeof Proxy === "undefined") {
    return componentFactory;
  }
  const componentCache = /* @__PURE__ */ new Map();
  const deprecatedFactoryFunction = (...args) => {
    if (false) {
      warnOnce(false, "motion() is deprecated. Use motion.create() instead.");
    }
    return componentFactory(...args);
  };
  return new Proxy(deprecatedFactoryFunction, {
    /**
     * Called when `motion` is referenced with a prop: `motion.div`, `motion.input` etc.
     * The prop name is passed through as `key` and we can use that to generate a `motion`
     * DOM component with that name.
     */
    get: (_target, key) => {
      if (key === "create")
        return componentFactory;
      if (!componentCache.has(key)) {
        componentCache.set(key, componentFactory(key));
      }
      return componentCache.get(key);
    }
  });
}
var init_create_proxy = __esm({
  "node_modules/framer-motion/dist/es/render/components/create-proxy.mjs"() {
  }
});

// node_modules/framer-motion/dist/es/animation/utils/is-animation-controls.mjs
function isAnimationControls(v5) {
  return v5 !== null && typeof v5 === "object" && typeof v5.start === "function";
}
var init_is_animation_controls = __esm({
  "node_modules/framer-motion/dist/es/animation/utils/is-animation-controls.mjs"() {
  }
});

// node_modules/framer-motion/dist/es/animation/utils/is-keyframes-target.mjs
var isKeyframesTarget;
var init_is_keyframes_target = __esm({
  "node_modules/framer-motion/dist/es/animation/utils/is-keyframes-target.mjs"() {
    isKeyframesTarget = (v5) => {
      return Array.isArray(v5);
    };
  }
});

// node_modules/framer-motion/dist/es/utils/shallow-compare.mjs
function shallowCompare(next2, prev2) {
  if (!Array.isArray(prev2))
    return false;
  const prevLength = prev2.length;
  if (prevLength !== next2.length)
    return false;
  for (let i13 = 0; i13 < prevLength; i13++) {
    if (prev2[i13] !== next2[i13])
      return false;
  }
  return true;
}
var init_shallow_compare = __esm({
  "node_modules/framer-motion/dist/es/utils/shallow-compare.mjs"() {
  }
});

// node_modules/framer-motion/dist/es/render/utils/is-variant-label.mjs
function isVariantLabel(v5) {
  return typeof v5 === "string" || Array.isArray(v5);
}
var init_is_variant_label = __esm({
  "node_modules/framer-motion/dist/es/render/utils/is-variant-label.mjs"() {
  }
});

// node_modules/framer-motion/dist/es/render/utils/resolve-variants.mjs
function getValueState(visualElement) {
  const state2 = [{}, {}];
  visualElement === null || visualElement === void 0 ? void 0 : visualElement.values.forEach((value, key) => {
    state2[0][key] = value.get();
    state2[1][key] = value.getVelocity();
  });
  return state2;
}
function resolveVariantFromProps(props, definition, custom2, visualElement) {
  if (typeof definition === "function") {
    const [current, velocity] = getValueState(visualElement);
    definition = definition(custom2 !== void 0 ? custom2 : props.custom, current, velocity);
  }
  if (typeof definition === "string") {
    definition = props.variants && props.variants[definition];
  }
  if (typeof definition === "function") {
    const [current, velocity] = getValueState(visualElement);
    definition = definition(custom2 !== void 0 ? custom2 : props.custom, current, velocity);
  }
  return definition;
}
var init_resolve_variants = __esm({
  "node_modules/framer-motion/dist/es/render/utils/resolve-variants.mjs"() {
  }
});

// node_modules/framer-motion/dist/es/render/utils/resolve-dynamic-variants.mjs
function resolveVariant(visualElement, definition, custom2) {
  const props = visualElement.getProps();
  return resolveVariantFromProps(props, definition, custom2 !== void 0 ? custom2 : props.custom, visualElement);
}
var init_resolve_dynamic_variants = __esm({
  "node_modules/framer-motion/dist/es/render/utils/resolve-dynamic-variants.mjs"() {
    init_resolve_variants();
  }
});

// node_modules/framer-motion/dist/es/render/utils/variant-props.mjs
var variantPriorityOrder, variantProps;
var init_variant_props = __esm({
  "node_modules/framer-motion/dist/es/render/utils/variant-props.mjs"() {
    variantPriorityOrder = [
      "animate",
      "whileInView",
      "whileFocus",
      "whileHover",
      "whileTap",
      "whileDrag",
      "exit"
    ];
    variantProps = ["initial", ...variantPriorityOrder];
  }
});

// node_modules/framer-motion/dist/es/render/html/utils/transform.mjs
var transformPropOrder, transformProps;
var init_transform = __esm({
  "node_modules/framer-motion/dist/es/render/html/utils/transform.mjs"() {
    transformPropOrder = [
      "transformPerspective",
      "x",
      "y",
      "z",
      "translateX",
      "translateY",
      "translateZ",
      "scale",
      "scaleX",
      "scaleY",
      "rotate",
      "rotateX",
      "rotateY",
      "rotateZ",
      "skew",
      "skewX",
      "skewY"
    ];
    transformProps = new Set(transformPropOrder);
  }
});

// node_modules/framer-motion/dist/es/utils/time-conversion.mjs
var secondsToMilliseconds, millisecondsToSeconds;
var init_time_conversion = __esm({
  "node_modules/framer-motion/dist/es/utils/time-conversion.mjs"() {
    secondsToMilliseconds = (seconds) => seconds * 1e3;
    millisecondsToSeconds = (milliseconds) => milliseconds / 1e3;
  }
});

// node_modules/framer-motion/dist/es/animation/utils/default-transitions.mjs
var underDampedSpring, criticallyDampedSpring, keyframesTransition, ease, getDefaultTransition;
var init_default_transitions = __esm({
  "node_modules/framer-motion/dist/es/animation/utils/default-transitions.mjs"() {
    init_transform();
    underDampedSpring = {
      type: "spring",
      stiffness: 500,
      damping: 25,
      restSpeed: 10
    };
    criticallyDampedSpring = (target) => ({
      type: "spring",
      stiffness: 550,
      damping: target === 0 ? 2 * Math.sqrt(550) : 30,
      restSpeed: 10
    });
    keyframesTransition = {
      type: "keyframes",
      duration: 0.8
    };
    ease = {
      type: "keyframes",
      ease: [0.25, 0.1, 0.35, 1],
      duration: 0.3
    };
    getDefaultTransition = (valueKey, { keyframes: keyframes2 }) => {
      if (keyframes2.length > 2) {
        return keyframesTransition;
      } else if (transformProps.has(valueKey)) {
        return valueKey.startsWith("scale") ? criticallyDampedSpring(keyframes2[1]) : underDampedSpring;
      }
      return ease;
    };
  }
});

// node_modules/framer-motion/dist/es/animation/utils/get-value-transition.mjs
function getValueTransition(transition, key) {
  return transition ? transition[key] || transition["default"] || transition : void 0;
}
var init_get_value_transition = __esm({
  "node_modules/framer-motion/dist/es/animation/utils/get-value-transition.mjs"() {
  }
});

// node_modules/framer-motion/dist/es/utils/GlobalConfig.mjs
var MotionGlobalConfig;
var init_GlobalConfig = __esm({
  "node_modules/framer-motion/dist/es/utils/GlobalConfig.mjs"() {
    MotionGlobalConfig = {
      skipAnimations: false,
      useManualTiming: false
    };
  }
});

// node_modules/framer-motion/dist/es/utils/use-instant-transition-state.mjs
var instantAnimationState;
var init_use_instant_transition_state = __esm({
  "node_modules/framer-motion/dist/es/utils/use-instant-transition-state.mjs"() {
    instantAnimationState = {
      current: false
    };
  }
});

// node_modules/framer-motion/dist/es/animation/animators/waapi/utils/get-final-keyframe.mjs
function getFinalKeyframe(keyframes2, { repeat, repeatType = "loop" }, finalKeyframe) {
  const resolvedKeyframes = keyframes2.filter(isNotNull);
  const index3 = repeat && repeatType !== "loop" && repeat % 2 === 1 ? 0 : resolvedKeyframes.length - 1;
  return !index3 || finalKeyframe === void 0 ? resolvedKeyframes[index3] : finalKeyframe;
}
var isNotNull;
var init_get_final_keyframe = __esm({
  "node_modules/framer-motion/dist/es/animation/animators/waapi/utils/get-final-keyframe.mjs"() {
    isNotNull = (value) => value !== null;
  }
});

// node_modules/motion-utils/dist/es/noop.mjs
var noop2;
var init_noop2 = __esm({
  "node_modules/motion-utils/dist/es/noop.mjs"() {
    noop2 = (any) => any;
  }
});

// node_modules/motion-utils/dist/es/errors.mjs
var warning, invariant;
var init_errors = __esm({
  "node_modules/motion-utils/dist/es/errors.mjs"() {
    init_noop2();
    warning = noop2;
    invariant = noop2;
    if (false) {
      warning = (check2, message) => {
        if (!check2 && typeof console !== "undefined") {
          console.warn(message);
        }
      };
      invariant = (check2, message) => {
        if (!check2) {
          throw new Error(message);
        }
      };
    }
  }
});

// node_modules/motion-utils/dist/es/index.mjs
var init_es = __esm({
  "node_modules/motion-utils/dist/es/index.mjs"() {
    init_errors();
    init_noop2();
  }
});

// node_modules/framer-motion/dist/es/frameloop/render-step.mjs
function createRenderStep(runNextFrame) {
  let thisFrame = /* @__PURE__ */ new Set();
  let nextFrame = /* @__PURE__ */ new Set();
  let isProcessing = false;
  let flushNextFrame = false;
  const toKeepAlive = /* @__PURE__ */ new WeakSet();
  let latestFrameData = {
    delta: 0,
    timestamp: 0,
    isProcessing: false
  };
  function triggerCallback(callback) {
    if (toKeepAlive.has(callback)) {
      step.schedule(callback);
      runNextFrame();
    }
    callback(latestFrameData);
  }
  const step = {
    /**
     * Schedule a process to run on the next frame.
     */
    schedule: (callback, keepAlive = false, immediate = false) => {
      const addToCurrentFrame = immediate && isProcessing;
      const queue = addToCurrentFrame ? thisFrame : nextFrame;
      if (keepAlive)
        toKeepAlive.add(callback);
      if (!queue.has(callback))
        queue.add(callback);
      return callback;
    },
    /**
     * Cancel the provided callback from running on the next frame.
     */
    cancel: (callback) => {
      nextFrame.delete(callback);
      toKeepAlive.delete(callback);
    },
    /**
     * Execute all schedule callbacks.
     */
    process: (frameData2) => {
      latestFrameData = frameData2;
      if (isProcessing) {
        flushNextFrame = true;
        return;
      }
      isProcessing = true;
      [thisFrame, nextFrame] = [nextFrame, thisFrame];
      nextFrame.clear();
      thisFrame.forEach(triggerCallback);
      isProcessing = false;
      if (flushNextFrame) {
        flushNextFrame = false;
        step.process(frameData2);
      }
    }
  };
  return step;
}
var init_render_step = __esm({
  "node_modules/framer-motion/dist/es/frameloop/render-step.mjs"() {
  }
});

// node_modules/framer-motion/dist/es/frameloop/batcher.mjs
function createRenderBatcher(scheduleNextBatch, allowKeepAlive) {
  let runNextFrame = false;
  let useDefaultElapsed = true;
  const state2 = {
    delta: 0,
    timestamp: 0,
    isProcessing: false
  };
  const flagRunNextFrame = () => runNextFrame = true;
  const steps = stepsOrder.reduce((acc, key) => {
    acc[key] = createRenderStep(flagRunNextFrame);
    return acc;
  }, {});
  const { read, resolveKeyframes, update, preRender, render, postRender } = steps;
  const processBatch = () => {
    const timestamp = MotionGlobalConfig.useManualTiming ? state2.timestamp : performance.now();
    runNextFrame = false;
    state2.delta = useDefaultElapsed ? 1e3 / 60 : Math.max(Math.min(timestamp - state2.timestamp, maxElapsed), 1);
    state2.timestamp = timestamp;
    state2.isProcessing = true;
    read.process(state2);
    resolveKeyframes.process(state2);
    update.process(state2);
    preRender.process(state2);
    render.process(state2);
    postRender.process(state2);
    state2.isProcessing = false;
    if (runNextFrame && allowKeepAlive) {
      useDefaultElapsed = false;
      scheduleNextBatch(processBatch);
    }
  };
  const wake = () => {
    runNextFrame = true;
    useDefaultElapsed = true;
    if (!state2.isProcessing) {
      scheduleNextBatch(processBatch);
    }
  };
  const schedule = stepsOrder.reduce((acc, key) => {
    const step = steps[key];
    acc[key] = (process2, keepAlive = false, immediate = false) => {
      if (!runNextFrame)
        wake();
      return step.schedule(process2, keepAlive, immediate);
    };
    return acc;
  }, {});
  const cancel = (process2) => {
    for (let i13 = 0; i13 < stepsOrder.length; i13++) {
      steps[stepsOrder[i13]].cancel(process2);
    }
  };
  return { schedule, cancel, state: state2, steps };
}
var stepsOrder, maxElapsed;
var init_batcher = __esm({
  "node_modules/framer-motion/dist/es/frameloop/batcher.mjs"() {
    init_GlobalConfig();
    init_render_step();
    stepsOrder = [
      "read",
      // Read
      "resolveKeyframes",
      // Write/Read/Write/Read
      "update",
      // Compute
      "preRender",
      // Compute
      "render",
      // Write
      "postRender"
      // Compute
    ];
    maxElapsed = 40;
  }
});

// node_modules/framer-motion/dist/es/frameloop/frame.mjs
var frame, cancelFrame, frameData, frameSteps;
var init_frame = __esm({
  "node_modules/framer-motion/dist/es/frameloop/frame.mjs"() {
    init_es();
    init_batcher();
    ({ schedule: frame, cancel: cancelFrame, state: frameData, steps: frameSteps } = createRenderBatcher(typeof requestAnimationFrame !== "undefined" ? requestAnimationFrame : noop2, true));
  }
});

// node_modules/framer-motion/dist/es/easing/cubic-bezier.mjs
function binarySubdivide(x3, lowerBound, upperBound, mX1, mX2) {
  let currentX;
  let currentT;
  let i13 = 0;
  do {
    currentT = lowerBound + (upperBound - lowerBound) / 2;
    currentX = calcBezier(currentT, mX1, mX2) - x3;
    if (currentX > 0) {
      upperBound = currentT;
    } else {
      lowerBound = currentT;
    }
  } while (Math.abs(currentX) > subdivisionPrecision && ++i13 < subdivisionMaxIterations);
  return currentT;
}
function cubicBezier(mX1, mY1, mX2, mY2) {
  if (mX1 === mY1 && mX2 === mY2)
    return noop2;
  const getTForX = (aX) => binarySubdivide(aX, 0, 1, mX1, mX2);
  return (t20) => t20 === 0 || t20 === 1 ? t20 : calcBezier(getTForX(t20), mY1, mY2);
}
var calcBezier, subdivisionPrecision, subdivisionMaxIterations;
var init_cubic_bezier = __esm({
  "node_modules/framer-motion/dist/es/easing/cubic-bezier.mjs"() {
    init_es();
    calcBezier = (t20, a1, a22) => (((1 - 3 * a22 + 3 * a1) * t20 + (3 * a22 - 6 * a1)) * t20 + 3 * a1) * t20;
    subdivisionPrecision = 1e-7;
    subdivisionMaxIterations = 12;
  }
});

// node_modules/framer-motion/dist/es/easing/modifiers/mirror.mjs
var mirrorEasing;
var init_mirror = __esm({
  "node_modules/framer-motion/dist/es/easing/modifiers/mirror.mjs"() {
    mirrorEasing = (easing) => (p11) => p11 <= 0.5 ? easing(2 * p11) / 2 : (2 - easing(2 * (1 - p11))) / 2;
  }
});

// node_modules/framer-motion/dist/es/easing/modifiers/reverse.mjs
var reverseEasing;
var init_reverse = __esm({
  "node_modules/framer-motion/dist/es/easing/modifiers/reverse.mjs"() {
    reverseEasing = (easing) => (p11) => 1 - easing(1 - p11);
  }
});

// node_modules/framer-motion/dist/es/easing/back.mjs
var backOut, backIn, backInOut;
var init_back = __esm({
  "node_modules/framer-motion/dist/es/easing/back.mjs"() {
    init_cubic_bezier();
    init_mirror();
    init_reverse();
    backOut = /* @__PURE__ */ cubicBezier(0.33, 1.53, 0.69, 0.99);
    backIn = /* @__PURE__ */ reverseEasing(backOut);
    backInOut = /* @__PURE__ */ mirrorEasing(backIn);
  }
});

// node_modules/framer-motion/dist/es/easing/anticipate.mjs
var anticipate;
var init_anticipate = __esm({
  "node_modules/framer-motion/dist/es/easing/anticipate.mjs"() {
    init_back();
    anticipate = (p11) => (p11 *= 2) < 1 ? 0.5 * backIn(p11) : 0.5 * (2 - Math.pow(2, -10 * (p11 - 1)));
  }
});

// node_modules/framer-motion/dist/es/easing/circ.mjs
var circIn, circOut, circInOut;
var init_circ = __esm({
  "node_modules/framer-motion/dist/es/easing/circ.mjs"() {
    init_mirror();
    init_reverse();
    circIn = (p11) => 1 - Math.sin(Math.acos(p11));
    circOut = reverseEasing(circIn);
    circInOut = mirrorEasing(circIn);
  }
});

// node_modules/framer-motion/dist/es/utils/is-zero-value-string.mjs
var isZeroValueString;
var init_is_zero_value_string = __esm({
  "node_modules/framer-motion/dist/es/utils/is-zero-value-string.mjs"() {
    isZeroValueString = (v5) => /^0[^.\s]+$/u.test(v5);
  }
});

// node_modules/framer-motion/dist/es/animation/utils/is-none.mjs
function isNone(value) {
  if (typeof value === "number") {
    return value === 0;
  } else if (value !== null) {
    return value === "none" || value === "0" || isZeroValueString(value);
  } else {
    return true;
  }
}
var init_is_none = __esm({
  "node_modules/framer-motion/dist/es/animation/utils/is-none.mjs"() {
    init_is_zero_value_string();
  }
});

// node_modules/framer-motion/dist/es/utils/is-numerical-string.mjs
var isNumericalString;
var init_is_numerical_string = __esm({
  "node_modules/framer-motion/dist/es/utils/is-numerical-string.mjs"() {
    isNumericalString = (v5) => /^-?(?:\d+(?:\.\d+)?|\.\d+)$/u.test(v5);
  }
});

// node_modules/framer-motion/dist/es/render/dom/utils/is-css-variable.mjs
var checkStringStartsWith, isCSSVariableName, startsAsVariableToken, isCSSVariableToken, singleCssVariableRegex;
var init_is_css_variable = __esm({
  "node_modules/framer-motion/dist/es/render/dom/utils/is-css-variable.mjs"() {
    checkStringStartsWith = (token) => (key) => typeof key === "string" && key.startsWith(token);
    isCSSVariableName = /* @__PURE__ */ checkStringStartsWith("--");
    startsAsVariableToken = /* @__PURE__ */ checkStringStartsWith("var(--");
    isCSSVariableToken = (value) => {
      const startsWithToken = startsAsVariableToken(value);
      if (!startsWithToken)
        return false;
      return singleCssVariableRegex.test(value.split("/*")[0].trim());
    };
    singleCssVariableRegex = /var\(--(?:[\w-]+\s*|[\w-]+\s*,(?:\s*[^)(\s]|\s*\((?:[^)(]|\([^)(]*\))*\))+\s*)\)$/iu;
  }
});

// node_modules/framer-motion/dist/es/render/dom/utils/css-variables-conversion.mjs
function parseCSSVariable(current) {
  const match = splitCSSVariableRegex.exec(current);
  if (!match)
    return [,];
  const [, token1, token2, fallback] = match;
  return [`--${token1 !== null && token1 !== void 0 ? token1 : token2}`, fallback];
}
function getVariableValue(current, element, depth = 1) {
  invariant(depth <= maxDepth, `Max CSS variable fallback depth detected in property "${current}". This may indicate a circular fallback dependency.`);
  const [token, fallback] = parseCSSVariable(current);
  if (!token)
    return;
  const resolved = window.getComputedStyle(element).getPropertyValue(token);
  if (resolved) {
    const trimmed = resolved.trim();
    return isNumericalString(trimmed) ? parseFloat(trimmed) : trimmed;
  }
  return isCSSVariableToken(fallback) ? getVariableValue(fallback, element, depth + 1) : fallback;
}
var splitCSSVariableRegex, maxDepth;
var init_css_variables_conversion = __esm({
  "node_modules/framer-motion/dist/es/render/dom/utils/css-variables-conversion.mjs"() {
    init_es();
    init_is_numerical_string();
    init_is_css_variable();
    splitCSSVariableRegex = // eslint-disable-next-line redos-detector/no-unsafe-regex -- false positive, as it can match a lot of words
    /^var\(--(?:([\w-]+)|([\w-]+), ?([a-zA-Z\d ()%#.,-]+))\)/u;
    maxDepth = 4;
  }
});

// node_modules/framer-motion/dist/es/utils/clamp.mjs
var clamp3;
var init_clamp2 = __esm({
  "node_modules/framer-motion/dist/es/utils/clamp.mjs"() {
    clamp3 = (min2, max2, v5) => {
      if (v5 > max2)
        return max2;
      if (v5 < min2)
        return min2;
      return v5;
    };
  }
});

// node_modules/framer-motion/dist/es/value/types/numbers/index.mjs
var number, alpha2, scale;
var init_numbers = __esm({
  "node_modules/framer-motion/dist/es/value/types/numbers/index.mjs"() {
    init_clamp2();
    number = {
      test: (v5) => typeof v5 === "number",
      parse: parseFloat,
      transform: (v5) => v5
    };
    alpha2 = {
      ...number,
      transform: (v5) => clamp3(0, 1, v5)
    };
    scale = {
      ...number,
      default: 1
    };
  }
});

// node_modules/framer-motion/dist/es/value/types/numbers/units.mjs
var createUnitType, degrees, percent, px2, vh, vw, progressPercentage;
var init_units = __esm({
  "node_modules/framer-motion/dist/es/value/types/numbers/units.mjs"() {
    createUnitType = (unit) => ({
      test: (v5) => typeof v5 === "string" && v5.endsWith(unit) && v5.split(" ").length === 1,
      parse: parseFloat,
      transform: (v5) => `${v5}${unit}`
    });
    degrees = /* @__PURE__ */ createUnitType("deg");
    percent = /* @__PURE__ */ createUnitType("%");
    px2 = /* @__PURE__ */ createUnitType("px");
    vh = /* @__PURE__ */ createUnitType("vh");
    vw = /* @__PURE__ */ createUnitType("vw");
    progressPercentage = {
      ...percent,
      parse: (v5) => percent.parse(v5) / 100,
      transform: (v5) => percent.transform(v5 * 100)
    };
  }
});

// node_modules/framer-motion/dist/es/render/dom/utils/unit-conversion.mjs
function removeNonTranslationalTransform(visualElement) {
  const removedTransforms = [];
  nonTranslationalTransformKeys.forEach((key) => {
    const value = visualElement.getValue(key);
    if (value !== void 0) {
      removedTransforms.push([key, value.get()]);
      value.set(key.startsWith("scale") ? 1 : 0);
    }
  });
  return removedTransforms;
}
var positionalKeys, isNumOrPxType, getPosFromMatrix, getTranslateFromMatrix, transformKeys, nonTranslationalTransformKeys, positionalValues;
var init_unit_conversion = __esm({
  "node_modules/framer-motion/dist/es/render/dom/utils/unit-conversion.mjs"() {
    init_transform();
    init_numbers();
    init_units();
    positionalKeys = /* @__PURE__ */ new Set([
      "width",
      "height",
      "top",
      "left",
      "right",
      "bottom",
      "x",
      "y",
      "translateX",
      "translateY"
    ]);
    isNumOrPxType = (v5) => v5 === number || v5 === px2;
    getPosFromMatrix = (matrix, pos) => parseFloat(matrix.split(", ")[pos]);
    getTranslateFromMatrix = (pos2, pos3) => (_bbox, { transform }) => {
      if (transform === "none" || !transform)
        return 0;
      const matrix3d = transform.match(/^matrix3d\((.+)\)$/u);
      if (matrix3d) {
        return getPosFromMatrix(matrix3d[1], pos3);
      } else {
        const matrix = transform.match(/^matrix\((.+)\)$/u);
        if (matrix) {
          return getPosFromMatrix(matrix[1], pos2);
        } else {
          return 0;
        }
      }
    };
    transformKeys = /* @__PURE__ */ new Set(["x", "y", "z"]);
    nonTranslationalTransformKeys = transformPropOrder.filter((key) => !transformKeys.has(key));
    positionalValues = {
      // Dimensions
      width: ({ x: x3 }, { paddingLeft = "0", paddingRight = "0" }) => x3.max - x3.min - parseFloat(paddingLeft) - parseFloat(paddingRight),
      height: ({ y: y4 }, { paddingTop = "0", paddingBottom = "0" }) => y4.max - y4.min - parseFloat(paddingTop) - parseFloat(paddingBottom),
      top: (_bbox, { top: top2 }) => parseFloat(top2),
      left: (_bbox, { left }) => parseFloat(left),
      bottom: ({ y: y4 }, { top: top2 }) => parseFloat(top2) + (y4.max - y4.min),
      right: ({ x: x3 }, { left }) => parseFloat(left) + (x3.max - x3.min),
      // Transform
      x: getTranslateFromMatrix(4, 13),
      y: getTranslateFromMatrix(5, 14)
    };
    positionalValues.translateX = positionalValues.x;
    positionalValues.translateY = positionalValues.y;
  }
});

// node_modules/framer-motion/dist/es/render/dom/value-types/test.mjs
var testValueType;
var init_test = __esm({
  "node_modules/framer-motion/dist/es/render/dom/value-types/test.mjs"() {
    testValueType = (v5) => (type2) => type2.test(v5);
  }
});

// node_modules/framer-motion/dist/es/render/dom/value-types/type-auto.mjs
var auto;
var init_type_auto = __esm({
  "node_modules/framer-motion/dist/es/render/dom/value-types/type-auto.mjs"() {
    auto = {
      test: (v5) => v5 === "auto",
      parse: (v5) => v5
    };
  }
});

// node_modules/framer-motion/dist/es/render/dom/value-types/dimensions.mjs
var dimensionValueTypes, findDimensionValueType;
var init_dimensions = __esm({
  "node_modules/framer-motion/dist/es/render/dom/value-types/dimensions.mjs"() {
    init_numbers();
    init_units();
    init_test();
    init_type_auto();
    dimensionValueTypes = [number, px2, percent, degrees, vw, vh, auto];
    findDimensionValueType = (v5) => dimensionValueTypes.find(testValueType(v5));
  }
});

// node_modules/framer-motion/dist/es/render/utils/KeyframesResolver.mjs
function measureAllKeyframes() {
  if (anyNeedsMeasurement) {
    const resolversToMeasure = Array.from(toResolve).filter((resolver) => resolver.needsMeasurement);
    const elementsToMeasure = new Set(resolversToMeasure.map((resolver) => resolver.element));
    const transformsToRestore = /* @__PURE__ */ new Map();
    elementsToMeasure.forEach((element) => {
      const removedTransforms = removeNonTranslationalTransform(element);
      if (!removedTransforms.length)
        return;
      transformsToRestore.set(element, removedTransforms);
      element.render();
    });
    resolversToMeasure.forEach((resolver) => resolver.measureInitialState());
    elementsToMeasure.forEach((element) => {
      element.render();
      const restore = transformsToRestore.get(element);
      if (restore) {
        restore.forEach(([key, value]) => {
          var _a;
          (_a = element.getValue(key)) === null || _a === void 0 ? void 0 : _a.set(value);
        });
      }
    });
    resolversToMeasure.forEach((resolver) => resolver.measureEndState());
    resolversToMeasure.forEach((resolver) => {
      if (resolver.suspendedScrollY !== void 0) {
        window.scrollTo(0, resolver.suspendedScrollY);
      }
    });
  }
  anyNeedsMeasurement = false;
  isScheduled = false;
  toResolve.forEach((resolver) => resolver.complete());
  toResolve.clear();
}
function readAllKeyframes() {
  toResolve.forEach((resolver) => {
    resolver.readKeyframes();
    if (resolver.needsMeasurement) {
      anyNeedsMeasurement = true;
    }
  });
}
function flushKeyframeResolvers() {
  readAllKeyframes();
  measureAllKeyframes();
}
var toResolve, isScheduled, anyNeedsMeasurement, KeyframeResolver;
var init_KeyframesResolver = __esm({
  "node_modules/framer-motion/dist/es/render/utils/KeyframesResolver.mjs"() {
    init_unit_conversion();
    init_frame();
    toResolve = /* @__PURE__ */ new Set();
    isScheduled = false;
    anyNeedsMeasurement = false;
    KeyframeResolver = class {
      constructor(unresolvedKeyframes, onComplete, name2, motionValue2, element, isAsync2 = false) {
        this.isComplete = false;
        this.isAsync = false;
        this.needsMeasurement = false;
        this.isScheduled = false;
        this.unresolvedKeyframes = [...unresolvedKeyframes];
        this.onComplete = onComplete;
        this.name = name2;
        this.motionValue = motionValue2;
        this.element = element;
        this.isAsync = isAsync2;
      }
      scheduleResolve() {
        this.isScheduled = true;
        if (this.isAsync) {
          toResolve.add(this);
          if (!isScheduled) {
            isScheduled = true;
            frame.read(readAllKeyframes);
            frame.resolveKeyframes(measureAllKeyframes);
          }
        } else {
          this.readKeyframes();
          this.complete();
        }
      }
      readKeyframes() {
        const { unresolvedKeyframes, name: name2, element, motionValue: motionValue2 } = this;
        for (let i13 = 0; i13 < unresolvedKeyframes.length; i13++) {
          if (unresolvedKeyframes[i13] === null) {
            if (i13 === 0) {
              const currentValue = motionValue2 === null || motionValue2 === void 0 ? void 0 : motionValue2.get();
              const finalKeyframe = unresolvedKeyframes[unresolvedKeyframes.length - 1];
              if (currentValue !== void 0) {
                unresolvedKeyframes[0] = currentValue;
              } else if (element && name2) {
                const valueAsRead = element.readValue(name2, finalKeyframe);
                if (valueAsRead !== void 0 && valueAsRead !== null) {
                  unresolvedKeyframes[0] = valueAsRead;
                }
              }
              if (unresolvedKeyframes[0] === void 0) {
                unresolvedKeyframes[0] = finalKeyframe;
              }
              if (motionValue2 && currentValue === void 0) {
                motionValue2.set(unresolvedKeyframes[0]);
              }
            } else {
              unresolvedKeyframes[i13] = unresolvedKeyframes[i13 - 1];
            }
          }
        }
      }
      setFinalKeyframe() {
      }
      measureInitialState() {
      }
      renderEndStyles() {
      }
      measureEndState() {
      }
      complete() {
        this.isComplete = true;
        this.onComplete(this.unresolvedKeyframes, this.finalKeyframe);
        toResolve.delete(this);
      }
      cancel() {
        if (!this.isComplete) {
          this.isScheduled = false;
          toResolve.delete(this);
        }
      }
      resume() {
        if (!this.isComplete)
          this.scheduleResolve();
      }
    };
  }
});

// node_modules/framer-motion/dist/es/value/types/utils/sanitize.mjs
var sanitize;
var init_sanitize = __esm({
  "node_modules/framer-motion/dist/es/value/types/utils/sanitize.mjs"() {
    sanitize = (v5) => Math.round(v5 * 1e5) / 1e5;
  }
});

// node_modules/framer-motion/dist/es/value/types/utils/float-regex.mjs
var floatRegex;
var init_float_regex = __esm({
  "node_modules/framer-motion/dist/es/value/types/utils/float-regex.mjs"() {
    floatRegex = /-?(?:\d+(?:\.\d+)?|\.\d+)/gu;
  }
});

// node_modules/framer-motion/dist/es/value/types/utils/is-nullish.mjs
function isNullish(v5) {
  return v5 == null;
}
var init_is_nullish = __esm({
  "node_modules/framer-motion/dist/es/value/types/utils/is-nullish.mjs"() {
  }
});

// node_modules/framer-motion/dist/es/value/types/utils/single-color-regex.mjs
var singleColorRegex;
var init_single_color_regex = __esm({
  "node_modules/framer-motion/dist/es/value/types/utils/single-color-regex.mjs"() {
    singleColorRegex = /^(?:#[\da-f]{3,8}|(?:rgb|hsl)a?\((?:-?[\d.]+%?[,\s]+){2}-?[\d.]+%?\s*(?:[,/]\s*)?(?:\b\d+(?:\.\d+)?|\.\d+)?%?\))$/iu;
  }
});

// node_modules/framer-motion/dist/es/value/types/color/utils.mjs
var isColorString, splitColor;
var init_utils4 = __esm({
  "node_modules/framer-motion/dist/es/value/types/color/utils.mjs"() {
    init_float_regex();
    init_is_nullish();
    init_single_color_regex();
    isColorString = (type2, testProp) => (v5) => {
      return Boolean(typeof v5 === "string" && singleColorRegex.test(v5) && v5.startsWith(type2) || testProp && !isNullish(v5) && Object.prototype.hasOwnProperty.call(v5, testProp));
    };
    splitColor = (aName, bName, cName) => (v5) => {
      if (typeof v5 !== "string")
        return v5;
      const [a18, b4, c15, alpha3] = v5.match(floatRegex);
      return {
        [aName]: parseFloat(a18),
        [bName]: parseFloat(b4),
        [cName]: parseFloat(c15),
        alpha: alpha3 !== void 0 ? parseFloat(alpha3) : 1
      };
    };
  }
});

// node_modules/framer-motion/dist/es/value/types/color/rgba.mjs
var clampRgbUnit, rgbUnit, rgba2;
var init_rgba2 = __esm({
  "node_modules/framer-motion/dist/es/value/types/color/rgba.mjs"() {
    init_clamp2();
    init_numbers();
    init_sanitize();
    init_utils4();
    clampRgbUnit = (v5) => clamp3(0, 255, v5);
    rgbUnit = {
      ...number,
      transform: (v5) => Math.round(clampRgbUnit(v5))
    };
    rgba2 = {
      test: /* @__PURE__ */ isColorString("rgb", "red"),
      parse: /* @__PURE__ */ splitColor("red", "green", "blue"),
      transform: ({ red, green, blue, alpha: alpha$1 = 1 }) => "rgba(" + rgbUnit.transform(red) + ", " + rgbUnit.transform(green) + ", " + rgbUnit.transform(blue) + ", " + sanitize(alpha2.transform(alpha$1)) + ")"
    };
  }
});

// node_modules/framer-motion/dist/es/value/types/color/hex.mjs
function parseHex(v5) {
  let r7 = "";
  let g7 = "";
  let b4 = "";
  let a18 = "";
  if (v5.length > 5) {
    r7 = v5.substring(1, 3);
    g7 = v5.substring(3, 5);
    b4 = v5.substring(5, 7);
    a18 = v5.substring(7, 9);
  } else {
    r7 = v5.substring(1, 2);
    g7 = v5.substring(2, 3);
    b4 = v5.substring(3, 4);
    a18 = v5.substring(4, 5);
    r7 += r7;
    g7 += g7;
    b4 += b4;
    a18 += a18;
  }
  return {
    red: parseInt(r7, 16),
    green: parseInt(g7, 16),
    blue: parseInt(b4, 16),
    alpha: a18 ? parseInt(a18, 16) / 255 : 1
  };
}
var hex;
var init_hex = __esm({
  "node_modules/framer-motion/dist/es/value/types/color/hex.mjs"() {
    init_rgba2();
    init_utils4();
    hex = {
      test: /* @__PURE__ */ isColorString("#"),
      parse: parseHex,
      transform: rgba2.transform
    };
  }
});

// node_modules/framer-motion/dist/es/value/types/color/hsla.mjs
var hsla;
var init_hsla = __esm({
  "node_modules/framer-motion/dist/es/value/types/color/hsla.mjs"() {
    init_numbers();
    init_units();
    init_sanitize();
    init_utils4();
    hsla = {
      test: /* @__PURE__ */ isColorString("hsl", "hue"),
      parse: /* @__PURE__ */ splitColor("hue", "saturation", "lightness"),
      transform: ({ hue, saturation, lightness, alpha: alpha$1 = 1 }) => {
        return "hsla(" + Math.round(hue) + ", " + percent.transform(sanitize(saturation)) + ", " + percent.transform(sanitize(lightness)) + ", " + sanitize(alpha2.transform(alpha$1)) + ")";
      }
    };
  }
});

// node_modules/framer-motion/dist/es/value/types/color/index.mjs
var color;
var init_color = __esm({
  "node_modules/framer-motion/dist/es/value/types/color/index.mjs"() {
    init_hex();
    init_hsla();
    init_rgba2();
    color = {
      test: (v5) => rgba2.test(v5) || hex.test(v5) || hsla.test(v5),
      parse: (v5) => {
        if (rgba2.test(v5)) {
          return rgba2.parse(v5);
        } else if (hsla.test(v5)) {
          return hsla.parse(v5);
        } else {
          return hex.parse(v5);
        }
      },
      transform: (v5) => {
        return typeof v5 === "string" ? v5 : v5.hasOwnProperty("red") ? rgba2.transform(v5) : hsla.transform(v5);
      }
    };
  }
});

// node_modules/framer-motion/dist/es/value/types/utils/color-regex.mjs
var colorRegex;
var init_color_regex = __esm({
  "node_modules/framer-motion/dist/es/value/types/utils/color-regex.mjs"() {
    colorRegex = /(?:#[\da-f]{3,8}|(?:rgb|hsl)a?\((?:-?[\d.]+%?[,\s]+){2}-?[\d.]+%?\s*(?:[,/]\s*)?(?:\b\d+(?:\.\d+)?|\.\d+)?%?\))/giu;
  }
});

// node_modules/framer-motion/dist/es/value/types/complex/index.mjs
function test(v5) {
  var _a, _b;
  return isNaN(v5) && typeof v5 === "string" && (((_a = v5.match(floatRegex)) === null || _a === void 0 ? void 0 : _a.length) || 0) + (((_b = v5.match(colorRegex)) === null || _b === void 0 ? void 0 : _b.length) || 0) > 0;
}
function analyseComplexValue(value) {
  const originalValue = value.toString();
  const values2 = [];
  const indexes = {
    color: [],
    number: [],
    var: []
  };
  const types = [];
  let i13 = 0;
  const tokenised = originalValue.replace(complexRegex, (parsedValue) => {
    if (color.test(parsedValue)) {
      indexes.color.push(i13);
      types.push(COLOR_TOKEN);
      values2.push(color.parse(parsedValue));
    } else if (parsedValue.startsWith(VAR_FUNCTION_TOKEN)) {
      indexes.var.push(i13);
      types.push(VAR_TOKEN);
      values2.push(parsedValue);
    } else {
      indexes.number.push(i13);
      types.push(NUMBER_TOKEN);
      values2.push(parseFloat(parsedValue));
    }
    ++i13;
    return SPLIT_TOKEN;
  });
  const split = tokenised.split(SPLIT_TOKEN);
  return { values: values2, split, indexes, types };
}
function parseComplexValue(v5) {
  return analyseComplexValue(v5).values;
}
function createTransformer(source) {
  const { split, types } = analyseComplexValue(source);
  const numSections = split.length;
  return (v5) => {
    let output = "";
    for (let i13 = 0; i13 < numSections; i13++) {
      output += split[i13];
      if (v5[i13] !== void 0) {
        const type2 = types[i13];
        if (type2 === NUMBER_TOKEN) {
          output += sanitize(v5[i13]);
        } else if (type2 === COLOR_TOKEN) {
          output += color.transform(v5[i13]);
        } else {
          output += v5[i13];
        }
      }
    }
    return output;
  };
}
function getAnimatableNone(v5) {
  const parsed = parseComplexValue(v5);
  const transformer = createTransformer(v5);
  return transformer(parsed.map(convertNumbersToZero));
}
var NUMBER_TOKEN, COLOR_TOKEN, VAR_TOKEN, VAR_FUNCTION_TOKEN, SPLIT_TOKEN, complexRegex, convertNumbersToZero, complex;
var init_complex = __esm({
  "node_modules/framer-motion/dist/es/value/types/complex/index.mjs"() {
    init_color();
    init_color_regex();
    init_float_regex();
    init_sanitize();
    NUMBER_TOKEN = "number";
    COLOR_TOKEN = "color";
    VAR_TOKEN = "var";
    VAR_FUNCTION_TOKEN = "var(";
    SPLIT_TOKEN = "${}";
    complexRegex = /var\s*\(\s*--(?:[\w-]+\s*|[\w-]+\s*,(?:\s*[^)(\s]|\s*\((?:[^)(]|\([^)(]*\))*\))+\s*)\)|#[\da-f]{3,8}|(?:rgb|hsl)a?\((?:-?[\d.]+%?[,\s]+){2}-?[\d.]+%?\s*(?:[,/]\s*)?(?:\b\d+(?:\.\d+)?|\.\d+)?%?\)|-?(?:\d+(?:\.\d+)?|\.\d+)/giu;
    convertNumbersToZero = (v5) => typeof v5 === "number" ? 0 : v5;
    complex = {
      test,
      parse: parseComplexValue,
      createTransformer,
      getAnimatableNone
    };
  }
});

// node_modules/framer-motion/dist/es/value/types/complex/filter.mjs
function applyDefaultFilter(v5) {
  const [name2, value] = v5.slice(0, -1).split("(");
  if (name2 === "drop-shadow")
    return v5;
  const [number2] = value.match(floatRegex) || [];
  if (!number2)
    return v5;
  const unit = value.replace(number2, "");
  let defaultValue2 = maxDefaults.has(name2) ? 1 : 0;
  if (number2 !== value)
    defaultValue2 *= 100;
  return name2 + "(" + defaultValue2 + unit + ")";
}
var maxDefaults, functionRegex, filter;
var init_filter = __esm({
  "node_modules/framer-motion/dist/es/value/types/complex/filter.mjs"() {
    init_complex();
    init_float_regex();
    maxDefaults = /* @__PURE__ */ new Set(["brightness", "contrast", "saturate", "opacity"]);
    functionRegex = /\b([a-z-]*)\(.*?\)/gu;
    filter = {
      ...complex,
      getAnimatableNone: (v5) => {
        const functions = v5.match(functionRegex);
        return functions ? functions.map(applyDefaultFilter).join(" ") : v5;
      }
    };
  }
});

// node_modules/framer-motion/dist/es/render/dom/value-types/number-browser.mjs
var browserNumberValueTypes;
var init_number_browser = __esm({
  "node_modules/framer-motion/dist/es/render/dom/value-types/number-browser.mjs"() {
    init_units();
    browserNumberValueTypes = {
      // Border props
      borderWidth: px2,
      borderTopWidth: px2,
      borderRightWidth: px2,
      borderBottomWidth: px2,
      borderLeftWidth: px2,
      borderRadius: px2,
      radius: px2,
      borderTopLeftRadius: px2,
      borderTopRightRadius: px2,
      borderBottomRightRadius: px2,
      borderBottomLeftRadius: px2,
      // Positioning props
      width: px2,
      maxWidth: px2,
      height: px2,
      maxHeight: px2,
      top: px2,
      right: px2,
      bottom: px2,
      left: px2,
      // Spacing props
      padding: px2,
      paddingTop: px2,
      paddingRight: px2,
      paddingBottom: px2,
      paddingLeft: px2,
      margin: px2,
      marginTop: px2,
      marginRight: px2,
      marginBottom: px2,
      marginLeft: px2,
      // Misc
      backgroundPositionX: px2,
      backgroundPositionY: px2
    };
  }
});

// node_modules/framer-motion/dist/es/render/dom/value-types/transform.mjs
var transformValueTypes;
var init_transform2 = __esm({
  "node_modules/framer-motion/dist/es/render/dom/value-types/transform.mjs"() {
    init_numbers();
    init_units();
    transformValueTypes = {
      rotate: degrees,
      rotateX: degrees,
      rotateY: degrees,
      rotateZ: degrees,
      scale,
      scaleX: scale,
      scaleY: scale,
      scaleZ: scale,
      skew: degrees,
      skewX: degrees,
      skewY: degrees,
      distance: px2,
      translateX: px2,
      translateY: px2,
      translateZ: px2,
      x: px2,
      y: px2,
      z: px2,
      perspective: px2,
      transformPerspective: px2,
      opacity: alpha2,
      originX: progressPercentage,
      originY: progressPercentage,
      originZ: px2
    };
  }
});

// node_modules/framer-motion/dist/es/render/dom/value-types/type-int.mjs
var int;
var init_type_int = __esm({
  "node_modules/framer-motion/dist/es/render/dom/value-types/type-int.mjs"() {
    init_numbers();
    int = {
      ...number,
      transform: Math.round
    };
  }
});

// node_modules/framer-motion/dist/es/render/dom/value-types/number.mjs
var numberValueTypes;
var init_number = __esm({
  "node_modules/framer-motion/dist/es/render/dom/value-types/number.mjs"() {
    init_numbers();
    init_units();
    init_number_browser();
    init_transform2();
    init_type_int();
    numberValueTypes = {
      ...browserNumberValueTypes,
      ...transformValueTypes,
      zIndex: int,
      size: px2,
      // SVG
      fillOpacity: alpha2,
      strokeOpacity: alpha2,
      numOctaves: int
    };
  }
});

// node_modules/framer-motion/dist/es/render/dom/value-types/defaults.mjs
var defaultValueTypes, getDefaultValueType;
var init_defaults = __esm({
  "node_modules/framer-motion/dist/es/render/dom/value-types/defaults.mjs"() {
    init_color();
    init_filter();
    init_number();
    defaultValueTypes = {
      ...numberValueTypes,
      // Color props
      color,
      backgroundColor: color,
      outlineColor: color,
      fill: color,
      stroke: color,
      // Border props
      borderColor: color,
      borderTopColor: color,
      borderRightColor: color,
      borderBottomColor: color,
      borderLeftColor: color,
      filter,
      WebkitFilter: filter
    };
    getDefaultValueType = (key) => defaultValueTypes[key];
  }
});

// node_modules/framer-motion/dist/es/render/dom/value-types/animatable-none.mjs
function getAnimatableNone2(key, value) {
  let defaultValueType = getDefaultValueType(key);
  if (defaultValueType !== filter)
    defaultValueType = complex;
  return defaultValueType.getAnimatableNone ? defaultValueType.getAnimatableNone(value) : void 0;
}
var init_animatable_none = __esm({
  "node_modules/framer-motion/dist/es/render/dom/value-types/animatable-none.mjs"() {
    init_complex();
    init_filter();
    init_defaults();
  }
});

// node_modules/framer-motion/dist/es/render/html/utils/make-none-animatable.mjs
function makeNoneKeyframesAnimatable(unresolvedKeyframes, noneKeyframeIndexes, name2) {
  let i13 = 0;
  let animatableTemplate = void 0;
  while (i13 < unresolvedKeyframes.length && !animatableTemplate) {
    const keyframe = unresolvedKeyframes[i13];
    if (typeof keyframe === "string" && !invalidTemplates.has(keyframe) && analyseComplexValue(keyframe).values.length) {
      animatableTemplate = unresolvedKeyframes[i13];
    }
    i13++;
  }
  if (animatableTemplate && name2) {
    for (const noneIndex of noneKeyframeIndexes) {
      unresolvedKeyframes[noneIndex] = getAnimatableNone2(name2, animatableTemplate);
    }
  }
}
var invalidTemplates;
var init_make_none_animatable = __esm({
  "node_modules/framer-motion/dist/es/render/html/utils/make-none-animatable.mjs"() {
    init_complex();
    init_animatable_none();
    invalidTemplates = /* @__PURE__ */ new Set(["auto", "none", "0"]);
  }
});

// node_modules/framer-motion/dist/es/render/dom/DOMKeyframesResolver.mjs
var DOMKeyframesResolver;
var init_DOMKeyframesResolver = __esm({
  "node_modules/framer-motion/dist/es/render/dom/DOMKeyframesResolver.mjs"() {
    init_is_none();
    init_css_variables_conversion();
    init_is_css_variable();
    init_unit_conversion();
    init_dimensions();
    init_KeyframesResolver();
    init_make_none_animatable();
    DOMKeyframesResolver = class extends KeyframeResolver {
      constructor(unresolvedKeyframes, onComplete, name2, motionValue2, element) {
        super(unresolvedKeyframes, onComplete, name2, motionValue2, element, true);
      }
      readKeyframes() {
        const { unresolvedKeyframes, element, name: name2 } = this;
        if (!element || !element.current)
          return;
        super.readKeyframes();
        for (let i13 = 0; i13 < unresolvedKeyframes.length; i13++) {
          let keyframe = unresolvedKeyframes[i13];
          if (typeof keyframe === "string") {
            keyframe = keyframe.trim();
            if (isCSSVariableToken(keyframe)) {
              const resolved = getVariableValue(keyframe, element.current);
              if (resolved !== void 0) {
                unresolvedKeyframes[i13] = resolved;
              }
              if (i13 === unresolvedKeyframes.length - 1) {
                this.finalKeyframe = keyframe;
              }
            }
          }
        }
        this.resolveNoneKeyframes();
        if (!positionalKeys.has(name2) || unresolvedKeyframes.length !== 2) {
          return;
        }
        const [origin, target] = unresolvedKeyframes;
        const originType = findDimensionValueType(origin);
        const targetType = findDimensionValueType(target);
        if (originType === targetType)
          return;
        if (isNumOrPxType(originType) && isNumOrPxType(targetType)) {
          for (let i13 = 0; i13 < unresolvedKeyframes.length; i13++) {
            const value = unresolvedKeyframes[i13];
            if (typeof value === "string") {
              unresolvedKeyframes[i13] = parseFloat(value);
            }
          }
        } else {
          this.needsMeasurement = true;
        }
      }
      resolveNoneKeyframes() {
        const { unresolvedKeyframes, name: name2 } = this;
        const noneKeyframeIndexes = [];
        for (let i13 = 0; i13 < unresolvedKeyframes.length; i13++) {
          if (isNone(unresolvedKeyframes[i13])) {
            noneKeyframeIndexes.push(i13);
          }
        }
        if (noneKeyframeIndexes.length) {
          makeNoneKeyframesAnimatable(unresolvedKeyframes, noneKeyframeIndexes, name2);
        }
      }
      measureInitialState() {
        const { element, unresolvedKeyframes, name: name2 } = this;
        if (!element || !element.current)
          return;
        if (name2 === "height") {
          this.suspendedScrollY = window.pageYOffset;
        }
        this.measuredOrigin = positionalValues[name2](element.measureViewportBox(), window.getComputedStyle(element.current));
        unresolvedKeyframes[0] = this.measuredOrigin;
        const measureKeyframe = unresolvedKeyframes[unresolvedKeyframes.length - 1];
        if (measureKeyframe !== void 0) {
          element.getValue(name2, measureKeyframe).jump(measureKeyframe, false);
        }
      }
      measureEndState() {
        var _a;
        const { element, name: name2, unresolvedKeyframes } = this;
        if (!element || !element.current)
          return;
        const value = element.getValue(name2);
        value && value.jump(this.measuredOrigin, false);
        const finalKeyframeIndex = unresolvedKeyframes.length - 1;
        const finalKeyframe = unresolvedKeyframes[finalKeyframeIndex];
        unresolvedKeyframes[finalKeyframeIndex] = positionalValues[name2](element.measureViewportBox(), window.getComputedStyle(element.current));
        if (finalKeyframe !== null && this.finalKeyframe === void 0) {
          this.finalKeyframe = finalKeyframe;
        }
        if ((_a = this.removedTransforms) === null || _a === void 0 ? void 0 : _a.length) {
          this.removedTransforms.forEach(([unsetTransformName, unsetTransformValue]) => {
            element.getValue(unsetTransformName).set(unsetTransformValue);
          });
        }
        this.resolveNoneKeyframes();
      }
    };
  }
});

// node_modules/framer-motion/dist/es/animation/generators/utils/is-generator.mjs
function isGenerator(type2) {
  return typeof type2 === "function";
}
var init_is_generator = __esm({
  "node_modules/framer-motion/dist/es/animation/generators/utils/is-generator.mjs"() {
  }
});

// node_modules/framer-motion/dist/es/frameloop/sync-time.mjs
function clearTime() {
  now = void 0;
}
var now, time2;
var init_sync_time = __esm({
  "node_modules/framer-motion/dist/es/frameloop/sync-time.mjs"() {
    init_GlobalConfig();
    init_frame();
    time2 = {
      now: () => {
        if (now === void 0) {
          time2.set(frameData.isProcessing || MotionGlobalConfig.useManualTiming ? frameData.timestamp : performance.now());
        }
        return now;
      },
      set: (newTime) => {
        now = newTime;
        queueMicrotask(clearTime);
      }
    };
  }
});

// node_modules/framer-motion/dist/es/animation/utils/is-animatable.mjs
var isAnimatable;
var init_is_animatable = __esm({
  "node_modules/framer-motion/dist/es/animation/utils/is-animatable.mjs"() {
    init_complex();
    isAnimatable = (value, name2) => {
      if (name2 === "zIndex")
        return false;
      if (typeof value === "number" || Array.isArray(value))
        return true;
      if (typeof value === "string" && // It's animatable if we have a string
      (complex.test(value) || value === "0") && // And it contains numbers and/or colors
      !value.startsWith("url(")) {
        return true;
      }
      return false;
    };
  }
});

// node_modules/framer-motion/dist/es/animation/animators/utils/can-animate.mjs
function hasKeyframesChanged(keyframes2) {
  const current = keyframes2[0];
  if (keyframes2.length === 1)
    return true;
  for (let i13 = 0; i13 < keyframes2.length; i13++) {
    if (keyframes2[i13] !== current)
      return true;
  }
}
function canAnimate(keyframes2, name2, type2, velocity) {
  const originKeyframe = keyframes2[0];
  if (originKeyframe === null)
    return false;
  if (name2 === "display" || name2 === "visibility")
    return true;
  const targetKeyframe = keyframes2[keyframes2.length - 1];
  const isOriginAnimatable = isAnimatable(originKeyframe, name2);
  const isTargetAnimatable = isAnimatable(targetKeyframe, name2);
  warning(isOriginAnimatable === isTargetAnimatable, `You are trying to animate ${name2} from "${originKeyframe}" to "${targetKeyframe}". ${originKeyframe} is not an animatable value - to enable this animation set ${originKeyframe} to a value animatable to ${targetKeyframe} via the \`style\` property.`);
  if (!isOriginAnimatable || !isTargetAnimatable) {
    return false;
  }
  return hasKeyframesChanged(keyframes2) || (type2 === "spring" || isGenerator(type2)) && velocity;
}
var init_can_animate = __esm({
  "node_modules/framer-motion/dist/es/animation/animators/utils/can-animate.mjs"() {
    init_es();
    init_is_generator();
    init_is_animatable();
  }
});

// node_modules/framer-motion/dist/es/animation/animators/BaseAnimation.mjs
var MAX_RESOLVE_DELAY, BaseAnimation;
var init_BaseAnimation = __esm({
  "node_modules/framer-motion/dist/es/animation/animators/BaseAnimation.mjs"() {
    init_sync_time();
    init_KeyframesResolver();
    init_use_instant_transition_state();
    init_can_animate();
    init_get_final_keyframe();
    MAX_RESOLVE_DELAY = 40;
    BaseAnimation = class {
      constructor({ autoplay = true, delay: delay3 = 0, type: type2 = "keyframes", repeat = 0, repeatDelay = 0, repeatType = "loop", ...options }) {
        this.isStopped = false;
        this.hasAttemptedResolve = false;
        this.createdAt = time2.now();
        this.options = {
          autoplay,
          delay: delay3,
          type: type2,
          repeat,
          repeatDelay,
          repeatType,
          ...options
        };
        this.updateFinishedPromise();
      }
      /**
       * This method uses the createdAt and resolvedAt to calculate the
       * animation startTime. *Ideally*, we would use the createdAt time as t=0
       * as the following frame would then be the first frame of the animation in
       * progress, which would feel snappier.
       *
       * However, if there's a delay (main thread work) between the creation of
       * the animation and the first commited frame, we prefer to use resolvedAt
       * to avoid a sudden jump into the animation.
       */
      calcStartTime() {
        if (!this.resolvedAt)
          return this.createdAt;
        return this.resolvedAt - this.createdAt > MAX_RESOLVE_DELAY ? this.resolvedAt : this.createdAt;
      }
      /**
       * A getter for resolved data. If keyframes are not yet resolved, accessing
       * this.resolved will synchronously flush all pending keyframe resolvers.
       * This is a deoptimisation, but at its worst still batches read/writes.
       */
      get resolved() {
        if (!this._resolved && !this.hasAttemptedResolve) {
          flushKeyframeResolvers();
        }
        return this._resolved;
      }
      /**
       * A method to be called when the keyframes resolver completes. This method
       * will check if its possible to run the animation and, if not, skip it.
       * Otherwise, it will call initPlayback on the implementing class.
       */
      onKeyframesResolved(keyframes2, finalKeyframe) {
        this.resolvedAt = time2.now();
        this.hasAttemptedResolve = true;
        const { name: name2, type: type2, velocity, delay: delay3, onComplete, onUpdate, isGenerator: isGenerator2 } = this.options;
        if (!isGenerator2 && !canAnimate(keyframes2, name2, type2, velocity)) {
          if (instantAnimationState.current || !delay3) {
            onUpdate === null || onUpdate === void 0 ? void 0 : onUpdate(getFinalKeyframe(keyframes2, this.options, finalKeyframe));
            onComplete === null || onComplete === void 0 ? void 0 : onComplete();
            this.resolveFinishedPromise();
            return;
          } else {
            this.options.duration = 0;
          }
        }
        const resolvedAnimation = this.initPlayback(keyframes2, finalKeyframe);
        if (resolvedAnimation === false)
          return;
        this._resolved = {
          keyframes: keyframes2,
          finalKeyframe,
          ...resolvedAnimation
        };
        this.onPostResolved();
      }
      onPostResolved() {
      }
      /**
       * Allows the returned animation to be awaited or promise-chained. Currently
       * resolves when the animation finishes at all but in a future update could/should
       * reject if its cancels.
       */
      then(resolve, reject) {
        return this.currentFinishedPromise.then(resolve, reject);
      }
      flatten() {
        this.options.type = "keyframes";
        this.options.ease = "linear";
      }
      updateFinishedPromise() {
        this.currentFinishedPromise = new Promise((resolve) => {
          this.resolveFinishedPromise = resolve;
        });
      }
    };
  }
});

// node_modules/framer-motion/dist/es/utils/progress.mjs
var progress;
var init_progress = __esm({
  "node_modules/framer-motion/dist/es/utils/progress.mjs"() {
    progress = (from, to, value) => {
      const toFromDifference = to - from;
      return toFromDifference === 0 ? 1 : (value - from) / toFromDifference;
    };
  }
});

// node_modules/framer-motion/dist/es/animation/animators/waapi/utils/linear.mjs
var generateLinearEasing;
var init_linear = __esm({
  "node_modules/framer-motion/dist/es/animation/animators/waapi/utils/linear.mjs"() {
    init_progress();
    generateLinearEasing = (easing, duration, resolution = 10) => {
      let points = "";
      const numPoints = Math.max(Math.round(duration / resolution), 2);
      for (let i13 = 0; i13 < numPoints; i13++) {
        points += easing(progress(0, numPoints - 1, i13)) + ", ";
      }
      return `linear(${points.substring(0, points.length - 2)})`;
    };
  }
});

// node_modules/framer-motion/dist/es/utils/velocity-per-second.mjs
function velocityPerSecond(velocity, frameDuration) {
  return frameDuration ? velocity * (1e3 / frameDuration) : 0;
}
var init_velocity_per_second = __esm({
  "node_modules/framer-motion/dist/es/utils/velocity-per-second.mjs"() {
  }
});

// node_modules/framer-motion/dist/es/animation/generators/utils/velocity.mjs
function calcGeneratorVelocity(resolveValue, t20, current) {
  const prevT = Math.max(t20 - velocitySampleDuration, 0);
  return velocityPerSecond(current - resolveValue(prevT), t20 - prevT);
}
var velocitySampleDuration;
var init_velocity = __esm({
  "node_modules/framer-motion/dist/es/animation/generators/utils/velocity.mjs"() {
    init_velocity_per_second();
    velocitySampleDuration = 5;
  }
});

// node_modules/framer-motion/dist/es/animation/generators/spring/defaults.mjs
var springDefaults;
var init_defaults2 = __esm({
  "node_modules/framer-motion/dist/es/animation/generators/spring/defaults.mjs"() {
    springDefaults = {
      // Default spring physics
      stiffness: 100,
      damping: 10,
      mass: 1,
      velocity: 0,
      // Default duration/bounce-based options
      duration: 800,
      // in ms
      bounce: 0.3,
      visualDuration: 0.3,
      // in seconds
      // Rest thresholds
      restSpeed: {
        granular: 0.01,
        default: 2
      },
      restDelta: {
        granular: 5e-3,
        default: 0.5
      },
      // Limits
      minDuration: 0.01,
      // in seconds
      maxDuration: 10,
      // in seconds
      minDamping: 0.05,
      maxDamping: 1
    };
  }
});

// node_modules/framer-motion/dist/es/animation/generators/spring/find.mjs
function findSpring({ duration = springDefaults.duration, bounce = springDefaults.bounce, velocity = springDefaults.velocity, mass = springDefaults.mass }) {
  let envelope;
  let derivative;
  warning(duration <= secondsToMilliseconds(springDefaults.maxDuration), "Spring duration must be 10 seconds or less");
  let dampingRatio = 1 - bounce;
  dampingRatio = clamp3(springDefaults.minDamping, springDefaults.maxDamping, dampingRatio);
  duration = clamp3(springDefaults.minDuration, springDefaults.maxDuration, millisecondsToSeconds(duration));
  if (dampingRatio < 1) {
    envelope = (undampedFreq2) => {
      const exponentialDecay = undampedFreq2 * dampingRatio;
      const delta = exponentialDecay * duration;
      const a18 = exponentialDecay - velocity;
      const b4 = calcAngularFreq(undampedFreq2, dampingRatio);
      const c15 = Math.exp(-delta);
      return safeMin - a18 / b4 * c15;
    };
    derivative = (undampedFreq2) => {
      const exponentialDecay = undampedFreq2 * dampingRatio;
      const delta = exponentialDecay * duration;
      const d12 = delta * velocity + velocity;
      const e14 = Math.pow(dampingRatio, 2) * Math.pow(undampedFreq2, 2) * duration;
      const f15 = Math.exp(-delta);
      const g7 = calcAngularFreq(Math.pow(undampedFreq2, 2), dampingRatio);
      const factor = -envelope(undampedFreq2) + safeMin > 0 ? -1 : 1;
      return factor * ((d12 - e14) * f15) / g7;
    };
  } else {
    envelope = (undampedFreq2) => {
      const a18 = Math.exp(-undampedFreq2 * duration);
      const b4 = (undampedFreq2 - velocity) * duration + 1;
      return -safeMin + a18 * b4;
    };
    derivative = (undampedFreq2) => {
      const a18 = Math.exp(-undampedFreq2 * duration);
      const b4 = (velocity - undampedFreq2) * (duration * duration);
      return a18 * b4;
    };
  }
  const initialGuess = 5 / duration;
  const undampedFreq = approximateRoot(envelope, derivative, initialGuess);
  duration = secondsToMilliseconds(duration);
  if (isNaN(undampedFreq)) {
    return {
      stiffness: springDefaults.stiffness,
      damping: springDefaults.damping,
      duration
    };
  } else {
    const stiffness = Math.pow(undampedFreq, 2) * mass;
    return {
      stiffness,
      damping: dampingRatio * 2 * Math.sqrt(mass * stiffness),
      duration
    };
  }
}
function approximateRoot(envelope, derivative, initialGuess) {
  let result = initialGuess;
  for (let i13 = 1; i13 < rootIterations; i13++) {
    result = result - envelope(result) / derivative(result);
  }
  return result;
}
function calcAngularFreq(undampedFreq, dampingRatio) {
  return undampedFreq * Math.sqrt(1 - dampingRatio * dampingRatio);
}
var safeMin, rootIterations;
var init_find = __esm({
  "node_modules/framer-motion/dist/es/animation/generators/spring/find.mjs"() {
    init_es();
    init_clamp2();
    init_time_conversion();
    init_defaults2();
    safeMin = 1e-3;
    rootIterations = 12;
  }
});

// node_modules/framer-motion/dist/es/animation/generators/utils/calc-duration.mjs
function calcGeneratorDuration(generator) {
  let duration = 0;
  const timeStep = 50;
  let state2 = generator.next(duration);
  while (!state2.done && duration < maxGeneratorDuration) {
    duration += timeStep;
    state2 = generator.next(duration);
  }
  return duration >= maxGeneratorDuration ? Infinity : duration;
}
var maxGeneratorDuration;
var init_calc_duration = __esm({
  "node_modules/framer-motion/dist/es/animation/generators/utils/calc-duration.mjs"() {
    maxGeneratorDuration = 2e4;
  }
});

// node_modules/framer-motion/dist/es/animation/generators/spring/index.mjs
function isSpringType(options, keys2) {
  return keys2.some((key) => options[key] !== void 0);
}
function getSpringOptions(options) {
  let springOptions = {
    velocity: springDefaults.velocity,
    stiffness: springDefaults.stiffness,
    damping: springDefaults.damping,
    mass: springDefaults.mass,
    isResolvedFromDuration: false,
    ...options
  };
  if (!isSpringType(options, physicsKeys) && isSpringType(options, durationKeys)) {
    if (options.visualDuration) {
      const visualDuration = options.visualDuration;
      const root5 = 2 * Math.PI / (visualDuration * 1.2);
      const stiffness = root5 * root5;
      const damping = 2 * clamp3(0.05, 1, 1 - options.bounce) * Math.sqrt(stiffness);
      springOptions = {
        ...springOptions,
        mass: springDefaults.mass,
        stiffness,
        damping
      };
    } else {
      const derived = findSpring(options);
      springOptions = {
        ...springOptions,
        ...derived,
        mass: springDefaults.mass
      };
      springOptions.isResolvedFromDuration = true;
    }
  }
  return springOptions;
}
function spring(optionsOrVisualDuration = springDefaults.visualDuration, bounce = springDefaults.bounce) {
  const options = typeof optionsOrVisualDuration !== "object" ? {
    visualDuration: optionsOrVisualDuration,
    keyframes: [0, 1],
    bounce
  } : optionsOrVisualDuration;
  let { restSpeed, restDelta } = options;
  const origin = options.keyframes[0];
  const target = options.keyframes[options.keyframes.length - 1];
  const state2 = { done: false, value: origin };
  const { stiffness, damping, mass, duration, velocity, isResolvedFromDuration } = getSpringOptions({
    ...options,
    velocity: -millisecondsToSeconds(options.velocity || 0)
  });
  const initialVelocity = velocity || 0;
  const dampingRatio = damping / (2 * Math.sqrt(stiffness * mass));
  const initialDelta = target - origin;
  const undampedAngularFreq = millisecondsToSeconds(Math.sqrt(stiffness / mass));
  const isGranularScale = Math.abs(initialDelta) < 5;
  restSpeed || (restSpeed = isGranularScale ? springDefaults.restSpeed.granular : springDefaults.restSpeed.default);
  restDelta || (restDelta = isGranularScale ? springDefaults.restDelta.granular : springDefaults.restDelta.default);
  let resolveSpring;
  if (dampingRatio < 1) {
    const angularFreq = calcAngularFreq(undampedAngularFreq, dampingRatio);
    resolveSpring = (t20) => {
      const envelope = Math.exp(-dampingRatio * undampedAngularFreq * t20);
      return target - envelope * ((initialVelocity + dampingRatio * undampedAngularFreq * initialDelta) / angularFreq * Math.sin(angularFreq * t20) + initialDelta * Math.cos(angularFreq * t20));
    };
  } else if (dampingRatio === 1) {
    resolveSpring = (t20) => target - Math.exp(-undampedAngularFreq * t20) * (initialDelta + (initialVelocity + undampedAngularFreq * initialDelta) * t20);
  } else {
    const dampedAngularFreq = undampedAngularFreq * Math.sqrt(dampingRatio * dampingRatio - 1);
    resolveSpring = (t20) => {
      const envelope = Math.exp(-dampingRatio * undampedAngularFreq * t20);
      const freqForT = Math.min(dampedAngularFreq * t20, 300);
      return target - envelope * ((initialVelocity + dampingRatio * undampedAngularFreq * initialDelta) * Math.sinh(freqForT) + dampedAngularFreq * initialDelta * Math.cosh(freqForT)) / dampedAngularFreq;
    };
  }
  const generator = {
    calculatedDuration: isResolvedFromDuration ? duration || null : null,
    next: (t20) => {
      const current = resolveSpring(t20);
      if (!isResolvedFromDuration) {
        let currentVelocity = 0;
        if (dampingRatio < 1) {
          currentVelocity = t20 === 0 ? secondsToMilliseconds(initialVelocity) : calcGeneratorVelocity(resolveSpring, t20, current);
        }
        const isBelowVelocityThreshold = Math.abs(currentVelocity) <= restSpeed;
        const isBelowDisplacementThreshold = Math.abs(target - current) <= restDelta;
        state2.done = isBelowVelocityThreshold && isBelowDisplacementThreshold;
      } else {
        state2.done = t20 >= duration;
      }
      state2.value = state2.done ? target : current;
      return state2;
    },
    toString: () => {
      const calculatedDuration = Math.min(calcGeneratorDuration(generator), maxGeneratorDuration);
      const easing = generateLinearEasing((progress3) => generator.next(calculatedDuration * progress3).value, calculatedDuration, 30);
      return calculatedDuration + "ms " + easing;
    }
  };
  return generator;
}
var durationKeys, physicsKeys;
var init_spring = __esm({
  "node_modules/framer-motion/dist/es/animation/generators/spring/index.mjs"() {
    init_linear();
    init_time_conversion();
    init_velocity();
    init_find();
    init_calc_duration();
    init_clamp2();
    init_defaults2();
    durationKeys = ["duration", "bounce"];
    physicsKeys = ["stiffness", "damping", "mass"];
  }
});

// node_modules/framer-motion/dist/es/animation/generators/inertia.mjs
function inertia({ keyframes: keyframes2, velocity = 0, power = 0.8, timeConstant = 325, bounceDamping = 10, bounceStiffness = 500, modifyTarget, min: min2, max: max2, restDelta = 0.5, restSpeed }) {
  const origin = keyframes2[0];
  const state2 = {
    done: false,
    value: origin
  };
  const isOutOfBounds = (v5) => min2 !== void 0 && v5 < min2 || max2 !== void 0 && v5 > max2;
  const nearestBoundary = (v5) => {
    if (min2 === void 0)
      return max2;
    if (max2 === void 0)
      return min2;
    return Math.abs(min2 - v5) < Math.abs(max2 - v5) ? min2 : max2;
  };
  let amplitude = power * velocity;
  const ideal = origin + amplitude;
  const target = modifyTarget === void 0 ? ideal : modifyTarget(ideal);
  if (target !== ideal)
    amplitude = target - origin;
  const calcDelta = (t20) => -amplitude * Math.exp(-t20 / timeConstant);
  const calcLatest = (t20) => target + calcDelta(t20);
  const applyFriction = (t20) => {
    const delta = calcDelta(t20);
    const latest = calcLatest(t20);
    state2.done = Math.abs(delta) <= restDelta;
    state2.value = state2.done ? target : latest;
  };
  let timeReachedBoundary;
  let spring$1;
  const checkCatchBoundary = (t20) => {
    if (!isOutOfBounds(state2.value))
      return;
    timeReachedBoundary = t20;
    spring$1 = spring({
      keyframes: [state2.value, nearestBoundary(state2.value)],
      velocity: calcGeneratorVelocity(calcLatest, t20, state2.value),
      // TODO: This should be passing * 1000
      damping: bounceDamping,
      stiffness: bounceStiffness,
      restDelta,
      restSpeed
    });
  };
  checkCatchBoundary(0);
  return {
    calculatedDuration: null,
    next: (t20) => {
      let hasUpdatedFrame = false;
      if (!spring$1 && timeReachedBoundary === void 0) {
        hasUpdatedFrame = true;
        applyFriction(t20);
        checkCatchBoundary(t20);
      }
      if (timeReachedBoundary !== void 0 && t20 >= timeReachedBoundary) {
        return spring$1.next(t20 - timeReachedBoundary);
      } else {
        !hasUpdatedFrame && applyFriction(t20);
        return state2;
      }
    }
  };
}
var init_inertia = __esm({
  "node_modules/framer-motion/dist/es/animation/generators/inertia.mjs"() {
    init_spring();
    init_velocity();
  }
});

// node_modules/framer-motion/dist/es/easing/ease.mjs
var easeIn, easeOut, easeInOut;
var init_ease = __esm({
  "node_modules/framer-motion/dist/es/easing/ease.mjs"() {
    init_cubic_bezier();
    easeIn = /* @__PURE__ */ cubicBezier(0.42, 0, 1, 1);
    easeOut = /* @__PURE__ */ cubicBezier(0, 0, 0.58, 1);
    easeInOut = /* @__PURE__ */ cubicBezier(0.42, 0, 0.58, 1);
  }
});

// node_modules/framer-motion/dist/es/easing/utils/is-easing-array.mjs
var isEasingArray;
var init_is_easing_array = __esm({
  "node_modules/framer-motion/dist/es/easing/utils/is-easing-array.mjs"() {
    isEasingArray = (ease2) => {
      return Array.isArray(ease2) && typeof ease2[0] !== "number";
    };
  }
});

// node_modules/framer-motion/dist/es/easing/utils/is-bezier-definition.mjs
var isBezierDefinition;
var init_is_bezier_definition = __esm({
  "node_modules/framer-motion/dist/es/easing/utils/is-bezier-definition.mjs"() {
    isBezierDefinition = (easing) => Array.isArray(easing) && typeof easing[0] === "number";
  }
});

// node_modules/framer-motion/dist/es/easing/utils/map.mjs
var easingLookup, easingDefinitionToFunction;
var init_map = __esm({
  "node_modules/framer-motion/dist/es/easing/utils/map.mjs"() {
    init_es();
    init_cubic_bezier();
    init_ease();
    init_circ();
    init_back();
    init_anticipate();
    init_is_bezier_definition();
    easingLookup = {
      linear: noop2,
      easeIn,
      easeInOut,
      easeOut,
      circIn,
      circInOut,
      circOut,
      backIn,
      backInOut,
      backOut,
      anticipate
    };
    easingDefinitionToFunction = (definition) => {
      if (isBezierDefinition(definition)) {
        invariant(definition.length === 4, `Cubic bezier arrays must contain four numerical values.`);
        const [x1, y1, x22, y22] = definition;
        return cubicBezier(x1, y1, x22, y22);
      } else if (typeof definition === "string") {
        invariant(easingLookup[definition] !== void 0, `Invalid easing type '${definition}'`);
        return easingLookup[definition];
      }
      return definition;
    };
  }
});

// node_modules/framer-motion/dist/es/utils/pipe.mjs
var combineFunctions, pipe;
var init_pipe = __esm({
  "node_modules/framer-motion/dist/es/utils/pipe.mjs"() {
    combineFunctions = (a18, b4) => (v5) => b4(a18(v5));
    pipe = (...transformers) => transformers.reduce(combineFunctions);
  }
});

// node_modules/framer-motion/dist/es/utils/mix/number.mjs
var mixNumber;
var init_number2 = __esm({
  "node_modules/framer-motion/dist/es/utils/mix/number.mjs"() {
    mixNumber = (from, to, progress3) => {
      return from + (to - from) * progress3;
    };
  }
});

// node_modules/framer-motion/dist/es/utils/hsla-to-rgba.mjs
function hueToRgb(p11, q2, t20) {
  if (t20 < 0)
    t20 += 1;
  if (t20 > 1)
    t20 -= 1;
  if (t20 < 1 / 6)
    return p11 + (q2 - p11) * 6 * t20;
  if (t20 < 1 / 2)
    return q2;
  if (t20 < 2 / 3)
    return p11 + (q2 - p11) * (2 / 3 - t20) * 6;
  return p11;
}
function hslaToRgba({ hue, saturation, lightness, alpha: alpha3 }) {
  hue /= 360;
  saturation /= 100;
  lightness /= 100;
  let red = 0;
  let green = 0;
  let blue = 0;
  if (!saturation) {
    red = green = blue = lightness;
  } else {
    const q2 = lightness < 0.5 ? lightness * (1 + saturation) : lightness + saturation - lightness * saturation;
    const p11 = 2 * lightness - q2;
    red = hueToRgb(p11, q2, hue + 1 / 3);
    green = hueToRgb(p11, q2, hue);
    blue = hueToRgb(p11, q2, hue - 1 / 3);
  }
  return {
    red: Math.round(red * 255),
    green: Math.round(green * 255),
    blue: Math.round(blue * 255),
    alpha: alpha3
  };
}
var init_hsla_to_rgba = __esm({
  "node_modules/framer-motion/dist/es/utils/hsla-to-rgba.mjs"() {
  }
});

// node_modules/framer-motion/dist/es/utils/mix/immediate.mjs
function mixImmediate(a18, b4) {
  return (p11) => p11 > 0 ? b4 : a18;
}
var init_immediate = __esm({
  "node_modules/framer-motion/dist/es/utils/mix/immediate.mjs"() {
  }
});

// node_modules/framer-motion/dist/es/utils/mix/color.mjs
function asRGBA(color2) {
  const type2 = getColorType(color2);
  warning(Boolean(type2), `'${color2}' is not an animatable color. Use the equivalent color code instead.`);
  if (!Boolean(type2))
    return false;
  let model = type2.parse(color2);
  if (type2 === hsla) {
    model = hslaToRgba(model);
  }
  return model;
}
var mixLinearColor, colorTypes, getColorType, mixColor;
var init_color2 = __esm({
  "node_modules/framer-motion/dist/es/utils/mix/color.mjs"() {
    init_number2();
    init_es();
    init_hsla_to_rgba();
    init_hex();
    init_rgba2();
    init_hsla();
    init_immediate();
    mixLinearColor = (from, to, v5) => {
      const fromExpo = from * from;
      const expo = v5 * (to * to - fromExpo) + fromExpo;
      return expo < 0 ? 0 : Math.sqrt(expo);
    };
    colorTypes = [hex, rgba2, hsla];
    getColorType = (v5) => colorTypes.find((type2) => type2.test(v5));
    mixColor = (from, to) => {
      const fromRGBA = asRGBA(from);
      const toRGBA = asRGBA(to);
      if (!fromRGBA || !toRGBA) {
        return mixImmediate(from, to);
      }
      const blended = { ...fromRGBA };
      return (v5) => {
        blended.red = mixLinearColor(fromRGBA.red, toRGBA.red, v5);
        blended.green = mixLinearColor(fromRGBA.green, toRGBA.green, v5);
        blended.blue = mixLinearColor(fromRGBA.blue, toRGBA.blue, v5);
        blended.alpha = mixNumber(fromRGBA.alpha, toRGBA.alpha, v5);
        return rgba2.transform(blended);
      };
    };
  }
});

// node_modules/framer-motion/dist/es/utils/mix/visibility.mjs
function mixVisibility(origin, target) {
  if (invisibleValues.has(origin)) {
    return (p11) => p11 <= 0 ? origin : target;
  } else {
    return (p11) => p11 >= 1 ? target : origin;
  }
}
var invisibleValues;
var init_visibility = __esm({
  "node_modules/framer-motion/dist/es/utils/mix/visibility.mjs"() {
    invisibleValues = /* @__PURE__ */ new Set(["none", "hidden"]);
  }
});

// node_modules/framer-motion/dist/es/utils/mix/complex.mjs
function mixNumber2(a18, b4) {
  return (p11) => mixNumber(a18, b4, p11);
}
function getMixer(a18) {
  if (typeof a18 === "number") {
    return mixNumber2;
  } else if (typeof a18 === "string") {
    return isCSSVariableToken(a18) ? mixImmediate : color.test(a18) ? mixColor : mixComplex;
  } else if (Array.isArray(a18)) {
    return mixArray;
  } else if (typeof a18 === "object") {
    return color.test(a18) ? mixColor : mixObject;
  }
  return mixImmediate;
}
function mixArray(a18, b4) {
  const output = [...a18];
  const numValues = output.length;
  const blendValue = a18.map((v5, i13) => getMixer(v5)(v5, b4[i13]));
  return (p11) => {
    for (let i13 = 0; i13 < numValues; i13++) {
      output[i13] = blendValue[i13](p11);
    }
    return output;
  };
}
function mixObject(a18, b4) {
  const output = { ...a18, ...b4 };
  const blendValue = {};
  for (const key in output) {
    if (a18[key] !== void 0 && b4[key] !== void 0) {
      blendValue[key] = getMixer(a18[key])(a18[key], b4[key]);
    }
  }
  return (v5) => {
    for (const key in blendValue) {
      output[key] = blendValue[key](v5);
    }
    return output;
  };
}
function matchOrder(origin, target) {
  var _a;
  const orderedOrigin = [];
  const pointers = { color: 0, var: 0, number: 0 };
  for (let i13 = 0; i13 < target.values.length; i13++) {
    const type2 = target.types[i13];
    const originIndex = origin.indexes[type2][pointers[type2]];
    const originValue = (_a = origin.values[originIndex]) !== null && _a !== void 0 ? _a : 0;
    orderedOrigin[i13] = originValue;
    pointers[type2]++;
  }
  return orderedOrigin;
}
var mixComplex;
var init_complex2 = __esm({
  "node_modules/framer-motion/dist/es/utils/mix/complex.mjs"() {
    init_number2();
    init_color2();
    init_pipe();
    init_es();
    init_color();
    init_complex();
    init_is_css_variable();
    init_visibility();
    init_immediate();
    mixComplex = (origin, target) => {
      const template = complex.createTransformer(target);
      const originStats = analyseComplexValue(origin);
      const targetStats = analyseComplexValue(target);
      const canInterpolate = originStats.indexes.var.length === targetStats.indexes.var.length && originStats.indexes.color.length === targetStats.indexes.color.length && originStats.indexes.number.length >= targetStats.indexes.number.length;
      if (canInterpolate) {
        if (invisibleValues.has(origin) && !targetStats.values.length || invisibleValues.has(target) && !originStats.values.length) {
          return mixVisibility(origin, target);
        }
        return pipe(mixArray(matchOrder(originStats, targetStats), targetStats.values), template);
      } else {
        warning(true, `Complex values '${origin}' and '${target}' too different to mix. Ensure all colors are of the same type, and that each contains the same quantity of number and color values. Falling back to instant transition.`);
        return mixImmediate(origin, target);
      }
    };
  }
});

// node_modules/framer-motion/dist/es/utils/mix/index.mjs
function mix(from, to, p11) {
  if (typeof from === "number" && typeof to === "number" && typeof p11 === "number") {
    return mixNumber(from, to, p11);
  }
  const mixer = getMixer(from);
  return mixer(from, to);
}
var init_mix = __esm({
  "node_modules/framer-motion/dist/es/utils/mix/index.mjs"() {
    init_complex2();
    init_number2();
  }
});

// node_modules/framer-motion/dist/es/utils/interpolate.mjs
function createMixers(output, ease2, customMixer) {
  const mixers = [];
  const mixerFactory = customMixer || mix;
  const numMixers = output.length - 1;
  for (let i13 = 0; i13 < numMixers; i13++) {
    let mixer = mixerFactory(output[i13], output[i13 + 1]);
    if (ease2) {
      const easingFunction = Array.isArray(ease2) ? ease2[i13] || noop2 : ease2;
      mixer = pipe(easingFunction, mixer);
    }
    mixers.push(mixer);
  }
  return mixers;
}
function interpolate(input2, output, { clamp: isClamp = true, ease: ease2, mixer } = {}) {
  const inputLength = input2.length;
  invariant(inputLength === output.length, "Both input and output ranges must be the same length");
  if (inputLength === 1)
    return () => output[0];
  if (inputLength === 2 && input2[0] === input2[1])
    return () => output[1];
  if (input2[0] > input2[inputLength - 1]) {
    input2 = [...input2].reverse();
    output = [...output].reverse();
  }
  const mixers = createMixers(output, ease2, mixer);
  const numMixers = mixers.length;
  const interpolator = (v5) => {
    let i13 = 0;
    if (numMixers > 1) {
      for (; i13 < input2.length - 2; i13++) {
        if (v5 < input2[i13 + 1])
          break;
      }
    }
    const progressInRange = progress(input2[i13], input2[i13 + 1], v5);
    return mixers[i13](progressInRange);
  };
  return isClamp ? (v5) => interpolator(clamp3(input2[0], input2[inputLength - 1], v5)) : interpolator;
}
var init_interpolate = __esm({
  "node_modules/framer-motion/dist/es/utils/interpolate.mjs"() {
    init_clamp2();
    init_pipe();
    init_progress();
    init_es();
    init_mix();
  }
});

// node_modules/framer-motion/dist/es/utils/offsets/fill.mjs
function fillOffset(offset4, remaining) {
  const min2 = offset4[offset4.length - 1];
  for (let i13 = 1; i13 <= remaining; i13++) {
    const offsetProgress = progress(0, remaining, i13);
    offset4.push(mixNumber(min2, 1, offsetProgress));
  }
}
var init_fill = __esm({
  "node_modules/framer-motion/dist/es/utils/offsets/fill.mjs"() {
    init_number2();
    init_progress();
  }
});

// node_modules/framer-motion/dist/es/utils/offsets/default.mjs
function defaultOffset(arr) {
  const offset4 = [0];
  fillOffset(offset4, arr.length - 1);
  return offset4;
}
var init_default = __esm({
  "node_modules/framer-motion/dist/es/utils/offsets/default.mjs"() {
    init_fill();
  }
});

// node_modules/framer-motion/dist/es/utils/offsets/time.mjs
function convertOffsetToTimes(offset4, duration) {
  return offset4.map((o8) => o8 * duration);
}
var init_time2 = __esm({
  "node_modules/framer-motion/dist/es/utils/offsets/time.mjs"() {
  }
});

// node_modules/framer-motion/dist/es/animation/generators/keyframes.mjs
function defaultEasing(values2, easing) {
  return values2.map(() => easing || easeInOut).splice(0, values2.length - 1);
}
function keyframes({ duration = 300, keyframes: keyframeValues, times, ease: ease2 = "easeInOut" }) {
  const easingFunctions = isEasingArray(ease2) ? ease2.map(easingDefinitionToFunction) : easingDefinitionToFunction(ease2);
  const state2 = {
    done: false,
    value: keyframeValues[0]
  };
  const absoluteTimes = convertOffsetToTimes(
    // Only use the provided offsets if they're the correct length
    // TODO Maybe we should warn here if there's a length mismatch
    times && times.length === keyframeValues.length ? times : defaultOffset(keyframeValues),
    duration
  );
  const mapTimeToKeyframe = interpolate(absoluteTimes, keyframeValues, {
    ease: Array.isArray(easingFunctions) ? easingFunctions : defaultEasing(keyframeValues, easingFunctions)
  });
  return {
    calculatedDuration: duration,
    next: (t20) => {
      state2.value = mapTimeToKeyframe(t20);
      state2.done = t20 >= duration;
      return state2;
    }
  };
}
var init_keyframes = __esm({
  "node_modules/framer-motion/dist/es/animation/generators/keyframes.mjs"() {
    init_ease();
    init_is_easing_array();
    init_map();
    init_interpolate();
    init_default();
    init_time2();
  }
});

// node_modules/framer-motion/dist/es/animation/animators/drivers/driver-frameloop.mjs
var frameloopDriver;
var init_driver_frameloop = __esm({
  "node_modules/framer-motion/dist/es/animation/animators/drivers/driver-frameloop.mjs"() {
    init_sync_time();
    init_frame();
    frameloopDriver = (update) => {
      const passTimestamp = ({ timestamp }) => update(timestamp);
      return {
        start: () => frame.update(passTimestamp, true),
        stop: () => cancelFrame(passTimestamp),
        /**
         * If we're processing this frame we can use the
         * framelocked timestamp to keep things in sync.
         */
        now: () => frameData.isProcessing ? frameData.timestamp : time2.now()
      };
    };
  }
});

// node_modules/framer-motion/dist/es/animation/animators/MainThreadAnimation.mjs
var generators, percentToProgress, MainThreadAnimation;
var init_MainThreadAnimation = __esm({
  "node_modules/framer-motion/dist/es/animation/animators/MainThreadAnimation.mjs"() {
    init_KeyframesResolver();
    init_spring();
    init_inertia();
    init_keyframes();
    init_BaseAnimation();
    init_pipe();
    init_mix();
    init_calc_duration();
    init_time_conversion();
    init_clamp2();
    init_es();
    init_driver_frameloop();
    init_get_final_keyframe();
    init_is_generator();
    generators = {
      decay: inertia,
      inertia,
      tween: keyframes,
      keyframes,
      spring
    };
    percentToProgress = (percent2) => percent2 / 100;
    MainThreadAnimation = class extends BaseAnimation {
      constructor(options) {
        super(options);
        this.holdTime = null;
        this.cancelTime = null;
        this.currentTime = 0;
        this.playbackSpeed = 1;
        this.pendingPlayState = "running";
        this.startTime = null;
        this.state = "idle";
        this.stop = () => {
          this.resolver.cancel();
          this.isStopped = true;
          if (this.state === "idle")
            return;
          this.teardown();
          const { onStop } = this.options;
          onStop && onStop();
        };
        const { name: name2, motionValue: motionValue2, element, keyframes: keyframes2 } = this.options;
        const KeyframeResolver$1 = (element === null || element === void 0 ? void 0 : element.KeyframeResolver) || KeyframeResolver;
        const onResolved = (resolvedKeyframes, finalKeyframe) => this.onKeyframesResolved(resolvedKeyframes, finalKeyframe);
        this.resolver = new KeyframeResolver$1(keyframes2, onResolved, name2, motionValue2, element);
        this.resolver.scheduleResolve();
      }
      flatten() {
        super.flatten();
        if (this._resolved) {
          Object.assign(this._resolved, this.initPlayback(this._resolved.keyframes));
        }
      }
      initPlayback(keyframes$1) {
        const { type: type2 = "keyframes", repeat = 0, repeatDelay = 0, repeatType, velocity = 0 } = this.options;
        const generatorFactory = isGenerator(type2) ? type2 : generators[type2] || keyframes;
        let mapPercentToKeyframes;
        let mirroredGenerator;
        if (generatorFactory !== keyframes && typeof keyframes$1[0] !== "number") {
          if (false) {
            invariant(keyframes$1.length === 2, `Only two keyframes currently supported with spring and inertia animations. Trying to animate ${keyframes$1}`);
          }
          mapPercentToKeyframes = pipe(percentToProgress, mix(keyframes$1[0], keyframes$1[1]));
          keyframes$1 = [0, 100];
        }
        const generator = generatorFactory({ ...this.options, keyframes: keyframes$1 });
        if (repeatType === "mirror") {
          mirroredGenerator = generatorFactory({
            ...this.options,
            keyframes: [...keyframes$1].reverse(),
            velocity: -velocity
          });
        }
        if (generator.calculatedDuration === null) {
          generator.calculatedDuration = calcGeneratorDuration(generator);
        }
        const { calculatedDuration } = generator;
        const resolvedDuration = calculatedDuration + repeatDelay;
        const totalDuration = resolvedDuration * (repeat + 1) - repeatDelay;
        return {
          generator,
          mirroredGenerator,
          mapPercentToKeyframes,
          calculatedDuration,
          resolvedDuration,
          totalDuration
        };
      }
      onPostResolved() {
        const { autoplay = true } = this.options;
        this.play();
        if (this.pendingPlayState === "paused" || !autoplay) {
          this.pause();
        } else {
          this.state = this.pendingPlayState;
        }
      }
      tick(timestamp, sample = false) {
        const { resolved } = this;
        if (!resolved) {
          const { keyframes: keyframes3 } = this.options;
          return { done: true, value: keyframes3[keyframes3.length - 1] };
        }
        const { finalKeyframe, generator, mirroredGenerator, mapPercentToKeyframes, keyframes: keyframes2, calculatedDuration, totalDuration, resolvedDuration } = resolved;
        if (this.startTime === null)
          return generator.next(0);
        const { delay: delay3, repeat, repeatType, repeatDelay, onUpdate } = this.options;
        if (this.speed > 0) {
          this.startTime = Math.min(this.startTime, timestamp);
        } else if (this.speed < 0) {
          this.startTime = Math.min(timestamp - totalDuration / this.speed, this.startTime);
        }
        if (sample) {
          this.currentTime = timestamp;
        } else if (this.holdTime !== null) {
          this.currentTime = this.holdTime;
        } else {
          this.currentTime = Math.round(timestamp - this.startTime) * this.speed;
        }
        const timeWithoutDelay = this.currentTime - delay3 * (this.speed >= 0 ? 1 : -1);
        const isInDelayPhase = this.speed >= 0 ? timeWithoutDelay < 0 : timeWithoutDelay > totalDuration;
        this.currentTime = Math.max(timeWithoutDelay, 0);
        if (this.state === "finished" && this.holdTime === null) {
          this.currentTime = totalDuration;
        }
        let elapsed = this.currentTime;
        let frameGenerator = generator;
        if (repeat) {
          const progress3 = Math.min(this.currentTime, totalDuration) / resolvedDuration;
          let currentIteration = Math.floor(progress3);
          let iterationProgress = progress3 % 1;
          if (!iterationProgress && progress3 >= 1) {
            iterationProgress = 1;
          }
          iterationProgress === 1 && currentIteration--;
          currentIteration = Math.min(currentIteration, repeat + 1);
          const isOddIteration = Boolean(currentIteration % 2);
          if (isOddIteration) {
            if (repeatType === "reverse") {
              iterationProgress = 1 - iterationProgress;
              if (repeatDelay) {
                iterationProgress -= repeatDelay / resolvedDuration;
              }
            } else if (repeatType === "mirror") {
              frameGenerator = mirroredGenerator;
            }
          }
          elapsed = clamp3(0, 1, iterationProgress) * resolvedDuration;
        }
        const state2 = isInDelayPhase ? { done: false, value: keyframes2[0] } : frameGenerator.next(elapsed);
        if (mapPercentToKeyframes) {
          state2.value = mapPercentToKeyframes(state2.value);
        }
        let { done } = state2;
        if (!isInDelayPhase && calculatedDuration !== null) {
          done = this.speed >= 0 ? this.currentTime >= totalDuration : this.currentTime <= 0;
        }
        const isAnimationFinished = this.holdTime === null && (this.state === "finished" || this.state === "running" && done);
        if (isAnimationFinished && finalKeyframe !== void 0) {
          state2.value = getFinalKeyframe(keyframes2, this.options, finalKeyframe);
        }
        if (onUpdate) {
          onUpdate(state2.value);
        }
        if (isAnimationFinished) {
          this.finish();
        }
        return state2;
      }
      get duration() {
        const { resolved } = this;
        return resolved ? millisecondsToSeconds(resolved.calculatedDuration) : 0;
      }
      get time() {
        return millisecondsToSeconds(this.currentTime);
      }
      set time(newTime) {
        newTime = secondsToMilliseconds(newTime);
        this.currentTime = newTime;
        if (this.holdTime !== null || this.speed === 0) {
          this.holdTime = newTime;
        } else if (this.driver) {
          this.startTime = this.driver.now() - newTime / this.speed;
        }
      }
      get speed() {
        return this.playbackSpeed;
      }
      set speed(newSpeed) {
        const hasChanged = this.playbackSpeed !== newSpeed;
        this.playbackSpeed = newSpeed;
        if (hasChanged) {
          this.time = millisecondsToSeconds(this.currentTime);
        }
      }
      play() {
        if (!this.resolver.isScheduled) {
          this.resolver.resume();
        }
        if (!this._resolved) {
          this.pendingPlayState = "running";
          return;
        }
        if (this.isStopped)
          return;
        const { driver = frameloopDriver, onPlay, startTime } = this.options;
        if (!this.driver) {
          this.driver = driver((timestamp) => this.tick(timestamp));
        }
        onPlay && onPlay();
        const now2 = this.driver.now();
        if (this.holdTime !== null) {
          this.startTime = now2 - this.holdTime;
        } else if (!this.startTime) {
          this.startTime = startTime !== null && startTime !== void 0 ? startTime : this.calcStartTime();
        } else if (this.state === "finished") {
          this.startTime = now2;
        }
        if (this.state === "finished") {
          this.updateFinishedPromise();
        }
        this.cancelTime = this.startTime;
        this.holdTime = null;
        this.state = "running";
        this.driver.start();
      }
      pause() {
        var _a;
        if (!this._resolved) {
          this.pendingPlayState = "paused";
          return;
        }
        this.state = "paused";
        this.holdTime = (_a = this.currentTime) !== null && _a !== void 0 ? _a : 0;
      }
      complete() {
        if (this.state !== "running") {
          this.play();
        }
        this.pendingPlayState = this.state = "finished";
        this.holdTime = null;
      }
      finish() {
        this.teardown();
        this.state = "finished";
        const { onComplete } = this.options;
        onComplete && onComplete();
      }
      cancel() {
        if (this.cancelTime !== null) {
          this.tick(this.cancelTime);
        }
        this.teardown();
        this.updateFinishedPromise();
      }
      teardown() {
        this.state = "idle";
        this.stopDriver();
        this.resolveFinishedPromise();
        this.updateFinishedPromise();
        this.startTime = this.cancelTime = null;
        this.resolver.cancel();
      }
      stopDriver() {
        if (!this.driver)
          return;
        this.driver.stop();
        this.driver = void 0;
      }
      sample(time3) {
        this.startTime = 0;
        return this.tick(time3, true);
      }
    };
  }
});

// node_modules/framer-motion/dist/es/animation/animators/utils/accelerated-values.mjs
var acceleratedValues;
var init_accelerated_values = __esm({
  "node_modules/framer-motion/dist/es/animation/animators/utils/accelerated-values.mjs"() {
    acceleratedValues = /* @__PURE__ */ new Set([
      "opacity",
      "clipPath",
      "filter",
      "transform"
      // TODO: Can be accelerated but currently disabled until https://issues.chromium.org/issues/41491098 is resolved
      // or until we implement support for linear() easing.
      // "background-color"
    ]);
  }
});

// node_modules/framer-motion/dist/es/utils/memo.mjs
function memo3(callback) {
  let result;
  return () => {
    if (result === void 0)
      result = callback();
    return result;
  };
}
var init_memo = __esm({
  "node_modules/framer-motion/dist/es/utils/memo.mjs"() {
  }
});

// node_modules/framer-motion/dist/es/animation/animators/waapi/utils/supports-flags.mjs
var supportsFlags;
var init_supports_flags = __esm({
  "node_modules/framer-motion/dist/es/animation/animators/waapi/utils/supports-flags.mjs"() {
    supportsFlags = {
      linearEasing: void 0
    };
  }
});

// node_modules/framer-motion/dist/es/animation/animators/waapi/utils/memo-supports.mjs
function memoSupports(callback, supportsFlag) {
  const memoized = memo3(callback);
  return () => {
    var _a;
    return (_a = supportsFlags[supportsFlag]) !== null && _a !== void 0 ? _a : memoized();
  };
}
var init_memo_supports = __esm({
  "node_modules/framer-motion/dist/es/animation/animators/waapi/utils/memo-supports.mjs"() {
    init_memo();
    init_supports_flags();
  }
});

// node_modules/framer-motion/dist/es/animation/animators/waapi/utils/supports-linear-easing.mjs
var supportsLinearEasing;
var init_supports_linear_easing = __esm({
  "node_modules/framer-motion/dist/es/animation/animators/waapi/utils/supports-linear-easing.mjs"() {
    init_memo_supports();
    supportsLinearEasing = /* @__PURE__ */ memoSupports(() => {
      try {
        document.createElement("div").animate({ opacity: 0 }, { easing: "linear(0, 1)" });
      } catch (e14) {
        return false;
      }
      return true;
    }, "linearEasing");
  }
});

// node_modules/framer-motion/dist/es/animation/animators/waapi/easing.mjs
function isWaapiSupportedEasing(easing) {
  return Boolean(typeof easing === "function" && supportsLinearEasing() || !easing || typeof easing === "string" && (easing in supportedWaapiEasing || supportsLinearEasing()) || isBezierDefinition(easing) || Array.isArray(easing) && easing.every(isWaapiSupportedEasing));
}
function mapEasingToNativeEasing(easing, duration) {
  if (!easing) {
    return void 0;
  } else if (typeof easing === "function" && supportsLinearEasing()) {
    return generateLinearEasing(easing, duration);
  } else if (isBezierDefinition(easing)) {
    return cubicBezierAsString(easing);
  } else if (Array.isArray(easing)) {
    return easing.map((segmentEasing) => mapEasingToNativeEasing(segmentEasing, duration) || supportedWaapiEasing.easeOut);
  } else {
    return supportedWaapiEasing[easing];
  }
}
var cubicBezierAsString, supportedWaapiEasing;
var init_easing = __esm({
  "node_modules/framer-motion/dist/es/animation/animators/waapi/easing.mjs"() {
    init_is_bezier_definition();
    init_linear();
    init_supports_linear_easing();
    cubicBezierAsString = ([a18, b4, c15, d12]) => `cubic-bezier(${a18}, ${b4}, ${c15}, ${d12})`;
    supportedWaapiEasing = {
      linear: "linear",
      ease: "ease",
      easeIn: "ease-in",
      easeOut: "ease-out",
      easeInOut: "ease-in-out",
      circIn: /* @__PURE__ */ cubicBezierAsString([0, 0.65, 0.55, 1]),
      circOut: /* @__PURE__ */ cubicBezierAsString([0.55, 0, 1, 0.45]),
      backIn: /* @__PURE__ */ cubicBezierAsString([0.31, 0.01, 0.66, -0.59]),
      backOut: /* @__PURE__ */ cubicBezierAsString([0.33, 1.53, 0.69, 0.99])
    };
  }
});

// node_modules/framer-motion/dist/es/animation/animators/waapi/index.mjs
function startWaapiAnimation(element, valueName, keyframes2, { delay: delay3 = 0, duration = 300, repeat = 0, repeatType = "loop", ease: ease2 = "easeInOut", times } = {}) {
  const keyframeOptions = { [valueName]: keyframes2 };
  if (times)
    keyframeOptions.offset = times;
  const easing = mapEasingToNativeEasing(ease2, duration);
  if (Array.isArray(easing))
    keyframeOptions.easing = easing;
  return element.animate(keyframeOptions, {
    delay: delay3,
    duration,
    easing: !Array.isArray(easing) ? easing : "linear",
    fill: "both",
    iterations: repeat + 1,
    direction: repeatType === "reverse" ? "alternate" : "normal"
  });
}
var init_waapi = __esm({
  "node_modules/framer-motion/dist/es/animation/animators/waapi/index.mjs"() {
    init_easing();
  }
});

// node_modules/framer-motion/dist/es/animation/animators/waapi/utils/attach-timeline.mjs
function attachTimeline(animation, timeline) {
  animation.timeline = timeline;
  animation.onfinish = null;
}
var init_attach_timeline = __esm({
  "node_modules/framer-motion/dist/es/animation/animators/waapi/utils/attach-timeline.mjs"() {
  }
});

// node_modules/framer-motion/dist/es/animation/animators/waapi/utils/supports-waapi.mjs
var supportsWaapi;
var init_supports_waapi = __esm({
  "node_modules/framer-motion/dist/es/animation/animators/waapi/utils/supports-waapi.mjs"() {
    init_memo();
    supportsWaapi = /* @__PURE__ */ memo3(() => Object.hasOwnProperty.call(Element.prototype, "animate"));
  }
});

// node_modules/framer-motion/dist/es/animation/animators/AcceleratedAnimation.mjs
function requiresPregeneratedKeyframes(options) {
  return isGenerator(options.type) || options.type === "spring" || !isWaapiSupportedEasing(options.ease);
}
function pregenerateKeyframes(keyframes2, options) {
  const sampleAnimation = new MainThreadAnimation({
    ...options,
    keyframes: keyframes2,
    repeat: 0,
    delay: 0,
    isGenerator: true
  });
  let state2 = { done: false, value: keyframes2[0] };
  const pregeneratedKeyframes = [];
  let t20 = 0;
  while (!state2.done && t20 < maxDuration) {
    state2 = sampleAnimation.sample(t20);
    pregeneratedKeyframes.push(state2.value);
    t20 += sampleDelta;
  }
  return {
    times: void 0,
    keyframes: pregeneratedKeyframes,
    duration: t20 - sampleDelta,
    ease: "linear"
  };
}
function isUnsupportedEase(key) {
  return key in unsupportedEasingFunctions;
}
var sampleDelta, maxDuration, unsupportedEasingFunctions, AcceleratedAnimation;
var init_AcceleratedAnimation = __esm({
  "node_modules/framer-motion/dist/es/animation/animators/AcceleratedAnimation.mjs"() {
    init_anticipate();
    init_back();
    init_circ();
    init_DOMKeyframesResolver();
    init_es();
    init_time_conversion();
    init_is_generator();
    init_BaseAnimation();
    init_MainThreadAnimation();
    init_accelerated_values();
    init_waapi();
    init_easing();
    init_attach_timeline();
    init_get_final_keyframe();
    init_supports_linear_easing();
    init_supports_waapi();
    sampleDelta = 10;
    maxDuration = 2e4;
    unsupportedEasingFunctions = {
      anticipate,
      backInOut,
      circInOut
    };
    AcceleratedAnimation = class extends BaseAnimation {
      constructor(options) {
        super(options);
        const { name: name2, motionValue: motionValue2, element, keyframes: keyframes2 } = this.options;
        this.resolver = new DOMKeyframesResolver(keyframes2, (resolvedKeyframes, finalKeyframe) => this.onKeyframesResolved(resolvedKeyframes, finalKeyframe), name2, motionValue2, element);
        this.resolver.scheduleResolve();
      }
      initPlayback(keyframes2, finalKeyframe) {
        var _a;
        let { duration = 300, times, ease: ease2, type: type2, motionValue: motionValue2, name: name2, startTime } = this.options;
        if (!((_a = motionValue2.owner) === null || _a === void 0 ? void 0 : _a.current)) {
          return false;
        }
        if (typeof ease2 === "string" && supportsLinearEasing() && isUnsupportedEase(ease2)) {
          ease2 = unsupportedEasingFunctions[ease2];
        }
        if (requiresPregeneratedKeyframes(this.options)) {
          const { onComplete, onUpdate, motionValue: motionValue3, element, ...options } = this.options;
          const pregeneratedAnimation = pregenerateKeyframes(keyframes2, options);
          keyframes2 = pregeneratedAnimation.keyframes;
          if (keyframes2.length === 1) {
            keyframes2[1] = keyframes2[0];
          }
          duration = pregeneratedAnimation.duration;
          times = pregeneratedAnimation.times;
          ease2 = pregeneratedAnimation.ease;
          type2 = "keyframes";
        }
        const animation = startWaapiAnimation(motionValue2.owner.current, name2, keyframes2, { ...this.options, duration, times, ease: ease2 });
        animation.startTime = startTime !== null && startTime !== void 0 ? startTime : this.calcStartTime();
        if (this.pendingTimeline) {
          attachTimeline(animation, this.pendingTimeline);
          this.pendingTimeline = void 0;
        } else {
          animation.onfinish = () => {
            const { onComplete } = this.options;
            motionValue2.set(getFinalKeyframe(keyframes2, this.options, finalKeyframe));
            onComplete && onComplete();
            this.cancel();
            this.resolveFinishedPromise();
          };
        }
        return {
          animation,
          duration,
          times,
          type: type2,
          ease: ease2,
          keyframes: keyframes2
        };
      }
      get duration() {
        const { resolved } = this;
        if (!resolved)
          return 0;
        const { duration } = resolved;
        return millisecondsToSeconds(duration);
      }
      get time() {
        const { resolved } = this;
        if (!resolved)
          return 0;
        const { animation } = resolved;
        return millisecondsToSeconds(animation.currentTime || 0);
      }
      set time(newTime) {
        const { resolved } = this;
        if (!resolved)
          return;
        const { animation } = resolved;
        animation.currentTime = secondsToMilliseconds(newTime);
      }
      get speed() {
        const { resolved } = this;
        if (!resolved)
          return 1;
        const { animation } = resolved;
        return animation.playbackRate;
      }
      set speed(newSpeed) {
        const { resolved } = this;
        if (!resolved)
          return;
        const { animation } = resolved;
        animation.playbackRate = newSpeed;
      }
      get state() {
        const { resolved } = this;
        if (!resolved)
          return "idle";
        const { animation } = resolved;
        return animation.playState;
      }
      get startTime() {
        const { resolved } = this;
        if (!resolved)
          return null;
        const { animation } = resolved;
        return animation.startTime;
      }
      /**
       * Replace the default DocumentTimeline with another AnimationTimeline.
       * Currently used for scroll animations.
       */
      attachTimeline(timeline) {
        if (!this._resolved) {
          this.pendingTimeline = timeline;
        } else {
          const { resolved } = this;
          if (!resolved)
            return noop2;
          const { animation } = resolved;
          attachTimeline(animation, timeline);
        }
        return noop2;
      }
      play() {
        if (this.isStopped)
          return;
        const { resolved } = this;
        if (!resolved)
          return;
        const { animation } = resolved;
        if (animation.playState === "finished") {
          this.updateFinishedPromise();
        }
        animation.play();
      }
      pause() {
        const { resolved } = this;
        if (!resolved)
          return;
        const { animation } = resolved;
        animation.pause();
      }
      stop() {
        this.resolver.cancel();
        this.isStopped = true;
        if (this.state === "idle")
          return;
        this.resolveFinishedPromise();
        this.updateFinishedPromise();
        const { resolved } = this;
        if (!resolved)
          return;
        const { animation, keyframes: keyframes2, duration, type: type2, ease: ease2, times } = resolved;
        if (animation.playState === "idle" || animation.playState === "finished") {
          return;
        }
        if (this.time) {
          const { motionValue: motionValue2, onUpdate, onComplete, element, ...options } = this.options;
          const sampleAnimation = new MainThreadAnimation({
            ...options,
            keyframes: keyframes2,
            duration,
            type: type2,
            ease: ease2,
            times,
            isGenerator: true
          });
          const sampleTime = secondsToMilliseconds(this.time);
          motionValue2.setWithVelocity(sampleAnimation.sample(sampleTime - sampleDelta).value, sampleAnimation.sample(sampleTime).value, sampleDelta);
        }
        const { onStop } = this.options;
        onStop && onStop();
        this.cancel();
      }
      complete() {
        const { resolved } = this;
        if (!resolved)
          return;
        resolved.animation.finish();
      }
      cancel() {
        const { resolved } = this;
        if (!resolved)
          return;
        resolved.animation.cancel();
      }
      static supports(options) {
        const { motionValue: motionValue2, name: name2, repeatDelay, repeatType, damping, type: type2 } = options;
        return supportsWaapi() && name2 && acceleratedValues.has(name2) && motionValue2 && motionValue2.owner && motionValue2.owner.current instanceof HTMLElement && /**
         * If we're outputting values to onUpdate then we can't use WAAPI as there's
         * no way to read the value from WAAPI every frame.
         */
        !motionValue2.owner.getProps().onUpdate && !repeatDelay && repeatType !== "mirror" && damping !== 0 && type2 !== "inertia";
      }
    };
  }
});

// node_modules/framer-motion/dist/es/render/dom/scroll/supports.mjs
var supportsScrollTimeline;
var init_supports = __esm({
  "node_modules/framer-motion/dist/es/render/dom/scroll/supports.mjs"() {
    init_memo();
    supportsScrollTimeline = memo3(() => window.ScrollTimeline !== void 0);
  }
});

// node_modules/framer-motion/dist/es/animation/GroupPlaybackControls.mjs
var GroupPlaybackControls;
var init_GroupPlaybackControls = __esm({
  "node_modules/framer-motion/dist/es/animation/GroupPlaybackControls.mjs"() {
    init_supports();
    GroupPlaybackControls = class {
      constructor(animations2) {
        this.stop = () => this.runAll("stop");
        this.animations = animations2.filter(Boolean);
      }
      then(onResolve, onReject) {
        return Promise.all(this.animations).then(onResolve).catch(onReject);
      }
      /**
       * TODO: Filter out cancelled or stopped animations before returning
       */
      getAll(propName) {
        return this.animations[0][propName];
      }
      setAll(propName, newValue) {
        for (let i13 = 0; i13 < this.animations.length; i13++) {
          this.animations[i13][propName] = newValue;
        }
      }
      attachTimeline(timeline, fallback) {
        const subscriptions = this.animations.map((animation) => {
          if (supportsScrollTimeline() && animation.attachTimeline) {
            return animation.attachTimeline(timeline);
          } else {
            return fallback(animation);
          }
        });
        return () => {
          subscriptions.forEach((cancel, i13) => {
            cancel && cancel();
            this.animations[i13].stop();
          });
        };
      }
      get time() {
        return this.getAll("time");
      }
      set time(time3) {
        this.setAll("time", time3);
      }
      get speed() {
        return this.getAll("speed");
      }
      set speed(speed) {
        this.setAll("speed", speed);
      }
      get startTime() {
        return this.getAll("startTime");
      }
      get duration() {
        let max2 = 0;
        for (let i13 = 0; i13 < this.animations.length; i13++) {
          max2 = Math.max(max2, this.animations[i13].duration);
        }
        return max2;
      }
      runAll(methodName) {
        this.animations.forEach((controls) => controls[methodName]());
      }
      flatten() {
        this.runAll("flatten");
      }
      play() {
        this.runAll("play");
      }
      pause() {
        this.runAll("pause");
      }
      cancel() {
        this.runAll("cancel");
      }
      complete() {
        this.runAll("complete");
      }
    };
  }
});

// node_modules/framer-motion/dist/es/animation/utils/is-transition-defined.mjs
function isTransitionDefined({ when, delay: _delay, delayChildren, staggerChildren, staggerDirection, repeat, repeatType, repeatDelay, from, elapsed, ...transition }) {
  return !!Object.keys(transition).length;
}
var init_is_transition_defined = __esm({
  "node_modules/framer-motion/dist/es/animation/utils/is-transition-defined.mjs"() {
  }
});

// node_modules/framer-motion/dist/es/animation/interfaces/motion-value.mjs
var animateMotionValue;
var init_motion_value = __esm({
  "node_modules/framer-motion/dist/es/animation/interfaces/motion-value.mjs"() {
    init_time_conversion();
    init_default_transitions();
    init_get_value_transition();
    init_GlobalConfig();
    init_use_instant_transition_state();
    init_get_final_keyframe();
    init_frame();
    init_AcceleratedAnimation();
    init_MainThreadAnimation();
    init_GroupPlaybackControls();
    init_is_transition_defined();
    animateMotionValue = (name2, value, target, transition = {}, element, isHandoff) => (onComplete) => {
      const valueTransition = getValueTransition(transition, name2) || {};
      const delay3 = valueTransition.delay || transition.delay || 0;
      let { elapsed = 0 } = transition;
      elapsed = elapsed - secondsToMilliseconds(delay3);
      let options = {
        keyframes: Array.isArray(target) ? target : [null, target],
        ease: "easeOut",
        velocity: value.getVelocity(),
        ...valueTransition,
        delay: -elapsed,
        onUpdate: (v5) => {
          value.set(v5);
          valueTransition.onUpdate && valueTransition.onUpdate(v5);
        },
        onComplete: () => {
          onComplete();
          valueTransition.onComplete && valueTransition.onComplete();
        },
        name: name2,
        motionValue: value,
        element: isHandoff ? void 0 : element
      };
      if (!isTransitionDefined(valueTransition)) {
        options = {
          ...options,
          ...getDefaultTransition(name2, options)
        };
      }
      if (options.duration) {
        options.duration = secondsToMilliseconds(options.duration);
      }
      if (options.repeatDelay) {
        options.repeatDelay = secondsToMilliseconds(options.repeatDelay);
      }
      if (options.from !== void 0) {
        options.keyframes[0] = options.from;
      }
      let shouldSkip = false;
      if (options.type === false || options.duration === 0 && !options.repeatDelay) {
        options.duration = 0;
        if (options.delay === 0) {
          shouldSkip = true;
        }
      }
      if (instantAnimationState.current || MotionGlobalConfig.skipAnimations) {
        shouldSkip = true;
        options.duration = 0;
        options.delay = 0;
      }
      if (shouldSkip && !isHandoff && value.get() !== void 0) {
        const finalKeyframe = getFinalKeyframe(options.keyframes, valueTransition);
        if (finalKeyframe !== void 0) {
          frame.update(() => {
            options.onUpdate(finalKeyframe);
            options.onComplete();
          });
          return new GroupPlaybackControls([]);
        }
      }
      if (!isHandoff && AcceleratedAnimation.supports(options)) {
        return new AcceleratedAnimation(options);
      } else {
        return new MainThreadAnimation(options);
      }
    };
  }
});

// node_modules/framer-motion/dist/es/utils/resolve-value.mjs
var isCustomValue, resolveFinalValueInKeyframes;
var init_resolve_value = __esm({
  "node_modules/framer-motion/dist/es/utils/resolve-value.mjs"() {
    init_is_keyframes_target();
    isCustomValue = (v5) => {
      return Boolean(v5 && typeof v5 === "object" && v5.mix && v5.toValue);
    };
    resolveFinalValueInKeyframes = (v5) => {
      return isKeyframesTarget(v5) ? v5[v5.length - 1] || 0 : v5;
    };
  }
});

// node_modules/framer-motion/dist/es/utils/array.mjs
function addUniqueItem(arr, item2) {
  if (arr.indexOf(item2) === -1)
    arr.push(item2);
}
function removeItem(arr, item2) {
  const index3 = arr.indexOf(item2);
  if (index3 > -1)
    arr.splice(index3, 1);
}
var init_array = __esm({
  "node_modules/framer-motion/dist/es/utils/array.mjs"() {
  }
});

// node_modules/framer-motion/dist/es/utils/subscription-manager.mjs
var SubscriptionManager;
var init_subscription_manager = __esm({
  "node_modules/framer-motion/dist/es/utils/subscription-manager.mjs"() {
    init_array();
    SubscriptionManager = class {
      constructor() {
        this.subscriptions = [];
      }
      add(handler2) {
        addUniqueItem(this.subscriptions, handler2);
        return () => removeItem(this.subscriptions, handler2);
      }
      notify(a18, b4, c15) {
        const numSubscriptions = this.subscriptions.length;
        if (!numSubscriptions)
          return;
        if (numSubscriptions === 1) {
          this.subscriptions[0](a18, b4, c15);
        } else {
          for (let i13 = 0; i13 < numSubscriptions; i13++) {
            const handler2 = this.subscriptions[i13];
            handler2 && handler2(a18, b4, c15);
          }
        }
      }
      getSize() {
        return this.subscriptions.length;
      }
      clear() {
        this.subscriptions.length = 0;
      }
    };
  }
});

// node_modules/framer-motion/dist/es/value/index.mjs
function motionValue(init, options) {
  return new MotionValue(init, options);
}
var MAX_VELOCITY_DELTA, isFloat, collectMotionValues, MotionValue;
var init_value = __esm({
  "node_modules/framer-motion/dist/es/value/index.mjs"() {
    init_subscription_manager();
    init_velocity_per_second();
    init_sync_time();
    init_frame();
    MAX_VELOCITY_DELTA = 30;
    isFloat = (value) => {
      return !isNaN(parseFloat(value));
    };
    collectMotionValues = {
      current: void 0
    };
    MotionValue = class {
      /**
       * @param init - The initiating value
       * @param config - Optional configuration options
       *
       * -  `transformer`: A function to transform incoming values with.
       *
       * @internal
       */
      constructor(init, options = {}) {
        this.version = "11.13.1";
        this.canTrackVelocity = null;
        this.events = {};
        this.updateAndNotify = (v5, render = true) => {
          const currentTime = time2.now();
          if (this.updatedAt !== currentTime) {
            this.setPrevFrameValue();
          }
          this.prev = this.current;
          this.setCurrent(v5);
          if (this.current !== this.prev && this.events.change) {
            this.events.change.notify(this.current);
          }
          if (render && this.events.renderRequest) {
            this.events.renderRequest.notify(this.current);
          }
        };
        this.hasAnimated = false;
        this.setCurrent(init);
        this.owner = options.owner;
      }
      setCurrent(current) {
        this.current = current;
        this.updatedAt = time2.now();
        if (this.canTrackVelocity === null && current !== void 0) {
          this.canTrackVelocity = isFloat(this.current);
        }
      }
      setPrevFrameValue(prevFrameValue = this.current) {
        this.prevFrameValue = prevFrameValue;
        this.prevUpdatedAt = this.updatedAt;
      }
      /**
       * Adds a function that will be notified when the `MotionValue` is updated.
       *
       * It returns a function that, when called, will cancel the subscription.
       *
       * When calling `onChange` inside a React component, it should be wrapped with the
       * `useEffect` hook. As it returns an unsubscribe function, this should be returned
       * from the `useEffect` function to ensure you don't add duplicate subscribers..
       *
       * ```jsx
       * export const MyComponent = () => {
       *   const x = useMotionValue(0)
       *   const y = useMotionValue(0)
       *   const opacity = useMotionValue(1)
       *
       *   useEffect(() => {
       *     function updateOpacity() {
       *       const maxXY = Math.max(x.get(), y.get())
       *       const newOpacity = transform(maxXY, [0, 100], [1, 0])
       *       opacity.set(newOpacity)
       *     }
       *
       *     const unsubscribeX = x.on("change", updateOpacity)
       *     const unsubscribeY = y.on("change", updateOpacity)
       *
       *     return () => {
       *       unsubscribeX()
       *       unsubscribeY()
       *     }
       *   }, [])
       *
       *   return <motion.div style={{ x }} />
       * }
       * ```
       *
       * @param subscriber - A function that receives the latest value.
       * @returns A function that, when called, will cancel this subscription.
       *
       * @deprecated
       */
      onChange(subscription) {
        if (false) {
          warnOnce(false, `value.onChange(callback) is deprecated. Switch to value.on("change", callback).`);
        }
        return this.on("change", subscription);
      }
      on(eventName, callback) {
        if (!this.events[eventName]) {
          this.events[eventName] = new SubscriptionManager();
        }
        const unsubscribe = this.events[eventName].add(callback);
        if (eventName === "change") {
          return () => {
            unsubscribe();
            frame.read(() => {
              if (!this.events.change.getSize()) {
                this.stop();
              }
            });
          };
        }
        return unsubscribe;
      }
      clearListeners() {
        for (const eventManagers in this.events) {
          this.events[eventManagers].clear();
        }
      }
      /**
       * Attaches a passive effect to the `MotionValue`.
       *
       * @internal
       */
      attach(passiveEffect, stopPassiveEffect) {
        this.passiveEffect = passiveEffect;
        this.stopPassiveEffect = stopPassiveEffect;
      }
      /**
       * Sets the state of the `MotionValue`.
       *
       * @remarks
       *
       * ```jsx
       * const x = useMotionValue(0)
       * x.set(10)
       * ```
       *
       * @param latest - Latest value to set.
       * @param render - Whether to notify render subscribers. Defaults to `true`
       *
       * @public
       */
      set(v5, render = true) {
        if (!render || !this.passiveEffect) {
          this.updateAndNotify(v5, render);
        } else {
          this.passiveEffect(v5, this.updateAndNotify);
        }
      }
      setWithVelocity(prev2, current, delta) {
        this.set(current);
        this.prev = void 0;
        this.prevFrameValue = prev2;
        this.prevUpdatedAt = this.updatedAt - delta;
      }
      /**
       * Set the state of the `MotionValue`, stopping any active animations,
       * effects, and resets velocity to `0`.
       */
      jump(v5, endAnimation = true) {
        this.updateAndNotify(v5);
        this.prev = v5;
        this.prevUpdatedAt = this.prevFrameValue = void 0;
        endAnimation && this.stop();
        if (this.stopPassiveEffect)
          this.stopPassiveEffect();
      }
      /**
       * Returns the latest state of `MotionValue`
       *
       * @returns - The latest state of `MotionValue`
       *
       * @public
       */
      get() {
        if (collectMotionValues.current) {
          collectMotionValues.current.push(this);
        }
        return this.current;
      }
      /**
       * @public
       */
      getPrevious() {
        return this.prev;
      }
      /**
       * Returns the latest velocity of `MotionValue`
       *
       * @returns - The latest velocity of `MotionValue`. Returns `0` if the state is non-numerical.
       *
       * @public
       */
      getVelocity() {
        const currentTime = time2.now();
        if (!this.canTrackVelocity || this.prevFrameValue === void 0 || currentTime - this.updatedAt > MAX_VELOCITY_DELTA) {
          return 0;
        }
        const delta = Math.min(this.updatedAt - this.prevUpdatedAt, MAX_VELOCITY_DELTA);
        return velocityPerSecond(parseFloat(this.current) - parseFloat(this.prevFrameValue), delta);
      }
      /**
       * Registers a new animation to control this `MotionValue`. Only one
       * animation can drive a `MotionValue` at one time.
       *
       * ```jsx
       * value.start()
       * ```
       *
       * @param animation - A function that starts the provided animation
       *
       * @internal
       */
      start(startAnimation) {
        this.stop();
        return new Promise((resolve) => {
          this.hasAnimated = true;
          this.animation = startAnimation(resolve);
          if (this.events.animationStart) {
            this.events.animationStart.notify();
          }
        }).then(() => {
          if (this.events.animationComplete) {
            this.events.animationComplete.notify();
          }
          this.clearAnimation();
        });
      }
      /**
       * Stop the currently active animation.
       *
       * @public
       */
      stop() {
        if (this.animation) {
          this.animation.stop();
          if (this.events.animationCancel) {
            this.events.animationCancel.notify();
          }
        }
        this.clearAnimation();
      }
      /**
       * Returns `true` if this value is currently animating.
       *
       * @public
       */
      isAnimating() {
        return !!this.animation;
      }
      clearAnimation() {
        delete this.animation;
      }
      /**
       * Destroy and clean up subscribers to this `MotionValue`.
       *
       * The `MotionValue` hooks like `useMotionValue` and `useTransform` automatically
       * handle the lifecycle of the returned `MotionValue`, so this method is only necessary if you've manually
       * created a `MotionValue` via the `motionValue` function.
       *
       * @public
       */
      destroy() {
        this.clearListeners();
        this.stop();
        if (this.stopPassiveEffect) {
          this.stopPassiveEffect();
        }
      }
    };
  }
});

// node_modules/framer-motion/dist/es/render/utils/setters.mjs
function setMotionValue(visualElement, key, value) {
  if (visualElement.hasValue(key)) {
    visualElement.getValue(key).set(value);
  } else {
    visualElement.addValue(key, motionValue(value));
  }
}
function setTarget(visualElement, definition) {
  const resolved = resolveVariant(visualElement, definition);
  let { transitionEnd = {}, transition = {}, ...target } = resolved || {};
  target = { ...target, ...transitionEnd };
  for (const key in target) {
    const value = resolveFinalValueInKeyframes(target[key]);
    setMotionValue(visualElement, key, value);
  }
}
var init_setters = __esm({
  "node_modules/framer-motion/dist/es/render/utils/setters.mjs"() {
    init_resolve_value();
    init_value();
    init_resolve_dynamic_variants();
  }
});

// node_modules/framer-motion/dist/es/render/dom/utils/camel-to-dash.mjs
var camelToDash;
var init_camel_to_dash = __esm({
  "node_modules/framer-motion/dist/es/render/dom/utils/camel-to-dash.mjs"() {
    camelToDash = (str) => str.replace(/([a-z])([A-Z])/gu, "$1-$2").toLowerCase();
  }
});

// node_modules/framer-motion/dist/es/animation/optimized-appear/data-id.mjs
var optimizedAppearDataId, optimizedAppearDataAttribute;
var init_data_id = __esm({
  "node_modules/framer-motion/dist/es/animation/optimized-appear/data-id.mjs"() {
    init_camel_to_dash();
    optimizedAppearDataId = "framerAppearId";
    optimizedAppearDataAttribute = "data-" + camelToDash(optimizedAppearDataId);
  }
});

// node_modules/framer-motion/dist/es/animation/optimized-appear/get-appear-id.mjs
function getOptimisedAppearId(visualElement) {
  return visualElement.props[optimizedAppearDataAttribute];
}
var init_get_appear_id = __esm({
  "node_modules/framer-motion/dist/es/animation/optimized-appear/get-appear-id.mjs"() {
    init_data_id();
  }
});

// node_modules/framer-motion/dist/es/value/utils/is-motion-value.mjs
var isMotionValue;
var init_is_motion_value = __esm({
  "node_modules/framer-motion/dist/es/value/utils/is-motion-value.mjs"() {
    isMotionValue = (value) => Boolean(value && value.getVelocity);
  }
});

// node_modules/framer-motion/dist/es/value/use-will-change/is.mjs
function isWillChangeMotionValue(value) {
  return Boolean(isMotionValue(value) && value.add);
}
var init_is = __esm({
  "node_modules/framer-motion/dist/es/value/use-will-change/is.mjs"() {
    init_is_motion_value();
  }
});

// node_modules/framer-motion/dist/es/value/use-will-change/add-will-change.mjs
function addValueToWillChange(visualElement, key) {
  const willChange = visualElement.getValue("willChange");
  if (isWillChangeMotionValue(willChange)) {
    return willChange.add(key);
  }
}
var init_add_will_change = __esm({
  "node_modules/framer-motion/dist/es/value/use-will-change/add-will-change.mjs"() {
    init_is();
  }
});

// node_modules/framer-motion/dist/es/animation/interfaces/visual-element-target.mjs
function shouldBlockAnimation({ protectedKeys, needsAnimating }, key) {
  const shouldBlock = protectedKeys.hasOwnProperty(key) && needsAnimating[key] !== true;
  needsAnimating[key] = false;
  return shouldBlock;
}
function animateTarget(visualElement, targetAndTransition, { delay: delay3 = 0, transitionOverride, type: type2 } = {}) {
  var _a;
  let { transition = visualElement.getDefaultTransition(), transitionEnd, ...target } = targetAndTransition;
  if (transitionOverride)
    transition = transitionOverride;
  const animations2 = [];
  const animationTypeState = type2 && visualElement.animationState && visualElement.animationState.getState()[type2];
  for (const key in target) {
    const value = visualElement.getValue(key, (_a = visualElement.latestValues[key]) !== null && _a !== void 0 ? _a : null);
    const valueTarget = target[key];
    if (valueTarget === void 0 || animationTypeState && shouldBlockAnimation(animationTypeState, key)) {
      continue;
    }
    const valueTransition = {
      delay: delay3,
      ...getValueTransition(transition || {}, key)
    };
    let isHandoff = false;
    if (window.MotionHandoffAnimation) {
      const appearId = getOptimisedAppearId(visualElement);
      if (appearId) {
        const startTime = window.MotionHandoffAnimation(appearId, key, frame);
        if (startTime !== null) {
          valueTransition.startTime = startTime;
          isHandoff = true;
        }
      }
    }
    addValueToWillChange(visualElement, key);
    value.start(animateMotionValue(key, value, valueTarget, visualElement.shouldReduceMotion && transformProps.has(key) ? { type: false } : valueTransition, visualElement, isHandoff));
    const animation = value.animation;
    if (animation) {
      animations2.push(animation);
    }
  }
  if (transitionEnd) {
    Promise.all(animations2).then(() => {
      frame.update(() => {
        transitionEnd && setTarget(visualElement, transitionEnd);
      });
    });
  }
  return animations2;
}
var init_visual_element_target = __esm({
  "node_modules/framer-motion/dist/es/animation/interfaces/visual-element-target.mjs"() {
    init_transform();
    init_motion_value();
    init_setters();
    init_get_value_transition();
    init_get_appear_id();
    init_add_will_change();
    init_frame();
  }
});

// node_modules/framer-motion/dist/es/animation/interfaces/visual-element-variant.mjs
function animateVariant(visualElement, variant, options = {}) {
  var _a;
  const resolved = resolveVariant(visualElement, variant, options.type === "exit" ? (_a = visualElement.presenceContext) === null || _a === void 0 ? void 0 : _a.custom : void 0);
  let { transition = visualElement.getDefaultTransition() || {} } = resolved || {};
  if (options.transitionOverride) {
    transition = options.transitionOverride;
  }
  const getAnimation = resolved ? () => Promise.all(animateTarget(visualElement, resolved, options)) : () => Promise.resolve();
  const getChildAnimations = visualElement.variantChildren && visualElement.variantChildren.size ? (forwardDelay = 0) => {
    const { delayChildren = 0, staggerChildren, staggerDirection } = transition;
    return animateChildren(visualElement, variant, delayChildren + forwardDelay, staggerChildren, staggerDirection, options);
  } : () => Promise.resolve();
  const { when } = transition;
  if (when) {
    const [first, last] = when === "beforeChildren" ? [getAnimation, getChildAnimations] : [getChildAnimations, getAnimation];
    return first().then(() => last());
  } else {
    return Promise.all([getAnimation(), getChildAnimations(options.delay)]);
  }
}
function animateChildren(visualElement, variant, delayChildren = 0, staggerChildren = 0, staggerDirection = 1, options) {
  const animations2 = [];
  const maxStaggerDuration = (visualElement.variantChildren.size - 1) * staggerChildren;
  const generateStaggerDuration = staggerDirection === 1 ? (i13 = 0) => i13 * staggerChildren : (i13 = 0) => maxStaggerDuration - i13 * staggerChildren;
  Array.from(visualElement.variantChildren).sort(sortByTreeOrder).forEach((child, i13) => {
    child.notify("AnimationStart", variant);
    animations2.push(animateVariant(child, variant, {
      ...options,
      delay: delayChildren + generateStaggerDuration(i13)
    }).then(() => child.notify("AnimationComplete", variant)));
  });
  return Promise.all(animations2);
}
function sortByTreeOrder(a18, b4) {
  return a18.sortNodePosition(b4);
}
var init_visual_element_variant = __esm({
  "node_modules/framer-motion/dist/es/animation/interfaces/visual-element-variant.mjs"() {
    init_resolve_dynamic_variants();
    init_visual_element_target();
  }
});

// node_modules/framer-motion/dist/es/animation/interfaces/visual-element.mjs
function animateVisualElement(visualElement, definition, options = {}) {
  visualElement.notify("AnimationStart", definition);
  let animation;
  if (Array.isArray(definition)) {
    const animations2 = definition.map((variant) => animateVariant(visualElement, variant, options));
    animation = Promise.all(animations2);
  } else if (typeof definition === "string") {
    animation = animateVariant(visualElement, definition, options);
  } else {
    const resolvedDefinition = typeof definition === "function" ? resolveVariant(visualElement, definition, options.custom) : definition;
    animation = Promise.all(animateTarget(visualElement, resolvedDefinition, options));
  }
  return animation.then(() => {
    visualElement.notify("AnimationComplete", definition);
  });
}
var init_visual_element = __esm({
  "node_modules/framer-motion/dist/es/animation/interfaces/visual-element.mjs"() {
    init_resolve_dynamic_variants();
    init_visual_element_target();
    init_visual_element_variant();
  }
});

// node_modules/framer-motion/dist/es/render/utils/get-variant-context.mjs
function getVariantContext(visualElement) {
  if (!visualElement)
    return void 0;
  if (!visualElement.isControllingVariants) {
    const context2 = visualElement.parent ? getVariantContext(visualElement.parent) || {} : {};
    if (visualElement.props.initial !== void 0) {
      context2.initial = visualElement.props.initial;
    }
    return context2;
  }
  const context = {};
  for (let i13 = 0; i13 < numVariantProps; i13++) {
    const name2 = variantProps[i13];
    const prop = visualElement.props[name2];
    if (isVariantLabel(prop) || prop === false) {
      context[name2] = prop;
    }
  }
  return context;
}
var numVariantProps;
var init_get_variant_context = __esm({
  "node_modules/framer-motion/dist/es/render/utils/get-variant-context.mjs"() {
    init_is_variant_label();
    init_variant_props();
    numVariantProps = variantProps.length;
  }
});

// node_modules/framer-motion/dist/es/render/utils/animation-state.mjs
function animateList(visualElement) {
  return (animations2) => Promise.all(animations2.map(({ animation, options }) => animateVisualElement(visualElement, animation, options)));
}
function createAnimationState(visualElement) {
  let animate = animateList(visualElement);
  let state2 = createState();
  let isInitialRender = true;
  const buildResolvedTypeValues = (type2) => (acc, definition) => {
    var _a;
    const resolved = resolveVariant(visualElement, definition, type2 === "exit" ? (_a = visualElement.presenceContext) === null || _a === void 0 ? void 0 : _a.custom : void 0);
    if (resolved) {
      const { transition, transitionEnd, ...target } = resolved;
      acc = { ...acc, ...target, ...transitionEnd };
    }
    return acc;
  };
  function setAnimateFunction(makeAnimator) {
    animate = makeAnimator(visualElement);
  }
  function animateChanges(changedActiveType) {
    const { props } = visualElement;
    const context = getVariantContext(visualElement.parent) || {};
    const animations2 = [];
    const removedKeys = /* @__PURE__ */ new Set();
    let encounteredKeys = {};
    let removedVariantIndex = Infinity;
    for (let i13 = 0; i13 < numAnimationTypes; i13++) {
      const type2 = reversePriorityOrder[i13];
      const typeState = state2[type2];
      const prop = props[type2] !== void 0 ? props[type2] : context[type2];
      const propIsVariant = isVariantLabel(prop);
      const activeDelta = type2 === changedActiveType ? typeState.isActive : null;
      if (activeDelta === false)
        removedVariantIndex = i13;
      let isInherited = prop === context[type2] && prop !== props[type2] && propIsVariant;
      if (isInherited && isInitialRender && visualElement.manuallyAnimateOnMount) {
        isInherited = false;
      }
      typeState.protectedKeys = { ...encounteredKeys };
      if (
        // If it isn't active and hasn't *just* been set as inactive
        !typeState.isActive && activeDelta === null || // If we didn't and don't have any defined prop for this animation type
        !prop && !typeState.prevProp || // Or if the prop doesn't define an animation
        isAnimationControls(prop) || typeof prop === "boolean"
      ) {
        continue;
      }
      const variantDidChange = checkVariantsDidChange(typeState.prevProp, prop);
      let shouldAnimateType = variantDidChange || // If we're making this variant active, we want to always make it active
      type2 === changedActiveType && typeState.isActive && !isInherited && propIsVariant || // If we removed a higher-priority variant (i is in reverse order)
      i13 > removedVariantIndex && propIsVariant;
      let handledRemovedValues = false;
      const definitionList = Array.isArray(prop) ? prop : [prop];
      let resolvedValues = definitionList.reduce(buildResolvedTypeValues(type2), {});
      if (activeDelta === false)
        resolvedValues = {};
      const { prevResolvedValues = {} } = typeState;
      const allKeys2 = {
        ...prevResolvedValues,
        ...resolvedValues
      };
      const markToAnimate = (key) => {
        shouldAnimateType = true;
        if (removedKeys.has(key)) {
          handledRemovedValues = true;
          removedKeys.delete(key);
        }
        typeState.needsAnimating[key] = true;
        const motionValue2 = visualElement.getValue(key);
        if (motionValue2)
          motionValue2.liveStyle = false;
      };
      for (const key in allKeys2) {
        const next2 = resolvedValues[key];
        const prev2 = prevResolvedValues[key];
        if (encounteredKeys.hasOwnProperty(key))
          continue;
        let valueHasChanged = false;
        if (isKeyframesTarget(next2) && isKeyframesTarget(prev2)) {
          valueHasChanged = !shallowCompare(next2, prev2);
        } else {
          valueHasChanged = next2 !== prev2;
        }
        if (valueHasChanged) {
          if (next2 !== void 0 && next2 !== null) {
            markToAnimate(key);
          } else {
            removedKeys.add(key);
          }
        } else if (next2 !== void 0 && removedKeys.has(key)) {
          markToAnimate(key);
        } else {
          typeState.protectedKeys[key] = true;
        }
      }
      typeState.prevProp = prop;
      typeState.prevResolvedValues = resolvedValues;
      if (typeState.isActive) {
        encounteredKeys = { ...encounteredKeys, ...resolvedValues };
      }
      if (isInitialRender && visualElement.blockInitialAnimation) {
        shouldAnimateType = false;
      }
      const willAnimateViaParent = isInherited && variantDidChange;
      const needsAnimating = !willAnimateViaParent || handledRemovedValues;
      if (shouldAnimateType && needsAnimating) {
        animations2.push(...definitionList.map((animation) => ({
          animation,
          options: { type: type2 }
        })));
      }
    }
    if (removedKeys.size) {
      const fallbackAnimation = {};
      removedKeys.forEach((key) => {
        const fallbackTarget = visualElement.getBaseTarget(key);
        const motionValue2 = visualElement.getValue(key);
        if (motionValue2)
          motionValue2.liveStyle = true;
        fallbackAnimation[key] = fallbackTarget !== null && fallbackTarget !== void 0 ? fallbackTarget : null;
      });
      animations2.push({ animation: fallbackAnimation });
    }
    let shouldAnimate = Boolean(animations2.length);
    if (isInitialRender && (props.initial === false || props.initial === props.animate) && !visualElement.manuallyAnimateOnMount) {
      shouldAnimate = false;
    }
    isInitialRender = false;
    return shouldAnimate ? animate(animations2) : Promise.resolve();
  }
  function setActive(type2, isActive) {
    var _a;
    if (state2[type2].isActive === isActive)
      return Promise.resolve();
    (_a = visualElement.variantChildren) === null || _a === void 0 ? void 0 : _a.forEach((child) => {
      var _a2;
      return (_a2 = child.animationState) === null || _a2 === void 0 ? void 0 : _a2.setActive(type2, isActive);
    });
    state2[type2].isActive = isActive;
    const animations2 = animateChanges(type2);
    for (const key in state2) {
      state2[key].protectedKeys = {};
    }
    return animations2;
  }
  return {
    animateChanges,
    setActive,
    setAnimateFunction,
    getState: () => state2,
    reset: () => {
      state2 = createState();
      isInitialRender = true;
    }
  };
}
function checkVariantsDidChange(prev2, next2) {
  if (typeof next2 === "string") {
    return next2 !== prev2;
  } else if (Array.isArray(next2)) {
    return !shallowCompare(next2, prev2);
  }
  return false;
}
function createTypeState(isActive = false) {
  return {
    isActive,
    protectedKeys: {},
    needsAnimating: {},
    prevResolvedValues: {}
  };
}
function createState() {
  return {
    animate: createTypeState(true),
    whileInView: createTypeState(),
    whileHover: createTypeState(),
    whileTap: createTypeState(),
    whileDrag: createTypeState(),
    whileFocus: createTypeState(),
    exit: createTypeState()
  };
}
var reversePriorityOrder, numAnimationTypes;
var init_animation_state = __esm({
  "node_modules/framer-motion/dist/es/render/utils/animation-state.mjs"() {
    init_is_animation_controls();
    init_is_keyframes_target();
    init_shallow_compare();
    init_is_variant_label();
    init_resolve_dynamic_variants();
    init_variant_props();
    init_visual_element();
    init_get_variant_context();
    reversePriorityOrder = [...variantPriorityOrder].reverse();
    numAnimationTypes = variantPriorityOrder.length;
  }
});

// node_modules/framer-motion/dist/es/motion/features/Feature.mjs
var Feature;
var init_Feature = __esm({
  "node_modules/framer-motion/dist/es/motion/features/Feature.mjs"() {
    Feature = class {
      constructor(node) {
        this.isMounted = false;
        this.node = node;
      }
      update() {
      }
    };
  }
});

// node_modules/framer-motion/dist/es/motion/features/animation/index.mjs
var AnimationFeature;
var init_animation = __esm({
  "node_modules/framer-motion/dist/es/motion/features/animation/index.mjs"() {
    init_is_animation_controls();
    init_animation_state();
    init_Feature();
    AnimationFeature = class extends Feature {
      /**
       * We dynamically generate the AnimationState manager as it contains a reference
       * to the underlying animation library. We only want to load that if we load this,
       * so people can optionally code split it out using the `m` component.
       */
      constructor(node) {
        super(node);
        node.animationState || (node.animationState = createAnimationState(node));
      }
      updateAnimationControlsSubscription() {
        const { animate } = this.node.getProps();
        if (isAnimationControls(animate)) {
          this.unmountControls = animate.subscribe(this.node);
        }
      }
      /**
       * Subscribe any provided AnimationControls to the component's VisualElement
       */
      mount() {
        this.updateAnimationControlsSubscription();
      }
      update() {
        const { animate } = this.node.getProps();
        const { animate: prevAnimate } = this.node.prevProps || {};
        if (animate !== prevAnimate) {
          this.updateAnimationControlsSubscription();
        }
      }
      unmount() {
        var _a;
        this.node.animationState.reset();
        (_a = this.unmountControls) === null || _a === void 0 ? void 0 : _a.call(this);
      }
    };
  }
});

// node_modules/framer-motion/dist/es/motion/features/animation/exit.mjs
var id, ExitAnimationFeature;
var init_exit = __esm({
  "node_modules/framer-motion/dist/es/motion/features/animation/exit.mjs"() {
    init_Feature();
    id = 0;
    ExitAnimationFeature = class extends Feature {
      constructor() {
        super(...arguments);
        this.id = id++;
      }
      update() {
        if (!this.node.presenceContext)
          return;
        const { isPresent, onExitComplete } = this.node.presenceContext;
        const { isPresent: prevIsPresent } = this.node.prevPresenceContext || {};
        if (!this.node.animationState || isPresent === prevIsPresent) {
          return;
        }
        const exitAnimation = this.node.animationState.setActive("exit", !isPresent);
        if (onExitComplete && !isPresent) {
          exitAnimation.then(() => onExitComplete(this.id));
        }
      }
      mount() {
        const { register } = this.node.presenceContext || {};
        if (register) {
          this.unmount = register(this.id);
        }
      }
      unmount() {
      }
    };
  }
});

// node_modules/framer-motion/dist/es/motion/features/animations.mjs
var animations;
var init_animations = __esm({
  "node_modules/framer-motion/dist/es/motion/features/animations.mjs"() {
    init_animation();
    init_exit();
    animations = {
      animation: {
        Feature: AnimationFeature
      },
      exit: {
        Feature: ExitAnimationFeature
      }
    };
  }
});

// node_modules/motion-dom/dist/es/utils/resolve-elements.mjs
function resolveElements(elementOrSelector, scope, selectorCache) {
  var _a;
  if (elementOrSelector instanceof Element) {
    return [elementOrSelector];
  } else if (typeof elementOrSelector === "string") {
    let root5 = document;
    if (scope) {
      root5 = scope.current;
    }
    const elements = (_a = selectorCache === null || selectorCache === void 0 ? void 0 : selectorCache[elementOrSelector]) !== null && _a !== void 0 ? _a : root5.querySelectorAll(elementOrSelector);
    return elements ? Array.from(elements) : [];
  }
  return Array.from(elementOrSelector);
}
var init_resolve_elements = __esm({
  "node_modules/motion-dom/dist/es/utils/resolve-elements.mjs"() {
  }
});

// node_modules/motion-dom/dist/es/gestures/drag/state/is-active.mjs
function isDragActive() {
  return isDragging.x || isDragging.y;
}
var isDragging;
var init_is_active = __esm({
  "node_modules/motion-dom/dist/es/gestures/drag/state/is-active.mjs"() {
    isDragging = {
      x: false,
      y: false
    };
  }
});

// node_modules/motion-dom/dist/es/gestures/hover.mjs
function filterEvents(callback) {
  return (event) => {
    if (event.pointerType === "touch" || isDragActive())
      return;
    callback(event);
  };
}
function hover(elementOrSelector, onHoverStart, options = {}) {
  const gestureAbortController = new AbortController();
  const eventOptions = {
    passive: true,
    ...options,
    signal: gestureAbortController.signal
  };
  const onPointerEnter = filterEvents((enterEvent) => {
    const { target } = enterEvent;
    const onHoverEnd = onHoverStart(enterEvent);
    if (!onHoverEnd || !target)
      return;
    const onPointerLeave = filterEvents((leaveEvent) => {
      onHoverEnd(leaveEvent);
      target.removeEventListener("pointerleave", onPointerLeave);
    });
    target.addEventListener("pointerleave", onPointerLeave, eventOptions);
  });
  resolveElements(elementOrSelector).forEach((element) => {
    element.addEventListener("pointerenter", onPointerEnter, eventOptions);
  });
  return () => gestureAbortController.abort();
}
var init_hover = __esm({
  "node_modules/motion-dom/dist/es/gestures/hover.mjs"() {
    init_resolve_elements();
    init_is_active();
  }
});

// node_modules/motion-dom/dist/es/gestures/drag/state/set-active.mjs
function setDragLock(axis) {
  if (axis === "x" || axis === "y") {
    if (isDragging[axis]) {
      return null;
    } else {
      isDragging[axis] = true;
      return () => {
        isDragging[axis] = false;
      };
    }
  } else {
    if (isDragging.x || isDragging.y) {
      return null;
    } else {
      isDragging.x = isDragging.y = true;
      return () => {
        isDragging.x = isDragging.y = false;
      };
    }
  }
}
var init_set_active = __esm({
  "node_modules/motion-dom/dist/es/gestures/drag/state/set-active.mjs"() {
    init_is_active();
  }
});

// node_modules/motion-dom/dist/es/index.mjs
var init_es2 = __esm({
  "node_modules/motion-dom/dist/es/index.mjs"() {
    init_hover();
    init_resolve_elements();
    init_is_active();
    init_set_active();
  }
});

// node_modules/framer-motion/dist/es/events/utils/is-primary-pointer.mjs
var isPrimaryPointer;
var init_is_primary_pointer = __esm({
  "node_modules/framer-motion/dist/es/events/utils/is-primary-pointer.mjs"() {
    isPrimaryPointer = (event) => {
      if (event.pointerType === "mouse") {
        return typeof event.button !== "number" || event.button <= 0;
      } else {
        return event.isPrimary !== false;
      }
    };
  }
});

// node_modules/framer-motion/dist/es/events/event-info.mjs
function extractEventInfo(event) {
  return {
    point: {
      x: event.pageX,
      y: event.pageY
    }
  };
}
var addPointerInfo;
var init_event_info = __esm({
  "node_modules/framer-motion/dist/es/events/event-info.mjs"() {
    init_is_primary_pointer();
    addPointerInfo = (handler2) => {
      return (event) => isPrimaryPointer(event) && handler2(event, extractEventInfo(event));
    };
  }
});

// node_modules/framer-motion/dist/es/events/add-dom-event.mjs
function addDomEvent(target, eventName, handler2, options = { passive: true }) {
  target.addEventListener(eventName, handler2, options);
  return () => target.removeEventListener(eventName, handler2);
}
var init_add_dom_event = __esm({
  "node_modules/framer-motion/dist/es/events/add-dom-event.mjs"() {
  }
});

// node_modules/framer-motion/dist/es/events/add-pointer-event.mjs
function addPointerEvent(target, eventName, handler2, options) {
  return addDomEvent(target, eventName, addPointerInfo(handler2), options);
}
var init_add_pointer_event = __esm({
  "node_modules/framer-motion/dist/es/events/add-pointer-event.mjs"() {
    init_add_dom_event();
    init_event_info();
  }
});

// node_modules/framer-motion/dist/es/utils/distance.mjs
function distance2D(a18, b4) {
  const xDelta = distance(a18.x, b4.x);
  const yDelta = distance(a18.y, b4.y);
  return Math.sqrt(xDelta ** 2 + yDelta ** 2);
}
var distance;
var init_distance = __esm({
  "node_modules/framer-motion/dist/es/utils/distance.mjs"() {
    distance = (a18, b4) => Math.abs(a18 - b4);
  }
});

// node_modules/framer-motion/dist/es/gestures/pan/PanSession.mjs
function transformPoint(info, transformPagePoint) {
  return transformPagePoint ? { point: transformPagePoint(info.point) } : info;
}
function subtractPoint(a18, b4) {
  return { x: a18.x - b4.x, y: a18.y - b4.y };
}
function getPanInfo({ point }, history2) {
  return {
    point,
    delta: subtractPoint(point, lastDevicePoint(history2)),
    offset: subtractPoint(point, startDevicePoint(history2)),
    velocity: getVelocity(history2, 0.1)
  };
}
function startDevicePoint(history2) {
  return history2[0];
}
function lastDevicePoint(history2) {
  return history2[history2.length - 1];
}
function getVelocity(history2, timeDelta) {
  if (history2.length < 2) {
    return { x: 0, y: 0 };
  }
  let i13 = history2.length - 1;
  let timestampedPoint = null;
  const lastPoint = lastDevicePoint(history2);
  while (i13 >= 0) {
    timestampedPoint = history2[i13];
    if (lastPoint.timestamp - timestampedPoint.timestamp > secondsToMilliseconds(timeDelta)) {
      break;
    }
    i13--;
  }
  if (!timestampedPoint) {
    return { x: 0, y: 0 };
  }
  const time3 = millisecondsToSeconds(lastPoint.timestamp - timestampedPoint.timestamp);
  if (time3 === 0) {
    return { x: 0, y: 0 };
  }
  const currentVelocity = {
    x: (lastPoint.x - timestampedPoint.x) / time3,
    y: (lastPoint.y - timestampedPoint.y) / time3
  };
  if (currentVelocity.x === Infinity) {
    currentVelocity.x = 0;
  }
  if (currentVelocity.y === Infinity) {
    currentVelocity.y = 0;
  }
  return currentVelocity;
}
var PanSession;
var init_PanSession = __esm({
  "node_modules/framer-motion/dist/es/gestures/pan/PanSession.mjs"() {
    init_event_info();
    init_time_conversion();
    init_add_pointer_event();
    init_pipe();
    init_distance();
    init_is_primary_pointer();
    init_frame();
    PanSession = class {
      constructor(event, handlers, { transformPagePoint, contextWindow, dragSnapToOrigin = false } = {}) {
        this.startEvent = null;
        this.lastMoveEvent = null;
        this.lastMoveEventInfo = null;
        this.handlers = {};
        this.contextWindow = window;
        this.updatePoint = () => {
          if (!(this.lastMoveEvent && this.lastMoveEventInfo))
            return;
          const info2 = getPanInfo(this.lastMoveEventInfo, this.history);
          const isPanStarted = this.startEvent !== null;
          const isDistancePastThreshold = distance2D(info2.offset, { x: 0, y: 0 }) >= 3;
          if (!isPanStarted && !isDistancePastThreshold)
            return;
          const { point: point2 } = info2;
          const { timestamp: timestamp2 } = frameData;
          this.history.push({ ...point2, timestamp: timestamp2 });
          const { onStart, onMove } = this.handlers;
          if (!isPanStarted) {
            onStart && onStart(this.lastMoveEvent, info2);
            this.startEvent = this.lastMoveEvent;
          }
          onMove && onMove(this.lastMoveEvent, info2);
        };
        this.handlePointerMove = (event2, info2) => {
          this.lastMoveEvent = event2;
          this.lastMoveEventInfo = transformPoint(info2, this.transformPagePoint);
          frame.update(this.updatePoint, true);
        };
        this.handlePointerUp = (event2, info2) => {
          this.end();
          const { onEnd, onSessionEnd, resumeAnimation } = this.handlers;
          if (this.dragSnapToOrigin)
            resumeAnimation && resumeAnimation();
          if (!(this.lastMoveEvent && this.lastMoveEventInfo))
            return;
          const panInfo = getPanInfo(event2.type === "pointercancel" ? this.lastMoveEventInfo : transformPoint(info2, this.transformPagePoint), this.history);
          if (this.startEvent && onEnd) {
            onEnd(event2, panInfo);
          }
          onSessionEnd && onSessionEnd(event2, panInfo);
        };
        if (!isPrimaryPointer(event))
          return;
        this.dragSnapToOrigin = dragSnapToOrigin;
        this.handlers = handlers;
        this.transformPagePoint = transformPagePoint;
        this.contextWindow = contextWindow || window;
        const info = extractEventInfo(event);
        const initialInfo = transformPoint(info, this.transformPagePoint);
        const { point } = initialInfo;
        const { timestamp } = frameData;
        this.history = [{ ...point, timestamp }];
        const { onSessionStart } = handlers;
        onSessionStart && onSessionStart(event, getPanInfo(initialInfo, this.history));
        this.removeListeners = pipe(addPointerEvent(this.contextWindow, "pointermove", this.handlePointerMove), addPointerEvent(this.contextWindow, "pointerup", this.handlePointerUp), addPointerEvent(this.contextWindow, "pointercancel", this.handlePointerUp));
      }
      updateHandlers(handlers) {
        this.handlers = handlers;
      }
      end() {
        this.removeListeners && this.removeListeners();
        cancelFrame(this.updatePoint);
      }
    };
  }
});

// node_modules/framer-motion/dist/es/utils/is-ref-object.mjs
function isRefObject(ref) {
  return ref && typeof ref === "object" && Object.prototype.hasOwnProperty.call(ref, "current");
}
var init_is_ref_object = __esm({
  "node_modules/framer-motion/dist/es/utils/is-ref-object.mjs"() {
  }
});

// node_modules/framer-motion/dist/es/projection/geometry/delta-calc.mjs
function calcLength(axis) {
  return axis.max - axis.min;
}
function isNear(value, target, maxDistance) {
  return Math.abs(value - target) <= maxDistance;
}
function calcAxisDelta(delta, source, target, origin = 0.5) {
  delta.origin = origin;
  delta.originPoint = mixNumber(source.min, source.max, delta.origin);
  delta.scale = calcLength(target) / calcLength(source);
  delta.translate = mixNumber(target.min, target.max, delta.origin) - delta.originPoint;
  if (delta.scale >= SCALE_MIN && delta.scale <= SCALE_MAX || isNaN(delta.scale)) {
    delta.scale = 1;
  }
  if (delta.translate >= TRANSLATE_MIN && delta.translate <= TRANSLATE_MAX || isNaN(delta.translate)) {
    delta.translate = 0;
  }
}
function calcBoxDelta(delta, source, target, origin) {
  calcAxisDelta(delta.x, source.x, target.x, origin ? origin.originX : void 0);
  calcAxisDelta(delta.y, source.y, target.y, origin ? origin.originY : void 0);
}
function calcRelativeAxis(target, relative, parent) {
  target.min = parent.min + relative.min;
  target.max = target.min + calcLength(relative);
}
function calcRelativeBox(target, relative, parent) {
  calcRelativeAxis(target.x, relative.x, parent.x);
  calcRelativeAxis(target.y, relative.y, parent.y);
}
function calcRelativeAxisPosition(target, layout2, parent) {
  target.min = layout2.min - parent.min;
  target.max = target.min + calcLength(layout2);
}
function calcRelativePosition(target, layout2, parent) {
  calcRelativeAxisPosition(target.x, layout2.x, parent.x);
  calcRelativeAxisPosition(target.y, layout2.y, parent.y);
}
var SCALE_PRECISION, SCALE_MIN, SCALE_MAX, TRANSLATE_PRECISION, TRANSLATE_MIN, TRANSLATE_MAX;
var init_delta_calc = __esm({
  "node_modules/framer-motion/dist/es/projection/geometry/delta-calc.mjs"() {
    init_number2();
    SCALE_PRECISION = 1e-4;
    SCALE_MIN = 1 - SCALE_PRECISION;
    SCALE_MAX = 1 + SCALE_PRECISION;
    TRANSLATE_PRECISION = 0.01;
    TRANSLATE_MIN = 0 - TRANSLATE_PRECISION;
    TRANSLATE_MAX = 0 + TRANSLATE_PRECISION;
  }
});

// node_modules/framer-motion/dist/es/gestures/drag/utils/constraints.mjs
function applyConstraints(point, { min: min2, max: max2 }, elastic) {
  if (min2 !== void 0 && point < min2) {
    point = elastic ? mixNumber(min2, point, elastic.min) : Math.max(point, min2);
  } else if (max2 !== void 0 && point > max2) {
    point = elastic ? mixNumber(max2, point, elastic.max) : Math.min(point, max2);
  }
  return point;
}
function calcRelativeAxisConstraints(axis, min2, max2) {
  return {
    min: min2 !== void 0 ? axis.min + min2 : void 0,
    max: max2 !== void 0 ? axis.max + max2 - (axis.max - axis.min) : void 0
  };
}
function calcRelativeConstraints(layoutBox, { top: top2, left, bottom, right }) {
  return {
    x: calcRelativeAxisConstraints(layoutBox.x, left, right),
    y: calcRelativeAxisConstraints(layoutBox.y, top2, bottom)
  };
}
function calcViewportAxisConstraints(layoutAxis, constraintsAxis) {
  let min2 = constraintsAxis.min - layoutAxis.min;
  let max2 = constraintsAxis.max - layoutAxis.max;
  if (constraintsAxis.max - constraintsAxis.min < layoutAxis.max - layoutAxis.min) {
    [min2, max2] = [max2, min2];
  }
  return { min: min2, max: max2 };
}
function calcViewportConstraints(layoutBox, constraintsBox) {
  return {
    x: calcViewportAxisConstraints(layoutBox.x, constraintsBox.x),
    y: calcViewportAxisConstraints(layoutBox.y, constraintsBox.y)
  };
}
function calcOrigin(source, target) {
  let origin = 0.5;
  const sourceLength = calcLength(source);
  const targetLength = calcLength(target);
  if (targetLength > sourceLength) {
    origin = progress(target.min, target.max - sourceLength, source.min);
  } else if (sourceLength > targetLength) {
    origin = progress(source.min, source.max - targetLength, target.min);
  }
  return clamp3(0, 1, origin);
}
function rebaseAxisConstraints(layout2, constraints) {
  const relativeConstraints = {};
  if (constraints.min !== void 0) {
    relativeConstraints.min = constraints.min - layout2.min;
  }
  if (constraints.max !== void 0) {
    relativeConstraints.max = constraints.max - layout2.min;
  }
  return relativeConstraints;
}
function resolveDragElastic(dragElastic = defaultElastic) {
  if (dragElastic === false) {
    dragElastic = 0;
  } else if (dragElastic === true) {
    dragElastic = defaultElastic;
  }
  return {
    x: resolveAxisElastic(dragElastic, "left", "right"),
    y: resolveAxisElastic(dragElastic, "top", "bottom")
  };
}
function resolveAxisElastic(dragElastic, minLabel, maxLabel) {
  return {
    min: resolvePointElastic(dragElastic, minLabel),
    max: resolvePointElastic(dragElastic, maxLabel)
  };
}
function resolvePointElastic(dragElastic, label4) {
  return typeof dragElastic === "number" ? dragElastic : dragElastic[label4] || 0;
}
var defaultElastic;
var init_constraints = __esm({
  "node_modules/framer-motion/dist/es/gestures/drag/utils/constraints.mjs"() {
    init_progress();
    init_delta_calc();
    init_clamp2();
    init_number2();
    defaultElastic = 0.35;
  }
});

// node_modules/framer-motion/dist/es/projection/geometry/models.mjs
var createAxisDelta, createDelta, createAxis, createBox;
var init_models = __esm({
  "node_modules/framer-motion/dist/es/projection/geometry/models.mjs"() {
    createAxisDelta = () => ({
      translate: 0,
      scale: 1,
      origin: 0,
      originPoint: 0
    });
    createDelta = () => ({
      x: createAxisDelta(),
      y: createAxisDelta()
    });
    createAxis = () => ({ min: 0, max: 0 });
    createBox = () => ({
      x: createAxis(),
      y: createAxis()
    });
  }
});

// node_modules/framer-motion/dist/es/projection/utils/each-axis.mjs
function eachAxis(callback) {
  return [callback("x"), callback("y")];
}
var init_each_axis = __esm({
  "node_modules/framer-motion/dist/es/projection/utils/each-axis.mjs"() {
  }
});

// node_modules/framer-motion/dist/es/projection/geometry/conversion.mjs
function convertBoundingBoxToBox({ top: top2, left, right, bottom }) {
  return {
    x: { min: left, max: right },
    y: { min: top2, max: bottom }
  };
}
function convertBoxToBoundingBox({ x: x3, y: y4 }) {
  return { top: y4.min, right: x3.max, bottom: y4.max, left: x3.min };
}
function transformBoxPoints(point, transformPoint2) {
  if (!transformPoint2)
    return point;
  const topLeft = transformPoint2({ x: point.left, y: point.top });
  const bottomRight = transformPoint2({ x: point.right, y: point.bottom });
  return {
    top: topLeft.y,
    left: topLeft.x,
    bottom: bottomRight.y,
    right: bottomRight.x
  };
}
var init_conversion = __esm({
  "node_modules/framer-motion/dist/es/projection/geometry/conversion.mjs"() {
  }
});

// node_modules/framer-motion/dist/es/projection/utils/has-transform.mjs
function isIdentityScale(scale2) {
  return scale2 === void 0 || scale2 === 1;
}
function hasScale({ scale: scale2, scaleX, scaleY }) {
  return !isIdentityScale(scale2) || !isIdentityScale(scaleX) || !isIdentityScale(scaleY);
}
function hasTransform(values2) {
  return hasScale(values2) || has2DTranslate(values2) || values2.z || values2.rotate || values2.rotateX || values2.rotateY || values2.skewX || values2.skewY;
}
function has2DTranslate(values2) {
  return is2DTranslate(values2.x) || is2DTranslate(values2.y);
}
function is2DTranslate(value) {
  return value && value !== "0%";
}
var init_has_transform = __esm({
  "node_modules/framer-motion/dist/es/projection/utils/has-transform.mjs"() {
  }
});

// node_modules/framer-motion/dist/es/projection/geometry/delta-apply.mjs
function scalePoint(point, scale2, originPoint) {
  const distanceFromOrigin = point - originPoint;
  const scaled = scale2 * distanceFromOrigin;
  return originPoint + scaled;
}
function applyPointDelta(point, translate, scale2, originPoint, boxScale) {
  if (boxScale !== void 0) {
    point = scalePoint(point, boxScale, originPoint);
  }
  return scalePoint(point, scale2, originPoint) + translate;
}
function applyAxisDelta(axis, translate = 0, scale2 = 1, originPoint, boxScale) {
  axis.min = applyPointDelta(axis.min, translate, scale2, originPoint, boxScale);
  axis.max = applyPointDelta(axis.max, translate, scale2, originPoint, boxScale);
}
function applyBoxDelta(box, { x: x3, y: y4 }) {
  applyAxisDelta(box.x, x3.translate, x3.scale, x3.originPoint);
  applyAxisDelta(box.y, y4.translate, y4.scale, y4.originPoint);
}
function applyTreeDeltas(box, treeScale, treePath, isSharedTransition = false) {
  const treeLength = treePath.length;
  if (!treeLength)
    return;
  treeScale.x = treeScale.y = 1;
  let node;
  let delta;
  for (let i13 = 0; i13 < treeLength; i13++) {
    node = treePath[i13];
    delta = node.projectionDelta;
    const { visualElement } = node.options;
    if (visualElement && visualElement.props.style && visualElement.props.style.display === "contents") {
      continue;
    }
    if (isSharedTransition && node.options.layoutScroll && node.scroll && node !== node.root) {
      transformBox(box, {
        x: -node.scroll.offset.x,
        y: -node.scroll.offset.y
      });
    }
    if (delta) {
      treeScale.x *= delta.x.scale;
      treeScale.y *= delta.y.scale;
      applyBoxDelta(box, delta);
    }
    if (isSharedTransition && hasTransform(node.latestValues)) {
      transformBox(box, node.latestValues);
    }
  }
  if (treeScale.x < TREE_SCALE_SNAP_MAX && treeScale.x > TREE_SCALE_SNAP_MIN) {
    treeScale.x = 1;
  }
  if (treeScale.y < TREE_SCALE_SNAP_MAX && treeScale.y > TREE_SCALE_SNAP_MIN) {
    treeScale.y = 1;
  }
}
function translateAxis(axis, distance2) {
  axis.min = axis.min + distance2;
  axis.max = axis.max + distance2;
}
function transformAxis(axis, axisTranslate, axisScale, boxScale, axisOrigin = 0.5) {
  const originPoint = mixNumber(axis.min, axis.max, axisOrigin);
  applyAxisDelta(axis, axisTranslate, axisScale, originPoint, boxScale);
}
function transformBox(box, transform) {
  transformAxis(box.x, transform.x, transform.scaleX, transform.scale, transform.originX);
  transformAxis(box.y, transform.y, transform.scaleY, transform.scale, transform.originY);
}
var TREE_SCALE_SNAP_MIN, TREE_SCALE_SNAP_MAX;
var init_delta_apply = __esm({
  "node_modules/framer-motion/dist/es/projection/geometry/delta-apply.mjs"() {
    init_number2();
    init_has_transform();
    TREE_SCALE_SNAP_MIN = 0.999999999999;
    TREE_SCALE_SNAP_MAX = 1.0000000000001;
  }
});

// node_modules/framer-motion/dist/es/projection/utils/measure.mjs
function measureViewportBox(instance, transformPoint2) {
  return convertBoundingBoxToBox(transformBoxPoints(instance.getBoundingClientRect(), transformPoint2));
}
function measurePageBox(element, rootProjectionNode2, transformPagePoint) {
  const viewportBox = measureViewportBox(element, transformPagePoint);
  const { scroll } = rootProjectionNode2;
  if (scroll) {
    translateAxis(viewportBox.x, scroll.offset.x);
    translateAxis(viewportBox.y, scroll.offset.y);
  }
  return viewportBox;
}
var init_measure = __esm({
  "node_modules/framer-motion/dist/es/projection/utils/measure.mjs"() {
    init_conversion();
    init_delta_apply();
  }
});

// node_modules/framer-motion/dist/es/utils/get-context-window.mjs
var getContextWindow;
var init_get_context_window = __esm({
  "node_modules/framer-motion/dist/es/utils/get-context-window.mjs"() {
    getContextWindow = ({ current }) => {
      return current ? current.ownerDocument.defaultView : null;
    };
  }
});

// node_modules/framer-motion/dist/es/gestures/drag/VisualElementDragControls.mjs
function shouldDrag(direction, drag2, currentDirection) {
  return (drag2 === true || drag2 === direction) && (currentDirection === null || currentDirection === direction);
}
function getCurrentDirection(offset4, lockThreshold = 10) {
  let direction = null;
  if (Math.abs(offset4.y) > lockThreshold) {
    direction = "y";
  } else if (Math.abs(offset4.x) > lockThreshold) {
    direction = "x";
  }
  return direction;
}
var elementDragControls, VisualElementDragControls;
var init_VisualElementDragControls = __esm({
  "node_modules/framer-motion/dist/es/gestures/drag/VisualElementDragControls.mjs"() {
    init_es();
    init_es2();
    init_PanSession();
    init_is_ref_object();
    init_add_pointer_event();
    init_constraints();
    init_models();
    init_each_axis();
    init_measure();
    init_event_info();
    init_conversion();
    init_add_dom_event();
    init_delta_calc();
    init_number2();
    init_units();
    init_motion_value();
    init_get_context_window();
    init_add_will_change();
    init_frame();
    elementDragControls = /* @__PURE__ */ new WeakMap();
    VisualElementDragControls = class {
      constructor(visualElement) {
        this.openDragLock = null;
        this.isDragging = false;
        this.currentDirection = null;
        this.originPoint = { x: 0, y: 0 };
        this.constraints = false;
        this.hasMutatedConstraints = false;
        this.elastic = createBox();
        this.visualElement = visualElement;
      }
      start(originEvent, { snapToCursor = false } = {}) {
        const { presenceContext } = this.visualElement;
        if (presenceContext && presenceContext.isPresent === false)
          return;
        const onSessionStart = (event) => {
          const { dragSnapToOrigin: dragSnapToOrigin2 } = this.getProps();
          dragSnapToOrigin2 ? this.pauseAnimation() : this.stopAnimation();
          if (snapToCursor) {
            this.snapToCursor(extractEventInfo(event).point);
          }
        };
        const onStart = (event, info) => {
          const { drag: drag2, dragPropagation, onDragStart } = this.getProps();
          if (drag2 && !dragPropagation) {
            if (this.openDragLock)
              this.openDragLock();
            this.openDragLock = setDragLock(drag2);
            if (!this.openDragLock)
              return;
          }
          this.isDragging = true;
          this.currentDirection = null;
          this.resolveConstraints();
          if (this.visualElement.projection) {
            this.visualElement.projection.isAnimationBlocked = true;
            this.visualElement.projection.target = void 0;
          }
          eachAxis((axis) => {
            let current = this.getAxisMotionValue(axis).get() || 0;
            if (percent.test(current)) {
              const { projection } = this.visualElement;
              if (projection && projection.layout) {
                const measuredAxis = projection.layout.layoutBox[axis];
                if (measuredAxis) {
                  const length = calcLength(measuredAxis);
                  current = length * (parseFloat(current) / 100);
                }
              }
            }
            this.originPoint[axis] = current;
          });
          if (onDragStart) {
            frame.postRender(() => onDragStart(event, info));
          }
          addValueToWillChange(this.visualElement, "transform");
          const { animationState } = this.visualElement;
          animationState && animationState.setActive("whileDrag", true);
        };
        const onMove = (event, info) => {
          const { dragPropagation, dragDirectionLock, onDirectionLock, onDrag } = this.getProps();
          if (!dragPropagation && !this.openDragLock)
            return;
          const { offset: offset4 } = info;
          if (dragDirectionLock && this.currentDirection === null) {
            this.currentDirection = getCurrentDirection(offset4);
            if (this.currentDirection !== null) {
              onDirectionLock && onDirectionLock(this.currentDirection);
            }
            return;
          }
          this.updateAxis("x", info.point, offset4);
          this.updateAxis("y", info.point, offset4);
          this.visualElement.render();
          onDrag && onDrag(event, info);
        };
        const onSessionEnd = (event, info) => this.stop(event, info);
        const resumeAnimation = () => eachAxis((axis) => {
          var _a;
          return this.getAnimationState(axis) === "paused" && ((_a = this.getAxisMotionValue(axis).animation) === null || _a === void 0 ? void 0 : _a.play());
        });
        const { dragSnapToOrigin } = this.getProps();
        this.panSession = new PanSession(originEvent, {
          onSessionStart,
          onStart,
          onMove,
          onSessionEnd,
          resumeAnimation
        }, {
          transformPagePoint: this.visualElement.getTransformPagePoint(),
          dragSnapToOrigin,
          contextWindow: getContextWindow(this.visualElement)
        });
      }
      stop(event, info) {
        const isDragging2 = this.isDragging;
        this.cancel();
        if (!isDragging2)
          return;
        const { velocity } = info;
        this.startAnimation(velocity);
        const { onDragEnd } = this.getProps();
        if (onDragEnd) {
          frame.postRender(() => onDragEnd(event, info));
        }
      }
      cancel() {
        this.isDragging = false;
        const { projection, animationState } = this.visualElement;
        if (projection) {
          projection.isAnimationBlocked = false;
        }
        this.panSession && this.panSession.end();
        this.panSession = void 0;
        const { dragPropagation } = this.getProps();
        if (!dragPropagation && this.openDragLock) {
          this.openDragLock();
          this.openDragLock = null;
        }
        animationState && animationState.setActive("whileDrag", false);
      }
      updateAxis(axis, _point, offset4) {
        const { drag: drag2 } = this.getProps();
        if (!offset4 || !shouldDrag(axis, drag2, this.currentDirection))
          return;
        const axisValue = this.getAxisMotionValue(axis);
        let next2 = this.originPoint[axis] + offset4[axis];
        if (this.constraints && this.constraints[axis]) {
          next2 = applyConstraints(next2, this.constraints[axis], this.elastic[axis]);
        }
        axisValue.set(next2);
      }
      resolveConstraints() {
        var _a;
        const { dragConstraints, dragElastic } = this.getProps();
        const layout2 = this.visualElement.projection && !this.visualElement.projection.layout ? this.visualElement.projection.measure(false) : (_a = this.visualElement.projection) === null || _a === void 0 ? void 0 : _a.layout;
        const prevConstraints = this.constraints;
        if (dragConstraints && isRefObject(dragConstraints)) {
          if (!this.constraints) {
            this.constraints = this.resolveRefConstraints();
          }
        } else {
          if (dragConstraints && layout2) {
            this.constraints = calcRelativeConstraints(layout2.layoutBox, dragConstraints);
          } else {
            this.constraints = false;
          }
        }
        this.elastic = resolveDragElastic(dragElastic);
        if (prevConstraints !== this.constraints && layout2 && this.constraints && !this.hasMutatedConstraints) {
          eachAxis((axis) => {
            if (this.constraints !== false && this.getAxisMotionValue(axis)) {
              this.constraints[axis] = rebaseAxisConstraints(layout2.layoutBox[axis], this.constraints[axis]);
            }
          });
        }
      }
      resolveRefConstraints() {
        const { dragConstraints: constraints, onMeasureDragConstraints } = this.getProps();
        if (!constraints || !isRefObject(constraints))
          return false;
        const constraintsElement = constraints.current;
        invariant(constraintsElement !== null, "If `dragConstraints` is set as a React ref, that ref must be passed to another component's `ref` prop.");
        const { projection } = this.visualElement;
        if (!projection || !projection.layout)
          return false;
        const constraintsBox = measurePageBox(constraintsElement, projection.root, this.visualElement.getTransformPagePoint());
        let measuredConstraints = calcViewportConstraints(projection.layout.layoutBox, constraintsBox);
        if (onMeasureDragConstraints) {
          const userConstraints = onMeasureDragConstraints(convertBoxToBoundingBox(measuredConstraints));
          this.hasMutatedConstraints = !!userConstraints;
          if (userConstraints) {
            measuredConstraints = convertBoundingBoxToBox(userConstraints);
          }
        }
        return measuredConstraints;
      }
      startAnimation(velocity) {
        const { drag: drag2, dragMomentum, dragElastic, dragTransition, dragSnapToOrigin, onDragTransitionEnd } = this.getProps();
        const constraints = this.constraints || {};
        const momentumAnimations = eachAxis((axis) => {
          if (!shouldDrag(axis, drag2, this.currentDirection)) {
            return;
          }
          let transition = constraints && constraints[axis] || {};
          if (dragSnapToOrigin)
            transition = { min: 0, max: 0 };
          const bounceStiffness = dragElastic ? 200 : 1e6;
          const bounceDamping = dragElastic ? 40 : 1e7;
          const inertia2 = {
            type: "inertia",
            velocity: dragMomentum ? velocity[axis] : 0,
            bounceStiffness,
            bounceDamping,
            timeConstant: 750,
            restDelta: 1,
            restSpeed: 10,
            ...dragTransition,
            ...transition
          };
          return this.startAxisValueAnimation(axis, inertia2);
        });
        return Promise.all(momentumAnimations).then(onDragTransitionEnd);
      }
      startAxisValueAnimation(axis, transition) {
        const axisValue = this.getAxisMotionValue(axis);
        addValueToWillChange(this.visualElement, axis);
        return axisValue.start(animateMotionValue(axis, axisValue, 0, transition, this.visualElement, false));
      }
      stopAnimation() {
        eachAxis((axis) => this.getAxisMotionValue(axis).stop());
      }
      pauseAnimation() {
        eachAxis((axis) => {
          var _a;
          return (_a = this.getAxisMotionValue(axis).animation) === null || _a === void 0 ? void 0 : _a.pause();
        });
      }
      getAnimationState(axis) {
        var _a;
        return (_a = this.getAxisMotionValue(axis).animation) === null || _a === void 0 ? void 0 : _a.state;
      }
      /**
       * Drag works differently depending on which props are provided.
       *
       * - If _dragX and _dragY are provided, we output the gesture delta directly to those motion values.
       * - Otherwise, we apply the delta to the x/y motion values.
       */
      getAxisMotionValue(axis) {
        const dragKey = `_drag${axis.toUpperCase()}`;
        const props = this.visualElement.getProps();
        const externalMotionValue = props[dragKey];
        return externalMotionValue ? externalMotionValue : this.visualElement.getValue(axis, (props.initial ? props.initial[axis] : void 0) || 0);
      }
      snapToCursor(point) {
        eachAxis((axis) => {
          const { drag: drag2 } = this.getProps();
          if (!shouldDrag(axis, drag2, this.currentDirection))
            return;
          const { projection } = this.visualElement;
          const axisValue = this.getAxisMotionValue(axis);
          if (projection && projection.layout) {
            const { min: min2, max: max2 } = projection.layout.layoutBox[axis];
            axisValue.set(point[axis] - mixNumber(min2, max2, 0.5));
          }
        });
      }
      /**
       * When the viewport resizes we want to check if the measured constraints
       * have changed and, if so, reposition the element within those new constraints
       * relative to where it was before the resize.
       */
      scalePositionWithinConstraints() {
        if (!this.visualElement.current)
          return;
        const { drag: drag2, dragConstraints } = this.getProps();
        const { projection } = this.visualElement;
        if (!isRefObject(dragConstraints) || !projection || !this.constraints)
          return;
        this.stopAnimation();
        const boxProgress = { x: 0, y: 0 };
        eachAxis((axis) => {
          const axisValue = this.getAxisMotionValue(axis);
          if (axisValue && this.constraints !== false) {
            const latest = axisValue.get();
            boxProgress[axis] = calcOrigin({ min: latest, max: latest }, this.constraints[axis]);
          }
        });
        const { transformTemplate } = this.visualElement.getProps();
        this.visualElement.current.style.transform = transformTemplate ? transformTemplate({}, "") : "none";
        projection.root && projection.root.updateScroll();
        projection.updateLayout();
        this.resolveConstraints();
        eachAxis((axis) => {
          if (!shouldDrag(axis, drag2, null))
            return;
          const axisValue = this.getAxisMotionValue(axis);
          const { min: min2, max: max2 } = this.constraints[axis];
          axisValue.set(mixNumber(min2, max2, boxProgress[axis]));
        });
      }
      addListeners() {
        if (!this.visualElement.current)
          return;
        elementDragControls.set(this.visualElement, this);
        const element = this.visualElement.current;
        const stopPointerListener = addPointerEvent(element, "pointerdown", (event) => {
          const { drag: drag2, dragListener = true } = this.getProps();
          drag2 && dragListener && this.start(event);
        });
        const measureDragConstraints = () => {
          const { dragConstraints } = this.getProps();
          if (isRefObject(dragConstraints) && dragConstraints.current) {
            this.constraints = this.resolveRefConstraints();
          }
        };
        const { projection } = this.visualElement;
        const stopMeasureLayoutListener = projection.addEventListener("measure", measureDragConstraints);
        if (projection && !projection.layout) {
          projection.root && projection.root.updateScroll();
          projection.updateLayout();
        }
        frame.read(measureDragConstraints);
        const stopResizeListener = addDomEvent(window, "resize", () => this.scalePositionWithinConstraints());
        const stopLayoutUpdateListener = projection.addEventListener("didUpdate", ({ delta, hasLayoutChanged }) => {
          if (this.isDragging && hasLayoutChanged) {
            eachAxis((axis) => {
              const motionValue2 = this.getAxisMotionValue(axis);
              if (!motionValue2)
                return;
              this.originPoint[axis] += delta[axis].translate;
              motionValue2.set(motionValue2.get() + delta[axis].translate);
            });
            this.visualElement.render();
          }
        });
        return () => {
          stopResizeListener();
          stopPointerListener();
          stopMeasureLayoutListener();
          stopLayoutUpdateListener && stopLayoutUpdateListener();
        };
      }
      getProps() {
        const props = this.visualElement.getProps();
        const { drag: drag2 = false, dragDirectionLock = false, dragPropagation = false, dragConstraints = false, dragElastic = defaultElastic, dragMomentum = true } = props;
        return {
          ...props,
          drag: drag2,
          dragDirectionLock,
          dragPropagation,
          dragConstraints,
          dragElastic,
          dragMomentum
        };
      }
    };
  }
});

// node_modules/framer-motion/dist/es/gestures/drag/index.mjs
var DragGesture;
var init_drag = __esm({
  "node_modules/framer-motion/dist/es/gestures/drag/index.mjs"() {
    init_Feature();
    init_es();
    init_VisualElementDragControls();
    DragGesture = class extends Feature {
      constructor(node) {
        super(node);
        this.removeGroupControls = noop2;
        this.removeListeners = noop2;
        this.controls = new VisualElementDragControls(node);
      }
      mount() {
        const { dragControls } = this.node.getProps();
        if (dragControls) {
          this.removeGroupControls = dragControls.subscribe(this.controls);
        }
        this.removeListeners = this.controls.addListeners() || noop2;
      }
      unmount() {
        this.removeGroupControls();
        this.removeListeners();
      }
    };
  }
});

// node_modules/framer-motion/dist/es/gestures/pan/index.mjs
var asyncHandler, PanGesture;
var init_pan = __esm({
  "node_modules/framer-motion/dist/es/gestures/pan/index.mjs"() {
    init_PanSession();
    init_add_pointer_event();
    init_Feature();
    init_es();
    init_get_context_window();
    init_frame();
    asyncHandler = (handler2) => (event, info) => {
      if (handler2) {
        frame.postRender(() => handler2(event, info));
      }
    };
    PanGesture = class extends Feature {
      constructor() {
        super(...arguments);
        this.removePointerDownListener = noop2;
      }
      onPointerDown(pointerDownEvent) {
        this.session = new PanSession(pointerDownEvent, this.createPanHandlers(), {
          transformPagePoint: this.node.getTransformPagePoint(),
          contextWindow: getContextWindow(this.node)
        });
      }
      createPanHandlers() {
        const { onPanSessionStart, onPanStart, onPan, onPanEnd } = this.node.getProps();
        return {
          onSessionStart: asyncHandler(onPanSessionStart),
          onStart: asyncHandler(onPanStart),
          onMove: onPan,
          onEnd: (event, info) => {
            delete this.session;
            if (onPanEnd) {
              frame.postRender(() => onPanEnd(event, info));
            }
          }
        };
      }
      mount() {
        this.removePointerDownListener = addPointerEvent(this.node.current, "pointerdown", (event) => this.onPointerDown(event));
      }
      update() {
        this.session && this.session.updateHandlers(this.createPanHandlers());
      }
      unmount() {
        this.removePointerDownListener();
        this.session && this.session.end();
      }
    };
  }
});

// node_modules/framer-motion/dist/es/context/PresenceContext.mjs
var import_react234, PresenceContext;
var init_PresenceContext = __esm({
  "node_modules/framer-motion/dist/es/context/PresenceContext.mjs"() {
    "use client";
    import_react234 = __toESM(require_react(), 1);
    PresenceContext = (0, import_react234.createContext)(null);
  }
});

// node_modules/framer-motion/dist/es/components/AnimatePresence/use-presence.mjs
function usePresence() {
  const context = (0, import_react235.useContext)(PresenceContext);
  if (context === null)
    return [true, null];
  const { isPresent, onExitComplete, register } = context;
  const id3 = (0, import_react235.useId)();
  (0, import_react235.useEffect)(() => register(id3), []);
  const safeToRemove = (0, import_react235.useCallback)(() => onExitComplete && onExitComplete(id3), [id3, onExitComplete]);
  return !isPresent && onExitComplete ? [false, safeToRemove] : [true];
}
var import_react235;
var init_use_presence = __esm({
  "node_modules/framer-motion/dist/es/components/AnimatePresence/use-presence.mjs"() {
    import_react235 = __toESM(require_react(), 1);
    init_PresenceContext();
  }
});

// node_modules/framer-motion/dist/es/context/LayoutGroupContext.mjs
var import_react236, LayoutGroupContext;
var init_LayoutGroupContext = __esm({
  "node_modules/framer-motion/dist/es/context/LayoutGroupContext.mjs"() {
    "use client";
    import_react236 = __toESM(require_react(), 1);
    LayoutGroupContext = (0, import_react236.createContext)({});
  }
});

// node_modules/framer-motion/dist/es/context/SwitchLayoutGroupContext.mjs
var import_react237, SwitchLayoutGroupContext;
var init_SwitchLayoutGroupContext = __esm({
  "node_modules/framer-motion/dist/es/context/SwitchLayoutGroupContext.mjs"() {
    "use client";
    import_react237 = __toESM(require_react(), 1);
    SwitchLayoutGroupContext = (0, import_react237.createContext)({});
  }
});

// node_modules/framer-motion/dist/es/projection/node/state.mjs
var globalProjectionState;
var init_state = __esm({
  "node_modules/framer-motion/dist/es/projection/node/state.mjs"() {
    globalProjectionState = {
      /**
       * Global flag as to whether the tree has animated since the last time
       * we resized the window
       */
      hasAnimatedSinceResize: true,
      /**
       * We set this to true once, on the first update. Any nodes added to the tree beyond that
       * update will be given a `data-projection-id` attribute.
       */
      hasEverUpdated: false
    };
  }
});

// node_modules/framer-motion/dist/es/projection/styles/scale-border-radius.mjs
function pixelsToPercent(pixels, axis) {
  if (axis.max === axis.min)
    return 0;
  return pixels / (axis.max - axis.min) * 100;
}
var correctBorderRadius;
var init_scale_border_radius = __esm({
  "node_modules/framer-motion/dist/es/projection/styles/scale-border-radius.mjs"() {
    init_units();
    correctBorderRadius = {
      correct: (latest, node) => {
        if (!node.target)
          return latest;
        if (typeof latest === "string") {
          if (px2.test(latest)) {
            latest = parseFloat(latest);
          } else {
            return latest;
          }
        }
        const x3 = pixelsToPercent(latest, node.target.x);
        const y4 = pixelsToPercent(latest, node.target.y);
        return `${x3}% ${y4}%`;
      }
    };
  }
});

// node_modules/framer-motion/dist/es/projection/styles/scale-box-shadow.mjs
var correctBoxShadow;
var init_scale_box_shadow = __esm({
  "node_modules/framer-motion/dist/es/projection/styles/scale-box-shadow.mjs"() {
    init_number2();
    init_complex();
    correctBoxShadow = {
      correct: (latest, { treeScale, projectionDelta }) => {
        const original = latest;
        const shadow = complex.parse(latest);
        if (shadow.length > 5)
          return original;
        const template = complex.createTransformer(latest);
        const offset4 = typeof shadow[0] !== "number" ? 1 : 0;
        const xScale = projectionDelta.x.scale * treeScale.x;
        const yScale = projectionDelta.y.scale * treeScale.y;
        shadow[0 + offset4] /= xScale;
        shadow[1 + offset4] /= yScale;
        const averageScale = mixNumber(xScale, yScale, 0.5);
        if (typeof shadow[2 + offset4] === "number")
          shadow[2 + offset4] /= averageScale;
        if (typeof shadow[3 + offset4] === "number")
          shadow[3 + offset4] /= averageScale;
        return template(shadow);
      }
    };
  }
});

// node_modules/framer-motion/dist/es/projection/styles/scale-correction.mjs
function addScaleCorrector(correctors) {
  Object.assign(scaleCorrectors, correctors);
}
var scaleCorrectors;
var init_scale_correction = __esm({
  "node_modules/framer-motion/dist/es/projection/styles/scale-correction.mjs"() {
    scaleCorrectors = {};
  }
});

// node_modules/framer-motion/dist/es/frameloop/microtask.mjs
var microtask, cancelMicrotask;
var init_microtask = __esm({
  "node_modules/framer-motion/dist/es/frameloop/microtask.mjs"() {
    init_batcher();
    ({ schedule: microtask, cancel: cancelMicrotask } = createRenderBatcher(queueMicrotask, false));
  }
});

// node_modules/framer-motion/dist/es/motion/features/layout/MeasureLayout.mjs
function MeasureLayout(props) {
  const [isPresent, safeToRemove] = usePresence();
  const layoutGroup = (0, import_react238.useContext)(LayoutGroupContext);
  return (0, import_jsx_runtime170.jsx)(MeasureLayoutWithContext, { ...props, layoutGroup, switchLayoutGroup: (0, import_react238.useContext)(SwitchLayoutGroupContext), isPresent, safeToRemove });
}
var import_jsx_runtime170, import_react238, MeasureLayoutWithContext, defaultScaleCorrectors;
var init_MeasureLayout = __esm({
  "node_modules/framer-motion/dist/es/motion/features/layout/MeasureLayout.mjs"() {
    "use client";
    import_jsx_runtime170 = __toESM(require_jsx_runtime(), 1);
    import_react238 = __toESM(require_react(), 1);
    init_use_presence();
    init_LayoutGroupContext();
    init_SwitchLayoutGroupContext();
    init_state();
    init_scale_border_radius();
    init_scale_box_shadow();
    init_scale_correction();
    init_microtask();
    init_frame();
    MeasureLayoutWithContext = class extends import_react238.Component {
      /**
       * This only mounts projection nodes for components that
       * need measuring, we might want to do it for all components
       * in order to incorporate transforms
       */
      componentDidMount() {
        const { visualElement, layoutGroup, switchLayoutGroup, layoutId } = this.props;
        const { projection } = visualElement;
        addScaleCorrector(defaultScaleCorrectors);
        if (projection) {
          if (layoutGroup.group)
            layoutGroup.group.add(projection);
          if (switchLayoutGroup && switchLayoutGroup.register && layoutId) {
            switchLayoutGroup.register(projection);
          }
          projection.root.didUpdate();
          projection.addEventListener("animationComplete", () => {
            this.safeToRemove();
          });
          projection.setOptions({
            ...projection.options,
            onExitComplete: () => this.safeToRemove()
          });
        }
        globalProjectionState.hasEverUpdated = true;
      }
      getSnapshotBeforeUpdate(prevProps) {
        const { layoutDependency, visualElement, drag: drag2, isPresent } = this.props;
        const projection = visualElement.projection;
        if (!projection)
          return null;
        projection.isPresent = isPresent;
        if (drag2 || prevProps.layoutDependency !== layoutDependency || layoutDependency === void 0) {
          projection.willUpdate();
        } else {
          this.safeToRemove();
        }
        if (prevProps.isPresent !== isPresent) {
          if (isPresent) {
            projection.promote();
          } else if (!projection.relegate()) {
            frame.postRender(() => {
              const stack3 = projection.getStack();
              if (!stack3 || !stack3.members.length) {
                this.safeToRemove();
              }
            });
          }
        }
        return null;
      }
      componentDidUpdate() {
        const { projection } = this.props.visualElement;
        if (projection) {
          projection.root.didUpdate();
          microtask.postRender(() => {
            if (!projection.currentAnimation && projection.isLead()) {
              this.safeToRemove();
            }
          });
        }
      }
      componentWillUnmount() {
        const { visualElement, layoutGroup, switchLayoutGroup: promoteContext } = this.props;
        const { projection } = visualElement;
        if (projection) {
          projection.scheduleCheckAfterUnmount();
          if (layoutGroup && layoutGroup.group)
            layoutGroup.group.remove(projection);
          if (promoteContext && promoteContext.deregister)
            promoteContext.deregister(projection);
        }
      }
      safeToRemove() {
        const { safeToRemove } = this.props;
        safeToRemove && safeToRemove();
      }
      render() {
        return null;
      }
    };
    defaultScaleCorrectors = {
      borderRadius: {
        ...correctBorderRadius,
        applyTo: [
          "borderTopLeftRadius",
          "borderTopRightRadius",
          "borderBottomLeftRadius",
          "borderBottomRightRadius"
        ]
      },
      borderTopLeftRadius: correctBorderRadius,
      borderTopRightRadius: correctBorderRadius,
      borderBottomLeftRadius: correctBorderRadius,
      borderBottomRightRadius: correctBorderRadius,
      boxShadow: correctBoxShadow
    };
  }
});

// node_modules/framer-motion/dist/es/projection/animation/mix-values.mjs
function mixValues(target, follow, lead, progress3, shouldCrossfadeOpacity, isOnlyMember) {
  if (shouldCrossfadeOpacity) {
    target.opacity = mixNumber(
      0,
      // TODO Reinstate this if only child
      lead.opacity !== void 0 ? lead.opacity : 1,
      easeCrossfadeIn(progress3)
    );
    target.opacityExit = mixNumber(follow.opacity !== void 0 ? follow.opacity : 1, 0, easeCrossfadeOut(progress3));
  } else if (isOnlyMember) {
    target.opacity = mixNumber(follow.opacity !== void 0 ? follow.opacity : 1, lead.opacity !== void 0 ? lead.opacity : 1, progress3);
  }
  for (let i13 = 0; i13 < numBorders; i13++) {
    const borderLabel = `border${borders[i13]}Radius`;
    let followRadius = getRadius2(follow, borderLabel);
    let leadRadius = getRadius2(lead, borderLabel);
    if (followRadius === void 0 && leadRadius === void 0)
      continue;
    followRadius || (followRadius = 0);
    leadRadius || (leadRadius = 0);
    const canMix = followRadius === 0 || leadRadius === 0 || isPx(followRadius) === isPx(leadRadius);
    if (canMix) {
      target[borderLabel] = Math.max(mixNumber(asNumber(followRadius), asNumber(leadRadius), progress3), 0);
      if (percent.test(leadRadius) || percent.test(followRadius)) {
        target[borderLabel] += "%";
      }
    } else {
      target[borderLabel] = leadRadius;
    }
  }
  if (follow.rotate || lead.rotate) {
    target.rotate = mixNumber(follow.rotate || 0, lead.rotate || 0, progress3);
  }
}
function getRadius2(values2, radiusName) {
  return values2[radiusName] !== void 0 ? values2[radiusName] : values2.borderRadius;
}
function compress(min2, max2, easing) {
  return (p11) => {
    if (p11 < min2)
      return 0;
    if (p11 > max2)
      return 1;
    return easing(progress(min2, max2, p11));
  };
}
var borders, numBorders, asNumber, isPx, easeCrossfadeIn, easeCrossfadeOut;
var init_mix_values = __esm({
  "node_modules/framer-motion/dist/es/projection/animation/mix-values.mjs"() {
    init_circ();
    init_progress();
    init_number2();
    init_es();
    init_units();
    borders = ["TopLeft", "TopRight", "BottomLeft", "BottomRight"];
    numBorders = borders.length;
    asNumber = (value) => typeof value === "string" ? parseFloat(value) : value;
    isPx = (value) => typeof value === "number" || px2.test(value);
    easeCrossfadeIn = /* @__PURE__ */ compress(0, 0.5, circOut);
    easeCrossfadeOut = /* @__PURE__ */ compress(0.5, 0.95, noop2);
  }
});

// node_modules/framer-motion/dist/es/projection/geometry/copy.mjs
function copyAxisInto(axis, originAxis) {
  axis.min = originAxis.min;
  axis.max = originAxis.max;
}
function copyBoxInto(box, originBox) {
  copyAxisInto(box.x, originBox.x);
  copyAxisInto(box.y, originBox.y);
}
function copyAxisDeltaInto(delta, originDelta) {
  delta.translate = originDelta.translate;
  delta.scale = originDelta.scale;
  delta.originPoint = originDelta.originPoint;
  delta.origin = originDelta.origin;
}
var init_copy = __esm({
  "node_modules/framer-motion/dist/es/projection/geometry/copy.mjs"() {
  }
});

// node_modules/framer-motion/dist/es/projection/geometry/delta-remove.mjs
function removePointDelta(point, translate, scale2, originPoint, boxScale) {
  point -= translate;
  point = scalePoint(point, 1 / scale2, originPoint);
  if (boxScale !== void 0) {
    point = scalePoint(point, 1 / boxScale, originPoint);
  }
  return point;
}
function removeAxisDelta(axis, translate = 0, scale2 = 1, origin = 0.5, boxScale, originAxis = axis, sourceAxis = axis) {
  if (percent.test(translate)) {
    translate = parseFloat(translate);
    const relativeProgress = mixNumber(sourceAxis.min, sourceAxis.max, translate / 100);
    translate = relativeProgress - sourceAxis.min;
  }
  if (typeof translate !== "number")
    return;
  let originPoint = mixNumber(originAxis.min, originAxis.max, origin);
  if (axis === originAxis)
    originPoint -= translate;
  axis.min = removePointDelta(axis.min, translate, scale2, originPoint, boxScale);
  axis.max = removePointDelta(axis.max, translate, scale2, originPoint, boxScale);
}
function removeAxisTransforms(axis, transforms2, [key, scaleKey, originKey], origin, sourceAxis) {
  removeAxisDelta(axis, transforms2[key], transforms2[scaleKey], transforms2[originKey], transforms2.scale, origin, sourceAxis);
}
function removeBoxTransforms(box, transforms2, originBox, sourceBox) {
  removeAxisTransforms(box.x, transforms2, xKeys, originBox ? originBox.x : void 0, sourceBox ? sourceBox.x : void 0);
  removeAxisTransforms(box.y, transforms2, yKeys, originBox ? originBox.y : void 0, sourceBox ? sourceBox.y : void 0);
}
var xKeys, yKeys;
var init_delta_remove = __esm({
  "node_modules/framer-motion/dist/es/projection/geometry/delta-remove.mjs"() {
    init_number2();
    init_units();
    init_delta_apply();
    xKeys = ["x", "scaleX", "originX"];
    yKeys = ["y", "scaleY", "originY"];
  }
});

// node_modules/framer-motion/dist/es/projection/geometry/utils.mjs
function isAxisDeltaZero(delta) {
  return delta.translate === 0 && delta.scale === 1;
}
function isDeltaZero(delta) {
  return isAxisDeltaZero(delta.x) && isAxisDeltaZero(delta.y);
}
function axisEquals(a18, b4) {
  return a18.min === b4.min && a18.max === b4.max;
}
function boxEquals(a18, b4) {
  return axisEquals(a18.x, b4.x) && axisEquals(a18.y, b4.y);
}
function axisEqualsRounded(a18, b4) {
  return Math.round(a18.min) === Math.round(b4.min) && Math.round(a18.max) === Math.round(b4.max);
}
function boxEqualsRounded(a18, b4) {
  return axisEqualsRounded(a18.x, b4.x) && axisEqualsRounded(a18.y, b4.y);
}
function aspectRatio(box) {
  return calcLength(box.x) / calcLength(box.y);
}
function axisDeltaEquals(a18, b4) {
  return a18.translate === b4.translate && a18.scale === b4.scale && a18.originPoint === b4.originPoint;
}
var init_utils5 = __esm({
  "node_modules/framer-motion/dist/es/projection/geometry/utils.mjs"() {
    init_delta_calc();
  }
});

// node_modules/framer-motion/dist/es/projection/shared/stack.mjs
var NodeStack;
var init_stack = __esm({
  "node_modules/framer-motion/dist/es/projection/shared/stack.mjs"() {
    init_array();
    NodeStack = class {
      constructor() {
        this.members = [];
      }
      add(node) {
        addUniqueItem(this.members, node);
        node.scheduleRender();
      }
      remove(node) {
        removeItem(this.members, node);
        if (node === this.prevLead) {
          this.prevLead = void 0;
        }
        if (node === this.lead) {
          const prevLead = this.members[this.members.length - 1];
          if (prevLead) {
            this.promote(prevLead);
          }
        }
      }
      relegate(node) {
        const indexOfNode = this.members.findIndex((member) => node === member);
        if (indexOfNode === 0)
          return false;
        let prevLead;
        for (let i13 = indexOfNode; i13 >= 0; i13--) {
          const member = this.members[i13];
          if (member.isPresent !== false) {
            prevLead = member;
            break;
          }
        }
        if (prevLead) {
          this.promote(prevLead);
          return true;
        } else {
          return false;
        }
      }
      promote(node, preserveFollowOpacity) {
        const prevLead = this.lead;
        if (node === prevLead)
          return;
        this.prevLead = prevLead;
        this.lead = node;
        node.show();
        if (prevLead) {
          prevLead.instance && prevLead.scheduleRender();
          node.scheduleRender();
          node.resumeFrom = prevLead;
          if (preserveFollowOpacity) {
            node.resumeFrom.preserveOpacity = true;
          }
          if (prevLead.snapshot) {
            node.snapshot = prevLead.snapshot;
            node.snapshot.latestValues = prevLead.animationValues || prevLead.latestValues;
          }
          if (node.root && node.root.isUpdating) {
            node.isLayoutDirty = true;
          }
          const { crossfade } = node.options;
          if (crossfade === false) {
            prevLead.hide();
          }
        }
      }
      exitAnimationComplete() {
        this.members.forEach((node) => {
          const { options, resumingFrom } = node;
          options.onExitComplete && options.onExitComplete();
          if (resumingFrom) {
            resumingFrom.options.onExitComplete && resumingFrom.options.onExitComplete();
          }
        });
      }
      scheduleRender() {
        this.members.forEach((node) => {
          node.instance && node.scheduleRender(false);
        });
      }
      /**
       * Clear any leads that have been removed this render to prevent them from being
       * used in future animations and to prevent memory leaks
       */
      removeLeadSnapshot() {
        if (this.lead && this.lead.snapshot) {
          this.lead.snapshot = void 0;
        }
      }
    };
  }
});

// node_modules/framer-motion/dist/es/projection/styles/transform.mjs
function buildProjectionTransform(delta, treeScale, latestTransform) {
  let transform = "";
  const xTranslate = delta.x.translate / treeScale.x;
  const yTranslate = delta.y.translate / treeScale.y;
  const zTranslate = (latestTransform === null || latestTransform === void 0 ? void 0 : latestTransform.z) || 0;
  if (xTranslate || yTranslate || zTranslate) {
    transform = `translate3d(${xTranslate}px, ${yTranslate}px, ${zTranslate}px) `;
  }
  if (treeScale.x !== 1 || treeScale.y !== 1) {
    transform += `scale(${1 / treeScale.x}, ${1 / treeScale.y}) `;
  }
  if (latestTransform) {
    const { transformPerspective, rotate, rotateX, rotateY, skewX, skewY } = latestTransform;
    if (transformPerspective)
      transform = `perspective(${transformPerspective}px) ${transform}`;
    if (rotate)
      transform += `rotate(${rotate}deg) `;
    if (rotateX)
      transform += `rotateX(${rotateX}deg) `;
    if (rotateY)
      transform += `rotateY(${rotateY}deg) `;
    if (skewX)
      transform += `skewX(${skewX}deg) `;
    if (skewY)
      transform += `skewY(${skewY}deg) `;
  }
  const elementScaleX = delta.x.scale * treeScale.x;
  const elementScaleY = delta.y.scale * treeScale.y;
  if (elementScaleX !== 1 || elementScaleY !== 1) {
    transform += `scale(${elementScaleX}, ${elementScaleY})`;
  }
  return transform || "none";
}
var init_transform3 = __esm({
  "node_modules/framer-motion/dist/es/projection/styles/transform.mjs"() {
  }
});

// node_modules/framer-motion/dist/es/render/utils/compare-by-depth.mjs
var compareByDepth;
var init_compare_by_depth = __esm({
  "node_modules/framer-motion/dist/es/render/utils/compare-by-depth.mjs"() {
    compareByDepth = (a18, b4) => a18.depth - b4.depth;
  }
});

// node_modules/framer-motion/dist/es/render/utils/flat-tree.mjs
var FlatTree;
var init_flat_tree = __esm({
  "node_modules/framer-motion/dist/es/render/utils/flat-tree.mjs"() {
    init_array();
    init_compare_by_depth();
    FlatTree = class {
      constructor() {
        this.children = [];
        this.isDirty = false;
      }
      add(child) {
        addUniqueItem(this.children, child);
        this.isDirty = true;
      }
      remove(child) {
        removeItem(this.children, child);
        this.isDirty = true;
      }
      forEach(callback) {
        this.isDirty && this.children.sort(compareByDepth);
        this.isDirty = false;
        this.children.forEach(callback);
      }
    };
  }
});

// node_modules/framer-motion/dist/es/value/utils/resolve-motion-value.mjs
function resolveMotionValue(value) {
  const unwrappedValue = isMotionValue(value) ? value.get() : value;
  return isCustomValue(unwrappedValue) ? unwrappedValue.toValue() : unwrappedValue;
}
var init_resolve_motion_value = __esm({
  "node_modules/framer-motion/dist/es/value/utils/resolve-motion-value.mjs"() {
    init_resolve_value();
    init_is_motion_value();
  }
});

// node_modules/framer-motion/dist/es/utils/delay.mjs
function delay2(callback, timeout) {
  const start = time2.now();
  const checkElapsed = ({ timestamp }) => {
    const elapsed = timestamp - start;
    if (elapsed >= timeout) {
      cancelFrame(checkElapsed);
      callback(elapsed - timeout);
    }
  };
  frame.read(checkElapsed, true);
  return () => cancelFrame(checkElapsed);
}
var init_delay2 = __esm({
  "node_modules/framer-motion/dist/es/utils/delay.mjs"() {
    init_sync_time();
    init_frame();
  }
});

// node_modules/framer-motion/dist/es/render/dom/utils/is-svg-element.mjs
function isSVGElement(element) {
  return element instanceof SVGElement && element.tagName !== "svg";
}
var init_is_svg_element = __esm({
  "node_modules/framer-motion/dist/es/render/dom/utils/is-svg-element.mjs"() {
  }
});

// node_modules/framer-motion/dist/es/animation/animate/single-value.mjs
function animateSingleValue(value, keyframes2, options) {
  const motionValue$1 = isMotionValue(value) ? value : motionValue(value);
  motionValue$1.start(animateMotionValue("", motionValue$1, keyframes2, options));
  return motionValue$1.animation;
}
var init_single_value = __esm({
  "node_modules/framer-motion/dist/es/animation/animate/single-value.mjs"() {
    init_motion_value();
    init_value();
    init_is_motion_value();
  }
});

// node_modules/framer-motion/dist/es/projection/node/create-projection-node.mjs
function resetDistortingTransform(key, visualElement, values2, sharedAnimationValues) {
  const { latestValues } = visualElement;
  if (latestValues[key]) {
    values2[key] = latestValues[key];
    visualElement.setStaticValue(key, 0);
    if (sharedAnimationValues) {
      sharedAnimationValues[key] = 0;
    }
  }
}
function cancelTreeOptimisedTransformAnimations(projectionNode) {
  projectionNode.hasCheckedOptimisedAppear = true;
  if (projectionNode.root === projectionNode)
    return;
  const { visualElement } = projectionNode.options;
  if (!visualElement)
    return;
  const appearId = getOptimisedAppearId(visualElement);
  if (window.MotionHasOptimisedAnimation(appearId, "transform")) {
    const { layout: layout2, layoutId } = projectionNode.options;
    window.MotionCancelOptimisedAnimation(appearId, "transform", frame, !(layout2 || layoutId));
  }
  const { parent } = projectionNode;
  if (parent && !parent.hasCheckedOptimisedAppear) {
    cancelTreeOptimisedTransformAnimations(parent);
  }
}
function createProjectionNode({ attachResizeListener, defaultParent, measureScroll, checkIsScrollRoot, resetTransform }) {
  return class ProjectionNode {
    constructor(latestValues = {}, parent = defaultParent === null || defaultParent === void 0 ? void 0 : defaultParent()) {
      this.id = id2++;
      this.animationId = 0;
      this.children = /* @__PURE__ */ new Set();
      this.options = {};
      this.isTreeAnimating = false;
      this.isAnimationBlocked = false;
      this.isLayoutDirty = false;
      this.isProjectionDirty = false;
      this.isSharedProjectionDirty = false;
      this.isTransformDirty = false;
      this.updateManuallyBlocked = false;
      this.updateBlockedByResize = false;
      this.isUpdating = false;
      this.isSVG = false;
      this.needsReset = false;
      this.shouldResetTransform = false;
      this.hasCheckedOptimisedAppear = false;
      this.treeScale = { x: 1, y: 1 };
      this.eventHandlers = /* @__PURE__ */ new Map();
      this.hasTreeAnimated = false;
      this.updateScheduled = false;
      this.scheduleUpdate = () => this.update();
      this.projectionUpdateScheduled = false;
      this.checkUpdateFailed = () => {
        if (this.isUpdating) {
          this.isUpdating = false;
          this.clearAllSnapshots();
        }
      };
      this.updateProjection = () => {
        this.projectionUpdateScheduled = false;
        if (isDebug) {
          metrics.totalNodes = metrics.resolvedTargetDeltas = metrics.recalculatedProjection = 0;
        }
        this.nodes.forEach(propagateDirtyNodes);
        this.nodes.forEach(resolveTargetDelta);
        this.nodes.forEach(calcProjection);
        this.nodes.forEach(cleanDirtyNodes);
        if (isDebug) {
          window.MotionDebug.record(metrics);
        }
      };
      this.resolvedRelativeTargetAt = 0;
      this.hasProjected = false;
      this.isVisible = true;
      this.animationProgress = 0;
      this.sharedNodes = /* @__PURE__ */ new Map();
      this.latestValues = latestValues;
      this.root = parent ? parent.root || parent : this;
      this.path = parent ? [...parent.path, parent] : [];
      this.parent = parent;
      this.depth = parent ? parent.depth + 1 : 0;
      for (let i13 = 0; i13 < this.path.length; i13++) {
        this.path[i13].shouldResetTransform = true;
      }
      if (this.root === this)
        this.nodes = new FlatTree();
    }
    addEventListener(name2, handler2) {
      if (!this.eventHandlers.has(name2)) {
        this.eventHandlers.set(name2, new SubscriptionManager());
      }
      return this.eventHandlers.get(name2).add(handler2);
    }
    notifyListeners(name2, ...args) {
      const subscriptionManager = this.eventHandlers.get(name2);
      subscriptionManager && subscriptionManager.notify(...args);
    }
    hasListeners(name2) {
      return this.eventHandlers.has(name2);
    }
    /**
     * Lifecycles
     */
    mount(instance, isLayoutDirty = this.root.hasTreeAnimated) {
      if (this.instance)
        return;
      this.isSVG = isSVGElement(instance);
      this.instance = instance;
      const { layoutId, layout: layout2, visualElement } = this.options;
      if (visualElement && !visualElement.current) {
        visualElement.mount(instance);
      }
      this.root.nodes.add(this);
      this.parent && this.parent.children.add(this);
      if (isLayoutDirty && (layout2 || layoutId)) {
        this.isLayoutDirty = true;
      }
      if (attachResizeListener) {
        let cancelDelay;
        const resizeUnblockUpdate = () => this.root.updateBlockedByResize = false;
        attachResizeListener(instance, () => {
          this.root.updateBlockedByResize = true;
          cancelDelay && cancelDelay();
          cancelDelay = delay2(resizeUnblockUpdate, 250);
          if (globalProjectionState.hasAnimatedSinceResize) {
            globalProjectionState.hasAnimatedSinceResize = false;
            this.nodes.forEach(finishAnimation);
          }
        });
      }
      if (layoutId) {
        this.root.registerSharedNode(layoutId, this);
      }
      if (this.options.animate !== false && visualElement && (layoutId || layout2)) {
        this.addEventListener("didUpdate", ({ delta, hasLayoutChanged, hasRelativeTargetChanged, layout: newLayout }) => {
          if (this.isTreeAnimationBlocked()) {
            this.target = void 0;
            this.relativeTarget = void 0;
            return;
          }
          const layoutTransition = this.options.transition || visualElement.getDefaultTransition() || defaultLayoutTransition;
          const { onLayoutAnimationStart, onLayoutAnimationComplete } = visualElement.getProps();
          const targetChanged = !this.targetLayout || !boxEqualsRounded(this.targetLayout, newLayout) || hasRelativeTargetChanged;
          const hasOnlyRelativeTargetChanged = !hasLayoutChanged && hasRelativeTargetChanged;
          if (this.options.layoutRoot || this.resumeFrom && this.resumeFrom.instance || hasOnlyRelativeTargetChanged || hasLayoutChanged && (targetChanged || !this.currentAnimation)) {
            if (this.resumeFrom) {
              this.resumingFrom = this.resumeFrom;
              this.resumingFrom.resumingFrom = void 0;
            }
            this.setAnimationOrigin(delta, hasOnlyRelativeTargetChanged);
            const animationOptions = {
              ...getValueTransition(layoutTransition, "layout"),
              onPlay: onLayoutAnimationStart,
              onComplete: onLayoutAnimationComplete
            };
            if (visualElement.shouldReduceMotion || this.options.layoutRoot) {
              animationOptions.delay = 0;
              animationOptions.type = false;
            }
            this.startAnimation(animationOptions);
          } else {
            if (!hasLayoutChanged) {
              finishAnimation(this);
            }
            if (this.isLead() && this.options.onExitComplete) {
              this.options.onExitComplete();
            }
          }
          this.targetLayout = newLayout;
        });
      }
    }
    unmount() {
      this.options.layoutId && this.willUpdate();
      this.root.nodes.remove(this);
      const stack3 = this.getStack();
      stack3 && stack3.remove(this);
      this.parent && this.parent.children.delete(this);
      this.instance = void 0;
      cancelFrame(this.updateProjection);
    }
    // only on the root
    blockUpdate() {
      this.updateManuallyBlocked = true;
    }
    unblockUpdate() {
      this.updateManuallyBlocked = false;
    }
    isUpdateBlocked() {
      return this.updateManuallyBlocked || this.updateBlockedByResize;
    }
    isTreeAnimationBlocked() {
      return this.isAnimationBlocked || this.parent && this.parent.isTreeAnimationBlocked() || false;
    }
    // Note: currently only running on root node
    startUpdate() {
      if (this.isUpdateBlocked())
        return;
      this.isUpdating = true;
      this.nodes && this.nodes.forEach(resetSkewAndRotation);
      this.animationId++;
    }
    getTransformTemplate() {
      const { visualElement } = this.options;
      return visualElement && visualElement.getProps().transformTemplate;
    }
    willUpdate(shouldNotifyListeners = true) {
      this.root.hasTreeAnimated = true;
      if (this.root.isUpdateBlocked()) {
        this.options.onExitComplete && this.options.onExitComplete();
        return;
      }
      if (window.MotionCancelOptimisedAnimation && !this.hasCheckedOptimisedAppear) {
        cancelTreeOptimisedTransformAnimations(this);
      }
      !this.root.isUpdating && this.root.startUpdate();
      if (this.isLayoutDirty)
        return;
      this.isLayoutDirty = true;
      for (let i13 = 0; i13 < this.path.length; i13++) {
        const node = this.path[i13];
        node.shouldResetTransform = true;
        node.updateScroll("snapshot");
        if (node.options.layoutRoot) {
          node.willUpdate(false);
        }
      }
      const { layoutId, layout: layout2 } = this.options;
      if (layoutId === void 0 && !layout2)
        return;
      const transformTemplate = this.getTransformTemplate();
      this.prevTransformTemplateValue = transformTemplate ? transformTemplate(this.latestValues, "") : void 0;
      this.updateSnapshot();
      shouldNotifyListeners && this.notifyListeners("willUpdate");
    }
    update() {
      this.updateScheduled = false;
      const updateWasBlocked = this.isUpdateBlocked();
      if (updateWasBlocked) {
        this.unblockUpdate();
        this.clearAllSnapshots();
        this.nodes.forEach(clearMeasurements);
        return;
      }
      if (!this.isUpdating) {
        this.nodes.forEach(clearIsLayoutDirty);
      }
      this.isUpdating = false;
      this.nodes.forEach(resetTransformStyle);
      this.nodes.forEach(updateLayout);
      this.nodes.forEach(notifyLayoutUpdate);
      this.clearAllSnapshots();
      const now2 = time2.now();
      frameData.delta = clamp3(0, 1e3 / 60, now2 - frameData.timestamp);
      frameData.timestamp = now2;
      frameData.isProcessing = true;
      frameSteps.update.process(frameData);
      frameSteps.preRender.process(frameData);
      frameSteps.render.process(frameData);
      frameData.isProcessing = false;
    }
    didUpdate() {
      if (!this.updateScheduled) {
        this.updateScheduled = true;
        microtask.read(this.scheduleUpdate);
      }
    }
    clearAllSnapshots() {
      this.nodes.forEach(clearSnapshot);
      this.sharedNodes.forEach(removeLeadSnapshots);
    }
    scheduleUpdateProjection() {
      if (!this.projectionUpdateScheduled) {
        this.projectionUpdateScheduled = true;
        frame.preRender(this.updateProjection, false, true);
      }
    }
    scheduleCheckAfterUnmount() {
      frame.postRender(() => {
        if (this.isLayoutDirty) {
          this.root.didUpdate();
        } else {
          this.root.checkUpdateFailed();
        }
      });
    }
    /**
     * Update measurements
     */
    updateSnapshot() {
      if (this.snapshot || !this.instance)
        return;
      this.snapshot = this.measure();
    }
    updateLayout() {
      if (!this.instance)
        return;
      this.updateScroll();
      if (!(this.options.alwaysMeasureLayout && this.isLead()) && !this.isLayoutDirty) {
        return;
      }
      if (this.resumeFrom && !this.resumeFrom.instance) {
        for (let i13 = 0; i13 < this.path.length; i13++) {
          const node = this.path[i13];
          node.updateScroll();
        }
      }
      const prevLayout = this.layout;
      this.layout = this.measure(false);
      this.layoutCorrected = createBox();
      this.isLayoutDirty = false;
      this.projectionDelta = void 0;
      this.notifyListeners("measure", this.layout.layoutBox);
      const { visualElement } = this.options;
      visualElement && visualElement.notify("LayoutMeasure", this.layout.layoutBox, prevLayout ? prevLayout.layoutBox : void 0);
    }
    updateScroll(phase = "measure") {
      let needsMeasurement = Boolean(this.options.layoutScroll && this.instance);
      if (this.scroll && this.scroll.animationId === this.root.animationId && this.scroll.phase === phase) {
        needsMeasurement = false;
      }
      if (needsMeasurement) {
        const isRoot = checkIsScrollRoot(this.instance);
        this.scroll = {
          animationId: this.root.animationId,
          phase,
          isRoot,
          offset: measureScroll(this.instance),
          wasRoot: this.scroll ? this.scroll.isRoot : isRoot
        };
      }
    }
    resetTransform() {
      if (!resetTransform)
        return;
      const isResetRequested = this.isLayoutDirty || this.shouldResetTransform || this.options.alwaysMeasureLayout;
      const hasProjection = this.projectionDelta && !isDeltaZero(this.projectionDelta);
      const transformTemplate = this.getTransformTemplate();
      const transformTemplateValue = transformTemplate ? transformTemplate(this.latestValues, "") : void 0;
      const transformTemplateHasChanged = transformTemplateValue !== this.prevTransformTemplateValue;
      if (isResetRequested && (hasProjection || hasTransform(this.latestValues) || transformTemplateHasChanged)) {
        resetTransform(this.instance, transformTemplateValue);
        this.shouldResetTransform = false;
        this.scheduleRender();
      }
    }
    measure(removeTransform = true) {
      const pageBox = this.measurePageBox();
      let layoutBox = this.removeElementScroll(pageBox);
      if (removeTransform) {
        layoutBox = this.removeTransform(layoutBox);
      }
      roundBox(layoutBox);
      return {
        animationId: this.root.animationId,
        measuredBox: pageBox,
        layoutBox,
        latestValues: {},
        source: this.id
      };
    }
    measurePageBox() {
      var _a;
      const { visualElement } = this.options;
      if (!visualElement)
        return createBox();
      const box = visualElement.measureViewportBox();
      const wasInScrollRoot = ((_a = this.scroll) === null || _a === void 0 ? void 0 : _a.wasRoot) || this.path.some(checkNodeWasScrollRoot);
      if (!wasInScrollRoot) {
        const { scroll } = this.root;
        if (scroll) {
          translateAxis(box.x, scroll.offset.x);
          translateAxis(box.y, scroll.offset.y);
        }
      }
      return box;
    }
    removeElementScroll(box) {
      var _a;
      const boxWithoutScroll = createBox();
      copyBoxInto(boxWithoutScroll, box);
      if ((_a = this.scroll) === null || _a === void 0 ? void 0 : _a.wasRoot) {
        return boxWithoutScroll;
      }
      for (let i13 = 0; i13 < this.path.length; i13++) {
        const node = this.path[i13];
        const { scroll, options } = node;
        if (node !== this.root && scroll && options.layoutScroll) {
          if (scroll.wasRoot) {
            copyBoxInto(boxWithoutScroll, box);
          }
          translateAxis(boxWithoutScroll.x, scroll.offset.x);
          translateAxis(boxWithoutScroll.y, scroll.offset.y);
        }
      }
      return boxWithoutScroll;
    }
    applyTransform(box, transformOnly = false) {
      const withTransforms = createBox();
      copyBoxInto(withTransforms, box);
      for (let i13 = 0; i13 < this.path.length; i13++) {
        const node = this.path[i13];
        if (!transformOnly && node.options.layoutScroll && node.scroll && node !== node.root) {
          transformBox(withTransforms, {
            x: -node.scroll.offset.x,
            y: -node.scroll.offset.y
          });
        }
        if (!hasTransform(node.latestValues))
          continue;
        transformBox(withTransforms, node.latestValues);
      }
      if (hasTransform(this.latestValues)) {
        transformBox(withTransforms, this.latestValues);
      }
      return withTransforms;
    }
    removeTransform(box) {
      const boxWithoutTransform = createBox();
      copyBoxInto(boxWithoutTransform, box);
      for (let i13 = 0; i13 < this.path.length; i13++) {
        const node = this.path[i13];
        if (!node.instance)
          continue;
        if (!hasTransform(node.latestValues))
          continue;
        hasScale(node.latestValues) && node.updateSnapshot();
        const sourceBox = createBox();
        const nodeBox = node.measurePageBox();
        copyBoxInto(sourceBox, nodeBox);
        removeBoxTransforms(boxWithoutTransform, node.latestValues, node.snapshot ? node.snapshot.layoutBox : void 0, sourceBox);
      }
      if (hasTransform(this.latestValues)) {
        removeBoxTransforms(boxWithoutTransform, this.latestValues);
      }
      return boxWithoutTransform;
    }
    setTargetDelta(delta) {
      this.targetDelta = delta;
      this.root.scheduleUpdateProjection();
      this.isProjectionDirty = true;
    }
    setOptions(options) {
      this.options = {
        ...this.options,
        ...options,
        crossfade: options.crossfade !== void 0 ? options.crossfade : true
      };
    }
    clearMeasurements() {
      this.scroll = void 0;
      this.layout = void 0;
      this.snapshot = void 0;
      this.prevTransformTemplateValue = void 0;
      this.targetDelta = void 0;
      this.target = void 0;
      this.isLayoutDirty = false;
    }
    forceRelativeParentToResolveTarget() {
      if (!this.relativeParent)
        return;
      if (this.relativeParent.resolvedRelativeTargetAt !== frameData.timestamp) {
        this.relativeParent.resolveTargetDelta(true);
      }
    }
    resolveTargetDelta(forceRecalculation = false) {
      var _a;
      const lead = this.getLead();
      this.isProjectionDirty || (this.isProjectionDirty = lead.isProjectionDirty);
      this.isTransformDirty || (this.isTransformDirty = lead.isTransformDirty);
      this.isSharedProjectionDirty || (this.isSharedProjectionDirty = lead.isSharedProjectionDirty);
      const isShared = Boolean(this.resumingFrom) || this !== lead;
      const canSkip = !(forceRecalculation || isShared && this.isSharedProjectionDirty || this.isProjectionDirty || ((_a = this.parent) === null || _a === void 0 ? void 0 : _a.isProjectionDirty) || this.attemptToResolveRelativeTarget || this.root.updateBlockedByResize);
      if (canSkip)
        return;
      const { layout: layout2, layoutId } = this.options;
      if (!this.layout || !(layout2 || layoutId))
        return;
      this.resolvedRelativeTargetAt = frameData.timestamp;
      if (!this.targetDelta && !this.relativeTarget) {
        const relativeParent = this.getClosestProjectingParent();
        if (relativeParent && relativeParent.layout && this.animationProgress !== 1) {
          this.relativeParent = relativeParent;
          this.forceRelativeParentToResolveTarget();
          this.relativeTarget = createBox();
          this.relativeTargetOrigin = createBox();
          calcRelativePosition(this.relativeTargetOrigin, this.layout.layoutBox, relativeParent.layout.layoutBox);
          copyBoxInto(this.relativeTarget, this.relativeTargetOrigin);
        } else {
          this.relativeParent = this.relativeTarget = void 0;
        }
      }
      if (!this.relativeTarget && !this.targetDelta)
        return;
      if (!this.target) {
        this.target = createBox();
        this.targetWithTransforms = createBox();
      }
      if (this.relativeTarget && this.relativeTargetOrigin && this.relativeParent && this.relativeParent.target) {
        this.forceRelativeParentToResolveTarget();
        calcRelativeBox(this.target, this.relativeTarget, this.relativeParent.target);
      } else if (this.targetDelta) {
        if (Boolean(this.resumingFrom)) {
          this.target = this.applyTransform(this.layout.layoutBox);
        } else {
          copyBoxInto(this.target, this.layout.layoutBox);
        }
        applyBoxDelta(this.target, this.targetDelta);
      } else {
        copyBoxInto(this.target, this.layout.layoutBox);
      }
      if (this.attemptToResolveRelativeTarget) {
        this.attemptToResolveRelativeTarget = false;
        const relativeParent = this.getClosestProjectingParent();
        if (relativeParent && Boolean(relativeParent.resumingFrom) === Boolean(this.resumingFrom) && !relativeParent.options.layoutScroll && relativeParent.target && this.animationProgress !== 1) {
          this.relativeParent = relativeParent;
          this.forceRelativeParentToResolveTarget();
          this.relativeTarget = createBox();
          this.relativeTargetOrigin = createBox();
          calcRelativePosition(this.relativeTargetOrigin, this.target, relativeParent.target);
          copyBoxInto(this.relativeTarget, this.relativeTargetOrigin);
        } else {
          this.relativeParent = this.relativeTarget = void 0;
        }
      }
      if (isDebug) {
        metrics.resolvedTargetDeltas++;
      }
    }
    getClosestProjectingParent() {
      if (!this.parent || hasScale(this.parent.latestValues) || has2DTranslate(this.parent.latestValues)) {
        return void 0;
      }
      if (this.parent.isProjecting()) {
        return this.parent;
      } else {
        return this.parent.getClosestProjectingParent();
      }
    }
    isProjecting() {
      return Boolean((this.relativeTarget || this.targetDelta || this.options.layoutRoot) && this.layout);
    }
    calcProjection() {
      var _a;
      const lead = this.getLead();
      const isShared = Boolean(this.resumingFrom) || this !== lead;
      let canSkip = true;
      if (this.isProjectionDirty || ((_a = this.parent) === null || _a === void 0 ? void 0 : _a.isProjectionDirty)) {
        canSkip = false;
      }
      if (isShared && (this.isSharedProjectionDirty || this.isTransformDirty)) {
        canSkip = false;
      }
      if (this.resolvedRelativeTargetAt === frameData.timestamp) {
        canSkip = false;
      }
      if (canSkip)
        return;
      const { layout: layout2, layoutId } = this.options;
      this.isTreeAnimating = Boolean(this.parent && this.parent.isTreeAnimating || this.currentAnimation || this.pendingAnimation);
      if (!this.isTreeAnimating) {
        this.targetDelta = this.relativeTarget = void 0;
      }
      if (!this.layout || !(layout2 || layoutId))
        return;
      copyBoxInto(this.layoutCorrected, this.layout.layoutBox);
      const prevTreeScaleX = this.treeScale.x;
      const prevTreeScaleY = this.treeScale.y;
      applyTreeDeltas(this.layoutCorrected, this.treeScale, this.path, isShared);
      if (lead.layout && !lead.target && (this.treeScale.x !== 1 || this.treeScale.y !== 1)) {
        lead.target = lead.layout.layoutBox;
        lead.targetWithTransforms = createBox();
      }
      const { target } = lead;
      if (!target) {
        if (this.prevProjectionDelta) {
          this.createProjectionDeltas();
          this.scheduleRender();
        }
        return;
      }
      if (!this.projectionDelta || !this.prevProjectionDelta) {
        this.createProjectionDeltas();
      } else {
        copyAxisDeltaInto(this.prevProjectionDelta.x, this.projectionDelta.x);
        copyAxisDeltaInto(this.prevProjectionDelta.y, this.projectionDelta.y);
      }
      calcBoxDelta(this.projectionDelta, this.layoutCorrected, target, this.latestValues);
      if (this.treeScale.x !== prevTreeScaleX || this.treeScale.y !== prevTreeScaleY || !axisDeltaEquals(this.projectionDelta.x, this.prevProjectionDelta.x) || !axisDeltaEquals(this.projectionDelta.y, this.prevProjectionDelta.y)) {
        this.hasProjected = true;
        this.scheduleRender();
        this.notifyListeners("projectionUpdate", target);
      }
      if (isDebug) {
        metrics.recalculatedProjection++;
      }
    }
    hide() {
      this.isVisible = false;
    }
    show() {
      this.isVisible = true;
    }
    scheduleRender(notifyAll = true) {
      var _a;
      (_a = this.options.visualElement) === null || _a === void 0 ? void 0 : _a.scheduleRender();
      if (notifyAll) {
        const stack3 = this.getStack();
        stack3 && stack3.scheduleRender();
      }
      if (this.resumingFrom && !this.resumingFrom.instance) {
        this.resumingFrom = void 0;
      }
    }
    createProjectionDeltas() {
      this.prevProjectionDelta = createDelta();
      this.projectionDelta = createDelta();
      this.projectionDeltaWithTransform = createDelta();
    }
    setAnimationOrigin(delta, hasOnlyRelativeTargetChanged = false) {
      const snapshot2 = this.snapshot;
      const snapshotLatestValues = snapshot2 ? snapshot2.latestValues : {};
      const mixedValues = { ...this.latestValues };
      const targetDelta = createDelta();
      if (!this.relativeParent || !this.relativeParent.options.layoutRoot) {
        this.relativeTarget = this.relativeTargetOrigin = void 0;
      }
      this.attemptToResolveRelativeTarget = !hasOnlyRelativeTargetChanged;
      const relativeLayout = createBox();
      const snapshotSource = snapshot2 ? snapshot2.source : void 0;
      const layoutSource = this.layout ? this.layout.source : void 0;
      const isSharedLayoutAnimation = snapshotSource !== layoutSource;
      const stack3 = this.getStack();
      const isOnlyMember = !stack3 || stack3.members.length <= 1;
      const shouldCrossfadeOpacity = Boolean(isSharedLayoutAnimation && !isOnlyMember && this.options.crossfade === true && !this.path.some(hasOpacityCrossfade));
      this.animationProgress = 0;
      let prevRelativeTarget;
      this.mixTargetDelta = (latest) => {
        const progress3 = latest / 1e3;
        mixAxisDelta(targetDelta.x, delta.x, progress3);
        mixAxisDelta(targetDelta.y, delta.y, progress3);
        this.setTargetDelta(targetDelta);
        if (this.relativeTarget && this.relativeTargetOrigin && this.layout && this.relativeParent && this.relativeParent.layout) {
          calcRelativePosition(relativeLayout, this.layout.layoutBox, this.relativeParent.layout.layoutBox);
          mixBox(this.relativeTarget, this.relativeTargetOrigin, relativeLayout, progress3);
          if (prevRelativeTarget && boxEquals(this.relativeTarget, prevRelativeTarget)) {
            this.isProjectionDirty = false;
          }
          if (!prevRelativeTarget)
            prevRelativeTarget = createBox();
          copyBoxInto(prevRelativeTarget, this.relativeTarget);
        }
        if (isSharedLayoutAnimation) {
          this.animationValues = mixedValues;
          mixValues(mixedValues, snapshotLatestValues, this.latestValues, progress3, shouldCrossfadeOpacity, isOnlyMember);
        }
        this.root.scheduleUpdateProjection();
        this.scheduleRender();
        this.animationProgress = progress3;
      };
      this.mixTargetDelta(this.options.layoutRoot ? 1e3 : 0);
    }
    startAnimation(options) {
      this.notifyListeners("animationStart");
      this.currentAnimation && this.currentAnimation.stop();
      if (this.resumingFrom && this.resumingFrom.currentAnimation) {
        this.resumingFrom.currentAnimation.stop();
      }
      if (this.pendingAnimation) {
        cancelFrame(this.pendingAnimation);
        this.pendingAnimation = void 0;
      }
      this.pendingAnimation = frame.update(() => {
        globalProjectionState.hasAnimatedSinceResize = true;
        this.currentAnimation = animateSingleValue(0, animationTarget, {
          ...options,
          onUpdate: (latest) => {
            this.mixTargetDelta(latest);
            options.onUpdate && options.onUpdate(latest);
          },
          onComplete: () => {
            options.onComplete && options.onComplete();
            this.completeAnimation();
          }
        });
        if (this.resumingFrom) {
          this.resumingFrom.currentAnimation = this.currentAnimation;
        }
        this.pendingAnimation = void 0;
      });
    }
    completeAnimation() {
      if (this.resumingFrom) {
        this.resumingFrom.currentAnimation = void 0;
        this.resumingFrom.preserveOpacity = void 0;
      }
      const stack3 = this.getStack();
      stack3 && stack3.exitAnimationComplete();
      this.resumingFrom = this.currentAnimation = this.animationValues = void 0;
      this.notifyListeners("animationComplete");
    }
    finishAnimation() {
      if (this.currentAnimation) {
        this.mixTargetDelta && this.mixTargetDelta(animationTarget);
        this.currentAnimation.stop();
      }
      this.completeAnimation();
    }
    applyTransformsToTarget() {
      const lead = this.getLead();
      let { targetWithTransforms, target, layout: layout2, latestValues } = lead;
      if (!targetWithTransforms || !target || !layout2)
        return;
      if (this !== lead && this.layout && layout2 && shouldAnimatePositionOnly(this.options.animationType, this.layout.layoutBox, layout2.layoutBox)) {
        target = this.target || createBox();
        const xLength = calcLength(this.layout.layoutBox.x);
        target.x.min = lead.target.x.min;
        target.x.max = target.x.min + xLength;
        const yLength = calcLength(this.layout.layoutBox.y);
        target.y.min = lead.target.y.min;
        target.y.max = target.y.min + yLength;
      }
      copyBoxInto(targetWithTransforms, target);
      transformBox(targetWithTransforms, latestValues);
      calcBoxDelta(this.projectionDeltaWithTransform, this.layoutCorrected, targetWithTransforms, latestValues);
    }
    registerSharedNode(layoutId, node) {
      if (!this.sharedNodes.has(layoutId)) {
        this.sharedNodes.set(layoutId, new NodeStack());
      }
      const stack3 = this.sharedNodes.get(layoutId);
      stack3.add(node);
      const config = node.options.initialPromotionConfig;
      node.promote({
        transition: config ? config.transition : void 0,
        preserveFollowOpacity: config && config.shouldPreserveFollowOpacity ? config.shouldPreserveFollowOpacity(node) : void 0
      });
    }
    isLead() {
      const stack3 = this.getStack();
      return stack3 ? stack3.lead === this : true;
    }
    getLead() {
      var _a;
      const { layoutId } = this.options;
      return layoutId ? ((_a = this.getStack()) === null || _a === void 0 ? void 0 : _a.lead) || this : this;
    }
    getPrevLead() {
      var _a;
      const { layoutId } = this.options;
      return layoutId ? (_a = this.getStack()) === null || _a === void 0 ? void 0 : _a.prevLead : void 0;
    }
    getStack() {
      const { layoutId } = this.options;
      if (layoutId)
        return this.root.sharedNodes.get(layoutId);
    }
    promote({ needsReset, transition, preserveFollowOpacity } = {}) {
      const stack3 = this.getStack();
      if (stack3)
        stack3.promote(this, preserveFollowOpacity);
      if (needsReset) {
        this.projectionDelta = void 0;
        this.needsReset = true;
      }
      if (transition)
        this.setOptions({ transition });
    }
    relegate() {
      const stack3 = this.getStack();
      if (stack3) {
        return stack3.relegate(this);
      } else {
        return false;
      }
    }
    resetSkewAndRotation() {
      const { visualElement } = this.options;
      if (!visualElement)
        return;
      let hasDistortingTransform = false;
      const { latestValues } = visualElement;
      if (latestValues.z || latestValues.rotate || latestValues.rotateX || latestValues.rotateY || latestValues.rotateZ || latestValues.skewX || latestValues.skewY) {
        hasDistortingTransform = true;
      }
      if (!hasDistortingTransform)
        return;
      const resetValues = {};
      if (latestValues.z) {
        resetDistortingTransform("z", visualElement, resetValues, this.animationValues);
      }
      for (let i13 = 0; i13 < transformAxes.length; i13++) {
        resetDistortingTransform(`rotate${transformAxes[i13]}`, visualElement, resetValues, this.animationValues);
        resetDistortingTransform(`skew${transformAxes[i13]}`, visualElement, resetValues, this.animationValues);
      }
      visualElement.render();
      for (const key in resetValues) {
        visualElement.setStaticValue(key, resetValues[key]);
        if (this.animationValues) {
          this.animationValues[key] = resetValues[key];
        }
      }
      visualElement.scheduleRender();
    }
    getProjectionStyles(styleProp) {
      var _a, _b;
      if (!this.instance || this.isSVG)
        return void 0;
      if (!this.isVisible) {
        return hiddenVisibility;
      }
      const styles = {
        visibility: ""
      };
      const transformTemplate = this.getTransformTemplate();
      if (this.needsReset) {
        this.needsReset = false;
        styles.opacity = "";
        styles.pointerEvents = resolveMotionValue(styleProp === null || styleProp === void 0 ? void 0 : styleProp.pointerEvents) || "";
        styles.transform = transformTemplate ? transformTemplate(this.latestValues, "") : "none";
        return styles;
      }
      const lead = this.getLead();
      if (!this.projectionDelta || !this.layout || !lead.target) {
        const emptyStyles = {};
        if (this.options.layoutId) {
          emptyStyles.opacity = this.latestValues.opacity !== void 0 ? this.latestValues.opacity : 1;
          emptyStyles.pointerEvents = resolveMotionValue(styleProp === null || styleProp === void 0 ? void 0 : styleProp.pointerEvents) || "";
        }
        if (this.hasProjected && !hasTransform(this.latestValues)) {
          emptyStyles.transform = transformTemplate ? transformTemplate({}, "") : "none";
          this.hasProjected = false;
        }
        return emptyStyles;
      }
      const valuesToRender = lead.animationValues || lead.latestValues;
      this.applyTransformsToTarget();
      styles.transform = buildProjectionTransform(this.projectionDeltaWithTransform, this.treeScale, valuesToRender);
      if (transformTemplate) {
        styles.transform = transformTemplate(valuesToRender, styles.transform);
      }
      const { x: x3, y: y4 } = this.projectionDelta;
      styles.transformOrigin = `${x3.origin * 100}% ${y4.origin * 100}% 0`;
      if (lead.animationValues) {
        styles.opacity = lead === this ? (_b = (_a = valuesToRender.opacity) !== null && _a !== void 0 ? _a : this.latestValues.opacity) !== null && _b !== void 0 ? _b : 1 : this.preserveOpacity ? this.latestValues.opacity : valuesToRender.opacityExit;
      } else {
        styles.opacity = lead === this ? valuesToRender.opacity !== void 0 ? valuesToRender.opacity : "" : valuesToRender.opacityExit !== void 0 ? valuesToRender.opacityExit : 0;
      }
      for (const key in scaleCorrectors) {
        if (valuesToRender[key] === void 0)
          continue;
        const { correct, applyTo } = scaleCorrectors[key];
        const corrected = styles.transform === "none" ? valuesToRender[key] : correct(valuesToRender[key], lead);
        if (applyTo) {
          const num = applyTo.length;
          for (let i13 = 0; i13 < num; i13++) {
            styles[applyTo[i13]] = corrected;
          }
        } else {
          styles[key] = corrected;
        }
      }
      if (this.options.layoutId) {
        styles.pointerEvents = lead === this ? resolveMotionValue(styleProp === null || styleProp === void 0 ? void 0 : styleProp.pointerEvents) || "" : "none";
      }
      return styles;
    }
    clearSnapshot() {
      this.resumeFrom = this.snapshot = void 0;
    }
    // Only run on root
    resetTree() {
      this.root.nodes.forEach((node) => {
        var _a;
        return (_a = node.currentAnimation) === null || _a === void 0 ? void 0 : _a.stop();
      });
      this.root.nodes.forEach(clearMeasurements);
      this.root.sharedNodes.clear();
    }
  };
}
function updateLayout(node) {
  node.updateLayout();
}
function notifyLayoutUpdate(node) {
  var _a;
  const snapshot2 = ((_a = node.resumeFrom) === null || _a === void 0 ? void 0 : _a.snapshot) || node.snapshot;
  if (node.isLead() && node.layout && snapshot2 && node.hasListeners("didUpdate")) {
    const { layoutBox: layout2, measuredBox: measuredLayout } = node.layout;
    const { animationType } = node.options;
    const isShared = snapshot2.source !== node.layout.source;
    if (animationType === "size") {
      eachAxis((axis) => {
        const axisSnapshot = isShared ? snapshot2.measuredBox[axis] : snapshot2.layoutBox[axis];
        const length = calcLength(axisSnapshot);
        axisSnapshot.min = layout2[axis].min;
        axisSnapshot.max = axisSnapshot.min + length;
      });
    } else if (shouldAnimatePositionOnly(animationType, snapshot2.layoutBox, layout2)) {
      eachAxis((axis) => {
        const axisSnapshot = isShared ? snapshot2.measuredBox[axis] : snapshot2.layoutBox[axis];
        const length = calcLength(layout2[axis]);
        axisSnapshot.max = axisSnapshot.min + length;
        if (node.relativeTarget && !node.currentAnimation) {
          node.isProjectionDirty = true;
          node.relativeTarget[axis].max = node.relativeTarget[axis].min + length;
        }
      });
    }
    const layoutDelta = createDelta();
    calcBoxDelta(layoutDelta, layout2, snapshot2.layoutBox);
    const visualDelta = createDelta();
    if (isShared) {
      calcBoxDelta(visualDelta, node.applyTransform(measuredLayout, true), snapshot2.measuredBox);
    } else {
      calcBoxDelta(visualDelta, layout2, snapshot2.layoutBox);
    }
    const hasLayoutChanged = !isDeltaZero(layoutDelta);
    let hasRelativeTargetChanged = false;
    if (!node.resumeFrom) {
      const relativeParent = node.getClosestProjectingParent();
      if (relativeParent && !relativeParent.resumeFrom) {
        const { snapshot: parentSnapshot, layout: parentLayout } = relativeParent;
        if (parentSnapshot && parentLayout) {
          const relativeSnapshot = createBox();
          calcRelativePosition(relativeSnapshot, snapshot2.layoutBox, parentSnapshot.layoutBox);
          const relativeLayout = createBox();
          calcRelativePosition(relativeLayout, layout2, parentLayout.layoutBox);
          if (!boxEqualsRounded(relativeSnapshot, relativeLayout)) {
            hasRelativeTargetChanged = true;
          }
          if (relativeParent.options.layoutRoot) {
            node.relativeTarget = relativeLayout;
            node.relativeTargetOrigin = relativeSnapshot;
            node.relativeParent = relativeParent;
          }
        }
      }
    }
    node.notifyListeners("didUpdate", {
      layout: layout2,
      snapshot: snapshot2,
      delta: visualDelta,
      layoutDelta,
      hasLayoutChanged,
      hasRelativeTargetChanged
    });
  } else if (node.isLead()) {
    const { onExitComplete } = node.options;
    onExitComplete && onExitComplete();
  }
  node.options.transition = void 0;
}
function propagateDirtyNodes(node) {
  if (isDebug) {
    metrics.totalNodes++;
  }
  if (!node.parent)
    return;
  if (!node.isProjecting()) {
    node.isProjectionDirty = node.parent.isProjectionDirty;
  }
  node.isSharedProjectionDirty || (node.isSharedProjectionDirty = Boolean(node.isProjectionDirty || node.parent.isProjectionDirty || node.parent.isSharedProjectionDirty));
  node.isTransformDirty || (node.isTransformDirty = node.parent.isTransformDirty);
}
function cleanDirtyNodes(node) {
  node.isProjectionDirty = node.isSharedProjectionDirty = node.isTransformDirty = false;
}
function clearSnapshot(node) {
  node.clearSnapshot();
}
function clearMeasurements(node) {
  node.clearMeasurements();
}
function clearIsLayoutDirty(node) {
  node.isLayoutDirty = false;
}
function resetTransformStyle(node) {
  const { visualElement } = node.options;
  if (visualElement && visualElement.getProps().onBeforeLayoutMeasure) {
    visualElement.notify("BeforeLayoutMeasure");
  }
  node.resetTransform();
}
function finishAnimation(node) {
  node.finishAnimation();
  node.targetDelta = node.relativeTarget = node.target = void 0;
  node.isProjectionDirty = true;
}
function resolveTargetDelta(node) {
  node.resolveTargetDelta();
}
function calcProjection(node) {
  node.calcProjection();
}
function resetSkewAndRotation(node) {
  node.resetSkewAndRotation();
}
function removeLeadSnapshots(stack3) {
  stack3.removeLeadSnapshot();
}
function mixAxisDelta(output, delta, p11) {
  output.translate = mixNumber(delta.translate, 0, p11);
  output.scale = mixNumber(delta.scale, 1, p11);
  output.origin = delta.origin;
  output.originPoint = delta.originPoint;
}
function mixAxis(output, from, to, p11) {
  output.min = mixNumber(from.min, to.min, p11);
  output.max = mixNumber(from.max, to.max, p11);
}
function mixBox(output, from, to, p11) {
  mixAxis(output.x, from.x, to.x, p11);
  mixAxis(output.y, from.y, to.y, p11);
}
function hasOpacityCrossfade(node) {
  return node.animationValues && node.animationValues.opacityExit !== void 0;
}
function roundAxis(axis) {
  axis.min = roundPoint(axis.min);
  axis.max = roundPoint(axis.max);
}
function roundBox(box) {
  roundAxis(box.x);
  roundAxis(box.y);
}
function shouldAnimatePositionOnly(animationType, snapshot2, layout2) {
  return animationType === "position" || animationType === "preserve-aspect" && !isNear(aspectRatio(snapshot2), aspectRatio(layout2), 0.2);
}
function checkNodeWasScrollRoot(node) {
  var _a;
  return node !== node.root && ((_a = node.scroll) === null || _a === void 0 ? void 0 : _a.wasRoot);
}
var metrics, isDebug, transformAxes, hiddenVisibility, animationTarget, id2, defaultLayoutTransition, userAgentContains, roundPoint;
var init_create_projection_node = __esm({
  "node_modules/framer-motion/dist/es/projection/node/create-projection-node.mjs"() {
    init_subscription_manager();
    init_mix_values();
    init_copy();
    init_delta_apply();
    init_delta_calc();
    init_delta_remove();
    init_get_value_transition();
    init_utils5();
    init_stack();
    init_scale_correction();
    init_transform3();
    init_each_axis();
    init_has_transform();
    init_flat_tree();
    init_resolve_motion_value();
    init_state();
    init_delay2();
    init_number2();
    init_is_svg_element();
    init_single_value();
    init_clamp2();
    init_frame();
    init_es();
    init_sync_time();
    init_microtask();
    init_get_appear_id();
    init_models();
    metrics = {
      type: "projectionFrame",
      totalNodes: 0,
      resolvedTargetDeltas: 0,
      recalculatedProjection: 0
    };
    isDebug = typeof window !== "undefined" && window.MotionDebug !== void 0;
    transformAxes = ["", "X", "Y", "Z"];
    hiddenVisibility = { visibility: "hidden" };
    animationTarget = 1e3;
    id2 = 0;
    defaultLayoutTransition = {
      duration: 0.45,
      ease: [0.4, 0, 0.1, 1]
    };
    userAgentContains = (string) => typeof navigator !== "undefined" && navigator.userAgent && navigator.userAgent.toLowerCase().includes(string);
    roundPoint = userAgentContains("applewebkit/") && !userAgentContains("chrome/") ? Math.round : noop2;
  }
});

// node_modules/framer-motion/dist/es/projection/node/DocumentProjectionNode.mjs
var DocumentProjectionNode;
var init_DocumentProjectionNode = __esm({
  "node_modules/framer-motion/dist/es/projection/node/DocumentProjectionNode.mjs"() {
    init_create_projection_node();
    init_add_dom_event();
    DocumentProjectionNode = createProjectionNode({
      attachResizeListener: (ref, notify) => addDomEvent(ref, "resize", notify),
      measureScroll: () => ({
        x: document.documentElement.scrollLeft || document.body.scrollLeft,
        y: document.documentElement.scrollTop || document.body.scrollTop
      }),
      checkIsScrollRoot: () => true
    });
  }
});

// node_modules/framer-motion/dist/es/projection/node/HTMLProjectionNode.mjs
var rootProjectionNode, HTMLProjectionNode;
var init_HTMLProjectionNode = __esm({
  "node_modules/framer-motion/dist/es/projection/node/HTMLProjectionNode.mjs"() {
    init_create_projection_node();
    init_DocumentProjectionNode();
    rootProjectionNode = {
      current: void 0
    };
    HTMLProjectionNode = createProjectionNode({
      measureScroll: (instance) => ({
        x: instance.scrollLeft,
        y: instance.scrollTop
      }),
      defaultParent: () => {
        if (!rootProjectionNode.current) {
          const documentNode = new DocumentProjectionNode({});
          documentNode.mount(window);
          documentNode.setOptions({ layoutScroll: true });
          rootProjectionNode.current = documentNode;
        }
        return rootProjectionNode.current;
      },
      resetTransform: (instance, value) => {
        instance.style.transform = value !== void 0 ? value : "none";
      },
      checkIsScrollRoot: (instance) => Boolean(window.getComputedStyle(instance).position === "fixed")
    });
  }
});

// node_modules/framer-motion/dist/es/motion/features/drag.mjs
var drag;
var init_drag2 = __esm({
  "node_modules/framer-motion/dist/es/motion/features/drag.mjs"() {
    init_drag();
    init_pan();
    init_MeasureLayout();
    init_HTMLProjectionNode();
    drag = {
      pan: {
        Feature: PanGesture
      },
      drag: {
        Feature: DragGesture,
        ProjectionNode: HTMLProjectionNode,
        MeasureLayout
      }
    };
  }
});

// node_modules/framer-motion/dist/es/gestures/hover.mjs
function handleHoverEvent(node, event, isActive) {
  const { props } = node;
  if (node.animationState && props.whileHover) {
    node.animationState.setActive("whileHover", isActive);
  }
  const callback = props[isActive ? "onHoverStart" : "onHoverEnd"];
  if (callback) {
    frame.postRender(() => callback(event, extractEventInfo(event)));
  }
}
var HoverGesture;
var init_hover2 = __esm({
  "node_modules/framer-motion/dist/es/gestures/hover.mjs"() {
    init_Feature();
    init_es2();
    init_event_info();
    init_frame();
    HoverGesture = class extends Feature {
      mount() {
        const { current, props } = this.node;
        if (!current)
          return;
        this.unmount = hover(current, (startEvent) => {
          handleHoverEvent(this.node, startEvent, true);
          return (endEvent) => handleHoverEvent(this.node, endEvent, false);
        }, { passive: !props.onHoverStart && !props.onHoverEnd });
      }
      unmount() {
      }
    };
  }
});

// node_modules/framer-motion/dist/es/gestures/focus.mjs
var FocusGesture;
var init_focus = __esm({
  "node_modules/framer-motion/dist/es/gestures/focus.mjs"() {
    init_add_dom_event();
    init_Feature();
    init_pipe();
    FocusGesture = class extends Feature {
      constructor() {
        super(...arguments);
        this.isActive = false;
      }
      onFocus() {
        let isFocusVisible = false;
        try {
          isFocusVisible = this.node.current.matches(":focus-visible");
        } catch (e14) {
          isFocusVisible = true;
        }
        if (!isFocusVisible || !this.node.animationState)
          return;
        this.node.animationState.setActive("whileFocus", true);
        this.isActive = true;
      }
      onBlur() {
        if (!this.isActive || !this.node.animationState)
          return;
        this.node.animationState.setActive("whileFocus", false);
        this.isActive = false;
      }
      mount() {
        this.unmount = pipe(addDomEvent(this.node.current, "focus", () => this.onFocus()), addDomEvent(this.node.current, "blur", () => this.onBlur()));
      }
      unmount() {
      }
    };
  }
});

// node_modules/framer-motion/dist/es/gestures/utils/is-node-or-child.mjs
var isNodeOrChild;
var init_is_node_or_child = __esm({
  "node_modules/framer-motion/dist/es/gestures/utils/is-node-or-child.mjs"() {
    isNodeOrChild = (parent, child) => {
      if (!child) {
        return false;
      } else if (parent === child) {
        return true;
      } else {
        return isNodeOrChild(parent, child.parentElement);
      }
    };
  }
});

// node_modules/framer-motion/dist/es/gestures/press.mjs
function fireSyntheticPointerEvent(name2, handler2) {
  if (!handler2)
    return;
  const syntheticPointerEvent = new PointerEvent("pointer" + name2);
  handler2(syntheticPointerEvent, extractEventInfo(syntheticPointerEvent));
}
var PressGesture;
var init_press = __esm({
  "node_modules/framer-motion/dist/es/gestures/press.mjs"() {
    init_event_info();
    init_add_dom_event();
    init_add_pointer_event();
    init_Feature();
    init_pipe();
    init_es2();
    init_is_node_or_child();
    init_es();
    init_frame();
    PressGesture = class extends Feature {
      constructor() {
        super(...arguments);
        this.removeStartListeners = noop2;
        this.removeEndListeners = noop2;
        this.removeAccessibleListeners = noop2;
        this.startPointerPress = (startEvent, startInfo) => {
          if (this.isPressing)
            return;
          this.removeEndListeners();
          const props = this.node.getProps();
          const endPointerPress = (endEvent, endInfo) => {
            if (!this.checkPressEnd())
              return;
            const { onTap, onTapCancel, globalTapTarget } = this.node.getProps();
            const handler2 = !globalTapTarget && !isNodeOrChild(this.node.current, endEvent.target) ? onTapCancel : onTap;
            if (handler2) {
              frame.update(() => handler2(endEvent, endInfo));
            }
          };
          const removePointerUpListener = addPointerEvent(window, "pointerup", endPointerPress, {
            passive: !(props.onTap || props["onPointerUp"])
          });
          const removePointerCancelListener = addPointerEvent(window, "pointercancel", (cancelEvent, cancelInfo) => this.cancelPress(cancelEvent, cancelInfo), {
            passive: !(props.onTapCancel || props["onPointerCancel"])
          });
          this.removeEndListeners = pipe(removePointerUpListener, removePointerCancelListener);
          this.startPress(startEvent, startInfo);
        };
        this.startAccessiblePress = () => {
          const handleKeydown = (keydownEvent) => {
            if (keydownEvent.key !== "Enter" || this.isPressing)
              return;
            const handleKeyup = (keyupEvent) => {
              if (keyupEvent.key !== "Enter" || !this.checkPressEnd())
                return;
              fireSyntheticPointerEvent("up", (event, info) => {
                const { onTap } = this.node.getProps();
                if (onTap) {
                  frame.postRender(() => onTap(event, info));
                }
              });
            };
            this.removeEndListeners();
            this.removeEndListeners = addDomEvent(this.node.current, "keyup", handleKeyup);
            fireSyntheticPointerEvent("down", (event, info) => {
              this.startPress(event, info);
            });
          };
          const removeKeydownListener = addDomEvent(this.node.current, "keydown", handleKeydown);
          const handleBlur = () => {
            if (!this.isPressing)
              return;
            fireSyntheticPointerEvent("cancel", (cancelEvent, cancelInfo) => this.cancelPress(cancelEvent, cancelInfo));
          };
          const removeBlurListener = addDomEvent(this.node.current, "blur", handleBlur);
          this.removeAccessibleListeners = pipe(removeKeydownListener, removeBlurListener);
        };
      }
      startPress(event, info) {
        this.isPressing = true;
        const { onTapStart, whileTap } = this.node.getProps();
        if (whileTap && this.node.animationState) {
          this.node.animationState.setActive("whileTap", true);
        }
        if (onTapStart) {
          frame.postRender(() => onTapStart(event, info));
        }
      }
      checkPressEnd() {
        this.removeEndListeners();
        this.isPressing = false;
        const props = this.node.getProps();
        if (props.whileTap && this.node.animationState) {
          this.node.animationState.setActive("whileTap", false);
        }
        return !isDragActive();
      }
      cancelPress(event, info) {
        if (!this.checkPressEnd())
          return;
        const { onTapCancel } = this.node.getProps();
        if (onTapCancel) {
          frame.postRender(() => onTapCancel(event, info));
        }
      }
      mount() {
        const props = this.node.getProps();
        const removePointerListener = addPointerEvent(props.globalTapTarget ? window : this.node.current, "pointerdown", this.startPointerPress, {
          passive: !(props.onTapStart || props["onPointerStart"])
        });
        const removeFocusListener = addDomEvent(this.node.current, "focus", this.startAccessiblePress);
        this.removeStartListeners = pipe(removePointerListener, removeFocusListener);
      }
      unmount() {
        this.removeStartListeners();
        this.removeEndListeners();
        this.removeAccessibleListeners();
      }
    };
  }
});

// node_modules/framer-motion/dist/es/motion/features/viewport/observers.mjs
function initIntersectionObserver({ root: root5, ...options }) {
  const lookupRoot = root5 || document;
  if (!observers.has(lookupRoot)) {
    observers.set(lookupRoot, {});
  }
  const rootObservers = observers.get(lookupRoot);
  const key = JSON.stringify(options);
  if (!rootObservers[key]) {
    rootObservers[key] = new IntersectionObserver(fireAllObserverCallbacks, { root: root5, ...options });
  }
  return rootObservers[key];
}
function observeIntersection(element, options, callback) {
  const rootInteresectionObserver = initIntersectionObserver(options);
  observerCallbacks.set(element, callback);
  rootInteresectionObserver.observe(element);
  return () => {
    observerCallbacks.delete(element);
    rootInteresectionObserver.unobserve(element);
  };
}
var observerCallbacks, observers, fireObserverCallback, fireAllObserverCallbacks;
var init_observers = __esm({
  "node_modules/framer-motion/dist/es/motion/features/viewport/observers.mjs"() {
    observerCallbacks = /* @__PURE__ */ new WeakMap();
    observers = /* @__PURE__ */ new WeakMap();
    fireObserverCallback = (entry) => {
      const callback = observerCallbacks.get(entry.target);
      callback && callback(entry);
    };
    fireAllObserverCallbacks = (entries) => {
      entries.forEach(fireObserverCallback);
    };
  }
});

// node_modules/framer-motion/dist/es/motion/features/viewport/index.mjs
function hasViewportOptionChanged({ viewport = {} }, { viewport: prevViewport = {} } = {}) {
  return (name2) => viewport[name2] !== prevViewport[name2];
}
var thresholdNames, InViewFeature;
var init_viewport = __esm({
  "node_modules/framer-motion/dist/es/motion/features/viewport/index.mjs"() {
    init_Feature();
    init_observers();
    thresholdNames = {
      some: 0,
      all: 1
    };
    InViewFeature = class extends Feature {
      constructor() {
        super(...arguments);
        this.hasEnteredView = false;
        this.isInView = false;
      }
      startObserver() {
        this.unmount();
        const { viewport = {} } = this.node.getProps();
        const { root: root5, margin: rootMargin, amount = "some", once: once2 } = viewport;
        const options = {
          root: root5 ? root5.current : void 0,
          rootMargin,
          threshold: typeof amount === "number" ? amount : thresholdNames[amount]
        };
        const onIntersectionUpdate = (entry) => {
          const { isIntersecting } = entry;
          if (this.isInView === isIntersecting)
            return;
          this.isInView = isIntersecting;
          if (once2 && !isIntersecting && this.hasEnteredView) {
            return;
          } else if (isIntersecting) {
            this.hasEnteredView = true;
          }
          if (this.node.animationState) {
            this.node.animationState.setActive("whileInView", isIntersecting);
          }
          const { onViewportEnter, onViewportLeave } = this.node.getProps();
          const callback = isIntersecting ? onViewportEnter : onViewportLeave;
          callback && callback(entry);
        };
        return observeIntersection(this.node.current, options, onIntersectionUpdate);
      }
      mount() {
        this.startObserver();
      }
      update() {
        if (typeof IntersectionObserver === "undefined")
          return;
        const { props, prevProps } = this.node;
        const hasOptionsChanged = ["amount", "margin", "root"].some(hasViewportOptionChanged(props, prevProps));
        if (hasOptionsChanged) {
          this.startObserver();
        }
      }
      unmount() {
      }
    };
  }
});

// node_modules/framer-motion/dist/es/motion/features/gestures.mjs
var gestureAnimations;
var init_gestures = __esm({
  "node_modules/framer-motion/dist/es/motion/features/gestures.mjs"() {
    init_hover2();
    init_focus();
    init_press();
    init_viewport();
    gestureAnimations = {
      inView: {
        Feature: InViewFeature
      },
      tap: {
        Feature: PressGesture
      },
      focus: {
        Feature: FocusGesture
      },
      hover: {
        Feature: HoverGesture
      }
    };
  }
});

// node_modules/framer-motion/dist/es/motion/features/layout.mjs
var layout;
var init_layout = __esm({
  "node_modules/framer-motion/dist/es/motion/features/layout.mjs"() {
    init_HTMLProjectionNode();
    init_MeasureLayout();
    layout = {
      layout: {
        ProjectionNode: HTMLProjectionNode,
        MeasureLayout
      }
    };
  }
});

// node_modules/framer-motion/dist/es/context/MotionConfigContext.mjs
var import_react239, MotionConfigContext;
var init_MotionConfigContext = __esm({
  "node_modules/framer-motion/dist/es/context/MotionConfigContext.mjs"() {
    "use client";
    import_react239 = __toESM(require_react(), 1);
    MotionConfigContext = (0, import_react239.createContext)({
      transformPagePoint: (p11) => p11,
      isStatic: false,
      reducedMotion: "never"
    });
  }
});

// node_modules/framer-motion/dist/es/context/MotionContext/index.mjs
var import_react240, MotionContext;
var init_MotionContext = __esm({
  "node_modules/framer-motion/dist/es/context/MotionContext/index.mjs"() {
    "use client";
    import_react240 = __toESM(require_react(), 1);
    MotionContext = (0, import_react240.createContext)({});
  }
});

// node_modules/framer-motion/dist/es/utils/is-browser.mjs
var isBrowser2;
var init_is_browser = __esm({
  "node_modules/framer-motion/dist/es/utils/is-browser.mjs"() {
    isBrowser2 = typeof window !== "undefined";
  }
});

// node_modules/framer-motion/dist/es/utils/use-isomorphic-effect.mjs
var import_react241, useIsomorphicLayoutEffect;
var init_use_isomorphic_effect2 = __esm({
  "node_modules/framer-motion/dist/es/utils/use-isomorphic-effect.mjs"() {
    import_react241 = __toESM(require_react(), 1);
    init_is_browser();
    useIsomorphicLayoutEffect = isBrowser2 ? import_react241.useLayoutEffect : import_react241.useEffect;
  }
});

// node_modules/framer-motion/dist/es/context/LazyContext.mjs
var import_react242, LazyContext;
var init_LazyContext = __esm({
  "node_modules/framer-motion/dist/es/context/LazyContext.mjs"() {
    "use client";
    import_react242 = __toESM(require_react(), 1);
    LazyContext = (0, import_react242.createContext)({ strict: false });
  }
});

// node_modules/framer-motion/dist/es/motion/utils/use-visual-element.mjs
function useVisualElement(Component2, visualState, props, createVisualElement, ProjectionNodeConstructor) {
  var _a, _b;
  const { visualElement: parent } = (0, import_react243.useContext)(MotionContext);
  const lazyContext = (0, import_react243.useContext)(LazyContext);
  const presenceContext = (0, import_react243.useContext)(PresenceContext);
  const reducedMotionConfig = (0, import_react243.useContext)(MotionConfigContext).reducedMotion;
  const visualElementRef = (0, import_react243.useRef)();
  createVisualElement = createVisualElement || lazyContext.renderer;
  if (!visualElementRef.current && createVisualElement) {
    visualElementRef.current = createVisualElement(Component2, {
      visualState,
      parent,
      props,
      presenceContext,
      blockInitialAnimation: presenceContext ? presenceContext.initial === false : false,
      reducedMotionConfig
    });
  }
  const visualElement = visualElementRef.current;
  const initialLayoutGroupConfig = (0, import_react243.useContext)(SwitchLayoutGroupContext);
  if (visualElement && !visualElement.projection && ProjectionNodeConstructor && (visualElement.type === "html" || visualElement.type === "svg")) {
    createProjectionNode2(visualElementRef.current, props, ProjectionNodeConstructor, initialLayoutGroupConfig);
  }
  const isMounted = (0, import_react243.useRef)(false);
  (0, import_react243.useInsertionEffect)(() => {
    if (visualElement && isMounted.current) {
      visualElement.update(props, presenceContext);
    }
  });
  const optimisedAppearId = props[optimizedAppearDataAttribute];
  const wantsHandoff = (0, import_react243.useRef)(Boolean(optimisedAppearId) && !((_a = window.MotionHandoffIsComplete) === null || _a === void 0 ? void 0 : _a.call(window, optimisedAppearId)) && ((_b = window.MotionHasOptimisedAnimation) === null || _b === void 0 ? void 0 : _b.call(window, optimisedAppearId)));
  useIsomorphicLayoutEffect(() => {
    if (!visualElement)
      return;
    isMounted.current = true;
    window.MotionIsMounted = true;
    visualElement.updateFeatures();
    microtask.render(visualElement.render);
    if (wantsHandoff.current && visualElement.animationState) {
      visualElement.animationState.animateChanges();
    }
  });
  (0, import_react243.useEffect)(() => {
    if (!visualElement)
      return;
    if (!wantsHandoff.current && visualElement.animationState) {
      visualElement.animationState.animateChanges();
    }
    if (wantsHandoff.current) {
      queueMicrotask(() => {
        var _a2;
        (_a2 = window.MotionHandoffMarkAsComplete) === null || _a2 === void 0 ? void 0 : _a2.call(window, optimisedAppearId);
      });
      wantsHandoff.current = false;
    }
  });
  return visualElement;
}
function createProjectionNode2(visualElement, props, ProjectionNodeConstructor, initialPromotionConfig) {
  const { layoutId, layout: layout2, drag: drag2, dragConstraints, layoutScroll, layoutRoot } = props;
  visualElement.projection = new ProjectionNodeConstructor(visualElement.latestValues, props["data-framer-portal-id"] ? void 0 : getClosestProjectingNode(visualElement.parent));
  visualElement.projection.setOptions({
    layoutId,
    layout: layout2,
    alwaysMeasureLayout: Boolean(drag2) || dragConstraints && isRefObject(dragConstraints),
    visualElement,
    /**
     * TODO: Update options in an effect. This could be tricky as it'll be too late
     * to update by the time layout animations run.
     * We also need to fix this safeToRemove by linking it up to the one returned by usePresence,
     * ensuring it gets called if there's no potential layout animations.
     *
     */
    animationType: typeof layout2 === "string" ? layout2 : "both",
    initialPromotionConfig,
    layoutScroll,
    layoutRoot
  });
}
function getClosestProjectingNode(visualElement) {
  if (!visualElement)
    return void 0;
  return visualElement.options.allowProjection !== false ? visualElement.projection : getClosestProjectingNode(visualElement.parent);
}
var import_react243;
var init_use_visual_element = __esm({
  "node_modules/framer-motion/dist/es/motion/utils/use-visual-element.mjs"() {
    import_react243 = __toESM(require_react(), 1);
    init_PresenceContext();
    init_MotionContext();
    init_use_isomorphic_effect2();
    init_LazyContext();
    init_MotionConfigContext();
    init_data_id();
    init_microtask();
    init_is_ref_object();
    init_SwitchLayoutGroupContext();
  }
});

// node_modules/framer-motion/dist/es/motion/utils/use-motion-ref.mjs
function useMotionRef(visualState, visualElement, externalRef) {
  return (0, import_react244.useCallback)(
    (instance) => {
      instance && visualState.mount && visualState.mount(instance);
      if (visualElement) {
        if (instance) {
          visualElement.mount(instance);
        } else {
          visualElement.unmount();
        }
      }
      if (externalRef) {
        if (typeof externalRef === "function") {
          externalRef(instance);
        } else if (isRefObject(externalRef)) {
          externalRef.current = instance;
        }
      }
    },
    /**
     * Only pass a new ref callback to React if we've received a visual element
     * factory. Otherwise we'll be mounting/remounting every time externalRef
     * or other dependencies change.
     */
    [visualElement]
  );
}
var import_react244;
var init_use_motion_ref = __esm({
  "node_modules/framer-motion/dist/es/motion/utils/use-motion-ref.mjs"() {
    import_react244 = __toESM(require_react(), 1);
    init_is_ref_object();
  }
});

// node_modules/framer-motion/dist/es/render/utils/is-controlling-variants.mjs
function isControllingVariants(props) {
  return isAnimationControls(props.animate) || variantProps.some((name2) => isVariantLabel(props[name2]));
}
function isVariantNode(props) {
  return Boolean(isControllingVariants(props) || props.variants);
}
var init_is_controlling_variants = __esm({
  "node_modules/framer-motion/dist/es/render/utils/is-controlling-variants.mjs"() {
    init_is_animation_controls();
    init_is_variant_label();
    init_variant_props();
  }
});

// node_modules/framer-motion/dist/es/context/MotionContext/utils.mjs
function getCurrentTreeVariants(props, context) {
  if (isControllingVariants(props)) {
    const { initial, animate } = props;
    return {
      initial: initial === false || isVariantLabel(initial) ? initial : void 0,
      animate: isVariantLabel(animate) ? animate : void 0
    };
  }
  return props.inherit !== false ? context : {};
}
var init_utils6 = __esm({
  "node_modules/framer-motion/dist/es/context/MotionContext/utils.mjs"() {
    init_is_variant_label();
    init_is_controlling_variants();
  }
});

// node_modules/framer-motion/dist/es/context/MotionContext/create.mjs
function useCreateMotionContext(props) {
  const { initial, animate } = getCurrentTreeVariants(props, (0, import_react245.useContext)(MotionContext));
  return (0, import_react245.useMemo)(() => ({ initial, animate }), [variantLabelsAsDependency(initial), variantLabelsAsDependency(animate)]);
}
function variantLabelsAsDependency(prop) {
  return Array.isArray(prop) ? prop.join(" ") : prop;
}
var import_react245;
var init_create = __esm({
  "node_modules/framer-motion/dist/es/context/MotionContext/create.mjs"() {
    import_react245 = __toESM(require_react(), 1);
    init_MotionContext();
    init_utils6();
  }
});

// node_modules/framer-motion/dist/es/motion/features/definitions.mjs
var featureProps, featureDefinitions;
var init_definitions = __esm({
  "node_modules/framer-motion/dist/es/motion/features/definitions.mjs"() {
    featureProps = {
      animation: [
        "animate",
        "variants",
        "whileHover",
        "whileTap",
        "exit",
        "whileInView",
        "whileFocus",
        "whileDrag"
      ],
      exit: ["exit"],
      drag: ["drag", "dragControls"],
      focus: ["whileFocus"],
      hover: ["whileHover", "onHoverStart", "onHoverEnd"],
      tap: ["whileTap", "onTap", "onTapStart", "onTapCancel"],
      pan: ["onPan", "onPanStart", "onPanSessionStart", "onPanEnd"],
      inView: ["whileInView", "onViewportEnter", "onViewportLeave"],
      layout: ["layout", "layoutId"]
    };
    featureDefinitions = {};
    for (const key in featureProps) {
      featureDefinitions[key] = {
        isEnabled: (props) => featureProps[key].some((name2) => !!props[name2])
      };
    }
  }
});

// node_modules/framer-motion/dist/es/motion/features/load-features.mjs
function loadFeatures(features) {
  for (const key in features) {
    featureDefinitions[key] = {
      ...featureDefinitions[key],
      ...features[key]
    };
  }
}
var init_load_features = __esm({
  "node_modules/framer-motion/dist/es/motion/features/load-features.mjs"() {
    init_definitions();
  }
});

// node_modules/framer-motion/dist/es/motion/utils/symbol.mjs
var motionComponentSymbol;
var init_symbol = __esm({
  "node_modules/framer-motion/dist/es/motion/utils/symbol.mjs"() {
    motionComponentSymbol = Symbol.for("motionComponentSymbol");
  }
});

// node_modules/framer-motion/dist/es/motion/index.mjs
function createRendererMotionComponent({ preloadedFeatures, createVisualElement, useRender, useVisualState, Component: Component2 }) {
  preloadedFeatures && loadFeatures(preloadedFeatures);
  function MotionComponent(props, externalRef) {
    let MeasureLayout2;
    const configAndProps = {
      ...(0, import_react246.useContext)(MotionConfigContext),
      ...props,
      layoutId: useLayoutId(props)
    };
    const { isStatic } = configAndProps;
    const context = useCreateMotionContext(props);
    const visualState = useVisualState(props, isStatic);
    if (!isStatic && isBrowser2) {
      useStrictMode(configAndProps, preloadedFeatures);
      const layoutProjection = getProjectionFunctionality(configAndProps);
      MeasureLayout2 = layoutProjection.MeasureLayout;
      context.visualElement = useVisualElement(Component2, visualState, configAndProps, createVisualElement, layoutProjection.ProjectionNode);
    }
    return (0, import_jsx_runtime171.jsxs)(MotionContext.Provider, { value: context, children: [MeasureLayout2 && context.visualElement ? (0, import_jsx_runtime171.jsx)(MeasureLayout2, { visualElement: context.visualElement, ...configAndProps }) : null, useRender(Component2, props, useMotionRef(visualState, context.visualElement, externalRef), visualState, isStatic, context.visualElement)] });
  }
  const ForwardRefMotionComponent = (0, import_react246.forwardRef)(MotionComponent);
  ForwardRefMotionComponent[motionComponentSymbol] = Component2;
  return ForwardRefMotionComponent;
}
function useLayoutId({ layoutId }) {
  const layoutGroupId = (0, import_react246.useContext)(LayoutGroupContext).id;
  return layoutGroupId && layoutId !== void 0 ? layoutGroupId + "-" + layoutId : layoutId;
}
function useStrictMode(configAndProps, preloadedFeatures) {
  const isStrict = (0, import_react246.useContext)(LazyContext).strict;
  if (false) {
    const strictMessage = "You have rendered a `motion` component within a `LazyMotion` component. This will break tree shaking. Import and render a `m` component instead.";
    configAndProps.ignoreStrict ? warning(false, strictMessage) : invariant(false, strictMessage);
  }
}
function getProjectionFunctionality(props) {
  const { drag: drag2, layout: layout2 } = featureDefinitions;
  if (!drag2 && !layout2)
    return {};
  const combined = { ...drag2, ...layout2 };
  return {
    MeasureLayout: (drag2 === null || drag2 === void 0 ? void 0 : drag2.isEnabled(props)) || (layout2 === null || layout2 === void 0 ? void 0 : layout2.isEnabled(props)) ? combined.MeasureLayout : void 0,
    ProjectionNode: combined.ProjectionNode
  };
}
var import_jsx_runtime171, import_react246;
var init_motion = __esm({
  "node_modules/framer-motion/dist/es/motion/index.mjs"() {
    "use client";
    import_jsx_runtime171 = __toESM(require_jsx_runtime(), 1);
    import_react246 = __toESM(require_react(), 1);
    init_MotionConfigContext();
    init_MotionContext();
    init_use_visual_element();
    init_use_motion_ref();
    init_create();
    init_load_features();
    init_is_browser();
    init_LayoutGroupContext();
    init_LazyContext();
    init_symbol();
    init_es();
    init_definitions();
  }
});

// node_modules/framer-motion/dist/es/render/svg/lowercase-elements.mjs
var lowercaseSVGElements;
var init_lowercase_elements = __esm({
  "node_modules/framer-motion/dist/es/render/svg/lowercase-elements.mjs"() {
    lowercaseSVGElements = [
      "animate",
      "circle",
      "defs",
      "desc",
      "ellipse",
      "g",
      "image",
      "line",
      "filter",
      "marker",
      "mask",
      "metadata",
      "path",
      "pattern",
      "polygon",
      "polyline",
      "rect",
      "stop",
      "switch",
      "symbol",
      "svg",
      "text",
      "tspan",
      "use",
      "view"
    ];
  }
});

// node_modules/framer-motion/dist/es/render/dom/utils/is-svg-component.mjs
function isSVGComponent(Component2) {
  if (
    /**
     * If it's not a string, it's a custom React component. Currently we only support
     * HTML custom React components.
     */
    typeof Component2 !== "string" || /**
     * If it contains a dash, the element is a custom HTML webcomponent.
     */
    Component2.includes("-")
  ) {
    return false;
  } else if (
    /**
     * If it's in our list of lowercase SVG tags, it's an SVG component
     */
    lowercaseSVGElements.indexOf(Component2) > -1 || /**
     * If it contains a capital letter, it's an SVG component
     */
    /[A-Z]/u.test(Component2)
  ) {
    return true;
  }
  return false;
}
var init_is_svg_component = __esm({
  "node_modules/framer-motion/dist/es/render/dom/utils/is-svg-component.mjs"() {
    init_lowercase_elements();
  }
});

// node_modules/framer-motion/dist/es/render/html/utils/render.mjs
function renderHTML(element, { style, vars }, styleProp, projection) {
  Object.assign(element.style, style, projection && projection.getProjectionStyles(styleProp));
  for (const key in vars) {
    element.style.setProperty(key, vars[key]);
  }
}
var init_render = __esm({
  "node_modules/framer-motion/dist/es/render/html/utils/render.mjs"() {
  }
});

// node_modules/framer-motion/dist/es/render/svg/utils/camel-case-attrs.mjs
var camelCaseAttributes;
var init_camel_case_attrs = __esm({
  "node_modules/framer-motion/dist/es/render/svg/utils/camel-case-attrs.mjs"() {
    camelCaseAttributes = /* @__PURE__ */ new Set([
      "baseFrequency",
      "diffuseConstant",
      "kernelMatrix",
      "kernelUnitLength",
      "keySplines",
      "keyTimes",
      "limitingConeAngle",
      "markerHeight",
      "markerWidth",
      "numOctaves",
      "targetX",
      "targetY",
      "surfaceScale",
      "specularConstant",
      "specularExponent",
      "stdDeviation",
      "tableValues",
      "viewBox",
      "gradientTransform",
      "pathLength",
      "startOffset",
      "textLength",
      "lengthAdjust"
    ]);
  }
});

// node_modules/framer-motion/dist/es/render/svg/utils/render.mjs
function renderSVG(element, renderState, _styleProp, projection) {
  renderHTML(element, renderState, void 0, projection);
  for (const key in renderState.attrs) {
    element.setAttribute(!camelCaseAttributes.has(key) ? camelToDash(key) : key, renderState.attrs[key]);
  }
}
var init_render2 = __esm({
  "node_modules/framer-motion/dist/es/render/svg/utils/render.mjs"() {
    init_camel_to_dash();
    init_render();
    init_camel_case_attrs();
  }
});

// node_modules/framer-motion/dist/es/motion/utils/is-forced-motion-value.mjs
function isForcedMotionValue(key, { layout: layout2, layoutId }) {
  return transformProps.has(key) || key.startsWith("origin") || (layout2 || layoutId !== void 0) && (!!scaleCorrectors[key] || key === "opacity");
}
var init_is_forced_motion_value = __esm({
  "node_modules/framer-motion/dist/es/motion/utils/is-forced-motion-value.mjs"() {
    init_scale_correction();
    init_transform();
  }
});

// node_modules/framer-motion/dist/es/render/html/utils/scrape-motion-values.mjs
function scrapeMotionValuesFromProps(props, prevProps, visualElement) {
  var _a;
  const { style } = props;
  const newValues = {};
  for (const key in style) {
    if (isMotionValue(style[key]) || prevProps.style && isMotionValue(prevProps.style[key]) || isForcedMotionValue(key, props) || ((_a = visualElement === null || visualElement === void 0 ? void 0 : visualElement.getValue(key)) === null || _a === void 0 ? void 0 : _a.liveStyle) !== void 0) {
      newValues[key] = style[key];
    }
  }
  return newValues;
}
var init_scrape_motion_values = __esm({
  "node_modules/framer-motion/dist/es/render/html/utils/scrape-motion-values.mjs"() {
    init_is_forced_motion_value();
    init_is_motion_value();
  }
});

// node_modules/framer-motion/dist/es/render/svg/utils/scrape-motion-values.mjs
function scrapeMotionValuesFromProps2(props, prevProps, visualElement) {
  const newValues = scrapeMotionValuesFromProps(props, prevProps, visualElement);
  for (const key in props) {
    if (isMotionValue(props[key]) || isMotionValue(prevProps[key])) {
      const targetKey = transformPropOrder.indexOf(key) !== -1 ? "attr" + key.charAt(0).toUpperCase() + key.substring(1) : key;
      newValues[targetKey] = props[key];
    }
  }
  return newValues;
}
var init_scrape_motion_values2 = __esm({
  "node_modules/framer-motion/dist/es/render/svg/utils/scrape-motion-values.mjs"() {
    init_is_motion_value();
    init_scrape_motion_values();
    init_transform();
  }
});

// node_modules/framer-motion/dist/es/utils/use-constant.mjs
function useConstant(init) {
  const ref = (0, import_react247.useRef)(null);
  if (ref.current === null) {
    ref.current = init();
  }
  return ref.current;
}
var import_react247;
var init_use_constant = __esm({
  "node_modules/framer-motion/dist/es/utils/use-constant.mjs"() {
    import_react247 = __toESM(require_react(), 1);
  }
});

// node_modules/framer-motion/dist/es/motion/utils/use-visual-state.mjs
function makeState({ scrapeMotionValuesFromProps: scrapeMotionValuesFromProps3, createRenderState, onMount }, props, context, presenceContext) {
  const state2 = {
    latestValues: makeLatestValues(props, context, presenceContext, scrapeMotionValuesFromProps3),
    renderState: createRenderState()
  };
  if (onMount) {
    state2.mount = (instance) => onMount(props, instance, state2);
  }
  return state2;
}
function makeLatestValues(props, context, presenceContext, scrapeMotionValues) {
  const values2 = {};
  const motionValues = scrapeMotionValues(props, {});
  for (const key in motionValues) {
    values2[key] = resolveMotionValue(motionValues[key]);
  }
  let { initial, animate } = props;
  const isControllingVariants$1 = isControllingVariants(props);
  const isVariantNode$1 = isVariantNode(props);
  if (context && isVariantNode$1 && !isControllingVariants$1 && props.inherit !== false) {
    if (initial === void 0)
      initial = context.initial;
    if (animate === void 0)
      animate = context.animate;
  }
  let isInitialAnimationBlocked = presenceContext ? presenceContext.initial === false : false;
  isInitialAnimationBlocked = isInitialAnimationBlocked || initial === false;
  const variantToSet = isInitialAnimationBlocked ? animate : initial;
  if (variantToSet && typeof variantToSet !== "boolean" && !isAnimationControls(variantToSet)) {
    const list = Array.isArray(variantToSet) ? variantToSet : [variantToSet];
    for (let i13 = 0; i13 < list.length; i13++) {
      const resolved = resolveVariantFromProps(props, list[i13]);
      if (resolved) {
        const { transitionEnd, transition, ...target } = resolved;
        for (const key in target) {
          let valueTarget = target[key];
          if (Array.isArray(valueTarget)) {
            const index3 = isInitialAnimationBlocked ? valueTarget.length - 1 : 0;
            valueTarget = valueTarget[index3];
          }
          if (valueTarget !== null) {
            values2[key] = valueTarget;
          }
        }
        for (const key in transitionEnd) {
          values2[key] = transitionEnd[key];
        }
      }
    }
  }
  return values2;
}
var import_react248, makeUseVisualState;
var init_use_visual_state = __esm({
  "node_modules/framer-motion/dist/es/motion/utils/use-visual-state.mjs"() {
    import_react248 = __toESM(require_react(), 1);
    init_is_animation_controls();
    init_PresenceContext();
    init_resolve_variants();
    init_use_constant();
    init_resolve_motion_value();
    init_MotionContext();
    init_is_controlling_variants();
    makeUseVisualState = (config) => (props, isStatic) => {
      const context = (0, import_react248.useContext)(MotionContext);
      const presenceContext = (0, import_react248.useContext)(PresenceContext);
      const make = () => makeState(config, props, context, presenceContext);
      return isStatic ? make() : useConstant(make);
    };
  }
});

// node_modules/framer-motion/dist/es/render/html/utils/create-render-state.mjs
var createHtmlRenderState;
var init_create_render_state = __esm({
  "node_modules/framer-motion/dist/es/render/html/utils/create-render-state.mjs"() {
    createHtmlRenderState = () => ({
      style: {},
      transform: {},
      transformOrigin: {},
      vars: {}
    });
  }
});

// node_modules/framer-motion/dist/es/render/svg/utils/create-render-state.mjs
var createSvgRenderState;
var init_create_render_state2 = __esm({
  "node_modules/framer-motion/dist/es/render/svg/utils/create-render-state.mjs"() {
    init_create_render_state();
    createSvgRenderState = () => ({
      ...createHtmlRenderState(),
      attrs: {}
    });
  }
});

// node_modules/framer-motion/dist/es/render/dom/value-types/get-as-type.mjs
var getValueAsType;
var init_get_as_type = __esm({
  "node_modules/framer-motion/dist/es/render/dom/value-types/get-as-type.mjs"() {
    getValueAsType = (value, type2) => {
      return type2 && typeof value === "number" ? type2.transform(value) : value;
    };
  }
});

// node_modules/framer-motion/dist/es/render/html/utils/build-transform.mjs
function buildTransform(latestValues, transform, transformTemplate) {
  let transformString = "";
  let transformIsDefault = true;
  for (let i13 = 0; i13 < numTransforms; i13++) {
    const key = transformPropOrder[i13];
    const value = latestValues[key];
    if (value === void 0)
      continue;
    let valueIsDefault = true;
    if (typeof value === "number") {
      valueIsDefault = value === (key.startsWith("scale") ? 1 : 0);
    } else {
      valueIsDefault = parseFloat(value) === 0;
    }
    if (!valueIsDefault || transformTemplate) {
      const valueAsType = getValueAsType(value, numberValueTypes[key]);
      if (!valueIsDefault) {
        transformIsDefault = false;
        const transformName = translateAlias[key] || key;
        transformString += `${transformName}(${valueAsType}) `;
      }
      if (transformTemplate) {
        transform[key] = valueAsType;
      }
    }
  }
  transformString = transformString.trim();
  if (transformTemplate) {
    transformString = transformTemplate(transform, transformIsDefault ? "" : transformString);
  } else if (transformIsDefault) {
    transformString = "none";
  }
  return transformString;
}
var translateAlias, numTransforms;
var init_build_transform = __esm({
  "node_modules/framer-motion/dist/es/render/html/utils/build-transform.mjs"() {
    init_transform();
    init_get_as_type();
    init_number();
    translateAlias = {
      x: "translateX",
      y: "translateY",
      z: "translateZ",
      transformPerspective: "perspective"
    };
    numTransforms = transformPropOrder.length;
  }
});

// node_modules/framer-motion/dist/es/render/html/utils/build-styles.mjs
function buildHTMLStyles(state2, latestValues, transformTemplate) {
  const { style, vars, transformOrigin } = state2;
  let hasTransform2 = false;
  let hasTransformOrigin = false;
  for (const key in latestValues) {
    const value = latestValues[key];
    if (transformProps.has(key)) {
      hasTransform2 = true;
      continue;
    } else if (isCSSVariableName(key)) {
      vars[key] = value;
      continue;
    } else {
      const valueAsType = getValueAsType(value, numberValueTypes[key]);
      if (key.startsWith("origin")) {
        hasTransformOrigin = true;
        transformOrigin[key] = valueAsType;
      } else {
        style[key] = valueAsType;
      }
    }
  }
  if (!latestValues.transform) {
    if (hasTransform2 || transformTemplate) {
      style.transform = buildTransform(latestValues, state2.transform, transformTemplate);
    } else if (style.transform) {
      style.transform = "none";
    }
  }
  if (hasTransformOrigin) {
    const { originX = "50%", originY = "50%", originZ = 0 } = transformOrigin;
    style.transformOrigin = `${originX} ${originY} ${originZ}`;
  }
}
var init_build_styles = __esm({
  "node_modules/framer-motion/dist/es/render/html/utils/build-styles.mjs"() {
    init_build_transform();
    init_is_css_variable();
    init_transform();
    init_get_as_type();
    init_number();
  }
});

// node_modules/framer-motion/dist/es/render/svg/utils/transform-origin.mjs
function calcOrigin2(origin, offset4, size4) {
  return typeof origin === "string" ? origin : px2.transform(offset4 + size4 * origin);
}
function calcSVGTransformOrigin(dimensions, originX, originY) {
  const pxOriginX = calcOrigin2(originX, dimensions.x, dimensions.width);
  const pxOriginY = calcOrigin2(originY, dimensions.y, dimensions.height);
  return `${pxOriginX} ${pxOriginY}`;
}
var init_transform_origin = __esm({
  "node_modules/framer-motion/dist/es/render/svg/utils/transform-origin.mjs"() {
    init_units();
  }
});

// node_modules/framer-motion/dist/es/render/svg/utils/path.mjs
function buildSVGPath(attrs, length, spacing = 1, offset4 = 0, useDashCase = true) {
  attrs.pathLength = 1;
  const keys2 = useDashCase ? dashKeys : camelKeys;
  attrs[keys2.offset] = px2.transform(-offset4);
  const pathLength = px2.transform(length);
  const pathSpacing = px2.transform(spacing);
  attrs[keys2.array] = `${pathLength} ${pathSpacing}`;
}
var dashKeys, camelKeys;
var init_path = __esm({
  "node_modules/framer-motion/dist/es/render/svg/utils/path.mjs"() {
    init_units();
    dashKeys = {
      offset: "stroke-dashoffset",
      array: "stroke-dasharray"
    };
    camelKeys = {
      offset: "strokeDashoffset",
      array: "strokeDasharray"
    };
  }
});

// node_modules/framer-motion/dist/es/render/svg/utils/build-attrs.mjs
function buildSVGAttrs(state2, {
  attrX,
  attrY,
  attrScale,
  originX,
  originY,
  pathLength,
  pathSpacing = 1,
  pathOffset = 0,
  // This is object creation, which we try to avoid per-frame.
  ...latest
}, isSVGTag2, transformTemplate) {
  buildHTMLStyles(state2, latest, transformTemplate);
  if (isSVGTag2) {
    if (state2.style.viewBox) {
      state2.attrs.viewBox = state2.style.viewBox;
    }
    return;
  }
  state2.attrs = state2.style;
  state2.style = {};
  const { attrs, style, dimensions } = state2;
  if (attrs.transform) {
    if (dimensions)
      style.transform = attrs.transform;
    delete attrs.transform;
  }
  if (dimensions && (originX !== void 0 || originY !== void 0 || style.transform)) {
    style.transformOrigin = calcSVGTransformOrigin(dimensions, originX !== void 0 ? originX : 0.5, originY !== void 0 ? originY : 0.5);
  }
  if (attrX !== void 0)
    attrs.x = attrX;
  if (attrY !== void 0)
    attrs.y = attrY;
  if (attrScale !== void 0)
    attrs.scale = attrScale;
  if (pathLength !== void 0) {
    buildSVGPath(attrs, pathLength, pathSpacing, pathOffset, false);
  }
}
var init_build_attrs = __esm({
  "node_modules/framer-motion/dist/es/render/svg/utils/build-attrs.mjs"() {
    init_build_styles();
    init_transform_origin();
    init_path();
  }
});

// node_modules/framer-motion/dist/es/render/svg/utils/is-svg-tag.mjs
var isSVGTag;
var init_is_svg_tag = __esm({
  "node_modules/framer-motion/dist/es/render/svg/utils/is-svg-tag.mjs"() {
    isSVGTag = (tag2) => typeof tag2 === "string" && tag2.toLowerCase() === "svg";
  }
});

// node_modules/framer-motion/dist/es/render/svg/config-motion.mjs
var svgMotionConfig;
var init_config_motion = __esm({
  "node_modules/framer-motion/dist/es/render/svg/config-motion.mjs"() {
    init_render2();
    init_scrape_motion_values2();
    init_use_visual_state();
    init_create_render_state2();
    init_build_attrs();
    init_is_svg_tag();
    init_frame();
    svgMotionConfig = {
      useVisualState: makeUseVisualState({
        scrapeMotionValuesFromProps: scrapeMotionValuesFromProps2,
        createRenderState: createSvgRenderState,
        onMount: (props, instance, { renderState, latestValues }) => {
          frame.read(() => {
            try {
              renderState.dimensions = typeof instance.getBBox === "function" ? instance.getBBox() : instance.getBoundingClientRect();
            } catch (e14) {
              renderState.dimensions = {
                x: 0,
                y: 0,
                width: 0,
                height: 0
              };
            }
          });
          frame.render(() => {
            buildSVGAttrs(renderState, latestValues, isSVGTag(instance.tagName), props.transformTemplate);
            renderSVG(instance, renderState);
          });
        }
      })
    };
  }
});

// node_modules/framer-motion/dist/es/render/html/config-motion.mjs
var htmlMotionConfig;
var init_config_motion2 = __esm({
  "node_modules/framer-motion/dist/es/render/html/config-motion.mjs"() {
    init_use_visual_state();
    init_scrape_motion_values();
    init_create_render_state();
    htmlMotionConfig = {
      useVisualState: makeUseVisualState({
        scrapeMotionValuesFromProps,
        createRenderState: createHtmlRenderState
      })
    };
  }
});

// node_modules/framer-motion/dist/es/render/html/use-props.mjs
function copyRawValuesOnly(target, source, props) {
  for (const key in source) {
    if (!isMotionValue(source[key]) && !isForcedMotionValue(key, props)) {
      target[key] = source[key];
    }
  }
}
function useInitialMotionValues({ transformTemplate }, visualState) {
  return (0, import_react249.useMemo)(() => {
    const state2 = createHtmlRenderState();
    buildHTMLStyles(state2, visualState, transformTemplate);
    return Object.assign({}, state2.vars, state2.style);
  }, [visualState]);
}
function useStyle(props, visualState) {
  const styleProp = props.style || {};
  const style = {};
  copyRawValuesOnly(style, styleProp, props);
  Object.assign(style, useInitialMotionValues(props, visualState));
  return style;
}
function useHTMLProps(props, visualState) {
  const htmlProps = {};
  const style = useStyle(props, visualState);
  if (props.drag && props.dragListener !== false) {
    htmlProps.draggable = false;
    style.userSelect = style.WebkitUserSelect = style.WebkitTouchCallout = "none";
    style.touchAction = props.drag === true ? "none" : `pan-${props.drag === "x" ? "y" : "x"}`;
  }
  if (props.tabIndex === void 0 && (props.onTap || props.onTapStart || props.whileTap)) {
    htmlProps.tabIndex = 0;
  }
  htmlProps.style = style;
  return htmlProps;
}
var import_react249;
var init_use_props2 = __esm({
  "node_modules/framer-motion/dist/es/render/html/use-props.mjs"() {
    import_react249 = __toESM(require_react(), 1);
    init_is_forced_motion_value();
    init_is_motion_value();
    init_build_styles();
    init_create_render_state();
  }
});

// node_modules/framer-motion/dist/es/motion/utils/valid-prop.mjs
function isValidMotionProp(key) {
  return key.startsWith("while") || key.startsWith("drag") && key !== "draggable" || key.startsWith("layout") || key.startsWith("onTap") || key.startsWith("onPan") || key.startsWith("onLayout") || validMotionProps.has(key);
}
var validMotionProps;
var init_valid_prop = __esm({
  "node_modules/framer-motion/dist/es/motion/utils/valid-prop.mjs"() {
    validMotionProps = /* @__PURE__ */ new Set([
      "animate",
      "exit",
      "variants",
      "initial",
      "style",
      "values",
      "variants",
      "transition",
      "transformTemplate",
      "custom",
      "inherit",
      "onBeforeLayoutMeasure",
      "onAnimationStart",
      "onAnimationComplete",
      "onUpdate",
      "onDragStart",
      "onDrag",
      "onDragEnd",
      "onMeasureDragConstraints",
      "onDirectionLock",
      "onDragTransitionEnd",
      "_dragX",
      "_dragY",
      "onHoverStart",
      "onHoverEnd",
      "onViewportEnter",
      "onViewportLeave",
      "globalTapTarget",
      "ignoreStrict",
      "viewport"
    ]);
  }
});

// node_modules/framer-motion/dist/es/render/dom/utils/filter-props.mjs
function loadExternalIsValidProp(isValidProp) {
  if (!isValidProp)
    return;
  shouldForward = (key) => key.startsWith("on") ? !isValidMotionProp(key) : isValidProp(key);
}
function filterProps2(props, isDom, forwardMotionProps) {
  const filteredProps = {};
  for (const key in props) {
    if (key === "values" && typeof props.values === "object")
      continue;
    if (shouldForward(key) || forwardMotionProps === true && isValidMotionProp(key) || !isDom && !isValidMotionProp(key) || // If trying to use native HTML drag events, forward drag listeners
    props["draggable"] && key.startsWith("onDrag")) {
      filteredProps[key] = props[key];
    }
  }
  return filteredProps;
}
var shouldForward;
var init_filter_props2 = __esm({
  "node_modules/framer-motion/dist/es/render/dom/utils/filter-props.mjs"() {
    init_valid_prop();
    shouldForward = (key) => !isValidMotionProp(key);
    try {
      loadExternalIsValidProp(__require("@emotion/is-prop-valid").default);
    } catch (_a) {
    }
  }
});

// node_modules/framer-motion/dist/es/render/svg/use-props.mjs
function useSVGProps(props, visualState, _isStatic, Component2) {
  const visualProps = (0, import_react250.useMemo)(() => {
    const state2 = createSvgRenderState();
    buildSVGAttrs(state2, visualState, isSVGTag(Component2), props.transformTemplate);
    return {
      ...state2.attrs,
      style: { ...state2.style }
    };
  }, [visualState]);
  if (props.style) {
    const rawStyles = {};
    copyRawValuesOnly(rawStyles, props.style, props);
    visualProps.style = { ...rawStyles, ...visualProps.style };
  }
  return visualProps;
}
var import_react250;
var init_use_props3 = __esm({
  "node_modules/framer-motion/dist/es/render/svg/use-props.mjs"() {
    import_react250 = __toESM(require_react(), 1);
    init_use_props2();
    init_build_attrs();
    init_create_render_state2();
    init_is_svg_tag();
  }
});

// node_modules/framer-motion/dist/es/render/dom/use-render.mjs
function createUseRender(forwardMotionProps = false) {
  const useRender = (Component2, props, ref, { latestValues }, isStatic) => {
    const useVisualProps = isSVGComponent(Component2) ? useSVGProps : useHTMLProps;
    const visualProps = useVisualProps(props, latestValues, isStatic, Component2);
    const filteredProps = filterProps2(props, typeof Component2 === "string", forwardMotionProps);
    const elementProps = Component2 !== import_react251.Fragment ? { ...filteredProps, ...visualProps, ref } : {};
    const { children } = props;
    const renderedChildren = (0, import_react251.useMemo)(() => isMotionValue(children) ? children.get() : children, [children]);
    return (0, import_react251.createElement)(Component2, {
      ...elementProps,
      children: renderedChildren
    });
  };
  return useRender;
}
var import_react251;
var init_use_render = __esm({
  "node_modules/framer-motion/dist/es/render/dom/use-render.mjs"() {
    import_react251 = __toESM(require_react(), 1);
    init_use_props2();
    init_filter_props2();
    init_is_svg_component();
    init_use_props3();
    init_is_motion_value();
  }
});

// node_modules/framer-motion/dist/es/render/components/create-factory.mjs
function createMotionComponentFactory(preloadedFeatures, createVisualElement) {
  return function createMotionComponent2(Component2, { forwardMotionProps } = { forwardMotionProps: false }) {
    const baseConfig = isSVGComponent(Component2) ? svgMotionConfig : htmlMotionConfig;
    const config = {
      ...baseConfig,
      preloadedFeatures,
      useRender: createUseRender(forwardMotionProps),
      createVisualElement,
      Component: Component2
    };
    return createRendererMotionComponent(config);
  };
}
var init_create_factory = __esm({
  "node_modules/framer-motion/dist/es/render/components/create-factory.mjs"() {
    init_motion();
    init_is_svg_component();
    init_config_motion();
    init_config_motion2();
    init_use_render();
  }
});

// node_modules/framer-motion/dist/es/utils/reduced-motion/state.mjs
var prefersReducedMotion, hasReducedMotionListener;
var init_state2 = __esm({
  "node_modules/framer-motion/dist/es/utils/reduced-motion/state.mjs"() {
    prefersReducedMotion = { current: null };
    hasReducedMotionListener = { current: false };
  }
});

// node_modules/framer-motion/dist/es/utils/reduced-motion/index.mjs
function initPrefersReducedMotion() {
  hasReducedMotionListener.current = true;
  if (!isBrowser2)
    return;
  if (window.matchMedia) {
    const motionMediaQuery = window.matchMedia("(prefers-reduced-motion)");
    const setReducedMotionPreferences = () => prefersReducedMotion.current = motionMediaQuery.matches;
    motionMediaQuery.addListener(setReducedMotionPreferences);
    setReducedMotionPreferences();
  } else {
    prefersReducedMotion.current = false;
  }
}
var init_reduced_motion = __esm({
  "node_modules/framer-motion/dist/es/utils/reduced-motion/index.mjs"() {
    init_is_browser();
    init_state2();
  }
});

// node_modules/framer-motion/dist/es/render/utils/motion-values.mjs
function updateMotionValuesFromProps(element, next2, prev2) {
  for (const key in next2) {
    const nextValue = next2[key];
    const prevValue = prev2[key];
    if (isMotionValue(nextValue)) {
      element.addValue(key, nextValue);
      if (false) {
        warnOnce(nextValue.version === "11.13.1", `Attempting to mix Motion versions ${nextValue.version} with 11.13.1 may not work as expected.`);
      }
    } else if (isMotionValue(prevValue)) {
      element.addValue(key, motionValue(nextValue, { owner: element }));
    } else if (prevValue !== nextValue) {
      if (element.hasValue(key)) {
        const existingValue = element.getValue(key);
        if (existingValue.liveStyle === true) {
          existingValue.jump(nextValue);
        } else if (!existingValue.hasAnimated) {
          existingValue.set(nextValue);
        }
      } else {
        const latestValue = element.getStaticValue(key);
        element.addValue(key, motionValue(latestValue !== void 0 ? latestValue : nextValue, { owner: element }));
      }
    }
  }
  for (const key in prev2) {
    if (next2[key] === void 0)
      element.removeValue(key);
  }
  return next2;
}
var init_motion_values = __esm({
  "node_modules/framer-motion/dist/es/render/utils/motion-values.mjs"() {
    init_value();
    init_is_motion_value();
  }
});

// node_modules/framer-motion/dist/es/render/store.mjs
var visualElementStore;
var init_store2 = __esm({
  "node_modules/framer-motion/dist/es/render/store.mjs"() {
    visualElementStore = /* @__PURE__ */ new WeakMap();
  }
});

// node_modules/framer-motion/dist/es/render/dom/value-types/find.mjs
var valueTypes, findValueType;
var init_find2 = __esm({
  "node_modules/framer-motion/dist/es/render/dom/value-types/find.mjs"() {
    init_color();
    init_complex();
    init_dimensions();
    init_test();
    valueTypes = [...dimensionValueTypes, color, complex];
    findValueType = (v5) => valueTypes.find(testValueType(v5));
  }
});

// node_modules/framer-motion/dist/es/render/VisualElement.mjs
var propEventHandlers, VisualElement;
var init_VisualElement = __esm({
  "node_modules/framer-motion/dist/es/render/VisualElement.mjs"() {
    init_reduced_motion();
    init_state2();
    init_subscription_manager();
    init_value();
    init_is_motion_value();
    init_transform();
    init_is_controlling_variants();
    init_motion_values();
    init_resolve_variants();
    init_definitions();
    init_store2();
    init_KeyframesResolver();
    init_is_numerical_string();
    init_is_zero_value_string();
    init_find2();
    init_complex();
    init_animatable_none();
    init_models();
    init_sync_time();
    init_frame();
    propEventHandlers = [
      "AnimationStart",
      "AnimationComplete",
      "Update",
      "BeforeLayoutMeasure",
      "LayoutMeasure",
      "LayoutAnimationStart",
      "LayoutAnimationComplete"
    ];
    VisualElement = class {
      /**
       * This method takes React props and returns found MotionValues. For example, HTML
       * MotionValues will be found within the style prop, whereas for Three.js within attribute arrays.
       *
       * This isn't an abstract method as it needs calling in the constructor, but it is
       * intended to be one.
       */
      scrapeMotionValuesFromProps(_props, _prevProps, _visualElement) {
        return {};
      }
      constructor({ parent, props, presenceContext, reducedMotionConfig, blockInitialAnimation, visualState }, options = {}) {
        this.current = null;
        this.children = /* @__PURE__ */ new Set();
        this.isVariantNode = false;
        this.isControllingVariants = false;
        this.shouldReduceMotion = null;
        this.values = /* @__PURE__ */ new Map();
        this.KeyframeResolver = KeyframeResolver;
        this.features = {};
        this.valueSubscriptions = /* @__PURE__ */ new Map();
        this.prevMotionValues = {};
        this.events = {};
        this.propEventSubscriptions = {};
        this.notifyUpdate = () => this.notify("Update", this.latestValues);
        this.render = () => {
          if (!this.current)
            return;
          this.triggerBuild();
          this.renderInstance(this.current, this.renderState, this.props.style, this.projection);
        };
        this.renderScheduledAt = 0;
        this.scheduleRender = () => {
          const now2 = time2.now();
          if (this.renderScheduledAt < now2) {
            this.renderScheduledAt = now2;
            frame.render(this.render, false, true);
          }
        };
        const { latestValues, renderState } = visualState;
        this.latestValues = latestValues;
        this.baseTarget = { ...latestValues };
        this.initialValues = props.initial ? { ...latestValues } : {};
        this.renderState = renderState;
        this.parent = parent;
        this.props = props;
        this.presenceContext = presenceContext;
        this.depth = parent ? parent.depth + 1 : 0;
        this.reducedMotionConfig = reducedMotionConfig;
        this.options = options;
        this.blockInitialAnimation = Boolean(blockInitialAnimation);
        this.isControllingVariants = isControllingVariants(props);
        this.isVariantNode = isVariantNode(props);
        if (this.isVariantNode) {
          this.variantChildren = /* @__PURE__ */ new Set();
        }
        this.manuallyAnimateOnMount = Boolean(parent && parent.current);
        const { willChange, ...initialMotionValues } = this.scrapeMotionValuesFromProps(props, {}, this);
        for (const key in initialMotionValues) {
          const value = initialMotionValues[key];
          if (latestValues[key] !== void 0 && isMotionValue(value)) {
            value.set(latestValues[key], false);
          }
        }
      }
      mount(instance) {
        this.current = instance;
        visualElementStore.set(instance, this);
        if (this.projection && !this.projection.instance) {
          this.projection.mount(instance);
        }
        if (this.parent && this.isVariantNode && !this.isControllingVariants) {
          this.removeFromVariantTree = this.parent.addVariantChild(this);
        }
        this.values.forEach((value, key) => this.bindToMotionValue(key, value));
        if (!hasReducedMotionListener.current) {
          initPrefersReducedMotion();
        }
        this.shouldReduceMotion = this.reducedMotionConfig === "never" ? false : this.reducedMotionConfig === "always" ? true : prefersReducedMotion.current;
        if (false) {
          warnOnce(this.shouldReduceMotion !== true, "You have Reduced Motion enabled on your device. Animations may not appear as expected.");
        }
        if (this.parent)
          this.parent.children.add(this);
        this.update(this.props, this.presenceContext);
      }
      unmount() {
        visualElementStore.delete(this.current);
        this.projection && this.projection.unmount();
        cancelFrame(this.notifyUpdate);
        cancelFrame(this.render);
        this.valueSubscriptions.forEach((remove) => remove());
        this.valueSubscriptions.clear();
        this.removeFromVariantTree && this.removeFromVariantTree();
        this.parent && this.parent.children.delete(this);
        for (const key in this.events) {
          this.events[key].clear();
        }
        for (const key in this.features) {
          const feature = this.features[key];
          if (feature) {
            feature.unmount();
            feature.isMounted = false;
          }
        }
        this.current = null;
      }
      bindToMotionValue(key, value) {
        if (this.valueSubscriptions.has(key)) {
          this.valueSubscriptions.get(key)();
        }
        const valueIsTransform = transformProps.has(key);
        const removeOnChange = value.on("change", (latestValue) => {
          this.latestValues[key] = latestValue;
          this.props.onUpdate && frame.preRender(this.notifyUpdate);
          if (valueIsTransform && this.projection) {
            this.projection.isTransformDirty = true;
          }
        });
        const removeOnRenderRequest = value.on("renderRequest", this.scheduleRender);
        let removeSyncCheck;
        if (window.MotionCheckAppearSync) {
          removeSyncCheck = window.MotionCheckAppearSync(this, key, value);
        }
        this.valueSubscriptions.set(key, () => {
          removeOnChange();
          removeOnRenderRequest();
          if (removeSyncCheck)
            removeSyncCheck();
          if (value.owner)
            value.stop();
        });
      }
      sortNodePosition(other) {
        if (!this.current || !this.sortInstanceNodePosition || this.type !== other.type) {
          return 0;
        }
        return this.sortInstanceNodePosition(this.current, other.current);
      }
      updateFeatures() {
        let key = "animation";
        for (key in featureDefinitions) {
          const featureDefinition = featureDefinitions[key];
          if (!featureDefinition)
            continue;
          const { isEnabled, Feature: FeatureConstructor } = featureDefinition;
          if (!this.features[key] && FeatureConstructor && isEnabled(this.props)) {
            this.features[key] = new FeatureConstructor(this);
          }
          if (this.features[key]) {
            const feature = this.features[key];
            if (feature.isMounted) {
              feature.update();
            } else {
              feature.mount();
              feature.isMounted = true;
            }
          }
        }
      }
      triggerBuild() {
        this.build(this.renderState, this.latestValues, this.props);
      }
      /**
       * Measure the current viewport box with or without transforms.
       * Only measures axis-aligned boxes, rotate and skew must be manually
       * removed with a re-render to work.
       */
      measureViewportBox() {
        return this.current ? this.measureInstanceViewportBox(this.current, this.props) : createBox();
      }
      getStaticValue(key) {
        return this.latestValues[key];
      }
      setStaticValue(key, value) {
        this.latestValues[key] = value;
      }
      /**
       * Update the provided props. Ensure any newly-added motion values are
       * added to our map, old ones removed, and listeners updated.
       */
      update(props, presenceContext) {
        if (props.transformTemplate || this.props.transformTemplate) {
          this.scheduleRender();
        }
        this.prevProps = this.props;
        this.props = props;
        this.prevPresenceContext = this.presenceContext;
        this.presenceContext = presenceContext;
        for (let i13 = 0; i13 < propEventHandlers.length; i13++) {
          const key = propEventHandlers[i13];
          if (this.propEventSubscriptions[key]) {
            this.propEventSubscriptions[key]();
            delete this.propEventSubscriptions[key];
          }
          const listenerName = "on" + key;
          const listener = props[listenerName];
          if (listener) {
            this.propEventSubscriptions[key] = this.on(key, listener);
          }
        }
        this.prevMotionValues = updateMotionValuesFromProps(this, this.scrapeMotionValuesFromProps(props, this.prevProps, this), this.prevMotionValues);
        if (this.handleChildMotionValue) {
          this.handleChildMotionValue();
        }
      }
      getProps() {
        return this.props;
      }
      /**
       * Returns the variant definition with a given name.
       */
      getVariant(name2) {
        return this.props.variants ? this.props.variants[name2] : void 0;
      }
      /**
       * Returns the defined default transition on this component.
       */
      getDefaultTransition() {
        return this.props.transition;
      }
      getTransformPagePoint() {
        return this.props.transformPagePoint;
      }
      getClosestVariantNode() {
        return this.isVariantNode ? this : this.parent ? this.parent.getClosestVariantNode() : void 0;
      }
      /**
       * Add a child visual element to our set of children.
       */
      addVariantChild(child) {
        const closestVariantNode = this.getClosestVariantNode();
        if (closestVariantNode) {
          closestVariantNode.variantChildren && closestVariantNode.variantChildren.add(child);
          return () => closestVariantNode.variantChildren.delete(child);
        }
      }
      /**
       * Add a motion value and bind it to this visual element.
       */
      addValue(key, value) {
        const existingValue = this.values.get(key);
        if (value !== existingValue) {
          if (existingValue)
            this.removeValue(key);
          this.bindToMotionValue(key, value);
          this.values.set(key, value);
          this.latestValues[key] = value.get();
        }
      }
      /**
       * Remove a motion value and unbind any active subscriptions.
       */
      removeValue(key) {
        this.values.delete(key);
        const unsubscribe = this.valueSubscriptions.get(key);
        if (unsubscribe) {
          unsubscribe();
          this.valueSubscriptions.delete(key);
        }
        delete this.latestValues[key];
        this.removeValueFromRenderState(key, this.renderState);
      }
      /**
       * Check whether we have a motion value for this key
       */
      hasValue(key) {
        return this.values.has(key);
      }
      getValue(key, defaultValue2) {
        if (this.props.values && this.props.values[key]) {
          return this.props.values[key];
        }
        let value = this.values.get(key);
        if (value === void 0 && defaultValue2 !== void 0) {
          value = motionValue(defaultValue2 === null ? void 0 : defaultValue2, { owner: this });
          this.addValue(key, value);
        }
        return value;
      }
      /**
       * If we're trying to animate to a previously unencountered value,
       * we need to check for it in our state and as a last resort read it
       * directly from the instance (which might have performance implications).
       */
      readValue(key, target) {
        var _a;
        let value = this.latestValues[key] !== void 0 || !this.current ? this.latestValues[key] : (_a = this.getBaseTargetFromProps(this.props, key)) !== null && _a !== void 0 ? _a : this.readValueFromInstance(this.current, key, this.options);
        if (value !== void 0 && value !== null) {
          if (typeof value === "string" && (isNumericalString(value) || isZeroValueString(value))) {
            value = parseFloat(value);
          } else if (!findValueType(value) && complex.test(target)) {
            value = getAnimatableNone2(key, target);
          }
          this.setBaseTarget(key, isMotionValue(value) ? value.get() : value);
        }
        return isMotionValue(value) ? value.get() : value;
      }
      /**
       * Set the base target to later animate back to. This is currently
       * only hydrated on creation and when we first read a value.
       */
      setBaseTarget(key, value) {
        this.baseTarget[key] = value;
      }
      /**
       * Find the base target for a value thats been removed from all animation
       * props.
       */
      getBaseTarget(key) {
        var _a;
        const { initial } = this.props;
        let valueFromInitial;
        if (typeof initial === "string" || typeof initial === "object") {
          const variant = resolveVariantFromProps(this.props, initial, (_a = this.presenceContext) === null || _a === void 0 ? void 0 : _a.custom);
          if (variant) {
            valueFromInitial = variant[key];
          }
        }
        if (initial && valueFromInitial !== void 0) {
          return valueFromInitial;
        }
        const target = this.getBaseTargetFromProps(this.props, key);
        if (target !== void 0 && !isMotionValue(target))
          return target;
        return this.initialValues[key] !== void 0 && valueFromInitial === void 0 ? void 0 : this.baseTarget[key];
      }
      on(eventName, callback) {
        if (!this.events[eventName]) {
          this.events[eventName] = new SubscriptionManager();
        }
        return this.events[eventName].add(callback);
      }
      notify(eventName, ...args) {
        if (this.events[eventName]) {
          this.events[eventName].notify(...args);
        }
      }
    };
  }
});

// node_modules/framer-motion/dist/es/render/dom/DOMVisualElement.mjs
var DOMVisualElement;
var init_DOMVisualElement = __esm({
  "node_modules/framer-motion/dist/es/render/dom/DOMVisualElement.mjs"() {
    init_VisualElement();
    init_DOMKeyframesResolver();
    init_is_motion_value();
    DOMVisualElement = class extends VisualElement {
      constructor() {
        super(...arguments);
        this.KeyframeResolver = DOMKeyframesResolver;
      }
      sortInstanceNodePosition(a18, b4) {
        return a18.compareDocumentPosition(b4) & 2 ? 1 : -1;
      }
      getBaseTargetFromProps(props, key) {
        return props.style ? props.style[key] : void 0;
      }
      removeValueFromRenderState(key, { vars, style }) {
        delete vars[key];
        delete style[key];
      }
      handleChildMotionValue() {
        if (this.childSubscription) {
          this.childSubscription();
          delete this.childSubscription;
        }
        const { children } = this.props;
        if (isMotionValue(children)) {
          this.childSubscription = children.on("change", (latest) => {
            if (this.current) {
              this.current.textContent = `${latest}`;
            }
          });
        }
      }
    };
  }
});

// node_modules/framer-motion/dist/es/render/html/HTMLVisualElement.mjs
function getComputedStyle3(element) {
  return window.getComputedStyle(element);
}
var HTMLVisualElement;
var init_HTMLVisualElement = __esm({
  "node_modules/framer-motion/dist/es/render/html/HTMLVisualElement.mjs"() {
    init_build_styles();
    init_is_css_variable();
    init_transform();
    init_scrape_motion_values();
    init_render();
    init_defaults();
    init_measure();
    init_DOMVisualElement();
    HTMLVisualElement = class extends DOMVisualElement {
      constructor() {
        super(...arguments);
        this.type = "html";
        this.renderInstance = renderHTML;
      }
      readValueFromInstance(instance, key) {
        if (transformProps.has(key)) {
          const defaultType = getDefaultValueType(key);
          return defaultType ? defaultType.default || 0 : 0;
        } else {
          const computedStyle = getComputedStyle3(instance);
          const value = (isCSSVariableName(key) ? computedStyle.getPropertyValue(key) : computedStyle[key]) || 0;
          return typeof value === "string" ? value.trim() : value;
        }
      }
      measureInstanceViewportBox(instance, { transformPagePoint }) {
        return measureViewportBox(instance, transformPagePoint);
      }
      build(renderState, latestValues, props) {
        buildHTMLStyles(renderState, latestValues, props.transformTemplate);
      }
      scrapeMotionValuesFromProps(props, prevProps, visualElement) {
        return scrapeMotionValuesFromProps(props, prevProps, visualElement);
      }
    };
  }
});

// node_modules/framer-motion/dist/es/render/svg/SVGVisualElement.mjs
var SVGVisualElement;
var init_SVGVisualElement = __esm({
  "node_modules/framer-motion/dist/es/render/svg/SVGVisualElement.mjs"() {
    init_scrape_motion_values2();
    init_DOMVisualElement();
    init_build_attrs();
    init_camel_to_dash();
    init_camel_case_attrs();
    init_transform();
    init_render2();
    init_defaults();
    init_models();
    init_is_svg_tag();
    SVGVisualElement = class extends DOMVisualElement {
      constructor() {
        super(...arguments);
        this.type = "svg";
        this.isSVGTag = false;
        this.measureInstanceViewportBox = createBox;
      }
      getBaseTargetFromProps(props, key) {
        return props[key];
      }
      readValueFromInstance(instance, key) {
        if (transformProps.has(key)) {
          const defaultType = getDefaultValueType(key);
          return defaultType ? defaultType.default || 0 : 0;
        }
        key = !camelCaseAttributes.has(key) ? camelToDash(key) : key;
        return instance.getAttribute(key);
      }
      scrapeMotionValuesFromProps(props, prevProps, visualElement) {
        return scrapeMotionValuesFromProps2(props, prevProps, visualElement);
      }
      build(renderState, latestValues, props) {
        buildSVGAttrs(renderState, latestValues, this.isSVGTag, props.transformTemplate);
      }
      renderInstance(instance, renderState, styleProp, projection) {
        renderSVG(instance, renderState, styleProp, projection);
      }
      mount(instance) {
        this.isSVGTag = isSVGTag(instance.tagName);
        super.mount(instance);
      }
    };
  }
});

// node_modules/framer-motion/dist/es/render/dom/create-visual-element.mjs
var import_react252, createDomVisualElement;
var init_create_visual_element = __esm({
  "node_modules/framer-motion/dist/es/render/dom/create-visual-element.mjs"() {
    import_react252 = __toESM(require_react(), 1);
    init_HTMLVisualElement();
    init_SVGVisualElement();
    init_is_svg_component();
    createDomVisualElement = (Component2, options) => {
      return isSVGComponent(Component2) ? new SVGVisualElement(options) : new HTMLVisualElement(options, {
        allowProjection: Component2 !== import_react252.Fragment
      });
    };
  }
});

// node_modules/framer-motion/dist/es/render/components/motion/create.mjs
var createMotionComponent;
var init_create2 = __esm({
  "node_modules/framer-motion/dist/es/render/components/motion/create.mjs"() {
    init_animations();
    init_drag2();
    init_gestures();
    init_layout();
    init_create_factory();
    init_create_visual_element();
    createMotionComponent = /* @__PURE__ */ createMotionComponentFactory({
      ...animations,
      ...gestureAnimations,
      ...drag,
      ...layout
    }, createDomVisualElement);
  }
});

// node_modules/framer-motion/dist/es/render/components/motion/proxy.mjs
var motion;
var init_proxy = __esm({
  "node_modules/framer-motion/dist/es/render/components/motion/proxy.mjs"() {
    init_create_proxy();
    init_create2();
    motion = /* @__PURE__ */ createDOMMotionComponentProxy(createMotionComponent);
  }
});

// node_modules/framer-motion/dist/es/index.mjs
var init_es3 = __esm({
  "node_modules/framer-motion/dist/es/index.mjs"() {
    "use client";
    init_proxy();
    init_es();
    init_es2();
  }
});

// node_modules/lodash.debounce/index.js
var require_lodash2 = __commonJS({
  "node_modules/lodash.debounce/index.js"(exports, module) {
    var FUNC_ERROR_TEXT = "Expected a function";
    var NAN = 0 / 0;
    var symbolTag = "[object Symbol]";
    var reTrim = /^\s+|\s+$/g;
    var reIsBadHex = /^[-+]0x[0-9a-f]+$/i;
    var reIsBinary = /^0b[01]+$/i;
    var reIsOctal = /^0o[0-7]+$/i;
    var freeParseInt = parseInt;
    var freeGlobal2 = typeof global == "object" && global && global.Object === Object && global;
    var freeSelf2 = typeof self == "object" && self && self.Object === Object && self;
    var root5 = freeGlobal2 || freeSelf2 || Function("return this")();
    var objectProto3 = Object.prototype;
    var objectToString2 = objectProto3.toString;
    var nativeMax = Math.max;
    var nativeMin = Math.min;
    var now2 = function() {
      return root5.Date.now();
    };
    function debounce3(func, wait, options) {
      var lastArgs, lastThis, maxWait, result, timerId, lastCallTime, lastInvokeTime = 0, leading = false, maxing = false, trailing = true;
      if (typeof func != "function") {
        throw new TypeError(FUNC_ERROR_TEXT);
      }
      wait = toNumber(wait) || 0;
      if (isObject4(options)) {
        leading = !!options.leading;
        maxing = "maxWait" in options;
        maxWait = maxing ? nativeMax(toNumber(options.maxWait) || 0, wait) : maxWait;
        trailing = "trailing" in options ? !!options.trailing : trailing;
      }
      function invokeFunc(time3) {
        var args = lastArgs, thisArg = lastThis;
        lastArgs = lastThis = void 0;
        lastInvokeTime = time3;
        result = func.apply(thisArg, args);
        return result;
      }
      function leadingEdge(time3) {
        lastInvokeTime = time3;
        timerId = setTimeout(timerExpired, wait);
        return leading ? invokeFunc(time3) : result;
      }
      function remainingWait(time3) {
        var timeSinceLastCall = time3 - lastCallTime, timeSinceLastInvoke = time3 - lastInvokeTime, result2 = wait - timeSinceLastCall;
        return maxing ? nativeMin(result2, maxWait - timeSinceLastInvoke) : result2;
      }
      function shouldInvoke(time3) {
        var timeSinceLastCall = time3 - lastCallTime, timeSinceLastInvoke = time3 - lastInvokeTime;
        return lastCallTime === void 0 || timeSinceLastCall >= wait || timeSinceLastCall < 0 || maxing && timeSinceLastInvoke >= maxWait;
      }
      function timerExpired() {
        var time3 = now2();
        if (shouldInvoke(time3)) {
          return trailingEdge(time3);
        }
        timerId = setTimeout(timerExpired, remainingWait(time3));
      }
      function trailingEdge(time3) {
        timerId = void 0;
        if (trailing && lastArgs) {
          return invokeFunc(time3);
        }
        lastArgs = lastThis = void 0;
        return result;
      }
      function cancel() {
        if (timerId !== void 0) {
          clearTimeout(timerId);
        }
        lastInvokeTime = 0;
        lastArgs = lastCallTime = lastThis = timerId = void 0;
      }
      function flush() {
        return timerId === void 0 ? result : trailingEdge(now2());
      }
      function debounced() {
        var time3 = now2(), isInvoking = shouldInvoke(time3);
        lastArgs = arguments;
        lastThis = this;
        lastCallTime = time3;
        if (isInvoking) {
          if (timerId === void 0) {
            return leadingEdge(lastCallTime);
          }
          if (maxing) {
            timerId = setTimeout(timerExpired, wait);
            return invokeFunc(lastCallTime);
          }
        }
        if (timerId === void 0) {
          timerId = setTimeout(timerExpired, wait);
        }
        return result;
      }
      debounced.cancel = cancel;
      debounced.flush = flush;
      return debounced;
    }
    function isObject4(value) {
      var type2 = typeof value;
      return !!value && (type2 == "object" || type2 == "function");
    }
    function isObjectLike2(value) {
      return !!value && typeof value == "object";
    }
    function isSymbol(value) {
      return typeof value == "symbol" || isObjectLike2(value) && objectToString2.call(value) == symbolTag;
    }
    function toNumber(value) {
      if (typeof value == "number") {
        return value;
      }
      if (isSymbol(value)) {
        return NAN;
      }
      if (isObject4(value)) {
        var other = typeof value.valueOf == "function" ? value.valueOf() : value;
        value = isObject4(other) ? other + "" : other;
      }
      if (typeof value != "string") {
        return value === 0 ? value : +value;
      }
      value = value.replace(reTrim, "");
      var isBinary = reIsBinary.test(value);
      return isBinary || reIsOctal.test(value) ? freeParseInt(value.slice(2), isBinary ? 2 : 8) : reIsBadHex.test(value) ? NAN : +value;
    }
    module.exports = debounce3;
  }
});

// node_modules/usehooks-ts/dist/index.js
function useStep(maxStep) {
  const [currentStep, setCurrentStep] = (0, import_react253.useState)(1);
  const canGoToNextStep = currentStep + 1 <= maxStep;
  const canGoToPrevStep = currentStep - 1 > 0;
  const setStep = (0, import_react253.useCallback)(
    (step) => {
      const newStep = step instanceof Function ? step(currentStep) : step;
      if (newStep >= 1 && newStep <= maxStep) {
        setCurrentStep(newStep);
        return;
      }
      throw new Error("Step not valid");
    },
    [maxStep, currentStep]
  );
  const goToNextStep = (0, import_react253.useCallback)(() => {
    if (canGoToNextStep) {
      setCurrentStep((step) => step + 1);
    }
  }, [canGoToNextStep]);
  const goToPrevStep = (0, import_react253.useCallback)(() => {
    if (canGoToPrevStep) {
      setCurrentStep((step) => step - 1);
    }
  }, [canGoToPrevStep]);
  const reset2 = (0, import_react253.useCallback)(() => {
    setCurrentStep(1);
  }, []);
  return [
    currentStep,
    {
      goToNextStep,
      goToPrevStep,
      canGoToNextStep,
      canGoToPrevStep,
      setStep,
      reset: reset2
    }
  ];
}
function useTimeout(callback, delay3) {
  const savedCallback = (0, import_react253.useRef)(callback);
  useIsomorphicLayoutEffect2(() => {
    savedCallback.current = callback;
  }, [callback]);
  (0, import_react253.useEffect)(() => {
    if (!delay3 && delay3 !== 0) {
      return;
    }
    const id3 = setTimeout(() => {
      savedCallback.current();
    }, delay3);
    return () => {
      clearTimeout(id3);
    };
  }, [delay3]);
}
var import_react253, import_lodash, useIsomorphicLayoutEffect2;
var init_dist8 = __esm({
  "node_modules/usehooks-ts/dist/index.js"() {
    import_react253 = __toESM(require_react(), 1);
    import_lodash = __toESM(require_lodash2(), 1);
    useIsomorphicLayoutEffect2 = typeof window !== "undefined" ? import_react253.useLayoutEffect : import_react253.useEffect;
  }
});

// dist/server/chunks/chunk-D2VJvvfc.js
var button, prev, next, endText, s27;
var init_chunk_D2VJvvfc = __esm({
  "dist/server/chunks/chunk-D2VJvvfc.js"() {
    "use strict";
    button = "_button_1ntx3_1";
    prev = "_prev_1ntx3_21";
    next = "_next_1ntx3_25";
    endText = "_endText_1ntx3_37";
    s27 = {
      button,
      prev,
      next,
      endText
    };
  }
});

// dist/server/chunks/chunk-BBEzQH-L.js
var wrapper4, root3, hint, rephrase, s28;
var init_chunk_BBEzQH_L = __esm({
  "dist/server/chunks/chunk-BBEzQH-L.js"() {
    "use strict";
    wrapper4 = "_wrapper_pz879_1";
    root3 = "_root_pz879_11";
    hint = "_hint_pz879_24";
    rephrase = "_rephrase_pz879_37";
    s28 = {
      wrapper: wrapper4,
      root: root3,
      hint,
      rephrase
    };
  }
});

// dist/server/chunks/chunk-dx6TuK17.js
var radioRoot, s29;
var init_chunk_dx6TuK17 = __esm({
  "dist/server/chunks/chunk-dx6TuK17.js"() {
    "use strict";
    radioRoot = "_radioRoot_1a9lh_1";
    s29 = {
      radioRoot
    };
  }
});

// dist/server/chunks/chunk-D_xZIPI6.js
var wrapper5, radioWrapper, agreed, top, s30;
var init_chunk_D_xZIPI6 = __esm({
  "dist/server/chunks/chunk-D_xZIPI6.js"() {
    "use strict";
    wrapper5 = "_wrapper_yn3j2_1";
    radioWrapper = "_radioWrapper_yn3j2_7";
    agreed = "_agreed_yn3j2_38";
    top = "_top_yn3j2_53";
    s30 = {
      wrapper: wrapper5,
      radioWrapper,
      agreed,
      top
    };
  }
});

// dist/server/chunks/chunk-D2rqoRx3.js
var text2, s31;
var init_chunk_D2rqoRx3 = __esm({
  "dist/server/chunks/chunk-D2rqoRx3.js"() {
    "use strict";
    text2 = "_text_1s4o0_1";
    s31 = {
      text: text2
    };
  }
});

// dist/server/chunks/chunk-BQCpanuU.js
var input, s32;
var init_chunk_BQCpanuU = __esm({
  "dist/server/chunks/chunk-BQCpanuU.js"() {
    "use strict";
    input = "_input_16u8w_1";
    s32 = {
      input
    };
  }
});

// dist/server/chunks/chunk-Da3RY6hU.js
var wrap, root4, hint2, rephrase2, checkboxWrapper, s33;
var init_chunk_Da3RY6hU = __esm({
  "dist/server/chunks/chunk-Da3RY6hU.js"() {
    "use strict";
    wrap = "_wrap_15yh4_1";
    root4 = "_root_15yh4_8";
    hint2 = "_hint_15yh4_16";
    rephrase2 = "_rephrase_15yh4_24";
    checkboxWrapper = "_checkboxWrapper_15yh4_30";
    s33 = {
      wrap,
      root: root4,
      hint: hint2,
      rephrase: rephrase2,
      checkboxWrapper
    };
  }
});

// dist/server/chunks/chunk-CyrXnp8T.js
var wrapper6, stack2, s34;
var init_chunk_CyrXnp8T = __esm({
  "dist/server/chunks/chunk-CyrXnp8T.js"() {
    "use strict";
    wrapper6 = "_wrapper_1pero_1";
    stack2 = "_stack_1pero_13";
    s34 = {
      wrapper: wrapper6,
      stack: stack2
    };
  }
});

// dist/server/chunks/chunk-VuBd_7-9.js
var progress2, text3, s35;
var init_chunk_VuBd_7_9 = __esm({
  "dist/server/chunks/chunk-VuBd_7-9.js"() {
    "use strict";
    progress2 = "_progress_c4pxp_1";
    text3 = "_text_c4pxp_5";
    s35 = {
      progress: progress2,
      text: text3
    };
  }
});

// dist/server/entries/src_pages_test.mjs
var src_pages_test_exports = {};
__export(src_pages_test_exports, {
  configValuesSerialized: () => configValuesSerialized5
});
function Page5() {
  return /* @__PURE__ */ (0, import_jsx_runtime172.jsxs)(TestContainer, { children: [
    /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(TestFlow, {}),
    /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(TestControls, {}),
    /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(SubmitTest, {})
  ] });
}
var import_jsx_runtime172, import_react254, InputBorderless, SCALE_RADIO_ITEMS, useRephrasing, QuestionTitle, RadioElement, ScaleQuestion, AnswerLabel, useMultipleQuestionViewModel, MultipleQuestion, SingleQuestion, TestContainer, TestProgress, $preparedQuestions2, $currentPage$12, $currentValue$12, formPageChanged2, submitModalStateChanged$12, TestControls, $currentQuestion$12, $isLoadingState2, $isSubmitModalShown2, submitScaleForm2, submitModalStateChanged2, SubmitTest, $currentQuestion2, $currentPage2, $currentValue2, $currentProgress2, scaleFormFieldChanged2, TestFlow, import75, pageInitiated, import8, configValuesSerialized5;
var init_src_pages_test = __esm({
  "dist/server/entries/src_pages_test.mjs"() {
    "use strict";
    init_Loading();
    init_onRenderHtml();
    init_chunk_Y_L1G_uK();
    import_jsx_runtime172 = __toESM(require_jsx_runtime(), 1);
    init_chunk_Cbus1V3P();
    import_react254 = __toESM(require_react(), 1);
    init_chunk_Vlh7XtbV();
    init_clsx();
    init_esm2();
    init_chunk_BUtdu6VE();
    init_dist();
    init_effector_react();
    init_es3();
    init_lodash();
    init_dist8();
    init_chunk_D2VJvvfc();
    init_chunk_BzlClNuB();
    init_chunk_BBEzQH_L();
    init_chunk_dx6TuK17();
    init_chunk_D_xZIPI6();
    init_chunk_D2rqoRx3();
    init_chunk_BQCpanuU();
    init_chunk_Da3RY6hU();
    init_esm();
    init_chunk_CyrXnp8T();
    init_chunk_VuBd_7_9();
    init_effector();
    init_usePageContext();
    init_chunk_D3GmwNoI();
    init_core();
    init_chunk_BDsA7YFO();
    init_router();
    InputBorderless = ({
      value,
      onChange
    }) => /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(TextInput, { fz: 16, autoFocus: true, classNames: s32, value, onChange });
    SCALE_RADIO_ITEMS = [{
      value: "-3",
      size: "50px"
    }, {
      value: "-2",
      size: "40px"
    }, {
      value: "-1",
      size: "30px"
    }, {
      value: "0",
      size: "20px"
    }, {
      value: "1",
      size: "30px"
    }, {
      value: "2",
      size: "40px"
    }, {
      value: "3",
      size: "50px"
    }];
    useRephrasing = ({
      text: text4,
      rephrasing,
      hint: hint3
    }) => {
      const [currentStep, helpers] = useStep(3);
      const {
        canGoToNextStep,
        reset: reset2,
        goToNextStep
      } = helpers;
      const phrases = [{
        hint: hint3,
        text: text4
      }, ...rephrasing ?? []];
      const onRephrasingHandler = (0, import_react254.useCallback)(() => canGoToNextStep ? goToNextStep() : reset2(), [canGoToNextStep]);
      const currentPhrase = (0, import_react254.useMemo)(() => ({
        text: phrases[currentStep - 1].text,
        hint: phrases[currentStep - 1].hint
      }), [currentStep, phrases]);
      return {
        onRephrasingHandler,
        currentPhrase,
        phrases
      };
    };
    QuestionTitle = ({
      onRephrasing,
      text: text4,
      hint: hint3,
      phrases
    }) => {
      const isLarge = useIsLarge();
      return /* @__PURE__ */ (0, import_jsx_runtime172.jsxs)(Group, { className: s28.wrapper, gap: 0, align: "start", wrap: "nowrap", children: [
        /* @__PURE__ */ (0, import_jsx_runtime172.jsxs)(Stack, { gap: "sm", children: [
          /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(Title, { classNames: s28, order: 4, children: text4 }),
          /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(Text, { className: s28.hint, children: hint3 })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(ActionIcon, { hidden: phrases.length < 2, flex: isLarge ? 0 : 1, size: "lg", c: "dark.6", variant: "transparent", className: s28.rephrase, onClick: onRephrasing, children: /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(A2, { weight: "bold", size: "22px" }) })
      ] });
    };
    RadioElement = ({
      size: size4,
      value
    }) => {
      return /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(Radio, { className: s29.radioRoot, icon: IconCheck, size: size4, value });
    };
    ScaleQuestion = ({
      value,
      page,
      text: text4,
      hint: hint3,
      rephrasing,
      id: id3,
      onChange
    }) => {
      const [localValue, setLocalValue] = (0, import_react254.useState)("");
      const isLarge = useIsLarge();
      const [mounted, setIsMounted] = (0, import_react254.useState)(false);
      const {
        onRephrasingHandler,
        currentPhrase,
        phrases
      } = useRephrasing({
        hint: hint3,
        text: text4,
        rephrasing
      });
      (0, import_react254.useEffect)(() => {
        setLocalValue("");
        setIsMounted(true);
      }, [page]);
      (0, import_react254.useEffect)(() => {
        if (localValue !== "") {
          setIsMounted(false);
        }
      }, [localValue]);
      (0, import_react254.useEffect)(() => {
        if (!localValue) return;
        onChange({
          question: id3,
          answer: {
            value: Number(localValue)
          },
          index: page - 1
        });
      }, [localValue]);
      return /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(Transition, { mounted, transition: "pop", children: (transition) => /* @__PURE__ */ (0, import_jsx_runtime172.jsxs)(Paper, { className: s30.wrapper, style: transition, children: [
        /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(QuestionTitle, { phrases, text: currentPhrase.text, hint: currentPhrase.hint, onRephrasing: onRephrasingHandler }),
        /* @__PURE__ */ (0, import_jsx_runtime172.jsxs)(Stack, { pos: "relative", maw: 1145, m: "auto", gap: "xs", children: [
          /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(Radio.Group, { maw: isLarge ? 700 : "100%", m: isLarge ? "auto" : 0, name: id3, value: value ?? localValue, onChange: setLocalValue, children: /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(Flex, { gap: isLarge ? 50 : 0, className: s30.radioWrapper, justify: "space-between", children: SCALE_RADIO_ITEMS.map((radio) => /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(RadioElement, { size: radio.size, value: radio.value }, radio.value)) }) }),
          /* @__PURE__ */ (0, import_jsx_runtime172.jsxs)(Flex, { className: s30.agreedBlock, justify: "space-between", children: [
            /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(Text, { className: s30.agreed, c: "indigo.8", fw: 700, children: "\u041D\u0435 \u0441\u043E\u0433\u043B\u0430\u0441\u0435\u043D" }),
            /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(Text, { className: s30.agreed, c: "lime.8", fw: 700, children: "\u0421\u043E\u0433\u043B\u0430\u0441\u0435\u043D" })
          ] })
        ] })
      ] }) });
    };
    AnswerLabel = ({
      children
    }) => {
      return /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(Text, { className: s31.text, children });
    };
    useMultipleQuestionViewModel = ({
      onChange,
      options,
      page,
      id: id3,
      value
    }) => {
      const [input2, setInput] = (0, import_react254.useState)("");
      const [localValues, setLocalValues] = (0, import_react254.useState)([]);
      const [debounced] = useDebouncedValue(input2, 200);
      (0, import_react254.useEffect)(() => {
        const obj = [];
        if (localValues.length < 1) {
          onChange({
            question: id3,
            answer: obj,
            index: page - 1,
            isMultiple: true
          });
          return;
        }
        localValues.forEach((v5, idx) => {
          if (!options) return;
          if (localValues[idx] === options[(options == null ? void 0 : options.length) - 1].id) {
            return obj[idx] = {
              value: v5,
              input: debounced
            };
          }
          obj[idx] = {
            value: v5
          };
        });
        onChange({
          question: id3,
          answer: obj,
          index: page - 1,
          isMultiple: true
        });
      }, [localValues, debounced]);
      (0, import_react254.useEffect)(() => {
        var _a;
        setInput(((_a = value == null ? void 0 : value.find((el2) => el2.input)) == null ? void 0 : _a.input) ?? "");
        setLocalValues((value == null ? void 0 : value.map((el2) => el2.value)) ?? []);
      }, [id3, page]);
      return {
        localValues,
        input: input2,
        setLocalValues,
        setInput
      };
    };
    MultipleQuestion = ({
      options,
      value,
      page,
      text: text4,
      hint: hint3,
      rephrasing,
      id: id3,
      onChange
    }) => {
      var _a, _b;
      const {
        currentPhrase,
        onRephrasingHandler,
        phrases
      } = useRephrasing({
        hint: hint3,
        text: text4,
        rephrasing
      });
      const {
        localValues,
        input: input2,
        setLocalValues,
        setInput
      } = useMultipleQuestionViewModel({
        onChange,
        options,
        value,
        page,
        id: id3
      });
      const showInput = options && ((_a = value == null ? void 0 : value.map((el2) => el2.value)) == null ? void 0 : _a.includes(options[(options == null ? void 0 : options.length) - 1].id));
      const isLarge = useIsLarge();
      return /* @__PURE__ */ (0, import_jsx_runtime172.jsxs)(Paper, { mb: "5xl", children: [
        /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(QuestionTitle, { phrases, text: currentPhrase.text, hint: currentPhrase.hint, onRephrasing: onRephrasingHandler }),
        /* @__PURE__ */ (0, import_jsx_runtime172.jsxs)(Stack, { gap: "xs", className: s33.wrap, children: [
          /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(Checkbox.Group, { value: localValues.length ? localValues : (value == null ? void 0 : value.map((v5) => v5.value)) ?? localValues, onChange: setLocalValues, children: /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(Stack, { gap: "lg", className: s33.checkboxWrapper, children: options == null ? void 0 : options.map((option) => /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(Checkbox, { size: isLarge ? "32px" : "lg", radius: "xs", color: "lime.8", label: /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(AnswerLabel, { children: option.text }), value: option.id, icon: IconCheck }, option.id)) }) }),
          showInput && /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(InputBorderless, { value: input2.length > 0 ? input2 : (_b = value == null ? void 0 : value.find((el2) => el2.input)) == null ? void 0 : _b.input, onChange: (e14) => setInput(e14.target.value) })
        ] })
      ] });
    };
    SingleQuestion = ({
      options,
      value,
      page,
      text: text4,
      hint: hint3,
      rephrasing,
      id: id3,
      onChange
    }) => {
      var _a;
      const {
        currentPhrase,
        onRephrasingHandler,
        phrases
      } = useRephrasing({
        hint: hint3,
        text: text4,
        rephrasing
      });
      const [input2, setInput] = (0, import_react254.useState)("");
      const [localValue, setLocalValues] = (0, import_react254.useState)("");
      const [debounced] = useDebouncedValue(input2, 200);
      const showInput = (_a = options == null ? void 0 : options.find((el2) => el2.id === localValue)) == null ? void 0 : _a.requires_input;
      (0, import_react254.useEffect)(() => {
        if (value == null ? void 0 : value.value) {
          setLocalValues(value.value);
        }
        if (value == null ? void 0 : value.input) {
          setInput(value.input);
        }
      }, [value]);
      (0, import_react254.useEffect)(() => {
        let obj = {};
        if (!localValue) return;
        if (!options) return;
        if (showInput) {
          obj = {
            value: localValue,
            input: debounced
          };
        } else {
          obj = {
            value: localValue
          };
        }
        onChange({
          question: id3,
          answer: obj,
          index: page - 1,
          isSingle: true
        });
      }, [localValue, debounced, showInput]);
      const isLarge = useIsLarge();
      return /* @__PURE__ */ (0, import_jsx_runtime172.jsxs)(Paper, { mb: "5xl", children: [
        /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(QuestionTitle, { phrases, text: currentPhrase.text, hint: currentPhrase.hint, onRephrasing: onRephrasingHandler }),
        /* @__PURE__ */ (0, import_jsx_runtime172.jsxs)(Stack, { gap: "xs", className: s34.stack, children: [
          /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(Radio.Group, { name: id3, value: localValue, onChange: setLocalValues, children: /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(Stack, { gap: "lg", className: s34.wrapper, children: options == null ? void 0 : options.map((option) => /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(Radio, { color: "lime.8", value: option.id, size: isLarge ? "xl" : "lg", checked: localValue === option.id, label: /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(AnswerLabel, { children: option.text }) }, option.id)) }) }),
          showInput && /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(InputBorderless, { autoFocus: true, value: input2, onChange: (e14) => setInput(e14.target.value) })
        ] })
      ] });
    };
    TestContainer = ({
      children
    }) => /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(Box, { component: "section", children: /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(Container, { className: s26.container, children }) });
    TestProgress = ({
      value
    }) => {
      const isLarge = useIsLarge();
      return /* @__PURE__ */ (0, import_jsx_runtime172.jsxs)(Flex, { gap: "xl", mt: isLarge ? 60 : 0, mb: "lg", align: "center", h: "fit-content", children: [
        /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(Progress, { color: "violet.4", size: isLarge ? "xl" : "lg", className: s35.progress, value, transitionDuration: 200 }),
        /* @__PURE__ */ (0, import_jsx_runtime172.jsxs)(Text, { c: "dark.2", className: s35.text, children: [
          value,
          "%"
        ] })
      ] });
    };
    ({
      $preparedQuestions: $preparedQuestions2,
      $currentPage: $currentPage$12,
      $currentValue: $currentValue$12
    } = TestStores);
    ({
      formPageChanged: formPageChanged2,
      submitModalStateChanged: submitModalStateChanged$12
    } = TestEvents);
    TestControls = () => {
      const [visible2, setVisible] = (0, import_react254.useState)(false);
      const {
        questions,
        page,
        onChange,
        currentValue,
        controlModal
      } = c2({
        questions: $preparedQuestions2,
        page: $currentPage$12,
        onChange: formPageChanged2,
        currentValue: $currentValue$12,
        controlModal: submitModalStateChanged$12
      });
      const isExists = isArray_default(currentValue) ? currentValue.length > 0 : currentValue !== null;
      useTimeout(() => isExists ? setVisible(true) : setVisible(false), isExists ? 1e3 : 0);
      (0, import_react254.useEffect)(() => {
        if (isArray_default(currentValue) && currentValue.length > 0) {
          setVisible(true);
        }
      }, [currentValue]);
      if (!questions) return null;
      const isFirst = page === 1;
      const isLast = page === questions.length;
      return /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(Pagination.Root, { total: questions.length, mt: "auto", value: page, onChange, children: /* @__PURE__ */ (0, import_jsx_runtime172.jsxs)(Group, { justify: "space-between", pb: 20, children: [
        !isFirst && /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(Pagination.Previous, { disabled: false, className: clsx_default(s27.button, s27.prev), icon: () => /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(I3, { weight: "bold" }) }),
        /* @__PURE__ */ (0, import_jsx_runtime172.jsxs)(motion.div, { style: {
          marginLeft: "auto"
        }, initial: {
          opacity: 0
        }, animate: {
          opacity: 1
        }, exit: {
          opacity: 0
        }, children: [
          /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(Pagination.Next, { hidden: !visible2 || isLast, className: clsx_default(s27.button, s27.next), icon: () => /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(d8, { weight: "bold" }) }),
          /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(Button, { fw: "700", fz: 16, c: "dark.6", variant: "subtle", hidden: !isLast, className: s27.end, onClick: controlModal, rightSection: /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(d8, { weight: "bold" }), children: /* @__PURE__ */ (0, import_jsx_runtime172.jsx)("span", { className: s27.endText, children: "\u0417\u0430\u0432\u0435\u0440\u0448\u0438\u0442\u044C" }) })
        ] })
      ] }) });
    };
    ({
      $currentQuestion: $currentQuestion$12,
      $isLoadingState: $isLoadingState2,
      $isSubmitModalShown: $isSubmitModalShown2
    } = TestStores);
    ({
      submitScaleForm: submitScaleForm2,
      submitModalStateChanged: submitModalStateChanged2
    } = TestEvents);
    SubmitTest = () => {
      const {
        onSubmit,
        isOpen,
        controlModal,
        pending: pending2
      } = c2({
        question: $currentQuestion$12,
        onSubmit: submitScaleForm2,
        isOpen: $isSubmitModalShown2,
        controlModal: submitModalStateChanged2,
        pending: $isLoadingState2
      });
      return /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(SubmitTestModal, { loading: pending2, onSubmit, opened: isOpen, onClose: controlModal });
    };
    ({
      $currentQuestion: $currentQuestion2,
      $currentPage: $currentPage2,
      $currentValue: $currentValue2,
      $currentProgress: $currentProgress2
    } = TestStores);
    ({
      scaleFormFieldChanged: scaleFormFieldChanged2
    } = TestEvents);
    TestFlow = () => {
      const {
        question,
        onChange,
        page,
        currentValue,
        progress: progress3
      } = c2({
        question: $currentQuestion2,
        onChange: scaleFormFieldChanged2,
        page: $currentPage2,
        currentValue: $currentValue2,
        progress: $currentProgress2
      });
      if (!question) return null;
      const Map2 = {
        scale: /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(ScaleQuestion, { ...question, value: String(currentValue), page, onChange }),
        multiple_choice: question.options && /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(MultipleQuestion, { ...question, page, onChange, value: isArray_default(currentValue) ? currentValue : null }),
        single_choice: question.options && /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(SingleQuestion, { ...question, page, onChange, value: currentValue })
      };
      return /* @__PURE__ */ (0, import_jsx_runtime172.jsxs)(import_jsx_runtime172.Fragment, { children: [
        /* @__PURE__ */ (0, import_jsx_runtime172.jsx)(TestProgress, { value: progress3 }),
        Map2[question.type]
      ] });
    };
    import75 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
      __proto__: null,
      default: Page5
    }, Symbol.toStringTag, { value: "Module" }));
    pageInitiated = createPageInit();
    x({
      and: [{
        clock: pageInitiated,
        target: getQuestionsQuery.start
      }],
      or: {
        sid: "5ayzqy"
      }
    });
    import8 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
      __proto__: null,
      pageInitiated
    }, Symbol.toStringTag, { value: "Module" }));
    configValuesSerialized5 = {
      ["isClientRuntimeLoaded"]: {
        type: "computed",
        definedAtData: null,
        valueSerialized: {
          type: "js-serialized",
          value: true
        }
      },
      ["Loading"]: {
        type: "standard",
        definedAtData: { "filePathToShowToUser": "vike-react/__internal/integration/Loading", "fileExportPathToShowToUser": [] },
        valueSerialized: {
          type: "pointer-import",
          value: Loading_default
        }
      },
      ["onRenderHtml"]: {
        type: "standard",
        definedAtData: { "filePathToShowToUser": "vike-react/__internal/integration/onRenderHtml", "fileExportPathToShowToUser": [] },
        valueSerialized: {
          type: "pointer-import",
          value: onRenderHtml
        }
      },
      ["Wrapper"]: {
        type: "cumulative",
        definedAtData: [{ "filePathToShowToUser": "/src/pages/+Wrapper.tsx", "fileExportPathToShowToUser": [] }],
        valueSerialized: [{
          type: "plus-file",
          exportValues: import3
        }]
      },
      ["passToClient"]: {
        type: "cumulative",
        definedAtData: [{ "filePathToShowToUser": "/src/pages/+config.ts", "fileExportPathToShowToUser": ["default", "passToClient"] }, { "filePathToShowToUser": "vike-react/config", "fileExportPathToShowToUser": ["default", "passToClient"] }],
        valueSerialized: [{
          type: "js-serialized",
          value: ["scopeValues"]
        }, {
          type: "js-serialized",
          value: ["_configFromHook"]
        }]
      },
      ["cacheControl"]: {
        type: "standard",
        definedAtData: { "filePathToShowToUser": "/src/pages/+config.ts", "fileExportPathToShowToUser": ["default", "cacheControl"] },
        valueSerialized: {
          type: "js-serialized",
          value: "public, max-age=604800"
        }
      },
      ["title"]: {
        type: "standard",
        definedAtData: { "filePathToShowToUser": "/src/pages/+config.ts", "fileExportPathToShowToUser": ["default", "title"] },
        valueSerialized: {
          type: "js-serialized",
          value: "Cognitive Lab"
        }
      },
      ["lang"]: {
        type: "standard",
        definedAtData: { "filePathToShowToUser": "/src/pages/+config.ts", "fileExportPathToShowToUser": ["default", "lang"] },
        valueSerialized: {
          type: "js-serialized",
          value: "ru"
        }
      },
      ["reactStrictMode"]: {
        type: "standard",
        definedAtData: { "filePathToShowToUser": "/src/pages/+config.ts", "fileExportPathToShowToUser": ["default", "reactStrictMode"] },
        valueSerialized: {
          type: "js-serialized",
          value: false
        }
      },
      ["onBeforeRender"]: {
        type: "standard",
        definedAtData: { "filePathToShowToUser": "/src/pages/+onBeforeRender.ts", "fileExportPathToShowToUser": [] },
        valueSerialized: {
          type: "plus-file",
          exportValues: import4
        }
      },
      ["Head"]: {
        type: "cumulative",
        definedAtData: [{ "filePathToShowToUser": "/src/widgets/HeadDefault/index.tsx", "fileExportPathToShowToUser": ["HeadDefault"] }],
        valueSerialized: [{
          type: "pointer-import",
          value: HeadDefault
        }]
      },
      ["Layout"]: {
        type: "cumulative",
        definedAtData: [{ "filePathToShowToUser": "/src/widgets/RootLayout/index.tsx", "fileExportPathToShowToUser": ["RootLayout"] }],
        valueSerialized: [{
          type: "pointer-import",
          value: RootLayout
        }]
      },
      ["Page"]: {
        type: "standard",
        definedAtData: { "filePathToShowToUser": "/src/pages/test/+Page.tsx", "fileExportPathToShowToUser": [] },
        valueSerialized: {
          type: "plus-file",
          exportValues: import75
        }
      },
      ["pageInitiated"]: {
        type: "standard",
        definedAtData: { "filePathToShowToUser": "/src/pages/test/+pageInitiated.ts", "fileExportPathToShowToUser": [] },
        valueSerialized: {
          type: "plus-file",
          exportValues: import8
        }
      }
    };
  }
});

// dist/server/entry.mjs
var entry_exports = {};
var pageFilesLazy, pageFilesEager, pageFilesExportNamesLazy, pageFilesExportNamesEager, pageFilesList, neverLoaded, pageConfigsSerialized, pageConfigGlobalSerialized, pageFilesLazyIsomorph1, pageFilesLazyIsomorph, pageFilesExportNamesEagerIsomorph1, pageFilesExportNamesEagerIsomorph, pageFilesLazyServer1, pageFilesLazyServer, pageFilesExportNamesEagerServer1, pageFilesExportNamesEagerServer, pageFilesEagerRoute1, pageFilesEagerRoute, pageFilesExportNamesEagerClient1, pageFilesExportNamesEagerClient, pageFiles;
var init_entry = __esm({
  "dist/server/entry.mjs"() {
    "use strict";
    init_loadImportBuild();
    pageFilesLazy = {};
    pageFilesEager = {};
    pageFilesExportNamesLazy = {};
    pageFilesExportNamesEager = {};
    pageFilesList = [];
    neverLoaded = {};
    pageConfigsSerialized = [
      {
        pageId: "/src/pages/_error",
        isErrorPage: true,
        routeFilesystem: void 0,
        loadConfigValuesAll: () => Promise.resolve().then(() => (init_src_pages_error(), src_pages_error_exports)),
        configValuesSerialized: {
          ["isClientRuntimeLoaded"]: {
            type: "computed",
            definedAtData: null,
            valueSerialized: {
              type: "js-serialized",
              value: true
            }
          },
          ["clientRouting"]: {
            type: "standard",
            definedAtData: { "filePathToShowToUser": "vike-react/config", "fileExportPathToShowToUser": ["default", "clientRouting"] },
            valueSerialized: {
              type: "js-serialized",
              value: true
            }
          }
        }
      },
      {
        pageId: "/src/pages/free-report",
        isErrorPage: void 0,
        routeFilesystem: { "routeString": "/free-report", "definedBy": "/src/pages/free-report/" },
        loadConfigValuesAll: () => Promise.resolve().then(() => (init_src_pages_free_report(), src_pages_free_report_exports)),
        configValuesSerialized: {
          ["isClientRuntimeLoaded"]: {
            type: "computed",
            definedAtData: null,
            valueSerialized: {
              type: "js-serialized",
              value: true
            }
          },
          ["clientRouting"]: {
            type: "standard",
            definedAtData: { "filePathToShowToUser": "vike-react/config", "fileExportPathToShowToUser": ["default", "clientRouting"] },
            valueSerialized: {
              type: "js-serialized",
              value: true
            }
          }
        }
      },
      {
        pageId: "/src/pages/index",
        isErrorPage: void 0,
        routeFilesystem: { "routeString": "/", "definedBy": "/src/pages/index/" },
        loadConfigValuesAll: () => Promise.resolve().then(() => (init_src_pages_index(), src_pages_index_exports)),
        configValuesSerialized: {
          ["isClientRuntimeLoaded"]: {
            type: "computed",
            definedAtData: null,
            valueSerialized: {
              type: "js-serialized",
              value: true
            }
          },
          ["clientRouting"]: {
            type: "standard",
            definedAtData: { "filePathToShowToUser": "vike-react/config", "fileExportPathToShowToUser": ["default", "clientRouting"] },
            valueSerialized: {
              type: "js-serialized",
              value: true
            }
          }
        }
      },
      {
        pageId: "/src/pages/purchase-report",
        isErrorPage: void 0,
        routeFilesystem: { "routeString": "/purchase-report", "definedBy": "/src/pages/purchase-report/" },
        loadConfigValuesAll: () => Promise.resolve().then(() => (init_src_pages_purchase_report(), src_pages_purchase_report_exports)),
        configValuesSerialized: {
          ["isClientRuntimeLoaded"]: {
            type: "computed",
            definedAtData: null,
            valueSerialized: {
              type: "js-serialized",
              value: true
            }
          },
          ["clientRouting"]: {
            type: "standard",
            definedAtData: { "filePathToShowToUser": "vike-react/config", "fileExportPathToShowToUser": ["default", "clientRouting"] },
            valueSerialized: {
              type: "js-serialized",
              value: true
            }
          }
        }
      },
      {
        pageId: "/src/pages/test",
        isErrorPage: void 0,
        routeFilesystem: { "routeString": "/test", "definedBy": "/src/pages/test/" },
        loadConfigValuesAll: () => Promise.resolve().then(() => (init_src_pages_test(), src_pages_test_exports)),
        configValuesSerialized: {
          ["isClientRuntimeLoaded"]: {
            type: "computed",
            definedAtData: null,
            valueSerialized: {
              type: "js-serialized",
              value: true
            }
          },
          ["clientRouting"]: {
            type: "standard",
            definedAtData: { "filePathToShowToUser": "vike-react/config", "fileExportPathToShowToUser": ["default", "clientRouting"] },
            valueSerialized: {
              type: "js-serialized",
              value: true
            }
          }
        }
      }
    ];
    pageConfigGlobalSerialized = {
      configValuesSerialized: {}
    };
    pageFilesLazyIsomorph1 = /* @__PURE__ */ Object.assign({});
    pageFilesLazyIsomorph = { ...pageFilesLazyIsomorph1 };
    pageFilesLazy[".page"] = pageFilesLazyIsomorph;
    pageFilesExportNamesEagerIsomorph1 = /* @__PURE__ */ Object.assign({});
    pageFilesExportNamesEagerIsomorph = { ...pageFilesExportNamesEagerIsomorph1 };
    pageFilesExportNamesEager[".page"] = pageFilesExportNamesEagerIsomorph;
    pageFilesLazyServer1 = /* @__PURE__ */ Object.assign({});
    pageFilesLazyServer = { ...pageFilesLazyServer1 };
    pageFilesLazy[".page.server"] = pageFilesLazyServer;
    pageFilesExportNamesEagerServer1 = /* @__PURE__ */ Object.assign({});
    pageFilesExportNamesEagerServer = { ...pageFilesExportNamesEagerServer1 };
    pageFilesExportNamesEager[".page.server"] = pageFilesExportNamesEagerServer;
    pageFilesEagerRoute1 = /* @__PURE__ */ Object.assign({});
    pageFilesEagerRoute = { ...pageFilesEagerRoute1 };
    pageFilesEager[".page.route"] = pageFilesEagerRoute;
    pageFilesExportNamesEagerClient1 = /* @__PURE__ */ Object.assign({});
    pageFilesExportNamesEagerClient = { ...pageFilesExportNamesEagerClient1 };
    pageFilesExportNamesEager[".page.client"] = pageFilesExportNamesEagerClient;
    pageFiles = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
      __proto__: null,
      neverLoaded,
      pageConfigGlobalSerialized,
      pageConfigsSerialized,
      pageFilesEager,
      pageFilesExportNamesEager,
      pageFilesExportNamesLazy,
      pageFilesLazy,
      pageFilesList
    }, Symbol.toStringTag, { value: "Module" }));
    {
      const assetsManifest = {
        "_chunk-!~{007}~.js": {
          "file": "assets/static/src_app_styles_index-fd12f9be.rL321cbO.css",
          "src": "_chunk-!~{007}~.js"
        },
        "_chunk-!~{008}~.js": {
          "file": "assets/static/src_entities_Report_ui_BarChart_BarChart.Cp7EXhRx.css",
          "src": "_chunk-!~{008}~.js"
        },
        "_chunk-!~{009}~.js": {
          "file": "assets/static/src_entities_Report_ui_FilledBulletList_FilledBulletList.C1fDojTG.css",
          "src": "_chunk-!~{009}~.js"
        },
        "_chunk-!~{00a}~.js": {
          "file": "assets/static/src_entities_Report_ui_IconList_IconList.UTU9HnS1.css",
          "src": "_chunk-!~{00a}~.js"
        },
        "_chunk-!~{00b}~.js": {
          "file": "assets/static/src_entities_Report_ui_Paragraph_Paragraph.NU27Prjb.css",
          "src": "_chunk-!~{00b}~.js"
        },
        "_chunk-!~{00c}~.js": {
          "file": "assets/static/src_entities_Test_ui_AnswerLabel_AnswerLabel.DMmq4EDr.css",
          "src": "_chunk-!~{00c}~.js"
        },
        "_chunk-!~{00d}~.js": {
          "file": "assets/static/src_entities_Test_ui_MultipleQuestion_MultipleQuestion.CULDix2F.css",
          "src": "_chunk-!~{00d}~.js"
        },
        "_chunk-!~{00e}~.js": {
          "file": "assets/static/src_entities_Test_ui_QuestionTitle_QuestionTitle.BHUHhKKt.css",
          "src": "_chunk-!~{00e}~.js"
        },
        "_chunk-!~{00f}~.js": {
          "file": "assets/static/src_entities_Test_ui_RadioElement_RadioElement.5Apkl2ZM.css",
          "src": "_chunk-!~{00f}~.js"
        },
        "_chunk-!~{00g}~.js": {
          "file": "assets/static/src_entities_Test_ui_ScaleQuestion_ScaleQuestion.rUUi4hNP.css",
          "src": "_chunk-!~{00g}~.js"
        },
        "_chunk-!~{00h}~.js": {
          "file": "assets/static/src_entities_Test_ui_SingleQuestion_SingleQuestion.BD0PLrMz.css",
          "src": "_chunk-!~{00h}~.js"
        },
        "_chunk-!~{00i}~.js": {
          "file": "assets/static/src_entities_Test_ui_TestContainer_TestContainer.DhU-bDgf.css",
          "src": "_chunk-!~{00i}~.js"
        },
        "_chunk-!~{00j}~.js": {
          "file": "assets/static/src_entities_Test_ui_TestProgress_TestProgress.CITWBiK1.css",
          "src": "_chunk-!~{00j}~.js"
        },
        "_chunk-!~{00k}~.js": {
          "file": "assets/static/src_features_SendReportEmail_SendReportEmail.BQzfTVjx.css",
          "src": "_chunk-!~{00k}~.js"
        },
        "_chunk-!~{00l}~.js": {
          "file": "assets/static/src_features_TestControls_TestControls.3ekmfEQZ.css",
          "src": "_chunk-!~{00l}~.js"
        },
        "_chunk-!~{00m}~.js": {
          "file": "assets/static/src_pages_purchase-report_PurchaseReportPage.BUi4TkvD.css",
          "src": "_chunk-!~{00m}~.js"
        },
        "_chunk-!~{00n}~.js": {
          "file": "assets/static/src_shared_ui_BackButton_BackButton.CffSITK_.css",
          "src": "_chunk-!~{00n}~.js"
        },
        "_chunk-!~{00o}~.js": {
          "file": "assets/static/src_shared_ui_InnerContainer_InnerContainer.DnFkmxHw.css",
          "src": "_chunk-!~{00o}~.js"
        },
        "_chunk-!~{00p}~.js": {
          "file": "assets/static/src_shared_ui_InputBorderless_InputBorderless.C9uL0j1h.css",
          "src": "_chunk-!~{00p}~.js"
        },
        "_chunk-!~{00q}~.js": {
          "file": "assets/static/src_widgets_BuyReport_ReportBuyForm_ReportBuyForm.0S66SZIZ.css",
          "src": "_chunk-!~{00q}~.js"
        },
        "_chunk-!~{00r}~.js": {
          "file": "assets/static/src_widgets_BuyReport_ReportPriceInfo_ReportPriceInfo.B5guUdZ5.css",
          "src": "_chunk-!~{00r}~.js"
        },
        "_chunk-!~{00s}~.js": {
          "file": "assets/static/src_widgets_ContentResolver_ContentResolver.CFO6099h.css",
          "src": "_chunk-!~{00s}~.js"
        },
        "_chunk-!~{00t}~.js": {
          "file": "assets/static/src_widgets_ReportHeader_ReportHeader.uy7wcq6x.css",
          "src": "_chunk-!~{00t}~.js"
        },
        "_chunk-!~{00u}~.js": {
          "file": "assets/static/src_widgets_ReportNavigation_ReportNavigation.C4xTS4Ke.css",
          "src": "_chunk-!~{00u}~.js"
        },
        "_chunk-!~{00v}~.js": {
          "file": "assets/static/src_widgets_RootLayout_ui_Header_Header.DT_8umEw.css",
          "src": "_chunk-!~{00v}~.js"
        },
        "_chunk-!~{00w}~.js": {
          "file": "assets/static/vike-react-b64a028b.BcWtY8Ol.css",
          "src": "_chunk-!~{00w}~.js"
        },
        "_chunk-64G7xTOu.js": {
          "file": "assets/chunks/chunk-64G7xTOu.js",
          "name": "src_shared_ui_InnerContainer_InnerContainer.module-2f16f634",
          "css": [
            "assets/static/src_shared_ui_InnerContainer_InnerContainer.DnFkmxHw.css"
          ]
        },
        "_chunk-B-7x8vzC.js": {
          "file": "assets/chunks/chunk-B-7x8vzC.js",
          "name": "src_entities_Test_ui_RadioElement_RadioElement.module-bea969b5",
          "css": [
            "assets/static/src_entities_Test_ui_RadioElement_RadioElement.5Apkl2ZM.css"
          ]
        },
        "_chunk-BMEtLDV2.js": {
          "file": "assets/chunks/chunk-BMEtLDV2.js",
          "name": "src_entities_Test_ui_QuestionTitle_QuestionTitle.module-4bcad1a4",
          "css": [
            "assets/static/src_entities_Test_ui_QuestionTitle_QuestionTitle.BHUHhKKt.css"
          ]
        },
        "_chunk-BMrSlKbf.js": {
          "file": "assets/chunks/chunk-BMrSlKbf.js",
          "name": "src_widgets_BuyReport_ReportPriceInfo_ReportPriceInfo.module-907966de",
          "css": [
            "assets/static/src_widgets_BuyReport_ReportPriceInfo_ReportPriceInfo.B5guUdZ5.css"
          ]
        },
        "_chunk-BNzThrt5.js": {
          "file": "assets/chunks/chunk-BNzThrt5.js",
          "name": "src_entities_Test_ui_SingleQuestion_SingleQuestion.module-16a61d89",
          "css": [
            "assets/static/src_entities_Test_ui_SingleQuestion_SingleQuestion.BD0PLrMz.css"
          ]
        },
        "_chunk-BOK1Suzk.js": {
          "file": "assets/chunks/chunk-BOK1Suzk.js",
          "name": "src_features_SendReportEmail_SendReportEmail.module-f58d5aab",
          "css": [
            "assets/static/src_features_SendReportEmail_SendReportEmail.BQzfTVjx.css"
          ]
        },
        "_chunk-BS2FAdj6.js": {
          "file": "assets/chunks/chunk-BS2FAdj6.js",
          "name": "src_widgets_RootLayout_ui_Header_Header.module-2c07e7f4",
          "css": [
            "assets/static/src_widgets_RootLayout_ui_Header_Header.DT_8umEw.css"
          ]
        },
        "_chunk-BbMBgJ5h.js": {
          "file": "assets/chunks/chunk-BbMBgJ5h.js",
          "name": "src_shared_ui_BackButton_BackButton.module-b9460a9f",
          "css": [
            "assets/static/src_shared_ui_BackButton_BackButton.CffSITK_.css"
          ]
        },
        "_chunk-Bjao3PSY.js": {
          "file": "assets/chunks/chunk-Bjao3PSY.js",
          "name": "src_entities_Test_ui_TestContainer_TestContainer.module-5d036018",
          "css": [
            "assets/static/src_entities_Test_ui_TestContainer_TestContainer.DhU-bDgf.css"
          ]
        },
        "_chunk-Bk3K2m6c.js": {
          "file": "assets/chunks/chunk-Bk3K2m6c.js",
          "name": "navigate",
          "imports": [
            "_chunk-Z2Rw1ehA.js",
            "_chunk-DXMN5A6d.js",
            "_chunk-C78j02Lg.js"
          ]
        },
        "_chunk-BqOsCu5i.js": {
          "file": "assets/chunks/chunk-BqOsCu5i.js",
          "name": "src_entities_Report_ui_IconList_IconList.module-eada0e91",
          "css": [
            "assets/static/src_entities_Report_ui_IconList_IconList.UTU9HnS1.css"
          ]
        },
        "_chunk-BuvHUZPa.js": {
          "file": "assets/chunks/chunk-BuvHUZPa.js",
          "name": "src_entities_Report_ui_Paragraph_Paragraph.module-c5c61b4f",
          "css": [
            "assets/static/src_entities_Report_ui_Paragraph_Paragraph.NU27Prjb.css"
          ]
        },
        "_chunk-C78j02Lg.js": {
          "file": "assets/chunks/chunk-C78j02Lg.js",
          "name": "initOnPopState",
          "imports": [
            "_chunk-DXMN5A6d.js"
          ]
        },
        "_chunk-C8-F7eMu.js": {
          "file": "assets/chunks/chunk-C8-F7eMu.js",
          "name": "src_features_TestControls_TestControls.module-a8e5b4bd",
          "css": [
            "assets/static/src_features_TestControls_TestControls.3ekmfEQZ.css"
          ]
        },
        "_chunk-CAlJSGON.js": {
          "file": "assets/chunks/chunk-CAlJSGON.js",
          "name": "ArrowLeft",
          "imports": [
            "_chunk-Z2Rw1ehA.js",
            "_chunk-CXYSvpFE.js"
          ]
        },
        "_chunk-CEr3l8uZ.js": {
          "file": "assets/chunks/chunk-CEr3l8uZ.js",
          "name": "src_entities_Test_ui_TestProgress_TestProgress.module-004b81d2",
          "css": [
            "assets/static/src_entities_Test_ui_TestProgress_TestProgress.CITWBiK1.css"
          ]
        },
        "_chunk-CIBkxxtg.js": {
          "file": "assets/chunks/chunk-CIBkxxtg.js",
          "name": "index",
          "imports": [
            "_chunk-Z2Rw1ehA.js",
            "_chunk-CAlJSGON.js",
            "_chunk-BbMBgJ5h.js",
            "_chunk-CXYSvpFE.js"
          ]
        },
        "_chunk-CXYSvpFE.js": {
          "file": "assets/chunks/chunk-CXYSvpFE.js",
          "name": "index",
          "imports": [
            "_chunk-Z2Rw1ehA.js"
          ]
        },
        "_chunk-CsXiY2pl.js": {
          "file": "assets/chunks/chunk-CsXiY2pl.js",
          "name": "index",
          "imports": [
            "_chunk-Z2Rw1ehA.js"
          ]
        },
        "_chunk-Ctmyzjzw.js": {
          "file": "assets/chunks/chunk-Ctmyzjzw.js",
          "name": "index",
          "imports": [
            "_chunk-Z2Rw1ehA.js",
            "_chunk-CXYSvpFE.js",
            "_chunk-Bk3K2m6c.js",
            "_chunk-D9seXHe0.js",
            "_chunk-C78j02Lg.js"
          ]
        },
        "_chunk-D9seXHe0.js": {
          "file": "assets/chunks/chunk-D9seXHe0.js",
          "name": "index",
          "imports": [
            "_chunk-Z2Rw1ehA.js"
          ]
        },
        "_chunk-DTBjtrD8.js": {
          "file": "assets/chunks/chunk-DTBjtrD8.js",
          "name": "src_widgets_ReportHeader_ReportHeader.module-e744868c",
          "css": [
            "assets/static/src_widgets_ReportHeader_ReportHeader.uy7wcq6x.css"
          ]
        },
        "_chunk-DUZZMUrQ.js": {
          "file": "assets/chunks/chunk-DUZZMUrQ.js",
          "name": "src_widgets_ContentResolver_ContentResolver.module-e17f5f8b",
          "css": [
            "assets/static/src_widgets_ContentResolver_ContentResolver.CFO6099h.css"
          ]
        },
        "_chunk-DXMN5A6d.js": {
          "file": "assets/chunks/chunk-DXMN5A6d.js",
          "name": "executeHook"
        },
        "_chunk-DdRQpjQy.js": {
          "file": "assets/chunks/chunk-DdRQpjQy.js",
          "name": "src_entities_Test_ui_MultipleQuestion_MultipleQuestion.module-e5244730",
          "css": [
            "assets/static/src_entities_Test_ui_MultipleQuestion_MultipleQuestion.CULDix2F.css"
          ]
        },
        "_chunk-Dhe0Vrzr.js": {
          "file": "assets/chunks/chunk-Dhe0Vrzr.js",
          "name": "src_widgets_ReportNavigation_ReportNavigation.module-66fab8d2",
          "css": [
            "assets/static/src_widgets_ReportNavigation_ReportNavigation.C4xTS4Ke.css"
          ]
        },
        "_chunk-DkvQoWgU.js": {
          "file": "assets/chunks/chunk-DkvQoWgU.js",
          "name": "src_entities_Test_ui_AnswerLabel_AnswerLabel.module-2d5e1b7e",
          "css": [
            "assets/static/src_entities_Test_ui_AnswerLabel_AnswerLabel.DMmq4EDr.css"
          ]
        },
        "_chunk-Dl6e6cWP.js": {
          "file": "assets/chunks/chunk-Dl6e6cWP.js",
          "name": "src_pages_purchase-report_PurchaseReportPage.module-f0c4c7f6",
          "css": [
            "assets/static/src_pages_purchase-report_PurchaseReportPage.BUi4TkvD.css"
          ]
        },
        "_chunk-DmT-OBLA.js": {
          "file": "assets/chunks/chunk-DmT-OBLA.js",
          "name": "src_entities_Test_ui_ScaleQuestion_ScaleQuestion.module-b3fef66b",
          "css": [
            "assets/static/src_entities_Test_ui_ScaleQuestion_ScaleQuestion.rUUi4hNP.css"
          ]
        },
        "_chunk-DsrZrEw3.js": {
          "file": "assets/chunks/chunk-DsrZrEw3.js",
          "name": "index",
          "imports": [
            "_chunk-Z2Rw1ehA.js",
            "_chunk-64G7xTOu.js"
          ]
        },
        "_chunk-DyvFJec0.js": {
          "file": "assets/chunks/chunk-DyvFJec0.js",
          "name": "man-temp_large",
          "imports": [
            "_chunk-Z2Rw1ehA.js",
            "_chunk-Bk3K2m6c.js"
          ],
          "assets": [
            "assets/static/man-temp_large.C17Lahl-.webp"
          ]
        },
        "_chunk-RyBGSSJj.js": {
          "file": "assets/chunks/chunk-RyBGSSJj.js",
          "name": "src_shared_ui_InputBorderless_InputBorderless.module-fc01daf5",
          "css": [
            "assets/static/src_shared_ui_InputBorderless_InputBorderless.C9uL0j1h.css"
          ]
        },
        "_chunk-TrDyS8ji.js": {
          "file": "assets/chunks/chunk-TrDyS8ji.js",
          "name": "src_widgets_BuyReport_ReportBuyForm_ReportBuyForm.module-7c2cf409",
          "css": [
            "assets/static/src_widgets_BuyReport_ReportBuyForm_ReportBuyForm.0S66SZIZ.css"
          ]
        },
        "_chunk-Waq5p_I2.js": {
          "file": "assets/chunks/chunk-Waq5p_I2.js",
          "name": "_pageStarted",
          "imports": [
            "_chunk-Z2Rw1ehA.js",
            "_chunk-D9seXHe0.js"
          ]
        },
        "_chunk-Z2Rw1ehA.js": {
          "file": "assets/chunks/chunk-Z2Rw1ehA.js",
          "name": "index",
          "imports": [
            "_chunk-DXMN5A6d.js",
            "_chunk-BS2FAdj6.js"
          ],
          "css": [
            "assets/static/vike-react-b64a028b.BcWtY8Ol.css",
            "assets/static/src_app_styles_index-fd12f9be.rL321cbO.css"
          ],
          "assets": [
            "assets/static/raleway-v34-cyrillic_latin-regular.B2J1s-V4.woff2",
            "assets/static/raleway-v34-cyrillic_latin-500.CgpFJeFS.woff2",
            "assets/static/raleway-v34-cyrillic_latin-600.DRu2qh9T.woff2",
            "assets/static/raleway-v34-cyrillic_latin-700.CV4g2AhU.woff2",
            "assets/static/raleway-v34-cyrillic_latin-800.C2UAHJem.woff2"
          ]
        },
        "_chunk-osHy6G_S.js": {
          "file": "assets/chunks/chunk-osHy6G_S.js",
          "name": "src_entities_Report_ui_FilledBulletList_FilledBulletList.module-f94789d1",
          "css": [
            "assets/static/src_entities_Report_ui_FilledBulletList_FilledBulletList.C1fDojTG.css"
          ]
        },
        "_chunk-xJnKZTLz.js": {
          "file": "assets/chunks/chunk-xJnKZTLz.js",
          "name": "src_entities_Report_ui_BarChart_BarChart.module-f82d69e4",
          "css": [
            "assets/static/src_entities_Report_ui_BarChart_BarChart.Cp7EXhRx.css"
          ]
        },
        "node_modules/vike/dist/esm/client/client-routing-runtime/entry.js": {
          "file": "assets/entries/entry-client-routing.ZrJOZ8_z.js",
          "name": "entries/entry-client-routing",
          "src": "node_modules/vike/dist/esm/client/client-routing-runtime/entry.js",
          "isEntry": true,
          "imports": [
            "_chunk-C78j02Lg.js",
            "_chunk-DXMN5A6d.js"
          ],
          "dynamicImports": [
            "virtual:vike:pageConfigValuesAll:client:/src/pages/_error",
            "virtual:vike:pageConfigValuesAll:client:/src/pages/free-report",
            "virtual:vike:pageConfigValuesAll:client:/src/pages/index",
            "virtual:vike:pageConfigValuesAll:client:/src/pages/purchase-report",
            "virtual:vike:pageConfigValuesAll:client:/src/pages/test"
          ]
        },
        "src/app/assets/fonts/raleway-v34-cyrillic_latin-500.woff2": {
          "file": "assets/static/raleway-v34-cyrillic_latin-500.CgpFJeFS.woff2",
          "src": "src/app/assets/fonts/raleway-v34-cyrillic_latin-500.woff2"
        },
        "src/app/assets/fonts/raleway-v34-cyrillic_latin-600.woff2": {
          "file": "assets/static/raleway-v34-cyrillic_latin-600.DRu2qh9T.woff2",
          "src": "src/app/assets/fonts/raleway-v34-cyrillic_latin-600.woff2"
        },
        "src/app/assets/fonts/raleway-v34-cyrillic_latin-700.woff2": {
          "file": "assets/static/raleway-v34-cyrillic_latin-700.CV4g2AhU.woff2",
          "src": "src/app/assets/fonts/raleway-v34-cyrillic_latin-700.woff2"
        },
        "src/app/assets/fonts/raleway-v34-cyrillic_latin-800.woff2": {
          "file": "assets/static/raleway-v34-cyrillic_latin-800.C2UAHJem.woff2",
          "src": "src/app/assets/fonts/raleway-v34-cyrillic_latin-800.woff2"
        },
        "src/app/assets/fonts/raleway-v34-cyrillic_latin-regular.woff2": {
          "file": "assets/static/raleway-v34-cyrillic_latin-regular.B2J1s-V4.woff2",
          "src": "src/app/assets/fonts/raleway-v34-cyrillic_latin-regular.woff2"
        },
        "src/app/assets/icons/envelope.webp": {
          "file": "assets/static/envelope.fCgPZGsq.webp",
          "src": "src/app/assets/icons/envelope.webp"
        },
        "src/app/assets/icons/gem.webp": {
          "file": "assets/static/gem.ld0lVkQd.webp",
          "src": "src/app/assets/icons/gem.webp"
        },
        "src/app/assets/man-temp_large.webp": {
          "file": "assets/static/man-temp_large.C17Lahl-.webp",
          "src": "src/app/assets/man-temp_large.webp"
        },
        "src/app/assets/men-temp.webp": {
          "file": "assets/static/men-temp.e7uCjLN3.webp",
          "src": "src/app/assets/men-temp.webp"
        },
        "virtual:vike:pageConfigValuesAll:client:/src/pages/_error": {
          "file": "assets/entries/src_pages_error.kTXGZPE0.js",
          "name": "entries/src/pages/_error",
          "src": "virtual:vike:pageConfigValuesAll:client:/src/pages/_error",
          "isEntry": true,
          "isDynamicEntry": true,
          "imports": [
            "_chunk-Z2Rw1ehA.js",
            "_chunk-Waq5p_I2.js",
            "_chunk-CIBkxxtg.js",
            "_chunk-CXYSvpFE.js",
            "_chunk-DsrZrEw3.js",
            "_chunk-DXMN5A6d.js",
            "_chunk-BS2FAdj6.js",
            "_chunk-D9seXHe0.js",
            "_chunk-CAlJSGON.js",
            "_chunk-BbMBgJ5h.js",
            "_chunk-64G7xTOu.js"
          ],
          "css": [
            "assets/static/vike-react-b64a028b.BcWtY8Ol.css",
            "assets/static/src_app_styles_index-fd12f9be.rL321cbO.css"
          ],
          "assets": [
            "assets/static/raleway-v34-cyrillic_latin-regular.B2J1s-V4.woff2",
            "assets/static/raleway-v34-cyrillic_latin-500.CgpFJeFS.woff2",
            "assets/static/raleway-v34-cyrillic_latin-600.DRu2qh9T.woff2",
            "assets/static/raleway-v34-cyrillic_latin-700.CV4g2AhU.woff2",
            "assets/static/raleway-v34-cyrillic_latin-800.C2UAHJem.woff2",
            "assets/static/logo.2_7Lo9tV.svg"
          ]
        },
        "virtual:vike:pageConfigValuesAll:client:/src/pages/free-report": {
          "file": "assets/entries/src_pages_free-report.JWgQr19D.js",
          "name": "entries/src/pages/free-report",
          "src": "virtual:vike:pageConfigValuesAll:client:/src/pages/free-report",
          "isEntry": true,
          "isDynamicEntry": true,
          "imports": [
            "_chunk-Z2Rw1ehA.js",
            "_chunk-DyvFJec0.js",
            "_chunk-Bk3K2m6c.js",
            "_chunk-CXYSvpFE.js",
            "_chunk-CsXiY2pl.js",
            "_chunk-DsrZrEw3.js",
            "_chunk-BOK1Suzk.js",
            "_chunk-xJnKZTLz.js",
            "_chunk-BuvHUZPa.js",
            "_chunk-BqOsCu5i.js",
            "_chunk-osHy6G_S.js",
            "_chunk-DUZZMUrQ.js",
            "_chunk-DTBjtrD8.js",
            "_chunk-Dhe0Vrzr.js",
            "_chunk-C78j02Lg.js",
            "_chunk-DXMN5A6d.js",
            "_chunk-BS2FAdj6.js",
            "_chunk-64G7xTOu.js"
          ],
          "css": [
            "assets/static/vike-react-b64a028b.BcWtY8Ol.css",
            "assets/static/src_app_styles_index-fd12f9be.rL321cbO.css"
          ],
          "assets": [
            "assets/static/men-temp.e7uCjLN3.webp",
            "assets/static/envelope.fCgPZGsq.webp",
            "assets/static/gem.ld0lVkQd.webp",
            "assets/static/raleway-v34-cyrillic_latin-regular.B2J1s-V4.woff2",
            "assets/static/raleway-v34-cyrillic_latin-500.CgpFJeFS.woff2",
            "assets/static/raleway-v34-cyrillic_latin-600.DRu2qh9T.woff2",
            "assets/static/raleway-v34-cyrillic_latin-700.CV4g2AhU.woff2",
            "assets/static/raleway-v34-cyrillic_latin-800.C2UAHJem.woff2",
            "assets/static/logo.2_7Lo9tV.svg"
          ]
        },
        "virtual:vike:pageConfigValuesAll:client:/src/pages/index": {
          "file": "assets/entries/src_pages_index.BkDd7RvH.js",
          "name": "entries/src/pages/index",
          "src": "virtual:vike:pageConfigValuesAll:client:/src/pages/index",
          "isEntry": true,
          "isDynamicEntry": true,
          "imports": [
            "_chunk-Z2Rw1ehA.js",
            "_chunk-Waq5p_I2.js",
            "_chunk-DXMN5A6d.js",
            "_chunk-BS2FAdj6.js",
            "_chunk-D9seXHe0.js"
          ],
          "css": [
            "assets/static/vike-react-b64a028b.BcWtY8Ol.css",
            "assets/static/src_app_styles_index-fd12f9be.rL321cbO.css"
          ],
          "assets": [
            "assets/static/raleway-v34-cyrillic_latin-regular.B2J1s-V4.woff2",
            "assets/static/raleway-v34-cyrillic_latin-500.CgpFJeFS.woff2",
            "assets/static/raleway-v34-cyrillic_latin-600.DRu2qh9T.woff2",
            "assets/static/raleway-v34-cyrillic_latin-700.CV4g2AhU.woff2",
            "assets/static/raleway-v34-cyrillic_latin-800.C2UAHJem.woff2",
            "assets/static/logo.2_7Lo9tV.svg"
          ]
        },
        "virtual:vike:pageConfigValuesAll:client:/src/pages/purchase-report": {
          "file": "assets/entries/src_pages_purchase-report.CWvwgnsC.js",
          "name": "entries/src/pages/purchase-report",
          "src": "virtual:vike:pageConfigValuesAll:client:/src/pages/purchase-report",
          "isEntry": true,
          "isDynamicEntry": true,
          "imports": [
            "_chunk-Z2Rw1ehA.js",
            "_chunk-DyvFJec0.js",
            "_chunk-CIBkxxtg.js",
            "_chunk-CXYSvpFE.js",
            "_chunk-DsrZrEw3.js",
            "_chunk-Bk3K2m6c.js",
            "_chunk-TrDyS8ji.js",
            "_chunk-Ctmyzjzw.js",
            "_chunk-BMrSlKbf.js",
            "_chunk-Dl6e6cWP.js",
            "_chunk-DXMN5A6d.js",
            "_chunk-BS2FAdj6.js",
            "_chunk-CAlJSGON.js",
            "_chunk-BbMBgJ5h.js",
            "_chunk-64G7xTOu.js",
            "_chunk-C78j02Lg.js",
            "_chunk-D9seXHe0.js"
          ],
          "css": [
            "assets/static/vike-react-b64a028b.BcWtY8Ol.css",
            "assets/static/src_app_styles_index-fd12f9be.rL321cbO.css"
          ],
          "assets": [
            "assets/static/raleway-v34-cyrillic_latin-regular.B2J1s-V4.woff2",
            "assets/static/raleway-v34-cyrillic_latin-500.CgpFJeFS.woff2",
            "assets/static/raleway-v34-cyrillic_latin-600.DRu2qh9T.woff2",
            "assets/static/raleway-v34-cyrillic_latin-700.CV4g2AhU.woff2",
            "assets/static/raleway-v34-cyrillic_latin-800.C2UAHJem.woff2",
            "assets/static/logo.2_7Lo9tV.svg"
          ]
        },
        "virtual:vike:pageConfigValuesAll:client:/src/pages/test": {
          "file": "assets/entries/src_pages_test.CSuMMqoN.js",
          "name": "entries/src/pages/test",
          "src": "virtual:vike:pageConfigValuesAll:client:/src/pages/test",
          "isEntry": true,
          "isDynamicEntry": true,
          "imports": [
            "_chunk-Z2Rw1ehA.js",
            "_chunk-Ctmyzjzw.js",
            "_chunk-CXYSvpFE.js",
            "_chunk-Bjao3PSY.js",
            "_chunk-CAlJSGON.js",
            "_chunk-C8-F7eMu.js",
            "_chunk-CsXiY2pl.js",
            "_chunk-BMEtLDV2.js",
            "_chunk-Bk3K2m6c.js",
            "_chunk-B-7x8vzC.js",
            "_chunk-DmT-OBLA.js",
            "_chunk-DkvQoWgU.js",
            "_chunk-RyBGSSJj.js",
            "_chunk-DdRQpjQy.js",
            "_chunk-BNzThrt5.js",
            "_chunk-CEr3l8uZ.js",
            "_chunk-D9seXHe0.js",
            "_chunk-DXMN5A6d.js",
            "_chunk-BS2FAdj6.js",
            "_chunk-C78j02Lg.js"
          ],
          "css": [
            "assets/static/vike-react-b64a028b.BcWtY8Ol.css",
            "assets/static/src_app_styles_index-fd12f9be.rL321cbO.css"
          ],
          "assets": [
            "assets/static/raleway-v34-cyrillic_latin-regular.B2J1s-V4.woff2",
            "assets/static/raleway-v34-cyrillic_latin-500.CgpFJeFS.woff2",
            "assets/static/raleway-v34-cyrillic_latin-600.DRu2qh9T.woff2",
            "assets/static/raleway-v34-cyrillic_latin-700.CV4g2AhU.woff2",
            "assets/static/raleway-v34-cyrillic_latin-800.C2UAHJem.woff2",
            "assets/static/logo.2_7Lo9tV.svg"
          ]
        }
      };
      const pluginManifest = {
        "version": "0.4.206",
        "usesClientRouter": false,
        "baseServer": "/",
        "baseAssets": "/",
        "includeAssetsImportedByServer": true,
        "redirects": {},
        "trailingSlash": false,
        "disableUrlNormalization": false
      };
      setImportBuildGetters({
        pageFiles: () => pageFiles,
        getAssetsManifest: () => assetsManifest,
        pluginManifest: () => pluginManifest
      });
    }
  }
});

// node_modules/@brillout/vite-plugin-server-entry/dist/runtime/autoImporter.js
var require_autoImporter = __commonJS({
  "node_modules/@brillout/vite-plugin-server-entry/dist/runtime/autoImporter.js"(exports) {
    exports.status = "SET";
    exports.loadServerEntry = async () => {
      await Promise.resolve().then(() => (init_entry(), entry_exports));
    };
    exports.paths = {
      autoImporterFilePathOriginal: "/Users/20061526/WebstormProjects/collab-vike/node_modules/@brillout/vite-plugin-server-entry/dist/runtime/autoImporter.js",
      autoImporterFileDirActual: (() => {
        try {
          return __dirname;
        } catch {
          return null;
        }
      })(),
      serverEntryFilePathRelative: "../../../../../dist/server/entry.mjs",
      serverEntryFilePathOriginal: "/Users/20061526/WebstormProjects/collab-vike/dist/server/entry.mjs",
      serverEntryFilePathResolved: () => __require.resolve("../../../../../dist/server/entry.mjs")
    };
  }
});

// node_modules/@brillout/vite-plugin-server-entry/dist/runtime/importServerProductionEntry.js
var require_importServerProductionEntry = __commonJS({
  "node_modules/@brillout/vite-plugin-server-entry/dist/runtime/importServerProductionEntry.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.importServerProductionEntry = importServerProductionEntry2;
    var utils_1 = require_utils2();
    var debugLogsRuntime_1 = require_debugLogsRuntime();
    var debug_1 = require_debug();
    var crawlServerEntry_1 = require_crawlServerEntry();
    async function importServerProductionEntry2({ tolerateNotFound, outDir } = {}) {
      const autoImporter = require_autoImporter();
      (0, debugLogsRuntime_1.debugLogsRuntimePre)(autoImporter);
      (0, utils_1.assertUsage)(autoImporter.status !== "DISABLED_BY_INJECT", "As a library author, make sure your library doesn't call importServerProductionEntry() when using `inject: true`");
      let success = false;
      let requireError;
      let isOutsideOfCwd = null;
      if (autoImporter.status === "SET") {
        try {
          await autoImporter.loadServerEntry();
          success = true;
        } catch (err) {
          if (debug_1.DEBUG) {
            requireError = err;
          } else {
            throw err;
          }
        }
        isOutsideOfCwd = isServerEntryOutsideOfCwd(autoImporter.paths);
        if (isOutsideOfCwd) {
          success = false;
        }
      }
      if (!success) {
        success = await (0, crawlServerEntry_1.crawlServerEntry)(outDir);
      }
      (0, debugLogsRuntime_1.debugLogsRuntimePost)({ success, requireError, isOutsideOfCwd, outDir });
      if (tolerateNotFound) {
        return success;
      } else {
        (0, utils_1.assertUsage)(success, "The server production entry is missing. (Re-)build your app and try again. If you still get this error, then you need to manually import the server production entry, see https://github.com/brillout/vite-plugin-server-entry#manual-import");
        return null;
      }
    }
    function isServerEntryOutsideOfCwd(paths) {
      const cwd = (0, utils_1.getCwdSafe)();
      if (!cwd)
        return null;
      let serverEntryFilePath;
      try {
        serverEntryFilePath = paths.serverEntryFilePathResolved();
      } catch {
        return null;
      }
      if ((0, utils_1.isWebpackResolve)(serverEntryFilePath))
        return null;
      serverEntryFilePath = (0, utils_1.toPosixPath)(serverEntryFilePath);
      (0, utils_1.assertPosixPath)(cwd);
      return !serverEntryFilePath.startsWith(cwd);
    }
  }
});

// node_modules/@brillout/vite-plugin-server-entry/dist/runtime/index.js
var require_runtime = __commonJS({
  "node_modules/@brillout/vite-plugin-server-entry/dist/runtime/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.importServerProductionEntry = void 0;
    var importServerProductionEntry_1 = require_importServerProductionEntry();
    Object.defineProperty(exports, "importServerProductionEntry", { enumerable: true, get: function() {
      return importServerProductionEntry_1.importServerProductionEntry;
    } });
  }
});

// node_modules/vike/dist/esm/node/runtime/globalContext/loadImportBuild.js
function setImportBuildGetters(getters) {
  buildGetters.getters = getters;
}
async function loadImportBuild(outDir) {
  if (!buildGetters.getters) {
    await (0, import_runtime.importServerProductionEntry)({ outDir });
    assert(buildGetters.getters);
  }
  const [pageFiles2, assetsManifest, pluginManifest] = await Promise.all([
    buildGetters.getters.pageFiles(),
    buildGetters.getters.getAssetsManifest(),
    buildGetters.getters.pluginManifest()
  ]);
  const buildEntries = { pageFiles: pageFiles2, assetsManifest, pluginManifest };
  return buildEntries;
}
var import_runtime, buildGetters;
var init_loadImportBuild = __esm({
  "node_modules/vike/dist/esm/node/runtime/globalContext/loadImportBuild.js"() {
    import_runtime = __toESM(require_runtime(), 1);
    init_utils3();
    buildGetters = globalThis.__vike_buildGetters = globalThis.__vike_buildGetters || {
      getters: null
    };
  }
});

// node_modules/vike/dist/esm/shared/getPageFiles/assert_exports_old_design.js
function assertExportValues(pageFile) {
  enforceTrue.forEach((exportName) => {
    assert(pageFile.fileExports);
    if (!(exportName in pageFile.fileExports))
      return;
    const explainer = `The value of \`${exportName}\` is only allowed to be \`true\`.`;
    assertUsage2(pageFile.fileExports[exportName] !== false, `${pageFile.filePath} has \`export { ${exportName} }\` with the value \`false\` which is prohibited: remove \`export { ${exportName} }\` instead. (${explainer})`);
    assertUsage2(pageFile.fileExports[exportName] === true, `${pageFile.filePath} has \`export { ${exportName} }\` with a forbidden value. ${explainer}`);
  });
}
function assertDefaultExports(defaultExportName, filePath) {
  assertUsage2(!forbiddenDefaultExports.includes(defaultExportName), `${filePath} has \`export default { ${defaultExportName} }\` which is prohibited, use \`export { ${defaultExportName} }\` instead.`);
}
var enforceTrue, forbiddenDefaultExports;
var init_assert_exports_old_design = __esm({
  "node_modules/vike/dist/esm/shared/getPageFiles/assert_exports_old_design.js"() {
    init_utils();
    enforceTrue = ["clientRouting"];
    forbiddenDefaultExports = ["render", "clientRouting", "prerender", "doNotPrerender"];
  }
});

// node_modules/vike/dist/esm/shared/determinePageIdOld.js
function determinePageIdOld(filePath) {
  const pageSuffix = ".page.";
  const pageId = slice(filePath.split(pageSuffix), 0, -1).join(pageSuffix);
  assert(!pageId.includes("\\"));
  return pageId;
}
var init_determinePageIdOld = __esm({
  "node_modules/vike/dist/esm/shared/determinePageIdOld.js"() {
    init_utils();
  }
});

// node_modules/vike/dist/esm/shared/assertPageFilePath.js
function assertPageFilePath(filePath) {
  assertPosixPath(filePath);
}
var init_assertPageFilePath = __esm({
  "node_modules/vike/dist/esm/shared/assertPageFilePath.js"() {
    init_utils();
  }
});

// node_modules/vike/dist/esm/shared/getPageFiles/fileTypes.js
function determineFileType(filePath) {
  assertPosixPath(filePath);
  {
    const isCSS = filePath.endsWith(".css");
    if (isCSS) {
      return ".css";
    }
  }
  assert(isScriptFile(filePath), filePath);
  const fileName = filePath.split("/").slice(-1)[0];
  const parts = fileName.split(".");
  const suffix1 = parts.slice(-3)[0];
  const suffix2 = parts.slice(-2)[0];
  if (suffix2 === "page") {
    return ".page";
  }
  assert(suffix1 === "page", filePath);
  if (suffix2 === "server") {
    return ".page.server";
  }
  if (suffix2 === "client") {
    return ".page.client";
  }
  if (suffix2 === "route") {
    return ".page.route";
  }
  assert(false, filePath);
}
var fileTypes;
var init_fileTypes = __esm({
  "node_modules/vike/dist/esm/shared/getPageFiles/fileTypes.js"() {
    init_utils();
    init_isScriptFile();
    fileTypes = [
      ".page",
      ".page.server",
      ".page.route",
      ".page.client",
      // New type `.page.css`/`.page.server.css`/`.page.client.css` for `extensions[number].pageFileDist`.
      //  - Extensions using `pageFileDist` are expected to use a bundler that generates a `.css` colocated next to the original `.page.js` file (e.g. `/renderer/_default.page.server.css` for `/renderer/_default.page.server.js`.
      //  - Since these `.page.css` files Bundlers We can therefore expect that there isn't any `.page.server.sass`/...
      ".css"
    ];
  }
});

// node_modules/vike/dist/esm/shared/getPageFiles/getPageFileObject.js
function getPageFileObject(filePath) {
  const isRelevant = (pageId) => pageFile.pageId === pageId || pageFile.isDefaultPageFile && (isRendererFilePath(pageFile.filePath) || isAncestorDefaultPage(pageId, pageFile.filePath));
  const fileType = determineFileType(filePath);
  const isEnv = (env2) => {
    assert(fileType !== ".page.route");
    if (env2 === "CLIENT_ONLY") {
      return fileType === ".page.client" || fileType === ".css";
    }
    if (env2 === "SERVER_ONLY") {
      return fileType === ".page.server";
    }
    if (env2 === "CLIENT_AND_SERVER") {
      return fileType === ".page";
    }
    assert(false);
  };
  const pageFile = {
    filePath,
    fileType,
    isEnv,
    isRelevant,
    isDefaultPageFile: isDefaultFilePath(filePath),
    isRendererPageFile: fileType !== ".css" && isDefaultFilePath(filePath) && isRendererFilePath(filePath),
    isErrorPageFile: isErrorPageId(filePath, false),
    pageId: determinePageIdOld(filePath)
  };
  return pageFile;
}
function isDefaultFilePath(filePath) {
  assertPageFilePath(filePath);
  if (isErrorPageId(filePath, false)) {
    return false;
  }
  return filePath.includes("/_default");
}
function isRendererFilePath(filePath) {
  assertPageFilePath(filePath);
  return filePath.includes("/renderer/");
}
function isAncestorDefaultPage(pageId, defaultPageFilePath) {
  assertPageFilePath(pageId);
  assertPageFilePath(defaultPageFilePath);
  assert(!pageId.endsWith("/"));
  assert(!defaultPageFilePath.endsWith("/"));
  assert(isDefaultFilePath(defaultPageFilePath));
  const defaultPageDir = slice(defaultPageFilePath.split("/"), 0, -1).filter((filePathSegment) => filePathSegment !== "_default").join("/");
  return pageId.startsWith(defaultPageDir);
}
var init_getPageFileObject = __esm({
  "node_modules/vike/dist/esm/shared/getPageFiles/getPageFileObject.js"() {
    init_determinePageIdOld();
    init_assertPageFilePath();
    init_error_page();
    init_utils();
    init_fileTypes();
  }
});

// node_modules/@brillout/json-serializer/dist/cjs/parse.js
var require_parse = __commonJS({
  "node_modules/@brillout/json-serializer/dist/cjs/parse.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.parseTransform = exports.parse = void 0;
    var types_1 = require_types();
    function parse3(str) {
      const value = JSON.parse(str);
      return parseTransform2(value);
    }
    exports.parse = parse3;
    function parseTransform2(value) {
      if (typeof value === "string") {
        return reviver(value);
      }
      if (
        // Also matches arrays
        typeof value === "object" && value !== null
      ) {
        Object.entries(value).forEach(([key, val]) => {
          ;
          value[key] = parseTransform2(val);
        });
      }
      return value;
    }
    exports.parseTransform = parseTransform2;
    function reviver(value) {
      for (const { match, deserialize } of types_1.types) {
        if (match(value)) {
          return deserialize(value, parse3);
        }
      }
      return value;
    }
  }
});

// node_modules/vike/dist/esm/shared/page-configs/assertPlusFileExport.js
function assertPlusFileExport(fileExports, filePathToShowToUser, configName) {
  const exportNames = Object.keys(fileExports).filter((exportName) => !EXPORTS_IGNORE.includes(exportName));
  const isValid2 = (exportName) => exportName === "default" || exportName === configName;
  const exportNamesValid = exportNames.filter(isValid2);
  const exportNamesInvalid = exportNames.filter((e14) => !isValid2(e14));
  if (exportNamesValid.length === 1 && exportNamesInvalid.length === 0) {
    return;
  }
  const exportDefault = import_picocolors15.default.code("export default");
  const exportNamed = import_picocolors15.default.code(`export { ${configName} }`);
  assert(exportNamesValid.length <= 2);
  if (exportNamesValid.length === 0) {
    assertUsage2(false, `${filePathToShowToUser} should have ${exportNamed} or ${exportDefault}`);
  }
  if (exportNamesValid.length === 2) {
    assertUsage2(false, `${filePathToShowToUser} is ambiguous: remove ${exportDefault} or ${exportNamed}`);
  }
  if (!TOLERATE_SIDE_EXPORTS.some((ext) => filePathToShowToUser.endsWith(ext))) {
    exportNamesInvalid.forEach((exportInvalid) => {
      assertWarning2(false, `${filePathToShowToUser} unexpected ${import_picocolors15.default.cyan(`export { ${exportInvalid} }`)}`, {
        onlyOnce: true
      });
    });
  }
}
var import_picocolors15, EXPORTS_IGNORE, TOLERATE_SIDE_EXPORTS;
var init_assertPlusFileExport = __esm({
  "node_modules/vike/dist/esm/shared/page-configs/assertPlusFileExport.js"() {
    init_utils();
    import_picocolors15 = __toESM(require_picocolors(), 1);
    EXPORTS_IGNORE = [
      // vite-plugin-solid adds `export { $$registrations }`
      "$$registrations",
      // @vitejs/plugin-vue adds `export { _rerender_only }`
      "_rerender_only"
    ];
    TOLERATE_SIDE_EXPORTS = [".md", ".mdx"];
  }
});

// node_modules/vike/dist/esm/shared/page-configs/serialize/parsePageConfigs.js
function parseConfigValuesSerialized(configValuesSerialized6) {
  const configValues = parseConfigValuesSerialized_tmp(configValuesSerialized6);
  return configValues;
}
function parsePageConfigs(pageConfigsSerialized2, pageConfigGlobalSerialized2) {
  const pageConfigs = pageConfigsSerialized2.map((pageConfigSerialized) => {
    const configValues = parseConfigValuesSerialized(pageConfigSerialized.configValuesSerialized);
    const { pageId, isErrorPage: isErrorPage2, routeFilesystem, loadConfigValuesAll } = pageConfigSerialized;
    assertRouteConfigValue(configValues);
    return {
      pageId,
      isErrorPage: isErrorPage2,
      routeFilesystem,
      configValues,
      loadConfigValuesAll
    };
  });
  const pageConfigGlobal = { configValues: {} };
  {
    const configValues = parseConfigValuesSerialized(pageConfigGlobalSerialized2.configValuesSerialized);
    Object.assign(pageConfigGlobal.configValues, configValues);
  }
  return { pageConfigs, pageConfigGlobal };
}
function assertRouteConfigValue(configValues) {
  const configName = "route";
  const configValue = configValues[configName];
  if (!configValue)
    return;
  const { value, definedAtData } = configValue;
  const configValueType = typeof value;
  assert(definedAtData);
  const configDefinedAt = getConfigDefinedAt("Config", configName, definedAtData);
  assertUsage2(configValueType === "string" || isCallable(value), `${configDefinedAt} has an invalid type '${configValueType}': it should be a string or a function instead, see https://vike.dev/route`);
}
function parseConfigValuesSerialized_tmp(configValuesSerialized6) {
  const configValues = {};
  Object.entries(configValuesSerialized6).forEach(([configName, configValueSeriliazed]) => {
    let configValue;
    if (configValueSeriliazed.type === "cumulative") {
      const { valueSerialized, ...common } = configValueSeriliazed;
      const value = valueSerialized.map((valueSerializedElement, i13) => {
        const { value: value2, sideExports } = parseValueSerialized(valueSerializedElement, configName, () => {
          const definedAtFile = configValueSeriliazed.definedAtData[i13];
          assert(definedAtFile);
          return definedAtFile;
        });
        addSideExports(sideExports);
        return value2;
      });
      configValue = { value, ...common };
    } else {
      const { valueSerialized, ...common } = configValueSeriliazed;
      const { value, sideExports } = parseValueSerialized(valueSerialized, configName, () => {
        assert(configValueSeriliazed.type !== "computed");
        return configValueSeriliazed.definedAtData;
      });
      addSideExports(sideExports);
      configValue = { value, ...common };
    }
    configValues[configName] = configValue;
  });
  return configValues;
  function addSideExports(sideExports) {
    sideExports.forEach((sideExport) => {
      const { configName, configValue } = sideExport;
      if (!configValues[configName]) {
        configValues[configName] = configValue;
      } else {
      }
    });
  }
}
function parseValueSerialized(valueSerialized, configName, getDefinedAtFile) {
  if (valueSerialized.type === "js-serialized") {
    let { value } = valueSerialized;
    value = (0, import_parse.parseTransform)(value);
    return { value, sideExports: [] };
  }
  if (valueSerialized.type === "pointer-import") {
    const { value } = valueSerialized;
    return { value, sideExports: [] };
  }
  if (valueSerialized.type === "plus-file") {
    const definedAtFile = getDefinedAtFile();
    const { exportValues } = valueSerialized;
    assertPlusFileExport(exportValues, definedAtFile.filePathToShowToUser, configName);
    let value;
    let valueWasFound = false;
    const sideExports = [];
    Object.entries(exportValues).forEach(([exportName, exportValue]) => {
      const isSideExport = exportName !== "default" && exportName !== configName;
      if (!isSideExport) {
        value = exportValue;
        assert(!valueWasFound);
        valueWasFound = true;
      } else {
        sideExports.push({
          configName: exportName,
          configValue: {
            type: "standard",
            // We don't support side exports for cumulative values. We could support it but it isn't trivial.
            value: exportValue,
            definedAtData: {
              filePathToShowToUser: definedAtFile.filePathToShowToUser,
              fileExportPathToShowToUser: [exportName]
            }
          }
        });
      }
    });
    assert(valueWasFound);
    return { value, sideExports };
  }
  assert(false);
}
var import_parse;
var init_parsePageConfigs = __esm({
  "node_modules/vike/dist/esm/shared/page-configs/serialize/parsePageConfigs.js"() {
    init_utils();
    init_getConfigDefinedAt();
    import_parse = __toESM(require_parse(), 1);
    init_assertPlusFileExport();
  }
});

// node_modules/vike/dist/esm/shared/getPageFiles/parseGlobResults.js
function parseGlobResults(pageFilesExports) {
  assert(hasProp(pageFilesExports, "pageFilesLazy", "object"));
  assert(hasProp(pageFilesExports, "pageFilesEager", "object"));
  assert(hasProp(pageFilesExports, "pageFilesExportNamesLazy", "object"));
  assert(hasProp(pageFilesExports, "pageFilesExportNamesEager", "object"));
  assert(hasProp(pageFilesExports.pageFilesLazy, ".page"));
  assert(hasProp(pageFilesExports.pageFilesLazy, ".page.client") || hasProp(pageFilesExports.pageFilesLazy, ".page.server"));
  assert(hasProp(pageFilesExports, "pageFilesList", "string[]"));
  assert(hasProp(pageFilesExports, "pageConfigsSerialized"));
  assert(hasProp(pageFilesExports, "pageConfigGlobalSerialized"));
  const { pageConfigsSerialized: pageConfigsSerialized2, pageConfigGlobalSerialized: pageConfigGlobalSerialized2 } = pageFilesExports;
  assertPageConfigsSerialized(pageConfigsSerialized2);
  assertPageConfigGlobalSerialized(pageConfigGlobalSerialized2);
  const { pageConfigs, pageConfigGlobal } = parsePageConfigs(pageConfigsSerialized2, pageConfigGlobalSerialized2);
  const pageFilesMap = {};
  parseGlobResult(pageFilesExports.pageFilesLazy).forEach(({ filePath, pageFile, globValue }) => {
    pageFile = pageFilesMap[filePath] = pageFilesMap[filePath] ?? pageFile;
    const loadModule = globValue;
    assertLoadModule(loadModule);
    pageFile.loadFile = async () => {
      if (!("fileExports" in pageFile)) {
        pageFile.fileExports = await loadModule();
        assertExportValues(pageFile);
      }
    };
  });
  parseGlobResult(pageFilesExports.pageFilesExportNamesLazy).forEach(({ filePath, pageFile, globValue }) => {
    pageFile = pageFilesMap[filePath] = pageFilesMap[filePath] ?? pageFile;
    const loadModule = globValue;
    assertLoadModule(loadModule);
    pageFile.loadExportNames = async () => {
      if (!("exportNames" in pageFile)) {
        const moduleExports = await loadModule();
        assert(hasProp(moduleExports, "exportNames", "string[]"), pageFile.filePath);
        pageFile.exportNames = moduleExports.exportNames;
      }
    };
  });
  parseGlobResult(pageFilesExports.pageFilesEager).forEach(({ filePath, pageFile, globValue }) => {
    pageFile = pageFilesMap[filePath] = pageFilesMap[filePath] ?? pageFile;
    const moduleExports = globValue;
    assert(isObject(moduleExports));
    pageFile.fileExports = moduleExports;
  });
  parseGlobResult(pageFilesExports.pageFilesExportNamesEager).forEach(({ filePath, pageFile, globValue }) => {
    pageFile = pageFilesMap[filePath] = pageFilesMap[filePath] ?? pageFile;
    const moduleExports = globValue;
    assert(isObject(moduleExports));
    assert(hasProp(moduleExports, "exportNames", "string[]"), pageFile.filePath);
    pageFile.exportNames = moduleExports.exportNames;
  });
  pageFilesExports.pageFilesList.forEach((filePath) => {
    pageFilesMap[filePath] = pageFilesMap[filePath] ?? getPageFileObject(filePath);
  });
  const pageFiles2 = Object.values(pageFilesMap);
  pageFiles2.forEach(({ filePath }) => {
    assert(!filePath.includes("\\"));
  });
  return { pageFiles: pageFiles2, pageConfigs, pageConfigGlobal };
}
function parseGlobResult(globObject) {
  const ret = [];
  Object.entries(globObject).forEach(([fileType, globFiles]) => {
    cast(fileType);
    assert(fileTypes.includes(fileType));
    assert(isObject(globFiles));
    Object.entries(globFiles).forEach(([filePath, globValue]) => {
      const pageFile = getPageFileObject(filePath);
      assert(pageFile.fileType === fileType);
      ret.push({ filePath, pageFile, globValue });
    });
  });
  return ret;
}
function assertLoadModule(globValue) {
  assert(isCallable(globValue));
}
function assertPageConfigsSerialized(pageConfigsSerialized2) {
  assert(isArray(pageConfigsSerialized2));
  pageConfigsSerialized2.forEach((pageConfigSerialized) => {
    assert(isObject(pageConfigSerialized));
    assert(hasProp(pageConfigSerialized, "pageId", "string"));
    assert(hasProp(pageConfigSerialized, "routeFilesystem"));
    assert(hasProp(pageConfigSerialized, "configValuesSerialized"));
  });
}
function assertPageConfigGlobalSerialized(pageConfigGlobalSerialized2) {
  assert(hasProp(pageConfigGlobalSerialized2, "configValuesSerialized"));
}
var init_parseGlobResults = __esm({
  "node_modules/vike/dist/esm/shared/getPageFiles/parseGlobResults.js"() {
    init_utils();
    init_assert_exports_old_design();
    init_getPageFileObject();
    init_fileTypes();
    init_parsePageConfigs();
  }
});

// node_modules/vike/dist/esm/shared/getPageFiles/setPageFiles.js
function setPageFiles(pageFilesExports) {
  const { pageFiles: pageFiles2, pageConfigs, pageConfigGlobal } = parseGlobResults(pageFilesExports);
  globalObject5.pageFilesAll = pageFiles2;
  globalObject5.pageConfigs = pageConfigs;
  globalObject5.pageConfigGlobal = pageConfigGlobal;
}
function setPageFilesAsync(getPageFilesExports2) {
  globalObject5.pageFilesGetter = async () => {
    setPageFiles(await getPageFilesExports2());
  };
}
async function getPageFilesAll(isClientSide, isProduction) {
  if (isClientSide) {
    assert(!globalObject5.pageFilesGetter);
    assert(isProduction === void 0);
  } else {
    assert(globalObject5.pageFilesGetter);
    assert(typeof isProduction === "boolean");
    if (!globalObject5.pageFilesAll || // We reload all glob imports in dev to make auto-reload work
    !isProduction) {
      await globalObject5.pageFilesGetter();
    }
  }
  const { pageFilesAll, pageConfigs, pageConfigGlobal } = globalObject5;
  assert(pageFilesAll && pageConfigs && pageConfigGlobal);
  const allPageIds = getAllPageIds(pageFilesAll, pageConfigs);
  return { pageFilesAll, allPageIds, pageConfigs, pageConfigGlobal };
}
function getAllPageIds(allPageFiles, pageConfigs) {
  const fileIds = allPageFiles.filter(({ isDefaultPageFile }) => !isDefaultPageFile).map(({ pageId }) => pageId);
  const allPageIds = unique(fileIds);
  const allPageIds2 = pageConfigs.map((p11) => p11.pageId);
  return [...allPageIds, ...allPageIds2];
}
var globalObject5;
var init_setPageFiles = __esm({
  "node_modules/vike/dist/esm/shared/getPageFiles/setPageFiles.js"() {
    init_utils();
    init_parseGlobResults();
    init_getGlobalObject();
    globalObject5 = getGlobalObject("setPageFiles.ts", {});
  }
});

// node_modules/vike/dist/esm/shared/getPageFiles/getAllPageIdFiles.js
function getPageFilesClientSide(pageFilesAll, pageId) {
  return determine(pageFilesAll, pageId, true);
}
function getPageFilesServerSide(pageFilesAll, pageId) {
  return determine(pageFilesAll, pageId, false);
}
function determine(pageFilesAll, pageId, envIsClient) {
  const env2 = envIsClient ? "CLIENT_ONLY" : "SERVER_ONLY";
  const pageFilesRelevant = pageFilesAll.filter((p11) => p11.isRelevant(pageId) && p11.fileType !== ".page.route").sort(getPageFilesSorter(envIsClient, pageId));
  const getPageIdFile = (iso) => {
    const files = pageFilesRelevant.filter((p11) => p11.pageId === pageId && p11.isEnv(iso ? "CLIENT_AND_SERVER" : env2));
    assertUsage2(files.length <= 1, `Merge the following files into a single file: ${files.map((p11) => p11.filePath).join(" ")}`);
    const pageIdFile = files[0];
    assert(pageIdFile === void 0 || !pageIdFile.isDefaultPageFile);
    return pageIdFile;
  };
  const pageIdFileEnv = getPageIdFile(false);
  const pageIdFileIso = getPageIdFile(true);
  const getRendererFile = (iso) => pageFilesRelevant.filter((p11) => p11.isRendererPageFile && p11.isEnv(iso ? "CLIENT_AND_SERVER" : env2))[0];
  const rendererFileEnv = getRendererFile(false);
  const rendererFileIso = getRendererFile(true);
  const defaultFilesNonRenderer = pageFilesRelevant.filter((p11) => p11.isDefaultPageFile && !p11.isRendererPageFile && (p11.isEnv(env2) || p11.isEnv("CLIENT_AND_SERVER")));
  const pageFiles2 = [pageIdFileEnv, pageIdFileIso, ...defaultFilesNonRenderer, rendererFileEnv, rendererFileIso].filter(isNotNullish);
  return pageFiles2;
}
function getPageFilesSorter(envIsClient, pageId) {
  const env2 = envIsClient ? "CLIENT_ONLY" : "SERVER_ONLY";
  const e1First = -1;
  const e2First = 1;
  const noOrder = 0;
  return (e1, e22) => {
    if (!e1.isDefaultPageFile && e22.isDefaultPageFile) {
      return e1First;
    }
    if (!e22.isDefaultPageFile && e1.isDefaultPageFile) {
      return e2First;
    }
    {
      const e1_isRenderer = e1.isRendererPageFile;
      const e2_isRenderer = e22.isRendererPageFile;
      if (!e1_isRenderer && e2_isRenderer) {
        return e1First;
      }
      if (!e2_isRenderer && e1_isRenderer) {
        return e2First;
      }
      assert(e1_isRenderer === e2_isRenderer);
    }
    {
      const e1_distance = getPathDistance(pageId, e1.filePath);
      const e2_distance = getPathDistance(pageId, e22.filePath);
      if (e1_distance < e2_distance) {
        return e1First;
      }
      if (e2_distance < e1_distance) {
        return e2First;
      }
      assert(e1_distance === e2_distance);
    }
    {
      if (e1.isEnv(env2) && e22.isEnv("CLIENT_AND_SERVER")) {
        return e1First;
      }
      if (e22.isEnv(env2) && e1.isEnv("CLIENT_AND_SERVER")) {
        return e2First;
      }
    }
    return noOrder;
  };
}
function getPathDistance(pathA, pathB) {
  assertPageFilePath(pathA);
  assertPageFilePath(pathB);
  let idx = 0;
  for (; idx < pathA.length && idx < pathB.length; idx++) {
    if (pathA[idx] !== pathB[idx])
      break;
  }
  const pathAWithoutCommon = pathA.slice(idx);
  const pathBWithoutCommon = pathB.slice(idx);
  const distanceA = pathAWithoutCommon.split("/").length;
  const distanceB = pathBWithoutCommon.split("/").length;
  const distance2 = distanceA + distanceB;
  return distance2;
}
var init_getAllPageIdFiles = __esm({
  "node_modules/vike/dist/esm/shared/getPageFiles/getAllPageIdFiles.js"() {
    init_utils();
    init_assertPageFilePath();
  }
});

// node_modules/vike/dist/esm/shared/getPageFiles/getExports.js
function getPageContextExports(pageFiles2, pageConfig) {
  const configEntries = {};
  const config = {};
  const exportsAll = {};
  pageFiles2.forEach((pageFile) => {
    const exportValues = getExportValues(pageFile);
    exportValues.forEach(({ exportName, exportValue, isFromDefaultExport }) => {
      assert(exportName !== "default");
      exportsAll[exportName] = exportsAll[exportName] ?? [];
      exportsAll[exportName].push({
        exportValue,
        exportSource: `${pageFile.filePath} > ${isFromDefaultExport ? `\`export default { ${exportName} }\`` : `\`export { ${exportName} }\``}`,
        filePath: pageFile.filePath,
        _filePath: pageFile.filePath,
        // TODO/next-major-release: remove
        _fileType: pageFile.fileType,
        _isFromDefaultExport: isFromDefaultExport
      });
    });
  });
  const source = {};
  const sources = {};
  const addSrc = (src, configName) => {
    source[configName] = src;
    sources[configName] ?? (sources[configName] = []);
    sources[configName].push(src);
  };
  const from = {
    configsStandard: {},
    configsCumulative: {},
    configsComputed: {}
  };
  if (pageConfig) {
    Object.entries(pageConfig.configValues).forEach(([configName, configValue]) => {
      const { value } = configValue;
      const configValueFilePathToShowToUser = getConfigValueFilePathToShowToUser(configValue.definedAtData);
      const configDefinedAt = getConfigDefinedAtOptional("Config", configName, configValue.definedAtData);
      config[configName] = config[configName] ?? value;
      configEntries[configName] = configEntries[configName] ?? [];
      assert(configEntries[configName].length === 0);
      configEntries[configName].push({
        configValue: value,
        configDefinedAt,
        configDefinedByFile: configValueFilePathToShowToUser
      });
      if (configValue.type === "standard") {
        const src = {
          type: "configsStandard",
          value: configValue.value,
          definedAt: getDefinedAtString(configValue.definedAtData, configName)
        };
        addSrc(src, configName);
        from.configsStandard[configName] = src;
      }
      if (configValue.type === "cumulative") {
        const src = {
          type: "configsCumulative",
          values: configValue.value.map((value2, i13) => {
            const definedAtFile = configValue.definedAtData[i13];
            assert(definedAtFile);
            const definedAt = getDefinedAtString(definedAtFile, configName);
            return {
              value: value2,
              definedAt
            };
          })
        };
        addSrc(src, configName);
        from.configsCumulative[configName] = src;
      }
      if (configValue.type === "computed") {
        const src = {
          type: "configsComputed",
          value: configValue.value
        };
        addSrc(src, configName);
        from.configsComputed[configName] = src;
      }
      const exportName = configName;
      exportsAll[exportName] = exportsAll[exportName] ?? [];
      exportsAll[exportName].push({
        exportValue: value,
        exportSource: configDefinedAt,
        filePath: configValueFilePathToShowToUser,
        _filePath: configValueFilePathToShowToUser,
        _fileType: null,
        _isFromDefaultExport: null
      });
    });
  }
  const pageExports = createObjectWithDeprecationWarning();
  const exports = {};
  Object.entries(exportsAll).forEach(([exportName, values2]) => {
    values2.forEach(({ exportValue, _fileType, _isFromDefaultExport }) => {
      exports[exportName] = exports[exportName] ?? exportValue;
      if (_fileType === ".page" && !_isFromDefaultExport) {
        if (!(exportName in pageExports)) {
          pageExports[exportName] = exportValue;
        }
      }
    });
  });
  assert(!("default" in exports));
  assert(!("default" in exportsAll));
  const pageContextExports = {
    from,
    source,
    sources,
    // TODO/eventually: deprecate/remove every prop below
    config,
    configEntries,
    exports,
    exportsAll,
    pageExports
  };
  return pageContextExports;
}
function getExportValues(pageFile) {
  const { filePath, fileExports } = pageFile;
  assert(fileExports);
  assert(isScriptFile(filePath));
  const exportValues = [];
  Object.entries(fileExports).sort(makeLast(([exportName]) => exportName === "default")).forEach(([exportName, exportValue]) => {
    let isFromDefaultExport = exportName === "default";
    if (isFromDefaultExport) {
      if (isTemplateFile(filePath)) {
        exportName = "Page";
      } else {
        assertUsage2(isObject(exportValue), `The ${import_picocolors16.default.cyan("export default")} of ${filePath} should be an object.`);
        Object.entries(exportValue).forEach(([defaultExportName, defaultExportValue]) => {
          assertDefaultExports(defaultExportName, filePath);
          exportValues.push({
            exportName: defaultExportName,
            exportValue: defaultExportValue,
            isFromDefaultExport
          });
        });
        return;
      }
    }
    exportValues.push({
      exportName,
      exportValue,
      isFromDefaultExport
    });
  });
  exportValues.forEach(({ exportName, isFromDefaultExport }) => {
    assert(!(isFromDefaultExport && forbiddenDefaultExports.includes(exportName)));
  });
  return exportValues;
}
function createObjectWithDeprecationWarning() {
  return new Proxy({}, {
    get(...args) {
      if (!isBrowser()) {
        assertWarning2(false, "`pageContext.pageExports` is outdated. Use `pageContext.exports` instead, see https://vike.dev/exports", { onlyOnce: true, showStackTrace: true });
      }
      return Reflect.get(...args);
    }
  });
}
var import_picocolors16;
var init_getExports = __esm({
  "node_modules/vike/dist/esm/shared/getPageFiles/getExports.js"() {
    init_isScriptFile();
    init_utils();
    init_assert_exports_old_design();
    init_getConfigDefinedAt();
    init_helpers();
    import_picocolors16 = __toESM(require_picocolors(), 1);
  }
});

// node_modules/vike/dist/esm/shared/getPageFiles.js
var init_getPageFiles = __esm({
  "node_modules/vike/dist/esm/shared/getPageFiles.js"() {
    init_setPageFiles();
    init_setPageFiles();
    init_setPageFiles();
    init_getAllPageIdFiles();
    init_getAllPageIdFiles();
    init_getExports();
  }
});

// node_modules/vike/dist/esm/node/shared/assertRuntimeManifest.js
function assertRuntimeManifest(obj) {
  assert(obj);
  assert(isObject(obj));
  assert(hasProp(obj, "baseServer", "string"));
  assert(hasProp(obj, "baseAssets", "string"));
  assert(isBaseServer(obj.baseServer));
  assert(isBaseAssets(obj.baseAssets));
  assert(hasProp(obj, "includeAssetsImportedByServer", "boolean"));
  assert(hasProp(obj, "redirects", "object"));
  castType(obj);
  assert(hasProp(obj, "trailingSlash", "boolean"));
  assert(hasProp(obj, "disableUrlNormalization", "boolean"));
  checkType(obj);
}
var init_assertRuntimeManifest = __esm({
  "node_modules/vike/dist/esm/node/shared/assertRuntimeManifest.js"() {
    init_utils2();
  }
});

// node_modules/vike/dist/esm/node/shared/assertPluginManifest.js
function assertPluginManifest(pluginManifest) {
  assert(isPlainObject(pluginManifest));
  assertUsage2(pluginManifest.version === projectInfo.projectVersion, `Re-build your app (you're using vike@${projectInfo.projectVersion} but your app was built with vike@${pluginManifest.version})`);
  assertRuntimeManifest(pluginManifest);
  assert(hasProp(pluginManifest, "usesClientRouter", "boolean"));
  assert(hasProp(pluginManifest, "version", "string"));
  const pluginManifestClone = { ...pluginManifest };
  checkType(pluginManifestClone);
}
var init_assertPluginManifest = __esm({
  "node_modules/vike/dist/esm/node/shared/assertPluginManifest.js"() {
    init_assertRuntimeManifest();
    init_utils2();
  }
});

// node_modules/vike/dist/esm/node/shared/getConfigVike.js
async function getConfigVike(config) {
  const { configVikePromise } = config;
  assert(configVikePromise);
  const configVike = await configVikePromise;
  return configVike;
}
var init_getConfigVike = __esm({
  "node_modules/vike/dist/esm/node/shared/getConfigVike.js"() {
    init_utils2();
  }
});

// node_modules/vike/dist/esm/node/shared/virtual-files.js
function getVirtualFileId(id3) {
  if (id3.startsWith(tag)) {
    id3 = id3.slice(tag.length);
  }
  assert(!id3.startsWith(tag));
  return id3;
}
var import_picocolors17, tag;
var init_virtual_files = __esm({
  "node_modules/vike/dist/esm/node/shared/virtual-files.js"() {
    import_picocolors17 = __toESM(require_picocolors(), 1);
    init_utils2();
    tag = "\0";
  }
});

// node_modules/vike/dist/esm/node/shared/virtual-files/virtualFileImportUserCode.js
var idBase, virtualFileIdImportUserCodeServer, virtualFileIdImportUserCodeClientSR, virtualFileIdImportUserCodeClientCR;
var init_virtualFileImportUserCode = __esm({
  "node_modules/vike/dist/esm/node/shared/virtual-files/virtualFileImportUserCode.js"() {
    init_utils2();
    init_virtual_files();
    idBase = "virtual:vike:importUserCode";
    virtualFileIdImportUserCodeServer = `${idBase}:server`;
    virtualFileIdImportUserCodeClientSR = `${idBase}:client:server-routing`;
    virtualFileIdImportUserCodeClientCR = `${idBase}:client:client-routing`;
  }
});

// node_modules/vike/dist/esm/node/runtime/page-files/getPageFilesExports.js
async function getPageFilesExports() {
  const viteDevServer = getViteDevServer();
  assert(viteDevServer);
  let moduleExports;
  try {
    moduleExports = await viteDevServer.ssrLoadModule(virtualFileIdImportUserCodeServer);
  } catch (err) {
    debugGlob(`Glob error: ${virtualFileIdImportUserCodeServer} transpile error: `, err);
    throw err;
  }
  moduleExports = moduleExports.default || moduleExports;
  debugGlob("Glob result: ", moduleExports);
  assert(isObject(moduleExports));
  return moduleExports;
}
var init_getPageFilesExports = __esm({
  "node_modules/vike/dist/esm/node/runtime/page-files/getPageFilesExports.js"() {
    init_utils3();
    init_globalContext();
    init_virtualFileImportUserCode();
  }
});

// node_modules/vike/dist/esm/node/runtime/globalContext.js
function getGlobalContext() {
  assert(globalObject6.globalContext);
  return globalObject6.globalContext;
}
function getViteDevServer() {
  return globalObject6.viteDevServer ?? null;
}
async function initGlobalContext_renderPage() {
  await initGlobalContext(!globalObject6.isDev);
}
async function initGlobalContext(isProduction) {
  if (globalObject6.globalContext) {
    assert(globalObject6.globalContext.isProduction === isProduction);
    assert(globalObject6.globalContext.isPrerendering === (globalObject6.isPrerendering ?? false));
    return;
  }
  const { viteDevServer, viteConfig, isDev, isPrerendering } = globalObject6;
  assertNodeEnv_runtime(isDev ?? false);
  if (!isProduction) {
    assert(viteConfig);
    assert(viteDevServer);
    assert(!isPrerendering);
    const configVike = await getConfigVike(viteConfig);
    const pluginManifest = getRuntimeManifest(configVike);
    globalObject6.globalContext = {
      isProduction: false,
      isPrerendering: false,
      assetsManifest: null,
      pluginManifest: null,
      viteDevServer,
      viteConfig,
      baseServer: pluginManifest.baseServer,
      baseAssets: pluginManifest.baseAssets,
      includeAssetsImportedByServer: pluginManifest.includeAssetsImportedByServer,
      redirects: pluginManifest.redirects,
      trailingSlash: pluginManifest.trailingSlash,
      disableUrlNormalization: pluginManifest.disableUrlNormalization
    };
  } else {
    const buildEntries = await loadImportBuild(globalObject6.outDirRoot);
    assertBuildEntries(buildEntries, isPrerendering ?? false);
    const { pageFiles: pageFiles2, assetsManifest, pluginManifest } = buildEntries;
    setPageFiles(pageFiles2);
    assertViteManifest(assetsManifest);
    assertPluginManifest(pluginManifest);
    const globalContext = {
      isProduction: true,
      assetsManifest,
      pluginManifest,
      viteDevServer: null,
      baseServer: pluginManifest.baseServer,
      baseAssets: pluginManifest.baseAssets,
      includeAssetsImportedByServer: pluginManifest.includeAssetsImportedByServer,
      redirects: pluginManifest.redirects,
      trailingSlash: pluginManifest.trailingSlash,
      disableUrlNormalization: pluginManifest.disableUrlNormalization
    };
    if (isPrerendering) {
      assert(viteConfig);
      const configVike = await getConfigVike(viteConfig);
      assert(configVike);
      objectAssign(globalContext, {
        isPrerendering: true,
        viteConfig
      });
      globalObject6.globalContext = globalContext;
    } else {
      objectAssign(globalContext, {
        isPrerendering: false,
        viteConfig: null
      });
      globalObject6.globalContext = globalContext;
    }
  }
}
function getRuntimeManifest(configVike) {
  const { includeAssetsImportedByServer, baseServer: baseServer2, baseAssets, redirects, trailingSlash, disableUrlNormalization } = configVike;
  const manifest = {
    baseServer: baseServer2,
    baseAssets,
    includeAssetsImportedByServer,
    redirects,
    trailingSlash,
    disableUrlNormalization
  };
  assertRuntimeManifest(manifest);
  return manifest;
}
function assertBuildEntries(buildEntries, isPreRendering) {
  const errMsg = [
    `You are tyring to run`,
    isPreRendering ? "pre-rendering" : "the server for production",
    `but your app isn't built yet. Run ${import_picocolors18.default.cyan("$ vite build")} before `,
    isPreRendering ? "pre-rendering." : "running the server."
  ].join(" ");
  assertUsage2(buildEntries, errMsg);
}
function assertViteManifest(manifest) {
  assert(isPlainObject(manifest));
}
var import_picocolors18, globalObject6;
var init_globalContext = __esm({
  "node_modules/vike/dist/esm/node/runtime/globalContext.js"() {
    init_utils3();
    init_loadImportBuild();
    init_getPageFiles();
    init_assertPluginManifest();
    init_getConfigVike();
    init_assertRuntimeManifest();
    import_picocolors18 = __toESM(require_picocolors(), 1);
    init_getPageFilesExports();
    globalObject6 = getGlobalObject("globalContext.ts", (() => {
      const { promise: viteDevServerPromise, resolve: viteDevServerPromiseResolve } = genPromise();
      return {
        viteDevServerPromise,
        viteDevServerPromiseResolve
      };
    })());
  }
});

// node_modules/vike/dist/esm/node/runtime/html/injectAssets/getHtmlTags.js
function getHtmlTags(pageContext, streamFromReactStreamingPackage, injectFilter, pageAssets, viteDevScript, isStream2) {
  assert([true, false].includes(pageContext._isHtmlOnly));
  const isHtmlOnly = pageContext._isHtmlOnly;
  const { isProduction } = getGlobalContext();
  const injectScriptsAt = getInjectScriptsAt(pageContext.pageId, pageContext._pageConfigs);
  const injectFilterEntries = pageAssets.filter((asset) => {
    if (asset.isEntry && asset.assetType === "script") {
      return false;
    }
    return true;
  }).map((asset) => {
    const inject = (() => {
      if (!isProduction) {
        return "HTML_BEGIN";
      }
      if (asset.assetType === "style" || asset.assetType === "font") {
        return "HTML_BEGIN";
      }
      if (asset.assetType === "script") {
        if (isHtmlOnly)
          return false;
        return "HTML_END";
      }
      return false;
    })();
    const entry = {
      ...asset,
      inject,
      // @ts-ignore
      [stamp]: true
    };
    return entry;
  });
  assertInjectFilterEntries(injectFilterEntries);
  if (injectFilter && isProduction) {
    Object.seal(injectFilterEntries);
    Object.values(injectFilterEntries).forEach((entry) => freezePartial(entry, { inject: (val) => val === false || val === "HTML_BEGIN" || val === "HTML_END" }));
    const res = injectFilter(injectFilterEntries);
    assertUsage2(res === void 0, `injectFilter() should return ${import_picocolors19.default.cyan("undefined")}, see https://vike.dev/injectFilter`);
    assertInjectFilterUsage(injectFilterEntries);
  }
  const htmlTags = [];
  injectFilterEntries.filter((asset) => asset.assetType !== "script" && asset.inject).forEach((asset) => {
    if (!asset.inject)
      return;
    const htmlTag = asset.isEntry ? inferAssetTag(asset) : inferPreloadTag(asset);
    htmlTags.push({ htmlTag, position: asset.inject });
  });
  const positionJavaScriptDefault = "HTML_END";
  const positionJavaScriptEntry = (() => {
    if (injectScriptsAt !== null) {
      if (pageContext._pageContextPromise) {
        assertWarning2(injectScriptsAt === "HTML_END" || !isStream2, `You're setting injectScriptsAt to ${import_picocolors19.default.code(JSON.stringify(injectScriptsAt))} while using HTML streaming with a pageContext promise (https://vike.dev/streaming#initial-data-after-stream-end) which is contradictory: the pageContext promise is skipped.`, { onlyOnce: true });
      }
      if (injectScriptsAt === "HTML_STREAM" && !isStream2) {
        return positionJavaScriptDefault;
      }
      return injectScriptsAt;
    }
    if (pageContext._pageContextPromise) {
      return positionJavaScriptDefault;
    }
    if (streamFromReactStreamingPackage && !streamFromReactStreamingPackage.hasStreamEnded()) {
      return "HTML_STREAM";
    }
    return positionJavaScriptDefault;
  })();
  if (pageContext._pageContextPromise && streamFromReactStreamingPackage) {
    assertWarning2(false, "We recommend against using HTML streaming and a pageContext promise (https://vike.dev/streaming#initial-data-after-stream-end) at the same time, because progressive hydration (https://vike.dev/streaming#progressive-rendering) won't work.", { onlyOnce: true });
  }
  if (!isHtmlOnly) {
    htmlTags.push({
      htmlTag: () => (
        // Needs to be called after resolvePageContextPromise()
        getPageContextJsonScriptTag(pageContext)
      ),
      position: positionJavaScriptEntry
    });
  }
  const scriptEntry = mergeScriptEntries(pageAssets, viteDevScript);
  if (scriptEntry) {
    htmlTags.push({
      htmlTag: scriptEntry,
      position: positionJavaScriptEntry
    });
  }
  injectFilterEntries.filter((asset) => asset.assetType === "script").forEach((asset) => {
    assert(!asset.isEntry);
    const htmlTag = inferPreloadTag(asset);
    if (!asset.inject)
      return;
    const position = asset.inject === "HTML_END" ? positionJavaScriptEntry : asset.inject;
    htmlTags.push({ htmlTag, position });
  });
  return htmlTags;
}
function mergeScriptEntries(pageAssets, viteDevScript) {
  const scriptEntries = pageAssets.filter((pageAsset) => pageAsset.isEntry && pageAsset.assetType === "script");
  const scriptTagsHtml = `${viteDevScript}${scriptEntries.map((asset) => inferAssetTag(asset)).join("")}`;
  const scriptTag = mergeScriptTags(scriptTagsHtml);
  return scriptTag;
}
function getPageContextJsonScriptTag(pageContext) {
  const pageContextSerialized = sanitizeJson(serializePageContextClientSide(pageContext));
  const htmlTag = `<script id="vike_pageContext" type="application/json">${pageContextSerialized}</script>`;
  pageContext._pageContextHtmlTag = htmlTag;
  return htmlTag;
}
function assertInjectFilterEntries(injectFilterEntries) {
  try {
    checkForWrongUsage(injectFilterEntries);
  } catch (err) {
    if (err?.message.includes("[Wrong Usage]")) {
      assert(false);
    }
    throw err;
  }
}
function assertInjectFilterUsage(injectFilterEntries) {
  checkForWrongUsage(injectFilterEntries);
  checkForWarnings(injectFilterEntries);
}
function checkForWrongUsage(injectFilterEntries) {
  injectFilterEntries.forEach((entry, i13) => {
    assertUsage2(isObject(entry), `[injectFilter()] Entry ${i13} isn't an object`);
    assertUsage2(typeof entry.src === "string", `[injectFilter()] Entry ${i13} is missing property ${import_picocolors19.default.cyan("src")}`);
    assertUsage2(entry[stamp] === true, `[injectFilter()] Entry ${i13} (${entry.src}) isn't the original object, see https://vike.dev/injectFilter`);
    assert([false, "HTML_BEGIN", "HTML_END"].includes(entry.inject));
    assert(entry.assetType === null || typeof entry.assetType === "string");
    assert(entry.mediaType === null || typeof entry.mediaType === "string");
    assert(typeof entry.isEntry === "boolean");
    assert(Object.keys(entry).length === 6);
  });
}
function checkForWarnings(injectFilterEntries) {
  injectFilterEntries.forEach((a18) => {
    if (a18.assetType === "style" && a18.isEntry) {
      assertWarning2(a18.inject, `[injectFilter()] We recommend against not injecting ${a18.src}`, {
        onlyOnce: true
      });
    }
    if (a18.assetType === "script") {
      assertWarning2(a18.inject, `[injectFilter()] We recommend against not preloading JavaScript (${a18.src})`, {
        onlyOnce: true
      });
    }
  });
}
function getInjectScriptsAt(pageId, pageConfigs) {
  if (pageConfigs.length === 0)
    return null;
  const pageConfig = getPageConfig(pageId, pageConfigs);
  const configValue = getConfigValueRuntime(pageConfig, "injectScriptsAt");
  if (!configValue)
    return null;
  const injectScriptsAt = configValue.value;
  assert(configValue.definedAtData);
  const configDefinedAt = getConfigDefinedAt("Config", "injectScriptsAt", configValue.definedAtData);
  assertUsage2(injectScriptsAt === null || injectScriptsAt === "HTML_BEGIN" || injectScriptsAt === "HTML_END" || injectScriptsAt === "HTML_STREAM", `${configDefinedAt} has an invalid value`);
  return injectScriptsAt;
}
var import_picocolors19, stamp;
var init_getHtmlTags = __esm({
  "node_modules/vike/dist/esm/node/runtime/html/injectAssets/getHtmlTags.js"() {
    init_utils3();
    init_serializePageContextClientSide();
    init_sanitizeJson();
    init_inferHtmlTags();
    init_mergeScriptTags();
    init_helpers();
    init_getConfigValue();
    init_globalContext();
    import_picocolors19 = __toESM(require_picocolors(), 1);
    init_getConfigDefinedAt();
    stamp = "__injectFilterEntry";
  }
});

// node_modules/vike/dist/esm/node/runtime/html/injectAssets/getViteDevScript.js
async function getViteDevScript() {
  const globalContext = getGlobalContext();
  if (globalContext.isProduction) {
    return "";
  }
  const { viteDevServer } = globalContext;
  const fakeHtmlBegin = "<html> <head>";
  const fakeHtmlEnd = "</head><body></body></html>";
  let fakeHtml = fakeHtmlBegin + fakeHtmlEnd;
  fakeHtml = await viteDevServer.transformIndexHtml("/", fakeHtml);
  assertUsage2(!fakeHtml.includes("vite-plugin-pwa"), `The HTML transformer of ${import_picocolors20.default.cyan("vite-plugin-pwa")} cannot be applied, see workaround at https://github.com/vikejs/vike/issues/388#issuecomment-1199280084`);
  assertUsage2(!fakeHtml.startsWith(fakeHtmlBegin.replace(" ", "")), "Vite plugins that minify the HTML are not supported by vike, see https://github.com/vikejs/vike/issues/224");
  assertUsage2(fakeHtml.startsWith(fakeHtmlBegin) && fakeHtml.endsWith(fakeHtmlEnd), "You are using a Vite Plugin that transforms the HTML in a way that conflicts with vike. Create a new GitHub ticket to discuss a solution.");
  const viteInjection = fakeHtml.slice(fakeHtmlBegin.length, -1 * fakeHtmlEnd.length);
  assert(viteInjection.includes("script"));
  assertWarning2(!viteInjection.includes("import("), "Unexpected Vite HMR code. Reach out to a Vike maintainer on GitHub.", { onlyOnce: true });
  const viteDevScript = viteInjection;
  return viteDevScript;
}
var import_picocolors20;
var init_getViteDevScript = __esm({
  "node_modules/vike/dist/esm/node/runtime/html/injectAssets/getViteDevScript.js"() {
    init_globalContext();
    init_utils3();
    import_picocolors20 = __toESM(require_picocolors(), 1);
  }
});

// node_modules/vike/dist/esm/node/runtime/html/injectAssets.js
async function injectHtmlTagsToString(htmlParts, pageContext, injectFilter) {
  const pageAssets = await pageContext.__getPageAssets();
  const viteDevScript = await getViteDevScript();
  const htmlTags = getHtmlTags(pageContext, null, injectFilter, pageAssets, viteDevScript, false);
  let htmlString = htmlPartsToString(htmlParts, pageAssets);
  htmlString = injectToHtmlBegin(htmlString, htmlTags);
  htmlString = injectToHtmlEnd(htmlString, htmlTags);
  assert(htmlTags.filter((snippet) => snippet.position === "HTML_STREAM").length === 0);
  return htmlString;
}
function injectHtmlTagsToStream(pageContext, streamFromReactStreamingPackage, injectFilter) {
  let htmlTags;
  return {
    injectAtStreamBegin,
    injectAtStreamAfterFirstChunk,
    injectAtStreamEnd
  };
  async function injectAtStreamBegin(htmlPartsBegin) {
    const pageAssets = await pageContext.__getPageAssets();
    const viteDevScript = await getViteDevScript();
    htmlTags = getHtmlTags(pageContext, streamFromReactStreamingPackage, injectFilter, pageAssets, viteDevScript, true);
    let htmlBegin = htmlPartsToString(htmlPartsBegin, pageAssets);
    htmlBegin = injectToHtmlBegin(htmlBegin, htmlTags);
    if (streamFromReactStreamingPackage) {
      injectHtmlTagsUsingStream(htmlTags, streamFromReactStreamingPackage);
    }
    return htmlBegin;
  }
  function injectAtStreamAfterFirstChunk() {
    if (streamFromReactStreamingPackage)
      return null;
    assert(htmlTags);
    const tags = htmlTags.filter((h6) => h6.position === "HTML_STREAM");
    if (tags.length === 0)
      return null;
    const htmlFragment = joinHtmlTags(tags);
    return htmlFragment;
  }
  async function injectAtStreamEnd(htmlPartsEnd) {
    assert(htmlTags);
    await resolvePageContextPromise(pageContext);
    const pageAssets = await pageContext.__getPageAssets();
    let htmlEnd = htmlPartsToString(htmlPartsEnd, pageAssets);
    htmlEnd = injectToHtmlEnd(htmlEnd, htmlTags);
    return htmlEnd;
  }
}
function injectToHtmlBegin(htmlBegin, htmlTags) {
  htmlBegin = createHtmlHeadIfMissing(htmlBegin);
  htmlBegin = injectHtmlTags(htmlBegin, htmlTags, "HTML_BEGIN");
  return htmlBegin;
}
function injectToHtmlEnd(htmlEnd, htmlTags) {
  htmlEnd = injectHtmlTags(htmlEnd, htmlTags, "HTML_END");
  return htmlEnd;
}
async function resolvePageContextPromise(pageContext) {
  const pageContextPromise = pageContext._pageContextPromise;
  if (!pageContextPromise) {
    return;
  }
  let pageContextProvidedByUser;
  if (isCallable(pageContextPromise)) {
    pageContextProvidedByUser = await pageContextPromise();
  } else if (isPromise(pageContextPromise)) {
    pageContextProvidedByUser = await pageContextPromise;
  } else {
    assert(false);
  }
  assertPageContextProvidedByUser(pageContextProvidedByUser, pageContext._renderHook);
  Object.assign(pageContext, pageContextProvidedByUser);
}
function htmlPartsToString(htmlParts, pageAssets) {
  let htmlString = "";
  htmlParts.forEach((p11) => {
    htmlString += typeof p11 === "string" ? p11 : p11(pageAssets);
  });
  return htmlString;
}
var init_injectAssets = __esm({
  "node_modules/vike/dist/esm/node/runtime/html/injectAssets.js"() {
    init_utils3();
    init_assertPageContextProvidedByUser();
    init_injectHtmlTags();
    init_getHtmlTags();
    init_getViteDevScript();
  }
});

// node_modules/vike/dist/esm/node/runtime/html/stream/react-streaming.js
function streamFromReactStreamingPackageToString(stream) {
  if (stream.pipe) {
    return streamPipeNodeToString(stream.pipe);
  }
  if (stream.readable) {
    return streamReadableWebToString(stream.readable);
  }
  assert(false);
}
function isStreamFromReactStreamingPackage(thing) {
  if (hasProp(thing, "injectToStream", "function")) {
    assertUsage2(hasProp(thing, "hasStreamEnded", "function"), isVikeReactApp() ? (
      //
      "Update vike-react to its latest version"
    ) : "Update react-streaming to its latest version");
    return true;
  }
  return false;
}
function getStreamOfReactStreamingPackage(stream) {
  if (stream.pipe) {
    return { __streamPipeNode: stream.pipe };
  }
  if (stream.readable) {
    return stream.readable;
  }
  assert(false);
}
var init_react_streaming = __esm({
  "node_modules/vike/dist/esm/node/runtime/html/stream/react-streaming.js"() {
    init_utils3();
    init_stream();
  }
});

// node_modules/vike/dist/esm/node/runtime/html/stream.js
function isStreamReadableWeb(thing) {
  return typeof ReadableStream !== "undefined" && thing instanceof ReadableStream;
}
function isStreamWritableWeb(thing) {
  return typeof WritableStream !== "undefined" && thing instanceof WritableStream;
}
function isStreamReadableNode(thing) {
  if (isStreamReadableWeb(thing)) {
    return false;
  }
  return hasProp(thing, "read", "function");
}
function isStreamWritableNode(thing) {
  if (isStreamWritableWeb(thing)) {
    return false;
  }
  return hasProp(thing, "write", "function");
}
async function streamReadableNodeToString(readableNode) {
  const chunks = [];
  return new Promise((resolve, reject) => {
    readableNode.on("data", (chunk2) => chunks.push(Buffer.from(chunk2)));
    readableNode.on("error", (err) => reject(err));
    readableNode.on("end", () => resolve(Buffer.concat(chunks).toString("utf8")));
  });
}
async function streamReadableWebToString(readableWeb) {
  const reader = readableWeb.getReader();
  const { decode: decode2, getClosingChunk } = decodeChunks();
  let str = "";
  while (true) {
    const { done, value } = await reader.read();
    if (done)
      break;
    str += decode2(value);
  }
  str += getClosingChunk();
  return str;
}
async function stringToStreamReadableNode(str) {
  const { Readable } = await loadStreamNodeModule();
  return Readable.from(str);
}
function stringToStreamReadableWeb(str) {
  assertReadableStreamConstructor();
  const readableStream = new ReadableStream({
    start(controller) {
      controller.enqueue(encodeForWebStream(str));
      controller.close();
    }
  });
  return readableStream;
}
function stringToStreamPipeNode(str) {
  return (writable) => {
    writable.write(str);
    writable.end();
  };
}
function stringToStreamPipeWeb(str) {
  return (writable) => {
    const writer = writable.getWriter();
    writer.write(encodeForWebStream(str));
    writer.close();
  };
}
async function streamPipeNodeToString(streamPipeNode) {
  let str = "";
  let resolve;
  let reject;
  const promise = new Promise((resolve_, reject_) => {
    resolve = () => resolve_(str);
    reject = reject_;
  });
  const { Writable } = await loadStreamNodeModule();
  const writable = new Writable({
    write(chunk2, _encoding, callback) {
      const s36 = chunk2.toString();
      assert(typeof s36 === "string");
      str += s36;
      callback();
    },
    final(callback) {
      resolve();
      callback();
    },
    destroy(err) {
      if (err) {
        reject(err);
      } else {
        resolve();
      }
    }
  });
  streamPipeNode(writable);
  return promise;
}
function streamPipeWebToString(streamPipeWeb) {
  const { decode: decode2, getClosingChunk } = decodeChunks();
  let str = "";
  let resolve;
  const promise = new Promise((r7) => resolve = r7);
  const writable = new WritableStream({
    write(chunk2) {
      str += decode2(chunk2);
    },
    close() {
      str += getClosingChunk();
      resolve(str);
    }
  });
  streamPipeWeb(writable);
  return promise;
}
async function getStreamReadableNode(htmlRender) {
  if (typeof htmlRender === "string") {
    return stringToStreamReadableNode(htmlRender);
  }
  if (isStreamReadableNode(htmlRender)) {
    return htmlRender;
  }
  return null;
}
function getStreamReadableWeb(htmlRender) {
  if (typeof htmlRender === "string") {
    return stringToStreamReadableWeb(htmlRender);
  }
  if (isStreamReadableWeb(htmlRender)) {
    return htmlRender;
  }
  return null;
}
function pipeToStreamWritableWeb(htmlRender, writable) {
  if (typeof htmlRender === "string") {
    const streamPipeWeb = stringToStreamPipeWeb(htmlRender);
    streamPipeWeb(writable);
    return true;
  }
  if (isStreamReadableWeb(htmlRender)) {
    htmlRender.pipeTo(writable);
    return true;
  }
  if (isStreamPipeWeb(htmlRender)) {
    const streamPipeWeb = getStreamPipeWeb(htmlRender);
    assert(streamPipeWeb);
    streamPipeWeb(writable);
    return true;
  }
  if (isStreamReadableNode(htmlRender) || isStreamPipeNode(htmlRender)) {
    return false;
  }
  checkType(htmlRender);
  assert(false);
}
function pipeToStreamWritableNode(htmlRender, writable) {
  if (typeof htmlRender === "string") {
    const streamPipeNode = stringToStreamPipeNode(htmlRender);
    streamPipeNode(writable);
    return true;
  }
  if (isStreamReadableNode(htmlRender)) {
    htmlRender.pipe(writable);
    return true;
  }
  if (isStreamPipeNode(htmlRender)) {
    const streamPipeNode = getStreamPipeNode(htmlRender);
    assert(streamPipeNode);
    streamPipeNode(writable);
    return true;
  }
  if (isStreamReadableWeb(htmlRender) || isStreamPipeWeb(htmlRender)) {
    return false;
  }
  checkType(htmlRender);
  assert(false);
}
async function processStream(streamOriginal, { injectStringAtBegin, injectStringAfterFirstChunk, injectStringAtEnd, onErrorWhileStreaming, enableEagerStreaming }) {
  const buffer = [];
  let streamOriginalHasStartedEmitting = false;
  let streamOriginalEnded = false;
  let streamClosed = false;
  let onEndWasCalled = false;
  let isReadyToWrite = false;
  let wrapperCreated = false;
  let shouldFlushStream = false;
  let resolve;
  let reject;
  let promiseHasResolved = false;
  let injectStringAfterFirstChunk_done = false;
  const streamWrapperPromise = new Promise((resolve_, reject_) => {
    resolve = (streamWrapper2) => {
      promiseHasResolved = true;
      resolve_(streamWrapper2);
    };
    reject = (err) => {
      promiseHasResolved = true;
      reject_(err);
    };
  });
  let resolveReadyToWrite;
  const promiseReadyToWrite = new Promise((r7) => resolveReadyToWrite = r7);
  if (injectStringAtBegin) {
    const injectedChunk = await injectStringAtBegin();
    writeStream(injectedChunk);
    flushStream();
  }
  const onStreamDataOrEnd = (cb) => {
    assert(streamOriginalEnded === false);
    streamOriginalHasStartedEmitting = true;
    cb();
    if (wrapperCreated)
      resolvePromise();
  };
  const { streamWrapper, streamWrapperOperations } = await createStreamWrapper({
    streamOriginal,
    onReadyToWrite() {
      debug4("stream begin");
      isReadyToWrite = true;
      flushBuffer();
      resolveReadyToWrite();
    },
    onError(err) {
      if (!promiseHasResolved) {
        reject(err);
      } else {
        onErrorWhileStreaming(err);
      }
    },
    onData(chunk2) {
      onStreamDataOrEnd(() => {
        writeStream(chunk2);
        if (injectStringAfterFirstChunk && !injectStringAfterFirstChunk_done) {
          const injectedChunk = injectStringAfterFirstChunk();
          if (injectedChunk !== null)
            writeStream(injectedChunk);
          injectStringAfterFirstChunk_done = true;
        }
      });
    },
    async onEnd(isCancel) {
      try {
        assert(!onEndWasCalled);
        onEndWasCalled = true;
        debug4("stream end");
        onStreamDataOrEnd(() => {
          streamOriginalEnded = true;
        });
        if (injectStringAtEnd) {
          const injectedChunk = await injectStringAtEnd();
          writeStream(injectedChunk);
        }
        await promiseReadyToWrite;
        assert(isReady());
        flushBuffer();
        streamClosed = true;
        debug4("stream ended");
      } catch (err) {
        if (!isBug(err)) {
          console.error(err);
          assert(false);
        }
        throw err;
      }
    },
    onFlush() {
      flushStream();
    }
  });
  wrapperCreated = true;
  flushBuffer();
  if (!delayStreamStart())
    resolvePromise();
  return streamWrapperPromise;
  function writeStream(chunk2) {
    buffer.push(chunk2);
    flushBuffer();
  }
  function flushBuffer() {
    if (!isReady())
      return;
    assert(!streamClosed);
    buffer.forEach((chunk2) => {
      streamWrapperOperations.writeChunk(chunk2);
    });
    buffer.length = 0;
    if (shouldFlushStream)
      flushStream();
  }
  function resolvePromise() {
    assert(!delayStreamStart());
    assert(wrapperCreated);
    debug4("stream promise resolved");
    resolve(streamWrapper);
  }
  function flushStream() {
    if (!isReady()) {
      shouldFlushStream = true;
      return;
    }
    if (streamWrapperOperations.flushStream === null)
      return;
    streamWrapperOperations.flushStream();
    shouldFlushStream = false;
    debug4("stream flushed");
  }
  function isReady() {
    return isReadyToWrite && // We can't use streamWrapperOperations.writeChunk() if it isn't defined yet
    wrapperCreated && // See comment below
    !delayStreamStart();
  }
  function delayStreamStart() {
    return !enableEagerStreaming && !streamOriginalHasStartedEmitting;
  }
}
async function createStreamWrapper({ streamOriginal, onError, onData, onEnd, onFlush, onReadyToWrite }) {
  if (isStreamFromReactStreamingPackage(streamOriginal)) {
    debug4(`onRenderHtml() hook returned ${import_picocolors21.default.cyan("react-streaming")} result`);
    const stream = getStreamOfReactStreamingPackage(streamOriginal);
    streamOriginal = stream;
  }
  if (isStreamPipeNode(streamOriginal)) {
    debug4("onRenderHtml() hook returned Node.js Stream Pipe");
    let writableOriginal = null;
    const pipeProxy = (writable_) => {
      writableOriginal = writable_;
      debug4("original Node.js Writable received");
      onReadyToWrite();
      if (hasEnded) {
        writableOriginal.end();
      }
    };
    stampPipe(pipeProxy, "node-stream");
    const writeChunk = (chunk2) => {
      assert(writableOriginal);
      writableOriginal.write(chunk2);
      debugWithChunk("data written (Node.js Writable)", chunk2);
    };
    const flushStream = () => {
      assert(writableOriginal);
      if (typeof writableOriginal.flush === "function") {
        writableOriginal.flush();
        debug4("stream flush() performed (Node.js Writable)");
      }
    };
    let hasEnded = false;
    const endStream = () => {
      hasEnded = true;
      if (writableOriginal) {
        writableOriginal.end();
      }
    };
    const { Writable } = await loadStreamNodeModule();
    const writableProxy = new Writable({
      async write(chunk2, _encoding, callback) {
        onData(chunk2);
        callback();
      },
      async destroy(err, callback) {
        if (err) {
          onError(err);
        } else {
          await onEnd();
        }
        callback(err);
        endStream();
      }
    });
    objectAssign(writableProxy, {
      flush: () => {
        onFlush();
      }
    });
    assert(typeof writableProxy.flush === "function");
    const pipeOriginal = getStreamPipeNode(streamOriginal);
    pipeOriginal(writableProxy);
    return { streamWrapper: pipeProxy, streamWrapperOperations: { writeChunk, flushStream } };
  }
  if (isStreamPipeWeb(streamOriginal)) {
    debug4("onRenderHtml() hook returned Web Stream Pipe");
    let writerOriginal = null;
    const pipeProxy = (writableOriginal) => {
      writerOriginal = writableOriginal.getWriter();
      debug4("original Web Writable received");
      (async () => {
        try {
          await writerOriginal.ready;
        } catch (e14) {
        }
        onReadyToWrite();
        if (hasEnded) {
          writerOriginal.close();
        }
      })();
    };
    stampPipe(pipeProxy, "web-stream");
    const writeChunk = (chunk2) => {
      assert(writerOriginal);
      writerOriginal.write(encodeForWebStream(chunk2));
      debugWithChunk("data written (Web Writable)", chunk2);
    };
    const flushStream = null;
    let hasEnded = false;
    const endStream = () => {
      hasEnded = true;
      if (writerOriginal) {
        writerOriginal.close();
      }
    };
    let writableProxy;
    if (typeof ReadableStream !== "function") {
      writableProxy = new WritableStream({
        write(chunk2) {
          onData(chunk2);
        },
        async close() {
          await onEnd();
          endStream();
        },
        abort(err) {
          onError(err);
          endStream();
        }
      });
    } else {
      const { readable, writable } = new TransformStream();
      writableProxy = writable;
      handleReadableWeb(readable, {
        onData,
        onError(err) {
          onError(err);
          endStream();
        },
        async onEnd() {
          await onEnd();
          endStream();
        }
      });
    }
    const pipeOriginal = getStreamPipeWeb(streamOriginal);
    pipeOriginal(writableProxy);
    return { streamWrapper: pipeProxy, streamWrapperOperations: { writeChunk, flushStream } };
  }
  if (isStreamReadableWeb(streamOriginal)) {
    debug4("onRenderHtml() hook returned Web Readable");
    const readableOriginal = streamOriginal;
    let controllerProxyIsClosed = false;
    let isClosed = false;
    let isCancel = false;
    const closeStream = async () => {
      if (isClosed)
        return;
      isClosed = true;
      await onEnd(isCancel);
      controllerProxy.close();
      controllerProxyIsClosed = true;
    };
    let controllerProxy;
    assertReadableStreamConstructor();
    const readableProxy = new ReadableStream({
      start(controller) {
        controllerProxy = controller;
        onReadyToWrite();
        handleReadableWeb(readableOriginal, {
          onData,
          onError(err) {
            onError(err);
            controllerProxy.close();
          },
          async onEnd() {
            await closeStream();
          }
        });
      },
      async cancel(...args) {
        isCancel = true;
        await readableOriginal.cancel(...args);
        await closeStream();
      }
    });
    const writeChunk = (chunk2) => {
      if (
        // If readableOriginal doesn't implement readableOriginal.cancel() then it may still emit data after we close the stream. We therefore need to check whether we closed `controllerProxy`.
        !controllerProxyIsClosed
      ) {
        controllerProxy.enqueue(encodeForWebStream(chunk2));
        debugWithChunk("data written (Web Readable)", chunk2);
      } else {
        debugWithChunk("data emitted but not written (Web Readable)", chunk2);
      }
    };
    const flushStream = null;
    return {
      streamWrapper: readableProxy,
      streamWrapperOperations: { writeChunk, flushStream }
    };
  }
  if (isStreamReadableNode(streamOriginal)) {
    debug4("onRenderHtml() hook returned Node.js Readable");
    const readableOriginal = streamOriginal;
    const { Readable } = await loadStreamNodeModule();
    if (readableOriginal._read === Readable.prototype._read) {
      readableOriginal._read = function() {
      };
    }
    const writeChunk = (chunk2) => {
      readableProxy.push(chunk2);
      debugWithChunk("data written (Node.js Readable)", chunk2);
    };
    const flushStream = null;
    const closeProxy = () => {
      readableProxy.push(null);
    };
    const readableProxy = new Readable({ read() {
    } });
    onReadyToWrite();
    readableOriginal.on("data", (chunk2) => {
      onData(chunk2);
    });
    readableOriginal.on("error", (err) => {
      onError(err);
      closeProxy();
    });
    readableOriginal.on("end", async () => {
      await onEnd();
      closeProxy();
    });
    return {
      streamWrapper: readableProxy,
      streamWrapperOperations: { writeChunk, flushStream }
    };
  }
  assert(false);
}
async function handleReadableWeb(readable, { onData, onError, onEnd }) {
  const reader = readable.getReader();
  while (true) {
    let result;
    try {
      result = await reader.read();
    } catch (err) {
      onError(err);
      return;
    }
    const { value, done } = result;
    if (done) {
      break;
    }
    onData(value);
  }
  await onEnd();
}
function isStream(something) {
  if (isStreamReadableWeb(something) || isStreamReadableNode(something) || isStreamPipeNode(something) || isStreamPipeWeb(something) || isStreamFromReactStreamingPackage(something)) {
    checkType(something);
    return true;
  }
  return false;
}
function getStreamPipeWeb(thing) {
  if (!isStreamPipeWeb(thing)) {
    return null;
  }
  if (isObject(thing)) {
    assert(__streamPipeWeb && thing);
    return thing[__streamPipeWeb];
  } else {
    assert(isCallable(thing) && "isWebStreamPipe" in thing);
    return thing;
  }
}
function isStreamPipeWeb(thing) {
  if (isObject(thing) && __streamPipeWeb in thing) {
    return true;
  }
  if (isCallable(thing) && "isWebStreamPipe" in thing) {
    return true;
  }
  return false;
}
function getStreamPipeNode(thing) {
  if (!isStreamPipeNode(thing)) {
    return null;
  }
  if (isObject(thing)) {
    assert(__streamPipeNode in thing);
    return thing[__streamPipeNode];
  } else {
    assert(isCallable(thing) && "isNodeStreamPipe" in thing);
    return thing;
  }
}
function isStreamPipeNode(thing) {
  if (isObject(thing) && __streamPipeNode in thing) {
    return true;
  }
  if (isCallable(thing) && "isNodeStreamPipe" in thing) {
    return true;
  }
  return false;
}
function stampPipe(pipe2, pipeType) {
  assertUsage2(pipeType, `stampPipe(pipe, pipeType): argument ${import_picocolors21.default.cyan("pipeType")} is missing.)`, {
    showStackTrace: true
  });
  assertUsage2(["web-stream", "node-stream"].includes(pipeType), `stampPipe(pipe, pipeType): argument ${import_picocolors21.default.cyan("pipeType")} should be either ${import_picocolors21.default.cyan("'web-stream'")} or ${import_picocolors21.default.cyan("'node-stream'")}.`, { showStackTrace: true });
  if (pipeType === "node-stream") {
    Object.assign(pipe2, { isNodeStreamPipe: true });
  } else {
    Object.assign(pipe2, { isWebStreamPipe: true });
  }
}
async function streamToString(stream) {
  if (isStreamReadableWeb(stream)) {
    return await streamReadableWebToString(stream);
  }
  if (isStreamReadableNode(stream)) {
    return await streamReadableNodeToString(stream);
  }
  if (isStreamPipeNode(stream)) {
    return await streamPipeNodeToString(getStreamPipeNode(stream));
  }
  if (isStreamPipeWeb(stream)) {
    return await streamPipeWebToString(getStreamPipeWeb(stream));
  }
  if (isStreamFromReactStreamingPackage(stream)) {
    return await streamFromReactStreamingPackageToString(stream);
  }
  assert(false);
}
function assertReadableStreamConstructor() {
  assertUsage2(
    typeof ReadableStream === "function",
    // Error message copied from vue's renderToWebStream() implementation
    "ReadableStream constructor isn't available in the global scope. If the target environment does support web streams, consider using pipeToWebWritable() with an existing WritableStream instance instead."
  );
}
function encodeForWebStream(thing) {
  if (!encoder) {
    encoder = new TextEncoder();
  }
  if (typeof thing === "string") {
    return encoder.encode(thing);
  }
  return thing;
}
async function loadStreamNodeModule() {
  const streamModule = (await (0, import_import.import_)("stream")).default;
  const { Readable, Writable } = streamModule;
  return { Readable, Writable };
}
function getStreamName(kind, type2) {
  let typeName = capitalizeFirstLetter(type2);
  if (typeName === "Node") {
    typeName = "Node.js";
  }
  const kindName = capitalizeFirstLetter(kind);
  if (kind !== "pipe") {
    return `a ${kindName} ${typeName} Stream`;
  }
  if (kind === "pipe") {
    return `a ${typeName} Stream Pipe`;
  }
  assert(false);
}
function inferStreamName(stream) {
  if (isStreamReadableWeb(stream)) {
    return getStreamName("readable", "web");
  }
  if (isStreamReadableNode(stream)) {
    return getStreamName("readable", "node");
  }
  if (isStreamPipeNode(stream)) {
    return getStreamName("pipe", "node");
  }
  if (isStreamPipeWeb(stream)) {
    return getStreamName("pipe", "web");
  }
  assert(false);
}
function decodeChunks() {
  const decoder = new TextDecoder();
  const decode2 = (chunk2) => {
    if (typeof chunk2 === "string") {
      return chunk2;
    } else if (chunk2 instanceof Uint8Array) {
      return decoder.decode(chunk2, { stream: true });
    } else {
      assert(false);
    }
  };
  const getClosingChunk = () => {
    return decoder.decode();
  };
  return { decode: decode2, getClosingChunk };
}
function debugWithChunk(msg, chunk2) {
  if (!debug4.isActivated)
    return;
  let chunkStr;
  try {
    chunkStr = new TextDecoder().decode(chunk2);
  } catch (err) {
    chunkStr = String(chunk2);
  }
  debug4(msg, chunkStr);
}
var import_import, import_picocolors21, debug4, __streamPipeWeb, __streamPipeNode, encoder;
var init_stream = __esm({
  "node_modules/vike/dist/esm/node/runtime/html/stream.js"() {
    init_utils3();
    init_react_streaming();
    import_import = __toESM(require_dist2(), 1);
    import_picocolors21 = __toESM(require_picocolors(), 1);
    debug4 = createDebugger("vike:stream");
    __streamPipeWeb = "__streamPipeWeb";
    __streamPipeNode = "__streamPipeNode";
  }
});

// node_modules/vike/dist/esm/node/runtime/html/renderHtml.js
function isDocumentHtml(something) {
  if (isTemplateWrapped(something) || isEscapedString(something) || isStream(something)) {
    checkType(something);
    return true;
  }
  return false;
}
async function renderDocumentHtml(documentHtml, pageContext, onErrorWhileStreaming, injectFilter) {
  if (isEscapedString(documentHtml)) {
    objectAssign(pageContext, { _isStream: false });
    let htmlString = getEscapedString(documentHtml);
    htmlString = await injectHtmlTagsToString([htmlString], pageContext, injectFilter);
    return htmlString;
  }
  if (isStream(documentHtml)) {
    objectAssign(pageContext, { _isStream: true });
    const stream = documentHtml;
    const streamWrapper = await renderHtmlStream(stream, null, pageContext, onErrorWhileStreaming, injectFilter);
    return streamWrapper;
  }
  if (isTemplateWrapped(documentHtml)) {
    const templateContent = documentHtml._template;
    const render = await renderTemplate(templateContent, pageContext);
    if (!("htmlStream" in render)) {
      objectAssign(pageContext, { _isStream: false });
      const { htmlPartsAll } = render;
      const htmlString = await injectHtmlTagsToString(htmlPartsAll, pageContext, injectFilter);
      return htmlString;
    } else {
      objectAssign(pageContext, { _isStream: true });
      const { htmlStream } = render;
      const streamWrapper = await renderHtmlStream(htmlStream, {
        htmlPartsBegin: render.htmlPartsBegin,
        htmlPartsEnd: render.htmlPartsEnd
      }, pageContext, onErrorWhileStreaming, injectFilter);
      return streamWrapper;
    }
  }
  checkType(documentHtml);
  assert(false);
}
async function renderHtmlStream(streamOriginal, injectString, pageContext, onErrorWhileStreaming, injectFilter) {
  const processStreamOptions = {
    onErrorWhileStreaming,
    enableEagerStreaming: pageContext.enableEagerStreaming
  };
  if (injectString) {
    let streamFromReactStreamingPackage = null;
    if (isStreamFromReactStreamingPackage(streamOriginal) && !streamOriginal.disabled) {
      streamFromReactStreamingPackage = streamOriginal;
    }
    const { injectAtStreamBegin, injectAtStreamAfterFirstChunk, injectAtStreamEnd } = injectHtmlTagsToStream(pageContext, streamFromReactStreamingPackage, injectFilter);
    processStreamOptions.injectStringAtBegin = async () => {
      return await injectAtStreamBegin(injectString.htmlPartsBegin);
    };
    processStreamOptions.injectStringAtEnd = async () => {
      return await injectAtStreamEnd(injectString.htmlPartsEnd);
    };
    processStreamOptions.injectStringAfterFirstChunk = () => {
      return injectAtStreamAfterFirstChunk();
    };
  }
  const streamWrapper = await processStream(streamOriginal, processStreamOptions);
  return streamWrapper;
}
function isTemplateWrapped(something) {
  return hasProp(something, "_template");
}
function isEscapedString(something) {
  const result = hasProp(something, "_escaped");
  if (result) {
    assert(hasProp(something, "_escaped", "string"));
    checkType(something);
  }
  return result;
}
function getEscapedString(escapedString) {
  let htmlString;
  assert(hasProp(escapedString, "_escaped"));
  htmlString = escapedString._escaped;
  assert(typeof htmlString === "string");
  return htmlString;
}
function escapeInject(templateStrings, ...templateVariables) {
  assertUsage2(templateStrings.length === templateVariables.length + 1 && templateStrings.every((str) => typeof str === "string"), `You're using ${import_picocolors22.default.cyan("escapeInject")} as a function, but ${import_picocolors22.default.cyan("escapeInject")} is a string template tag, see https://vike.dev/escapeInject`, { showStackTrace: true });
  return {
    _template: {
      templateStrings,
      templateVariables
    }
  };
}
function dangerouslySkipEscape(alreadyEscapedString) {
  return _dangerouslySkipEscape(alreadyEscapedString);
}
function _dangerouslySkipEscape(arg) {
  if (hasProp(arg, "_escaped")) {
    assert(isEscapedString(arg));
    return arg;
  }
  assertUsage2(!isPromise(arg), `[dangerouslySkipEscape(${import_picocolors22.default.cyan("str")})] Argument ${import_picocolors22.default.cyan("str")} is a promise. It should be a string instead (or a stream). Make sure to ${import_picocolors22.default.cyan("await str")}.`, { showStackTrace: true });
  if (typeof arg === "string") {
    return { _escaped: arg };
  }
  assertWarning2(false, `[dangerouslySkipEscape(${import_picocolors22.default.cyan("str")})] Argument ${import_picocolors22.default.cyan("str")} should be a string but we got ${import_picocolors22.default.cyan(`typeof str === "${typeof arg}"`)}.`, {
    onlyOnce: false,
    showStackTrace: true
  });
  return { _escaped: String(arg) };
}
async function renderTemplate(templateContent, pageContext) {
  const htmlPartsBegin = [];
  const htmlPartsEnd = [];
  let htmlStream = null;
  const addHtmlPart = (htmlPart) => {
    if (htmlStream === null) {
      htmlPartsBegin.push(htmlPart);
    } else {
      htmlPartsEnd.push(htmlPart);
    }
  };
  const setStream = (stream) => {
    const { hookName, hookFilePath } = pageContext._renderHook;
    assertUsage2(!htmlStream, `Injecting two streams in ${import_picocolors22.default.cyan("escapeInject")} template tag of ${hookName}() hook defined by ${hookFilePath}. Inject only one stream instead.`);
    htmlStream = stream;
  };
  const { templateStrings, templateVariables } = templateContent;
  for (let i13 = 0; i13 < templateVariables.length; i13++) {
    addHtmlPart(templateStrings[i13]);
    let templateVar = templateVariables[i13];
    if (isEscapedString(templateVar)) {
      const htmlString = getEscapedString(templateVar);
      addHtmlPart(htmlString);
      continue;
    }
    if (isTemplateWrapped(templateVar)) {
      const templateContentInner = templateVar._template;
      const result = await renderTemplate(templateContentInner, pageContext);
      if (!("htmlStream" in result)) {
        result.htmlPartsAll.forEach(addHtmlPart);
      } else {
        result.htmlPartsBegin.forEach(addHtmlPart);
        setStream(result.htmlStream);
        result.htmlPartsEnd.forEach(addHtmlPart);
      }
      continue;
    }
    if (isStream(templateVar)) {
      setStream(templateVar);
      continue;
    }
    const getErrMsg2 = (msg) => {
      const { hookName, hookFilePath } = pageContext._renderHook;
      const nth = i13 === 0 && "1st" || i13 === 1 && "2nd" || i13 === 2 && "3rd" || `${i13}-th`;
      return [
        `The ${nth} HTML variable is ${msg}`,
        `The HTML was provided by the ${hookName}() hook at ${hookFilePath}.`
      ].filter(Boolean).join(" ");
    };
    assertUsage2(!isPromise(templateVar), getErrMsg2(`a promise, did you forget to ${import_picocolors22.default.cyan("await")} the promise?`));
    if (templateVar === void 0 || templateVar === null) {
      const msgVal = import_picocolors22.default.cyan(String(templateVar));
      const msgEmptyString = import_picocolors22.default.cyan("''");
      const msg = `${msgVal} which will be converted to an empty string. Pass the empty string ${msgEmptyString} instead of ${msgVal} to remove this warning.`;
      assertWarning2(false, getErrMsg2(msg), { onlyOnce: false });
      templateVar = "";
    }
    {
      const varType = typeof templateVar;
      if (varType !== "string") {
        const msgType = import_picocolors22.default.cyan(`typeof htmlVariable === "${varType}"`);
        const msg = `${msgType} but a string or stream (https://vike.dev/streaming) is expected instead.`;
        assertUsage2(false, getErrMsg2(msg));
      }
    }
    {
      const { isProduction } = getGlobalContext();
      if (isHtml(templateVar) && // We don't show this warning in production because it's expected that some users may (un)willingly do some XSS injection: we avoid flooding the production logs.
      !isProduction) {
        const msgVal = import_picocolors22.default.cyan(String(templateVar));
        const msg = `${msgVal} which seems to be HTML code. Did you forget to wrap the value with dangerouslySkipEscape()?`;
        assertWarning2(false, getErrMsg2(msg), { onlyOnce: false });
      }
    }
    addHtmlPart(escapeHtml(templateVar));
  }
  assert(templateStrings.length === templateVariables.length + 1);
  addHtmlPart(templateStrings[templateStrings.length - 1]);
  if (htmlStream === null) {
    assert(htmlPartsEnd.length === 0);
    return {
      htmlPartsAll: htmlPartsBegin
    };
  }
  return {
    htmlStream,
    htmlPartsBegin,
    htmlPartsEnd
  };
}
async function getHtmlString(htmlRender) {
  if (typeof htmlRender === "string") {
    return htmlRender;
  }
  if (isStream(htmlRender)) {
    return streamToString(htmlRender);
  }
  checkType(htmlRender);
  assert(false);
}
var import_picocolors22;
var init_renderHtml = __esm({
  "node_modules/vike/dist/esm/node/runtime/html/renderHtml.js"() {
    init_utils3();
    init_injectAssets();
    init_stream();
    init_react_streaming();
    init_globalContext();
    import_picocolors22 = __toESM(require_picocolors(), 1);
  }
});

// node_modules/vike/dist/esm/shared/getPageContextUrlComputed.js
function getPageContextUrlComputed(pageContext) {
  assert(typeof pageContext.urlOriginal === "string");
  assertPageContextUrlComputed(pageContext);
  const pageContextUrlComputed = {};
  objectDefineProperty(pageContextUrlComputed, "urlPathname", {
    get: urlPathnameGetter,
    enumerable: true,
    configurable: true
  });
  objectDefineProperty(pageContextUrlComputed, "url", {
    get: urlGetter,
    enumerable: false,
    configurable: true
  });
  objectDefineProperty(pageContextUrlComputed, "urlParsed", {
    get: urlParsedGetter,
    enumerable: true,
    configurable: true
  });
  return pageContextUrlComputed;
}
function getUrlParsed(pageContext) {
  let urlResolved;
  let baseToBeRemoved;
  if (pageContext.urlLogical) {
    urlResolved = pageContext.urlLogical;
    baseToBeRemoved = false;
  } else if (pageContext._urlRewrite) {
    urlResolved = pageContext._urlRewrite;
    baseToBeRemoved = false;
  } else {
    urlResolved = pageContext.urlOriginal;
    baseToBeRemoved = true;
  }
  assert(urlResolved && typeof urlResolved === "string");
  let urlHandler = pageContext._urlHandler;
  if (!urlHandler)
    urlHandler = (url) => url;
  urlResolved = urlHandler(urlResolved);
  const baseServer2 = !baseToBeRemoved ? "/" : pageContext._baseServer;
  return parseUrl(urlResolved, baseServer2);
}
function urlPathnameGetter() {
  const { pathname } = getUrlParsed(this);
  const urlPathname = pathname;
  assert(urlPathname.startsWith("/"));
  return urlPathname;
}
function urlGetter() {
  assertWarning2(false, "`pageContext.url` is outdated. Use `pageContext.urlPathname`, `pageContext.urlParsed`, or `pageContext.urlOriginal` instead. (See https://vike.dev/migration/0.4.23 for more information.)", { onlyOnce: true, showStackTrace: true });
  return urlPathnameGetter.call(this);
}
function urlParsedGetter() {
  const {
    // remove hasBaseServer as it isn't part of UrlPublic
    hasBaseServer: _2,
    ...urlParsed
  } = getUrlParsed(this);
  const hashIsAvailable = isBrowser();
  const warnHashNotAvailable = (prop) => {
    assertWarning2(hashIsAvailable, `pageContext.urlParsed.${prop} isn't available on the server-side (HTTP requests don't include the URL hash)`, { onlyOnce: true, showStackTrace: true });
  };
  const urlParsedEnhanced = {
    ...urlParsed,
    get hash() {
      warnHashNotAvailable("hash");
      return urlParsed.hash;
    },
    get hashOriginal() {
      warnHashNotAvailable("hashOriginal");
      return urlParsed.hashOriginal;
    },
    // TODO/next-major-release: remove
    get hashString() {
      assertWarning2(false, "pageContext.urlParsed.hashString has been renamed to pageContext.urlParsed.hashOriginal", {
        onlyOnce: true,
        showStackTrace: true
      });
      warnHashNotAvailable("hashString");
      return urlParsed.hashOriginal;
    },
    // TODO/next-major-release: remove
    get searchString() {
      assertWarning2(false, "pageContext.urlParsed.searchString has been renamed to pageContext.urlParsed.searchOriginal", { onlyOnce: true, showStackTrace: true });
      return urlParsed.searchOriginal;
    }
  };
  changeEnumerable(urlParsedEnhanced, "hashString", false);
  changeEnumerable(urlParsedEnhanced, "searchString", false);
  if (!hashIsAvailable) {
    changeEnumerable(urlParsedEnhanced, "hash", false);
    changeEnumerable(urlParsedEnhanced, "hashOriginal", false);
  }
  return urlParsedEnhanced;
}
function assertPageContextUrl(pageContext) {
  assert(typeof pageContext.urlOriginal === "string");
  assert(typeof pageContext.urlPathname === "string");
  assert(isPlainObject(pageContext.urlParsed));
  assert(pageContext.urlPathname === pageContext.urlParsed.pathname);
  assertPageContextUrlComputed(pageContext);
}
function assertPageContextUrlComputed(pageContext) {
  if ("urlPathname" in pageContext) {
    assert(typeof pageContext.urlPathname === "string");
    assert(isPropertyGetter(pageContext, "urlPathname"));
    assert(isPropertyGetter(pageContext, "urlParsed"));
    assert(isPropertyGetter(pageContext, "url"));
  } else {
    assert(!("urlParsed" in pageContext));
    assert(!("url" in pageContext));
  }
}
var init_getPageContextUrlComputed = __esm({
  "node_modules/vike/dist/esm/shared/getPageContextUrlComputed.js"() {
    init_objectDefineProperty();
    init_utils();
  }
});

// node_modules/vike/dist/esm/node/runtime/renderPage/getHttpResponseBody.js
function getHttpResponseBody(htmlRender, renderHook) {
  if (typeof htmlRender !== "string") {
    assertUsage2(false, getErrMsg(htmlRender, renderHook, "body", `Use ${import_picocolors23.default.cyan("pageContext.httpResponse.pipe()")} instead`));
  }
  const body = htmlRender;
  return body;
}
function getHttpResponseBodyStreamHandlers(htmlRender, renderHook) {
  return {
    pipe(writable) {
      const getErrMsgMixingStreamTypes = (writableType) => `The ${getErrMsgBody(htmlRender, renderHook)} while a ${writableType} was passed to pageContext.httpResponse.pipe() which is contradictory. You cannot mix a Web Stream with a Node.js Stream.`;
      if (isStreamWritableWeb(writable)) {
        const success = pipeToStreamWritableWeb(htmlRender, writable);
        if (success) {
          return;
        } else {
          assert(isStreamReadableNode(htmlRender) || isStreamPipeNode(htmlRender));
          assertUsage2(false, getErrMsgMixingStreamTypes("Web Writable"));
        }
      }
      if (isStreamWritableNode(writable)) {
        const success = pipeToStreamWritableNode(htmlRender, writable);
        if (success) {
          return;
        } else {
          assert(isStreamReadableWeb(htmlRender) || isStreamPipeWeb(htmlRender));
          assertUsage2(false, getErrMsgMixingStreamTypes("Node.js Writable"));
        }
      }
      assertUsage2(false, `The argument ${import_picocolors23.default.cyan("writable")} passed to ${import_picocolors23.default.cyan("pageContext.httpResponse.pipe(writable)")} doesn't seem to be ${getStreamName("writable", "web")} nor ${getStreamName("writable", "node")}.`);
    },
    getReadableWebStream() {
      const webStream = getStreamReadableWeb(htmlRender);
      if (webStream === null) {
        assertUsage2(false, getErrMsg(htmlRender, renderHook, "getReadableWebStream()", getFixMsg("readable", "web")));
      }
      return webStream;
    },
    async getReadableNodeStream() {
      const nodeStream = await getStreamReadableNode(htmlRender);
      if (nodeStream === null) {
        assertUsage2(false, getErrMsg(htmlRender, renderHook, "getReadableNodeStream()", getFixMsg("readable", "node")));
      }
      return nodeStream;
    },
    async getBody() {
      const body = await getHtmlString(htmlRender);
      return body;
    },
    // TODO/v1-release: remove
    async getNodeStream() {
      assertWarning2(false, "`pageContext.httpResponse.getNodeStream()` is outdated, use `pageContext.httpResponse.getReadableNodeStream()` instead. " + streamDocs, { onlyOnce: true, showStackTrace: true });
      const nodeStream = await getStreamReadableNode(htmlRender);
      if (nodeStream === null) {
        assertUsage2(false, getErrMsg(htmlRender, renderHook, "getNodeStream()", getFixMsg("readable", "node")));
      }
      return nodeStream;
    },
    // TODO/v1-release: remove
    getWebStream() {
      assertWarning2(false, "`pageContext.httpResponse.getWebStream(res)` is outdated, use `pageContext.httpResponse.getReadableWebStream(res)` instead. " + streamDocs, { onlyOnce: true, showStackTrace: true });
      const webStream = getStreamReadableWeb(htmlRender);
      if (webStream === null) {
        assertUsage2(false, getErrMsg(htmlRender, renderHook, "getWebStream()", getFixMsg("readable", "web")));
      }
      return webStream;
    },
    // TODO/v1-release: remove
    pipeToWebWritable(writable) {
      assertWarning2(false, "`pageContext.httpResponse.pipeToWebWritable(res)` is outdated, use `pageContext.httpResponse.pipe(res)` instead. " + streamDocs, { onlyOnce: true, showStackTrace: true });
      const success = pipeToStreamWritableWeb(htmlRender, writable);
      if (!success) {
        assertUsage2(false, getErrMsg(htmlRender, renderHook, "pipeToWebWritable()"));
      }
    },
    // TODO/v1-release: remove
    pipeToNodeWritable(writable) {
      assertWarning2(false, "`pageContext.httpResponse.pipeToNodeWritable(res)` is outdated, use `pageContext.httpResponse.pipe(res)` instead. " + streamDocs, { onlyOnce: true, showStackTrace: true });
      const success = pipeToStreamWritableNode(htmlRender, writable);
      if (!success) {
        assertUsage2(false, getErrMsg(htmlRender, renderHook, "pipeToNodeWritable()"));
      }
    }
  };
  function getFixMsg(kind, type2) {
    const streamName = getStreamName(kind, type2);
    assert(["a ", "an ", "the "].some((s36) => streamName.startsWith(s36)));
    assert(renderHook);
    const { hookFilePath, hookName } = renderHook;
    return `Make sure the ${hookName}() defined by ${hookFilePath} hook provides ${streamName} instead`;
  }
}
function getErrMsg(htmlRender, renderHook, method, msgAddendum) {
  assert(!msgAddendum || !msgAddendum.endsWith("."));
  const errMsgBody = getErrMsgBody(htmlRender, renderHook);
  return [`pageContext.httpResponse.${method} can't be used because the ${errMsgBody}`, msgAddendum, streamDocs].filter(Boolean).join(". ");
}
function getErrMsgBody(htmlRender, renderHook) {
  assert(renderHook);
  const { hookFilePath, hookName } = renderHook;
  const hookReturnType = getHookReturnType(htmlRender);
  assert(["a ", "an ", "the "].some((s36) => hookReturnType.startsWith(s36)));
  const errMsgBody = `${hookName}() hook defined by ${hookFilePath} provides ${hookReturnType}`;
  assert(!errMsgBody.endsWith(" "));
  return errMsgBody;
}
function getHookReturnType(htmlRender) {
  if (typeof htmlRender === "string") {
    return "an HTML string";
  } else if (isStream(htmlRender)) {
    return inferStreamName(htmlRender);
  } else {
    assert(false);
  }
}
var import_picocolors23, streamDocs;
var init_getHttpResponseBody = __esm({
  "node_modules/vike/dist/esm/node/runtime/renderPage/getHttpResponseBody.js"() {
    init_stream();
    init_utils3();
    init_renderHtml();
    import_picocolors23 = __toESM(require_picocolors(), 1);
    streamDocs = "See https://vike.dev/streaming for more information.";
  }
});

// node_modules/vike/dist/esm/node/runtime/renderPage/getEarlyHints.js
function getEarlyHints(assets) {
  const earlyHints = [];
  {
    assets.forEach((asset) => {
      if (isFontFallback(asset, earlyHints))
        return;
      earlyHints.push({
        ...asset,
        earlyHintLink: inferEarlyHintLink(asset)
      });
    });
  }
  return earlyHints;
}
function isFontFallback(asset, earlyHints) {
  if (asset.assetType !== "font") {
    return false;
  }
  const fontUrlBase = removeFileExtentionAndHash(asset.src);
  return earlyHints.some((hint3) => {
    return hint3.assetType === "font" && removeFileExtentionAndHash(hint3.src) === fontUrlBase;
  });
}
function removeFileExtentionAndHash(assetUrl) {
  assert(!assetUrl.includes("\\"));
  assert(!assetUrl.endsWith(".js"));
  const paths = assetUrl.split("/");
  {
    const filename = paths[paths.length - 1];
    const filenameParts = filename.split(".");
    assert(filenameParts.length >= 2);
    const filenameBase = filenameParts.slice(0, filenameParts.length === 2 ? -1 : -2);
    assert(filenameBase.length >= 1);
    paths[paths.length - 1] = filenameBase.join(".");
  }
  return paths.join("/");
}
var init_getEarlyHints = __esm({
  "node_modules/vike/dist/esm/node/runtime/renderPage/getEarlyHints.js"() {
    init_inferHtmlTags();
    init_utils3();
  }
});

// node_modules/vike/dist/esm/node/runtime/renderPage/createHttpResponse/getCacheControl.js
function getCacheControl(pageId, pageConfigs, statusCode) {
  if (pageConfigs.length === 0)
    return defaultValue;
  if (statusCode > 499)
    return defaultValue;
  const pageConfig = getPageConfig(pageId, pageConfigs);
  const configValue = getConfigValueRuntime(pageConfig, "cacheControl", "string");
  const value = configValue?.value;
  if (value)
    return value;
  return defaultValue;
}
var defaultValue;
var init_getCacheControl = __esm({
  "node_modules/vike/dist/esm/node/runtime/renderPage/createHttpResponse/getCacheControl.js"() {
    init_helpers();
    init_getConfigValue();
    defaultValue = "no-store, max-age=0";
  }
});

// node_modules/vike/dist/esm/node/runtime/renderPage/createHttpResponse/assertNoInfiniteHttpRedirect.js
function assertNoInfiniteHttpRedirect(urlRedirectTarget, urlLogical) {
  if (!urlRedirectTarget.startsWith("/")) {
    return;
  }
  assert(urlLogical.startsWith("/"));
  const graph = copy(globalObject7.redirectGraph);
  graph[urlRedirectTarget] ?? (graph[urlRedirectTarget] = /* @__PURE__ */ new Set());
  graph[urlRedirectTarget].add(urlLogical);
  validate(graph);
  globalObject7.redirectGraph = graph;
}
function copy(G3) {
  return Object.fromEntries(Object.entries(G3).map(([key, val]) => [key, new Set(val)]));
}
function validate(G3) {
  Object.keys(G3).forEach((n13) => check(G3, n13, []));
}
function check(G3, n13, path) {
  if (path.includes(n13)) {
    const cycle = path.slice(path.indexOf(n13)).concat(n13);
    assertUsage2(false, `Infinite loop of HTTP URL redirects: ${cycle.map(import_picocolors24.default.cyan).join(" -> ")}`);
  }
  G3[n13]?.forEach((node) => check(G3, node, [...path, n13]));
}
var import_picocolors24, globalObject7;
var init_assertNoInfiniteHttpRedirect = __esm({
  "node_modules/vike/dist/esm/node/runtime/renderPage/createHttpResponse/assertNoInfiniteHttpRedirect.js"() {
    init_utils3();
    import_picocolors24 = __toESM(require_picocolors(), 1);
    globalObject7 = getGlobalObject("assertNoInfiniteHttpRedirect.ts", {
      redirectGraph: {}
    });
  }
});

// node_modules/vike/dist/esm/node/runtime/renderPage/createHttpResponse.js
async function createHttpResponsePage(htmlRender, renderHook, pageContext) {
  let statusCode = pageContext.abortStatusCode;
  if (!statusCode) {
    const isError = !pageContext.pageId || isErrorPage(pageContext.pageId, pageContext._pageConfigs);
    if (pageContext.errorWhileRendering) {
      assert(isError);
    }
    if (!isError) {
      assert(pageContext.is404 === null);
      statusCode = 200;
    } else {
      assert(pageContext.is404 === true || pageContext.is404 === false);
      statusCode = pageContext.is404 ? 404 : 500;
    }
  }
  const earlyHints = getEarlyHints(await pageContext.__getPageAssets());
  const headers = [];
  assert(pageContext.pageId);
  const cacheControl = getCacheControl(pageContext.pageId, pageContext._pageConfigs, statusCode);
  if (cacheControl) {
    headers.push(["Cache-Control", cacheControl]);
  }
  return createHttpResponse(statusCode, "text/html;charset=utf-8", headers, htmlRender, earlyHints, renderHook);
}
function createHttpResponseFavicon404() {
  const httpResponse = createHttpResponse(404, "text/html;charset=utf-8", [], "<p>No favicon.ico found.</p><script>console.log('This HTTP response was generated by Vike.')</script>");
  return httpResponse;
}
function createHttpResponseError(pageContext) {
  const reason = (() => {
    if (!pageContext) {
      return "no error page (https://vike.dev/error-page) could be rendered";
    }
    const errorPageId = getErrorPageId(pageContext._pageFilesAll, pageContext._pageConfigs);
    if (errorPageId) {
      return "the error page (https://vike.dev/error-page) couldn't be rendered (for example if an error occurred while rendering the error page)";
    } else {
      return "no error page (https://vike.dev/error-page) is defined, make sure to create one";
    }
  })();
  const httpResponse = createHttpResponse(500, "text/html;charset=utf-8", [], `<p>An error occurred.</p><script>console.log(${JSON.stringify(`This HTTP response was generated by Vike. Vike returned this response because ${reason}.`)})</script>`);
  return httpResponse;
}
async function createHttpResponsePageContextJson(pageContextSerialized) {
  const httpResponse = createHttpResponse(200, "application/json", [], pageContextSerialized, [], null);
  return httpResponse;
}
function createHttpResponseRedirect({ url, statusCode }, urlLogical) {
  assertNoInfiniteHttpRedirect(url, urlLogical);
  assert(url);
  assert(statusCode);
  assert(300 <= statusCode && statusCode <= 399);
  const headers = [["Location", url]];
  return createHttpResponse(
    statusCode,
    "text/html;charset=utf-8",
    headers,
    // For bots / programmatic crawlig: show what's going on.
    // For users: showing a blank page is probably better than a flickering text.
    `<p style="display: none">Redirecting to ${escapeHtml(url)}</p><script>console.log('This HTTP response was generated by Vike.')</script>`
  );
}
function createHttpResponse(statusCode, contentType, headers, htmlRender, earlyHints = [], renderHook = null) {
  headers.push(["Content-Type", contentType]);
  assert(renderHook || typeof htmlRender === "string");
  return {
    statusCode,
    headers,
    // TODO/v1-release: remove
    get contentType() {
      assertWarning2(false, "pageContext.httpResponse.contentType is deprecated and will be removed in the next major release. Use pageContext.httpResponse.headers instead, see https://vike.dev/migration/0.4.134", { onlyOnce: true });
      return contentType;
    },
    earlyHints,
    get body() {
      return getHttpResponseBody(htmlRender, renderHook);
    },
    ...getHttpResponseBodyStreamHandlers(htmlRender, renderHook)
  };
}
var init_createHttpResponse = __esm({
  "node_modules/vike/dist/esm/node/runtime/renderPage/createHttpResponse.js"() {
    init_utils3();
    init_error_page();
    init_getHttpResponseBody();
    init_getEarlyHints();
    init_getCacheControl();
    init_assertNoInfiniteHttpRedirect();
  }
});

// node_modules/vike/dist/esm/shared/getPageFiles/analyzePageClientSide/getExportNames.js
function getExportNames(p11) {
  if (p11.fileType === ".css") {
    return [];
  }
  if (p11.exportNames) {
    return p11.exportNames;
  }
  assert(p11.fileExports, p11.filePath);
  const exportNames = Object.keys(p11.fileExports);
  return exportNames;
}
var init_getExportNames = __esm({
  "node_modules/vike/dist/esm/shared/getPageFiles/analyzePageClientSide/getExportNames.js"() {
    init_utils();
  }
});

// node_modules/vike/dist/esm/shared/getPageFiles/analyzePageClientSide/analyzeExports.js
function analyzeExports({ pageFilesClientSide, pageFilesServerSide, pageId }) {
  return { isHtmlOnly: isHtmlOnly(), isClientRouting: isClientRouting() };
  function isHtmlOnly() {
    {
      const hasPageIdIsmrphFile = pageFilesServerSide.some((p11) => p11.pageId === pageId && p11.fileType === ".page");
      if (hasPageIdIsmrphFile) {
        assertClientSideRenderHook();
        return false;
      }
    }
    {
      const hasPageIdServerFile = pageFilesServerSide.some((p11) => p11.pageId === pageId && p11.fileType === ".page.server");
      if (!hasPageIdServerFile) {
        return false;
      }
    }
    {
      const definesClientRenderer = pageFilesClientSide.some((p11) => p11.pageId === pageId && p11.fileType === ".page.client" && getExportNames(p11).includes("render"));
      if (definesClientRenderer) {
        return false;
      }
    }
    return true;
  }
  function assertClientSideRenderHook() {
    const hasClientSideRenderHook = pageFilesClientSide.some((p11) => {
      return getExportNames(p11).includes("render");
    });
    assertUsage2(hasClientSideRenderHook, [
      "No client-side `render()` hook found.",
      "See https://vike.dev/render-modes for more information.",
      [
        "Loaded client-side page files (none of them `export { render }`):",
        ...pageFilesClientSide.map((p11, i13) => ` (${i13 + 1}): ${p11.filePath}`)
      ].join("\n")
    ].join(" "));
  }
  function isClientRouting() {
    const hasClientRoutingExport = pageFilesClientSide.some((p11) => {
      return getExportNames(p11).includes("clientRouting");
    });
    return hasClientRoutingExport;
  }
}
var init_analyzeExports = __esm({
  "node_modules/vike/dist/esm/shared/getPageFiles/analyzePageClientSide/analyzeExports.js"() {
    init_getExportNames();
    init_utils();
  }
});

// node_modules/vike/dist/esm/shared/getPageFiles/analyzePageClientSide/determineClientEntry.js
function determineClientEntry({ pageFilesClientSide, pageFilesServerSide, isHtmlOnly, isClientRouting }) {
  let clientEntries = [];
  const pageFilesServerSideOnly = pageFilesServerSide.filter((p11) => !pageFilesClientSide.includes(p11));
  const clientDependencies = [];
  clientDependencies.push(...pageFilesClientSide.map((p11) => ({ id: p11.filePath, onlyAssets: false, eagerlyImported: false })));
  clientDependencies.push(...pageFilesServerSideOnly.map((p11) => ({ id: p11.filePath, onlyAssets: true, eagerlyImported: false })));
  if (isHtmlOnly) {
    clientEntries = pageFilesClientSide.map((p11) => p11.filePath);
  } else {
    const clientEntry = getVikeClientEntry(isClientRouting);
    clientDependencies.push({ id: clientEntry, onlyAssets: false, eagerlyImported: false });
    clientEntries = [clientEntry];
  }
  return { clientEntries, clientDependencies };
}
function getVikeClientEntry(isClientRouting) {
  return isClientRouting ? "@@vike/dist/esm/client/client-routing-runtime/entry.js" : "@@vike/dist/esm/client/server-routing-runtime/entry.js";
}
var init_determineClientEntry = __esm({
  "node_modules/vike/dist/esm/shared/getPageFiles/analyzePageClientSide/determineClientEntry.js"() {
  }
});

// node_modules/vike/dist/esm/shared/getPageFiles/analyzePageClientSide.js
function analyzePageClientSide(pageFilesAll, pageId) {
  let pageFilesClientSide = getPageFilesClientSide(pageFilesAll, pageId);
  const pageFilesServerSide = getPageFilesServerSide(pageFilesAll, pageId);
  const { isHtmlOnly, isClientRouting } = analyzeExports({ pageFilesClientSide, pageFilesServerSide, pageId });
  if (isHtmlOnly) {
    pageFilesClientSide = pageFilesClientSide.filter((p11) => p11.isEnv("CLIENT_ONLY") && !getExportNames(p11).includes("render"));
    pageFilesClientSide = removeOverridenPageFiles(pageFilesClientSide);
  }
  const { clientEntries, clientDependencies } = determineClientEntry({
    pageFilesClientSide,
    pageFilesServerSide,
    isHtmlOnly,
    isClientRouting
  });
  return { isHtmlOnly, isClientRouting, clientEntries, clientDependencies, pageFilesClientSide, pageFilesServerSide };
}
async function analyzePageClientSideInit(pageFilesAll, pageId, { sharedPageFilesAlreadyLoaded }) {
  const pageFilesClientSide = getPageFilesClientSide(pageFilesAll, pageId);
  await Promise.all(pageFilesClientSide.map(async (p11) => {
    assert(p11.isEnv("CLIENT_ONLY") || p11.isEnv("CLIENT_AND_SERVER"));
    if (sharedPageFilesAlreadyLoaded && p11.isEnv("CLIENT_AND_SERVER")) {
      return;
    }
    await p11.loadExportNames?.();
  }));
}
function removeOverridenPageFiles(pageFilesClientSide) {
  const pageFilesClientSide_ = [];
  for (const p11 of pageFilesClientSide) {
    pageFilesClientSide_.push(p11);
    if (getExportNames(p11).includes("overrideDefaultPages")) {
      break;
    }
  }
  return pageFilesClientSide_;
}
var init_analyzePageClientSide = __esm({
  "node_modules/vike/dist/esm/shared/getPageFiles/analyzePageClientSide.js"() {
    init_analyzeExports();
    init_determineClientEntry();
    init_getAllPageIdFiles();
    init_getAllPageIdFiles();
    init_utils();
    init_getExportNames();
  }
});

// node_modules/vike/dist/esm/node/runtime/renderPage/getPageAssets/retrieveAssetsDev.js
async function retrieveAssetsDev(clientDependencies, viteDevServer) {
  const visitedModules = /* @__PURE__ */ new Set();
  const assetUrls = /* @__PURE__ */ new Set();
  await Promise.all(clientDependencies.map(async ({ id: id3 }) => {
    if (id3.startsWith("@@vike"))
      return;
    assert(id3);
    const { moduleGraph } = viteDevServer;
    const [_2, graphId] = await moduleGraph.resolveUrl(id3);
    assert(graphId, { id: id3 });
    const mod = moduleGraph.getModuleById(graphId);
    if (!mod) {
      return;
    }
    assert(mod, { id: id3 });
    collectCss(mod, assetUrls, visitedModules);
  }));
  return Array.from(assetUrls);
}
function collectCss(mod, styleUrls, visitedModules, importer) {
  assert(mod);
  if (!mod.url)
    return;
  if (visitedModules.has(mod.url))
    return;
  visitedModules.add(mod.url);
  if (isStyle(mod) && (!importer || !isStyle(importer))) {
    if (mod.url.startsWith("/")) {
      styleUrls.add(mod.url);
    } else if (mod.url.startsWith("\0")) {
      styleUrls.add(`/@id/__x00__${mod.url.substring(1)}`);
    } else {
      styleUrls.add(`/@id/${mod.url}`);
    }
  }
  mod.importedModules.forEach((dep) => {
    collectCss(dep, styleUrls, visitedModules, mod);
  });
}
function isStyle(mod) {
  if (styleFileRE.test(mod.url) || mod.id && /\?vue&type=style/.test(mod.id)) {
    assert(mod.type === "js");
    return true;
  }
  return false;
}
var init_retrieveAssetsDev = __esm({
  "node_modules/vike/dist/esm/node/runtime/renderPage/getPageAssets/retrieveAssetsDev.js"() {
    init_utils3();
  }
});

// node_modules/vike/dist/esm/node/shared/extractAssetsQuery.js
function extractAssetsAddQuery(id3) {
  const fileExtension = getFileExtension(id3);
  if (!fileExtension || id3.includes("virtual:vike:")) {
    return `${id3}?${query}`;
  } else {
    if (!id3.includes("?")) {
      return `${id3}?${query}&lang.${fileExtension}`;
    } else {
      return id3.replace("?", `?${query}&`);
    }
  }
}
function extractAssetsRemoveQuery(id3) {
  if (!id3.includes("?"))
    return id3;
  const suffix = `?${query}`;
  assert(id3.endsWith(query));
  return id3.slice(0, -1 * suffix.length);
}
var query;
var init_extractAssetsQuery = __esm({
  "node_modules/vike/dist/esm/node/shared/extractAssetsQuery.js"() {
    init_utils2();
    query = "extractAssets";
  }
});

// node_modules/vike/dist/esm/node/shared/virtual-files/virtualFilePageConfigValuesAll.js
function getVirtualFileIdPageConfigValuesAll(pageId, isForClientSide) {
  const id3 = `${isForClientSide ? idBaseClient : idBaseServer}${pageId}`;
  return id3;
}
function isVirtualFileIdPageConfigValuesAll(id3) {
  id3 = getVirtualFileId(id3);
  if (!id3.includes(idBase2))
    return false;
  assert(id3.startsWith(idBase2));
  const idOriginal = id3;
  id3 = extractAssetsRemoveQuery(id3);
  const isExtractAssets = idOriginal !== id3;
  if (id3.startsWith(idBaseClient)) {
    assert(isExtractAssets === false);
    return {
      pageId: id3.slice(idBaseClient.length),
      isForClientSide: true,
      isExtractAssets
    };
  }
  if (id3.startsWith(idBaseServer)) {
    return {
      pageId: id3.slice(idBaseServer.length),
      isForClientSide: false,
      isExtractAssets
    };
  }
  assert(false);
}
var idBase2, idBaseClient, idBaseServer;
var init_virtualFilePageConfigValuesAll = __esm({
  "node_modules/vike/dist/esm/node/shared/virtual-files/virtualFilePageConfigValuesAll.js"() {
    init_extractAssetsQuery();
    init_utils2();
    init_virtual_files();
    idBase2 = "virtual:vike:pageConfigValuesAll:";
    idBaseClient = `${idBase2}client:`;
    idBaseServer = `${idBase2}server:`;
  }
});

// node_modules/vike/dist/esm/node/shared/prependEntriesDir.js
function prependEntriesDir(entryName) {
  if (entryName.startsWith("/")) {
    entryName = entryName.slice(1);
  }
  assert(!entryName.startsWith("/"));
  assert(entryName);
  entryName = `entries/${entryName}`;
  return entryName;
}
var init_prependEntriesDir = __esm({
  "node_modules/vike/dist/esm/node/shared/prependEntriesDir.js"() {
    init_utils2();
  }
});

// node_modules/vike/dist/esm/node/runtime/renderPage/getPageAssets/getManifestEntry.js
function getManifestEntry(id3, assetsManifest) {
  const debugInfo = getDebugInfo(id3, assetsManifest);
  if (id3.startsWith("@@vike/")) {
    const manifestKeyEnd = slice(id3, "@@vike".length, 0);
    const { manifestKey: manifestKey2, manifestEntry: manifestEntry2 } = findEntryWithKeyEnd(manifestKeyEnd, assetsManifest, id3);
    assert(manifestEntry2 && manifestKey2, debugInfo);
    return { manifestEntry: manifestEntry2, manifestKey: manifestKey2 };
  }
  if (isVirtualFileIdPageConfigValuesAll(id3)) {
    {
      const manifestKey3 = id3;
      const manifestEntry3 = assetsManifest[manifestKey3];
      if (manifestEntry3) {
        return { manifestEntry: manifestEntry3, manifestKey: manifestKey3 };
      }
    }
    const manifestKeyEnd = id3;
    const { manifestKey: manifestKey2, manifestEntry: manifestEntry2 } = getEntryWithKeyEnd(manifestKeyEnd, assetsManifest, id3);
    assert(manifestEntry2, debugInfo);
    return { manifestEntry: manifestEntry2, manifestKey: manifestKey2 };
  }
  if (id3.startsWith("/")) {
    const manifestKey2 = id3.slice(1);
    let manifestEntry2 = assetsManifest[manifestKey2];
    assert(manifestEntry2, debugInfo);
    return { manifestEntry: manifestEntry2, manifestKey: manifestKey2 };
  }
  assertIsNpmPackageImport(id3);
  const found = Object.entries(assetsManifest).find(([, e14]) => e14.name === prependEntriesDir(id3));
  assert(found);
  const [manifestKey, manifestEntry] = found;
  return { manifestEntry, manifestKey };
}
function findEntryWithKeyEnd(manifestKeyEnd, assetsManifest, id3) {
  const debugInfo = getDebugInfo(id3, assetsManifest, manifestKeyEnd);
  assert(manifestKeyEnd.startsWith("/"), debugInfo);
  const manifestKeys = [];
  for (const manifestKey2 in assetsManifest) {
    if (manifestKey2.endsWith(manifestKeyEnd)) {
      manifestKeys.push(manifestKey2);
    }
  }
  const manifestKeysRelative = manifestKeys.filter((k5) => k5.startsWith("../"));
  assert(manifestKeysRelative.length <= 1, debugInfo);
  const manifestKey = manifestKeysRelative[0] ?? manifestKeys[0] ?? null;
  if (!manifestKey) {
    return { manifestEntry: null, manifestKey: null };
  }
  const manifestEntry = assetsManifest[manifestKey];
  return { manifestEntry, manifestKey };
}
function getEntryWithKeyEnd(manifestKeyEnd, assetsManifest, id3) {
  const debugInfo = getDebugInfo(id3, assetsManifest, manifestKeyEnd);
  const manifestKeys = [];
  for (const manifestKey2 in assetsManifest) {
    if (manifestKey2.endsWith(manifestKeyEnd)) {
      manifestKeys.push(manifestKey2);
    }
  }
  assert(manifestKeys.length <= 1, debugInfo);
  const manifestKey = manifestKeys[0];
  if (!manifestKey) {
    return { manifestEntry: null, manifestKey: null };
  }
  const manifestEntry = assetsManifest[manifestKey];
  return { manifestEntry, manifestKey };
}
function getDebugInfo(id3, assetsManifest, manifestKeyEnd) {
  const manifestKeys = Object.keys(assetsManifest);
  if (manifestKeyEnd === void 0) {
    return { manifestKeys, id: id3 };
  } else {
    return { manifestKeys, manifestKeyEnd, id: id3 };
  }
}
var init_getManifestEntry = __esm({
  "node_modules/vike/dist/esm/node/runtime/renderPage/getPageAssets/getManifestEntry.js"() {
    init_utils3();
    init_virtualFilePageConfigValuesAll();
    init_prependEntriesDir();
  }
});

// node_modules/vike/dist/esm/node/runtime/renderPage/getPageAssets/retrieveAssetsProd.js
function retrieveAssetsProd(clientDependencies, assetsManifest, includeAssetsImportedByServer) {
  let assetUrls = /* @__PURE__ */ new Set();
  assert(assetsManifest);
  const visistedAssets = /* @__PURE__ */ new Set();
  clientDependencies.forEach(({ id: id3, onlyAssets, eagerlyImported }) => {
    if (eagerlyImported)
      return;
    if (includeAssetsImportedByServer && onlyAssets && id3.includes(".page.server.") && // We assume that all npm packages have already built their files: bundlers (Rollup, esbuild, tsup, ...) extract the CSS out of JavaScript => we can assume JavaScript to not import any CSS/assets.
    !isNpmPackageImport(id3, {
      // I presume Vite already resolves path aliases when Vite sets the module's id
      cannotBePathAlias: true
    })) {
      id3 = extractAssetsAddQuery(id3);
    }
    const { manifestKey } = getManifestEntry(id3, assetsManifest);
    collectAssets(manifestKey, assetUrls, visistedAssets, assetsManifest, onlyAssets);
  });
  collectSingleStyle(assetUrls, assetsManifest);
  return Array.from(assetUrls);
}
function collectAssets(manifestKey, assetUrls, visistedAssets, assetsManifest, onlyCollectStaticAssets) {
  if (visistedAssets.has(manifestKey))
    return;
  visistedAssets.add(manifestKey);
  const manifestEntry = assetsManifest[manifestKey];
  assert(manifestEntry, { manifestKey });
  const { file } = manifestEntry;
  if (!onlyCollectStaticAssets) {
    assetUrls.add(`/${file}`);
  }
  const { imports = [], assets = [], css = [] } = manifestEntry;
  for (const manifestKey2 of imports) {
    const importManifestEntry = assetsManifest[manifestKey2];
    assert(importManifestEntry);
    collectAssets(manifestKey2, assetUrls, visistedAssets, assetsManifest, onlyCollectStaticAssets);
  }
  for (const cssAsset of css) {
    assetUrls.add(`/${cssAsset}`);
  }
  for (const asset of assets) {
    assetUrls.add(`/${asset}`);
  }
}
function collectSingleStyle(assetUrls, assetsManifest) {
  const style = assetsManifest["style.css"];
  if (style && Object.values(assetsManifest).filter((asset) => asset.file.endsWith(".css")).length === 1) {
    assetUrls.add(`/${style.file}`);
  }
}
var init_retrieveAssetsProd = __esm({
  "node_modules/vike/dist/esm/node/runtime/renderPage/getPageAssets/retrieveAssetsProd.js"() {
    init_utils3();
    init_getManifestEntry();
    init_extractAssetsQuery();
  }
});

// node_modules/vike/dist/esm/node/runtime/renderPage/inferMediaType.js
function inferMediaType(href) {
  if (styleFileRE.test(href)) {
    return { mediaType: "text/css", assetType: "style" };
  }
  if (isScriptFile(href)) {
    return { mediaType: "text/javascript", assetType: "script" };
  }
  if (href.endsWith(".png")) {
    return { assetType: "image", mediaType: "image/png" };
  }
  if (href.endsWith(".webp")) {
    return { assetType: "image", mediaType: "image/webp" };
  }
  if (href.endsWith(".jpg") || href.endsWith(".jpeg")) {
    return { assetType: "image", mediaType: "image/jpeg" };
  }
  if (href.endsWith(".gif")) {
    return { assetType: "image", mediaType: "image/gif" };
  }
  if (href.endsWith(".svg")) {
    return { assetType: "image", mediaType: "image/svg+xml" };
  }
  if (href.endsWith(".avif")) {
    return { assetType: "image", mediaType: "image/avif" };
  }
  if (href.endsWith(".ttf")) {
    return { assetType: "font", mediaType: "font/ttf" };
  }
  if (href.endsWith(".woff")) {
    return { assetType: "font", mediaType: "font/woff" };
  }
  if (href.endsWith(".woff2")) {
    return { assetType: "font", mediaType: "font/woff2" };
  }
  if (href.endsWith(".mp4")) {
    return { assetType: "video", mediaType: "video/mp4" };
  }
  if (href.endsWith(".webm")) {
    return { assetType: "video", mediaType: "video/webm" };
  }
  if (href.endsWith(".ogv")) {
    return { assetType: "video", mediaType: "video/ogg" };
  }
  if (href.endsWith(".mpeg") || href.endsWith(".mpg")) {
    return { assetType: "video", mediaType: "video/mpeg" };
  }
  if (href.endsWith(".avi")) {
    return { assetType: "video", mediaType: "video/x-msvideo" };
  }
  if (href.endsWith(".mov") || href.endsWith(".qt")) {
    return { assetType: "video", mediaType: "video/quicktime" };
  }
  if (href.endsWith(".mp3")) {
    return { assetType: "audio", mediaType: "audio/mpeg" };
  }
  if (href.endsWith(".wav")) {
    return { assetType: "audio", mediaType: "audio/wav" };
  }
  if (href.endsWith(".ogg")) {
    return { assetType: "audio", mediaType: "audio/ogg" };
  }
  if (href.endsWith(".m4a")) {
    return { assetType: "audio", mediaType: "audio/aac" };
  }
  if (href.endsWith("midi") || href.endsWith(".mid")) {
    return { assetType: "audio", mediaType: "audio/midi" };
  }
  if (href.endsWith(".flac")) {
    return { assetType: "audio", mediaType: "audio/flac" };
  }
  return null;
}
var init_inferMediaType = __esm({
  "node_modules/vike/dist/esm/node/runtime/renderPage/inferMediaType.js"() {
    init_utils3();
  }
});

// node_modules/vike/dist/esm/node/runtime/renderPage/getPageAssets/sortPageAssetsForEarlyHintsHeader.js
function sortPageAssetsForEarlyHintsHeader(pageAssets) {
  const globalContext = getGlobalContext();
  pageAssets.sort(higherFirst(({ assetType }) => {
    if (!globalContext.isProduction && assetType === "script") {
      return 1;
    }
    let priority = 0;
    if (assetType === "style")
      return priority;
    priority--;
    if (assetType === "font")
      return priority;
    priority--;
    if (assetType === "image")
      return priority;
    priority--;
    if (assetType === "video")
      return priority;
    priority--;
    if (assetType === "audio")
      return priority;
    priority--;
    if (assetType !== "script")
      return priority;
    priority--;
    return priority;
  }));
}
var init_sortPageAssetsForEarlyHintsHeader = __esm({
  "node_modules/vike/dist/esm/node/runtime/renderPage/getPageAssets/sortPageAssetsForEarlyHintsHeader.js"() {
    init_globalContext();
    init_utils3();
  }
});

// node_modules/vike/dist/esm/node/runtime/renderPage/getPageAssets.js
async function getPageAssets(pageContext, clientDependencies, clientEntries) {
  const globalContext = getGlobalContext();
  const isDev = !globalContext.isProduction;
  let assetUrls;
  let clientEntriesSrc;
  if (isDev) {
    const { viteDevServer } = globalContext;
    clientEntriesSrc = await Promise.all(clientEntries.map((clientEntry) => globalObject8.resolveClientEntriesDev(clientEntry, viteDevServer)));
    assetUrls = await retrieveAssetsDev(clientDependencies, viteDevServer);
  } else {
    const { assetsManifest } = globalContext;
    clientEntriesSrc = clientEntries.map((clientEntry) => resolveClientEntriesProd(clientEntry, assetsManifest));
    assetUrls = retrieveAssetsProd(clientDependencies, assetsManifest, pageContext._includeAssetsImportedByServer);
  }
  let pageAssets = [];
  unique([...clientEntriesSrc, ...assetUrls]).forEach((src) => {
    const { mediaType = null, assetType = null } = inferMediaType(src) || {};
    if (isDev && assetType === "style") {
      if (src.endsWith("?inline")) {
        return;
      }
      src = src + "?direct";
    }
    const isEntry = clientEntriesSrc.includes(src) || // Vite automatically injects CSS, not only in development, but also in production (albeit with a FOUC). Therefore, strictly speaking, CSS aren't entries. We still, however, set `isEntry: true` for CSS, in order to denote page assets that should absolutely be injected in the HTML, regardless of preload strategy (not injecting CSS leads to FOUC).
    assetType === "style";
    pageAssets.push({
      src,
      assetType,
      mediaType,
      isEntry
    });
  });
  pageAssets.forEach(({ src }) => {
    assert(1 === pageAssets.filter((p11) => p11.src === src).length);
  });
  pageAssets = pageAssets.map((pageAsset) => {
    const baseServerAssets = pageContext._baseAssets || pageContext._baseServer;
    pageAsset.src = prependBase(toPosixPath(pageAsset.src), baseServerAssets);
    return pageAsset;
  });
  sortPageAssetsForEarlyHintsHeader(pageAssets);
  return pageAssets;
}
function resolveClientEntriesProd(clientEntry, assetsManifest) {
  const { manifestEntry } = getManifestEntry(clientEntry, assetsManifest);
  assert(manifestEntry.isEntry || manifestEntry.isDynamicEntry || clientEntry.endsWith(".css"), { clientEntry });
  let { file } = manifestEntry;
  assert(!file.startsWith("/"));
  return "/" + file;
}
var globalObject8;
var init_getPageAssets = __esm({
  "node_modules/vike/dist/esm/node/runtime/renderPage/getPageAssets.js"() {
    init_utils3();
    init_retrieveAssetsDev();
    init_retrieveAssetsProd();
    init_inferMediaType();
    init_getManifestEntry();
    init_sortPageAssetsForEarlyHintsHeader();
    init_globalContext();
    globalObject8 = getGlobalObject("getPageAssets.ts", {
      resolveClientEntriesDev: null
    });
  }
});

// node_modules/vike/dist/esm/node/runtime/renderPage/debugPageFiles.js
function debugPageFiles({ pageContext, isHtmlOnly, isClientRouting, pageFilesLoaded, pageFilesServerSide, pageFilesClientSide, clientEntries, clientDependencies }) {
  const debug6 = createDebugger("vike:pageFiles", { serialization: { emptyArray: "None" } });
  const padding = "   - ";
  debug6("All page files:", printPageFiles(pageContext._pageFilesAll, true));
  debug6(`URL:`, pageContext.urlOriginal);
  debug6.options({ serialization: { emptyArray: "No match" } })(`Routing:`, printRouteMatches(pageContext._debugRouteMatches));
  debug6(`pageId:`, pageContext.pageId);
  debug6("Page type:", isHtmlOnly ? "HTML-only" : "SSR/SPA");
  debug6(`Routing type:`, !isHtmlOnly && isClientRouting ? "Client Routing" : "Server Routing");
  debug6("Server-side page files:", printPageFiles(pageFilesLoaded));
  assert(samePageFiles(pageFilesLoaded, pageFilesServerSide));
  debug6("Client-side page files:", printPageFiles(pageFilesClientSide));
  debug6("Client-side entries:", clientEntries);
  debug6("Client-side dependencies:", clientDependencies);
  return;
  function printRouteMatches(debugRouteMatches) {
    if (debugRouteMatches === "ROUTING_ERROR") {
      return "Routing Failed";
    }
    if (debugRouteMatches === "CUSTOM_ROUTING") {
      return "Custom Routing";
    }
    return debugRouteMatches;
  }
  function printPageFiles(pageFiles2, genericPageFilesLast = false) {
    if (pageFiles2.length === 0) {
      return "None";
    }
    return "\n" + pageFiles2.sort((p1, p22) => p1.filePath.localeCompare(p22.filePath)).sort(makeFirst((p11) => p11.isRendererPageFile ? !genericPageFilesLast : null)).sort(makeFirst((p11) => p11.isDefaultPageFile ? !genericPageFilesLast : null)).map((p11) => p11.filePath).map((s36) => s36.split("_default.page.").join(`${import_picocolors25.default.blue("_default")}.page.`)).map((s36) => s36.split("/renderer/").join(`/${import_picocolors25.default.red("renderer")}/`)).map((s36) => padding + s36).join("\n");
  }
}
function samePageFiles(pageFiles1, pageFiles2) {
  return pageFiles1.every((p1) => pageFiles2.some((p22) => p22.filePath === p1.filePath)) && pageFiles2.every((p22) => pageFiles1.some((p1) => p1.filePath === p22.filePath));
}
var import_picocolors25;
var init_debugPageFiles = __esm({
  "node_modules/vike/dist/esm/node/runtime/renderPage/debugPageFiles.js"() {
    import_picocolors25 = __toESM(require_picocolors(), 1);
    init_utils3();
  }
});

// node_modules/vike/dist/esm/shared/page-configs/findPageConfig.js
function findPageConfig(pageConfigs, pageId) {
  const result = pageConfigs.filter((p11) => p11.pageId === pageId);
  assert(result.length <= 1);
  const pageConfig = result[0] ?? null;
  return pageConfig;
}
var init_findPageConfig = __esm({
  "node_modules/vike/dist/esm/shared/page-configs/findPageConfig.js"() {
    init_utils();
  }
});

// node_modules/vike/dist/esm/shared/getPageFiles/analyzeClientSide.js
function analyzeClientSide(pageConfig, pageFilesAll, pageId) {
  if (pageConfig) {
    const isClientRouting = getConfigValueRuntime(pageConfig, "clientRouting", "boolean")?.value ?? false;
    const isClientRuntimeLoaded = getConfigValueRuntime(pageConfig, "isClientRuntimeLoaded", "boolean")?.value ?? false;
    return { isClientRuntimeLoaded, isClientRouting };
  } else {
    const { isHtmlOnly, isClientRouting } = analyzePageClientSide(pageFilesAll, pageId);
    return { isClientRuntimeLoaded: !isHtmlOnly, isClientRouting };
  }
}
var init_analyzeClientSide = __esm({
  "node_modules/vike/dist/esm/shared/getPageFiles/analyzeClientSide.js"() {
    init_getConfigValue();
    init_analyzePageClientSide();
  }
});

// node_modules/vike/dist/esm/node/runtime/renderPage/analyzePage.js
function analyzePage(pageFilesAll, pageConfig, pageId) {
  if (pageConfig) {
    const { isClientRuntimeLoaded, isClientRouting } = analyzeClientSide(pageConfig, pageFilesAll, pageId);
    const clientEntries = [];
    const clientFilePath = getConfigValueRuntime(pageConfig, "client", "string")?.value ?? null;
    if (clientFilePath)
      clientEntries.push(clientFilePath);
    if (isClientRuntimeLoaded)
      clientEntries.push(getVikeClientEntry(isClientRouting));
    const clientDependencies = [];
    clientDependencies.push({
      id: getVirtualFileIdPageConfigValuesAll(pageConfig.pageId, true),
      onlyAssets: isClientRuntimeLoaded ? false : true,
      eagerlyImported: false
    });
    if (!getGlobalContext().isProduction) {
      clientDependencies.push({
        id: getVirtualFileIdPageConfigValuesAll(pageConfig.pageId, false),
        onlyAssets: true,
        eagerlyImported: false
      });
    }
    clientEntries.forEach((clientEntry) => {
      clientDependencies.push({
        id: clientEntry,
        onlyAssets: false,
        eagerlyImported: false
      });
    });
    return {
      isHtmlOnly: !isClientRuntimeLoaded,
      isClientRouting,
      clientEntries,
      clientDependencies,
      // pageFilesClientSide and pageFilesServerSide are only used for debugging
      pageFilesClientSide: [],
      pageFilesServerSide: []
    };
  } else {
    return analyzePageClientSide(pageFilesAll, pageId);
  }
}
var init_analyzePage = __esm({
  "node_modules/vike/dist/esm/node/runtime/renderPage/analyzePage.js"() {
    init_determineClientEntry();
    init_analyzePageClientSide();
    init_virtualFilePageConfigValuesAll();
    init_analyzeClientSide();
    init_globalContext();
    init_getConfigValue();
  }
});

// node_modules/vike/dist/esm/shared/page-configs/loadConfigValues.js
async function loadConfigValues(pageConfig, isDev) {
  if ("isAllLoaded" in pageConfig && // We don't need to cache in dev, since Vite already caches the virtual module
  !isDev) {
    return pageConfig;
  }
  const configValuesLoaded = await pageConfig.loadConfigValuesAll();
  const configValues = parseConfigValuesSerialized(configValuesLoaded.configValuesSerialized);
  Object.assign(pageConfig.configValues, configValues);
  objectAssign(pageConfig, { isAllLoaded: true });
  return pageConfig;
}
var init_loadConfigValues = __esm({
  "node_modules/vike/dist/esm/shared/page-configs/loadConfigValues.js"() {
    init_utils();
    init_parsePageConfigs();
  }
});

// node_modules/vike/dist/esm/node/runtime/renderPage/loadUserFilesServerSide.js
async function loadUserFilesServerSide(pageContext) {
  const pageConfig = findPageConfig(pageContext._pageConfigs, pageContext.pageId);
  const [{ pageFilesLoaded, pageContextExports }] = await Promise.all([
    loadPageUserFiles(pageContext._pageFilesAll, pageConfig, pageContext.pageId, !getGlobalContext().isProduction),
    analyzePageClientSideInit(pageContext._pageFilesAll, pageContext.pageId, { sharedPageFilesAlreadyLoaded: true })
  ]);
  const { isHtmlOnly, isClientRouting, clientEntries, clientDependencies, pageFilesClientSide, pageFilesServerSide } = analyzePage(pageContext._pageFilesAll, pageConfig, pageContext.pageId);
  const isV1Design = !!pageConfig;
  const passToClient = [];
  const errMsg = " should be an array of strings.";
  if (!isV1Design) {
    pageContextExports.exportsAll.passToClient?.forEach((e14) => {
      assertUsage2(hasProp(e14, "exportValue", "string[]"), `${e14.exportSource}${errMsg}`);
      passToClient.push(...e14.exportValue);
    });
  } else {
    pageContextExports.configEntries.passToClient?.forEach((e14) => {
      const { configValue } = e14;
      assert(isArray(configValue));
      const vals = configValue.flat(1);
      assertUsage2(isArrayOfStrings(vals), `${e14.configDefinedAt}${errMsg}`);
      passToClient.push(...vals);
    });
  }
  const pageContextAddendum = {};
  objectAssign(pageContextAddendum, pageContextExports);
  objectAssign(pageContextAddendum, {
    Page: pageContextExports.exports.Page,
    _isHtmlOnly: isHtmlOnly,
    _passToClient: passToClient,
    _pageFilePathsLoaded: pageFilesLoaded.map((p11) => p11.filePath)
  });
  objectAssign(pageContextAddendum, {
    __getPageAssets: async () => {
      if ("_pageAssets" in pageContext) {
        return pageContext._pageAssets;
      } else {
        const pageAssets = await getPageAssets(pageContext, clientDependencies, clientEntries);
        objectAssign(pageContext, { _pageAssets: pageAssets });
        return pageContext._pageAssets;
      }
    }
  });
  Object.assign(pageContextAddendum, {
    _getPageAssets: async () => {
      assertWarning2(false, "pageContext._getPageAssets() deprecated, see https://vike.dev/preloading", {
        onlyOnce: true,
        showStackTrace: true
      });
      const pageAssetsOldFormat = [];
      (await pageContextAddendum.__getPageAssets()).forEach((p11) => {
        if (p11.assetType === "script" && p11.isEntry) {
          pageAssetsOldFormat.push({
            src: p11.src,
            preloadType: null,
            assetType: "script",
            mediaType: p11.mediaType
          });
        }
        pageAssetsOldFormat.push({
          src: p11.src,
          preloadType: p11.assetType,
          assetType: p11.assetType === "style" ? "style" : "preload",
          mediaType: p11.mediaType
        });
      });
      return pageAssetsOldFormat;
    }
  });
  {
    debugPageFiles({
      pageContext,
      isHtmlOnly,
      isClientRouting,
      pageFilesLoaded,
      pageFilesClientSide,
      pageFilesServerSide,
      clientEntries,
      clientDependencies
    });
  }
  return pageContextAddendum;
}
async function loadPageUserFiles(pageFilesAll, pageConfig, pageId, isDev) {
  const pageFilesServerSide = getPageFilesServerSide(pageFilesAll, pageId);
  const pageConfigLoaded = !pageConfig ? null : await loadConfigValues(pageConfig, isDev);
  await Promise.all(pageFilesServerSide.map((p11) => p11.loadFile?.()));
  const pageContextExports = getPageContextExports(pageFilesServerSide, pageConfigLoaded);
  return {
    pageContextExports,
    pageFilesLoaded: pageFilesServerSide
  };
}
var init_loadUserFilesServerSide = __esm({
  "node_modules/vike/dist/esm/node/runtime/renderPage/loadUserFilesServerSide.js"() {
    init_getPageFiles();
    init_analyzePageClientSide();
    init_utils3();
    init_getPageAssets();
    init_debugPageFiles();
    init_findPageConfig();
    init_analyzePage();
    init_globalContext();
    init_loadConfigValues();
  }
});

// node_modules/vike/dist/esm/shared/hooks/getHook.js
function getHook(pageContext, hookName) {
  if (!(hookName in pageContext.exports)) {
    return null;
  }
  const { hooksTimeout } = pageContext.config;
  const hookTimeout = getHookTimeout(hooksTimeout, hookName);
  const hookFn = pageContext.exports[hookName];
  const file = pageContext.exportsAll[hookName][0];
  assert(file.exportValue === hookFn);
  if (hookFn === null)
    return null;
  const hookFilePath = file.filePath;
  assert(hookFilePath);
  assert(!hookFilePath.endsWith(" "));
  assertHookFn(hookFn, { hookName, hookFilePath });
  return { hookFn, hookName, hookFilePath, hookTimeout };
}
function getHookFromPageConfigGlobal(pageConfigGlobal, hookName) {
  const configValue = pageConfigGlobal.configValues[hookName];
  if (!configValue)
    return null;
  const hookFn = configValue.value;
  if (!hookFn)
    return null;
  const hookFilePath = getHookFilePathToShowToUser(configValue.definedAtData);
  assert(hookFilePath);
  assertHookFn(hookFn, { hookName, hookFilePath });
  const hookTimeout = getHookTimeoutDefault(hookName);
  return { hookFn, hookName, hookFilePath, hookTimeout };
}
function assertHookFn(hookFn, { hookName, hookFilePath }) {
  assert(hookName && hookFilePath);
  assert(!hookName.endsWith(")"));
  assertUsage2(isCallable(hookFn), `Hook ${hookName}() defined by ${hookFilePath} should be a function`);
  checkType(hookFn);
}
function getHookTimeout(hooksTimeoutProvidedByUser, hookName) {
  const hooksTimeoutProvidedbyUserNormalized = getHooksTimeoutProvidedByUserNormalized(hooksTimeoutProvidedByUser);
  if (hooksTimeoutProvidedbyUserNormalized === false)
    return { error: false, warning: false };
  const providedbyUser = hooksTimeoutProvidedbyUserNormalized[hookName];
  const hookTimeout = getHookTimeoutDefault(hookName);
  if (providedbyUser?.error !== void 0)
    hookTimeout.error = providedbyUser.error;
  if (providedbyUser?.warning !== void 0)
    hookTimeout.warning = providedbyUser.warning;
  return hookTimeout;
}
function getHooksTimeoutProvidedByUserNormalized(hooksTimeoutProvidedByUser) {
  if (hooksTimeoutProvidedByUser === void 0)
    return {};
  if (hooksTimeoutProvidedByUser === false)
    return false;
  assertUsage2(isObject(hooksTimeoutProvidedByUser), `Setting ${import_picocolors26.default.cyan("hooksTimeout")} should be ${import_picocolors26.default.cyan("false")} or an object`);
  const hooksTimeoutProvidedByUserNormalized = {};
  Object.entries(hooksTimeoutProvidedByUser).forEach(([hookName, hookTimeoutProvidedbyUser]) => {
    if (hookTimeoutProvidedbyUser === false) {
      hooksTimeoutProvidedByUserNormalized[hookName] = { error: false, warning: false };
      return;
    }
    assertUsage2(isObject(hookTimeoutProvidedbyUser), `Setting ${import_picocolors26.default.cyan(`hooksTimeout.${hookName}`)} should be ${import_picocolors26.default.cyan("false")} or an object`);
    const [error, warning2] = ["error", "warning"].map((timeoutName) => {
      const timeoutVal = hookTimeoutProvidedbyUser[timeoutName];
      if (timeoutVal === void 0 || timeoutVal === false)
        return timeoutVal;
      const errPrefix = `Setting ${import_picocolors26.default.cyan(`hooksTimeout.${hookName}.${timeoutName}`)} should be`;
      assertUsage2(typeof timeoutVal === "number", `${errPrefix} ${import_picocolors26.default.cyan("false")} or a number`);
      assertUsage2(timeoutVal > 0, `${errPrefix} a positive number`);
      return timeoutVal;
    });
    hooksTimeoutProvidedByUserNormalized[hookName] = { error, warning: warning2 };
  });
  return hooksTimeoutProvidedByUserNormalized;
}
function getHookTimeoutDefault(hookName) {
  if (hookName === "onBeforeRoute") {
    return {
      error: 5 * 1e3,
      warning: 1 * 1e3
    };
  }
  if (globalObject9.isPrerendering) {
    return {
      error: 2 * 60 * 1e3,
      warning: 30 * 1e3
    };
  } else {
    assert(!hookName.toLowerCase().includes("prerender"));
  }
  return {
    error: 30 * 1e3,
    warning: 4 * 1e3
  };
}
var import_picocolors26, globalObject9;
var init_getHook = __esm({
  "node_modules/vike/dist/esm/shared/hooks/getHook.js"() {
    init_getGlobalObject();
    init_helpers();
    init_getConfigValue();
    init_utils();
    import_picocolors26 = __toESM(require_picocolors(), 1);
    globalObject9 = getGlobalObject("getHook.ts", {});
  }
});

// node_modules/vike/dist/esm/shared/preparePageContextForUserConsumption.js
function preparePageContextForUserConsumption(pageContext) {
  assert(pageContext.pageId);
  assert("config" in pageContext);
  assert("configEntries" in pageContext);
  addIs404ToPageProps(pageContext);
  if (!("_pageId" in pageContext)) {
    Object.defineProperty(pageContext, "_pageId", {
      get() {
        assertWarning2(false, "pageContext._pageId has been renamed to pageContext.pageId", {
          showStackTrace: true,
          onlyOnce: true
        });
        return pageContext.pageId;
      },
      enumerable: false
    });
  }
  sortPageContext(pageContext);
}
function sortPageContext(pageContext) {
  let descriptors = Object.getOwnPropertyDescriptors(pageContext);
  for (const key of Object.keys(pageContext))
    delete pageContext[key];
  descriptors = Object.fromEntries(Object.entries(descriptors).sort(([key1], [key2]) => compareString(key1, key2)));
  Object.defineProperties(pageContext, descriptors);
}
var init_preparePageContextForUserConsumption = __esm({
  "node_modules/vike/dist/esm/shared/preparePageContextForUserConsumption.js"() {
    init_utils();
    init_addIs404ToPageProps();
  }
});

// node_modules/vike/dist/esm/node/runtime/renderPage/preparePageContextForUserConsumptionServerSide.js
function preparePageContextForUserConsumptionServerSide(pageContext) {
  assertPageContextUrl(pageContext);
  assert(isPlainObject(pageContext.routeParams));
  assert("Page" in pageContext);
  assert(typeof pageContext.isClientSideNavigation === "boolean");
  preparePageContextForUserConsumption(pageContext);
}
var init_preparePageContextForUserConsumptionServerSide = __esm({
  "node_modules/vike/dist/esm/node/runtime/renderPage/preparePageContextForUserConsumptionServerSide.js"() {
    init_utils3();
    init_getPageContextUrlComputed();
    init_preparePageContextForUserConsumption();
  }
});

// node_modules/vike/dist/esm/shared/assertHookReturnedObject.js
function assertHookReturnedObject(obj, keysExpected, errPrefix) {
  assert(!errPrefix.endsWith(" "));
  const keysUnknown = [];
  const keys2 = Object.keys(obj);
  for (const key of keys2) {
    if (!keysExpected.includes(key)) {
      keysUnknown.push(key);
    }
  }
  assertUsage2(keysUnknown.length === 0, [
    errPrefix,
    "returned an object with following unknown keys:",
    stringifyStringArray(keysUnknown) + ".",
    "Only following keys are allowed:",
    stringifyStringArray(keysExpected) + "."
  ].join(" "));
}
var init_assertHookReturnedObject = __esm({
  "node_modules/vike/dist/esm/shared/assertHookReturnedObject.js"() {
    init_utils();
  }
});

// node_modules/vike/dist/esm/utils/assertRoutingType.js
function assertClientRouting() {
  assertNoContradiction(checkIfClientRouting());
  state.isClientRouting = true;
}
function checkIfClientRouting() {
  return state.isClientRouting !== false;
}
function assertNoContradiction(noContradiction) {
  assertUsage2(isBrowser(), `${import_picocolors27.default.cyan("import { something } from 'vike/client/router'")} is forbidden on the server-side`, { showStackTrace: true });
  assertWarning2(noContradiction, "You shouldn't `import { something } from 'vike/client/router'` when using Server Routing. The 'vike/client/router' utilities work only with Client Routing. In particular, don't `import { navigate }` nor `import { prefetch }` as they unnecessarily bloat your client-side bundle sizes.", { showStackTrace: true, onlyOnce: true });
}
var import_picocolors27, state;
var init_assertRoutingType = __esm({
  "node_modules/vike/dist/esm/utils/assertRoutingType.js"() {
    init_assert();
    init_getGlobalObject();
    init_isBrowser();
    import_picocolors27 = __toESM(require_picocolors(), 1);
    state = getGlobalObject("utils/assertRouterType.ts", {});
  }
});

// node_modules/vike/dist/esm/shared/route/utils.js
var init_utils7 = __esm({
  "node_modules/vike/dist/esm/shared/route/utils.js"() {
    init_assertRoutingType();
    init_isBrowser();
    init_assert();
    init_hasProp();
    init_isObjectWithKeys();
    init_sorter();
    init_isPromise();
    init_isPlainObject();
    init_objectAssign();
    init_slice();
    init_unique();
    init_isBrowser();
    init_parseUrl();
    init_checkType();
    init_joinEnglish();
    init_truncateString();
    init_isCallable();
    if (isBrowser()) {
      assertClientRouting();
    }
  }
});

// node_modules/vike/dist/esm/shared/route/abort.js
function isAbortError(thing) {
  return typeof thing === "object" && thing !== null && stamp2 in thing;
}
function logAbortErrorHandled(err, isProduction, pageContext) {
  if (isProduction)
    return;
  const urlCurrent = pageContext._urlRewrite ?? pageContext.urlOriginal;
  assert(urlCurrent);
  const abortCall = err._pageContextAbort._abortCall;
  assert(abortCall);
  const hookLoc = isUserHookError(err);
  let thrownBy = "";
  if (hookLoc) {
    thrownBy = ` by ${import_picocolors28.default.cyan(`${hookLoc.hookName}()`)} hook defined at ${hookLoc.hookFilePath}`;
  } else {
  }
  assertInfo(false, `${import_picocolors28.default.cyan(abortCall)} thrown${thrownBy} while rendering ${import_picocolors28.default.cyan(urlCurrent)}`, {
    onlyOnce: false
  });
}
function getPageContextFromAllRewrites(pageContextsFromRewrite) {
  assertNoInfiniteLoop(pageContextsFromRewrite);
  const pageContextFromAllRewrites = { _urlRewrite: null };
  pageContextsFromRewrite.forEach((pageContextFromRewrite) => {
    Object.assign(pageContextFromAllRewrites, pageContextFromRewrite);
  });
  return pageContextFromAllRewrites;
}
function assertNoInfiniteLoop(pageContextsFromRewrite) {
  const urlRewrites = [];
  pageContextsFromRewrite.forEach((pageContext) => {
    const urlRewrite = pageContext._urlRewrite;
    {
      const idx = urlRewrites.indexOf(urlRewrite);
      if (idx !== -1) {
        const loop = [...urlRewrites.slice(idx), urlRewrite].map((url) => `render('${url}')`).join(" => ");
        assertUsage2(false, `Infinite loop of render() calls: ${loop}`);
      }
    }
    urlRewrites.push(urlRewrite);
  });
}
function assertNoInfiniteAbortLoop(rewriteCount, redirectCount) {
  const abortCalls = [
    // prettier-ignore
    // biome-ignore format:
    rewriteCount > 0 && import_picocolors28.default.cyan("throw render('/some-url')"),
    redirectCount > 0 && import_picocolors28.default.cyan("throw redirect('/some-url')")
  ].filter(Boolean).join(" and ");
  assertUsage2(rewriteCount + redirectCount <= 7, `Maximum chain length of 7 ${abortCalls} exceeded. Did you define an infinite loop of ${abortCalls}?`);
}
var import_picocolors28, stamp2;
var init_abort = __esm({
  "node_modules/vike/dist/esm/shared/route/abort.js"() {
    init_executeHook();
    init_utils7();
    import_picocolors28 = __toESM(require_picocolors(), 1);
    stamp2 = "_isAbortError";
  }
});

// node_modules/vike/dist/esm/node/runtime/renderPage/isNewError.js
function isNewError(errErrorPage, errNominalPage) {
  warnIfErrorIsNotObject(errErrorPage);
  return !isSameErrorMessage(errNominalPage, errErrorPage) || // Isn't this redudant/superfluous? I think we can remove this entire file and only use isSameErrorMessage() instead.
  !hasAlreadyLogged(errNominalPage);
}
function hasAlreadyLogged(err) {
  if (!isObject(err))
    return false;
  return globalObject10.wasAlreadyLogged.has(err);
}
function setAlreadyLogged(err) {
  if (!isObject(err))
    return;
  globalObject10.wasAlreadyLogged.add(err);
}
var globalObject10;
var init_isNewError = __esm({
  "node_modules/vike/dist/esm/node/runtime/renderPage/isNewError.js"() {
    init_utils3();
    globalObject10 = getGlobalObject("runtime/renderPage/isNewError.ts", {
      wasAlreadyLogged: /* @__PURE__ */ new WeakSet()
    });
  }
});

// node_modules/vike/dist/esm/node/runtime/renderPage/logErrorHint.js
function logErrorHint(error) {
  const hint3 = getErrorHint(error);
  if (hint3)
    logHint(hint3);
}
function getErrorHint(error) {
  {
    const knownErr = isKnownError(error);
    if (knownErr) {
      if (knownErr.link) {
        return hintLinkPrefix + knownErr.link;
      } else {
        return hintDefault;
      }
    }
  }
  return null;
}
function logHint(hint3) {
  hint3 = formatHintLog(hint3);
  hint3 = import_picocolors29.default.bold(hint3);
  console.error(hint3);
}
function isKnownError(error) {
  const anywhere = getAnywhere(error);
  const knownErr = [
    //
    ...errorsMisc,
    ...errorsReact,
    ...errorsCjsEsm_withPreciseLink,
    ...errorsCjsEsm
  ].find((knownErorr) => {
    if (!includesLowercase(anywhere, knownErorr.errMsg))
      return false;
    if (knownErorr.shouldMentionNodeModules !== false && !includesLowercase(anywhere, "node_modules"))
      return false;
    return true;
  });
  if (!knownErr)
    return false;
  return knownErr;
}
function includesLowercase(str, substr) {
  if (substr instanceof RegExp) {
    let { flags: flags2 } = substr;
    if (!flags2.includes("i"))
      flags2 += "i";
    const regex2 = new RegExp(substr.source, flags2);
    return regex2.test(str);
  }
  if (typeof substr === "string") {
    return str.toLowerCase().includes(substr.toLowerCase());
  }
  assert(false);
}
function getAnywhere(error) {
  const code = getErrCode(error);
  const message = getErrMessage(error);
  const stack3 = getErrStack(error);
  const anywhere = [code, message, stack3].filter(Boolean).join("\n");
  return anywhere;
}
function getErrMessage(err) {
  if (!isObject(err))
    return null;
  if (!err.message)
    return null;
  if (typeof err.message !== "string")
    return null;
  return err.message;
}
function getErrCode(err) {
  if (!isObject(err))
    return null;
  if (!err.code)
    return null;
  if (typeof err.code !== "string")
    return null;
  return err.code;
}
function getErrStack(err) {
  if (!isObject(err))
    return null;
  if (!err.stack)
    return null;
  if (typeof err.stack !== "string")
    return null;
  return err.stack;
}
var import_picocolors29, hintDefault, hintLinkPrefix, errorsMisc, errorsReact, errorsCjsEsm_withPreciseLink, errorsCjsEsm;
var init_logErrorHint = __esm({
  "node_modules/vike/dist/esm/node/runtime/renderPage/logErrorHint.js"() {
    init_utils3();
    import_picocolors29 = __toESM(require_picocolors(), 1);
    hintDefault = "The error could be a CJS/ESM issue, see https://vike.dev/broken-npm-package";
    hintLinkPrefix = "To fix this error, see ";
    errorsMisc = [
      {
        errMsg: "jsxDEV is not a function",
        link: "https://github.com/vikejs/vike/issues/1469#issuecomment-1919518096",
        shouldMentionNodeModules: false
      },
      {
        // ```
        // Error [RollupError]: Could not resolve "../dist/client/assets.json" from "renderer/+onRenderHtml.tsx"
        // ```
        errMsg: "assets.json",
        link: "https://vike.dev/getGlobalContext",
        shouldMentionNodeModules: false
      }
    ];
    errorsReact = [
      {
        errMsg: "Element type is invalid: expected a string (for built-in components) or a class/function (for composite components)",
        link: "https://vike.dev/broken-npm-package#react-invalid-component",
        // The stack trace can be user-land while the import is coming from node_modules
        shouldMentionNodeModules: false
      },
      {
        // React's "Invalid hook call.", see https://github.com/vikejs/vike/discussions/1637#discussioncomment-9424712
        errMsg: "Cannot read properties of null (reading 'useContext')"
      }
    ];
    errorsCjsEsm_withPreciseLink = [
      {
        // `SyntaxError: Named export '${exportName}' not found. The requested module '${packageName}' is a CommonJS module, which may not support all module.exports as named exports.`
        errMsg: /Named export.*not found/i,
        link: "https://vike.dev/broken-npm-package#named-export-not-found",
        // It seems that this always points to an npm package import.
        shouldMentionNodeModules: false
      }
    ];
    errorsCjsEsm = [
      { errMsg: "ERR_UNSUPPORTED_DIR_IMPORT" },
      { errMsg: "ERR_REQUIRE_ESM" },
      { errMsg: "Must use import" },
      { errMsg: /Cannot find \S+ '(\S+)' imported from (\S+)/ },
      { errMsg: "ERR_UNKNOWN_FILE_EXTENSION" },
      { errMsg: /Unknown file extension "\S+" for (\S+)/ },
      // `SyntaxError: Cannot use import statement outside a module`.
      {
        errMsg: "Cannot use import statement",
        // Since user code is always ESM, this error must always originate from an npm package.
        shouldMentionNodeModules: false
      },
      { errMsg: "is not exported" },
      { errMsg: "Cannot read properties of undefined" },
      { errMsg: ".default is not" },
      // Using CJS inside ESM modules.
      { errMsg: "require is not a function" },
      { errMsg: "exports is not defined" },
      { errMsg: "module is not defined" },
      { errMsg: "window is not defined" },
      { errMsg: "not defined in ES" },
      { errMsg: "Unexpected token 'export'" }
    ];
  }
});

// node_modules/vike/dist/esm/node/runtime/renderPage/loggerProd.js
function logErrorProd(err, _httpRquestId) {
  warnIfErrorIsNotObject(err);
  setAlreadyLogged(err);
  if (isAbortError(err)) {
    return;
  }
  const errStr = isObject(err) && "stack" in err ? String(err.stack) : String(err);
  console.error(import_picocolors30.default.red(errStr));
  onRuntimeError(err);
}
function onRuntimeError(err) {
  logErrorHint(err);
}
var import_picocolors30;
var init_loggerProd = __esm({
  "node_modules/vike/dist/esm/node/runtime/renderPage/loggerProd.js"() {
    init_abort();
    init_isNewError();
    init_utils3();
    import_picocolors30 = __toESM(require_picocolors(), 1);
    init_logErrorHint();
  }
});

// node_modules/vike/dist/esm/node/runtime/renderPage/loggerRuntime.js
var logRuntimeError, logRuntimeInfo;
var init_loggerRuntime = __esm({
  "node_modules/vike/dist/esm/node/runtime/renderPage/loggerRuntime.js"() {
    init_loggerProd();
    logRuntimeInfo = null;
    logRuntimeError = // @ts-expect-error
    logRuntimeError ?? // Default
    logErrorProd;
  }
});

// node_modules/vike/dist/esm/node/runtime/renderPage/executeOnRenderHtmlHook.js
async function executeOnRenderHtmlHook(pageContext) {
  const { renderHook, hookFn } = getRenderHook(pageContext);
  objectAssign(pageContext, { _renderHook: renderHook });
  preparePageContextForUserConsumptionServerSide(pageContext);
  const hookReturnValue = await executeHook(() => hookFn(pageContext), renderHook, pageContext);
  const { documentHtml, pageContextProvidedByRenderHook, pageContextPromise, injectFilter } = processHookReturnValue(hookReturnValue, renderHook);
  Object.assign(pageContext, pageContextProvidedByRenderHook);
  objectAssign(pageContext, { _pageContextPromise: pageContextPromise });
  const onErrorWhileStreaming = (err) => {
    logRuntimeError(err, pageContext._httpRequestId);
    if (!pageContext.errorWhileRendering) {
      pageContext.errorWhileRendering = err;
    }
  };
  const htmlRender = await renderDocumentHtml(documentHtml, pageContext, onErrorWhileStreaming, injectFilter);
  assert(typeof htmlRender === "string" || isStream(htmlRender));
  return { htmlRender, renderHook };
}
function getRenderHook(pageContext) {
  let hookFound;
  {
    let hook;
    let hookName = void 0;
    hook = getHook(pageContext, "onRenderHtml");
    if (hook) {
      hookName = "onRenderHtml";
    } else {
      hook = getHook(pageContext, "render");
      if (hook) {
        hookName = "render";
      }
    }
    if (hook) {
      assert(hookName);
      const { hookFilePath, hookFn, hookTimeout } = hook;
      hookFound = {
        hookFn,
        renderHook: { hookFn, hookFilePath, hookName, hookTimeout }
      };
    }
  }
  if (!hookFound) {
    const hookName = pageContext._pageConfigs.length > 0 ? "onRenderHtml" : "render";
    assertUsage2(false, [
      `No ${hookName}() hook found, see https://vike.dev/${hookName}`
      /*
      'See https://vike.dev/render-modes for more information.',
      [
        // 'Loaded config files (none of them define the onRenderHtml() hook):',
        'Loaded server-side page files (none of them `export { render }`):',
        ...pageContext._pageFilePathsLoaded.map((f, i) => ` (${i + 1}): ${f}`)
      ].join('\n')
      */
    ].join(" "));
  }
  return hookFound;
}
function processHookReturnValue(hookReturnValue, renderHook) {
  let documentHtml;
  let pageContextPromise = null;
  let pageContextProvidedByRenderHook = null;
  let injectFilter = null;
  const ret = () => ({ documentHtml, pageContextProvidedByRenderHook, pageContextPromise, injectFilter });
  if (hookReturnValue === null)
    return ret();
  if (isDocumentHtml(hookReturnValue)) {
    documentHtml = hookReturnValue;
    return ret();
  }
  const errPrefix = `The ${renderHook.hookName}() hook defined at ${renderHook.hookFilePath}`;
  const errSuffix = `a string generated with the ${import_picocolors31.default.cyan("escapeInject`<html>...</html>`")} template tag or a string returned by ${import_picocolors31.default.cyan("dangerouslySkipEscape()")}, see https://vike.dev/escapeInject`;
  if (typeof hookReturnValue === "string") {
    assertWarning2(false, [errPrefix, "returned a plain JavaScript string which is dangerous: it should instead return", errSuffix].join(" "), { onlyOnce: true });
    hookReturnValue = dangerouslySkipEscape(hookReturnValue);
  }
  assertUsage2(isObject(hookReturnValue), [
    errPrefix,
    `should return ${import_picocolors31.default.cyan("null")}, the value ${import_picocolors31.default.cyan("documentHtml")}, or an object ${import_picocolors31.default.cyan("{ documentHtml, pageContext }")} where ${import_picocolors31.default.cyan("pageContext")} is ${import_picocolors31.default.cyan("undefined")} or an object holding additional pageContext values, and where ${import_picocolors31.default.cyan("documentHtml")} is`,
    errSuffix
  ].join(" "));
  assertHookReturnedObject(hookReturnValue, ["documentHtml", "pageContext", "injectFilter"], errPrefix);
  if (hookReturnValue.injectFilter) {
    assertUsage2(isCallable(hookReturnValue.injectFilter), "injectFilter should be a function");
    injectFilter = hookReturnValue.injectFilter;
  }
  assertUsage2(hookReturnValue.documentHtml, `${errPrefix} returned an object that is missing the ${import_picocolors31.default.code("documentHtml")} property.`);
  {
    let val = hookReturnValue.documentHtml;
    const errBegin = `${errPrefix} returned ${import_picocolors31.default.cyan("{ documentHtml }")}, but ${import_picocolors31.default.cyan("documentHtml")}`;
    if (typeof val === "string") {
      assertWarning2(false, [
        errBegin,
        `is a plain JavaScript string which is dangerous: ${import_picocolors31.default.cyan("documentHtml")} should be`,
        errSuffix
      ].join(" "), { onlyOnce: true });
      val = dangerouslySkipEscape(val);
    }
    assertUsage2(isDocumentHtml(val), [errBegin, "should be", errSuffix].join(" "));
    documentHtml = val;
  }
  if (hookReturnValue.pageContext) {
    const val = hookReturnValue.pageContext;
    const errBegin = `${errPrefix} returned ${import_picocolors31.default.cyan("{ pageContext }")}, but ${import_picocolors31.default.cyan("pageContext")}`;
    if (isPromise(val) || isCallable(val)) {
      assertWarning2(!isPromise(val), `${errBegin} is a promise which is deprecated in favor of async functions, see https://vike.dev/streaming#initial-data-after-stream-end`, { onlyOnce: true });
      pageContextPromise = val;
    } else {
      assertUsage2(isObject(val), `${errBegin} should be an object or an async function, see https://vike.dev/streaming#initial-data-after-stream-end`);
      assertPageContextProvidedByUser(val, renderHook);
      pageContextProvidedByRenderHook = val;
    }
  }
  return ret();
}
var import_picocolors31;
var init_executeOnRenderHtmlHook = __esm({
  "node_modules/vike/dist/esm/node/runtime/renderPage/executeOnRenderHtmlHook.js"() {
    init_renderHtml();
    init_getHook();
    init_utils3();
    init_stream();
    init_assertPageContextProvidedByUser();
    init_preparePageContextForUserConsumptionServerSide();
    init_assertHookReturnedObject();
    init_loggerRuntime();
    import_picocolors31 = __toESM(require_picocolors(), 1);
    init_executeHook();
  }
});

// node_modules/vike/dist/esm/shared/assertOnBeforeRenderHookReturn.js
function assertOnBeforeRenderHookReturn(hookReturnValue, hookFilePath) {
  if (hookReturnValue === void 0 || hookReturnValue === null) {
    return;
  }
  const errPrefix = `The onBeforeRender() hook defined by ${hookFilePath}`;
  assertUsage2(isPlainObject(hookReturnValue), `${errPrefix} should return a plain JavaScript object, ${import_picocolors32.default.cyan("undefined")}, or ${import_picocolors32.default.cyan("null")}`);
  assertHookReturnedObject(hookReturnValue, ["pageContext"], errPrefix);
  if (hookReturnValue.pageContext) {
    assertPageContextProvidedByUser(hookReturnValue["pageContext"], { hookName: "onBeforeRender", hookFilePath });
  }
}
var import_picocolors32;
var init_assertOnBeforeRenderHookReturn = __esm({
  "node_modules/vike/dist/esm/shared/assertOnBeforeRenderHookReturn.js"() {
    init_utils();
    init_assertPageContextProvidedByUser();
    init_assertHookReturnedObject();
    import_picocolors32 = __toESM(require_picocolors(), 1);
  }
});

// node_modules/vike/dist/esm/node/runtime/renderPage/executeOnBeforeRenderAndDataHooks.js
async function executeOnBeforeRenderAndDataHooks(pageContext) {
  if (pageContext._pageContextAlreadyProvidedByOnPrerenderHook) {
    return;
  }
  const dataHook = getHook(pageContext, "data");
  const onBeforeRenderHook = getHook(pageContext, "onBeforeRender");
  if (!dataHook && !onBeforeRenderHook) {
    return;
  }
  preparePageContextForUserConsumptionServerSide(pageContext);
  if (dataHook) {
    const hookResult = await executeHook(() => dataHook.hookFn(pageContext), dataHook, pageContext);
    const pageContextFromHook = {
      data: hookResult
    };
    Object.assign(pageContext, pageContextFromHook);
  }
  if (onBeforeRenderHook) {
    const hookResult = await executeHook(() => onBeforeRenderHook.hookFn(pageContext), onBeforeRenderHook, pageContext);
    assertOnBeforeRenderHookReturn(hookResult, onBeforeRenderHook.hookFilePath);
    const pageContextFromHook = hookResult?.pageContext;
    Object.assign(pageContext, pageContextFromHook);
  }
}
var init_executeOnBeforeRenderAndDataHooks = __esm({
  "node_modules/vike/dist/esm/node/runtime/renderPage/executeOnBeforeRenderAndDataHooks.js"() {
    init_getHook();
    init_preparePageContextForUserConsumptionServerSide();
    init_assertOnBeforeRenderHookReturn();
    init_executeHook();
  }
});

// node_modules/vike/dist/esm/shared/route/executeGuardHook.js
async function executeGuardHook(pageContext, prepareForUserConsumption) {
  let hook;
  if (pageContext._pageFilesAll.length > 0) {
    assert(pageContext._pageConfigs.length === 0);
    hook = findPageGuard(pageContext.pageId, pageContext._pageFilesAll);
  } else {
    hook = getHook(pageContext, "guard");
  }
  if (!hook)
    return;
  const guard = hook.hookFn;
  let pageContextForUserConsumption = pageContext;
  const res = prepareForUserConsumption(pageContext);
  if (res)
    pageContextForUserConsumption = res;
  const hookResult = await executeHook(() => guard(pageContextForUserConsumption), hook, pageContext);
  assertUsage2(hookResult === void 0, `${errIntro} ${hook.hookFilePath} returns a value, but guard() shouldn't return any value`);
}
function findPageGuard(pageId, pageFilesAll) {
  const pageRouteFile = pageFilesAll.find((p11) => p11.pageId === pageId && p11.fileType === ".page.route");
  if (!pageRouteFile)
    return null;
  const { filePath, fileExports } = pageRouteFile;
  assert(fileExports);
  const hookFn = fileExports.guard;
  if (!hookFn)
    return null;
  const hookFilePath = filePath;
  const hookTimeout = getHookTimeoutDefault("guard");
  assertUsage2(isCallable(hookFn), `${errIntro} ${hookFilePath} should be a function`);
  return { hookFn, hookName: "guard", hookFilePath, hookTimeout };
}
var errIntro;
var init_executeGuardHook = __esm({
  "node_modules/vike/dist/esm/shared/route/executeGuardHook.js"() {
    init_getHook();
    init_utils7();
    init_executeHook();
    errIntro = "The guard() hook defined by";
  }
});

// node_modules/vike/dist/esm/shared/route/deduceRouteStringFromFilesystemPath.js
function deduceRouteStringFromFilesystemPath(pageId, filesystemRoots) {
  const filesystemRootsMatch = filesystemRoots.filter(({ filesystemRoot }) => pageId.startsWith(filesystemRoot)).sort(higherFirst(({ filesystemRoot }) => filesystemRoot.length));
  const fsBase = filesystemRootsMatch[0];
  let filesystemRoute;
  if (fsBase) {
    const { filesystemRoot, urlRoot } = fsBase;
    const debugInfo = { pageId, filesystemRoot, urlRoot };
    assert(urlRoot.startsWith("/") && pageId.startsWith("/") && filesystemRoot.startsWith("/"), debugInfo);
    assert(pageId.startsWith(filesystemRoot), debugInfo);
    if (filesystemRoot !== "/") {
      assert(!filesystemRoot.endsWith("/"), debugInfo);
      filesystemRoute = slice(pageId, filesystemRoot.length, 0);
    } else {
      filesystemRoute = pageId;
    }
    assert(filesystemRoute.startsWith("/"), debugInfo);
    filesystemRoute = urlRoot + (urlRoot.endsWith("/") ? "" : "/") + slice(filesystemRoute, 1, 0);
  } else {
    filesystemRoute = pageId;
  }
  assert(filesystemRoute.startsWith("/"));
  filesystemRoute = filesystemRoute.split("/").filter((dir) => dir !== "pages" && dir !== "src" && dir !== "index").join("/");
  assert(!filesystemRoute.includes(".page."));
  assert(!filesystemRoute.endsWith("."));
  if (filesystemRoute.endsWith("/index")) {
    filesystemRoute = slice(filesystemRoute, 0, -"/index".length);
  }
  if (filesystemRoute === "") {
    filesystemRoute = "/";
  }
  assert(filesystemRoute.startsWith("/"));
  assert(!filesystemRoute.endsWith("/") || filesystemRoute === "/");
  return filesystemRoute;
}
var init_deduceRouteStringFromFilesystemPath = __esm({
  "node_modules/vike/dist/esm/shared/route/deduceRouteStringFromFilesystemPath.js"() {
    init_utils7();
  }
});

// node_modules/vike/dist/esm/shared/route/resolveRouteFunction.js
async function resolveRouteFunction(routeFunction, pageContext, routeDefinedAtString) {
  assertPageContextUrl(pageContext);
  let result = routeFunction(pageContext);
  assertSyncRouting(result, `The Route Function ${routeDefinedAtString}`);
  result = await result;
  if (result === false) {
    return null;
  }
  if (result === true) {
    result = {};
  }
  assertUsage2(isPlainObject(result), `The Route Function ${routeDefinedAtString} should return a boolean or a plain JavaScript object (but it's ${import_picocolors33.default.cyan(`typeof result === ${JSON.stringify(typeof result)}`)} instead)`);
  if ("match" in result) {
    const { match } = result;
    assertUsage2(typeof match === "boolean", `The ${import_picocolors33.default.cyan("match")} value returned by the Route Function ${routeDefinedAtString} should be a boolean.`);
    if (!match) {
      return null;
    }
  }
  let precedence = null;
  if ("precedence" in result) {
    precedence = result.precedence;
    assertUsage2(typeof precedence === "number", `The ${import_picocolors33.default.cyan("precedence")} value returned by the Route Function ${routeDefinedAtString} should be a number.`);
  }
  assertRouteParams(result, `The ${import_picocolors33.default.cyan("routeParams")} object returned by the Route Function ${routeDefinedAtString} should`);
  const routeParams = result.routeParams || {};
  assertUsage2(!("pageContext" in result), `Providing ${import_picocolors33.default.cyan("pageContext")} in Route Functions is prohibited, see https://vike.dev/route-function#cannot-provide-pagecontext`);
  assert(isPlainObject(routeParams));
  Object.keys(result).forEach((key) => {
    assertUsage2(key === "match" || key === "routeParams" || key === "precedence", `The Route Function ${routeDefinedAtString} returned an object with an unknown property ${import_picocolors33.default.cyan(key)} (the known properties are ${import_picocolors33.default.cyan("match")}, ${import_picocolors33.default.cyan("routeParams")}, and ${import_picocolors33.default.cyan("precedence")})`);
  });
  return {
    precedence,
    routeParams
  };
}
function assertSyncRouting(res, errPrefix) {
  assertWarning2(!isPromise(res), `${errPrefix} returned a promise, but asynchronous routing is deprecated and will be removed in the next major release, see https://vike.dev/route-function#async`, { onlyOnce: true });
}
function warnDeprecatedAllowKey() {
  const allowKey = import_picocolors33.default.cyan("iKnowThePerformanceRisksOfAsyncRouteFunctions");
  assertWarning2(false, `${allowKey} is deprecated and will be removed in the next major release`, { onlyOnce: true });
}
function assertRouteParams(result, errPrefix) {
  assert(errPrefix.endsWith(" should"));
  if (!hasProp(result, "routeParams")) {
    return;
  }
  assert(errPrefix.endsWith(" should"));
  assertUsage2(hasProp(result, "routeParams", "string{}"), `${errPrefix} be an object holding string values.`);
}
var import_picocolors33;
var init_resolveRouteFunction = __esm({
  "node_modules/vike/dist/esm/shared/route/resolveRouteFunction.js"() {
    init_getPageContextUrlComputed();
    init_utils7();
    import_picocolors33 = __toESM(require_picocolors(), 1);
  }
});

// node_modules/vike/dist/esm/shared/route/loadPageRoutes.js
async function loadPageRoutes(pageFilesAll, pageConfigs, pageConfigGlobal, allPageIds) {
  await Promise.all(pageFilesAll.filter((p11) => p11.fileType === ".page.route").map((p11) => p11.loadFile?.()));
  const { onBeforeRouteHook, filesystemRoots } = getGlobalHooks(pageFilesAll, pageConfigs, pageConfigGlobal);
  const pageRoutes = getPageRoutes(filesystemRoots, pageFilesAll, pageConfigs, allPageIds);
  return { pageRoutes, onBeforeRouteHook };
}
function getPageRoutes(filesystemRoots, pageFilesAll, pageConfigs, allPageIds) {
  const pageRoutes = [];
  if (pageConfigs.length > 0) {
    assert(filesystemRoots === null);
    const comesFromV1PageConfig = true;
    pageConfigs.filter((p11) => !p11.isErrorPage).forEach((pageConfig) => {
      const pageId = pageConfig.pageId;
      let pageRoute = null;
      {
        const configName = "route";
        const configValue = getConfigValueRuntime(pageConfig, configName);
        if (configValue) {
          const route2 = configValue.value;
          assert(configValue.definedAtData);
          const definedAtString = getDefinedAtString(configValue.definedAtData, configName);
          if (typeof route2 === "string") {
            pageRoute = {
              pageId,
              comesFromV1PageConfig,
              routeString: route2,
              routeDefinedAtString: definedAtString,
              routeType: "STRING"
            };
          } else {
            assert(isCallable(route2));
            if (getConfigValueRuntime(pageConfig, "iKnowThePerformanceRisksOfAsyncRouteFunctions", "boolean"))
              warnDeprecatedAllowKey();
            pageRoute = {
              pageId,
              comesFromV1PageConfig,
              routeFunction: route2,
              routeDefinedAtString: definedAtString,
              routeType: "FUNCTION"
            };
          }
        }
      }
      if (!pageRoute) {
        const { routeFilesystem } = pageConfig;
        assert(routeFilesystem);
        const { routeString, definedBy } = routeFilesystem;
        assert(routeFilesystem.routeString.startsWith("/"));
        pageRoute = {
          pageId,
          routeFilesystemDefinedBy: definedBy,
          comesFromV1PageConfig,
          routeString,
          routeDefinedAtString: null,
          routeType: "FILESYSTEM"
        };
      }
      assert(pageRoute);
      pageRoutes.push(pageRoute);
    });
  }
  if (pageConfigs.length === 0) {
    assert(filesystemRoots);
    const comesFromV1PageConfig = false;
    allPageIds.filter((pageId) => !isErrorPageId(pageId, false)).forEach((pageId) => {
      const pageRouteFile = pageFilesAll.find((p11) => p11.pageId === pageId && p11.fileType === ".page.route");
      if (!pageRouteFile || !("default" in pageRouteFile.fileExports)) {
        const routeString = deduceRouteStringFromFilesystemPath(pageId, filesystemRoots);
        assert(routeString.startsWith("/"));
        assert(!routeString.endsWith("/") || routeString === "/");
        pageRoutes.push({
          pageId,
          comesFromV1PageConfig,
          routeString,
          routeDefinedAtString: null,
          routeFilesystemDefinedBy: `${pageId}.page.*`,
          routeType: "FILESYSTEM"
        });
      } else {
        const { filePath, fileExports } = pageRouteFile;
        assert(fileExports.default);
        if (hasProp(fileExports, "default", "string")) {
          const routeString = fileExports.default;
          assertUsage2(routeString.startsWith("/"), `A Route String should start with a leading slash '/' but ${filePath} has \`export default '${routeString}'\`. Make sure to \`export default '/${routeString}'\` instead.`);
          pageRoutes.push({
            pageId,
            comesFromV1PageConfig,
            routeString,
            routeDefinedAtString: filePath,
            routeType: "STRING"
          });
          return;
        }
        if (hasProp(fileExports, "default", "function")) {
          const routeFunction = fileExports.default;
          {
            const allowKey = "iKnowThePerformanceRisksOfAsyncRouteFunctions";
            if (allowKey in fileExports) {
              warnDeprecatedAllowKey();
            }
          }
          pageRoutes.push({
            pageId,
            comesFromV1PageConfig,
            routeFunction,
            routeDefinedAtString: filePath,
            routeType: "FUNCTION"
          });
          return;
        }
        assertUsage2(false, `The default export of ${filePath} should be a string or a function.`);
      }
    });
  }
  return pageRoutes;
}
function getGlobalHooks(pageFilesAll, pageConfigs, pageConfigGlobal) {
  if (pageConfigs.length > 0) {
    const hook = getHookFromPageConfigGlobal(pageConfigGlobal, "onBeforeRoute");
    return { onBeforeRouteHook: hook, filesystemRoots: null };
  }
  let onBeforeRouteHook = null;
  const filesystemRoots = [];
  pageFilesAll.filter((p11) => p11.fileType === ".page.route" && p11.isDefaultPageFile).forEach(({ filePath, fileExports }) => {
    assert(fileExports);
    if ("onBeforeRoute" in fileExports) {
      assertUsage2(hasProp(fileExports, "onBeforeRoute", "function"), `\`export { onBeforeRoute }\` of ${filePath} should be a function.`);
      const { onBeforeRoute } = fileExports;
      const hookName = "onBeforeRoute";
      onBeforeRouteHook = {
        hookFilePath: filePath,
        hookFn: onBeforeRoute,
        hookName,
        hookTimeout: getHookTimeoutDefault(hookName)
      };
    }
    if ("filesystemRoutingRoot" in fileExports) {
      assertUsage2(hasProp(fileExports, "filesystemRoutingRoot", "string"), `\`export { filesystemRoutingRoot }\` of ${filePath} should be a string.`);
      assertUsage2(hasProp(fileExports, "filesystemRoutingRoot", "string"), `\`export { filesystemRoutingRoot }\` of ${filePath} is \`'${fileExports.filesystemRoutingRoot}'\` but it should start with a leading slash \`/\`.`);
      filesystemRoots.push({
        filesystemRoot: dirname(filePath),
        urlRoot: fileExports.filesystemRoutingRoot
      });
    }
  });
  return { onBeforeRouteHook, filesystemRoots };
}
function dirname(filePath) {
  assert(filePath.startsWith("/"));
  assert(!filePath.endsWith("/"));
  const paths = filePath.split("/");
  const dirPath = slice(paths, 0, -1).join("/") || "/";
  assert(dirPath.startsWith("/"));
  assert(!dirPath.endsWith("/") || dirPath === "/");
  return dirPath;
}
var init_loadPageRoutes = __esm({
  "node_modules/vike/dist/esm/shared/route/loadPageRoutes.js"() {
    init_error_page();
    init_utils7();
    init_deduceRouteStringFromFilesystemPath();
    init_utils();
    init_getConfigValue();
    init_getConfigDefinedAt();
    init_resolveRouteFunction();
    init_getHook();
  }
});

// node_modules/vike/dist/esm/node/shared/assertV1Design.js
function assertV1Design(pageConfigs, pageFilesAll) {
  const isOldDesign = pageFilesAll === true || pageFilesAll !== false && pageFilesAll.length > 0;
  const isV1Design = pageConfigs === true || pageConfigs !== false && pageConfigs.length > 0;
  if (isV1Design && isOldDesign) {
    const lines = ["Mixing the new V1 design with the old V0.4 design is forbidden."];
    const indent = "- ";
    if (typeof pageConfigs !== "boolean") {
      assert(pageConfigs.length > 0);
      const filesV1 = unique(pageConfigs.map((p11) => Object.values(p11.configValueSources).map((sources) => sources.map((c15) => c15.definedAtFilePath.filePathAbsoluteUserRootDir).filter(isNotNullish).map((filePathToShowToUser) => indent + filePathToShowToUser))).flat(2));
      lines.push(...["V1 design files:", ...filesV1]);
    }
    if (typeof pageFilesAll !== "boolean") {
      assert(pageFilesAll.length > 0);
      const filesOld = pageFilesAll.map((p11) => indent + p11.filePath);
      lines.push(...["Old design files:", ...filesOld]);
    }
    assertUsage2(false, lines.join("\n"));
  }
  assertWarning2(!isOldDesign, "You are using Vike's deprecated design. Update to the new V1 design, see https://vike.dev/migration/v1-design for how to migrate.", { onlyOnce: true });
}
var init_assertV1Design = __esm({
  "node_modules/vike/dist/esm/node/shared/assertV1Design.js"() {
    init_utils2();
  }
});

// node_modules/vike/dist/esm/node/runtime/renderPage/renderPageAlreadyRouted.js
async function renderPageAlreadyRouted(pageContext) {
  assert(hasProp(pageContext, "pageId", "string"));
  const isError = pageContext.is404 || !!pageContext.errorWhileRendering;
  assert(isError === (pageContext.pageId === getErrorPageId(pageContext._pageFilesAll, pageContext._pageConfigs)));
  objectAssign(pageContext, await loadUserFilesServerSide(pageContext));
  if (!isError) {
    await executeGuardHook(pageContext, (pageContext2) => preparePageContextForUserConsumptionServerSide(pageContext2));
  }
  if (!isError) {
    await executeOnBeforeRenderAndDataHooks(pageContext);
  } else {
    try {
      await executeOnBeforeRenderAndDataHooks(pageContext);
    } catch (err) {
      if (isNewError(err, pageContext.errorWhileRendering)) {
        logRuntimeError(err, pageContext._httpRequestId);
      }
    }
  }
  if (pageContext.isClientSideNavigation) {
    if (isError) {
      objectAssign(pageContext, { [isServerSideError]: true });
    }
    const pageContextSerialized = serializePageContextClientSide(pageContext);
    const httpResponse2 = await createHttpResponsePageContextJson(pageContextSerialized);
    objectAssign(pageContext, { httpResponse: httpResponse2 });
    return pageContext;
  }
  const renderHookResult = await executeOnRenderHtmlHook(pageContext);
  const { htmlRender, renderHook } = renderHookResult;
  const httpResponse = await createHttpResponsePage(htmlRender, renderHook, pageContext);
  objectAssign(pageContext, { httpResponse });
  return pageContext;
}
function getPageContextInitEnhanced(pageContextInit, renderContext, { ssr: { urlRewrite, urlHandler, isClientSideNavigation } = {
  urlRewrite: null,
  urlHandler: null,
  isClientSideNavigation: false
} } = {}) {
  assert(pageContextInit.urlOriginal);
  const globalContext = getGlobalContext();
  const pageContextInitEnhanced = {};
  objectAssign(pageContextInitEnhanced, pageContextInit);
  objectAssign(pageContextInitEnhanced, {
    _objectCreatedByVike: true,
    // The following is defined on `pageContext` because we can eventually make these non-global (e.g. sot that two pages can have different includeAssetsImportedByServer settings)
    _baseServer: globalContext.baseServer,
    _baseAssets: globalContext.baseAssets,
    _includeAssetsImportedByServer: globalContext.includeAssetsImportedByServer,
    // TODO: use GloablContext instead
    _pageFilesAll: renderContext.pageFilesAll,
    _pageConfigs: renderContext.pageConfigs,
    _pageConfigGlobal: renderContext.pageConfigGlobal,
    _allPageIds: renderContext.allPageIds,
    _pageRoutes: renderContext.pageRoutes,
    _onBeforeRouteHook: renderContext.onBeforeRouteHook,
    _pageContextInit: pageContextInit,
    _urlRewrite: urlRewrite,
    _urlHandler: urlHandler,
    isClientSideNavigation
  });
  const pageContextUrlComputed = getPageContextUrlComputed(pageContextInitEnhanced);
  objectAssign(pageContextInitEnhanced, pageContextUrlComputed);
  {
    let headers;
    if (pageContextInit.headersOriginal) {
      headers = normalizeHeaders(pageContextInit.headersOriginal);
      assertUsage2(!("headers" in pageContextInit), "You're defining pageContextInit.headersOriginal as well as pageContextInit.headers but you should only define pageContextInit.headersOriginal instead, see https://vike.dev/headers");
    } else if (pageContextInit.headers) {
      headers = pageContextInit.headers;
      assertWarning2(false, "Setting pageContextInit.headers is deprecated: set pageContextInit.headersOriginal instead, see https://vike.dev/headers", { onlyOnce: true });
    } else {
      headers = null;
    }
    objectAssign(pageContextInitEnhanced, { headers });
  }
  return pageContextInitEnhanced;
}
async function getRenderContext() {
  const globalContext = getGlobalContext();
  const { pageFilesAll, allPageIds, pageConfigs, pageConfigGlobal } = await getPageFilesAll(false, globalContext.isProduction);
  const { pageRoutes, onBeforeRouteHook } = await loadPageRoutes(pageFilesAll, pageConfigs, pageConfigGlobal, allPageIds);
  assertV1Design(
    // pageConfigs is PageConfigRuntime[] but assertV1Design() requires PageConfigBuildTime[]
    pageConfigs.length > 0,
    pageFilesAll
  );
  const renderContext = {
    pageFilesAll,
    pageConfigs,
    pageConfigGlobal,
    allPageIds,
    pageRoutes,
    onBeforeRouteHook
  };
  return renderContext;
}
var import_picocolors34;
var init_renderPageAlreadyRouted = __esm({
  "node_modules/vike/dist/esm/node/runtime/renderPage/renderPageAlreadyRouted.js"() {
    init_error_page();
    init_renderHtml();
    init_getPageFiles();
    init_utils3();
    init_serializePageContextClientSide();
    init_getPageContextUrlComputed();
    init_globalContext();
    init_createHttpResponse();
    init_loadUserFilesServerSide();
    init_executeOnRenderHtmlHook();
    init_executeOnBeforeRenderAndDataHooks();
    init_loggerRuntime();
    init_isNewError();
    init_preparePageContextForUserConsumptionServerSide();
    init_executeGuardHook();
    init_loadPageRoutes();
    import_picocolors34 = __toESM(require_picocolors(), 1);
    init_isServerSideError();
    init_assertV1Design();
  }
});

// node_modules/vike/dist/esm/shared/route/resolveRouteString.js
function assertRouteString(routeString, errPrefix = "Invalid") {
  let errPrefix2 = `${errPrefix} Route String ${highlight(routeString)}`;
  assertUsage2(routeString !== "", `${errPrefix2} (empty string): set it to ${highlight("/")} instead`);
  assertUsage2(["/", "*"].includes(routeString[0]), `${errPrefix2}: it should start with ${highlight("/")} or ${highlight("*")}`);
  assertUsage2(!routeString.includes("**"), `${errPrefix2}: set it to ${highlight(routeString.split("**").join("*"))} instead`);
}
function resolveRouteString(routeString, urlPathname) {
  assertRouteString(routeString);
  const segments = parseRouteString(routeString);
  const routeRegexStrInner = segments.map((segment) => {
    if (segment.param) {
      return "[^/]+";
    }
    if (segment.glob) {
      return ".*";
    }
    return escapeRegex(segment.static);
  }).map((s36) => `(${s36})`).join("");
  const routeRegex = new RegExp(`^${routeRegexStrInner}/?$`);
  const routeRegexMatch = urlPathname.match(routeRegex);
  if (!routeRegexMatch)
    return null;
  const routeParams = {};
  const [_2, ...segmentsValue] = routeRegexMatch;
  let globIdx = 0;
  const hasMultipleGlobs = segments.filter((segment) => segment.glob).length > 1;
  segments.forEach((segment, i13) => {
    let val = segmentsValue[i13];
    if (segment.param) {
      routeParams[segment.param] = val;
    }
    if (segment.glob) {
      const param = `*${hasMultipleGlobs ? ++globIdx : ""}`;
      routeParams[param] = val;
    }
  });
  return { routeParams };
}
function parseRouteString(routeString) {
  const segments = [];
  const pushStatic = (s36) => {
    const segmentLast = segments[segments.length - 1];
    if (segmentLast?.static) {
      segmentLast.static += s36;
    } else {
      segments.push({ static: s36 });
    }
  };
  const parts = routeString.split("/");
  parts.forEach((s36, i13) => {
    if (i13 !== 0)
      pushStatic("/");
    if (isParam(s36)) {
      assertWarning2(!s36.startsWith(PARAM_TOKEN_OLD), `Outdated Route String ${highlight(routeString)}, use ${highlight(routeString.split(PARAM_TOKEN_OLD).join(PARAM_TOKEN_NEW))} instead`, { onlyOnce: true });
      segments.push({ param: s36.slice(1) });
    } else {
      if (s36 === "*" && i13 === parts.length - 1 && routeString !== "*" && routeString !== "/*") {
        segments.push({ glob: true });
      } else {
        s36.split("*").forEach((s37, i14) => {
          if (i14 !== 0)
            segments.push({ glob: true });
          if (s37 !== "") {
            pushStatic(s37);
          }
        });
      }
    }
  });
  return segments;
}
function analyzeRouteString(routeString) {
  const segments = parseRouteString(routeString);
  const countStaticParts = (s36) => s36?.split("/").filter(Boolean).length || 0;
  let numberOfStaticPartsBeginning = 0;
  for (const segment of segments) {
    if (!segment.static)
      break;
    numberOfStaticPartsBeginning += countStaticParts(segment.static);
  }
  const numberOfStaticParts = segments.map((s36) => countStaticParts(s36.static)).reduce((sum, a18) => sum + a18, 0);
  const numberOfParams = segments.filter((s36) => s36.param).length;
  const numberOfGlobs = segments.filter((s36) => s36.glob).length;
  return { numberOfStaticPartsBeginning, numberOfStaticParts, numberOfParams, numberOfGlobs };
}
function isParam(routeSegment) {
  return routeSegment.startsWith(PARAM_TOKEN_NEW) || routeSegment.startsWith(PARAM_TOKEN_OLD);
}
function isStaticRouteString(routeString) {
  const url = routeString;
  const match = resolveRouteString(routeString, url);
  assert(match);
  return Object.keys(match.routeParams).length === 0;
}
function highlight(routeString) {
  if (isBrowser()) {
    return `'${routeString}'`;
  } else {
    if (routeString === "") {
      routeString = "''";
    }
    return import_picocolors35.default.cyan(routeString);
  }
}
var import_picocolors35, PARAM_TOKEN_NEW, PARAM_TOKEN_OLD;
var init_resolveRouteString = __esm({
  "node_modules/vike/dist/esm/shared/route/resolveRouteString.js"() {
    init_utils();
    init_utils7();
    import_picocolors35 = __toESM(require_picocolors(), 1);
    PARAM_TOKEN_NEW = "@";
    PARAM_TOKEN_OLD = ":";
  }
});

// node_modules/vike/dist/esm/shared/route/resolvePrecedence.js
function resolvePrecendence(routeMatches) {
  routeMatches.sort(sortMatches).sort(makeFirst((routeMatch) => routeMatch.routeType === "FUNCTION" && !!routeMatch.precedence && routeMatch.precedence < 0)).sort(makeFirst((routeMatch) => routeMatch.routeType === "STRING" && isStaticRouteString(routeMatch.routeString) === false)).sort(makeFirst((routeMatch) => routeMatch.routeType === "FUNCTION" && !routeMatch.precedence)).sort(makeFirst((routeMatch) => routeMatch.routeType === "STRING" && isStaticRouteString(routeMatch.routeString) === true)).sort(makeFirst((routeMatch) => routeMatch.routeType === "FILESYSTEM")).sort(makeFirst((routeMatch) => routeMatch.routeType === "FUNCTION" && !!routeMatch.precedence && routeMatch.precedence > 0));
}
function sortMatches(routeMatch1, routeMatch2) {
  {
    const precedence1 = routeMatch1.precedence ?? 0;
    const precedence2 = routeMatch2.precedence ?? 0;
    if (precedence1 !== precedence2) {
      return precedence1 > precedence2 ? -1 : 1;
    }
  }
  if (!routeMatch2.routeString) {
    return 0;
  }
  if (!routeMatch1.routeString) {
    return 0;
  }
  {
    const getValue = (routeString) => analyzeRouteString(routeString).numberOfStaticPartsBeginning;
    const result = higherFirst(getValue)(routeMatch1.routeString, routeMatch2.routeString);
    if (result !== 0) {
      return result;
    }
  }
  {
    const getValue = (routeString) => analyzeRouteString(routeString).numberOfStaticParts;
    const result = higherFirst(getValue)(routeMatch1.routeString, routeMatch2.routeString);
    if (result !== 0) {
      return result;
    }
  }
  {
    const getValue = (routeString) => analyzeRouteString(routeString).numberOfGlobs;
    const result = lowerFirst(getValue)(routeMatch1.routeString, routeMatch2.routeString);
    if (result !== 0) {
      return result;
    }
  }
  {
    const getValue = (routeString) => analyzeRouteString(routeString).numberOfParams;
    const result = higherFirst(getValue)(routeMatch1.routeString, routeMatch2.routeString);
    if (result !== 0) {
      return result;
    }
  }
  return 0;
}
var init_resolvePrecedence = __esm({
  "node_modules/vike/dist/esm/shared/route/resolvePrecedence.js"() {
    init_resolveRouteString();
    init_utils7();
    init_utils7();
    init_resolveRouteString();
  }
});

// node_modules/vike/dist/esm/shared/route/executeOnBeforeRouteHook.js
async function executeOnBeforeRouteHook(pageContext) {
  const pageContextFromOnBeforeRouteHook = {};
  if (!pageContext._onBeforeRouteHook)
    return null;
  const pageContextFromHook = await getPageContextFromHook(pageContext._onBeforeRouteHook, pageContext);
  if (pageContextFromHook) {
    objectAssign(pageContextFromOnBeforeRouteHook, pageContextFromHook);
    if (hasProp(pageContextFromOnBeforeRouteHook, "pageId", "string") || hasProp(pageContextFromOnBeforeRouteHook, "pageId", "null")) {
      if (!hasProp(pageContextFromOnBeforeRouteHook, "routeParams")) {
        objectAssign(pageContextFromOnBeforeRouteHook, { routeParams: {} });
      } else {
        assert(hasProp(pageContextFromOnBeforeRouteHook, "routeParams", "object"));
      }
      objectAssign(pageContextFromOnBeforeRouteHook, {
        _routingProvidedByOnBeforeRouteHook: true,
        _debugRouteMatches: "CUSTOM_ROUTING"
      });
      return pageContextFromOnBeforeRouteHook;
    }
  }
  objectAssign(pageContextFromOnBeforeRouteHook, {
    _routingProvidedByOnBeforeRouteHook: false
  });
  return pageContextFromOnBeforeRouteHook;
}
async function getPageContextFromHook(onBeforeRouteHook, pageContext) {
  let hookReturn = onBeforeRouteHook.hookFn(pageContext);
  assertSyncRouting(hookReturn, `The onBeforeRoute() hook ${onBeforeRouteHook.hookFilePath}`);
  hookReturn = await executeHook(() => hookReturn, onBeforeRouteHook, pageContext);
  const errPrefix = `The onBeforeRoute() hook defined by ${onBeforeRouteHook.hookFilePath}`;
  assertUsage2(hookReturn === null || hookReturn === void 0 || isObjectWithKeys(hookReturn, ["pageContext"]) && hasProp(hookReturn, "pageContext"), `${errPrefix} should return ${import_picocolors36.default.cyan("null")}, ${import_picocolors36.default.cyan("undefined")}, or a plain JavaScript object ${import_picocolors36.default.cyan("{ pageContext: { /* ... */ } }")}`);
  if (hookReturn === null || hookReturn === void 0) {
    return null;
  }
  assertUsage2(hasProp(hookReturn, "pageContext", "object"), `${errPrefix} returned ${import_picocolors36.default.cyan("{ pageContext }")} but pageContext should be a plain JavaScript object.`);
  if (hasProp(hookReturn.pageContext, "pageId") && !hasProp(hookReturn.pageContext, "pageId", "null")) {
    const errPrefix2 = `${errPrefix} returned ${import_picocolors36.default.cyan("{ pageContext: { pageId } }")} but ${import_picocolors36.default.cyan("pageId")} should be`;
    assertUsage2(hasProp(hookReturn.pageContext, "pageId", "string"), `${errPrefix2} a string or null`);
    assertUsage2(pageContext._allPageIds.includes(hookReturn.pageContext.pageId), `${errPrefix2} ${joinEnglish(pageContext._allPageIds.map((s36) => import_picocolors36.default.cyan(s36)), "or")}`);
  }
  if (hasProp(hookReturn.pageContext, "routeParams")) {
    assertRouteParams(hookReturn.pageContext, `${errPrefix} returned ${import_picocolors36.default.cyan("{ pageContext: { routeParams } }")} but routeParams should`);
  }
  const deprecatedReturn = (prop) => `${errPrefix} returned ${import_picocolors36.default.cyan(`{ pageContext: { ${prop} } }`)} which is deprecated. Return ${import_picocolors36.default.cyan("{ pageContext: { urlLogical } }")} instead.`;
  if (hasProp(hookReturn.pageContext, "url")) {
    assertWarning2(false, deprecatedReturn("url"), { onlyOnce: true });
    hookReturn.pageContext.urlLogical = hookReturn.pageContext.url;
    delete hookReturn.pageContext.url;
  }
  if (hasProp(hookReturn.pageContext, "urlOriginal")) {
    assertWarning2(false, deprecatedReturn("urlOriginal"), { onlyOnce: true });
    hookReturn.pageContext.urlLogical = hookReturn.pageContext.urlOriginal;
    delete hookReturn.pageContext.urlOriginal;
  }
  if (hasProp(hookReturn.pageContext, "urlLogical")) {
    assertUsageUrlPathnameAbsolute(
      // We skip validation and type-cast instead of assertUsage() in order to save client-side KBs
      hookReturn.pageContext.urlLogical,
      `${errPrefix} returned ${import_picocolors36.default.cyan("{ pageContext: { urlLogical } }")} but ${import_picocolors36.default.cyan("urlLogical")}`
    );
  }
  assertPageContextProvidedByUser(hookReturn.pageContext, {
    hookFilePath: onBeforeRouteHook.hookFilePath,
    hookName: "onBeforeRoute"
  });
  const pageContextAddendumHook = {};
  objectAssign(pageContextAddendumHook, hookReturn.pageContext);
  return pageContextAddendumHook;
}
var import_picocolors36;
var init_executeOnBeforeRouteHook = __esm({
  "node_modules/vike/dist/esm/shared/route/executeOnBeforeRouteHook.js"() {
    init_assertPageContextProvidedByUser();
    init_utils7();
    init_resolveRouteFunction();
    import_picocolors36 = __toESM(require_picocolors(), 1);
    init_executeHook();
  }
});

// node_modules/vike/dist/esm/shared/route/debug.js
function debug5(...args) {
  if (!_debug) {
    _debug = globalThis.__brillout_debug_createDebugger?.("vike:routing");
  }
  if (_debug) {
    _debug(...args);
  }
}
var _debug;
var init_debug3 = __esm({
  "node_modules/vike/dist/esm/shared/route/debug.js"() {
  }
});

// node_modules/vike/dist/esm/shared/route/index.js
async function route(pageContext) {
  debug5("Pages routes:", pageContext._pageRoutes);
  assertPageContextUrl(pageContext);
  const pageContextFromRoute = {};
  const pageContextFromOnBeforeRouteHook = await executeOnBeforeRouteHook(pageContext);
  if (pageContextFromOnBeforeRouteHook) {
    if (pageContextFromOnBeforeRouteHook._routingProvidedByOnBeforeRouteHook) {
      assert(pageContextFromOnBeforeRouteHook.pageId);
      return pageContextFromOnBeforeRouteHook;
    } else {
      objectAssign(pageContextFromRoute, pageContextFromOnBeforeRouteHook);
    }
  }
  objectAssign(pageContext, pageContextFromOnBeforeRouteHook);
  const allPageIds = pageContext._allPageIds;
  assertUsage2(allPageIds.length > 0, "No page found. You must create at least one page.");
  assert(pageContext._pageFilesAll.length > 0 || pageContext._pageConfigs.length > 0);
  const { urlPathname } = pageContext;
  assert(urlPathname.startsWith("/"));
  const routeMatches = [];
  await Promise.all(pageContext._pageRoutes.map(async (pageRoute) => {
    const { pageId, routeType } = pageRoute;
    if (pageRoute.routeType === "FILESYSTEM") {
      const { routeString } = pageRoute;
      const match = resolveRouteString(routeString, urlPathname);
      if (match) {
        const { routeParams } = match;
        routeMatches.push({ pageId, routeParams, routeString, routeType });
      }
      return;
    }
    if (pageRoute.routeType === "STRING") {
      const { routeString } = pageRoute;
      const match = resolveRouteString(routeString, urlPathname);
      if (match) {
        const { routeParams } = match;
        assert(routeType === "STRING");
        routeMatches.push({
          pageId,
          routeString,
          routeParams,
          routeType
        });
      }
      return;
    }
    if (pageRoute.routeType === "FUNCTION") {
      const { routeFunction, routeDefinedAtString } = pageRoute;
      const match = await resolveRouteFunction(routeFunction, pageContext, routeDefinedAtString);
      if (match) {
        const { routeParams, precedence } = match;
        routeMatches.push({ pageId, precedence, routeParams, routeType });
      }
      return;
    }
    assert(false);
  }));
  resolvePrecendence(routeMatches);
  const winner = routeMatches[0] ?? null;
  debug5(`Route matches for URL ${import_picocolors37.default.cyan(urlPathname)} (in precedence order):`, routeMatches);
  objectAssign(pageContextFromRoute, { _debugRouteMatches: routeMatches });
  objectAssign(pageContextFromRoute, { _routeMatch: winner });
  if (!winner) {
    objectAssign(pageContextFromRoute, {
      pageId: null,
      routeParams: {}
    });
    return pageContextFromRoute;
  }
  {
    const { routeParams } = winner;
    assert(isPlainObject(routeParams));
    objectAssign(pageContextFromRoute, {
      pageId: winner.pageId,
      routeParams: winner.routeParams
    });
  }
  return pageContextFromRoute;
}
var import_picocolors37;
var init_route = __esm({
  "node_modules/vike/dist/esm/shared/route/index.js"() {
    init_assertRoutingType();
    init_isBrowser();
    init_utils7();
    init_getPageContextUrlComputed();
    init_resolvePrecedence();
    init_resolveRouteString();
    init_resolveRouteFunction();
    init_executeOnBeforeRouteHook();
    init_debug3();
    import_picocolors37 = __toESM(require_picocolors(), 1);
    if (isBrowser()) {
      assertClientRouting();
    }
  }
});

// node_modules/vike/dist/esm/shared/getPageContextRequestUrl.js
var pageContextJsonFileExtension, doNotCreateExtraDirectory;
var init_getPageContextRequestUrl = __esm({
  "node_modules/vike/dist/esm/shared/getPageContextRequestUrl.js"() {
    init_urlToFile();
    pageContextJsonFileExtension = ".pageContext.json";
    doNotCreateExtraDirectory = false;
  }
});

// node_modules/vike/dist/esm/node/runtime/renderPage/handlePageContextRequestUrl.js
function handlePageContextRequestUrl(url) {
  if (!hasSuffix(url)) {
    return { urlWithoutPageContextRequestSuffix: url, isPageContextRequest: false };
  }
  return { urlWithoutPageContextRequestSuffix: removePageContextUrlSuffix(url), isPageContextRequest: true };
}
function hasSuffix(url) {
  const { pathnameOriginal, pathname } = parseUrl(url, baseServer);
  assert(pathnameOriginal.endsWith(pageContextJsonFileExtension) === pathname.endsWith(pageContextJsonFileExtension), {
    url
  });
  return pathnameOriginal.endsWith(pageContextJsonFileExtension);
}
function removePageContextUrlSuffix(url) {
  const urlParsed = parseUrl(url, baseServer);
  const { origin, pathnameOriginal, searchOriginal, hashOriginal } = urlParsed;
  assert(doNotCreateExtraDirectory === false);
  const urlSuffix = `/index${pageContextJsonFileExtension}`;
  assert(pathnameOriginal.endsWith(urlSuffix), { url });
  let pathnameModified = slice(pathnameOriginal, 0, -1 * urlSuffix.length);
  if (pathnameModified === "")
    pathnameModified = "/";
  assert(url === `${origin || ""}${pathnameOriginal}${searchOriginal || ""}${hashOriginal || ""}`, { url });
  return `${origin || ""}${pathnameModified}${searchOriginal || ""}${hashOriginal || ""}`;
}
var init_handlePageContextRequestUrl = __esm({
  "node_modules/vike/dist/esm/node/runtime/renderPage/handlePageContextRequestUrl.js"() {
    init_getPageContextRequestUrl();
    init_utils3();
  }
});

// node_modules/vike/dist/esm/node/runtime/renderPage/assertArguments.js
function assertArguments(...args) {
  const prefix = `${import_picocolors38.default.code("renderPage(pageContextInit)")} (https://vike.dev/renderPage)`;
  const pageContextInit = args[0];
  assertUsage2(pageContextInit !== void 0 && pageContextInit !== null, prefix + ` argument ${import_picocolors38.default.cyan("pageContextInit")} is missing`, { showStackTrace: true });
  const len = args.length;
  assertUsage2(len === 1, `${prefix} called with ${len} arguments but renderPage() accepts only one argument.'`, {
    showStackTrace: true
  });
  assertUsage2(isObject(pageContextInit), `${prefix} called with ${import_picocolors38.default.code(`typeof pageContextInit === ${JSON.stringify(typeof pageContextInit)}`)} but ${import_picocolors38.default.code("pageContextInit")} should be an object.`, { showStackTrace: true });
  if ("url" in pageContextInit) {
    assertWarning2(false, "`pageContextInit.url` has been renamed to `pageContextInit.urlOriginal`: replace `renderPage({ url })` with `renderPage({ urlOriginal })`. (See https://vike.dev/migration/0.4.23 for more information.)", { showStackTrace: true, onlyOnce: true });
    pageContextInit.urlOriginal = pageContextInit.url;
    delete pageContextInit.url;
  }
  assert(!("url" in pageContextInit));
  assertUsage2(hasProp(pageContextInit, "urlOriginal"), prefix + ` ${import_picocolors38.default.cyan("pageContextInit")} is missing the property ${import_picocolors38.default.cyan("pageContextInit.urlOriginal")}`, { showStackTrace: true });
  const { urlOriginal } = pageContextInit;
  assertUsage2(typeof urlOriginal === "string", prefix + ` ${import_picocolors38.default.cyan("pageContextInit.urlOriginal")} should be a string but ${import_picocolors38.default.cyan(`typeof pageContextInit.urlOriginal === ${JSON.stringify(typeof urlOriginal)}`)}`, { showStackTrace: true });
  assertUsage2(urlOriginal.startsWith("/") || urlOriginal.startsWith("https://") || urlOriginal.startsWith("http://"), prefix + ` ${import_picocolors38.default.cyan("pageContextInit.urlOriginal")} should start with ${import_picocolors38.default.cyan("/")} (e.g. ${import_picocolors38.default.cyan("/product/42")}), ${import_picocolors38.default.cyan("http://")}, or ${import_picocolors38.default.cyan("https://")} (e.g. ${import_picocolors38.default.cyan("https://example.com/product/42")}), but ${import_picocolors38.default.cyan(`pageContextInit.urlOriginal === ${JSON.stringify(urlOriginal)}`)}`, { showStackTrace: true });
  const urlOriginalWithoutOrigin = urlOriginal.startsWith("http") ? urlOriginal : "http://fake-origin.example.org" + urlOriginal;
  try {
    new URL(urlOriginalWithoutOrigin);
  } catch (err) {
    assertUsage2(false, prefix + ` ${import_picocolors38.default.cyan("pageContextInit.urlOriginal")} should be a URL but ${import_picocolors38.default.cyan(`pageContextInit.urlOriginal === ${JSON.stringify(urlOriginal)}`)}`, { showStackTrace: true });
  }
}
var import_picocolors38;
var init_assertArguments = __esm({
  "node_modules/vike/dist/esm/node/runtime/renderPage/assertArguments.js"() {
    init_utils3();
    import_picocolors38 = __toESM(require_picocolors(), 1);
  }
});

// node_modules/vike/dist/esm/shared/route/noRouteMatch.js
var noRouteMatch;
var init_noRouteMatch = __esm({
  "node_modules/vike/dist/esm/shared/route/noRouteMatch.js"() {
    noRouteMatch = "doesn't match the route of any of your pages";
  }
});

// node_modules/vike/dist/esm/node/runtime/renderPage/log404/index.js
async function log404(pageContext) {
  const { urlPathname } = pageContext;
  const pageRoutes = pageContext._pageRoutes;
  assertUsage2(
    pageRoutes.length > 0,
    "No page found."
    /* TODO/v1-release: use this
    'No page found. Create at least one /pages/some-page/+Page.js file.'
    */
  );
  const globalContext = getGlobalContext();
  if (!globalContext.isProduction && !isFileRequest(urlPathname) && !pageContext.isClientSideNavigation) {
    const routesInfo = getRoutesInfo(pageRoutes);
    let msg = `URL ${import_picocolors39.default.cyan(urlPathname)} ${noRouteMatch}`;
    const outro = "See https://vike.dev/routing for more information about routing.";
    if (!routesInfo) {
      msg = `${msg}. ${import_picocolors39.default.dim(outro)}`;
    } else {
      msg = `${msg}:
${routesInfo}
${outro}`;
    }
    assertInfo(false, msg, { onlyOnce: false });
  }
}
function getRoutesInfo(pageRoutes) {
  const entries = pageRoutes.map((pageRoute) => {
    let routeStr;
    let routeTypeSrc;
    let routeDefinedBy;
    if (pageRoute.routeType === "FILESYSTEM") {
      assert(pageRoute.routeFilesystemDefinedBy);
      routeDefinedBy = pageRoute.routeFilesystemDefinedBy;
    } else {
      assert(pageRoute.routeDefinedAtString);
      routeDefinedBy = pageRoute.routeDefinedAtString;
    }
    if (pageRoute.routeType === "STRING") {
      routeStr = pageRoute.routeString;
      routeTypeSrc = "Route String";
    } else if (pageRoute.routeType === "FUNCTION") {
      routeStr = String(pageRoute.routeFunction);
      routeTypeSrc = "Route Function";
    } else {
      routeStr = pageRoute.routeString;
      routeTypeSrc = "Filesystem Route";
    }
    assert(routeStr && routeTypeSrc && routeDefinedBy);
    return { routeStr, routeTypeSrc, routeDefinedBy };
  }).sort((e1, e22) => {
    if (e1.routeTypeSrc !== "Route Function" && e22.routeTypeSrc === "Route Function") {
      return -1;
    }
    if (e1.routeTypeSrc === "Route Function" && e22.routeTypeSrc !== "Route Function") {
      return 1;
    }
    return compareString(e1.routeStr, e22.routeStr);
  });
  const linesContent = [
    {
      routeStr: "ROUTE",
      routeTypeSrc: "TYPE",
      routeDefinedBy: "DEFINED BY"
    },
    ...entries
  ];
  const terminalWidth = getTerminalWidth() || 134;
  let width2 = Math.max(...linesContent.map(({ routeTypeSrc }) => stripAnsi(routeTypeSrc).length));
  let width3 = Math.max(...linesContent.map(({ routeDefinedBy }) => stripAnsi(routeDefinedBy).length));
  const width1_max = terminalWidth - width3 - width2 - // Total width of table border & padding
  10;
  if (width1_max < 10)
    return null;
  linesContent.forEach((lineContent) => {
    let { routeStr } = lineContent;
    if (lineContent.routeTypeSrc === "Route Function") {
      routeStr = truncateRouteFunction(routeStr, width1_max);
      assert(stripAnsi(routeStr).length <= width1_max);
    }
    lineContent.routeStr = routeStr;
  });
  let width1 = Math.max(...linesContent.map(({ routeStr }) => stripAnsi(routeStr).length));
  if (width1 > width1_max)
    return null;
  let lines = linesContent.map(({ routeStr, routeTypeSrc, routeDefinedBy }, i13) => {
    let cell1 = padEnd(routeStr, width1 + (stripAnsi(routeStr).length - stripAnsi(routeStr).length));
    let cell2 = padEnd(routeTypeSrc, width2);
    let cell3 = padEnd(routeDefinedBy, width3);
    const isHeader = i13 === 0;
    if (isHeader) {
      cell1 = import_picocolors39.default.dim(cell1);
      cell2 = import_picocolors39.default.dim(cell2);
      cell3 = import_picocolors39.default.dim(cell3);
    }
    let line = [cell1, cell2, cell3].join(import_picocolors39.default.dim(" \u2502 "));
    line = import_picocolors39.default.dim("\u2502 ") + line + import_picocolors39.default.dim(" \u2502");
    return line;
  });
  width1 = width1 + 2;
  width2 = width2 + 2;
  width3 = width3 + 2;
  lines = [
    import_picocolors39.default.dim(`\u250C${"\u2500".repeat(width1)}\u252C${"\u2500".repeat(width2)}\u252C${"\u2500".repeat(width3)}\u2510`),
    lines[0],
    import_picocolors39.default.dim(`\u251C${"\u2500".repeat(width1)}\u253C${"\u2500".repeat(width2)}\u253C${"\u2500".repeat(width3)}\u2524`),
    ...lines.slice(1),
    import_picocolors39.default.dim(`\u2514${"\u2500".repeat(width1)}\u2534${"\u2500".repeat(width2)}\u2534${"\u2500".repeat(width3)}\u2518`)
  ];
  lines.forEach((line) => {
    assert(stripAnsi(line).length <= terminalWidth);
  });
  return lines.join("\n");
}
function truncateRouteFunction(routeStr, lenMax) {
  routeStr = stripAnsi(routeStr);
  routeStr = removeNonAscii(routeStr);
  routeStr = routeStr.split(/\s/).filter(Boolean).join(" ");
  routeStr = truncateString(routeStr, lenMax);
  return routeStr;
}
function padEnd(str, width) {
  const padWidth = Math.max(0, width - stripAnsi(str).length);
  return str + "".padEnd(padWidth, " ");
}
function removeNonAscii(str) {
  return str.replace(/[^\x00-\x7F]/g, "");
}
function isFileRequest(urlPathname) {
  assert(urlPathname.startsWith("/"));
  const paths = urlPathname.split("/");
  const lastPath = paths[paths.length - 1];
  assert(typeof lastPath === "string");
  const parts = lastPath.split(".");
  if (parts.length < 2) {
    return false;
  }
  const fileExtension = parts[parts.length - 1];
  assert(typeof fileExtension === "string");
  return /^[a-z0-9]+$/.test(fileExtension);
}
var import_picocolors39;
var init_log404 = __esm({
  "node_modules/vike/dist/esm/node/runtime/renderPage/log404/index.js"() {
    init_noRouteMatch();
    init_globalContext();
    init_utils3();
    import_picocolors39 = __toESM(require_picocolors(), 1);
  }
});

// node_modules/vike/dist/esm/node/runtime/renderPage/isConfigInvalid.js
var isConfigInvalid;
var init_isConfigInvalid = __esm({
  "node_modules/vike/dist/esm/node/runtime/renderPage/isConfigInvalid.js"() {
    init_utils3();
  }
});

// node_modules/vike/dist/esm/node/runtime/renderPage/handleErrorWithoutErrorPage.js
async function handleErrorWithoutErrorPage(pageContext) {
  assert(pageContext.pageId === null);
  assert(pageContext.errorWhileRendering || pageContext.is404);
  {
    const isV1 = pageContext._pageConfigs.length > 0;
    warnMissingErrorPage(isV1);
  }
  if (!pageContext.isClientSideNavigation) {
    const httpResponse = createHttpResponseError(pageContext);
    objectAssign(pageContext, { httpResponse });
    return pageContext;
  } else {
    const __getPageAssets = async () => [];
    objectAssign(pageContext, { __getPageAssets });
    const httpResponse = await createHttpResponsePage((0, import_stringify3.stringify)({ serverSideError: true }), null, pageContext);
    objectAssign(pageContext, { httpResponse });
    return pageContext;
  }
}
function warnMissingErrorPage(isV1) {
  const globalContext = getGlobalContext();
  if (!globalContext.isProduction) {
    const msg = [
      `No ${isV1 ? "error page" : import_picocolors40.default.cyan("_error.page.js")} found,`,
      "we recommend defining one",
      "https://vike.dev/error-page"
    ].join(" ");
    assertWarning2(false, msg, { onlyOnce: true });
  }
}
var import_stringify3, import_picocolors40;
var init_handleErrorWithoutErrorPage = __esm({
  "node_modules/vike/dist/esm/node/runtime/renderPage/handleErrorWithoutErrorPage.js"() {
    import_stringify3 = __toESM(require_stringify(), 1);
    init_globalContext();
    init_utils3();
    init_createHttpResponse();
    import_picocolors40 = __toESM(require_picocolors(), 1);
  }
});

// node_modules/vike/dist/esm/shared/route/resolveUrlPathname.js
function resolveUrlPathname(routeString, routeParams) {
  let parts = [{ val: routeString, type: "ROUTE_STRING" }];
  Object.entries(routeParams).forEach(([key, val]) => {
    if (key.startsWith("*")) {
      assert(key === "*" || /\d+/.test(key.slice(1)));
      assertUsage2(key === "*", "Resolving URL with multiple globs isn't implemented yet");
    } else {
      key = `@${key}`;
    }
    parts = parts.map((part) => {
      if (part.type === "URL") {
        return part;
      } else {
        return part.val.split(key).map((rest, i13) => {
          const partURL = { val, type: "URL" };
          const partRouteString = { val: rest, type: "ROUTE_STRING" };
          return i13 === 0 ? [partRouteString] : [partURL, partRouteString];
        }).flat();
      }
    }).flat();
  });
  const urlPathname = parts.map((p11) => p11.val).join("");
  return urlPathname;
}
var init_resolveUrlPathname = __esm({
  "node_modules/vike/dist/esm/shared/route/resolveUrlPathname.js"() {
    init_assertIsNotBrowser();
    init_utils();
    assertIsNotBrowser();
  }
});

// node_modules/vike/dist/esm/node/runtime/renderPage/resolveRedirects.js
function resolveRedirects(redirects, urlPathname) {
  for (const [urlSource, urlTarget] of Object.entries(redirects)) {
    const urlResolved = resolveRouteStringRedirect(urlSource, urlTarget, urlPathname);
    if (urlResolved)
      return urlResolved;
  }
  return null;
}
function resolveRouteStringRedirect(urlSource, urlTarget, urlPathname) {
  assertRouteString(urlSource, `${configSrc} Invalid`);
  assertUsageUrlRedirectTarget(urlTarget, `${configSrc} The URL redirection target`, true);
  assertParams(urlSource, urlTarget);
  const match = resolveRouteString(urlSource, urlPathname);
  if (!match)
    return null;
  const urlResolved = resolveUrlPathname(urlTarget, match.routeParams);
  if (urlResolved === urlPathname)
    return null;
  assert(isUrlRedirectTarget(urlResolved));
  return urlResolved;
}
function assertParams(urlSource, urlTarget) {
  const routeSegments = urlTarget.split("/");
  routeSegments.forEach((routeSegment) => {
    if (routeSegment.startsWith("@") || routeSegment.startsWith("*")) {
      const segments = urlSource.split("/");
      assertUsage2(segments.includes(routeSegment), `${configSrc} The redirection source URL ${import_picocolors41.default.string(urlSource)} is missing the URL parameter ${import_picocolors41.default.string(routeSegment)} used by the redirection target URL ${import_picocolors41.default.string(urlTarget)}`);
    }
  });
}
var import_picocolors41, configSrc;
var init_resolveRedirects = __esm({
  "node_modules/vike/dist/esm/node/runtime/renderPage/resolveRedirects.js"() {
    init_assertIsNotBrowser();
    init_utils();
    init_resolveUrlPathname();
    init_resolveRouteString();
    import_picocolors41 = __toESM(require_picocolors(), 1);
    assertIsNotBrowser();
    configSrc = "[vite.config.js > vike({ redirects })]";
  }
});

// node_modules/vike/dist/esm/node/runtime/renderPage.js
async function renderPage(pageContextInit) {
  assertArguments(...arguments);
  assert(hasProp(pageContextInit, "urlOriginal", "string"));
  assertEnv();
  assertIsUrl(pageContextInit.urlOriginal);
  const urlPathnameWithBase = parseUrl(pageContextInit.urlOriginal, "/").pathname;
  assertIsNotViteRequest(urlPathnameWithBase, pageContextInit.urlOriginal);
  if (urlPathnameWithBase.endsWith("/favicon.ico"))
    return getPageContextHttpResponseFavicon404(pageContextInit);
  const httpRequestId = getRequestId();
  const urlOriginalPretty = getUrlPretty(pageContextInit.urlOriginal);
  logHttpRequest(urlOriginalPretty, httpRequestId);
  const { pageContextReturn } = await renderPage_wrapper(httpRequestId, () => renderPageAndPrepare(pageContextInit, httpRequestId));
  logHttpResponse(urlOriginalPretty, httpRequestId, pageContextReturn);
  checkType(pageContextReturn);
  assert(pageContextReturn.httpResponse);
  return pageContextReturn;
}
async function renderPageAndPrepare(pageContextInit, httpRequestId) {
  const handleInvalidConfig = (err) => {
    logRuntimeInfo?.(import_picocolors42.default.bold(import_picocolors42.default.red("Error while loading a Vike config file, see error above.")), httpRequestId, "error");
    const pageContextWithError = getPageContextHttpResponseError(err, pageContextInit, null);
    return pageContextWithError;
  };
  if (isConfigInvalid) {
    if (1 < 2) {
      return handleInvalidConfig(isConfigInvalid.err);
    }
  }
  let renderContext;
  try {
    await initGlobalContext_renderPage();
    renderContext = await getRenderContext();
  } catch (err) {
    assert(!isAbortError(err));
    logRuntimeError(err, httpRequestId);
    const pageContextWithError = getPageContextHttpResponseError(err, pageContextInit, null);
    return pageContextWithError;
  }
  if (isConfigInvalid) {
    return handleInvalidConfig(isConfigInvalid.err);
  } else {
  }
  assertBaseUrl(pageContextInit);
  {
    const pageContextHttpResponse = normalizeUrl(pageContextInit, httpRequestId);
    if (pageContextHttpResponse)
      return pageContextHttpResponse;
  }
  {
    const pageContextHttpResponse = getPermanentRedirect(pageContextInit, httpRequestId);
    if (pageContextHttpResponse)
      return pageContextHttpResponse;
  }
  return await renderPageAlreadyPrepared(pageContextInit, httpRequestId, renderContext, []);
}
async function renderPageAlreadyPrepared(pageContextInit, httpRequestId, renderContext, pageContextsFromRewrite) {
  assertNoInfiniteAbortLoop(
    pageContextsFromRewrite.length,
    // There doesn't seem to be a way to count the number of HTTP redirects (vike don't have access to the HTTP request headers/cookies)
    // https://stackoverflow.com/questions/9683007/detect-infinite-http-redirect-loop-on-server-side
    0
  );
  let pageContextNominalPageSuccess;
  let pageContextNominalPageInit = {};
  {
    const pageContextFromAllRewrites = getPageContextFromAllRewrites(pageContextsFromRewrite);
    objectAssign(pageContextNominalPageInit, pageContextFromAllRewrites);
  }
  {
    const pageContextInitEnhanced = getPageContextInitEnhancedSSR(pageContextInit, renderContext, pageContextNominalPageInit._urlRewrite, httpRequestId);
    objectAssign(pageContextNominalPageInit, pageContextInitEnhanced);
  }
  let errNominalPage;
  {
    try {
      pageContextNominalPageSuccess = await renderPageNominal(pageContextNominalPageInit);
    } catch (err) {
      errNominalPage = err;
      assert(errNominalPage);
      logRuntimeError(errNominalPage, httpRequestId);
    }
    if (!errNominalPage) {
      assert(pageContextNominalPageSuccess === pageContextNominalPageInit);
    }
  }
  if (pageContextNominalPageSuccess && "is404" in pageContextNominalPageSuccess && pageContextNominalPageSuccess.is404 === true) {
    await log404(pageContextNominalPageSuccess);
  }
  if (errNominalPage === void 0) {
    assert(pageContextNominalPageSuccess);
    return pageContextNominalPageSuccess;
  } else {
    assert(errNominalPage);
    assert(pageContextNominalPageSuccess === void 0);
    assert(pageContextNominalPageInit);
    assert(hasProp(pageContextNominalPageInit, "urlOriginal", "string"));
    const pageContextErrorPageInit = await getPageContextErrorPageInit(pageContextInit, errNominalPage, pageContextNominalPageInit, renderContext, httpRequestId);
    if (isAbortError(errNominalPage)) {
      const handled = await handleAbortError(errNominalPage, pageContextsFromRewrite, pageContextInit, pageContextNominalPageInit, httpRequestId, renderContext, pageContextErrorPageInit);
      if (handled.pageContextReturn) {
        return handled.pageContextReturn;
      } else {
      }
      Object.assign(pageContextErrorPageInit, handled.pageContextAbort);
    }
    {
      const errorPageId = getErrorPageId(renderContext.pageFilesAll, renderContext.pageConfigs);
      if (!errorPageId) {
        objectAssign(pageContextErrorPageInit, { pageId: null });
        return handleErrorWithoutErrorPage(pageContextErrorPageInit);
      } else {
        objectAssign(pageContextErrorPageInit, { pageId: errorPageId });
      }
    }
    let pageContextErrorPage;
    try {
      pageContextErrorPage = await renderPageAlreadyRouted(pageContextErrorPageInit);
    } catch (errErrorPage) {
      if (isAbortError(errErrorPage)) {
        const handled = await handleAbortError(errErrorPage, pageContextsFromRewrite, pageContextInit, pageContextNominalPageInit, httpRequestId, renderContext, pageContextErrorPageInit);
        if (!handled.pageContextReturn) {
          const pageContextAbort = errErrorPage._pageContextAbort;
          assertWarning2(false, `Failed to render error page because ${import_picocolors42.default.cyan(pageContextAbort._abortCall)} was called: make sure ${import_picocolors42.default.cyan(pageContextAbort._abortCaller)} doesn't occur while the error page is being rendered.`, { onlyOnce: false });
          const pageContextHttpWithError = getPageContextHttpResponseError(errNominalPage, pageContextInit, pageContextErrorPageInit);
          return pageContextHttpWithError;
        }
        return handled.pageContextReturn;
      }
      if (isNewError(errErrorPage, errNominalPage)) {
        logRuntimeError(errErrorPage, httpRequestId);
      }
      const pageContextWithError = getPageContextHttpResponseError(errNominalPage, pageContextInit, pageContextErrorPageInit);
      return pageContextWithError;
    }
    return pageContextErrorPage;
  }
}
function logHttpRequest(urlOriginal, httpRequestId) {
  logRuntimeInfo?.(getRequestInfoMessage(urlOriginal), httpRequestId, "info");
}
function getRequestInfoMessage(urlOriginal) {
  return `HTTP request: ${prettyUrl(urlOriginal)}`;
}
function logHttpResponse(urlOriginalPretty, httpRequestId, pageContextReturn) {
  const statusCode = pageContextReturn.httpResponse?.statusCode ?? null;
  let msg;
  let isNominal;
  {
    const { errorWhileRendering } = pageContextReturn;
    const isSkipped = statusCode === null && !errorWhileRendering;
    if (isSkipped) {
      assert(errorWhileRendering === null || errorWhileRendering === void 0);
      msg = `HTTP response ${prettyUrl(urlOriginalPretty)} ${import_picocolors42.default.dim("null")}`;
      isNominal = true;
    } else {
      const isSuccess = statusCode !== null && statusCode >= 200 && statusCode <= 399;
      isNominal = isSuccess || statusCode === 404;
      const color2 = (s36) => import_picocolors42.default.bold(isSuccess ? import_picocolors42.default.green(String(s36)) : import_picocolors42.default.red(String(s36)));
      const isRedirect = statusCode && 300 <= statusCode && statusCode <= 399;
      const type2 = isRedirect ? "redirect" : "response";
      if (isRedirect) {
        assert(pageContextReturn.httpResponse);
        const headerRedirect = pageContextReturn.httpResponse.headers.slice().reverse().find((header2) => header2[0] === "Location");
        assert(headerRedirect);
        const urlRedirect = headerRedirect[1];
        urlOriginalPretty = urlRedirect;
      }
      msg = `HTTP ${type2} ${prettyUrl(urlOriginalPretty)} ${color2(statusCode ?? "ERR")}`;
    }
  }
  logRuntimeInfo?.(msg, httpRequestId, isNominal ? "info" : "error");
}
function prettyUrl(url) {
  return import_picocolors42.default.bold(decodeURI(url));
}
function getPageContextHttpResponseError(err, pageContextInit, pageContext) {
  const pageContextWithError = createPageContext(pageContextInit);
  const httpResponse = createHttpResponseError(pageContext);
  objectAssign(pageContextWithError, {
    httpResponse,
    errorWhileRendering: err
  });
  return pageContextWithError;
}
function getPageContextHttpResponseFavicon404(pageContextInit) {
  const pageContext = createPageContext(pageContextInit);
  const httpResponse = createHttpResponseFavicon404();
  objectAssign(pageContext, {
    httpResponse
  });
  checkType(pageContext);
  return pageContext;
}
function createPageContext(pageContextInit) {
  const pageContext = {
    _isPageContextObject: true
  };
  Object.assign(pageContext, pageContextInit);
  return pageContext;
}
async function renderPageNominal(pageContext) {
  objectAssign(pageContext, { errorWhileRendering: null });
  {
    const pageContextFromRoute = await route(pageContext);
    objectAssign(pageContext, pageContextFromRoute);
    objectAssign(pageContext, { is404: pageContext.pageId ? null : true });
    if (pageContext.pageId === null) {
      const errorPageId = getErrorPageId(pageContext._pageFilesAll, pageContext._pageConfigs);
      if (!errorPageId) {
        assert(hasProp(pageContext, "pageId", "null"));
        return handleErrorWithoutErrorPage(pageContext);
      }
      objectAssign(pageContext, { pageId: errorPageId });
    }
  }
  assert(hasProp(pageContext, "pageId", "string"));
  assert(pageContext.errorWhileRendering === null);
  const pageContextAfterRender = await renderPageAlreadyRouted(pageContext);
  assert(pageContext === pageContextAfterRender);
  return pageContextAfterRender;
}
async function getPageContextErrorPageInit(pageContextInit, errNominalPage, pageContextNominalPagePartial, renderContext, httpRequestId) {
  const pageContextInitEnhanced = getPageContextInitEnhancedSSR(pageContextInit, renderContext, null, httpRequestId);
  assert(errNominalPage);
  const pageContext = {};
  objectAssign(pageContext, pageContextInitEnhanced);
  objectAssign(pageContext, {
    is404: false,
    errorWhileRendering: errNominalPage,
    routeParams: {}
  });
  objectAssign(pageContext, {
    _debugRouteMatches: pageContextNominalPagePartial._debugRouteMatches || "ROUTING_ERROR"
  });
  assert(pageContext.errorWhileRendering);
  return pageContext;
}
function getPageContextInitEnhancedSSR(pageContextInit, renderContext, urlRewrite, httpRequestId) {
  const { isClientSideNavigation, _urlHandler } = handlePageContextUrl(pageContextInit.urlOriginal);
  const pageContextInitEnhanced = getPageContextInitEnhanced(pageContextInit, renderContext, {
    ssr: {
      urlRewrite,
      urlHandler: _urlHandler,
      isClientSideNavigation
    }
  });
  objectAssign(pageContextInitEnhanced, { _httpRequestId: httpRequestId });
  return pageContextInitEnhanced;
}
function handlePageContextUrl(urlOriginal) {
  const { isPageContextRequest } = handlePageContextRequestUrl(urlOriginal);
  return {
    isClientSideNavigation: isPageContextRequest,
    _urlHandler: (url) => handlePageContextRequestUrl(url).urlWithoutPageContextRequestSuffix
  };
}
function getRequestId() {
  const httpRequestId = ++globalObject11.httpRequestsCount;
  assert(httpRequestId >= 1);
  return httpRequestId;
}
function assertIsUrl(urlOriginal) {
  assertUsage2(isUrl(urlOriginal), `${import_picocolors42.default.code("renderPage(pageContextInit)")} (https://vike.dev/renderPage) called with ${import_picocolors42.default.code(`pageContextInit.urlOriginal===${JSON.stringify(urlOriginal)}`)} which isn't a valid URL.`);
}
function assertIsNotViteRequest(urlPathname, urlOriginal) {
  const isViteRequest = urlPathname.startsWith("/@vite/client") || urlPathname.startsWith("/@fs/") || urlPathname.startsWith("/__vite_ping");
  assertUsage2(!isViteRequest, `${import_picocolors42.default.code("renderPage(pageContextInit)")} (https://vike.dev/renderPage) called with ${import_picocolors42.default.code(`pageContextInit.urlOriginal===${JSON.stringify(urlOriginal)}`)} which is unexpected because this URL should have already been handled by Vite's development middleware. Make sure to 1. install Vite's development middleware and 2. add Vite's middleware *before* Vike's middleware, see https://vike.dev/renderPage`);
}
function normalizeUrl(pageContextInit, httpRequestId) {
  const { trailingSlash, disableUrlNormalization, baseServer: baseServer2 } = getGlobalContext();
  if (disableUrlNormalization)
    return null;
  const { urlOriginal } = pageContextInit;
  const { isPageContextRequest } = handlePageContextRequestUrl(urlOriginal);
  if (isPageContextRequest)
    return null;
  const urlNormalized = normalizeUrlPathname(urlOriginal, trailingSlash, baseServer2);
  if (!urlNormalized)
    return null;
  logRuntimeInfo?.(`URL normalized from ${import_picocolors42.default.cyan(urlOriginal)} to ${import_picocolors42.default.cyan(urlNormalized)} (https://vike.dev/url-normalization)`, httpRequestId, "info");
  const httpResponse = createHttpResponseRedirect({ url: urlNormalized, statusCode: 301 }, pageContextInit.urlOriginal);
  const pageContextHttpResponse = createPageContext(pageContextInit);
  objectAssign(pageContextHttpResponse, { httpResponse });
  return pageContextHttpResponse;
}
function getPermanentRedirect(pageContextInit, httpRequestId) {
  const { redirects, baseServer: baseServer2 } = getGlobalContext();
  const urlWithoutBase = removeBaseServer2(pageContextInit.urlOriginal, baseServer2);
  let origin = null;
  let urlTargetExternal = null;
  let urlTarget = modifyUrlPathname(urlWithoutBase, (urlPathname) => {
    const urlTarget2 = resolveRedirects(redirects, urlPathname);
    if (urlTarget2 === null)
      return null;
    if (!isUrl(urlTarget2)) {
      assert(isUri(urlTarget2));
      urlTargetExternal = urlTarget2;
      return null;
    }
    const { urlModified, origin: origin_ } = removeUrlOrigin(urlTarget2);
    origin = origin_;
    return urlModified;
  });
  if (urlTargetExternal) {
    urlTarget = urlTargetExternal;
  } else {
    let originChanged = false;
    if (origin) {
      const urlModified = setUrlOrigin(urlTarget, origin);
      if (urlModified !== false) {
        originChanged = true;
        urlTarget = urlModified;
      }
    }
    if (normalize2(urlTarget) === normalize2(urlWithoutBase))
      return null;
    if (!originChanged)
      urlTarget = prependBase(urlTarget, baseServer2);
    assert(urlTarget !== pageContextInit.urlOriginal);
  }
  logRuntimeInfo?.(`Permanent redirection defined by config.redirects (https://vike.dev/redirects)`, httpRequestId, "info");
  const httpResponse = createHttpResponseRedirect({ url: urlTarget, statusCode: 301 }, urlWithoutBase);
  const pageContextHttpResponse = createPageContext(pageContextInit);
  objectAssign(pageContextHttpResponse, { httpResponse });
  return pageContextHttpResponse;
}
function normalize2(url) {
  return url || "/";
}
async function handleAbortError(errAbort, pageContextsFromRewrite, pageContextInit, pageContextNominalPageInit, httpRequestId, renderContext, pageContextErrorPageInit) {
  logAbortErrorHandled(errAbort, getGlobalContext().isProduction, pageContextNominalPageInit);
  const pageContextAbort = errAbort._pageContextAbort;
  let pageContextSerialized;
  if (pageContextNominalPageInit.isClientSideNavigation) {
    if (pageContextAbort.abortStatusCode) {
      const errorPageId = getErrorPageId(renderContext.pageFilesAll, renderContext.pageConfigs);
      const abortCall = pageContextAbort._abortCall;
      assert(abortCall);
      assertUsage2(errorPageId, `You called ${import_picocolors42.default.cyan(abortCall)} but you didn't define an error page, make sure to define one https://vike.dev/error-page`);
      const pageContext = createPageContext({});
      objectAssign(pageContext, { pageId: errorPageId });
      objectAssign(pageContext, pageContextAbort);
      objectAssign(pageContext, pageContextErrorPageInit);
      objectAssign(pageContext, renderContext);
      objectAssign(pageContext, await loadUserFilesServerSide(pageContext));
      pageContextSerialized = serializePageContextClientSide(pageContext);
    } else {
      pageContextSerialized = serializePageContextAbort(pageContextAbort);
    }
    const httpResponse = await createHttpResponsePageContextJson(pageContextSerialized);
    const pageContextReturn = { httpResponse };
    return { pageContextReturn };
  }
  if (pageContextAbort._urlRewrite) {
    const pageContextReturn = await renderPageAlreadyPrepared(pageContextInit, httpRequestId, renderContext, [
      ...pageContextsFromRewrite,
      pageContextAbort
    ]);
    Object.assign(pageContextReturn, pageContextAbort);
    return { pageContextReturn };
  }
  if (pageContextAbort._urlRedirect) {
    const pageContextReturn = createPageContext(pageContextInit);
    objectAssign(pageContextReturn, pageContextAbort);
    const httpResponse = createHttpResponseRedirect(pageContextAbort._urlRedirect, (() => {
      const { pathname, searchOriginal } = pageContextNominalPageInit.urlParsed;
      const urlLogical = createUrlFromComponents(
        null,
        pathname,
        searchOriginal,
        // The server-side doesn't have access to the hash
        null
      );
      return urlLogical;
    })());
    objectAssign(pageContextReturn, { httpResponse });
    return { pageContextReturn };
  }
  assert(pageContextAbort.abortStatusCode);
  return { pageContextAbort };
}
function assertBaseUrl(pageContextInit) {
  const { baseServer: baseServer2 } = getGlobalContext();
  const { urlOriginal } = pageContextInit;
  const { urlWithoutPageContextRequestSuffix } = handlePageContextRequestUrl(urlOriginal);
  const { hasBaseServer } = parseUrl(urlWithoutPageContextRequestSuffix, baseServer2);
  assertUsage2(hasBaseServer, `${import_picocolors42.default.code("renderPage(pageContextInit)")} (https://vike.dev/renderPage) called with ${import_picocolors42.default.code(`pageContextInit.urlOriginal===${JSON.stringify(urlOriginal)}`)} which doesn't start with Base URL ${import_picocolors42.default.code(baseServer2)} (https://vike.dev/base-url)`);
}
var import_picocolors42, globalObject11, renderPage_wrapper;
var init_renderPage = __esm({
  "node_modules/vike/dist/esm/node/runtime/renderPage.js"() {
    init_renderPageAlreadyRouted();
    init_route();
    init_utils3();
    init_abort();
    init_globalContext();
    init_handlePageContextRequestUrl();
    init_createHttpResponse();
    init_loggerRuntime();
    init_isNewError();
    init_assertArguments();
    init_log404();
    init_isConfigInvalid();
    import_picocolors42 = __toESM(require_picocolors(), 1);
    init_serializePageContextClientSide();
    init_error_page();
    init_handleErrorWithoutErrorPage();
    init_loadUserFilesServerSide();
    init_resolveRedirects();
    globalObject11 = getGlobalObject("runtime/renderPage.ts", {
      httpRequestsCount: 0
    });
    renderPage_wrapper = async (_httpRequestId, ret) => ({
      pageContextReturn: await ret()
    });
  }
});

// node_modules/vike/dist/esm/node/runtime/html/injectAssets/injectAssets__public.js
var init_injectAssets_public = __esm({
  "node_modules/vike/dist/esm/node/runtime/html/injectAssets/injectAssets__public.js"() {
    init_utils3();
    init_injectAssets();
  }
});

// node_modules/vike/dist/esm/node/createPageRenderer.js
var init_createPageRenderer = __esm({
  "node_modules/vike/dist/esm/node/createPageRenderer.js"() {
    init_renderPage();
    init_utils3();
  }
});

// node_modules/vike/dist/esm/node/runtime/page-files/setup.js
var init_setup = __esm({
  "node_modules/vike/dist/esm/node/runtime/page-files/setup.js"() {
    init_getPageFiles();
    init_getPageFilesExports();
    setPageFilesAsync(getPageFilesExports);
  }
});

// node_modules/vike/dist/esm/node/runtime/index-common.js
function addEcosystemStamp2() {
  const g7 = globalThis;
  g7._isVikeApp = /* Don't set to true so that consumers do `!!globalThis._isVikeApp` instead of `globalThis._isVikeApp === true`.
  true
  */
  // We use an object so that we can eventually, in the future, add helpful information as needed. (E.g. the Vike version, or global settings.)
  {};
  g7._isVitePluginSsr = true;
}
var init_index_common = __esm({
  "node_modules/vike/dist/esm/node/runtime/index-common.js"() {
    init_renderPage();
    init_renderHtml();
    init_stream();
    init_utils3();
    init_globalContext();
    init_injectAssets_public();
    init_createPageRenderer();
    init_setup();
    addEcosystemStamp2();
  }
});

// node_modules/vike/dist/esm/node/runtime/index.js
var init_runtime = __esm({
  "node_modules/vike/dist/esm/node/runtime/index.js"() {
    init_isBrowser();
    init_assert();
    init_index_common();
    assertUsage2(!isBrowser(), "It's forbidden to `import { something } from 'vike/server'` on the client-side: the module 'vike/server' is a server-only module.", { showStackTrace: true });
  }
});

// node_modules/es-errors/index.js
var require_es_errors = __commonJS({
  "node_modules/es-errors/index.js"(exports, module) {
    "use strict";
    module.exports = Error;
  }
});

// node_modules/es-errors/eval.js
var require_eval = __commonJS({
  "node_modules/es-errors/eval.js"(exports, module) {
    "use strict";
    module.exports = EvalError;
  }
});

// node_modules/es-errors/range.js
var require_range = __commonJS({
  "node_modules/es-errors/range.js"(exports, module) {
    "use strict";
    module.exports = RangeError;
  }
});

// node_modules/es-errors/ref.js
var require_ref = __commonJS({
  "node_modules/es-errors/ref.js"(exports, module) {
    "use strict";
    module.exports = ReferenceError;
  }
});

// node_modules/es-errors/syntax.js
var require_syntax = __commonJS({
  "node_modules/es-errors/syntax.js"(exports, module) {
    "use strict";
    module.exports = SyntaxError;
  }
});

// node_modules/es-errors/type.js
var require_type = __commonJS({
  "node_modules/es-errors/type.js"(exports, module) {
    "use strict";
    module.exports = TypeError;
  }
});

// node_modules/es-errors/uri.js
var require_uri = __commonJS({
  "node_modules/es-errors/uri.js"(exports, module) {
    "use strict";
    module.exports = URIError;
  }
});

// node_modules/gopd/gOPD.js
var require_gOPD = __commonJS({
  "node_modules/gopd/gOPD.js"(exports, module) {
    "use strict";
    module.exports = Object.getOwnPropertyDescriptor;
  }
});

// node_modules/gopd/index.js
var require_gopd = __commonJS({
  "node_modules/gopd/index.js"(exports, module) {
    "use strict";
    var $gOPD = require_gOPD();
    if ($gOPD) {
      try {
        $gOPD([], "length");
      } catch (e14) {
        $gOPD = null;
      }
    }
    module.exports = $gOPD;
  }
});

// node_modules/es-define-property/index.js
var require_es_define_property = __commonJS({
  "node_modules/es-define-property/index.js"(exports, module) {
    "use strict";
    var $defineProperty = Object.defineProperty || false;
    if ($defineProperty) {
      try {
        $defineProperty({}, "a", { value: 1 });
      } catch (e14) {
        $defineProperty = false;
      }
    }
    module.exports = $defineProperty;
  }
});

// node_modules/has-symbols/shams.js
var require_shams = __commonJS({
  "node_modules/has-symbols/shams.js"(exports, module) {
    "use strict";
    module.exports = function hasSymbols() {
      if (typeof Symbol !== "function" || typeof Object.getOwnPropertySymbols !== "function") {
        return false;
      }
      if (typeof Symbol.iterator === "symbol") {
        return true;
      }
      var obj = {};
      var sym = Symbol("test");
      var symObj = Object(sym);
      if (typeof sym === "string") {
        return false;
      }
      if (Object.prototype.toString.call(sym) !== "[object Symbol]") {
        return false;
      }
      if (Object.prototype.toString.call(symObj) !== "[object Symbol]") {
        return false;
      }
      var symVal = 42;
      obj[sym] = symVal;
      for (var _2 in obj) {
        return false;
      }
      if (typeof Object.keys === "function" && Object.keys(obj).length !== 0) {
        return false;
      }
      if (typeof Object.getOwnPropertyNames === "function" && Object.getOwnPropertyNames(obj).length !== 0) {
        return false;
      }
      var syms = Object.getOwnPropertySymbols(obj);
      if (syms.length !== 1 || syms[0] !== sym) {
        return false;
      }
      if (!Object.prototype.propertyIsEnumerable.call(obj, sym)) {
        return false;
      }
      if (typeof Object.getOwnPropertyDescriptor === "function") {
        var descriptor = (
          /** @type {PropertyDescriptor} */
          Object.getOwnPropertyDescriptor(obj, sym)
        );
        if (descriptor.value !== symVal || descriptor.enumerable !== true) {
          return false;
        }
      }
      return true;
    };
  }
});

// node_modules/has-symbols/index.js
var require_has_symbols = __commonJS({
  "node_modules/has-symbols/index.js"(exports, module) {
    "use strict";
    var origSymbol = typeof Symbol !== "undefined" && Symbol;
    var hasSymbolSham = require_shams();
    module.exports = function hasNativeSymbols() {
      if (typeof origSymbol !== "function") {
        return false;
      }
      if (typeof Symbol !== "function") {
        return false;
      }
      if (typeof origSymbol("foo") !== "symbol") {
        return false;
      }
      if (typeof Symbol("bar") !== "symbol") {
        return false;
      }
      return hasSymbolSham();
    };
  }
});

// node_modules/function-bind/implementation.js
var require_implementation = __commonJS({
  "node_modules/function-bind/implementation.js"(exports, module) {
    "use strict";
    var ERROR_MESSAGE = "Function.prototype.bind called on incompatible ";
    var toStr = Object.prototype.toString;
    var max2 = Math.max;
    var funcType = "[object Function]";
    var concatty = function concatty2(a18, b4) {
      var arr = [];
      for (var i13 = 0; i13 < a18.length; i13 += 1) {
        arr[i13] = a18[i13];
      }
      for (var j3 = 0; j3 < b4.length; j3 += 1) {
        arr[j3 + a18.length] = b4[j3];
      }
      return arr;
    };
    var slicy = function slicy2(arrLike, offset4) {
      var arr = [];
      for (var i13 = offset4 || 0, j3 = 0; i13 < arrLike.length; i13 += 1, j3 += 1) {
        arr[j3] = arrLike[i13];
      }
      return arr;
    };
    var joiny = function(arr, joiner) {
      var str = "";
      for (var i13 = 0; i13 < arr.length; i13 += 1) {
        str += arr[i13];
        if (i13 + 1 < arr.length) {
          str += joiner;
        }
      }
      return str;
    };
    module.exports = function bind(that) {
      var target = this;
      if (typeof target !== "function" || toStr.apply(target) !== funcType) {
        throw new TypeError(ERROR_MESSAGE + target);
      }
      var args = slicy(arguments, 1);
      var bound;
      var binder = function() {
        if (this instanceof bound) {
          var result = target.apply(
            this,
            concatty(args, arguments)
          );
          if (Object(result) === result) {
            return result;
          }
          return this;
        }
        return target.apply(
          that,
          concatty(args, arguments)
        );
      };
      var boundLength = max2(0, target.length - args.length);
      var boundArgs = [];
      for (var i13 = 0; i13 < boundLength; i13++) {
        boundArgs[i13] = "$" + i13;
      }
      bound = Function("binder", "return function (" + joiny(boundArgs, ",") + "){ return binder.apply(this,arguments); }")(binder);
      if (target.prototype) {
        var Empty = function Empty2() {
        };
        Empty.prototype = target.prototype;
        bound.prototype = new Empty();
        Empty.prototype = null;
      }
      return bound;
    };
  }
});

// node_modules/function-bind/index.js
var require_function_bind = __commonJS({
  "node_modules/function-bind/index.js"(exports, module) {
    "use strict";
    var implementation = require_implementation();
    module.exports = Function.prototype.bind || implementation;
  }
});

// node_modules/call-bind-apply-helpers/functionCall.js
var require_functionCall = __commonJS({
  "node_modules/call-bind-apply-helpers/functionCall.js"(exports, module) {
    "use strict";
    module.exports = Function.prototype.call;
  }
});

// node_modules/call-bind-apply-helpers/functionApply.js
var require_functionApply = __commonJS({
  "node_modules/call-bind-apply-helpers/functionApply.js"(exports, module) {
    "use strict";
    module.exports = Function.prototype.apply;
  }
});

// node_modules/call-bind-apply-helpers/reflectApply.js
var require_reflectApply = __commonJS({
  "node_modules/call-bind-apply-helpers/reflectApply.js"(exports, module) {
    "use strict";
    module.exports = typeof Reflect === "function" && Reflect.apply;
  }
});

// node_modules/call-bind-apply-helpers/actualApply.js
var require_actualApply = __commonJS({
  "node_modules/call-bind-apply-helpers/actualApply.js"(exports, module) {
    "use strict";
    var bind = require_function_bind();
    var $apply = require_functionApply();
    var $call = require_functionCall();
    var $reflectApply = require_reflectApply();
    module.exports = $reflectApply || bind.call($call, $apply);
  }
});

// node_modules/call-bind-apply-helpers/index.js
var require_call_bind_apply_helpers = __commonJS({
  "node_modules/call-bind-apply-helpers/index.js"(exports, module) {
    "use strict";
    var bind = require_function_bind();
    var $TypeError = require_type();
    var $call = require_functionCall();
    var $actualApply = require_actualApply();
    module.exports = function callBindBasic(args) {
      if (args.length < 1 || typeof args[0] !== "function") {
        throw new $TypeError("a function is required");
      }
      return $actualApply(bind, $call, args);
    };
  }
});

// node_modules/dunder-proto/get.js
var require_get = __commonJS({
  "node_modules/dunder-proto/get.js"(exports, module) {
    "use strict";
    var callBind = require_call_bind_apply_helpers();
    var gOPD = require_gopd();
    var hasProtoAccessor = (
      /** @type {{ __proto__?: typeof Array.prototype }} */
      [].__proto__ === Array.prototype
    );
    var desc = hasProtoAccessor && gOPD && gOPD(
      Object.prototype,
      /** @type {keyof typeof Object.prototype} */
      "__proto__"
    );
    var $Object = Object;
    var $getPrototypeOf = $Object.getPrototypeOf;
    module.exports = desc && typeof desc.get === "function" ? callBind([desc.get]) : typeof $getPrototypeOf === "function" ? (
      /** @type {import('./get')} */
      function getDunder(value) {
        return $getPrototypeOf(value == null ? value : $Object(value));
      }
    ) : false;
  }
});

// node_modules/hasown/index.js
var require_hasown = __commonJS({
  "node_modules/hasown/index.js"(exports, module) {
    "use strict";
    var call = Function.prototype.call;
    var $hasOwn = Object.prototype.hasOwnProperty;
    var bind = require_function_bind();
    module.exports = bind.call(call, $hasOwn);
  }
});

// node_modules/get-intrinsic/index.js
var require_get_intrinsic = __commonJS({
  "node_modules/get-intrinsic/index.js"(exports, module) {
    "use strict";
    var undefined2;
    var $Error = require_es_errors();
    var $EvalError = require_eval();
    var $RangeError = require_range();
    var $ReferenceError = require_ref();
    var $SyntaxError = require_syntax();
    var $TypeError = require_type();
    var $URIError = require_uri();
    var $Function = Function;
    var getEvalledConstructor = function(expressionSyntax) {
      try {
        return $Function('"use strict"; return (' + expressionSyntax + ").constructor;")();
      } catch (e14) {
      }
    };
    var $gOPD = require_gopd();
    var $defineProperty = require_es_define_property();
    var throwTypeError = function() {
      throw new $TypeError();
    };
    var ThrowTypeError = $gOPD ? function() {
      try {
        arguments.callee;
        return throwTypeError;
      } catch (calleeThrows) {
        try {
          return $gOPD(arguments, "callee").get;
        } catch (gOPDthrows) {
          return throwTypeError;
        }
      }
    }() : throwTypeError;
    var hasSymbols = require_has_symbols()();
    var getDunderProto = require_get();
    var getProto = typeof Reflect === "function" && Reflect.getPrototypeOf || Object.getPrototypeOf || getDunderProto;
    var $apply = require_functionApply();
    var $call = require_functionCall();
    var needsEval = {};
    var TypedArray = typeof Uint8Array === "undefined" || !getProto ? undefined2 : getProto(Uint8Array);
    var INTRINSICS = {
      __proto__: null,
      "%AggregateError%": typeof AggregateError === "undefined" ? undefined2 : AggregateError,
      "%Array%": Array,
      "%ArrayBuffer%": typeof ArrayBuffer === "undefined" ? undefined2 : ArrayBuffer,
      "%ArrayIteratorPrototype%": hasSymbols && getProto ? getProto([][Symbol.iterator]()) : undefined2,
      "%AsyncFromSyncIteratorPrototype%": undefined2,
      "%AsyncFunction%": needsEval,
      "%AsyncGenerator%": needsEval,
      "%AsyncGeneratorFunction%": needsEval,
      "%AsyncIteratorPrototype%": needsEval,
      "%Atomics%": typeof Atomics === "undefined" ? undefined2 : Atomics,
      "%BigInt%": typeof BigInt === "undefined" ? undefined2 : BigInt,
      "%BigInt64Array%": typeof BigInt64Array === "undefined" ? undefined2 : BigInt64Array,
      "%BigUint64Array%": typeof BigUint64Array === "undefined" ? undefined2 : BigUint64Array,
      "%Boolean%": Boolean,
      "%DataView%": typeof DataView === "undefined" ? undefined2 : DataView,
      "%Date%": Date,
      "%decodeURI%": decodeURI,
      "%decodeURIComponent%": decodeURIComponent,
      "%encodeURI%": encodeURI,
      "%encodeURIComponent%": encodeURIComponent,
      "%Error%": $Error,
      "%eval%": eval,
      // eslint-disable-line no-eval
      "%EvalError%": $EvalError,
      "%Float32Array%": typeof Float32Array === "undefined" ? undefined2 : Float32Array,
      "%Float64Array%": typeof Float64Array === "undefined" ? undefined2 : Float64Array,
      "%FinalizationRegistry%": typeof FinalizationRegistry === "undefined" ? undefined2 : FinalizationRegistry,
      "%Function%": $Function,
      "%GeneratorFunction%": needsEval,
      "%Int8Array%": typeof Int8Array === "undefined" ? undefined2 : Int8Array,
      "%Int16Array%": typeof Int16Array === "undefined" ? undefined2 : Int16Array,
      "%Int32Array%": typeof Int32Array === "undefined" ? undefined2 : Int32Array,
      "%isFinite%": isFinite,
      "%isNaN%": isNaN,
      "%IteratorPrototype%": hasSymbols && getProto ? getProto(getProto([][Symbol.iterator]())) : undefined2,
      "%JSON%": typeof JSON === "object" ? JSON : undefined2,
      "%Map%": typeof Map === "undefined" ? undefined2 : Map,
      "%MapIteratorPrototype%": typeof Map === "undefined" || !hasSymbols || !getProto ? undefined2 : getProto((/* @__PURE__ */ new Map())[Symbol.iterator]()),
      "%Math%": Math,
      "%Number%": Number,
      "%Object%": Object,
      "%Object.getOwnPropertyDescriptor%": $gOPD,
      "%parseFloat%": parseFloat,
      "%parseInt%": parseInt,
      "%Promise%": typeof Promise === "undefined" ? undefined2 : Promise,
      "%Proxy%": typeof Proxy === "undefined" ? undefined2 : Proxy,
      "%RangeError%": $RangeError,
      "%ReferenceError%": $ReferenceError,
      "%Reflect%": typeof Reflect === "undefined" ? undefined2 : Reflect,
      "%RegExp%": RegExp,
      "%Set%": typeof Set === "undefined" ? undefined2 : Set,
      "%SetIteratorPrototype%": typeof Set === "undefined" || !hasSymbols || !getProto ? undefined2 : getProto((/* @__PURE__ */ new Set())[Symbol.iterator]()),
      "%SharedArrayBuffer%": typeof SharedArrayBuffer === "undefined" ? undefined2 : SharedArrayBuffer,
      "%String%": String,
      "%StringIteratorPrototype%": hasSymbols && getProto ? getProto(""[Symbol.iterator]()) : undefined2,
      "%Symbol%": hasSymbols ? Symbol : undefined2,
      "%SyntaxError%": $SyntaxError,
      "%ThrowTypeError%": ThrowTypeError,
      "%TypedArray%": TypedArray,
      "%TypeError%": $TypeError,
      "%Uint8Array%": typeof Uint8Array === "undefined" ? undefined2 : Uint8Array,
      "%Uint8ClampedArray%": typeof Uint8ClampedArray === "undefined" ? undefined2 : Uint8ClampedArray,
      "%Uint16Array%": typeof Uint16Array === "undefined" ? undefined2 : Uint16Array,
      "%Uint32Array%": typeof Uint32Array === "undefined" ? undefined2 : Uint32Array,
      "%URIError%": $URIError,
      "%WeakMap%": typeof WeakMap === "undefined" ? undefined2 : WeakMap,
      "%WeakRef%": typeof WeakRef === "undefined" ? undefined2 : WeakRef,
      "%WeakSet%": typeof WeakSet === "undefined" ? undefined2 : WeakSet,
      "%Function.prototype.call%": $call,
      "%Function.prototype.apply%": $apply,
      "%Object.defineProperty%": $defineProperty
    };
    if (getProto) {
      try {
        null.error;
      } catch (e14) {
        errorProto = getProto(getProto(e14));
        INTRINSICS["%Error.prototype%"] = errorProto;
      }
    }
    var errorProto;
    var doEval = function doEval2(name2) {
      var value;
      if (name2 === "%AsyncFunction%") {
        value = getEvalledConstructor("async function () {}");
      } else if (name2 === "%GeneratorFunction%") {
        value = getEvalledConstructor("function* () {}");
      } else if (name2 === "%AsyncGeneratorFunction%") {
        value = getEvalledConstructor("async function* () {}");
      } else if (name2 === "%AsyncGenerator%") {
        var fn = doEval2("%AsyncGeneratorFunction%");
        if (fn) {
          value = fn.prototype;
        }
      } else if (name2 === "%AsyncIteratorPrototype%") {
        var gen = doEval2("%AsyncGenerator%");
        if (gen && getProto) {
          value = getProto(gen.prototype);
        }
      }
      INTRINSICS[name2] = value;
      return value;
    };
    var LEGACY_ALIASES = {
      __proto__: null,
      "%ArrayBufferPrototype%": ["ArrayBuffer", "prototype"],
      "%ArrayPrototype%": ["Array", "prototype"],
      "%ArrayProto_entries%": ["Array", "prototype", "entries"],
      "%ArrayProto_forEach%": ["Array", "prototype", "forEach"],
      "%ArrayProto_keys%": ["Array", "prototype", "keys"],
      "%ArrayProto_values%": ["Array", "prototype", "values"],
      "%AsyncFunctionPrototype%": ["AsyncFunction", "prototype"],
      "%AsyncGenerator%": ["AsyncGeneratorFunction", "prototype"],
      "%AsyncGeneratorPrototype%": ["AsyncGeneratorFunction", "prototype", "prototype"],
      "%BooleanPrototype%": ["Boolean", "prototype"],
      "%DataViewPrototype%": ["DataView", "prototype"],
      "%DatePrototype%": ["Date", "prototype"],
      "%ErrorPrototype%": ["Error", "prototype"],
      "%EvalErrorPrototype%": ["EvalError", "prototype"],
      "%Float32ArrayPrototype%": ["Float32Array", "prototype"],
      "%Float64ArrayPrototype%": ["Float64Array", "prototype"],
      "%FunctionPrototype%": ["Function", "prototype"],
      "%Generator%": ["GeneratorFunction", "prototype"],
      "%GeneratorPrototype%": ["GeneratorFunction", "prototype", "prototype"],
      "%Int8ArrayPrototype%": ["Int8Array", "prototype"],
      "%Int16ArrayPrototype%": ["Int16Array", "prototype"],
      "%Int32ArrayPrototype%": ["Int32Array", "prototype"],
      "%JSONParse%": ["JSON", "parse"],
      "%JSONStringify%": ["JSON", "stringify"],
      "%MapPrototype%": ["Map", "prototype"],
      "%NumberPrototype%": ["Number", "prototype"],
      "%ObjectPrototype%": ["Object", "prototype"],
      "%ObjProto_toString%": ["Object", "prototype", "toString"],
      "%ObjProto_valueOf%": ["Object", "prototype", "valueOf"],
      "%PromisePrototype%": ["Promise", "prototype"],
      "%PromiseProto_then%": ["Promise", "prototype", "then"],
      "%Promise_all%": ["Promise", "all"],
      "%Promise_reject%": ["Promise", "reject"],
      "%Promise_resolve%": ["Promise", "resolve"],
      "%RangeErrorPrototype%": ["RangeError", "prototype"],
      "%ReferenceErrorPrototype%": ["ReferenceError", "prototype"],
      "%RegExpPrototype%": ["RegExp", "prototype"],
      "%SetPrototype%": ["Set", "prototype"],
      "%SharedArrayBufferPrototype%": ["SharedArrayBuffer", "prototype"],
      "%StringPrototype%": ["String", "prototype"],
      "%SymbolPrototype%": ["Symbol", "prototype"],
      "%SyntaxErrorPrototype%": ["SyntaxError", "prototype"],
      "%TypedArrayPrototype%": ["TypedArray", "prototype"],
      "%TypeErrorPrototype%": ["TypeError", "prototype"],
      "%Uint8ArrayPrototype%": ["Uint8Array", "prototype"],
      "%Uint8ClampedArrayPrototype%": ["Uint8ClampedArray", "prototype"],
      "%Uint16ArrayPrototype%": ["Uint16Array", "prototype"],
      "%Uint32ArrayPrototype%": ["Uint32Array", "prototype"],
      "%URIErrorPrototype%": ["URIError", "prototype"],
      "%WeakMapPrototype%": ["WeakMap", "prototype"],
      "%WeakSetPrototype%": ["WeakSet", "prototype"]
    };
    var bind = require_function_bind();
    var hasOwn = require_hasown();
    var $concat = bind.call($call, Array.prototype.concat);
    var $spliceApply = bind.call($apply, Array.prototype.splice);
    var $replace = bind.call($call, String.prototype.replace);
    var $strSlice = bind.call($call, String.prototype.slice);
    var $exec = bind.call($call, RegExp.prototype.exec);
    var rePropName = /[^%.[\]]+|\[(?:(-?\d+(?:\.\d+)?)|(["'])((?:(?!\2)[^\\]|\\.)*?)\2)\]|(?=(?:\.|\[\])(?:\.|\[\]|%$))/g;
    var reEscapeChar = /\\(\\)?/g;
    var stringToPath = function stringToPath2(string) {
      var first = $strSlice(string, 0, 1);
      var last = $strSlice(string, -1);
      if (first === "%" && last !== "%") {
        throw new $SyntaxError("invalid intrinsic syntax, expected closing `%`");
      } else if (last === "%" && first !== "%") {
        throw new $SyntaxError("invalid intrinsic syntax, expected opening `%`");
      }
      var result = [];
      $replace(string, rePropName, function(match, number2, quote, subString) {
        result[result.length] = quote ? $replace(subString, reEscapeChar, "$1") : number2 || match;
      });
      return result;
    };
    var getBaseIntrinsic = function getBaseIntrinsic2(name2, allowMissing) {
      var intrinsicName = name2;
      var alias;
      if (hasOwn(LEGACY_ALIASES, intrinsicName)) {
        alias = LEGACY_ALIASES[intrinsicName];
        intrinsicName = "%" + alias[0] + "%";
      }
      if (hasOwn(INTRINSICS, intrinsicName)) {
        var value = INTRINSICS[intrinsicName];
        if (value === needsEval) {
          value = doEval(intrinsicName);
        }
        if (typeof value === "undefined" && !allowMissing) {
          throw new $TypeError("intrinsic " + name2 + " exists, but is not available. Please file an issue!");
        }
        return {
          alias,
          name: intrinsicName,
          value
        };
      }
      throw new $SyntaxError("intrinsic " + name2 + " does not exist!");
    };
    module.exports = function GetIntrinsic(name2, allowMissing) {
      if (typeof name2 !== "string" || name2.length === 0) {
        throw new $TypeError("intrinsic name must be a non-empty string");
      }
      if (arguments.length > 1 && typeof allowMissing !== "boolean") {
        throw new $TypeError('"allowMissing" argument must be a boolean');
      }
      if ($exec(/^%?[^%]*%?$/, name2) === null) {
        throw new $SyntaxError("`%` may not be present anywhere but at the beginning and end of the intrinsic name");
      }
      var parts = stringToPath(name2);
      var intrinsicBaseName = parts.length > 0 ? parts[0] : "";
      var intrinsic = getBaseIntrinsic("%" + intrinsicBaseName + "%", allowMissing);
      var intrinsicRealName = intrinsic.name;
      var value = intrinsic.value;
      var skipFurtherCaching = false;
      var alias = intrinsic.alias;
      if (alias) {
        intrinsicBaseName = alias[0];
        $spliceApply(parts, $concat([0, 1], alias));
      }
      for (var i13 = 1, isOwn = true; i13 < parts.length; i13 += 1) {
        var part = parts[i13];
        var first = $strSlice(part, 0, 1);
        var last = $strSlice(part, -1);
        if ((first === '"' || first === "'" || first === "`" || (last === '"' || last === "'" || last === "`")) && first !== last) {
          throw new $SyntaxError("property names with quotes must have matching quotes");
        }
        if (part === "constructor" || !isOwn) {
          skipFurtherCaching = true;
        }
        intrinsicBaseName += "." + part;
        intrinsicRealName = "%" + intrinsicBaseName + "%";
        if (hasOwn(INTRINSICS, intrinsicRealName)) {
          value = INTRINSICS[intrinsicRealName];
        } else if (value != null) {
          if (!(part in value)) {
            if (!allowMissing) {
              throw new $TypeError("base intrinsic for " + name2 + " exists, but the property is not available.");
            }
            return void 0;
          }
          if ($gOPD && i13 + 1 >= parts.length) {
            var desc = $gOPD(value, part);
            isOwn = !!desc;
            if (isOwn && "get" in desc && !("originalValue" in desc.get)) {
              value = desc.get;
            } else {
              value = value[part];
            }
          } else {
            isOwn = hasOwn(value, part);
            value = value[part];
          }
          if (isOwn && !skipFurtherCaching) {
            INTRINSICS[intrinsicRealName] = value;
          }
        }
      }
      return value;
    };
  }
});

// node_modules/define-data-property/index.js
var require_define_data_property = __commonJS({
  "node_modules/define-data-property/index.js"(exports, module) {
    "use strict";
    var $defineProperty = require_es_define_property();
    var $SyntaxError = require_syntax();
    var $TypeError = require_type();
    var gopd = require_gopd();
    module.exports = function defineDataProperty(obj, property, value) {
      if (!obj || typeof obj !== "object" && typeof obj !== "function") {
        throw new $TypeError("`obj` must be an object or a function`");
      }
      if (typeof property !== "string" && typeof property !== "symbol") {
        throw new $TypeError("`property` must be a string or a symbol`");
      }
      if (arguments.length > 3 && typeof arguments[3] !== "boolean" && arguments[3] !== null) {
        throw new $TypeError("`nonEnumerable`, if provided, must be a boolean or null");
      }
      if (arguments.length > 4 && typeof arguments[4] !== "boolean" && arguments[4] !== null) {
        throw new $TypeError("`nonWritable`, if provided, must be a boolean or null");
      }
      if (arguments.length > 5 && typeof arguments[5] !== "boolean" && arguments[5] !== null) {
        throw new $TypeError("`nonConfigurable`, if provided, must be a boolean or null");
      }
      if (arguments.length > 6 && typeof arguments[6] !== "boolean") {
        throw new $TypeError("`loose`, if provided, must be a boolean");
      }
      var nonEnumerable = arguments.length > 3 ? arguments[3] : null;
      var nonWritable = arguments.length > 4 ? arguments[4] : null;
      var nonConfigurable = arguments.length > 5 ? arguments[5] : null;
      var loose = arguments.length > 6 ? arguments[6] : false;
      var desc = !!gopd && gopd(obj, property);
      if ($defineProperty) {
        $defineProperty(obj, property, {
          configurable: nonConfigurable === null && desc ? desc.configurable : !nonConfigurable,
          enumerable: nonEnumerable === null && desc ? desc.enumerable : !nonEnumerable,
          value,
          writable: nonWritable === null && desc ? desc.writable : !nonWritable
        });
      } else if (loose || !nonEnumerable && !nonWritable && !nonConfigurable) {
        obj[property] = value;
      } else {
        throw new $SyntaxError("This environment does not support defining a property as non-configurable, non-writable, or non-enumerable.");
      }
    };
  }
});

// node_modules/has-property-descriptors/index.js
var require_has_property_descriptors = __commonJS({
  "node_modules/has-property-descriptors/index.js"(exports, module) {
    "use strict";
    var $defineProperty = require_es_define_property();
    var hasPropertyDescriptors = function hasPropertyDescriptors2() {
      return !!$defineProperty;
    };
    hasPropertyDescriptors.hasArrayLengthDefineBug = function hasArrayLengthDefineBug() {
      if (!$defineProperty) {
        return null;
      }
      try {
        return $defineProperty([], "length", { value: 1 }).length !== 1;
      } catch (e14) {
        return true;
      }
    };
    module.exports = hasPropertyDescriptors;
  }
});

// node_modules/set-function-length/index.js
var require_set_function_length = __commonJS({
  "node_modules/set-function-length/index.js"(exports, module) {
    "use strict";
    var GetIntrinsic = require_get_intrinsic();
    var define = require_define_data_property();
    var hasDescriptors = require_has_property_descriptors()();
    var gOPD = require_gopd();
    var $TypeError = require_type();
    var $floor = GetIntrinsic("%Math.floor%");
    module.exports = function setFunctionLength(fn, length) {
      if (typeof fn !== "function") {
        throw new $TypeError("`fn` is not a function");
      }
      if (typeof length !== "number" || length < 0 || length > 4294967295 || $floor(length) !== length) {
        throw new $TypeError("`length` must be a positive 32-bit integer");
      }
      var loose = arguments.length > 2 && !!arguments[2];
      var functionLengthIsConfigurable = true;
      var functionLengthIsWritable = true;
      if ("length" in fn && gOPD) {
        var desc = gOPD(fn, "length");
        if (desc && !desc.configurable) {
          functionLengthIsConfigurable = false;
        }
        if (desc && !desc.writable) {
          functionLengthIsWritable = false;
        }
      }
      if (functionLengthIsConfigurable || functionLengthIsWritable || !loose) {
        if (hasDescriptors) {
          define(
            /** @type {Parameters<define>[0]} */
            fn,
            "length",
            length,
            true,
            true
          );
        } else {
          define(
            /** @type {Parameters<define>[0]} */
            fn,
            "length",
            length
          );
        }
      }
      return fn;
    };
  }
});

// node_modules/call-bind-apply-helpers/applyBind.js
var require_applyBind = __commonJS({
  "node_modules/call-bind-apply-helpers/applyBind.js"(exports, module) {
    "use strict";
    var bind = require_function_bind();
    var $apply = require_functionApply();
    var actualApply = require_actualApply();
    module.exports = function applyBind() {
      return actualApply(bind, $apply, arguments);
    };
  }
});

// node_modules/call-bind/index.js
var require_call_bind = __commonJS({
  "node_modules/call-bind/index.js"(exports, module) {
    "use strict";
    var setFunctionLength = require_set_function_length();
    var $defineProperty = require_es_define_property();
    var callBindBasic = require_call_bind_apply_helpers();
    var applyBind = require_applyBind();
    module.exports = function callBind(originalFunction) {
      var func = callBindBasic(arguments);
      var adjustedLength = originalFunction.length - (arguments.length - 1);
      return setFunctionLength(
        func,
        1 + (adjustedLength > 0 ? adjustedLength : 0),
        true
      );
    };
    if ($defineProperty) {
      $defineProperty(module.exports, "apply", { value: applyBind });
    } else {
      module.exports.apply = applyBind;
    }
  }
});

// node_modules/call-bind/callBound.js
var require_callBound = __commonJS({
  "node_modules/call-bind/callBound.js"(exports, module) {
    "use strict";
    var GetIntrinsic = require_get_intrinsic();
    var callBind = require_call_bind();
    var $indexOf = callBind(GetIntrinsic("String.prototype.indexOf"));
    module.exports = function callBoundIntrinsic(name2, allowMissing) {
      var intrinsic = GetIntrinsic(name2, !!allowMissing);
      if (typeof intrinsic === "function" && $indexOf(name2, ".prototype.") > -1) {
        return callBind(intrinsic);
      }
      return intrinsic;
    };
  }
});

// node_modules/object-inspect/util.inspect.js
var require_util_inspect = __commonJS({
  "node_modules/object-inspect/util.inspect.js"(exports, module) {
    module.exports = __require("util").inspect;
  }
});

// node_modules/object-inspect/index.js
var require_object_inspect = __commonJS({
  "node_modules/object-inspect/index.js"(exports, module) {
    var hasMap = typeof Map === "function" && Map.prototype;
    var mapSizeDescriptor = Object.getOwnPropertyDescriptor && hasMap ? Object.getOwnPropertyDescriptor(Map.prototype, "size") : null;
    var mapSize = hasMap && mapSizeDescriptor && typeof mapSizeDescriptor.get === "function" ? mapSizeDescriptor.get : null;
    var mapForEach = hasMap && Map.prototype.forEach;
    var hasSet = typeof Set === "function" && Set.prototype;
    var setSizeDescriptor = Object.getOwnPropertyDescriptor && hasSet ? Object.getOwnPropertyDescriptor(Set.prototype, "size") : null;
    var setSize = hasSet && setSizeDescriptor && typeof setSizeDescriptor.get === "function" ? setSizeDescriptor.get : null;
    var setForEach = hasSet && Set.prototype.forEach;
    var hasWeakMap = typeof WeakMap === "function" && WeakMap.prototype;
    var weakMapHas = hasWeakMap ? WeakMap.prototype.has : null;
    var hasWeakSet = typeof WeakSet === "function" && WeakSet.prototype;
    var weakSetHas = hasWeakSet ? WeakSet.prototype.has : null;
    var hasWeakRef = typeof WeakRef === "function" && WeakRef.prototype;
    var weakRefDeref = hasWeakRef ? WeakRef.prototype.deref : null;
    var booleanValueOf = Boolean.prototype.valueOf;
    var objectToString2 = Object.prototype.toString;
    var functionToString = Function.prototype.toString;
    var $match = String.prototype.match;
    var $slice = String.prototype.slice;
    var $replace = String.prototype.replace;
    var $toUpperCase = String.prototype.toUpperCase;
    var $toLowerCase = String.prototype.toLowerCase;
    var $test = RegExp.prototype.test;
    var $concat = Array.prototype.concat;
    var $join = Array.prototype.join;
    var $arrSlice = Array.prototype.slice;
    var $floor = Math.floor;
    var bigIntValueOf = typeof BigInt === "function" ? BigInt.prototype.valueOf : null;
    var gOPS = Object.getOwnPropertySymbols;
    var symToString = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? Symbol.prototype.toString : null;
    var hasShammedSymbols = typeof Symbol === "function" && typeof Symbol.iterator === "object";
    var toStringTag = typeof Symbol === "function" && Symbol.toStringTag && (typeof Symbol.toStringTag === hasShammedSymbols ? "object" : "symbol") ? Symbol.toStringTag : null;
    var isEnumerable = Object.prototype.propertyIsEnumerable;
    var gPO = (typeof Reflect === "function" ? Reflect.getPrototypeOf : Object.getPrototypeOf) || ([].__proto__ === Array.prototype ? function(O5) {
      return O5.__proto__;
    } : null);
    function addNumericSeparator(num, str) {
      if (num === Infinity || num === -Infinity || num !== num || num && num > -1e3 && num < 1e3 || $test.call(/e/, str)) {
        return str;
      }
      var sepRegex = /[0-9](?=(?:[0-9]{3})+(?![0-9]))/g;
      if (typeof num === "number") {
        var int2 = num < 0 ? -$floor(-num) : $floor(num);
        if (int2 !== num) {
          var intStr = String(int2);
          var dec = $slice.call(str, intStr.length + 1);
          return $replace.call(intStr, sepRegex, "$&_") + "." + $replace.call($replace.call(dec, /([0-9]{3})/g, "$&_"), /_$/, "");
        }
      }
      return $replace.call(str, sepRegex, "$&_");
    }
    var utilInspect = require_util_inspect();
    var inspectCustom = utilInspect.custom;
    var inspectSymbol = isSymbol(inspectCustom) ? inspectCustom : null;
    var quotes = {
      __proto__: null,
      "double": '"',
      single: "'"
    };
    var quoteREs = {
      __proto__: null,
      "double": /(["\\])/g,
      single: /(['\\])/g
    };
    module.exports = function inspect_(obj, options, depth, seen) {
      var opts = options || {};
      if (has(opts, "quoteStyle") && !has(quotes, opts.quoteStyle)) {
        throw new TypeError('option "quoteStyle" must be "single" or "double"');
      }
      if (has(opts, "maxStringLength") && (typeof opts.maxStringLength === "number" ? opts.maxStringLength < 0 && opts.maxStringLength !== Infinity : opts.maxStringLength !== null)) {
        throw new TypeError('option "maxStringLength", if provided, must be a positive integer, Infinity, or `null`');
      }
      var customInspect = has(opts, "customInspect") ? opts.customInspect : true;
      if (typeof customInspect !== "boolean" && customInspect !== "symbol") {
        throw new TypeError("option \"customInspect\", if provided, must be `true`, `false`, or `'symbol'`");
      }
      if (has(opts, "indent") && opts.indent !== null && opts.indent !== "	" && !(parseInt(opts.indent, 10) === opts.indent && opts.indent > 0)) {
        throw new TypeError('option "indent" must be "\\t", an integer > 0, or `null`');
      }
      if (has(opts, "numericSeparator") && typeof opts.numericSeparator !== "boolean") {
        throw new TypeError('option "numericSeparator", if provided, must be `true` or `false`');
      }
      var numericSeparator = opts.numericSeparator;
      if (typeof obj === "undefined") {
        return "undefined";
      }
      if (obj === null) {
        return "null";
      }
      if (typeof obj === "boolean") {
        return obj ? "true" : "false";
      }
      if (typeof obj === "string") {
        return inspectString(obj, opts);
      }
      if (typeof obj === "number") {
        if (obj === 0) {
          return Infinity / obj > 0 ? "0" : "-0";
        }
        var str = String(obj);
        return numericSeparator ? addNumericSeparator(obj, str) : str;
      }
      if (typeof obj === "bigint") {
        var bigIntStr = String(obj) + "n";
        return numericSeparator ? addNumericSeparator(obj, bigIntStr) : bigIntStr;
      }
      var maxDepth2 = typeof opts.depth === "undefined" ? 5 : opts.depth;
      if (typeof depth === "undefined") {
        depth = 0;
      }
      if (depth >= maxDepth2 && maxDepth2 > 0 && typeof obj === "object") {
        return isArray3(obj) ? "[Array]" : "[Object]";
      }
      var indent = getIndent(opts, depth);
      if (typeof seen === "undefined") {
        seen = [];
      } else if (indexOf(seen, obj) >= 0) {
        return "[Circular]";
      }
      function inspect(value, from, noIndent) {
        if (from) {
          seen = $arrSlice.call(seen);
          seen.push(from);
        }
        if (noIndent) {
          var newOpts = {
            depth: opts.depth
          };
          if (has(opts, "quoteStyle")) {
            newOpts.quoteStyle = opts.quoteStyle;
          }
          return inspect_(value, newOpts, depth + 1, seen);
        }
        return inspect_(value, opts, depth + 1, seen);
      }
      if (typeof obj === "function" && !isRegExp(obj)) {
        var name2 = nameOf(obj);
        var keys2 = arrObjKeys(obj, inspect);
        return "[Function" + (name2 ? ": " + name2 : " (anonymous)") + "]" + (keys2.length > 0 ? " { " + $join.call(keys2, ", ") + " }" : "");
      }
      if (isSymbol(obj)) {
        var symString = hasShammedSymbols ? $replace.call(String(obj), /^(Symbol\(.*\))_[^)]*$/, "$1") : symToString.call(obj);
        return typeof obj === "object" && !hasShammedSymbols ? markBoxed(symString) : symString;
      }
      if (isElement3(obj)) {
        var s36 = "<" + $toLowerCase.call(String(obj.nodeName));
        var attrs = obj.attributes || [];
        for (var i13 = 0; i13 < attrs.length; i13++) {
          s36 += " " + attrs[i13].name + "=" + wrapQuotes(quote(attrs[i13].value), "double", opts);
        }
        s36 += ">";
        if (obj.childNodes && obj.childNodes.length) {
          s36 += "...";
        }
        s36 += "</" + $toLowerCase.call(String(obj.nodeName)) + ">";
        return s36;
      }
      if (isArray3(obj)) {
        if (obj.length === 0) {
          return "[]";
        }
        var xs2 = arrObjKeys(obj, inspect);
        if (indent && !singleLineValues(xs2)) {
          return "[" + indentedJoin(xs2, indent) + "]";
        }
        return "[ " + $join.call(xs2, ", ") + " ]";
      }
      if (isError(obj)) {
        var parts = arrObjKeys(obj, inspect);
        if (!("cause" in Error.prototype) && "cause" in obj && !isEnumerable.call(obj, "cause")) {
          return "{ [" + String(obj) + "] " + $join.call($concat.call("[cause]: " + inspect(obj.cause), parts), ", ") + " }";
        }
        if (parts.length === 0) {
          return "[" + String(obj) + "]";
        }
        return "{ [" + String(obj) + "] " + $join.call(parts, ", ") + " }";
      }
      if (typeof obj === "object" && customInspect) {
        if (inspectSymbol && typeof obj[inspectSymbol] === "function" && utilInspect) {
          return utilInspect(obj, { depth: maxDepth2 - depth });
        } else if (customInspect !== "symbol" && typeof obj.inspect === "function") {
          return obj.inspect();
        }
      }
      if (isMap(obj)) {
        var mapParts = [];
        if (mapForEach) {
          mapForEach.call(obj, function(value, key) {
            mapParts.push(inspect(key, obj, true) + " => " + inspect(value, obj));
          });
        }
        return collectionOf("Map", mapSize.call(obj), mapParts, indent);
      }
      if (isSet(obj)) {
        var setParts = [];
        if (setForEach) {
          setForEach.call(obj, function(value) {
            setParts.push(inspect(value, obj));
          });
        }
        return collectionOf("Set", setSize.call(obj), setParts, indent);
      }
      if (isWeakMap(obj)) {
        return weakCollectionOf("WeakMap");
      }
      if (isWeakSet(obj)) {
        return weakCollectionOf("WeakSet");
      }
      if (isWeakRef(obj)) {
        return weakCollectionOf("WeakRef");
      }
      if (isNumber2(obj)) {
        return markBoxed(inspect(Number(obj)));
      }
      if (isBigInt(obj)) {
        return markBoxed(inspect(bigIntValueOf.call(obj)));
      }
      if (isBoolean(obj)) {
        return markBoxed(booleanValueOf.call(obj));
      }
      if (isString(obj)) {
        return markBoxed(inspect(String(obj)));
      }
      if (typeof window !== "undefined" && obj === window) {
        return "{ [object Window] }";
      }
      if (typeof globalThis !== "undefined" && obj === globalThis || typeof global !== "undefined" && obj === global) {
        return "{ [object globalThis] }";
      }
      if (!isDate(obj) && !isRegExp(obj)) {
        var ys = arrObjKeys(obj, inspect);
        var isPlainObject2 = gPO ? gPO(obj) === Object.prototype : obj instanceof Object || obj.constructor === Object;
        var protoTag = obj instanceof Object ? "" : "null prototype";
        var stringTag = !isPlainObject2 && toStringTag && Object(obj) === obj && toStringTag in obj ? $slice.call(toStr(obj), 8, -1) : protoTag ? "Object" : "";
        var constructorTag = isPlainObject2 || typeof obj.constructor !== "function" ? "" : obj.constructor.name ? obj.constructor.name + " " : "";
        var tag2 = constructorTag + (stringTag || protoTag ? "[" + $join.call($concat.call([], stringTag || [], protoTag || []), ": ") + "] " : "");
        if (ys.length === 0) {
          return tag2 + "{}";
        }
        if (indent) {
          return tag2 + "{" + indentedJoin(ys, indent) + "}";
        }
        return tag2 + "{ " + $join.call(ys, ", ") + " }";
      }
      return String(obj);
    };
    function wrapQuotes(s36, defaultStyle, opts) {
      var style = opts.quoteStyle || defaultStyle;
      var quoteChar = quotes[style];
      return quoteChar + s36 + quoteChar;
    }
    function quote(s36) {
      return $replace.call(String(s36), /"/g, "&quot;");
    }
    function isArray3(obj) {
      return toStr(obj) === "[object Array]" && (!toStringTag || !(typeof obj === "object" && toStringTag in obj));
    }
    function isDate(obj) {
      return toStr(obj) === "[object Date]" && (!toStringTag || !(typeof obj === "object" && toStringTag in obj));
    }
    function isRegExp(obj) {
      return toStr(obj) === "[object RegExp]" && (!toStringTag || !(typeof obj === "object" && toStringTag in obj));
    }
    function isError(obj) {
      return toStr(obj) === "[object Error]" && (!toStringTag || !(typeof obj === "object" && toStringTag in obj));
    }
    function isString(obj) {
      return toStr(obj) === "[object String]" && (!toStringTag || !(typeof obj === "object" && toStringTag in obj));
    }
    function isNumber2(obj) {
      return toStr(obj) === "[object Number]" && (!toStringTag || !(typeof obj === "object" && toStringTag in obj));
    }
    function isBoolean(obj) {
      return toStr(obj) === "[object Boolean]" && (!toStringTag || !(typeof obj === "object" && toStringTag in obj));
    }
    function isSymbol(obj) {
      if (hasShammedSymbols) {
        return obj && typeof obj === "object" && obj instanceof Symbol;
      }
      if (typeof obj === "symbol") {
        return true;
      }
      if (!obj || typeof obj !== "object" || !symToString) {
        return false;
      }
      try {
        symToString.call(obj);
        return true;
      } catch (e14) {
      }
      return false;
    }
    function isBigInt(obj) {
      if (!obj || typeof obj !== "object" || !bigIntValueOf) {
        return false;
      }
      try {
        bigIntValueOf.call(obj);
        return true;
      } catch (e14) {
      }
      return false;
    }
    var hasOwn = Object.prototype.hasOwnProperty || function(key) {
      return key in this;
    };
    function has(obj, key) {
      return hasOwn.call(obj, key);
    }
    function toStr(obj) {
      return objectToString2.call(obj);
    }
    function nameOf(f15) {
      if (f15.name) {
        return f15.name;
      }
      var m12 = $match.call(functionToString.call(f15), /^function\s*([\w$]+)/);
      if (m12) {
        return m12[1];
      }
      return null;
    }
    function indexOf(xs2, x3) {
      if (xs2.indexOf) {
        return xs2.indexOf(x3);
      }
      for (var i13 = 0, l12 = xs2.length; i13 < l12; i13++) {
        if (xs2[i13] === x3) {
          return i13;
        }
      }
      return -1;
    }
    function isMap(x3) {
      if (!mapSize || !x3 || typeof x3 !== "object") {
        return false;
      }
      try {
        mapSize.call(x3);
        try {
          setSize.call(x3);
        } catch (s36) {
          return true;
        }
        return x3 instanceof Map;
      } catch (e14) {
      }
      return false;
    }
    function isWeakMap(x3) {
      if (!weakMapHas || !x3 || typeof x3 !== "object") {
        return false;
      }
      try {
        weakMapHas.call(x3, weakMapHas);
        try {
          weakSetHas.call(x3, weakSetHas);
        } catch (s36) {
          return true;
        }
        return x3 instanceof WeakMap;
      } catch (e14) {
      }
      return false;
    }
    function isWeakRef(x3) {
      if (!weakRefDeref || !x3 || typeof x3 !== "object") {
        return false;
      }
      try {
        weakRefDeref.call(x3);
        return true;
      } catch (e14) {
      }
      return false;
    }
    function isSet(x3) {
      if (!setSize || !x3 || typeof x3 !== "object") {
        return false;
      }
      try {
        setSize.call(x3);
        try {
          mapSize.call(x3);
        } catch (m12) {
          return true;
        }
        return x3 instanceof Set;
      } catch (e14) {
      }
      return false;
    }
    function isWeakSet(x3) {
      if (!weakSetHas || !x3 || typeof x3 !== "object") {
        return false;
      }
      try {
        weakSetHas.call(x3, weakSetHas);
        try {
          weakMapHas.call(x3, weakMapHas);
        } catch (s36) {
          return true;
        }
        return x3 instanceof WeakSet;
      } catch (e14) {
      }
      return false;
    }
    function isElement3(x3) {
      if (!x3 || typeof x3 !== "object") {
        return false;
      }
      if (typeof HTMLElement !== "undefined" && x3 instanceof HTMLElement) {
        return true;
      }
      return typeof x3.nodeName === "string" && typeof x3.getAttribute === "function";
    }
    function inspectString(str, opts) {
      if (str.length > opts.maxStringLength) {
        var remaining = str.length - opts.maxStringLength;
        var trailer = "... " + remaining + " more character" + (remaining > 1 ? "s" : "");
        return inspectString($slice.call(str, 0, opts.maxStringLength), opts) + trailer;
      }
      var quoteRE = quoteREs[opts.quoteStyle || "single"];
      quoteRE.lastIndex = 0;
      var s36 = $replace.call($replace.call(str, quoteRE, "\\$1"), /[\x00-\x1f]/g, lowbyte);
      return wrapQuotes(s36, "single", opts);
    }
    function lowbyte(c15) {
      var n13 = c15.charCodeAt(0);
      var x3 = {
        8: "b",
        9: "t",
        10: "n",
        12: "f",
        13: "r"
      }[n13];
      if (x3) {
        return "\\" + x3;
      }
      return "\\x" + (n13 < 16 ? "0" : "") + $toUpperCase.call(n13.toString(16));
    }
    function markBoxed(str) {
      return "Object(" + str + ")";
    }
    function weakCollectionOf(type2) {
      return type2 + " { ? }";
    }
    function collectionOf(type2, size4, entries, indent) {
      var joinedEntries = indent ? indentedJoin(entries, indent) : $join.call(entries, ", ");
      return type2 + " (" + size4 + ") {" + joinedEntries + "}";
    }
    function singleLineValues(xs2) {
      for (var i13 = 0; i13 < xs2.length; i13++) {
        if (indexOf(xs2[i13], "\n") >= 0) {
          return false;
        }
      }
      return true;
    }
    function getIndent(opts, depth) {
      var baseIndent;
      if (opts.indent === "	") {
        baseIndent = "	";
      } else if (typeof opts.indent === "number" && opts.indent > 0) {
        baseIndent = $join.call(Array(opts.indent + 1), " ");
      } else {
        return null;
      }
      return {
        base: baseIndent,
        prev: $join.call(Array(depth + 1), baseIndent)
      };
    }
    function indentedJoin(xs2, indent) {
      if (xs2.length === 0) {
        return "";
      }
      var lineJoiner = "\n" + indent.prev + indent.base;
      return lineJoiner + $join.call(xs2, "," + lineJoiner) + "\n" + indent.prev;
    }
    function arrObjKeys(obj, inspect) {
      var isArr = isArray3(obj);
      var xs2 = [];
      if (isArr) {
        xs2.length = obj.length;
        for (var i13 = 0; i13 < obj.length; i13++) {
          xs2[i13] = has(obj, i13) ? inspect(obj[i13], obj) : "";
        }
      }
      var syms = typeof gOPS === "function" ? gOPS(obj) : [];
      var symMap;
      if (hasShammedSymbols) {
        symMap = {};
        for (var k5 = 0; k5 < syms.length; k5++) {
          symMap["$" + syms[k5]] = syms[k5];
        }
      }
      for (var key in obj) {
        if (!has(obj, key)) {
          continue;
        }
        if (isArr && String(Number(key)) === key && key < obj.length) {
          continue;
        }
        if (hasShammedSymbols && symMap["$" + key] instanceof Symbol) {
          continue;
        } else if ($test.call(/[^\w$]/, key)) {
          xs2.push(inspect(key, obj) + ": " + inspect(obj[key], obj));
        } else {
          xs2.push(key + ": " + inspect(obj[key], obj));
        }
      }
      if (typeof gOPS === "function") {
        for (var j3 = 0; j3 < syms.length; j3++) {
          if (isEnumerable.call(obj, syms[j3])) {
            xs2.push("[" + inspect(syms[j3]) + "]: " + inspect(obj[syms[j3]], obj));
          }
        }
      }
      return xs2;
    }
  }
});

// node_modules/side-channel/index.js
var require_side_channel = __commonJS({
  "node_modules/side-channel/index.js"(exports, module) {
    "use strict";
    var GetIntrinsic = require_get_intrinsic();
    var callBound = require_callBound();
    var inspect = require_object_inspect();
    var $TypeError = require_type();
    var $WeakMap = GetIntrinsic("%WeakMap%", true);
    var $Map = GetIntrinsic("%Map%", true);
    var $weakMapGet = callBound("WeakMap.prototype.get", true);
    var $weakMapSet = callBound("WeakMap.prototype.set", true);
    var $weakMapHas = callBound("WeakMap.prototype.has", true);
    var $mapGet = callBound("Map.prototype.get", true);
    var $mapSet = callBound("Map.prototype.set", true);
    var $mapHas = callBound("Map.prototype.has", true);
    var listGetNode = function(list, key) {
      var prev2 = list;
      var curr;
      for (; (curr = prev2.next) !== null; prev2 = curr) {
        if (curr.key === key) {
          prev2.next = curr.next;
          curr.next = /** @type {NonNullable<typeof list.next>} */
          list.next;
          list.next = curr;
          return curr;
        }
      }
    };
    var listGet = function(objects, key) {
      var node = listGetNode(objects, key);
      return node && node.value;
    };
    var listSet = function(objects, key, value) {
      var node = listGetNode(objects, key);
      if (node) {
        node.value = value;
      } else {
        objects.next = /** @type {import('.').ListNode<typeof value>} */
        {
          // eslint-disable-line no-param-reassign, no-extra-parens
          key,
          next: objects.next,
          value
        };
      }
    };
    var listHas = function(objects, key) {
      return !!listGetNode(objects, key);
    };
    module.exports = function getSideChannel() {
      var $wm;
      var $m;
      var $o;
      var channel = {
        assert: function(key) {
          if (!channel.has(key)) {
            throw new $TypeError("Side channel does not contain " + inspect(key));
          }
        },
        get: function(key) {
          if ($WeakMap && key && (typeof key === "object" || typeof key === "function")) {
            if ($wm) {
              return $weakMapGet($wm, key);
            }
          } else if ($Map) {
            if ($m) {
              return $mapGet($m, key);
            }
          } else {
            if ($o) {
              return listGet($o, key);
            }
          }
        },
        has: function(key) {
          if ($WeakMap && key && (typeof key === "object" || typeof key === "function")) {
            if ($wm) {
              return $weakMapHas($wm, key);
            }
          } else if ($Map) {
            if ($m) {
              return $mapHas($m, key);
            }
          } else {
            if ($o) {
              return listHas($o, key);
            }
          }
          return false;
        },
        set: function(key, value) {
          if ($WeakMap && key && (typeof key === "object" || typeof key === "function")) {
            if (!$wm) {
              $wm = new $WeakMap();
            }
            $weakMapSet($wm, key, value);
          } else if ($Map) {
            if (!$m) {
              $m = new $Map();
            }
            $mapSet($m, key, value);
          } else {
            if (!$o) {
              $o = { key: {}, next: null };
            }
            listSet($o, key, value);
          }
        }
      };
      return channel;
    };
  }
});

// node_modules/@vite-plugin-vercel/vike/node_modules/qs/lib/formats.js
var require_formats = __commonJS({
  "node_modules/@vite-plugin-vercel/vike/node_modules/qs/lib/formats.js"(exports, module) {
    "use strict";
    var replace = String.prototype.replace;
    var percentTwenties = /%20/g;
    var Format = {
      RFC1738: "RFC1738",
      RFC3986: "RFC3986"
    };
    module.exports = {
      "default": Format.RFC3986,
      formatters: {
        RFC1738: function(value) {
          return replace.call(value, percentTwenties, "+");
        },
        RFC3986: function(value) {
          return String(value);
        }
      },
      RFC1738: Format.RFC1738,
      RFC3986: Format.RFC3986
    };
  }
});

// node_modules/@vite-plugin-vercel/vike/node_modules/qs/lib/utils.js
var require_utils7 = __commonJS({
  "node_modules/@vite-plugin-vercel/vike/node_modules/qs/lib/utils.js"(exports, module) {
    "use strict";
    var formats = require_formats();
    var has = Object.prototype.hasOwnProperty;
    var isArray3 = Array.isArray;
    var hexTable = function() {
      var array = [];
      for (var i13 = 0; i13 < 256; ++i13) {
        array.push("%" + ((i13 < 16 ? "0" : "") + i13.toString(16)).toUpperCase());
      }
      return array;
    }();
    var compactQueue = function compactQueue2(queue) {
      while (queue.length > 1) {
        var item2 = queue.pop();
        var obj = item2.obj[item2.prop];
        if (isArray3(obj)) {
          var compacted = [];
          for (var j3 = 0; j3 < obj.length; ++j3) {
            if (typeof obj[j3] !== "undefined") {
              compacted.push(obj[j3]);
            }
          }
          item2.obj[item2.prop] = compacted;
        }
      }
    };
    var arrayToObject = function arrayToObject2(source, options) {
      var obj = options && options.plainObjects ? { __proto__: null } : {};
      for (var i13 = 0; i13 < source.length; ++i13) {
        if (typeof source[i13] !== "undefined") {
          obj[i13] = source[i13];
        }
      }
      return obj;
    };
    var merge = function merge2(target, source, options) {
      if (!source) {
        return target;
      }
      if (typeof source !== "object" && typeof source !== "function") {
        if (isArray3(target)) {
          target.push(source);
        } else if (target && typeof target === "object") {
          if (options && (options.plainObjects || options.allowPrototypes) || !has.call(Object.prototype, source)) {
            target[source] = true;
          }
        } else {
          return [target, source];
        }
        return target;
      }
      if (!target || typeof target !== "object") {
        return [target].concat(source);
      }
      var mergeTarget = target;
      if (isArray3(target) && !isArray3(source)) {
        mergeTarget = arrayToObject(target, options);
      }
      if (isArray3(target) && isArray3(source)) {
        source.forEach(function(item2, i13) {
          if (has.call(target, i13)) {
            var targetItem = target[i13];
            if (targetItem && typeof targetItem === "object" && item2 && typeof item2 === "object") {
              target[i13] = merge2(targetItem, item2, options);
            } else {
              target.push(item2);
            }
          } else {
            target[i13] = item2;
          }
        });
        return target;
      }
      return Object.keys(source).reduce(function(acc, key) {
        var value = source[key];
        if (has.call(acc, key)) {
          acc[key] = merge2(acc[key], value, options);
        } else {
          acc[key] = value;
        }
        return acc;
      }, mergeTarget);
    };
    var assign = function assignSingleSource(target, source) {
      return Object.keys(source).reduce(function(acc, key) {
        acc[key] = source[key];
        return acc;
      }, target);
    };
    var decode2 = function(str, defaultDecoder, charset) {
      var strWithoutPlus = str.replace(/\+/g, " ");
      if (charset === "iso-8859-1") {
        return strWithoutPlus.replace(/%[0-9a-f]{2}/gi, unescape);
      }
      try {
        return decodeURIComponent(strWithoutPlus);
      } catch (e14) {
        return strWithoutPlus;
      }
    };
    var limit = 1024;
    var encode2 = function encode3(str, defaultEncoder, charset, kind, format2) {
      if (str.length === 0) {
        return str;
      }
      var string = str;
      if (typeof str === "symbol") {
        string = Symbol.prototype.toString.call(str);
      } else if (typeof str !== "string") {
        string = String(str);
      }
      if (charset === "iso-8859-1") {
        return escape(string).replace(/%u[0-9a-f]{4}/gi, function($0) {
          return "%26%23" + parseInt($0.slice(2), 16) + "%3B";
        });
      }
      var out = "";
      for (var j3 = 0; j3 < string.length; j3 += limit) {
        var segment = string.length >= limit ? string.slice(j3, j3 + limit) : string;
        var arr = [];
        for (var i13 = 0; i13 < segment.length; ++i13) {
          var c15 = segment.charCodeAt(i13);
          if (c15 === 45 || c15 === 46 || c15 === 95 || c15 === 126 || c15 >= 48 && c15 <= 57 || c15 >= 65 && c15 <= 90 || c15 >= 97 && c15 <= 122 || format2 === formats.RFC1738 && (c15 === 40 || c15 === 41)) {
            arr[arr.length] = segment.charAt(i13);
            continue;
          }
          if (c15 < 128) {
            arr[arr.length] = hexTable[c15];
            continue;
          }
          if (c15 < 2048) {
            arr[arr.length] = hexTable[192 | c15 >> 6] + hexTable[128 | c15 & 63];
            continue;
          }
          if (c15 < 55296 || c15 >= 57344) {
            arr[arr.length] = hexTable[224 | c15 >> 12] + hexTable[128 | c15 >> 6 & 63] + hexTable[128 | c15 & 63];
            continue;
          }
          i13 += 1;
          c15 = 65536 + ((c15 & 1023) << 10 | segment.charCodeAt(i13) & 1023);
          arr[arr.length] = hexTable[240 | c15 >> 18] + hexTable[128 | c15 >> 12 & 63] + hexTable[128 | c15 >> 6 & 63] + hexTable[128 | c15 & 63];
        }
        out += arr.join("");
      }
      return out;
    };
    var compact = function compact2(value) {
      var queue = [{ obj: { o: value }, prop: "o" }];
      var refs = [];
      for (var i13 = 0; i13 < queue.length; ++i13) {
        var item2 = queue[i13];
        var obj = item2.obj[item2.prop];
        var keys2 = Object.keys(obj);
        for (var j3 = 0; j3 < keys2.length; ++j3) {
          var key = keys2[j3];
          var val = obj[key];
          if (typeof val === "object" && val !== null && refs.indexOf(val) === -1) {
            queue.push({ obj, prop: key });
            refs.push(val);
          }
        }
      }
      compactQueue(queue);
      return value;
    };
    var isRegExp = function isRegExp2(obj) {
      return Object.prototype.toString.call(obj) === "[object RegExp]";
    };
    var isBuffer = function isBuffer2(obj) {
      if (!obj || typeof obj !== "object") {
        return false;
      }
      return !!(obj.constructor && obj.constructor.isBuffer && obj.constructor.isBuffer(obj));
    };
    var combine = function combine2(a18, b4) {
      return [].concat(a18, b4);
    };
    var maybeMap = function maybeMap2(val, fn) {
      if (isArray3(val)) {
        var mapped = [];
        for (var i13 = 0; i13 < val.length; i13 += 1) {
          mapped.push(fn(val[i13]));
        }
        return mapped;
      }
      return fn(val);
    };
    module.exports = {
      arrayToObject,
      assign,
      combine,
      compact,
      decode: decode2,
      encode: encode2,
      isBuffer,
      isRegExp,
      maybeMap,
      merge
    };
  }
});

// node_modules/@vite-plugin-vercel/vike/node_modules/qs/lib/stringify.js
var require_stringify2 = __commonJS({
  "node_modules/@vite-plugin-vercel/vike/node_modules/qs/lib/stringify.js"(exports, module) {
    "use strict";
    var getSideChannel = require_side_channel();
    var utils = require_utils7();
    var formats = require_formats();
    var has = Object.prototype.hasOwnProperty;
    var arrayPrefixGenerators = {
      brackets: function brackets(prefix) {
        return prefix + "[]";
      },
      comma: "comma",
      indices: function indices(prefix, key) {
        return prefix + "[" + key + "]";
      },
      repeat: function repeat(prefix) {
        return prefix;
      }
    };
    var isArray3 = Array.isArray;
    var push = Array.prototype.push;
    var pushToArray = function(arr, valueOrArray) {
      push.apply(arr, isArray3(valueOrArray) ? valueOrArray : [valueOrArray]);
    };
    var toISO = Date.prototype.toISOString;
    var defaultFormat = formats["default"];
    var defaults = {
      addQueryPrefix: false,
      allowDots: false,
      allowEmptyArrays: false,
      arrayFormat: "indices",
      charset: "utf-8",
      charsetSentinel: false,
      commaRoundTrip: false,
      delimiter: "&",
      encode: true,
      encodeDotInKeys: false,
      encoder: utils.encode,
      encodeValuesOnly: false,
      filter: void 0,
      format: defaultFormat,
      formatter: formats.formatters[defaultFormat],
      // deprecated
      indices: false,
      serializeDate: function serializeDate(date) {
        return toISO.call(date);
      },
      skipNulls: false,
      strictNullHandling: false
    };
    var isNonNullishPrimitive = function isNonNullishPrimitive2(v5) {
      return typeof v5 === "string" || typeof v5 === "number" || typeof v5 === "boolean" || typeof v5 === "symbol" || typeof v5 === "bigint";
    };
    var sentinel = {};
    var stringify3 = function stringify4(object, prefix, generateArrayPrefix, commaRoundTrip, allowEmptyArrays, strictNullHandling, skipNulls, encodeDotInKeys, encoder2, filter2, sort, allowDots, serializeDate, format2, formatter, encodeValuesOnly, charset, sideChannel) {
      var obj = object;
      var tmpSc = sideChannel;
      var step = 0;
      var findFlag = false;
      while ((tmpSc = tmpSc.get(sentinel)) !== void 0 && !findFlag) {
        var pos = tmpSc.get(object);
        step += 1;
        if (typeof pos !== "undefined") {
          if (pos === step) {
            throw new RangeError("Cyclic object value");
          } else {
            findFlag = true;
          }
        }
        if (typeof tmpSc.get(sentinel) === "undefined") {
          step = 0;
        }
      }
      if (typeof filter2 === "function") {
        obj = filter2(prefix, obj);
      } else if (obj instanceof Date) {
        obj = serializeDate(obj);
      } else if (generateArrayPrefix === "comma" && isArray3(obj)) {
        obj = utils.maybeMap(obj, function(value2) {
          if (value2 instanceof Date) {
            return serializeDate(value2);
          }
          return value2;
        });
      }
      if (obj === null) {
        if (strictNullHandling) {
          return encoder2 && !encodeValuesOnly ? encoder2(prefix, defaults.encoder, charset, "key", format2) : prefix;
        }
        obj = "";
      }
      if (isNonNullishPrimitive(obj) || utils.isBuffer(obj)) {
        if (encoder2) {
          var keyValue = encodeValuesOnly ? prefix : encoder2(prefix, defaults.encoder, charset, "key", format2);
          return [formatter(keyValue) + "=" + formatter(encoder2(obj, defaults.encoder, charset, "value", format2))];
        }
        return [formatter(prefix) + "=" + formatter(String(obj))];
      }
      var values2 = [];
      if (typeof obj === "undefined") {
        return values2;
      }
      var objKeys;
      if (generateArrayPrefix === "comma" && isArray3(obj)) {
        if (encodeValuesOnly && encoder2) {
          obj = utils.maybeMap(obj, encoder2);
        }
        objKeys = [{ value: obj.length > 0 ? obj.join(",") || null : void 0 }];
      } else if (isArray3(filter2)) {
        objKeys = filter2;
      } else {
        var keys2 = Object.keys(obj);
        objKeys = sort ? keys2.sort(sort) : keys2;
      }
      var encodedPrefix = encodeDotInKeys ? String(prefix).replace(/\./g, "%2E") : String(prefix);
      var adjustedPrefix = commaRoundTrip && isArray3(obj) && obj.length === 1 ? encodedPrefix + "[]" : encodedPrefix;
      if (allowEmptyArrays && isArray3(obj) && obj.length === 0) {
        return adjustedPrefix + "[]";
      }
      for (var j3 = 0; j3 < objKeys.length; ++j3) {
        var key = objKeys[j3];
        var value = typeof key === "object" && key && typeof key.value !== "undefined" ? key.value : obj[key];
        if (skipNulls && value === null) {
          continue;
        }
        var encodedKey = allowDots && encodeDotInKeys ? String(key).replace(/\./g, "%2E") : String(key);
        var keyPrefix = isArray3(obj) ? typeof generateArrayPrefix === "function" ? generateArrayPrefix(adjustedPrefix, encodedKey) : adjustedPrefix : adjustedPrefix + (allowDots ? "." + encodedKey : "[" + encodedKey + "]");
        sideChannel.set(object, step);
        var valueSideChannel = getSideChannel();
        valueSideChannel.set(sentinel, sideChannel);
        pushToArray(values2, stringify4(
          value,
          keyPrefix,
          generateArrayPrefix,
          commaRoundTrip,
          allowEmptyArrays,
          strictNullHandling,
          skipNulls,
          encodeDotInKeys,
          generateArrayPrefix === "comma" && encodeValuesOnly && isArray3(obj) ? null : encoder2,
          filter2,
          sort,
          allowDots,
          serializeDate,
          format2,
          formatter,
          encodeValuesOnly,
          charset,
          valueSideChannel
        ));
      }
      return values2;
    };
    var normalizeStringifyOptions = function normalizeStringifyOptions2(opts) {
      if (!opts) {
        return defaults;
      }
      if (typeof opts.allowEmptyArrays !== "undefined" && typeof opts.allowEmptyArrays !== "boolean") {
        throw new TypeError("`allowEmptyArrays` option can only be `true` or `false`, when provided");
      }
      if (typeof opts.encodeDotInKeys !== "undefined" && typeof opts.encodeDotInKeys !== "boolean") {
        throw new TypeError("`encodeDotInKeys` option can only be `true` or `false`, when provided");
      }
      if (opts.encoder !== null && typeof opts.encoder !== "undefined" && typeof opts.encoder !== "function") {
        throw new TypeError("Encoder has to be a function.");
      }
      var charset = opts.charset || defaults.charset;
      if (typeof opts.charset !== "undefined" && opts.charset !== "utf-8" && opts.charset !== "iso-8859-1") {
        throw new TypeError("The charset option must be either utf-8, iso-8859-1, or undefined");
      }
      var format2 = formats["default"];
      if (typeof opts.format !== "undefined") {
        if (!has.call(formats.formatters, opts.format)) {
          throw new TypeError("Unknown format option provided.");
        }
        format2 = opts.format;
      }
      var formatter = formats.formatters[format2];
      var filter2 = defaults.filter;
      if (typeof opts.filter === "function" || isArray3(opts.filter)) {
        filter2 = opts.filter;
      }
      var arrayFormat;
      if (opts.arrayFormat in arrayPrefixGenerators) {
        arrayFormat = opts.arrayFormat;
      } else if ("indices" in opts) {
        arrayFormat = opts.indices ? "indices" : "repeat";
      } else {
        arrayFormat = defaults.arrayFormat;
      }
      if ("commaRoundTrip" in opts && typeof opts.commaRoundTrip !== "boolean") {
        throw new TypeError("`commaRoundTrip` must be a boolean, or absent");
      }
      var allowDots = typeof opts.allowDots === "undefined" ? opts.encodeDotInKeys === true ? true : defaults.allowDots : !!opts.allowDots;
      return {
        addQueryPrefix: typeof opts.addQueryPrefix === "boolean" ? opts.addQueryPrefix : defaults.addQueryPrefix,
        allowDots,
        allowEmptyArrays: typeof opts.allowEmptyArrays === "boolean" ? !!opts.allowEmptyArrays : defaults.allowEmptyArrays,
        arrayFormat,
        charset,
        charsetSentinel: typeof opts.charsetSentinel === "boolean" ? opts.charsetSentinel : defaults.charsetSentinel,
        commaRoundTrip: !!opts.commaRoundTrip,
        delimiter: typeof opts.delimiter === "undefined" ? defaults.delimiter : opts.delimiter,
        encode: typeof opts.encode === "boolean" ? opts.encode : defaults.encode,
        encodeDotInKeys: typeof opts.encodeDotInKeys === "boolean" ? opts.encodeDotInKeys : defaults.encodeDotInKeys,
        encoder: typeof opts.encoder === "function" ? opts.encoder : defaults.encoder,
        encodeValuesOnly: typeof opts.encodeValuesOnly === "boolean" ? opts.encodeValuesOnly : defaults.encodeValuesOnly,
        filter: filter2,
        format: format2,
        formatter,
        serializeDate: typeof opts.serializeDate === "function" ? opts.serializeDate : defaults.serializeDate,
        skipNulls: typeof opts.skipNulls === "boolean" ? opts.skipNulls : defaults.skipNulls,
        sort: typeof opts.sort === "function" ? opts.sort : null,
        strictNullHandling: typeof opts.strictNullHandling === "boolean" ? opts.strictNullHandling : defaults.strictNullHandling
      };
    };
    module.exports = function(object, opts) {
      var obj = object;
      var options = normalizeStringifyOptions(opts);
      var objKeys;
      var filter2;
      if (typeof options.filter === "function") {
        filter2 = options.filter;
        obj = filter2("", obj);
      } else if (isArray3(options.filter)) {
        filter2 = options.filter;
        objKeys = filter2;
      }
      var keys2 = [];
      if (typeof obj !== "object" || obj === null) {
        return "";
      }
      var generateArrayPrefix = arrayPrefixGenerators[options.arrayFormat];
      var commaRoundTrip = generateArrayPrefix === "comma" && options.commaRoundTrip;
      if (!objKeys) {
        objKeys = Object.keys(obj);
      }
      if (options.sort) {
        objKeys.sort(options.sort);
      }
      var sideChannel = getSideChannel();
      for (var i13 = 0; i13 < objKeys.length; ++i13) {
        var key = objKeys[i13];
        var value = obj[key];
        if (options.skipNulls && value === null) {
          continue;
        }
        pushToArray(keys2, stringify3(
          value,
          key,
          generateArrayPrefix,
          commaRoundTrip,
          options.allowEmptyArrays,
          options.strictNullHandling,
          options.skipNulls,
          options.encodeDotInKeys,
          options.encode ? options.encoder : null,
          options.filter,
          options.sort,
          options.allowDots,
          options.serializeDate,
          options.format,
          options.formatter,
          options.encodeValuesOnly,
          options.charset,
          sideChannel
        ));
      }
      var joined = keys2.join(options.delimiter);
      var prefix = options.addQueryPrefix === true ? "?" : "";
      if (options.charsetSentinel) {
        if (options.charset === "iso-8859-1") {
          prefix += "utf8=%26%2310003%3B&";
        } else {
          prefix += "utf8=%E2%9C%93&";
        }
      }
      return joined.length > 0 ? prefix + joined : "";
    };
  }
});

// node_modules/@vite-plugin-vercel/vike/node_modules/qs/lib/parse.js
var require_parse2 = __commonJS({
  "node_modules/@vite-plugin-vercel/vike/node_modules/qs/lib/parse.js"(exports, module) {
    "use strict";
    var utils = require_utils7();
    var has = Object.prototype.hasOwnProperty;
    var isArray3 = Array.isArray;
    var defaults = {
      allowDots: false,
      allowEmptyArrays: false,
      allowPrototypes: false,
      allowSparse: false,
      arrayLimit: 20,
      charset: "utf-8",
      charsetSentinel: false,
      comma: false,
      decodeDotInKeys: false,
      decoder: utils.decode,
      delimiter: "&",
      depth: 5,
      duplicates: "combine",
      ignoreQueryPrefix: false,
      interpretNumericEntities: false,
      parameterLimit: 1e3,
      parseArrays: true,
      plainObjects: false,
      strictDepth: false,
      strictNullHandling: false
    };
    var interpretNumericEntities = function(str) {
      return str.replace(/&#(\d+);/g, function($0, numberStr) {
        return String.fromCharCode(parseInt(numberStr, 10));
      });
    };
    var parseArrayValue = function(val, options) {
      if (val && typeof val === "string" && options.comma && val.indexOf(",") > -1) {
        return val.split(",");
      }
      return val;
    };
    var isoSentinel = "utf8=%26%2310003%3B";
    var charsetSentinel = "utf8=%E2%9C%93";
    var parseValues = function parseQueryStringValues(str, options) {
      var obj = { __proto__: null };
      var cleanStr = options.ignoreQueryPrefix ? str.replace(/^\?/, "") : str;
      cleanStr = cleanStr.replace(/%5B/gi, "[").replace(/%5D/gi, "]");
      var limit = options.parameterLimit === Infinity ? void 0 : options.parameterLimit;
      var parts = cleanStr.split(options.delimiter, limit);
      var skipIndex = -1;
      var i13;
      var charset = options.charset;
      if (options.charsetSentinel) {
        for (i13 = 0; i13 < parts.length; ++i13) {
          if (parts[i13].indexOf("utf8=") === 0) {
            if (parts[i13] === charsetSentinel) {
              charset = "utf-8";
            } else if (parts[i13] === isoSentinel) {
              charset = "iso-8859-1";
            }
            skipIndex = i13;
            i13 = parts.length;
          }
        }
      }
      for (i13 = 0; i13 < parts.length; ++i13) {
        if (i13 === skipIndex) {
          continue;
        }
        var part = parts[i13];
        var bracketEqualsPos = part.indexOf("]=");
        var pos = bracketEqualsPos === -1 ? part.indexOf("=") : bracketEqualsPos + 1;
        var key;
        var val;
        if (pos === -1) {
          key = options.decoder(part, defaults.decoder, charset, "key");
          val = options.strictNullHandling ? null : "";
        } else {
          key = options.decoder(part.slice(0, pos), defaults.decoder, charset, "key");
          val = utils.maybeMap(
            parseArrayValue(part.slice(pos + 1), options),
            function(encodedVal) {
              return options.decoder(encodedVal, defaults.decoder, charset, "value");
            }
          );
        }
        if (val && options.interpretNumericEntities && charset === "iso-8859-1") {
          val = interpretNumericEntities(String(val));
        }
        if (part.indexOf("[]=") > -1) {
          val = isArray3(val) ? [val] : val;
        }
        var existing = has.call(obj, key);
        if (existing && options.duplicates === "combine") {
          obj[key] = utils.combine(obj[key], val);
        } else if (!existing || options.duplicates === "last") {
          obj[key] = val;
        }
      }
      return obj;
    };
    var parseObject = function(chain, val, options, valuesParsed) {
      var leaf = valuesParsed ? val : parseArrayValue(val, options);
      for (var i13 = chain.length - 1; i13 >= 0; --i13) {
        var obj;
        var root5 = chain[i13];
        if (root5 === "[]" && options.parseArrays) {
          obj = options.allowEmptyArrays && (leaf === "" || options.strictNullHandling && leaf === null) ? [] : [].concat(leaf);
        } else {
          obj = options.plainObjects ? { __proto__: null } : {};
          var cleanRoot = root5.charAt(0) === "[" && root5.charAt(root5.length - 1) === "]" ? root5.slice(1, -1) : root5;
          var decodedRoot = options.decodeDotInKeys ? cleanRoot.replace(/%2E/g, ".") : cleanRoot;
          var index3 = parseInt(decodedRoot, 10);
          if (!options.parseArrays && decodedRoot === "") {
            obj = { 0: leaf };
          } else if (!isNaN(index3) && root5 !== decodedRoot && String(index3) === decodedRoot && index3 >= 0 && (options.parseArrays && index3 <= options.arrayLimit)) {
            obj = [];
            obj[index3] = leaf;
          } else if (decodedRoot !== "__proto__") {
            obj[decodedRoot] = leaf;
          }
        }
        leaf = obj;
      }
      return leaf;
    };
    var parseKeys = function parseQueryStringKeys(givenKey, val, options, valuesParsed) {
      if (!givenKey) {
        return;
      }
      var key = options.allowDots ? givenKey.replace(/\.([^.[]+)/g, "[$1]") : givenKey;
      var brackets = /(\[[^[\]]*])/;
      var child = /(\[[^[\]]*])/g;
      var segment = options.depth > 0 && brackets.exec(key);
      var parent = segment ? key.slice(0, segment.index) : key;
      var keys2 = [];
      if (parent) {
        if (!options.plainObjects && has.call(Object.prototype, parent)) {
          if (!options.allowPrototypes) {
            return;
          }
        }
        keys2.push(parent);
      }
      var i13 = 0;
      while (options.depth > 0 && (segment = child.exec(key)) !== null && i13 < options.depth) {
        i13 += 1;
        if (!options.plainObjects && has.call(Object.prototype, segment[1].slice(1, -1))) {
          if (!options.allowPrototypes) {
            return;
          }
        }
        keys2.push(segment[1]);
      }
      if (segment) {
        if (options.strictDepth === true) {
          throw new RangeError("Input depth exceeded depth option of " + options.depth + " and strictDepth is true");
        }
        keys2.push("[" + key.slice(segment.index) + "]");
      }
      return parseObject(keys2, val, options, valuesParsed);
    };
    var normalizeParseOptions = function normalizeParseOptions2(opts) {
      if (!opts) {
        return defaults;
      }
      if (typeof opts.allowEmptyArrays !== "undefined" && typeof opts.allowEmptyArrays !== "boolean") {
        throw new TypeError("`allowEmptyArrays` option can only be `true` or `false`, when provided");
      }
      if (typeof opts.decodeDotInKeys !== "undefined" && typeof opts.decodeDotInKeys !== "boolean") {
        throw new TypeError("`decodeDotInKeys` option can only be `true` or `false`, when provided");
      }
      if (opts.decoder !== null && typeof opts.decoder !== "undefined" && typeof opts.decoder !== "function") {
        throw new TypeError("Decoder has to be a function.");
      }
      if (typeof opts.charset !== "undefined" && opts.charset !== "utf-8" && opts.charset !== "iso-8859-1") {
        throw new TypeError("The charset option must be either utf-8, iso-8859-1, or undefined");
      }
      var charset = typeof opts.charset === "undefined" ? defaults.charset : opts.charset;
      var duplicates = typeof opts.duplicates === "undefined" ? defaults.duplicates : opts.duplicates;
      if (duplicates !== "combine" && duplicates !== "first" && duplicates !== "last") {
        throw new TypeError("The duplicates option must be either combine, first, or last");
      }
      var allowDots = typeof opts.allowDots === "undefined" ? opts.decodeDotInKeys === true ? true : defaults.allowDots : !!opts.allowDots;
      return {
        allowDots,
        allowEmptyArrays: typeof opts.allowEmptyArrays === "boolean" ? !!opts.allowEmptyArrays : defaults.allowEmptyArrays,
        allowPrototypes: typeof opts.allowPrototypes === "boolean" ? opts.allowPrototypes : defaults.allowPrototypes,
        allowSparse: typeof opts.allowSparse === "boolean" ? opts.allowSparse : defaults.allowSparse,
        arrayLimit: typeof opts.arrayLimit === "number" ? opts.arrayLimit : defaults.arrayLimit,
        charset,
        charsetSentinel: typeof opts.charsetSentinel === "boolean" ? opts.charsetSentinel : defaults.charsetSentinel,
        comma: typeof opts.comma === "boolean" ? opts.comma : defaults.comma,
        decodeDotInKeys: typeof opts.decodeDotInKeys === "boolean" ? opts.decodeDotInKeys : defaults.decodeDotInKeys,
        decoder: typeof opts.decoder === "function" ? opts.decoder : defaults.decoder,
        delimiter: typeof opts.delimiter === "string" || utils.isRegExp(opts.delimiter) ? opts.delimiter : defaults.delimiter,
        // eslint-disable-next-line no-implicit-coercion, no-extra-parens
        depth: typeof opts.depth === "number" || opts.depth === false ? +opts.depth : defaults.depth,
        duplicates,
        ignoreQueryPrefix: opts.ignoreQueryPrefix === true,
        interpretNumericEntities: typeof opts.interpretNumericEntities === "boolean" ? opts.interpretNumericEntities : defaults.interpretNumericEntities,
        parameterLimit: typeof opts.parameterLimit === "number" ? opts.parameterLimit : defaults.parameterLimit,
        parseArrays: opts.parseArrays !== false,
        plainObjects: typeof opts.plainObjects === "boolean" ? opts.plainObjects : defaults.plainObjects,
        strictDepth: typeof opts.strictDepth === "boolean" ? !!opts.strictDepth : defaults.strictDepth,
        strictNullHandling: typeof opts.strictNullHandling === "boolean" ? opts.strictNullHandling : defaults.strictNullHandling
      };
    };
    module.exports = function(str, opts) {
      var options = normalizeParseOptions(opts);
      if (str === "" || str === null || typeof str === "undefined") {
        return options.plainObjects ? { __proto__: null } : {};
      }
      var tempObj = typeof str === "string" ? parseValues(str, options) : str;
      var obj = options.plainObjects ? { __proto__: null } : {};
      var keys2 = Object.keys(tempObj);
      for (var i13 = 0; i13 < keys2.length; ++i13) {
        var key = keys2[i13];
        var newObj = parseKeys(key, tempObj[key], options, typeof str === "string");
        obj = utils.merge(obj, newObj, options);
      }
      if (options.allowSparse === true) {
        return obj;
      }
      return utils.compact(obj);
    };
  }
});

// node_modules/@vite-plugin-vercel/vike/node_modules/qs/lib/index.js
var require_lib = __commonJS({
  "node_modules/@vite-plugin-vercel/vike/node_modules/qs/lib/index.js"(exports, module) {
    "use strict";
    var stringify3 = require_stringify2();
    var parse3 = require_parse2();
    var formats = require_formats();
    module.exports = {
      formats,
      parse: parse3,
      stringify: stringify3
    };
  }
});

// node_modules/@vite-plugin-vercel/vike/templates/ssr_.template.ts
init_runtime();

// node_modules/@vite-plugin-vercel/vike/templates/utils.ts
var import_qs = __toESM(require_lib(), 1);
function getOriginalUrl(xNowRouteMatchesHeader, originalPath, url) {
  const matches = (
    // FIXME x-now-route-matches is not definitive https://github.com/orgs/vercel/discussions/577#discussioncomment-2769478
    typeof xNowRouteMatchesHeader === "string" ? (0, import_qs.parse)(xNowRouteMatchesHeader) : null
  );
  return typeof originalPath === "string" ? originalPath : matches && typeof matches?.["1"] === "string" ? matches["1"] : url ?? "";
}

// node_modules/@vite-plugin-vercel/vike/templates/node-helpers.ts
function getDefaultPageContextInit(request) {
  const query2 = request.query ?? {};
  const url = getOriginalUrl(request.headers["x-now-route-matches"], query2.__original_path, request.url);
  return {
    url,
    urlOriginal: url,
    body: request.body,
    cookies: request.cookies
  };
}
function getDefaultEmptyResponseHandler(response) {
  response.statusCode = 200;
  response.setHeader("content-type", "text/html; charset=UTF-8");
  return response.end("");
}
function getDefaultResponseHandler(response, httpResponse) {
  const { statusCode, body, headers } = httpResponse;
  response.statusCode = statusCode;
  for (const [name2, value] of headers) {
    response.setHeader(name2, value);
  }
  return response.end(body);
}

// node_modules/@vite-plugin-vercel/vike/templates/ssr_.template.ts
async function handler(request, response) {
  console.debug("query", request.query);
  console.debug("url", request.url);
  console.debug("headers", request.headers);
  const pageContextInit = getDefaultPageContextInit(request);
  const pageContext = await renderPage(pageContextInit);
  const { httpResponse } = pageContext;
  if (!httpResponse) {
    return getDefaultEmptyResponseHandler(response);
  }
  return getDefaultResponseHandler(response, httpResponse);
}
export {
  handler as default
};
/*! Bundled license information:

react/cjs/react.production.min.js:
  (**
   * @license React
   * react.production.min.js
   *
   * Copyright (c) Facebook, Inc. and its affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)

react-dom/cjs/react-dom-server-legacy.node.production.min.js:
  (**
   * @license React
   * react-dom-server-legacy.node.production.min.js
   *
   * Copyright (c) Facebook, Inc. and its affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)

react-dom/cjs/react-dom-server.node.production.min.js:
  (**
   * @license React
   * react-dom-server.node.production.min.js
   *
   * Copyright (c) Facebook, Inc. and its affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)

react-dom/cjs/react-dom-server-legacy.browser.production.min.js:
  (**
   * @license React
   * react-dom-server-legacy.browser.production.min.js
   *
   * Copyright (c) Facebook, Inc. and its affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)

react-dom/cjs/react-dom-server.browser.production.min.js:
  (**
   * @license React
   * react-dom-server.browser.production.min.js
   *
   * Copyright (c) Facebook, Inc. and its affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)

react/cjs/react-jsx-runtime.production.min.js:
  (**
   * @license React
   * react-jsx-runtime.production.min.js
   *
   * Copyright (c) Facebook, Inc. and its affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)

scheduler/cjs/scheduler.production.min.js:
  (**
   * @license React
   * scheduler.production.min.js
   *
   * Copyright (c) Facebook, Inc. and its affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)

react-dom/cjs/react-dom.production.min.js:
  (**
   * @license React
   * react-dom.production.min.js
   *
   * Copyright (c) Facebook, Inc. and its affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)

use-sync-external-store/cjs/use-sync-external-store-shim.production.js:
  (**
   * @license React
   * use-sync-external-store-shim.production.js
   *
   * Copyright (c) Meta Platforms, Inc. and affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)

use-sync-external-store/cjs/use-sync-external-store-shim/with-selector.production.js:
  (**
   * @license React
   * use-sync-external-store-shim/with-selector.production.js
   *
   * Copyright (c) Meta Platforms, Inc. and affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)

node-fetch-native/dist/node.mjs:
  (**
  * @license
  * web-streams-polyfill v3.3.3
  * Copyright 2024 Mattias Buelens, Diwank Singh Tomer and other contributors.
  * This code is released under the MIT license.
  * SPDX-License-Identifier: MIT
  *)
  (*! fetch-blob. MIT License. Jimmy Wärting <https://jimmy.warting.se/opensource> *)
  (*! formdata-polyfill. MIT License. Jimmy Wärting <https://jimmy.warting.se/opensource> *)
  (*! node-domexception. MIT License. Jimmy Wärting <https://jimmy.warting.se/opensource> *)

lodash-es/lodash.js:
  (**
   * @license
   * Lodash (Custom Build) <https://lodash.com/>
   * Build: `lodash modularize exports="es" -o ./`
   * Copyright OpenJS Foundation and other contributors <https://openjsf.org/>
   * Released under MIT license <https://lodash.com/license>
   * Based on Underscore.js 1.8.3 <http://underscorejs.org/LICENSE>
   * Copyright Jeremy Ashkenas, DocumentCloud and Investigative Reporters & Editors
   *)
*/
